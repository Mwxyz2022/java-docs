---
layout: default
title: "Способи створення потоків"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 4
---

# Способи створення потоків

* короткий опис: Stream API надає різноманітні способи створення стрімів з різних джерел: колекцій, масивів, окремих значень, генераторів нескінченних послідовностей, файлів.

* поняття: `collection.stream()`, `Stream.of()`, `Arrays.stream()`, `Stream.empty()`, `Stream.generate(Supplier)`, `Stream.iterate(seed, f)`, `Files.lines(Path)`.

* приклади: `List.of("A", "B").stream()`, `Stream.of(1, 2, 3)`, `Arrays.stream(new int[]{1, 2})`, `Stream.generate(Math::random).limit(3)`, `Files.lines(path)`.

* ключові моменти: Кожен спосіб підходить для конкретного сценарію. Нескінченні стріми потребують `limit()`.

* що важливо знати на співбесіді: Коли використовувати кожен спосіб.

### **✅ Офіційне визначення:**

У Java 8+ Stream API дозволяє працювати з послідовностями елементів функціонально. Потік (stream) — це **послідовність даних, яку можна обробляти різними операціями без зміни джерела даних**.

### **🧠 Простими словами:**

**Stream** — це як "труба", по якій "течуть" елементи з колекції, масиву або навіть нескінченного генератора. Ти просто кажеш: “Фільтруй, перетвори, збери”, а Java робить магію

---

## 📊 **Популярні способи створення стрімів:**

|              Метод              |                        Опис                         |               Приклад                |
|:-------------------------------:|:---------------------------------------------------:|:------------------------------------:|
|    **`collection.stream()`**    | Створення стріму з колекції (**`List`**, **`Set`**) |         **`list.stream()`**          |
|      **`Stream.of(...)`**       |      Створення стріму з вручну заданих значень      |       **`Stream.of(1, 2, 3)`**       |
|   **`Arrays.stream(array)`**    |                   Стрім із масиву                   | **`Arrays.stream(new int[]{1,2})`**  |
|      **`Stream.empty()`**       |                   Порожній стрім                    |         **`Stream.empty()`**         |
| **`Stream.generate(Supplier)`** |    Нескінченний стрім зі значенням з генератора     | **`Stream.generate(Math::random)`**  |
|  **`Stream.iterate(seed, f)`**  |      Нескінченний стрім з початковим значенням      | **`Stream.iterate(0, n -> n + 2)`**  |
|     **`Files.lines(Path)`**     |          Читання рядків з файлу як стріму           | **`Files.lines(Paths.get("file"))`** |

---

## **Пояснення на пальцях (💡)**

* Хочеш обробити **колекцію** --> **`collection.stream()`**
* Маєш масив --> **`Arrays.stream(...)`**
* Маєш список значень --> **`Stream.of(...)`**
* Потрібно **порожній** стрім --> **`Stream.empty()`**
* Хочеш створити **безкінечну послідовність** --> **`generate()`** або **`iterate()`**
* Потрібно читати **рядки з файлу** --> **`Files.lines(path)`**

---

## **Корисні приклади (🧪)**

```java
// З колекції
List<String> names = List.of("Alice", "Bob");
Stream<String> nameStream = names.stream();

// З масиву
int[] nums = {1, 2, 3};
IntStream intStream = Arrays.stream(nums);

// З окремих значень
Stream<String> stringStream = Stream.of("A", "B", "C");

// Порожній стрім
Stream<Object> empty = Stream.empty();

// Генерація випадкових чисел
Stream<Double> randoms = Stream.generate(Math::random).limit(3);

// Числа з приростом
Stream<Integer> evens = Stream.iterate(0, n -> n + 2).limit(5);

// Читання з файлу
try (Stream<String> lines = Files.lines(Paths.get("data.txt"))) {
    lines.forEach(System.out::println);
}
```

---

## **Порівняння (⚖️)**

|          Метод          |        Джерело        |          Обмеження/Переваги          |
|:-----------------------:|:---------------------:|:------------------------------------:|
|     **`.stream()`**     |       Колекції        |          Найбільш поширений          |
|    **`Stream.of()`**    |     Ручне задання     |     Швидко, якщо знаєш значення      |
|  **`Arrays.stream()`**  |        Масиви         | Для роботи з примітивами є спец. API |
|  **`Stream.empty()`**   |   Створення пустоти   | Добре для дефолтів або null-safe API |
| **`Stream.generate()`** | Нескінченна генерація | Обов’язково обмежувати (**`limit`**) |
| **`Stream.iterate()`**  |  Нескінченна логіка   |  Потужний, але обережно з ресурсами  |
|   **`Files.lines()`**   |     Файли (рядки)     |  Закривати стрім після використання  |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"CAGE-FI" — як клітка для стрімів:**

* **C**ollection --> **`stream()`**
* **A**rray --> **`Arrays.stream()`**
* **G**iven values --> **`Stream.of()`**
* **E**mpty --> **`Stream.empty()`**
* **F**iles --> **`Files.lines()`**
* **I**nfinite --> **`generate()`** / **`iterate()`**

---

## **🟩 Підсумок:**

* Java пропонує **багато способів створення стрімів** для різних потреб: від колекцій до файлів і генераторів.
* **Кожен спосіб має своє призначення** — правильний вибір допоможе зробити код чистим і ефективним.
* Обережно з **нескінченними стрімами** — завжди обмежуй **`limit()`**
* **`Files.lines()`** — чудовий інструмент для обробки файлів, але не забудь **закривати потік**.

---

### **🔗 Додаткові джерела:**

* [**Java Stream API — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/Stream.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Який є найпоширеніший спосіб створення потоку (`Stream`) в Java?

> #### **💬 Відповідь:**
>
> Найпоширеніший і найпростіший спосіб — це створення потоку **з існуючої колекції** за допомогою методу `.stream()`.
>
> Будь-яка колекція в Java, що реалізує інтерфейс `java.util.Collection` (наприклад, `List`, `Set`), має цей метод за замовчуванням (доданий у Java 8 як `default`-метод).
>
> Цей підхід є основним, оскільки в більшості випадків дані, які потрібно обробити, вже зберігаються в колекціях.

#### **📝 Приклад:**

```java
import java.util.List;
import java.util.stream.Stream;

public class StreamFromCollection {
    public static void main(String[] args) {
        List<String> names = List.of("Alice", "Bob", "Charlie");

        // Створюємо потік з колекції List
        Stream<String> nameStream = names.stream();
        
        nameStream.forEach(System.out::println);
    }
}
```

#### **📌 Що перевіряють:**

*Знання найбазовішого та найчастіше використовуваного способу роботи зі Stream API. Це перше, що очікують почути у відповідь.*

* * *

### **2 ❓ Питання:**

Як створити потік з масиву? Чи є різниця для примітивних та об'єктних масивів?

> #### **💬 Відповідь:**
>
> Для створення потоку з масиву використовується статичний метод **`Arrays.stream(array)`**.
>
> **Так, є різниця** для примітивних та об'єктних масивів:
>
> * **Для об'єктних масивів (`String[]`, `User[]` тощо):**
>     `Arrays.stream()` повертає `Stream<T>`.
>
>     ```java
>     String[] words = {"a", "b", "c"};
>     Stream<String> wordStream = Arrays.stream(words);
>     ```
>
> * **Для примітивних масивів (`int[]`, `double[]`, `long[]`):**
>     `Arrays.stream()` повертає відповідний **примітивний потік** (`IntStream`, `DoubleStream`, `LongStream`). Це зроблено для оптимізації, щоб уникнути непотрібного boxing/unboxing.
>
>     ```java
>     int[] numbers = {1, 2, 3};
>     IntStream numberStream = Arrays.stream(numbers);
>     ```

#### **📝 Альтернатива:**

Також можна використовувати `Stream.of(array)`, але для примітивних масивів він створить `Stream<int[]>`, тобто потік з одного елемента (самого масиву), що зазвичай не є бажаним результатом. Тому `Arrays.stream()` є кращим вибором.

#### **📌 Що перевіряють:**

*Знання API для роботи з масивами та розуміння різниці в обробці примітивних та об'єктних типів, що важливо для продуктивності.*

* * *

### **3 ❓ Питання:**

Для чого потрібен метод `Stream.of()`?

> #### **💬 Відповідь:**
>
> Статичний метод **`Stream.of(T... values)`** використовується для створення потоку з **перерахованих елементів**, без необхідності спочатку створювати для них колекцію чи масив.
>
> Він дуже зручний для:
>
> * **Тестування:** Коли потрібно швидко створити невеликий потік з тестовими даними.
> * **Невеликих, фіксованих наборів даних:** Коли дані відомі наперед і їх небагато.
>
> Метод може приймати будь-яку кількість аргументів, включаючи жодного (в цьому випадку він створить порожній потік).

#### **📝 Приклад:**

```java
// Створення потоку з кількох рядків
Stream<String> stream = Stream.of("Java", "Stream", "API");

stream.map(String::toUpperCase)
      .forEach(System.out::println); // JAVA, STREAM, API
```

#### **📌 Що перевіряють:**

*Знання зручних фабричних методів `Stream` для швидкого створення потоків з довільних даних.*

* * *

### **4 ❓ Питання:**

Що таке нескінченні потоки і як їх створювати за допомогою `Stream.iterate()` та `Stream.generate()`?

> #### **💬 Відповідь:**
>
> **Нескінченні потоки** — це потоки, які не мають фіксованого розміру і можуть генерувати елементи нескінченно. Такі потоки **обов'язково** повинні бути обмежені проміжною операцією `limit()`, щоб уникнути нескінченного циклу.
>
> Існує два основні способи їх створення:
>
> * **`Stream.iterate(T seed, UnaryOperator<T> f)`:**
>   * **Призначення:** Створює послідовність, де кожен наступний елемент обчислюється на основі попереднього.
>   * **Як працює:** Починається з `seed` (початкове значення). Наступний елемент — це `f(seed)`, потім `f(f(seed))` і так далі.
>   * **Приклад (потік парних чисел):** `Stream.iterate(0, n -> n + 2)` -> 0, 2, 4, 6, ...
>
> * **`Stream.generate(Supplier<T> s)`:**
>   * **Призначення:** Створює послідовність, де кожен елемент генерується незалежно від інших.
>   * **Як працює:** Для кожного елемента викликається метод `get()` у наданого `Supplier`.
>   * **Приклад (потік випадкових чисел):** `Stream.generate(Math::random)`

#### **📝 Коментар:**

`iterate` — для послідовностей, де є залежність від попереднього елемента. `generate` — для послідовностей, де елементи є незалежними.

#### **📌 Що перевіряють:**

*Розуміння концепції нескінченних потоків та обов'язкового використання `limit()` з ними. Це перевіряє уважність та знання просунутих можливостей API.*

* * *

### **5 ❓ Питання:**

Як створити порожній потік і коли це може бути корисним?

> #### **💬 Відповідь:**
>
> Для створення порожнього потоку використовується статичний метод **`Stream.empty()`**.
>
> **Коли це корисно:**
>
> Це може бути дуже корисним для уникнення `NullPointerException` та написання більш чистого коду, коли метод має повернути потік, але за певних умов даних для нього немає.
>
> Замість того, щоб повертати `null` і змушувати клієнтський код робити перевірку `if (stream != null)`, ви можете безпечно повернути порожній потік. Подальші операції (`filter`, `map`, `collect`) на порожньому потоці просто не виконаються і коректно повернуть порожній результат (напр., порожню колекцію).

#### **📝 Приклад:**

```java
public Stream<User> findActiveUsers(Department department) {
    if (department == null || department.getUsers().isEmpty()) {
        // Замість `return null;` повертаємо порожній потік
        return Stream.empty();
    }
    return department.getUsers().stream().filter(User::isActive);
}
```

#### **📌 Що перевіряють:**

*Розуміння хороших практик API дизайну та способів уникнення `null` у своєму коді.*

* * *

### **6 ❓ Питання:**

Як створити потік з рядків файлу? Яку особливість слід пам'ятати?

> #### **💬 Відповідь:**
>
> Для читання файлу по рядках у вигляді потоку використовується статичний метод **`Files.lines(Path path)`** з пакету `java.nio.file`.
>
> Цей метод є дуже зручним та ефективним для обробки великих файлів, оскільки він читає файл **ліниво**, тобто рядок за рядком, а не завантажує весь файл у пам'ять одразу.
>
> **Важлива особливість, яку слід пам'ятати:**
>
> Потік, створений `Files.lines()`, "тримає" відкритим файловий ресурс. Його **обов'язково потрібно закривати**, щоб уникнути витоку ресурсів. Найкращий спосіб зробити це — використовувати блок **`try-with-resources`**.

#### **📝 Приклад:**

```java
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.io.IOException;
import java.util.stream.Stream;

public class FileStreamExample {
    public static void main(String[] args) {
        Path path = Paths.get("my-file.txt");
        
        // Використовуємо try-with-resources для автоматичного закриття потоку
        try (Stream<String> lines = Files.lines(path)) {
            lines.filter(line -> line.contains("ERROR"))
                 .forEach(System.out::println);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
```

#### **📌 Що перевіряють:**

*Знання NIO API та правил роботи з ресурсами, які потрібно закривати. Це демонструє відповідальний підхід до написання коду.*

* * *

### **7 ❓ Питання:**

Як створити примітивний потік (наприклад, `IntStream`)? Які є переваги? ⭐️

> #### **💬 Відповідь:**
>
> Існують спеціалізовані методи для створення примітивних потоків:
>
> * **`IntStream.range(int startInclusive, int endExclusive)`:** Створює потік `int`'ів у заданому діапазоні.
> * **`IntStream.rangeClosed(int startInclusive, int endInclusive)`:** Те саме, але включаючи кінцеве значення.
> * **`Arrays.stream(int[] array)`:** Створює `IntStream` з масиву `int`.
> * `Random.ints()`: Створює нескінченний потік випадкових `int`.
>
> Аналогічні методи є і для `LongStream` та `DoubleStream`.
>
> **Переваги:**
>
> 1. **Продуктивність:** Вони працюють безпосередньо з примітивними типами, що **уникає boxing/unboxing**, і є значно ефективнішим при роботі з великою кількістю чисел.
> 2. **Спеціалізовані операції:** Надають додаткові корисні термінальні операції для чисел, як-от `sum()`, `average()`, `summaryStatistics()`.

#### **📝 Приклад:**

```java
// Створення потоку чисел від 1 до 9
IntStream rangeStream = IntStream.range(1, 10);

// Обчислення суми парних чисел
int sum = rangeStream.filter(n -> n % 2 == 0).sum(); 

System.out.println(sum); // 20 (2+4+6+8)
```

#### **📌 Що перевіряють:**

*Розуміння необхідності та переваг використання примітивних спеціалізацій стрімів для оптимізації продуктивності.*

* * *

### **8 ❓ Питання:**

Чим відрізняються `Stream.iterate()` та `Stream.generate()`? Коли який використовувати? ⭐️

> #### **💬 Відповідь:**
>
> Обидва методи створюють нескінченні потоки, але вони базуються на різних принципах генерації елементів.
>
> * **`Stream.iterate(T seed, UnaryOperator<T> f)`:**
>   * **Принцип:** **Послідовний.** Кожен наступний елемент генерується на основі **попереднього**.
>   * **Як працює:** Перший елемент — `seed`. Другий — `f(seed)`. Третій — `f(f(seed))`, і так далі.
>   * **Коли використовувати:** Коли вам потрібно створити послідовність, де є чітка залежність між елементами (напр., арифметична або геометрична прогресія).
>
> * **`Stream.generate(Supplier<T> s)`:**
>   * **Принцип:** **Незалежний.** Кожен елемент генерується незалежно від інших.
>   * **Як працює:** Для створення кожного елемента просто викликається метод `get()` у `Supplier`'а.
>   * **Коли використовувати:** Коли вам потрібні однакові об'єкти, випадкові значення або будь-які дані, генерація яких не залежить від попередніх.

#### **📝 Приклади:**

```java
// iterate: кожен елемент = попередній + 3
Stream<Integer> iterateStream = Stream.iterate(0, n -> n + 3); // 0, 3, 6, 9, ...

// generate: кожен елемент - це нове випадкове число
Stream<Double> generateStream = Stream.generate(Math::random); // 0.123..., 0.876..., ...
```

#### **📌 Що перевіряють:**

*Знання двох основних способів створення нескінченних потоків та розуміння семантичної різниці між ними.*

* * *

### **9 ❓ Питання:**

Чи можна створити потік символів з рядка `String`? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Для цього у класу `String` є метод **`.chars()`**.
>
> Однак, важливо пам'ятати, що цей метод повертає не `Stream<Character>`, а **`IntStream`**.
>
> **Чому `IntStream`?**
> Тому що `Stream`'и оптимізовані для роботи з примітивами, а `char` є числовим типом (може бути неявно перетворений на `int`). Робота з `IntStream` є набагато продуктивнішою, ніж створення `Stream<Character>`, яке б вимагало boxing кожного символу в об'єкт `Character`.
>
> Якщо вам все ж потрібен `Stream<Character>`, ви можете перетворити `IntStream` за допомогою `mapToObj`.

#### **📝 Приклад:**

```java
String text = "hello";

// Створення IntStream з символів
IntStream charStream = text.chars();

// Обробка (напр., вивід кодів символів)
charStream.forEach(System.out::println); // 104 101 108 108 111

// Перетворення в Stream<Character>
Stream<Character> characterStream = text.chars()
                                         .mapToObj(c -> (char) c);
```

#### **📌 Що перевіряють:**

*Знання додаткових, менш очевидних способів створення стрімів та розуміння нюансів роботи з примітивами.*

* * *

### **10 ❓ Питання:**

Як створити `Stream` з мапи (`Map`)? ⭐️

> #### **💬 Відповідь:**
>
> Інтерфейс `Map` напряму **не має методу `.stream()`**, оскільки мапа не є лінійною колекцією і не зрозуміло, з чого саме створювати потік: з ключів, значень чи пар.
>
> Однак, можна легко створити потік з її "видів" (views):
>
> 1. **Потік ключів (`Stream<K>`):**
>
>     ```java
>     Stream<K> keyStream = myMap.keySet().stream();
>     ```
>
> 2. **Потік значень (`Stream<V>`):**
>
>     ```java
>     Stream<V> valueStream = myMap.values().stream();
>     ```
>
> 3. **Потік ключ-значення (`Stream<Map.Entry<K, V>>`):**
>     Це найпоширеніший спосіб, якщо потрібно працювати і з ключами, і зі значеннями одночасно.
>
>     ```java
>     Stream<Map.Entry<K, V>> entryStream = myMap.entrySet().stream();
>     ```

#### **📝 Приклад обробки `entryStream`:**

```java
Map<String, Integer> map = Map.of("a", 1, "b", 2);

map.entrySet().stream()
   .filter(entry -> entry.getValue() > 1)
   .forEach(entry -> System.out.println(entry.getKey())); // b
```

#### **📌 Що перевіряють:**

*Розуміння того, що не всі колекції однакові, та знання стандартних способів отримання потоків з `Map` через її `entrySet`, `keySet` та `values`.*

* * *

### **11 ❓ Питання:**

Яким чином `Stream.Builder` може бути корисним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Stream.Builder<T>`** — це реалізація патерну "Будівельник" для створення `Stream`. Він корисний тоді, коли вам потрібно **поступово, імперативно** наповнювати потік елементами перед тим, як його передати кудись для обробки.
>
> **Процес:**
>
> 1. Створюєте екземпляр будівельника: `Stream.builder()`.
> 2. Додаєте елементи один за одним за допомогою методів `.add(T t)` або `.accept(T t)`.
> 3. Завершуєте побудову і отримуєте готовий `Stream` за допомогою методу `.build()`.
>
> **Коли це корисно:**
> Уявіть, що ви пишете метод, який будує потік на основі якоїсь складної логіки з `if`-ами.
>
> ```java
> public Stream<String> buildStream(boolean condition1, boolean condition2) {
>     Stream.Builder<String> builder = Stream.builder();
>     builder.add("default");
>     if (condition1) {
>         builder.add("extra1");
>     }
>     if (condition2) {
>         builder.add("extra2");
>     }
>     return builder.build();
> }
> ```
>
> Це дозволяє динамічно конструювати потік в імперативному стилі.

#### **📌 Що перевіряють:**

*Знання більш просунутих та менш поширених способів створення стрімів.*

* * *

### **12 ❓ Питання:**

Як створити об'єднаний потік з двох інших потоків? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для об'єднання (конкатенації) двох потоків в один послідовний використовується статичний метод **`Stream.concat(Stream<? extends T> a, Stream<? extends T> b)`**.
>
> Цей метод приймає два потоки і повертає новий, ледачий потік, який спочатку містить усі елементи першого потоку, а потім — усі елементи другого.

#### **📝 Приклад:**

```java
Stream<String> stream1 = Stream.of("A", "B");
Stream<String> stream2 = Stream.of("C", "D");

Stream<String> combinedStream = Stream.concat(stream1, stream2);

combinedStream.forEach(System.out::print); // Виведе: ABCD
```

**Важливо:** Обидва початкові потоки після цієї операції стануть "спожитими", навіть якщо сам `combinedStream` ще не оброблено.

#### **📌 Що перевіряють:**

*Знання утилітарних методів `Stream` для композиції потоків.*

* * *

### **13 ❓ Питання:**

Що таке `Spliterator` і яка його роль у створенні потоків, особливо паралельних? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Spliterator` (Splitable Iterator)** — це спеціальний тип ітератора, введений в Java 8, який є основою для роботи `Stream API`.
>
> **Його роль:**
>
> 1. **Джерело даних для потоку:** Будь-яка колекція має метод `spliterator()`, який повертає об'єкт, здатний ефективно проходити по її елементах.
> 2. **Підтримка паралелізму:** Це його ключова особливість. `Spliterator` має метод `trySplit()`, який намагається **розбити послідовність даних на дві частини**. Framework `ForkJoinPool`, що використовується в `parallelStream()`, багаторазово викликає `trySplit`, щоб розділити дані на менші шматки, які можна обробити паралельно.
>
> Ефективність `parallelStream` напряму залежить від того, наскільки добре `Spliterator` джерела даних може розбиватися (`ArrayList` — чудово, `LinkedList` — погано).
>
> Хоча розробники рідко працюють з `Spliterator` напряму, розуміння його ролі є ключем до розуміння того, як `Stream API` працює під капотом.

#### **📌 Що перевіряють:**

*Дуже глибокі знання `Stream API` та `Collections Framework`. Це питання рівня Senior, яке перевіряє розуміння низькорівневих механізмів паралельної обробки.*

* * *

### **14 ❓ Питання:**

У чому різниця в `iterate` між Java 8 та Java 9? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У Java 9 було додано нову, **більш гнучку і безпечну** перевантажену версію `Stream.iterate`.
>
> * **`Stream.iterate(seed, unaryOperator)` (версія з Java 8):**
>   * **Створює нескінченний потік.**
>   * Завжди вимагає `limit()` для уникнення нескінченного циклу.
>
> * **`Stream.iterate(seed, hasNextPredicate, nextFunction)` (нова версія з Java 9):**
>   * **Створює скінченний потік.**
>   * Вона приймає три аргументи:
>         1. `seed`: початкове значення.
>         2. `hasNextPredicate`: `Predicate`, який перевіряє, чи потрібно продовжувати генерацію.
>         3. `nextFunction`: `UnaryOperator`, який генерує наступний елемент.
>
> Ця нова версія є аналогом класичного `for` циклу (`for (int i = 0; i < 10; i++)`) і є значно безпечнішою, оскільки не може випадково призвести до нескінченного циклу.

#### **📝 Приклад (Java 9):**

```java
// Створити потік чисел від 0 до 9
Stream.iterate(0, i -> i < 10, i -> i + 1)
      .forEach(System.out::println);
```

#### **📌 Що перевіряють:**

*Знання еволюції Java API та покращень, що з'являються у нових версіях. Це показує, що кандидат слідкує за розвитком платформи.*

* * *

### **15 ❓ Питання:**

Як би ви створили потік з об'єкта, який не є колекцією (наприклад, з власного кастомного об'єкта `Book`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо об'єкт не є колекцією, потрібно вирішити, що саме має бути елементами потоку. Є кілька підходів:
>
> 1. **Якщо об'єкт — це просто контейнер для одного значення:**
>     Найпростіше використати `Stream.of(book)`. Ви отримаєте потік з одного елемента. Це корисно, якщо ви хочете застосувати до одного об'єкта ланцюжок операцій у функціональному стилі.
>
> 2. **Якщо об'єкт логічно представляє послідовність:**
>     Наприклад, `Book` може складатися з `Chapter`. Найкращим підходом буде додати до класу `Book` метод, який повертає потік його компонентів.
>
>     ```java
>     class Book {
>         private List<Chapter> chapters;
>         
>         public Stream<Chapter> chapters() {
>             return chapters.stream();
>         }
>     }
>     
>     // Використання:
>     book.chapters().map(...).collect(...)
>     ```
>
>     Це є найбільш ідіоматичним та чистим рішенням.
>
> 3. **Створення потоку "на льоту" з полів об'єкта:**
>     Якщо вам потрібен потік з кількох полів, можна використати `Stream.of()`.
>
>     ```java
>     // Потік з назви та автора книги
>     Stream<String> bookInfo = Stream.of(book.getTitle(), book.getAuthor());
>     ```
>
> Вибір конкретного способу залежить від семантики та завдання, яке потрібно вирішити.

#### **📌 Що перевіряють:**

*Здатність до проектування API та вміння застосовувати принципи `Stream API` до власних класів. Кандидат повинен думати про те, як зробити свої об'єкти зручними для використання у функціональному стилі.*

* * *
