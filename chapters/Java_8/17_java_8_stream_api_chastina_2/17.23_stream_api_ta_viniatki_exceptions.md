---
layout: default
title: "Stream API та винятки (Exceptions)"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 23
---

# Stream API та винятки (Exceptions)

* короткий опис: Stream API не надає спеціального механізму для обробки винятків усередині лямбда-виразів. `Checked Exceptions` не можуть бути напряму проброшені з лямбда (через обмеження функціональних інтерфейсів), тому їх треба обробляти `try-catch` або обгортати в `RuntimeException`.

* поняття: Checked/Unchecked exceptions, `exception wrapping`, `try-catch` у лямбді.

* приклади: Обгортання `IOException` у `RuntimeException` всередині `map()`-операції.

* ключові моменти: `Checked Exceptions` – проблема в лямбдах. Обробка винятків у стрімах вимагає додаткових "трюків".

* що важливо знати на співбесіді: Проблема `checked exceptions` у лямбдах, способи вирішення.

### **✅ Офіційне визначення:**

Stream API не надає спеціального механізму для обробки винятків усередині лямбда-виразів. Зокрема, **checked exceptions
** не можуть бути напряму проброшені з лямбда, що є обмеженням у дизайні.

### **🧠 Простими словами:**

В Java стрімах нема чарівної таблетки для винятків — доводиться вигинатися. Лямбди не люблять **`throws`**, тому **доводиться або обгортати, або ловити винятки на місці.**

---

## 📊 **Типові ситуації:**

* **Checked Exceptions (наприклад, IOException)** — не можна просто написати **`stream.map(x -> someMethodThatThrows())`
  **.
* **Unchecked Exceptions (наприклад, NullPointerException)** — проходять нормально, але краще контролювати.
* Не можна напряму використовувати **`throw`** у функціональних інтерфейсах (**`Function, Supplier`**, тощо).

---

## **Пояснення на пальцях (💡)**

Лямбди в Java — це ніби **невидимі контрактники**, які не хочуть підписувати угоди на checked винятки. Якщо твій методкидає **`IOException`**, Java каже:

"У лямбді це не годиться, друже. Закатай це у try-catch або обгорни."

---

## **Корисні приклади (🧪)**

🔹 **Проблема з checked exception:**

```java
Files.lines(Path.of("file.txt"))  // throws IOException
     .map(line -> line.toUpperCase()) // OK
     .collect(Collectors.toList());
```

🔹 **Обгортання через try-catch у лямбді:**

```java
List<String> result = list.stream()
        .map(s -> {
          try {
            return someMethodThatThrows(s);
          } catch (IOException e) {
            throw new RuntimeException(e); // або логування
          }
        })
        .collect(Collectors.toList());
```

🔹 **Створення утилітного методу (обгортка):**

```java
@FunctionalInterface
interface ThrowingFunction<T, R> {
    R apply(T t) throws Exception;
}

static <T, R> Function<T, R> wrap(ThrowingFunction<T, R> f) {
    return t -> {
        try {
            return f.apply(t);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    };
}

// Використання:
list.stream()
    .map(wrap(MyClass::methodThatThrows))
    .collect(Collectors.toList());
```

🔹 **Обробка через Optional:**

```java
public Optional<String> tryProcess(String input) {
    try {
        return Optional.of(process(input));
    } catch (Exception e) {
        return Optional.empty();
    }
}

list.stream()
    .map(MyClass::tryProcess)
    .filter(Optional::isPresent)
    .map(Optional::get)
    .toList();
```

---

## **Порівняння (⚖️)**

|               Підхід               |             Плюси              |             Мінуси             |
|:----------------------------------:|:------------------------------:|:------------------------------:|
| **Вбудований try-catch у лямбді**  |        Прямий і простий        |       Може захламити код       |
| **Обгортка через утилітний метод** | Повторне використання, чистіше |       Ускладнює читання        |
|    **Optional для результатів**    |     Безпечне ланцюжкування     | Може приховати причину помилки |
|        **RuntimeException**        |     Проброс винятків, як є     |  Менш контрольована поведінка  |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Stream ловить не все — checked кине тебе в лице."**

Папам’ятай: **unchecked пропустить, checked — вимагатиме try-catch.**

---

## **🟩 Підсумок:**

* Винятки у Stream API потребують додаткової уваги.

* **`Checked`** `exceptions` не підтримуються напряму у лямбдах.

* Для обробки:
  * використовуй try-catch у лямбді,
  * обгортай у RuntimeException,
  * або працюй через Optional.

* Добра практика — **витягнути обробку винятків за межі стріму** або **винести в окрему функцію**.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Stream API**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/stream/package-summary.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка основна проблема з обробкою перевірених винятків (checked exceptions) у лямбда-виразах?

> #### **💬 Відповідь:**
>
> Основна проблема полягає в тому, що **стандартні функціональні інтерфейси** з пакету `java.util.function` (такі як `Function`, `Predicate`, `Consumer`) **не оголошують перевірених винятків** у сигнатурах своїх абстрактних методів.
>
> Наприклад, метод `apply` в `Function<T, R>` має сигнатуру: `R apply(T t)`.
> Вона не містить блоку `throws IOException` чи `throws SQLException`.
>
> Згідно з правилами Java, ви не можете кинути перевірений виняток з методу, якщо він не оголошений в його сигнатурі. Тому, якщо тіло вашого лямбда-виразу містить код, що кидає перевірений виняток (наприклад, операції з файлами), ви отримаєте **помилку компіляції**.
>
> З неперевіреними винятками (`RuntimeException`) такої проблеми немає, оскільки їх можна кидати з будь-якого місця.

#### **📝 Аналогія:**

Уявіть, що функціональний інтерфейс — це посадова інструкція, а лямбда — працівник. Якщо в інструкції не вказано, що працівник може повідомити про форс-мажор (кинути перевірений виняток), то він не має права цього робити.

#### **📌 Що перевіряють:**

*Розуміння кореневої причини проблеми: невідповідність сигнатур методів стандартних функціональних інтерфейсів та методів, що кидають перевірені винятки. Це фундаментальне питання.*

* * *

### **2 ❓ Питання:**

Який найпростіший спосіб обробити перевірений виняток всередині лямбда-виразу?

> #### **💬 Відповідь:**
>
> Найпростіший і найбільш прямолінійний спосіб — це обгорнути "небезпечний" код у блок **`try-catch`** безпосередньо всередині тіла лямбди.
>
> Це дозволяє "спіймати" перевірений виняток і вирішити, що з ним робити, не порушуючи контракт функціонального інтерфейсу.

#### **📝 Приклад:**

Нехай є метод `processLine(String line)`, що кидає `IOException`.

```java
List<String> lines = ...;

lines.stream()
     .forEach(line -> {
         try {
             processLine(line);
         } catch (IOException e) {
             // 1. Можна загорнути і кинути як неперевірений
             // throw new UncheckedIOException(e);
             
             // 2. Або просто залогувати помилку
             System.err.println("Помилка обробки рядка: " + line);
         }
     });
```

#### **📝 Коментар:**

Хоча цей підхід робочий, він робить лямбда-вираз громіздким та може призвести до дублювання коду, якщо таку обробку потрібно робити в кількох місцях.

#### **📌 Що перевіряють:**

*Практичні навички вирішення проблеми. Кандидат повинен знати про `try-catch` всередині лямбди як про базове, хоч і не найнайелегантніше, рішення.*

* * *

### **3 ❓ Питання:**

Що таке "обгортання винятків" (exception wrapping) і як воно допомагає в `Stream API`?

> #### **💬 Відповідь:**
>
> **Обгортання винятків** — це техніка, при якій ви перехоплюєте перевірений виняток (`checked exception`) і кидаєте замість нього **неперевірений виняток (`unchecked exception`)**, зазвичай `RuntimeException` або його нащадка, передаючи оригінальний виняток у його конструктор.
>
> **Як це допомагає:**
>
> Це дозволяє "прокинути" інформацію про помилку вгору по стеку викликів, не оголошуючи `throws`. Лямбда-вираз стає значно чистішим. Помилку можна буде перехопити одним `try-catch` блоком навколо всього ланцюжка стріму.
>
> З Java 8 для цієї мети ідеально підходить `java.io.UncheckedIOException`.

#### **📝 Приклад:**

```java
public String readFileContent(String path) {
    try {
        return Files.readString(Paths.get(path));
    } catch (IOException e) {
        // Загортаємо перевірений IOException у неперевірений
        throw new UncheckedIOException(e);
    }
}

// Тепер лямбда чиста
List<String> contents = paths.stream()
                              .map(this::readFileContent) // Метод можна передавати напряму
                              .collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Знання поширеного патерну для обробки винятків у функціональному стилі. Це показує прагматичний підхід до вирішення проблеми.*

* * *

### **4 ❓ Питання:**

Що станеться, якщо лямбда всередині `Stream` кине неперевірений виняток (`RuntimeException`)?

> #### **💬 Відповідь:**
>
> Якщо лямбда-вираз, що використовується в будь-якій операції `Stream`, кине неперевірений виняток, то **виконання всього потоку негайно припиниться**, і цей виняток буде "прокинуто" вгору по стеку викликів.
>
> Подальші елементи потоку не будуть оброблені. Цей виняток можна перехопити за допомогою блоку `try-catch` навколо всієї операції зі стрімом.

#### **📝 Приклад:**

```java
List<String> list = Arrays.asList("1", "2", "abc", "4");

try {
    List<Integer> numbers = list.stream()
                                  .map(Integer::parseInt) // Кине NumberFormatException (нащадок RuntimeException) на "abc"
                                  .collect(Collectors.toList());
} catch (NumberFormatException e) {
    System.err.println("Помилка під час парсингу: " + e.getMessage());
}
// Обробка не буде продовжена для елемента "4". Стрім "зламається" на "abc".
```

#### **📌 Що перевіряють:**

*Розуміння механізму обробки помилок у `Stream API`. Кандидат повинен знати, що `RuntimeException` зупиняє всю конвеєрну обробку.*

* * *

### **5 ❓ Питання:**

Як можна обробляти винятки в `Stream`, не перериваючи його роботу, а, наприклад, просто пропускаючи "проблемні" елементи?

> #### **💬 Відповідь:**
>
> Для цього можна використати підхід з **`Optional`** або `null`-значеннями у поєднанні з `filter`.
>
> **Ідея:** Замість того, щоб кидати виняток, метод-обробник повертає `Optional.empty()` (або `null`) у разі помилки. Потім у ланцюжку стріму додається крок `.filter()`, який відсіює ці порожні результати.
>
> **Кроки:**
>
> 1. Створити метод-обгортку, який викликає "небезпечний" код, і в `catch` блоці повертає `Optional.empty()`.
> 2. У стрімі використовувати `map` з цим методом. Результатом буде `Stream<Optional<T>>`.
> 3. Використати `flatMap(Optional::stream)` (в Java 9+) або `filter(Optional::isPresent).map(Optional::get)` (в Java 8), щоб "розпакувати" значення і відкинути порожні.

#### **📝 Приклад:**

```java
public Optional<Integer> tryParseInt(String s) {
    try {
        return Optional.of(Integer.parseInt(s));
    } catch (NumberFormatException e) {
        return Optional.empty();
    }
}

List<String> list = Arrays.asList("1", "2", "abc", "4");

List<Integer> numbers = list.stream()
                           .map(this::tryParseInt)  // Stream<Optional<Integer>>
                           .flatMap(Optional::stream) // Stream<Integer>
                           .collect(Collectors.toList());

System.out.println(numbers); // [1, 2, 4]
```

#### **📌 Що перевіряють:**

*Здатність реалізувати граційну обробку помилок (`graceful error handling`) у функціональному стилі, не перериваючи весь потік.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати блок `try-with-resources` разом зі `Stream API`?

> #### **💬 Відповідь:**
>
> **Так, і це є обов'язковою практикою**, коли потік створюється з ресурсу, який потрібно закривати (наприклад, файл).
>
> Метод `Files.lines()` є яскравим прикладом. Він повертає `Stream<String>`, але цей стрім "тримає" відкритим файловий потік. Якщо його не закрити, відбудеться витік ресурсів.
>
> Конструкція `try-with-resources` гарантує, що метод `.close()` на потоці буде викликаний автоматично, навіть якщо під час обробки виникне виняток.

#### **📝 Приклад:**

```java
Path path = Paths.get("data.txt");

try (Stream<String> lines = Files.lines(path)) { // Потік буде автоматично закрито
    lines.filter(line -> !line.isBlank())
         .forEach(System.out::println);
} catch (IOException e) {
    // Обробка помилки відкриття файлу
    e.printStackTrace();
}
```

#### **📌 Що перевіряють:**

*Знання правил роботи з ресурсами в Java (`AutoCloseable`) та вміння застосовувати `try-with-resources` для безпечної роботи зі стрімами, що базуються на ресурсах.*

* * *

### **7 ❓ Питання:**

Які недоліки має обгортання `Checked` винятків у `RuntimeException`?

> #### **💬 Відповідь:**
>
> Хоча обгортання є поширеним підходом, він має свої недоліки.
>
> 1. **Втрата перевірки на етапі компіляції:** Основна перевага `checked` винятків полягає в тому, що вони змушують розробника обробляти потенційну помилку. Перетворюючи їх на `unchecked`, ми втрачаємо цю гарантію. Клієнтський код може не знати, що операція може провалитися, і не бути готовим обробити `RuntimeException`.
> 2. **Порушення семантики:** `RuntimeException` зазвичай сигналізує про **програмні помилки** (баги), а не про очікувані проблеми середовища (як відсутність файлу). Це може заплутати розробників, які будуть підтримувати код.
> 3. **Ускладнення `catch` блоків:** Якщо ви хочете зловити конкретний `IOException`, вам доведеться перехоплювати `RuntimeException`, а потім перевіряти його причину (`getCause()`), що робить код менш читабельним.
>
> Цей підхід є компромісом між чистотою коду всередині лямбди та типізованою обробкою помилок.

#### **📌 Що перевіряють:**

*Здатність до критичного аналізу. Кандидат повинен розуміти не тільки переваги, але і недоліки поширених патернів.*

* * *

### **8 ❓ Питання:**

Що таке "утилітарна функція-обгортка" (wrapper function) і як вона допомагає уникнути дублювання коду `try-catch`? ⭐️

> #### **💬 Відповідь:**
>
> Це елегантний спосіб інкапсулювати логіку `try-catch` в одному місці, щоб не повторювати її в кожній лямбді.
>
> Ідея полягає в тому, щоб створити **функцію вищого порядку**, яка:
>
> 1. Приймає "небезпечну" функцію, що кидає перевірений виняток (для цього нам потрібен власний функціональний інтерфейс).
> 2. Повертає стандартну, "безпечну" функцію (напр., `Function`), всередині якої вже реалізовано блок `try-catch` з логікою обгортання.
>
> Це дозволяє використовувати таку функцію-обгортку для будь-якої лямбди з відповідною сигнатурою, роблячи код у стрімі дуже чистим.

#### **📝 Приклад:**

```java
// 1. Власний інтерфейс
@FunctionalInterface
interface ThrowingFunction<T, R> {
    R apply(T t) throws Exception;
}
// 2. Утиліта-обгортка
public static <T, R> Function<T, R> unchecked(ThrowingFunction<T, R> f) {
    return t -> {
        try {
            return f.apply(t);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    };
}
// 3. Використання
list.stream()
    .map(unchecked(MyClass::methodThatThrowsIOException))
    .collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Здатність до написання утилітарного, перевикористовуваного коду. Це демонструє розуміння функцій вищого порядку та принципів чистого коду.*

* * *

### **9 ❓ Питання:**

Що більш семантично правильно в `catch` блоці всередині лямбди для операції `filter`: кидати `RuntimeException` чи повертати `false`? ⭐️

> #### **💬 Відповідь:**
>
> Вибір залежить від того, що означає помилка для вашої бізнес-логіки, але в більшості випадків **повернення `false` є більш безпечним та семантично правильним** для `filter`.
>
> * **Повернення `false`:**
>   * **Семантика:** "Я не зміг перевірити цей елемент через помилку, тому вважаю, що він не відповідає умові, і його слід пропустити".
>   * **Поведінка:** `Stream.filter()` просто **відкине** цей елемент, і потік продовжить свою роботу. Це є формою **граційної обробки помилок**.
>
> * **Кидання `RuntimeException`:**
>   * **Семантика:** "Під час перевірки сталася критична помилка, яка робить подальшу обробку всього потоку неможливою або безглуздою".
>   * **Поведінка:** Виконання всього стріму **негайно припиниться**.
>
> **Коли що обирати:**
>
> * Якщо помилка є **очікуваною і локальною** (напр., тимчасова недоступність зовнішнього сервісу для валідації одного елемента) — **повертайте `false`**.
> * Якщо помилка свідчить про **серйозний збій** (неправильна конфігурація, баг у коді) — **кидайте `RuntimeException`**, щоб зупинити процес і повідомити про проблему якомога швидше.

#### **📌 Що перевіряють:**

*Здатність аналізувати семантику помилок та обирати відповідну стратегію їх обробки. Це ознака інженерної зрілості.*

* * *

### **10 ❓ Питання:**

Чи існують сторонні бібліотеки, які спрощують обробку винятків у стрімах? ⭐️

> #### **💬 Відповідь:**
>
> **Так, існують.** Оскільки ця проблема є дуже поширеною, з'явилися бібліотеки, які надають готові рішення.
>
> 1. **Vavr (колишня Javaslang):**
>     * Надає потужний контейнер **`Try<T>`**, який є значно кращою альтернативою `try-catch`.
>     * Ви можете обгорнути "небезпечний" код у `Try.of(...)`. Результатом буде або `Success<T>`, що містить успішний результат, або `Failure<Throwable>`, що містить виняток.
>     * `Try` має методи, як `map`, `filter`, `getOrElse`, що дозволяє обробляти помилки в ланцюжковому, функціональному стилі.
>
> 2. **Lombok (`@SneakyThrows`):**
>     * Це анотація, яка "обманює" компілятор, змушуючи його думати, що перевірений виняток є неперевіреним. Вона автоматично генерує необхідний `try-catch` блок, який "прокидає" виняток далі.
>     * Робить код дуже чистим, але є певною "магією", яка може приховувати реальну логіку.
>
> 3. **StreamEx:**
>     * Це розширена бібліотека для роботи зі стрімами, яка також надає свої механізми для більш зручної обробки винятків.
>
> Використання таких бібліотек може значно спростити код, але вимагає, щоб вся команда знала, як вони працюють.

#### **📌 Що перевіряють:**

*Кругозір та знання екосистеми Java. Кандидат повинен знати про існування інструментів, що вирішують поширені проблеми.*

* * *

### **11 ❓ Питання:**

Як обробити винятки, що можуть виникнути в паралельному потоці (`parallelStream`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Обробка така сама, як і в послідовному:** якщо будь-яка з лямбд в **будь-якому** з потоків кине неперевірений виняток, виконання **всього** `parallelStream` буде перервано, і цей виняток буде прокинуто.
>
> **Але є важливий нюанс:**
>
> * **Загортання:** Якщо ви використовуєте стратегію обгортання перевірених винятків у `RuntimeException` і збираєте результати, наприклад, в `collect`, ви можете отримати не один, а **кілька винятків** з різних потоків.
> * **Перша помилка "перемагає"**: Зазвичай `parallelStream` зупиняється і прокидає **перший виняток**, який трапився в будь-якому потоці. Інші винятки можуть бути "заглушені".
> * **Збирання винятків:** Більш просунутий підхід — не кидати `RuntimeException` одразу, а збирати всі помилки в потокобезпечну колекцію, а вже після завершення стріму аналізувати їх. Але це значно ускладнює код.
>
> Загалом, обробка помилок у паралельних потоках вимагає ще більшої обережності.

#### **📌 Що перевіряють:**

*Розуміння того, як винятки поводяться в багатопотоковому середовищі. Це демонструє знання concurrency.*

* * *

### **12 ❓ Питання:**

Чому `forEach` є не найкращим місцем для обробки винятків, які вимагають переривання? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Проблема `forEach` полягає в тому, що він є **термінальною операцією**, і ви не можете легко "вийти" з нього або з зовнішнього методу, як у звичайному циклі.
>
> У лямбді, переданій в `forEach`:
>
> * `return;` просто вийде з поточної ітерації лямбди (як `continue`).
> * Немає простого способу зробити аналог `break`.
>
> Якщо в `catch` блоці ви просто залогуєте помилку і нічого не зробите, `forEach` **продовжить ітеруватися** по решті елементів, що може бути небажаною поведінкою, якщо помилка була критичною.
>
> Щоб гарантовано зупинити всю обробку, ви змушені **кинути `RuntimeException`** з `catch` блоку. Це працює, але є менш елегантним, ніж використання проміжних операцій та колекторів. Кращим підходом часто є обробка помилок на етапі `map` і відфільтровування "проблемних" елементів, перш ніж вони потраплять у фінальну обробку.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантики `forEach` та його відмінностей від класичних циклів у контексті керування потоком виконання.*

* * *

### **13 ❓ Питання:**

Як пов'язані обробка винятків у лямбдах та патерн "монада `Either`" у функціональних мовах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Проблема обробки перевірених винятків у Java виникає через те, що в мові є два окремих "канали" для повернення результату з методу: основний (через `return`) і помилковий (через `throw`).
>
> У чисто функціональних мовах, як-от Scala чи Haskell, цю проблему вирішують за допомогою **монади `Either`** (або схожих конструкцій, як `Result` в Rust).
>
> * **Що таке `Either<L, R>`:**
>     Це контейнер, який може зберігати значення **або** лівого типу (`L`), **або** правого типу (`R`).
>   * За конвенцією, **`Left`** використовується для зберігання **помилки**.
>   * **`Right`** використовується для зберігання **успішного результату**.
>
> **Як це допомагає:**
>
> Функція, яка може "впасти", замість кидання винятку повертає `Either`. Наприклад, `Either<Exception, String>`. Це означає, що **помилка стає звичайною частиною повернутого значення**.
>
> У `Stream API` такий метод у `map` не порушував би жодних контрактів. Потім можна було б легко відфільтрувати або обробити результати, що містять `Left`, а з `Right` — продовжити роботу.
>
> Бібліотека **Vavr** для Java надає реалізацію `Either`, що дозволяє писати код у такому стилі.

#### **📌 Що перевіряють:**

*Дуже глибокі теоретичні знання функціонального програмування. Кандидат повинен знати про альтернативні підходи до обробки помилок, що існують в інших мовах, і розуміти їхні переваги.*

* * *

### **14 ❓ Питання:**

Чи можна реалізувати `flatMap` для "безпечної" обробки винятків? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, це дуже потужний і елегантний патерн.** Ідея полягає в тому, щоб створити функцію, яка у випадку успіху повертає потік з одного елемента, а у випадку помилки — порожній потік.
>
> Потім цю функцію можна використовувати з `flatMap`, який автоматично "викине" всі помилкові випадки (оскільки `flatMap(stream -> Stream.empty())` нічого не додає до кінцевого потоку).

#### **📝 Приклад:**

```java
public Stream<Integer> safeParseInt(String s) {
    try {
        return Stream.of(Integer.parseInt(s));
    } catch (NumberFormatException e) {
        return Stream.empty(); // Повертаємо порожній потік у разі помилки
    }
}

List<String> list = List.of("1", "2", "abc", "4");

List<Integer> numbers = list.stream()
                           .flatMap(this::safeParseInt) // Застосовуємо нашу "безпечну" функцію
                           .collect(Collectors.toList());

System.out.println(numbers); // [1, 2, 4]
```

Цей підхід є одним з найбільш "функціональних" та чистих, оскільки він повністю уникає кидання винятків і обробляє помилки через маніпуляцію потоком.

#### **📌 Що перевіряють:**

*Здатність комбінувати різні концепції (`flatMap`, обробка помилок) для створення елегантних та ідіоматичних рішень.*

* * *

### **15 ❓ Питання:**

Як `CompletableFuture` API спрощує обробку винятків в асинхронних стрімах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `CompletableFuture` надає вбудований, декларативний механізм для обробки помилок, що ідеально поєднується зі стилем `Stream API`.
>
> Якщо всередині `thenApply` або `thenAccept` лямбда кидає виняток, `CompletableFuture` не "падає" одразу, а переходить у **завершений з помилкою (exceptionally completed)** стан, зберігаючи цей виняток у собі.
>
> Далі по ланцюжку ви можете обробити цей виняток за допомогою спеціальних методів, що приймають функціональні інтерфейси:
>
> 1. **`.exceptionally(Function<Throwable, T> fn)`**:
>     * Цей блок виконується **тільки якщо** сталася помилка.
>     * Він дозволяє "відновити" ланцюжок, повернувши значення за замовчуванням.
>
> 2. **`.handle(BiFunction<T, Throwable, U> fn)`**:
>     * Цей блок виконується **завжди**.
>     * Він приймає або результат, або виняток (один з них буде `null`) і дозволяє обробити обидва сценарії в одному місці.
>
> #### **Приклад:**
>
> ```java
> CompletableFuture.supplyAsync(() -> {
>     if (true) throw new RuntimeException("Something went wrong");
>     return "Success";
> })
> .thenApply(String::toUpperCase) // Цей крок буде пропущено
> .exceptionally(ex -> {
>     System.err.println(ex.getMessage()); // Обробляємо помилку
>     return "Default Value"; // Відновлюємо ланцюжок
> })
> .thenAccept(System.out::println); // Виведе "Default Value"
> ```
>
> Цей підхід є набагато чистішим і потужнішим, ніж традиційні `try-catch`, оскільки обробка помилок стає частиною декларативного конвеєра.

#### **📌 Що перевіряють:**

*Досвід роботи з `CompletableFuture` та розуміння його механізмів обробки помилок. Це демонструє вміння писати надійний асинхронний код.*

* * *
