---
layout: default
title: "Пошук елементів: findFirst(), findAny(), anyMatch()"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 12
---

# Пошук елементів: findFirst(), findAny(), anyMatch()

* короткий опис: Термінальні операції Stream API для пошуку елементів: `findFirst()` (повертає перший елемент як `Optional<T>`), `findAny()` (повертає будь-який елемент як `Optional<T>`), `anyMatch()` (перевіряє, чи хоча б один елемент відповідає умові, повертає `boolean`).

* поняття: `findFirst()`, `findAny()`, `anyMatch()`, `Optional` (для обробки відсутніх значень).

* приклади: `stream.filter(...).findFirst()`, `stream.parallel().findAny()`, `stream.anyMatch(n -> n < 0)`.

* ключові моменти: `findFirst()` – детермінований, `findAny()` – може бути швидшим у паралельних стрімах. `anyMatch()` повертає `boolean`. Всі три зупиняються на першому збігу (short-circuiting).

* що важливо знати на співбесіді: Різниця, `Optional` як результат.

### **✅ Офіційне визначення:**

Методи **`findFirst(), findAny(), anyMatch()`** — термінальні операції Stream API для пошуку або перевірки наявності елементів у стрімі. Вони не повертають колекцію, а результат у вигляді **`Optional<T>`** або **`boolean`**.

### **🧠 Простими словами:**

Це спосіб сказати: «Дай мені перший/будь-який/якийсь елемент, що задовольняє умову, або скажи, чи хоч один такий є»

---

## 📊 **Методи пошуку та їх особливості:**

|       Метод       | Тип повернення |                                Опис                                |         Властивість         |
|:-----------------:|:--------------:|:------------------------------------------------------------------:|:---------------------------:|
| **`findFirst()`** | `Optional<T>`  |                   Повертає перший елемент стріму                   |       Детермінований        |
|  **`findAny()`**  | `Optional<T>`  |                    Повертає *будь-який* елемент                    | Недетермінований у паралелі |
| **`anyMatch()`**  |   `boolean`    | Перевіряє, чи *існує хоча б один* елемент, що відповідає предикату |      Булева перевірка       |

---

## **Пояснення на пальцях (💡)**

* 🔍 **`findFirst()`** — "Дай перший підходящий".
* 🎲 **`findAny()`** — "Мені все одно який, просто будь-який, що підходить".
* ✅ **`anyMatch()`** — "Чи є хоч один, що підходить?"

💡 **`findAny()`** може працювати швидше на **паралельних стрімах**, бо не чекає порядок.

---

## **Корисні приклади (🧪)**

```java
// 🔎 Пошук першого парного числа
Optional<Integer> firstEven = Stream.of(1, 3, 4, 6)
    .filter(n -> n % 2 == 0)
    .findFirst(); // --> Optional[4]
```

```java
// 🎯 Пошук будь-якого елемента у паралельному стрімі
Optional<Integer> anyEven = Stream.of(2, 4, 6, 8)
    .parallel()
    .filter(n -> n % 2 == 0)
    .findAny(); // --> Optional[2] або інше
```

```java
// ✅ Чи є хоч один негативний елемент?
boolean hasNegative = Stream.of(3, -1, 4)
    .anyMatch(n -> n < 0); // --> true
```

```java
// 🔁 Обробка результату findFirst
String result = Stream.of("a", "b", "c")
    .filter(s -> s.equals("x"))
    .findFirst()
    .orElse("нема такого"); // --> "нема такого"
```

---

## **Порівняння (⚖️)**

|       Метод       |     Повертає      | Порядок важливий | Паралельне виконання | Тип результату |
|:-----------------:|:-----------------:|:----------------:|:--------------------:|:--------------:|
| **`findFirst()`** |  Перший елемент   |      ✅ Так       |    ⚠️ Повільніше     | `Optional<T>`  |
|  **`findAny()`**  | Будь-який елемент |       ❌ Ні       |     ✅ Ефективне      | `Optional<T>`  |
| **`anyMatch()`**  | Чи існує елемент  |       ❌ Ні       |    ✅ Добре працює    |   `boolean`    |

---

## **🧠 Мнемоніка для запам’ятовування:**

**first** --> перший,  
**any** --> будь-який,  
**match** --> просто "є такий?".

Або:  
**"First — впевнений, Any — імпульсивний, Match — питає".**

---

## **🟩 Підсумок:**

* Всі три — **термінальні** операції: після них стрім закінчується.
* **`findFirst()`** — зберігає порядок, краще для **послідовних стрімів**.
* **`findAny()`** — швидший у **паралельних стрімах**, коли порядок не важливий.
* **`anyMatch()`** — повертає **`boolean`**, не **`Optional`**.
* **`Optional`** — ключова концепція: значення може бути, а може й не бути. Не забувай обробляти\!

---

### **🔗 Додаткові джерела:**

* [**Oracle JavaDoc — Stream.findFirst()**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/Stream.html#findFirst--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке операції `findFirst()` і `findAny()`, і чим вони схожі?

> #### **💬 Відповідь:**
>
> `findFirst()` та `findAny()` — це **термінальні операції** з коротким замиканням (short-circuiting), призначені для **пошуку одного елемента** у потоці.
>
> **Їхні спільні риси:**
>
> * **Термінальні:** Вони запускають обробку потоку і завершують його.
> * **Short-circuiting:** Вони припиняють обробку потоку, як тільки знаходять перший-ліпший елемент, що задовольняє умовам.
> * **Повертають `Optional<T>`:** Обидві операції повертають `Optional`, оскільки потік може бути порожнім або не містити елементів, що відповідають умовам. Це змушує розробника безпечно обробляти випадок, коли елемент не знайдено.

#### **📝 Аналогія:**

Ви шукаєте першу вільну парковку на вулиці. Як `findFirst`, так і `findAny` зупинять пошук, як тільки ви її знайдете, не перевіряючи решту вулиці.

#### **📌 Що перевіряють:**

*Базове розуміння призначення двох основних операцій пошуку та знання того, що вони повертають `Optional`.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між `findFirst()` та `findAny()`?

> #### **💬 Відповідь:**
>
> Ключова різниця полягає в їхній поведінці щодо **порядку елементів**, що особливо важливо для паралельних потоків.
>
> * **`findFirst()`:**
>   * **Гарантує** повернення **першого** елемента потоку відповідно до його encounter order (порядку, в якому елементи зустрічаються).
>   * Цей метод є **детермінованим**: при кожному запуску на одному й тому ж потоці він поверне той самий результат.
>
> * **`findAny()`:**
>   * **Не гарантує**, який саме елемент буде повернуто. Він повертає будь-який елемент з потоку, що відповідає умовам.
>   * Цей метод є **недетермінованим** для паралельних потоків.
>
> **Коли ця різниця важлива:**
>
> * У **послідовних** потоках `findAny()` зазвичай повертає перший елемент, тому різниця непомітна.
> * У **паралельних** потоках `findAny()` є значно **продуктивнішим**, оскільки він може повернути перший-ліпший результат, знайдений будь-яким потоком, не чекаючи завершення інших. `findFirst()`, навпаки, вимагає синхронізації, щоб визначити, який елемент є "справді" першим.

#### **📌 Що перевіряють:**

*Розуміння нюансів поведінки цих методів у паралельних потоках. Це ключове питання для перевірки знань про паралельну обробку.*

* * *

### **3 ❓ Питання:**

Коли слід віддавати перевагу `findFirst()`, а коли — `findAny()`?

> #### **💬 Відповідь:**
>
> Правило вибору дуже просте:
>
> * **Використовуйте `findFirst()`, коли:**
>   * **Порядок важливий.** Вам потрібен саме перший елемент, що відповідає умові (наприклад, "знайти першого переможця у списку").
>   * Ви працюєте з послідовним потоком (`stream()`) і хочете отримати передбачуваний результат.
>
> * **Використовуйте `findAny()`, коли:**
>   * **Порядок не важливий.** Вам достатньо знайти будь-який елемент, що відповідає умові (наприклад, "перевірити, чи є в системі хоч один активний користувач").
>   * Ви працюєте з **паралельним потоком (`parallelStream()`)**. `findAny()` дозволяє досягти максимальної продуктивності, оскільки не накладає обмежень на порядок.

#### **📝 Коментар:**

Навіть якщо ви не використовуєте паралельні потоки, звичка використовувати `findAny()`, коли порядок не важливий, робить ваш код більш "готовим" до майбутньої паралелізації.

#### **📌 Що перевіряють:**

*Практичні навички вибору правильного інструменту на основі вимог до порядку та продуктивності.*

* * *

### **4 ❓ Питання:**

Наведіть приклад, як безпечно обробити результат, повернутий `findFirst()` або `findAny()`.

> #### **💬 Відповідь:**
>
> Оскільки ці методи повертають `Optional<T>`, результат потрібно обробляти за допомогою методів `Optional`. Найпоширеніші підходи:
>
> * **`isPresent()` + `get()` (не рекомендується):**
>
>     ```java
>     Optional<String> result = ...;
>     if (result.isPresent()) {
>         System.out.println(result.get());
>     } else {
>         System.out.println("Не знайдено.");
>     }
>     ```
>
> * **`ifPresent(Consumer<T> action)` (кращий варіант для дій):**
>     Виконує дію, тільки якщо значення існує.
>
>     ```java
>     result.ifPresent(System.out::println);
>     ```
>
> * **`orElse(T other)` (надання значення за замовчуванням):**
>     Повертає значення, якщо воно є, або надане значення за замовчуванням.
>
>     ```java
>     String name = result.orElse("Guest");
>     ```
>
> * **`orElseGet(Supplier<T> other)` (ліниве надання значення):**
>     Те саме, але значення за замовчуванням обчислюється лише тоді, коли `Optional` порожній.
>
>     ```java
>     String name = result.orElseGet(() -> getDefaultName());
>     ```
>
> * **`orElseThrow(Supplier<? extends X> exceptionSupplier)`:**
>     Кидає виняток, якщо значення відсутнє.
>
>     ```java
>     String name = result.orElseThrow(() -> new NotFoundException());
>     ```

#### **📌 Що перевіряють:**

*Знання API класу `Optional` та вміння писати безпечний, `null-safe` код.*

* * *

### **5 ❓ Питання:**

Що таке операція `anyMatch()` і чим вона відрізняється від `findAny()`?

> #### **💬 Відповідь:**
>
> `anyMatch()` — це також термінальна операція з коротким замиканням, але вона вирішує іншу задачу.
>
> * **`anyMatch(Predicate<T> predicate)`:**
>   * **Призначення:** **Перевірити**, чи відповідає **хоча б один** елемент потоку заданій умові.
>   * **Що повертає:** **`boolean`**.
>
> **Відмінність від `findAny()`:**
>
> | **Характеристика** | **`findAny()`** | **`anyMatch()`** |
> | :---: | :---: | :---: |
> | **Мета** | **Знайти** елемент | **Перевірити** наявність |
> | **Тип результату**| `Optional<T>` | `boolean` |
> | **Що робить** | Повертає сам елемент | Повертає `true` або `false` |
>
> **Коротко:** `findAny()` відповідає на питання "дай мені елемент", а `anyMatch()` — на питання "чи є такий елемент?".

#### **📝 Приклад:**

```java
List<Integer> numbers = List.of(1, 3, 5, 8, 10);

// Чи є хоча б одне парне число?
boolean hasEven = numbers.stream().anyMatch(n -> n % 2 == 0); // true

// Знайдемо будь-яке парне число
Optional<Integer> anyEven = numbers.stream().filter(n -> n % 2 == 0).findAny(); // Optional[8]
```

#### **📌 Що перевіряють:**

*Розуміння семантичної різниці між операціями пошуку та перевірки. Кандидат повинен знати, що для булевих перевірок існують спеціалізовані та більш ефективні методи.*

* * *

### **6 ❓ Питання:**

Назвіть ще дві схожі на `anyMatch()` операції.

> #### **💬 Відповідь:**
>
> Крім `anyMatch()`, існують ще дві термінальні операції з коротким замиканням для перевірки умов:
>
> * **`allMatch(Predicate<T> predicate)`:**
>   * Перевіряє, чи **всі** елементи потоку відповідають заданій умові.
>   * Зупиниться і поверне `false`, як тільки знайде **перший** елемент, що **не** відповідає умові.
>
> * **`noneMatch(Predicate<T> predicate)`:**
>   * Перевіряє, чи **жоден** з елементів потоку **не** відповідає умові.
>   * Зупиниться і поверне `false`, як тільки знайде **перший** елемент, що **відповідає** умові.
>
> Ці три методи (`anyMatch`, `allMatch`, `noneMatch`) складають повний набір для виконання логічних перевірок над потоками.

#### **📌 Що перевіряють:**

*Знання повного спектру операцій для перевірки умов. Це базовий функціонал `Stream API`.*

* * *

### **7 ❓ Питання:**

Як `findFirst()` та `anyMatch()` працюють з нескінченними потоками? Наведіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> Обидві ці операції є `short-circuiting`, що робить їх ідеальними для роботи з нескінченними потоками. Вони дозволяють отримати результат, не входячи в нескінченний цикл.
>
> **Приклад:**
> Знайти перше число, більше 100, яке ділиться на 7 без остачі.
>
> ```java
> Optional<Integer> result = IntStream.iterate(100, n -> n + 1) // Нескінченний потік з 100, 101, 102...
>                                    .filter(n -> n % 7 == 0)
>                                    .findFirst(); // Зупиниться, як тільки знайде
> 
> result.ifPresent(System.out::println); // 105
> ```
>
> `findFirst()` тут змушує потік генерувати та фільтрувати числа одне за одним, доки не буде знайдено перше, що задовольняє умові (`105`). Після цього обробка негайно припиняється.
>
> Так само спрацював би і `anyMatch(n -> n % 7 == 0)`.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як `short-circuiting` операції уможливлюють роботу з нескінченними послідовностями.*

* * *

### **8 ❓ Питання:**

Чи можна отримати `NullPointerException` при роботі з `findFirst()`? Якщо так, то як? ⭐️

> #### **💬 Відповідь:**
>
> `NullPointerException` може виникнути, але **не від самого `findFirst()`**, а від подальшої неправильної обробки його результату.
>
> `findFirst()` повертає `Optional`. Якщо потік порожній, `Optional` буде порожнім. Проблема виникне, якщо ви **без перевірки** викличете на ньому метод **`.get()`**.
>
> ```java
> Stream<String> emptyStream = Stream.empty();
>
> Optional<String> result = emptyStream.findFirst(); // result - це Optional.empty
>
> String value = result.get(); // Кине NoSuchElementException, не NPE. 
> ```
>
> **Оновлена відповідь**: Раніше була поширена помилка думати, що буде `NPE`. Насправді, `Optional.get()` на порожньому `Optional` кидає `NoSuchElementException`.
>
> **NPE може виникнути опосередковано:**
>
> ```java
> String name = stream.findFirst().orElse(null);
> System.out.println(name.length()); // Ось тут буде NPE, якщо Optional був порожнім.
> ```
>
> Тому завжди потрібно безпечно обробляти результат `Optional`.
>

#### **📌 Що перевіряють:**

*Уважність та знання API `Optional`. Кандидат повинен знати, що `Optional` захищає від `NPE` лише за умови його правильного використання.*

* * *

### **9 ❓ Питання:**

Якщо у вас є потік з `null` елементами, що поверне `findFirst()`? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від того, чи є `null` першим елементом. `findFirst()` просто поверне перший елемент, яким би він не був.
>
> Якщо потік виглядає так: `Stream.of(null, "a", "b")`
>
> Тоді `findFirst()` поверне **`Optional.ofNullable(null)`**, що еквівалентно **`Optional.empty()`**.
>
> Якщо потік: `Stream.of("a", null, "b")`
>
> Тоді `findFirst()` поверне **`Optional.of("a")`**.
>
> **Важливий нюанс з `Optional.of` та `Optional.ofNullable`:**
>
> * `Optional.of(value)`: вимагає, щоб `value` не було `null`, інакше кине `NPE`.
> * `Optional.ofNullable(value)`: безпечно створює `Optional`. Якщо `value` є `null`, поверне `Optional.empty()`.
>
> `findFirst()` працює подібно до `ofNullable`: якщо перший елемент потоку — це `null`, результат буде порожнім `Optional`.

#### **📌 Що перевіряють:**

*Знання того, як операції `Stream` та `Optional` обробляють `null` значення.*

* * *

### **10 ❓ Питання:**

Чому `anyMatch` може бути продуктивнішим за `count() > 0` для перевірки, чи не порожній потік після фільтрації? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання на розуміння `short-circuiting`.
>
> * **`.anyMatch(predicate)`:**
>   * Є операцією з **коротким замиканням**.
>   * Вона припинить обробку потоку, як тільки знайде **перший** елемент, що відповідає умові.
>
> * **`.filter(predicate).count() > 0`:**
>   * Тут **`count()`** є **повною термінальною операцією**.
>   * Вона змушена буде **пройти по всьому потоку** і перебрати всі мільйони елементів, щоб порахувати їх, навіть якщо перший же елемент задовольняв умові.
>
> **Приклад:**
> У вас є мільярд чисел, і ви хочете перевірити, чи є серед них хоча б одне парне.
>
> * `.anyMatch(n -> n % 2 == 0)` знайде перше парне число (напр., "2") і миттєво поверне `true`.
> * `.filter(n -> n % 2 == 0).count() > 0` буде перевіряти всі мільярд чисел, щоб порахувати їх, що буде вкрай повільно.
>
> **Висновок:** Для простих перевірок наявності завжди слід використовувати `anyMatch`, `allMatch` або `noneMatch` замість `count`.

#### **📌 Що перевіряють:**

*Здатність обирати найефективнішу операцію для вирішення задачі та розуміння наслідків для продуктивності.*

* * *

### **11 ❓ Питання:**

Як `findAny()` поводиться в послідовному (`sequential`) потоці? Чи можна розраховувати, що він поверне перший елемент? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, у більшості реалізацій він поверне перший елемент.**
>
> Документація `findAny()` говорить:
> > "The behavior of this operation is explicitly non-deterministic; it is free to select any element in the stream."
> > ("Поведінка цієї операції є явно недетермінованою; вона вільна обрати будь-який елемент у потоці.")
>
> Однак, для **послідовних потоків** немає жодних переваг у тому, щоб шукати якийсь інший елемент, окрім першого. Тому існуючі реалізації JDK просто повертають перший елемент, так само як і `findFirst()`.
>
> Але **важливо не покладатися** на цю поведінку. Контракт API цього не гарантує. Якщо вам **гарантовано** потрібен саме перший елемент, ви повинні використовувати `findFirst()`. Використовувати `findAny()` і розраховувати на те, що він поверне перший елемент — це покладатися на деталь реалізації, яка може змінитися в майбутніх версіях Java.

#### **📌 Що перевіряють:**

*Розуміння різниці між контрактом API та конкретною реалізацією. Хороший розробник пише код, що спирається на контракт.*

* * *

### **12 ❓ Питання:**

Покажіть на прикладі, як методи `findFirst` та `anyMatch` можуть працювати разом з `Optional.filter`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Методи `find...` повертають `Optional`. `Optional` сам має метод `filter`, який дозволяє робити додаткові перевірки вже на знайденому результаті. Це створює елегантний ланцюжок.
>
> **Сценарій:**
> Знайти першого користувача, чиє ім'я починається на "A", і, **додатково**, перевірити, що він є повнолітнім.
>
> ```java
> Optional<User> result = users.stream()
>     .filter(u -> u.getName().startsWith("A")) // Фільтруємо потік
>     .findFirst()                                // Знаходимо першого -> Optional<User>
>     .filter(u -> u.getAge() >= 18);          // Фільтруємо сам Optional
> ```
>
> **Як це працює:**
>
> 1. `findFirst()` повертає `Optional<User>`.
> 2. `Optional.filter()` приймає `Predicate`. Якщо `Optional` порожній, нічого не відбувається.
> 3. Якщо `Optional` містить `User`, до нього застосовується предикат `u -> u.getAge() >= 18`.
> 4. Якщо предикат повертає `true`, `Optional.filter()` повертає той самий `Optional<User>`.
> 5. Якщо `false` — `Optional.filter()` повертає `Optional.empty()`.
>
> Це більш декларативний спосіб, ніж писати `if (optional.isPresent() && optional.get()...)`

#### **📌 Що перевіряють:**

*Знання сучасного API `Optional` та здатність писати чистий, функціональний код, уникаючи `if`-перевірок.*

* * *

### **13 ❓ Питання:**

У чому може бути перевага використання `stream().findAny().isPresent()` над `stream().anyMatch()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом. Практично у **жодному** випадку.
>
> **`stream().anyMatch(predicate)`** буде завжди **кращим** за `stream().filter(predicate).findAny().isPresent()`.
>
> **Причина:**
>
> * `anyMatch` — це одна термінальна операція, спеціально розроблена для цієї задачі. Її реалізація оптимізована.
> * Ланцюжок `filter().findAny().isPresent()` є значно більш громіздким з точки зору реалізації. Він вимагає:
>     1. Створення проміжного потоку після `filter`.
>     2. Створення об'єкта `Optional` після `findAny`.
>     3. Виклику методу `.isPresent()` на цьому `Optional`.
>
> Хоча JIT-компілятор може оптимізувати другий варіант, перший є більш **ідіоматичним, читабельним та семантично правильним**. Він чітко говорить про намір "перевірити, чи існує хоч один", тоді як другий є обхідним шляхом "знайти будь-який, а потім перевірити, чи ми щось знайшли".
>
> Єдиний гіпотетичний випадок, коли це могло б мати сенс, — якщо вам спочатку потрібно знайти елемент, а потім, залежно від іншої логіки, лише перевірити його наявність. Але це дуже надуманий сценарій.

#### **📌 Що перевіряють:**

*Здатність обирати найбільш прямий та ідіоматичний спосіб вирішення задачі. Кандидат не повинен писати складний код, коли існує простий аналог.*

* * *

### **14 ❓ Питання:**

Ви маєте потік, в якому ви точно знаєте, що є лише один елемент. Як його отримати, і чи є `findFirst()` найкращим вибором? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `findFirst()` є хорошим і безпечним вибором, але він поверне `Optional`. Якщо ви **абсолютно впевнені**, що елемент один, і хочете уникнути роботи з `Optional`, можна використати інші підходи.
>
> **1. `findFirst().get()` (Просто, але ризиковано):**
>
> * Найпростіший варіант. Але якщо ваше припущення хибне і потік порожній, ви отримаєте `NoSuchElementException`.
>
> **2. `limit(1).collect(Collectors.toList()).get(0)` (Громіздко):**
>
> * Дуже поганий варіант.
>
> **3. `reduce((a, b) -> { throw new IllegalStateException("Більше одного елемента!"); })` (Надійно):**
>
> * Це найбільш надійний, хоч і багатослівний, спосіб. `reduce` з одним аргументом повертає `Optional`.
> * Ви можете використати `reduce`, щоб переконатися, що елемент справді один. Якщо елементів буде більше, функція-акумулятор кине виняток.
>
> ```java
> Optional<T> singleElement = stream.reduce((a, b) -> {
>     throw new IllegalStateException("Потік містить більше одного елемента");
> });
>    ```
>
> **4. Використання сторонніх бібліотек (найкраща практика):**
>
> * Бібліотеки, як-от Google Guava (`MoreCollectors.onlyElement()`) або jOOL (`Seq.single()`), надають готові методи, які роблять саме це: повертають єдиний елемент або кидають виняток, якщо їх більше одного або нуль.
>
> **Висновок:** Для простоти зазвичай використовують `findFirst().get()`, покладаючись на контракт. Для максимальної надійності — `reduce` або сторонні бібліотеки.

#### **📌 Що перевіряють:**

*Вміння працювати з неявними контрактами та обирати між простотою, безпекою та використанням сторонніх бібліотек.*

* * *

### **15 ❓ Питання:**

Як методи пошуку взаємодіють з операцією `sorted()`? Чи є різниця у продуктивності? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `sorted()` — це **`stateful` проміжна операція**. Це означає, що вона повинна **побачити всі елементи** потоку, щоб їх відсортувати.
>
> **Як це взаємодіє:**
>
> 1. Навіть якщо ви використовуєте `short-circuiting` операцію, як `findFirst()` після `sorted()`, це не допоможе.
> 2. `sorted()` **зламає "лінивий" потік** по елементах. Вона буде змушена спочатку "витягнути" всі елементи з попередніх операцій, відсортувати їх у внутрішньому буфері, і лише потім віддати перший елемент `findFirst()`.
>
> **Різниця у продуктивності:**
>
> Ланцюжок, що містить `sorted()`, буде **значно повільнішим**, ніж ланцюжок без нього, навіть якщо ви шукаєте лише перший елемент. Сортування всього потоку — це дорога операція (зазвичай `O(n log n)`).
>
> **Приклад:**
>
> ```java
> // ДУЖЕ ПОВІЛЬНО, хоча є findFirst()
> Optional<Integer> result = veryLargeList.stream()
>                                          .sorted()
>                                          .findFirst();
> ```
>
> Тут будуть відсортовані **всі** елементи `veryLargeList`, перш ніж `findFirst` отримає перший.
>
> На відміну від `min()` чи `max()`, які також є термінальними, але не вимагають повного сортування, а можуть знайти екстремум за один прохід `O(n)`.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як `stateful` операції впливають на ліниву природу стрімів і, відповідно, на їхню продуктивність. Це важливий аспект для написання ефективного коду.*

* * *
