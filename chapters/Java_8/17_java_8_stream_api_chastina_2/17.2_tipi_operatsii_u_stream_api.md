---
layout: default
title: "Типи операцій у Stream API"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 2
---

# Типи операцій у Stream API

* короткий опис: Операції у Stream API поділяються на проміжні (`intermediate`) та кінцеві (`terminal`). Проміжні трансформують стрім, повертаючи новий стрім. Кінцеві запускають обчислення та повертають результат.

* поняття: Проміжні операції (`filter`, `map`, `sorted`, `distinct`, `peek`, `limit`, `skip`), Кінцеві операції (`collect`, `forEach`, `reduce`, `count`, `anyMatch`, `findFirst`).

* приклади: `names.stream().filter(...).map(...).collect(...)`.

* ключові моменти: Проміжні операції є лінивими. Обчислення починається лише з виклику термінальної операції.

* що важливо знати на співбесіді: Чим відрізняються проміжні та кінцеві операції.

### **✅ Офіційне визначення:**

У **`Stream`** **`API`** всі операції поділяються на два типи: **проміжні (intermediate)** та **кінцеві (terminal)**. Проміжні операції трансформують стрім і повертають новий стрім, а кінцеві — запускають обчислення та повертають результат.

### **🧠 Простими словами:**

**`Stream`** в **`Java`** працює за принципом "побудуй ланцюжок — натисни пуск". Проміжні операції додають кроки до ланцюга, а кінцева операція — запускає всю обробку і повертає результат (а не стрім\!)

---

## **Основні види**

📊 **Проміжні операції:**

* **`filter(Predicate)`**
* **`map(Function)`**
* **`flatMap(Function)`**
* **`sorted(), sorted(Comparator)`**
* **`distinct()`**
* **`peek(Consumer)`**
* **`limit(long), skip(long)`**

📊 **Кінцеві операції:**

* **`collect(Collector)`**
* **`forEach(Consumer)`**
* **`reduce(BinaryOperator)`**
* **`count()`**
* **`anyMatch(), allMatch(), noneMatch()`**
* **`findFirst(), findAny()`**
* **`min(Comparator), max(Comparator)`**

---

## **Пояснення на пальцях (💡)**

**Уяви, що ти замовляєш бургер у ресторані:**

* Проміжні операції \= інгредієнти, які ти додаєш: фільтруєш тільки вегетаріанські котлети, змінюєш булку, додаєш соус.
* Кінцева операція \= момент, коли шеф готує бургер і подає тобі в руки 🍔. До цього все було лише рецептом, а не їжею.

---

## **Корисні приклади (🧪)**

```java
List<String> names = List.of("Anna", "Bob", "Amanda", "Alex");

List<String> result = names.stream()                   // Джерело
    .filter(n -> n.startsWith("A"))                    // Проміжна
    .map(String::toUpperCase)                          // Проміжна
    .sorted()                                          // Проміжна
    .collect(Collectors.toList());                     // Кінцева

System.out.println(result); // [ALEX, AMANDA, ANNA]
```

---

## **Порівняння (⚖️)**

|    Операція     |   Тип    |   Повертає    | Ледача |              Приклад               |
|:---------------:|:--------:|:-------------:|:------:|:----------------------------------:|
|   **`map()`**   | Проміжна |  **Stream**   |   ✅    |       **`map(x -> x * 2)`**        |
| **`filter()`**  | Проміжна |  **Stream**   |   ✅    |      **`filter(x -> x > 5)`**      |
| **`sorted()`**  | Проміжна |  **Stream**   |   ✅    |           **`sorted()`**           |
| **`collect()`** | Кінцева  | **Результат** |   ❌    | **`collect(Collectors.toList())`** |
| **`forEach()`** | Кінцева  |   **void**    |   ❌    | **`forEach(System.out::println)`** |
|  **`count()`**  | Кінцева  |   **long**    |   ❌    |           **`count()`**            |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"P --> P --> ... --> K"**  
(*Проміжна --> Проміжна --> ... --> Кінцева*)

Або придумай свою історію:  
👉 *"Петро Пробував Пекти, але Котлета не з'явилась без Кінцевої операції\!"*

---

## **🟩 Підсумок:**

* **`Stream`** **`API`** поділяє операції на проміжні та кінцеві
* **`Проміжні`** — будують pipeline, повертають новий стрім
* **`Кінцева`** — запускає обробку і повертає результат
* Вся магія відбувається лише після термінальної операції
* Розуміння цієї різниці — ключ до ефективної роботи з потоками

---

### **🔗 Додаткові джерела:**

* [**Java Streams Guide — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/package-summary.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке проміжні (intermediate) та кінцеві (термінальні) операції в Stream API?

> #### **💬 Відповідь:**
>
> Усі операції в `Stream API` поділяються на дві категорії, які визначають їхню роль у побудові та виконанні ланцюжка обробки даних (pipeline).
>
> 1. **Проміжні операції (Intermediate Operations):**
>     * **Що роблять:** Це операції, які приймають потік і **повертають новий потік**. Вони є будівельними блоками для ланцюжка обробки.
>     * **Характеристика:** Вони завжди є **лінивими (lazy)**. Це означає, що вони не виконуються в момент їх виклику, а лише описують, як потрібно трансформувати потік.
>     * **Приклади:** `filter()`, `map()`, `sorted()`, `distinct()`, `limit()`.
>
> 2. **Кінцеві (Термінальні) операції (Terminal Operations):**
>     * **Що роблять:** Це операції, які **запускають обробку** потоку і **завершують** його.
>     * **Характеристика:** Вони "гарячі" (eager) і ініціюють виконання всіх попередніх проміжних операцій. Після виклику термінальної операції потік вважається "спожитим" і не може бути перевикористаний.
>     * **Приклади:** `collect()`, `forEach()`, `reduce()`, `count()`, `findFirst()`.

#### **📝 Аналогія:**

Уявіть собі конвеєр на заводі:

* **Проміжні операції** — це як налаштування різних станків на конвеєрі: станція для фарбування (`map`), станція для відбраковки (`filter`), станція для сортування (`sorted`). Самі по собі вони не працюють.
* **Термінальна операція** — це як натискання кнопки "Пуск" (`collect`), яке запускає весь конвеєр.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння того, як будується і виконується ланцюжок операцій в `Stream API`. Це найбазовіше питання по цій темі.*

* * *

### **2 ❓ Питання:**

Поясніть, що таке `map()` і `filter()` і які функціональні інтерфейси вони приймають.

> #### **💬 Відповідь:**
>
> `map()` і `filter()` — це дві найчастіше використовувані проміжні операції.
>
> * **`filter(Predicate<T> predicate)`:**
>   * **Призначення:** **Фільтрація**. Вона залишає в потоці лише ті елементи, які відповідають певній умові.
>   * **Що приймає:** **`Predicate<T>`** — функціональний інтерфейс, метод якого `test(T t)` повертає `boolean`. Якщо `true`, елемент залишається; якщо `false` — відкидається.
>   * **Приклад:** `stream.filter(s -> s.startsWith("A"))`
>
> * **`map(Function<T, R> mapper)`:**
>   * **Призначення:** **Перетворення**. Вона перетворює кожен елемент потоку на інший елемент.
>   * **Що приймає:** **`Function<T, R>`** — функціональний інтерфейс, метод якого `apply(T t)` приймає елемент типу `T` і повертає елемент типу `R`.
>   * **Приклад:** `stream.map(String::length)`

#### **📝 Коментар:**

Мнемоніка:

* `filter` — це сито (залишає те, що підходить).
* `map` — це мапа/карта (трансформує одну місцевість в іншу).

#### **📌 Що перевіряють:**

*Знання двох основних проміжних операцій та відповідних їм функціональних інтерфейсів. Це базові "будівельні блоки" для роботи зі стрімами.*

* * *

### **3 ❓ Питання:**

Чи можна викликати кілька проміжних операцій і кілька термінальних операцій в одному ланцюжку?

> #### **💬 Відповідь:**
>
> * **Проміжні операції:** Так, можна викликати **скільки завгодно** проміжних операцій. Кожна з них повертає новий потік, до якого можна застосувати наступну операцію. Саме так будуються складні ланцюжки обробки.
>
> * **Термінальні операції:** Ні, можна викликати **лише одну**. Як тільки викликається термінальна операція, потік "закривається" і стає "спожитим". Будь-яка спроба виконати ще одну операцію на тому ж потоці призведе до `IllegalStateException`.
>
> Отже, будь-який валідний ланцюжок стріму має таку структуру:
> **`(Джерело) -> (0+ Проміжних операцій) -> (1 Термінальна операція)`**

#### **📝 Приклад:**

```java
Stream<String> stream = List.of("a", "b", "c").stream();
long count = stream.count(); // Термінальна операція. Потік закрито.

// ПОМИЛКА КОМПІЛЯЦІЇ (або IllegalStateException):
// stream.forEach(System.out::println);
```

#### **📌 Що перевіряють:**

*Розуміння того, що потік є одноразовим. Це фундаментальне правило роботи зі `Stream API`.*

* * *

### **4 ❓ Питання:**

Назвіть найпоширенішу термінальну операцію і поясніть її призначення.

> #### **💬 Відповідь:**
>
> Найпоширенішою термінальною операцією є **`collect(Collector collector)`**.
>
> **Призначення:** Вона служить для збору елементів потоку в якусь кінцеву структуру даних, найчастіше — в **колекцію** (`List`, `Set`) або **мапу** (`Map`).
>
> Операція `collect` є дуже гнучкою і використовує спеціальні об'єкти-збирачі (`Collector`), які визначають, як саме потрібно збирати елементи. Утилітарний клас **`java.util.stream.Collectors`** надає багато готових реалізацій колекторів.
>
> **Найпопулярніші колектори:**
>
> * `Collectors.toList()`: збирає елементи в `List`.
> * `Collectors.toSet()`: збирає елементи в `Set`.
> * `Collectors.toMap(keyMapper, valueMapper)`: збирає елементи в `Map`.
> * `Collectors.joining(delimiter)`: об'єднує елементи-рядки в один рядок.
> * `Collectors.groupingBy(classifier)`: групує елементи в `Map`.

#### **📝 Приклад:**

```java
List<String> words = List.of("a", "b", "c");
List<String> upperCaseList = words.stream()
                                  .map(String::toUpperCase)
                                  .collect(Collectors.toList()); // Збираємо результат у новий List
```

#### **📌 Що перевіряють:**

*Знання найважливішої термінальної операції та її основного застосування. Практично кожен складний стрім завершується викликом `collect`.*

* * *

### **5 ❓ Питання:**

Для чого потрібна проміжна операція `peek(Consumer<T> action)`? Чим вона небезпечна?

> #### **💬 Відповідь:**
>
> **`peek()`** — це спеціальна проміжна операція, призначена в основному для **налагодження (debugging)** ланцюжків стрімів.
>
> **Що робить:** Вона приймає `Consumer` і виконує його для кожного елемента потоку, який проходить через цю операцію. При цьому сама операція **не змінює елементи** і повертає потік далі. Це дозволяє "підглянути" за станом елементів у певній точці конвеєра.
>
> **Чим небезпечна:**
>
> Головна небезпека полягає в тому, що `peek` можуть спробувати використати для **зміни стану елементів (побічних ефектів)**. Це є **антипатерном**, оскільки:
>
> * **Порушує принципи ФП:** Проміжні операції мають бути чистими.
> * **Ненадійно:** Через лінивість та можливі оптимізації немає гарантії, що `peek` буде викликано для всіх елементів, якщо термінальна операція цього не вимагає (напр., `findFirst`).
>
> **Правило:** Використовуйте `peek()` лише для виводу в лог або для налагодження. Для зміни елементів використовуйте `map()`.

#### **📝 Приклад (правильне використання):**

```java
List<String> result = List.of("one", "two", "three")
    .stream()
    .map(String::toUpperCase)
    .peek(s -> System.out.println("After map: " + s)) // Підглядаємо
    .filter(s -> s.length() > 3)
    .collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Розуміння призначення специфічних операцій та пов'язаних з ними антипатернів. `peek` — класичний приклад інструменту, який легко використати неправильно.*

* * *

### **6 ❓ Питання:**

Що таке stateless та stateful операції в Stream API? Наведіть приклади.

> #### **💬 Відповідь:**
>
> Проміжні операції можна поділити на ті, що не зберігають стан, і ті, що його накопичують.
>
> * **Stateless Operations (Операції без стану):**
>   * **Опис:** Це операції, які обробляють кожен елемент **незалежно від інших**. Їм не потрібна інформація про попередні або наступні елементи потоку.
>   * **Характеристика:** Дуже добре підходять для паралельної обробки, оскільки не потребують координації.
>   * **Приклади:** `filter()`, `map()`.
>
> * **Stateful Operations (Операції зі станом):**
>   * **Опис:** Це операції, яким для обробки елементів потрібно **накопичувати стан** або інформацію про частину або весь потік.
>   * **Характеристика:** Можуть обмежувати можливості паралелізму. Деякі з них вимагають буферизації всіх елементів перед тим, як повернути результат.
>   * **Приклади:**
>     * **`distinct()`**: Повинен зберігати сет унікальних елементів, які він вже бачив.
>     * **`sorted()`**: Повинен "побачити" всі елементи, щоб їх відсортувати.
>     * **`limit()`, `skip()`**: Повинні рахувати, скільки елементів вони вже пропустили або взяли.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють різні проміжні операції та як вони можуть впливати на продуктивність, особливо в паралельних потоках.*

* * *

### **7 ❓ Питання:**

Порівняйте `limit()` та `skip()`.

> #### **💬 Відповідь:**
>
> `limit()` та `skip()` — це дві stateful проміжні операції, які дозволяють працювати з частиною потоку.
>
> * **`limit(long maxSize)`:**
>   * **Що робить:** **Обмежує** потік, повертаючи новий потік, що містить не більше, ніж `maxSize` перших елементів.
>   * Є **short-circuiting** операцією: як тільки буде досягнуто ліміту, подальша обробка початкового потоку припиниться.
>
> * **`skip(long n)`:**
>   * **Що робить:** **Пропускає** перші `n` елементів потоку, повертаючи новий потік з решти елементів.
>
> Обидві операції корисні для пагінації даних.

#### **📝 Приклад:**

```java
IntStream.range(1, 10) // [1, 2, 3, 4, 5, 6, 7, 8, 9]
         .skip(3)     // [4, 5, 6, 7, 8, 9]
         .limit(4)    // [4, 5, 6, 7]
         .forEach(System.out::print); // Виведе: 4567
```

#### **📌 Що перевіряють:**

*Знання операцій для роботи з підмножинами потоків та їхньої взаємодії.*

* * *

### **8 ❓ Питання:**

Чим відрізняються термінальні операції `findFirst()` та `findAny()`? В якому випадку одна є кращою за іншу? ⭐️

> #### **💬 Відповідь:**
>
> Обидві операції є **short-circuiting** і повертають `Optional` з одним елементом потоку, але мають різну семантику, особливо в паралельних потоках.
>
> * **`findFirst()`:**
>   * **Що робить:** **Гарантовано** повертає **перший** елемент потоку, відповідно до його порядку.
>   * **Поведінка в паралельному потоці:** Може бути менш продуктивним, оскільки система змушена координувати потоки, щоб визначити, який елемент справді є першим.
>
> * **`findAny()`:**
>   * **Що робить:** Повертає **будь-який** елемент потоку. У послідовному потоці це зазвичай буде перший елемент, але це не гарантовано.
>   * **Поведінка в паралельному потоці:** **Набагато продуктивніший**. Він повертає перший-ліпший елемент, який будь-який з потоків встиг обробити.
>
> **Коли що використовувати:**
>
> * Якщо вам **важливий порядок** і потрібен саме перший елемент — використовуйте `findFirst()`.
> * Якщо вам **неважливо, який** саме елемент (аби він відповідав умовам), особливо **в паралельних потоках**, завжди віддавайте перевагу `findAny()` для кращої продуктивності.

#### **📌 Що перевіряють:**

*Розуміння нюансів паралельної обробки та здатність обирати правильний інструмент для оптимізації продуктивності.*

* * *

### **9 ❓ Питання:**

Поясніть, що таке операція `reduce()` і наведіть приклад її використання. ⭐️

> #### **💬 Відповідь:**
>
> **`reduce()`** — це потужна термінальна операція, яка виконує "згортання" (редукцію) всіх елементів потоку до **одного єдиного значення**.
>
> Вона приймає два основних компоненти:
>
> 1. **Початкове значення (identity):** Значення, з якого починається операція, і яке повертається, якщо потік порожній.
> 2. **Акумулятор (accumulator):** `BinaryOperator`, який приймає два аргументи: `(проміжний_результат, наступний_елемент)` і повертає новий проміжний результат.
>
> `reduce()` послідовно застосовує акумулятор до всіх елементів, накопичуючи результат.
>
> **Поширені сценарії:**
>
> * Знаходження суми, добутку, min/max значення.
> * Об'єднання рядків в один.

#### **📝 Приклад (пошук найдовшого рядка):**

```java
List<String> words = List.of("java", "is", "awesome");

// Початкове значення може бути null або пустий рядок, але тут використано
// перевантажену версію, що повертає Optional.
Optional<String> longestWord = words.stream()
    .reduce((word1, word2) -> 
        word1.length() > word2.length() ? word1 : word2);
    
longestWord.ifPresent(System.out::println); // awesome
```

#### **📌 Що перевіряють:**

*Знання однієї з фундаментальних операцій ФП. `reduce` є аналогом `fold` в інших функціональних мовах і є дуже важливим для агрегації даних.*

* * *

### **10 ❓ Питання:**

Чому `forEach` є термінальною операцією, а `peek` — проміжною, хоча обидві приймають `Consumer`? ⭐️

> #### **💬 Відповідь:**
>
> Хоча обидві операції приймають `Consumer` для виконання дії з побічним ефектом, вони мають різну мету та різний тип повернутого значення, що і визначає їхню категорію.
>
> * **`peek(Consumer<T> action)`:**
>   * **Що повертає:** `Stream<T>`.
>   * **Призначення:** "Підглянути" за елементами в середині конвеєра **без його завершення**. Оскільки він повертає потік, ланцюжок операцій може бути продовжено. Це робить його **проміжною** операцією.
>
> * **`forEach(Consumer<T> action)`:**
>   * **Що повертає:** `void`.
>   * **Призначення:** Виконати фінальну дію для кожного елемента **після завершення** всіх проміжних операцій. Оскільки він нічого не повертає, ланцюжок операцій на цьому завершується. Це робить його **термінальною** операцією.
>
> По суті, все визначається тим, чи повертає операція новий `Stream` чи ні.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що визначає операцію як проміжну або термінальну — її повернуте значення.*

* * *

### **11 ❓ Питання:**

Які переваги використання `flatMap` перед кількома `map` та `filter`? Наведіть сценарій. ⭐️

> #### **💬 Відповідь:**
>
> `flatMap` використовується для вирішення специфічної задачі: **перетворення кожного елемента в потік інших елементів і подальшого "сплющення" цих потоків в один**. Цього не можна досягти за допомогою `map`.
>
> **Сценарій:** У нас є список студентів, і у кожного студента є список курсів. Ми хочемо отримати єдиний список усіх унікальних курсів, на які записані всі студенти.
>
> **Поганий підхід (з `map`):**
>
> ```java
> studentList.stream()
>     .map(Student::getCourses) // Поверне Stream<List<String>> - потік списків!
> ```
>
> Подальша обробка такого "потоку потоків" є складною.
>
> **Правильний підхід (з `flatMap`):**
>
> ```java
> List<String> uniqueCourses = studentList.stream()
>     .flatMap(student -> student.getCourses().stream()) // Перетворює Stream<List<String>> на Stream<String>
>     .distinct()
>     .collect(Collectors.toList());
> ```
>
> `flatMap` "розгортає" список курсів кожного студента і об'єднує їх в один великий потік рядків, з яким вже легко працювати.

#### **📌 Що перевіряють:**

*Розуміння практичного застосування `flatMap`. Це питання-маркер для перевірки, чи стикався кандидат з реальними задачами обробки вкладених структур.*

* * *

### **12 ❓ Питання:**

Як порядок операцій `filter` та `map` може вплинути на продуктивність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Порядок цих операцій має **критичне значення** для продуктивності.
>
> **Золоте правило:** **Завжди фільтруйте якомога раніше (`filter` перед `map`).**
>
> **Пояснення:**
>
> * **Правильний порядок (`filter` -> `map`):**
>     1. Спочатку ви **відкидаєте непотрібні елементи**, значно зменшуючи розмір потоку.
>     2. Потім ви виконуєте "дорогу" операцію `map` лише на **невеликій кількості елементів**, що залишилися.
>
> * **Неправильний порядок (`map` -> `filter`):**
>     1. Спочатку ви виконуєте операцію `map` на **всіх елементах** початкового потоку, витрачаючи ресурси на перетворення, можливо, тисяч елементів.
>     2. І лише потім ви **відкидаєте більшість** з цих щойно перетворених елементів на етапі `filter`. Це марна робота.

#### **📝 Приклад:**

```java
// ДОБРЕ
stream.filter(user -> user.isActive()) // спочатку зменшили кількість
      .map(user -> callExpensiveApi(user)); // дорогий виклик для небагатьох

// ПОГАНО
stream.map(user -> callExpensiveApi(user)) // дорогий виклик для всіх
      .filter(result -> result != null); // а потім викинули результати
```

#### **📌 Що перевіряють:**

*Здатність писати ефективний код зі `Stream API` та мислити про оптимізацію конвеєра обробки даних.*

* * *

### **13 ❓ Питання:**

Чи можна створити паралельний стрім з `List`, а потім повернутися до послідовного? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** `Stream API` надає методи для перемикання між паралельним та послідовним режимами обробки.
>
> * **`.parallel()`**: Це **проміжна** операція, яка перетворює будь-який потік (навіть той, що вже є паралельним) на паралельний.
> * **`.sequential()`**: Це **проміжна** операція, яка перетворює будь-який потік (навіть той, що вже є послідовним) на послідовний.
>
> **Коли це корисно?**
> Це може бути корисно для оптимізації. Наприклад, у вас є великий ланцюжок операцій. Частину з них (напр., `map`, що робить складні обчислення) вигідно виконати паралельно, але іншу частину (напр., `forEach` для запису в одне місце, де важливий порядок) — послідовно.
>
> ```java
> list.stream()
>     .parallel() // Переходимо в паралельний режим для дорогої операції
>     .map(this::expensiveTransform)
>     .sequential() // Повертаємося в послідовний режим
>     .forEachOrdered(this::saveResult); // для операції, що вимагає порядку
> ```
>
> Це надає дуже гнучкий контроль над виконанням.

#### **📌 Що перевіряють:**

*Глибокі знання `Stream API` та інструментів для тонкого налаштування продуктивності.*

* * *

### **14 ❓ Питання:**

Чому `forEach` не гарантує порядок виконання в паралельних потоках, а `forEachOrdered` — гарантує? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з компромісом між **продуктивністю** та **передбачуваністю**.
>
> * **`forEach`:**
>   * **У паралельному потоці:** Його головна мета — максимальна швидкість. Тому кожен потік виконує дію над своєю частиною даних **незалежно** і в довільному порядку. Немає ніякої синхронізації, що робить його дуже швидким.
>
> * **`forEachOrdered`:**
>   * **У паралельному потоці:** Його головна мета — **зберегти початковий порядок** елементів (encounter order). Для цього фреймворку доводиться докладати додаткових зусиль для **синхронізації** потоків, щоб переконатися, що дія для першого елемента буде виконана перед дією для другого, і так далі.
>   * Ця синхронізація значно **знижує продуктивність** і може нівелювати всі переваги паралельної обробки.
>
> **Висновок:**
> Використовуйте `forEach` у паралельних потоках, якщо порядок не має значення. Якщо порядок важливий — використовуйте `forEachOrdered`, але будьте готові до втрати продуктивності (або краще взагалі подумайте, чи потрібен тут `parallelStream`).

#### **📌 Що перевіряють:**

*Розуміння компромісів у паралельному програмуванні. Це питання демонструє, що кандидат усвідомлює ціну, яку доводиться платити за певні гарантії.*

* * *

### **15 ❓ Питання:**

Чи всі джерела даних однаково добре підходять для паралельної обробки (`parallelStream`)? Яке джерело є найкращим? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не всі.** Ефективність `parallelStream` сильно залежить від того, наскільки **добре** джерело даних можна **розбити на частини (split)** для паралельної обробки.
>
> Для цього використовується `Spliterator`, який надає `Stream`.
>
> **Найкращим джерелом є `ArrayList`:**
>
> * `ArrayList` базується на масиві. Розбити масив на дві рівні частини — це дуже "дешева" операція `O(1)`. Ви просто обчислюєте середній індекс. Тому `ArrayList` розпаралелюється **дуже ефективно**.
>
> **Поганим джерелом є `LinkedList`:**
>
> * `LinkedList` базується на вузлах з посиланнями. Щоб знайти середину списку, потрібно пройти по ньому від початку, що є операцією `O(n)`. Розбиття є дуже "дорогим", і накладні витрати на нього можуть перевищити вигоду від паралелізму. `parallelStream()` на `LinkedList` часто працює **повільніше**, ніж послідовний `stream()`.
>
> **Інші структури:**
>
> * `HashSet` розбивається непогано.
> * `TreeSet` також розбивається досить добре через свою збалансовану деревоподібну структуру.
>
> **Висновок:** Найбільшу вигоду від `parallelStream` ви отримаєте при роботі з `ArrayList` та масивами.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як внутрішня структура даних впливає на продуктивність алгоритмів, що працюють з нею. Це питання вимагає глибоких знань не тільки `Stream API`, а й усього `Collections Framework`.*

* * *
