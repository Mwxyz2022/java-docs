---
layout: default
title: "Стріми примітивів"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 5
---

# Стріми примітивів

* короткий опис: Stream API надає спеціалізовані стріми для примітивних типів (`IntStream`, `LongStream`, `DoubleStream`), що дозволяють ефективно працювати з числовими значеннями, уникаючи витрат на `autoboxing`/`unboxing`.

* поняття: `IntStream`, `LongStream`, `DoubleStream`, `sum()`, `average()`, `min()`, `max()`, `range()`, `rangeClosed()`.

* приклади: `IntStream.range(1, 5).sum()`, `IntStream.of(nums).average()`.

* ключові моменти: Працюють напряму з примітивами, мають унікальні агрегуючі методи.

* що важливо знати на співбесіді: Переваги використання примітивних стрімів (уникнення boxing/unboxing).

### **✅ Офіційне визначення:**

**`Java Stream API`** надає спеціалізовані інтерфейси для обробки примітивних типів: **`IntStream`**, **`LongStream`** та **`DoubleStream`**. Вони дозволяють ефективно працювати з числовими значеннями, мінімізуючи витрати на autoboxing.

### **🧠 Простими словами:**

Це як звичайні стріми, але **тільки для чисел** (int, long, double). Працюють швидше, не створюють коробки-обгортки (**`Integer`**, **`Double`**), і мають зручні методи типу **`sum()`**, **`average()`**

---

## **Основні типи / методи**

📊 **Типи примітивних стрімів:**

|        Тип         |       Призначення        |          Приклади методів          |
|:------------------:|:------------------------:|:----------------------------------:|
|  **`IntStream`**   |  Для **`int`** значень   |  **`sum(), average(), range()`**   |
|  **`LongStream`**  |  Для **`long`** значень  | **`sum(), min(), rangeClosed()`**  |
| **`DoubleStream`** | Для **`double`** значень | **`average(), max(), mapToInt()`** |

📌 **Ключові методи тільки для примітивів:**

* **`sum()`** — обчислює суму
* **`average()`** — повертає **`OptionalDouble`**
* **`min(), max()`** — знаходить мін/макс
* **`range(start, end)`** — діапазон **`[start, end]`** (без останнього)
* **`rangeClosed(start, end)`** — діапазон **`[start, end]`** (включно)

---

## **Пояснення на пальцях (💡)**

Уяви, що тобі треба просумувати 10 чисел:

* Якщо ти створиш **`Stream<Integer>`** — кожне число буде упаковуватись в **`Integer`**, потім розпаковуватись \= повільно.
* Якщо використовуєш **`IntStream`** — Java працює без упаковки, напряму з int \= швидко 🚀.

---

## **Корисні приклади (🧪)**

```java
// Створити IntStream з діапазону чисел
IntStream.range(1, 5).forEach(System.out::print); // 1234
IntStream.rangeClosed(1, 5).forEach(System.out::print); // 12345

// З масиву
int[] nums = {1, 2, 3, 4};
int sum = IntStream.of(nums).sum(); // 10

// Середнє значення
OptionalDouble avg = IntStream.of(2, 4, 6).average(); // 4.0

// Мінімум, максимум
int max = IntStream.of(3, 7, 1).max().orElse(-1); // 7
int min = IntStream.of(3, 7, 1).min().orElse(-1); // 1

// Згенерувати DoubleStream
DoubleStream.generate(Math::random).limit(3).forEach(System.out::println);
```

---

## **Порівняння (⚖️)**

|      Характеристика       | `Stream<Integer>` |   `IntStream` (та інші примітиви)    |
|:-------------------------:|:-----------------:|:------------------------------------:|
|  **Autoboxing/unboxing**  | Так (повільніше)  |           Ні (ефективніше)           |
|   **Спеціальні методи**   |       Немає       | **`sum(), average(), range()`** тощо |
|      **Призначення**      |     Загальне      |        Тільки для примітивів         |
| **Тип значення в стрімі** |   **`Integer`**   |       **`int, long, double`**        |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"I Love Data" — Int, Long, Double — три типи примітивних стрімів**

І ще одне:

**"RR \= Range / RangeClosed"** — тільки примітиви мають ці методи\!

---

## **🟩 Підсумок:**

* Примітивні стріми (**`IntStream`**, **`LongStream`**, **`DoubleStream`**) — це **ефективна альтернатива об'єктнимстрімам** для чисел.
* Мають **унікальні методи** (**`sum(), average(), range()`**), яких немає в **`Stream<T>`**
* Уникають **автобоксингу**, що зменшує навантаження на пам’ять і GC.
* Використовуй їх коли працюєш із **великою кількістю чисел** — буде швидше і чистіше.

---

### **🔗 Додаткові джерела:**

* [**Primitive Streams — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/package-summary.html#StreamTypes)
* [**Java IntStream API — Java SE 8 Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/IntStream.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке стріми примітивів (primitive streams) і навіщо вони потрібні в Java?

> #### **💬 Відповідь:**
>
> **Стріми примітивів** — це спеціалізовані версії `Stream`, призначені для роботи безпосередньо з примітивними числовими типами.
>
> У `Stream API` існує три основних типи таких потоків:
>
> * **`IntStream`** — для роботи з `int`.
> * **`LongStream`** — для роботи з `long`.
> * **`DoubleStream`** — для роботи з `double`.
>
> **Навіщо вони потрібні:**
>
> Головна мета їхнього існування — **уникнення накладних витрат на boxing/unboxing** для підвищення продуктивності.
>
> * **Проблема зі `Stream<Integer>`:** Коли ви працюєте зі звичайним `Stream<Integer>`, кожен примітив `int` "пакується" (boxing) в об'єкт `Integer`. Це створює багато непотрібних об'єктів у купі (heap), що призводить до більшого навантаження на збирача сміття (GC) і погіршення кеш-локальності.
>
> * **Рішення з `IntStream`:** `IntStream` працює безпосередньо з примітивами `int`, уникаючи цих операцій. Це робить обробку великих масивів числових даних значно ефективнішою.

#### **📝 Аналогія:**

Уявіть, що ви перевозите пісок.

* **`Stream<Integer>`:** Це як пакувати кожну піщинку в окрему коробку (`Integer`), а потім перевозити коробки.
* **`IntStream`:** Це як перевозити пісок насипом у вантажівці (`int[]`). Набагато швидше і ефективніше.

#### **📌 Що перевіряють:**

*Розуміння проблеми автопакування (autoboxing) та її впливу на продуктивність. Кандидат повинен знати, що для роботи з числовими примітивами існують спеціалізовані, більш ефективні інструменти.*

* * *

### **2 ❓ Питання:**

Як створити `IntStream`? Назвіть кілька способів.

> #### **💬 Відповідь:**
>
> Існують кілька зручних способів створення `IntStream`:
>
> 1. **`IntStream.range(int startInclusive, int endExclusive)`:**
>     Створює потік цілих чисел від `startInclusive` до `endExclusive - 1`.
>     * `IntStream.range(1, 5)` -> `1, 2, 3, 4`
>
> 2. **`IntStream.rangeClosed(int startInclusive, int endInclusive)`:**
>     Те ж саме, але включаючи кінцеве значення.
>     * `IntStream.rangeClosed(1, 5)` -> `1, 2, 3, 4, 5`
>
> 3. **`Arrays.stream(int[] array)`:**
>     Створює `IntStream` з існуючого масиву `int[]`.
>
>     ```java
>     int[] numbers = {10, 20, 30};
>     IntStream stream = Arrays.stream(numbers);
>     ```
>
> 4. **`IntStream.of(int... values)`:**
>     Створює потік з перерахованих значень.
>
>     ```java
>     IntStream stream = IntStream.of(5, 10, 15);
>     ```
>
> 5. **`String.chars()`:**
>     Метод `.chars()` на рядку повертає `IntStream` з кодами символів.

#### **📌 Що перевіряють:**

*Практичні знання API для створення примітивних потоків. Методи `range` та `rangeClosed` є дуже поширеними.*

* * *

### **3 ❓ Питання:**

Які спеціалізовані термінальні операції є в примітивних потоках, яких немає у звичайному `Stream<T>`?

> #### **💬 Відповідь:**
>
> Примітивні потоки надають кілька дуже корисних термінальних операцій для агрегації числових даних, яких немає в `Stream<T>`:
>
> * **`sum()`:** Обчислює суму всіх елементів у потоці. Повертає `int`, `long` або `double`.
>
> * **`average()`:** Обчислює середнє арифметичне. Повертає `OptionalDouble`, оскільки потік може бути порожнім.
>
> * **`summaryStatistics()`:** Найпотужніша з них. Повертає спеціальний об'єкт (`IntSummaryStatistics`, `LongSummaryStatistics` тощо), який за один прохід по потоку обчислює **одразу все**: кількість елементів, суму, мінімум, максимум та середнє значення. Це значно ефективніше, ніж викликати `count()`, `sum()`, `min()`, `max()` окремо.

#### **📝 Приклад:**

```java
int[] numbers = {1, 2, 3, 4, 5};
IntSummaryStatistics stats = IntStream.of(numbers).summaryStatistics();

System.out.println("Count: " + stats.getCount()); // 5
System.out.println("Sum: " + stats.getSum());     // 15
System.out.println("Min: " + stats.getMin());     // 1
System.out.println("Max: " + stats.getMax());     // 5
System.out.println("Average: " + stats.getAverage()); // 3.0
```

#### **📌 Що перевіряють:**

*Знання переваг примітивних потоків. Кандидат повинен знати, що вони не тільки швидші, але й надають більш зручне API для математичних операцій.*

* * *

### **4 ❓ Питання:**

Як перетворити звичайний потік `Stream<T>` на примітивний і навпаки?

> #### **💬 Відповідь:**
>
> Для цього існують спеціальні проміжні операції:
>
> **1. Перетворення `Stream<T>` на примітивний потік:**
> Для цього використовуються операції `mapTo...`:
>
> * **`mapToInt(ToIntFunction<T> mapper)`:** Приймає функцію, що витягує `int` з об'єкта `T`, і повертає `IntStream`.
> * **`mapToLong(ToLongFunction<T> mapper)`:** Повертає `LongStream`.
> * **`mapToDouble(ToDoubleFunction<T> mapper)`:** Повертає `DoubleStream`.
>
> **2. Перетворення примітивного потоку на `Stream<T>`:**
> Для цього використовується операція **`boxed()`**:
>
> * **`boxed()`**: Перетворює примітивний потік (напр., `IntStream`) на потік об'єктів-обгорток (напр., `Stream<Integer>`). Ця операція виконує boxing для кожного елемента.
>
>     Або операція **`mapToObj(IntFunction<U> mapper)`**, яка перетворює примітив в об'єкт заданого типу.

#### **📝 Приклад:**

```java
List<String> words = List.of("Java", "is", "cool");

// Stream<String> -> IntStream
IntStream lengths = words.stream().mapToInt(String::length);
int totalLength = lengths.sum(); // 12

// IntStream -> Stream<Integer> -> List<Integer>
IntStream.range(1, 4).boxed().collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Знання API для перетворення між різними типами потоків. Це необхідна навичка для побудови складних ланцюжків обробки.*

* * *

### **5 ❓ Питання:**

Поясніть, що таке `OptionalDouble` і чому метод `average()` повертає саме його?

> #### **💬 Відповідь:**
>
> `OptionalDouble` — це спеціалізована версія `Optional` для примітивного типу `double`. Вона працює так само, як і `Optional`, але оптимізована для `double`, щоб уникнути boxing.
>
> **Чому `average()` повертає `OptionalDouble`:**
>
> Метод `average()` обчислює середнє значення елементів. Але якщо потік, на якому він викликається, є **порожнім**, то середнє значення **обчислити неможливо**.
>
> * Якби метод повертав просто `double`, він би був змушений повернути якесь значення за замовчуванням (напр., 0), що було б неправильним і могло призвести до логічних помилок.
> * `OptionalDouble` елегантно вирішує цю проблему:
>   * Якщо потік не порожній, метод повертає `OptionalDouble`, що містить обчислене середнє значення.
>   * Якщо потік порожній, він повертає порожній `OptionalDouble` (`OptionalDouble.empty()`).
>
> Це змушує розробника явно обробити випадок, коли середнє значення не може бути обчислено, що робить код більш надійним.
>
> `min()` та `max()` також повертають `OptionalInt/Long/Double` з тієї ж причини.

#### **📌 Що перевіряють:**

*Розуміння того, як `Stream API` використовує `Optional`-типи для безпечної обробки випадків, коли результат може бути відсутнім.*

* * *

### **6 ❓ Питання:**

Що більш продуктивно і чому: `Stream.of(1,2,3).reduce(0, Integer::sum)` чи `IntStream.of(1,2,3).sum()`? ⭐️

> #### **💬 Відповідь:**
>
> **`IntStream.of(1,2,3).sum()` буде значно продуктивнішим.**
>
> **Причини:**
>
> 1. **Відсутність Boxing/Unboxing:**
>     * **`Stream.of(1,2,3)`**: створює `Stream<Integer>`. Операція `reduce` буде працювати з об'єктами `Integer`, що вимагає постійного розпакування (unboxing) для додавання, а потім пакування (boxing) для проміжного результату.
>     * **`IntStream.of(1,2,3)`**: створює `IntStream`. Операція `sum` працює безпосередньо з примітивами `int`, уникаючи будь-яких накладних витрат на об'єкти-обгортки.
>
> 2. **Спеціалізована оптимізація:**
>     * Метод `sum()` є спеціалізованою, високооптимізованою реалізацією для підсумовування примітивів. Його реалізація "під капотом" може бути значно простішою та швидшою, ніж загальний механізм `reduce`, який має бути гнучким.
>
> **Висновок:** Для будь-яких стандартних математичних агрегацій (`sum`, `average` тощо) над числовими примітивами завжди слід використовувати спеціалізовані методи примітивних потоків.

#### **📌 Що перевіряють:**

*Здатність порівнювати два способи вирішення задачі та обирати більш ефективний, обґрунтовуючи свій вибір на основі знань про boxing та спеціалізовані API.*

* * *

### **7 ❓ Питання:**

Чому не існує `CharStream`, `ShortStream` або `FloatStream`? ⭐️

> #### **💬 Відповідь:**
>
> Розробники JDK прийняли прагматичне рішення не створювати окремі спеціалізації для `char`, `short`, `byte` та `float`.
>
> **Причина:**
>
> **Для `char`, `short`, `byte` можна безпечно та без втрати даних використовувати `IntStream`**. Ці типи неявно розширюються до `int`.
>
> * Метод `String.chars()` є яскравим прикладом — він повертає `IntStream`. Ви можете потім привести `int` назад до `char`, якщо це потрібно: `(char) intValue`.
>
> **Для `float` можна використовувати `DoubleStream`**.
>
> * Будь-яке значення `float` можна безпечно представити як `double`.
>
> Такий підхід дозволив уникнути "роздування" API (`Stream API` і так досить великий) і надмірної кількості класів, покривши при цьому всі практичні потреби за допомогою існуючих трьох спеціалізацій (`IntStream`, `LongStream`, `DoubleStream`).

#### **📌 Що перевіряють:**

*Розуміння дизайнерських рішень, що лежать в основі JDK, та способів роботи з різними примітивними типами у стрімах.*

* * *

### **8 ❓ Питання:**

Наведіть приклад, де перехід від `Stream<T>` до примітивного потоку і назад є корисним. ⭐️

> #### **💬 Відповідь:**
>
> Це часто буває корисно, коли потрібно виконати математичні операції над числовими властивостями об'єктів.
>
> **Сценарій:** У нас є список об'єктів `Product`, і ми хочемо обчислити загальну вартість всіх товарів.
>
> 1. Починаємо з `Stream<Product>`.
> 2. Нам потрібна вартість, яка є `double`. Ми перетворюємо потік на `DoubleStream` за допомогою **`mapToDouble`**.
> 3. На `DoubleStream` ми можемо легко викликати **`sum()`**.
> 4. (Якби потрібно було повернутися до об'єктів) ми б використали `mapToObj`.

#### **📝 Приклад:**

```java
class Product {
    private double price;
    // getter
}
List<Product> products = ...;

// Ланцюжок: Stream<Product> -> DoubleStream -> double
double totalPrice = products.stream()
                            .mapToDouble(Product::getPrice) // Переходимо на DoubleStream
                            .sum(); // Використовуємо спеціалізовану операцію

System.out.println("Загальна вартість: " + totalPrice);
```

#### **📌 Що перевіряють:**

*Здатність будувати складні ланцюжки перетворень, комбінуючи об'єктні та примітивні потоки для досягнення ефективності та читабельності.*

* * *

### **9 ❓ Питання:**

Які є способи створення нескінченного примітивного потоку? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Примітивні потоки, так само як і об'єктні, підтримують методи `iterate` та `generate`.
>
> * **`IntStream.iterate(int seed, IntUnaryOperator f)`:**
>     Створює нескінченний `IntStream` на основі попереднього елемента. `IntUnaryOperator` — це примітивна версія `UnaryOperator<Integer>`.
>   * **Приклад (нескінченний потік парних чисел):**
>         `IntStream.iterate(0, n -> n + 2)`
>
> * **`DoubleStream.generate(DoubleSupplier s)`:**
>     Створює нескінченний `DoubleStream` за допомогою "постачальника". `DoubleSupplier` — примітивна версія `Supplier<Double>`.
>   * **Приклад (потік випадкових чисел):**
>         `DoubleStream.generate(Math::random)`
>
> **Також існують специфічні для примітивів способи:**
>
> * **`Random.ints()` / `Random.longs()` / `Random.doubles()`:**
>     Клас `java.util.Random` має методи, що повертають нескінченні примітивні потоки випадкових чисел.

#### **📝 Коментар:**

Як і з будь-яким нескінченним потоком, обов'язково потрібно використовувати `limit()` для уникнення нескінченного циклу.

#### **📌 Що перевіряють:**

*Знання повного API для створення потоків, включаючи нескінченні примітивні потоки.*

* * *

### **10 ❓ Питання:**

Як `IntStream.range()` реалізовано "під капотом" і чому він ефективний? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `IntStream.range()` реалізовано дуже ефективно і не створює великого масиву в пам'яті. Це **ледачий (lazy)** генератор.
>
> Під капотом використовується спеціальна, оптимізована реалізація `Spliterator` для числових діапазонів.
>
> **Як це працює:**
>
> 1. Коли ви викликаєте `IntStream.range(1, 1_000_000_000)`, в пам'яті **не створюється** масив на мільярд елементів. Створюється лише невеликий об'єкт, що зберігає початкове та кінцеве значення діапазону.
>
> 2. Коли термінальна операція починає "тягнути" дані, `Spliterator` просто **обчислює наступне число** на льоту, інкрементуючи лічильник.
>
> 3. Цей `Spliterator` також дуже добре **розбивається на частини (split)**. Для `parallelStream()` він може миттєво розділити діапазон `[0, 100)` на `[0, 50)` і `[50, 100)`, що робить паралельну обробку діапазонів надзвичайно ефективною.
>
> Таким чином, ефективність досягається за рахунок лінивих обчислень та оптимізованого `Spliterator`'a.

#### **📌 Що перевіряють:**

*Глибокі знання про внутрішню реалізацію `Stream API`. Розуміння того, як працюють `range` та `Spliterator`, показує сильного кандидата.*

* * *

### **11 ❓ Питання:**

Що таке `IntSummaryStatistics` і як цей клас допомагає оптимізувати обчислення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `IntSummaryStatistics` (і його аналоги `LongSummaryStatistics`, `DoubleSummaryStatistics`) — це фінальний клас-контейнер, який використовується для збору **кількох статистичних метрик** про числовий потік **за один прохід**.
>
> Він обчислює:
>
> * Кількість елементів (`count`)
> * Суму (`sum`)
> * Мінімум (`min`)
> * Максимум (`max`)
> * Середнє значення (`average`)
>
> **Як він оптимізує обчислення:**
>
> Якби ви хотіли отримати всі ці п'ять значень окремо, вам довелося б викликати п'ять різних термінальних операцій, і кожна з них проходила б по всьому потоку знову. Це **5 проходів по даних**.
>
> `collect(Collectors.summarizingInt(...))` або `summaryStatistics()` на примітивному потоці робить це **за один прохід**. Вона ітерується по потоку лише один раз, оновлюючи всі п'ять значень паралельно. Це **значно** підвищує продуктивність, особливо для великих потоків, які неможливо або дорого обчислювати кілька разів.

#### **📌 Що перевіряють:**

*Розуміння того, як уникати багаторазової обробки потоку та знання просунутих колекторів та термінальних операцій для агрегації даних.*

* * *

### **12 ❓ Питання:**

Уявіть, що ви працюєте зі `Stream<String>`, але хочете відсортувати його за довжиною рядків. Чи варто перетворювати його на `IntStream`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не варто, і це не вирішить задачу.**
>
> Це питання з підступом, що перевіряє розуміння того, що робить кожна операція.
>
> **Чому це погана ідея:**
>
> 1. `mapToInt(String::length)` перетворить ваш `Stream<String>` на `IntStream`. Тепер ви маєте потік **довжин**, а не **самих рядків**. Ви втратили початкові дані. Якщо ви відсортуєте цей `IntStream`, ви отримаєте відсортований потік чисел, а не рядків.
>
> 2. Повернутися від потоку відсортованих довжин назад до початкових рядків у правильному порядку вже неможливо.
>
> **Правильний підхід:**
>
> Використовувати **`sorted(Comparator)`** на оригінальному `Stream<String>`. Ви можете надати йому `Comparator`, який буде порівнювати рядки за їхньою довжиною.
>
> ```java
> List<String> sortedByLength = words.stream()
>     .sorted(Comparator.comparingInt(String::length))
>     .collect(Collectors.toList());
> ```
>
> Таким чином, ви сортуєте початкові елементи, використовуючи їхню властивість як ключ, але не втрачаючи самі елементи. Примітивні потоки тут не потрібні.

#### **📌 Що перевіряють:**

*Уважність та точне розуміння того, як працюють `map`, `sorted` та перетворення потоків. Це перевіряє здатність не робити хибних кроків в оптимізації.*

* * *

### **13 ❓ Питання:**

Чи можна отримати паралельний примітивний потік? Як це вплине на продуктивність `sum()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Так само, як і для об'єктних потоків, можна викликати `.parallel()` на примітивному потоці або створити його з колекції через `.parallelStream().mapToInt(...)`.
>
> **Як це вплине на продуктивність `sum()`:**
>
> * Операція `sum()` на паралельному примітивному потоці **дуже добре розпаралелюється**.
> * `ForkJoinPool` розбиває діапазон або масив на частини.
> * Кожен потік обчислює **проміжну суму** для своєї частини.
> * Наприкінці всі ці проміжні суми **об'єднуються** в одну фінальну суму.
>
> Ця операція є класичним прикладом **асоціативної редукції**, яка ідеально підходить для паралельної обробки. Для великих масивів чисел `IntStream.of(hugeArray).parallel().sum()` буде значно швидшим за послідовну версію.

#### **📌 Що перевіряють:**

*Розуміння того, які саме операції добре піддаються паралельній обробці. `sum` є ідеальним кандидатом, і кандидат повинен це знати.*

* * *

### **14 ❓ Питання:**

Що поверне цей код і чому?
`OptionalInt result = IntStream.empty().max();` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код поверне **порожній `OptionalInt`** (`OptionalInt.empty`).
>
> **Чому:**
>
> Термінальна операція `max()` (так само як і `min()`, `average()`) призначена для знаходження максимального елемента в потоці. Однак, якщо потік є **порожнім**, то максимального елемента в ньому **не існує**.
>
> * Якби метод `max()` повертав просто `int`, він би був змушений повернути якесь довільне значення (напр., `0` або `Integer.MIN_VALUE`), що було б семантично неправильним і могло б призвести до логічних помилок у коді, який його викликає.
>
> * `OptionalInt` (примітивна спеціалізація `Optional<Integer>`) елегантно вирішує цю проблему. Він явно вказує в системі типів, що результат **може бути відсутнім**.
>
> Клієнтський код змушений обробити цей випадок, викликавши `isPresent()`, `orElse()`, `orElseThrow()` тощо, що робить програму більш надійною.

#### **📌 Що перевіряють:**

*Розуміння того, як `Optional`-типи використовуються в примітивних потоках для безпечної обробки випадків відсутності результату. Це важливий аспект API дизайну.*

* * *

### **15 ❓ Питання:**

Як перетворити `Stream<Integer>` на `int[]` найефективнішим способом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Найефективнішим способом буде **спочатку перетворити `Stream<Integer>` на `IntStream`, а потім викликати на ньому `toArray()`**.
>
> **Кроки:**
>
> 1. `stream.mapToInt(Integer::intValue)`: Ця операція перетворює `Stream<Integer>` на `IntStream`. Метод `Integer::intValue` виконує **unboxing** кожного об'єкта `Integer` у примітив `int`.
> 2. `.toArray()`: Це термінальна операція на `IntStream`, яка збирає всі примітиви `int` у новий масив `int[]`.
>
> **Чому це ефективно:**
>
> * Операція `toArray()` на `IntStream` оптимізована для роботи з примітивами. Вона знає точний розмір потоку (якщо він відомий) і може виділити масив потрібної довжини одразу.
>
> **Альтернативний, але менш ефективний підхід:**
>
> ```java
> Integer[] wrapperArray = stream.toArray(Integer[]::new);
> ```
>
> Цей підхід створить масив **об'єктів `Integer[]`**, а не примітивів `int[]`. Він буде менш ефективним з точки зору пам'яті та може бути повільнішим, якщо далі потрібні операції з примітивами.
>
> #### **Приклад правильного коду:**
>
> ```java
> Stream<Integer> stream = Stream.of(1, 2, 3);
> int[] intArray = stream.mapToInt(Integer::intValue).toArray();
> ```

#### **📌 Що перевіряють:**

*Здатність комбінувати різні операції для ефективного перетворення між об'єктними та примітивними типами. Це поширена практична задача.*

* * *
