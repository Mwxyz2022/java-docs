---
layout: default
title: "Введення в Stream API"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 1
---

# Введення в Stream API

* короткий опис: Stream API (з Java 8) – це функціональність для декларативної обробки послідовностей даних. Він не зберігає дані, працює за принципом pipeline (ланцюжка операцій) і не змінює початкове джерело.

* поняття: `Stream`, pipeline, Lazy Evaluation (лінивість), проміжні операції, кінцеві операції.

* приклади: `List.stream().filter(name -> name.startsWith("A")).map(String::toUpperCase).collect(Collectors.toList());`.

* ключові моменти: Стрім одноразовий, не зберігає дані, виконується лише при виклику кінцевої операції.

* що важливо знати на співбесіді: Що таке Stream, чим відрізняється від колекцій, переваги.

### **✅ Офіційне визначення:**

**Stream API** — це функціональність, введена в Java 8, яка дозволяє працювати з колекціями (і не тільки) в декларативному стилі через послідовність операцій над даними, які можуть бути проміжними або кінцевими.

### **🧠 Простими словами:**

**Стрім** — це "трубопровід" для даних. Він не зберігає самі елементи, а лише проводить над ними операції. Дані заходять — обробляються — виходить результат. Все це без зміни початкових колекцій

---

## 📊 **Основні принципи:**

* Не зберігають дані, а лише обробляють їх на льоту
* Працюють за принципом *pipeline* (ланцюжка операцій)
* Ледачі: обробка виконується лише при виклику кінцевої операції
* Операції не змінюють початкове джерело
* Один стрім \= одноразове використання

---

## **Пояснення на пальцях (💡)**

**Уяви, що в тебе є конвеєр на фабриці:**

* Дані \= ящики на початку стрічки
* Кожна операція стріму — це працівник, який щось робить з ящиком (фільтрує, фарбує, рахує)
* Лише коли хтось стоїть в кінці і збирає результат (термінальна операція), вся ця магія запускається

---

## **Корисні приклади (🧪)**

```java
List<String> names = List.of("Anna", "Bob", "Alex", "Amanda");

List<String> filtered = names.stream()
    .filter(name -> name.startsWith("A"))
    .map(String::toUpperCase)
    .collect(Collectors.toList());

System.out.println(filtered); // [ANNA, ALEX, AMANDA]
```

---

## **Порівняння (⚖️)**

|           Критерій            | Колекції |             Stream             |
|:-----------------------------:|:--------:|:------------------------------:|
|       **Зберігає дані**       |    ✅     |               ❌                |
| **Можна обійти кілька разів** |    ✅     |  ❌ (одноразове використання)   |
| **Підтримує лениву обробку**  |    ❌     |               ✅                |
|    **Паралельна обробка**     |    ❌     | ✅ (через **parallelStream()**) |
|        **Зміна даних**        |    ✅     |     ❌ (не змінює джерело)      |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"FML-C"** — *Filter --> Map --> Lazy --> Collect*

Уяви: ти *F*ільтруєш дані, *M*апиш їх у новий вигляд, стрім *L*енивий, а результат *C*олектиться наприкінці.

---

## **🟩 Підсумок:**

* **Stream API** \= декларативний спосіб обробки послідовностей даних
* Не зберігає дані, працює як труба з операціями
* Ледачий за природою — виконується тільки з термінальною операцією
* Відмінність від колекцій — обробка, а не зберігання
* Підходить для чистого, читаємого, функціонального коду

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Oracle**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/package-summary.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Stream API в Java і в чому його основна відмінність від колекцій?

> #### **💬 Відповідь:**
>
> **Stream API**, введене в Java 8, — це **не структура даних**, а **потік елементів** із джерела (наприклад, з колекції), що дозволяє обробляти дані в функціональному, декларативному стилі.
>
> **Основні відмінності від колекцій:**
>
> 1. **Не зберігає дані:** `Stream` не є сховищем даних. Він лише виступає як "конвеєр", по якому проходять дані з джерела.
> 2. **Незмінність джерела (Immutability):** Операції над потоком **не змінюють** початкову колекцію. Вони створюють новий потік з результатами.
> 3. **Лінивість (Lazy Evaluation):** Проміжні операції (наприклад, `filter`, `map`) не виконуються одразу. Вони лише будують план обчислень. Обробка починається лише тоді, коли викликається термінальна операція.
> 4. **Одноразовість:** Потік можна використати **лише один раз**. Після виклику термінальної операції він стає "спожитим". Для повторної обробки потрібно створювати новий потік.

#### **📝 Аналогія:**

* **Колекція** — це як пляшка з водою. Вона зберігає воду, і ви можете брати з неї воду скільки завгодно разів.
* **Stream** — це як річка. Вона не зберігає воду, а лише дозволяє їй текти. Ви можете побудувати на річці дамби (`filter`) та турбіни (`map`), але вода пройде по ній лише один раз.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння того, що `Stream` — це не колекція, а механізм для обробки даних. Це найперше і найважливіше питання по темі.*

* * *

### **2 ❓ Питання:**

Що таке проміжні та термінальні операції в Stream API?

> #### **💬 Відповідь:**
>
> Усі операції в Stream API поділяються на дві категорії, які визначають, як будується та виконується ланцюжок обробки.
>
> **1. Проміжні операції (Intermediate Operations):**
>
> * **Що роблять:** Приймають потік і **повертають новий потік**.
> * **Характеристика:** Вони завжди є **лінивими (lazy)**. Це означає, що вони не виконуються в момент виклику, а лише додаються до "плану" обробки.
> * **Призначення:** Побудова конвеєра для фільтрації, перетворення, сортування даних.
> * **Приклади:** `filter()`, `map()`, `sorted()`, `distinct()`, `limit()`.
>
> **2. Термінальні операції (Terminal Operations):**
>
> * **Що роблять:** **Запускають** обробку потоку, побудованого проміжними операціями, і **завершують** його.
> * **Характеристика:** Вони "гарячі" (eager). Після їх виклику потік більше не можна використовувати.
> * **Призначення:** Отримання кінцевого результату (колекції, значення) або виконання дії.
> * **Приклади:** `collect()`, `forEach()`, `reduce()`, `count()`, `findFirst()`.

#### **📝 Коментар:**

Ланцюжок стріму може містити нуль або більше проміжних операцій, але завжди завершується **рівно однією** термінальною операцією.

#### **📌 Що перевіряють:**

*Розуміння ключової концепції лінивості та того, як проміжні операції будують конвеєр, а термінальні — запускають його. Це основа роботи зі Stream API.*

* * *

### **3 ❓ Питання:**

Назвіть основні способи створення `Stream`'у.

> #### **💬 Відповідь:**
>
> Існує багато способів створити потік з різних джерел даних:
>
> * **З колекції (найпоширеніший спосіб):**
>     Будь-яка колекція (`List`, `Set` і т.д.) має метод `.stream()`.
>
>     ```java
>     List<String> list = Arrays.asList("a", "b");
>     Stream<String> streamFromList = list.stream();
>     ```
>
> * **З масиву:**
>     За допомогою статичного методу `Arrays.stream()` або `Stream.of()`.
>
>     ```java
>     String[] array = {"a", "b"};
>     Stream<String> streamFromArray = Arrays.stream(array);
>     ```
>
> * **З окремих елементів:**
>     За допомогою `Stream.of()`.
>
>     ```java
>     Stream<Integer> streamOfElements = Stream.of(1, 2, 3);
>     ```
>
> * **З файлу (рядків):**
>     Метод `Files.lines()` створює потік рядків з файлу.
>
>     ```java
>     try (Stream<String> streamOfLines = Files.lines(Paths.get("file.txt"))) {
>         // ...
>     }
>     ```
>
> * **Створення нескінченних потоків:**
>   * `Stream.iterate(seed, function)`: `Stream.iterate(0, n -> n + 2)`
>   * `Stream.generate(supplier)`: `Stream.generate(Math::random)`
>
> * **З рядка (потоку символів):**
>     `"abc".chars()` — повертає `IntStream`.

#### **📌 Що перевіряють:**

*Практичні знання. Кандидат повинен знати, як розпочати роботу зі стрімами, отримавши їх з найпоширеніших джерел даних.*

* * *

### **4 ❓ Питання:**

Поясніть, як працюють основні проміжні операції: `filter` та `map`.

> #### **💬 Відповідь:**
>
> **`filter()` (Фільтрація):**
>
> * **Призначення:** Відфільтрувати потік, залишивши лише ті елементи, які відповідають певній умові.
> * **Що приймає:** `Predicate<T>` — функціональний інтерфейс, який приймає елемент і повертає `true` (залишити елемент) або `false` (відкинути).
> * **Приклад:**
>
>     ```java
>     List<Integer> evenNumbers = numbers.stream()
>                                        .filter(n -> n % 2 == 0)
>                                        .collect(Collectors.toList());
>     ```
>
> **`map()` (Перетворення):**
>
> * **Призначення:** Перетворити кожен елемент потоку на інший елемент.
> * **Що приймає:** `Function<T, R>` — функціональний інтерфейс, який приймає елемент типу `T` і повертає новий елемент типу `R`.
> * **Приклад:**
>
>     ```java
>     List<Integer> lengths = names.stream()
>                                  .map(String::length) // або s -> s.length()
>                                  .collect(Collectors.toList());
>     ```
>
> Це дві найчастіше використовувані проміжні операції, які є основою для більшості ланцюжків обробки даних.

#### **📌 Що перевіряють:**

*Розуміння базових операцій трансформації даних та відповідних їм функціональних інтерфейсів (`Predicate` для `filter`, `Function` для `map`).*

* * *

### **5 ❓ Питання:**

Які є основні термінальні операції для отримання результату?

> #### **💬 Відповідь:**
>
> Після того, як конвеєр обробки побудовано, термінальні операції "збирають" результат. Основні з них:
>
> 1. **`collect(Collector collector)`:**
>     Найбільш гнучка операція. Дозволяє зібрати елементи потоку в `List`, `Set`, `Map` або іншу структуру. Використовує утилітарний клас `Collectors`.
>     * **Приклад:** `stream.collect(Collectors.toList())`
>
> 2. **`forEach(Consumer<T> action)`:**
>     Виконує певну дію для кожного елемента потоку. Не повертає жодного результату.
>     * **Приклад:** `stream.forEach(System.out::println)`
>
> 3. **`reduce(identity, accumulator)`:**
>     "Згортає" потік до одного значення (напр., сума всіх елементів).
>     * **Приклад:** `numbers.stream().reduce(0, Integer::sum)`
>
> 4. **`count()`:**
>     Повертає кількість елементів у потоці.
>     * **Приклад:** `stream.count()`
>
> 5. **`findFirst()` / `findAny()`:**
>     Повертають `Optional` з першим або будь-яким елементом, що задовольняє умовам.
>
> 6. **`anyMatch()`, `allMatch()`, `noneMatch()`:**
>     Приймають `Predicate` і перевіряють, чи відповідають умові хоч один, всі чи жоден елемент.

#### **📌 Що перевіряють:**

*Знання основних способів завершення роботи зі стрімом. Особливу увагу слід звернути на `collect`, як найуживаніший.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати один і той самий `Stream` кілька разів?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Потік є **одноразовим**.
>
> Як тільки на потоці викликається **термінальна операція**, він вважається **"спожитим" або "закритим" (consumed or closed)**. Будь-яка подальша спроба викликати на цьому ж екземплярі потоку іншу операцію (проміжну або термінальну) призведе до винятку `IllegalStateException` з повідомленням "stream has already been operated upon or closed".
>
> Якщо вам потрібно виконати іншу операцію з тими ж початковими даними, ви повинні **створити новий потік з джерела** (колекції, масиву і т.д.).

#### **📝 Приклад помилки:**

```java
List<String> names = List.of("a", "b", "c");
Stream<String> nameStream = names.stream();

// Перше використання - все гаразд
nameStream.forEach(System.out::println);

// Друге використання - ПОМИЛКА!
// long count = nameStream.count(); // Кине IllegalStateException
```

#### **📌 Що перевіряють:**

*Знання одного з фундаментальних правил роботи зі стрімами. Це поширена помилка для тих, хто починає вивчати Stream API.*

* * *

### **7 ❓ Питання:**

Що таке лінива оцінка (Lazy Evaluation) і яку перевагу вона дає?

> #### **💬 Відповідь:**
>
> **Лінива оцінка** — це ключовий принцип роботи Stream API, який означає, що **проміжні операції не виконуються одразу**. Вони лише будують план обчислень. Фактична обробка елементів відкладається до моменту виклику **термінальної операції**.
>
> **Основна перевага — оптимізація продуктивності.**
>
> Завдяки лінивості, Stream API може виконувати кілька оптимізацій:
>
> 1. **Обробка лише необхідних даних:** Якщо є "операція з коротким замиканням" (`short-circuiting`), як-от `findFirst()`, стрім припинить обробку, як тільки знайде результат. Він не буде перебирати решту мільйона елементів.
> 2. **Об'єднання операцій (Fusion):** Замість того, щоб проходити по всій колекції для `filter`, потім знову по всій для `map`, стрім обробляє елементи "вертикально". Кожен елемент проходить весь конвеєр операцій за один раз. Це значно зменшує кількість ітерацій.

#### **📌 Що перевіряють:**

*Розуміння, що є основою високої продуктивності стрімів. Це показує, що кандидат розуміє внутрішні механізми, а не лише синтаксис.*

* * *

### **8 ❓ Питання:**

Чим операція `map` відрізняється від `flatMap`? Наведіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> Обидві операції є проміжними і призначені для перетворення елементів, але вони вирішують різні завдання.
>
> * **`map(Function<T, R> mapper)`:**
>   * **Перетворення "один-в-один" (one-to-one).** Вона приймає один елемент і повертає один перетворений елемент.
>   * Якщо застосувати `map` до `Stream<List<T>>`, результатом буде `Stream<Stream<T>>` — потік потоків.
>
> * **`flatMap(Function<T, Stream<R>> mapper)`:**
>   * **Перетворення "один-в-багато" (one-to-many).** Вона приймає один елемент і повертає **потік** з нуля, одного або більше елементів.
>   * Головна особливість: `flatMap` потім "сплющує" (flattens) всі ці отримані потоки в один єдиний, płaski потік.
>
> `flatMap` використовується тоді, коли кожен елемент вашого потоку сам по собі є колекцією, і ви хочете працювати з усіма елементами цих внутрішніх колекцій як з єдиним потоком.

#### **📝 Приклад:**

```java
List<List<Integer>> listOfLists = List.of(List.of(1, 2), List.of(3, 4, 5));

// map створить Stream<List<Integer>>
List<Integer> firstElements = listOfLists.stream()
    .map(list -> list.get(0))
    .collect(Collectors.toList()); // [1, 3]

// flatMap створить Stream<Integer>
List<Integer> allElements = listOfLists.stream()
    .flatMap(list -> list.stream())
    .collect(Collectors.toList()); // [1, 2, 3, 4, 5]
```

#### **📌 Що перевіряють:**

*Знання однієї з найпотужніших, але і найскладніших для розуміння операцій. Це дуже поширене питання на співбесідах.*

* * *

### **9 ❓ Питання:**

Які основні функціональні інтерфейси використовуються в Stream API? ⭐️

> #### **💬 Відповідь:**
>
> Stream API активно використовує стандартні функціональні інтерфейси з пакету `java.util.function` для передачі поведінки в свої методи.
>
> **Ключові інтерфейси:**
>
> * **`Predicate<T>`** (`boolean test(T t)`):
>   * Використовується для фільтрації в методах **`filter()`**, `anyMatch()`, `allMatch()`, `noneMatch()`.
>
> * **`Function<T, R>`** (`R apply(T t)`):
>   * Використовується для перетворення в методах **`map()`**, `flatMap()`.
>
> * **`Consumer<T>`** (`void accept(T t)`):
>   * Використовується для виконання дії в термінальному методі **`forEach()`**.
>
> * **`Supplier<T>`** (`T get()`):
>   * Використовується для створення нескінченних потоків у **`Stream.generate()`**.
>
> * **`BinaryOperator<T>`** (`T apply(T t1, T t2)`):
>   * Використовується для "згортання" елементів у методі **`reduce()`**.
>
> Розуміння цих інтерфейсів є обов'язковим для ефективної роботи зі Stream API.

#### **📌 Що перевіряють:**

*Здатність пов'язати теорію (функціональні інтерфейси) з практикою (Stream API). Це демонструє цілісне розуміння функціонального програмування в Java.*

* * *

### **10 ❓ Питання:**

Навіщо в Stream API є окремі примітивні стріми, як-от `IntStream`, `LongStream`, `DoubleStream`? ⭐️

> #### **💬 Відповідь:**
>
> Примітивні спеціалізації стрімів були введені для **оптимізації продуктивності** при роботі з великими масивами числових даних.
>
> **Проблема, яку вони вирішують — це boxing/unboxing.**
>
> * Звичайний `Stream<Integer>` працює з об'єктами-обгортками `Integer`. Коли ви обробляєте масив `int[]` за допомогою такого стріму, кожен `int` "пакується" (boxing) в об'єкт `Integer`.
> * Це створює величезну кількість непотрібних об'єктів у купі (heap), що призводить до значного навантаження на збирача сміття та погіршення продуктивності.
>
> **Рішення примітивними стрімами:**
> `IntStream`, `LongStream`, `DoubleStream` працюють безпосередньо з примітивними типами, **уникаючи boxing/unboxing**.
>
> Крім того, вони надають додаткові корисні термінальні операції, специфічні для чисел, як-от `sum()`, `average()`, `summaryStatistics()`.
>
> **Правило:** Якщо ви працюєте з масивом числових примітивів, **завжди** використовуйте примітивні стріми (`IntStream` і т.д.) для кращої продуктивності.

#### **📌 Що перевіряють:**

*Розуміння аспектів продуктивності в Java та знання способів уникнення поширеної пастки з автопакуванням.*

* * *

### **11 ❓ Питання:**

Що таке stateless та stateful операції в Stream API? Наведіть приклади. ⭐️

> #### **💬 Відповідь:**
>
> Проміжні операції в стрімах можна розділити на ті, що не зберігають стан, і ті, що зберігають.
>
> * **Stateless Operations (Операції без стану):**
>   * **Що роблять:** Обробляють кожен елемент **незалежно** від інших. Для обробки одного елемента їм не потрібна інформація про елементи, що були до нього або будуть після.
>   * **Характеристика:** Дуже добре підходять для паралельної обробки.
>   * **Приклади:** `filter()`, `map()`.
>
> * **Stateful Operations (Операції зі станом):**
>   * **Що роблять:** Під час обробки вони **накопичують стан** з елементів, які вони вже бачили. Їх результат для одного елемента може залежати від інших.
>   * **Характеристика:** Можуть обмежувати можливості для паралельної оптимізації, оскільки може знадобитися обробити весь потік або його значну частину перед тим, як видати результат.
>   * **Приклади:** `distinct()` (повинен пам'ятати всі унікальні елементи, що вже бачив), `sorted()` (повинен зібрати всі елементи, перш ніж їх відсортувати), `limit()`, `skip()`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють різні проміжні операції, та їхнього впливу на продуктивність, особливо в паралельних потоках.*

* * *

### **12 ❓ Питання:**

Що повернуть операції `findFirst()` та `findAny()`? Чому саме `Optional`? ⭐️

> #### **💬 Відповідь:**
>
> * **`findFirst()`**: Повертає перший елемент потоку.
> * **`findAny()`**: Повертає будь-який елемент потоку (особливо корисно для паралельних потоків, де він може бути швидшим за `findFirst`).
>
> Обидві ці операції повертають результат, загорнутий в **`java.util.Optional`**.
>
> **Чому `Optional`?**
>
> Тому що **потік може бути порожнім**. Якщо потік після фільтрації не містить жодного елемента, то `findFirst()` або `findAny()` не можуть нічого повернути.
>
> * **Якби вони повертали `null`**, це змусило б розробника завжди пам'ятати про перевірку на `null` і створювало б ризик `NullPointerException`.
> * Натомість, **`Optional`** є значно кращим дизайнерським рішенням. Він явно вказує у системі типів, що значення **може бути відсутнім**. Це змушує розробника обробити обидва випадки (коли значення є і коли його немає) за допомогою методів `Optional`, як-от `isPresent()`, `orElse()`, `ifPresent()`, що робить код набагато більш надійним.

#### **📌 Що перевіряють:**

*Розуміння того, як Stream API використовує `Optional` для безпечної обробки потенційно відсутніх результатів. Це демонструє знання сучасних практик написання надійного коду.*

* * *

### **13 ❓ Питання:**

Чи гарантує `Stream` збереження порядку елементів з початкової колекції? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це залежить від **типу джерела** та **проміжних операцій**.
>
> **Коли порядок зберігається:**
>
> * Якщо джерелом потоку є **колекція зі збереженням порядку** (напр., `List`, `LinkedHashSet`), то потік спочатку є впорядкованим (`ordered`).
> * **Stateless** проміжні операції, як-от `filter()` і `map()`, **зберігають цей порядок**.
>
> **Коли порядок НЕ зберігається або може змінюватися:**
>
> * Якщо джерелом є **невпорядкована колекція** (напр., `HashSet`).
> * Якщо використовується `parallelStream()`, деякі операції (`findAny()`, `forEach()`) не гарантують дотримання порядку заради продуктивності.
> * Якщо явно викликати проміжну операцію **`.unordered()`**, яка "говорить" потоку, що порядок більше не важливий, що може дозволити деякі оптимізації.
>
> **Коли порядок нав'язується:**
>
> * Операція `sorted()` явно встановлює новий порядок для елементів.
>
> **Висновок:** У більшості випадків, при роботі з `List` та послідовним `stream()`, ви можете розраховувати на збереження порядку.

#### **📌 Що перевіряють:**

*Знання нюансів, що стосуються впорядкованості потоків. Це важливо для завдань, де послідовність елементів має значення.*

* * *

### **14 ❓ Питання:**

Поясніть, як відбувається виконання в цьому ланцюжку: `stream().map(...).filter(...).findFirst()`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей ланцюжок чудово демонструє **лінивість** та **вертикальну** обробку в `Stream API`.
>
> Давайте розберемо покроково:
>
> 1. `stream()`: Створюється потік. Обробка ще не почалася.
>
> 2. **`findFirst()`** — це термінальна операція, яка запускає процес. Їй потрібен **лише один** елемент.
>
> 3. Система запитує перший елемент з потоку.
>
> 4. **`map()`** бере перший елемент, застосовує до нього функцію-перетворювач і передає результат далі.
>
> 5. **`filter()`** отримує перетворений елемент. Він перевіряє, чи відповідає елемент умові (`Predicate`).
>     * **Якщо так (`true`)**: `filter()` передає цей елемент до `findFirst()`. `findFirst()` отримав те, що шукав, і **негайно завершує роботу всього потоку**. Решта елементів навіть не будуть оброблені.
>     * **Якщо ні (`false`)**: `filter()` відкидає елемент. `findFirst()` не отримав свого результату, тому система запитує **наступний** елемент, і цикл повторюється з кроку 4.
>
> **Ключовий момент:** `map` та `filter` не виконуються для всієї колекції "горизонтально". Вони працюють "вертикально", пропускаючи один елемент за раз через весь конвеєр, доки термінальна операція не буде задоволена.

#### **📌 Що перевіряють:**

*Здатність "мислити як стрім" і детально пояснити процес лінивої, вертикальної обробки даних, що є однією з найважливіших концепцій Stream API.*

* * *

### **15 ❓ Питання:**

Як обробити перевірені винятки (checked exceptions) в операціях Stream API, наприклад, у `map()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це одна з найпоширеніших практичних проблем, оскільки `Function`, який приймає `map`, не оголошує `throws`.
>
> Якщо метод, який ви хочете викликати в `map`, кидає перевірений виняток (напр., `IOException`), у вас є кілька стратегій:
>
> 1. **Обгортання в `try-catch` всередині лямбди:**
>     Найбільш прямолінійний, але громіздкий підхід. Ви перехоплюєте виняток і вирішуєте, що робити:
>     * Проковтнути і повернути `null` (погано).
>     * **Загорнути** в `RuntimeException` (кращий варіант), щоб "провалити" стрім.
>
>     ```java
>     .map(item -> {
>         try {
>             return processItem(item);
>         } catch (IOException e) {
>             throw new UncheckedIOException(e);
>         }
>     })
>     ```
>
> 2. **Створення методу-обгортки (wrapper):**
>     Створити утилітарний метод, який приймає "небезпечну" функцію, а повертає `Function`, що обробляє виняток під капотом. Це дозволяє уникнути дублювання `try-catch` блоків.
>
>     ```java
>     .map(wrap(item -> processItem(item)))
>     ```
>
> 3. **Використання сторонніх бібліотек:**
>     Бібліотеки, як-от Vavr, надають власні контейнери (`Try`), які дозволяють елегантно обробляти помилки у функціональному стилі.
>
> Найбільш поширеним у простих випадках є перший підхід з обгортанням. Для складних систем кращим є другий або третій.

#### **📌 Що перевіряють:**

*Практичний досвід та знання поширених проблем і патернів для їх вирішення. Обробка винятків у лямбдах є важливою та частою задачею.*

* * *
