---
layout: default
title: "Перетворення примітивів: boxed() та mapToObj()"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 11
---

# Перетворення примітивів: boxed() та mapToObj()

* короткий опис: `boxed()` – метод примітивних стрімів (`IntStream`, `LongStream`, `DoubleStream`), що перетворює їх на стрім об'єктів-обгорток (`Integer`, `Long`, `Double`). `mapToObj()` – метод, що перетворює примітивний стрім на стрім будь-яких об'єктів за допомогою функції перетворення.

* поняття: `boxed()`, `mapToObj()`.

* приклади: `IntStream.range(1, 5).boxed()` (IntStream --> Stream<Integer>), `IntStream.range(1, 4).mapToObj(i -> "Num:" + i)` (IntStream --> Stream<String>).

* ключові моменти: `boxed()` для простого обгортання. `mapToObj()` для довільної трансформації. Обидва допомагають інтегрувати примітивні стріми в об'єктні API.

* що важливо знати на співбесіді: Різниця, коли використовувати кожен метод.

### **✅ Офіційне визначення:**

* **`boxed()`** — метод примітивних стрімів (**`IntStream`**, **`LongStream`**, **`DoubleStream`**), який перетворює стрім примітивів у стрім об’єктів-обгорток (**`Integer`**, **`Long`**, **`Double`**).

* **`mapToObj()`** — метод, що перетворює примітивний стрім у стрім будь-яких об’єктів, використовуючи функцію перетворення.

### **🧠 Простими словами:**

**`boxed()`** — це "упакуй примітив", а **`mapToObj()`** — "перетвори примітив на щось інше": символ, рядок, об'єкт. Перший просто загортає, другий трансформує

---

## 📊 **Порівняння методів:**

|      Метод       |                  Призначення                  |     Гнучкість      |
|:----------------:|:---------------------------------------------:|:------------------:|
|  **`boxed()`**   |    Примітив --> обʼєкт (Integer, Double...)     |  ❌ лише обгортки   |
| **`mapToObj()`** | Примітив --> будь-який обʼєкт (String, Person…) | ✅ довільні обʼєкти |

---

## **Пояснення на пальцях (💡)**

Маєш числа і хочеш колекцію **`Integer`**? Використовуй **`boxed()`**  

Хочеш зробити зі стріму чисел стрім рядків або обʼєктів? Тут допоможе **`mapToObj()`**

🔧 **`boxed()`** — це як **`.wrap()`** — без змін значення, лише тип.  
🔧 **`mapToObj()`** — це **`.transform()`** — зміна типу та суті.

---

## **Корисні приклади (🧪)**

```java
// 🔁 Примітив у обʼєкт (автоматично Integer)
IntStream.range(1, 5)
    .boxed() // IntStream --> Stream<Integer>
    .forEach(System.out::println);
```

```java
// 🔤 Примітив у String
IntStream.range(1, 4)
    .mapToObj(i -> "Число: " + i)
    .forEach(System.out::println);
```

```java
// 👤 Примітив у кастомний обʼєкт
record User(int id, String name) {}

IntStream.rangeClosed(1, 3)
    .mapToObj(i -> new User(i, "User" + i))
    .forEach(System.out::println);
```

```java
// 🧬 Робота з символами рядка
String input = "hello";
input.chars() // IntStream
    .mapToObj(c -> (char) c) // Int --> Character
    .forEach(System.out::println);
```

---

## **Порівняння (⚖️)**

|        Властивість        |         `boxed()`          |              `mapToObj()`               |
|:-------------------------:|:--------------------------:|:---------------------------------------:|
|       **Простота**        |       ✅ Дуже проста        |    ⚠️ Потрібна функція перетворення     |
|    **Тип результату**     | **`Stream<Integer>`** тощо |     **`Stream<T>`** (будь-який тип)     |
|       **Гнучкість**       |       ❌ лише boxing        |        ✅ довільна трансформація         |
| **Перетворення символів** |       ❌ не підтримує       | ✅ (через **`mapToObj(c -> (char) c)`**) |

---

## **🧠 Мнемоніка для запам’ятовування:**

**boxed() — просто обгорнути,**  
**mapToObj() — трансформувати у що завгодно.**

Або:  
**"boxed — банально, mapToObj — магічно."**

---

## **🟩 Підсумок:**

* **`boxed()`** — для простого перетворення примітиву в обʼєктну обгортку.
* **`mapToObj()`** — для складніших перетворень у будь-які обʼєкти.
* Обидва методи потрібні, щоб обʼєднувати примітивні стріми з API, що очікують обʼєктні типи.
* Корисно при роботі з рядками, кастомними класами, колекціями.

---

### **🔗 Додаткові джерела:**

* [**Official JavaDoc: IntStream.boxed()**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/IntStream.html#boxed--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке операція `boxed()` і для чого вона потрібна?

> #### **💬 Відповідь:**
>
> **`boxed()`** — це проміжна операція, доступна для примітивних потоків (`IntStream`, `LongStream`, `DoubleStream`).
>
> **Її головна мета** — **перетворити примітивний потік на потік відповідних об'єктів-обгорток**.
>
> * `IntStream` перетворюється на `Stream<Integer>`.
> * `LongStream` перетворюється на `Stream<Long>`.
> * `DoubleStream` перетворюється на `Stream<Double>`.
>
> **Навіщо це потрібно:**
> Хоча примітивні потоки є продуктивнішими, іноді потрібно перейти до об'єктного потоку, щоб:
>
> * Зібрати результати в колекцію (`List`, `Set`), оскільки `Collectors` працюють з `Stream<T>`.
> * Використати операції, доступні лише для `Stream<T>` (напр., `sorted` з кастомним `Comparator`).
>
> `boxed()` виконує операцію **автопакування (boxing)** для кожного елемента потоку.

#### **📝 Аналогія:**

`boxed()` — це як взяти сипучі інгредієнти (примітиви `int`) і розкласти кожен з них по окремих брендованих пакетиках (об'єкти `Integer`), щоб далі працювати з ними як зі штучним товаром.

#### **📝 Приклад:**

```java
import java.util.List;
import java.util.stream.IntStream;
import java.util.stream.Collectors;

public class BoxedExample {
    public static void main(String[] args) {
        // Створюємо потік примітивів int
        IntStream intStream = IntStream.range(1, 5); // 1, 2, 3, 4

        // "Пакуємо" його в потік об'єктів Integer
        // IntStream -> Stream<Integer>
        List<Integer> integerList = intStream.boxed()
                                           .collect(Collectors.toList());
        
        System.out.println(integerList); // [1, 2, 3, 4]
    }
}
```

#### **📌 Що перевіряють:**

*Базове розуміння того, як переходити від примітивних потоків до об'єктних, та знання операції `boxed()`.*

* * *

### **2 ❓ Питання:**

Що таке операція `mapToObj()` і чим вона відрізняється від `boxed()`?

> #### **💬 Відповідь:**
>
> **`mapToObj(IntFunction<? extends U> mapper)`** — це також проміжна операція примітивних потоків, яка перетворює кожен примітивний елемент на **об'єкт**.
>
> **Принципова відмінність від `boxed()`:**
>
> * **`boxed()`**: Виконує **лише одну, фіксовану операцію** — пакування примітива у відповідний клас-обгортку (`int` -> `Integer`). Це операція без параметрів.
>
> * **`mapToObj()`**: Це **гнучка операція перетворення**. Вона приймає функцію (`IntFunction`, `LongFunction` тощо), яка визначає, **як саме** перетворити кожен примітивний елемент на **об'єкт будь-якого типу**.
>
> **Коротко:** `boxed()` — це просто "загорнути". `mapToObj()` — це "взяти примітив і створити з нього будь-який об'єкт".

#### **📝 Аналогія:**

* **`boxed()`**: Бере число 5 і кладе його в коробку з написом "5".
* **`mapToObj()`**: Бере число 5 і створює на його основі об'єкт `User` з `id=5` або рядок `"Number: 5"`.

#### **📌 Що перевіряють:**

*Розуміння різниці між простою "обгорткою" та гнучким перетворенням. Кандидат повинен знати, що `mapToObj` є значно потужнішим інструментом.*

* * *

### **3 ❓ Питання:**

Наведіть приклад, де використання `mapToObj()` є необхідним.

> #### **💬 Відповідь:**
>
> `mapToObj()` є необхідним, коли вам потрібно створити потік об'єктів **не-обгорткових типів** з примітивного потоку.
>
> **Задача:** Створити потік рядків (`Stream<String>`) з потоку чисел `IntStream`.
>
> `boxed()` тут не допоможе, бо він створить `Stream<Integer>`.
>
> **Рішення:**
> Використовуємо `mapToObj` і передаємо функцію, яка перетворює `int` на `String`.

#### **📝 Приклад коду:**

```java
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class MapToObjExample {
    public static void main(String[] args) {
        // Створюємо потік примітивів int від 1 до 3
        IntStream intStream = IntStream.rangeClosed(1, 3);
        
        // Перетворюємо кожен int на рядок "Item-N"
        Stream<String> stringStream = intStream.mapToObj(i -> "Item-" + i);
        
        stringStream.forEach(System.out::println);
        // Виведе:
        // Item-1
        // Item-2
        // Item-3
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички застосування `mapToObj` для вирішення типових завдань з перетворення даних.*

* * *

### **4 ❓ Питання:**

Які функціональні інтерфейси приймають методи `mapToObj` для `IntStream`, `LongStream`, `DoubleStream`?

> #### **💬 Відповідь:**
>
> Кожен примітивний потік має відповідний функціональний інтерфейс для `mapToObj`:
>
> * **`IntStream.mapToObj(IntFunction<? extends U> mapper)`**:
>   * Приймає **`IntFunction<U>`**, метод якої: `U apply(int value)`.
>
> * **`LongStream.mapToObj(LongFunction<? extends U> mapper)`**:
>   * Приймає **`LongFunction<U>`**, метод якої: `U apply(long value)`.
>
> * **`DoubleStream.mapToObj(DoubleFunction<? extends U> mapper)`**:
>   * Приймає **`DoubleFunction<U>`**, метод якої: `U apply(double value)`.
>
> Всі ці інтерфейси приймають примітив, а повертають об'єкт довільного узагальненого типу `U`.

#### **📌 Що перевіряють:**

*Знання спеціалізованих функціональних інтерфейсів з пакету `java.util.function`.*

* * *

### **5 ❓ Питання:**

Як за допомогою `mapToObj` перетворити `IntStream` кодів символів назад у `Stream<Character>`?

> #### **💬 Відповідь:**
>
> Це класичний випадок використання `mapToObj`. Метод `String.chars()` повертає `IntStream`, що містить Unicode-значення символів. Щоб працювати з ними як з об'єктами `Character`, потрібне перетворення.
>
> 1. Отримуємо `IntStream` за допомогою `.chars()`.
> 2. Використовуємо `mapToObj`, щоб перетворити кожен `int` на `char` за допомогою явного приведення типу (`(char) c`), і `char` буде автоматично "запаковано" в `Character`.

#### **📝 Приклад:**

```java
String text = "Java";
Stream<Character> charStream = text.chars() // Отримуємо IntStream: [74, 97, 118, 97]
                                   .mapToObj(code -> (char) code); // Перетворюємо на Stream<Character>
                                   
charStream.forEach(System.out::println); // J, a, v, a
```

#### **📌 Що перевіряють:**

*Практичні навички роботи з рядками через `Stream API` та розуміння, як працювати з потоками символів.*

* * *

### **6 ❓ Питання:**

Коли слід віддавати перевагу `boxed()`?

> #### **💬 Відповідь:**
>
> Перевагу `boxed()` слід віддавати лише тоді, коли вам потрібно виконати **просте і пряме перетворення** примітивного потоку на потік його об'єктів-обгорток **без будь-якої додаткової логіки**.
>
> **Сценарій:** У вас є `IntStream`, і вам потрібно зібрати його елементи в `List<Integer>`.
>
> ```java
> // Правильно і читабельно
> List<Integer> list = IntStream.range(1, 5).boxed().collect(Collectors.toList());
> ```
>
> Хоча це можна зробити і через `mapToObj`:
>
> ```java
> // Технічно правильно, але менш ідіоматично і трохи більш багатослівно
> List<Integer> list2 = IntStream.range(1, 5).mapToObj(i -> i).collect(Collectors.toList());
> ```
>
> **Висновок:** `boxed()` є більш **декларативним** і чітко виражає намір: "я просто хочу запакувати ці примітиви". Тому для простого пакування він є кращим вибором.

#### **📌 Що перевіряють:**

*Розуміння ідіоматичних конструкцій. Кандидат повинен знати, що для простої задачі слід обирати простіший і більш виразний інструмент.*

* * *

### **7 ❓ Питання:**

Чому цей код неефективний, і як його покращити за допомогою `boxed` і `mapToObj`? `List<String> list = Stream.iterate(0, n -> n + 1).limit(10).map(i -> "v" + i).collect(Collectors.toList());` ⭐️

> #### **💬 Відповідь:**
>
> Цей код технічно правильний, але неефективний через **зайвий boxing**.
>
> **Що відбувається:**
>
> 1. `Stream.iterate(0, n -> n + 1)` створює `Stream<Integer>`, а не `IntStream`.
> 2. Кожен раз, коли генерується нове число, `int` "пакується" в `Integer`. Для 10 елементів це 10 операцій boxing.
>
> **Як покращити:**
>
> Потрібно використовувати примітивний потік `IntStream`, а потім, коли потрібні об'єкти, — `mapToObj`.
>
> 1. `IntStream.iterate(0, n -> n + 1)` створює `IntStream`. Тут немає boxing'у.
> 2. `mapToObj(i -> "v" + i)` ефективно перетворює кожен `int` на `String`.
>
> #### **Приклад покращеного коду:**
>
> ```java
> List<String> list = IntStream.iterate(0, n -> n + 1)
>                             .limit(10)
>                             .mapToObj(i -> "v" + i) // Працюємо з IntStream
>                             .collect(Collectors.toList());
> ```
>
> Хоча `boxed()` тут напряму не потрібен, приклад ілюструє важливість використання примітивних потоків з самого початку. Якби нам потрібен був `List<Integer>`, ми б використали `.boxed()` в кінці.

#### **📌 Що перевіряють:**

*Здатність аналізувати код на предмет "прихованих" проблем з продуктивністю та вміння оптимізувати його за допомогою правильних типів потоків.*

* * *

### **8 ❓ Питання:**

Уявіть, що у вас є `IntStream` і ви хочете відсортувати його у зворотному порядку. Як це зробити, використовуючи `boxed()`? ⭐️

> #### **💬 Відповідь:**
>
> `IntStream`, як і інші примітивні потоки, має лише один метод `sorted()`, який сортує за зростанням. Він **не приймає `Comparator`**, тому відсортувати його у зворотному порядку напряму неможливо.
>
> Щоб вирішити цю задачу, потрібно:
>
> 1. **Перетворити `IntStream` на об'єктний `Stream<Integer>`** за допомогою операції **`.boxed()`**.
> 2. Тепер на `Stream<Integer>` можна викликати перевантажену версію `sorted(Comparator)`.
> 3. Передати `Comparator.reverseOrder()` для сортування за спаданням.

#### **📝 Приклад коду:**

```java
import java.util.Comparator;
import java.util.stream.IntStream;

public class ReverseSortExample {
    public static void main(String[] args) {
        IntStream.of(5, 1, 4, 2, 3)
                 .boxed() // IntStream -> Stream<Integer>
                 .sorted(Comparator.reverseOrder()) // Сортуємо у зворотному порядку
                 .forEach(System.out::print); // 54321
    }
}
```

#### **📌 Що перевіряють:**

*Знання обмежень примітивних потоків та вміння використовувати перехід до об'єктних потоків для доступу до більш гнучкого API (наприклад, кастомних компараторів).*

* * *

### **9 ❓ Питання:**

Чому `mapToObj` є проміжною, а не термінальною операцією? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що `mapToObj` **повертає новий потік (`Stream`)**, а не кінцевий результат (`void`, значення чи колекцію).
>
> **Її завдання** — змінити тип потоку в середині конвеєра, щоб до нього можна було застосувати наступні проміжні або термінальні операції, призначені для об'єктних потоків.
>
> **Приклад ланцюжка:**
> `IntStream` -> `mapToObj` -> `Stream<String>` -> `filter` -> `Stream<String>` -> `collect` -> `List<String>`
>
> Якби `mapToObj` була термінальною, ланцюжок би обірвався на ній, що зробило б її значно менш корисною.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що визначає тип операції — її повернуте значення.*

* * *

### **10 ❓ Питання:**

Уявіть, що у вас є `DoubleStream`. Як за допомогою `mapToObj` створити `Stream<BigDecimal>` для точних фінансових розрахунків? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це чудовий практичний приклад, де `mapToObj` є необхідним інструментом, оскільки `double` не підходить для точних фінансових обчислень через похибки заокруглення.
>
> Для перетворення кожного `double` в `BigDecimal` ми використовуємо `mapToObj` і передаємо функцію-конструктор.
>
> Найкраще для цього підходить посилання на статичний метод **`BigDecimal::valueOf`**. Цей метод є більш ефективним, ніж `new BigDecimal(double)`, оскільки він може кешувати часто використовувані значення.

#### **📝 Приклад коду:**

```java
import java.math.BigDecimal;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.DoubleStream;

public class BigDecimalConversion {
    public static void main(String[] args) {
        DoubleStream doubleStream = DoubleStream.of(0.1, 0.2, 0.3);

        List<BigDecimal> bigDecimalList = doubleStream
            .mapToObj(BigDecimal::valueOf) // Перетворюємо double в BigDecimal
            .collect(Collectors.toList());

        System.out.println(bigDecimalList);
    }
}
```

#### **📌 Що перевіряють:**

*Розуміння проблем з `double` у фінансових розрахунках та вміння використовувати `Stream API` для переходу до більш надійних типів, як `BigDecimal`.*

* * *

### **11 ❓ Питання:**

Як реалізовано `boxed()` "під капотом"? Чи він еквівалентний `mapToObj(i -> i)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча кінцевий результат є однаковим, внутрішня реалізація `boxed()` може бути **трохи більш оптимізованою**.
>
> **`mapToObj(i -> i)`:**
> Цей виклик вимагає створення та передачі лямбда-виразу (або посилання на метод `Integer::valueOf`).
>
> **`boxed()`:**
> Цей метод є **спеціалізованою, вбудованою** операцією. Під капотом він може не створювати зайвих об'єктів для самої функції перетворення.
>
> У сучасних JVM різниця в продуктивності, швидше за все, буде **незначною**, оскільки JIT-компілятор може оптимізувати обидва варіанти до дуже схожого коду.
>
> Однак, з точки зору **читабельності та ясності наміру**, `.boxed()` є **однозначно кращим**. Він чітко говорить: "я просто хочу запакувати примітиви", тоді як `mapToObj(i -> i)` є менш очевидним.
>
> **Висновок:** Для простого пакування завжди використовуйте `boxed()`.

#### **📌 Що перевіряють:**

*Здатність порівнювати два способи вирішення задачі не тільки за результатом, а й за семантикою та потенційною продуктивністю.*

* * *

### **12 ❓ Питання:**

Чи можна реалізувати `flatMap` за допомогою `mapToObj`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Вони вирішують фундаментально різні задачі.
>
> * `mapToObj` — це операція **"один-в-один"**: один примітив на вході дає один об'єкт на виході. Розмір потоку не змінюється.
> * `flatMap` — це операція **"один-в-багато"**: один елемент на вході дає цілий потік на виході, які потім об'єднуються.
>
> `mapToObj` може бути використаний як **частина** логіки для `flatMap`, але не може замінити його повністю.
>
> Наприклад, щоб перетворити `IntStream` на `Stream<String>` зі слів, ми не можемо цього зробити лише з `mapToObj`, оскільки кожен `int` не може бути "розгорнутий" у потік слів.
>
> **`mapToObj` — це перетворення елементів, `flatMap` — це перетворення структури.**

#### **📌 Що перевіряють:**

*Чітке розмежування семантики операцій `map`, `flatMap` та `mapToObj`.*

* * *

### **13 ❓ Питання:**

Покажіть на прикладі, як комбінація `flatMap` і `mapToObj` може бути корисною. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Уявімо складну структуру: у нас є `List<Department>`, кожен `Department` має `List<Employee>`, а кожен `Employee` має `int salary`. Нам потрібно отримати `Stream<String>` з зарплатами у форматі `"Salary: X"`.
>
> **Ланцюжок:**
>
> 1. `stream()` по `List<Department>`.
> 2. `flatMap` по співробітниках, щоб отримати один потік `Stream<Employee>`.
> 3. `mapToInt`, щоб отримати `IntStream` зарплат.
> 4. `mapToObj`, щоб перетворити кожну зарплату (`int`) на рядок (`String`).

#### **📝 Приклад коду:**

```java
List<Department> departments = ...;
Stream<String> salaryStrings = departments.stream() // Stream<Department>
    .flatMap(d -> d.getEmployees().stream())       // Stream<Employee>
    .mapToInt(Employee::getSalary)                 // IntStream
    .mapToObj(salary -> "Salary: " + salary);      // Stream<String>
```

#### **📌 Що перевіряють:**

*Здатність будувати складні ланцюжки перетворень, комбінуючи різні види операцій.*

* * *

### **14 ❓ Питання:**

Якщо `Stream<T>` містить `null`, що станеться при виклику `mapToInt`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **`NullPointerException`**.
>
> **Пояснення:**
>
> `mapToInt` приймає `ToIntFunction<T>`. Ця функція (наприклад, `obj -> obj.getIntValue()`) буде викликана для кожного елемента потоку.
>
> Якщо один з елементів є `null`, спроба викликати на ньому будь-який метод (`null.getIntValue()`) призведе до `NPE`.
>
> **Як цього уникнути:**
>
> Потрібно відфільтрувати `null` елементи **до** виклику `mapToInt`.
>
> ```java
> List<MyObject> listWithNulls = ...;
> int sum = listWithNulls.stream()
>                        .filter(Objects::nonNull) // Обов'язкова перевірка!
>                        .mapToInt(MyObject::getIntValue)
>                        .sum();
> ```

#### **📌 Що перевіряють:**

*Навички написання "захисного" (defensive) коду та передбачення потенційних помилок при роботі з даними, що можуть містити `null`.*

* * *

### **15 ❓ Питання:**

Уявіть, що вам потрібно реалізувати власний `boxed()` "вручну" за допомогою `mapToObj`. Як би виглядав цей код? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `boxed()` на `IntStream` є просто синтаксичним цукром для `mapToObj`, де функція-маппер виконує boxing.
>
> **Реалізація:**
>
> Потрібно просто передати в `mapToObj` функцію, яка приймає `int` і повертає `Integer`. Це можна зробити кількома способами:
>
> * Лямбда-вираз: `i -> i` (тут відбувається автопакування)
> * Посилання на метод: `Integer::valueOf`
>
> #### **Приклад:**
>
> ```java
> IntStream intStream = IntStream.range(1, 4);
> 
> // Еквівалент .boxed()
> Stream<Integer> streamOfIntegers = intStream.mapToObj(Integer::valueOf);
> ```
>
> `Integer::valueOf(int i)` є статичним методом, який приймає `int` і повертає `Integer`, тому він ідеально підходить для `mapToObj` в цьому випадку.
>
> Ця вправа демонструє, що `boxed()` є просто більш зручною та читабельною обгорткою над `mapToObj` для найпоширенішого сценарію перетворення.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як пов'язані різні методи `Stream API`, та здатність відтворити функціональність одного методу за допомогою іншого. Це показує досконале володіння API.*

* * *
