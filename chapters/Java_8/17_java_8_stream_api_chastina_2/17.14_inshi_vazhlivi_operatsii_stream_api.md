---
layout: default
title: "Інші важливі операції Stream API"
parent: "Stream API"
grand_parent: "Java 8"
nav_order: 14
---

# Інші важливі операції Stream API

* короткий опис: Набір додаткових, але часто використовуваних операцій для трансформації, фільтрації та агрегації даних: `sorted()`, `distinct()`, `limit()`, `skip()`, `allMatch()`, `noneMatch()`, `reduce()`.

* поняття: `sorted()`, `distinct()`, `limit()`, `skip()`, `allMatch()`, `noneMatch()`, `reduce()`.

* приклади: `stream.sorted().collect(...)`, `stream.distinct().forEach(...)`, `stream.skip(2).limit(3)`, `stream.allMatch(n -> n > 0)`, `stream.reduce(0, Integer::sum)`.

* ключові моменти: `sorted()` для впорядкування, `distinct()` для унікальності, `limit()`/`skip()` для обрізання, `allMatch()`/`noneMatch()` для перевірки всіх елементів, `reduce()` для агрегації.

* що важливо знати на співбесіді: Найпоширеніші операції на співбесідах.

### **✅ Офіційне визначення:**

Stream API надає набір додаткових, але критично важливих операцій для трансформації, фільтрації, агрегації та контролю потоку даних.

### **🧠 Простими словами:**

Це набір «універсальних інструментів» — обрізати, відсортувати, прибрати дублікати, підрахувати, звести до одного значення. Саме їх найчастіше люблять питати на співбесідах

---

## 📊 **Огляд важливих операцій:**

|       Метод       |     Тип     |                                  Опис                                  |
|:-----------------:|:-----------:|:----------------------------------------------------------------------:|
|  **`sorted()`**   |  Проміжна   |   Сортує елементи потоку (за **`Comparable`** або **`Comparator`**)    |
| **`distinct()`**  |  Проміжна   |                           Видаляє дублікати                            |
|  **`limit(n)`**   |  Проміжна   |                 Повертає лише перші **`n`** елементів                  |
|   **`skip(n)`**   |  Проміжна   |                   Пропускає перші **`n`** елементів                    |
| **`allMatch()`**  | Термінальна |             Перевіряє, чи всі елементи задовольняють умову             |
| **`noneMatch()`** | Термінальна |          Перевіряє, що **жоден** елемент не задовольняє умову          |
|  **`reduce()`**   | Термінальна | Агрегує елементи до одного значення за допомогою асоціативної операції |

---

## **Пояснення на пальцях (💡)**

* **`sorted()`** — «Вишикуйтесь по порядку\!»
* **`distinct()`** — «Кожен по одному, без клонів»
* **`limit()`** — «Вистачить, більше не треба»
* **`skip()`** — «Ці нецікаві, йдемо далі»
* **`allMatch()`** — «Всі підходять?»
* **`noneMatch()`** — «Чи точно нікого з такими ознаками немає?»
* **`reduce()`** — «Зведи все до одного результату: суму, добуток, конкатенацію...»

---

## **Корисні приклади (🧪)**

```java
// 🔢 Сортування чисел
List<Integer> sorted = Stream.of(5, 2, 3)
    .sorted()
    .collect(Collectors.toList()); // --> [2, 3, 5]
```

```java
// ✨ Унікальні значення
Stream.of("a", "b", "a", "c")
    .distinct()
    .forEach(System.out::println); // --> a, b, c
```

```java
// ⛔ Пропустити перші 2, взяти 3
Stream.of(1, 2, 3, 4, 5, 6)
    .skip(2)
    .limit(3)
    .forEach(System.out::println); // --> 3, 4, 5
```

```java
// ✅ Чи всі позитивні?
boolean allPositive = Stream.of(1, 2, 3)
    .allMatch(n -> n > 0); // --> true
```

```java
// ❌ Чи жоден не від'ємний?
boolean noneNegative = Stream.of(1, 0, 2)
    .noneMatch(n -> n < 0); // --> true
```

```java
// ➕ Зведення до суми
int sum = Stream.of(1, 2, 3)
    .reduce(0, Integer::sum); // --> 6
```

```java
// 🧵 Конкатенація рядків
String result = Stream.of("a", "b", "c")
    .reduce("", (a, b) -> a + b); // --> "abc"
```

---

## **Порівняння (⚖️)**

|       Метод       |     Тип     |           Результат           |              Ключова особливість               |
|:-----------------:|:-----------:|:-----------------------------:|:----------------------------------------------:|
|  **`sorted()`**   |  Проміжна   |         Новий stream          | Працює з **`Comparable`** або **`Comparator`** |
| **`distinct()`**  |  Проміжна   |         Новий stream          |                Усуває дублікати                |
|   **`limit()`**   |  Проміжна   |         Новий stream          |            Обрізає до `n` елементів            |
|   **`skip()`**    |  Проміжна   |         Новий stream          |               Ігнорує перші `n`                |
| **`allMatch()`**  | Термінальна |         **`boolean`**         |        Усі елементи відповідають умові         |
| **`noneMatch()`** | Термінальна |         **`boolean`**         |       Жоден елемент не відповідає умові        |
|  **`reduce()`**   | Термінальна | **`T`** або **`Optional<T>`** |         Зводить усі елементи до одного         |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Sort, Skip, Scan, Squash\!**  
(sorted, skip, allMatch, reduce)

Або:  
**"Лімітуй, скіпай, унікалізуй, перевіряй та зводь до суті\!"**

---

## **🟩 Підсумок:**

* Це **базовий набір** часто використовуваних методів, які тестують на співбесідах.
* Комбінуються з іншими операціями для гнучкої обробки даних.
* **`reduce()`** — найскладніший, але найпотужніший для агрегації.
* **`sorted()`** і **`distinct()`** змінюють потік, **`allMatch()`** та **`noneMatch()`** — перевіряють.
* Важливо знати: **всі ці методи — частина функціонального стилю програмування в Java.**

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Stream API Methods**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/Stream.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке операція `distinct()` і як вона визначає унікальність?

> #### **💬 Відповідь:**
>
> **`distinct()`** — це **`stateful` проміжна операція**, яка видаляє дублікати з потоку, залишаючи лише унікальні елементи.
>
> **Як вона визначає унікальність:**
> `distinct()` використовує методи **`hashCode()`** та **`equals()`** об'єктів для визначення їх унікальності.
>
> * Спочатку порівнюються `hashCode()` двох об'єктів.
> * Якщо хеш-коди однакові, викликається `equals()` для остаточного підтвердження рівності.
>
> Це означає, що для правильної роботи `distinct()` з кастомними об'єктами, у відповідних класах мають бути **коректно перевизначені `hashCode()` та `equals()`**.
>
> **Як працює під капотом:**
> `distinct()` є `stateful` операцією, тому що їй потрібно зберігати стан — **набір усіх унікальних елементів**, які вона вже зустріла, щоб порівнювати з ними нові елементи. Зазвичай для цього використовується внутрішня структура на кшталт `HashSet`.

#### **📝 Приклад:**

```java
Stream.of("A", "B", "A", "C", "B")
      .distinct()
      .forEach(System.out::print); // Виведе: ABC
```

#### **📌 Що перевіряють:**

*Знання операції для видалення дублікатів та розуміння її залежності від контракту `hashCode`/`equals`. Це пов'язує `Stream API` з фундаментальними концепціями Java.*

* * *

### **2 ❓ Питання:**

Поясніть, як працює операція `sorted()`.

> #### **💬 Відповідь:**
>
> **`sorted()`** — це також **`stateful` проміжна операція**, яка сортує елементи потоку. Вона має дві версії:
>
> 1. **`sorted()` (без аргументів):**
>     * Сортує елементи за їхнім **природним порядком**.
>     * Це вимагає, щоб елементи в потоці реалізовували інтерфейс **`Comparable`**.
>     * Приклад: сортування чисел або рядків.
>
> 2. **`sorted(Comparator<T> comparator)`:**
>     * Сортує елементи за допомогою наданого **`Comparator`**.
>     * Це дозволяє реалізувати будь-яку кастомну логіку сортування (напр., за довжиною рядка, за певним полем об'єкта).
>
> **Важливий аспект продуктивності:**
> `sorted()` є `stateful` операцією, яка **вимагає буферизації**. Щоб відсортувати потік, вона змушена спочатку **"побачити" всі його елементи**, зберегти їх у внутрішньому буфері, відсортувати, і лише потім передати далі по конвеєру. Це може бути дорогою операцією для великих потоків.

#### **📝 Приклад:**

```java
// За природним порядком
List.of(5, 2, 4, 1, 3).stream().sorted().forEach(System.out::print); // 12345

// З Comparator (за спаданням)
List.of(5, 2, 4).stream().sorted(Comparator.reverseOrder()).forEach(System.out::print); // 542
```

#### **📌 Що перевіряють:**

*Знання операції сортування та її залежності від `Comparable`/`Comparator`. Розуміння, що `sorted()` є ресурсомісткою `stateful` операцією.*

* * *

### **3 ❓ Питання:**

Чим відрізняються операції `limit()` та `skip()` і як їх можна використовувати разом?

> #### **💬 Відповідь:**
>
> `limit()` та `skip()` — це `stateful` проміжні операції, які дозволяють працювати з частиною потоку ("обрізати" його).
>
> * **`limit(long maxSize)`:**
>   * **Обмежує** потік, повертаючи новий потік, що містить не більше, ніж `maxSize` **перших** елементів.
>   * Є `short-circuiting` операцією.
>
> * **`skip(long n)`:**
>   * **Пропускає** перші `n` елементів потоку і повертає новий потік, що складається з решти елементів.
>
> **Спільне використання:**
> Їх можна комбінувати для реалізації **пагінації**.
>
> **Задача:** Отримати елементи для другої сторінки, якщо на кожній сторінці по 10 елементів.
> Це означає, що нам потрібно пропустити перші 10 елементів і взяти наступні 10.
>
> ```java
> int pageSize = 10;
> int pageNumber = 2;
> long elementsToSkip = (pageNumber - 1) * pageSize;
> 
> List<Item> page2 = allItems.stream()
>                            .skip(elementsToSkip) // Пропускаємо перші 10
>                            .limit(pageSize)      // Беремо наступні 10
>                            .collect(Collectors.toList());
> ```

#### **📌 Що перевіряють:**

*Знання операцій для роботи з підмножинами потоків та їхнє практичне застосування для таких завдань, як пагінація.*

* * *

### **4 ❓ Питання:**

Поясніть різницю між `anyMatch()`, `allMatch()` та `noneMatch()`.

> #### **💬 Відповідь:**
>
> Це три **термінальні операції** з **коротким замиканням (short-circuiting)**, які перевіряють, чи відповідають елементи потоку заданій умові (`Predicate`). Усі вони повертають `boolean`.
>
> * **`anyMatch(Predicate p)` ("хоча б один"):**
>   * Повертає `true`, якщо **хоча б один** елемент потоку задовольняє умові.
>   * Зупиняється на першому ж знайденому збігу.
>
> * **`allMatch(Predicate p)` ("всі без винятку"):**
>   * Повертає `true`, якщо **всі** елементи потоку задовольняють умові.
>   * Зупиняється і повертає `false` на першому ж елементі, що **не** задовольняє умові.
>
> * **`noneMatch(Predicate p)` ("жоден"):**
>   * Повертає `true`, якщо **жоден** з елементів потоку **не** задовольняє умові.
>   * Зупиняється і повертає `false` на першому ж елементі, що **задовольняє** умові.

#### **📝 Приклад:**

```java
List<Integer> nums = List.of(2, 4, 6, 8, 9);
Predicate<Integer> isEven = n -> n % 2 == 0;

boolean anyEven = nums.stream().anyMatch(isEven);    // true
boolean allEven = nums.stream().allMatch(isEven);    // false (через 9)
boolean noneOdd = nums.stream().noneMatch(n -> n % 2 != 0); // false (через 9)
```

#### **📌 Що перевіряють:**

*Розуміння семантичної різниці між трьома основними операціями перевірки та їхньої `short-circuiting` поведінки.*

* * *

### **5 ❓ Питання:**

Що таке операція `reduce()` і для чого вона використовується?

> #### **💬 Відповідь:**
>
> **`reduce()`** — це термінальна операція, яка виконує операцію **"згортання" (редукції)**. Вона обробляє всі елементи потоку і "зводить" їх до **одного єдиного значення**.
>
> `reduce()` — це узагальнена операція агрегації. Багато інших операцій (`sum`, `count`, `min`, `max`) є її спеціалізованими випадками.
>
> Найпоширеніша версія `reduce` приймає два аргументи:
>
> 1. **`identity` (початкове значення):** Значення, з якого починається операція, і яке повертається, якщо потік порожній.
> 2. **`accumulator` (`BinaryOperator<T>`):** Функція, яка приймає проміжний результат та наступний елемент, і повертає новий проміжний результат.
>
> **Приклади використання:**
>
> * Знаходження суми/добутку чисел.
> * Об'єднання рядків в один.
> * Пошук максимального елемента.

#### **📝 Приклад (сума чисел):**

```java
List<Integer> numbers = List.of(1, 2, 3, 4);

// 0 - початкове значення
// (subtotal, element) -> subtotal + element - акумулятор
int sum = numbers.stream().reduce(0, (subtotal, element) -> subtotal + element);
// або з посиланням на метод: reduce(0, Integer::sum);
System.out.println(sum); // 10
```

#### **📌 Що перевіряють:**

*Знання однієї з найпотужніших та найгнучкіших операцій в `Stream API` та вміння її застосовувати для агрегації даних.*

* * *

### **6 ❓ Питання:**

У чому різниця між трьома перевантаженими версіями методу `reduce()`? ⭐️

> #### **💬 Відповідь:**
>
> 1. **`Optional<T> reduce(BinaryOperator<T> accumulator)`:**
>     * Ця версія **не приймає початкового значення**.
>     * Вона повертає **`Optional<T>`**, тому що якщо потік порожній, результату не буде (`Optional.empty`).
>
> 2. **`T reduce(T identity, BinaryOperator<T> accumulator)`:**
>     * Це **найпоширеніша версія**.
>     * Приймає початкове значення (`identity`), тому результат завжди є.
>     * Повертає значення типу **`T`**, а не `Optional`. Якщо потік порожній, поверне `identity`.
>
> 3. **`<U> U reduce(U identity, BiFunction<U, ? super T, U> accumulator, BinaryOperator<U> combiner)`:**
>     * Це найскладніша версія, призначена в основному для **паралельних потоків**.
>     * `accumulator` може працювати з різними типами (`T` елемента потоку та `U` проміжного результату).
>     * `combiner` — це функція, яка знає, як об'єднати два проміжні результати, отримані в різних потоках.

#### **📌 Що перевіряють:**

*Глибоке знання API та здатність обрати правильну версію `reduce` для конкретної задачі, розуміючи різницю в типах, що повертаються, та поведінці.*

* * *

### **7 ❓ Питання:**

Чому `distinct()` і `sorted()` є `stateful` операціями? Як це впливає на продуктивність? ⭐️

> #### **💬 Відповідь:**
>
> **Stateful-операції** — це операції, яким для роботи потрібно **зберігати стан** (інформацію про елементи, які вони вже обробили).
>
> * **`distinct()`**: Щоб визначити, чи є поточний елемент унікальним, `distinct()` повинен пам'ятати **всі унікальні елементи**, які вже зустрічалися. Для цього він зазвичай використовує внутрішній `HashSet`.
>
> * **`sorted()`**: Щоб відсортувати потік, `sorted()` повинен спочатку **отримати та зберегти всі його елементи** у внутрішній буфер (напр., `ArrayList`), відсортувати цей буфер, і лише потім передати елементи далі по конвеєру.
>
> **Вплив на продуктивність:**
>
> 1. **Споживання пам'яті:** Ці операції можуть споживати багато пам'яті, оскільки їм потрібно буферизувати елементи. Для дуже великих потоків це може призвести до `OutOfMemoryError`.
> 2. **Проблеми з паралелізмом:** Робота зі спільним станом (як `Set` для `distinct()`) вимагає синхронізації, що може погіршити продуктивність у паралельних потоках.
> 3. **Втрата лінивості:** `sorted()` "ламає" повністю лінивий потік, оскільки вимагає повного проходження по даних перед тим, як віддати хоча б один елемент.

#### **📌 Що перевіряють:**

*Розуміння, що не всі проміжні операції однаково "дешеві". Кандидат повинен знати про ресурсомісткість `stateful`-операцій.*

* * *

### **8 ❓ Питання:**

Як `short-circuiting` операції (`anyMatch`, `limit`) працюють з нескінченними потоками? ⭐️

> #### **💬 Відповідь:**
>
> Операції з коротким замиканням (short-circuiting) є **єдиним способом** отримати кінцевий результат з нескінченного потоку.
>
> **Як це працює:**
> `short-circuiting` операція може завершити обробку, **не перебираючи всі елементи потоку**.
>
> * **Приклад з `anyMatch`:**
>
>     ```java
>     boolean hasEven = Stream.iterate(1, n -> n + 1) // 1, 2, 3, 4, ...
>                              .anyMatch(n -> n % 2 == 0); // true
>     ```
>
>     Тут потік згенерує `1`, перевірить умову (`false`), потім згенерує `2`, перевірить умову (`true`), і `anyMatch` негайно завершить роботу, повернувши `true`. Потік не буде генерувати `3, 4, 5...`
>
> * **Приклад з `limit`:**
>
>     ```java
>     Stream.generate(Math::random).limit(5).forEach(...)
>     ```
>
>     Тут `limit(5)` "говорить" потоку, що після генерації 5 елементів роботу можна припинити.
>
> Без таких операцій робота з нескінченним потоком призвела б до нескінченного циклу.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як лінивість та `short-circuiting` роблять можливим роботу з нескінченними послідовностями.*

* * *

### **9 ❓ Питання:**

Яким буде результат `reduce`, якщо потік порожній? Розгляньте дві версії методу. ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від того, яку версію `reduce` ви використовуєте.
>
> * **`reduce(T identity, BinaryOperator<T> accumulator)`:**
>   * Ця версія приймає **початкове значення (`identity`)**.
>   * Якщо потік порожній, вона просто **поверне це початкове значення**.
>   * Приклад: `Stream.<Integer>empty().reduce(0, Integer::sum)` поверне `0`.
>
> * **`reduce(BinaryOperator<T> accumulator)`:**
>   * Ця версія **не має початкового значення**.
>   * Оскільки потік порожній і початкового значення немає, вона **не може повернути результат**.
>   * Тому вона повертає **порожній `Optional<T>`** (`Optional.empty()`), сигналізуючи про відсутність результату.
>
> Це чудовий приклад того, як `Optional` використовується для безпечної обробки випадків, коли значення може бути відсутнім.

#### **📌 Що перевіряють:**

*Уважність до деталей API та розуміння ролі `Optional` при роботі з операціями, результат яких не гарантований.*

* * *

### **10 ❓ Питання:**

Яку операцію краще використовувати для перевірки, чи потік не порожній: `count() > 0` чи `findFirst().isPresent()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва підходи дадуть правильний результат, **`findFirst().isPresent()`** (або, що ще краще, `!isEmpty()` у Java 11+) є значно **продуктивнішим** і правильнішим з точки зору ідеології стрімів.
>
> **Чому `findFirst().isPresent()` краще:**
>
> * `findFirst()` — це **`short-circuiting`** операція. Вона припинить роботу, як тільки знайде **перший** елемент. Для перевірки, чи не порожній потік, цього достатньо.
>
> **Чому `count() > 0` гірше:**
>
> * `count()` — це **повна термінальна операція**. Вона **змушена пройти по всьому потоку** і порахувати всі елементи, щоб дати відповідь. Для великого або нескінченного потоку це буде вкрай неефективно.
>
> Якщо ж метою є лише перевірка наявності хоч одного елемента, що відповідає умові, то найкращою операцією буде **`anyMatch(predicate)`**.

#### **📌 Що перевіряють:**

*Здатність обирати найефективнішу операцію для вирішення задачі, розуміючи наслідки для продуктивності.*

* * *

### **11 ❓ Питання:**

Як за допомогою операцій `Stream API` об'єднати список рядків в один рядок через кому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього є два основних способи:
>
> **1. За допомогою `Collectors.joining(delimiter)` (кращий та ідіоматичний підхід):**
> У класі `Collectors` є спеціальний колектор, розроблений саме для цієї задачі.
>
> ```java
> List<String> list = List.of("a", "b", "c");
> String result = list.stream().collect(Collectors.joining(", "));
> // result буде "a, b, c"
>    ```
>
> Він також має версії з префіксом та суфіксом: `joining(",", "[", "]")` -> `"[a,b,c]"`.
>
> **2. За допомогою `reduce()` (більш універсальний, але менш спеціалізований):**
> Можна використовувати `reduce`, але код буде трохи складнішим.
>
> ```java
> Optional<String> resultOpt = list.stream()
>     .reduce((s1, s2) -> s1 + ", " + s2);
> 
> String result = resultOpt.orElse("");
>    ```
>
> Тут є нюанси: `reduce` повертає `Optional`, і можуть бути проблеми з зайвою комою на початку або в кінці. Тому `Collectors.joining()` є кращим вибором.

#### **📌 Що перевіряють:**

*Знання стандартних колекторів та вміння обирати найбільш підходящий та спеціалізований інструмент для конкретної задачі.*

* * *

### **12 ❓ Питання:**

Що таке операція `distinct` відносно `Set`, а `sorted` відносно `TreeSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`distinct` та `sorted` — це по суті реалізації функціональності `Set` та `SortedSet` у контексті `Stream API`**.
>
> * **`distinct()` та `Set`:**
>     Обидва гарантують **унікальність** елементів. Обидва використовують **`hashCode()`** та **`equals()`** для визначення унікальності. Можна сказати, що `stream.distinct().collect(Collectors.toList())` є функціональним еквівалентом створення `new ArrayList<>(new HashSet<>(list))`.
>
> * **`sorted()` та `TreeSet` / `SortedSet`:**
>     Обидва гарантують **відсортований порядок**. Обидва покладаються або на **`Comparable`**, або на переданий **`Comparator`**.
>
> Таким чином, `Stream API` дозволяє застосувати семантику `Set` та `SortedSet` до будь-якої послідовності даних "на льоту", не створюючи при цьому проміжних колекцій вручну.

#### **📌 Що перевіряють:**

*Здатність проводити аналогії та бачити концептуальні зв'язки між різними частинами `Collections` та `Stream API`.*

* * *

### **13 ❓ Питання:**

Поясніть, як працює перевантажена версія `reduce` з трьома аргументами. Навіщо потрібен `combiner`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Версія **`reduce(identity, accumulator, combiner)`** призначена в основному для ефективної роботи в **паралельних потоках**.
>
> * **`identity`**: Початкове значення.
> * **`accumulator` (`BiFunction`)**: Функція, яка приймає проміжний результат і наступний елемент, і додає його до проміжного результату. Використовується всередині кожного потоку.
> * **`combiner` (`BinaryOperator`)**: Функція, яка знає, як **об'єднати два проміжні результати**, отримані в різних потоках.
>
> **Як це працює:**
>
> 1. Потік розбивається на частини.
> 2. Кожен потік виконує редукцію своєї частини, починаючи з `identity` і використовуючи `accumulator`.
> 3. Коли всі потоки завершили свою роботу, `combiner` об'єднує їхні результати в один фінальний.
>
> **Приклад:** Обчислити довжину всіх рядків.
>
> ```java
> int totalLength = list.parallelStream().reduce(
>     0,                                 // identity
>     (sum, str) -> sum + str.length(), // accumulator (int, String) -> int
>     (sum1, sum2) -> sum1 + sum2       // combiner (int, int) -> int
> );
> ```

#### **📌 Що перевіряють:**

*Експертне розуміння механізму паралельної редукції. Це глибоке питання, що перевіряє знання `Fork/Join` фреймворку.*

* * *

### **14 ❓ Питання:**

У чому може бути проблема з `distinct()` на великих або нескінченних потоках? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Проблема полягає в тому, що `distinct()` — це **`stateful`** операція.
>
> **Щоб гарантувати унікальність**, їй потрібно **зберігати** кожен унікальний елемент, який вона вже зустріла, щоб порівнювати з ним нові. Для цього вона використовує внутрішню структуру даних, схожу на `HashSet`.
>
> **Наслідки:**
>
> * **Для великих потоків:** Це може призвести до значного **споживання пам'яті**. Якщо потік містить мільйони унікальних елементів, внутрішній `HashSet` також буде містити мільйони елементів. Це може призвести до `OutOfMemoryError`.
>
> * **Для нескінченних потоків:** Це призведе до **нескінченного росту** цього внутрішнього стану і гарантованого `OutOfMemoryError`. `distinct()` на нескінченному потоці (без `limit` перед ним) використовувати не можна.
>
> Тому, хоча `distinct()` є зручним, потрібно пам'ятати про його вимоги до пам'яті.

#### **📌 Що перевіряють:**

*Здатність аналізувати не тільки часову, а й просторову складність операцій. Це ознака досвідченого інженера, який думає про ресурси.*

* * *

### **15 ❓ Питання:**

Які вимоги до функцій, що передаються в `reduce`, для коректної роботи в паралельному режимі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для того, щоб паралельна редукція працювала коректно і давала передбачуваний результат, функції-акумулятор та комбайнер повинні відповідати кільком математичним властивостям.
>
> **Основні вимоги:**
>
> 1. **Асоціативність:** Операція комбінування не повинна залежати від порядку групування.
>     `(a * b) * c` повинно дорівнювати `a * (b * c)`.
>     * **Додавання** та **множення** є асоціативними.
>     * **Віднімання** — ні. `(5 - 3) - 2 != 5 - (3 - 2)`
>
> 2. **Відсутність стану (Stateless):** Функції не повинні покладатися на зовнішній стан або змінювати його.
>
> 3. **Ідентичність (для комбайнера):** Початкове значення (`identity`) має бути "нейтральним" елементом для операції комбінування.
>     `combiner.apply(identity, x)` повинно дорівнювати `x`.
>     * Для суми — це `0` (`0 + x = x`).
>     * Для множення — це `1` (`1 * x = x`).
>
> Якщо ці правила не дотримуються, результат паралельної редукції буде **непередбачуваним** і залежатиме від того, як `ForkJoinPool` розбив завдання.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про вимоги паралельного програмування. Це питання перевіряє не тільки знання API, а й фундаментальних принципів, що лежать в його основі.*

* * *
