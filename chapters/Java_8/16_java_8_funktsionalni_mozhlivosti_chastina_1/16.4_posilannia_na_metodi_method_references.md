---
layout: default
title: "Посилання на методи (Method References)"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 4
---

# Посилання на методи (Method References)

* короткий опис: Посилання на метод – це скорочений запис лямбда-виразів, що дозволяє посилатися на вже існуючий метод, використовуючи оператор `::`.

* поняття: Method reference, оператор `::`.

* приклади: `Integer::parseInt` (статичний метод), `System.out::println` (метод екземпляра конкретного об'єкта), `String::toUpperCase` (метод екземпляра через ім'я типу), `ArrayList::new` (конструктор).

* ключові моменти: Існує 4 типи посилань. Підвищують читабельність коду, еквівалентні простим лямбда-виразам.

* що важливо знати на співбесіді: Чотири типи посилань, синтаксис, коли використовувати.

### **✅ Офіційне визначення:**

**Посилання на метод** — це скорочений запис лямбда-виразів, що дозволяє посилатися на вже існуючий метод, використовуючи оператор **`::`**

### **🧠 Простими словами:**

Замість того, щоб писати лямбду, яка просто викликає метод, можна напряму сказати: "Ось цей метод, використовуй його". Це робить код чистішим і легшим для читання

---

## **Всього є 4 основних типи:**

|                       Тип посилання                       |            Синтаксис            |                             Опис                              |          Приклад          |
|:---------------------------------------------------------:|:-------------------------------:|:-------------------------------------------------------------:|:-------------------------:|
|           **1\. Посилання на статичний метод**            |  **`ClassName::staticMethod`**  |                Виклик статичного методу класу                 |  **`Integer::parseInt`**  |
| **2\. Посилання на метод екземпляра конкретного об'єкта** | **`instance::instanceMethod`**  |               Виклик методу конкретного об’єкта               | **`System.out::println`** |
|   **3\. Посилання на метод екземпляра через ім'я типу**   | **`ClassName::instanceMethod`** | Виклик методу екземпляра через тип (перший параметр — об'єкт) | **`String::toUpperCase`** |
|             **4\. Посилання на конструктор**              |      **`ClassName::new`**       |          Створення нового об'єкта через конструктор           |   **`ArrayList::new`**    |

---

## **Пояснення на пальцях (💡)**

Уяви, що лямбда — це як розповідь: "бери цей метод і роби так". А посилання на метод — це прямий шлях: "візьми саме цей метод, нічого не вигадуй"

---

## **Корисні приклади (🧪)**

```java
// 1. Статичний метод
Function<String, Integer> parser = Integer::parseInt;
System.out.println(parser.apply("123")); // 123

// 2. Метод екземпляра конкретного об'єкта
Consumer<String> printer = System.out::println;
printer.accept("Hello Method Reference!");

// 3. Метод екземпляра через ім'я типу
Function<String, String> toUpper = String::toUpperCase;
System.out.println(toUpper.apply("hello")); // HELLO

// 4. Конструктор
Supplier<List<String>> listSupplier = ArrayList::new;
List<String> list = listSupplier.get();
```

---

## **Порівняння (⚖️)**

* Лаконічний і читабельний код
* Можна замінити складні лямбда-вирази простим посиланням
* Зручність, коли потрібен простий виклик існуючого методу

---

## **🧠 Мнемоніка для запам’ятовування:**

🚀 **`::` — це ніби телепорт до методу, без зайвих слів\!**

---

## **🟩 Підсумок:**

* **Посилання на методи** — це більш читабельний варіант лямбда-виразів, коли треба просто викликати метод.
* **Існує 4 типи посилань:** статичні методи, методи конкретних об’єктів, методи екземпляра через клас і конструктори.
* **Застосовуються** для підвищення чистоти та зрозумілості коду.
* На співбесідах важливо знати синтаксис, типи і коли їх застосовувати.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке посилання на метод (Method Reference) в Java і яка його головна перевага над лямбда-виразом?

> #### **💬 Відповідь:**
>
> **Посилання на метод (Method Reference)** — це спеціальний, дуже компактний синтаксис лямбда-виразу, який дозволяє посилатися на існуючий метод за його іменем. Замість того, щоб надавати реалізацію, ви просто вказуєте, який метод потрібно викликати.
>
> Для посилань на методи використовується оператор **`::`** (подвійна двокрапка).
>
> **Головна перевага:**
> **Читабельність коду.** Якщо лямбда-вираз не робить нічого, окрім виклику одного існуючого методу, посилання на метод робить код значно чистішим та більш декларативним. Воно говорить "що" робити, а не "як".

#### **📝 Аналогія:**

* **Лямбда-вираз:** Це як детальна інструкція: "Візьми об'єкт `s` і виклич на ньому метод `toUpperCase()`".
  * `s -> s.toUpperCase()`
* **Посилання на метод:** Це як коротке посилання: "Використовуй метод `toUpperCase` з класу `String`".
  * `String::toUpperCase`

#### **📌 Що перевіряють:**

*Базове розуміння концепції. Кандидат повинен знати, що посилання на метод — це синтаксичний цукор для простих лямбд, що покращує читабельність.*

* * *

### **2 ❓ Питання:**

Які існують чотири основні типи посилань на методи?

> #### **💬 Відповідь:**
>
> У Java існує чотири типи посилань на методи, які покривають різні сценарії викликів:
>
> 1. **Посилання на статичний метод (Reference to a static method):**
>     * **Синтаксис:** `ClassName::staticMethodName`
>     * **Призначення:** Для виклику статичних методів.
>
> 2. **Посилання на метод екземпляра конкретного об'єкта (Reference to an instance method of a particular object):**
>     * **Синтаксис:** `instanceReference::instanceMethodName`
>     * **Призначення:** Для виклику методу на конкретному, вже існуючому екземплярі.
>
> 3. **Посилання на метод екземпляра довільного об'єкта певного типу (Reference to an instance method of an arbitrary object of a particular type):**
>     * **Синтаксис:** `ClassName::instanceMethodName`
>     * **Призначення:** Для виклику методу на об'єкті, який буде переданий як перший параметр лямбди.
>
> 4. **Посилання на конструктор (Reference to a constructor):**
>     * **Синтаксис:** `ClassName::new`
>     * **Призначення:** Для створення нових екземплярів класу.

#### **📝 Коментар:**

Розуміння цих чотирьох типів є ключовим для ефективного використання посилань на методи.

#### **📌 Що перевіряють:**

*Знання основної класифікації. Це фундаментальне питання по цій темі.*

* * *

### **3 ❓ Питання:**

Наведіть приклад посилання на статичний метод.

> #### **💬 Відповідь:**
>
> Цей тип посилання використовується, коли лямбда-вираз просто викликає статичний метод, передаючи йому свої параметри.
>
> **Сценарій:** У нас є список рядків, що містять числа, і ми хочемо перетворити його на список цілих чисел.
>
> **Лямбда-вираз:** `s -> Integer.parseInt(s)`
> Ця лямбда приймає рядок `s` і передає його у статичний метод `Integer.parseInt()`.
>
> **Посилання на метод:** `Integer::parseInt`
> Це більш компактний і читабельний еквівалент.

#### **📝 Приклад:**

```java
List<String> stringNumbers = Arrays.asList("1", "2", "3");

List<Integer> intNumbers = stringNumbers.stream()
                                        .map(Integer::parseInt) // Використовуємо посилання на статичний метод
                                        .collect(Collectors.toList());

System.out.println(intNumbers); // [1, 2, 3]
```

#### **📌 Що перевіряють:**

*Вміння розпізнавати та застосовувати найпростіший тип посилань на методи.*

* * *

### **4 ❓ Питання:**

Наведіть приклад посилання на метод екземпляра конкретного об'єкта.

> #### **💬 Відповідь:**
>
> Цей тип використовується, коли лямбда викликає метод на **конкретному, вже існуючому об'єкті**.
>
> **Сценарій:** У нас є список рядків, і ми хочемо вивести кожен з них у консоль.
>
> **Лямбда-вираз:** `s -> System.out.println(s)`
> Ця лямбда викликає метод `println()` на конкретному об'єкті `System.out`.
>
> **Посилання на метод:** `System.out::println`

#### **📝 Приклад:**

```java
List<String> names = Arrays.asList("Alice", "Bob", "Charlie");

names.forEach(System.out::println); // Використовуємо посилання на метод екземпляра

// Виведе:
// Alice
// Bob
// Charlie
```

#### **📌 Що перевіряють:**

*Розуміння другого типу посилань, де об'єкт, на якому викликається метод, є фіксованим.*

* * *

### **5 ❓ Питання:**

Поясніть, як працює посилання на метод екземпляра довільного об'єкта певного типу (`ClassName::instanceMethod`).

> #### **💬 Відповідь:**
>
> Це найменш інтуїтивний, але дуже потужний тип посилань на методи.
>
> **Як це працює:**
>
> Він використовується, коли лямбда-вираз приймає **один параметр**, і все, що вона робить — це викликає метод **на цьому ж самому параметрі**.
>
> **Сценарій:** Перетворити список рядків у верхній регістр.
>
> **Лямбда-вираз:** `s -> s.toUpperCase()`
> Тут `s` — це і параметр, і об'єкт, на якому викликається метод.
>
> **Посилання на метод:** `String::toUpperCase`
> Компілятор розуміє, що перший параметр лямбди (у випадку `Function<String, String>` це `String`) слід використати як об'єкт, на якому буде викликано метод `toUpperCase()`.

#### **📝 Приклад:**

```java
List<String> words = Arrays.asList("hello", "world");

List<String> upperWords = words.stream()
                                .map(String::toUpperCase) // Посилання на метод екземпляра
                                .collect(Collectors.toList());

System.out.println(upperWords); // [HELLO, WORLD]
```

#### **📌 Що перевіряють:**

*Знання та розуміння найскладнішого з чотирьох типів посилань. Це часте питання на співбесідах.*

* * *

### **6 ❓ Питання:**

Як використовується посилання на конструктор? Наведіть приклад.

> #### **💬 Відповідь:**
>
> **Посилання на конструктор (`ClassName::new`)** використовується, коли лямбда-вираз має створювати новий екземпляр класу. Це по суті посилання на `public ClassName()`.
>
> Цільовий функціональний інтерфейс повинен мати метод, що відповідає сигнатурі одного з конструкторів.
>
> **Сценарій 1 (без аргументів):** Використовується з інтерфейсом `Supplier`.
>
> * `() -> new ArrayList<>()` еквівалентно `ArrayList::new`.
>
> **Сценарій 2 (з аргументами):** Використовується з інтерфейсом `Function` (або іншим, що відповідає).
>
> * `name -> new User(name)` еквівалентно `User::new`.

#### **📝 Приклад:**

```java
// Створення колекції за допомогою посилання на конструктор
List<String> names = Arrays.asList("Alice", "Bob");
Set<String> nameSet = names.stream()
                           .collect(Collectors.toCollection(HashSet::new));

// Перетворення списку ID в список користувачів
List<Long> ids = Arrays.asList(1L, 2L, 3L);
List<User> users = ids.stream()
                      .map(User::new) // Викликає конструктор User(Long id)
                      .collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Розуміння того, як посилання на методи можуть використовуватися для створення об'єктів, особливо в контексті Stream API.*

* * *

### **7 ❓ Питання:**

Перетворіть наступні лямбда-вирази на посилання на методи, якщо це можливо.

> #### **💬 Відповідь:**
>
> **1. `str -> str.length()`**
> ***Посилання на метод:** `String::length`
>
> * (Тип 3: метод екземпляра довільного об'єкта)
>
> **2. `() -> Thread.currentThread()`**
> ***Посилання на метод:** `Thread::currentThread`
>
> * (Тип 1: статичний метод)
>
> **3. `user -> user.getName().toUpperCase()`**
>
> * **Перетворити неможливо.** Це не один, а два виклики методів (`getName()` та `toUpperCase()`). Посилання на метод можна використовувати лише для одного виклику.
>
> **4. `String s = "hello"; s -> s.isEmpty()`**
> *Припустимо, `s` - це об'єкт, визначений ззовні. Якщо лямбда така `() -> s.isEmpty()`
>
> * **Посилання на метод:** `s::isEmpty`
> * (Тип 2: метод екземпляра конкретного об'єкта `s`)

#### **📌 Що перевіряють:**

*Практичні навички ідентифікації сценаріїв, де можна і де не можна використовувати посилання на методи. Питання про неможливість перетворення є важливим для перевірки розуміння обмежень.*

* * *

### **8 ❓ Питання:**

Коли неможливо замінити лямбда-вираз посиланням на метод? ⭐️

> #### **💬 Відповідь:**
>
> Замінити лямбда-вираз на посилання на метод неможливо, якщо тіло лямбди містить **будь-яку додаткову логіку**, крім **одного-єдиного виклику методу**.
>
> **Приклади, коли заміна неможлива:**
>
> * **Ланцюжок викликів:**
>   * `s -> s.trim().toUpperCase()` — тут два виклики.
>
> * **Математичні чи логічні операції:**
>   * `x -> x * 2 + 1`
>
> * **Створення об'єкта і виклик методу:**
>   * `s -> new StringBuilder(s).reverse().toString()`
>
> * **Явна обробка параметрів:**
>   * `(x, y) -> Math.max(y, x)` — параметри передаються в іншому порядку.
>
> По суті, посилання на метод — це лише синтаксичний цукор для найпростіших лямбд. Як тільки з'являється будь-яка логіка, крім простого виклику, потрібно використовувати повний лямбда-вираз.

#### **📌 Що перевіряють:**

*Розуміння обмежень посилань на методи. Це демонструє, що кандидат не просто механічно застосовує синтаксис, а розуміє, коли він доречний.*

* * *

### **9 ❓ Питання:**

Як Java обирає правильний метод, якщо існують перевантажені (overloaded) методи? ⭐️

> #### **💬 Відповідь:**
>
> Компілятор Java використовує **контекст**, а саме — **сигнатуру методу цільового функціонального інтерфейсу**, щоб визначити, який з перевантажених методів потрібно викликати.
>
> Процес вибору виглядає так:
>
> 1. Компілятор аналізує цільовий тип (напр., `Function<String, Integer>`).
> 2. Він визначає сигнатуру його абстрактного методу (у цьому випадку `Integer apply(String s)`).
> 3. Далі він шукає серед перевантажених методів той, який **відповідає** цій сигнатурі (приймає `String` і повертає `int`/`Integer`).
>
> **Якщо** існує лише один відповідний метод, компілятор вибере його. **Якщо** існує кілька відповідних методів або жодного, ви отримаєте **помилку компіляції** через неоднозначність.

#### **📝 Приклад:**

```java
class OverloadExample {
    public static void print(Object o) { /*...*/ }
    public static void print(String s) { /*...*/ }
}

Consumer<String> consumer = OverloadExample::print;
// Компілятор обере `print(String s)`, оскільки метод функціонального інтерфейсу 
// `Consumer<String>` це `void accept(String s)`, і цей варіант підходить краще.
```

#### **📌 Що перевіряють:**

*Глибокі знання роботи компілятора та механізму виведення типів. Це демонструє розуміння того, як Java розв'язує неоднозначності.*

* * *

### **10 ❓ Питання:**

У чому різниця між посиланнями `list::add` та `List::add`? Чи обидва є валідними? ⭐️

> #### **💬 Відповідь:**
>
> Ці два посилання є валідними, але належать до різних типів і використовуються в різних контекстах.
>
> **1. `list::add`**
>
> * **Тип:** Посилання на метод **екземпляра конкретного об'єкта** (Тип 2).
> * **Об'єкт:** `list` — це вже існуючий екземпляр, наприклад, `List<String> list = new ArrayList<>();`.
> * **Еквівалентна лямбда:** `element -> list.add(element)`
> * **Цільовий інтерфейс:** `Consumer<String>` (приймає елемент, нічого не повертає).
>
> **2. `List::add`**
>
> * **Тип:** Посилання на метод **екземпляра довільного об'єкта певного типу** (Тип 3).
> * **Об'єкт:** Тут немає конкретного списку. Об'єкт списку буде передано як перший параметр лямбди.
> * **Еквівалентна лямбда:** `(listInstance, element) -> listInstance.add(element)`
> * **Цільовий інтерфейс:** `BiConsumer<List<String>, String>` (приймає два аргументи).
>
> Отже, вони вирішують різні задачі і вимагають різних функціональних інтерфейсів.

#### **📌 Що перевіряють:**

*Глибоке розуміння відмінностей між типами посилань, особливо між другим та третім типом, що є найпоширенішим джерелом плутанини.*

* * *

### **11 ❓ Питання:**

Наведіть приклад, де посилання на метод значно покращує читабельність коду в Stream API. ⭐️

> #### **💬 Відповідь:**
>
> Розглянемо сценарій, де потрібно зі списку користувачів отримати список їхніх імен, відфільтрувати порожні імена і зібрати їх у новий список.
>
> **Варіант з лямбда-виразами:**
>
> ```java
> List<String> names = users.stream()
>                           .map(user -> user.getName())
>                           .filter(name -> !name.isEmpty())
>                           .collect(Collectors.toList());
> ```
>
> Цей код є читабельним, але його можна зробити ще кращим.
>
> **Варіант з посиланнями на методи:**
>
> ```java
> List<String> names = users.stream()
>                           .map(User::getName)
>                           .filter(not(String::isEmpty)) // Використовуємо `not` з Java 11
>                           .collect(Collectors.toList());
> ```
>
> *`import static java.util.function.Predicate.not;`*
>
> **Чому другий варіант кращий:**
> Він є більш **декларативним**. Ви читаєте його як послідовність дій: "взяти потік, для кожного взяти ім'я, відфільтрувати не порожні, зібрати в список". Це описує "що" зробити, а не "як". `User::getName` миттєво говорить, яке поле нас цікавить.

#### **📌 Що перевіряють:**

*Розуміння не тільки синтаксису, а й "духу" функціонального програмування — прагнення до декларативного, виразного коду.*

* * *

### **12 ❓ Питання:**

Чи можна використовувати посилання на метод для `private`-методів? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна**, але за однієї умови: посилання на `private`-метод можна використовувати **тільки всередині того ж самого класу**, де цей метод оголошено.
>
> Це тому, що правила видимості в Java застосовуються і до посилань на методи. Оскільки `private` метод доступний лише всередині свого класу, то і посилання на нього буде валідним лише там.
>
> Цей підхід може бути корисним для передачі приватної логіки в методи Stream API, не роблячи її публічною.

#### **📝 Приклад:**

```java
public class Processor {
    private String processInternal(String data) {
        return "Processed: " + data;
    }

    public List<String> processList(List<String> dataList) {
        // Посилання на private-метод всередині свого ж класу
        return dataList.stream()
                       .map(this::processInternal)
                       .collect(Collectors.toList());
    }
}
```

#### **📌 Що перевіряють:**

*Знання правил видимості в Java та їх застосування до сучасних мовних конструкцій. Це показує глибоке розуміння мови.*

* * *

### **13 ❓ Питання:**

Чи мають посилання на методи якісь переваги в продуктивності порівняно з лямбда-виразами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, зазвичай ні.** Це поширена помилкова думка. З точки зору згенерованого байт-коду та механізму виконання в JVM, посилання на метод та еквівалентний йому лямбда-вираз є практично **ідентичними**.
>
> Обидві конструкції "під капотом" використовують інструкцію **`invokedynamic`**, яка динамічно створює об'єкт-реалізацію функціонального інтерфейсу під час першого виклику.
>
> Існують мікроскопічні відмінності:
>
> * Для посилань на методи JVM не потрібно генерувати додатковий синтетичний метод, оскільки він уже існує. Для лямбди такий метод генерується компілятором.
> * Однак, сучасні JIT-компілятори настільки добре оптимізують цей код, що різниця в продуктивності стає незначною або взагалі відсутньою.
>
> **Головне правило:** Вибір між лямбдою та посиланням на метод має базуватися виключно на **читабельності та ясності коду**, а не на мікрооптимізаціях.

#### **📌 Що перевіряють:**

*Глибоке розуміння внутрішньої реалізації Java. Кандидат не повинен вірити в "міфи" про продуктивність, а спиратися на знання того, як JVM працює насправді.*

* * *

### **14 ❓ Питання:**

Як працює посилання на конструктор перевантаженого класу? Як Java обирає, який конструктор викликати? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Механізм вибору конструктора такий самий, як і для перевантажених методів: компілятор спирається на **сигнатуру методу цільового функціонального інтерфейсу**.
>
> **Приклад:**
> Нехай є клас `User` з двома конструкторами:
>
> ```java
> class User {
>     User() { ... }
>     User(String name) { ... }
> }
> ```
>
> **Як Java обирає:**
>
> * Якщо ми використовуємо `User::new` з інтерфейсом `Supplier<User>`, компілятор бачить його метод `User get()`. Ця сигнатура (без аргументів, повертає `User`) відповідає конструктору `User()`.
>
>     ```java
>     Supplier<User> userSupplier = User::new; // Викличе User()
>     ```
>
> * Якщо ми використовуємо `User::new` з інтерфейсом `Function<String, User>`, компілятор бачить його метод `User apply(String name)`. Ця сигнатура відповідає конструктору `User(String name)`.
>
>     ```java
>     Function<String, User> userFunction = User::new; // Викличе User(String name)
>     ```
>
> Якщо компілятор не може знайти конструктор, що точно відповідає сигнатурі функціонального інтерфейсу, або якщо є неоднозначність, буде помилка компіляції.

#### **📌 Що перевіряють:**

*Розуміння того, як механізм виведення типів застосовується до конструкторів. Це показує глибоке володіння функціональним програмуванням в Java.*

* * *

### **15 ❓ Питання:**

Чи можна використовувати `super` у посиланнях на методи? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Посилання на метод за допомогою `super` є валідним і належить до другого типу (посилання на метод екземпляра конкретного об'єкта), де конкретним об'єктом є `super`.
>
> **Синтаксис:** `super::methodName`
>
> **Призначення:**
> Це використовується в дочірньому класі, коли потрібно передати реалізацію **батьківського класу**, а не перевизначену реалізацію з дочірнього класу.
>
> Це може бути корисним, якщо вам потрібно скомбінувати поведінку батьківського класу з додатковою логікою в функціональному стилі.

#### **📝 Приклад:**

```java
class Parent {
    public String Greet() {
        return "Hello from Parent";
    }
}

class Child extends Parent {
    @Override
    public String Greet() {
        return "Hello from Child";
    }

    public Supplier<String> getParentGreeter() {
        // Посилаємося на реалізацію методу з класу Parent
        return super::Greet; 
    }
}

// Виклик:
Child child = new Child();
Supplier<String> parentGreeter = child.getParentGreeter();
System.out.println(parentGreeter.get()); // Виведе: "Hello from Parent"
```

#### **📌 Що перевіряють:**

*Експертне знання синтаксису Java та правил наслідування в контексті функціонального програмування. Це досить рідкісний, але показовий сценарій.*

* * *
