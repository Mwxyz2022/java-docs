---
layout: default
title: "Обробка винятків у функціональних інтерфейсах"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 11
---

# Обробка винятків у функціональних інтерфейсах

* короткий опис: Стандартні функціональні інтерфейси Java не підтримують кидання `Checked Exceptions` безпосередньо. Для їх обробки у лямбдах потрібні обходи: `try-catch` всередині лямбди, обгортання `Checked Exceptions` у `RuntimeException`, або використання власних функціональних інтерфейсів, що оголошують `throws`.

* поняття: Checked vs Unchecked exceptions, `exception wrapping`, власний функціональний інтерфейс з `throws`.

* приклади: `s -> { try { Files.readAllLines(path); } catch (IOException e) { throw new RuntimeException(e); } }`.

* ключові моменти: `Checked Exceptions` не можуть бути напряму проброшені з лямбди.

* що важливо знати на співбесіді: Чому виникає проблема, способи вирішення.

### **✅ Офіційне визначення:**

Обробка винятків у функціональних інтерфейсах — це комплекс методів та підходів для роботи з checked (перевіреними) та unchecked (неперевіреними) винятками всередині лямбда-виразів і функціональних інтерфейсів, де стандартні методи не підтримують кидання checked exceptions.

### **🧠 Простими словами:**

У лямбдах не можна просто так кидати перевірені винятки, тож потрібні трюки, щоб їх “загорнути” або “поймати” інакше

---

## 📊 **Типи винятків:**

|       Тип винятку       |                      Опис                       |                      Приклад                       |
|:-----------------------:|:-----------------------------------------------:|:--------------------------------------------------:|
|  **Checked Exception**  |    Винятки, які треба обробити або оголосити    |       **`IOException`**, **`SQLException`**        |
| **Unchecked Exception** | Винятки часу виконання, не вимагають оголошення | **`NullPointerException`**, **`RuntimeException`** |

---

## **Пояснення на пальцях (💡)**

* Лямбда-вирази в Java працюють з функціональними інтерфейсами, методи яких не оголошують checked exceptions — отже, кинути checked exception напряму неможливо.
* Це означає, що якщо в тілі лямбди є код, який кидає checked exception, компілятор “поскаржиться”.
* Основні стратегії:
  * Ловити винятки всередині лямбди через try-catch
  * “Обгортати” checked exception в unchecked (наприклад, RuntimeException)
  * Створювати власні функціональні інтерфейси, які дозволяють кидати checked exceptions

---

## **Корисні приклади (🧪)**

**1\. try-catch всередині лямбди**

```java
Consumer<String> consumer = s -> {
    try {
        Files.readAllLines(Paths.get(s));
    } catch (IOException e) {
        e.printStackTrace();
    }
};
consumer.accept("file.txt");
```

**2\. Обгортання checked exception у RuntimeException**

```java
Function<String, String> function = s -> {
    try {
        return new String(Files.readAllBytes(Paths.get(s)));
    } catch (IOException e) {
        throw new RuntimeException(e);
    }
};
```

**3\. Власний функціональний інтерфейс з винятком**

```java
@FunctionalInterface
public interface ThrowingFunction<T, R> {
    R apply(T t) throws Exception;
}

// Використання
ThrowingFunction<String, String> readFile = s -> new String(Files.readAllBytes(Paths.get(s)));
```

---

## **Порівняння (⚖️)**

|              Підхід              |             Переваги              |            Недоліки            |
|:--------------------------------:|:---------------------------------:|:------------------------------:|
|      **try-catch в лямбді**      |  Простота, без зміни інтерфейсів  | Шумний код, важко масштабувати |
|     **Обгортання винятків**      | Чистий код, можна передавати далі | Втрата типу винятку, менш явне |
| **Власний інтерфейс з `throws`** |   Підтримка checked exceptions    | Потрібно додаткове визначення  |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Checked exception \+ лямбда \= try-catch або обгортання**
* **Unchecked — нікого не цікавить, кидай як хочеш\!**

---

## **🟩 Підсумок:**

* Лямбди і функціональні інтерфейси мають обмеження щодо **`checked`** **`exceptions`**.
* Для роботи з checked exceptions у лямбдах потрібні обходи: **`try-catch`**, обгортання, власні інтерфейси.
* Розуміння цих стратегій допомагає уникнути помилок і писати чистий, стабільний код.
* На співбесіді важливо пояснити, чому виникає ця проблема і як її розв’язувати.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs: Functional Interfaces**](https://docs.oracle.com/javase/8/docs/api/java/util/function/package-summary.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка основна проблема з обробкою винятків у лямбда-виразах в Java?

> #### **💬 Відповідь:**
>
> Основна проблема полягає в тому, що **стандартні функціональні інтерфейси** з пакету `java.util.function` (такі як `Function`, `Predicate`, `Consumer`) **не оголошують перевірених винятків (checked exceptions)** у сигнатурах своїх абстрактних методів.
>
> Наприклад, сигнатура методу `apply` в `Function<T, R>` виглядає так:
> `R apply(T t);`
>
> Вона не має блоку `throws IOException` чи `throws SQLException`.
>
> **Наслідок:**
> Згідно з правилами Java, ви не можете кинути перевірений виняток з методу, якщо він не оголошений в його сигнатурі (`throws` clause). Це означає, що якщо тіло вашого лямбда-виразу містить код, який може кинути перевірений виняток (наприклад, операції з файлами або базами даних), ви отримаєте **помилку компіляції**.
>
> З неперевіреними винятками (`RuntimeException` та його нащадки) такої проблеми немає, оскільки їх не потрібно оголошувати.

#### **📝 Аналогія:**

Уявіть, що функціональний інтерфейс — це контракт, а лямбда — його виконавець. Якщо в контракті не вказано, що виконавець має право повідомляти про форс-мажор (кидати перевірений виняток), то він і не може цього зробити.

#### **📌 Що перевіряють:**

*Розуміння кореневої причини проблеми: невідповідність сигнатур методів стандартних функціональних інтерфейсів та методів, що кидають перевірені винятки.*

* * *

### **2 ❓ Питання:**

Що таке перевірені (checked) та неперевірені (unchecked) винятки?

> #### **💬 Відповідь:**
>
> * **Перевірені винятки (Checked Exceptions):**
>   * Це винятки, що успадковуються від `java.lang.Exception`, але не від `java.lang.RuntimeException`.
>   * Компілятор **перевіряє** їх обробку. Ви **зобов'язані** або обробити такий виняток у блоці `try-catch`, або оголосити його в сигнатурі методу за допомогою `throws`.
>   * Вони представляють очікувані, хоч і помилкові, ситуації, які програма може коректно обробити (напр., файл не знайдено).
>   * **Приклади:** `IOException`, `SQLException`, `ClassNotFoundException`.
>
> * **Неперевірені винятки (Unchecked Exceptions):**
>   * Це винятки, що успадковуються від `java.lang.RuntimeException` або `java.lang.Error`.
>   * Компілятор **не перевіряє** їх обробку. Ви не зобов'язані їх ловити або оголошувати.
>   * Вони зазвичай сигналізують про **програмні помилки** або серйозні проблеми середовища, які навряд чи можна коректно обробити (напр., звернення до `null`-посилання).
>   * **Приклади:** `NullPointerException`, `IllegalArgumentException`, `OutOfMemoryError`.

#### **📌 Що перевіряють:**

*Фундаментальні знання системи обробки винятків у Java. Це базова тема, яку повинен знати кожен розробник.*

* * *

### **3 ❓ Питання:**

Який найпростіший спосіб обробити перевірений виняток всередині лямбда-виразу?

> #### **💬 Відповідь:**
>
> Найпростіший і найбільш прямолінійний спосіб — це обгорнути код, що кидає виняток, у традиційний блок **`try-catch`** безпосередньо всередині тіла лямбди.
>
> Це дозволяє "спіймати" перевірений виняток і вирішити, що з ним робити, не порушуючи контракт функціонального інтерфейсу.

#### **📝 Приклад:**

Нехай є метод `readFile(String path)`, що кидає `IOException`.

```java
List<String> paths = ...;
List<String> contents = new ArrayList<>();

// Обробляємо IOException всередині лямбди
paths.forEach(path -> {
    try {
        String content = readFile(path);
        contents.add(content);
    } catch (IOException e) {
        // Логуємо помилку або просто ігноруємо
        System.err.println("Failed to read file: " + path);
    }
});
```

#### **📝 Недоліки цього підходу:**

* Робить лямбда-вираз громіздким та менш читабельним.
* Призводить до дублювання коду, якщо таку обробку потрібно робити в кількох місцях.

#### **📌 Що перевіряють:**

*Практичні навички вирішення проблеми. Хоча це не найнайелегантніше рішення, воно є робочим і його потрібно знати.*

* * *

### **4 ❓ Питання:**

Що таке "обгортання винятків" (exception wrapping) і як воно допомагає?

> #### **💬 Відповідь:**
>
> **Обгортання винятків** — це техніка, при якій ви перехоплюєте перевірений виняток (`checked exception`) і кидаєте замість нього **неперевірений виняток (`unchecked exception`)**, зазвичай `RuntimeException`, передаючи оригінальний виняток у його конструктор.
>
> **Як це допомагає:**
> Це дозволяє "прокинути" інформацію про помилку вгору по стеку викликів, не оголошуючи `throws` у сигнатурі. Лямбда-вираз стає значно чистішим, а обробку помилки можна винести на вищий рівень.
>
> Це є компромісом: ми "обманюємо" компілятор, перетворюючи перевірений виняток на неперевірений, але зберігаємо можливість централізовано обробити помилку.

#### **📝 Приклад:**

```java
public String processFile(String path) {
    try {
        return Files.readString(Paths.get(path));
    } catch (IOException e) {
        // Загортаємо перевірений IOException у неперевірений
        throw new UncheckedIOException(e); // UncheckedIOException - стандартний з Java 8
    }
}

// Тепер лямбда чиста
List<String> contents = paths.stream()
                             .map(this::processFile)
                             .collect(Collectors.toList());
```

#### **📌 Що перевіряють:**

*Знання поширеного патерну для обробки винятків у функціональному стилі. Це показує прагматичний підхід до вирішення проблеми.*

* * *

### **5 ❓ Питання:**

Як створити власний функціональний інтерфейс, який дозволяє кидати перевірені винятки?

> #### **💬 Відповідь:**
>
> Якщо вам у проекті часто доводиться працювати з кодом, що кидає певний тип винятку, найкращим рішенням може бути створення власного функціонального інтерфейсу, метод якого явно оголошує цей виняток.
>
> **Кроки:**
>
> 1. Оголосити новий інтерфейс.
> 2. Анотувати його `@FunctionalInterface`.
> 3. Створити один абстрактний метод з такою ж сигнатурою, як у стандартного аналога, але з додаванням `throws ExceptionType` в кінець.

#### **📝 Приклад:**

Створимо аналог `Function`, що може кидати `IOException`.

```java
@FunctionalInterface
public interface ThrowingFunction<T, R, E extends Throwable> {
    R apply(T t) throws E;
}

// Тепер ми можемо створити метод, який приймає таку функцію
public <T, R> List<R> processList(List<T> list, ThrowingFunction<T, R, IOException> function) {
    // ...
    try {
        R result = function.apply(item);
    } catch (IOException e) {
        // ... обробка ...
    }
    // ...
}
```

Такий підхід робить код найбільш типізованим і безпечним, але вимагає створення додаткових інтерфейсів.

#### **📌 Що перевіряють:**

*Здатність до проектування власних абстракцій. Це показує глибоке розуміння та вміння створювати гнучкі API.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати `Predicate` з логікою, що кидає перевірений виняток?

> #### **💬 Відповідь:**
>
> Напряму — **ні**. Метод `test()` в `Predicate` не оголошує `throws`. Як і у випадку з `Function`, потрібно використовувати один з обхідних шляхів:
>
> 1. **Обробка всередині лямбди:** `try-catch` всередині `test()`. У `catch` блоці ви повинні вирішити, що повернути: `true` чи `false`, або кинути `RuntimeException`.
>
>     ```java
>     Predicate<File> canRead = file -> {
>         try {
>             checkAccess(file); // Кидає IOException
>             return true;
>         } catch (IOException e) {
>             return false; // Вважаємо, що не відповідає умові, якщо сталася помилка
>         }
>     };
>     ```
>
> 2. **Створення власного інтерфейсу `ThrowingPredicate`**.
>
> 3. **Використання утилітарної функції-обгортки**.
>
> Повернення `false` у `catch` блоці є поширеним патерном, оскільки він дозволяє `Stream.filter()` просто пропустити елемент, з яким виникла проблема.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про обробку винятків до різних типів функціональних інтерфейсів.*

* * *

### **7 ❓ Питання:**

Що станеться, якщо лямбда всередині `Stream` кине неперевірений виняток (`RuntimeException`)? ⭐️

> #### **💬 Відповідь:**
>
> Якщо лямбда-вираз, що використовується в **проміжній** або **термінальній** операції `Stream`, кине неперевірений виняток, то **виконання всього стріму негайно припиниться**, і цей виняток буде "прокинуто" вгору по стеку викликів.
>
> Його можна буде перехопити за допомогою блоку `try-catch` навколо всієї операції зі стрімом.

#### **📝 Приклад:**

```java
List<String> list = Arrays.asList("1", "2", "abc", "4");

try {
    List<Integer> numbers = list.stream()
                                  .map(Integer::parseInt) // Кине NumberFormatException на "abc"
                                  .collect(Collectors.toList());
} catch (NumberFormatException e) {
    System.err.println("Помилка під час парсингу: " + e.getMessage());
}
```

Обробка не буде продовжена для елемента "4". Стрім "зламається" на "abc".

#### **📌 Що перевіряють:**

*Розуміння механізму обробки помилок у Stream API. Кандидат повинен знати, що `RuntimeException` зупиняє всю конвеєрну обробку.*

* * *

### **8 ❓ Питання:**

Наведіть приклад утилітарного методу-обгортки (wrapper) для перетворення "небезпечної" функції в "безпечну". ⭐️

> #### **💬 Відповідь:**
>
> Утилітарний метод-обгортка — це елегантний спосіб уникнути дублювання `try-catch` блоків. Ідея полягає в тому, щоб створити функцію вищого порядку, яка приймає лямбду, що кидає перевірений виняток, і повертає стандартну `Function`, що загортає цей виняток у `RuntimeException`.
>
> Спочатку нам потрібен власний функціональний інтерфейс:
>
> ```java
> @FunctionalInterface
> interface ThrowingFunction<T, R, E extends Throwable> {
>     R apply(T t) throws E;
> }
> ```
>
> Тепер створюємо метод-обгортку:
>
> ```java
> public static <T, R, E extends Throwable> Function<T, R> wrap(ThrowingFunction<T, R, E> f) {
>     return arg -> {
>         try {
>             return f.apply(arg);
>         } catch (Throwable e) {
>             throw new RuntimeException(e);
>         }
>     };
> }
> ```

#### **📝 Приклад використання:**

```java
// Уявімо, що process(path) кидає IOException
// Використовуємо наш wrapper
List<Result> results = paths.stream()
                            .map(wrap(path -> process(path)))
                            .collect(Collectors.toList());
```

Це робить код у стрімі чистим, а вся логіка обробки винятків інкапсульована в методі `wrap`.

#### **📌 Що перевіряють:**

*Здатність до написання утилітарного, перевикористовуваного коду. Це показує глибоке розуміння функціонального програмування та патернів проектування.*

* * *

### **9 ❓ Питання:**

Які ризики несе "проковтування" винятків всередині `catch` блоку в лямбді? ⭐️

> #### **💬 Відповідь:**
>
> **"Проковтування" винятків** (коли `catch` блок є порожнім або лише логує помилку, не кидаючи новий виняток) є **дуже небезпечним антипатерном**.
>
> **Ризики:**
>
> 1. **Приховування проблем:** Система може продовжувати працювати в некоректному стані, і ви ніколи не дізнаєтеся, що щось пішло не так. Це може призвести до пошкодження даних або непередбачуваної поведінки на пізніших етапах.
>
> 2. **Ускладнення налагодження:** Знайти причину проблеми стає майже неможливо. Ви будете бачити дивні результати, але не будете знати, де сталася першопричина.
>
> 3. **Некоректні результати:** Якщо лямбда в `map` або `filter` просто поверне `null` або `false` у `catch` блоці, подальші операції в стрімі можуть отримати невалідні дані, що призведе до логічних помилок.
>
> **Що робити натомість:**
>
> * Або **загортати** перевірений виняток у неперевірений, щоб "провалити" операцію одразу.
> * Або повертати `Optional.empty()` чи інший "нейтральний" елемент, який подальший код вміє коректно обробляти.
> * Або, як мінімум, дуже детально логувати помилку на рівні `ERROR`.

#### **📌 Що перевіряють:**

*Розуміння кращих практик обробки помилок. "Проковтування" винятків є однією з найгірших практик, і кандидат повинен це усвідомлювати.*

* * *

### **10 ❓ Питання:**

Поясніть, що робить бібліотека, як-от `Vavr` (колишня Javaslang) або `Lombok` (`@SneakyThrows`), для спрощення обробки винятків у лямбдах. ⭐️

> #### **💬 Відповідь:**
>
> Ці бібліотеки надають готові рішення для проблеми обробки перевірених винятків, щоб не писати код-обгортку вручну.
>
> * **Lombok (`@SneakyThrows`):**
>   * Це анотація, яку можна поставити на метод або лямбду.
>   * Під час компіляції Lombok **генерує** необхідний `try-catch` блок, який "обманює" компілятор, кидаючи перевірений виняток так, ніби він неперевірений, без явного обгортання. Це робить код дуже чистим, але є певною "магією".
>
>     ```java
>     @SneakyThrows(IOException.class)
>     public String readFile(String path) {
>         return new String(Files.readAllBytes(Paths.get(path)));
>     }
>     // Тепер readFile можна передавати у `map` напряму.
>     ```
>
> * **Vavr (`Try`):**
>   * Надає монадний контейнер `Try`, який є більш потужною альтернативою `try-catch`.
>   * Ви можете обгорнути "небезпечну" операцію у `Try.of(...)`.
>   * Результатом буде або `Success<T>` з успішним значенням, або `Failure<Throwable>` з винятком.
>   * Це дозволяє обробляти помилки у функціональному, ланцюжковому стилі.
>
>     ```java
>     list.stream()
>         .map(path -> Try.of(() -> readFile(path)))
>         .filter(Try::isSuccess)
>         .map(Try::get)
>         //...
>     ```
>
> Ці інструменти абстрагують складність обробки помилок, дозволяючи писати більш чистий функціональний код.

#### **📌 Що перевіряють:**

*Кругозір кандидата та знання популярних сторонніх бібліотек, які вирішують поширені проблеми JDK. Це показує, що кандидат шукає ефективні рішення.*

* * *

### **11 ❓ Питання:**

Уявіть, що ви використовуєте `Stream.forEach`, щоб записати кожен елемент у файл. Як ви організуєте код, щоб він був чистим та коректно обробляв `IOException`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема: термінальна операція з побічним ефектом, що може кидати перевірений виняток. Найкращий підхід тут — **відокремити логіку взаємодії з файлом від логіки ітерації**.
>
> **Крок 1: Створити "безпечний" `Consumer`**
>
> Написати утилітарний метод, який приймає наш "небезпечний" `Consumer`, що кидає `IOException`, і повертає звичайний `Consumer`, що обробляє виняток.

#### **📝 Приклад коду:**

```java
// 1. Створюємо власний функціональний інтерфейс
@FunctionalInterface
interface ThrowingConsumer<T, E extends Throwable> {
    void accept(T t) throws E;
}

// 2. Створюємо метод-обгортку
public static <T> Consumer<T> uncheckedConsumer(ThrowingConsumer<T, IOException> consumer) {
    return t -> {
        try {
            consumer.accept(t);
        } catch (IOException e) {
            throw new UncheckedIOException(e); // Загортаємо і кидаємо далі
        }
    };
}

// 3. Використання у стрімі
public void writeToFile(List<String> lines, Path path) {
    try (BufferedWriter writer = Files.newBufferedWriter(path)) {
        ThrowingConsumer<String, IOException> lineWriter = line -> {
            writer.write(line);
            writer.newLine();
        };
        
        lines.stream().forEach(uncheckedConsumer(lineWriter));

    } catch (IOException | UncheckedIOException e) {
        // Ловимо як оригінальний, так і загорнутий виняток
        handleError(e);
    }
}
```

Такий підхід робить код всередині `.forEach()` дуже чистим (`uncheckedConsumer(lineWriter)`), а вся логіка обробки помилок винесена назовні.

#### **📌 Що перевіряють:**

*Здатність проектувати чисті та перевикористовувані рішення для складних практичних задач. Це ознака хорошого дизайну.*

* * *

### **12 ❓ Питання:**

Що більш семантично правильно в `catch` блоці всередині `Predicate`: кидати `RuntimeException` чи повертати `false`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це залежить від **контракту** та **очікувань** від вашої системи, але зазвичай **повернення `false` є більш семантично правильним** та безпечним для `Predicate`.
>
> **Повернення `false`:**
>
> * **Семантика:** Це означає: "Під час перевірки цього елемента сталася помилка, тому я не можу підтвердити, що він відповідає умові. Я вважаю його невалідним і пропоную пропустити".
> * **Поведінка у `filter()`:** `Stream.filter()` просто **проігнорує** цей елемент і продовжить обробку решти потоку. Це граційна обробка помилок.
>
> **Кидання `RuntimeException`:**
>
> * **Семантика:** Це означає: "Під час перевірки цього елемента сталася настільки критична помилка, що подальша обробка всього потоку не має сенсу і повинна бути негайно зупинена".
> * **Поведінка у `filter()`:** Виконання всього стріму **негайно припиниться**, і виняток буде прокинуто нагору.
>
> **Коли що обирати:**
>
> * Якщо помилка є **очікуваною** (напр., не вдалося з'єднатися з зовнішнім сервісом для валідації одного елемента), краще **повернути `false`**.
> * Якщо помилка є **неочікуваною** і вказує на серйозний збій у системі, краще **кинути `RuntimeException`**, щоб зупинити процес і повідомити про проблему якомога швидше.

#### **📌 Що перевіряють:**

*Здатність аналізувати семантику помилок та обирати відповідну стратегію їх обробки. Це показує зрілість розробника.*

* * *

### **13 ❓ Питання:**

Чи вирішує `try-with-resources` проблему з винятками в лямбдах, якщо ресурс оголошено ззовні? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, напряму не вирішує**, але дозволяє побудувати більш надійний код.
>
> `try-with-resources` гарантує автоматичне закриття ресурсу, але він **не змінює правил обробки винятків всередині лямбди**. Якщо ваш код всередині `forEach` використовує ресурс, що кидає `IOException`, вам все одно доведеться обробляти цей виняток.
>
> **Приклад правильної організації:**
>
> ```java
> public void writeLines(Path path, List<String> lines) throws IOException {
>     // Ресурс оголошено ЗЗОВНІ стріму
>     try (BufferedWriter writer = Files.newBufferedWriter(path)) {
>         lines.forEach(line -> {
>             try {
>                 writer.write(line); // Все ще кидає IOException
>                 writer.newLine();
>             } catch (IOException e) {
>                 // Проблема все ще тут
>                 throw new UncheckedIOException(e); 
>             }
>         });
>     } catch (UncheckedIOException e) {
>         // Перехоплюємо загорнутий виняток і кидаємо оригінальний
>         throw e.getCause(); 
>     }
> }
> ```
>
> `try-with-resources` тут корисний, бо гарантує, що `writer` буде закрито, навіть якщо всередині стріму станеться помилка. Але він не звільняє нас від необхідності обробляти винятки в лямбді.

#### **📌 Що перевіряють:**

*Розуміння того, як різні мовні конструкції взаємодіють, а також їхніх обмежень. Це демонструє здатність писати коректний та надійний код для роботи з ресурсами.*

* * *

### **14 ❓ Питання:**

Як `CompletableFuture` обробляє винятки, і як це пов'язано з функціональними інтерфейсами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `CompletableFuture` має вбудований, дуже потужний механізм для обробки винятків, який добре інтегрується з функціональним стилем.
>
> Якщо на будь-якому етапі асинхронного ланцюжка (`thenApply`, `thenCompose`) функція кидає виняток, `CompletableFuture` переходить у **завершений з помилкою (exceptionally completed)** стан. Цей виняток "передається" вниз по ланцюжку.
>
> **Як його обробити:**
> `CompletableFuture` надає спеціальні методи, що приймають `Function` для обробки винятків:
>
> * **`exceptionally(Function<Throwable, T> fn)`**:
>   * Приймає функцію, яка виконується, **тільки якщо** попередній етап завершився з помилкою.
>   * Ця функція отримує `Throwable` і може **повернути значення за замовчуванням**, тим самим "відновлюючи" ланцюжок.
>
> * **`handle(BiFunction<T, Throwable, U> fn)`**:
>   * Приймає `BiFunction`, яка виконується **завжди** — і при успішному завершенні, і при помилці.
>   * Вона отримує або результат `T`, або виняток `Throwable` (один з них буде `null`). Дозволяє обробити обидва сценарії в одному місці.
>
> Цей підхід є значно чистішим, ніж `try-catch`, оскільки дозволяє обробляти помилки в декларативному, ланцюжковому стилі.

#### **📌 Що перевіряють:**

*Досвід роботи з асинхронним програмуванням та його ідіоматичними патернами обробки помилок. Це ознака сильного розробника.*

* * *

### **15 ❓ Питання:**

Чи існують плани в майбутніх версіях Java спростити обробку перевірених винятків у лямбдах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це одне з найбільш обговорюваних питань в Java-спільноті. Наразі офіційних, затверджених планів щодо кардинальної зміни цього механізму **немає**.
>
> **Чому це так складно змінити:**
>
> * **Зворотна сумісність:** Будь-яка зміна може "зламати" величезну кількість існуючого коду, що покладається на поточну поведінку. Наприклад, якби `Function` раптом почала оголошувати `throws Exception`, це зламало б усі існуючі реалізації.
> * **Неоднозначність виведення типів:** Додавання перевірених винятків значно ускладнило б механізм виведення типів для лямбд, який і так є дуже складним.
>
> **Які ідеї обговорюються (в рамках досліджень, а не планів на реліз):**
>
> * **Полегшені перевірені винятки (Relaxed checked exceptions):** Ідеї про те, щоб дозволити компілятору бути "розумнішим" і автоматично виводити `throws` clause для лямбд.
> * **Нові стандартні інтерфейси:** Додавання в JDK нових стандартних "Throwing" інтерфейсів (`ThrowingFunction` і т.д.), щоб не доводилося створювати свої.
>
> Проте, на даний момент розробники продовжують покладатися на існуючі підходи: `try-catch`, обгортання винятків, або власні утиліти. Експерти, як-от Браян Гетц (Brian Goetz), неодноразово заявляли, що просте "вимкнення" перевірки для лямбд не є правильним рішенням.

#### **📌 Що перевіряють:**

*Кругозір кандидата, інтерес до розвитку платформи Java та розуміння складних дизайнерських компромісів. Це показує, що людина не просто пише код, а цікавиться майбутнім мови.*

* * *
