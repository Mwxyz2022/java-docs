---
layout: default
title: "Порівняння підходів: анонімні класи vs лямбда-вирази vs method references"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 13
---

# Порівняння підходів: анонімні класи vs лямбда-вирази vs method references

* короткий опис: Три основні способи реалізації функціональних інтерфейсів: `анонімні класи` (багатослівні), `лямбда-вирази` (лаконічні, з `->`), та `посилання на методи` (`::`, найбільш лаконічні).

* поняття: Анонімний клас, лямбда-вираз, method reference.

* приклади: `new Runnable() { public void run() { ... } }` (анонімний), `() -> { ... }` (лямбда), `System.out::println` (method reference).

* ключові моменти: Лямбда та method references компактніші та оптимізованіші. Анонімні класи потрібні для складної логіки або доступу до `this` анонімного класу.

* що важливо знати на співбесіді: Коли що краще використовувати, impact на performance.

### **✅ Офіційне визначення:**

Анонімні класи, лямбда-вирази та посилання на методи (method references) — це різні способи реалізації функціональних інтерфейсів у Java, що дозволяють передавати поведінку як параметр.

### **🧠 Простими словами:**

Це три способи написати кусок коду, який можна передати як функцію: старий добрий анонімний клас, більш лаконічний лямбда або ультрачитабельне посилання на метод

---

## 📊 **Порівняння:**

|        Підхід         |                Опис                |                 Переваги                  |                          Недоліки                          |
|:---------------------:|:----------------------------------:|:-----------------------------------------:|:----------------------------------------------------------:|
|  **Анонімний клас**   | Клас без імені, реалізує інтерфейс | Підтримка складної логіки, доступ до this |              Багатослівний, менш читабельний               |
|   **Лямбда-вирази**   |    Лаконічне вираження функції     |     Короткий код, легкий для читання      | Обмеження — працюють тільки з функціональними інтерфейсами |
| **Method references** |    Посилання на існуючий метод     |  Максимальна читабельність, мінімум коду  |        Не завжди застосовні (потрібен точний збіг)         |

---

## **Пояснення на пальцях (💡)**

* **Анонімний клас** — це як написати маленький клас на льоту, якби у вас була коробка з деталями, і ви зліпили з них конструктор.
* **Лямбда** — це коротка функція без зайвих деталей, як скорочений запис вашої ідеї.
* **Method reference** — це як сказати: "Зроби те саме, що й ця вже готова функція", не переписуючи її знову.

---

## **Корисні приклади (🧪)**

```java
// Анонімний клас
Runnable r1 = new Runnable() {
    @Override
    public void run() {
        System.out.println("Анонімний клас");
    }
};

// Лямбда-вираз
Runnable r2 = () -> System.out.println("Лямбда");

// Method reference
Runnable r3 = System.out::println;  // виведе пустий рядок, але демонструє синтаксис
```

**Інший приклад з функцією перетворення:**

```java
Function<String, Integer> f1 = new Function<String, Integer>() {
    @Override
    public Integer apply(String s) {
        return s.length();
    }
};

Function<String, Integer> f2 = s -> s.length();

Function<String, Integer> f3 = String::length;
```

---

## **Порівняння (⚖️)**

|       Критерій       |         Анонімний клас         |          Лямбда-вираз           |        Method references         |
|:--------------------:|:------------------------------:|:-------------------------------:|:--------------------------------:|
|       **Код**        |   Розгорнутий, багатослівний   |      Короткий, компактний       |      Максимально лаконічний      |
|  **Читабельність**   |          Менш зручний          |             Зручний             |    Найкращий, якщо підходить     |
|  **Продуктивність**  |  Трохи повільніший через клас  |     Оптимізований **`JVM`**     |       Аналогічний лямбдам        |
|    **Обмеження**     |             Жодних             | Тільки функціональні інтерфейси | Тільки точне співпадіння підпису |
| **Доступність this** | **`this`** — це анонімний клас |  **`this`** — оточуючого класу  |      Залежить від контексту      |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Анонімний клас \= повний «дядько» код**
* **Лямбда \= «функціональний хлопчик»**
* **Method reference \= «посилка» до готової роботи**

---

## **🟩 Підсумок:**

* Вибір між анонімним класом, лямбда-виразом і посиланням на метод залежить від задачі, читабельності і контексту.
* **Лямбда** — це універсальний варіант для чистого коду.
* **Method references** роблять код ще більш елегантним, але підходять не завжди.
* **Анонімні класи** — запасний варіант для складної логіки.
* **Розуміння цих підходів** — **must-have** для будь-якого Java-розробника та на співбесіді.

---

### **🔗 Додаткові джерела:**

* [**Oracle Tutorial: Lambda Expressions**](https://docs.oracle.com/javase/tutorial/java/javaOO/lambdaexpressions.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які є три основні способи реалізації функціонального інтерфейсу в Java і яка між ними основна різниця?

> #### **💬 Відповідь:**
>
> Існують три основні способи, які є еволюцією підходів у Java:
>
> 1. **Анонімні класи (Anonymous Classes):**
>     * **Опис:** Це "старий" спосіб, що існував до Java 8. Ви створюєте повний клас без імені, який реалізує інтерфейс, прямо в місці його використання.
>     * **Особливість:** Найбільш багатослівний, але й найбільш гнучкий (може мати поля, кілька методів).
>
> 2. **Лямбда-вирази (Lambda Expressions):**
>     * **Опис:** Це компактний, анонімний блок коду, що з'явився в Java 8. Дозволяє реалізувати один абстрактний метод функціонального інтерфейсу без "церемоніального" коду.
>     * **Особливість:** Короткий та виразний синтаксис (`(параметри) -> { тіло }`).
>
> 3. **Посилання на методи (Method References):**
>     * **Опис:** Це ще більш компактний синтаксис для лямбда-виразів, який можна використовувати, якщо лямбда лише викликає один існуючий метод.
>     * **Особливість:** Максимальна читабельність та стислість (`ClassName::methodName`).
>
> **Головна різниця:** полягає в рівні **стислості та читабельності**. Кожен наступний підхід є більш сучасним та лаконічним, ніж попередній.

#### **📝 Аналогія:**

Уявіть, що вам потрібно дати комусь інструкцію.

* **Анонімний клас:** Написати детальний, офіційний лист на кількох сторінках.
* **Лямбда-вираз:** Написати коротку записку: "Зроби ось це".
* **Посилання на метод:** Сказати: "Дій за стандартною інструкцією №5".

#### **📌 Що перевіряють:**

*Базове розуміння трьох підходів та їхньої еволюції в Java. Кандидат повинен знати, що це різні способи вирішення однієї задачі — передачі поведінки.*

* * *

### **2 ❓ Питання:**

Наведіть код, що демонструє всі три підходи на прикладі реалізації `Comparator`.

> #### **💬 Відповідь:**
>
> Розглянемо сортування списку рядків за їх довжиною. Для цього потрібно реалізувати функціональний інтерфейс `Comparator<String>`.
>
> Кожен з трьох підходів вирішує це завдання, але з різним ступенем багатослівності.

#### **📝 Приклад:**

```java
import java.util.Arrays;
import java.util.Comparator;
import java.util.List;

public class ComparisonExample {
    public static void main(String[] args) {
        List<String> names = Arrays.asList("David", "Alice", "Bob");

        // 1. Анонімний клас (найбільш багатослівний)
        Comparator<String> byLengthAnonymous = new Comparator<String>() {
            @Override
            public int compare(String s1, String s2) {
                return Integer.compare(s1.length(), s2.length());
            }
        };
        
        // 2. Лямбда-вираз (компактно)
        Comparator<String> byLengthLambda = (s1, s2) -> Integer.compare(s1.length(), s2.length());
        
        // 3. Посилання на метод + компаратор-помічник (найбільш декларативно)
        // Створюємо `Function`, що витягує ключ для сортування (довжину),
        // і передаємо її в `Comparator.comparingInt`.
        Comparator<String> byLengthMethodRef = Comparator.comparingInt(String::length);
        
        names.sort(byLengthMethodRef);
        System.out.println(names); // [Bob, Alice, David]
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички. Кандидат повинен вміти написати код, використовуючи кожен з трьох підходів, і розуміти, як вони співвідносяться.*

* * *

### **3 ❓ Питання:**

Коли варто використовувати анонімний клас замість лямбда-виразу?

> #### **💬 Відповідь:**
>
> Хоча лямбда-вирази є кращими в більшості випадків, існують сценарії, коли потрібно використовувати **анонімний клас**:
>
> 1. **Коли потрібно реалізувати інтерфейс з кількома абстрактними методами:**
>     Лямбда-вираз може реалізувати лише **функціональний інтерфейс** (з одним абстрактним методом). Якщо в інтерфейсі їх два чи більше, ви змушені використовувати анонімний клас.
>
> 2. **Коли потрібно наслідувати абстрактний (або звичайний) клас:**
>     Лямбда не може наслідувати клас. Для цього також потрібен анонімний клас.
>
> 3. **Коли потрібен стан (поля):**
>     Анонімний клас може мати власні поля-екземпляри (`instance fields`), що дозволяє йому зберігати стан між викликами методів. Лямбда-вираз є безстановим (stateless).
>
> 4. **Коли потрібне інше значення `this`:**
>     В анонімному класі `this` посилається на екземпляр самого анонімного класу. В лямбді `this` посилається на екземпляр зовнішнього класу.

#### **📝 Коментар:**

По суті, лямбда — це спеціалізований інструмент. Анонімний клас — це більш універсальний, хоч і громіздкий, механізм.

#### **📌 Що перевіряють:**

*Розуміння обмежень лямбда-виразів. Кандидат повинен знати, що лямбда — це не повна заміна анонімним класам.*

* * *

### **4 ❓ Питання:**

У чому різниця в поведінці ключового слова `this` для лямбд та анонімних класів?

> #### **💬 Відповідь:**
>
> Це ключова концептуальна відмінність між ними.
>
> * **Анонімний клас:**
>   * `this` посилається на **екземпляр самого анонімного класу**. Анонімний клас створює свою власну область видимості (`scope`) і може "затінювати" (shadow) `this` зовнішнього класу. Для доступу до зовнішнього `this` потрібно використовувати синтаксис `EnclosingClassName.this`.
>
> * **Лямбда-вираз:**
>   * Лямбда-вираз **не створює власної області видимості**.
>   * `this` всередині лямбди посилається на **екземпляр зовнішнього (enclosing) класу**, в якому ця лямбда була визначена. Вона є "лексично прозорою".
>
> Це робить поведінку `this` у лямбдах більш інтуїтивною для розробників, оскільки вона відповідає поведінці звичайного коду всередині методу.

#### **📝 Приклад:**

```java
class MyWorker {
    public void doWork() {
        Runnable r1 = new Runnable() {
            @Override
            public void run() {
                // `this` посилається на екземпляр Runnable, а не MyWorker
                System.out.println(this.getClass().getName()); 
            }
        };

        Runnable r2 = () -> {
            // `this` посилається на екземпляр MyWorker
            System.out.println(this.getClass().getName());
        };

        r1.run(); // Виведе щось на зразок MyWorker$1
        r2.run(); // Виведе MyWorker
    }
}
```

#### **📌 Що перевіряють:**

*Глибокі знання нюансів Java 8. Поведінка `this` — це класичне питання для перевірки розуміння того, як лямбди реалізовані концептуально.*

* * *

### **5 ❓ Питання:**

Коли лямбда-вираз не можна замінити посиланням на метод?

> #### **💬 Відповідь:**
>
> Посилання на метод — це синтаксичний цукор для найпростіших лямбда-виразів. Його можна використовувати лише тоді, коли тіло лямбди виконує **один-єдиний виклик існуючого методу**, передаючи в нього ті самі параметри.
>
> Замінити лямбду на посилання на метод **неможливо**, якщо:
>
> * **Тіло лямбди містить більше одного виклику або будь-яку додаткову логіку:**
>   * `s -> s.trim().toUpperCase()` (два виклики)
>   * `x -> x * 2` (логіка множення)
>   * `user -> "Name: " + user.getName()` (конкатенація)
>
> * **Порядок або кількість аргументів не збігається:**
>   * `(x, y) -> Math.max(y, x)` (параметри передаються у зворотному порядку)
>   * `x -> System.out.println("Value: " + x)` (виклик методу з іншим аргументом)

#### **📝 Коментар:**

Якщо ви не можете перетворити лямбду на посилання на метод одним рухом, значить, цього робити не можна.

#### **📌 Що перевіряють:**

*Розуміння обмежень посилань на методи. Кандидат повинен усвідомлювати, що це дуже вузькоспеціалізований синтаксис.*

* * *

### **6 ❓ Питання:**

Як анонімні класи та лямбди працюють із захопленням локальних змінних?

> #### **💬 Відповідь:**
>
> І анонімні класи, і лямбда-вирази можуть "захоплювати" (capture) локальні змінні з методу, в якому вони оголошені.
>
> Однак для цього діє суворе правило: захоплені змінні повинні бути **`final`** або **ефективно фінальними (effectively final)**.
>
> **Ефективно фінальна змінна** — це змінна, якій значення присвоюється лише один раз, і після цього вона не змінюється.
>
> **Чому існує це обмеження:**
>
> Екземпляр анонімного класу або лямбди може існувати довше, ніж метод, в якому його створено. Щоб він міг використовувати локальну змінну, коли вона вже зникла зі стеку, компілятор **копіює значення** цієї змінної у приховане поле анонімного класу/лямбди. Заборона на зміну змінної гарантує, що не буде розсинхронізації між оригіналом та копією.

#### **📌 Що перевіряють:**

*Знання правил захоплення змінних, які є спільними для обох підходів і є важливою концепцією функціонального програмування в Java.*

* * *

### **7 ❓ Питання:**

Чи існує різниця у продуктивності між анонімними класами, лямбдами та посиланнями на методи?

> #### **💬 Відповідь:**
>
> Так, існує, і вона пов'язана з тим, як JVM їх реалізує.
>
> * **Анонімні класи:**
>   * Це найменш продуктивний підхід. При компіляції для кожного анонімного класу створюється **окремий `.class` файл**. При виконанні JVM змушена завантажувати, верифікувати та ініціалізувати ці класи, що створює певні накладні витрати.
>
> * **Лямбда-вирази та посилання на методи:**
>   * Вони є значно **продуктивнішими**. Замість створення `.class` файлів, компілятор генерує спеціальну інструкцію байт-коду **`invokedynamic`**.
>   * `invokedynamic` дозволяє JVM динамічно, під час виконання, створювати об'єкт-реалізацію. Це дозволяє JVM застосовувати сучасні стратегії оптимізації та у багатьох випадках навіть уникати створення нових об'єктів для незахоплюючих (non-capturing) лямбд.
>
> **Між лямбдами та посиланнями на методи** суттєвої різниці в продуктивності немає. Вибір між ними має ґрунтуватися лише на читабельності коду.
>
> **Висновок:** Для продуктивності завжди віддавайте перевагу лямбдам або посиланням на методи.

#### **📌 Що перевіряють:**

*Глибокі знання про внутрішню реалізацію мовних конструкцій Java та їхній вплив на продуктивність. Це важливий аспект для написання ефективного коду.*

* * *

### **8 ❓ Питання:**

Покажіть на прикладі, як можна було б вирішити одну й ту ж задачу всіма трьома способами, щоб продемонструвати різницю в багатослівності. ⭐️

> #### **💬 Відповідь:**
>
> **Задача:** Відсортувати список рядків у порядку, що ігнорує регістр. Для цього нам потрібен `Comparator<String>`.

#### **📝 Приклад:**

```java
List<String> names = Arrays.asList("charlie", "Alice", "bob");

// 1. Анонімний клас
names.sort(new Comparator<String>() {
    @Override
    public int compare(String s1, String s2) {
        return s1.compareToIgnoreCase(s2);
    }
});

// 2. Лямбда-вираз
names.sort((s1, s2) -> s1.compareToIgnoreCase(s2));

// 3. Посилання на метод
names.sort(String::compareToIgnoreCase);

System.out.println(names); // [Alice, bob, charlie]
```

> Цей приклад наочно демонструє, як код стає все більш коротким і читабельним при переході від анонімних класів до лямбд і, нарешті, до посилань на методи.

#### **📌 Що перевіряють:**

*Здатність застосувати всі три підходи на практиці та візуально порівняти їхні переваги в компактності.*

* * *

### **9 ❓ Питання:**

Поясніть, що таке цільовий тип (target type) і яку роль він відіграє для лямбд та посилань на методи. ⭐️

> #### **💬 Відповідь:**
>
> **Цільовий тип** — це тип даних (зазвичай, функціональний інтерфейс), якого очікує контекст, у якому використовується лямбда-вираз або посилання на метод.
>
> Лямбди та посилання на методи самі по собі не мають типу. Їхній тип **виводиться (inferred)** компілятором саме з цього цільового типу.
>
> **Роль цільового типу:**
>
> 1. **Визначення типу:** Він "говорить" компілятору, в екземпляр якого функціонального інтерфейсу потрібно перетворити лямбду.
> 2. **Виведення типів параметрів:** З цільового типу компілятор дізнається про сигнатуру абстрактного методу і може автоматично вивести типи параметрів лямбди.
> 3. **Перевірка сумісності:** Компілятор перевіряє, чи сумісна сигнатура лямбди (кількість параметрів, їхні типи, тип повернутого значення) з абстрактним методом цільового інтерфейсу.

#### **📝 Приклад:**

```java
// Цільовий тип тут - Predicate<String>
Predicate<String> p = s -> s.isEmpty();

// Цільовий тип тут - параметр методу `process(Consumer<String> c)`
process(s -> System.out.println(s));
```

Без цільового типу компілятор не зміг би зрозуміти, що означає `s -> s.isEmpty()`.

#### **📌 Що перевіряють:**

*Розуміння механізму виведення типів, який є основою роботи лямбда-виразів у статично типізованій мові, як-от Java.*

* * *

### **10 ❓ Питання:**

Чому кажуть, що посилання на метод є ще більш декларативним, ніж лямбда-вираз? ⭐️

> #### **💬 Відповідь:**
>
> **Декларативний код** описує **"що"** потрібно зробити, а не **"як"** це робити (що є ознакою імперативного коду).
>
> * **Лямбда-вираз (`s -> s.length()`):** Є більш імперативним. Він каже: "візьми `s` і виклич на ньому `length()`". Він все ще описує певний процес.
>
> * **Посилання на метод (`String::length`):** Є максимально декларативним. Він просто **називає** операцію, яку потрібно виконати. Він не описує, як її виконувати, а просто посилається на її ім'я.
>
> Коли ви читаєте посилання на метод, ви зосереджуєтесь на **намірі**, а не на механіці реалізації. Це робить код чистішим і ближчим до опису бізнес-логіки природною мовою, особливо у ланцюжках `Stream API`. Це і є вершина читабельності у функціональному стилі Java.

#### **📌 Що перевіряють:**

*Розуміння різниці між імперативним та декларативним стилями програмування, а також філософських переваг функціонального підходу.*

* * *

### **11 ❓ Питання:**

Назвіть чотири типи посилань на методи і вкажіть для кожного відповідний цільовий інтерфейс. ⭐️⭐️

> #### **💬 Відповідь:**
>
| **Тип посилання** | **Приклад** | **Приклад лямбди** | **Відповідний функціональний інтерфейс** |
| :--- | :--- | :--- | :--- |
| **1. На статичний метод** | `Integer::parseInt` | `s -> Integer.parseInt(s)` | `Function<String, Integer>` |
| **2. На метод екземпляра (конкретного об'єкта)** | `System.out::println` | `s -> System.out.println(s)` | `Consumer<String>` |
| **3. На метод екземпляра (довільного об'єкта)** | `String::toUpperCase` | `s -> s.toUpperCase()` | `Function<String, String>` або `UnaryOperator<String>` |
| **4. На конструктор** | `ArrayList::new` | `() -> new ArrayList()` | `Supplier<ArrayList>` |

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як сигнатура посилання на метод повинна співвідноситися з сигнатурою методу цільового функціонального інтерфейсу. Це перевіряє здатність мислити на рівні типів.*

* * *

### **12 ❓ Питання:**

Чи може лямбда-вираз реалізувати `default`-метод інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.**
>
> Лямбда-вираз за своєю природою може надати реалізацію лише для **одного-єдиного абстрактного методу** функціонального інтерфейсу.
>
> `Default`-методи не є абстрактними, вони вже мають реалізацію за замовчуванням. Лямбда не має синтаксису для того, щоб перевизначити `default`-метод.
>
> Якщо вам потрібно перевизначити `default`-метод, ви повинні використовувати або **іменований клас**, або **анонімний клас**, який реалізує цей інтерфейс.

#### **📝 Приклад:**

```java
interface MyInterface {
    void abstractMethod();
    default void defaultMethod() { /*...*/ }
}
// Якщо потрібно перевизначити defaultMethod, доведеться робити так:
MyInterface instance = new MyInterface() {
    @Override public void abstractMethod() { /*...*/ }
    @Override public void defaultMethod() { /* інша реалізація */ }
};
```

#### **📌 Що перевіряють:**

*Знання обмежень лямбд та правил роботи з `default`-методами. Це питання на розуміння нюансів мови.*

* * *

### **13 ❓ Питання:**

Яким чином `return` та `throw` працюють всередині лямбда-виразу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Поведінка `return` та `throw` всередині лямбди така ж, як і в звичайному методі.
>
> * **`return`:**
>   * Завершує виконання **тільки лямбда-виразу**, а не зовнішнього методу, в якому він оголошений.
>   * Значення, що повертається, стає результатом виклику абстрактного методу функціонального інтерфейсу.
>   * Якщо тіло лямбди однорядкове, `return` є неявним.
>
> * **`throw`:**
>   * Кидає виняток з лямбда-виразу.
>   * Якщо це **неперевірений виняток (`RuntimeException`)**, його можна кидати вільно.
>   * Якщо це **перевірений виняток (`Checked Exception`)**, це дозволено лише тоді, коли абстрактний метод функціонального інтерфейсу має відповідне оголошення `throws`.
>
> Важливо пам'ятати, що `return` всередині `.forEach()` не вийде з зовнішнього методу, як у звичайному циклі. Для цього потрібно використовувати більш складні конструкції або маркувати лямбди.

#### **📌 Що перевіряють:**

*Розуміння того, як стандартні конструкції керування потоком (`return`, `throw`) поводяться всередині лямбд, та їх взаємодію з функціональними інтерфейсами.*

* * *

### **14 ❓ Питання:**

Уявіть, що у вас є метод `void process(Runnable r)`. Як передати в нього реалізацію анонімним класом, лямбдою та посиланням на метод? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання на демонстрацію всіх трьох підходів для одного й того ж функціонального інтерфейсу (`Runnable`).

#### **📝 Приклад:**

```java
class Processor {
    public void executeTask() {
        System.out.println("Executing a task...");
    }

    public void runExamples() {
        // 1. Анонімний клас
        process(new Runnable() {
            @Override
            public void run() {
                System.out.println("Anonymous class running...");
            }
        });

        // 2. Лямбда-вираз
        process(() -> System.out.println("Lambda expression running..."));

        // 3. Посилання на метод (викликаємо існуючий метод executeTask())
        process(this::executeTask);
    }

    void process(Runnable r) {
        r.run();
    }
}
```

#### **📌 Що перевіряють:**

*Здатність "на льоту" застосовувати всі три синтаксиси для конкретної задачі, демонструючи гнучкість та знання мови.*

* * *

### **15 ❓ Питання:**

Як реалізовані лямбда-вирази під капотом і чому вони продуктивніші за анонімні класи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це глибоке питання про внутрішню роботу JVM.
>
> **Анонімні класи:**
> При компіляції для кожного анонімного класу генерується **окремий `.class` файл** (напр., `MyClass$1.class`). При використанні JVM змушена завантажити, верифікувати та ініціалізувати цей клас. Це може бути ресурсомістким, якщо анонімних класів багато.
>
> **Лямбда-вирази (починаючи з Java 8):**
>
> 1. **Немає додаткових `.class` файлів:** Компілятор **не створює** окремі файли для лямбд. Замість цього, він перетворює тіло лямбди в **приватний статичний метод** всередині того ж класу.
>
> 2. **Використання `invokedynamic`:** У місці, де використовується лямбда, генерується спеціальна інструкція байт-коду `invokedynamic`. Це дуже потужна інструкція, яка відкладає визначення того, як саме викликати код, до моменту першого виконання.
>
> 3. **"Лямбда-фабрика":** Під час першого виклику `invokedynamic` створює об'єкт-реалізацію функціонального інтерфейсу ("фабрика"), який пов'язує виклик з згенерованим статичним методом.
>
> **Чому це продуктивніше:**
>
> * **Уникає завантаження класів:** Значно зменшує навантаження на ClassLoader.
> * **Краща оптимізація:** Дозволяє JVM застосовувати сучасні стратегії оптимізації. Наприклад, для лямбд, що не захоплюють змінних, JVM може створити єдиний екземпляр-синглтон і перевикористовувати його.
> * **Гнучкість для майбутнього:** Стратегія зв'язування може бути покращена в майбутніх версіях JVM без необхідності перекомпілювати код.

#### **📌 Що перевіряють:**

*Експертне розуміння внутрішніх механізмів JVM. Це показує, що кандидат не просто використовує мовні конструкції, а й розуміє, як вони реалізовані та чому вони ефективні.*

* * *
