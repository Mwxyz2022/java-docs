---
layout: default
title: "Function<T, R> - детальний розбір"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 7
---

# Function<T, R> - детальний розбір

* короткий опис: `Function<T, R>` – функціональний інтерфейс, що приймає аргумент типу `T` і повертає результат типу `R`. Основний метод – `R apply(T t)`. Підтримує композицію функцій через `andThen()` та `compose()`.

* поняття: `apply()`, `compose()`, `andThen()`, `identity()`.

* приклади: `s -> s.length()` (`apply`), `f1.compose(f2)` (композиція у зворотному порядку), `f1.andThen(f2)` (композиція у прямому порядку).

* ключові моменти: Використовується для перетворень типів. `compose()` виконує передану функцію першою, `andThen()` – своєю функцією першою. `identity()` повертає вхід без змін.

* що важливо знати на співбесіді: Основні методи, як працює композиція (`compose()` vs `andThen()`).

### **✅ Офіційне визначення:**

**`Function<T, R>`** — це функціональний інтерфейс в пакеті **`java.util.function`**, який представляє функцію, що приймає аргумент типу **`T`** і повертає результат типу **`R`**. Основний метод — **`R apply(T t)`**.

### **🧠 Простими словами:**

**`Function`** — це конвертер або трансформер: ти даєш йому щось (**`T`**), він щось повертає (**`R`**). Можна уявити як “чорний ящик”, який перетворює дані

---

## 📊 **Перелік основних методів:**

|                                      Метод                                       |                                      Опис                                      |
|:--------------------------------------------------------------------------------:|:------------------------------------------------------------------------------:|
|                                **`R apply(T t)`**                                |          Застосовує функцію до аргументу **`t`**, повертає результат.          |
| **`default <V> Function<V,R> compose(Function<? super V, ? extends T> before)`** | Повертає нову функцію, яка спочатку застосовує **`before`**, потім цю функцію. |
| **`default <V> Function<T, V> andThen(Function<? super R, ? extends V> after)`** | Повертає нову функцію, яка спочатку застосовує цю функцію, потім **`after`**.  |
|                    **`static <T> Function<T, T> identity()`**                    |         Повертає функцію, що повертає свій вхідний аргумент без змін.          |

---

## **Пояснення на пальцях (💡)**

* **`apply()`** — це серце **`Function:`** “дай мені щось, я щось поверну”.
* **`compose()`** — ланцюжок з двох функцій: спочатку виконуємо передфункцію, потім цю. Приклад: *
  *`f.compose(g) = f(g(x)).`**
* **`andThen()`** — також ланцюжок, але спочатку виконуємо цю функцію, потім “післяфункцію”. Приклад: *
  *`f.andThen(g) = g(f(x)).`**
* **`identity()`** — функція, яка нічого не робить, просто повертає вхідне значення (нейтральна).

---

## **Корисні приклади (🧪)**

```java
Function<String, Integer> toLength = s -> s.length();
System.out.println(toLength.apply("Java")); // 4

Function<Integer, Integer> square = x -> x * x;

// compose: спочатку конвертуємо String у Integer, потім підносимо до квадрату
Function<String, Integer> lengthThenSquare = square.compose(toLength);
System.out.println(lengthThenSquare.apply("Hello")); // 25 (5*5)

// andThen: спочатку беремо довжину, потім підносимо до квадрату
Function<String, Integer> squareThenLength = toLength.andThen(square);
System.out.println(squareThenLength.apply("Hello")); // 25

// identity - повертає те саме, що і отримує
Function<String, String> identityFunc = Function.identity();
System.out.println(identityFunc.apply("Тест")); // Тест
```

---

## **Порівняння (⚖️)**

|      Метод       |         Послідовність виконання          |      Приклад (x)       |        Опис        |
|:----------------:|:----------------------------------------:|:----------------------:|:------------------:|
| **`compose()`**  | Спочатку **`before`**, потім ця функція  | f.compose(g) --> f(g(x)) | Зворотний порядок  |
| **`andThen()`**  |  Спочатку ця функція, потім **`after`**  | f.andThen(g) --> g(f(x)) |   Прямий порядок   |
| **`identity()`** | Просто повертає вхідне значення без змін |    identity(x) --> x     | Функція "дзеркало" |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **apply** — “прийми і віддай”
* **compose** — “спочатку інша, потім я” (назад по ланцюжку)
* **andThen** — “спочатку я, потім інша” (прямо по ланцюжку)
* **identity** — “дзеркало”, нічого не міняю

---

## **🟩 Підсумок:**

* **`Function<T, R>`** — основа для перетворень у Java-функціональному стилі.
* Метод **`apply()`** — виконання функції.
* **`compose()`** і **`andThen()`** — потужні інструменти для композиції функцій, дають можливість комбінувати прості функції у складні.
* **`identity()`** — корисний метод для повернення вхідного аргументу без змін.
* Володіння цим інтерфейсом — must-have на співбесідах і у повсякденній розробці.

---

### **🔗 Додаткові джерела:**

* [**Java Documentation — Function Interface**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/function/Function.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке функціональний інтерфейс `Function<T, R>` і яка його основна мета?

> #### **💬 Відповідь:**
>
> **`Function<T, R>`** — це один з ключових стандартних функціональних інтерфейсів з пакету `java.util.function`.
>
> **Його основна мета** — представити операцію, яка **приймає один аргумент** певного типу (`T`) і **повертає результат** іншого (або того ж самого) типу (`R`). По суті, це функціональна обгортка для операції **перетворення (transformation)**.
>
> Він має лише один абстрактний метод: **`R apply(T t)`**.
>
> `Function<T, R>` широко використовується в Stream API, зокрема в методі `map()`, для перетворення кожного елемента потоку з одного типу в інший.

#### **📝 Аналогія:**

Уявіть `Function` як конвеєрний прес. Ви кладете на нього один матеріал (`T`), а він видає вам готову деталь іншої форми (`R`). Наприклад, `Function<String, Integer>` — це прес, який з "металевого листа" (рядок) робить "гайку" (число).

#### **📌 Що перевіряють:**

*Базове розуміння призначення `Function`. Кандидат повинен знати, що цей інтерфейс використовується для перетворення даних.*

* * *

### **2 ❓ Питання:**

Який основний метод в інтерфейсі `Function` і що він робить? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Основний і єдиний абстрактний метод в `Function` — це **`R apply(T t)`**.
>
> **Що він робить:**
> Метод `apply()` приймає один аргумент типу `T` і виконує над ним логіку, визначену в лямбда-виразі або посиланні на метод, повертаючи результат типу `R`. Це, по суті, і є "тіло" функції.

#### **📝 Приклад:**

```java
import java.util.function.Function;

public class FunctionExample {
    public static void main(String[] args) {
        // Створюємо функцію, яка приймає рядок (T=String)
        // і повертає його довжину (R=Integer).
        Function<String, Integer> getLengthFunction = s -> s.length();

        // Застосовуємо функцію до вхідних даних
        int length = getLengthFunction.apply("Hello, World!");
        
        System.out.println("Довжина рядка: " + length); // 13
    }
}
```

#### **📌 Що перевіряють:**

*Знання ключового методу інтерфейсу та практичні навички його застосування для виконання функції.*

* * *

### **3 ❓ Питання:**

Поясніть різницю між `default`-методами `andThen()` та `compose()`.

> #### **💬 Відповідь:**
>
> Обидва методи використовуються для **композиції функцій** (тобто створення нової функції шляхом об'єднання двох), але вони відрізняються **порядком виконання**.
>
> * **`f.andThen(g)` ("спочатку `f`, а ПОТІМ `g`"):**
>   * Цей метод створює нову функцію, яка спочатку застосовує поточну функцію (`f`), а до її результату — передану функцію (`g`).
>   * **Порядок:** Зліва направо (більш інтуїтивний для читання).
>   * **Математично:** `g(f(x))`
>
> * **`f.compose(g)` ("спочатку `g`, а потім, на її результат, `f`"):**
>   * Цей метод створює функцію, яка спочатку застосовує **передану** функцію (`g`), а до її результату — **поточну** функцію (`f`).
>   * **Порядок:** Справа наліво (ближче до математичної композиції функцій).
>   * **Математично:** `f(g(x))`
>
> Фактично, `f.andThen(g)` еквівалентно `g.compose(f)`.

#### **📝 Приклад:**

```java
Function<Integer, Integer> multiplyBy2 = x -> x * 2;
Function<Integer, Integer> add3 = x -> x + 3;

// andThen: (5 * 2) + 3 = 13
Function<Integer, Integer> calculation1 = multiplyBy2.andThen(add3);
System.out.println(calculation1.apply(5)); // 13

// compose: (5 + 3) * 2 = 16
Function<Integer, Integer> calculation2 = multiplyBy2.compose(add3);
System.out.println(calculation2.apply(5)); // 16
```

#### **📌 Що перевіряють:**

*Розуміння концепції композиції функцій та знання API `Function`. Це класичне питання для перевірки глибоких знань Java 8.*

* * *

### **4 ❓ Питання:**

Що робить статичний метод `Function.identity()` і де його найчастіше використовують?

> #### **💬 Відповідь:**
>
> Статичний метод `Function.identity()` повертає спеціальну функцію, яка **завжди повертає свій вхідний аргумент без будь-яких змін**.
>
> По суті, це реалізація лямбди `t -> t`.
>
> **Де його найчастіше використовують:**
>
> Цей метод є дуже зручним "помічником" у `Stream API`, особливо при роботі з колекторами, коли потрібно вказати, що в якості значення або ключа слід використовувати сам елемент потоку.
>
> Найпоширеніший випадок — це створення `Map` з потоку об'єктів.
>
> **Приклад:** Є список об'єктів `User`, і ми хочемо створити мапу `Map<Long, User>`, де ключ — це ID користувача, а значення — **сам об'єкт `User`**.
>
> ```java
> Map<Long, User> userMap = users.stream()
>     .collect(Collectors.toMap(User::getId, Function.identity()));
> ```
>
> Використання `Function.identity()` є набагато більш читабельним і декларативним, ніж написання `user -> user`.

#### **📌 Що перевіряють:**

*Знання ідіоматичних конструкцій функціонального програмування в Java. `identity()` — важливий елемент для написання чистого коду зі Stream API.*

* * *

### **5 ❓ Питання:**

У чому відмінність між `Function<T, R>` та `Predicate<T>`?

> #### **💬 Відповідь:**
>
> Обидва є функціональними інтерфейсами, що приймають один аргумент, але вони мають різне призначення, що відображається у типі їхнього повернутого значення.
>
> * **`Function<T, R>` (Перетворювач):**
>   * **Призначення:** **Перетворити** один об'єкт (`T`) в інший (`R`).
>   * **Метод:** `R apply(T t)`
>   * **Повертає:** Значення **будь-якого типу `R`**.
>   * **Використання:** `Stream.map()`
>
> * **`Predicate<T>` (Перевіряльник):**
>   * **Призначення:** **Перевірити** об'єкт `T` на відповідність певній умові.
>   * **Метод:** `boolean test(T t)`
>   * **Повертає:** Завжди **`boolean`**.
>   * **Використання:** `Stream.filter()`
>
> **Коротко:** `Function` відповідає на питання "що з цим зробити?", а `Predicate` — "чи це правда?".

#### **📌 Що перевіряють:**

*Розуміння семантичної різниці між основними функціональними інтерфейсами.*

* * *

### **6 ❓ Питання:**

Як за допомогою `Function` та Stream API перетворити список рядків на список їх довжин?

> #### **💬 Відповідь:**
>
> Для цього завдання ідеально підходить метод `Stream.map()`, який приймає `Function<T, R>` для перетворення кожного елемента потоку.
>
> 1. Отримуємо `Stream` зі списку рядків (`Stream<String>`).
> 2. Використовуємо `map()` для застосування функції, що перетворює кожен рядок (`String`) на його довжину (`Integer`). Для цього можна використати лямбду `s -> s.length()` або, що краще, посилання на метод `String::length`.
> 3. Збираємо результати в новий список (`List<Integer>`).

#### **📝 Приклад:**

```java
List<String> words = Arrays.asList("Java", "is", "awesome");

// Функція для отримання довжини
Function<String, Integer> getLength = String::length;

// Застосовуємо її в stream().map()
List<Integer> lengths = words.stream()
                             .map(getLength)
                             .collect(Collectors.toList());

System.out.println(lengths); // [4, 2, 7]
```

#### **📌 Що перевіряють:**

*Практичні навички застосування `Function` разом із Stream API для вирішення типових завдань з перетворення даних.*

* * *

### **7 ❓ Питання:**

Що таке `BiFunction<T, U, R>` і в якому сценарії він корисний?

> #### **💬 Відповідь:**
>
> **`BiFunction<T, U, R>`** — це "bi"-версія `Function`, яка приймає **два аргументи** типів `T` та `U` і повертає результат типу `R`.
>
> Його основний метод: **`R apply(T t, U u)`**.
>
> **Сценарії використання:**
> Він корисний для будь-якої операції, яка вимагає об'єднання або обробки двох різних значень для отримання третього.
>
> **Приклад:**
>
> * **Створення об'єктів:** Прийняти ім'я та вік, і повернути новий об'єкт `User`.
> * **Обчислення:** Прийняти два числа і повернути їх суму (хоча для цього краще підходить `BinaryOperator`, якщо типи однакові).
> * **Обробка результатів:** У методі `CompletableFuture.thenCombine()`, який об'єднує результати двох асинхронних операцій.
> * **Метод `Map.merge()`** приймає `BiFunction` для вирішення конфлікту, коли ключ вже існує.

#### **📝 Приклад з `Map.merge()`:**

```java
Map<String, Integer> wordCounts = new HashMap<>();
wordCounts.put("hello", 1);

// Якщо ключ "hello" вже є, застосувати BiFunction для об'єднання значень
wordCounts.merge("hello", 1, (oldValue, newValue) -> oldValue + newValue); // 1 + 1 = 2
wordCounts.merge("world", 1, (oldValue, newValue) -> oldValue + newValue); // ключа не було, просто вставить 1

System.out.println(wordCounts); // {hello=2, world=1}
```

#### **📌 Що перевіряють:**

*Знання розширених стандартних функціональних інтерфейсів та їх практичного застосування.*

* * *

### **8 ❓ Питання:**

Що таке примітивні спеціалізації `Function` (напр., `IntFunction`, `ToIntFunction`) і в чому їхня особливість? ⭐️

> #### **💬 Відповідь:**
>
> Це версії інтерфейсу `Function`, оптимізовані для роботи з примітивними типами, щоб **уникнути boxing/unboxing**.
>
> Їхні назви говорять самі за себе:
>
> * **`IntFunction<R>`:** `R apply(int value)` — приймає `int`, повертає об'єкт `R`.
> * **`ToIntFunction<T>`:** `int applyAsInt(T value)` — приймає об'єкт `T`, повертає `int`.
> * **`IntToLongFunction`**: `long applyAsLong(int value)` — приймає `int`, повертає `long`.
> * Також існують `LongFunction`, `DoubleFunction`, `ToLongFunction` і т.д.
>
> **Особливість:** Вони працюють з примітивами напряму, що значно підвищує продуктивність при обробці великих масивів числових даних у стрімах. Наприклад, `mapToInt(ToIntFunction)` в `Stream API` є набагато ефективнішим, ніж `map(Function)`, якщо ви працюєте з числами.

#### **📌 Що перевіряють:**

*Розуміння необхідності оптимізації при роботі з примітивами в функціональному стилі.*

* * *

### **9 ❓ Питання:**

Як можна використовувати композицію функцій (`andThen` / `compose`) для створення складного ланцюжка перетворень? ⭐️

> #### **💬 Відповідь:**
>
> Композиція дозволяє будувати складні операції з маленьких, перевикористовуваних функцій, роблячи код більш чистим та модульним.
>
> **Сценарій:** У нас є рядок, що містить числа через кому (`"1,2,3"`). Нам потрібно:
>
> 1. Розділити рядок на масив.
> 2. Взяти перший елемент.
> 3. Перетворити його на число.
> 4. Піднести до квадрату.
>
> Замість того, щоб писати один великий метод, ми можемо створити ланцюжок функцій.

#### **📝 Приклад:**

```java
Function<String, String[]> split = s -> s.split(",");
Function<String[], String> getFirst = arr -> arr[0];
Function<String, Integer> toInt = Integer::parseInt;
Function<Integer, Integer> square = x -> x * x;

// Комбінуємо все в один ланцюжок за допомогою andThen
Function<String, Integer> complexFunction = split.andThen(getFirst)
                                                 .andThen(toInt)
                                                 .andThen(square);
                                                 
System.out.println(complexFunction.apply("5,6,7")); // 25
```

Цей підхід робить кожен крок перетворення явним, зрозумілим та легким для тестування окремо.

#### **📌 Що перевіряють:**

*Здатність мислити у функціональному стилі та використовувати композицію для побудови складних алгоритмів.*

* * *

### **10 ❓ Питання:**

Чи може `Function` повертати `null`? Чи це гарна практика? ⭐️

> #### **💬 Відповідь:**
>
> **Так, `Function` може повертати `null`.** Інтерфейс ніяк не обмежує цього. Метод `apply` може повернути будь-яке значення типу `R`, включаючи `null`.
>
> **Чи це гарна практика?**
> **Ні, це вважається дуже поганою практикою.**
>
> **Причини:**
>
> * **Ризик `NullPointerException`:** Код, що викликає функцію, може не очікувати `null` і спробувати викликати на ньому якийсь метод, що призведе до `NPE`. Особливо це небезпечно у `Stream API`, де один `null` може "зламати" весь ланцюжок.
> * **Неявний контракт:** Повернення `null` створює неявний контракт, змушуючи клієнтів коду додавати перевірки `if (result != null)`.
>
> **Краща альтернатива:**
> Замість повернення `null` слід використовувати клас **`Optional<R>`**. Це явно вказує на те, що результат може бути відсутнім, і змушує клієнтський код обробляти цей випадок безпечно (за допомогою `isPresent()`, `orElse()`, `ifPresent()`).
>
> **Погано:** `Function<Long, User> findUser = id -> (id > 0) ? new User() : null;`
> **Добре:** `Function<Long, Optional<User>> findUser = id -> (id > 0) ? Optional.of(new User()) : Optional.empty();`

#### **📌 Що перевіряють:**

*Розуміння принципів написання чистого та надійного коду. Знання проблем, пов'язаних з `null`, та вміння використовувати `Optional` є ознакою хорошого Java-розробника.*

* * *

### **11 ❓ Питання:**

Яким чином `Function.identity()` може бути корисний при зборі даних в мапу зі збереженням дублікатів ключів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Сам по собі `Function.identity()` не вирішує проблему дублікатів, але він є частиною ідіоматичного рішення цієї задачі в `Collectors.toMap`.
>
> **Проблема:** Стандартний `Collectors.toMap(keyMapper, valueMapper)` кидає `IllegalStateException`, якщо зустрічає дублікат ключа.
>
> **Рішення:** Потрібно використовувати перевантажену версію `toMap`, яка приймає третій аргумент — **`mergeFunction`**. Це `BinaryOperator`, який визначає, як об'єднати два значення, якщо для них знайдено однаковий ключ.
>
> **Як `identity()` допомагає тут:**
> Уявімо, ми хочемо згрупувати список рядків за їх довжиною, отримавши `Map<Integer, List<String>>`. Ми можемо використати `Collectors.groupingBy`.
>
> А якщо ми хочемо зібрати мапу `Map<String, User>`, але деякі користувачі мають однакове ім'я? `identity()` можна використати, якщо потрібно вирішити, який з дублікатів залишити.
>
> ```java
> List<User> users = ...;
> Map<String, User> userMap = users.stream().collect(
>     Collectors.toMap(User::getName, 
>                      Function.identity(), // або user -> user
>                      (existingUser, newUser) -> existingUser) // mergeFunction: при дублікаті залишаємо існуючого
> );
> ```
>
> Тут `Function.identity()` елегантно передає сам об'єкт `User` як значення для мапи.

#### **📌 Що перевіряють:**

*Знання просунутих можливостей Stream API та колекторів. Це показує, як кандидат вирішує поширені проблеми з обробкою даних.*

* * *

### **12 ❓ Питання:**

Чи можна реалізувати `Function` за допомогою анонімного класу? Чи є сенс робити це після Java 8? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Будь-який функціональний інтерфейс можна реалізувати за допомогою анонімного класу.
>
> ```java
> Function<String, Integer> getLength = new Function<String, Integer>() {
>     @Override
>     public Integer apply(String s) {
>         return s.length();
>     }
> };
> ```
>
> **Чи є сенс робити це після Java 8?**
>
> **Практично ні.** Для реалізації функціонального інтерфейсу лямбда-вирази або посилання на методи є значно кращим вибором: вони компактніші, читабельніші та потенційно більш продуктивні.
>
> **Єдиний, дуже рідкісний виняток:**
> Коли вам потрібен екземпляр `Function`, який має **власний стан (поля)**. Лямбда-вираз є безстановим, а анонімний клас може мати власні поля. Однак, такий дизайн (`Function` зі станом) зазвичай є поганою практикою і суперечить ідеям функціонального програмування.

#### **📌 Що перевіряють:**

*Розуміння еволюції мови. Кандидат повинен знати про "старі" способи, але також розуміти, чому "нові" є кращими, і коли старі все ще можуть бути потрібними.*

* * *

### **13 ❓ Питання:**

Як за допомогою `Function` та композиції створити функцію, яка виконує валідацію, а потім перетворення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для такої задачі можна скомбінувати `Function` з `Predicate`. Проте, якщо ми хочемо залишитися в межах `Function`, ми можемо створити функцію, яка або повертає перетворене значення, або кидає виняток.
>
> **Підхід через композицію:**
>
> 1. Створюємо першу функцію, яка виконує валідацію. Вона або повертає вхідне значення без змін, або кидає виняток.
> 2. Створюємо другу функцію, яка виконує перетворення.
> 3. Об'єднуємо їх за допомогою `andThen()`.
>
> Цей підхід робить кожен крок явним та перевикористовуваним.

#### **📝 Приклад:**

```java
// Функція-валідатор
Function<String, String> validator = (input) -> {
    if (input == null || input.isBlank()) {
        throw new IllegalArgumentException("Input cannot be blank");
    }
    return input; // Повертає вхідне значення, якщо валідне
};

// Функція-трансформатор
Function<String, Integer> toLength = String::length;

// Комбінуємо: спочатку валідація, потім перетворення
Function<String, Integer> validatedToLength = validator.andThen(toLength);

System.out.println(validatedToLength.apply("Java")); // 4

// Кине IllegalArgumentException
assertThrows(IllegalArgumentException.class, () -> validatedToLength.apply(""));
```

#### **📌 Що перевіряють:**

*Здатність проектувати складні ланцюжки операцій у функціональному стилі, поєднуючи перевірку та обробку.*

* * *

### **14 ❓ Питання:**

Поясніть, що таке "часткове застосування" (partial application) і як його можна імітувати за допомогою `Function` в Java. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Часткове застосування** — це техніка функціонального програмування, де функція з кількома аргументами перетворюється на функцію з меншою кількістю аргументів шляхом "фіксації" одного або декількох початкових аргументів.
>
> Java не має вбудованої підтримки для цього, але це можна легко імітувати, використовуючи функції вищого порядку (функції, що повертають інші функції).
>
> **Як імітувати:**
> Ми можемо створити метод, який приймає один з аргументів і повертає `Function`, яка очікує на решту аргументів.
>
> **Приклад:**
> Нехай є `BiFunction<Double, Double, Double> power = (base, exp) -> Math.pow(base, exp);`
>
> Ми хочемо створити функцію "квадрат" (`x -> x²`) шляхом часткового застосування, зафіксувавши другий аргумент `exp = 2`.
>
> ```java
> public static Function<Double, Double> toPowerOf(double exponent) {
>     return base -> Math.pow(base, exponent);
> }
> 
> // Часткове застосування
> Function<Double, Double> square = toPowerOf(2.0);
> Function<Double, Double> cube = toPowerOf(3.0);
> 
> System.out.println(square.apply(5.0)); // 25.0
> System.out.println(cube.apply(3.0));   // 27.0
> ```
>
> Тут метод `toPowerOf` є фабрикою, яка створює нові, спеціалізовані функції шляхом "фіксації" одного з параметрів.

#### **📌 Що перевіряють:**

*Глибокі знання концепцій функціонального програмування (каррінг, часткове застосування) та здатність реалізовувати ці патерни в Java. Це питання рівня Senior.*

* * *

### **15 ❓ Питання:**

Які ризики несе захоплення (capturing) мутабельних об'єктів всередині `Function`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Захоплення **мутабельних** об'єктів всередині лямбди або `Function` є **дуже небезпечною практикою**, особливо в контексті паралельної обробки (наприклад, у `parallelStream()`).
>
> **Ризики:**
>
> 1. **Непередбачувані результати:** Якщо функція змінює захоплений об'єкт, її результат стає залежним не тільки від вхідних параметрів, а й від зовнішнього стану. Це руйнує чистоту функції. Якщо кілька потоків викликають таку функцію паралельно, вони будуть змагатися за зміну одного й того ж об'єкта.
>
> 2. **Проблеми з потокобезпечністю (Race Conditions):** Коли кілька потоків одночасно намагаються змінити один і той же мутабельний об'єкт (наприклад, додавати елементи до спільного `ArrayList`), це може призвести до пошкодження даних, винятків (`ConcurrentModificationException`) або непередбачуваної поведінки.
>
> **Правильний підхід:**
>
> `Function` та інші функціональні інтерфейси, що використовуються в `Stream API`, повинні бути **безстановими (stateless)** і не мати **побічних ефектів (side-effects)**. Вся логіка має залежати лише від вхідних параметрів. Замість того, щоб змінювати зовнішню колекцію, слід використовувати колектори `Stream API` для збору результатів у нову, безпечну структуру.
>
> **Погано:**
>
> ```java
> List<String> results = new ArrayList<>();
> source.parallelStream().forEach(item -> results.add(item.toString())); // НЕБЕЗПЕЧНО!
> ```
>
> **Добре:**
>
> ```java
> List<String> results = source.parallelStream().map(Object::toString).collect(Collectors.toList());
> ```

#### **📌 Що перевіряють:**

*Розуміння основних принципів функціонального програмування (чистота функцій, відсутність побічних ефектів) та правил безпечної роботи з паралельними потоками. Це критично важливе знання для написання надійного та передбачуваного коду.*

* * *
