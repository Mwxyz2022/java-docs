---
layout: default
title: "Stream API - основи"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 15
---

# Stream API - основи

* короткий опис: Stream API (з Java 8) – це компонент, що дозволяє працювати з колекціями даних у функціональному стилі, використовуючи послідовності елементів (потоки) та ланцюжок операцій (проміжних та термінальних).

* поняття: `Stream`, проміжні операції, термінальні операції, `Lazy evaluation` (лінивість), `Predicate`, `Function`, `Consumer`.

* приклади: `List.stream().filter(name -> name.startsWith("A")).map(String::toUpperCase).collect(Collectors.toList());`.

* ключові моменти: Не змінює початкове джерело, працює як pipeline, лінивий (обчислення лише при термінальній операції).

* що важливо знати на співбесіді: Як функціональні інтерфейси застосовуються у Stream API, базові операції.

### **✅ Офіційне визначення:**

**`Stream API`** — це компонент Java з версії 8, який дозволяє працювати з колекціями даних у функціональному стилі, використовуючи послідовності елементів (потоки) та ланцюжок операцій.

### **🧠 Простими словами:**

Це як конвеєр для даних: ти передаєш потік елементів через набір функцій, які фільтрують, перетворюють чи збирають їх. При цьому не змінюється вихідна колекція — усе працює «по-новому» і часто ліниво

---

## 📊 **Ключові концепції Stream API:**

|           Поняття            |                                  Опис                                  |
|:----------------------------:|:----------------------------------------------------------------------:|
|          **Stream**          |      Потік елементів з джерела (колекція, масив, генератор тощо)       |
|    **Проміжні операції**     |      Не виконуються одразу, а лише будують обчислювальний ланцюг       |
|   **Термінальні операції**   |   Запускають обробку потоку та повертають результат або побічну дію    |
|     **Лінивість (Lazy)**     | Дані обробляються лише по потребі (тільки після термінальної операції) |
| **Функціональні інтерфейси** |       Predicate, Function, Consumer — основа для Stream-операцій       |

---

## **Пояснення на пальцях (💡)**

* Уяви потік мандаринок (елементів), які ти пропускаєш через низку «машин» (**`filter`**, **`map`**).
* Але ці машини не починають працювати, поки не скажеш "Збери\!" (collect).
* Stream API дозволяє писати менше циклів **`for`**, а більше логіки через функції.

---

## **Корисні приклади (🧪)**

```java
List<String> names = List.of("Alice", "Bob", "Charlie", "Anna");

// Проміжні операції: filter, map
// Термінальна операція: collect
List<String> result = names.stream()
    .filter(name -> name.startsWith("A")) // Predicate
    .map(String::toUpperCase)             // Function
    .collect(Collectors.toList());        // Результат
```

```java
// forEach -- термінальна операція, що виконує дію (Consumer)
names.stream()
     .forEach(System.out::println);
```

---

## **Порівняння (⚖️)**

|         Критерій         | Звичайний цикл  |                  Stream API                   |
|:------------------------:|:---------------:|:---------------------------------------------:|
| **Стисло та читабельно** |       Ні        |                      Так                      |
|  **Підтримка функцій**   |      Немає      |         Є (Function, Predicate тощо)          |
|   **Багатопоточність**   | Потрібно вручну | Можна легко перейти на **`parallelStream()`** |
|    **Лінива обробка**    |       Ні        |                      Так                      |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"FMC" — як скорочення від "Фільтруй-Мап-Колект":**

* **F** — **`filter()`**
* **M** — **`map()`**
* **C** — **`collect()`**

Це базова комбінація для 80% задач у Stream API.

---

## **🟩 Підсумок:**

* Stream API — це спосіб обробки колекцій у функціональному стилі без зміни даних.
* Потік будується з проміжних операцій і активується термінальною.
* Широко використовує функціональні інтерфейси: **`Predicate`**, **`Function`**, **`Consumer`**.
* Переваги — лаконічність, читабельність, можливість паралельної обробки.
* Знання Stream API — одне з найчастіших очікувань на Java-співбесідах.

---

### **🔗 Додаткові джерела:**

* [**Oracle Stream API Guide**](https://docs.oracle.com/javase/8/docs/api/java/util/stream/package-summary.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Stream API в Java і яка його головна мета?

> #### **💬 Відповідь:**
>
> **Stream API**, введене в Java 8, — це **інструмент для обробки послідовностей даних (колекцій, масивів тощо) у функціональному, декларативному стилі**.
>
> **Stream** — це не структура даних, а потік елементів, що дозволяє застосовувати до них ланцюжок операцій.
>
> **Головна мета:**
>
> * **Спрощення коду:** Дозволяє замінити громіздкі імперативні цикли (`for`, `while`) на лаконічні та читабельні ланцюжки операцій.
> * **Декларативний підхід:** Ви описуєте **"що"** ви хочете отримати, а не **"як"** крок за кроком це робити.
> * **Лінива обробка (Lazy Evaluation):** Операції виконуються тільки тоді, коли потрібен кінцевий результат, що підвищує продуктивність.
> * **Проста паралелізація:** Легко розпаралелити обробку, просто замінивши `.stream()` на `.parallelStream()`.

#### **📝 Аналогія:**

Уявіть, що ваші дані — це потік води. Stream API — це система труб та фільтрів.

* Ви створюєте потік води з джерела (`.stream()`).
* Встановлюєте фільтри (`.filter()`) та станції для зміни кольору води (`.map()`).
* І тільки коли ви підставляєте відро в кінці (`.collect()`), вода починає текти через всю систему.

#### **📌 Що перевіряють:**

*Базове розуміння того, що таке `Stream` і яку проблему він вирішує. Кандидат повинен знати, що це не структура даних, а інструмент для обробки даних.*

* * *

### **2 ❓ Питання:**

Назвіть та поясніть дві основні категорії операцій у Stream API.

> #### **💬 Відповідь:**
>
> Усі операції в Stream API поділяються на дві категорії:
>
> **1. Проміжні операції (Intermediate Operations):**
>
> * **Що роблять:** Приймають потік і **повертають новий потік**. Вони завжди є **лінивими (lazy)**, тобто не виконуються одразу, а лише будують "план" або "рецепт" обробки.
> * **Призначення:** Фільтрація, перетворення, сортування, обмеження потоку.
> * **Приклади:** `filter()`, `map()`, `sorted()`, `distinct()`, `limit()`, `skip()`.
>
> **2. Термінальні операції (Terminal Operations):**
>
> * **Що роблять:** **Запускають** обробку потоку, який був побудований проміжними операціями. Після виконання термінальної операції потік "споживається" і не може бути перевикористаний.
> * **Призначення:** Збір результатів у колекцію, обчислення значень, виконання дій.
> * **Приклади:** `collect()`, `forEach()`, `count()`, `reduce()`, `findFirst()`, `anyMatch()`.

#### **📝 Коментар:**

Ланцюжок стріму може містити скільки завгодно проміжних операцій, але лише одну термінальну, яка його завершує.

#### **📌 Що перевіряють:**

*Розуміння ключової концепції "лінивості" та різниці між операціями, що будують потік, та тими, що його запускають. Це фундаментальне знання для роботи зі Stream API.*

* * *

### **3 ❓ Питання:**

Які є способи створення потоку (`Stream`)?

> #### **💬 Відповідь:**
>
> Існує багато способів створити потік з різних джерел:
>
> 1. **З колекції:**
>     Найпоширеніший спосіб. Будь-яка колекція (`List`, `Set` тощо) має метод `.stream()`.
>
>     ```java
>     List<String> list = ...;
>     Stream<String> stream = list.stream();
>     ```
>
> 2. **З масиву:**
>     Використовується статичний метод `Arrays.stream()` або `Stream.of()`.
>
>     ```java
>     String[] array = {"a", "b"};
>     Stream<String> streamFromArray = Arrays.stream(array);
>     ```
>
> 3. **З окремих елементів:**
>     За допомогою статичного методу `Stream.of()`.
>
>     ```java
>     Stream<String> streamOfElements = Stream.of("a", "b", "c");
>     ```
>
> 4. **З файлу:**
>     Метод `Files.lines()` повертає `Stream<String>`, де кожен елемент — це рядок файлу.
>
>     ```java
>     Stream<String> streamOfLines = Files.lines(Paths.get("file.txt"));
>     ```
>
> 5. **З нескінченних послідовностей:**
>     * `Stream.iterate(seed, unaryOperator)`: створює нескінченний потік, послідовно застосовуючи функцію.
>     * `Stream.generate(supplier)`: створює нескінченний потік, викликаючи `Supplier`.

#### **📌 Що перевіряють:**

*Практичні навички роботи зі Stream API. Кандидат повинен знати, як створити потік з найпоширеніших джерел даних.*

* * *

### **4 ❓ Питання:**

Поясніть, що роблять основні проміжні операції: `filter()`, `map()` та `sorted()`.

> #### **💬 Відповідь:**
>
> Це три найчастіше використовувані проміжні операції:
>
> * **`filter(Predicate<T> predicate)`:**
>   * **Призначення:** **Фільтрація**.
>   * **Що робить:** Приймає `Predicate` і повертає новий потік, що містить лише ті елементи, для яких предикат повернув `true`.
>   * **Приклад:** `stream.filter(s -> s.startsWith("A"))` — залишить лише рядки, що починаються з "А".
>
> * **`map(Function<T, R> mapper)`:**
>   * **Призначення:** **Перетворення**.
>   * **Що робить:** Приймає `Function` і застосовує її до кожного елемента, повертаючи новий потік з перетворених елементів. Тип елементів може змінюватися.
>   * **Приклад:** `stream.map(String::length)` — перетворить потік рядків `Stream<String>` на потік їхніх довжин `Stream<Integer>`.
>
> * **`sorted()` / `sorted(Comparator<T> comparator)`:**
>   * **Призначення:** **Сортування**.
>   * **Що робить:** Повертає новий потік, елементи якого відсортовані.
>     * `sorted()`: сортує за природним порядком (`Comparable`).
>     * `sorted(comparator)`: сортує за допомогою наданого `Comparator`.

#### **📌 Що перевіряють:**

*Знання основних "будівельних блоків" Stream API. `filter` і `map` використовуються майже в кожному ланцюжку стрімів.*

* * *

### **5 ❓ Питання:**

Які є основні термінальні операції і як вони відрізняються?

> #### **💬 Відповідь:**
>
> Термінальні операції можна умовно поділити на ті, що повертають результат, і ті, що виконують дію.
>
> **1. Збір результатів:**
>
> * **`collect(Collector collector)`:**
>     Найпотужніша операція. Збирає елементи потоку в колекцію (`List`, `Set`) або мапу (`Map`) за допомогою `Collectors`.
>   * Приклад: `collect(Collectors.toList())`.
>
> * **`toArray()`:**
>     Збирає елементи в масив.
>
> * **`reduce(...)`:**
>     Виконує операцію редукції, згортаючи потік до одного значення (напр., сума, максимум).
>
> **2. Виконання дій:**
>
> * **`forEach(Consumer<T> action)`:**
>     Виконує дію для кожного елемента потоку. **Не гарантує порядок** при паралельній обробці.
>
> * **`forEachOrdered(Consumer<T> action)`:**
>     Те ж саме, але **гарантує** виконання в порядку, визначеному потоком.
>
> **3. Перевірка та пошук:**
>
> * **`count()`**: Повертає кількість елементів.
> * **`anyMatch(Predicate)`, `allMatch(Predicate)`, `noneMatch(Predicate)`**: Перевіряють, чи відповідає умові хоч один, всі чи жоден елемент.
> * **`findFirst()`, `findAny()`**: Повертають `Optional` з першим знайденим елементом.

#### **📌 Що перевіряють:**

*Знання основних способів завершення роботи зі стрімом та отримання результату.*

* * *

### **6 ❓ Питання:**

Поясніть концепцію "лінивої оцінки" (Lazy Evaluation) в Stream API на прикладі.

> #### **💬 Відповідь:**
>
> **Лінива оцінка** означає, що проміжні операції (`filter`, `map` і т.д.) **не виконуються одразу**, коли їх викликають. Вони лише будують "конвеєр" або план обчислень.
>
> Реальні обчислення починаються **лише тоді, коли викликається термінальна операція**. При цьому елементи обробляються "вертикально", а не "горизонтально".
>
> **Приклад:**
>
> ```java
> List.of(1, 2, 3, 4, 5)
>     .stream()
>     .filter(n -> {
>         System.out.println("filtering: " + n);
>         return n % 2 == 0;
>     })
>     .map(n -> {
>         System.out.println("mapping: " + n);
>         return n * 2;
>     })
>     .findFirst();
> ```
>
> **Що буде виведено:**
>
> ```
> filtering: 1
> filtering: 2
> mapping: 2
> ```
>
> **Що сталося:**
>
> 1. `filter` взяв перший елемент `1`, він не пройшов умову.
> 2. `filter` взяв другий елемент `2`, він **пройшов** умову.
> 3. `map` взяв елемент `2`, перетворив його на `4`.
> 4. Термінальна операція `findFirst` отримала перший елемент (`4`) і **негайно завершила роботу**.
>
> Потік навіть не дивився на елементи 3, 4, 5. Це значно підвищує продуктивність, особливо для великих потоків.

#### **📌 Що перевіряють:**

*Глибоке розуміння внутрішньої роботи Stream API. Це ключова концепція, яка пояснює багато аспектів продуктивності.*

* * *

### **7 ❓ Питання:**

Чим `stream()` відрізняється від `parallelStream()`? ⭐️

> #### **💬 Відповідь:**
>
> * **`stream()`**:
>   * Створює **послідовний (sequential)** потік.
>   * Всі операції виконуються в **одному потоці**, один елемент за іншим.
>   * Використовується за замовчуванням. Підходить для більшості завдань.
>
> * **`parallelStream()`**:
>   * Створює **паралельний** потік.
>   * Java намагається розбити потік даних на кілька частин і обробити їх одночасно в **кількох потоках**. За замовчуванням для цього використовується спільний `ForkJoinPool`.
>   * Може значно прискорити обробку на багатоядерних процесорах.
>
> **Коли використовувати `parallelStream()`:**
>
> * Тільки для **великих обсягів даних** (десятки тисяч елементів і більше). Для малих колекцій накладні витрати на розпаралелювання будуть більшими за вигоду.
> * Тільки якщо операції, що виконуються над елементами, є **незалежними** та **не мають побічних ефектів**, що змінюють спільний стан.
>
> Використання `parallelStream()` з неправильним кодом може призвести до race conditions та непередбачуваних результатів.

#### **📌 Що перевіряють:**

*Розуміння того, як працює паралельна обробка в Stream API, та пов'язаних з цим ризиків. Це питання на розуміння concurrency.*

* * *

### **8 ❓ Питання:**

Чи можна перевикористати `Stream` після виклику термінальної операції? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Потік є **одноразовим**.
>
> Після того, як на потоці викликано термінальну операцію (`collect`, `forEach`, `count` тощо), він вважається **"спожитим" (consumed)**.
>
> Спроба викликати будь-яку іншу операцію (проміжну чи термінальну) на тому ж екземплярі потоку призведе до винятку **`IllegalStateException`** з повідомленням "stream has already been operated upon or closed".
>
> Якщо вам потрібно виконати іншу операцію з тими ж даними, ви повинні **створити новий потік** з початкового джерела (колекції, масиву).

#### **📝 Приклад:**

```java
List<String> list = List.of("a", "b", "c");
Stream<String> stream = list.stream();

stream.forEach(System.out::println); // OK, потік спожито

// ПОМИЛКА: IllegalStateException
long count = stream.count();
```

#### **📌 Що перевіряють:**

*Знання одного з основних правил роботи зі стрімами. Це поширена помилка для початківців.*

* * *

### **9 ❓ Питання:**

Чи змінює `Stream` початкову колекцію, з якої його створено? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, ніколи.** `Stream API` розроблено з дотриманням принципу **імутабельності (незмінності)** джерела даних.
>
> Операції над потоком, такі як `filter()` чи `map()`, не змінюють початкову колекцію. Вони повертають **новий потік** з відфільтрованими або перетвореними елементами.
>
> Кінцева термінальна операція, як-от `collect(Collectors.toList())`, також створює **нову колекцію**, а не модифікує стару.
>
> Цей підхід є безпечним і відповідає принципам функціонального програмування.

#### **📝 Коментар:**

Однак, якщо елементи всередині потоку є мутабельними об'єктами, то можна змінити їхній стан, наприклад, через `forEach`. Це вважається поганою практикою і побічним ефектом.

#### **📌 Що перевіряють:**

*Розуміння, що стріми працюють з даними, не змінюючи їх, що є однією з ключових переваг цього API.*

* * *

### **10 ❓ Питання:**

Що таке операція `reduce()` і для чого вона використовується? ⭐️

> #### **💬 Відповідь:**
>
> **`reduce()`** — це термінальна операція, яка "згортає" (редукує) всі елементи потоку в **одне єдине значення** за допомогою заданої функції.
>
> Вона приймає:
>
> 1. **Початкове значення (identity):** Значення, з якого починається редукція, і яке повертається, якщо потік порожній.
> 2. **Акумулятор (accumulator):** `BinaryOperator`, який приймає проміжний результат і наступний елемент, і повертає новий проміжний результат.
>
> **Приклади використання:**
>
> * Знаходження суми всіх чисел.
> * Знаходження максимального або мінімального елемента.
> * Конкатенація всіх рядків в один.

#### **📝 Приклад (сума чисел):**

```java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);

// 0 - початкове значення
// (subtotal, element) -> subtotal + element - акумулятор
int sum = numbers.stream().reduce(0, (subtotal, element) -> subtotal + element);
// або з посиланням на метод
int sumWithRef = numbers.stream().reduce(0, Integer::sum);

System.out.println(sum); // 15
```

#### **📌 Що перевіряють:**

*Знання однієї з найпотужніших операцій в Stream API та вміння її застосовувати для агрегації даних.*

* * *

### **11 ❓ Питання:**

У чому різниця між проміжними операціями `map()` та `flatMap()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидві операції призначені для перетворення елементів, але вони працюють на різних рівнях.
>
> * **`map(Function<T, R> mapper)`:**
>   * **Що робить:** Перетворює **один елемент на один елемент** (`one-to-one` mapping).
>   * **Результат:** Якщо у вас `Stream<T>`, то після `map` ви отримаєте `Stream<R>`.
>
> * **`flatMap(Function<T, Stream<R>> mapper)`:**
>   * **Що робить:** Перетворює **один елемент на нуль, один або більше елементів** (`one-to-many` mapping). Функція, передана в `flatMap`, повинна повертати `Stream`.
>   * **Результат:** `flatMap` "розгортає" або "сплющує" всі отримані потоки в один єдиний потік. Якщо у вас `Stream<T>`, а маппер повертає `Stream<R>`, то кінцевий результат — це `Stream<R>`, а не `Stream<Stream<R>>`.
>
> **Коли використовувати `flatMap`:**
> Коли кожен ваш елемент є колекцією або може генерувати потік інших елементів, і ви хочете об'єднати їх усі в один спільний потік.

#### **📝 Приклад:**

```java
List<List<Integer>> listOfLists = List.of(List.of(1, 2), List.of(3, 4));

// map створить потік потоків
Stream<Stream<Integer>> streamOfStreams = listOfLists.stream().map(List::stream);

// flatMap "сплющить" потік потоків в один потік чисел
Stream<Integer> flattenedStream = listOfLists.stream().flatMap(List::stream);
flattenedStream.forEach(System.out::print); // 1234
```

#### **📌 Що перевіряють:**

*Розуміння однієї з найскладніших для початківців, але дуже потужних операцій. Це питання часто ставлять на співбесідах для перевірки глибоких знань.*

* * *

### **12 ❓ Питання:**

Які операції в Stream API називаються short-circuiting? Наведіть приклади. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Short-circuiting (операції з коротким замиканням)** — це операції, які можуть завершити обробку потоку, **не перебираючи всі його елементи**, якщо результат вже відомий.
>
> Це важлива оптимізація для роботи з великими або нескінченними потоками.
>
> **Приклади:**
>
> * **Проміжні операції:**
>   * **`limit(n)`:** Припиняє обробку після того, як буде досягнуто `n` елементів.
>
> * **Термінальні операції:**
>   * **`findFirst()`:** Завершується, як тільки знайдено перший елемент.
>   * **`findAny()`:** Те ж саме, особливо ефективно для паралельних потоків.
>   * **`anyMatch(predicate)`:** Завершується, як тільки знайдено перший елемент, що відповідає умові.
>   * **`allMatch(predicate)`:** Може завершитися достроково, як тільки знайдено перший елемент, що **не** відповідає умові.
>   * **`noneMatch(predicate)`:** Може завершитися достроково, як тільки знайдено перший елемент, що **відповідає** умові.

#### **📌 Що перевіряють:**

*Знання оптимізаційних механізмів Stream API та розуміння того, які операції є найбільш ефективними для певних завдань.*

* * *

### **13 ❓ Питання:**

Який зв'язок між `Stream API` та функціональними інтерфейсами, такими як `Predicate`, `Function`, `Consumer`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Функціональні інтерфейси є **фундаментальними будівельними блоками**, на яких побудований весь `Stream API`. Кожна проміжна та термінальна операція, що приймає якусь логіку або поведінку, приймає її у вигляді реалізації одного зі стандартних функціональних інтерфейсів.
>
> * **`filter()` приймає `Predicate<T>`:**
>   * Щоб вирішити, залишати елемент чи ні (повернути `true` чи `false`).
>
> * **`map()` приймає `Function<T, R>`:**
>   * Щоб перетворити елемент одного типу на інший.
>
> * **`forEach()` приймає `Consumer<T>`:**
>   * Щоб виконати дію над кожним елементом, не повертаючи результату.
>
> * **`reduce()` приймає `BinaryOperator<T>` (що є `BiFunction<T, T, T>`):**
>   * Щоб об'єднати два елементи в один.
>
> * **`generate()` приймає `Supplier<T>`:**
>   * Щоб постачати нові елементи для потоку.
>
> По суті, `Stream API` надає "конвеєр", а функціональні інтерфейси, реалізовані через лямбди або посилання на методи, є "станками" на цьому конвеєрі, що виконують конкретні операції.

#### **📌 Що перевіряють:**

*Розуміння глибокого зв'язку та синергії між двома ключовими фічами Java 8. Кандидат повинен бачити Stream API як практичне застосування концепцій функціональних інтерфейсів.*

* * *

### **14 ❓ Питання:**

Що поверне `.findFirst()` і `.findAny()` на паралельному потоці (`parallelStream`)? Чи гарантований результат? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання на розуміння різниці між цими двома методами в контексті паралельної обробки.
>
> * **`.findFirst()`**:
>   * **Гарантує**, що поверне **перший елемент потоку** відповідно до його порядку (encounter order).
>   * **У паралельному потоці** це може бути **менш продуктивним**, оскільки система змушена буде координувати роботу потоків, щоб визначити, який елемент справді є "першим".
>
> * **`.findAny()`**:
>   * **Не гарантує**, який саме елемент буде повернуто. Він просто поверне будь-який елемент, який перший попадеться в будь-якому з паралельних потоків.
>   * **У паралельному потоці** цей метод є **набагато більш продуктивним**, оскільки не вимагає координації. Як тільки будь-який потік знаходить елемент, результат повертається.
>
> **Коли що використовувати:**
>
> * Якщо **порядок важливий**, і вам потрібен саме перший елемент — використовуйте `findFirst()`.
> * Якщо вам **неважливо, який** саме елемент буде знайдено (аби він відповідав умові), і ви працюєте з паралельним потоком, **завжди** віддавайте перевагу `findAny()` для кращої продуктивності.

#### **📌 Що перевіряють:**

*Розуміння нюансів паралельної обробки. Кандидат повинен знати, як обирати операції, що найкраще підходять для паралельних обчислень.*

* * *

### **15 ❓ Питання:**

Чому операції, що мають побічні ефекти (side-effects) (напр., зміна зовнішньої колекції в `forEach`), є небезпечними, особливо в паралельних потоках? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використання побічних ефектів у проміжних операціях або в `forEach` на паралельних потоках **категорично не рекомендується** і є джерелом серйозних помилок.
>
> **Проблеми, до яких це призводить:**
>
> 1. **"Умова гонитви" (Race Condition):**
>     Якщо кілька потоків одночасно намагаються змінити одну й ту саму мутабельну структуру (наприклад, додавати елементи в несинхронізований `ArrayList`), це призводить до пошкодження даних, винятків (`ConcurrentModificationException`, `ArrayIndexOutOfBoundsException`) або непередбачуваних результатів.
>
> 2. **Непередбачуваність:**
>     Результат виконання може залежати від того, як планувальник потоків розподілив роботу, і буде різним при кожному запуску. Це руйнує детермінізм.
>
> **Правильний підхід:**
>
> Лямбди, що використовуються в Stream API, мають бути **безстановими (stateless)** і **не мати побічних ефектів**.
>
> * Замість того, щоб змінювати зовнішню колекцію, використовуйте **колектори (`collect`)**. Колектори розроблені спеціально для того, щоб безпечно та ефективно збирати результати, в тому числі й у паралельному режимі.
> * Замість зміни стану, використовуйте операцію **редукції (`reduce`)**.
>
> **Погано:**
>
> ```java
> List<String> results = new ArrayList<>();
> source.parallelStream().forEach(item -> results.add(item.toString()));
> ```
>
> **Добре:**
>
> ```java
> List<String> results = source.parallelStream().map(Object::toString).collect(Collectors.toList());
> ```
>
> Це гарантує потокобезпечність та правильну роботу в паралельному режимі.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння принципів функціонального програмування та правил безпечної роботи з багатопотоковістю. Це ознака досвідченого та відповідального розробника.*

* * *
