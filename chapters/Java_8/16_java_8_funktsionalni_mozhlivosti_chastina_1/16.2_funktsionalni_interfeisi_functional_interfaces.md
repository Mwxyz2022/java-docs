---
layout: default
title: "Функціональні інтерфейси (Functional Interfaces)"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 2
---

# Функціональні інтерфейси (Functional Interfaces)

* короткий опис: Функціональний інтерфейс – це інтерфейс, який містить рівно один абстрактний метод (SAM – Single Abstract Method). Він є цільовим типом для лямбда-виразів та method references.

* поняття: Функціональний інтерфейс, SAM, `@FunctionalInterface` (анотація).

* приклади: `interface Printer { void print(String message); }`, використання лямбди `s -> System.out.println(s)`.

* ключові моменти: Дозволяють писати компактний і читабельний функціональний код. Можуть містити `default` та `static` методи.

* що важливо знати на співбесіді: Визначення функціонального інтерфейсу, що робить його таким.

### **✅ Офіційне визначення:**

Функціональний інтерфейс — це інтерфейс у Java, який містить рівно один абстрактний метод. Такий інтерфейс є цільовим типом для лямбда-виразів і method references. Анотація **`@FunctionalInterface`** використовується для явного позначення інтерфейсу як функціонального.

### **🧠 Простими словами:**

**Функціональний інтерфейс** — це як шаблон для функції з одним входом і одним виходом (або просто один метод). Завдяки цьому Java розуміє, що сюди можна вставити лямбду — маленький шматок коду без зайвих церемоній.

---

## **📊 Основні характеристики функціональних інтерфейсів:**

|          Характеристика           |                    Опис                    |
|:---------------------------------:|:------------------------------------------:|
| **Кількість абстрактних методів** |                  Рівно 1                   |
|           **Анотація**            | **`@FunctionalInterface`** (рекомендована) |
|     **Можливість реалізації**     |       Лямбда-вираз, method reference       |
|       **Додаткові методи**        |   Можуть містити default і static методи   |

---

## **Пояснення на пальцях (💡)**

Уяви, що функціональний інтерфейс — це інструкція: «Ось що треба зробити» — тільки з одним пунктом. Лямбда — це короткий спосіб сказати: «Я зроблю це так». І Java звіряє: «Хмм, твоя лямбда підходить для цього одного пункту, значить це функціональний інтерфейс»

---

## **Корисні приклади (🧪)**

```java
@FunctionalInterface
interface MyFunctionalInterface {
    void execute(String input);
    // default і static методи теж можливі
    default void defaultMethod() {
        System.out.println("Default implementation");
    }
}

// Використання з лямбда-виразом
MyFunctionalInterface func = (s) -> System.out.println("Hello, " + s);
func.execute("World");

// Використання з method reference
MyFunctionalInterface funcRef = System.out::println;
funcRef.execute("Hello via method reference");
```

---

## **Порівняння (⚖️)**

|                   Особливість                   | Функціональний інтерфейс | Звичайний інтерфейс |
|:-----------------------------------------------:|:------------------------:|:-------------------:|
|        **Кількість абстрактних методів**        |            1             |      Будь-яка       |
|          **Підтримка лямбда-виразів**           |           Так            |         Ні          |
| **Можливість використання з method references** |           Так            |         Ні          |
|       **Анотація `@FunctionalInterface`**       |      Рекомендована       |        Немає        |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔠 **"One method — one mission"**

Функціональний інтерфейс — це як агент з однією місією: виконай цю одну справу.

---

## **🟩 Підсумок:**

* Функціональний інтерфейс — це інтерфейс з одним абстрактним методом, що робить його ідеальним для лямбда-виразів і method references.
* Анотація **`@FunctionalInterface`** забезпечує додаткову перевірку на коректність.
* Можна мати default і static методи для додаткової логіки.
* Розуміння функціональних інтерфейсів — ключ до сучасного Java-коду і часто питається на співбесідах.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs – Functional Interfaces**](https://docs.oracle.com/javase/8/docs/api/java/lang/FunctionalInterface.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке функціональний інтерфейс в Java і яка його ключова вимога?

> #### **💬 Відповідь:**
>
> **Функціональний інтерфейс** — це будь-який інтерфейс в Java, який містить **рівно один абстрактний метод (Single Abstract Method, або SAM)**.
>
> Його ключова вимога — це саме наявність лише одного абстрактного методу. Ця вимога робить його цільовим типом для **лямбда-виразів** та **посилань на методи (method references)**, які є компактним способом реалізації цього єдиного методу.
>
> Функціональний інтерфейс може додатково містити будь-яку кількість `default` та `static` методів, оскільки вони мають реалізацію і не є абстрактними.
>
> Для явного позначення такого інтерфейсу використовується анотація `@FunctionalInterface`, хоча вона не є обов'язковою, але є хорошою практикою.

#### **📝 Аналогія:**

Функціональний інтерфейс — це як форма для заповнення з одним-єдиним полем. Ви точно знаєте, що туди вписати. Лямбда-вираз — це короткий текст, який ви вписуєте в це поле. Java бачить, що поле одне, і розуміє, куди саме вставити ваш текст.

#### **📌 Що перевіряють:**

*Базове визначення функціонального інтерфейсу та розуміння його головної умови — наявність одного абстрактного методу (SAM).*

* * *

### **2 ❓ Питання:**

Яке призначення анотації `@FunctionalInterface`? Чи є вона обов'язковою?

> #### **💬 Відповідь:**
>
> Анотація **`@FunctionalInterface`** слугує для кількох цілей:
>
> 1. **Перевірка на етапі компіляції:** Це її найважливіша функція. Якщо ви позначите інтерфейс цією анотацією, компілятор перевірить, чи справді він містить рівно один абстрактний метод. Якщо методів буде більше або менше, ви отримаєте помилку компіляції. Це захищає від випадкових змін, які можуть "зламати" функціональність інтерфейсу.
> 2. **Документація:** Вона явно повідомляє іншим розробникам (та інструментам, як-от Javadoc), що цей інтерфейс був спроектований для використання з лямбда-виразами.
> 3. **Читабельність коду:** Коли ви бачите цю анотацію, ви одразу розумієте призначення інтерфейсу.
>
> **Чи є вона обов'язковою?**
> **Ні, не є.** Будь-який інтерфейс з одним абстрактним методом вважається функціональним, навіть без цієї анотації. Однак її використання є **настійно рекомендованою** хорошою практикою.

#### **📝 Коментар:**

`@FunctionalInterface` — це як знак `@Override`. Код буде працювати і без нього, але він захищає від помилок і робить наміри розробника явними.

#### **📌 Що перевіряють:**

*Розуміння ролі анотації `@FunctionalInterface`. Кандидат повинен знати, що вона не обов'язкова, але дуже корисна для перевірки та документації.*

* * *

### **3 ❓ Питання:**

Як функціональні інтерфейси пов'язані з лямбда-виразами?

> #### **💬 Відповідь:**
>
> Функціональні інтерфейси є **цільовим типом (target type)** для лямбда-виразів. Це означає, що лямбда-вираз можна присвоїти лише змінній, тип якої є функціональним інтерфейсом.
>
> **Зв'язок:**
>
> * Лямбда-вираз сам по собі не має типу. Він є просто блоком коду.
> * Компілятор Java визначає тип лямбди з контексту, дивлячись на те, якій змінній або параметру методу її намагаються присвоїти.
> * Оскільки функціональний інтерфейс має лише один абстрактний метод, компілятор однозначно розуміє, що тіло лямбда-виразу є реалізацією саме цього єдиного методу. Сигнатура (аргументи та тип, що повертається) лямбди повинна збігатися з сигнатурою цього методу.

#### **📝 Приклад:**

```java
@FunctionalInterface
interface MyOperation {
    int operate(int a, int b);
}

// Компілятор розуміє, що `(x, y) -> x + y` є реалізацією методу `operate(int a, int b)`
MyOperation addition = (x, y) -> x + y;
System.out.println(addition.operate(5, 3)); // 8
```

#### **📌 Що перевіряють:**

*Розуміння механізму "визначення типу за контекстом" для лямбда-виразів та ключової ролі функціональних інтерфейсів у цьому процесі.*

* * *

### **4 ❓ Питання:**

Назвіть та опишіть чотири основні вбудовані функціональні інтерфейси з пакету `java.util.function`.

> #### **💬 Відповідь:**
>
> Java 8 ввела пакет `java.util.function`, який містить набір найбільш уживаних функціональних інтерфейсів. Чотири основних:
>
> 1. **`Predicate<T>`:**
>     * **Метод:** `boolean test(T t)`
>     * **Призначення:** Приймає один аргумент і повертає `boolean`. Використовується для перевірки певної умови (предикату).
>     * **Аналогія:** Питання, що вимагає відповіді "так" або "ні". (Напр., `s -> s.isEmpty()`).
>
> 2. **`Consumer<T>`:**
>     * **Метод:** `void accept(T t)`
>     * **Призначення:** Приймає один аргумент і нічого не повертає (`void`). Виконує якусь дію або операцію над об'єктом ("споживає" його).
>     * **Аналогія:** Інструкція. (Напр., `s -> System.out.println(s)`).
>
> 3. **`Function<T, R>`:**
>     * **Метод:** `R apply(T t)`
>     * **Призначення:** Приймає один аргумент типу `T` і повертає результат типу `R`. Виконується для перетворення (трансформації) одного об'єкта в інший.
>     * **Аналогія:** Перетворювач. (Напр., `s -> s.length()`).
>
> 4. **`Supplier<T>`:**
>     * **Метод:** `T get()`
>     * **Призначення:** Не приймає жодних аргументів, але повертає значення типу `T`. Використовується для "постачання" або генерування значень.
>     * **Аналогія:** Постачальник або фабрика. (Напр., `() -> new ArrayList<>()`).

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки функціональних інтерфейсів. Це основа для роботи зі Streams API та іншими сучасними можливостями Java.*

* * *

### **5 ❓ Питання:**

Чи може функціональний інтерфейс містити `default` та `static` методи?

> #### **💬 Відповідь:**
>
> **Так, може.**
>
> Вимога функціонального інтерфейсу стосується лише кількості **абстрактних** методів. `Default` та `static` методи не є абстрактними, оскільки вони мають реалізацію за замовчуванням.
>
> Отже, функціональний інтерфейс може мати:
>
> * **Рівно один** абстрактний метод.
> * **Будь-яку кількість** `default` методів.
> * **Будь-яку кількість** `static` методів.
>
> `Default` методи, як-от `and()`, `or()` в `Predicate`, або `andThen()`, `compose()` в `Function`, дозволяють комбінувати та розширювати функціональність без необхідності створювати утилітарні класи.

#### **📝 Приклад:**

```java
@FunctionalInterface
interface Calculator {
    int calculate(int x, int y);

    default int add(int x, int y) {
        return calculate(x, y); // Може викликати абстрактний метод
    }
    
    static void printInfo() {
        System.out.println("This is a calculator.");
    }
}
```

#### **📌 Що перевіряють:**

*Чітке розуміння правил визначення функціональних інтерфейсів та ролі `default` та `static` методів в інтерфейсах Java 8.*

* * *

### **6 ❓ Питання:**

Наведіть приклад створення та використання власного функціонального інтерфейсу.

> #### **💬 Відповідь:**
>
> Створимо простий функціональний інтерфейс для форматування рядків.
>
> **1. Оголошення інтерфейсу:**
> Ми створюємо інтерфейс з одним абстрактним методом `format` та анотуємо його `@FunctionalInterface` для надійності.
>
> **2. Реалізація за допомогою лямбди:**
> Створюємо дві різні реалізації: одна перетворює рядок у верхній регістр, інша — додає префікс.
>
> **3. Використання:**
> Викликаємо метод `format` на екземплярах нашого інтерфейсу.

#### **📝 Приклад коду:**

```java
import java.util.function.*;

@FunctionalInterface
interface StringFormatter {
    String format(String input);
}

public class CustomFunctionalInterfaceDemo {
    public static void main(String[] args) {
        // Реалізація №1: перетворення у верхній регістр
        StringFormatter toUpperCaseFormatter = (s) -> s.toUpperCase();
        
        // Реалізація №2: додавання префіксу
        StringFormatter prefixFormatter = (s) -> "PREFIX: " + s;

        String original = "hello world";

        // Використання
        System.out.println(toUpperCaseFormatter.format(original)); // HELLO WORLD
        System.out.println(prefixFormatter.format(original));   // PREFIX: hello world
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички. Кандидат повинен вміти не тільки розповідати про функціональні інтерфейси, а й створювати та використовувати їх.*

* * *

### **7 ❓ Питання:**

Що таке примітивні спеціалізації функціональних інтерфейсів (напр., `IntPredicate`, `LongFunction`) і для чого вони існують?

> #### **💬 Відповідь:**
>
> **Примітивні спеціалізації** — це версії стандартних функціональних інтерфейсів, які працюють безпосередньо з примітивними типами (`int`, `long`, `double`) замість їхніх об'єктних обгорток (`Integer`, `Long`, `Double`).
>
> **Приклади:**
>
> * `IntPredicate` замість `Predicate<Integer>`
> * `LongConsumer` замість `Consumer<Long>`
> * `DoubleFunction<R>` замість `Function<Double, R>`
>
> **Навіщо вони існують?**
>
> Їх головна мета — **оптимізація продуктивності**.
> Робота з об'єктними обгортками вимагає процесів **"пакування" (boxing)** та **"розпакування" (unboxing)**.
>
> * **Boxing:** Перетворення примітива в об'єкт (напр., `int` -> `Integer`). Це створює новий об'єкт у купі.
> * **Unboxing:** Перетворення об'єкта в примітив (напр., `Integer` -> `int`).
>
> Коли ці операції виконуються багато разів у циклі або стрімі, вони створюють значне навантаження на пам'ять та збирача сміття, що може суттєво сповільнити програму.
>
> Примітивні спеціалізації дозволяють **уникнути boxing/unboxing**, оскільки їхні методи напряму працюють з примітивами, що робить код значно ефективнішим.

#### **📌 Що перевіряють:**

*Розуміння нюансів продуктивності в Java, зокрема проблем, пов'язаних з автопакуванням. Це важлива тема для написання високоефективного коду, особливо при обробці великих масивів числових даних.*

* * *

### **8 ❓ Питання:**

Що таке "bi"-версії функціональних інтерфейсів (`BiFunction`, `BiPredicate`)? ⭐️

> #### **💬 Відповідь:**
>
> **"Bi"-версії** (від слова "два") — це варіації стандартних функціональних інтерфейсів, які приймають **два аргументи** замість одного.
>
> **Основні "bi"-інтерфейси:**
>
> * **`BiPredicate<T, U>`:**
>   * **Метод:** `boolean test(T t, U u)`
>   * Приймає два аргументи, повертає `boolean`. Використовується для перевірки умови, що залежить від двох об'єктів.
>
> * **`BiConsumer<T, U>`:**
>   * **Метод:** `void accept(T t, U u)`
>   * Приймає два аргументи, нічого не повертає. Виконує дію над двома об'єктами.
>   * *Дуже часто використовується з `Map.forEach((key, value) -> ...)`.*
>
> * **`BiFunction<T, U, R>`:**
>   * **Метод:** `R apply(T t, U u)`
>   * Приймає два аргументи (`T` та `U`) і повертає результат типу `R`. Використовується для перетворення пари об'єктів у один.
>
> Вони розширюють можливості функціонального програмування для сценаріїв, де потрібно працювати з парами значень.

#### **📝 Приклад з `BiFunction`:**

```java
// Створюємо функцію, що об'єднує рядок та число
BiFunction<String, Integer, String> combine = (text, number) -> text + ": " + number;
String result = combine.apply("Score", 95); // "Score: 95"
```

#### **📌 Що перевіряють:**

*Знання повного спектру стандартних функціональних інтерфейсів та їх призначення.*

* * *

### **9 ❓ Питання:**

Що таке `UnaryOperator` та `BinaryOperator` і як вони пов'язані з `Function`? ⭐️

> #### **💬 Відповідь:**
>
> **`UnaryOperator`** та **`BinaryOperator`** — це **спеціалізовані випадки** `Function` та `BiFunction` відповідно.
>
> **Спеціалізація** полягає в тому, що у них **тип вхідних аргументів та тип повернутого значення однакові**.
>
> * **`UnaryOperator<T>`:**
>   * **Наслідує:** `Function<T, T>`.
>   * **Призначення:** Приймає один аргумент типу `T` і повертає результат **того ж самого типу** `T`.
>   * **Аналогія:** Математична унарна операція (напр., `x -> -x`).
>
> * **`BinaryOperator<T>`:**
>   * **Наслідує:** `BiFunction<T, T, T>`.
>   * **Призначення:** Приймає два аргументи типу `T` і повертає результат **того ж самого типу** `T`.
>   * **Аналогія:** Математична бінарна операція (напр., `(x, y) -> x + y`). Дуже часто використовується в `Stream.reduce()`.
>
> Використання цих операторів робить код більш семантично точним, коли ви працюєте з операціями, що не змінюють тип.

#### **📝 Приклад:**

```java
UnaryOperator<Integer> square = x -> x * x;     // Function<Integer, Integer>
BinaryOperator<Integer> sum = (x, y) -> x + y;  // BiFunction<Integer, Integer, Integer>
```

#### **📌 Що перевіряють:**

*Знання ієрархії та спеціалізацій у `java.util.function`. Це показує більш глибоке розуміння дизайну цієї бібліотеки.*

* * *

### **10 ❓ Питання:**

Чи може функціональний інтерфейс успадковуватися від іншого інтерфейсу? Які при цьому діють правила? ⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Правила тут дуже логічні і випливають з визначення функціонального інтерфейсу (один абстрактний метод).
>
> **Правила:**
>
> 1. **Якщо батьківський інтерфейс не є функціональним:**
>     * Дочірній інтерфейс може стати функціональним, якщо він додасть **рівно один** абстрактний метод.
>
> 2. **Якщо батьківський інтерфейс є функціональним:**
>     * Дочірній інтерфейс **не може додавати нових абстрактних методів**. Якщо він це зробить, він перестане бути функціональним.
>     * Він може **перевизначити** абстрактний метод батьківського інтерфейсу (наприклад, для звуження типу, що повертається), але це все одно буде вважатися тим самим методом.
>     * Він може додавати будь-яку кількість `default` та `static` методів.
>
> По суті, для того, щоб інтерфейс залишався функціональним, у всій його ієрархії успадкування має бути **лише один** "нереалізований" абстрактний шлях.

#### **📌 Що перевіряють:**

*Розуміння правил успадкування в контексті функціональних інтерфейсів. Це питання на знання нюансів мови.*

* * *

### **11 ❓ Питання:**

Як реалізована робота з `this` у лямбда-виразах порівняно з анонімними класами? ⭐️

> #### **💬 Відповідь:**
>
> Це ключова відмінність, яку важливо знати.
>
> * **В анонімному класі:**
>   * Ключове слово `this` посилається на **екземпляр самого анонімного класу**. Анонімний клас створює свою власну область видимості (`scope`). Він може "затінювати" (shadow) змінні зовнішнього класу.
>
> * **У лямбда-виразі:**
>   * Лямбда-вираз **не створює власної області видимості**.
>   * Ключове слово `this` всередині лямбди посилається на **екземпляр зовнішнього (enclosing) класу**, в якому ця лямбда була визначена. Лямбди є лексично прозорими.
>
> **Наслідки:**
> Це означає, що всередині лямбди ви можете напряму доступатися до полів та методів зовнішнього класу, використовуючи `this`, так, ніби ви знаходитесь всередині його методу. В анонімному класі для цього довелося б писати `OuterClass.this.someField`.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між лямбдами та анонімними класами. Поведінка `this` є однією з фундаментальних відмінностей.*

* * *

### **12 ❓ Питання:**

Що роблять методи `compose()` та `andThen()` в інтерфейсі `Function`? ⭐️

> #### **💬 Відповідь:**
>
> `compose()` та `andThen()` — це `default`-методи, які дозволяють **комбінувати** кілька функцій в одну.
>
> * **`f.andThen(g)` ("спочатку f, ПОТІМ g"):**
>   * Цей метод створює нову функцію, яка спочатку застосовує **поточну функцію (`f`)**, а потім до її результату застосовує **функцію `g`**.
>   * Виконується зліва направо: `input -> f -> g -> output`.
>
> * **`f.compose(g)` ("спочатку g, а до її результату f"):**
>   * Цей метод створює нову функцію, яка спочатку застосовує **передану функцію (`g`)**, а потім до її результату застосовує **поточну функцію (`f`)**.
>   * Виконується справа наліво: `input -> g -> f -> output`.
>
> Вони є потужними інструментами для побудови складних ланцюжків перетворень даних у функціональному стилі.

#### **📝 Приклад:**

```java
Function<Integer, Integer> multiplyBy2 = x -> x * 2;
Function<Integer, Integer> add3 = x -> x + 3;

// (5 * 2) + 3 = 13
Function<Integer, Integer> combinedAndThen = multiplyBy2.andThen(add3);
System.out.println(combinedAndThen.apply(5)); // 13

// (5 + 3) * 2 = 16
Function<Integer, Integer> combinedCompose = multiplyBy2.compose(add3);
System.out.println(combinedCompose.apply(5)); // 16
```

#### **📌 Що перевіряють:**

*Знання просунутого функціонального API та здатність писати код у стилі композиції функцій.*

* * *

### **13 ❓ Питання:**

Як обробити перевірені винятки (checked exceptions) всередині лямбда-виразу, якщо стандартні функціональні інтерфейси їх не оголошують? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це одна з поширених проблем при роботі з лямбдами та функціями, що можуть кидати перевірені винятки (наприклад, `IOException`). Стандартні функціональні інтерфейси (`Function`, `Consumer` і т.д.) **не оголошують `throws`** у своїх методах.
>
> Існує кілька підходів до вирішення:
>
> 1. **Обгортання у `try-catch` (найпростіший, але громіздкий):**
>     Найбільш прямолінійний спосіб — обгорнути код у блок `try-catch` всередині лямбди. В блоці `catch` ви можете або проковтнути виняток, або загорнути його в `RuntimeException`.
>
>     ```java
>     list.forEach(item -> {
>         try {
>             processItem(item); // кидає IOException
>         } catch (IOException e) {
>             throw new UncheckedIOException(e); // Загортаємо
>         }
>     });
>     ```
>
> 2. **Створення власного функціонального інтерфейсу:**
>     Ви можете створити власний функціональний інтерфейс, який оголошує `throws Exception` у своєму методі.
>
>     ```java
>     @FunctionalInterface
>     interface ThrowingConsumer<T> {
>         void accept(T t) throws Exception;
>     }
>     ```
>
> 3. **Використання утилітарної функції-обгортки (найкращий підхід):**
>     Створити або використати готову утилітарну функцію, яка приймає "небезпечну" лямбду і повертає "безпечну", яка обробляє виняток під капотом.
>
>     ```java
>     // Уявімо, що є така утиліта
>     list.stream()
>         .map(wrapper(item -> mightThrowException(item)))
>         .collect(toList());
>     ```
>
> Перший підхід є найпростішим для разового використання. Третій — найкращий для чистоти коду, якщо такі операції відбуваються часто.

#### **📌 Що перевіряють:**

*Здатність вирішувати складні практичні проблеми, що виникають при інтеграції функціонального стилю зі старою частиною Java (перевіреними винятками). Це показує досвід кандидата.*

* * *

### **14 ❓ Питання:**

Що робить статичний метод `Function.identity()` і для чого він використовується? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Статичний метод **`Function.identity()`** повертає функцію, яка **завжди повертає свій вхідний аргумент**. Тобто, це функція `t -> t`.
>
> **Для чого він використовується:**
>
> На перший погляд, він здається непотрібним, але є дуже корисним у `Stream API`, особливо при роботі з `Collectors.toMap()` або `Collectors.groupingBy()`.
>
> **Приклад:**
> Уявіть, що у вас є список об'єктів `User`, і ви хочете створити `Map<Long, User>`, де ключ — це ID користувача, а значення — **сам об'єкт користувача**.
>
> ```java
> List<User> users = ...;
> Map<Long, User> userMap = users.stream()
>     .collect(Collectors.toMap(User::getId, Function.identity()));
> ```
>
> У цьому прикладі `Function.identity()` каже колектору: "для значення мапи просто візьми сам елемент стріму (`User`) без будь-яких перетворень". Це значно читабельніше і лаконічніше, ніж писати `user -> user`.

#### **📌 Що перевіряють:**

*Знання тонкощів Stream API. `Function.identity()` — це одна з ідіом функціонального програмування в Java, і її розуміння є ознакою досвідченого користувача стрімів.*

* * *

### **15 ❓ Питання:**

Яким чином JVM реалізує лямбда-вирази під капотом? Чи створюється анонімний клас для кожної лямбди? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, JVM не створює анонімний клас** для кожної лямбди. Це поширена помилкова думка. Для реалізації лямбд, починаючи з Java 8, було введено більш ефективний механізм.
>
> **Механізм `invokedynamic`:**
>
> 1. Під час компіляції, компілятор не генерує окремий `.class` файл для лямбда-виразу. Замість цього він перетворює тіло лямбди в **приватний статичний метод** всередині того ж класу.
>
> 2. У місці, де використовується лямбда, компілятор генерує спеціальну інструкцію байт-коду — **`invokedynamic`**.
>
> 3. Під час **першого** виконання цієї інструкції, JVM динамічно створює об'єкт-реалізацію функціонального інтерфейсу, який посилається на цей статичний метод. Цей процес називається **"lambda factory"**. При наступних викликах може бути перевикористаний той самий об'єкт (якщо лямбда не захоплює змінних).
>
> **Чому це краще за анонімні класи:**
>
> * **Продуктивність:** Уникає завантаження та ініціалізації безлічі дрібних класів, що було проблемою анонімних класів.
> * **Менший розмір додатку:** Не створюються сотні додаткових `.class` файлів.
> * **Гнучкість для майбутніх оптимізацій:** Механізм `invokedynamic` дозволяє JVM у майбутньому застосовувати нові, ще більш ефективні стратегії реалізації лямбд без зміни байт-коду.
>
> Таким чином, лямбди реалізовані набагато ефективніше і гнучкіше, ніж анонімні класи.

#### **📌 Що перевіряють:**

*Експертні знання JVM та того, як реалізовані ключові мовні конструкції. Це питання рівня Senior, що демонструє глибоке розуміння платформи.*

* * *
