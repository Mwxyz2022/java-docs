---
layout: default
title: "Компілятор та type inference у лямбда-виразах"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 16
---

# Компілятор та type inference у лямбда-виразах

* короткий опис: `Type inference` – це механізм, за допомогою якого компілятор автоматично виводить типи параметрів або лямбда-виразів, ґрунтуючись на контексті (`target typing`). Дозволяє писати компактніший код.

* поняття: `Type inference`, `target typing`, `diamond operator` (`<>`).

* приклади: `List<String> list = new ArrayList<>();` (виведення типу `String`), `names.stream().filter(name -> name.length() > 3)` (виведення типу `name` як `String`).

* ключові моменти: Зменшує кількість boilerplate коду. Якщо контекст неоднозначний, може вимагати явного вказання типу.

* що важливо знати на співбесіді: Як працює виведення типів, коли виникають проблеми.

### **✅ Офіційне визначення:**

* **Type inference** — це механізм, за допомогою якого компілятор автоматично виводить типи змінних, параметрів або лямбда-виразів, ґрунтуючись на контексті.
* **Target typing** — процес, у якому тип лямбда-виразу визначається на основі типу змінної або параметра, в який він передається.

### **🧠 Простими словами:**

Java-компілятор сам "додумає", який тип мався на увазі в лямбді — головне, щоб він міг це зрозуміти з контексту (тобто, куди ми передаємо цю лямбду). Але іноді йому потрібна наша допомога — як дитині з домашкою

---

## 📊 **Ключові концепції:**

|        Поняття        |                                 Опис                                 |
|:---------------------:|:--------------------------------------------------------------------:|
|  **Type Inference**   |         Виведення типу змінної/лямбди без явного оголошення          |
|    **Target Type**    |        Тип функціонального інтерфейсу, якого очікує контекст         |
| **Diamond Operator**  | **`<>`** — дозволяє уникнути дублювання типів при створенні колекцій |
| **Inference Failure** |  Випадок, коли компілятор не може вивести тип, і вимагає уточнення   |

---

## **Пояснення на пальцях (💡)**

* У лямбда-виразах немає оголошеного типу явно, бо Java бачить: "Ага, ти передаєш мене у метод, який чекає **`Predicate<String>`** — значить, я працюю зі **`String`**."
* Але якщо ти використовуєш лямбду поза контекстом — компілятор в паніці. Він такий: "Я не телепат, підкажи мені тип\!"

---

## **Корисні приклади (🧪)**

### **✅ Компіллятор сам виводить тип:**

```java
List<String> names = List.of("Max", "Alex", "John");
names.stream()
     .filter(name -> name.length() > 3) // Predicate<String>
     .forEach(System.out::println);
```

### **❌ Помилка без target type:**

```java
Function<Integer, Integer> lambda = x -> x + 1;
```

### **✅ Явне вказання типу:**

```java
Function<Integer, Integer> lambda = x -> x + 1;
```

### **✅ Diamond operator:**

```java
List<String> list = new ArrayList<>(); // Java виводить <String>
```

---

## **Порівняння (⚖️)**

|            Ситуація             | Type Inference працює? | Потрібно явно вказати типи? |
|:-------------------------------:|:----------------------:|:---------------------------:|
|   **Передача лямбди в метод**   |           ✅            |             Ні              |
| **Зберігання лямбди у змінній** |           ❌            |             Так             |
| **Ланцюжок складних викликів**  | ⚠️ Може не спрацювати  |             Так             |
|  **Generics без типів у \<\>**  |           ✅            |             Ні              |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Лямбда без дому — без типу"**

Якщо лямбда не має контексту (не «живе» в методі, що її чекає) — компілятор не знатиме, який у неї тип. Дай їй «дім» — оголоси тип вручну.

---

## **🟩 Підсумок:**

* Java виводить типи лямбд на основі контексту — target typing.
* Якщо контексту нема (наприклад, при присвоєнні в **`var`**), потрібне явне вказання типу.
* Diamond operator **`(<>)`** допомагає уникати дублювання типів у дженериках.
* На співбесідах люблять питати, як саме Java виводить типи — і де це може зламатися.

---

### **🔗 Додаткові джерела:**

* [**Official Java Docs — Type Inference**](https://docs.oracle.com/javase/specs/jls/se17/html/jls-18.html)
* [**StackOverflow — Why can't Java infer the lambda type here?**](https://stackoverflow.com/questions/29008553/why-cant-java-infer-the-lambda-type-here)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке виведення типів (Type Inference) у Java і як воно працює для лямбда-виразів?

> #### **💬 Відповідь:**
>
> **Виведення типів (Type Inference)** — це механізм компілятора Java, який дозволяє йому **автоматично визначати тип** даних без його явного оголошення розробником. Класичним прикладом є `diamond operator <>` (`List<String> list = new ArrayList<>();`), де компілятор сам розуміє, що створюється `ArrayList<String>`.
>
> **Для лямбда-виразів** цей механізм є ключовим, оскільки лямбди не мають явного типу. Комп_ілятор виводить тип лямбди, її параметрів та повернутого значення, ґрунтуючись на **контексті**, в якому вона використовується. Цей процес називається **"визначенням типу за ціллю" (Target Typing)**.
>
> Компілятор дивиться на **цільовий тип** — змінну, параметр методу або повернуте значення, якому намагаються присвоїти лямбду. Цей тип має бути **функціональним інтерфейсом**. З нього компілятор бере інформацію про єдиний абстрактний метод і "розуміє", які типи мають бути у параметрів та результату лямбди.

#### **📝 Аналогія:**

Уявіть, що ви даєте комусь ключ без бірки. Сам по собі ключ незрозуміло від чого. Але якщо ви підносите його до замка (`цільовий тип`), одразу стає ясно, що це "ключ від квартири". Комп_ілятор так само "дивиться" на "замок" (функціональний інтерфейс), щоб зрозуміти, що за "ключ" (лямбда) ви йому передали.

#### **📌 Що перевіряють:**

*Базове розуміння концепції Type Inference та його специфічного застосування для лямбда-виразів через механізм Target Typing.*

* * *

### **2 ❓ Питання:**

Поясніть, що таке цільовий тип (Target Type) для лямбда-виразу.

> #### **💬 Відповідь:**
>
> **Цільовий тип** — це тип даних, якого очікує Java в тому місці, де використовується лямбда-вираз. Лямбда-вираз не має власного типу, тому його тип визначається виключно з контексту.
>
> Цільовим типом для лямбди може бути тільки **функціональний інтерфейс**.
>
> Компілятор аналізує цільовий тип, щоб:
>
> 1. **Зіставити лямбду з абстрактним методом:** Він "розуміє", що тіло лямбди є реалізацією єдиного абстрактного методу цього інтерфейсу.
> 2. **Вивести типи параметрів:** З сигнатури абстрактного методу він дізнається, які типи мають параметри лямбди, тому їх можна не вказувати.
> 3. **Перевірити тип повернутого значення:** Він перевіряє, чи відповідає тип, який повертає лямбда, типу, що повертає абстрактний метод.

#### **📝 Приклад:**

```java
// Цільовий тип тут - Predicate<String>
Predicate<String> isValid = s -> s.length() > 5;

// Компілятор дивиться на Predicate<String>, бачить метод `boolean test(String s)`
// і розуміє, що `s` має бути String, а вираз `s.length() > 5` має повернути boolean.
```

#### **📌 Що перевіряють:**

*Чітке розуміння, звідки компілятор бере інформацію для виведення типів лямбди. Це фундаментальний механізм, що робить лямбди можливими в Java.*

* * *

### **3 ❓ Питання:**

Чому цей код не компілюється? `Object lambda = () -> {};`

> #### **💬 Відповідь:**
>
> Цей код не компілюється, тому що **компілятор не може вивести цільовий тип** для лямбда-виразу.
>
> Як було сказано, лямбда не має власного типу. Щоб компілятор "зрозумів" лямбду, її потрібно присвоїти змінній типу **функціонального інтерфейсу**.
>
> Клас **`Object` не є функціональним інтерфейсом**. Він не має жодного абстрактного методу, який могла б реалізувати лямбда `() -> {}`. Тому компілятор не знає, на що перетворити цей вираз, і видає помилку.
>
> **Щоб виправити код**, потрібно вказати конкретний функціональний інтерфейс, який відповідає сигнатурі лямбди (не приймає аргументів, нічого не повертає). Для цього ідеально підходить `Runnable`.

#### **📝 Приклад (правильний):**

```java
// Тепер компілятор знає, що лямбда є реалізацією методу `run()`
Runnable lambda = () -> {}; 
```

#### **📌 Що перевіряють:**

*Розуміння того, що лямбда не може існувати "у вакуумі" і завжди вимагає цільового типу, який є функціональним інтерфейсом.*

* * *

### **4 ❓ Питання:**

Чи можна використовувати `var` для оголошення змінної, що зберігає лямбда-вираз?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Спроба написати `var myLambda = () -> {};` призведе до помилки компіляції.
>
> **Причина:**
>
> Механізм виведення типів для `var` та для лямбда-виразів працюють у протилежних напрямках.
>
> * **`var`**: виводить тип **з правого боку** виразу. Тобто, `var x = new ArrayList<String>();` працює, бо компілятор знає тип `new ArrayList<String>()` і присвоює його `x`.
> * **Лямбда-вираз**: виводить тип **з лівого боку** виразу. Лямбда не має типу сама по собі; вона отримує його від цільового типу (змінної зліва).
>
> Виходить **замкнене коло**: щоб вивести тип для `var`, потрібно знати тип виразу справа. А щоб вивести тип для лямбди, потрібно знати тип змінної зліва. Оскільки ні те, ні інше невідомо, компілятор не може розв'язати цю неоднозначність.
>
> **Як виправити:**
>
> Потрібно явно надати компілятору тип з одного боку.
>
> * Або `Runnable r = () -> {};`
> * Або (для `var`) потрібно виконати явне приведення типу для лямбди: `var r = (Runnable) () -> {};`. Але це робить `var` тут безглуздим.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють `var` і лямбди. Це питання з підступом, яке перевіряє знання правил виведення типів для різних фіч Java.*

* * *

### **5 ❓ Питання:**

Наведіть приклад, де компілятор автоматично виводить тип параметру лямбда-виразу.

> #### **💬 Відповідь:**
>
> Комп_ілятор виводить типи параметрів, дивлячись на абстрактний метод цільового функціонального інтерфейсу.
>
> Розглянемо сортування списку. Метод `List.sort` приймає `Comparator<String>`. Абстрактний метод цього інтерфейсу — `int compare(String o1, String o2)`.

#### **📝 Приклад:**

```java
List<String> names = Arrays.asList("David", "Alice", "Bob");

// Повна версія з явним вказанням типів
names.sort((String s1, String s2) -> s1.compareTo(s2));

// Версія з виведенням типів
// Компілятор знає, що names - це List<String>, отже, `sort` очікує 
// Comparator<String>, і тому s1 та s2 мають бути типу String.
names.sort((s1, s2) -> s1.compareTo(s2)); 
```

Завдяки виведенню типів, код стає значно коротшим і чистішим.

#### **📌 Що перевіряють:**

*Практичні навички написання лямбда-виразів та розуміння, як виведення типів робить їх такими лаконічними.*

* * *

### **6 ❓ Питання:**

Що таке Diamond Operator (`<>`) і як він пов'язаний з виведенням типів?

> #### **💬 Відповідь:**
>
> **Diamond Operator (`<>`)** — це синтаксичний цукор, введений в Java 7, який дозволяє не дублювати типи-параметри (generics) при створенні екземпляра класу.
>
> Він також є формою виведення типів. Компілятор "дивиться" на ліву частину виразу, щоб визначити, які типи слід підставити в `diamond operator`.
>
> **Як це працює:**
>
> * **До Java 7 (з дублюванням):**
>     `Map<String, List<Integer>> myMap = new HashMap<String, List<Integer>>();`
>
> * **З Java 7 (з `<>`):**
>     `Map<String, List<Integer>> myMap = new HashMap<>();`
>
> Компілятор бачить, що змінна `myMap` має тип `Map<String, List<Integer>>`, і автоматично виводить ці ж типи для `HashMap`.
>
> Цей механізм є попередником більш просунутого виведення типів для лямбд, але працює за тим же принципом "визначення типу за контекстом зліва".

#### **📌 Що перевіряють:**

*Знання еволюції Java та різних механізмів виведення типів. `Diamond Operator` — це важливий елемент для написання чистого коду з колекціями.*

* * *

### **7 ❓ Питання:**

Чи завжди виведення типів для лямбда-виразів працює бездоганно? Наведіть приклад, коли компілятору може знадобитися допомога. ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не завжди.** Виведення типів може зазнати невдачі, якщо контекст є **неоднозначним (ambiguous)**.
>
> Найпоширеніший випадок — це робота з **перевантаженими (overloaded) методами**, які приймають різні функціональні інтерфейси як параметри.
>
> **Приклад неоднозначності:**
> Уявіть, що є два методи `execute`:
>
> ```java
> void execute(Runnable r) { ... }
> void execute(Callable<String> c) { ... }
> ```
>
> `Runnable` має метод `void run()`. `Callable<String>` — `String call()`.
>
> Тепер спробуємо викликати `execute` з лямбдою, що відповідає обом сигнатурам (не приймає аргументів):
>
> ```java
> // execute(() -> "done"); // ПОМИЛКА КОМПІЛЯЦІЇ
> ```
>
> Компілятор не знає, яку версію `execute` вибрати. Лямбда `() -> "done"` може бути як `Callable`, що повертає рядок, так і `Runnable`, де повернуте значення просто ігнорується.
>
> **Як допомогти компілятору:**
> Потрібно явно привести лямбду до потрібного типу.
>
> ```java
> execute((Callable<String>) () -> "done"); // OK
> ```
>
> Це усуває неоднозначність і допомагає компілятору зробити правильний вибір.

#### **📌 Що перевіряють:**

*Розуміння обмежень виведення типів. Це класичне питання з підступом, яке перевіряє, чи стикався кандидат зі складними сценаріями.*

* * *

### **8 ❓ Питання:**

Як виведення типів працює з посиланнями на методи? ⭐️

> #### **💬 Відповідь:**
>
> Виведення типів для посилань на методи працює за тим же принципом, що і для лямбд: компілятор спирається на **цільовий тип**.
>
> Компілятор аналізує сигнатуру абстрактного методу функціонального інтерфейсу і шукає відповідний метод у класі, на який вказує посилання.
>
> **Приклад:**
> `Stream.of("1", "2", "3").map(Integer::parseInt)`
>
> 1. `map` очікує `Function<String, Integer>`.
> 2. Абстрактний метод `Function` — `Integer apply(String s)`.
> 3. Компілятор шукає в класі `Integer` статичний метод, що приймає `String` і повертає `Integer` (або `int`).
> 4. Він знаходить `public static int parseInt(String s)` і успішно зв'язує посилання.
>
> Проблеми з неоднозначністю (якщо є перевантажені методи) вирішуються так само — компілятор шукає найбільш точну відповідність.

#### **📌 Що перевіряють:**

*Розуміння, що механізм `Target Typing` є спільним для лямбд і посилань на методи.*

* * *

### **9 ❓ Питання:**

Як виведення типів взаємодіє з узагальненими (generic) методами? ⭐️

> #### **💬 Відповідь:**
>
> Виведення типів відіграє ключову роль у тому, щоб робити виклики узагальнених методів лаконічними.
>
> До Java 8 вам часто доводилося явно вказувати типи-параметри перед викликом generic-методу.
>
> **Приклад:**
>
> ```java
> // Старий стиль
> Collections.<String, Integer>emptyMap();
> 
> // Сучасний стиль з виведенням типів
> Map<String, Integer> map = Collections.emptyMap(); 
> ```
>
> У другому випадку компілятор "дивиться" на тип змінної `map`, бачить `Map<String, Integer>` і розуміє, що метод `emptyMap()` повинен повернути саме такий тип. Він автоматично "підставляє" `String` та `Integer` як типи-параметри для методу `emptyMap`.
>
> Так само це працює і при передачі лямбд в generic-методи, що приймають функціональні інтерфейси. Компілятор використовує всю доступну інформацію, щоб вивести кінцевий тип.

#### **📌 Що перевіряють:**

*Розуміння того, як виведення типів працює у поєднанні з generics, що є однією з найскладніших частин системи типів Java.*

* * *

### **10 ❓ Питання:**

Уявіть ситуацію: `myMethod(null)`. Чи може компілятор вивести тип, якщо `myMethod` перевантажений: `void myMethod(String s)` і `void myMethod(Integer i)`?

> #### **💬 Відповідь:**
>
> **Ні, не може.** Цей код не скомпілюється через **неоднозначність виклику (ambiguous method call)**.
>
> **Причина:**
>
> Значення `null` в Java є сумісним з **будь-яким** об'єктним типом. Тобто `null` може бути і `String`, і `Integer`.
>
> Оскільки компілятор бачить, що виклик `myMethod(null)` відповідає обом сигнатурам, він не може зробити однозначний вибір і видає помилку.
>
> **Як виправити:**
>
> Потрібно явно вказати, до якого типу слід привести `null`, щоб компілятор зрозумів, яку версію методу викликати.
>
> ```java
> myMethod((String) null); // Викликає myMethod(String s)
> myMethod((Integer) null); // Викликає myMethod(Integer i)
> ```
>
> Цей приклад показує, що навіть у простих, не-лямбда ситуаціях виведення типів має свої межі.

#### **📌 Що перевіряють:**

*Знання правил розв'язання перевантаження в Java та того, як обробляється `null`. Це класичне питання з підступом.*

* * *

### **11 ❓ Питання:**

Що змінилося у виведенні типів для diamond operator в Java 9? ⭐️⭐️

> #### **💬 Відповідь:**
>
> В Java 9 було зроблено невелике, але важливе покращення для diamond operator (`<>`): йому **дозволили використовуватися з анонімними класами**.
>
> **Проблема в Java 7 та 8:**
>
> Цей код не компілювався:
>
> ```java
> Comparator<String> comp = new Comparator<>() { // ПОМИЛКА
>     @Override
>     public int compare(String o1, String o2) { ... }
> };
> ```
>
> Компілятор не міг вивести тип (`String`) для анонімного класу, коли використовувався diamond operator. Вам доводилося писати повний тип: `new Comparator<String>() { ... }`.
>
> **Рішення в Java 9:**
>
> Компілятор став "розумнішим". Тепер він може вивести тип для анонімного класу з контексту, і наведений вище код став валідним. Це зробило використання анонімних класів з generics трохи менш багатослівним.

#### **📌 Що перевіряють:**

*Знання еволюції Java за межами версії 8. Це демонструє, що кандидат слідкує за розвитком платформи.*

* * *

### **12 ❓ Питання:**

Розглянемо складний ланцюжок: `list.stream().collect(Collectors.toMap(k -> k, v -> v.length()))`. Як тут відбувається виведення типів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тут відбувається багатошарове виведення типів "ззовні всередину".
>
> Припустимо, `list` має тип `List<String>`.
>
> 1. `list.stream()`: Цільовий тип тут не потрібен. Метод повертає `Stream<String>`.
> 2. `.collect(...)`: Компілятор аналізує метод `collect`, який приймає `Collector`.
> 3. `Collectors.toMap(...)`: Це метод, який повертає `Collector`. Він має кілька перевантажених версій. Компілятор буде шукати ту, яка відповідає аргументам.
> 4. **`k -> k`**: Перший аргумент `toMap` — це `keyMapper`. Це `Function<T, K>`. Оскільки вхідний потік має тип `Stream<String>`, компілятор виводить, що `k` є `String`, а тип ключа мапи також буде `String`.
> 5. **`v -> v.length()`**: Другий аргумент — `valueMapper`. Це `Function<T, V>`. Компілятор знову бачить, що `v` має бути `String` (бо це тип елемента потоку). Він аналізує вираз `v.length()` і розуміє, що він повертає `int`. Отже, тип значення мапи буде `Integer`.
> 6. **Зведення:** Компілятор розуміє, що `Collectors.toMap` створить `Collector<String, ?, Map<String, Integer>>`.
> 7. **Кінцевий результат:** Метод `collect` поверне `Map<String, Integer>`.
>
> Увесь цей складний аналіз відбувається автоматично, що робить код таким коротким.

#### **📌 Що перевіряють:**

*Здатність "мислити як компілятор" і розбирати складні ланцюжки викликів з узагальненнями та лямбдами. Це ознака глибокого розуміння системи типів.*

* * *

### **13 ❓ Питання:**

Що таке перетин типів (Intersection Types) і як він пов'язаний з лямбдами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Перетин типів** — це можливість вказати, що об'єкт реалізує **одночасно кілька інтерфейсів**. Синтаксис використовує амперсанд (`&`).
> `T extends Runnable & Serializable`
>
> **Як це пов'язано з лямбдами:**
>
> Якщо лямбда-вираз присвоюється цільовому типу, що є перетином, то результуючий об'єкт буде реалізувати **всі інтерфейси** з цього перетину, за умови, що лише один з них є **функціональним**.
>
> Це рідкісний, але потужний механізм. Наприклад, ви можете створити лямбду, яка є одночасно і `Runnable`, і `Serializable`.
>
> #### **Приклад:**
>
> ```java
> // Лямбда присвоюється типу, що є перетином двох інтерфейсів
> Object myLambda = (Runnable & java.io.Serializable) () -> System.out.println("Hello");
> 
> // Тепер ми можемо безпечно привести її до кожного з типів
> Runnable runnable = (Runnable) myLambda;
> java.io.Serializable serializable = (java.io.Serializable) myLambda;
> ```
>
> Це можливо, тому що `Runnable` — функціональний інтерфейс, а `Serializable` — це "маркерний" інтерфейс без абстрактних методів. Компілятор може створити об'єкт, що задовольняє обом контрактам.

#### **📌 Що перевіряють:**

*Експертне знання системи типів Java. Це дуже просунута тема, знання якої вказує на високий рівень кандидата.*

* * *

### **14 ❓ Питання:**

Чому цей код компілюється: `Predicate<String> p = s -> s.isEmpty();`, а цей ні: `var p = s -> s.isEmpty();`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця різниця виникає через фундаментальні відмінності у тому, як `var` та явне оголошення типу взаємодіють з виведенням типів для лямбд.
>
> 1. **`Predicate<String> p = s -> s.isEmpty();` (Компілюється)**
>     * Тут **ліва частина є відомою**. `p` має явний тип `Predicate<String>`.
>     * Цей тип стає **цільовим типом (target type)** для лямбди.
>     * Компілятор "дивиться" на `Predicate<String>`, знаходить його метод `boolean test(String s)`, і використовує цю інформацію, щоб зрозуміти і перевірити лямбду `s -> s.isEmpty()`.
>
> 2. **`var p = s -> s.isEmpty();` (Не компілюється)**
>     * Тут **ліва частина невідома**. `var` наказує компілятору вивести тип змінної `p` з виразу **справа**.
>     * Але вираз справа — це лямбда, яка **сама по собі не має типу**!
>     * Виникає **"курка і яйце"**-проблема: щоб вивести тип для `var`, потрібен тип праворуч. Щоб вивести тип для лямбди, потрібен тип зліва. Оскільки обидва невідомі, компілятор не може нічого зробити.
>
> **Висновок:** `var` вимагає, щоб вираз, що присвоюється, мав свій власний, чітко визначений тип. Лямбда-вирази його не мають, тому їх не можна використовувати з `var` напряму.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, що `var` — це не "динамічна типізація", а синтаксичний цукор, який все ще вимагає, щоб тип можна було однозначно вивести на етапі компіляції. Це показує досконале володіння мовою.*

* * *

### **15 ❓ Питання:**

Як виведення типів пов'язане з механізмом `invokedynamic`, що використовується для реалізації лямбд? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони тісно пов'язані, але працюють на різних етапах. **Виведення типів** — це робота **компілятора (`javac`)**. **`invokedynamic`** — це механізм, який використовує **JVM** під час виконання.
>
> **Процес:**
>
> 1. **Етап компіляції (`javac`):**
>     * Компілятор використовує **виведення типів**, щоб перевірити, чи є лямбда валідною для даного цільового типу (функціонального інтерфейсу).
>     * Якщо все гаразд, він **не створює анонімний клас**. Замість цього він генерує:
>         * **`private static` метод**, що містить тіло лямбди.
>         * **`invokedynamic`** інструкцію в байт-коді на місці використання лямбди. Ця інструкція є "рецептом" для створення об'єкта лямбди.
>
> 2. **Етап виконання (JVM):**
>     * Коли JVM вперше доходить до інструкції `invokedynamic`, вона виконує **зв'язування (linking)**.
>     * Запускається спеціальний "bootstrap" метод, який динамічно створює об'єкт-реалізацію функціонального інтерфейсу. Цей об'єкт посилається на згенерований `private static` метод.
>
> **Зв'язок:**
>
> **Виведення типів** — це крок компілятора, який дозволяє йому підготувати всю необхідну інформацію (цільовий інтерфейс, сигнатуру методу) для того, щоб згенерувати правильну `invokedynamic` інструкцію. Без успішного виведення типів, компілятор просто не знав би, який "рецепт" записати в байт-код для JVM.

#### **📌 Що перевіряють:**

*Експертне розуміння всього життєвого циклу лямбда-виразу — від вихідного коду до виконання в JVM. Це поєднує знання компілятора, системи типів та внутрішніх механізмів JVM.*

* * *
