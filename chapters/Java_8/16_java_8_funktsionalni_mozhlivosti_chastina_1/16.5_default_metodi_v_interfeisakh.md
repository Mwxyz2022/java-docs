---
layout: default
title: "Default методи в інтерфейсах"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 5
---

# Default методи в інтерфейсах

* короткий опис: `Default` методи – це методи в інтерфейсах (з Java 8+), що мають реалізацію за замовчуванням. Дозволяють додавати нові методи до інтерфейсів без порушення зворотної сумісності для існуючих реалізацій.

* поняття: `default` методи, зворотна сумісність, множинне наслідування поведінки.

* приклади: `default void honk() { System.out.println("Beep"); }`.

* ключові моменти: Не можуть змінювати стан об'єкта. При конфліктах у множинному наслідуванні потребують явного перевизначення.

* що важливо знати на співбесіді: Призначення, обмеження, вирішення конфліктів.

### **✅ Офіційне визначення:**

**Default методи** — це методи в інтерфейсах з реалізацією за замовчуванням, які дозволяють додавати нову поведінку до інтерфейсів без порушення зворотної сумісності.

### **🧠 Простими словами:**

Тепер в інтерфейсах можна не тільки описувати, як має виглядати метод, а й давати йому базову реалізацію, щоб не ламати існуючий код, якщо інтерфейс зміниться

---

## **Основне**

* **default метод** — метод з реалізацією в інтерфейсі, позначений ключовим словом **`default`**.
* **Зворотна сумісність** — старий код, що реалізує інтерфейс, не потребує змін при додаванні default методів.
* **Множинне наслідування поведінки** — інтерфейс може наслідувати default методи з кількох інтерфейсів, що може
  призводити до конфліктів.

---

## **Пояснення на пальцях (💡)**

Раніше, щоб додати новий метод в інтерфейс, треба було змінювати всі реалізації. Тепер можна додати default метод — він працюватиме "за замовчуванням", і старі класи не зламаються

---

## **Корисні приклади (🧪)**

```java
interface Vehicle {
    void move();

    default void honk() {
        System.out.println("Beep beep!");
    }
}

class Car implements Vehicle {
    public void move() {
        System.out.println("Car is moving");
    }
}

public class Main {
    public static void main(String[] args) {
        Vehicle car = new Car();
        car.move();  // Car is moving
        car.honk();  // Beep beep! - default метод використовується
    }
}
```

**Вирішення конфлікту:**

```java
interface A {
    default void show() {
        System.out.println("Interface A");
    }
}

interface B {
    default void show() {
        System.out.println("Interface B");
    }
}

class C implements A, B {
    @Override
    public void show() {
        A.super.show();  // Явне звернення до потрібного default методу
        B.super.show();
    }
}
```

---

## **Порівняння (⚖️)**

|                Особливість                | Звичайний метод інтерфейсу (до Java 8\) |            Default метод             |
|:-----------------------------------------:|:---------------------------------------:|:------------------------------------:|
|        **Реалізація в інтерфейсі**        |                  Немає                  |                  Є                   |
|          **Зворотна сумісність**          |   Змінювати всі реалізації при зміні    |             Зберігається             |
| **Конфлікти при множинному наслідуванні** |            Не застосовується            |     Потрібно явне перевизначення     |
|       **Можливість змінювати стан**       |                   Ні                    | Ні (default методи не змінюють стан) |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔑 **"Default — це запасний план для інтерфейсу, щоб не ламати старий код, але дати новий фічер."**

---

## **🟩 Підсумок:**

* Default методи дозволяють додавати реалізацію в інтерфейси, забезпечуючи зворотну сумісність.
* Не можуть змінювати стан об’єкта, служать для надання базової поведінки.
* Виникають конфлікти при множинному наслідуванні — потрібно явне вирішення.
* Важливий інструмент для розвитку API без порушення існуючого коду.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `default`-методи в інтерфейсах і яку проблему вони вирішують?

> #### **💬 Відповідь:**
>
> **Default методи** — це методи, оголошені в інтерфейсі з ключовим словом `default`, які мають **реалізацію за замовчуванням**. Вони були введені в Java 8.
>
> **Головна проблема, яку вони вирішують — це проблема зворотної сумісності (backward compatibility).**
>
> До Java 8 інтерфейси могли містити лише абстрактні методи. Якщо потрібно було додати новий метод до існуючого інтерфейсу (наприклад, до `List`), це призводило до "зламу" всіх існуючих класів, що реалізують цей інтерфейс, оскільки вони були змушені додавати реалізацію цього нового методу.
>
> `Default`-методи вирішують цю проблему. Ви можете додати новий `default`-метод до інтерфейсу, і всі існуючі класи, що його реалізують, автоматично успадкують цю реалізацію за замовчуванням. Їм не потрібно змінювати свій код, і нічого не ламається.

#### **📝 Аналогія:**

Уявіть, що ви розробник конструктора "Lego". Якщо ви випустите нову деталь (новий метод) і скажете, що тепер всі старі набори мають її підтримувати, всі, хто купив старі набори, будуть розчаровані. `Default`-метод — це як додати нову деталь, але також додати інструкцію "якщо у вас старої деталі немає, ось як можна обійтися без неї".

#### **📌 Що перевіряють:**

*Розуміння основної мотивації введення `default`-методів — забезпечення еволюції API без порушення зворотної сумісності. Це ключова концепція Java 8.*

* * *

### **2 ❓ Питання:**

Наведіть приклад `default`-методу і поясніть, як він працює.

> #### **💬 Відповідь:**
>
> Розглянемо простий інтерфейс `Vehicle`, який має один абстрактний метод `startEngine()` і один `default`-метод `log()`.
>
> `Default`-метод оголошується з ключовим словом `default` і має тіло. Класи, що реалізують цей інтерфейс, **не зобов'язані** перевизначати `default`-метод — вони можуть використовувати його реалізацію за замовчуванням.

#### **📝 Приклад:**

```java
interface Vehicle {
    void startEngine(); // Абстрактний метод

    default void log(String message) {
        System.out.println("LOG: " + message);
    }
}

class Car implements Vehicle {
    @Override
    public void startEngine() {
        // Викликаємо default-метод з батьківського інтерфейсу
        log("Starting car engine..."); 
    }
}

public class Main {
    public static void main(String[] args) {
        Car car = new Car();
        car.startEngine(); // Викликає startEngine(), який викликає log()
        
        car.log("Test message"); // Можна викликати default-метод напряму
        // Обидва виклики виведуть повідомлення в консоль
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички: здатність написати та використати `default`-метод, а також розуміння того, що його можна викликати всередині класів, що реалізують інтерфейс.*

* * *

### **3 ❓ Питання:**

Чим `default`-метод в інтерфейсі відрізняється від методу в абстрактному класі?

> #### **💬 Відповідь:**
>
> Хоча і ті, і інші надають реалізацію за замовчуванням, між ними є ключові відмінності:
>
> | **Характеристика** | **Метод в абстрактному класі** | **`Default`-метод в інтерфейсі** |
> | :--- | :--- | :--- |
> | **Множинне наслідування**| Клас може наслідувати **лише один** абстрактний клас. | Клас може реалізовувати **багато** інтерфейсів. |
> | **Стан (поля)** | Абстрактний клас може мати **поля екземпляра** (instance fields), що дозволяє зберігати стан. | Інтерфейс **не може** мати полів екземпляра. Отже, `default`-метод не може напряму змінювати стан об'єкта. |
> | **Призначення** | Абстрактний клас використовується для створення ієрархії пов'язаних класів з **спільним станом та поведінкою**. | `Default`-метод використовується для додавання **поведінки** до існуючих класів з різних ієрархій. |
>
> **Висновок:** Абстрактні класи — це про те, "чим є об'єкт" (is-a relationship). Інтерфейси з `default`-методами — про те, "що об'єкт вміє робити" (can-do relationship).

#### **📌 Що перевіряють:**

*Глибоке розуміння відмінностей між інтерфейсами та абстрактними класами, особливо після введення `default`-методів. Це важливе питання для проектування архітектури.*

* * *

### **4 ❓ Питання:**

Що таке "проблема ромба" (Diamond Problem) і як Java вирішує її для `default`-методів?

> #### **💬 Відповідь:**
>
> **Проблема ромба** виникає при множинному наслідуванні, коли клас наслідує два батьківських класи, які мають метод з однаковою сигнатурою. Компілятор не може визначити, яку з реалізацій методу слід викликати.
>
> В Java ця проблема історично уникалася, оскільки множинне наслідування класів заборонено. Однак, з введенням `default`-методів, ця проблема може виникнути, якщо клас реалізує два інтерфейси, кожен з яких має `default`-метод з однаковою сигнатурою.
>
> **Як Java вирішує цей конфлікт:**
>
> Java змушує **розробника** явно вирішити цю неоднозначність. Якщо клас реалізує два інтерфейси з однаковим `default`-методом, компілятор видасть помилку. Щоб її виправити, розробник **зобов'язаний перевизначити** цей метод у своєму класі.
>
> Усередині перевизначеного методу він може:
>
> 1. Написати власну логіку.
> 2. Викликати реалізацію одного з батьківських інтерфейсів за допомогою спеціального синтаксису: **`InterfaceName.super.methodName()`**.
> 3. Скомбінувати логіку з обох батьківських інтерфейсів.

#### **📝 Приклад:**

```java
interface A {
    default void doWork() { System.out.println("Work from A"); }
}
interface B {
    default void doWork() { System.out.println("Work from B"); }
}

class C implements A, B {
    @Override
    public void doWork() {
        // Явно обираємо, яку версію викликати (або пишемо свою)
        A.super.doWork(); 
    }
}
```

#### **📌 Що перевіряють:**

*Знання правил вирішення конфліктів при множинному наслідуванні поведінки. Це класичне питання, що стосується `default`-методів.*

* * *

### **5 ❓ Питання:**

Чи можуть `default`-методи бути `final`, `static` або `synchronized`?

> #### **💬 Відповідь:**
>
> Ні, не можуть. Розглянемо кожен випадок:
>
> * **`final`**: `Default`-методи за своєю природою призначені для того, щоб їх можна було перевизначити в реалізуючих класах. Модифікатор `final` забороняє перевизначення, що суперечить самій ідеї. Тому `default final` є неможливим і призведе до помилки компіляції.
>
> * **`static`**: Метод в інтерфейсі може бути або `default`, або `static`, але не обома одночасно. Вони вирішують різні завдання:
>   * `default`-метод належить **екземпляру** і може бути успадкований.
>   * `static`-метод належить **самому інтерфейсу** і не успадковується класами.
>
> * **`synchronized`**: Модифікатор `synchronized` також не можна застосовувати до `default`-методів. Якщо потрібна синхронізація, розробник повинен реалізувати її у своєму класі, який перевизначає `default`-метод.
>
> `Default`-методи можуть бути лише `public` (цей модифікатор неявний).

#### **📌 Що перевіряють:**

*Знання обмежень та правил використання `default`-методів. Це демонструє глибину розуміння специфікації мови.*

* * *

### **6 ❓ Питання:**

Чи може `default`-метод в інтерфейсі викликати інший `default` або `abstract` метод цього ж інтерфейсу?

> #### **💬 Відповідь:**
>
> **Так, може.** Це поширена практика для створення складної поведінки за замовчуванням.
>
> * **Виклик іншого `default`-методу:** Дозволяє розбивати складну логіку на менші частини всередині самого інтерфейсу.
> * **Виклик `abstract`-методу:** Дозволяє `default`-методу залежати від специфічної реалізації, яку надасть клас. `Default`-метод визначає "шаблон" поведінки, а абстрактний метод — її "деталь".

#### **📝 Приклад:**

```java
interface Document {
    String getContent(); // Абстрактний метод, який має реалізувати клас

    default void print() {
        String content = getContent(); // Виклик абстрактного методу
        log(content); // Виклик іншого default-методу
    }
    
    private void log(String message) { // Починаючи з Java 9, можна використовувати private методи
        System.out.println("Printing: " + message);
    }
}

class Report implements Document {
    @Override
    public String getContent() {
        return "This is a report.";
    }
}

// Виклик:
Report report = new Report();
report.print(); // Використовує default-метод, який залежить від реалізації getContent()
```

#### **📌 Що перевіряють:**

*Розуміння того, як `default`-методи можуть взаємодіяти з іншими членами інтерфейсу для створення гнучкого API.*

* * *

### **7 ❓ Питання:**

Яким чином `default`-методи використовуються у функціональних інтерфейсах, таких як `Predicate` та `Function`?

> #### **💬 Відповідь:**
>
> У функціональних інтерфейсах `default`-методи використовуються для **композиції функцій**, тобто для об'єднання кількох функцій в одну складнішу.
>
> Це дозволяє писати виразний та декларативний код, уникаючи необхідності створювати утилітарні класи або складні лямбда-вирази.
>
> **Приклади:**
>
> * **Інтерфейс `Predicate<T>`:**
>   * `and(other)`: Створює новий предикат, що є логічним "І" поточного та іншого предиката.
>   * `or(other)`: Логічне "АБО".
>   * `negate()`: Логічне "НЕ".
>
> * **Інтерфейс `Function<T, R>`:**
>   * `andThen(after)`: Створює функцію, що спочатку застосовує поточну, а потім `after`.
>   * `compose(before)`: Створює функцію, що спочатку застосовує `before`, а потім поточну.
>
> Ці методи дозволяють створювати складні ланцюжки операцій прямо на екземплярах функціональних інтерфейсів.

#### **📝 Приклад:**

```java
Predicate<String> notEmpty = s -> !s.isEmpty();
Predicate<String> hasLength5 = s -> s.length() == 5;

// Комбінуємо два предикати за допомогою default-методу and()
Predicate<String> isValid = notEmpty.and(hasLength5);
```

#### **📌 Що перевіряють:**

*Розуміння практичного застосування `default`-методів у Java 8 Stream API та функціональному програмуванні.*

* * *

### **8 ❓ Питання:**

Яка основна відмінність між `default`-методами та `static`-методами в інтерфейсах? ⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва типи методів з'явилися в Java 8 і мають реалізацію, вони кардинально відрізняються за своїм призначенням та способом виклику.
>
> * **`Default`-метод:**
>   * **Належить екземпляру:** Є частиною API кожного об'єкта, що реалізує інтерфейс.
>   * **Успадковується:** Класи, що реалізують інтерфейс, успадковують цей метод.
>   * **Можна перевизначити:** Клас може надати свою власну реалізацію.
>   * **Викликається** на екземплярі: `myObject.defaultMethod()`.
>
> * **`Static`-метод:**
>   * **Належить інтерфейсу:** Є утилітарним методом, що належить самому інтерфейсу, а не його екземплярам.
>   * **Не успадковується:** Класи не успадковують статичні методи інтерфейсу.
>   * **Не можна перевизначити.**
>   * **Викликається** на імені інтерфейсу: `MyInterface.staticMethod()`.
>
> `Static`-методи в інтерфейсах часто використовуються для створення фабричних методів або утилітарних функцій, тісно пов'язаних з інтерфейсом.

#### **📌 Що перевіряють:**

*Чітке розуміння відмінностей між `static` та `default` контекстом. Це базове знання для роботи з інтерфейсами Java 8+.*

* * *

### **9 ❓ Питання:**

Чи можна отримати доступ до `default`-методу з батьківського інтерфейсу, якщо він був перевизначений у класі? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, напряму з екземпляра — не можна.** Коли клас перевизначає `default`-метод, цей новий метод повністю "затінює" реалізацію з інтерфейсу. Виклик `myObject.method()` завжди буде викликати версію з класу.
>
> Однак, якщо ви перебуваєте **всередині самого класу**, ви можете звернутися до `default`-реалізації батьківського інтерфейсу за допомогою спеціального синтаксису: **`InterfaceName.super.methodName()`**.
>
> Це той самий синтаксис, що використовується для вирішення конфлікту при "проблемі ромба".

#### **📝 Приклад:**

```java
interface Greeter {
    default void greet() {
        System.out.println("Hello from Greeter");
    }
}

class MyGreeter implements Greeter {
    @Override
    public void greet() {
        // Викликаємо оригінальну реалізацію
        Greeter.super.greet();
        // І додаємо свою логіку
        System.out.println("Hello from MyGreeter");
    }
}
```

#### **📌 Що перевіряють:**

*Знання спеціального синтаксису `Interface.super` та розуміння правил перевизначення методів.*

* * *

### **10 ❓ Питання:**

Які `default`-методи були додані до інтерфейсу `Collection` в Java 8, і який вплив це мало? ⭐️

> #### **💬 Відповідь:**
>
> Додавання `default`-методів до існуючих інтерфейсів Collections Framework є головним прикладом того, для чого ця фіча була створена. Це дозволило ввести **Stream API** без необхідності змінювати мільйони існуючих реалізацій колекцій.
>
> **Ключові `default`-методи, додані в `Collection`:**
>
> * **`stream()`**: Повертає послідовний `Stream` для елементів колекції.
> * **`parallelStream()`**: Повертає паралельний `Stream`.
> * **`spliterator()`**: Повертає `Spliterator` для обходу елементів.
> * **`removeIf(Predicate<? super E> filter)`**: Видаляє всі елементи, що відповідають предикату.
> * **`forEach(Consumer<? super E> action)`**: (Технічно в `Iterable`, від якого наслідує `Collection`) Виконує дію для кожного елемента.
>
> **Вплив:**
> Якби не `default`-методи, розробникам кожної бібліотеки колекцій у світі (Apache Commons, Guava і т.д.), а також кожному розробнику, що створив власну реалізацію `List` чи `Set`, довелося б додати реалізацію всіх цих методів. Завдяки `default`-методам, будь-яка стара колекція автоматично отримала нову функціональність, як-от `stream()`, "безкоштовно".

#### **📌 Що перевіряють:**

*Розуміння реального, практичного застосування `default`-методів у дизайні JDK та їхньої ролі в еволюції Java.*

* * *

### **11 ❓ Питання:**

Як `default`-методи взаємодіють з методом `Object.equals()`? ⭐️

> #### **💬 Відповідь:**
>
> Інтерфейс **не може** надати `default`-реалізацію для методів, які мають ту саму сигнатуру, що й `public` методи класу `Object` (такі як `equals()`, `hashCode()`, `toString()`).
>
> Спроба оголосити такий `default`-метод призведе до **помилки компіляції**.
>
> ```java
> interface MyInterface {
>     // ПОМИЛКА КОМПІЛЯЦІЇ
>     default boolean equals(Object obj) {
>         return true;
>     }
> }
> ```
>
> **Чому так зроблено?**
>
> Щоб уникнути плутанини та зберегти фундаментальну поведінку класу `Object`. Кожен об'єкт в Java вже має реалізацію `equals` та `hashCode`, і логіка цих методів тісно пов'язана зі станом об'єкта, якого інтерфейси не мають. Дозволивши `default`-реалізацію `equals` в інтерфейсі, можна було б легко порушити базові контракти `equals` (симетрія, транзитивність), особливо при множинній реалізації.

#### **📌 Що перевіряють:**

*Знання тонких обмежень `default`-методів та розуміння їх взаємодії з фундаментальними методами класу `Object`.*

* * *

### **12 ❓ Питання:**

Починаючи з Java 9, в інтерфейсах можна оголошувати `private` методи. Як вони доповнюють `default`-методи? ⭐️

> #### **💬 Відповідь:**
>
> Додавання **`private`-методів** в інтерфейси (з Java 9) було логічним продовженням еволюції, розпочатої `default`-методами.
>
> **Проблема, яку вони вирішують:**
>
> Іноді `default`-метод містить складну логіку. Як і в звичайних класах, таку логіку хочеться розбити на менші, допоміжні методи для кращої читабельності та уникнення дублювання коду.
>
> До Java 9, якщо вам потрібно було кілька `default`-методів зі спільною логікою, вам довелося б або дублювати код, або виносити цю спільну логіку в **ще один `default`-метод**. Але такий допоміжний `default`-метод ставав частиною **публічного API** інтерфейсу, що було небажано.
>
> **Рішення `private`-методами:**
>
> Тепер ви можете винести спільну логіку в `private` метод всередині інтерфейсу. Цей метод буде доступний **лише** для виклику з `default` (або інших `private`) методів цього ж інтерфейсу, і не буде "забруднювати" публічний API.

#### **📝 Приклад:**

```java
interface AdvancedDocument {
    default void save() {
        validate(); // Виклик private методу
        // ... логіка збереження
    }

    default void publish() {
        validate(); // Повторне використання private методу
        // ... логіка публікації
    }

    // Допоміжний метод, недоступний для реалізуючих класів
    private void validate() {
        System.out.println("Validation logic...");
    }
}
```

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови Java (після версії 8). Розуміння, як `private`-методи в інтерфейсах допомагають писати більш чистий та інкапсульований код.*

* * *

### **13 ❓ Питання:**

Як правило наслідування вирішує, який `default`-метод викликати, якщо клас успадковує його від класу та інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для вирішення цього конфлікту існує чітке правило, яке можна сформулювати як **"клас завжди перемагає" (class wins)**.
>
> **Правило:**
> Якщо клас успадковує метод з однаковою сигнатурою і від свого **батьківського класу**, і від **інтерфейсу** (як `default`-метод), то версія з **батьківського класу завжди має пріоритет**. Реалізація з інтерфейсу буде **проігнорована**.
>
> Цей механізм було введено, щоб забезпечити сумісність зі старим кодом і зберегти ієрархію класів як основну.

#### **📝 Приклад:**

```java
class MyBaseClass {
    public void doWork() {
        System.out.println("From Class");
    }
}
interface MyInterface {
    default void doWork() {
        System.out.println("From Interface");
    }
}
class MyChild extends MyBaseClass implements MyInterface {
    // Не потрібно перевизначати doWork(), компілятор сам вирішить конфлікт
}

// Виклик:
new MyChild().doWork(); // Виведе "From Class"
```

#### **📌 Що перевіряють:**

*Знання тонких правил вирішення конфліктів наслідування в Java. Це класичне питання, що перевіряє глибину знань мови.*

* * *

### **14 ❓ Питання:**

Чому `default`-метод не може змінювати стан об'єкта, хоча технічно це можливо? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча технічно ніщо не заважає `default`-методу викликати `setter`-и або інші методи, що змінюють стан (якщо вони оголошені в інтерфейсі), це вважається **дуже поганою практикою** і суперечить самій ідеї `default`-методів.
>
> **Причини:**
>
> 1. **Інтерфейси не мають стану:** Головна причина в тому, що інтерфейс не може мати полів екземпляра. Отже, він не може "володіти" станом. Зміна стану має бути відповідальністю самого класу, який цим станом володіє.
>
> 2. **Порушення принципу найменшого здивування:** Розробники очікують, що `default`-метод надає поведінку, а не несподівано змінює внутрішній стан об'єкта. Це може призвести до важких для відстеження багів.
>
> 3. **Проблеми з множинною реалізацією:** Якщо клас реалізує кілька інтерфейсів, і кожен з них має `default`-метод, що намагається змінити стан, це може призвести до хаотичної та непередбачуваної поведінки.
>
> **Правильний підхід:**
>
> `Default`-метод має базувати свою логіку на інших методах інтерфейсу (абстрактних або `default`), а не на припущеннях про внутрішній стан реалізуючого класу.
>
> **Висновок:** Хоча `default`-метод може опосередковано викликати мутацію, проектувати їх спеціально для цього — це антипатерн.

#### **📌 Що перевіряють:**

*Розуміння не тільки синтаксичних, а й семантичних, "духовних" правил мови. Кандидат повинен розуміти, як слід проектувати API та дотримуватися хороших практик.*

* * *

### **15 ❓ Питання:**

Як `default`-методи дозволили реалізувати метод `Map.forEach`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Інтерфейс `Map` сам по собі **не є `Iterable`**, тому на ньому не можна було просто так викликати `forEach`.
>
> `Default`-методи дозволили елегантно додати цю функціональність без зламу зворотної сумісності:
>
> **Як це реалізовано:**
>
> 1. До інтерфейсу `Map` було додано `default`-метод:
>     `default void forEach(BiConsumer<? super K, ? super V> action)`
>
> 2. Реалізація цього `default`-методу за замовчуванням виглядає приблизно так:
>     * Вона отримує набір ключ-значення мапи за допомогою методу `entrySet()`.
>     * Далі вона ітерується по цьому `Set` (який вже є `Iterable`).
>     * Для кожного запису (`Entry`) вона викликає наданий `BiConsumer`, передаючи в нього ключ та значення (`action.accept(entry.getKey(), entry.getValue())`).
>
> **Чому це важливо:**
>
> Оскільки `forEach` був доданий як `default`-метод, **всім існуючим реалізаціям** `Map` (таким як `HashMap`, `TreeMap`, а також незліченним кастомним реалізаціям у сторонніх бібліотеках) **не довелося** додавати його реалізацію. Вони "безкоштовно" отримали новий, зручний функціонал, сумісний з лямбдами. Це і є головна сила `default`-методів.

#### **📌 Що перевіряють:**

*Здатність проаналізувати дизайн існуючих API та пояснити, як і чому були застосовані певні мовні конструкції. Це питання на розуміння реальних прикладів застосування `default`-методів у JDK.*

* * *
