---
layout: default
title: "Лямбда-вирази (Lambda Expressions)"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 3
---

# Лямбда-вирази (Lambda Expressions)

* короткий опис: Лямбда-вираз – це анонімна функція, що дозволяє передавати блок коду як об'єкт функціонального інтерфейсу. Має синтаксис `(параметри) -> { тіло }`.

* поняття: Лямбда-вираз, оператор `->`, параметри, тіло функції.

* приклади: `x -> x * x` (один параметр), `(x, y) -> x + y` (кілька параметрів), `() -> System.out.println("Hello")` (без параметрів).

* ключові моменти: Коротший синтаксис порівняно з анонімними класами, працює тільки з функціональними інтерфейсами, захоплює `effectively final` змінні.

* що важливо знати на співбесіді: Синтаксис, обмеження використання, переваги.

### **✅ Офіційне визначення:**

Лямбда-вираз — це анонімна функція, яка дозволяє передавати блок коду як об’єкт функціонального інтерфейсу. В Java лямбда має синтаксис **`(параметри) -> { тіло }`**, де **`->`** — це оператор, що відокремлює список параметрів від тіла функції.

### **🧠 Простими словами:**

Лямбда — це короткий спосіб записати маленьку функцію без класу і методу, щоб було зручно передати код у вигляді змінної або параметра.

---

## 📊 Основні форми лямбда-виразів

|         Вхідні параметри          |        Приклад синтаксису        |       Пояснення       |
|:---------------------------------:|:--------------------------------:|:---------------------:|
|        **Без параметрів**         | `() -> System.out.println("Hi")` | Лямбда без аргументів |
|         **Один параметр**         |           `x -> x * x`           |  Параметр без дужок   |
|      **Декілька параметрів**      |        `(x, y) -> x + y`         |  Параметри у дужках   |
|    **Тіло у фігурних дужках**     |  `(x, y) -> { return x + y; }`   | Більш розгорнуте тіло |
| **Однорядкове тіло без `return`** |           `x -> x + 1`           |  Автоматичний return  |

---

## **Пояснення на пальцях (💡)**

Лямбда-вираз — це як секретний агент, який виконує одну функцію без зайвих церемоній: не треба писати цілий клас чи метод — просто дай йому інструкції і використай. Це економить купу коду та робить його чистішим і зрозумілішим

---

## **Корисні приклади (🧪)**

```java
// Без параметрів
Runnable r = () -> System.out.println("Hello!");

// Один параметр (без дужок)
UnaryOperator<Integer> square = x -> x * x;

// Декілька параметрів (з дужками)
BinaryOperator<Integer> add = (x, y) -> x + y;

// Тіло у фігурних дужках з return
BinaryOperator<Integer> multiply = (x, y) -> { return x * y; };

// Виклик
System.out.println(square.apply(5));  // 25
System.out.println(add.apply(3, 4));  // 7
System.out.println(multiply.apply(2, 3));  // 6
```

---

## **Порівняння (⚖️)**

|               Критерій               |               Лямбда-вирази               |                      Анонімні класи                      |
|:------------------------------------:|:-----------------------------------------:|:--------------------------------------------------------:|
|            Кількість коду            |         Менше, компактніші записи         |                  Більше, шаблонний код                   |
|             Зрозумілість             |         Вища, простіша синтаксис          |                 Можуть бути громіздкими                  |
| Підтримка функціональних інтерфейсів |         Обов’язкова (один метод)          |         Можуть реалізовувати будь-які інтерфейси         |
|           Зовнішні змінні            |        Потрібно effectively final         | Можуть змінювати локальні змінні (з деякими обмеженнями) |
|              Перформанс              | Легший, JVM оптимізує через invokedynamic |    Трохи важчий, оскільки створюється анонімний клас     |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔑 **"-\>" — це стріла, що вказує шлях від параметрів до дії**

Ти показуєш, що приймаєш — і куди це ведеш

---

## **🟩 Підсумок:**

* Лямбда-вирази — це компактний синтаксис для реалізації функціональних інтерфейсів.
* Вони замінюють анонімні класи, роблячи код чистішим і легшим для читання.
* Java підтримує різні форми запису в залежності від кількості параметрів і складності тіла.
* Працюють тільки з функціональними інтерфейсами (один абстрактний метод).
* На співбесідах часто питають синтаксис, обмеження і переваги лямбда-виразів.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs – Lambda Expressions**](https://docs.oracle.com/javase/tutorial/java/javaOO/lambdaexpressions.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке лямбда-вираз в Java?

> #### **💬 Відповідь:**
>
> **Лямбда-вираз (Lambda Expression)** — це, по суті, **анонімна (безіменна) функція**, яка дозволяє передавати блок коду як параметр методу або зберігати його у змінній. Лямбди були введені в Java 8 як спосіб реалізації **функціональних інтерфейсів** більш компактним та виразним синтаксисом.
>
> Основна мета лямбда-виразів — зменшити кількість "церемоніального" коду (boilerplate), який був необхідний при використанні анонімних класів.
>
> Лямбда-вираз складається з трьох частин:
>
> 1. **Список параметрів** (в дужках).
> 2. **Оператор-стрілка** (`->`).
> 3. **Тіло виразу**, що містить логіку.
>
> **Приклад:** `(x, y) -> x + y` — це лямбда, що приймає два аргументи і повертає їх суму.

#### **📝 Аналогія:**

Лямбда-вираз — це як короткий рецепт, записаний на серветці. Замість того, щоб створювати цілу книгу рецептів (клас) з окремим розділом (метод), ви просто пишете: `(яйця, борошно) -> змішати і пекти`. Це швидко, коротко і зрозуміло.

#### **📌 Що перевіряють:**

*Базове визначення та розуміння, що лямбда — це компактний спосіб передати поведінку (блок коду).*

* * *

### **2 ❓ Питання:**

Який основний синтаксис лямбда-виразів? Наведіть приклади.

> #### **💬 Відповідь:**
>
> Загальний синтаксис лямбда-виразу: `(параметри) -> { тіло_виразу }`. Однак існує кілька скорочених форм, що роблять його ще компактнішим.
>
> **Основні варіанти синтаксису:**
>
> 1. **Декілька параметрів:**
>
>     ```java
>     (int x, int y) -> { return x + y; }
>     ```
>
> 2. **З виведенням типів:** Компілятор може сам визначити типи параметрів з контексту, тому їх можна не вказувати.
>
>     ```java
>     (x, y) -> { return x + y; }
>     ```
>
> 3. **Один параметр:** Якщо параметр лише один, дужки навколо нього можна опустити.
>
>     ```java
>     x -> { return x * x; }
>     ```
>
> 4. **Без параметрів:** Використовуються порожні дужки.
>
>     ```java
>     () -> { System.out.println("Hello"); }
>     ```
>
> 5. **Однорядкове тіло:** Якщо тіло лямбди складається з одного виразу, фігурні дужки `{}` та слово `return` можна опустити. Компілятор автоматично поверне результат цього виразу.
>
>     ```java
>     (x, y) -> x + y  // `return` тут неявний
>     ```

#### **📌 Що перевіряють:**

*Знання різних синтаксичних форм лямбда-виразів. Кандидат повинен вміти читати та писати як повні, так і скорочені варіанти.*

* * *

### **3 ❓ Питання:**

З якими типами інтерфейсів можуть працювати лямбда-вирази?

> #### **💬 Відповідь:**
>
> Лямбда-вирази в Java можуть бути використані лише для реалізації **функціональних інтерфейсів**.
>
> **Функціональний інтерфейс** — це будь-який інтерфейс, що містить **рівно один абстрактний метод (Single Abstract Method - SAM)**.
>
> Коли ви присвоюєте лямбда-вираз змінній типу функціонального інтерфейсу, компілятор "розуміє", що тіло цієї лямбди є реалізацією того самого єдиного абстрактного методу.
>
> **Приклад:**
> Інтерфейс `Runnable` є функціональним, бо має лише один абстрактний метод `void run()`.
>
> ```java
> Runnable myTask = () -> System.out.println("Task is running");
> ```
>
> Тут лямбда `() -> System.out.println("Task is running")` є реалізацією методу `run()`.
>
> Якщо інтерфейс має два або більше абстрактних методів, його не можна реалізувати за допомогою лямбди, тому що компілятор не знатиме, який саме метод ви намагаєтесь реалізувати.

#### **📌 Що перевіряють:**

*Чітке розуміння зв'язку між лямбда-виразами та функціональними інтерфейсами. Це ключове обмеження та особливість лямбд у Java.*

* * *

### **4 ❓ Питання:**

У чому основні переваги лямбда-виразів порівняно з анонімними класами?

> #### **💬 Відповідь:**
>
> Лямбда-вирази надають значні переваги порівняно з анонімними класами, які були основним способом передачі поведінки до Java 8.
>
> 1. **Компактність та читабельність:**
>     Лямбди мають значно коротший та чистіший синтаксис. Вони прибирають увесь "церемоніальний" код (boilerplate), такий як `new InterfaceName() { @Override public ... }`.
>     * **Анонімний клас:** `new Thread(new Runnable() { public void run() { ... } });`
>     * **Лямбда:** `new Thread(() -> { ... });`
>
> 2. **Поведінка `this`:**
>     * В анонімному класі `this` посилається на екземпляр самого анонімного класу.
>     * В лямбда-виразі `this` посилається на екземпляр зовнішнього класу, в якому лямбда визначена. Це більш інтуїтивно.
>
> 3. **Продуктивність:**
>     * JVM реалізує лямбди через механізм `invokedynamic`, що є більш ефективним і гнучким, ніж створення окремого `.class` файлу для кожного анонімного класу.
>
> 4. **Фокус на поведінці:**
>     Лямбди чітко відокремлюють логіку (що робити) від об'єктної обгортки, що робить код більш функціональним.

#### **📌 Що перевіряють:**

*Розуміння, яку саме проблему вирішують лямбда-вирази, та здатність порівняти їх із застарілим підходом.*

* * *

### **5 ❓ Питання:**

Що означає "захоплення змінних" (variable capture) лямбда-виразом? Які обмеження накладаються на такі змінні?

> #### **💬 Відповідь:**
>
> Лямбда-вирази (а також анонімні та локальні класи) можуть "захоплювати", тобто використовувати, локальні змінні з того методу, в якому вони оголошені.
>
> Однак, на такі змінні накладається суворе обмеження: вони мають бути **`final`** або **"ефективно фінальними" (effectively final)**.
>
> **Ефективно фінальна змінна** — це змінна, значення якої не змінюється після її першої ініціалізації. Використання ключового слова `final` для таких змінних не є обов'язковим, компілятор сам перевіряє це.
>
> **Причина обмеження:**
> Лямбда може виконуватися набагато пізніше (наприклад, в іншому потоці), коли метод, у якому вона була створена, вже завершив свою роботу, а його локальні змінні в стеку — знищені. Щоб це працювало, компілятор **копіює значення** захопленої змінної. Щоб уникнути розсинхронізації між оригіналом і копією, Java просто забороняє змінювати таку змінну.

#### **📝 Приклад:**

```java
public void process() {
    String prefix = "LOG: "; // effectively final
    // prefix = "WARN: "; // Якщо розкоментувати, буде помилка компіляції

    Consumer<String> logger = message -> {
        // Лямбда захоплює копію змінної prefix
        System.out.println(prefix + message);
    };

    logger.accept("Something happened");
}
```

#### **📌 Що перевіряють:**

*Знання важливого правила щодо роботи лямбд з локальними змінними та розуміння причин цього обмеження.*

* * *

### **6 ❓ Питання:**

Наведіть приклад використання лямбда-виразу зі Stream API.

> #### **💬 Відповідь:**
>
> Лямбда-вирази є основою для роботи зі Stream API. Вони передаються як аргументи в такі методи, як `filter`, `map`, `forEach`.
>
> **Сценарій:** Зі списку рядків відфільтрувати ті, що довші за 3 символи, перетворити їх у верхній регістр і вивести в консоль.
>
> У цьому прикладі використовуються три лямбда-вирази:
>
> 1. `s -> s.length() > 3`: `Predicate` для фільтрації.
> 2. `s -> s.toUpperCase()`: `Function` для трансформації.
> 3. `s -> System.out.println(s)`: `Consumer` для кінцевої дії.

#### **📝 Приклад:**

```java
import java.util.Arrays;
import java.util.List;

public class StreamLambdaExample {
    public static void main(String[] args) {
        List<String> names = Arrays.asList("Alice", "Bob", "Charlie", "Dave");

        names.stream()
             .filter(s -> s.length() > 4)        // Лямбда-фільтр
             .map(s -> s.toUpperCase())     // Лямбда-трансформація
             .forEach(s -> System.out.println(s)); // Лямбда-дія

        // Виведе:
        // ALICE
        // CHARLIE
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички використання лямбд у найпоширенішому сценарії — роботі зі Stream API.*

* * *

### **7 ❓ Питання:**

Як реалізована робота з ключовим словом `this` у лямбда-виразі?

> #### **💬 Відповідь:**
>
> Це одна з ключових відмінностей лямбда-виразів від анонімних класів.
>
> **У лямбда-виразі ключове слово `this` є лексично прозорим.** Це означає, що `this` всередині лямбди посилається на **екземпляр зовнішнього (enclosing) класу**, в якому ця лямбда була визначена.
>
> Лямбда не створює власної області видимості (`scope`) для `this`. Це робить код більш інтуїтивним. Якщо ви всередині лямбди пишете `this.someMethod()`, ви звертаєтеся до методу того класу, в якому зараз перебуваєте.
>
> Для порівняння, в анонімному класі `this` посилається на екземпляр самого анонімного класу, і для доступу до зовнішнього класу довелося б писати `OuterClassName.this`.

#### **📌 Що перевіряють:**

*Розуміння нюансів поведінки лямбд. Питання про `this` допомагає перевірити глибину знань.*

* * *

### **8 ❓ Питання:**

Чи може лямбда-вираз кидати перевірені винятки (checked exceptions)? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від **сигнатури методу функціонального інтерфейсу**, який лямбда реалізує.
>
> * Якщо абстрактний метод в інтерфейсі **не оголошує** `throws SomeException`, то лямбда-вираз, що його реалізує, також **не може** кидати перевірений виняток. Це найпоширеніший випадок (наприклад, для стандартних `Predicate`, `Function`).
>
> * Якщо ж метод в інтерфейсі **оголошує** виняток (`throws IOException`), то лямбда може його кидати.
>
> **Як вирішити проблему, якщо потрібно кинути виняток?**
>
> 1. **Обгортання в `try-catch`:** Найпростіший спосіб. Всередині лямбди ви перехоплюєте перевірений виняток і або обробляєте його, або загортаєте в неперевірений (`RuntimeException`).
> 2. **Створення власного функціонального інтерфейсу:** Можна створити інтерфейс, метод якого оголошує `throws`.
>
> Стандартні функціональні інтерфейси були розроблені для роботи в `Stream API`, де кидання перевірених винятків ускладнило б API, тому вони їх не підтримують.

#### **📌 Що перевіряють:**

*Знання правил обробки винятків у контексті лямбд. Це поширена практична проблема.*

* * *

### **9 ❓ Питання:**

Що таке "посилання на метод" (Method Reference) і як вони пов'язані з лямбда-виразами? ⭐️

> #### **💬 Відповідь:**
>
> **Посилання на метод** — це ще більш компактний синтаксис для лямбда-виразів, який можна використовувати, якщо тіло лямбди просто викликає один-єдиний існуючий метод.
>
> Вони дозволяють посилатися на метод за його іменем, не реалізуючи його.
>
> **Чотири типи посилань на методи:**
>
> 1. **На статичний метод:** `ClassName::staticMethodName`
>     * `s -> Integer.parseInt(s)` еквівалентно `Integer::parseInt`
>
> 2. **На метод екземпляра конкретного об'єкта:** `instanceRef::instanceMethodName`
>     * `s -> System.out.println(s)` еквівалентно `System.out::println`
>
> 3. **На метод екземпляра довільного об'єкта певного типу:** `ClassName::instanceMethodName`
>     * `s -> s.toUpperCase()` еквівалентно `String::toUpperCase`
>
> 4. **На конструктор:** `ClassName::new`
>     * `() -> new ArrayList<>()` еквівалентно `ArrayList::new`
>
> Використання посилань на методи робить код ще більш декларативним та читабельним, оскільки ви фокусуєтеся на назві операції, а не на її реалізації.

#### **📌 Що перевіряють:**

*Знання сучасного синтаксису Java 8. Посилання на методи є ідіоматичним способом написання функціонального коду.*

* * *

### **10 ❓ Питання:**

Чому кажуть, що лямбда-вираз є "безстановим" (stateless)? ⭐️

> #### **💬 Відповідь:**
>
> Кажуть, що лямбда-вираз є безстановим, тому що він **не має власних полів для зберігання стану**. Він не є об'єктом у класичному розумінні, який може мати поля-екземпляри (`instance variables`).
>
> Все, що є у лямбди — це:
>
> * Список її параметрів.
> * Тіло її коду.
> * Захоплені (copied) значення локальних змінних.
>
> Лямбда не може змінювати свій внутрішній стан між викликами, тому її результат залежить виключно від переданих аргументів. Це робить її ідеальною для функціонального програмування та паралельної обробки в `Stream API`, оскільки відсутність стану усуває ризик виникнення проблем синхронізації (race conditions).
>
> Для порівняння, **анонімний клас** може мати власні поля-екземпляри, тобто він **може мати стан**.

#### **📌 Що перевіряють:**

*Глибоке розуміння концептуальних відмінностей лямбд від об'єктів та їх переваг для функціонального та паралельного програмування.*

* * *

### **11 ❓ Питання:**

Чи може лямбда-вираз бути рекурсивним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може**, але з одним важливим нюансом. Лямбда-вираз не має імені, тому він не може напряму викликати сам себе.
>
> Щоб зробити лямбду рекурсивною, її потрібно **присвоїти змінній** (полю класу), яка буде видима всередині самої лямбди.
>
> **Проблема з локальними змінними:**
> Якщо ви спробуєте присвоїти лямбду локальній змінній і одразу ж викликати цю змінну всередині, ви отримаєте помилку компіляції "variable might not have been initialized". Це тому, що змінна має бути ініціалізована до того, як її можна буде використати.
>
> **Рішення:**
> Оголосити змінну як **поле класу**. Тоді вона буде гарантовано ініціалізована до виклику.

#### **📝 Приклад (рекурсивний факторіал):**

```java
class LambdaRecursion {
    // Оголошуємо лямбду як поле класу
    static Function<Integer, Integer> factorial;

    public static void main(String[] args) {
        // Ініціалізуємо її
        factorial = n -> (n == 0) ? 1 : n * factorial.apply(n - 1);
        
        System.out.println(factorial.apply(5)); // 120
    }
}
```

Хоча це можливо, зазвичай це не найчитабельніший спосіб реалізації рекурсії. Звичайний метод для цього підходить краще.

#### **📌 Що перевіряють:**

*Знання тонкощів роботи лямбд та правил ініціалізації змінних в Java. Це питання з підступом для перевірки глибоких знань.*

* * *

### **12 ❓ Питання:**

Які `default`-методи з'явилися у функціональних інтерфейсах, як-от `Predicate`, і що вони дозволяють робити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У стандартні функціональні інтерфейси було додано `default`-методи для того, щоб можна було **комбінувати** їх між собою, створюючи складнішу логіку.
>
> **На прикладі `Predicate<T>`:**
>
> * **`and(Predicate<? super T> other)`**:
>   * Створює новий предикат, який буде `true` тільки тоді, коли **і поточний, і `other`** предикати є `true` (логічне І).
>
> * **`or(Predicate<? super T> other)`**:
>   * Створює новий предикат, який буде `true`, якщо **або поточний, або `other`** предикат є `true` (логічне АБО).
>
> * **`negate()`**:
>   * Створює новий предикат, який **інвертує** результат поточного предиката (логічне НЕ).
>
> Ці методи дозволяють будувати складні ланцюжки фільтрів у декларативному, функціональному стилі без написання громіздких `if`-блоків.

#### **📝 Приклад:**

```java
Predicate<String> startsWithA = s -> s.startsWith("A");
Predicate<String> hasLength5 = s -> s.length() == 5;

// Комбінуємо: рядок починається з 'A' І має довжину 5
Predicate<String> combined = startsWithA.and(hasLength5);

System.out.println(combined.test("Apple")); // true
System.out.println(combined.test("Ant"));   // false
```

#### **📌 Що перевіряють:**

*Знання сучасного функціонального API та здатність писати декларативний код замість імперативного. Це є важливою частиною функціонального програмування в Java.*

* * *

### **13 ❓ Питання:**

Як реалізовано лямбда-вирази "під капотом" JVM і чи створюють вони анонімні класи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це поширена помилкова думка. **Ні, лямбда-вирази НЕ створюють анонімні класи** під капотом. Вони реалізовані набагато ефективніше.
>
> **Механізм `invokedynamic`:**
>
> 1. **Компіляція:** Компілятор `javac` не генерує окремий `.class` файл для кожної лямбди. Замість цього він перетворює тіло лямбди в **приватний статичний метод** всередині того ж класу, де лямбда була визначена.
>
> 2. **Виконання:** У місці виклику лямбди в байт-коді розміщується спеціальна інструкція — **`invokedynamic`**. Ця інструкція є "рецептом" для JVM, як створити об'єкт, що реалізує функціональний інтерфейс.
>
> 3. **Lambda Factory:** При першому виконанні `invokedynamic`, JVM динамічно генерує об'єкт-реалізацію, який посилається на згенерований статичний метод. Цей процес називається "створенням лямбди на фабриці". При наступних викликах цей об'єкт може бути перевикористаний.
>
> **Переваги:**
>
> * **Ефективність:** Немає накладних витрат на завантаження тисяч `.class` файлів для анонімних класів.
> * **Гнучкість:** Дозволяє JVM у майбутньому застосовувати нові стратегії оптимізації для лямбд, не змінюючи вже скомпільований байт-код.

#### **📌 Що перевіряють:**

*Експертні знання про внутрішню роботу JVM. Це питання показує, що кандидат цікавиться не тільки тим, "як писати код", а й тим, "як він працює".*

* * *

### **14 ❓ Питання:**

Яка різниця між захопленням змінної (capturing lambda) і незахопленням (non-capturing lambda)? Як це впливає на продуктивність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це нюанс, пов'язаний з оптимізацією лямбда-виразів.
>
> * **Non-capturing lambda (незахоплююча лямбда):**
>   * Це лямбда, яка **не використовує жодних локальних змінних** із зовнішньої області видимості. Вона працює лише зі своїми параметрами.
>   * **Приклад:** `(x, y) -> x + y`
>   * **Оптимізація:** JVM може створити **єдиний, спільний екземпляр** для такої лямбди і перевикористовувати його при кожному виклику, оскільки вона не має стану. Це дуже ефективно.
>
> * **Capturing lambda (захоплююча лямбда):**
>   * Це лямбда, яка **використовує (захоплює) значення** локальної змінної з зовнішнього методу.
>   * **Приклад:** `x -> x + someLocalVariable`
>   * **Вплив на продуктивність:** Оскільки стан такої лямбди залежить від захопленої змінної, JVM може бути змушена створювати **новий екземпляр** кожного разу, коли викликається метод (хоча сучасні JVM також намагаються оптимізувати і це). Загалом, захоплюючі лямбди можуть мати трохи більші накладні витрати на створення, ніж незахоплюючі.
>
> По можливості, слід віддавати перевагу `non-capturing` лямбдам, передаючи всі необхідні дані через параметри.

#### **📌 Що перевіряють:**

*Глибокі знання про оптимізацію в JVM та про те, як невеликі зміни в коді можуть впливати на його продуктивність.*

* * *

### **15 ❓ Питання:**

Поясніть концепцію "виведення типу" (Type Inference) для лямбда-виразів. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Виведення типу (Type Inference)** — це процес, за допомогою якого компілятор Java **автоматично визначає** типи даних для лямбда-виразів, без необхідності їх явного оголошення розробником.
>
> **Як це працює:**
>
> Лямбда-вираз сам по собі не має типу. Його тип визначається **з контексту**, а саме з **цільового типу (target type)**, якому його намагаються присвоїти. Цільовим типом має бути функціональний інтерфейс.
>
> 1. Компілятор дивиться на цільовий тип (напр., `Predicate<String>`).
> 2. Він знаходить єдиний абстрактний метод в цьому інтерфейсі (`boolean test(String s)`).
> 3. З цього методу він дізнається:
>     * Типи параметрів (тут один, `String`).
>     * Тип, що повертається (тут `boolean`).
> 4. Тепер компілятор може перевірити, чи сумісна ваша лямбда з цією сигнатурою.
>
> **Приклад:**
>
> ```java
> Predicate<String> p = s -> s.isEmpty();
> ```
>
> Тут компілятор бачить, що `p` має тип `Predicate<String>`, отже, його метод — `test(String s)`. Тому компілятор "розуміє", що `s` у лямбді має бути типу `String`, а вираз `s.isEmpty()` повертає `boolean`, що відповідає вимогам. Вам не потрібно писати `(String s) -> ...`.
>
> Цей механізм робить лямбди набагато коротшими та зручнішими.

#### **📌 Що перевіряють:**

*Розуміння того, як працює компілятор Java з сучасними мовними конструкціями. Виведення типу є ключовою концепцією, що лежить в основі лаконічності лямбд.*

* * *
