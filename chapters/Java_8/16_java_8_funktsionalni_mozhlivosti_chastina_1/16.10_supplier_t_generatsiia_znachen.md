---
layout: default
title: "Supplier<T> - генерація значень"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 10
---

# Supplier<T> - генерація значень

* короткий опис: `Supplier<T>` – функціональний інтерфейс, що не приймає жодних параметрів, але повертає значення типу `T`. Основний метод – `T get()`.

* поняття: `get()`, лінива ініціалізація, фабричні методи.

* приклади: `() -> Math.random()` (`get`), `() -> new ArrayList<>()` (лінива ініціалізація об'єкта).

* ключові моменти: Використовується для лінивих обчислень (значення генерується лише при виклику `get()`) та фабричних методів.

* що важливо знати на співбесіді: Коли використовувати, переваги лінивої ініціалізації.

### **✅ Офіційне визначення:**

**`Supplier<T>`** — це функціональний інтерфейс з пакету **`java.util.function`**, який не приймає жодних параметрів, але повертає значення типу **`T`**. Основний метод — **`T get()`**.

### **🧠 Простими словами:**

**`Supplier`** — це “постачальник” або “фабрика”, яка дає тобі значення, коли ти попросиш, без жодних входів

---

## 📊 **Перелік основних методів:**

|     Метод     |                   Опис                    |
|:-------------:|:-----------------------------------------:|
| **`T get()`** | Повертає значення або об’єкт типу **`T`** |

---

## **Пояснення на пальцях (💡)**

* **`get()`** — це ніби кнопка “дай мені щось”. Ти нічого не передаєш, але отримуєш результат.
* Ідеально підходить для **лінивої ініціалізації** — створити об’єкт чи значення тільки тоді, коли воно реально потрібно.
* Використовується для **фабричних методів** — генерація нових об’єктів на замовлення.

---

## **Корисні приклади (🧪)**

```java
Supplier<Double> randomSupplier = () -> Math.random();

System.out.println(randomSupplier.get()); // виведе випадкове число

Supplier<String> helloSupplier = () -> "Hello, world!";
System.out.println(helloSupplier.get()); // Hello, world!

// Лінива ініціалізація об'єкта
Supplier<List<String>> listSupplier = () -> new ArrayList<>();
List<String> myList = listSupplier.get();
myList.add("test");
System.out.println(myList); // [test]
```

---

## **Порівняння (⚖️)**

|     Характеристика     |         Supplier\<T\>          |      Function\<T, R\>       |
|:----------------------:|:------------------------------:|:---------------------------:|
|     **Параметри**      |             Немає              |      Є (один параметр)      |
| **Повертає результат** |              Так               |             Так             |
|    **Призначення**     | Генерація / постачання значень |    Перетворення значень     |
|    **Використання**    | Лінива ініціалізація, фабрики  | Функціональна трансформація |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **get** — “дай мені щось”
* Supplier — “постачальник” без вхідних даних, але з вихідним значенням

---

## **🟩 Підсумок:**

* **`Supplier<T>`** — інтерфейс для генерації значень без вхідних параметрів.
* Основний метод — **`get()`**, що повертає значення типу **`T`**.
* Дуже корисний для лінивої ініціалізації та фабричних методів.
* Розуміння Supplier допомагає писати гнучкий та ефективний код із відкладеним створенням об’єктів.

---

### **🔗 Додаткові джерела:**

* [**Java Docs — Supplier**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/function/Supplier.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке функціональний інтерфейс `Supplier<T>` і яка його основна мета?

> #### **💬 Відповідь:**
>
> **`Supplier<T>`** — це один з ключових стандартних функціональних інтерфейсів з пакету `java.util.function`.
>
> **Його основна мета** — представити операцію, яка **не приймає жодних аргументів**, але **повертає (постачає) значення** певного типу (`T`).
>
> Він має лише один абстрактний метод: **`T get()`**.
>
> `Supplier<T>` є фундаментальним інструментом для:
>
> * **Лінивої ініціалізації (Lazy Initialization):** Об'єкт або значення створюється тільки в момент виклику методу `get()`.
> * **Реалізації патерну "Фабрика" (Factory Pattern):** Використовується для генерації нових об'єктів.
> * **Надання значень за замовчуванням** (наприклад, в `Optional.orElseGet()`).

#### **📝 Аналогія:**

`Supplier` — це як торговий автомат з напоями. Ви не даєте йому нічого (не кидаєте монети в цій аналогії), а просто натискаєте кнопку `get()` — і він "постачає" вам напій (значення).

#### **📌 Що перевіряють:**

*Базове визначення `Supplier` та розуміння його ключової характеристики — постачання значення без отримання вхідних даних.*

* * *

### **2 ❓ Питання:**

Який основний метод в інтерфейсі `Supplier` і як його використовувати? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Основний і єдиний абстрактний метод в `Supplier` — це **`T get()`**.
>
> **Як його використовувати:**
> Метод `get()` не приймає жодних аргументів і при виклику виконує логіку, визначену в лямбда-виразі або посиланні на метод, повертаючи результат типу `T`.
>
> Він є "тригером", що запускає генерацію або отримання значення.

#### **📝 Приклад:**

```java
import java.util.function.Supplier;
import java.time.LocalDateTime;

public class SupplierExample {
    public static void main(String[] args) {
        // Створюємо Supplier, який повертає поточний час
        Supplier<LocalDateTime> currentTimeSupplier = () -> LocalDateTime.now();
        
        // Викликаємо метод get(), щоб отримати значення
        LocalDateTime time1 = currentTimeSupplier.get();
        System.out.println("Час 1: " + time1);

        // Зачекаємо трохи
        try { Thread.sleep(1000); } catch (InterruptedException e) {}

        // Викликаємо get() ще раз - отримаємо нове значення
        LocalDateTime time2 = currentTimeSupplier.get();
        System.out.println("Час 2: " + time2);
    }
}
```

#### **📌 Що перевіряють:**

*Знання ключового методу інтерфейсу та практичні навички його застосування для генерації значення.*

* * *

### **3 ❓ Питання:**

У чому основна відмінність між `Supplier<T>` та `Consumer<T>`?

> #### **💬 Відповідь:**
>
> Вони є протилежностями один одному за своєю суттю.
>
> * **`Supplier<T>` (Постачальник):**
>   * **Призначення:** **Постачати** значення.
>   * **Метод:** `T get()`
>   * **Параметри:** **Не приймає** аргументів.
>   * **Результат:** **Повертає** значення типу `T`.
>
> * **`Consumer<T>` (Споживач):**
>   * **Призначення:** **Споживати** (обробляти) значення.
>   * **Метод:** `void accept(T t)`
>   * **Параметри:** **Приймає** один аргумент типу `T`.
>   * **Результат:** **Не повертає нічого** (`void`).
>
> **Коротко:** `Supplier` — це джерело даних. `Consumer` — це приймач даних.

#### **📌 Що перевіряють:**

*Розуміння семантичної різниці між основними функціональними інтерфейсами.*

* * *

### **4 ❓ Питання:**

Як `Supplier` використовується для реалізації лінивої ініціалізації (lazy initialization)?

> #### **💬 Відповідь:**
>
> **Лінива ініціалізація** — це патерн, при якому створення "дорогого" або ресурсномісткого об'єкта відкладається до моменту, коли він реально знадобиться.
>
> `Supplier` ідеально підходить для цього:
>
> 1. Логіка створення "дорогого" об'єкта загортається в лямбда-вираз, який присвоюється `Supplier`'у. У цей момент **об'єкт ще не створюється**.
> 2. Створення об'єкта відбудеться лише тоді, коли буде викликано метод **`supplier.get()`**.
>
> Це дозволяє уникнути непотрібних витрат ресурсів, якщо об'єкт може ніколи й не знадобитися.

#### **📝 Приклад:**

```java
class ExpensiveResource {
    public ExpensiveResource() {
        System.out.println("Створюємо дуже дорогий ресурс...");
        // Симуляція довгої ініціалізації
        try { Thread.sleep(2000); } catch (InterruptedException e) {}
    }
}

public class LazyInitDemo {
    public static void main(String[] args) {
        // Логіка створення ресурсу загорнута в Supplier
        Supplier<ExpensiveResource> resourceSupplier = () -> new ExpensiveResource();

        System.out.println("Supplier створено, але ресурс ще ні.");

        // ... якась інша логіка ...
        
        // Ресурс буде створено ТІЛЬКИ в момент виклику .get()
        ExpensiveResource resource = resourceSupplier.get(); 
        System.out.println("Ресурс нарешті створено.");
    }
}
```

#### **📌 Що перевіряють:**

*Розуміння одного з найважливіших практичних застосувань `Supplier` та концепції лінивих обчислень.*

* * *

### **5 ❓ Питання:**

Наведіть приклад використання `Supplier` з посиланням на конструктор.

> #### **💬 Відповідь:**
>
> `Supplier` дуже часто використовується разом із посиланнями на конструктори (`ClassName::new`) для реалізації патерну "Фабрика".
>
> Якщо вам потрібен об'єкт, який буде створювати нові екземпляри певного класу за запитом, `Supplier` є ідеальним кандидатом.
>
> Посилання на конструктор без аргументів (`ArrayList::new`) має сигнатуру, що ідеально відповідає методу `get()` інтерфейсу `Supplier<T>`: він не приймає аргументів і повертає об'єкт.

#### **📝 Приклад:**

```java
import java.util.ArrayList;
import java.util.List;
import java.util.function.Supplier;

public class FactoryExample {
    
    public static <T> T createInstance(Supplier<T> factory) {
        return factory.get();
    }
    
    public static void main(String[] args) {
        // Передаємо посилання на конструктор ArrayList як фабрику
        List<String> newList = createInstance(ArrayList::new);

        System.out.println(newList.getClass().getName()); // java.util.ArrayList
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички поєднання `Supplier` з посиланнями на методи для написання чистого, функціонального коду.*

* * *

### **6 ❓ Питання:**

Чи може `Supplier` повертати `null`? Чи є це хорошою практикою?

> #### **💬 Відповідь:**
>
> **Так, технічно `Supplier` може повертати `null`**. Інтерфейс ніяк цього не забороняє.
>
> ```java
> Supplier<String> nullSupplier = () -> null;
> String result = nullSupplier.get(); // result буде null
> ```
>
> **Чи є це хорошою практикою?**
> **Ні, це вважається поганою практикою**, оскільки створює ризик `NullPointerException` для клієнтського коду, який може не очікувати на `null`.
>
> **Краща альтернатива:**
>
> Якщо ваш `Supplier` може не повернути значення, краще зробити так, щоб він повертав **`Optional<T>`**. Це явно вказує на те, що результат може бути відсутнім, і змушує клієнтський код обробляти цей випадок безпечно.
>
> * **Погано:** `Supplier<User> userSupplier = () -> findUserInDb(id); // може повернути null`
> * **Добре:** `Supplier<Optional<User>> userSupplier = () -> Optional.ofNullable(findUserInDb(id));`

#### **📌 Що перевіряють:**

*Розуміння принципів написання надійного коду, уникнення `NPE` та правильного використання `Optional`.*

* * *

### **7 ❓ Питання:**

Чим відрізняються `Optional.orElse()` та `Optional.orElseGet()`, і яка роль `Supplier` у `orElseGet()`? ⭐️

> #### **💬 Відповідь:**
>
> Це класичний приклад, що демонструє переваги лінивих обчислень за допомогою `Supplier`.
>
> * **`orElse(T other)`:**
>   * Приймає **готове значення** типу `T`.
>   * Це значення **завжди** буде обчислено або створено, **незалежно від того, порожній `Optional` чи ні**. Це називається **"гарячим" (eager) обчисленням**.
>
> * **`orElseGet(Supplier<? extends T> other)`:**
>   * Приймає **`Supplier<T>`**, тобто логіку для створення значення.
>   * Код всередині `Supplier`'а буде виконано, і значення буде створено **лише тоді, коли `Optional` є порожнім**. Це називається **"лінивим" (lazy) обчисленням**.
>
> **Роль `Supplier`:** Він дозволяє **відкласти** виконання потенційно "дорогої" операції до моменту, коли вона дійсно потрібна.
>
> **Висновок:** Якщо значення за замовчуванням — це проста константа, використовуйте `orElse`. Якщо його створення — це дорога операція (напр., виклик методу, створення об'єкта), **завжди використовуйте `orElseGet`**, щоб уникнути непотрібних витрат продуктивності.

#### **📝 Приклад:**

```java
// Уявімо, що getDefaultValue() - дорога операція
String value = Optional.of("Hello")
                       .orElse(getDefaultValue()); // getDefaultValue() БУДЕ викликано

String valueLazy = Optional.of("Hello")
                           .orElseGet(() -> getDefaultValue()); // getDefaultValue() НЕ буде викликано
```

#### **📌 Що перевіряють:**

*Розуміння ключової різниці між "гарячим" та "лінивим" обчисленням. Це дуже важливе питання, яке демонструє, чи думає кандидат про продуктивність.*

* * *

### **8 ❓ Питання:**

Що таке примітивні спеціалізації `Supplier`? ⭐️

> #### **💬 Відповідь:**
>
> Це версії інтерфейсу `Supplier`, оптимізовані для повернення примітивних типів, що дозволяє уникнути **boxing** (автопакування).
>
> **Основні примітивні спеціалізації:**
>
> * **`IntSupplier`**:
>   * Метод: `int getAsInt()`
>   * Повертає `int`, а не `Integer`.
>
> * **`LongSupplier`**:
>   * Метод: `long getAsLong()`
>   * Повертає `long`, а не `Long`.
>
> * **`DoubleSupplier`**:
>   * Метод: `double getAsDouble()`
>   * Повертає `double`, а не `Double`.
>
> * **`BooleanSupplier`**:
>   * Метод: `boolean getAsBoolean()`
>   * Повертає `boolean`, а не `Boolean`.
>
> Використання цих спеціалізацій є кращим для продуктивності, коли ви працюєте з примітивними значеннями.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки та аспектів продуктивності, пов'язаних з примітивними типами.*

* * *

### **9 ❓ Питання:**

Чи може `Supplier` використовуватися в багатопотоковому середовищі? Які можуть виникнути проблеми? ⭐️

> #### **💬 Відповідь:**
>
> Так, може, але з цим пов'язані певні ризики, якщо `Supplier` не спроектований правильно.
>
> **Проблеми можуть виникнути, якщо:**
>
> 1. **`Supplier` не є потокобезпечним (thread-safe):**
>     * Якщо `Supplier` у своїй логіці `get()` змінює якийсь спільний стан (що є поганою практикою для `Supplier`), це може призвести до "умови гонитви" (race conditions).
>
> 2. **Проблема подвійного створення ресурсу:**
>     * Уявіть, що ви використовуєте `Supplier` для лінивої ініціалізації синглтона. Якщо два потоки одночасно викличуть `get()`, коли об'єкт ще не створено, вони обидва можуть пройти перевірку `if (instance == null)` і **створити два екземпляри** замість одного.
>
> **Як вирішити:**
>
> * Зробити реалізацію `Supplier`'а потокобезпечною. Для лінивої ініціалізації синглтона використовувати патерн **Double-Checked Locking** або `synchronized`-блок.
> * Найпростіший спосіб — доручити це стороннім бібліотекам. Наприклад, `Suppliers.memoize()` з бібліотеки Google Guava створює потокобезпечний `Supplier`, який гарантує, що його логіка буде виконана лише один раз.

#### **📌 Що перевіряють:**

*Розуміння проблем, що виникають при використанні функціональних інтерфейсів у багатопотоковому середовищі. Це демонструє знання concurrency.*

* * *

### **10 ❓ Питання:**

Як `Supplier` використовується в `Stream.generate()`? ⭐️

> #### **💬 Відповідь:**
>
> Статичний метод `Stream.generate(Supplier<T> s)` створює **нескінченний** послідовний потік, генеруючи його елементи за допомогою наданого `Supplier`.
>
> **Як це працює:**
>
> При кожному запиті нового елемента з потоку, викликається метод `get()` у `Supplier`'a. Цей процес може продовжуватися нескінченно.
>
> Оскільки потік нескінченний, його **обов'язково** потрібно обмежувати за допомогою проміжної операції, як-от `limit()`.
>
> `Supplier` ідеально підходить для цього, оскільки він постачає нові значення "на льоту" без будь-яких вхідних даних.

#### **📝 Приклад (генерація потоку випадкових чисел):**

```java
import java.util.function.Supplier;
import java.util.stream.Stream;

public class GenerateExample {
    public static void main(String[] args) {
        // Supplier, що генерує випадкові числа
        Supplier<Double> randomSupplier = Math::random;

        // Створюємо нескінченний потік, обмежуємо його 5 елементами, і виводимо їх
        Stream.generate(randomSupplier)
              .limit(5)
              .forEach(System.out::println);
    }
}
```

#### **📌 Що перевіряють:**

*Практичні знання `Stream API`, зокрема способів створення стрімів. Це поширений сценарій використання `Supplier`.*

* * *

### **11 ❓ Питання:**

Поясніть, що таке `Supplier<Optional<T>>` і коли такий підхід є корисним. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Supplier<Optional<T>>` — це `Supplier`, який повертає не безпосередньо значення `T`, а об'єкт `Optional`, що може містити це значення або бути порожнім.
>
> **Коли це корисно:**
>
> Цей підхід є надзвичайно корисним, коли операція постачання **може не вдатися** або **не знайти результат**. Це є правильною альтернативою поверненню `null` із `Supplier`.
>
> **Приклад:**
> Уявіть, що у вас є ліниве отримання конфігурації з файлу, який може бути відсутнім.
>
> * **Поганий дизайн:** `Supplier<Config>` може повернути `null`, якщо файл не знайдено.
> * **Хороший дизайн:** `Supplier<Optional<Config>>` поверне `Optional.of(config)`, якщо файл знайдено, і `Optional.empty()`, якщо ні.
>
> Клієнтський код, викликаючи `get()` на такому `Supplier`'і, одразу отримує `Optional` і змушений безпечно обробляти випадок відсутності значення, що робить код набагато надійнішим.

#### **📝 Приклад коду:**

```java
Supplier<Optional<String>> configSupplier = () -> {
    // логіка читання з файлу
    if (configExists()) {
        return Optional.of(readConfig());
    } else {
        return Optional.empty();
    }
};

Optional<String> config = configSupplier.get();
String finalConfig = config.orElse("Default Config");
```

#### **📌 Що перевіряють:**

*Здатність комбінувати різні можливості Java 8 (`Supplier`, `Optional`) для написання чистого, надійного та виразного коду.*

* * *

### **12 ❓ Питання:**

Як можна використовувати `Supplier` для реалізації патерну "Прототип" (Prototype)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн "Прототип" використовується для створення нових об'єктів шляхом копіювання існуючого об'єкта-прототипа. `Supplier` може слугувати як фабрика для створення цих копій.
>
> **Як реалізувати:**
>
> 1. Клас, що є прототипом, повинен мати метод `clone()` (або інший метод копіювання).
> 2. Ми можемо створити `Supplier`, який захоплює об'єкт-прототип і у своєму методі `get()` викликає `clone()` на цьому прототипі.
>
> Це дозволяє передавати "фабрику копій" як параметр, приховуючи деталі реалізації клонування.

#### **📝 Приклад:**

```java
class Sheep implements Cloneable {
    String name;
    public Sheep(String name) { this.name = name; }

    @Override
    public Sheep clone() {
        try {
            return (Sheep) super.clone();
        } catch (CloneNotSupportedException e) {
            throw new AssertionError(); // Не може статися
        }
    }
}

public class PrototypeDemo {
    public static void main(String[] args) {
        Sheep originalDolly = new Sheep("Dolly");

        // Створюємо Supplier, який буде клонувати "Доллі"
        Supplier<Sheep> sheepFactory = originalDolly::clone;
        
        // Створюємо дві копії
        Sheep clonedDolly1 = sheepFactory.get();
        Sheep clonedDolly2 = sheepFactory.get();

        System.out.println(clonedDolly1 != originalDolly); // true, це різні об'єкти
    }
}
```

#### **📌 Що перевіряють:**

*Здатність застосовувати функціональні інтерфейси для реалізації класичних патернів проектування. Це показує гнучкість мислення.*

* * *

### **13 ❓ Питання:**

Який `default`-метод було б логічно додати до інтерфейсу `Supplier`, якби його не було, але ми б хотіли його розширити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Стандартний `Supplier` не має жодних `default`-методів. Однак, якщо б ми хотіли його розширити, логічним доповненням міг би бути `default`-метод для **композиції з `Function`**.
>
> Наприклад, метод **`andThen(Function<? super T, ? extends R> after)`**.
>
> **Що б він робив:**
>
> Він би створював новий `Supplier`, який спочатку викликав би `get()` поточного `Supplier`'a, а потім застосовував би до результату передану функцію `after`.
>
> ```java
> // Гіпотетична реалізація
> default <R> Supplier<R> andThen(Function<? super T, ? extends R> after) {
>     return () -> after.apply(this.get());
> }
> ```
>
> **Приклад використання:**
>
> ```java
> Supplier<String> idSupplier = () -> "123-abc";
> Function<String, Integer> lengthFunction = String::length;
>
> // Створюємо новий Supplier, що одразу повертає довжину
> Supplier<Integer> lengthSupplier = idSupplier.andThen(lengthFunction);
>
> System.out.println(lengthSupplier.get()); // 6
> ```
>
> Це дозволило б будувати ланцюжки "постачання та перетворення" у більш елегантному стилі. Хоча цього немає в стандартному JDK, ця ідея лежить в основі багатьох функціональних бібліотек.

#### **📌 Що перевіряють:**

*Здатність до проектування API. Кандидат повинен розуміти принципи композиції і думати про те, як можна було б покращити існуючі інтерфейси.*

* * *

### **14 ❓ Питання:**

Як `Supplier` пов'язаний з методом `CompletableFuture.supplyAsync()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Статичний метод **`CompletableFuture.supplyAsync(Supplier<U> supplier)`** є одним з основних способів створення асинхронної задачі, яка **повертає результат**.
>
> **Роль `Supplier` тут:**
>
> * `Supplier` містить **блок коду**, який потрібно виконати асинхронно.
> * Цей блок коду буде виконано в **окремому потоці** (зазвичай з `ForkJoinPool`).
> * Значення, яке поверне метод `get()` `Supplier`'a, стане **результатом** `CompletableFuture`, коли він успішно завершиться.
>
> `Supplier` ідеально підходить для цього завдання, тому що асинхронна операція зазвичай не потребує вхідних параметрів, а її мета — "поставити" результат, коли він буде готовий.

#### **📝 Приклад:**

```java
// Supplier, що симулює довготривале обчислення
Supplier<String> longRunningTask = () -> {
    try { Thread.sleep(2000); } catch (InterruptedException e) {}
    return "Task result";
};

// Запускаємо завдання асинхронно
CompletableFuture<String> future = CompletableFuture.supplyAsync(longRunningTask);

// ... в цей час основний потік може робити іншу роботу ...

// Отримуємо результат, коли він буде готовий
String result = future.join(); 
System.out.println(result); // "Task result"
```

#### **📌 Що перевіряють:**

*Знання асинхронного програмування в Java та API `CompletableFuture`. Розуміння ролі `Supplier` у цьому контексті є дуже важливим.*

* * *

### **15 ❓ Питання:**

Створіть потокобезпечний `Supplier`, який гарантує, що його "дорога" операція буде виконана лише один раз (патерн `memoization`). ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання на реалізацію лінивої ініціалізації синглтона у потокобезпечному стилі. Найкращим підходом є використання патерну **Double-Checked Locking** з `volatile` змінною.
>
> **Структура:**
>
> 1. Оригінальний `Supplier` (`delegate`), що містить "дорогу" логіку.
> 2. `volatile` змінна `value` для зберігання кешованого результату. `volatile` гарантує видимість змін між потоками.
> 3. Синхронізований блок для ініціалізації, щоб уникнути "умови гонитви".
>
> **Метод `get()` буде працювати так:**
>
> 1. Спочатку він перевіряє, чи ініціалізовано значення, без блокування.
> 2. Якщо ні, він входить у `synchronized` блок.
> 3. Всередині блоку він **ще раз** перевіряє, чи не ініціалізував значення інший потік, поки цей чекав на блокуванні (це і є Double-Checked Locking).
> 4. Якщо значення все ще `null`, він викликає `delegate.get()`, зберігає результат і повертає його.

#### **📝 Приклад реалізації:**

```java
public class MemoizedSupplier<T> implements Supplier<T> {
    private final Supplier<T> delegate;
    private volatile T value;
    private final Object lock = new Object();

    public MemoizedSupplier(Supplier<T> delegate) {
        this.delegate = delegate;
    }

    @Override
    public T get() {
        if (value == null) { // Перша перевірка (без блокування)
            synchronized (lock) {
                if (value == null) { // Друга перевірка (всередині блокування)
                    value = delegate.get();
                }
            }
        }
        return value;
    }
}
```

Така реалізація є дуже ефективною, оскільки блокування відбувається лише один раз — при першому виклику. Всі наступні виклики `get()` миттєво повертатимуть кешоване значення.

#### **📌 Що перевіряють:**

*Глибокі знання concurrency в Java. Здатність реалізувати складні патерни, як-от `Double-Checked Locking`, є ознакою розробника рівня Senior.*

* * *
