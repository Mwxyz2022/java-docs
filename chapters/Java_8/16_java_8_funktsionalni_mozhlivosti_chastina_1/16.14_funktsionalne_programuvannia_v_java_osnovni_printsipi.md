---
layout: default
title: "Функціональне програмування в Java - основні принципи"
parent: "Функціональні можливості"
grand_parent: "Java 8"
nav_order: 14
---

# Функціональне програмування в Java - основні принципи

* короткий опис: Функціональне програмування (ФП) – це парадигма, яка трактує обчислення як оцінку математичних функцій та уникає зміни стану і мутабельних даних. Java підтримує ФП з Java 8.

* поняття: `Immutability` (незмінність), `Pure Functions` (чисті функції), `Side Effects` (побічні ефекти, їх уникнення), `Higher-Order Functions` (функції вищого порядку), `Function Composition` (композиція функцій), `Lazy Evaluation` (лінива оцінка).

* приклади: `list.stream().map(x -> x * 2)` (чиста функція, незмінність), `Function.andThen()` (композиція).

* ключові моменти: Чистота функцій, незмінність даних та уникнення побічних ефектів роблять код надійнішим, тестованішим та простішим для паралелізації.

* що важливо знати на співбесіді: Основні принципи, як Java підтримує ФП.

### **✅ Офіційне визначення:**

Функціональне програмування (ФП) — це парадигма програмування, яка трактує обчислення як оцінку математичних функцій та уникає зміни стану і мутабельних даних.

### **🧠 Простими словами:**

ФП — це стиль написання коду, де ти більше довіряєш функціям, які не змінюють нічого довкола, не мають побічних ефектів, і часто передаєш їх як параметри іншим функціям. У Java це стало можливим починаючи з Java 8\.

---

## 📊 **Ключові принципи ФП у Java:**

|          Поняття           |                                 Пояснення                                 |
|:--------------------------:|:-------------------------------------------------------------------------:|
|      **Immutability**      |                    Дані не змінюються після створення                     |
|     **Pure Function**      | Функція, яка завжди дає однаковий результат для одних і тих же аргументів |
|      **Side Effects**      |                   Зміна зовнішнього стану (погано в ФП)                   |
| **Higher-order functions** |            Функції, які приймають або повертають інші функції             |
|  **Function composition**  |     Об’єднання функцій у ланцюг (через **`andThen()`, `compose()`**)      |
|    **Lazy evaluation**     |   Вираховування тільки тоді, коли потрібно (наприклад, у **`Stream`**)    |

---

## **Пояснення на пальцях (💡)**

* **Immutability** — уяви, що ти не редагуєш документи, а створюєш нові копії з правками.
* **Pure function** — як калькулятор: 2+2 завжди буде 4, незалежно від настрою калькулятора.
* **Side effect** — як якщо б ти натиснув «=» на калькуляторі, і він видалив усі твої файли.
* **Higher-order functions** — як коробка, яка може містити інші функції: гнучко та модульно.

---

## **Корисні приклади (🧪)**

```java
// Pure function
int square(int x) {
    return x * x; // завжди однаковий результат, не змінює нічого
}

// Higher-order function (приймає іншу функцію)
public static <T, R> List<R> map(List<T> list, Function<T, R> func) {
    return list.stream()
               .map(func)
               .collect(Collectors.toList());
}

// Імм'ютабельність
List<String> names = List.of("Alice", "Bob", "Charlie"); // незмінний список
```

---

## **Порівняння (⚖️)**

|   Характеристика   | Імперативний підхід |      Функціональний підхід      |
|:------------------:|:-------------------:|:-------------------------------:|
|  **Зміна стану**   |      Дозволена      |   Заборонена або мінімізована   |
| **Побічні ефекти** |        Часто        |           Уникаються            |
|   **ООП-фокус**    |  Об’єкти та методи  |       Функції як значення       |
| **Читабельність**  |  Може бути важкою   |  Вища при правильному підході   |
|   **Тестування**   |      Складніше      | Простіше через предсказуваність |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"FISH" — Functional Code Loves FISH:**

* **F** — Functions first-class citizens
* **I** — Immutability
* **S** — Side-effect free
* **H** — Higher-order functions

---

## **🟩 Підсумок:**

* Java підтримує функціональне програмування через лямбда-вирази, функціональні інтерфейси та Stream API.
* Основні принципи ФП — це чистота функцій, незмінність даних та уникнення побічних ефектів.
* Функціональний стиль допомагає писати коротший, надійніший та тестований код.
* Знання основ ФП — must-have на сучасних Java-співбесідах.

---

### **🔗 Додаткові джерела:**

* [**Official Oracle Java Functional Programming Guide**](https://docs.oracle.com/javase/tutorial/java/javaOO/lambdaexpressions.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке функціональне програмування (ФП) і чим воно відрізняється від об'єктно-орієнтованого (ООП)?

> #### **💬 Відповідь:**
>
> **Функціональне програмування (ФП)** — це парадигма програмування, яка розглядає обчислення як оцінку математичних функцій і намагається уникати зміни стану та мутабельних даних.
>
> Основна відмінність від ООП полягає в тому, **навколо чого будується програма**:
>
> * **ООП (Об'єктно-орієнтоване програмування):**
>   * **Фокус:** На **об'єктах**, які об'єднують в собі **дані (стан)** та **поведінку (методи)**. Головна ідея — інкапсуляція стану та його зміна за допомогою методів.
>   * **Ключові поняття:** Клас, об'єкт, інкапсуляція, наслідування, поліморфізм.
>
> * **ФП (Функціональне програмування):**
>   * **Фокус:** На **функціях** як на громадянах першого класу. Дані та поведінка розділені. Головна ідея — перетворення даних за допомогою функцій без зміни початкового стану (імутабельність).
>   * **Ключові поняття:** Чисті функції, імутабельність, відсутність побічних ефектів, функції вищого порядку.
>
> **Коротко:** ООП — це про моделювання речей (об'єктів). ФП — це про моделювання процесів (перетворення даних). Сучасна Java дозволяє поєднувати найкраще з обох світів.

#### **📝 Аналогія:**

* **ООП:** Ви будуєте автомобіль (об'єкт). У нього є стан (колір, кількість пального) і методи (`їхати()`, `заправити()`). Метод `заправити()` **змінює** стан автомобіля.
* **ФП:** У вас є набір креслень (дані). Ви застосовуєте до них функцію "пофарбувати в червоний" і отримуєте **нові** креслення пофарбованого автомобіля. Початкові креслення залишаються незмінними.

#### **📌 Що перевіряють:**

*Розуміння базових відмінностей між двома основними парадигмами програмування. Кандидат повинен знати, що фокус зміщується з об'єктів та їх стану на функції та потік даних.*

* * *

### **2 ❓ Питання:**

Що таке "чиста функція" (Pure Function)?

> #### **💬 Відповідь:**
>
> **Чиста функція** — це функція, яка відповідає двом суворим правилам:
>
> 1. **Детермінізм:** Для однакових вхідних даних вона **завжди** повертає однаковий результат. Її результат залежить виключно від її аргументів.
> 2. **Відсутність побічних ефектів (No Side Effects):** Вона не змінює ніякого стану за межами своєї області видимості. Тобто, вона не модифікує свої вхідні аргументи, глобальні змінні, не пише в консоль чи файл, не звертається до бази даних тощо.
>
> Чисті функції є передбачуваними, їх легко тестувати та розуміти. Вам не потрібно думати про зовнішній контекст, щоб зрозуміти, що вона робить.

#### **📝 Приклад:**

```java
// Чиста функція
public int sum(int a, int b) {
    return a + b; // Завжди однаковий результат для тих самих a, b
}

// Нечиста функція
private int total = 0;
public int addToTotal(int a) {
    this.total += a; // Має побічний ефект: змінює стан класу
    return this.total;
}
```

#### **📌 Що перевіряють:**

*Знання одного з фундаментальних принципів ФП. Кандидат повинен чітко назвати обидві умови (детермінізм та відсутність побічних ефектів).*

* * *

### **3 ❓ Питання:**

Що таке незмінність (Immutability) і чому вона важлива в ФП?

> #### **💬 Відповідь:**
>
> **Незмінність (Immutability)** — це властивість об'єкта, стан якого **не можна змінити після його створення**. Якщо вам потрібен змінений об'єкт, ви створюєте **новий** екземпляр з потрібними змінами, а старий залишаєте недоторканим.
>
> **Чому вона важлива:**
>
> * **Передбачуваність:** Ви можете безпечно передавати імутабельний об'єкт у будь-яку частину програми, будучи впевненим, що його ніхто не змінить "під капотом". Це усуває цілий клас важких для відстеження багів.
> * **Потокобезпечність (Thread Safety):** Імутабельні об'єкти за своєю природою є потокобезпечними. Оскільки їхній стан не можна змінити, немає потреби в синхронізації при доступі до них з кількох потоків.
> * **Легкість кешування:** Результат операцій над імутабельними об'єктами можна безпечно кешувати, оскільки вхідні дані ніколи не зміняться.
>
> В Java прикладами імутабельних класів є `String`, `Integer`, а з Java 16 — `record`.

#### **📌 Що перевіряють:**

*Розуміння концепції імутабельності та її переваг, особливо для написання надійного та потокобезпечного коду.*

* * *

### **4 ❓ Питання:**

Що таке побічні ефекти (Side Effects) і чому в функціональному програмуванні їх намагаються уникати?

> #### **💬 Відповідь:**
>
> **Побічний ефект** — це будь-яка взаємодія функції з зовнішнім світом, яка виходить за межі повернення значення. Це будь-яка зміна стану, яку можна спостерігати ззовні.
>
> **Приклади побічних ефектів:**
>
> * Зміна глобальної змінної або поля об'єкта.
> * Запис у файл або базу даних.
> * Вивід інформації в консоль або лог.
> * Виклик мережевого API.
> * Модифікація вхідних аргументів (якщо вони мутабельні).
>
> **Чому їх уникають:**
>
> Побічні ефекти роблять код **непередбачуваним** та **важким для тестування**. Щоб зрозуміти, що робить функція з побічними ефектами, вам потрібно знати не тільки її аргументи, а й весь контекст, у якому вона виконується.
>
> Функціональне програмування прагне до **"чистоти"**, де логіка перетворення даних відокремлена від "брудних" операцій вводу-виводу. В ідеальній ФП-архітектурі, "брудний" код виноситься на межі системи.

#### **📌 Що перевіряють:**

*Знання того, що таке побічні ефекти, та здатність пояснити, чому вони є головним "ворогом" передбачуваного та тестованого коду.*

* * *

### **5 ❓ Питання:**

Як Java 8 підтримує функціональне програмування? Назвіть ключові фічі.

> #### **💬 Відповідь:**
>
> Java 8 здійснила революцію, додавши в мову потужні можливості для функціонального програмування.
>
> **Ключові фічі:**
>
> 1. **Лямбда-вирази (Lambda Expressions):**
>     Дозволяють передавати поведінку (блок коду) як параметр методу або зберігати у змінній. Це основа для написання функціонального коду.
>
> 2. **Функціональні інтерфейси (Functional Interfaces):**
>     Служать цільовим типом для лямбд. Пакет `java.util.function` надав стандартний набір таких інтерфейсів (`Predicate`, `Function`, `Consumer`, `Supplier`).
>
> 3. **Посилання на методи (Method References):**
>     Ще більш компактний синтаксис для лямбд, що просто викликають один існуючий метод.
>
> 4. **Stream API:**
>     Надає потужний, декларативний API для обробки послідовностей даних у функціональному стилі. Дозволяє створювати ланцюжки операцій (фільтрація, перетворення, редукція).
>
> 5. **`Optional`:**
>     Надає обгортку для значень, які можуть бути відсутніми, що дозволяє уникнути `NullPointerException` і обробляти відсутність даних у функціональному стилі.
>
> 6. **`Default`-методи в інтерфейсах:**
>     Дозволили еволюцію існуючих API (як `Collection`) без порушення зворотної сумісності.

#### **📌 Що перевіряють:**

*Знання ключових нововведень Java 8 та розуміння, як вони разом формують основу для функціонального програмування в Java.*

* * *

### **6 ❓ Питання:**

Що таке функція вищого порядку (Higher-Order Function)? Наведіть приклад в Java.

> #### **💬 Відповідь:**
>
> **Функція вищого порядку** — це функція, яка відповідає принаймні одній з цих умов:
>
> 1. Приймає одну або більше функцій як **аргументи**.
> 2. Повертає функцію як **результат**.
>
> Оскільки в Java функції представлені через функціональні інтерфейси, функцією вищого порядку буде **метод**, що приймає або повертає екземпляр функціонального інтерфейсу.
>
> Це одна з найпотужніших концепцій ФП, оскільки дозволяє створювати гнучкі та перевикористовувані абстракції.

#### **📝 Приклад:**

Метод `Stream.map()` є класичним прикладом функції вищого порядку.
`Stream<R> map(Function<? super T, ? extends R> mapper)`
>
> Він **приймає функцію** (`mapper`) як аргумент і застосовує її до кожного елемента потоку.

#### **📝 Інший приклад (метод, що повертає функцію):**

```java
public static Predicate<Integer> isGreaterThan(int number) {
    // Цей метод повертає предикат (тобто функцію)
    return x -> x > number;
}
// Використання:
Predicate<Integer> isGreaterThan10 = isGreaterThan(10);
```

#### **📌 Що перевіряють:**

*Розуміння однієї з центральних концепцій ФП. Кандидат повинен вміти пояснити, що це, і навести приклади зі стандартної бібліотеки Java.*

* * *

### **7 ❓ Питання:**

Які переваги функціонального стилю програмування порівняно з імперативним?

> #### **💬 Відповідь:**
>
> Функціональний стиль, порівняно з традиційним імперативним, надає низку переваг, особливо для складних систем.
>
> | **Характеристика** | **Імперативний стиль** | **Функціональний стиль** |
> | :---: | :---: | :---: |
> | **Опис** | **"Як"** зробити (крок за кроком: цикли, змінні, умови) | **"Що"** зробити (декларативно: потік даних та їх перетворення) |
> | **Зміна стану**| Часто мутує стан | Уникає мутації (імутабельність) |
> | **Побічні ефекти**| Допускаються і є нормою | Уникаються (чисті функції) |
> | **Читабельність** | Може бути складною через велику кількість змінних стану та циклів | Часто вища, оскільки код є більш декларативним |
> | **Паралелізм** | Складний. Вимагає ручної синхронізації та блокувань | **Набагато простіший.** Відсутність спільного мутабельного стану усуває ризики "умов гонитви". |
> | **Тестування** | Складніше через залежність від стану та побічні ефекти | Простіше. Чисті функції легко тестувати в ізоляції. |

#### **📌 Що перевіряють:**

*Здатність порівнювати різні парадигми та розуміти переваги функціонального підходу, особливо щодо надійності, тестованості та паралелізму.*

* * *

### **8 ❓ Питання:**

Що таке композиція функцій (Function Composition) і як вона реалізована в Java? ⭐️

> #### **💬 Відповідь:**
>
> **Композиція функцій** — це процес створення нової, складної функції шляхом об'єднання двох або більше простіших функцій, де результат однієї функції стає вхідним аргументом для іншої.
>
> В Java це реалізовано за допомогою `default`-методів в інтерфейсі `java.util.function.Function`:
>
> * **`andThen(Function after)`**:
>     Виконує функції "зліва направо". `f1.andThen(f2)` означає, що спочатку виконається `f1`, а її результат буде передано в `f2`. `f2(f1(x))`.
>
> * **`compose(Function before)`**:
>     Виконує функції "справа наліво". `f1.compose(f2)` означає, що спочатку виконається `f2`, а її результат буде передано в `f1`. `f1(f2(x))`.
>
> Композиція дозволяє будувати складні ланцюжки перетворення даних з невеликих, перевикористовуваних блоків.

#### **📝 Приклад:**

```java
Function<String, Integer> toLength = String::length;
Function<Integer, Integer> square = x -> x * x;

// Об'єднуємо: спочатку знаходимо довжину, потім підносимо до квадрату
Function<String, Integer> lengthAndSquare = toLength.andThen(square);

System.out.println(lengthAndSquare.apply("Hello")); // 25 (довжина 5 -> 5*5=25)
```

#### **📌 Що перевіряють:**

*Знання практичних інструментів для реалізації ФП в Java. Композиція є однією з ключових технік функціонального програмування.*

* * *

### **9 ❓ Питання:**

Поясніть, що таке "лінива оцінка" (Lazy Evaluation) і як вона використовується в Java Stream API. ⭐️

> #### **💬 Відповідь:**
>
> **Лінива оцінка** — це стратегія обчислень, при якій вираз **не обчислюється** в момент його визначення, а лише тоді, коли його результат **справді знадобиться**.
>
> **Як це працює в Stream API:**
>
> Всі **проміжні операції** (`filter`, `map`, `sorted` тощо) в `Stream API` є **лінивими**. Вони не виконуються одразу. Вони лише будують "рецепт" або план обчислень.
>
> Реальні обчислення починаються лише тоді, коли викликається **термінальна операція** (`collect`, `forEach`, `findFirst` тощо).
>
> **Переваги:**
>
> * **Продуктивність:** Уникаються зайві обчислення. Наприклад, якщо ви шукаєте перший елемент, що відповідає умові (`findFirst`), стрім припинить обробку, як тільки знайде його, і не буде перебирати решту мільйона елементів.
> * **Можливість роботи з нескінченними потоками:** Ви можете створити нескінченний потік (`Stream.iterate`), оскільки оброблятимуться лише ті елементи, які потрібні.

#### **📝 Приклад:**

```java
Stream.of("a", "b", "c")
      .map(s -> {
          System.out.println("map: " + s); // Цей рядок не виконається ніколи
          return s.toUpperCase();
      });
// Тут нічого не буде виведено в консоль, бо немає термінальної операції
```

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працює Stream API. Лінива оцінка — це одна з його ключових оптимізаційних характеристик.*

* * *

### **10 ❓ Питання:**

Як можна досягти імутабельності в Java, якщо об'єкти за замовчуванням є мутабельними? ⭐️

> #### **💬 Відповідь:**
>
> Щоб зробити клас імутабельним (незмінним), потрібно дотримуватися кількох правил:
>
> 1. **Зробити всі поля `private` і `final`:** `private` обмежує доступ, а `final` гарантує, що посилання на поле не буде змінено після ініціалізації.
> 2. **Не надавати сеттерів (`setter`-методів):** Відсутність сеттерів не дозволить змінювати стан об'єкта ззовні.
> 3. **Не дозволяти наслідування:** Оголосити клас як `final`, щоб підклас не міг порушити імутабельність.
> 4. **Ініціалізувати всі поля в конструкторі:** Всі поля мають отримати своє значення раз і назавжди під час створення об'єкта.
> 5. **Захисне копіювання (Defensive Copying):** Якщо клас містить поля, що є мутабельними об'єктами (наприклад, `Date` або `List`), потрібно:
>     * У конструкторі створювати копію переданого мутабельного об'єкта.
>     * У геттерах повертати копію внутрішнього мутабельного об'єкта, а не посилання на нього.
>
> **Сучасний підхід (Java 16+):**
> Використовувати **`record`**. `record` за замовчуванням є імутабельним і автоматично генерує `private final` поля, конструктор, геттери, `equals`, `hashCode` та `toString`.

#### **📌 Що перевіряють:**

*Практичні навички створення імутабельних об'єктів. Кандидат повинен знати про всі правила, особливо про захисне копіювання.*

* * *

### **11 ❓ Питання:**

Чому функціональне програмування значно спрощує роботу з багатопотоковістю? ⭐️

> #### **💬 Відповідь:**
>
> Функціональне програмування спрощує багатопотоковість, тому що воно усуває **головну причину** більшості проблем з concurrency: **спільний змінний стан (shared mutable state)**.
>
> **Основні переваги ФП для багатопотоковості:**
>
> 1. **Імутабельність:** Якщо дані є незмінними, їх можна безпечно передавати між потоками без необхідності в блокуваннях (`synchronized`) або інших механізмах синхронізації. Немає ризику, що один потік змінить дані, поки інший їх читає.
> 2. **Відсутність побічних ефектів:** Чисті функції не змінюють нічого за межами своєї області. Це означає, що їх можна безпечно виконувати паралельно, оскільки вони не будуть конфліктувати один з одним, намагаючись змінити один і той самий глобальний стан.
> 3. **Прозорість посилань (Referential Transparency):** Результат чистої функції залежить лише від її аргументів. Це дозволяє легко розпаралелювати обчислення (як це робить `parallelStream()`) та кешувати результати, не турбуючись про синхронізацію.
>
> Програми, написані в функціональному стилі, за своєю природою є більш "готовими" до паралельного виконання.

#### **📌 Що перевіряють:**

*Розуміння однієї з найважливіших переваг ФП. Здатність пояснити зв'язок між імутабельністю та потокобезпечністю є ознакою сильного розробника.*

* * *

### **12 ❓ Питання:**

Чи можливо написати чисто функціональну програму на Java? Які є обмеження? ⭐️

> #### **💬 Відповідь:**
>
> Написати **строго** або **чисто** функціональну програму на Java **практично неможливо і недоцільно**. Java за своєю суттю є об'єктно-орієнтованою мовою з імперативними рисами, і функціональні можливості були додані до неї як доповнення.
>
> **Обмеження та перешкоди:**
>
> 1. **Java не є "чистою" мовою:** Більшість стандартних бібліотек та класів є мутабельними (`ArrayList`, `StringBuilder` тощо). Уникнути їх використання повністю дуже складно.
> 2. **Побічні ефекти неминучі:** Будь-яка корисна програма повинна взаємодіяти з зовнішнім світом (читати файли, писати в БД, виводити на екран), що за визначенням є побічними ефектами. У чисто функціональних мовах (як Haskell) побічні ефекти ізолюються за допомогою спеціальних конструкцій (монад), яких у Java немає в такому вигляді.
> 3. **Система типів:** Система типів Java не така потужна, як у функціональних мовах, і не може на рівні компілятора гарантувати чистоту функцій.
>
> **Що можливо і є гарною практикою:**
> Можна і потрібно **писати в функціональному стилі**, дотримуючись принципів ФП там, де це доречно:
>
> * Віддавати перевагу імутабельним об'єктам для передачі даних.
> * Писати чисті функції для бізнес-логіки.
> * Ізолювати код з побічними ефектами на "краях" системи.
> * Використовувати Stream API для обробки даних.
>
> Це дозволяє отримати переваги ФП, не борючись з природою мови.

#### **📌 Що перевіряють:**

*Реалістичне розуміння можливостей Java. Кандидат не повинен ідеалізувати ФП в Java, а розуміти його як набір корисних інструментів, а не як всеохоплюючу парадигму для цієї мови.*

* * *

### **13 ❓ Питання:**

Розгляньте цей код. Чи є він прикладом хорошого функціонального стилю? Чому ні? ⭐️⭐️

```java
List<String> results = new ArrayList<>();
List<String> inputs = List.of("a", "b", "c");
inputs.parallelStream().forEach(item -> results.add(item.toUpperCase()));
```

> #### **💬 Відповідь:**
>
> **Ні, це приклад дуже поганого, небезпечного коду, який грубо порушує принципи функціонального програмування.**
>
> **Проблеми:**
>
> 1. **Побічні ефекти:** Лямбда, передана в `forEach`, має побічний ефект: вона **модифікує зовнішню колекцію `results`**. Функціональний стиль вимагає уникати таких побічних ефектів.
> 2. **Не потокобезпечно:** `ArrayList` не є потокобезпечною колекцією. Коли `parallelStream()` намагається одночасно з кількох потоків викликати `results.add()`, це може призвести до **"умови гонитви" (race condition)**. Результатом може бути:
>     * Неправильний розмір списку.
>     * `ArrayIndexOutOfBoundsException` або інший виняток.
>     * Пошкоджені дані.
>
> **Як це виправити (правильний функціональний підхід):**
> Потрібно використовувати проміжні операції та колектор, які є чистими і розроблені для паралельної обробки.
>
> ```java
> List<String> inputs = List.of("a", "b", "c");
> List<String> results = inputs.parallelStream()
>                                .map(item -> item.toUpperCase())
>                                .collect(Collectors.toList());
> ```
>
> У цьому варіанті кожна операція (`map`, `collect`) є чистою та потокобезпечною.

#### **📌 Що перевіряють:**

*Здатність розпізнавати небезпечні антипатерни у функціональному коді. Це питання перевіряє розуміння найпоширенішої помилки при роботі з `parallelStream`.*

* * *

### **14 ❓ Питання:**

Як ФП обходиться без циклів (`for`, `while`)? Яка є альтернатива в Java? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Функціональне програмування уникає традиційних циклів, оскільки вони зазвичай покладаються на **мутабельні змінні-лічильники** (`int i = 0; i++`).
>
> Замість циклів використовуються два основні підходи:
>
> 1. **Рекурсія:**
>     * Функція викликає сама себе з оновленими параметрами, доки не досягне базового випадку. Стан передається через аргументи, а не змінюється в мутабельних змінних. У чисто функціональних мовах компілятор оптимізує хвостову рекурсію, щоб уникнути `StackOverflowError`, але в Java така оптимізація не гарантована.
>
> 2. **Функції вищого порядку (основний підхід у Java):**
>     * Це основна альтернатива в Java. Замість того, щоб писати цикл вручну, ви використовуєте методи **`Stream API`**, такі як `map`, `filter`, `forEach`, `reduce`.
>     * Ви не пишете "як" ітерувати (це робить стрім під капотом), а лише declarative описуєте, **"що"** робити з кожним елементом.
>
> **Приклад:**
>
> * **Імперативний цикл:**
>
>     ```java
>     for (String s : list) {
>         System.out.println(s);
>     }
>     ```
>
> * **Функціональна альтернатива:**
>
>     ```java
>     list.forEach(System.out::println);
>     ```
>
> Цей підхід є більш декларативним, коротким та менш схильним до помилок.

#### **📌 Що перевіряють:**

*Розуміння того, як базові імперативні конструкції замінюються на функціональні аналоги. Це показує здатність мислити в іншій парадигмі.*

* * *

### **15 ❓ Питання:**

Як за допомогою функцій вищого порядку можна реалізувати патерн "Стратегія" (Strategy) у функціональному стилі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн "Стратегія" дозволяє визначати сімейство алгоритмів, інкапсулювати кожен з них і робити їх взаємозамінними.
>
> **Класичний ООП-підхід:**
>
> 1. Створити інтерфейс `Strategy` з методом `execute()`.
> 2. Створити кілька конкретних класів, що реалізують цей інтерфейс.
> 3. Клас-контекст приймає об'єкт `Strategy` і викликає його метод.
> Це вимагає написання багатьох класів.
>
> **Функціональний підхід:**
>
> 1. Оскільки "стратегія" — це просто поведінка, її можна представити **функціональним інтерфейсом**. Якщо стратегія, наприклад, приймає `int` і повертає `int`, ми можемо використати стандартний `Function<Integer, Integer>`.
> 2. Замість створення багатьох класів-реалізацій, кожна стратегія реалізується як **окремий лямбда-вираз** або посилання на метод.
> 3. Клас-контекст просто приймає цю `Function` як параметр і застосовує її.
>
> Цей підхід є набагато більш компактним і гнучким.
>
> #### **Приклад:**
>
> ```java
> // Контекст, що приймає стратегію як Function
> class Calculator {
>     public int executeStrategy(int a, int b, BinaryOperator<Integer> strategy) {
>         return strategy.apply(a, b);
>     }
> }
> 
> // Створюємо різні стратегії "на льоту"
> BinaryOperator<Integer> addition = (a, b) -> a + b;
> BinaryOperator<Integer> multiplication = (a, b) -> a * b;
> 
> Calculator calc = new Calculator();
> System.out.println(calc.executeStrategy(5, 3, addition)); // 8
> System.out.println(calc.executeStrategy(5, 3, multiplication)); // 15
> ```
>
> Ми реалізували патерн без створення жодного додаткового класу для стратегій.

#### **📌 Що перевіряють:**

*Здатність переосмислювати класичні ООП-патерни у функціональному стилі. Це показує глибоке розуміння обох парадигм та їх синергії.*

* * *
