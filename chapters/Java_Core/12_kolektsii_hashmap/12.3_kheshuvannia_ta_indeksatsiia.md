---
layout: default
title: "Хешування та індексація"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 3
---

# Хешування та індексація

* короткий опис: Хешування – процес перетворення ключа на числовий хеш-код (`hashCode()`). Індексація – розрахунок індексу бакета у внутрішньому масиві (`index = hashCode % capacity` або `(capacity - 1) & hash`).

* поняття: `hashCode()`, `capacity` (розмір масиву бакетів), `index`, `collision` (колізія).

* приклади: `String key = "March"; int hash = key.hashCode(); int index = hash % capacity;`.

* ключові моменти: Якість `hashCode()` впливає на розподіл ключів та частоту колізій. Оптимізована формула `(capacity - 1) & hash` використовується, якщо `capacity` є степенем двійки.

* що важливо знати на співбесіді: Як розраховується індекс, роль хеш-функції.

### **✅ Офіційне визначення:**

* **Хешування** — це процес перетворення об'єкта у числове значення (**хеш-код**) за допомогою методу **`hashCode()`**.
* **Індексація** — це розрахунок індексу в масиві бакетів (**`bucket`** `array`) за формулою, яка зазвичай включає модуль від поділу хешу на розмір масиву.

### **🧠 Простими словами:**

**Об’єкт** — це ключ. Ми його “перемелюємо” у число (**`hashCode()`**), а потім беремо залишок від поділу цього числа на розмір мапи, щоб знайти, в який комірці (**бакеті**) його зберігати.

---

## **📊 Ключові елементи хешування в `HashMap`:**

| Поняття | Пояснення |
| :---: | :---: |
| **`hashCode()`** | Метод, який повертає ціле число — хеш-код об'єкта |
| **`capacity`** | Поточна кількість бакетів у таблиці (масиві) |
| **`index`** | Обчислюється як **`hashCode % capacity`** або з урахуванням маски (бітовий **AND**) |
| **`collision` (колізія)** | Коли два ключі мають однаковий індекс після хешування |

---

## **Пояснення на пальцях (💡)**

Уяви, що в тебе є поштові скриньки (**бакети**), і ти кладеш туди листи (**значення**) за іменем (**ключ**). Щоб не перевіряти всі імена вручну, ти перетворюєш ім’я у число (**`hashCode`**) і дізнаєшся, яка скринька твоя — просто береш залишок від поділу на кількість скриньок (**capacity**).

---

## **Корисні приклади (🧪)**

```java
String key = "March";
int hash = key.hashCode();        // Наприклад, 77406290
int capacity = 16;
int index = hash % capacity;      // Наприклад, 2

System.out.println("Bucket index: " + index);
```

Але в реальній реалізації Java використовує:

```java
int index = (capacity - 1) & hash;
```

Це більш ефективно при умові, що **capacity** — ступінь двійки.

---

## **Порівняння (⚖️)**

| Метод | Призначення | Приклад |
| :---: | :---: | :---: |
| **`hashCode()`** | Перетворює ключ у хеш-код | **`"abc".hashCode()`** -> 96354 |
| **`hash % capacity`** | Розрахунок індексу бакету | **`96354 % 16 = 2`** |
| **`(n - 1) & hash`** | Альтернативна бітова оптимізація | **`15 & 96354`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“Hash -> Mod -> Bucket”**

Тобто: спочатку хеш, потім модуль, потім бакет.

👊 Хешуємо -> % -> кладемо

---

## **🟩 Підсумок:**

* Хешування: **`key.hashCode()`** -> генерує хеш-код.
* Індексація: **`hash % capacity`** або **`(n-1) & hash`** -> визначає індекс.
* Якщо два ключі мають однаковий індекс -> **колізія**.
* Java оптимізує розрахунок індексу за допомогою побітової операції.
* Важливо розуміти, як це впливає на продуктивність та розподіл даних.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Object.hashCode()**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/lang/Object.html#hashCode\(\))

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке хешування та індексація в контексті `HashMap`?

> #### **💬 Відповідь:**
>
> **Хешування та індексація** — це двокроковий процес, який `HashMap` використовує для визначення, де саме у внутрішньому масиві зберігати запис "ключ-значення".
>
> 1. **Хешування:**
>     * Це процес перетворення об'єкта-ключа (`key`) на ціле число, яке називається **хеш-кодом**.
>     * Цю роботу виконує метод **`key.hashCode()`**.
>     * `HashMap` додатково обробляє цей хеш-код власною внутрішньою функцією, щоб покращити розподіл бітів.
>
> 2. **Індексація:**
>     * Це процес обчислення **індексу** (позиції) у внутрішньому масиві "кошиків" (buckets) на основі отриманого хеш-коду.
>     * Індекс розраховується за формулою `index = (capacity - 1) & hash`, де `capacity` — поточна ємність масиву.
>
> Разом ці два кроки дозволяють `HashMap` швидко "стрибнути" до потрібного кошика для додавання, отримання або видалення елемента.

#### **📝 Аналогія:**

Це як система адресації.

* **Ключ:** "Вулиця Шевченка, 10".
* **Хешування:** Перетворення цієї адреси на унікальний поштовий індекс, наприклад, "01001".
* **Індексація:** Використання цього індексу для визначення, в який саме поштовий відділ (кошик) відправити лист.

#### **📌 Що перевіряють:**

*Розуміння двоетапного процесу, який лежить в основі роботи `HashMap`.*

* * *

### **2 ❓ Питання:**

Яка роль методу `hashCode()` у роботі `HashMap`?

> #### **💬 Відповідь:**
>
> Метод `hashCode()` відіграє **ключову роль** у забезпеченні **продуктивності** `HashMap`.
>
> **Його основне завдання** — якомога рівномірніше "розкидати" різні ключі по різних числовим значенням. Чим краще розподіл, тим менше буде хеш-колізій.
>
> `HashMap` використовує значення, повернуте `hashCode()`:
>
> 1. **Для визначення індексу кошика (bucket):** Це дозволяє виконувати операції `get` та `put` за середній час **O(1)**. `HashMap` не перебирає всі ключі, а відразу переходить до потрібного кошика.
>
> 2. **Для попереднього порівняння ключів:** У випадку колізії, перед викликом "дорогого" методу `equals()`, `HashMap` спочатку порівнює кешовані хеш-коди ключів. Якщо вони різні, `equals()` навіть не буде викликатися.
>
> **Висновок:** Якість реалізації `hashCode()` напряму впливає на кількість колізій і, як наслідок, на продуктивність `HashMap`.

#### **📌 Що перевіряють:**

*Розуміння того, що `hashCode()` — це основа продуктивності хеш-структур. Поганий `hashCode()` — повільний `HashMap`.*

* * *

### **3 ❓ Питання:**

Як `HashMap` обчислює індекс кошика (bucket) з хеш-коду? Чому використовується бітове "І" (`&`), а не операція `%`? ⭐️

> #### **💬 Відповідь:**
>
> Для обчислення індексу `HashMap` використовує формулу: **`index = (capacity - 1) & hash`**.
>
> **Чому використовується `&`, а не `%`?**
>
> 1. **Продуктивність:** Операція бітового "І" (`&`) є **значно швидшою** на рівні процесора, ніж операція взяття залишку від ділення (`%`). Це важлива мікро-оптимізація.
>
> 2. **Необхідна умова:** Цей "трюк" працює коректно **тільки тоді**, коли ємність таблиці (`capacity`) є **степенем двійки** (16, 32, 64...). `HashMap` спеціально підтримує свою ємність як степінь двійки (наприклад, при розширенні вона подвоюється).
>
>     Коли `capacity` є степенем двійки, `capacity - 1` у бінарному представленні є послідовністю одиниць (наприклад, для 16 (`10000`), `15` — це `01111`). Виконання `hash & (capacity - 1)` еквівалентно взяттю залишку від ділення на `capacity`, але значно швидше.

#### **📌 Що перевіряють:**

*Знання про внутрішні оптимізації `HashMap`, зокрема, використання бітових операцій для підвищення продуктивності.*

* * *

### **4 ❓ Питання:**

Що таке хеш-колізія і як `HashMap` з нею справляється?

> #### **💬 Відповідь:**
>
> **Хеш-колізія** — це ситуація, коли два **різні** за `equals()` ключі мають **однаковий** `hashCode()`. Через це вони потрапляють в один і той самий **кошик (bucket)**.
>
> **Як `HashMap` з нею справляється:**
>
> `HashMap` зберігає в кожному кошику структуру даних, яка може містити кілька записів.
>
> 1. Коли новий запис потрапляє в кошик, де вже є елементи, `HashMap` **ітерує** по ним.
> 2. Він порівнює новий ключ з кожним існуючим у кошику ключем, використовуючи **метод `equals()`**.
> 3. **Якщо `equals()` повертає `true`**, старе значення перезаписується.
> 4. **Якщо `equals()` для всіх повертає `false`**, новий запис додається в цей самий кошик як ще один елемент.
>
> * **До Java 8:** кошик був реалізований як **зв'язаний список**.
> * **З Java 8:** якщо кількість елементів у кошику перевищує поріг (8), він перетворюється на **червоно-чорне дерево** для підвищення продуктивності пошуку всередині кошика з O(n) до O(log n).

#### **📌 Що перевіряють:**

*Знання визначення колізії та механізму її обробки в `HashMap`, включаючи оптимізацію в Java 8.*

* * *

### **5 ❓ Питання:**

Навіщо `HashMap` додатково хешує результат `key.hashCode()`? ⭐️

> #### **💬 Відповідь:**
>
> `HashMap` застосовує додаткову власну хеш-функцію, щоб **покращити розподіл бітів** і зменшити кількість колізій, особливо для **поганих** реалізацій `hashCode()`.
>
> **Проблема:**
>
> Деякі реалізації `hashCode()` можуть мати поганий розподіл у **молодших бітах**. Наприклад, якщо хеш-коди для багатьох ключів відрізняються тільки в старших бітах. Оскільки `HashMap` використовує для індексації тільки молодші біти ( `(capacity - 1) & hash`), це призведе до великої кількості колізій.
>
> **Рішення `HashMap`:**
>
> Він "змішує" старші біти з молодшими, використовуючи операцію `XOR`.
> `static final int hash(Object key) { int h; return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16); }`
>
> Операція `h ^ (h >>> 16)` бере старші 16 біт хешу і "накладає" їх на молодші 16 біт. Це гарантує, що зміни у старших бітах також вплинуть на результат індексації, що робить розподіл більш рівномірним.

#### **📌 Що перевіряють:**

*Глибокі знання внутрішніх оптимізацій `HashMap`. Розуміння того, чому `HashMap` "не довіряє" зовнішнім хеш-функціям повністю.*

* * *

### **6 ❓ Питання:**

Як індекс розраховується для ключа `null`?

> #### **💬 Відповідь:**
>
> Для ключа `null` `HashMap` має спеціальну, вбудовану логіку.
>
> * `hashCode()` для `null` ключа завжди дорівнює **0**.
> * Відповідно, індекс кошика для `null` ключа також **завжди** дорівнює **0**.
>
> Коли ви викликаєте `put(null, value)` або `get(null)`, `HashMap` не обчислює хеш, а одразу переходить до кошика з індексом 0. Там він ітерується по елементах і шукає запис, у якого ключ є `null` (порівнюючи через `==`).
>
> Таким чином, `null`-ключ обробляється як особливий випадок.

#### **📌 Що перевіряють:**

*Знання того, як обробляються крайові випадки, як-от `null`-ключі.*

* * *

### **7 ❓ Питання:**

Якби ви реалізовували свою власну `HashMap`, яку початкову ємність (`capacity`) ви б обрали і чому? ⭐️

> #### **💬 Відповідь:**
>
> Для початкової ємності найкраще обирати **степінь двійки** (наприклад, 16, 32, 64).
>
> У стандартному `HashMap` початкова ємність за замовчуванням — **16**.
>
> **Чому степінь двійки?**
>
> Це дозволяє використовувати **дуже ефективну операцію** для обчислення індексу кошика: `index = hash & (capacity - 1)`.
>
> Якби `capacity` не було степенем двійки, довелося б використовувати **менш продуктивну** операцію взяття залишку від ділення `hash % capacity`.
>
> Обираючи степінь двійки, ми закладаємо основу для використання бітових оптимізацій, що є важливою частиною продуктивності `HashMap`. Значення 16 є хорошим компромісом: не занадто велике, щоб витрачати пам'ять для маленьких мап, і не занадто мале, щоб не викликати часті розширення для середніх.

#### **📌 Що перевіряють:**

*Розуміння того, як вибір початкової ємності впливає на алгоритм індексації та продуктивність.*

* * *

### **8 ❓ Питання:**

Який зв'язок між індексацією та `load factor` (коефіцієнтом завантаження)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Load factor` напряму **не впливає** на формулу індексації (`index = ...`), але він впливає на те, **коли** зміниться ключовий параметр цієї формули — **`capacity`**.
>
> 1. Індексація залежить від поточної ємності таблиці (`capacity`).
> 2. `Load factor` визначає, коли ця `capacity` буде збільшена. Коли `size` перевищує `capacity * load_factor`, відбувається `resize`.
>
> **Зв'язок:**
>
> `Load factor` є **компромісом** між використанням пам'яті та **ймовірністю колізій**, які ускладнюють індексацію.
>
> * **Низький `load factor`**: Мапа буде розширюватися частіше. `Capacity` буде великою відносно `size`. Це означає, що дані будуть "розрідженими", і **ймовірність колізій (потрапляння двох ключів в один індекс) буде меншою**.
> * **Високий `load factor`**: Мапа буде більш щільно заповненою. `Capacity` буде меншою, що економить пам'ять, але **збільшує ймовірність колізій**, оскільки ті ж самі дані доводиться "втискати" у меншу кількість кошиків.
>
> Таким чином, `load factor` опосередковано впливає на ефективність індексації, керуючи щільністю даних і, як наслідок, частотою колізій.

#### **📌 Що перевіряють:**

*Розуміння того, як `load factor` впливає на баланс між пам'яттю та продуктивністю, керуючи частотою колізій.*

* * *

### **9 ❓ Питання:**

Що таке `rehashing` (перехешування)? Як воно змінює індекси існуючих елементів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Rehashing** — це процес, що відбувається під час розширення (`resize`) `HashMap`. Коли створюється нова таблиця (масив кошиків) з більшою ємністю, всі існуючі записи повинні бути переміщені в неї.
>
> **Як воно змінює індекси:**
>
> * Індекс кожного елемента **залежить від `capacity`** (`index = (capacity - 1) & hash`).
> * Оскільки при розширенні `capacity` змінюється (зазвичай подвоюється), то і **індекс для кожного існуючого елемента потрібно обчислити заново**.
>
> Це означає, що `rehash` не просто копіює старі кошики в нові. Він ітерує по **кожному запису** в старій таблиці, **переобчислює його індекс** на основі нової ємності і розміщує його в правильний кошик у **новій** таблиці.
>
> Елемент, який раніше знаходився в кошику з індексом 5, у новій таблиці може потрапити в кошик 5 або в кошик 5 + `стара_ємність`. Це пов'язано з тим, як змінюється бітова маска при подвоєнні `capacity`.
>
> Цей процес є "дорогим" (O(n)), але він необхідний для збереження ефективного доступу після розширення.

#### **📌 Що перевіряють:**

*Розуміння того, що `rehash` — це не просто копіювання, а повний перерозподіл усіх елементів відповідно до нової ємності.*

* * *

### **10 ❓ Питання:**

Чи може `HashMap` мати від'ємний індекс кошика? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не може**.
>
> 1. `hashCode()` може повертати **від'ємне** число, оскільки діапазон `int` в Java — від -2^31 до 2^31-1.
>
> 2. Однак `HashMap` для індексації використовує операцію **бітового "І" (`&`)**: `index = hash & (capacity - 1)`.
>
> 3. Оскільки `capacity` (а отже, і `capacity - 1`) завжди є **додатнім числом**, результат бітового "І" з будь-яким числом (навіть від'ємним) **ніколи не буде від'ємним**.
>
> **Приклад:**
>
> * `capacity` = 16, `capacity - 1` = 15 (бінарне `0...01111`).
> * `hash` = -1 (бінарне `1...11111`).
> * `-1 & 15` дасть `15`. Результат завжди буде в діапазоні від `0` до `capacity - 1`.
>
> Якби використовувалася операція `%`, то для від'ємного хешу результат також міг би бути від'ємним, що потребувало б додаткової обробки (`Math.abs()`). Використання `&` є не тільки швидшим, але й більш елегантним вирішенням цієї проблеми.

#### **📌 Що перевіряють:**

*Дуже глибокі знання бітових операцій та їхньої поведінки з від'ємними числами.*

* * *

### **11 ❓ Питання:**

Як реалізовано `TreeMap.get(key)`? Чи використовує він хешування та індексацію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, `TreeMap` **не використовує хешування та індексацію** взагалі.
>
> `TreeMap` базується на **червоно-чорному дереві**, яке є збалансованим бінарним деревом пошуку.
>
> **Як працює `TreeMap.get(key)`:**
>
> 1. Він починає пошук з **кореня дерева**.
> 2. На кожному вузлі він **порівнює** шуканий ключ з ключем поточного вузла за допомогою методу **`compareTo()`** (якщо ключі реалізують `Comparable`) або **`compare()`** (з `Comparator`, переданого в конструктор).
> 3. * Якщо ключі рівні, вузол знайдено.
>     * Якщо шуканий ключ менший, пошук продовжується у **лівому піддереві**.
>     * Якщо шуканий ключ більший, пошук продовжується у **правому піддереві**.
> 4. Процес триває, доки ключ не буде знайдено або доки не буде досягнуто кінця гілки (листка).
>
> Складність цієї операції — **O(log n)**, що повільніше за O(1) у `HashMap`, але гарантує відсортований порядок ключів.

#### **📌 Що перевіряють:**

*Розуміння того, що різні реалізації `Map` використовують абсолютно різні внутрішні алгоритми.*

* * *

### **12 ❓ Питання:**

Припустимо, ви написали клас `Person` з полями `name` та `age`. Якщо ви використаєте його як ключ у `HashMap`, не перевизначивши `hashCode()`, що буде відбуватися при пошуку/додаванні?

> #### **💬 Відповідь:**
>
> Якщо `hashCode()` не перевизначено, буде використовуватися **реалізація за замовчуванням** з класу `Object`. Ця реалізація зазвичай генерує хеш-код на основі **внутрішньої адреси об'єкта в пам'яті**.
>
> **Наслідки:**
>
> 1. **Два логічно однакових об'єкти** будуть мати **різні хеш-коди** і потраплять у **різні кошики**.
>
>     ```java
>     Person p1 = new Person("John", 30);
>     Person p2 = new Person("John", 30);
>
>     // p1.hashCode() != p2.hashCode()
>     ```
>
> 2. **Ви не зможете знайти об'єкт за логічним еквівалентом:**
>
>     ```java
>     map.put(p1, "some value");
>
>     // Пошук за p2 дасть null, бо він потрапить в інший кошик
>     String value = map.get(p2); // null
>     ```
>
> 3. Ви зможете додати **обидва "однакові" об'єкти** в мапу як різні ключі.
>
> Таким чином, `HashMap` буде працювати в режимі "карти ідентичностей", розрізняючи об'єкти за їхнім посиланням, а не за вмістом.

#### **📌 Що перевіряють:**

*Практичне розуміння наслідків порушення контракту `equals/hashCode`.*

* * *

### **13 ❓ Питання:**

Чому реалізація `String.hashCode()` є кешованою, і як це впливає на продуктивність `HashMap`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `hashCode()` в `String` **кешується**, тому що `String` є **імутабельним**.
>
> **Як це працює:**
>
> 1. У класі `String` є поле `private int hash;`.
> 2. При першому виклику `hashCode()` він обчислює хеш-код, проходячи по всіх символах.
> 3. Результат зберігається в полі `hash`.
> 4. При всіх наступних викликах `hashCode()` метод просто повертає це кешоване значення, не виконуючи обчислень.
>
> **Вплив на `HashMap`:**
>
> Це **значно підвищує продуктивність**, коли рядки використовуються як ключі. Операція `put` або `get` для `HashMap` завжди вимагає обчислення хеш-коду ключа. Оскільки рядки є дуже поширеними ключами, кешування їхнього хеш-коду усуває необхідність щоразу перераховувати його, особливо для довгих рядків.
>
> Якби `String` був мутабельним, таке кешування було б неможливим, бо зміна рядка вимагала б перерахунку хешу.

#### **📌 Що перевіряють:**

*Розуміння переваг імутабельності та її зв'язку з оптимізаціями продуктивності, як-от кешування `hashCode`.*

* * *

### **14 ❓ Питання:**

Розглянемо `LinkedHashMap`. Чи використовує він той самий механізм хешування та індексації, що і `HashMap`?

> #### **💬 Відповідь:**
>
> Так, **повністю той самий**.
>
> `LinkedHashMap` **успадковує від `HashMap`**. Він перевикористовує всю його внутрішню структуру, що базується на хеш-таблиці, включаючи:
>
> * Внутрішній масив кошиків.
> * Механізм обчислення хешу та індексу (`hash & (capacity-1)`).
> * Обробку колізій (через зв'язані списки/дерева).
>
> **Що `LinkedHashMap` додає:**
>
> Він додає **двозв'язний список**, який проходить через **усі** записи (`Map.Entry`) в мапі. Цей список підтримує **порядок вставки**.
>
> Коли ви додаєте новий елемент, він не тільки розміщується в правильному кошику (як у `HashMap`), але й додається в кінець цього двозв'язного списку.
>
> Ітерація по `LinkedHashMap` відбувається саме по цьому списку, а не по кошиках, що і забезпечує збереження порядку.

#### **📌 Що перевіряють:**

*Знання того, як `LinkedHashMap` розширює функціональність `HashMap`, не змінюючи його основного механізму хешування.*

* * *

### **15 ❓ Питання:**

Якщо ваш `hashCode()` завжди повертає різні значення для об'єктів, які є рівними за `equals()`, що буде працювати некоректно: `put` чи `get`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Некоректно буде працювати **і `put`, і `get`**, а також `remove`, `containsKey` — по суті, **всі операції, що базуються на ключі**.
>
> Це пряме **порушення контракту `equals/hashCode`**.
>
> **Сценарій:**
>
> `Person p1 = new Person("John", 30);`
> `Person p2 = new Person("John", 30);`
>
> `p1.equals(p2)` -> `true`.
> `p1.hashCode()` -> `123`.
> `p2.hashCode()` -> `456`. (порушення)
>
> 1. **Проблема з `put`:**
>     `map.put(p1, "value1");` // p1 потрапляє в кошик, що відповідає хешу 123.
>     `map.put(p2, "value2");` // p2 потрапляє в кошик для хешу 456.
>     В результаті в мапі буде **два "однакові" ключі** з різними значеннями, що порушує семантику `Map`.
>
> 2. **Проблема з `get`:**
>     `map.put(p1, "value");`
>     `map.get(p2)` поверне `null`. `HashMap` буде шукати `p2` в кошику, що відповідає хешу 456, і нічого там не знайде, хоча логічно такий ключ є в мапі.
>
> **Висновок:** Порушення цього контракту призводить до абсолютно непередбачуваної і некоректної поведінки, роблячи `HashMap` непридатним для використання з такими об'єктами.

#### **📌 Що перевіряють:**

*Здатність проаналізувати наслідки порушення контракту для всіх основних операцій `Map`.*
