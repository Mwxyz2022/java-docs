---
layout: default
title: "HashMap: Основи"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 1
---

# HashMap: Основи

* короткий опис: `HashMap` – це реалізація інтерфейсу `Map`, що зберігає пари "ключ-значення" з унікальними ключами, використовуючи хеш-таблицю для швидкого доступу.

* поняття: `Map`, `HashMap`, ключ-значення.

* приклади: `monthAndNumberOfDays.put("January", 31);`, `monthAndNumberOfDays.get("March");`.

* ключові моменти: Ключі унікальні (нове значення перезаписує старе для того ж ключа), дозволяє один `null` ключ.

* що важливо знати на співбесіді: Поведінка при дублюванні ключів, особливості `null`-ключів і значень.

### **✅ Офіційне визначення:**

**`HashMap`** — це структура даних у Java, яка реалізує інтерфейс **`Map`** і зберігає пари ключ-значення, де кожен ключ є унікальним. Для зберігання та доступу до елементів використовує хеш-таблицю.

### **🧠 Простими словами:**

**`HashMap`** — це як словничок, де ти зберігаєш дані у вигляді "ключ — значення". Наприклад, "Січень — 31 день". Ключ не може повторюватись — якщо додаси ще раз той самий, попереднє значення буде замінено.

---

## **📊 Основні поняття:**

* **`Map<K, V>`** — інтерфейс, який описує поведінку асоціативного масиву (словника).
* **`HashMap<K, V>`** — реалізація Map на основі хеш-таблиці.
* Ключ **`(K)`** — унікальний ідентифікатор.
* Значення **`(V)`** — пов’язане з ключем значення.

---

## **Пояснення на пальцях (💡)**

Уяви собі шафу з 16 ящиків. Щоб покласти щось у правильний ящик, ти використовуєш спеціальну формулу (**хеш-функцію**), яка визначає номер ящика на основі ключа. Якщо в ящику вже щось є — додаємо поруч. Якщо покладеш ще раз той самий ключ — старе значення викидається, нове кладеться замість нього.

---

## **Корисні приклади (🧪)**

```java
Map<String, Integer> monthAndNumberOfDays = new HashMap<>();
monthAndNumberOfDays.put("January", 31);
monthAndNumberOfDays.put("February", 28);
monthAndNumberOfDays.put("March", 31);

System.out.println(monthAndNumberOfDays.get("March")); // 31
System.out.println(monthAndNumberOfDays.get("February")); // 28
```

Якщо викликати **`put("February", 29)`**, то значення перезапишеться на 29\.

---

## **Порівняння (⚖️)**

| Характеристика | HashMap | TreeMap | LinkedHashMap |
| :---: | :---: | :---: | :---: |
| **Порядок елементів** | Не гарантується | Відсортований за ключами | В порядку вставки |
| **Швидкість доступу** | Висока (O(1) в середньому) | Повільніше (O(log n)) | Висока (O(1)) |
| **Дозволяє null ключ** | Так | Ні | Так |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **H** — Hash-функція (вираховує bucket)
* **A** — Access fast (швидкий доступ)
* **S** — Stores key-value (зберігає пари)
* **H** — Handle collisions (обробляє колізії)

---

## **🟩 Підсумок:**

* **`HashMap`** — ключ-значення структура з швидким доступом.
* Ключ має бути унікальним, інакше значення буде перезаписано.
* Дозволяє один **`null`** як ключ і багато **`null`** як значення.
* Хеш-функція \+ масив бакетів \= ефективність.

### **🔗 Додаткові джерела:**

* [**Офіційна документація Java HashMap**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `HashMap` у Java і для чого він використовується?

> #### **💬 Відповідь:**
>
> **`java.util.HashMap<K, V>`** — це клас з Java Collections Framework, який реалізує інтерфейс `Map<K, V>` і використовується для зберігання даних у вигляді пар **"ключ-значення"**.
>
> **Основні характеристики:**
>
> * **Унікальність ключів:** Кожен ключ у `HashMap` є унікальним. При спробі додати запис з ключем, який вже існує, старе значення буде перезаписано новим.
> * **Швидкість:** Він базується на **хеш-таблиці**, що забезпечує дуже швидкі операції додавання (`put`), отримання (`get`) та видалення (`remove`) в середньому за **константний час (O(1))**.
> * **Порядок:** `HashMap` **не гарантує** жодного порядку елементів. Порядок може змінюватися з часом, наприклад, при розширенні таблиці.
> * **Підтримка `null`:** Дозволяє зберігати **один** ключ `null` та **багато** значень `null`.
>
> **Використовується:** Для завдань, де потрібен швидкий пошук даних за унікальним ідентифікатором, наприклад, кешування об'єктів, підрахунок частоти слів, зберігання налаштувань.

#### **📝 Аналогія:**

Це як гардероб у театрі. Кожен ключ — це унікальний номерок, а значення — це ваше пальто. Ви даєте номерок і миттєво отримуєте своє пальто, не переглядаючи всі інші.

#### **📌 Що перевіряють:**

*Розуміння базового призначення `HashMap` як структури "ключ-значення" з швидким доступом.*

* * *

### **2 ❓ Питання:**

Що станеться, якщо спробувати додати в `HashMap` запис з ключем, який вже існує?

> #### **💬 Відповідь:**
>
> Якщо ви викликаєте метод `put(key, value)` з ключем, який вже присутній у `HashMap`, відбудеться наступне:
>
> 1. `HashMap` знайде існуючий запис з цим ключем.
> 2. **Старе значення буде перезаписано** новим значенням, яке ви передали.
> 3. Метод `put()` поверне **попереднє (старе) значення**, яке було пов'язане з цим ключем. Якщо ключа раніше не було, метод поверне `null`.
>
> **Приклад:**
>
> ```java
> Map<String, Integer> scores = new HashMap<>();
> scores.put("Alice", 90);
>
> // Перезаписуємо значення для ключа "Alice"
> Integer previousScore = scores.put("Alice", 95);
>
> System.out.println("Новий рахунок: " + scores.get("Alice")); // 95
> System.out.println("Попередній рахунок: " + previousScore); // 90
> ```
>
> Унікальність ключів є фундаментальною властивістю `HashMap`.

#### **📌 Що перевіряють:**

*Знання того, як `HashMap` обробляє дублювання ключів та що саме повертає метод `put`.*

* * *

### **3 ❓ Питання:**

Чи можна використовувати `null` як ключ або значення в `HashMap`? А в `Hashtable`?

> #### **💬 Відповідь:**
>
> * **`HashMap`:**
>   * **Ключ `null`:** **Так, можна**. Дозволяється зберігати **один** ключ `null`. Він зазвичай зберігається в "кошику" (bucket) з індексом 0.
>   * **Значення `null`:** **Так, можна**. `HashMap` дозволяє зберігати **скільки завгодно** значень `null`.
>
> * **`Hashtable` (застарілий клас):**
>   * **Ключ `null`:** **Ні, не можна**. Спроба додати запис з ключем `null` призведе до **`NullPointerException`**.
>   * **Значення `null`:** **Ні, не можна**. Спроба додати запис зі значенням `null` також призведе до `NullPointerException`.
>
> Це одна з ключових відмінностей між цими двома класами. `HashMap` є більш гнучким, тоді як `Hashtable` є більш "суворим" і до того ж синхронізованим.

#### **📌 Що перевіряють:**

*Знання деталей API та відмінностей між сучасними (`HashMap`) та застарілими (`Hashtable`) класами.*

* * *

### **4 ❓ Питання:**

Назвіть три основні операції для роботи з `HashMap` та їхню асимптотичну складність.

> #### **💬 Відповідь:**
>
> Трьома основними операціями є `put`, `get` та `remove`.
>
> **Асимптотична складність:**
>
> * **Середній випадок: O(1) (константний час).**
>     Це досягається завдяки використанню хеш-функції, яка дозволяє миттєво знаходити потрібний "кошик" для даних.
>
> * **Найгірший випадок: O(log n) з Java 8+ / O(n) до Java 8.**
>     Цей випадок виникає при великій кількості **хеш-колізій**, коли всі ключі потрапляють в один кошик. Тоді для пошуку потрібного елемента доводиться перебирати всі елементи в цьому кошику.
>
> | Операція | Складність (середня) |
> |---|---|
> | **`put(K key, V value)`** | **O(1)** |
> | **`get(Object key)`** | **O(1)** |
> | **`remove(Object key)`** | **O(1)** |
>
> Для `containsKey(key)` та `containsValue(value)` складність становить O(1) та O(n) відповідно.

#### **📌 Що перевіряють:**

*Знання асимптотичної складності для основних операцій. Розуміння того, що `HashMap` дуже швидкий у середньому.*

* * *

### **5 ❓ Питання:**

Яким вимогам має відповідати об'єкт, щоб його можна було використовувати як ключ у `HashMap`?

> #### **💬 Відповідь:**
>
> Щоб об'єкт міг коректно використовуватися як ключ у `HashMap`, його клас повинен **правильно перевизначати два методи**:
>
> 1. **`hashCode()`**
> 2. **`equals(Object obj)`**
>
> **`HashMap` покладається на їхній контракт:**
>
> * **`hashCode()`** використовується для визначення "кошика" (bucket), в який буде поміщено запис.
> * **`equals()`** використовується для порівняння ключів усередині одного кошика (у випадку колізії), щоб знайти точний збіг.
>
> **Найважливіше правило:** якщо два об'єкти рівні за `equals()`, вони **зобов'язані** мати однаковий `hashCode()`.
>
> Крім того, настійно рекомендується, щоб ключі були **імутабельними (незмінними)**. Якщо ви зміните стан об'єкта-ключа після його додавання в мапу, його `hashCode` може змінитися, і ви більше не зможете знайти цей запис.

#### **📌 Що перевіряють:**

*Розуміння нерозривного зв'язку між `HashMap`, `hashCode` та `equals`. Знання вимоги до імутабельності ключів.*

* * *

### **6 ❓ Питання:**

Як обійти всі елементи `HashMap`? Назвіть кілька способів. ⭐️

> #### **💬 Відповідь:**
>
> Існує три основних способи ітерації по `HashMap`:
>
> 1. **Ітерація по парах "ключ-значення" (`entrySet()`):**
>     * **Це найкращий та найефективніший спосіб.** Він не вимагає додаткових пошуків значення за ключем.
>
>     ```java
>     for (Map.Entry<String, Integer> entry : map.entrySet()) {
>         String key = entry.getKey();
>         Integer value = entry.getValue();
>         System.out.println("Ключ: " + key + ", Значення: " + value);
>     }
>     ```
>
> 2. **Ітерація по ключах (`keySet()`):**
>
>     * Спочатку отримується множина всіх ключів, а потім для кожного ключа робиться окремий виклик `get()`.
>     * Це **менш ефективно**, ніж `entrySet`, бо для кожного ключа відбувається додатковий пошук у мапі.
>
>     ```java
>     for (String key : map.keySet()) {
>         Integer value = map.get(key); // Додатковий пошук
>         // ...
>     }
>     ```
>
> 3. **Ітерація по значеннях (`values()`):**
>     * Цей спосіб використовується, якщо вам потрібні **тільки значення**, а ключі не важливі.
>
>     ```java
>     for (Integer value : map.values()) {
>         System.out.println("Значення: " + value);
>     }
>     ```
>
> З Java 8 також популярна ітерація через `map.forEach((key, value) -> { ... })`.

#### **📌 Що перевіряють:**

*Практичні навички роботи з `HashMap`. Знання про існування `entrySet` та розуміння, чому він є кращим для повної ітерації.*

* * *

### **7 ❓ Питання:**

Чим `HashMap` відрізняється від `TreeMap` та `LinkedHashMap`? ⭐️

> #### **💬 Відповідь:**
>
> Всі три є реалізаціями `Map`, але вони відрізняються **порядком елементів** та **продуктивністю**.
>
> * **`HashMap`:**
>   * **Порядок:** **Не гарантує** жодного порядку. Елементи можуть бути розташовані хаотично.
>   * **Продуктивність:** **Найшвидший**. Операції `put`, `get`, `remove` виконуються за **O(1)** в середньому.
>
> * **`LinkedHashMap`:**
>   * **Порядок:** Зберігає елементи **в порядку їх додавання**.
>   * **Продуктивність:** Трохи повільніший за `HashMap` через накладні витрати на підтримку двозв'язного списку, але все ще має **O(1)** для основних операцій.
>
> * **`TreeMap`:**
>   * **Порядок:** Зберігає елементи **відсортованими** за ключами (природне сортування або за допомогою `Comparator`).
>   * **Продуктивність:** **Повільніший**. Операції `put`, `get`, `remove` виконуються за **O(log n)**, оскільки він базується на червоно-чорному дереві.
>
> **Вибір:**
>
> * `HashMap` — коли потрібна максимальна швидкість і порядок не важливий.
> * `LinkedHashMap` — коли важливий порядок вставки.
> * `TreeMap` — коли потрібні відсортовані за ключем дані.

#### **📌 Що перевіряють:**

*Знання основних реалізацій `Map` та вміння вибрати потрібну залежно від вимог до порядку та продуктивності.*

* * *

### **8 ❓ Питання:**

Яка початкова ємність (initial capacity) та коефіцієнт завантаження (load factor) у `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> * **Початкова ємність (`initial capacity`):**
>   * Це початковий розмір внутрішнього масиву "кошиків" (buckets).
>   * За замовчуванням дорівнює **16**.
>
> * **Коефіцієнт завантаження (`load factor`):**
>   * Це значення (від 0.0 до 1.0), яке визначає, наскільки заповненою може бути `HashMap`, перш ніж її ємність буде автоматично збільшена (операція `resize`).
>   * За замовчуванням дорівнює **0.75**.
>
> **Що це означає:**
>
> Коли кількість елементів у `HashMap` досягає `capacity * load_factor` (для значень за замовчуванням: 16 * 0.75 = 12), `HashMap` виконає операцію розширення, зазвичай подвоюючи свою ємність.
>
> Ці параметри можна задавати в конструкторі (`new HashMap<>(initialCapacity, loadFactor)`) для оптимізації, якщо ви заздалегідь знаєте приблизну кількість елементів.

#### **📌 Що перевіряють:**

*Знання параметрів конфігурації `HashMap` та їхнього впливу на продуктивність.*

* * *

### **9 ❓ Питання:**

Як `HashMap` обробляє ключ `null`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `HashMap` має спеціальну логіку для обробки `null` як ключа.
>
> 1. Хеш-код для `null` ключа завжди вважається **0**.
> 2. Тому запис з ключем `null` **завжди** потрапляє в **кошик (bucket) з індексом 0**.
>
> Коли ви викликаєте `put(null, value)`, `HashMap` не обчислює `null.hashCode()`, щоб уникнути `NullPointerException`, а просто використовує індекс 0.
>
> Коли ви викликаєте `get(null)`, він так само відразу перевіряє нульовий кошик.
>
> У кошику з індексом 0 можуть бути й інші записи (через колізії ключів, `hashCode()` яких дає 0), тому `HashMap` ітерується по ньому, порівнюючи ключ напряму `key == null`.
>
> Оскільки може бути лише **один** ключ `null`, будь-яке повторне додавання з ключем `null` просто перезапише попереднє значення.

#### **📌 Що перевіряють:**

*Детальне знання внутрішньої реалізації, зокрема, спеціальної обробки `null` ключів.*

* * *

### **10 ❓ Питання:**

Поясніть процес `resize` (або `rehash`) у `HashMap`. Коли він відбувається? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Resize`** — це процес збільшення ємності внутрішньої хеш-таблиці.
>
> **Коли він відбувається:**
>
> Він відбувається, коли кількість елементів у `HashMap` перевищує поріг, який обчислюється як `capacity * load_factor`.
>
> **Як він працює:**
>
> 1. Створюється **новий масив** "кошиків", ємність якого зазвичай **вдвічі** більша за стару.
> 2. Далі відбувається **перехешування (rehashing)**: `HashMap` **ітерує по всім існуючим записам** у старій таблиці.
> 3. Для кожного запису **переобчислюється індекс** його кошика на основі **нової ємності**.
> 4. Запис переміщується у відповідний кошик у **новій таблиці**.
>
> Це "дорога" операція зі складністю **O(n)**, де `n` — кількість елементів, оскільки потрібно пройти по всім записам. Однак, оскільки вона відбувається рідко, амортизована складність `put` залишається O(1).

#### **📌 Що перевіряють:**

*Розуміння динамічної природи `HashMap` та механізму `rehash` як інструменту для підтримки низького рівня колізій.*

* * *

### **11 ❓ Питання:**

Що нового з'явилося в реалізації `HashMap` в Java 8, що стосується обробки колізій? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У Java 8 було введено важливу оптимізацію для обробки **великої кількості колізій**.
>
> * **До Java 8:** Коли кілька елементів потрапляли в один кошик, вони зберігалися там у вигляді **зв'язаного списку (LinkedList)**. Якщо в кошику накопичувалося багато елементів, пошук у ньому вироджувався до лінійного, **O(n)**.
>
> * **З Java 8:**
>   * Кошик все ще починається як зв'язаний список.
>   * Але якщо кількість елементів у цьому кошику досягає певного порогу (`TREEIFY_THRESHOLD`, за замовчуванням **8**), зв'язаний список **перетворюється на збалансоване червоно-чорне дерево**.
>
> **Що це дає:**
>
> Складність пошуку елемента всередині такого "переповненого" кошика зменшується з **O(n)** до **O(log n)**. Це значно покращує продуктивність `HashMap` у **найгіршому випадку** і робить її більш стійкою до поганих хеш-функцій та атак через хеш-колізії.

#### **📌 Що перевіряють:**

*Знання еволюції JDK. Розуміння того, як Java 8 покращила продуктивність `HashMap` у найгіршому випадку.*

* * *

### **12 ❓ Питання:**

Чому `String` є хорошим кандидатом для ключа в `HashMap`?

> #### **💬 Відповідь:**
>
> `String` є чудовим кандидатом для ключа з кількох причин:
>
> 1. **Імутабельність (Immutability):** `String` є незмінним. Це **найважливіша** властивість. Оскільки його стан не можна змінити, його `hashCode()` та результат `equals()` завжди будуть однаковими. Це гарантує, що ми не "втратимо" ключ у мапі.
>
> 2. **Коректна реалізація `equals()` та `hashCode()`:** `String` має правильну, добре продуману реалізацію цих методів, яка базується на вмісті рядка. `hashCode()` для `String` також забезпечує хороший розподіл, що мінімізує колізії.
>
> 3. **Кешування `hashCode()`:** Клас `String` кешує свій хеш-код. Він обчислюється один раз при першому виклику `hashCode()` і потім зберігається у внутрішньому полі. Наступні виклики миттєво повертають це значення, що підвищує продуктивність при використанні рядків як ключів.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про вимоги до ключів (`імутабельність`, `equals`/`hashCode`) до конкретного популярного класу, як-от `String`.*

* * *

### **13 ❓ Питання:**

Чому мутабельний об'єкт (наприклад, `StringBuilder` або власний клас) є поганим кандидатом для ключа в `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> Використання мутабельного об'єкта як ключа є дуже **небезпечним** і призводить до того, що ви можете **"втратити"** дані в `HashMap`.
>
> **Сценарій:**
>
> 1. Ви створюєте мутабельний об'єкт-ключ: `MutableKey key = new MutableKey("initial_data");`.
> 2. Ви додаєте запис у мапу: `map.put(key, "value");`. `HashMap` обчислює його хеш-код і розміщує запис у відповідний кошик.
> 3. **Потім ви змінюєте стан ключа:** `key.setData("new_data");`.
> 4. Це призводить до того, що **його `hashCode()` змінюється**.
> 5. Коли ви намагаєтеся отримати значення `map.get(key)`, `HashMap` обчислює **новий** хеш-код і шукає запис у **новому**, неправильному кошику.
>
> **Результат:** `map.get(key)` поверне `null`, хоча запис все ще фізично знаходиться в мапі у **старому** кошику. Ви більше не зможете отримати доступ до цього значення за допомогою даного ключа.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, чому імутабельність є критично важливою властивістю для ключів `HashMap`.*

* * *

### **14 ❓ Питання:**

Назвіть атомарні, потокобезпечні операції "перевір-і-зміни" в `ConcurrentHashMap`, яких немає в `HashMap`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `HashMap` не надає атомарних операцій, які поєднують перевірку та зміну. Виклик `if(!map.containsKey(k)) map.put(k, v)` не є потокобезпечним.
>
> `ConcurrentHashMap` надає кілька таких методів:
>
> 1. **`putIfAbsent(K key, V value)`**:
>     * Атомарно додає запис, тільки якщо ключа ще немає.
>
> 2. **`replace(K key, V oldValue, V newValue)`**:
>     * Атомарно замінює значення, тільки якщо поточне значення дорівнює `oldValue`.
>
> 3. **`compute(K key, BiFunction remappingFunction)`**:
>     * Найпотужніший метод. Атомарно обчислює нове значення на основі ключа та поточного значення.
>     * `map.compute(key, (k, v) -> v == null ? 1 : v + 1);` — ідеально для реалізації потокобезпечного лічильника.
>
> 4. **`merge(K key, V value, BiFunction remappingFunction)`**:
>     * Атомарно об'єднує нове значення з існуючим, якщо ключ присутній.
>
> Ці методи дозволяють писати складну логіку оновлення без необхідності у зовнішніх `synchronized` блоках.

#### **📌 Що перевіряють:**

*Знання сучасного API `ConcurrentHashMap` та його переваг для написання ефективного конкурентного коду.*

* * *

### **15 ❓ Питання:**

Як `HashMap` реалізований "під капотом"? Опишіть основні поля та процес `put`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Основні поля:**
>
> * `transient Node<K,V>[] table;`: Внутрішній масив, який є хеш-таблицею. Кожен елемент є "кошиком" (bucket). `Node` — це вкладений клас, що зберігає `hash`, `key`, `value` та посилання `next`.
> * `transient Set<Map.Entry<K,V>> entrySet;`: Кешований `entrySet`.
> * `transient int size;`: Кількість записів у мапі.
> * `transient int modCount;`: Лічильник модифікацій для `fail-fast` ітераторів.
> * `int threshold;`: Поріг, при якому відбувається розширення (`capacity * loadFactor`).
> * `final float loadFactor;`: Коефіцієнт завантаження.
>
> **Процес `put(K key, V value)` (спрощено):**
>
> 1. **Обчислити хеш:** Викликається `key.hashCode()` (з перевіркою на `null` ключ), і результат додатково хешується власною функцією `HashMap` для кращого розподілу.
> 2. **Визначити індекс кошика:** `index = (capacity - 1) & hash`. Це ефективна заміна операції `%`.
> 3. **Перевірити кошик:**
>     * **Якщо кошик порожній:** Створюється новий `Node` і розміщується в ньому.
>     * **Якщо кошик не порожній:**
>         a.  Перевіряється перший елемент у кошику. Якщо його хеш та ключ збігаються (`equals()`) з новим, його значення перезаписується.
>         b.  Якщо ні, `HashMap` **ітерує** по елементах у кошику (який є зв'язаним списком або червоно-чорним деревом).
>         c.  Для кожного елемента перевіряється збіг хешу та ключа. Якщо знайдено — значення перезаписується.
>         d.  Якщо до кінця ітерації збігу не знайдено — новий `Node` додається в кінець списку (або у дерево).
> 4. **Перевірити розмір:** Після додавання нового елемента, `size` інкрементується, і перевіряється, чи не перевищено поріг (`size > threshold`). Якщо так, викликається метод `resize()`.

#### **📌 Що перевіряють:**

*Дуже глибоке, детальне розуміння внутрішньої реалізації `HashMap`. Це питання дозволяє відрізнити кандидата, який просто знає API, від того, хто розуміє, як він працює всередині.*
