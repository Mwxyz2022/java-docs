---
layout: default
title: "Ресайзинг (зміна розміру) HashMap"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 6
---

# Ресайзинг (зміна розміру) HashMap

* короткий опис: Ресайзинг – це процес автоматичного збільшення розміру внутрішнього масиву бакетів (`capacity`), що супроводжується повторним розміщенням (`rehashing`) наявних елементів, коли кількість записів перевищує поріг (`threshold = capacity * loadFactor`).

* поняття: `capacity`, `load factor` (0.75 за замовчуванням), `threshold`, `resize()`, `rehashing`.

* приклади: Збільшення масиву, коли кількість елементів > `threshold` (наприклад, 13-й елемент при `capacity=16`, `loadFactor=0.75`).

* ключові моменти: `capacity` зазвичай подвоюється. `Rehashing` – дорога операція O(N). Часті ресайзинги можуть погіршити продуктивність.

* що важливо знати на співбесіді: Коли і як відбувається `resize`, навіщо `load factor`.

### **✅ Офіційне визначення:**

**Ресайзинг** (**resize**) в **HashMap** — це процес автоматичного збільшення розміру внутрішнього масиву бакетів, що супроводжується повторним розміщенням (**rehashing**) наявних елементів, коли кількість записів перевищує певний поріг (**`threshold`**), розрахований як **`capacity * loadFactor`**.

### **🧠 Простими словами:**

Якщо твій шафовий ящик **HashMap** заповнений на 75%, Java каже: "Час купувати більшу шафу\!" І переміщає всі речі в нову шафу, перерахувавши адреси

---

## **Основне**

| Поняття | Опис |
| :---: | :---: |
| **capacity** | Поточний розмір масиву бакетів (початково 16\) |
| **loadFactor** | Допустиме співвідношення заповнення бакетів (типово 0.75) |
| **threshold** | **`capacity * loadFactor`**, межа для запуску ресайзу |
| **resize()** | Метод, який створює новий масив у 2 рази більший та переміщує дані |

---

## **Пояснення на пальцях (💡)**

Уяви собі книжкову полицю (capacity \= 16), яка тріщить від книг. Якщо вона заповнена більше ніж на 75% (12 книг), ти купуєш нову полицю вдвічі більшу (32), і акуратно перекладаєш кожну книгу (елемент **HashMap**), перераховуючи на яку полицю її тепер класти (**`hash % newCapacity`**)

---

## **Корисні приклади (🧪)**

```java
Map<String, String> map = new HashMap<>(4, 0.75f);

map.put("A", "Alpha");
map.put("B", "Bravo");
map.put("C", "Charlie");
// При додаванні 4-го елемента -- буде ресайзинг
map.put("D", "Delta");
```

📌 Capacity \= 4

📌 Load factor \= 0.75

📌 Threshold \= 4 \* 0.75 \= 3

-> Після додавання 4-го елементу — викликається resize(), capacity стає 8
---

## **Порівняння (⚖️)**

| Ознака | До ресайзингу | Після ресайзингу |
| :---: | :---: | :---: |
| **Розмір масиву** | Початковий (напр., 16\) | Подвоєний (напр., 32\) |
| **Пошук** | Швидкий (O(1)) | Швидкий, якщо мало колізій |
| **Вартість** | Низька | Тимчасово висока (O(n)) |
| **Розміщення** | Старі бакети | Рехешинг усіх елементів |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Load у 75%? — Resize і рехешай\!"**

Якщо вантаж мапи перевищив 3/4 — настав час подвоїти місткість і перерозкласти речі.

---

## **🟩 Підсумок:**

* HashMap автоматично змінює розмір при перевищенні порогового значення **`threshold`**.
* Threshold \= **`capacity * loadFactor`**. Стандартно: 0.75.
* Ресайзинг створює новий масив у 2 рази більший і **перерозміщує всі пари**.
* Часті ресайзинги — дорогоцінні в плані продуктивності, тому важливо одразу правильно виставляти **`initialCapacity`**.

---

### **🔗 Додаткові джерела:**

* [**Java Docs — HashMap**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке ресайзинг (зміна розміру) в `HashMap` і коли він відбувається?

> #### **💬 Відповідь:**
>
> **Ресайзинг (`resize`)** — це процес автоматичного **збільшення ємності (capacity)** внутрішнього масиву "кошиків" (buckets), який відбувається, коли `HashMap` стає занадто заповненою.
>
> **Коли він відбувається:**
>
> Ресайзинг запускається, коли кількість елементів (`size`) у мапі **перевищує порогове значення (`threshold`)**.
>
> Цей поріг обчислюється за формулою: **`threshold = capacity * loadFactor`**.
>
> * `capacity` — поточна ємність масиву кошиків (за замовчуванням 16).
> * `loadFactor` — коефіцієнт завантаження (за замовчуванням 0.75).
>
> Для значень за замовчуванням, ресайзинг відбудеться, коли ви спробуєте додати **13-й елемент** (оскільки `16 * 0.75 = 12`, і після додавання 12-го елемента `size` стає рівним `threshold`, а при 13-му — перевищує).

#### **📝 Аналогія:**

Це як офіціант у кафе. Коли він бачить, що столи заповнені на 75%, він не чекає, поки прийде наступна велика компанія, а заздалегідь готує і відкриває додаткову залу, пересаджуючи деяких гостей для кращого розміщення.

#### **📌 Що перевіряють:**

*Розуміння того, що таке `resize`, та знання умови його запуску (зв'язок `size`, `capacity`, `loadFactor` та `threshold`).*

* * *

### **2 ❓ Питання:**

Як саме відбувається процес ресайзингу "під капотом"?

> #### **💬 Відповідь:**
>
> Процес `resize` складається з кількох кроків і є досить "дорогою" операцією:
>
> 1. **Створення нового масиву:** Створюється новий масив кошиків, ємність якого зазвичай **вдвічі більша** за стару (наприклад, з 16 до 32).
>
> 2. **Обчислення нового порогу:** Розраховується новий `threshold` на основі нової ємності.
>
> 3. **Перехешування (Rehashing):** Це найважливіший крок. `HashMap` **проходить по кожному існуючому запису** (`Node`) у старій таблиці.
>
> 4. **Перерозподіл:** Для кожного запису **переобчислюється індекс** його кошика, але вже на основі **нової, більшої ємності**.
>
> 5. **Розміщення в новій таблиці:** Запис переміщується у відповідний кошик у **новій** таблиці. Елементи, що раніше були в одному кошику через колізію, тепер можуть потрапити в різні.
>
> 6. Стара таблиця стає доступною для збирача сміття.

#### **📌 Що перевіряють:**

*Знання повного алгоритму `resize`, зокрема ключового етапу — перехешування та перерозподілу всіх елементів.*

* * *

### **3 ❓ Питання:**

Яка асимптотична складність (Big O) операції ресайзингу?

> #### **💬 Відповідь:**
>
> Складність операції `resize` є **лінійною** і залежить від кількості елементів (`n`) у мапі — **O(n)**.
>
> **Чому O(n)?**
>
> Тому що під час ресайзингу потрібно **пройти по кожному з `n` існуючих записів**, переобчислити їхній індекс і помістити в нову таблицю. Чим більше елементів у мапі, тим довше триватиме цей процес.
>
> Хоча ця операція сама по собі є повільною, вона відбувається відносно рідко. Завдяки тому, що ємність збільшується експоненційно (подвоюється), "вартість" ресайзингу "розмазується" по всім попереднім швидким операціям `put`. Тому **амортизована** складність `put` залишається **O(1)**.

#### **📌 Що перевіряють:**

*Знання асимптотичної складності `resize` та розуміння концепції амортизованої складності.*

* * *

### **4 ❓ Питання:**

Що таке `load factor` (коефіцієнт завантаження) і як він впливає на продуктивність `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> **`Load factor`** — це число (за замовчуванням 0.75), яке визначає, наскільки заповненою може бути `HashMap`, перш ніж її розмір буде збільшено.
>
> Він є ключовим параметром для **балансування між використанням пам'яті та продуктивністю (кількістю колізій)**.
>
> * **Високий `load factor` (наприклад, 0.9):**
>   * **Перевага:** Економить пам'ять. `HashMap` буде розширюватися рідше.
>   * **Недолік:** Збільшує кількість колізій, оскільки дані "ущільнюються" в меншій кількості кошиків. Це погіршує продуктивність операцій `get`/`put`.
>
> * **Низький `load factor` (наприклад, 0.6):**
>   * **Перевага:** Зменшує кількість колізій. Дані більш "розріджені", що підвищує продуктивність.
>   * **Недолік:** Використовує більше пам'яті. Операції `resize` будуть відбуватися частіше.
>
> Значення за замовчуванням **0.75** вважається оптимальним компромісом для більшості випадків.

#### **📌 Що перевіряють:**

*Розуміння ролі `load factor` як інструменту для налаштування компромісу між пам'яттю та швидкістю.*

* * *

### **5 ❓ Питання:**

Як можна уникнути або зменшити кількість операцій ресайзингу? ⭐️

> #### **💬 Відповідь:**
>
> Уникнути частих операцій ресайзингу можна, якщо ви **заздалегідь знаєте** приблизну кількість елементів, які будете зберігати.
>
> Для цього потрібно використовувати **конструктор `HashMap`, який приймає початкову ємність (`initial capacity`)**.
>
> ```java
> int expectedSize = 1_000_000;
> // Створюємо мапу з достатньою ємністю, щоб вмістити всі елементи без ресайзингу
> Map<String, Integer> map = new HashMap<>(expectedSize);
> ```
>
> **Як правильно розрахувати `initialCapacity`?**
>
> Слід враховувати `load factor`. Якщо ви очікуєте `N` елементів, то початкову ємність слід встановити як `(int) (N / loadFactor) + 1`, щоб поріг `threshold` був більшим за `N`.
>
> `new HashMap<>((int) (1_000_000 / 0.75f) + 1);`
>
> Однак `HashMap` всередині все одно округлить це значення до найближчого вищого степеня двійки. Тому часто достатньо просто передати очікуваний розмір, а `HashMap` підбере відповідну ємність.
>
> Це дуже важлива оптимізація при роботі з великими обсягами даних, оскільки вона дозволяє уникнути "дорогих" операцій `resize`.

#### **📌 Що перевіряють:**

*Практичні знання про те, як оптимізувати `HashMap`, правильно задаючи початкову ємність.*

* * *

### **6 ❓ Питання:**

Чому `HashMap` збільшує свою ємність зазвичай **вдвічі**? ⭐️

> #### **💬 Відповідь:**
>
> Подвоєння ємності є ефективною стратегією з кількох причин:
>
> 1. **Підтримка `capacity` як степеня двійки:**
>     * Як ми знаємо, `HashMap` використовує оптимізацію `index = hash & (capacity - 1)`. Цей трюк працює тільки, якщо `capacity` є степенем двійки. Подвоєння ( `<< 1`) — це найпростіший спосіб гарантувати, що нова ємність також буде степенем двійки.
>
> 2. **Оптимізація перехешування:**
>     * Коли `capacity` подвоюється, нова бітова маска (`newCapacity - 1`) отримує один додатковий біт.
>     * Це означає, що при перерозподілі елементів кожен елемент зі старого кошика може залишитися у **тому ж самому індексі** або переміститися на **індекс `oldIndex + oldCapacity`**. Не потрібно обчислювати індекс повністю з нуля. Це значно спрощує та прискорює логіку `rehash`.
>
> 3. **Амортизована складність:**
>     * Експоненційне зростання (подвоєння) є ключем до того, щоб амортизована складність `put` залишалася O(1).

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як вибір стратегії розширення (подвоєння) пов'язаний з іншими оптимізаціями в `HashMap`.*

* * *

### **7 ❓ Питання:**

Уявіть, що ви додаєте в `HashMap` новий елемент, який спричиняє колізію, і при цьому `size` перевищує `threshold`. Що станеться першим: обробка колізії чи ресайзинг? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Першим станеться **ресайзинг**.
>
> Логіка методу `putVal` в `HashMap` (який виконує основну роботу) побудована так:
>
> 1. Обчислюється хеш та індекс кошика.
> 2. Якщо кошик порожній, просто додається новий вузол.
> 3. Якщо кошик не порожній (колізія!), `HashMap` ітерує по ньому для пошуку існуючого ключа.
> 4. **Після** додавання нового елемента (або оновлення старого, але тут розглядаємо додавання) інкрементується `size`.
> 5. І **тільки тоді** відбувається перевірка: `if (++size > threshold)`.
>
> **Однак** в `putVal` перед додаванням, є перевірка `if(tab==null)` яка приведе до `resize()`, після додавання нового `node` перевіряється, чи треба перетворювати ланцюжок в дерево (`treeifyBin`). Логіка трохи запутана, якщо викличеться `resize()` з `putVal()`, то перерозподіляються ноди. І тільки тоді додається новий елемент, або апдейтиться старий.

#### **📌 Що перевіряють:**

*Дуже детальне знання реалізації `HashMap`. Здатність проаналізувати порядок виконання операцій у складному методі.*

* * *

### **8 ❓ Питання:**

Як впливає на ресайзинг якість `hashCode()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якість `hashCode()` напряму не впливає на те, **коли** відбудеться ресайзинг (це залежить від `size` та `load factor`), але вона сильно впливає на **ефективність `HashMap` після ресайзингу**.
>
> * **При `resize`** всі елементи перерозподіляються по новій, більшій таблиці.
> * Якщо `hashCode()` хороший і дає рівномірний розподіл, то після ресайзингу елементи, які раніше були в колізії, **потраплять у різні кошики**. Це значно зменшить довжину ланцюжків/дерев і підвищить продуктивність.
> * Якщо `hashCode()` поганий (наприклад, повертає константу), то навіть після ресайзингу **всі елементи знову потраплять в один і той самий кошик**, і збільшення `capacity` не дасть жодного виграшу в продуктивності.
>
> Таким чином, `resize` ефективно бореться з колізіями тільки за умови хорошої хеш-функції.

#### **📌 Що перевіряють:**

*Розуміння того, що ресайзинг та якість хеш-функції — це два взаємодоповнюючі механізми для підтримки ефективності `HashMap`.*

* * *

### **9 ❓ Питання:**

Чи можна змінити `load factor` для вже існуючої `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, `load factor` встановлюється **тільки в конструкторі** і не може бути змінений для вже існуючого екземпляра `HashMap`.
>
> Це поле є `final` в реалізації `HashMap`: `final float loadFactor;`.
>
> Якщо вам потрібно змінити цю характеристику для існуючої мапи, єдиний спосіб — **створити нову `HashMap`** з потрібним `load factor` і скопіювати в неї всі дані зі старої мапи.
>
> `Map<K, V> newMap = new HashMap<>(oldMap.size(), newLoadFactor);`
> `newMap.putAll(oldMap);`
>
> Така поведінка є логічною, оскільки `load factor` напряму впливає на поріг `threshold` і момент `resize`, що є фундаментальними характеристиками таблиці.

#### **📌 Що перевіряють:**

*Знання API `HashMap` та його обмежень. Уважність до `final`-полів.*

* * *

### **10 ❓ Питання:**

Припустимо, у вас `HashMap` з `capacity=16` та `loadFactor=0.75`. На якому етапі почнеться перехід кошика на дерево? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом, яке перевіряє розуміння **двох різних умов**.
>
> 1. Перехід кошика зі зв'язаного списку на червоно-чорне дерево відбувається, коли кількість елементів **в одному конкретному кошику** досягає **`TREEIFY_THRESHOLD`**, який дорівнює **8**.
>
> 2. **Але є друга умова:** цей перехід відбудеться, тільки якщо загальна ємність (`capacity`) всієї `HashMap` **більша або дорівнює `MIN_TREEIFY_CAPACITY`**, яка дорівнює **64**.
>
> **Що це означає для нашого випадку (`capacity=16`):**
>
> * Навіть якщо в один кошик потраплять 8, 9, 10 або більше елементів, **перетворення на дерево не відбудеться**, тому що загальна ємність (16) менша за 64.
> * Замість цього, коли `size` досягне `12` (`16 * 0.75`), відбудеться **`resize`**. `capacity` збільшиться до 32.
> * Після `resize` елементи перерозподіляться, і довжина ланцюжка в кошику, ймовірно, зменшиться.
>
> Отже, в даній ситуації `HashMap` скоріше виконає `resize`, ніж перетворить кошик на дерево.
> Цей механізм запобігає перетворенню на дерево у дуже маленьких мапах, де розширення є більш ефективною стратегією.

#### **📌 Що перевіряють:**

*Дуже глибоке знання деталей реалізації Java 8, включаючи `MIN_TREEIFY_CAPACITY`. Це питання для рівня Senior.*

* * *

### **11 ❓ Питання:**

Чому ініціалізація `new HashMap<>(1000)` не є такою ж ефективною, як може здатися на перший погляд? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що `HashMap` **округлить** передану ємність до **найближчого вищого степеня двійки**.
>
> 1. Ви передаєте `1000` в конструктор.
> 2. Внутрішній алгоритм знайде найменший степінь двійки, який є більшим або рівним `1000`.
> 3. `2^9 = 512` (замало).
> 4. `2^10 = 1024` (підходить).
>
> В результаті, буде створено внутрішній масив з ємністю **1024**, а не 1000. Це може призвести до невеликої надлишкової витрати пам'яті, якщо вам потрібно було рівно 1000 елементів.
>
> Для точного контролю ініціалізації потрібно використовувати формулу `(int) (expectedSize / loadFactor) + 1` і пам'ятати про це округлення, якщо ви оптимізуєте використання пам'яті до останнього байта.

#### **📌 Що перевіряють:**

*Знання про те, що `HashMap` завжди працює з ємністю, що є степенем двійки, і як це впливає на ініціалізацію.*

* * *

### **12 ❓ Питання:**

У `ConcurrentHashMap` також є ресайзинг. Чим він відрізняється від ресайзингу в `HashMap` з точки зору блокувань? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ресайзинг у `ConcurrentHashMap` є значно складнішим і розроблений так, щоб **мінімізувати блокування**.
>
> * **У `HashMap`:** Ресайзинг є "stop-the-world" операцією для цієї мапи. Ніякі інші операції не можуть виконуватися, поки ресайзинг не завершено.
>
> * **У `ConcurrentHashMap`:**
>   * Процес ресайзингу є **інкрементальним та паралельним**.
>   * Коли один потік ініціює `resize`, він починає переміщувати кошики зі старої таблиці в нову.
>   * **Інші потоки** можуть йому **допомагати**. Якщо інший потік намагається виконати `put` і бачить, що відбувається `resize`, він може "допомогти" перемістити кілька кошиків, перш ніж виконати свою операцію.
>   * Операції **читання (`get`)** можуть продовжуватися паралельно, повертаючи дані або зі старої, або з нової таблиці, залежно від того, чи відповідний кошик вже було переміщено.
>
> Таким чином, `ConcurrentHashMap` не "завмирає" повністю під час ресайзингу, що забезпечує значно вищу доступність та пропускну здатність у високонавантажених багатопотокових середовищах.

#### **📌 Що перевіряють:**

*Розуміння того, як конкурентні колекції реалізують складні операції, як-от `resize`, у потокобезпечний та ефективний спосіб.*

* * *

### **13 ❓ Питання:**

Що таке `threshold` і як він обчислюється?

> #### **💬 Відповідь:**
>
> **`threshold` (поріг)** — це поле в `HashMap`, яке зберігає максимальну кількість елементів, яку може містити мапа, перш ніж відбудеться ресайзинг.
>
> Він обчислюється за простою формулою:
>
> **`threshold = capacity * loadFactor`**
>
> Де:
>
> * `capacity` — поточна ємність внутрішнього масиву.
> * `loadFactor` — коефіцієнт завантаження.
>
> **Приклад:**
>
> * При створенні `new HashMap()` `capacity` = 16, `loadFactor` = 0.75.
> * `threshold` буде дорівнювати `16 * 0.75 = 12`.
> * Коли `size` стане 12, і ви спробуєте додати 13-й елемент, буде запущено `resize`. `capacity` стане 32, а новий `threshold` буде `32 * 0.75 = 24`.

#### **📌 Що перевіряють:**

*Знання базової термінології та формули, що керує ресайзингом.*

* * *

### **14 ❓ Питання:**

Чи може `loadFactor` бути більшим за 1.0? Якщо так, то що це означатиме? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, **технічно** ви можете передати в конструктор `loadFactor` більший за 1.0, наприклад, 2.0.
>
> **Що це означатиме:**
>
> * `threshold` буде дорівнювати `capacity * 2.0`. Це означає, що поріг для ресайзингу стане **вищим**, ніж сама ємність таблиці.
> * **Ресайзинг практично ніколи не відбудеться.**
> * Всі елементи будуть додаватися в таблицю фіксованого розміру.
>
> **Наслідки:**
>
> * Це призведе до **дуже великої кількості колізій**.
> * Довжина ланцюжків/дерев у кошиках буде постійно зростати.
> * Продуктивність `HashMap` сильно **деградує** і наблизиться до O(n) або O(log n).
>
> Це є **дуже поганою практикою** і свідчить про непорозуміння того, як працює `HashMap`. Це приклад екстремального компромісу на користь економії пам'яті ціною повної втрати продуктивності.

#### **📌 Що перевіряють:**

*Здатність мислити про "крайні випадки" та аналізувати, до яких наслідків вони призводять.*

* * *

### **15 ❓ Питання:**

Коли елементи в `HashMap` перерозподіляються, чи зберігається порядок елементів усередині кошика, якщо він був зв'язаним списком? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не обов'язково.** Порядок елементів всередині кошиків після `resize` може змінитися.
>
> При перехешуванні `HashMap` ітерує по вузлах у старому кошику і розміщує їх у новій таблиці. При цьому логіка розбиває старий ланцюжок на **два нових** (один для старого індексу, інший — для `oldIndex + oldCapacity`) і **зберігає відносний порядок елементів у цих нових ланцюжках**.
>
> Однак сам **абсолютний порядок** елементів (якщо розглядати всю `HashMap`) може змінитися. Наприклад, елемент, який був третім у ланцюжку, може стати першим у новому.
>
> Це ще раз підкреслює, що `HashMap` **не гарантує жодного порядку ітерації**, і цей порядок може непередбачувано змінитися після `resize`.
>
> На відміну від `HashMap`, **`LinkedHashMap`** при ресайзингу **зберігає** загальний порядок ітерації (порядок вставки), оскільки він додатково підтримує окремий двозв'язний список, який не залежить від кошиків.

#### **📌 Що перевіряють:**

*Дуже детальні знання про реалізацію `resize` та його вплив на внутрішню структуру. Розуміння відмінностей між `HashMap` та `LinkedHashMap` на цьому рівні.*
