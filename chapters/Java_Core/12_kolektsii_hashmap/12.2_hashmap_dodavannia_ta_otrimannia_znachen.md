---
layout: default
title: "HashMap: Додавання та отримання значень"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 2
---

# HashMap: Додавання та отримання значень

* короткий опис: Основні методи `put(K key, V value)` для додавання/оновлення пар та `get(Object key)` для отримання значення за ключем. `put()` повертає старе значення або `null`, `get()` – значення або `null` для відсутнього ключа.

* поняття: `put()`, `get()`, `containsKey()`, `containsValue()`.

* приклади: `daysInMonth.put("January", 31);`, `daysInMonth.get("February");` (поверне `28`), `daysInMonth.get("December");` (поверне `null`).

* ключові моменти: Ключі унікальні, `put()` перезаписує значення, `get()` повертає `null` для неіснуючих ключів (не викидає виняток).

* що важливо знати на співбесіді: Алгоритм доступу до елементів, поведінка з неіснуючими ключами.

### **✅ Офіційне визначення:**

Методи **`put(K key, V value)`** та **`get(Object key)`** є основними для додавання та отримання значень у структурі **`HashMap`**. Вони реалізують базову функціональність зберігання пари ключ–значення.

### **🧠 Простими словами:**

* **`put`** — кладеш щось у мапу: ключ \+ значення.
* **`get`** — запитуєш значення по ключу. Якщо такого ключа немає — отримаєш **`null`**.

---

## **📊 Основні методи для роботи з елементами HashMap:**

| Метод | Опис | Повертає |
| :---: | :---: | :---: |
| **`put(K, V)`** | Додає нову пару або оновлює значення для ключа | Старе значення або **`null`** |
| **`get(key)`** | Повертає значення за ключем | Значення або **`null`** |
| **`containsKey(key)`** | Перевіряє наявність ключа | **`true`** / **`false`** |
| **`containsValue(val)`** | Перевіряє наявність значення | **`true`** / **`false`** |

---

## **Пояснення на пальцях (💡)**

Уяви собі велику шафу (**HashMap**), де в кожному ящику (**ключ**) зберігається якась штука (**значення**).

* Коли ти використовуєш **`put`**, ти кладеш щось у конкретний ящик.
* Коли ти використовуєш **`get`**, ти відкриваєш ящик і дивишся, що там. Якщо ящик порожній або його взагалі немає — отримаєш **`null`**.

---

## **Корисні приклади (🧪)**

```java
Map<String, Integer> daysInMonth = new HashMap<>();
daysInMonth.put("January", 31);
daysInMonth.put("February", 28);
daysInMonth.put("March", 31);

// Отримати значення
System.out.println(daysInMonth.get("February")); // 28

// Якщо ключа немає
System.out.println(daysInMonth.get("December")); // null
```

---

## **Порівняння (⚖️)**

| Особливість | `put` | `get` |
| :---: | :---: | :---: |
| **Призначення** | Додає або оновлює пару | Отримує значення за ключем |
| **Повертає** | Старе значення або **`null`** | Значення або **`null`** |
| **Поведінка при дублюванні** | Перезаписує значення | Просто повертає значення |
| **Якщо ключ не існує** | Створює новий запис | Повертає **`null`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**PUT -> Push data**

**GET -> Grab by key**

Просто уяви, що кладеш (**`put`**) або хапаєш (**`get`**) щось із шафи

---

## **🟩 Підсумок:**

* **`put(K, V)`** додає або оновлює значення.
* **`get(K)`** повертає значення або **`null`**, якщо ключ не знайдено.
* Ключ у **`HashMap`** унікальний — при повторному **`put`** значення перезаписується.
* Важливо: **`get`** **не кидає винятків**, коли ключа немає — повертає **`null`**.

---

### **🔗 Додаткові джерела:**

* [**Java HashMap Documentation (Oracle)**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Опишіть, як працює метод `put(K key, V value)` в `HashMap`.

> #### **💬 Відповідь:**
>
> Метод `put(key, value)` додає або оновлює пару "ключ-значення". Процес "під капотом" складається з кількох кроків:
>
> 1. **Обчислення хешу:** Спочатку `HashMap` обчислює `hashCode()` для переданого `key`. Якщо `key` дорівнює `null`, його хеш-код вважається 0.
>
> 2. **Визначення індексу "кошика" (bucket):** На основі отриманого хеш-коду розраховується індекс кошика у внутрішньому масиві (таблиці). Зазвичай це робиться за формулою `index = (capacity - 1) & hash`.
>
> 3. **Обробка кошика:**
>     * **Якщо кошик порожній:** Створюється новий вузол (`Node`), який містить ключ, значення та хеш, і розміщується в цьому кошику.
>     * **Якщо кошик не порожній (колізія):** `HashMap` починає ітерувати по елементах у цьому кошику (який може бути зв'язаним списком або червоно-чорним деревом).
>         * Для кожного існуючого елемента перевіряється, чи його хеш-код та ключ (через `equals()`) збігаються з новим ключем.
>         * **Якщо збіг знайдено:** Старе значення перезаписується новим, і метод `put` повертає **старе значення**.
>         * **Якщо збігу не знайдено:** Новий вузол `Node` додається в кінець списку (або у дерево) в цьому кошику.
>
> 4. **Перевірка на розширення:** Після додавання нового елемента розмір (`size`) мапи збільшується, і перевіряється, чи не перевищено поріг (`threshold`). Якщо так, викликається метод `resize()`.

#### **📌 Що перевіряють:**

*Розуміння повного алгоритму `put`, включаючи обчислення хешу, визначення індексу, обробку колізій та розширення таблиці.*

* * *

### **2 ❓ Питання:**

Що повертає метод `put()` і як це можна використовувати?

> #### **💬 Відповідь:**
>
> Метод `V put(K key, V value)` повертає **попереднє значення (`V`)**, яке було пов'язане з цим ключем.
>
> * Якщо такого ключа в мапі **раніше не було**, метод повертає **`null`**.
>
> **Як це можна використовувати:**
>
> Це дозволяє легко перевірити, чи був запис оновлений чи створений з нуля.
>
> **Приклад:**
>
> ```java
> Map<String, Integer> wordCount = new HashMap<>();
>
> // ... логіка підрахунку слів
>
> Integer oldCount = wordCount.put("hello", 10);
> if (oldCount == null) {
>     System.out.println("Слово 'hello' додано вперше.");
> } else {
>     System.out.println("Значення для слова 'hello' було оновлено. Старе значення: " + oldCount);
> }
> ```
>
> Ця поведінка також корисна для реалізації кешів, коли потрібно знати, чи було перезаписано існуюче значення.

#### **📌 Що перевіряють:**

*Знання про значення, що повертається методом `put()`, та вміння придумати сценарій його практичного застосування.*

* * *

### **3 ❓ Питання:**

Опишіть, як працює метод `get(Object key)` в `HashMap`.

> #### **💬 Відповідь:**
>
> Метод `get(key)` повертає значення, пов'язане з вказаним ключем. Алгоритм дуже схожий на `put`:
>
> 1. **Обчислення хешу та індексу:** Так само, як і для `put`, обчислюється хеш-код ключа та індекс кошика у внутрішній таблиці.
>
> 2. **Пошук у кошику:**
>     * `HashMap` перевіряє, чи не порожній знайдений кошик. Якщо порожній, ключа точно немає, і метод повертає **`null`**.
>     * Якщо кошик не порожній, `HashMap` починає ітерувати по елементах у ньому.
>     * Для кожного елемента перевіряється, чи його хеш-код та ключ (через `equals()`) збігаються з шуканим ключем.
>     * **Якщо збіг знайдено:** Метод повертає значення (`value`) з цього вузла.
>     * **Якщо після перевірки всього кошика збігу не знайдено:** Ключа в мапі немає, і метод повертає **`null`**.

#### **📌 Що перевіряють:**

*Розуміння того, що `get` використовує той самий механізм хешування, що і `put`, для швидкого доступу до даних.*

* * *

### **4 ❓ Питання:**

Що поверне `map.get(nonExistentKey)`?

> #### **💬 Відповідь:**
>
> Він поверне **`null`**.
>
> Метод `get` в `HashMap` **не кидає виняток**, якщо ключ не знайдено. Він просто повертає `null`, щоб сигналізувати про відсутність такого ключа.
>
> Це одна з важливих особливостей API, оскільки це дозволяє легко перевіряти наявність ключа:
>
> ```java
> String value = map.get("some_key");
> if (value == null) {
>     // Ключа немає (або його значення null)
> }
> ```

#### **📌 Що перевіряють:**

*Знання стандартної поведінки `get` для відсутніх ключів.*

* * *

### **5 ❓ Питання:**

Як надійно перевірити, чи існує ключ у `HashMap`? Чому перевірка `map.get(key) != null` може бути ненадійною? ⭐️

> #### **💬 Відповідь:**
>
> Перевірка `map.get(key) != null` є **ненадійною**, тому що `HashMap` **дозволяє зберігати `null` як значення**.
>
> **Проблема:**
>
> Метод `get(key)` поверне `null` у двох випадках:
>
> 1. Коли **ключа не існує** в мапі.
> 2. Коли **ключ існує**, але його значення дорівнює **`null`**.
>
> Таким чином, `map.get(key) == null` не дозволяє розрізнити ці дві ситуації.
>
> **Надійний спосіб:**
>
> Для перевірки наявності ключа завжди слід використовувати метод **`containsKey(Object key)`**. Він повертає `true`, якщо ключ існує, і `false`, якщо ні, незалежно від того, яке значення з ним пов'язане.
>
> ```java
> Map<String, Integer> map = new HashMap<>();
> map.put("user", null);
>
> // Ненадійна перевірка
> if (map.get("user") == null) {
>     System.out.println("Ключ 'user' відсутній або його значення null"); // Неоднозначно
> }
>
> // Надійна перевірка
> if (map.containsKey("user")) {
>     System.out.println("Ключ 'user' існує!"); // Однозначно
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння неоднозначності, яку створює дозвіл на `null`-значення, та знання про `containsKey` як про правильний спосіб перевірки.*

* * *

### **6 ❓ Питання:**

Порівняйте часову складність `containsKey(key)` та `containsValue(value)`. ⭐️

> #### **💬 Відповідь:**
>
> * **`containsKey(key)`:**
>   * Має складність **O(1)** в середньому.
>   * **Чому:** Цей метод працює так само швидко, як і `get`. Він використовує хеш-код ключа для миттєвого знаходження потрібного кошика, а потім перевіряє елементи тільки в ньому.
>
> * **`containsValue(value)`:**
>   * Має складність **O(n)**.
>   * **Чому:** `HashMap` оптимізований для пошуку за **ключем**, а не за значенням. Щоб перевірити наявність значення, `HashMap` змушений **пройти по всім** своїм записам (`entrySet` або `values`) і порівняти кожне значення з шуканим. Це вимагає повного перебору.
>
> **Висновок:** `containsKey` є дуже швидкою операцією, тоді як `containsValue` — повільною, і її слід уникати у критичних до продуктивності ділянках коду.

#### **📌 Що перевіряють:**

*Розуміння того, що `HashMap` оптимізований виключно для роботи з ключами.*

* * *

### **7 ❓ Питання:**

З Java 8 з'явилися методи `getOrDefault()`, `putIfAbsent()`. Поясніть, що вони роблять. ⭐️

> #### **💬 Відповідь:**
>
> Це нові утилітарні методи, які роблять код більш стислим і виразним.
>
> * **`getOrDefault(Object key, V defaultValue)`:**
>   * Повертає значення для ключа `key`, якщо він існує.
>   * Якщо ключ **не існує**, повертає **`defaultValue`**, яке ви передали.
>   * **Перевага:** Дозволяє уникнути перевірки на `null` і одразу отримати значення за замовчуванням.
>
>     **Замість цього:**
>     `String name = map.get("user"); if (name == null) name = "Guest";`
>     **Пишемо так:**
>     `String name = map.getOrDefault("user", "Guest");`
>
> * **`putIfAbsent(K key, V value)`:**
>   * Додає запис `(key, value)` **тільки в тому випадку, якщо ключа `key` ще немає** в мапі або він пов'язаний з `null`.
>   * **Перевага:** Це **атомарна** операція. Вона вирішує проблему стану гонитви, коли в багатопотоковому середовищі потрібно було б написати `if(!map.containsKey(key)) map.put(key, value)`.
>   * Він також повертає поточне значення, якщо ключ вже існує, або `null`, якщо було додано новий запис.

#### **📌 Що перевіряють:**

*Знання сучасного API Java 8 для `Map` та розуміння переваг цих методів.*

* * *

### **8 ❓ Питання:**

Як ефективно реалізувати лічильник слів у тексті за допомогою `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> **Класичний підхід (до Java 8):**
>
> ```java
> Map<String, Integer> wordCount = new HashMap<>();
> for (String word : words) {
>     Integer count = wordCount.get(word);
>     if (count == null) {
>         wordCount.put(word, 1);
>     } else {
>         wordCount.put(word, count + 1);
>     }
> }
> ```
>
> **Сучасний підхід (з Java 8+), використовуючи `getOrDefault()`:**
>
> ```java
> Map<String, Integer> wordCount = new HashMap<>();
> for (String word : words) {
>     // Отримуємо поточне значення або 0, якщо ключа немає, і додаємо 1
>     wordCount.put(word, wordCount.getOrDefault(word, 0) + 1);
> }
> ```
>
> **Найбільш ефективний та виразний підхід з `compute`:**
>
> ```java
> Map<String, Integer> wordCount = new HashMap<>();
> for (String word : words) {
>     // Атомарно обчислює нове значення
>     wordCount.compute(word, (k, v) -> (v == null) ? 1 : v + 1);
> }
> ```
>
> Варіант з `compute` або `merge` є найбільш сучасним і кращим для реалізації таких завдань.

#### **📌 Що перевіряють:**

*Практичні навички вирішення поширених задач. Здатність написати ефективний код з використанням сучасних методів `Map`.*

* * *

### **9 ❓ Питання:**

Чому ітерація по `map.keySet()` і виклик `map.get()` для кожного ключа є менш ефективним, ніж ітерація по `map.entrySet()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ітерація по `keySet()`:**
>
> ```java
> for (String key : map.keySet()) {
>     Integer value = map.get(key); // (1)
> }
> ```
>
> **Як це працює:**
>
> 1. На кожній ітерації ми отримуємо ключ.
> 2. Потім ми викликаємо `map.get(key)`. Цей виклик **знову виконує пошук** у хеш-таблиці: обчислює хеш-код ключа, знаходить кошик, ітерується по ньому, викликаючи `equals`, щоб знайти потрібний запис. Тобто, для кожного елемента виконується **подвійна робота**: один раз, щоб знайти його в `keySet`, інший — щоб знайти його для `get()`.
>
> **Ітерація по `entrySet()`:**
>
> ```java
> for (Map.Entry<String, Integer> entry : map.entrySet()) {
>     String key = entry.getKey();
>     Integer value = entry.getValue();
> }
> ```
>
> **Як це працює:**
> `entrySet()` повертає множину об'єктів `Map.Entry`, кожен з яких вже **містить і ключ, і значення**. Коли ітератор проходить по `entrySet`, він одразу надає вам **і ключ, і значення без додаткового пошуку**. Ви отримуєте все, що вам потрібно, за одну операцію.
>
> **Висновок:** ітерація по `entrySet` вимагає лише одного проходу по структурі `HashMap`, тоді як ітерація по `keySet` з викликами `get` вимагає `n+1` проходів (один для ітератора, і `n` для `get`), що є значно менш ефективним.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють "види" (views) колекцій (`keySet`, `entrySet`). Здатність аналізувати продуктивність різних підходів до ітерації.*

* * *

### **10 ❓ Питання:**

Що поверне `map.remove(key)`? Чи можна за результатом зрозуміти, чи був такий ключ у мапі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `remove(Object key)` повертає **значення (`V`)**, яке було пов'язане з видаленим ключем. Якщо такого ключа не було, він повертає **`null`**.
>
> **Чи можна зрозуміти, чи був такий ключ?**
>
> **Не завжди.** Існує та сама неоднозначність, що й з `get()`. Повернення `null` може означати одне з двох:
>
> 1. Ключа **не було** в мапі.
> 2. Ключ **був**, але його значення дорівнювало **`null`**.
>
> **Надійний спосіб:**
> Якщо вам потрібно точно знати, чи відбулося видалення, вам слід спочатку перевірити наявність ключа за допомогою **`containsKey()`**:
>
> ```java
> if (map.containsKey(key)) {
>     map.remove(key);
>     // тепер ми точно знаємо, що видалення відбулося
> }
> ```
>
> З Java 8 є більш зручний атомарний метод: **`remove(Object key, Object value)`**, який видаляє запис, тільки якщо ключ пов'язаний з вказаним значенням. Він повертає `boolean`.

#### **📌 Що перевіряють:**

*Уважність до деталей API, зокрема до семантики значень, що повертаються, та обробки `null`.*

* * *

### **11 ❓ Питання:**

Розглянемо `map.values()`. Який тип колекції повертає цей метод і як її зміни впливають на оригінальну мапу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `values()` повертає об'єкт, що реалізує інтерфейс **`Collection<V>`**.
>
> **Важлива особливість:** Ця колекція **не є копією**, а є **"видом" (view)** значень оригінальної мапи.
>
> **Як зміни впливають на мапу?**
>
> * **Видалення елемента з колекції `values` призводить до видалення відповідного запису (ключ-значення) з оригінальної `HashMap`.**
>
>     ```java
>     Map<String, Integer> map = new HashMap<>();
>     map.put("a", 1);
>     map.put("b", 2);
>
>     Collection<Integer> values = map.values();
>     // Видаляємо значення 1 з "виду"
>     values.remove(1);
>
>     System.out.println(map); // Виведе {b=2}. Запис "a"=1 був видалений.
>     ```
>
> * Операція `add` для колекції `values` **не підтримується** і кине `UnsupportedOperationException`, оскільки незрозуміло, який ключ мав би бути доданий.
>
> Це означає, що `keySet()`, `values()` та `entrySet()` надають не копії, а динамічні "види", зміни в яких (там, де це дозволено) відображаються на вихідній мапі.

#### **📌 Що перевіряють:**

*Розуміння концепції "видів" (views) у колекціях та їхнього зв'язку з оригінальною структурою даних.*

* * *

### **12 ❓ Питання:**

Як змінився алгоритм обчислення індексу кошика в `HashMap` з часом? Чому `(h & (length-1))` краще, ніж `h % length`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для обчислення індексу кошика `HashMap` використовує операцію **`hash & (length - 1)`**.
>
> **Чому це краще, ніж `hash % length`?**
>
> Це є мікро-оптимізацією, яка працює **тільки тоді**, коли `length` (ємність таблиці) є **степенем двійки** (16, 32, 64...). `HashMap` завжди підтримує свою ємність як степінь двійки.
>
> **Переваги `&` над `%`:**
>
> 1. **Продуктивність:** Операція бітового "І" (`&`) є **значно швидшою** на рівні процесора, ніж операція ділення за модулем (`%`).
>
> 2. **Кращий розподіл для поганих хеш-функцій:** Коли `length` є степенем двійки, `hash % length` просто бере молодші біти хешу. Якщо у хеш-функції є поганий розподіл у молодших бітах, це призведе до колізій.
>     * `HashMap` перед цим **додатково хешує** отриманий `hashCode` (змішує старші біти з молодшими), щоб покращити розподіл, а потім використовує `& (length - 1)`, що ефективно використовує всі біти.
>
> Отже, це поєднання розумного вибору розміру таблиці та бітових операцій для максимальної продуктивності.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішні оптимізації `HashMap`. Розуміння бітової арифметики та її застосування.*

* * *

### **13 ❓ Питання:**

Яку колекцію `Map` слід використовувати, якщо порядок ітерації має відповідати порядку додавання елементів?

> #### **💬 Відповідь:**
>
> Для цього слід використовувати **`java.util.LinkedHashMap`**.
>
> `LinkedHashMap` є нащадком `HashMap` і має дві основні відмінності:
>
> 1. **Зберігає порядок вставки:** "Під капотом", крім хеш-таблиці, він підтримує **двозв'язний список**, який проходить через усі його записи. Цей список і визначає порядок ітерації.
>
> 2. **Трохи менша продуктивність:** Через накладні витрати на підтримку цього списку, операції `put` та `remove` у `LinkedHashMap` є трохи повільнішими, ніж у `HashMap`, хоча асимптотична складність залишається **O(1)**.
>
> **Використання:** `LinkedHashMap` є ідеальним вибором, коли вам потрібна швидкість хеш-таблиці, але при цьому важливий передбачуваний, стабільний порядок елементів.

#### **📌 Що перевіряють:**

*Знання про існування та призначення `LinkedHashMap`.*

* * *

### **14 ❓ Питання:**

Уявіть, `HashMap` має 10 мільйонів записів. Чи є операція `size()` швидкою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, операція `size()` в `HashMap` є **дуже швидкою** і має складність **O(1)**.
>
> **Чому?**
>
> `HashMap` не перераховує всі елементи кожного разу, коли викликається `size()`. Він підтримує **внутрішнє поле-лічильник** ( `private int size;`).
>
> * При кожному **додаванні** нового елемента (коли ключ справді новий), цей лічильник **інкрементується**.
> * При кожному **видаленні** елемента, лічильник **декрементується**.
>
> Метод `size()` просто **повертає поточне значення цього поля**. Тому його швидкість абсолютно не залежить від кількості елементів у мапі.

#### **📌 Що перевіряють:**

*Розуміння того, що `HashMap` оптимізований для швидкого доступу не тільки до даних, а й до метаданих, як-от розмір.*

* * *

### **15 ❓ Питання:**

Чи можна безпечно ітерувати по `HashMap` і змінювати його в циклі `for-each`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. `HashMap` (як і більшість колекцій з `java.util`) використовує **`fail-fast` ітератори**.
>
> **Що це означає:**
> Якщо ви ітеруєте по `HashMap` (наприклад, по його `entrySet` чи `keySet`) і в цей час структурно змінюєте мапу (через `put` або `remove` самої мапи), ітератор виявить цю зміну і негайно кине `ConcurrentModificationException`.
>
> **Неправильний код:**
>
> ```java
> for (String key : map.keySet()) {
>     if (someCondition(key)) {
>         map.remove(key); // Кине ConcurrentModificationException
>     }
> }
> ```
>
> **Правильний спосіб видалення під час ітерації:**
>
> 1. **Використовувати `Iterator.remove()`:**
>
>     ```java
>     Iterator<Map.Entry<String, Integer>> iterator = map.entrySet().iterator();
>     while (iterator.hasNext()) {
>         Map.Entry<String, Integer> entry = iterator.next();
>         if (someCondition(entry)) {
>             iterator.remove(); // Безпечне видалення
>         }
>     }
>     ```
>
> 2. **Використовувати `removeIf()` (з Java 8):**
>
>     `map.entrySet().removeIf(entry -> someCondition(entry));`
>
> `entrySet()` є кращим для цього, оскільки дозволяє видаляти на основі і ключа, і значення.

#### **📌 Що перевіряють:**

*Знання про `fail-fast` ітератори в `HashMap`. Вміння застосовувати безпечні способи модифікації під час ітерації.*
