---
layout: default
title: "Колізії в HashMap"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 5
---

# Колізії в HashMap

* короткий опис: Колізія – це ситуація, коли два різні ключі мають однаковий хеш-індекс. `HashMap` вирішує їх за допомогою ланцюгової адресації (зв'язаний список) або переходом до червоно-чорного дерева (з Java 8+, при > 8 елементів у бакеті).

* поняття: Колізія, зв'язаний список, червоно-чорне дерево (`TREEIFY_THRESHOLD`).

* приклади: Різні ключі (`FB`, `Ea`) з однаковим `hashCode()`.

* ключові моменти: Колізії допустимі, але їх велика кількість знижує продуктивність. Якість `hashCode()` впливає на частоту колізій.

* що важливо знати на співбесіді: Які бувають колізії, як `HashMap` їх обробляє.

### **✅ Офіційне визначення:**

**Колізія** в **HashMap** — це ситуація, коли два або більше ключів мають однаковий **хеш-індекс** у внутрішньому масиві бакетів, що призводить до необхідності зберігати кілька пар у одному бакеті.

### **🧠 Простими словами:**

**Колізія** — це коли кілька ключів хочуть оселитися в одному й тому ж **«відрі»** (бакеті) через однаковий індекс. Java каже: "Гаразд, усі живіть тут, тільки будьте в рядочок\!"

---

## **📊 Основні типи колізій та методи їх вирішення:**

* **Колізія через однаковий `hashCode()`** — різні об'єкти повертають однакове значення хешу.
* **Колізія через однаковий індекс** — після обчислення **`hashCode % capacity`** кілька ключів отримують той самий індекс.

Методи вирішення:

* **Однозв'язний список** (до Java 8\)
* **Червоно-чорне дерево** (з Java 8, коли кількість елементів у бакеті \> 8\)
* **Перехешування** (при перевищенні **load** **factor** — реорганізація бакетів)

---

## **Пояснення на пальцях (💡)**

Уяви, що ти ділиш кімнати між учасниками конференції на основі останніх двох цифр їхніх паспортів (**`%100`**). Якщо два учасники мають "**42**", вони отримують одну й ту ж кімнату. Щоб не пересварились, їх розміщують у чергу або організовують чергу-дерево

---

## **Корисні приклади (🧪)**

```java
String key1 = "FB";
String key2 = "Ea";

System.out.println(key1.hashCode()); // 2236
System.out.println(key2.hashCode()); // 2236

Map<String, String> map = new HashMap<>();
map.put(key1, "Value1");
map.put(key2, "Value2");

// Обидва ключі мають однаковий hashCode, тому виникає колізія
```

---

## **Порівняння (⚖️)**

| Метод вирішення | Версія Java | Підходить для | Мінуси |
| :---: | :---: | :---: | :---: |
| **Однозв'язний список** | до Java 8 | Невелика кількість колізій | Повільний доступ (O(n)) |
| **Червоно-чорне дерево** | Java 8+ | \> 8 елементів у бакеті | Складність реалізації |
| **Перехешування (resize)** | всі | При заповненні мапи \> 75% | Витрати ресурсів |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Колізія — Компанія Ключів в Комунальній Комірці"**

Усі ключі з однаковим "адресним кодом" — живуть в одній комірці та організовані в чергу або дерево.

---

## **🟩 Підсумок:**

* Колізії в **HashMap** — це нормальне явище при зберіганні ключів з однаковими індексами.
* Основне завдання — ефективно вирішувати колізії: зв'язним списком або деревом.
* Якість **`hashCode()`** напряму впливає на частоту колізій.
* При надмірних колізіях може бути проведене **resize()** — розширення масиву і перерозподіл.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs \- HashMap**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке хеш-колізія в `HashMap`?

> #### **💬 Відповідь:**
>
> **Хеш-колізія** в `HashMap` — це ситуація, коли два або більше різних ключів (`key`) після хешування та обчислення індексу потрапляють в **один і той самий "кошик" (bucket)** у внутрішньому масиві.
>
> Це може статися з двох причин:
>
> 1. **Два різні ключі мають однаковий `hashCode()`**. Це класична колізія хеш-функції.
> 2. **Два різні ключі мають різні `hashCode()`, але після обчислення індексу** (`(capacity - 1) & hash`) результат виявляється однаковим. Це трапляється значно частіше.
>
> Колізії є **нормальним і очікуваним явищем** у роботі будь-якої хеш-таблиці. Ефективність `HashMap` залежить не від повної відсутності колізій, а від здатності швидко їх обробляти.

#### **📝 Аналогія:**</h4>

Це як кілька людей з різними іменами мають день народження в один і той же день. День року — це "кошик". Той факт, що вони потрапили в одну дату, не означає, що це одна і та сама людина.

#### **📌 Що перевіряють:**

*Знання визначення колізії та розуміння того, що це нормальна частина роботи хеш-таблиць.*

* * *

### **2 ❓ Питання:**

Як `HashMap` вирішує проблему колізій?

> #### **💬 Відповідь:**
>
> `HashMap` використовує метод **ланцюгової адресації (chaining)**. Це означає, що кожен "кошик" у внутрішньому масиві може зберігати не один елемент, а **структуру даних**, що містить усі елементи, які потрапили в цей кошик.
>
> 1. Спочатку ця структура є **зв'язаним списком (linked list)**. Кожен елемент `Node` має посилання `next` на наступний елемент у цьому ж кошику.
>
> 2. **З Java 8**, якщо кількість елементів у кошику досягає певного порогу (`TREEIFY_THRESHOLD`, за замовчуванням 8), цей зв'язаний список **перетворюється на збалансоване червоно-чорне дерево**.
>
> Коли потрібно знайти елемент, `HashMap` спочатку знаходить кошик за `hashCode`, а потім послідовно перебирає елементи у цьому списку/дереві, викликаючи `equals()` для кожного, щоб знайти точний збіг.

#### **📌 Що перевіряють:**

*Знання механізму обробки колізій через ланцюжки та оптимізації за допомогою дерев у Java 8.*

* * *

### **3 ❓ Питання:**

Як колізії впливають на продуктивність `HashMap`?

> #### **💬 Відповідь:**
>
> Колізії **напряму погіршують продуктивність** `HashMap`, оскільки вони нівелюють головну перевагу хеш-таблиці — доступ за константний час.
>
> * **Ідеальний випадок (без колізій):**
>     Операції `put`, `get`, `remove` мають складність **O(1)**.
>
> * **Випадок з колізіями:**
>     Коли в одному кошику накопичується `k` елементів, пошук всередині цього кошика вимагає перебору цих `k` елементів.
>   * Якщо кошик є **зв'язаним списком** (до Java 8 або при k < 8), складність пошуку в кошику стає **O(k)**. У найгіршому випадку, коли всі `n` елементів потрапили в один кошик, загальна складність операції стає **O(n)**.
>   * Якщо кошик є **червоно-чорним деревом** (з Java 8), складність пошуку в кошику стає **O(log k)**. У найгіршому випадку загальна складність — **O(log n)**.
>
> Таким чином, велика кількість колізій перетворює `HashMap` з дуже швидкої структури даних на значно повільнішу.

#### **📌 Що перевіряють:**

*Розуміння того, як колізії впливають на асимптотичну складність операцій.*

* * *

### **4 ❓ Питання:**

Яку роль відіграє метод `hashCode()` у виникненні колізій?

> #### **💬 Відповідь:**
>
> Якість реалізації `hashCode()` є **ключовим фактором**, що впливає на кількість колізій.
>
> * **Хороша реалізація `hashCode()`:**
>   * Забезпечує **рівномірний розподіл** хеш-кодів по всьому діапазону значень `int`.
>   * Це призводить до того, що ключі рівномірно "розкидаються" по всіх кошиках, і кількість колізій **мінімізується**.
>
> * **Погана реалізація `hashCode()`:**
>   * Наприклад, якщо `hashCode()` завжди повертає те саме число, або генерує хеш-коди, що скупчуються в одному діапазоні.
>   * Це призводить до **масових колізій**, оскільки багато різних ключів будуть потрапляти в одні й ті самі кошики.
>   * Це "вбиває" продуктивність `HashMap`.
>
> Отже, правильне перевизначення `hashCode()` (разом з `equals()`) є критично важливим для ефективного використання `HashMap`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що саме якість хеш-функції є першопричиною низької або високої кількості колізій.*

* * *

### **5 ❓ Питання:**

Назвіть відомий приклад ключів, що створюють колізію в `HashMap` у Java. ⭐️

> #### **💬 Відповідь:**
>
> Класичним прикладом є рядки **`"Aa"`** та **`"BB"`**.
>
> Хоча це різні рядки, їхні `hashCode()`, обчислені за стандартним алгоритмом Java, є **однаковими**.
>
> ` "Aa".hashCode() == 2112 `
> ` "BB".hashCode() == 2112 `
>
> Якщо ви додасте обидва ці рядки як ключі в `HashMap` з невеликою ємністю, вони, ймовірно, потраплять в один і той самий кошик, створюючи колізію. `HashMap` розрізнить їх за допомогою `equals()`.
>
> Іншим прикладом є пари **`"FB"`** та **`"Ea"`**.

#### **📌 Що перевіряють:**

*Ерудицію та знання поширених прикладів, що демонструють колізії.*

* * *

### **6 ❓ Питання:**

Яка оптимізація для обробки колізій з'явилася в Java 8? ⭐️

> #### **💬 Відповідь:**
>
> У Java 8 було введено дуже важливу оптимізацію: **перетворення зв'язаного списку в червоно-чорне дерево**.
>
> * **Механізм:**
>   * `HashMap` відстежує кількість елементів у кожному кошику.
>   * Якщо ця кількість досягає певного порогу, який називається **`TREEIFY_THRESHOLD`** (і дорівнює **8** за замовчуванням), то зв'язаний список у цьому кошику **автоматично перетворюється на збалансоване червоно-чорне дерево**.
>
> * **Перевага:**
>   * Це покращує продуктивність у **найгіршому випадку** (коли багато колізій).
>   * Час пошуку елемента всередині такого "переповненого" кошика зменшується з **O(k)** (для списку) до **O(log k)** (для дерева).
>
> Якщо кількість елементів у кошику згодом зменшується (через видалення) нижче іншого порогу (`UNTREEIFY_THRESHOLD`, за замовчуванням 6), дерево може бути перетворене назад на зв'язаний список.

#### **📌 Що перевіряють:**

*Знання ключової оптимізації в Java 8 для `HashMap` та пов'язаних з нею констант.*

* * *

### **7 ❓ Питання:**

Поясніть, як `equals()` використовується при колізіях.

> #### **💬 Відповідь:**
>
> `equals()` є **другим і фінальним етапом** перевірки при колізіях.
>
> Коли `HashMap` знаходить потрібний кошик за `hashCode`, він починає ітерувати по всіх елементах (`Node`) у цьому кошику.
>
> Для кожного елемента `node` виконується перевірка:
>
> **`if (node.hash == searchHash && node.key.equals(searchKey))`**
>
> 1. Спочатку він порівнює кешовані хеш-коди. Це швидка перевірка.
> 2. І **тільки якщо хеш-коди збігаються**, він викликає "дорожчий" метод `equals()` для точного порівняння ключів.
>
> Таким чином, `equals()` є вирішальним фактором, який дозволяє `HashMap` **розрізняти** ключі, що потрапили в один кошик через колізію, і визначити, чи це оновлення існуючого запису, чи додавання нового.

#### **📌 Що перевіряють:**

*Чітке розуміння двоступеневого процесу перевірки в кошику: спочатку `hashCode`, потім `equals`.*

* * *

### **8 ❓ Питання:**

Як параметр `load factor` впливає на кількість колізій? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Load factor` (коефіцієнт завантаження) — це компроміс між використанням пам'яті та кількістю колізій (а отже, і продуктивністю).
>
> * **Високий `load factor` (наприклад, 0.9 або 1.0):**
>   * `HashMap` буде розширюватися (`resize`) рідше, оскільки він чекає, поки таблиця стане дуже заповненою.
>   * **Наслідок:** **Більше колізій**. Велика кількість елементів "втискується" у меншу кількість кошиків. Це економить пам'ять, але знижує продуктивність пошуку.
>
> * **Низький `load factor` (наприклад, 0.5):**
>   * `HashMap` буде розширюватися частіше. Таблиця буде "розрідженою".
>   * **Наслідок:** **Менше колізій**. Елементи краще розподіляються по більшій кількості кошиків. Це підвищує продуктивність, але **використовує більше пам'яті**.
>
> Значення за замовчуванням **0.75** є хорошим балансом для більшості випадків.

#### **📌 Що перевіряють:**

*Розуміння того, як налаштування `HashMap` впливають на його продуктивність та використання пам'яті.*

* * *

### **9 ❓ Питання:**

Якщо `hashCode()` реалізовано погано, наприклад, завжди повертає `42`, до чого це призведе з точки зору колізій та структури `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **максимально можливої кількості колізій** і повної деградації `HashMap`.
>
> **Що станеться:**
>
> 1. Всі без винятку ключі будуть мати однаковий хеш-код `42`.
> 2. Після обчислення індексу `(capacity - 1) & 42` всі вони потраплять в **один і той самий кошик**.
> 3. **Структура `HashMap` виродиться.** Замість хеш-таблиці з розподіленими даними, ви отримаєте структуру, що складається з **одного дуже довгого "ланцюжка"** (або одного великого дерева з Java 8), а всі інші кошики будуть порожніми.
>
> **Наслідки:**
>
> * Продуктивність всіх основних операцій (`put`, `get`, `remove`) впаде з O(1) до **O(n)** (для зв'язаного списку) або **O(log n)** (для дерева).
> * Усі переваги хешування будуть втрачені.

#### **📌 Що перевіряють:**

*Здатність проаналізувати найгірший сценарій та його вплив на внутрішню структуру та продуктивність.*

* * *

### **10 ❓ Питання:**

Поясніть, як операція `resize` (перехешування) допомагає зменшити кількість колізій. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Операція `resize` допомагає зменшити колізії, оскільки вона **перерозподіляє** існуючі елементи по **більшій кількості кошиків**.
>
> **Як це працює:**
>
> 1. Коли `HashMap` розширюється, його `capacity` зазвичай **подвоюється** (наприклад, з 16 до 32).
> 2. Для кожного існуючого елемента **індекс його кошика обчислюється заново**, але вже з урахуванням нової `capacity`.
> 3. Через зміну `capacity` елементи, які **раніше потрапляли в один кошик** (мали однаковий індекс), тепер мають високий шанс потрапити **в різні кошики** у новій, більшій таблиці.
>
> **Приклад:**
>
> * При `capacity=16` ключі з хешами `10` та `26` потрапляли в один кошик `10` (`10 % 16 = 10`, `26 % 16 = 10`).
> * При `capacity=32` вони потраплять у різні кошики (`10 % 32 = 10`, `26 % 32 = 26`).
>
> Таким чином, `resize` "розріджує" дані по таблиці, зменшуючи середню довжину ланцюжків у кошиках і підтримуючи високу продуктивність.

#### **📌 Що перевіряють:**

*Розуміння того, що `resize` є механізмом не тільки для збільшення ємності, але й для боротьби з колізіями.*

* * *

### **11 ❓ Питання:**

Чому додаткове хешування, яке `HashMap` застосовує до `hashCode()` ключа, допомагає при колізіях? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Додаткове хешування `h ^ (h >>> 16)` допомагає **покращити розподіл** і зменшити колізії, особливо для ключів з **поганими хеш-функціями**.
>
> **Проблема:**
>
> * Обчислення індексу (`(capacity - 1) & hash`) використовує тільки **молодші біти** хеш-коду.
> * Якщо хеш-функція генерує хеш-коди, що відрізняються тільки в **старших бітах**, то всі ці ключі матимуть однакові молодші біти і потраплять в один кошик.
>
> **Рішення `HashMap`:**
>
> * Операція `(h = key.hashCode()) ^ (h >>> 16)` бере **старші 16 біт** хеш-коду (`h >>> 16`) і **змішує** їх з **молодшими 16 бітами** за допомогою операції `XOR`.
> * Це призводить до того, що зміни у **старших** бітах оригінального хеш-коду також **впливають на молодші біти** фінального хешу, який використовується для індексації.
>
> **Результат:** Розподіл стає значно кращим, і ймовірність колізій зменшується, навіть якщо оригінальний `hashCode()` був неідеальним.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішні оптимізації `HashMap`. Розуміння ролі бітових операцій для покращення якості хешування.*

* * *

### **12 ❓ Питання:**

Припустимо, два об'єкти рівні за `equals()`, але їх `hashCode()` різний. Як це вплине на колізії та роботу `HashMap`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це є **прямим порушенням контракту `equals-hashCode`** і призведе до абсолютно некоректної роботи, але не через колізії, а через їхню відсутність там, де вона мала б бути.
>
> `Person p1 = new Person("A");` (`hashCode`=100)
> `Person p2 = new Person("A");` (`hashCode`=200)
>
> **Що станеться:**
>
> 1. Ви викликаєте `map.put(p1, "value")`. `p1` потрапляє в кошик для хешу 100.
> 2. Ви викликаєте `map.put(p2, "value")`. `p2` потрапляє в кошик для хешу 200.
>
> **`HashMap` буде вважати `p1` і `p2` абсолютно різними ключами**. Він **не виявить** колізію, бо хеш-коди різні. Метод `equals()` навіть не буде викликаний.
>
> **Наслідки:**
>
> * В `HashMap` можна буде додати **два логічно однакових ключі**.
> * `map.get(p2)` поверне `null`, навіть якщо `map` містить `p1`.
> * Пошук за `containsKey(p2)` також поверне `false`.
>
> Це не проблема колізій, а проблема того, що `HashMap` просто не може знайти відповідність через порушений контракт.

#### **📌 Що перевіряють:**

*Розуміння того, що порушення контракту — це проблема логіки, яка є значно гіршою за проблему продуктивності (колізії).*

* * *

### **13 ❓ Питання:**

Як впливає початкове значення `capacity` на ймовірність колізій при додаванні перших елементів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вища початкова `capacity` **зменшує** ймовірність колізій.
>
> **Пояснення:**
>
> Індекс кошика обчислюється на основі `capacity`. Чим більша `capacity`, тим на більший діапазон індексів можуть "розмазуватися" хеш-коди.
>
> * **Якщо `capacity` = 16 (за замовчуванням):** Усі ключі будуть розподілені між 16 кошиками. Ймовірність, що два ключі потраплять в один кошик, відносно висока.
>
> * **Якщо `capacity` = 1024:** Ті ж самі ключі будуть розподілені вже між 1024 кошиками. Ймовірність того, що вони потраплять в один і той самий, значно менша.
>
> **Коли це корисно?**
>
> Якщо ви заздалегідь знаєте, що будете зберігати в `HashMap` велику кількість елементів (наприклад, мільйон), то створення її з адекватною початковою ємністю (`new HashMap<>(1000000)`) дає дві переваги:
>
> 1. **Уникаєте дорогих операцій `resize`**.
> 2. **Зменшуєте кількість колізій** з самого початку, що підтримує високу продуктивність.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як правильна ініціалізація `HashMap` може оптимізувати її роботу.*

* * *

### **14 ❓ Питання:**

Що таке атака через хеш-колізії (Hash Collision DoS Attack) і як перехід на дерево в Java 8 допоміг захиститися від неї? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Атака через хеш-колізії** — це DoS-атака, коли зловмисник відправляє на сервер велику кількість даних, спеціально підібраних так, щоб **всі вони мали однаковий хеш-код**.
>
> **Механізм атаки (до Java 8):**
>
> * Коли сервер намагався розпарсити ці дані і покласти їх у `HashMap` (наприклад, HTTP-параметри), всі вони потрапляли в **один кошик**.
> * Цей кошик вироджувався у **дуже довгий зв'язаний список**.
> * Кожне наступне додавання вимагало **проходу по всьому списку (O(n))**, що викликало **різке навантаження на процесор** і могло "підвісити" сервер.
>
> **Як допоміг захист у Java 8:**
>
> * Перехід від зв'язаного списку до **червоно-чорного дерева**, коли кількість елементів у кошику перевищує 8.
> * Це змінило складність операцій у найгіршому випадку з **O(n)** на **O(log n)**.
> * `O(log n)` зростає набагато повільніше, ніж `O(n)`. Це не усуває атаку повністю, але робить її **значно менш ефективною** і вимагає від зловмисника на порядки більше даних для досягнення того ж ефекту.
>
> Таким чином, ця оптимізація є важливим елементом захисту від цього типу атак.

#### **📌 Що перевіряють:**

*Розуміння аспектів безпеки, пов'язаних з реалізацією структур даних, та ролі оптимізації Java 8 у їх пом'якшенні.*

* * *

### **15 ❓ Питання:**

Уявіть `HashMap`, де всі елементи потрапили в один кошик, який перетворився на дерево. Яку складність матиме операція `remove(key)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо кошик перетворився на червоно-чорне дерево, складність операції `remove(key)` у цьому кошику буде **O(log k)**, де `k` — кількість елементів у цьому конкретному кошику.
>
> **Процес `remove(key)` у такому кошику:**
>
> 1. **Пошук вузла:** Спочатку виконується пошук вузла з потрібним ключем у червоно-чорному дереві. Пошук у збалансованому дереві має складність **O(log k)**.
> 2. **Видалення вузла:** Після того, як вузол знайдено, він видаляється з дерева.
> 3. **Ребалансування:** Видалення може порушити властивості червоно-чорного дерева (наприклад, баланс висоти). Тому після видалення може знадобитися **ребалансування**, яке виконується за допомогою обертань та перефарбовування вузлів. Ця операція також має складність **O(log k)**.
>
> Таким чином, загальна складність `remove` у такому "дерев'яному" кошику становить `O(log k)`.

#### **📌 Що перевіряють:**

*Знання складності операцій для збалансованих дерев та розуміння того, що видалення з них також є ефективною операцією.*
