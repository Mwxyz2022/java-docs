---
layout: default
title: "Основні методи HashMap"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 7
---

# Основні методи HashMap

* короткий опис: Набір API для повного контролю над мапою: додавання (`put()`), отримання (`get()`), видалення (`remove()`), перевірка наявності (`containsKey()`, `containsValue()`), отримання ключів (`keySet()`), значень (`values()`), пар (`entrySet()`).

* поняття: `size()`, `isEmpty()`, `containsKey()`, `containsValue()`, `remove()`, `clear()`, `keySet()`, `values()`, `entrySet()`.

* приклади: `map.get("January")`, `map.containsKey("March")`, ітерація через `map.entrySet()`.

* ключові моменти: `containsKey()` швидкий (O(1)), `containsValue()` повільніший (O(N)). Методи `keySet()`, `values()`, `entrySet()` повертають колекції, які можна стрімити.

* що важливо знати на співбесіді: Методи часто запитуються для перевірки знань API.

### **✅ Офіційне визначення:**

**Методи** **HashMap** — це набір API, що дозволяє працювати з колекцією пар **`ключ–значення`**: додавати, видаляти, перевіряти наявність, ітеруватись тощо. Вони забезпечують повний контроль над станом мапи.

### **🧠 Простими словами:**

Це інструменти, щоб заглянути в мапу, щось у ній знайти, прибрати, перевірити або обійти — як набір кнопок на пульті від телевізора мап.

---

## **📊 Основні методи HashMap:**

| Метод | Призначення |
| :---: | :---: |
| **`size()`** | Повертає кількість елементів у мапі |
| **`isEmpty()`** | Перевіряє, чи порожня мапа |
| **`containsKey(k)`** | Перевіряє наявність ключа |
| **`containsValue(v)`** | Перевіряє наявність значення |
| **`get(k)`** | Повертає значення за ключем |
| **`put(k, v)`** | Додає пару **`ключ–значення`** або замінює значення |
| **`remove(k)`** | Видаляє пару за ключем |
| **`clear()`** | Очищає всю мапу |
| **`keySet()`** | Повертає **`Set`** усіх ключів |
| **`values()`** | Повертає **`Collection`** усіх значень |
| **`entrySet()`** | Повертає **`Set`** пар **`Map.Entry<K, V>`** |

---

## **Пояснення на пальцях (💡)**

Уяви мапу як шафу з ящиками:

* **`put()`** — кладе речі в ящик.
* **`get()`** — відкриває потрібний ящик.
* **`containsKey()`** — питає, чи є ящик із такою назвою.
* **`entrySet()`** — дає список усіх ящиків із вмістом.
* **`clear()`** — викидає все з шафи.
* **`remove()`** — витягує конкретну річ.

---

## **Корисні приклади (🧪)**

```java
Map<String, Integer> months = new HashMap<>();
months.put("January", 31);
months.put("February", 28);

// Отримати значення
int days = months.get("January"); // 31

// Перевірка
if (months.containsKey("March")) {
    System.out.println("March є!");
}

// Видалення
months.remove("February");

// Ітерація
for (Map.Entry<String, Integer> entry : months.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}
```

---

## **Порівняння (⚖️)**

| Метод | Повертає | Побічна дія |
| :---: | :---: | :---: |
| **`get(key)`** | Значення або **`null`** | Ні |
| **`put(k, v)`** | Старе значення або **`null`** | Додає або змінює |
| **`remove(k)`** | Видалене значення або **`null`** | Видаляє |
| **`containsKey(k)`** | **`true`** або **`false`** | Ні |
| **`clear()`** | Нічого (**`void`**) | Очищає все |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Get, Put, Remove — трійця HashMap’и\!"**

А решта — як опції:

**`containsKey`** — питає, **`entrySet`** — показує, **`clear`** — зносить усе 🔥

---

## **🟩 Підсумок:**

* HashMap має набір корисних методів для повного керування структурами даних.
* Найбільш уживані: **`put(), get(), remove(), containsKey(), entrySet().`**
* **`containsValue()`** — найповільніший (лінійна складність).
* Методи **`keySet(), values(), entrySet()`** — зручні для ітерацій.
* Знання цих методів — must-have для будь-якого Java-розробника.

---

### **🔗 Додаткові джерела:**

* [**Java HashMap API – Oracle Docs**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які три основні групи методів можна виділити в `HashMap`?

> #### **💬 Відповідь:**
>
> Методи `HashMap` можна умовно поділити на три основні групи:
>
> 1. **Основні операції:**
>     * `put(K key, V value)`: Додає або оновлює запис.
>     * `get(Object key)`: Отримує значення за ключем.
>     * `remove(Object key)`: Видаляє запис за ключем.
>     * `containsKey(Object key)`: Перевіряє наявність ключа.
>
> 2. **"Види" колекцій (Collection Views):**
>     * `keySet()`: Повертає `Set` всіх ключів.
>     * `values()`: Повертає `Collection` всіх значень.
>     * `entrySet()`: Повертає `Set` всіх записів "ключ-значення" (`Map.Entry`).
>
> 3. **Загальні методи / масові операції:**
>     * `size()`: Повертає кількість записів.
>     * `isEmpty()`: Перевіряє, чи порожня мапа.
>     * `clear()`: Видаляє всі записи.
>     * `putAll(Map<? extends K, ? extends V> m)`: Копіює всі записи з іншої мапи.

#### **📌 Що перевіряють:**

*Загальне розуміння API `HashMap` та здатність структурувати його методи за призначенням.*

* * *

### **2 ❓ Питання:**

Поясніть, що повертають методи `put`, `get` та `remove` у `HashMap`.

> #### **💬 Відповідь:**
>
> * **`V put(K key, V value)`:**
>   * **Повертає попереднє значення**, яке було пов'язане з цим ключем.
>   * Якщо ключа раніше не було, повертає **`null`**.
>
> * **`V get(Object key)`:**
>   * **Повертає значення**, пов'язане з ключем.
>   * Якщо ключ не знайдено, повертає **`null`**.
>
> * **`V remove(Object key)`:**
>   * **Повертає значення**, яке було пов'язане з видаленим ключем.
>   * Якщо такого ключа не було, повертає **`null`**.
>
> Важливо пам'ятати, що повернення `null` з `get` або `remove` не завжди означає, що ключа не було. Можливо, ключ був, але його значення було `null`.

#### **📌 Що перевіряють:**

*Знання сигнатур та значень, що повертаються, для основних методів. Розуміння неоднозначності `null`.*

* * *

### **3 ❓ Питання:**

Чому ітерація по `entrySet()` є більш ефективною, ніж ітерація по `keySet()` з викликом `get()` для кожного ключа?

> #### **💬 Відповідь:**
>
> **Ітерація по `entrySet()` є значно ефективнішою**, тому що вона вимагає лише **одного проходу** по внутрішній структурі `HashMap`.
>
> * **`for (Map.Entry<K, V> entry : map.entrySet())`**:
>   * Ітератор для `entrySet` проходить по кошиках і вузлах `HashMap` один раз.
>   * На кожному кроці він одразу отримує об'єкт `Map.Entry`, який вже **містить і ключ, і значення**. Не потрібно жодних додаткових пошуків.
>
> * **`for (K key : map.keySet())`**:
>   * Ітератор проходить по `keySet` (що саме по собі вже є операцією).
>   * Потім всередині циклу для **кожного** ключа викликається `map.get(key)`.
>   * Кожен виклик `get()` — це **повноцінна операція пошуку** в хеш-таблиці (обчислення хешу, індексу, прохід по кошику).
>
> Таким чином, підхід з `keySet()` виконує `n` додаткових пошуків, що робить його значно повільнішим, особливо для великих мап.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють "види" (views) колекцій, та здатність вибрати найпродуктивніший спосіб ітерації.*

* * *

### **4 ❓ Питання:**

Як надійно перевірити, чи існує ключ у `HashMap`, і чому перевірка `map.get(key) != null` може бути помилковою? ⭐️

> #### **💬 Відповідь:**
>
> Перевірка `map.get(key) != null` є **ненадійною**, тому що `HashMap` дозволяє зберігати **`null` як значення**.
>
> Метод `get(key)` поверне `null` у двох випадках:
>
> 1. Коли ключа `key` не існує в мапі.
> 2. Коли ключ `key` існує, але його значення — `null`.
>
> Таким чином, `map.get(key) == null` не дозволяє розрізнити ці дві ситуації.
>
> **Надійний спосіб** — це використовувати метод **`boolean containsKey(Object key)`**. Він повертає `true`, якщо ключ існує, і `false`, якщо ні, незалежно від значення, пов'язаного з цим ключем.
>
> ```java
> if (map.containsKey(key)) {
>     // Ключ точно існує
> }
> ```

#### **📌 Що перевіряють:**

*Усвідомлення неоднозначності, пов'язаної з `null`-значеннями, та знання про `containsKey` як про правильний інструмент.*

* * *

### **5 ❓ Питання:**

Порівняйте часову складність `containsKey(key)` та `containsValue(value)`.

> #### **💬 Відповідь:**
>
> * **`containsKey(key)`:**
>   * Має складність **O(1)** в середньому.
>   * Вона працює дуже швидко, оскільки використовує хеш-код ключа для миттєвого знаходження потрібного кошика.
>
> * **`containsValue(value)`:**
>   * Має складність **O(n)**, де `n` — кількість елементів у мапі.
>   * `HashMap` не індексує значення. Щоб перевірити наявність значення, вона змушена **повністю пройти по всім своїм записам** (зазвичай через `values()` або `entrySet()`) і порівняти кожне значення з шуканим. Це лінійний пошук.
>
> **Висновок:** `containsKey` — це дуже швидка операція, а `containsValue` — повільна, і її слід уникати, якщо продуктивність є важливою.

#### **📌 Що перевіряють:**

*Розуміння того, що `HashMap` оптимізована виключно для пошуку за ключем.*

* * *

### **6 ❓ Питання:**

Що роблять методи `getOrDefault()`, `putIfAbsent()` та `compute()` з Java 8? ⭐️

> #### **💬 Відповідь:**
>
> Це нові методи, які значно спрощують поширені операції.
>
> * **`getOrDefault(Object key, V defaultValue)`:**
>   * Повертає значення для ключа, а якщо ключа немає, повертає `defaultValue`. Дозволяє уникнути перевірки на `null`.
>
> * **`putIfAbsent(K key, V value)`:**
>   * Додає запис **тільки в тому випадку, якщо ключа ще немає** в мапі (або він зіставлений з `null`). Це атомарна операція, корисна в багатопотоковому середовищі.
>
> * **`compute(K key, BiFunction<? super K, ? super V, ? extends V> remappingFunction)`:**
>   * **Найпотужніший з них.** Атомарно обчислює нове значення для ключа на основі самого ключа та його **поточного значення**. Лямбда `remappingFunction` приймає `(ключ, старе_значення)` і повертає `нове_значення`.
>
>     **Приклад лічильника слів з `compute`:**
>
>     ```java
>     map.compute(word, (k, v) -> (v == null) ? 1 : v + 1);
>     ```
>
> Цей код є більш ефективним і потокобезпечним (у випадку `ConcurrentHashMap`), ніж ручна перевірка і `put`.

#### **📌 Що перевіряють:**

*Знання сучасного API Java 8 для `Map` та переваг, які він надає.*

* * *

### **7 ❓ Питання:**

Що повертають "види" `keySet()`, `values()` та `entrySet()`. Чи є вони копіями? ⭐️

> #### **💬 Відповідь:**
>
> Вони повертають **не копії**, а **"види" (views)**, які посилаються на оригінальну мапу.
>
> * **`keySet()`**: Повертає `Set<K>`, що містить всі ключі.
> * **`values()`**: Повертає `Collection<V>`, що містить всі значення.
> * **`entrySet()`**: Повертає `Set<Map.Entry<K, V>>`, що містить всі записи.
>
> **Найважливіша властивість:** Зміни в цих "видах" **відображаються** на оригінальній мапі, і навпаки.
>
> **Приклад:**
>
> ```java
> Map<String, Integer> map = new HashMap<>();
> map.put("A", 1);
>
> Set<String> keys = map.keySet();
> keys.remove("A"); // Видаляємо ключ через "вид"
>
> System.out.println(map.isEmpty()); // true. Мапа стала порожньою.
> ```
>
> Ця поведінка є дуже потужною, але вимагає обережності, оскільки модифікація "виду" може несподівано змінити стан базової мапи.

#### **📌 Що перевіряють:**

*Розуміння концепції "видів" колекцій та їхнього зв'язку з оригінальною структурою.*

* * *

### **8 ❓ Питання:**

Як безпечно видалити записи з `HashMap` під час ітерації? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Неправильний спосіб:** Робити це в циклі `for-each`, викликаючи `map.remove()`. Це призведе до `ConcurrentModificationException`.
>
> **Правильні способи:**
>
> 1. **Використовувати `Iterator` для `entrySet` (або `keySet`) і його метод `remove()`:**
>     * Це класичний, надійний спосіб. Найкраще ітерувати по `entrySet`, щоб мати доступ і до ключа, і до значення для умови видалення.
>
>     ```java
>     Iterator<Map.Entry<String, Integer>> iterator = map.entrySet().iterator();
>     while (iterator.hasNext()) {
>         Map.Entry<String, Integer> entry = iterator.next();
>         if (entry.getValue() < 0) { // Наприклад, видаляємо за умовою на значення
>             iterator.remove(); // Безпечне видалення
>         }
>     }
>     ```
>
> 2. **Використовувати `removeIf()` (з Java 8) для `entrySet`:**
>     * Це найбільш сучасний, лаконічний і рекомендований спосіб.
>
>     ```java
>     map.entrySet().removeIf(entry -> entry.getValue() < 0);
>     ```
>
#### **📌 Що перевіряють:**

*Знання `fail-fast` ітераторів. Вміння запропонувати кілька способів вирішення проблеми, включаючи сучасний з `removeIf`.*

* * *

### **9 ❓ Питання:**

Що таке `Map.Entry` і як отримати до нього доступ?

> #### **💬 Відповідь:**
>
> **`Map.Entry<K, V>`** — це вкладений інтерфейс всередині `Map`, який представляє **один запис** (одну пару "ключ-значення") у мапі.
>
> Він надає методи для доступу до ключа та значення:
>
> * `K getKey()`
> * `V getValue()`
> * `V setValue(V value)` (дозволяє змінити значення прямо в записі, що відобразиться і в мапі).
>
> **Як отримати до нього доступ:**
>
> Найпростіший спосіб — **ітерувати по `map.entrySet()`**. Цей метод повертає `Set` об'єктів `Map.Entry`.
>
> ```java
> for (Map.Entry<String, Integer> entry : map.entrySet()) {
>     String key = entry.getKey();
>     Integer value = entry.getValue();
>     // ...
> }
> ```
>
> Використання `entrySet` є найбільш ефективним способом обходу `HashMap`, коли вам потрібні і ключі, і значення.

#### **📌 Що перевіряють:**

*Знання про існування `Map.Entry` та його роль як об'єкта, що представляє один запис.*

* * *

### **10 ❓ Питання:**

Як працюють методи `replaceAll`, `computeIfAbsent`, `merge` з Java 8? Наведіть короткі приклади. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це потужні методи, що дозволяють виконувати складні операції атомарно (для `ConcurrentHashMap`) і виразно.
>
> * **`replaceAll(BiFunction<? super K, ? super V, ? extends V> function)`:**
>   * Замінює **кожне значення** у мапі на результат застосування переданої функції до ключа та старого значення.
>   * `map.replaceAll((key, oldValue) -> oldValue * 2); // Подвоїти всі значення`
>
> * **`computeIfAbsent(K key, Function<? super K, ? extends V> mappingFunction)`:**
>   * Якщо ключа немає, обчислює значення за допомогою функції `mappingFunction` і додає його в мапу.
>   * Ідеально для створення **кешів**.
>   * `V value = map.computeIfAbsent(key, k -> heavyComputation(k));`
>
> * **`merge(K key, V value, BiFunction<? super V, ? super V, ? extends V> remappingFunction)`:**
>   * Дозволяє "об'єднати" нове значення з існуючим.
>   * Якщо ключа немає, просто додає `(key, value)`.
>   * Якщо ключ є, застосовує функцію до **старого і нового** значень, щоб отримати фінальний результат.
>   * Дуже зручно для лічильників: `map.merge(word, 1, Integer::sum);`

#### **📌 Що перевіряють:**

*Знання сучасного функціонального API `Map`. Здатність пояснити призначення складних методів.*

* * *

### **11 ❓ Питання:**

Чому виклик `map.values().contains(value)` може бути неточним у `ConcurrentHashMap`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано зі **"слабкою консистентністю"** `ConcurrentHashMap`.
>
> * `values()` повертає "живий" вид значень.
> * Метод `contains` для цього виду буде ітерувати по записах мапи, щоб знайти значення.
>
> **Проблема:**
> У `ConcurrentHashMap` дозволено, щоб один потік **змінював** мапу, поки інший **ітерує** по ній. Можлива така ситуація:
>
> 1. `put("A", "target_value")`
> 2. Інший потік викликає `remove("A")`
>
> А ваш потік викликає `map.values().contains("target_value")`.
>
> Оскільки немає глобального блокування, не гарантується, що ваш виклик `contains` відбудеться до чи після `remove`. Виклик може повернути як `true`, так і `false`, залежно від таймінгів. `ConcurrentHashMap` гарантує відсутність `ConcurrentModificationException`, але **не гарантує**, що ви побачите абсолютно останній, консистентний стан мапи.
>
> Результат `containsValue` відображає стан мапи "на певний момент у часі".

#### **📌 Що перевіряють:**

*Глибоке розуміння концепції слабкої консистентності у конкурентних колекціях.*

* * *

### **12 ❓ Питання:**

Як отримати з `HashMap` відсортований список ключів?

> #### **💬 Відповідь:**
>
> `HashMap` не зберігає ключі у відсортованому порядку. Щоб отримати їх відсортованими, потрібно виконати наступні кроки:
>
> 1. Отримати множину всіх ключів за допомогою **`map.keySet()`**.
> 2. Перетворити цю множину на `List` (наприклад, `ArrayList`), оскільки `Collections.sort` працює зі списками.
> 3. Відсортувати цей список за допомогою **`Collections.sort()`** або **`list.sort()`**.
>
> **Приклад коду:**
>
> ```java
> Map<String, Integer> map = new HashMap<>();
> map.put("C", 3);
> map.put("A", 1);
> map.put("B", 2);
>
> // 1. Отримуємо Set<String> ключів
> // 2. Створюємо ArrayList з нього
> List<String> sortedKeys = new ArrayList<>(map.keySet());
>
> // 3. Сортуємо
> Collections.sort(sortedKeys);
>
> System.out.println(sortedKeys); // Виведе [A, B, C]
> ```
>
> Якщо відсортований порядок потрібен постійно, краще одразу використовувати **`TreeMap`**.

#### **📌 Що перевіряють:**

*Практичні навички роботи з колекціями. Розуміння того, як поєднувати різні структури для досягнення результату.*

* * *

### **13 ❓ Питання:**

Поясніть, що повертають `map.keySet()` та `map.entrySet()`? Чи є елементи, видалені з цих `Set`-ів, також видаленими з оригінального `Map`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * `keySet()` повертає `Set<K>`, який є **"видом" (view)** всіх ключів у мапі.
> * `entrySet()` повертає `Set<Map.Entry<K, V>>`, який є **"видом"** всіх записів у мапі.
>
> Обидва **не є копіями**, а є динамічними представленнями, пов'язаними з оригінальною мапою.
>
> **Чи будуть елементи видалені з мапи?**
> **Так.**
>
> **Зміни у "виді" відображаються на оригінальній мапі.**
>
> **Приклад:**
>
> ```java
> Map<String, Integer> map = new HashMap<>();
> map.put("A", 1);
> map.put("B", 2);
>
> Set<String> keys = map.keySet();
> // Видалення елемента через "вид"
> keys.remove("A");
>
> System.out.println(map); // Виведе {B=2}. Елемент було видалено і з мапи.
> ```
>
> Це стосується як `keySet`, так і `entrySet`. Видалення елемента з `entrySet` через ітератор також видалить відповідний запис з мапи. Це потужний, але іноді небезпечний механізм, який вимагає обережності.

#### **📌 Що перевіряють:**

*Розуміння того, що `keySet` та `entrySet` є не копіями, а динамічними представленнями, пов'язаними з мапою.*

* * *

### **14 ❓ Питання:**

Якщо у вас є `Map<User, String>`, і ви змінюєте поле об'єкта `User`, який вже є ключем, що може піти не так? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже **небезпечна операція**, яка може призвести до того, що ви **"втратите"** доступ до запису в `HashMap`.
>
> **Сценарій проблеми:**
>
> 1. Ви створюєте `User user = new User("John");`.
> 2. Додаєте його в мапу: `map.put(user, "data");`. `HashMap` обчислює хеш-код `user` (наприклад, H1) і розміщує запис у відповідний кошик.
> 3. **Ви змінюєте стан ключа:** `user.setName("Peter");`.
> 4. **Хеш-код ключа змінюється!** Тепер `user.hashCode()` повертає нове значення, наприклад, H2.
> 5. Коли ви намагаєтеся отримати значення `map.get(user)`, `HashMap` обчислює **новий** хеш-код H2 і шукає запис у кошику для H2.
>
> **Результат:** `map.get(user)` поверне **`null`**.
>
> Хоча запис все ще фізично знаходиться в мапі (у кошику, що відповідає старому хешу H1), ви більше не можете отримати до нього доступ за допомогою цього зміненого об'єкта-ключа.
>
> **Висновок:** **Ключі `HashMap` мають бути імутабельними (незмінними)**, або, принаймні, не можна змінювати ті поля, які беруть участь в `equals` та `hashCode`.

#### **📌 Що перевіряють:**

*Глибоке розуміння вимоги до імутабельності ключів `HashMap` та наслідків її порушення.*

* * *

### **15 ❓ Питання:**

У `HashMap`, яка операція швидша: ітерація по `values()` чи ітерація по `keySet()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітерація по `values()` та ітерація по `keySet()` мають **практично однакову продуктивність**.
>
> **Чому?**
>
> Ітератори для обох "видів" (`values()` і `keySet()`) насправді працюють однаково. Вони обидва **ітерують по внутрішній структурі `HashMap` — по записах `Map.Entry`**.
>
> * **Ітератор для `keySet()`:** на кожному кроці бере `entry` і повертає `entry.getKey()`.
> * **Ітератор для `values()`:** на кожному кроці бере `entry` і повертає `entry.getValue()`.
>
> Основна робота — обхід хеш-таблиці — однакова в обох випадках. Різниця полягає лише в тому, яке поле з `Map.Entry` повертається назовні. Цією мікроскопічною різницею в продуктивності можна знехтувати.
>
> Тому вибір між ними має базуватися виключно на тому, що вам потрібно: ключі, значення, чи і те, і інше (тоді `entrySet` буде найкращим вибором).

#### **📌 Що перевіряють:**

*Детальне розуміння того, що ітератори для всіх "видів" працюють на основі однієї і тієї ж внутрішньої ітерації по `entrySet`.*
