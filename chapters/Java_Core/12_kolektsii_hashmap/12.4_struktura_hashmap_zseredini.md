---
layout: default
title: "Структура HashMap зсередини"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 4
---

# Структура HashMap зсередини

* короткий опис: `HashMap` зберігає дані у масиві бакетів, де кожен бакет містить зв'язаний список елементів типу `Node<K, V>`. З Java 8+, якщо кількість елементів у бакеті перевищує поріг (8), зв'язаний список перетворюється на червоно-чорне дерево.

* поняття: `Node<K, V>` (внутрішній клас елемента), `bucket[]` (масив бакетів), `hash`, `key`, `value`, `next` (посилання на наступний вузол), Червоно-чорне дерево.

* приклади: Структура вузла (`hash`, `key`, `value`, `next`). Візуалізація зв'язаного списку у бакеті.

* ключові моменти: Вузли зв'язуються у список при колізіях. Перехід до дерева покращує продуктивність у найгірших випадках (з O(N) до O(log N)).

* що важливо знати на співбесіді: Як працює зберігання, особливо при колізіях.

### **✅ Офіційне визначення:**

**`HashMap`** зберігає пари ключ-значення у масиві бакетів. Кожен бакет містить зв’язаний список (або дерево з Java 8+), який складається з елементів типу **`Node<K, V>`** — це внутрішній клас, що представляє окремий елемент мапи.

### **🧠 Простими словами:**

Уяви коробку з комірками (масив). У кожній комірці може бути кілька елементів, зв’язаних між собою в ланцюжок. Ці елементи — це вузли **`Node`**, що містять ключ, значення і посилання на наступний вузол.

---

## **Основне**

| Компонент | Пояснення |
| :---: | :---: |
| **`Node<K, V>`** | Внутрішній клас, що представляє один елемент мапи |
| **`bucket[]`** | Масив, у який кладуться вузли (**`Node`**) |
| **`hash`** | Хеш-код ключа |
| **`key`** | Сам ключ |
| **`value`** | Значення, прив’язане до ключа |
| **`next`** | Посилання на наступний вузол у випадку колізії (ланцюжок) |

---

## **Пояснення на пальцях (💡)**

Уяви шафу з 16 ящиками. У кожному ящику — стопка конвертів. Один конверт \= один **`Node`**. Якщо два листи мають однакову адресу (хеш), вони потрапляють в один ящик і кладуться один поверх одного — це і є зв’язок **`next`**

---

## **Корисні приклади (🧪)**

```java
static class Node<K,V> implements Map.Entry<K,V> {
    final int hash;
    final K key;
    V value;
    Node<K,V> next;

    Node(int hash, K key, V value, Node<K,V> next) {
        this.hash = hash;
        this.key = key;
        this.value = value;
        this.next = next;
    }
}
```

Принцип вставки:

```java
int index = hash(key) % capacity;
if (bucket[index] == null) {
    bucket[index] = new Node<>(hash, key, value, null);
} else {
    Node<K, V> current = bucket[index];
    while (current != null) {
        if (current.key.equals(key)) {
            current.value = value; // перезапис
            return;
        }
        current = current.next;
    }
    // вставка нового вузла в ланцюжок
    bucket[index] = new Node<>(hash, key, value, bucket[index]);
}
```

---

## **Порівняння (⚖️)**

| Аспект | Значення |
| :---: | :---: |
| **Масив** | Основна структура для зберігання бакетів |
| **Вузол (`Node<K, V>`)** | Зберігає пару ключ-значення |
| **Колізія** | Призводить до створення ланцюжка вузлів |
| **З Java 8** | Може бути не список, а дерево (**`TreeNode`**) при великій кількості колізій |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“KV-hash-next”** —

Кожен вузол має: **Key**, **Value**, **Hash**, **Next**.

Це як паспорт: ім'я, номер, код, і контакт наступного родича

---

## **🟩 Підсумок:**

* **`HashMap`** використовує масив вузлів **`Node<K, V>`** як основну структуру.
* Кожен вузол має: **`key`**, **`value`**, **`hash`**, **`next`**.
* При колізіях елементи зв’язуються у список.
* З Java 8 при великій кількості елементів — використовується дерево.
* Ця структура забезпечує швидкий доступ і вставку, поки немає великої кількості колізій.

---

### **🔗 Додаткові джерела:**

* [**Java 17 Source Code — HashMap**](https://github.com/openjdk/jdk/blob/master/src/java.base/share/classes/java/util/HashMap.java)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка основна структура даних лежить в основі `HashMap`?

> #### **💬 Відповідь:**
>
> В основі `HashMap` лежить структура даних, відома як **хеш-таблиця**. У Java вона реалізована як **масив "кошиків" (buckets)**.
>
> * Цей масив зазвичай називають `table`, і його тип — `Node<K, V>[]`.
> * `Node<K, V>` — це внутрішній статичний вкладений клас, який представляє один запис у мапі (пару "ключ-значення").
>
> Кожен елемент цього масиву (кожен кошик) може бути або `null` (якщо він порожній), або може містити посилання на **перший вузол (`Node`) у ланцюжку** елементів, що потрапили в цей кошик через колізію.

#### **📝 Аналогія:**</h4>

Це як картотека з пронумерованими ящиками (масив). Коли ви кладете картку (запис "ключ-значення"), ви спочатку визначаєте номер ящика, а потім кладете картку туди. Якщо в ящику вже є картки, ви кладете свою поверх інших.

#### **📌 Що перевіряють:**

*Розуміння того, що `HashMap` — це масив, елементами якого є структури для обробки колізій (ланцюжки або дерева).*

* * *

### **2 ❓ Питання:**

З чого складається внутрішній клас `Node<K, V>` в `HashMap`?

> #### **💬 Відповідь:**
>
> Статичний вкладений клас `Node<K, V>` (який реалізує `Map.Entry<K, V>`) є будівельним блоком `HashMap` і містить чотири основні поля:
>
> 1. **`final int hash;`**:
>     * Зберігає кешований хеш-код ключа. Це дозволяє уникнути повторного обчислення хешу при операціях порівняння або розширення.
>
> 2. **`final K key;`**:
>     * Зберігає ключ. Він є `final`, що підкреслює ідею, що ключі не повинні змінюватися після додавання.
>
> 3. **`V value;`**:
>     * Зберігає значення. Воно не є `final`, оскільки значення можна змінювати (наприклад, через `put()` або `entry.setValue()`).
>
> 4. **`Node<K,V> next;`**:
>     * Посилання на **наступний вузол (`Node`)** у цьому ж кошику. Саме це поле дозволяє формувати **зв'язаний список** для обробки колізій.
>
> З Java 8 існує також підклас `TreeNode`, який додатково містить посилання `parent`, `left`, `right` для побудови червоно-чорного дерева.

#### **📌 Що перевіряють:**

*Знання внутрішньої структури елемента `HashMap`. Розуміння, чому ключ є `final`, а значення — ні.*

* * *

### **3 ❓ Питання:**

Як `HashMap` зберігає дані, якщо кілька ключів потрапляють в один і той самий "кошик" (bucket)?

> #### **💬 Відповідь:**
>
> Ця ситуація називається **хеш-колізією**. `HashMap` обробляє її, зберігаючи всі елементи, що потрапили в один кошик, у вигляді **ланцюжка (зв'язаного списку)** або, з Java 8, **червоно-чорного дерева**.
>
> **Процес при колізії:**
>
> 1. Новий `Node` створюється.
> 2. Якщо в кошику вже є один або кілька вузлів, новий вузол додається **на початок** цього ланцюжка. Поле `next` нового вузла буде посилатися на попередній перший елемент у кошику, а сам новий вузол стає першим.
>
> **Оптимізація в Java 8:**
>
> * Якщо довжина цього ланцюжка (кількість елементів у кошику) перевищує певний поріг (`TREEIFY_THRESHOLD`, за замовчуванням **8**), зв'язаний список **перетворюється на збалансоване червоно-чорне дерево**.
> * Це покращує продуктивність пошуку всередині "переповненого" кошика з **O(n)** до **O(log n)**.

#### **📌 Що перевіряють:**

*Розуміння того, як `HashMap` вирішує колізії, та знання про оптимізацію з Java 8 (перехід до дерева).*

* * *

### **4 ❓ Питання:**

Поясніть крок за кроком, що відбувається під час виклику `map.put("key", "value")`, коли кошик порожній.

> #### **💬 Відповідь:**
>
> 1. **Хешування:** Викликається `"key".hashCode()`, і `HashMap` застосовує до нього свою додаткову хеш-функцію, щоб покращити розподіл.
> 2. **Індексація:** На основі отриманого хешу та поточної ємності (`capacity`) таблиці обчислюється індекс кошика: `index = (capacity - 1) & hash`.
> 3. **Перевірка кошика:** `HashMap` звертається до масиву `table` за індексом `index`.
> 4. Він бачить, що `table[index]` дорівнює **`null`** (кошик порожній).
> 5. **Створення вузла:** Створюється новий об'єкт `Node<String, String>` з переданим ключем, значенням, хешем, а поле `next` встановлюється в `null`.
> 6. **Розміщення:** Цей новий `Node` розміщується у `table[index]`.
> 7. **Інкремент `size`:** Лічильник розміру мапи (`size`) збільшується на 1.
> 8. **Перевірка на `resize`:** `HashMap` перевіряє, чи не перевищив `size` поріг `threshold`. Якщо так, запускається процес розширення таблиці.

#### **📌 Що перевіряють:**

*Здатність покроково описати найпростіший сценарій роботи `put`.*

* * *

### **5 ❓ Питання:**

Поясніть крок за кроком, що відбувається під час виклику `map.put("key", "newValue")`, коли такий ключ вже існує в кошику.

> #### **💬 Відповідь:**
>
> 1. **Хешування та індексація:** Ці кроки відбуваються так само: обчислюється хеш та індекс кошика.
> 2. **Перевірка кошика:** `HashMap` бачить, що кошик за цим індексом **не є порожнім**.
> 3. **Ітерація по кошику:** `HashMap` починає проходити по ланцюжку (або дереву) вузлів у цьому кошику.
> 4. **Порівняння:** Для кожного вузла (`Node`) виконується перевірка:
>     * `if (node.hash == newKeyHash && node.key.equals(newKey))`
>     * Спочатку порівнюються кешовані хеш-коди (це швидко), і тільки якщо вони однакові, викликається "дорожчий" метод `equals()`.
> 5. **Знайдено збіг:** Коли знаходиться вузол, ключ якого рівний новому, `HashMap`:
>     * Зберігає його старе значення у тимчасову змінну.
>     * **Перезаписує** поле `value` цього вузла новим значенням `newValue`.
>     * Лічильник `size` **не змінюється**.
>     * Повертає збережене **старе значення**.
> 6. **Збігу не знайдено:** Якщо `HashMap` пройде по всьому кошику і не знайде збігу, він додасть новий `Node` у кінець (або початок, залежно від реалізації) ланцюжка.

#### **📌 Що перевіряють:**

*Розуміння того, як `HashMap` обробляє оновлення існуючих записів.*

* * *

### **6 ❓ Питання:**

Що таке початкова ємність (`initial capacity`) та коефіцієнт завантаження (`load factor`) `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> * **Початкова ємність (`initial capacity`):**
>   * Це **початковий розмір** внутрішнього масиву кошиків.
>   * За замовчуванням дорівнює **16**.
>
> * **Коефіцієнт завантаження (`load factor`):**
>   * Це число (за замовчуванням **0.75**), яке визначає, **наскільки заповненою** може бути мапа, перш ніж її ємність буде автоматично збільшена (операція `resize`).
>
> Поріг для розширення (`threshold`) обчислюється як `capacity * load_factor`. Для значень за замовчуванням це `16 * 0.75 = 12`. Коли кількість елементів у мапі досягає 12, її ємність буде подвоєна до 32.

#### **📌 Що перевіряють:**

*Знання конфігураційних параметрів `HashMap` та їхніх значень за замовчуванням.*

* * *

### **7 ❓ Питання:**

Що таке операція `resize` (або `rehash`) і чому вона "дорога"? ⭐️

> #### **💬 Відповідь:**
>
> **`Resize`** — це процес збільшення ємності внутрішньої хеш-таблиці, який відбувається, коли кількість елементів перевищує поріг `capacity * load_factor`.
>
> **Чому вона "дорога"?**
>
> 1. **Створення нового масиву:** Створюється новий масив кошиків, зазвичай **вдвічі більший** за старий. Це вимагає виділення нової ділянки пам'яті.
> 2. **Перехешування (Rehashing):** Потрібно пройти по **кожному** існуючому запису в старій таблиці, **переобчислити його індекс** на основі нової, більшої ємності, і розмістити його в правильний кошик у **новій** таблиці.
>
> Ця операція має **лінійну складність O(n)**, де `n` — кількість елементів у мапі, оскільки вимагає повного перерозподілу всіх даних.
>
> Хоча ця операція повільна, вона відбувається відносно рідко, тому **амортизована** складність `put` залишається O(1).

#### **📌 Що перевіряють:**

*Розуміння того, що `resize` є ресурсоємною операцією, яка включає не просто копіювання, а повне перехешування.*

* * *

### **8 ❓ Питання:**

Чому `HashMap` підтримує свою ємність як степінь двійки? ⭐️

> #### **💬 Відповідь:**
>
> `HashMap` підтримує ємність як степінь двійки (16, 32, 64...), щоб використовувати дуже ефективну **бітову оптимізацію** для обчислення індексу кошика.
>
> Замість "дорогої" операції ділення за модулем `hash % capacity`, він використовує значно швидшу операцію бітового "І":
>
> **`index = hash & (capacity - 1)`**
>
> Цей "трюк" дає той самий результат, що й `%`, **тільки якщо `capacity` є степенем двійки**.
>
> **Приклад:**
>
> * `capacity` = 16. `capacity - 1` = 15 (у бінарному вигляді `00...01111`).
> * `hash & 15` відкине всі старші біти і залишить тільки останні 4 біти, що еквівалентно залишку від ділення на 16.
>
> Це одна з ключових мікро-оптимізацій, що робить `HashMap` таким швидким.

#### **📌 Що перевіряють:**

*Знання про оптимізації на рівні бітових операцій та їхній зв'язок з вибором ємності `HashMap`.*

* * *

### **9 ❓ Питання:**

Як змінилася реалізація `HashMap` в Java 8 для обробки колізій? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У Java 8 було введено суттєву оптимізацію, яка покращує продуктивність у найгіршому випадку.
>
> * **До Java 8:** Кожен кошик був просто **зв'язаним списком**. У найгіршому випадку (багато колізій) пошук у кошику вимагав **O(k)** часу, де `k` — кількість елементів у ньому.
>
> * **З Java 8:**
>   * Кошик все ще починається як зв'язаний список.
>   * Але якщо кількість елементів у ньому досягає певного порогу (`TREEIFY_THRESHOLD`, за замовчуванням **8**), цей зв'язаний список **динамічно перетворюється на збалансоване червоно-чорне дерево**.
>
> **Що це дає:**
>
> Складність пошуку елемента всередині "переповненого" кошика зменшується з **O(k)** до **O(log k)**. Це робить `HashMap` значно стійкішим до поганих хеш-функцій та атак через хеш-колізії, покращуючи його продуктивність у найгіршому випадку. Якщо кількість елементів у кошику зменшується (через видалення), дерево може бути перетворене назад на список.

#### **📌 Що перевіряють:**

*Знання про еволюцію `HashMap` та оптимізацію за допомогою дерев, яка з'явилася в Java 8.*

* * *

### **10 ❓ Питання:**

Яким вимогам має відповідати ключ `HashMap` для коректної роботи?

> #### **💬 Відповідь:**
>
> 1. **Коректне перевизначення `equals()` та `hashCode()`:**
>     * Клас ключа **зобов'язаний** перевизначати ці два методи.
>     * Вони повинні дотримуватися контракту: якщо об'єкти рівні за `equals()`, вони мають мати однаковий `hashCode()`.
>
> 2. **Імутабельність (незмінність):**
>     * Це **настійно рекомендована**, хоча й не сувора, вимога.
>     * Якщо об'єкт-ключ змінюється після додавання в `HashMap` таким чином, що це впливає на його `hashCode()` або `equals()`, ви ризикуєте "втратити" цей запис у мапі.
>
> Класи, як-от `String`, `Integer` та інші обгортки, є ідеальними ключами, оскільки вони імутабельні і мають правильну реалізацію `equals`/`hashCode`.

#### **📌 Що перевіряють:**

*Розуміння вимог до об'єктів-ключів, особливо важливості імутабельності.*

* * *

### **11 ❓ Питання:**

Що таке `entrySet`, `keySet`, `values` у `HashMap`? Чи є вони копіями даних? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `entrySet`, `keySet` та `values` — це методи, які повертають різні **"види" (views)** даних з `HashMap`.
>
> Вони **не є копіями**. Вони є **"живими" представленнями**, які посилаються на оригінальну мапу.
>
> * **`keySet()`**: Повертає `Set<K>` — множину всіх **ключів**.
> * **`values()`**: Повертає `Collection<V>` — колекцію всіх **значень**.
> * **`entrySet()`**: Повертає `Set<Map.Entry<K, V>>` — множину всіх **записів "ключ-значення"**.
>
> **Зв'язок з оригінальною мапою:**
>
> **Зміни у "виді" відображаються на мапі, і навпаки.**
>
> Наприклад, якщо ви видалите елемент з `keySet()`, відповідний запис буде видалено і з `HashMap`.
>
> ```java
> map.keySet().remove("some_key"); // Запис з цим ключем буде видалено з map
> ```
>
> Це дозволяє ефективно маніпулювати даними, не створюючи повних копій, але вимагає обережності, особливо при роботі з ітераторами.

#### **📌 Що перевіряють:**

*Розуміння концепції "видів" колекцій та їхнього зв'язку з оригінальною структурою даних.*

* * *

### **12 ❓ Питання:**

Назвіть відмінності між `HashMap` та `ConcurrentHashMap` з точки зору внутрішньої структури. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Основна відмінність полягає в **механізмі синхронізації**, який впливає на їхню структуру.
>
> * **`HashMap`:**
>   * **Не є потокобезпечним.**
>   * Має **одну** спільну хеш-таблицю для всіх операцій. Немає вбудованих механізмів блокування.
>
> * **`ConcurrentHashMap`:**
>   * **Є потокобезпечним.**
>   * **До Java 8:** Використовував **сегментацію**. Таблиця була розділена на кілька сегментів, кожен з яких мав своє власне блокування. Це дозволяло кільком потокам одночасно працювати з різними сегментами.
>   * **З Java 8:** Відмовився від сегментації на користь більш дрібногранулярного блокування **на рівні першого вузла кожного кошика**.
>     * Операції запису (`put`, `remove`) використовують `synchronized` блок на вузлі або CAS-операції, блокуючи тільки один кошик, а не всю таблицю.
>     * Операції читання (`get`) зазвичай є повністю **неблокуючими**.
>
> Ця структура дозволяє `ConcurrentHashMap` досягати значно вищого рівня паралелізму та пропускної здатності, ніж `HashMap`, обгорнутий у `synchronizedMap`.

#### **📌 Що перевіряють:**

*Знання про `ConcurrentHashMap` та розуміння його внутрішньої архітектури (сегментація, дрібногранулярне блокування).*

* * *

### **13 ❓ Питання:**

Чи може `HashMap` зменшувати свою ємність (`capacity`) автоматично? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, `HashMap` **не зменшує свою ємність автоматично** після видалення великої кількості елементів.
>
> * Якщо ви додали мільйон елементів (що спричинило кілька розширень), а потім видалили майже всі, внутрішній масив "кошиків" все одно **залишиться великим**.
>
> **Чому так зроблено?**
>
> Це оптимізація. Часто після видалення даних слідує додавання нових. Постійне зменшення та збільшення таблиці було б дуже неефективним. `HashMap` припускає, що якщо колекція одного разу досягла певного розміру, вона може досягти його знову.
>
> **Що робити, якщо потрібно звільнити пам'ять?**
>
> У `HashMap` **немає** публічного методу, як `trimToSize()` в `ArrayList`.
> Єдиний спосіб — це створити **нову `HashMap`**, передавши їй стару в конструктор.
>
> `map = new HashMap<>(map);`
>
> Нова мапа буде створена з оптимальною ємністю для поточної кількості елементів, а стара, велика, стане доступною для збирача сміття.

#### **📌 Що перевіряють:**

*Детальне знання про те, як `HashMap` керує пам'яттю, та відсутність у нього механізму автоматичного "стиснення".*

* * *

### **14 ❓ Питання:**

Як реалізована ітерація по `HashMap`? В якому порядку обходяться елементи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітерація по `HashMap` відбувається шляхом **послідовного обходу її внутрішнього масиву "кошиків"**.
>
> **Алгоритм ітератора:**
>
> 1. Він починається з кошика з індексом 0.
> 2. Якщо кошик не порожній, ітератор проходить по всьому ланцюжку (або дереву) вузлів у цьому кошику, повертаючи кожен з них.
> 3. Після завершення обходу поточного кошика, він переходить до наступного кошика (індекс 1, 2, 3...) і повторює процес.
> 4. Порожні кошики просто пропускаються.
>
> **В якому порядку?**
>
> Оскільки розташування елементів у кошиках залежить від їхніх хеш-кодів та ємності таблиці, **порядок ітерації не гарантується** і може здаватися **хаотичним**. Він не відповідає ані порядку додавання, ані відсортованому порядку. Більше того, цей порядок може змінитися після операції `resize`.
>
> Якщо потрібен певний порядок, слід використовувати `LinkedHashMap` (порядок вставки) або `TreeMap` (сортування).

#### **📌 Що перевіряють:**

*Розуміння того, як внутрішня структура `HashMap` впливає на порядок ітерації.*

* * *

### **15 ❓ Питання:**

Уявіть, що два різні ключі випадково мають ідеально однакові бітові представлення їхніх `hashCode()`. Як `HashMap` їх розрізнить? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний випадок **хеш-колізії**. Якщо і `key1.hashCode()`, і `key2.hashCode()` повертають абсолютно однакове значення `int`, то:
>
> 1. Додаткове хешування всередині `HashMap` (`h ^ (h >>> 16)`) також дасть однаковий результат.
> 2. Обидва ключі потраплять в **один і той самий кошик** (bucket).
>
> Тепер `HashMap` змушений розрізняти їх за допомогою **методу `equals()`**.
>
> Коли ви спробуєте додати другий ключ, `HashMap` почне ітерувати по елементах у цьому кошику.
>
> * Для першого елемента він перевірить: `node.hash == newKeyHash` (буде `true`) **і** `node.key.equals(newKey)`.
> * Оскільки ключі **різні**, `equals()` поверне **`false`**.
> * `HashMap` продовжить ітерацію. Оскільки інших елементів немає, він просто додасть другий ключ у цей самий кошик (наприклад, у кінець зв'язаного списку).
>
> **Висновок:** Навіть при повній колізії хеш-кодів, `HashMap` продовжує працювати коректно завдяки фінальній перевірці через `equals()`. Саме тому **контракт `equals/hashCode` є настільки критичним**.

#### **📌 Що перевіряють:**

*Дуже чітке розуміння двоступеневого механізму `HashMap`: спочатку швидка фільтрація за `hashCode`, потім точне порівняння за `equals`.*
