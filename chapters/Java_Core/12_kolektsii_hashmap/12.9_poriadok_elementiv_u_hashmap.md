---
layout: default
title: "Порядок елементів у HashMap"
parent: "Колекції: HashMap"
grand_parent: "Java Core"
nav_order: 9
---

# Порядок елементів у HashMap

* короткий опис: `HashMap` не гарантує порядок елементів під час ітерації. Порядок може змінюватися при додаванні, видаленні або зміні розміру (`resize`).

* поняття: Порядок елементів, `LinkedHashMap` (зберігає порядок вставки), `TreeMap` (сортує за ключами).

* приклади: Ітерація через `entrySet()` може бути непередбачуваною.

* ключові моменти: Порядок може змінитися після `resize()`. Якщо потрібен порядок, слід використовувати `LinkedHashMap` або `TreeMap`.

* що важливо знати на співбесіді: Якщо потрібен порядок — краще використовувати `LinkedHashMap`.

### **✅ Офіційне визначення:**

**`HashMap`** — це реалізація інтерфейсу **`Map`**, яка **не гарантує порядок** розміщення елементів під час ітерації. Порядок може змінюватися при додаванні, видаленні або зміні розміру (**resize**).

### **🧠 Простими словами:**

Якщо додавати елементи в **`HashMap`**, не варто розраховувати, що вони залишаться в тому ж порядку — **`HashMap`** кидає все у "хеш-бакети", де логіка збереження залежить від хеш-кодів і поточного розміру. Місця на поличках — як пощастить.

---

## **📊 Варіанти `Map`, де порядок має значення:**

| Тип Map | Гарантія порядку | Примітки |
| :---: | :---: | :---: |
| **`HashMap`** | ❌ Ні | Порядок не фіксований |
| **`LinkedHashMap`** | ✅ Так | Порядок вставки зберігається |
| **`TreeMap`** | ✅ Так | Сортування за ключами |
| **`ConcurrentHashMap`** | ❌ Ні | Немає гарантій порядку |

---

## **Пояснення на пальцях (💡)**

Уяви, що ти кидаєш картки в коробки з підписами (хеш-бакети). Якщо коробок стане замало (resize), картки розкидають по нових коробках. І ось ти дивишся — де ж мій улюблений порядок? А його вже перемішали\

---

## **Корисні приклади (🧪)**

```java
Map<String, Integer> map = new HashMap<>();
map.put("January", 31);
map.put("February", 28);
map.put("March", 31);

// Ітерація без гарантії порядку
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}
```

🧪 Якщо ж потрібно зберегти порядок вставки:

```java
Map<String, Integer> linkedMap = new LinkedHashMap<>();
linkedMap.put("January", 31);
linkedMap.put("February", 28);
linkedMap.put("March", 31);

// Тут порядок буде гарантовано збережений
```

---

## **Порівняння (⚖️)**

| Властивість | `HashMap` | `LinkedHashMap` |
| :---: | :---: | :---: |
| **Порядок елементів** | ❌ Немає гарантій | ✅ Зберігається |
| **Продуктивність** | 🔼 Вища | Трохи повільніша |
| **Використання пам’яті** | Менше | Трохи більше |
| **Основне застосування** | Швидкий доступ | Доступ \+ порядок |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"HashMap — хаос, LinkedHashMap — лінійність."**

або

**"HashMap — не обіцяє, LinkedHashMap — пам’ятає."**

---

## **🟩 Підсумок:**

* **`HashMap`** **не гарантує порядок** елементів при ітерації.
* Порядок може змінюватися після **`resize()`**.
* Якщо важлива черговість вставки — **використовуй `LinkedHashMap`**.
* На співбесіді часто перевіряють, чи розумієш ти ці відмінності — особливо при роботі з API або багатопоточністю.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — HashMap**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/HashMap.html)
* [**Oracle Docs — LinkedHashMap**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/LinkedHashMap.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Чи гарантує `HashMap` порядок елементів при ітерації?

> #### **💬 Відповідь:**
>
> Ні, **`HashMap` абсолютно не гарантує жодного порядку** елементів.
>
> **Офіційна документація Java** прямо говорить про це: _"This class makes no guarantees as to the order of the map; in particular, it does not guarantee that the order will remain constant over time."_
>
> Це означає, що порядок, у якому ви обходите `HashMap` (через `entrySet`, `keySet` чи `values`), може бути **хаотичним** і непередбачуваним. Він не відповідає ані порядку додавання, ані відсортованому порядку. Більше того, цей порядок може **змінитися** після операції ресайзингу (`resize`).

#### **📝 Аналогія:**</h4>

Це як кидати речі у велику сумку. Коли ви їх дістаєте, вони можуть бути в будь-якому порядку, залежно від того, як вони там "втряслися".

#### **📌 Що перевіряють:**

_Знання фундаментальної властивості `HashMap` — відсутності гарантій порядку._

* * *

### **2 ❓ Питання:**

Чому `HashMap` не зберігає порядок? Як влаштована її внутрішня структура, що призводить до цього?

> #### **💬 Відповідь:**
>
> `HashMap` не зберігає порядок, тому що її основна мета — забезпечити **дуже швидкий доступ до даних (O(1))**, а не підтримувати послідовність.
>
> **Внутрішня структура, що призводить до цього:**
>
> 1. `HashMap` використовує **масив "кошиків" (buckets)**.
> 2. Коли ви додаєте елемент, його позиція у цьому масиві визначається на основі **`hashCode()` ключа**. Індекс кошика — це результат хешування, а не позиція вставки.
> 3. Елементи з різними хеш-кодами потрапляють у **різні, не пов'язані між собою кошики**.
> 4. **Ітерація** по `HashMap` зазвичай відбувається шляхом **послідовного проходу по масиву кошиків** (з індексу 0 до кінця), а потім — по елементах усередині кожного кошика.
>
> Оскільки розташування елементів залежить від їхніх хеш-кодів, а не від порядку додавання, послідовність при ітерації здається випадковою.

#### **📌 Що перевіряють:**

_Розуміння того, що розташування елементів у `HashMap` визначається хеш-кодом, а не послідовністю додавання._

* * *

### **3 ❓ Питання:**

Яка реалізація `Map` зберігає елементи в порядку їх додавання?

> #### **💬 Відповідь:**
>
> Для цього слід використовувати **`java.util.LinkedHashMap`**.
>
> `LinkedHashMap` є **нащадком `HashMap`**. Він успадковує всю логіку хеш-таблиці (швидкий доступ за ключем), але **додатково** підтримує **двозв'язний список**, який проходить через усі його записи.
>
> Цей список **фіксує порядок**, у якому елементи були вставлені в мапу.
>
> **Коли використовувати:** `LinkedHashMap` є ідеальним вибором, коли вам потрібна швидкість доступу як у `HashMap`, але при цьому **важлива стабільна, передбачувана послідовність ітерації** (наприклад, для кешів з політикою LRU або для збереження послідовності полів з конфігураційного файлу).

#### **📌 Що перевіряють:**

_Знання про існування та призначення `LinkedHashMap`._

* * *

### **4 ❓ Питання:**

Яка реалізація `Map` зберігає елементи відсортованими за ключем?

> #### **💬 Відповідь:**
>
> Для цього слід використовувати **`java.util.TreeMap`**.
>
> `TreeMap` є реалізацією інтерфейсу `NavigableMap` і базується на **червоно-чорному дереві**.
>
> **Як вона працює:**
>
> * `TreeMap` **сортує** всі свої записи на основі **ключів**.
> * Сортування відбувається або за **природним порядком** ключів (якщо вони реалізують `Comparable`), або за допомогою **`Comparator`**, переданого в конструктор.
> * Будь-яка ітерація по `TreeMap` (по ключах, значеннях чи записах) завжди буде повертати елементи у **відсортованому порядку**.
>
> **Недолік:** Продуктивність операцій (`put`, `get`, `remove`) є **O(log n)**, що повільніше за O(1) у `HashMap`.

#### **📌 Що перевіряють:**

_Знання про існування та призначення `TreeMap`._

* * *

### **5 ❓ Питання:**

Що станеться з порядком елементів у `HashMap` після операції `resize`? ⭐️

> #### **💬 Відповідь:**
>
> Після операції `resize` порядок елементів при ітерації **майже напевно зміниться**.
>
> **Чому так відбувається:**
>
> 1. Під час `resize` ємність (`capacity`) внутрішнього масиву кошиків збільшується (зазвичай подвоюється).
> 2. Індекс кошика для кожного елемента обчислюється за формулою, що залежить від ємності: `index = hash & (capacity - 1)`.
> 3. Оскільки `capacity` змінилася, для **кожного елемента** потрібно **переобчислити його індекс**.
> 4. В результаті, елементи, які раніше знаходилися в одному кошику або в сусідніх, можуть бути "розкидані" по новій, більшій таблиці абсолютно по-іншому.
>
> Це ще раз підкреслює, що на порядок елементів у `HashMap` **не можна покладатися**.

#### **📌 Що перевіряють:**

_Глибоке розуміння того, як ресайзинг впливає на внутрішню структуру і, як наслідок, на порядок ітерації._

* * *

### **6 ❓ Питання:**

Уявіть, що ви виводите на екран вміст `HashMap`, запускаєте програму кілька разів і бачите, що порядок елементів однаковий. Чи означає це, що порядок все ж таки гарантується для вашого випадку?

> #### **💬 Відповідь:**
>
> Ні, **не означає**.
>
> Те, що ви бачите стабільний порядок при кількох запусках, є просто **випадковістю** або особливістю реалізації **конкретної версії JVM** на вашій конкретній машині.
>
> * Алгоритм `String.hashCode()` є детермінованим, тому однакові рядки завжди даватимуть однакові хеш-коди.
> * `HashMap` також створюється з однаковою початковою ємністю.
>
> Тому для невеликого, фіксованого набору даних цілком можливо, що розподіл по кошиках буде однаковим при кожному запуску.
>
> **Але це не є гарантією.** Будь-яка зміна може порушити цей "стабільний" порядок:
>
> * Запуск на іншій версії Java або іншій JVM.
> * Додавання або видалення навіть одного елемента може спричинити `resize` і повністю змішати порядок.
> * Якщо ключі — це ваші власні об'єкти, їхній стандартний `hashCode()` може залежати від адреси в пам'яті, яка різна при кожному запуску.
>
> **Висновок:** ніколи не слід покладатися на спостережуваний порядок у `HashMap`.

#### **📌 Що перевіряють:**

_Розуміння різниці між "гарантованою поведінкою" та "випадковою стабільністю реалізації"._

* * *

### **7 ❓ Питання:**

Якщо вам потрібна мапа, яка є потокобезпечною і при цьому зберігає порядок, яку реалізацію ви оберете? ⭐️

> #### **💬 Відповідь:**
>
> У стандартній бібліотеці Java **немає** єдиного класу, який би задовольняв обидві ці вимоги "з коробки".
>
> * `ConcurrentHashMap` — потокобезпечний, але не зберігає порядок.
> * `LinkedHashMap` — зберігає порядок, але не є потокобезпечним.
>
> **Рішення:**
>
> Найпростішим способом є **створення синхронізованої обгортки** над `LinkedHashMap`.
>
> ```java
> Map<K, V> orderedThreadSafeMap = Collections.synchronizedMap(new LinkedHashMap<>());
> ```
>
> **Що це дає:**
>
> * Ви отримуєте **порядок вставки**, гарантований `LinkedHashMap`.
> * Ви отримуєте **потокобезпечність**, гарантовану `synchronizedMap`.
>
> **Недоліки:**
>
> * Ця реалізація матиме **низьку продуктивність** у високонавантаженому середовищі через глобальне блокування (як і `Hashtable`).
> * Ітерація по ній все одно вимагатиме зовнішньої синхронізації.
>
> Якщо потрібна висока продуктивність, можливо, доведеться розглядати більш складні рішення або сторонні бібліотеки, наприклад `ConcurrentSkipListMap` (для сортованого порядку).

#### **📌 Що перевіряють:**

_Здатність комбінувати існуючі класи для досягнення потрібної функціональності та розуміння компромісів такого рішення._

* * *

### **8 ❓ Питання:**

Як влаштований `LinkedHashMap` "під капотом", що дозволяє йому зберігати порядок вставки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `LinkedHashMap` **успадковує `HashMap`** і перевикористовує всю його логіку хеш-таблиці.
>
> **Ключова відмінність:**
>
> На додаток до хеш-таблиці, `LinkedHashMap` підтримує **двозв'язний список**, який проходить через **усі** його записи (`Entry`).
>
> * Кожен запис у `LinkedHashMap` (його внутрішній клас `Entry`) зберігає не тільки `key`, `value`, `hash`, `next` (для ланцюжка колізій), але й два додаткових посилання: **`before`** та **`after`**.
> * Ці два посилання зв'язують усі записи в єдиний ланцюжок у тому порядку, в якому вони були додані.
> * Коли ви додаєте новий елемент, він не тільки розміщується у відповідний кошик, але й додається в **кінець цього двозв'язного списку**.
> * Ітерація по `LinkedHashMap` відбувається не по кошиках, а саме по **цьому двозв'язному списку**, що і гарантує збереження порядку вставки.

#### **📌 Що перевіряють:**

_Глибоке розуміння того, як `LinkedHashMap` комбінує дві структури даних (хеш-таблицю та двозв'язний список) для досягнення своїх властивостей._

* * *

### **9 ❓ Питання:**

Що таке "порядок доступу" (`access order`) в `LinkedHashMap` і як його увімкнути? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Порядок доступу** — це спеціальний режим `LinkedHashMap`, в якому порядок ітерації визначається не порядком вставки, а **порядком останнього доступу** до елементів.
>
> Кожного разу, коли ви викликаєте метод `get()`, `put()` або `compute()` для існуючого елемента, цей елемент **переміщується в кінець** внутрішнього двозв'язного списку.
>
> Таким чином, на початку списку завжди будуть знаходитися **"найменш недавно використані" (Least Recently Used, LRU)** елементи.
>
> **Як увімкнути:**
>
> Порядок доступу вмикається через спеціальний конструктор, передаючи `true` як третій аргумент:
>
> `new LinkedHashMap<>(initialCapacity, loadFactor, true);`
>
> **Навіщо це потрібно?**
>
> Цей механізм є ідеальною основою для реалізації **кешів з політикою витіснення LRU**. Перевизначивши метод `removeEldestEntry()`, можна легко створити кеш, який автоматично видаляє найстаріші елементи, коли його розмір перевищує ліміт.

#### **📌 Що перевіряють:**

_Знання про просунуті можливості `LinkedHashMap` та їх застосування для реалізації кешів._

* * *

### **10 ❓ Питання:**

Як реалізована ітерація по `keySet()` у `HashMap`? Чи створюється нова колекція?

> #### **💬 Відповідь:**
>
> Ітерація по `keySet()` є дуже ефективною і **не створює нової колекції** ключів.
>
> Метод `keySet()` повертає екземпляр внутрішнього класу `KeySet`, який реалізує інтерфейс `Set`. Цей `KeySet` є **"видом" (view)**, а не копією.
>
> Коли ви отримуєте ітератор для `keySet()`, "під капотом" насправді створюється **ітератор для `entrySet()`**. Цей ітератор проходить по записах `Map.Entry`, але при кожному виклику `next()` він повертає не весь `entry`, а тільки `entry.getKey()`.
>
> Таким чином, `HashMap` не дублює дані і не витрачає зайву пам'ять, а просто надає інший "ракурс" для обходу тих самих внутрішніх даних.

#### **📌 Що перевіряють:**

_Розуміння концепції "видів" та того, що `keySet`, `values` та `entrySet` є ефективними операціями, які не створюють копій._

* * *

### **11 ❓ Питання:**

Чи гарантує `TreeMap` стабільний порядок ітерації? Якщо так, то який?

> #### **💬 Відповідь:**
>
> Так, `TreeMap` **гарантує** стабільний і передбачуваний порядок ітерації.
>
> **Який порядок:**
>
> Елементи в `TreeMap` завжди **відсортовані за ключами**.
>
> * Сортування відбувається або згідно з **природним порядком** ключів (якщо клас ключа реалізує `Comparable`).
> * Або згідно з **`Comparator`**, який було передано в конструктор `TreeMap`.
>
> Ітерація по `TreeMap` (по ключах, значеннях чи записах) завжди буде відбуватися у цьому відсортованому порядку.
>
> **Приклад:**
>
> ```java
> Map<Integer, String> map = new TreeMap<>();
> map.put(3, "C");
> map.put(1, "A");
> map.put(2, "B");
>
> // Виведе ключі у відсортованому порядку: [1, 2, 3]
> System.out.println(map.keySet());
> ```

#### **📌 Що перевіряють:**

_Знання ключової властивості `TreeMap` — гарантованого сортування._

* * *

### **12 ❓ Питання:**

Припустимо, у вас є `HashMap`. Вам потрібно отримати її вміст, відсортований за значеннями. Як це зробити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `HashMap` не підтримує сортування. `TreeMap` сортує тільки за ключами. Щоб відсортувати `HashMap` за значеннями, потрібно виконати наступні кроки:
>
> 1. Отримати `entrySet()` з `HashMap`. `Set<Map.Entry<K, V>>` містить пари "ключ-значення".
> 2. Перетворити цей `Set` у `List`, оскільки `Collections.sort` працює зі списками.
> 3. Відсортувати цей список, використовуючи `list.sort()` та кастомний **`Comparator`**, який буде порівнювати записи (`Map.Entry`) за їхніми **значеннями (`getValue()`)**.
> 4. (Опціонально) Якщо потрібна відсортована `Map`, можна створити новий `LinkedHashMap` і перекласти в нього відсортовані записи, щоб зберегти новий порядок.
>
> **Приклад коду (з Java 8):**
>
> ```java
> Map<String, Integer> map = ...;
>
> // 1-3. Отримуємо, перетворюємо і сортуємо
> List<Map.Entry<String, Integer>> entryList = new ArrayList<>(map.entrySet());
> entryList.sort(Map.Entry.comparingByValue()); // Зручний компаратор з Java 8
>
> // 4. Збираємо у новий LinkedHashMap
> Map<String, Integer> sortedMap = new LinkedHashMap<>();
> for (Map.Entry<String, Integer> entry : entryList) {
>     sortedMap.put(entry.getKey(), entry.getValue());
> }
>
> // Тепер `sortedMap` буде містити елементи, відсортовані за значенням
> ```

#### **📌 Що перевіряють:**

_Здатність вирішувати нетривіальні задачі з колекціями. Знання `Stream API` або `Comparator` для сортування._

* * *

### **13 ❓ Питання:**

Чому `ConcurrentHashMap` також не гарантує порядок ітерації? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `ConcurrentHashMap` не гарантує порядок з тієї ж причини, що і `HashMap`: його основна мета — **максимальна продуктивність та пропускна здатність** у багатопотоковому середовищі, а не підтримка порядку.
>
> Його внутрішня структура також базується на **хеш-таблиці**. Розташування елементів залежить від їхніх хеш-кодів, і ітератор проходить по кошиках, як і в `HashMap`.
>
> Більше того, через конкурентну природу порядок може бути ще більш непередбачуваним. Ітератор для `ConcurrentHashMap` надає "слабко-консистентний" знімок. Це означає, що він **може** відображати деякі зміни, що відбулися після його створення, але не зобов'язаний. Тому порядок може виглядати дуже дивним, якщо мапа активно змінюється.
>
> Якщо в багатопотоковому середовищі потрібна відсортована мапа, слід використовувати **`ConcurrentSkipListMap`**.

#### **📌 Що перевіряють:**

_Знання про `ConcurrentHashMap` та розуміння того, що його головний пріоритет — конкурентність, а не порядок._

* * *

### **14 ❓ Питання:**

Припустимо, у вас є `LinkedHashMap`. Чи можна змінити його порядок ітерації з "порядку вставки" на "порядок доступу"? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна, але це потрібно вказати **при створенні** `LinkedHashMap`.
>
> `LinkedHashMap` має спеціальний конструктор:
> `public LinkedHashMap(int initialCapacity, float loadFactor, boolean accessOrder)`
>
> * `accessOrder`: Це `boolean` прапорець.
>   * Якщо він `false` (за замовчуванням), `LinkedHashMap` використовує **порядок вставки**.
>   * Якщо він **`true`**, `LinkedHashMap` переключається на **порядок доступу**.
>
> Коли увімкнено `accessOrder`, будь-який виклик `get()` або `put()` (для існуючого ключа) переміщує відповідний елемент у **кінець** внутрішнього двозв'язного списку. Таким чином, на початку списку завжди будуть найменш недавно використані елементи.
>
> **Змінити цю поведінку для вже існуючого екземпляра не можна.** Потрібно створювати новий `LinkedHashMap`.

#### **📌 Що перевіряють:**

_Знання просунутих можливостей `LinkedHashMap` та того, як її можна використовувати для реалізації LRU-кешів._

* * *

### **15 ❓ Питання:**

Яка реалізація `Set` не гарантує порядку, а яка гарантує? Поясніть аналогію з `Map`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Аналогія між реалізаціями `Set` та `Map` є прямою, тому що більшість реалізацій `Set` **"під капотом" використовують відповідну `Map`**.
>
> * **`HashSet` (не гарантує порядку):**
>   * "Під капотом" використовує **`HashMap`**. Елементи множини зберігаються як **ключі** у `HashMap`, а як значення — одна і та ж сама константа-заглушка (`private static final Object PRESENT`).
>   * Оскільки `HashMap` не гарантує порядку, `HashSet` — теж.
>
> * **`LinkedHashSet` (зберігає порядок вставки):**
>   * "Під капотом" використовує **`LinkedHashMap`**.
>   * Оскільки `LinkedHashMap` зберігає порядок вставки, `LinkedHashSet` також його зберігає.
>
> * **`TreeSet` (зберігає відсортований порядок):**
>   * "Під капотом" використовує **`TreeMap`**.
>   * Оскільки `TreeMap` зберігає ключі відсортованими, `TreeSet` також зберігає елементи відсортованими.
>
> Розуміння цієї залежності допомагає одразу запам'ятати характеристики всіх основних реалізацій `Set`.

#### **📌 Що перевіряють:**

_Дуже глибоке знання про те, як реалізовані колекції `Set`. Розуміння того, що `Set` є, по суті, обгорткою над `Map`._
