---
layout: default
title: "Generic Classes (Класи з дженеріками)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 2
---

# Generic Classes (Класи з дженеріками)

* короткий опис: Generic-класи – це класи, що використовують один або кілька параметризованих типів (`<T>`, `<K, V>`),
  що дозволяє їм працювати з різними типами даних, зберігаючи при цьому типобезпечність.

* поняття: Generic class, `<T>`, `<K, V>`, конвенції імен типів (`T`, `E`, `K`, `V`).

* приклади: `public class Box<T> { ... }`, `Pair<String, Integer>`.

* ключові моменти: Дозволяють створювати універсальні, багаторазові класи, які адаптуються до різних типів даних.

* що важливо знати на співбесіді: Синтаксис, використання, конвенції.

### **✅ Офіційне визначення:**

**Generic class** — це клас, який використовує один або кілька параметризованих типів, що дозволяє йому працювати з
різними типами даних без втрати типобезпечності. Типи задаються за допомогою дженеріків у кутових дужках: *
*`class Box<T> { ... }.`**

### **🧠 Простими словами:**

**Generic-клас
** — це шаблон для створення класів, які можуть зберігати, повертати або працювати з будь-яким типом даних. Наприклад, один і той самий клас
**`Box`** може зберігати **`String, Integer, User`** — що завгодно, і при цьому бути безпечним щодо типів.

---

## **📊 Основні аспекти generic-класів:**

**Синтаксис:**

```java
public class Box<T> {
    private T value;

    public void setValue(T value) {
        this.value = value;
    }

    public T getValue() {
        return value;
    }
}
```

* **Конвенції імен типів:**
  * **`T`** — Type (загальний тип)
  * **`E`** — Element (наприклад, у колекціях)
  * **`K`** — Key (у парах ключ-значення)
  * **`V`** — Value (у парах ключ-значення)
  * **`N`** — Number
  * **`S, U, R`** — другорядні типи, якщо кілька
* **Можна параметризувати кількома типами:**

```java
class Pair<K, V> { ...
}
```

---

## **Пояснення на пальцях (💡)**

Уяви, що ти створюєш клас "Скринька". Без дженеріків ти створюєш одну скриньку для яблук, іншу — для книг, третю — для
булочок. Це незручно. З дженеріками ти створюєш універсальну скриньку **`Box<T>`**, і просто кажеш: ця скринька для
яблук (**`Box<Apple>`**), ця — для книг (**`Box<Book>`**). І скринька сама знає, з чим працює — і що важливо, **не
переплутає типи**.

---

## **Корисні приклади (🧪)**

### **🔸 Generic клас із одним типом:**

```java
public class Box<T> {
    private T content;

    public void setContent(T content) {
        this.content = content;
    }

    public T getContent() {
        return content;
    }
}

// Використання
Box<String> stringBox = new Box<>();
stringBox.

setContent("Привіт");

String value = stringBox.getContent(); // "Привіт"
```

### **🔸 Generic клас із двома типами:**

```java
public class Pair<K, V> {
    private K key;
    private V value;

    public Pair(K key, V value) {
        this.key = key;
        this.value = value;
    }

    public K getKey() {
        return key;
    }

    public V getValue() {
        return value;
    }
}

// Використання
Pair<String, Integer> age = new Pair<>("Alice", 25);
```

---

## **Порівняння (⚖️)**

|             Без Generic              |              З Generic              |
|:------------------------------------:|:-----------------------------------:|
| **`Object`** використовується як тип |   Тип вказано явно через **`<>`**   |
|       Потрібно робити кастинг        |         Кастинг не потрібен         |
| Немає перевірки типів на компіляції  |    Перевірка типів на компіляції    |
|      Потенційні runtime-помилки      | Безпечніше, типи відомі заздалегідь |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Box\<T\>** — як коробка з етикеткою: “тут зберігається **`T`**”
* **T \= Type**, **E \= Element**, **K \= Key**, **V \= Value**
* “Якщо клас має `<>`, то він розумний щодо типів”

---

## **🟩 Підсумок:**

**Generic-класи** — це спосіб зробити свій код універсальним і типобезпечним. Вони дозволяють створювати повторно
використовувані компоненти, які працюють з будь-яким типом, не втрачаючи при цьому контроль над типами. Правильне
використання дженеріків робить API зрозумілішим і знижує ризик помилок.

---

### **🔗 Додаткові джерела:**

* [**Generics (Oracle Java Tutorial)**](https://docs.oracle.com/javase/tutorial/java/generics/types.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке дженерик-клас (Generic Class) у Java?

> #### **💬 Відповідь:**
>
> **Дженерик-клас** — це клас, який параметризований одним або кількома **типами-параметрами**. Ці типи-параметри вказуються в кутових дужках (`<>`) одразу після назви класу.
>
> Це дозволяє створювати один клас, який може безпечно працювати з різними типами даних, що визначаються під час створення його екземпляра.
>
> **Приклад:**
>
> `public class Box<T> { ... }`
>
> Тут `T` — це тип-параметр, який буде замінений конкретним типом (наприклад, `String`, `Integer` або `User`) при створенні об'єкта `Box`.
>
> **Мета:** Створювати гнучкий, типобезпечний код, який можна використовувати повторно для різних типів даних.

#### **📝 Аналогія:**</h4>

Це як рецепт для пирога з начинкою. Рецепт (`Box`) описує загальні кроки, а начинка (`T`) — це те, що ви вибираєте самі: яблучна, вишнева або м'ясна. Для кожної начинки ви отримуєте свій власний тип пирога (`Box<Apple>`, `Box<Cherry>`).

#### **📌 Що перевіряють:**

*Розуміння базового синтаксису та призначення дженерик-класів.*

* * *

### **2 ❓ Питання:**

Як оголосити простий дженерик-клас і як створити його екземпляр?

> #### **💬 Відповідь:**
>
> **Оголошення дженерик-класу:**
>
> 1. Після назви класу додаються кутові дужки з типом-параметром (наприклад, `<T>`).
> 2. Всередині класу цей тип-параметр `T` можна використовувати як звичайний тип для полів, методів та їхніх аргументів.
>
>     ```java
>     public class Container<T> {
>         private T item;
>
>         public void set(T item) { this.item = item; }
>         public T get() { return item; }
>     }
>     ```
>
> **Створення екземпляра:**
>
> При створенні екземпляра потрібно вказати **конкретний тип-аргумент** замість `T`.
>
> ```java
> // Створюємо контейнер для рядків
> Container<String> stringContainer = new Container<>();
> stringContainer.set("Hello, World!");
> String value = stringContainer.get(); // Приведення типу не потрібне
>
> // Створюємо контейнер для цілих чисел
> Container<Integer> integerContainer = new Container<>();
> integerContainer.set(123);
> Integer number = integerContainer.get();
> ```
>
> Оператор `<>` (діамант) праворуч дозволяє компілятору автоматично вивести тип, що спрощує синтаксис.

#### **📌 Що перевіряють:**

*Практичні навички написання та використання простого дженерик-класу.*

* * *

### **3 ❓ Питання:**

Які існують стандартні конвенції іменування для типів-параметрів?

> #### **💬 Відповідь:**
>
> Так, існують загальноприйняті однолітерні імена, які роблять дженерик-код більш читабельним і передбачуваним:
>
> * **`T`** — **T**ype (Тип)
>   * Найпоширеніший параметр, використовується для загального типу. `class Box<T> { ... }`
>
> * **`E`** — **E**lement (Елемент)
>   * Зазвичай використовується для елементів колекцій. `interface List<E> { ... }`
>
> * **`K`** — **K**ey (Ключ)
>   * Використовується для ключів у мапах та інших асоціативних структурах. `interface Map<K, V> { ... }`
>
> * **`V`** — **V**alue (Значення)
>   * Використовується для значень у мапах. `interface Map<K, V> { ... }`
>
> * **`N`** — **N**umber (Число)
>   * Використовується для типів, що є нащадками `Number`. `class Stats<T extends Number> { ... }`
>
> * **`S`, `U`, `R`** — використовуються для другого, третього, четвертого типу, якщо їх потрібно кілька.
>
> Дотримання цих конвенцій є гарним тоном і спрощує розуміння коду іншими розробниками.

#### **📌 Що перевіряють:**

*Знання стандартних конвенцій, що є ознакою досвіду та дотримання "чистого коду".*

* * *

### **4 ❓ Питання:**

Чи може дженерик-клас мати кілька типів-параметрів? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Так, може. Щоб оголосити кілька типів-параметрів, їх потрібно просто перерахувати через кому в кутових дужках.
>
> Класичним прикладом є клас, що представляє **пару значень (Pair)**, де кожне значення може мати свій власний тип.
>
> ```java
> public class Pair<K, V> {
>     private K key;
>     private V value;
>
>     public Pair(K key, V value) {
>         this.key = key;
>         this.value = value;
>     }
>
>     public K getKey() { return key; }
>     public V getValue() { return value; }
>
>     // ...
> }
> ```
>
> **Використання:**
>
> ```java
> // Створюємо пару, де ключ - рядок, а значення - ціле число
> Pair<String, Integer> studentGrade = new Pair<>("Alice", 95);
> String name = studentGrade.getKey();
> Integer grade = studentGrade.getValue();
>
> // Створюємо іншу пару з іншими типами
> Pair<Integer, String> product = new Pair<>(101, "Laptop");
> ```
>
> Цей підхід широко використовується в мапах (`Map<K, V>`).

#### **📌 Що перевіряють:**

*Розуміння того, як параметризувати клас кількома типами, та здатність навести канонічний приклад.*

* * *

### **5 ❓ Питання:**

Чи можуть статичні члени (поля та методи) дженерик-класу посилатися на його тип-параметр `T`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можуть**.
>
> **Статичні** члени класу належать **самому класу**, а не його екземпляру. Вони існують в єдиному екземплярі для всіх можливих параметризацій дженерик-класу.
>
> Тип-параметр `T` визначається тільки **на рівні екземпляра** (`new Box<String>()`, `new Box<Integer>()`). Оскільки статичне поле є спільним для `Box<String>` і `Box<Integer>`, воно не може знати, до якого саме типу (`String` чи `Integer`) воно має належати.
>
> **Приклад помилок компіляції:**
>
> ```java
> public class GenericStaticExample<T> {
>     // ПОМИЛКА: статичне поле не може мати тип T
>     private static T staticField;
>
>     // ПОМИЛКА: статичний метод не може посилатися на T
>     public static void setStaticField(T field) {
>         staticField = field;
>     }
> }
> ```
>
> Однак, **статичний дженерик-метод** може мати **свій власний** тип-параметр, який не залежить від типу класу:
>
> ```java
> public static <V> void aStaticGenericMethod(V value) { /* ... */ }
> ```

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між контекстом екземпляра та статичним контекстом. Це важливий нюанс, пов'язаний зі стиранням типів.*

* * *

### **6 ❓ Питання:**

Як дженерик-класи пов'язані з успадкуванням? Чи є `List<String>` нащадком `List<Object>`? ⭐️

> #### **💬 Відповідь:**
>
> Це питання стосується **коваріантності** та **інваріантності** в дженеріках.
>
> Хоча `String` є нащадком `Object`, дженерик-тип **`List<String>` НЕ є нащадком `List<Object>`**. Дженеріки в Java є **інваріантними** щодо своїх типів-параметрів.
>
> `List<String> stringList = new ArrayList<>();`
> `List<Object> objectList = stringList; // ПОМИЛКА КОМПІЛЯЦІЇ!`
>
> **Чому так зроблено?**
>
> Це зроблено для забезпечення **типобезпечності**. Якби таке присвоєння було можливим, виникла б така проблема:
>
> ```java
> // Уявімо, що це дозволено:
> List<String> strings = new ArrayList<>();
> List<Object> objects = strings; // Посилаються на той самий список
>
> // Тепер через посилання типу List<Object> ми можемо додати будь-який об'єкт
> objects.add(123); // Додаємо Integer
>
> // Але ж насправді це список рядків! При спробі дістати елемент:
> String s = strings.get(0); // Отримуємо ClassCastException під час виконання
> ```
>
> Щоб запобігти таким ситуаціям, дженеріки є інваріантними. Для гнучкої роботи з ієрархіями потрібно використовувати маски (wildcards), наприклад `List<? extends Object>`.

#### **📌 Що перевіряють:**

*Розуміння інваріантності дженеріків, що є однією з ключових і не завжди інтуїтивних концепцій.*

* * *

### **7 ❓ Питання:**

Чи можна використовувати `instanceof` з параметризованим типом, наприклад `myObject instanceof List<String>`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Код `obj instanceof List<String>` призведе до **помилки компіляції**.
>
> **Причина** — **стирання типів (type erasure)**.
>
> Під час виконання (runtime) вся інформація про тип-аргумент (`<String>`) стирається. JVM бачить тільки "сирий" тип, тобто просто `List`. Вона не може відрізнити `List<String>` від `List<Integer>`, оскільки для неї обидва є просто `List`.
>
> **Що можна робити:**
>
> Ви можете перевірити тільки базовий тип:
> `if (obj instanceof List) { ... }`
>
> Якщо вам все ж потрібно перевірити тип елементів, доведеться ітерувати по колекції і перевіряти тип кожного елемента окремо, але це ненадійно (колекція може бути порожньою).

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів обмежує можливості перевірки типів під час виконання.*

* * *

### **8 ❓ Питання:**

Як дженерик-клас може накласти обмеження на свій тип-параметр (наприклад, щоб він працював тільки з числами)? ⭐️

> #### **💬 Відповідь:**
>
> Для цього використовуються **обмежені типи-параметри (bounded type parameters)** за допомогою ключового слова **`extends`**.
>
> `public class MyClass<T extends UpperBound> { ... }`
>
> Це означає, що тип `T` може бути або самим класом `UpperBound`, або будь-яким його нащадком.
>
> **Приклад: клас для роботи з числами:**
>
> ```java
> // T може бути Integer, Double, Float тощо, бо всі вони є нащадками Number
> public class NumericStats<T extends Number> {
>     private T[] nums;
>
>     public NumericStats(T[] nums) { this.nums = nums; }
>
>     public double average() {
>         double sum = 0.0;
>         for (T num : nums) {
>             // Ми можемо викликати методи Number, наприклад, doubleValue()
>             sum += num.doubleValue();
>         }
>         return sum / nums.length;
>     }
> }
> ```
>
> **Використання:**
> `NumericStats<Integer> intStats = new NumericStats<>(new Integer[]{1, 2, 3});` // OK
> `NumericStats<String> strStats = new NumericStats<>(new String[]{"a"});` // ПОМИЛКА КОМПІЛЯЦІЇ
>
> Таким чином, `extends` дозволяє гарантувати, що тип-параметр матиме певні методи або властивості, успадковані від базового класу/інтерфейсу.

#### **📌 Що перевіряють:**

*Знання механізму обмежених типів (`bounded types`) та його застосування для створення більш спеціалізованих дженерик-класів.*

* * *

### **9 ❓ Питання:**

Уявіть, що ви створюєте дженерик-клас `Cache<K, V>`. Які проблеми виникають при роботі з дженеріками, і як їх вирішувати? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Створення такого класу висвітлює кілька поширених проблем.
>
> ```java
> public class Cache<K, V> {
>     private Map<K, V> cacheMap = new HashMap<>();
>     // ...
> }
> ```
>
> **Проблеми та рішення:**
>
> 1. **Створення масиву `new K[]` або `new V[]`:**
>     * **Проблема:** Не можна створити масив дженерик-типу (`new K[...]`).
>     * **Рішення:** Використовувати колекції (`List<K>`, `List<V>`) або створювати масив `Object[]` і потім приводити тип, що є небезпечним (type-unsafe).
>
> 2. **Створення екземплярів `new K()` або `new V()`:**
>     * **Проблема:** Не можна викликати конструктор дженерик-типу.
>     * **Рішення:** Передавати `Class<K>` та `Class<V>` у конструктор кешу, щоб створювати екземпляри через рефлексію, або передавати `Supplier<V>` як "фабрику" значень за замовчуванням.
>
> 3. **Перевірка `instanceof K` або `instanceof V`:**
>     * **Проблема:** Не можна використовувати `instanceof` з параметризованими типами.
>     * **Рішення:** Знову ж таки, передавати об'єкти `Class<K>` і `Class<V>` і використовувати методи, як-от `keyType.isInstance(obj)`.
>
> 4. **Статичні поля:**
>     * **Проблема:** Не можна оголошувати статичні поля типу `K` або `V`.
>     * **Рішення:** Якщо потрібні статичні константи або кеш, вони не можуть бути дженериками і повинні працювати з `Object` або сирими типами (що погано). Зазвичай статичний контекст і дженерік-типи екземпляра несумісні.

#### **📌 Що перевіряють:**

*Глибоке розуміння обмежень, які накладає стирання типів. Здатність пропонувати стандартні "обхідні шляхи" для цих проблем.*

* * *

### **10 ❓ Питання:**

Як дженерик-клас може реалізовувати дженерик-інтерфейс? Наведіть приклад `Comparable`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Дженерик-клас може реалізовувати дженерик-інтерфейс, передаючи йому свій власний тип-параметр. Це дозволяє створювати ієрархії типів, пов'язаних певним контрактом.
>
> **Приклад з `Comparable`:**
>
> `Comparable<T>` — це дженерик-інтерфейс з методом `int compareTo(T other)`. Він означає, що об'єкт можна порівнювати з іншими об'єктами типу `T`.
>
> Ми можемо створити дженерик-клас `Team`, який можна порівнювати з іншими командами на основі їхнього рейтингу.
>
> ```java
> public class Team<T> implements Comparable<Team<T>> {
>     private String name;
>     private int rating;
>
>     // ...
>
>     @Override
>     public int compareTo(Team<T> other) {
>         // Порівнюємо за рейтингом
>         return Integer.compare(this.rating, other.rating);
>     }
> }
> ```
>
> **Пояснення:**
>
> * Клас `Team<T>` сам по собі є дженериком ( `T` може представляти, наприклад, тип гравців).
> * Він реалізує `Comparable<Team<T>>`. Це означає, що екземпляр `Team<Player>` можна порівнювати **тільки з іншим** `Team<Player>`.
> * Це забезпечує типобезпечність при порівнянні.
>
> Таким чином, дженерики дозволяють будувати складні, але типобезпечні ієрархії успадкування та реалізації.

#### **📌 Що перевіряють:**

*Розуміння того, як поєднуються дженерик-класи та дженерик-інтерфейси. Здатність читати та розуміти складні сигнатури типів.*

* * *

### **11 ❓ Питання:**

Уявіть, що ви маєте дженерик-клас `Box<T>`. Як ви можете створити метод, який приймає інший `Box`, але з будь-яким числовим типом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього потрібно використати **обмежену маску (bounded wildcard)**, а саме `? extends Number`.
>
> **Приклад:**
>
> ```java
> public class Box<T> {
>     private T item;
>
>     // ...
>
>     // Цей метод може порівняти "вагу" свого вмісту з вагою вмісту іншої
>     // коробки, яка містить будь-який числовий тип
>     public void compareWeight(Box<? extends Number> anotherBox) {
>         // Помилка: ми не можемо напряму порівнювати T та невідомий тип
>         // if(this.item > anotherBox.item) {}
>
>         // Але ми можемо безпечно працювати з ними як з Number
>         if(this.item instanceof Number){
>             double thisWeight = ((Number) this.item).doubleValue();
>             double anotherWeight = anotherBox.item.doubleValue(); // Гарантовано має doubleValue()
>             System.out.println("Різниця у вазі: " + (thisWeight - anotherWeight));
>         }
>     }
> }
> ```
>
> * `Box<? extends Number>` означає "коробка, що містить щось, що є нащадком `Number`".
> * Це дозволяє передати в метод `Box<Integer>`, `Box<Double>`, `Box<Float>` і т.д.
> * Всередині методу ми можемо безпечно викликати методи класу `Number` (наприклад, `doubleValue()`), оскільки компілятор гарантує, що невідомий тип є нащадком `Number`.
>
> Це робить API дуже гнучким, дозволяючи працювати з цілими ієрархіями типів.

#### **📌 Що перевіряють:**

*Практичне застосування wildcards для створення гнучких методів у дженерик-класах.*

* * *

### **12 ❓ Питання:**

Що буде, якщо спробувати створити екземпляр дженерик-класу, використовуючи "сирий" тип (`raw type`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Створити екземпляр, використовуючи сирий тип, можна, але компілятор видасть **попередження (warning)** про небезпечну операцію.
>
> `Box rawBox = new Box(); // Компілятор видасть попередження`
>
> **Наслідки:**
>
> 1. **Втрата типобезпечності:** Всередині класу `Box` тип `T` буде стертий до `Object`. Ви втрачаєте всі переваги дженеріків.
> 2. **Можливість `ClassCastException`:**
>
>     ```java
>     Box<String> stringBox = new Box<>();
>     stringBox.set("test");
>
>     // Використання сирого типу
>     Box rawBox = stringBox; // Присвоєння дозволено, але небезпечно
>     rawBox.set(123); // Компілятор дозволяє додати Integer, бо тип "сирий"!
>
>     // Пізніше хтось спробує отримати значення з оригінальної коробки
>     String value = stringBox.get(); // Кине ClassCastException, бо там лежить Integer
>     ```
>
> Використання сирих типів — це спосіб обійти систему типів дженеріків, і він був залишений у мові **тільки для зворотної сумісності** зі старим кодом. У новому коді цього слід уникати.

#### **📌 Що перевіряють:**

*Глибоке розуміння наслідків використання сирих типів та того, як вони "ламають" гарантії, що надаються дженеріками.*

* * *

### **13 ❓ Питання:**

Розглянемо `ArrayList<E>`. Чому його метод `add(E element)` працює, а `toArray()` повертає `Object[]`, а не `E[]`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з **обмеженнями стирання типів**.
>
> * **`add(E element)`**: Цей метод працює, бо він **приймає** об'єкт типу `E`. Компілятор перевіряє тип на етапі компіляції і переконується, що ви додаєте правильний об'єкт. Всередині `ArrayList` зберігає елементи у масиві типу `Object[]`, і приведення типів відбувається неявно.
>
> * **`toArray()` повертає `Object[]`:**
>   * Цей метод повинен створити та повернути масив, що містить елементи списку.
>   * Однак, як ми знаємо, в Java **не можна створити масив дженерик-типу (`new E[]`)**, бо під час виконання тип `E` невідомий.
>   * Тому найбезпечніше, що може зробити цей метод — це створити та повернути масив типу `Object[]`.
>
> **А як же працює `toArray(T[] a)`?**
>
> Існує перевантажена версія `toArray(T[] a)`, яка вирішує цю проблему. Ви **передаєте масив потрібного типу** як аргумент.
>
> ```java
> String[] stringArray = list.toArray(new String[0]);
> ```
>
> У цьому випадку метод використовує переданий масив для визначення типу елементів під час виконання і може створити новий масив правильного типу (`String[]`) за допомогою рефлексії (`Array.newInstance`).

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння реалізації стандартних колекцій та "обхідних шляхів", які вони використовують для подолання обмежень дженеріків.*

* * *

### **14 ❓ Питання:**

Чи можна успадковувати від дженерик-класу, вказавши конкретний тип? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна. Це поширена практика, коли ви хочете створити **спеціалізовану версію** дженерик-класу.
>
> При успадкуванні ви можете "зафіксувати" тип-параметр, зробивши ваш новий клас вже **не-дженериком**.
>
> **Приклад:**
>
> У вас є дженерик-клас для роботи з будь-якими словниками:
>
> ```java
> public class Dictionary<K, V> {
>     // якась логіка
> }
> ```
>
> Ви хочете створити спеціалізований клас для роботи тільки з рядковими словниками.
>
> ```java
> // StringDictionary вже не є дженериком.
> // Він "фіксує" тип V як String.
> public class StringDictionary<K> extends Dictionary<K, String> {
>     // Додаткова логіка, специфічна для рядкових значень
>     public String getConcatenatedValues() {
>         // ...
>     }
> }
> ```
>
> Або можна зафіксувати всі типи:
>
> ```java
> public class EnglishToUkrainianDictionary extends Dictionary<String, String> {
>     // ...
> }
> ```
>
> Цей підхід дозволяє додати нову, специфічну для типу функціональність, при цьому перевикористовуючи загальну логіку з дженерик-суперкласу.

#### **📌 Що перевіряють:**

*Розуміння того, як працює успадкування у поєднанні з дженеріками, та здатність бачити, як це можна використовувати для створення спеціалізованих класів.*

* * *

### **15 ❓ Питання:**

Як можна використовувати дженерик-класи для реалізації типобезпечного гетерогенного контейнера (typesafe heterogeneous container)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це просунутий патерн, описаний у "Effective Java", який дозволяє зберігати значення різних типів в одній колекції, але при цьому отримувати їх типобезпечно, без явних приведень типів.
>
> **Ідея:** Ключем у такій колекції виступає не звичайне значення, а **параметризований об'єкт `Class<T>`**. Це і є "type token".
>
> **Реалізація:**
>
> ```java
> public class Favorites {
>     // Мапа, де ключ - це тип, а значення - об'єкт цього типу
>     private Map<Class<?>, Object> favorites = new HashMap<>();
>
>     // <T> - дженерик-метод, що дозволяє пов'язати тип ключа з типом значення
>     public <T> void putFavorite(Class<T> type, T instance) {
>         favorites.put(Objects.requireNonNull(type), type.cast(instance));
>     }
>
>     public <T> T getFavorite(Class<T> type) {
>         // type.cast() виконує безпечне приведення типу під час виконання
>         return type.cast(favorites.get(type));
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> Favorites f = new Favorites();
> f.putFavorite(String.class, "Java");
> f.putFavorite(Integer.class, 123);
> f.putFavorite(Class.class, Favorites.class);
>
> // Отримання значень відбувається типобезпечно, без явних кастів
> String favoriteString = f.getFavorite(String.class);
> Integer favoriteInteger = f.getFavorite(Integer.class);
> ```
>
> Цей патерн є потужним прикладом того, як дженеріки (зокрема, параметризовані об'єкти `Class<T>`) можуть використовуватися для створення дуже гнучких, але при цьому типобезпечних структур.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про дженеріки та рефлексію. Здатність розуміти та пояснювати складні патерни проектування, які вирішують фундаментальні проблеми типобезпечності.*
