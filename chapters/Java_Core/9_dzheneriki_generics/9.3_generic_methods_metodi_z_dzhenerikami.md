---
layout: default
title: "Generic Methods (Методи з дженеріками)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 3
---

# Generic Methods (Методи з дженеріками)

* короткий опис: Generic-методи – це методи, які мають свій власний параметризований тип (`<T>`), незалежний від дженеріків класу. Тип `T` оголошується перед типом повернення методу.

* поняття: Generic method, `<T> void methodName(...)`.

* приклади: `public static <T> void printItem(T item)`, `public static <K, V> boolean compare(Pair<K, V> p1, Pair<K, V> p2)`.

* ключові моменти: Можуть бути `static`. Дозволяють створювати універсальні утилітарні методи без параметризації всього класу.

* що важливо знати на співбесіді: Синтаксис, коли використовувати, переваги над generic класами.

### **✅ Офіційне визначення:**

**Generic method** — це метод, який має свій власний параметризований тип, незалежний від дженеріків класу. Такий метод починається зі спеціального запису **`<T>`** перед типом повернення.

### **🧠 Простими словами:**

**Generic метод** — це метод, який працює з будь-яким типом, і цей тип ти задаєш під час виклику методу. Він розуміє: "Я не знаю, з чим працюватиму — з рядком, числом чи тостером — але зроблю це правильно".

---

## **📊 Основні риси Generic методів:**

Оголошення починається з параметру типу:

```java
public static <T> void print(T item)
```

* Параметри типу можуть бути кількома: **`<K, V>`, `<T extends Number>`**
* Можна використовувати в static методах
* Не потребують параметризації всього класу

**📌 Коли варто використовувати:**

* Якщо метод має працювати з будь-яким типом
* Якщо клас не потребує дженеріків, а метод — потребує
* Коли потрібно зберегти гнучкість і перевірку типу

---

## **Пояснення на пальцях (💡)**

Уяви, що ти кухар. Іноді ти готуєш суп, іноді піцу, іноді каву. Але твоя техніка (**generic method**) — універсальна: вона може обробити будь-який інгредієнт, бо ти задаєш, з чим вона працює, лише перед стартом. Метод такий собі "**універсальний працівник**", що підлаштовується під ситуацію

---

## **Корисні приклади (🧪)**

### **🔸 Метод із одним параметром типу:**

```java
public class Printer {
    public static <T> void printItem(T item) {
        System.out.println("Item: " + item);
    }
}

// Виклик:
Printer.printItem("Hello");
Printer.printItem(42);
Printer.printItem(true);
```

### **🔸 Метод compare — порівняння пар:**

```java
public class Util {
    public static <K, V> boolean compare(Pair<K, V> p1, Pair<K, V> p2) {
        return p1.getKey().equals(p2.getKey()) &&
               p1.getValue().equals(p2.getValue());
    }
}
```

---

## **Порівняння (⚖️)**

| Статичний generic метод | Generic клас |
| :---: | :---: |
| Не залежить від типів класу | Типи задаються для всього класу |
| Підходить для утиліт | Підходить для структури даних |
| Гнучкість в одному методі | Універсальність у всьому класі |
| Легше для повторного використання | Використовується як шаблон |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **`<T>`** перед типом методу — "тип ще не відомий, але ти скоро дізнаєшся"
* Generic method — метод, що "типує себе сам"
* **`static + <T>`** — завжди дружать
* Утиліти люблять generic methods, як кіт любить коробку

---

## **🟩 Підсумок:**

**Generic методи** — це чудовий інструмент для написання універсальних, багаторазових методів. Вони незалежні від класу і дуже корисні в утилітних структурах, таких як колекції, API, тощо. Вони підвищують гнучкість коду без шкоди для типобезпечності.

---

### **🔗 Додаткові джерела:**

* [**Java Generics \- Methods (Oracle Docs)**](https://docs.oracle.com/javase/tutorial/java/generics/methods.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке дженерик-метод (Generic Method) і як він оголошується?

> #### **💬 Відповідь:**
>
> **Дженерик-метод** — це метод, який має свої **власні типи-параметри**, незалежні від типів-параметрів класу, в якому він оголошений.
>
> **Як він оголошується:**
>
> Оголошення типів-параметрів (наприклад, `<T>`) розміщується **перед типом повернення** методу.
>
> ```java
> public class Utils {
>     // <T> - оголошення типу-параметра для цього методу
>     // void - тип повернення
>     public static <T> void printItem(T item) {
>         System.out.println("Елемент: " + item.toString());
>     }
> }
> ```
>
> Цей метод `printItem` тепер може приймати аргумент будь-якого типу (`String`, `Integer`, `User`), і компілятор автоматично визначить (infer) тип `T` під час виклику.
>
> **Ключова особливість:** дженерик-методи можуть існувати як у дженерик, так і в не-дженерик класах. Вони можуть бути як статичними, так і методами екземпляра.

#### **📝 Аналогія:**</h4>

Це як універсальний інструмент (наприклад, гайковий ключ з регульованим розміром). Ви не створюєте окремий ключ для кожного розміру гайки. Ви берете один ключ (дженерик-метод) і "налаштовуєте" його на потрібний розмір (тип) прямо перед використанням.

#### **📌 Що перевіряють:**

*Знання синтаксису оголошення дженерик-методу та розуміння його основної ідеї — параметризація на рівні методу, а не класу.*

* * *

### **2 ❓ Питання:**

У чому різниця між дженерик-методом та методом у дженерик-класі?

> #### **💬 Відповідь:**
>
> Основна різниця полягає в тому, **де оголошено тип-параметр** і яка його **область видимості**.
>
> * **Метод у дженерик-класі:**
>   * Використовує тип-параметр, оголошений **на рівні класу**.
>   * Область видимості типу `T` — **весь клас**. Всі методи екземпляра можуть його використовувати.
>   * Тип `T` є однаковим для конкретного екземпляра класу.
>
>     ```java
>     public class Box<T> { // T оголошено тут
>         private T item;
>         public void set(T item) { this.item = item; } // Метод використовує T класу
>     }
>     ```
>
> * **Дженерик-метод:**
>   * Оголошує свій **власний тип-параметр**.
>   * Область видимості типу `T` — **тільки цей метод**.
>   * Тип `T` може бути різним при кожному виклику методу.
>
>     ```java
>     public class Converter {
>         public <T, U> U convert(T from, Function<T, U> converter) { // T і U живуть тільки тут
>             return converter.apply(from);
>         }
>     }
>     ```

#### **📌 Що перевіряють:**

*Розуміння різниці між параметризацією на рівні класу та на рівні методу, а також області видимості типів-параметрів.*

* * *

### **3 ❓ Питання:**

Наведіть приклад, коли доцільно використовувати статичний дженерик-метод.

> #### **💬 Відповідь:**
>
> Статичні дженерик-методи ідеально підходять для створення **утилітарних функцій**, які виконують загальні операції над об'єктами різних типів, не потребуючи при цьому створення екземпляра класу.
>
> **Класичний приклад:** Метод, який міняє місцями два елементи в масиві будь-якого типу.
>
> ```java
> public class ArrayUtils {
>     // Не потрібно створювати екземпляр ArrayUtils, щоб викликати цей метод
>     public static <T> void swap(T[] array, int i, int j) {
>         T temp = array[i];
>         array[i] = array[j];
>         array[j] = temp;
>     }
>
>     public static void main(String[] args) {
>         String[] names = {"Alice", "Bob"};
>         swap(names, 0, 1); // Працює для String[]
>         System.out.println(Arrays.toString(names)); // [Bob, Alice]
>
>         Integer[] numbers = {1, 2};
>         swap(numbers, 0, 1); // Працює для Integer[]
>         System.out.println(Arrays.toString(numbers)); // [2, 1]
>     }
> }
> ```
>
> Використання статичного дженерик-методу дозволяє створити одну універсальну утиліту `swap` для всіх типів масивів, не створюючи при цьому `ArrayUtils<T>`.

#### **📌 Що перевіряють:**

*Розуміння того, що статичні дженерик-методи є ідеальним інструментом для написання утилітарних, типобезпечних функцій.*

* * *

### **4 ❓ Питання:**

Що таке "виведення типів" (type inference) для дженерик-методів?

> #### **💬 Відповідь:**
>
> **Виведення типів** — це механізм компілятора Java, який **автоматично визначає тип-аргумент** для дженерик-методу на основі типів аргументів, переданих при його виклику.
>
> Це дозволяє не вказувати тип у кутових дужках явно, що робить код значно коротшим і чистішим.
>
> **Приклад:**
>
> ```java
> public static <T> T pick(T a, T b) { return Math.random() > 0.5 ? a : b; }
>
> // З явним вказанням типу:
> String chosenName = Utils.<String>pick("Alice", "Bob");
>
> // З автоматичним виведенням типу (type inference):
> String chosenNameInferred = Utils.pick("Alice", "Bob"); // Компілятор сам розуміє, що T - це String
> ```
>
> З Java 7 та 8 механізми виведення типів стали значно потужнішими, і сьогодні явне вказання типів для дженерик-методів є рідкістю.

#### **📌 Що перевіряють:**

*Знання концепції "type inference" та її ролі у спрощенні коду, що використовує дженерик-методи.*

* * *

### **5 ❓ Питання:**

Наведіть приклад дженерик-методу з обмеженим типом-параметром (bounded type parameter). ⭐️

> #### **💬 Відповідь:**
>
> Обмежені типи-параметри використовуються, коли потрібно гарантувати, що тип `T` має певні властивості або методи (зазвичай успадковані від якогось базового класу чи інтерфейсу).
>
> **Класичний приклад:** Метод для пошуку мінімального елемента в масиві. Для цього елементи мають бути порівнюваними, тобто реалізовувати інтерфейс `Comparable`.
>
> ```java
> public class MinFinder {
>
>     // <T extends Comparable<T>> - обмежуємо T типами, які можна порівнювати
>     public static <T extends Comparable<T>> T findMin(T[] array) {
>         if (array == null || array.length == 0) {
>             return null;
>         }
>
>         T min = array[0];
>         for (int i = 1; i < array.length; i++) {
>             // Ми можемо викликати compareTo, бо компілятор знає, що T - Comparable
>             if (array[i].compareTo(min) < 0) {
>                 min = array[i];
>             }
>         }
>         return min;
>     }
> }
> ```
>
> Цей метод можна викликати для `Integer[]`, `String[]` або будь-якого іншого масиву об'єктів, класи яких реалізують `Comparable`, але він не скомпілюється для масиву об'єктів, які не можна порівнювати.

#### **📌 Що перевіряють:**

*Практичні навички використання обмежених типів для створення більш функціональних та безпечних дженерик-методів.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати `var` (з Java 10+) з дженерик-методами?

> #### **💬 Відповідь:**
>
> Так, можна, і це ще більше спрощує код.
>
> `var` дозволяє компілятору автоматично виводити тип локальної змінної. Коли ви викликаєте дженерик-метод, компілятор спочатку виводить тип результату цього методу, а потім `var` використовує цей виведений тип для змінної.
>
> **Приклад:**
>
> ```java
> public static <K, V> Pair<K, V> createPair(K key, V value) {
>     return new Pair<>(key, value);
> }
>
> // Без var
> Pair<String, Integer> myPair = Utils.createPair("age", 30);
>
> // З var
> var myPairWithVar = Utils.createPair("age", 30);
> ```
>
> Компілятор спочатку визначає, що `createPair` поверне `Pair<String, Integer>`, а потім присвоює цей тип змінній `myPairWithVar`.
>
> Використання `var` робить код ще більш лаконічним, особливо коли типи-аргументи є довгими та складними.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови (як `var`) та їх взаємодії з існуючими механізмами, як-от дженеріки.*

* * *

### **7 ❓ Питання:**

Як реалізувати дженерик-метод, який працює з двома різними типами-параметрами? Наведіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> Щоб працювати з кількома типами, їх потрібно просто перерахувати через кому в оголошенні типів-параметрів.
>
> **Приклад:** Утилітарний метод, який перевіряє, чи два об'єкти типу `Pair` є рівними за певними критеріями, навіть якщо їхні типи значень різні, але можуть бути порівняні.
>
> ```java
> public class PairUtils {
>
>     // Метод приймає два типи: K - для ключів, V - для значень
>     public static <K, V> boolean arePairsEqual(Pair<K, V> p1, Pair<K, V> p2) {
>         if (p1 == p2) return true;
>         if (p1 == null || p2 == null) return false;
>
>         boolean keysEqual = Objects.equals(p1.getKey(), p2.getKey());
>         boolean valuesEqual = Objects.equals(p1.getValue(), p2.getValue());
>
>         return keysEqual && valuesEqual;
>     }
> }
>
> // Використання:
> Pair<String, Integer> pair1 = new Pair<>("ID", 1);
> Pair<String, Integer> pair2 = new Pair<>("ID", 1);
> boolean isEqual = PairUtils.arePairsEqual(pair1, pair2); // true
> ```
>
> Цей підхід дозволяє створювати узагальнені функції для роботи з комплексними дженерик-типами.

#### **📌 Що перевіряють:**

*Вміння працювати з кількома типами-параметрами та писати дженерик-код для складних структур.*

* * *

### **8 ❓ Питання:**

Уявіть, що ви маєте метод, який працює з `List<Object>`. Чим може бути кращим його перетворення на дженерик-метод `public <T> void processList(List<T> list)`? ⭐️

> #### **💬 Відповідь:**
>
> Перетворення методу на дженерик-метод дає суттєву перевагу в **типобезпечності при виклику**.
>
> * **Метод з `List<Object>`:**
>   * Цей метод може приймати **тільки** `List<Object>`. Ви **не можете** передати в нього `List<String>` або `List<Integer>`, оскільки `List<String>` не є підтипом `List<Object>`.
>   * Це робить метод дуже негнучким.
>
> * **Дженерик-метод `void processList(List<T> list)`:**
>   * Цей метод може бути викликаний з листом **будь-якого типу**: `processList(stringList)`, `processList(integerList)`.
>   * Компілятор автоматично виведе тип `T` (`String`, `Integer` тощо) і забезпечить типобезпечність всередині самого списку.
>
> **Якщо ж вам потрібно приймати будь-який лист і лише читати з нього**, то ще кращим варіантом буде дженерик-метод з wildcard: `void processList(List<?> list)`.
>
> **Висновок:** Дженерик-метод є значно більш гнучким та універсальним, ніж метод, що приймає конкретну параметризацію, як-от `List<Object>`.

#### **📌 Що перевіряють:**

*Глибоке розуміння інваріантності дженеріків та того, як дженерик-методи допомагають вирішити цю проблему для параметрів методів.*

* * *

### **9 ❓ Питання:**

Як би ви написали дженерик-метод, що повертає порожню колекцію, наприклад, `List`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання, яке вирішується статичним фабричним методом `Collections.emptyList()`.
>
> `Collections.emptyList()` є **дженерик-методом**.
>
> ```java
> public class Collections {
>     private static final List<?> EMPTY_LIST = new ArrayList<>(0);
>
>     @SuppressWarnings("unchecked")
>     public static <T> List<T> emptyList() {
>         // Повертає той самий, єдиний, імутабельний екземпляр порожнього списку
>         return (List<T>) EMPTY_LIST;
>     }
> }
> ```
>
> **Як це працює:**
>
> 1. Оголошується статичний дженерик-метод `public static <T> List<T> emptyList()`.
> 2. Він завжди повертає один і той самий **єдиний екземпляр** порожнього списку.
> 3. Компілятор виконує неперевірене приведення типів `(List<T>)`. Це є **безпечним**, тому що список порожній і з нього неможливо отримати елемент, який міг би спричинити `ClassCastException`. Крім того, він імутабельний, тому в нього не можна нічого додати.
>
> Таким чином, ви можете присвоїти результат будь-якій змінній:
> `List<String> strings = Collections.emptyList();`
> `List<Integer> integers = Collections.emptyList();`
>
> Це ефективно, оскільки не створює нових об'єктів для кожного виклику.

#### **📌 Що перевіряють:**

*Знання про те, як реалізовані деякі утилітарні методи JDK, та розуміння, як дженеріки та неперевірені приведення типів можуть безпечно використовуватися для оптимізації.*

* * *

### **10 ❓ Питання:**

Розглянемо такий код: `Collections.max(collection)`. Поясніть, якою є повна сигнатура цього дженерик-методу і що вона означає. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Повна сигнатура цього методу виглядає складною, але демонструє потужність дженеріків:
>
> ```java
> public static <T extends Object & Comparable<? super T>> T max(Collection<? extends T> coll)
> ```
>
> Давайте розберемо її по частинах:
>
> 1. **`<T extends Object & Comparable<? super T>>`** — це оголошення типу-параметра `T`.
>     * `extends Object & Comparable...` — `T` має бути `Object` **і** реалізовувати `Comparable`.
>     * `Comparable<? super T>` — це **рекурсивне обмеження з wildcard**. Воно означає, що `T` має бути порівнюваним з **самим собою або з будь-яким зі своїх предків**. Це потрібно для гнучкості. Наприклад, якщо `Student extends Person`, і `Person` реалізує `Comparable<Person>`, то `Student` можна порівнювати з іншими `Person`, і він підійде під цю умову.
>
> 2. **`T max(...)`** — метод повертає об'єкт типу `T`.
>
> 3. **`Collection<? extends T> coll`** — це параметр.
>     * Метод приймає колекцію, яка містить елементи типу `T` **або будь-якого його нащадка**.
>     * Це слідує принципу **PECS**: колекція `coll` є **виробником (Producer)**, ми з неї тільки читаємо, тому використовуємо `extends`.
>
> Таким чином, цей метод є дуже гнучким, але водночас повністю типобезпечним завдяки складним дженерик-обмеженням.

#### **📌 Що перевіряють:**

*Дуже глибокі знання дженеріків. Здатність читати, розуміти та пояснювати складні дженерик-сигнатури з JDK.*

* * *

### **11 ❓ Питання:**

Чому іноді доводиться явно вказувати тип-аргумент для дженерик-методу (наприклад, `Collections.<String>emptyList()`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Явно вказувати тип-аргумент потрібно в ситуаціях, коли **компілятор не може самостійно вивести (infer) правильний тип** з контексту.
>
> Зазвичай це трапляється, коли:
>
> 1. **Результат методу одразу передається в інший перевантажений метод**, і компілятору незрозуміло, яку саме версію методу вибрати.
>
>     ```java
>     // Уявімо, є два методи:
>     void process(List<String> list) {}
>     void process(List<Integer> list) {}
>
>     // Помилка: неоднозначний виклик. Компілятор не знає,
>     // List<String> чи List<Integer> створює emptyList()
>     process(Collections.emptyList());
>
>     // Рішення: явне вказання типу
>     process(Collections.<String>emptyList());
>     ```
>
> 2. **Метод не має аргументів, з яких можна вивести тип, а результат присвоюється "сирому" типу.**
>
>     ```java
>     // Тут компілятор не знає, що робити, і видає попередження
>     List rawList = Collections.emptyList();
>     ```
>
> В сучасних версіях Java (8+) механізми виведення типів значно покращилися, і такі ситуації виникають все рідше. Однак знати про цю можливість важливо.

#### **📌 Що перевіряють:**

*Розуміння обмежень механізму "type inference" та вміння вирішувати проблеми неоднозначності типів.*

* * *

### **12 ❓ Питання:**

Напишіть дженерик-метод, який приймає `List` будь-чого, що є `Number`, і повертає суму всіх елементів у вигляді `double`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього потрібно використовувати **обмежену маску `<? extends Number>`**. Це дозволить методу приймати `List<Integer>`, `List<Double>` тощо.
>
> ```java
> public class ListCalculator {
>
>     public static double sum(List<? extends Number> numbers) {
>         if (numbers == null) {
>             return 0.0;
>         }
>
>         double total = 0.0;
>         // Ми можемо безпечно ітерувати і викликати методи класу Number
>         for (Number number : numbers) {
>             total += number.doubleValue();
>         }
>         return total;
>     }
>
>     public static void main(String[] args) {
>         List<Integer> ints = List.of(1, 2, 3);
>         List<Double> doubles = List.of(1.5, 2.5);
>
>         System.out.println("Сума Integer: " + sum(ints));     // 6.0
>         System.out.println("Сума Double: " + sum(doubles)); // 4.0
>     }
> }
> ```
>
> * `List<? extends Number>`: параметр методу є **виробником (producer)**, оскільки ми з нього лише читаємо. Тому, згідно з принципом PECS, ми використовуємо `extends`.
> * `for (Number number : numbers)`: Ми можемо безпечно отримати кожен елемент як `Number`, оскільки wildcard гарантує, що тип є нащадком `Number`.
> * `number.doubleValue()`: Ми можемо викликати методи класу `Number`.

#### **📌 Що перевіряють:**

*Практичне вміння застосовувати обмежені маски для написання гнучких утилітарних методів.*

* * *

### **13 ❓ Питання:**

Чи можна оголосити масив дженерик-методів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, не можна. Поняття "масив дженерик-методів" не існує в Java. Методи не є громадянами першого класу в тому ж сенсі, що й об'єкти.
>
> Однак, з появою лямбда-виразів та посилань на методи в Java 8, можна створити **масив функціональних інтерфейсів**, які представляють собою дженерик-методи.
>
> **Приклад:**
>
> ```java
> @FunctionalInterface
> interface MyGenericFunction<T> {
>     T apply(T a, T b);
> }
>
> // Створюємо масив, що містить посилання на дженерик-методи
> // Тут ми фіксуємо тип T як Integer
> MyGenericFunction<Integer>[] operations = new MyGenericFunction[] {
>     (a, b) -> a + b, // посилання на метод "додавання"
>     (a, b) -> a * b  // посилання на метод "множення"
> };
>
> int result = operations[0].apply(10, 20); // 30
> ```
>
> Таким чином, ви не можете мати `MyGenericFunction<T>[]` (масив з невизначеним типом), але можете мати масив `MyGenericFunction<Integer>[]`, що посилається на реалізації дженерик-методу для конкретного типу.

#### **📌 Що перевіряють:**

*Здатність мислити про обмеження мови. Розуміння того, як функціональні інтерфейси можуть бути використані для емуляції більш складних конструкцій.*

* * *

### **14 ❓ Питання:**

Чому такий дженерик-метод не скомпілюється? Як його виправити?
`public <T> T createInstance() { return new T(); }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється через **стирання типів (type erasure)**.
>
> **Причина помилки:**
> Під час виконання (runtime) інформація про тип `T` стирається, і JVM не знає, конструктор якого саме класу (`String`, `User` чи іншого) їй потрібно викликати. Синтаксис `new T()` є невалідним, бо `T` не є конкретним класом.
>
> **Як його виправити:**
>
> Потрібно явно передати інформацію про тип у метод, щоб її можна було використати під час виконання. Найпоширеніший спосіб — передати об'єкт `Class<T>`.
>
> **Виправлена версія:**
>
> ```java
> // Передаємо "type token" - об'єкт Class<T>
> public <T> T createInstance(Class<T> clazz) {
>     try {
>         // Використовуємо рефлексію для створення екземпляра
>         return clazz.getDeclaredConstructor().newInstance();
>     } catch (Exception e) {
>         // Обробка винятків рефлексії
>         throw new RuntimeException("Не вдалося створити екземпляр", e);
>     }
> }
> ```
>
> Тепер метод може створити об'єкт, бо він має доступ до його `Class`-об'єкта, що містить всю необхідну інформацію.
>
> `User user = createInstance(User.class);`

#### **📌 Що перевіряють:**

*Глибоке розуміння обмежень стирання типів та знання стандартного патерну ("type token") для їх обходу.*

* * *

### **15 ❓ Питання:**

Розглянемо `private static <T> void myMethod(T param)`. Якщо викликати його з `Integer` та `String`, чи буде це один і той самий метод в байт-коді, чи два різних? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це буде **один і той самий** метод у байт-коді.
>
> Це прямий наслідок **стирання типів**. Компілятор Java генерує тільки **одну версію** дженерик-методу.
>
> **Що відбувається під час компіляції:**
>
> 1. Компілятор перевіряє типобезпечність при кожному виклику:
>     * При виклику з `Integer` він перевіряє, що ви працюєте з ним як з `Integer`.
>     * При виклику з `String` — як зі `String`.
>
> 2. Після перевірки, тип `T` **стирається** і замінюється на його верхню межу (upper bound). Оскільки в нашому випадку обмеження немає (`<T>`), то `T` замінюється на `Object`.
>
> 3. У байт-коді метод буде мати таку сигнатуру: `private static void myMethod(Object param)`.
>
> 4. Якщо всередині методу ви отримуєте дані і повертаєте їх, компілятор автоматично додає неявні приведення типів (casts) у місцях виклику.
>
> Таким чином, дженеріки в Java — це, по суті, **синтаксичний цукор на рівні компілятора**, який забезпечує типобезпечність, але не створює кількох копій методів у байт-коді, як, наприклад, шаблони (templates) в C++.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як саме працює механізм стирання типів, та його впливу на згенерований байт-код.*
