---
layout: default
title: "Type Erasure (Стирання типу)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 5
---

# Type Erasure (Стирання типу)

* короткий опис: `Type Erasure` – це механізм у Java, за якого всі дженеріки стираються на етапі компіляції. У байткоді параметризовані типи замінюються на `Object` або на їхню верхню межу.

* поняття: Type Erasure, `raw types`, `new T[]` (заборонено), `instanceof T` (заборонено).

* приклади: `List<String>` стає просто `List` у байткоді. `Box<T extends Animal>` стає `Box` з полем типу `Animal`.

* ключові моменти: Забезпечує зворотну сумісність зі старим кодом. Обмежує можливості роботи з дженеріками під час виконання (наприклад, не можна створити масив дженерік-типу).

* що важливо знати на співбесіді: Причина `Type Erasure`, наслідки (не можна `new T[]`, `instanceof T`).

### **✅ Офіційне визначення:**

**Type Erasure** — це механізм у Java, згідно з яким усі параметризовані типи (дженеріки) видаляються на етапі компіляції. Усі типи замінюються на **`Object`**, або на межу типу (bounded type), якщо така задана.

### **🧠 Простими словами:**

Java насправді не зберігає інформацію про дженеріки під час виконання програми. Вона використовує її лише на етапі компіляції для перевірки типів. Потім компілятор “стирає” всі дженеріки, перетворюючи їх на звичайні типи — зазвичай **`Object`**. Тому на етапі виконання твоя **`List<String>`** виглядає як просто **`List`**.

---

## **📊 Важливі моменти:**

* **Мета type erasure** — сумісність із кодом, написаним до JDK 5, коли дженеріків ще не було.
* **Стирається до Object** або до обмеження (**`extends`**) типу.
* **Недоступні дженерік-типи під час виконання.**
* **Типова сигнатура методу з дженеріками виглядає однаково після компіляції.**

---

## **Пояснення на пальцях (💡)**

Уяви, що ти пакуєш різні речі в коробки з написом: "тут яблука", "тут книги". Але перед відправкою всі коробки заклеюють білим скотчем — і всі написи зникають. Поки пакуєш — усе видно (компіляція), але під час доставки (виконання) вже не видно, що всередині. Саме це і є type erasure.

---

## **Корисні приклади (🧪)**

### **🔸 Клас до компіляції:**

```java
public class Box<T> {
    private T content;

    public void setContent(T content) { this.content = content; }

    public T getContent() { return content; }
}
```

### **🔸 Клас після стирання типу (фактично, у байткоді):**

```java
public class Box {
    private Object content;

    public void setContent(Object content) { this.content = content; }

    public Object getContent() { return content; }
}
```

### **🔸 З обмеженням:**

```java
public class Box<T extends Animal> {
    private T content;
    public T getContent() { return content; }
}
```

### **🔸 Після стирання:**

```java
public class Box {
    private Animal content;
    public Animal getContent() { return content; }
}
```

---

## **Порівняння (⚖️)**

| До компіляції (із дженеріками) | Після компіляції (type erasure) |
| :---: | :---: |
| **`Box<T>`** | **`Box`** |
| **`T content`** | **`Object content`** або **`Animal content`** |
| Можна писати **`T item = get()`** | Потрібно кастити: **`(T) get()`** |
| Можна викликати **`new T()`** | ❌ Неможливо, `T` вже не існує |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **“Type Erasure — типи зникають після компіляції”**
* **“\<\> живе тільки до .class файлу”**
* **“Java пам’ятає типи лише до запуску”**
* **"Generics are compile-time sugar" 🍬**

---

## **🟩 Підсумок:**

Стирання типу — це те, що дозволяє дженерікам існувати у Java без зламу зворотної сумісності. Воно гарантує, що програми, написані до JDK 5, продовжать працювати. Але через це Java має деякі обмеження з дженеріками: не можна створювати **`new T()`**, перевіряти **`instanceof`** `T`, або отримувати **`T.class.`** Пам’ятай про ці обмеження і обирай структуру коду відповідно.

---

### **🔗 Додаткові джерела:**

* [**Java Generics \- Type Erasure (Oracle Docs)**](https://docs.oracle.com/javase/tutorial/java/generics/erasure.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке стирання типів (Type Erasure) у Java і яка його головна мета?

> #### **💬 Відповідь:**
>
> **Стирання типів** — це механізм, який використовує компілятор Java, щоб реалізувати дженеріки. Суть цього механізму полягає в тому, що після перевірки типобезпечності на етапі компіляції, вся інформація про типи-параметри (наприклад, `<String>` у `List<String>`) **видаляється** з байт-коду.
>
> **Як це працює:**
>
> 1. Дженерик-типи замінюються на їхні "межі" (bounds) або на `Object`, якщо межі не вказано. Наприклад, `List<T>` стає просто `List`, а `List<T extends Number>` стає `List`, що працює з `Number`.
> 2. Компілятор автоматично додає необхідні **приведення типів** (`casts`) там, де це потрібно (наприклад, при отриманні елемента з колекції).
>
> **Головна мета:**
>
> **Забезпечення зворотної сумісності (backward compatibility)**. Дженеріки були додані в Java 5. Щоб новий код з дженеріками міг працювати зі старими бібліотеками (написаними до Java 5) і старою JVM, було вирішено не змінювати байт-код кардинально, а реалізувати перевірку типів тільки на рівні компілятора.

#### **📝 Аналогія:**

Уявіть, що у вас є набір формочок для печива різної форми (дженеріки). Ви використовуєте їх, щоб вирізати тісто правильної форми (перевірка типів на етапі компіляції). Але коли печиво потрапляє в піч (runtime), всі вони стають просто "печивом" (`Object`), і інформація про конкретну формочку "стирається".

#### **📌 Що перевіряють:**

*Розуміння того, що таке стирання типів, і, найголовніше, знання його ключової причини — зворотної сумісності.*

* * *

### **2 ❓ Питання:**

Як `List<String>` виглядає у байт-коді після стирання типів?

> #### **💬 Відповідь:**
>
> Після стирання типів, `List<String>` у байт-коді перетворюється на свій "сирий" тип, тобто просто **`List`**.
>
> **Що це означає на практиці:**
>
> * Клас `List` (точніше, його реалізації, як `ArrayList`) внутрішньо працює з масивом типу **`Object[]`**.
> * Інформація про те, що цей список призначений для `String`, повністю **відсутня під час виконання (runtime)**.
>
> **Приклад перетворення:**
>
> **Код, який ви пишете:**
>
> ```java
> List<String> list = new ArrayList<>();
> list.add("hello");
> String s = list.get(0);
> ```
>
> **Код, який "бачить" JVM (спрощено):**
>
> ```java
> List list = new ArrayList();
> list.add("hello");
> String s = (String) list.get(0); // Компілятор додає неявний каст
> ```
>
> Таким чином, вся магія дженеріків відбувається на етапі компіляції, а під час виконання код працює так, ніби дженеріків ніколи й не було.

#### **📌 Що перевіряють:**

*Розуміння того, як конкретний параметризований тип перетворюється на сирий тип у згенерованому коді.*

* * *

### **3 ❓ Питання:**

Які основні наслідки та обмеження накладає стирання типів?

> #### **💬 Відповідь:**
>
> Стирання типів призводить до кількох важливих обмежень, з якими стикаються розробники:
>
> 1. **Не можна використовувати примітивні типи:** Ви не можете написати `List<int>`, тому що `int` не є `Object`. Потрібно використовувати класи-обгортки (`List<Integer>`).
>
> 2. **Не можна отримати інформацію про тип-аргумент під час виконання:**
>     * `instanceof List<String>` — неможливо. JVM бачить тільки `instanceof List`.
>     * `List<String>.class` — неможливо.
>
> 3. **Не можна створювати екземпляри типу-параметра:**
>     * `new T()` — неможливо, оскільки JVM не знає, що таке `T`.
>
> 4. **Не можна створювати масиви дженерик-типів:**
>     * `new List<String>[10]` — неможливо через конфлікт між механізмами типобезпечності дженеріків (compile-time) та масивів (runtime).
>
> 5. **Статичні члени не можуть посилатися на тип-параметр класу:** Статичне поле є спільним для всіх екземплярів (`List<String>`, `List<Integer>`), тому воно не може бути типу `T`.
>
> Всі ці обмеження є прямим наслідком того, що інформація про дженерик-тип втрачається після компіляції.

#### **📌 Що перевіряють:**

*Чітке знання основних обмежень, які накладає type erasure. Це найважливіша частина теми.*

* * *

### **4 ❓ Питання:**

Поясніть, чому такий код `new T()` не скомпілюється.

> #### **💬 Відповідь:**
>
> Код `new T()` не скомпілюється через **стирання типів**.
>
> 1. **На етапі компіляції** `T` є просто "заповнювачем" (placeholder), який може бути замінений будь-яким типом (`String`, `User` тощо). Компілятор не знає, який саме конструктор потрібно викликати.
>
> 2. **На етапі виконання (runtime)** інформація про конкретний тип `T` **повністю стирається** і замінюється на `Object` (або на межу). JVM бачить запит на створення об'єкта `new Object()`, а не `new User()` чи `new String()`. Вона просто не має інформації про те, який саме клас потрібно інстанціювати.
>
> Щоб обійти це обмеження, потрібно **явно передати інформацію про тип** під час виконання. Зазвичай це робиться через передачу об'єкта `Class<T>` або фабрики `Supplier<T>`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, чому саме стирання типів робить створення екземпляра `T` неможливим.*

* * *

### **5 ❓ Питання:**

Чому заборонено створювати масиви дженерик-типів, наприклад, `new List<String>[10]`? ⭐️

> #### **💬 Відповідь:**
>
> Це заборонено через **фундаментальний конфлікт** між тим, як забезпечується типобезпечність у дженеріків та у масивів.
>
> * **Дженеріки:** Типобезпечність перевіряється **на етапі компіляції**. Під час виконання типи стираються (`List<String>` стає `List`).
>
> * **Масиви:** Типобезпечність перевіряється **під час виконання**. Об'єкт масиву знає свій тип (наприклад, `String[]`). Спроба покласти в `String[]` об'єкт `Integer` призведе до `ArrayStoreException` в рантаймі.
>
> **Якби створення `new List<String>[10]` було дозволено, виникла б така "діра" в системі типів:**
>
> ```java
> // 1. Припустимо, це дозволено
> List<String>[] stringLists = new List<String>[10];
>
> // 2. Масиви в Java є коваріантними, тому це присвоєння валідне
> Object[] objectArray = stringLists;
>
> // 3. Тепер ми "обманюємо" систему типів
> List<Integer> intList = new ArrayList<>();
> intList.add(42);
> objectArray[0] = intList; // Помилка не виникає тут, бо масив тепер типу Object[]!
>
> // 4. Пізніше хтось намагається дістати елемент і очікує List<String>
> String s = stringLists[0].get(0); // Отримуємо ClassCastException під час виконання!
> ```
>
> Щоб запобігти таким ситуаціям, які порушують гарантії типобезпечності дженеріків, створення масивів дженерик-типів було заборонено.

#### **📌 Що перевіряють:**

*Розуміння концепції коваріантності масивів та її конфлікту з інваріантністю та стиранням типів у дженеріках.*

* * *

### **6 ❓ Питання:**

Що таке "неперевірені" попередження (`unchecked warnings`), і як вони пов'язані зі стиранням типів? ⭐️

> #### **💬 Відповідь:**
>
> **"Неперевірені" попередження** — це попередження від компілятора (`unchecked cast`, `unchecked call` тощо), які сигналізують, що він **не може гарантувати типобезпечність** операції через стирання типів.
>
> Вони виникають, коли ви виконуєте операцію, що потенційно може призвести до `ClassCastException` під час виконання, і компілятор не може це перевірити.
>
> **Як вони пов'язані зі стиранням типів:**
>
> Зазвичай вони виникають при змішуванні **сирого (raw)** та **параметризованого** коду.
>
> **Приклад:**
>
> ```java
> List rawList = new ArrayList();
> rawList.add(123);
>
> // Коли ми присвоюємо сирий список параметризованому,
> // компілятор не може перевірити, чи містить rawList тільки рядки.
> List<String> stringList = rawList; // тут компілятор видасть unchecked warning
>
> String s = stringList.get(0); // тут буде ClassCastException
> ```
>
> Компілятор каже: "Я бачу, що ти робиш щось потенційно небезпечне через те, що типи були стерті, але я дозволю тобі це зробити, попереджаючи тебе про ризик".
>
> Такі попередження слід виправляти. Якщо ви впевнені, що операція безпечна, можна пригнічити попередження за допомогою анотації `@SuppressWarnings("unchecked")`.

#### **📌 Що перевіряють:**

*Розуміння того, що таке "unchecked" попередження, чому вони виникають, і як з ними правильно поводитися.*

* * *

### **7 ❓ Питання:**

Як стирання типів працює з обмеженими дженеріками (`<T extends Number>`)?

> #### **💬 Відповідь:**
>
> Коли для типу-параметра є обмеження, компілятор під час стирання замінює його **не на `Object`**, а на **клас-обмеження**.
>
> **Приклад:**
>
> **Код, який ви пишете:**
>
> ```java
> public class NumberBox<T extends Number> {
>     private T value;
>     public T get() { return value; }
>     public void inspect() { System.out.println(value.doubleValue()); }
> }
> ```
>
> **Як це виглядає після стирання типів:**
>
> ```java
> public class NumberBox {
>     private Number value; // T замінюється на Number
>     public Number get() { return value; }
>     public void inspect() {
>         // Компілятор додає неявний каст
>         System.out.println(((Number) value).doubleValue());
>     }
> }
> ```
>
> Це дозволяє компілятору і JVM знати, що поле `value` є як мінімум `Number`, тому виклик методу `doubleValue()` є валідним. При отриманні значення ззовні (`NumberBox<Integer> box = ...; Integer i = box.get();`), компілятор додасть ще один неявний каст до `Integer`.

#### **📌 Що перевіряють:**

*Розуміння того, як `bounded types` впливають на процес стирання, замінюючи `Object` на більш конкретний тип.*

* * *

### **8 ❓ Питання:**

Що таке "мостикові" методи (bridge methods) і як вони пов'язані зі стиранням типів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"Мостикові" методи** — це синтетичні (згенеровані компілятором) методи, які створюються для того, щоб зберегти **поліморфізм** після стирання типів.
>
> **Проблема:**
>
> Розглянемо дженерик-інтерфейс та його реалізацію.
>
> ```java
> interface MyComparable<T> { void compare(T other); }
>
> class MyNumber implements MyComparable<MyNumber> {
>     @Override
>     public void compare(MyNumber other) { /* ... */ }
> }
> ```
>
> Після стирання типів:
> `interface MyComparable { void compare(Object other); }`
> `class MyNumber implements MyComparable { public void compare(MyNumber other) { ... } }`
>
> Метод `compare(MyNumber)` **не перевизначає** метод `compare(Object)` з інтерфейсу, бо у них **різні сигнатури**! Це зламало б поліморфізм.
>
> **Рішення ("мостиковий" метод):**
>
> Компілятор генерує в класі `MyNumber` **додатковий, невидимий для вас, "мостиковий" метод**:
>
> ```java
> // Синтетичний міст
> public void compare(Object other) {
>     // Він приймає Object і викликає ваш, конкретний метод
>     compare((MyNumber) other);
> }
> ```
>
> Цей "міст" задовольняє вимоги інтерфейсу (`MyComparable`) і делегує виклик вашому типізованому методу, зберігаючи поліморфізм і типобезпечність.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішні механізми роботи компілятора. Здатність пояснити, як JVM "обходить" проблеми, створені стиранням типів, для підтримки основних принципів ООП.*

* * *

### **9 ❓ Питання:**

Чи може `equals()` бути перевантажений для роботи з дженериками? Чи спрацює такий код? ⭐️⭐️
`public boolean equals(List<String> other)`

> #### **💬 Відповідь:**
>
> **Технічно**, ви можете створити такий перевантажений метод. **Практично**, він **не буде** викликатися так, як ви очікуєте, і це призведе до помилок.
>
> ```java
> public class MyList extends ArrayList<String> {
>     // Перевантаження, а НЕ перевизначення!
>     public boolean equals(List<String> other) { /* ... */ }
>
>     // Оригінальний equals(Object) успадковано
> }
> ```
>
> **Проблема:**
>
> Метод `equals` має сигнатуру `equals(Object obj)`. Коли ви викликаєте `list1.equals(list2)`, поліморфізм працює з `Object`, тому буде викликано `equals(Object)`, а не ваш `equals(List<String>)`.
>
> **Наслідки:**
>
> * Ваша нова логіка порівняння не буде використовуватися, коли об'єкти порівнюються через `equals(Object)` (наприклад, у `HashSet` чи `HashMap`).
> * Ви можете порушити контракт симетричності: `a.equals(b)` може викликати одну версію, а `b.equals(a)` — іншу.
>
> **Чому це пов'язано зі стиранням типів?**
>
> Під час компіляції сигнатура `equals(List<String> other)` після стирання стає `equals(List other)`. JVM бачить два методи: `equals(Object)` і `equals(List)`. Вона вибере найбільш специфічний (`equals(List)`) тільки якщо статичний тип об'єкта є `List`, але в поліморфних сценаріях завжди буде викликатися `equals(Object)`.
>
> **Правильне рішення:** **Завжди перевизначайте (`@Override`) метод `public boolean equals(Object obj)`** і всередині нього виконуйте перевірки типів.

#### **📌 Що перевіряють:**

*Розуміння різниці між перевантаженням (overloading) та перевизначенням (overriding) та того, як стирання типів може вплинути на вибір методу.*

* * *

### **10 ❓ Питання:**

Як отримати інформацію про дженерик-тип під час виконання (наприклад, `String` у `List<String>`)? Які є підходи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Отримати цю інформацію напряму неможливо через стирання типів, але є кілька "обхідних шляхів":
>
> 1. **Зберегти `Class<T>` об'єкт (Type Token):**
>     * При створенні дженерик-класу ви явно передаєте об'єкт `Class` для типу-параметра.
>     * `public MyClass(Class<T> type) { this.type = type; }`
>     * Цей об'єкт `Class` можна потім використовувати для перевірок або створення екземплярів через рефлексію.
>
> 2. **Для полів та методів з відомим дженериком:**
>     * Якщо дженерик-тип є частиною сигнатури поля або методу, його можна отримати через рефлексію.
>
>     ```java
>     public List<String> myList;
>     // ...
>     Field field = MyClass.class.getDeclaredField("myList");
>     Type genericType = field.getGenericType();
>     // genericType буде ParameterizedType, з якого можна дістати <String>
>     ```
>
> 3. **Super Type Token (найпотужніший підхід):**
>     * Створюється анонімний підклас, який "захоплює" інформацію про дженерик-тип свого суперкласу.
>
>     ```java
>     TypeToken<List<String>> token = new TypeToken<>() {};
>     ```
>
>     * Потім за допомогою рефлексії (`getGenericSuperclass()`) можна витягнути параметризований тип `List<String>`. Цей підхід використовується в бібліотеках, як-от `Guava`, `Jackson`, `Gson`.
>
> Ці техніки дозволяють обійти обмеження, накладені стиранням типів, у ситуаціях, де інформація про тип у рантаймі є критично важливою.

#### **📌 Що перевіряють:**

*Знання просунутих технік рефлексії та патернів, що використовуються для роботи з дженериками в рантаймі.*

* * *

### **11 ❓ Питання:**

Чому цей код працює, хоча здається, що він порушує типобезпечність? `List list = new ArrayList<Integer>(); list.add("hello");` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код **скомпілюється**, але **видасть попередження (`unchecked call`)**.
>
> Давайте розберемо, чому він працює до певного моменту:
>
> 1. `List list = new ArrayList<Integer>();`
>     * Ви створюєте `ArrayList`, який параметризований як `Integer`.
>     * Але ви присвоюєте його "сирій" (`raw`) змінній `List`. У цей момент **компілятор "вимикає"** перевірку типів для всіх операцій з цією змінною `list`.
>
> 2. `list.add("hello");`
>     * Оскільки `list` є сирим типом, його метод `add` після стирання має сигнатуру `add(Object o)`.
>     * Тому компілятор дозволяє додати туди `String` (або будь-який інший `Object`), хоча фізично "під капотом" цей об'єкт додається в `ArrayList`, який був спочатку створений як `<Integer>`.
>
> **Де виникне проблема?**
> Проблема виникне, коли хтось спробує працювати з цією колекцією в типобезпечному контексті.
>
> ```java
> List<Integer> intList = new ArrayList<>();
> List rawList = intList;
> rawList.add("не число");
>
> Integer num = intList.get(0); // Кине ClassCastException!
> ```
>
> Це демонструє, чому сирі типи є "дірою" в системі типобезпечності і чому їхнє використання є небезпечним.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як взаємодіють сирі та параметризовані типи, та усвідомлення відкладеного характеру помилки.*

* * *

### **12 ❓ Питання:**

Розглянемо `private T[] elements;`. Як `ArrayList` обходить обмеження `new T[]`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `ArrayList` всередині себе використовує **хитрість** для обходу цього обмеження.
>
> Його внутрішнє сховище оголошено не як `T[]`, а як `Object[]`.
>
> ```java
> public class ArrayList<E> {
>     transient Object[] elementData; // Зберігає елементи як Object
>     // ...
> }
> ```
>
> 1. **При додаванні (`add(E e)`):** Елемент просто додається в масив `Object[]`.
>
> 2. **При отриманні (`get(int index)`):** Метод `get` виконує **неявне неперевірене приведення типу**.
>
>     ```java
>     @SuppressWarnings("unchecked")
>     E elementData(int index) {
>         return (E) elementData[index]; // Ось ця небезпечна операція
>     }
>     public E get(int index) {
>         // ...
>         return elementData(index);
>     }
>     ```
>
> Цей `cast` до `(E)` є "неперевіреним", тому що під час виконання тип `E` стертий. Однак ця операція вважається **безпечною** в контексті `ArrayList`, тому що **механізм дженеріків на етапі компіляції** гарантує, що в `ArrayList<String>` можна покласти тільки `String`, отже, елементи в `elementData` будуть тільки типу `String`.
>
> Ця реалізація є стандартним "обхідним шляхом" для дженерик-колекцій, які базуються на масивах.

#### **📌 Що перевіряють:**

*Знання внутрішньої реалізації ключових колекцій з JDK. Розуміння того, як `@SuppressWarnings("unchecked")` використовується в обґрунтованих випадках.*

* * *

### **13 ❓ Питання:**

Що поверне `new ArrayList<String>().getClass() == new ArrayList<Integer>().getClass()` і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз поверне **`true`**.
>
> **Причина:**
>
> Через **стирання типів**, під час виконання і `ArrayList<String>`, і `ArrayList<Integer>` стають просто `ArrayList`.
>
> 1. `new ArrayList<String>().getClass()` повертає `java.util.ArrayList.class`.
> 2. `new ArrayList<Integer>().getClass()` повертає `java.util.ArrayList.class`.
>
> Оскільки обидва методи `getClass()` повертають той самий об'єкт `Class`, оператор `==` дає `true`.
>
> Це ще раз наочно демонструє, що **JVM під час виконання не розрізняє параметризовані типи одного й того ж дженерик-класу**. Вся інформація про `String` та `Integer` для неї втрачена.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про стирання типів до конкретних виразів з рефлексією та передбачити їхній результат.*

* * *

### **14 ❓ Питання:**

Чи можна зловити дженерик-виняток, наприклад, `catch (MyException<String> e)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Дженерик-класи **не можуть** напряму успадковувати від `Throwable` (а отже, і від `Exception` та `Error`). Спроба створити `class MyException<T> extends Exception {}` призведе до **помилки компіляції**.
>
> **Причина:**
>
> Механізм обробки винятків (`catch`) працює **під час виконання (runtime)**. JVM повинна точно знати, який тип винятку ловити. Через **стирання типів** вона не змогла б відрізнити `MyException<String>` від `MyException<Integer>`, бо для неї обидва були б просто `MyException`.
>
> Це створило б неоднозначність і зробило б механізм `catch` ненадійним.
>
> Однак, **дженерик-клас може оголошувати, що він кидає виняток, тип якого залежить від типу-параметра**, але це вважається поганою практикою і може призвести до неперевірених попереджень.
>
> `public <T extends Throwable> void doWork() throws T { ... }`
>
> Але ловити `catch (T e)` також не можна.

#### **📌 Що перевіряють:**

*Знання про обмеження, які стосуються системи обробки винятків та їх взаємодії з дженеріками.*

* * *

### **15 ❓ Питання:**

У C++ шаблони (templates) працюють інакше, ніж дженеріки в Java. У чому ключова відмінність і як це пов'язано зі стиранням типів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ключова відмінність полягає в механізмі реалізації.
>
> * **Java Generics (реалізація через стирання типів):**
>   * Компілятор генерує **одну** версію байт-коду для дженерик-класу, де типи стерті до `Object` або їхніх меж.
>   * Це робиться для зворотної сумісності.
>   * **Наслідки:** Втрата інформації про типи в рантаймі, обмеження ( `new T[]`, `instanceof` тощо), але менший розмір `jar`-файлів.
>
> * **C++ Templates (реалізація через генерацію коду):**
>   * Компілятор C++ створює **окрему копію коду** для **кожної конкретної параметризації** шаблону. Якщо ви використовуєте `vector<int>` і `vector<string>`, буде згенеровано дві абсолютно різні версії класу `vector`.
>   * Це називається **мономорфізацією**.
>   * **Наслідки:**
>     * **Повна інформація про типи** доступна під час виконання.
>     * **Немає обмежень**, як у Java. `new T()` та інші операції є валідними.
>     * **Потенційне роздуття коду (code bloat):** Розмір скомпільованої програми може значно зрости, якщо шаблон використовується з великою кількістю різних типів.
>
> **Коротко:** Дженеріки Java — це "один код для всіх" з перевірками під час компіляції. Шаблони C++ — це "новий код для кожного типу". Стирання типів є унікальною особливістю реалізації Java.

#### **📌 Що перевіряють:**

*Широкий кругозір. Знання не тільки Java, а й того, як схожі проблеми вирішуються в інших мовах. Здатність порівнювати різні підходи та аналізувати їхні компроміси.*
