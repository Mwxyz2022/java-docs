---
layout: default
title: "Generic Inheritance (Успадкування з дженеріками)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 10
---

# Generic Inheritance (Успадкування з дженеріками)

* короткий опис: Механізм, за допомогою якого класи або інтерфейси, що використовують параметри типу, можуть бути успадковані або реалізовані іншими класами з збереженням або уточненням цих параметрів.

* поняття: `Generic Inheritance`, фіксований тип, передача параметрів.

* приклади: `class StringBox extends Box<String>` (фіксований тип), `class MyBox<T> extends Box<T>` (передача параметра), `class OrderedPair<K, V> implements Pair<K, V>`.

* ключові моменти: Дозволяє як конкретизувати тип (`StringBox`), так і передавати параметри далі по ієрархії (`CustomBox<T>`).

* що важливо знати на співбесіді: Як успадковувати generic класи/інтерфейси.

### **✅ Офіційне визначення:**

**Generic Inheritance** — це механізм, за допомогою якого клас або інтерфейс, що використовує параметри типу, може бути успадкований або реалізований іншим класом із збереженням або уточненням цих параметрів.

### **🧠 Простими словами:**

Успадкування з дженеріками — це коли ти продовжуєш клас або реалізуєш інтерфейс, який використовує **`<T>`**, і можеш або залишити цей тип відкритим (**`<T>`**), або конкретизувати його (**`String`**, **`Integer`** тощо).

---

## **📊 Варіанти успадкування:**

* **Фіксований тип при спадкуванні:**

```java
class StringBox extends Box<String> { }
```

* **Передача параметрів далі:**

```java
class MyBox<T> extends Box<T> { }
```

* **Кілька параметрів у інтерфейсах/класах:**

```java
interface Pair<K, V> { ... }
class OrderedPair<K, V> implements Pair<K, V> { ... }
```

* **Можна обмежити тип під час успадкування:**

```java
class NumberBox<T extends Number> extends Box<T> { }
```

---

## **Пояснення на пальцях (💡)**

Уяви, що ти створюєш шаблон коробки (**`Box<T>`**), а потім вирішуєш:

— “Хочу коробку лише для рядків” -> **`StringBox`** `extends Box<String>`

— “Хочу коробку, яка буде універсальною, але з додатковими можливостями” -> **`class`** `MyBox<T> extends Box<T>`.

Це як мати універсальну інструкцію (generic), і передати її далі або прикрутити її до конкретного випадку

---

## **Корисні приклади (🧪)**

### **🔸 Generic-клас:**

```java
public class Box<T> {
    private T content;
    public void setContent(T content) { this.content = content; }
    public T getContent() { return content; }
}
```

### **🔸 Успадкування з фіксованим типом:**

```java
public class StringBox extends Box<String> {
    public void printUpperCase() {
        System.out.println(getContent().toUpperCase());
    }
}
```

### **🔸 Успадкування з переданим параметром:**

```java
public class CustomBox<T> extends Box<T> {
    public void printContent() {
        System.out.println(getContent());
    }
}
```

### **🔸 Інтерфейс з дженеріками:**

```java
public interface Pair<K, V> {
    K getKey();
    V getValue();
}

public class OrderedPair<K, V> implements Pair<K, V> {
    private final K key;
    private final V value;

    public OrderedPair(K key, V value) {
        this.key = key;
        this.value = value;
    }

    public K getKey() { return key; }
    public V getValue() { return value; }
}
```

---

## **Порівняння (⚖️)**

| Тип спадкування | Особливість |
| ----- | ----- |
| **`class A<T> extends B<T>`** | Передає параметр далі |
| **`class A extends B<String>`** | Фіксує тип при успадкування |
| **`implements Interface<T>`** | Параметризує інтерфейс |
| **`implements Interface<String>`** | Конкретизує тип інтерфейсу |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **“T” іде у спадок, поки не скажеш “Я String”**
* **“extends” передає, “extends з типом” фіксує**
* **“Generic \= інструкція, спадкування \= готовий виріб”**

---

## **🟩 Підсумок:**

Успадкування з дженеріками — це ще одна гнучка можливість Java, яка дозволяє як передавати параметри типів у підкласи, так і конкретизувати їх. Такий підхід дозволяє створювати розширювані та повторно використовувані структури, які можуть бути як загальними, так і спеціалізованими.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Inheritance and Generics**](https://docs.oracle.com/javase/tutorial/java/generics/inheritance.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке успадкування з дженеріками (Generic Inheritance)?

> #### **💬 Відповідь:**
>
> **Успадкування з дженеріками** — це механізм, який дозволяє створювати класи або інтерфейси, що розширюють або реалізують інші дженерик-класи чи інтерфейси.
>
> Існує два основних сценарії:
>
> 1. **Конкретизація (фіксація) типу:** Підклас може вказати конкретний тип для типу-параметра свого батьківського класу. У цьому випадку підклас сам може бути не-дженериком.
>     * `class StringBox extends Box<String> { ... }`
>
> 2. **Продовження параметризації:** Підклас може залишатися дженериком і "прокидати" свої типи-параметри вгору по ієрархії до батьківського класу.
>     * `class SpecializedBox<T> extends Box<T> { ... }`
>
> Цей механізм дозволяє будувати складні та типобезпечні ієрархії класів, поєднуючи принципи ООП та узагальненого програмування.

#### **📝 Аналогія:**

Уявіть, що `Vehicle<T>` — це дженерик-клас для будь-якого транспортного засобу, що перевозить вантаж типу `T`.

* **Конкретизація:** `GrainTruck extends Vehicle<Grain>` — вантажівка, призначена **тільки для зерна**.
* **Продовження параметризації:** `Truck<T> extends Vehicle<T>` — більш спеціалізований тип транспортного засобу (вантажівка), але він все ще може перевозити будь-який вантаж `T`.

#### **📌 Що перевіряють:**

*Розуміння двох основних способів успадкування від дженерик-типів: з фіксацією типу та з продовженням параметризації.*

* * *

### **2 ❓ Питання:**

Наведіть приклад успадкування від дженерик-класу, де підклас **конкретизує** тип.

> #### **💬 Відповідь:**
>
> У цьому випадку підклас стає звичайним, не-дженерик класом, оскільки він фіксує тип-параметр, успадкований від батька.
>
> **Батьківський дженерик-клас:**
>
> ```java
> public class DataStorage<T> {
>     private T data;
>     public void setData(T data) { this.data = data; }
>     public T getData() { return this.data; }
> }
> ```
>
> **Підклас, що конкретизує тип:**
>
> ```java
> // Цей клас працює ТІЛЬКИ з рядками. Він успадковує всю логіку
> // від DataStorage<String>.
> public class StringStorage extends DataStorage<String> {
>
>     // Може додавати власну, специфічну для String логіку
>     public int getLength() {
>         // Метод getContent() тепер гарантовано повертає String
>         return getData() != null ? getData().length() : 0;
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> StringStorage stringStorage = new StringStorage();
> stringStorage.setData("hello"); // OK
> // stringStorage.setData(123); // ПОМИЛКА КОМПІЛЯЦІЇ
> String data = stringStorage.getData(); // Повертає String, кастинг не потрібен
> ```

#### **📌 Що перевіряють:**

*Практичні навички реалізації першого сценарію успадкування (з фіксацією типу).*

* * *

### **3 ❓ Питання:**

Наведіть приклад успадкування, де підклас **залишається дженериком**.

> #### **💬 Відповідь:**
>
> У цьому випадку підклас оголошує свій власний тип-параметр і "передає" його батьківському класу. Це дозволяє розширювати функціональність дженерик-класу, залишаючи його гнучким.
>
> **Батьківський дженерик-клас:**
>
> ```java
> public class Box<T> {
>     protected T item;
>     public void put(T item) { this.item = item; }
> }
> ```
>
> **Підклас, що залишається дженериком:**
>
> ```java
> // Клас також є дженериком з типом-параметром T,
> // який передається в Box<T>
> public class LoggingBox<T> extends Box<T> {
>
>     // Додає нову поведінку
>     @Override
>     public void put(T item) {
>         System.out.println("Додаємо елемент: " + item);
>         super.put(item);
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> LoggingBox<Integer> intBox = new LoggingBox<>();
> intBox.put(42); // Виведе "Додаємо елемент: 42"
> ```

#### **📌 Що перевіряють:**

*Практичні навички реалізації другого сценарію успадкування (з продовженням параметризації).*

* * *

### **4 ❓ Питання:**

Чи можна реалізувати дженерик-інтерфейс, не роблячи сам клас дженериком?

> #### **💬 Відповідь:**
>
> Так, можна. Це той самий випадок, що й успадкування з **конкретизацією типу**. Ваш клас реалізує дженерик-інтерфейс, вказуючи для його типів-параметрів конкретні типи.
>
> **Дженерик-інтерфейс:**
>
> ```java
> public interface Comparable<T> {
>     int compareTo(T other);
> }
> ```
>
> **Клас, що реалізує його з конкретним типом:**
>
> ```java
>
// Клас `Person` не є дженериком, але реалізує `Comparable<Person>`
> public class Person implements Comparable<Person> {
> private String name;
> private int age;
>
>     // ...
>
>     // Метод compareTo тепер типобезпечний і приймає тільки Person
>     @Override
>     public int compareTo(Person other) {
>         return Integer.compare(this.age, other.age);
>     }
> }
>
> ```
> Це дуже поширена практика в Java, яка дозволяє класам "включатися" в контракти, визначені дженерик-інтерфейсами.

#### **📌 Що перевіряють:**

*Розуміння того, як дженерик-інтерфейси використовуються для надання типобезпечних контрактів звичайним класам.*

* * *

### **5 ❓ Питання:**

Як успадкування дженеріків пов'язане зі стиранням типів (Type Erasure)? ⭐️

> #### **💬 Відповідь:**
>
> Стирання типів так само впливає і на успадкування. Після компіляції вся інформація про типи-параметри стирається, і компілятор генерує **"мостикові" методи (bridge methods)**, щоб зберегти поліморфізм.
>
> **Приклад:**
>
> **До стирання:**
>
> ```java
> class NumberStorage extends DataStorage<Integer> {
>     @Override
>     public void setData(Integer data) { /* ... */ }
>
>     @Override
>     public Integer getData() { /* ... */ }
> }
> ```
>
> **Після стирання (як це бачить JVM):**
>
> Батьківський клас:
> `class DataStorage { public void setData(Object data); public Object getData(); }`
>
> Нащадок:
> `class NumberStorage extends DataStorage { public void setData(Integer data); public Integer getData(); }`
>
> Як бачимо, сигнатура `setData(Integer)` **не збігається** з `setData(Object)`. Щоб вирішити цю проблему, компілятор генерує в `NumberStorage` **синтетичний "мостиковий" метод**:
>
> ```java
> // Згенерований компілятором міст
> public void setData(Object data) {
>     setData((Integer) data); // Викликає ваш, конкретний метод
> }
> ```
>
> Цей "міст" забезпечує, що при поліморфному виклику буде викликано правильний, типізований метод.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як "мостикові" методи забезпечують коректну роботу поліморфізму в умовах стирання типів.*

* * *

### **6 ❓ Питання:**

Чи можна змінити або обмежити тип-параметр при успадкуванні? ⭐️

> #### **💬 Відповідь:**
>
> Так, можна. Підклас може накласти **більш суворі обмеження** на тип-параметр, ніж батьківський клас.
>
> **Приклад:**
>
> У нас є загальна коробка для будь-яких предметів.
>
> ```java
> class Box<T> {
>     private T item;
> }
> ```
>
> Ми хочемо створити спеціалізовану коробку, яка може зберігати **тільки числа**, і яка є сортованою.
>
> ```java
> // Клас SortedNumberBox є дженериком, але з більш строгим обмеженням
> class SortedNumberBox<T extends Number & Comparable<T>> extends Box<T> {
>     // Цей клас може тепер працювати тільки з типами, що є Number І Comparable,
>     // наприклад, Integer, Double.
>
>     // Додаткова логіка, можлива завдяки обмеженню
>     public void sortAndPrint(List<T> items) {
>         Collections.sort(items);
>         System.out.println(items);
>     }
> }
> ```
>
> `SortedNumberBox<String>` не скомпілюється, навіть якщо `Box<String>` є валідним, оскільки `String` не є нащадком `Number`.
>
> Це дозволяє створювати ієрархії, де класи стають все більш і більш спеціалізованими.

#### **📌 Що перевіряють:**

*Розуміння того, як обмеження типів (`bounds`) можуть бути звужені при успадкуванні.*

* * *

### **7 ❓ Питання:**

Розглянемо `class MyList<E> implements List<E>`. Чи зобов'язаний `MyList` реалізувати всі методи `List`? Як `AbstractList` допомагає в цьому? ⭐️

> #### **💬 Відповідь:**
>
> Так, якщо клас реалізує інтерфейс напряму, він **зобов'язаний** надати реалізацію для **всіх** його методів. Для `List` це понад 20 методів, що дуже незручно.
>
> **Як `AbstractList` допомагає (патерн `Template Method`):**
>
> `java.util.AbstractList<E>` — це абстрактний клас, який **вже реалізує** інтерфейс `List<E>`. Він надає **скелетну реалізацію** для більшості "складних" методів (`iterator`, `indexOf`, `equals` тощо), реалізуючи їх на основі кількох "простих" абстрактних методів.
>
> Щоб створити свою власну незмінну реалізацію `List`, вам достатньо:
>
> 1. Успадкувати від `AbstractList<E>`.
> 2. Реалізувати **лише два методи**: `get(int index)` та `size()`.
>
> Всі інші методи `AbstractList` будуть використовувати ваші реалізації `get` та `size` для своєї роботи. Це є чудовим прикладом того, як успадкування від дженерик-класу може значно спростити створення нових класів.

#### **📌 Що перевіряють:**

*Знання дизайну стандартних колекцій Java та ролі абстрактних класів-скелетів, як-от `AbstractList`, `AbstractSet`.*

* * *

### **8 ❓ Питання:**

Що буде, якщо успадкувати від дженерик-класу, використовуючи **сирий тип (raw type)**? `class MyList extends ArrayList` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Такий код **скомпілюється**, але компілятор видасть **попередження (`unchecked warning`)**, і це вважається дуже поганою практикою.
>
> `public class MyList extends ArrayList { ... }`
>
> **Наслідки:**
>
> 1. Ваш клас `MyList` стає **не-дженериком**.
> 2. Оскільки ви успадкували від сирого `ArrayList`, всі успадковані методи (`add`, `get` тощо) також будуть працювати з **сирими типами**, тобто з `Object`.
> 3. **Вся типобезпечність дженеріків втрачається.** Ваш новий клас `MyList` буде поводитися так само, як старі колекції до Java 5.
>
> ```java
> MyList myList = new MyList();
> myList.add("hello");
> myList.add(123); // Ніяких помилок компіляції
>
> Object obj = myList.get(0); // Повертає Object
> ```
>
> **Коли це може бути (неохоче) використано?**
> Тільки для інтеграції з дуже старим, застарілим кодом, який неможливо змінити і який вимагає класи, що працюють з сирими типами. У новому коді цього слід уникати за будь-яку ціну.

#### **📌 Що перевіряють:**

*Розуміння того, як використання сирих типів при успадкуванні "заражає" підклас, позбавляючи його переваг дженеріків.*

* * *

### **9 ❓ Питання:**

Чи можна успадкувати від типу-параметра `T`? `class MyClass<T> extends T` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Такий код призведе до **помилки компіляції**.
>
> `public class MySubclass<T> extends T { ... } // ПОМИЛКА`
>
> **Причина:**
>
> Частина `extends` в оголошенні класу вимагає **конкретного класу або інтерфейсу**, який відомий на етапі компіляції.
>
> `T` — це просто **тип-параметр (заповнювач)**, а не реальний клас. Компілятор нічого не знає про те, що таке `T` — чи це клас, чи інтерфейс, чи має він доступний конструктор для виклику `super()` і т.д.
>
> Інформація про конкретний тип `T` з'явиться тільки при створенні екземпляра (`new MySubclass<String>()`), а структура класу має бути визначена вже на етапі компіляції.
>
> Тому успадкування від типу-параметра є неможливим.

#### **📌 Що перевіряють:**

*Знання синтаксичних та семантичних обмежень мови, зокрема, що можна, а що не можна ставити після `extends`.*

* * *

### **10 ❓ Питання:**

Як успадкування з дженеріками працює з перевантаженням методів? Поясніть на прикладі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Успадкування з дженеріками може призвести до несподіваної поведінки з перевантаженими методами через **стирання типів**.
>
> **Приклад:**
>
> ```java
> public class Parent<T> {
>     public void process(T data) {
>         System.out.println("Parent processing: " + data);
>     }
> }
>
> public class Child extends Parent<String> {
>     // Це НЕ перевизначення, а ПЕРЕВАНТАЖЕННЯ!
>     public void process(String data) {
>         System.out.println("Child processing string: " + data);
>     }
> }
> ```
>
> **Після стирання типів, клас `Parent` виглядає так:**
> `public class Parent { public void process(Object data) { ... } }`
>
> У класі `Child` тепер є **два методи**:
>
> 1. Успадкований `public void process(Object data)`
> 2. Власний `public void process(String data)`
>
> **Несподівана поведінка:**
>
> ```java
> Child child = new Child();
> Parent<String> parent = child;
>
> child.process("hello");  // Викличеться Child.process(String)
> parent.process("hello"); // Викличеться Parent.process(Object)!
> ```
>
> Коли ви викликаєте метод через посилання типу `Parent`, компілятор бачить тільки метод `process(Object)`, оскільки ваш `process(String)` не перевизначив його. Щоб досягти поліморфізму, потрібно було б **перевизначити** успадкований метод, а не перевантажувати його.
>
> **Правильне перевизначення:**
>
> ```java
> @Override
> public void process(String data) { /*...*/ }
> ```
>
> Якщо батьківський метод `process(T)` успадковується, то `process(String)` стане його реалізацією, і буде створено "мостиковий" метод `process(Object)`.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між перевантаженням та перевизначенням і того, як стирання типів впливає на цей вибір.*

* * *

### **11 ❓ Питання:**

Що таке "проблема повторюваної реалізації" дженерик-інтерфейсу і як вона виникає? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Проблема виникає, коли клас намагається реалізувати **один і той самий дженерик-інтерфейс двічі з різними типами-аргументами**. Це **заборонено** в Java.
>
> **Приклад помилки:**
>
> ```java
> interface Handler<T> {
>     void handle(T data);
> }
>
> class StringHandler implements Handler<String> { /* ... */ }
> class IntegerHandler implements Handler<Integer> { /* ... */ }
>
> // ПОМИЛКА КОМПІЛЯЦІЇ
> class CombinedHandler extends StringHandler implements Handler<Integer> {
>     // ...
> }
> ```
>
> **Причина:**
> Через **стирання типів**. Після компіляції і `Handler<String>`, і `Handler<Integer>` перетворюються на один і той самий сирий тип `Handler`.
> Виходить, що клас `CombinedHandler` намагається реалізувати один і той самий інтерфейс `Handler` двічі, що неприпустимо.
>
> У байт-коді це призвело б до конфлікту методів, оскільки після стирання `handle(String)` і `handle(Integer)` обидва перетворилися б на `handle(Object)`, і неможливо мати два методи з однаковою сигнатурою.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів впливає на ієрархію реалізації інтерфейсів.*

* * *

### **12 ❓ Питання:**

Як успадкування дженеріків використовується для реалізації патерну "рекурсивне самотипування" (Recursive Self-Typing)? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Рекурсивне самотипування** — це просунута техніка, де дженерик-клас обмежує свій тип-параметр **своїм же власним підтипом**.
>
> `public class MyClass<T extends MyClass<T>> { ... }`
>
> **Навіщо це потрібно?**
>
> Це дозволяє методам у базовому класі повертати екземпляр **конкретного підкласу (`this`)**, а не базового класу. Це є основою для реалізації **плинних інтерфейсів (fluent interfaces)** в ієрархіях успадкування.
>
> **Приклад:**
>
> ```java
> // Базовий клас з рекурсивним типом
> abstract class Animal<T extends Animal<T>> {
>     protected abstract T self(); // "Магічний" метод
>
>     public T setName(String name) {
>         System.out.println("Встановлено ім'я: " + name);
>         return self();
>     }
> }
>
> class Cat extends Animal<Cat> {
>     @Override protected Cat self() { return this; }
>     public Cat meow() { System.out.println("Meow!"); return this; }
> }
>
> class Dog extends Animal<Dog> {
>     @Override protected Dog self() { return this; }
>     public Dog bark() { System.out.println("Woof!"); return this; }
> }
> ```
>
> **Використання:**
>
> ```java
> Cat cat = new Cat().setName("Tom").meow(); // setName() повертає Cat, а не Animal
> Dog dog = new Dog().setName("Rex").bark(); // setName() повертає Dog, а не Animal
> ```
>
> Без цього патерну метод `setName()` в `Animal` мав би повертати `Animal`, що "зламало" б ланцюжок викликів для специфічних методів підкласів.

#### **📌 Що перевіряють:**

*Знання дуже просунутих дженерик-ідіом. Розуміння того, як вони використовуються для створення гнучких та потужних fluent API.*

* * *

### **13 ❓ Питання:**

Припустимо, у вас є `class A<T>` та `class B extends A<String>`. Чи є `B` дженерик-класом?

> #### **💬 Відповідь:**
>
> Ні, `B` **не є** дженерик-класом.
>
> Він є **звичайним (конкретним)** класом, який успадковує від **параметризованого типу** `A<String>`.
>
> * `A<T>` — це дженерик-клас.
> * `A<String>` — це параметризований тип, конкретна інстанціація `A<T>`.
> * `B` успадковує від цієї конкретної інстанціації, тому сам він вже не є узагальненим.
>
> Ви можете створити екземпляр `B` без вказування будь-яких типів:
> `B myB = new B();`
>
> Всі успадковані методи, які в `A` працювали з `T`, у `B` будуть працювати з `String`.

#### **📌 Що перевіряють:**

*Уважність та чітке розуміння термінології: дженерик-клас vs клас, що успадковує від параметризованого типу.*

* * *

### **14 ❓ Питання:**

Чи може клас реалізовувати один і той же інтерфейс двічі: один раз як сирий тип, інший — як параметризований? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не може**. Це призведе до **помилки компіляції**.
>
> ```java
> interface MyInterface<T> { ... }
>
> // ПОМИЛКА КОМПІЛЯЦІЇ
> class MyClass implements MyInterface<String>, MyInterface { ... }
> ```
>
> **Причина:**
> Знову ж таки — **стирання типів**.
>
> Після стирання і `MyInterface<String>`, і сирий `MyInterface` стають просто `MyInterface`. Виходить, що клас намагається реалізувати один і той самий інтерфейс двічі, що заборонено.
>
> Крім того, це призвело б до конфліктів методів. Наприклад, якщо інтерфейс мав метод `void process(T data)`, то після стирання в обох випадках сигнатура була б `void process(Object data)`, і неможливо реалізувати два методи з однаковою сигнатурою.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів впливає на правила успадкування та реалізації інтерфейсів, особливо при змішуванні сирих та параметризованих типів.*

* * *

### **15 ❓ Питання:**

Як дженеріки та успадкування взаємодіють з wildcards? Розглянемо `List<Integer>` і змінну `List<? extends Number>`. Чому присвоєння `List<? extends Number> list = new ArrayList<Integer>()` є валідним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це присвоєння є валідним, оскільки `extends`-wildcard робить дженерик-тип **коваріантним**.
>
> * **Коваріантність** означає, що якщо `Integer` є підтипом `Number`, то `List<Integer>` також розглядається як підтип `List<? extends Number>`.
>
> `? extends Number` означає "список будь-якого типу, що є `Number` або його нащадком".
>
> * `Integer` є нащадком `Number`, тому `List<Integer>` задовольняє цю умову.
> * `Double` є нащадком `Number`, тому `List<Double>` також задовольняє цю умову.
>
> Ця гнучкість дозволяє писати методи, які можуть працювати з цілою ієрархією типів.
>
> **Протилежний випадок — контрваріантність:**
> `? super Integer` робить тип **контрваріантним**.
>
> * Якщо `Number` є супертипом `Integer`, то `List<Number>` розглядається як підтип `List<? super Integer>`.
> * Тому присвоєння `List<? super Integer> list = new ArrayList<Number>()` також є **валідним**.
>
> Wildcards (`extends`, `super`) — це інструменти, які дозволяють керувати варіантністю дженериків, роблячи їх більш гнучкими, ніж стандартна інваріантність.

#### **📌 Що перевіряють:**

*Глибоке розуміння коваріантності та контрваріантності, що надаються wildcards, та їх відмінності від стандартної інваріантності дженериків. Це ключова концепція для написання гнучкого API.*
