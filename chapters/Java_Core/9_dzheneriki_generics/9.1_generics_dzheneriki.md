---
layout: default
title: "Generics (Дженеріки)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 1
---

# Generics (Дженеріки)

* короткий опис: `Generics` – це механізм у Java 5+, що дозволяє параметризувати типи класів, інтерфейсів та методів, забезпечуючи перевірку типів на етапі компіляції та уникнення помилок приведення типів під час виконання.

* поняття: `Generics`, параметризація типу, типобезпечність, `<T>`, `type inference`.

* приклади: `List<String> list = new ArrayList<>();` (типобезпечний список), порівняння з `raw types` (`List list = new ArrayList();`).

* ключові моменти: Запобігають `ClassCastException` під час виконання, роблять код безпечнішим, чистішим та читабельнішим.

* що важливо знати на співбесіді: Навіщо потрібні, переваги, `type inference`.

### **✅ Офіційне визначення:**

**Generics** — це механізм у Java, який дозволяє параметризувати типи класів, інтерфейсів та методів. Вони були додані у JDK 5.0, щоб забезпечити перевірку типів на етапі компіляції та уникнути помилок приведення типів під час виконання.

### **🧠 Простими словами:**

**Дженеріки** — це спосіб сказати Java: “цей клас або метод працює з певним типом, який ти дізнаєшся пізніше”. Це допомагає уникати помилок, коли, наприклад, ти додаєш рядок у список чисел. Дженеріки роблять код безпечнішим, зрозумілішим і не вимагають ручного приведення типів.

---

## **📊 Основні місця, де використовуються дженеріки:**

* **Generic Classes** — класи з параметром типу: **`class Box<T> { ... }`**
* **Generic Methods** — методи, які самі мають параметр типу: **`<T> void print(T t)`**
* **Generic Interfaces** — інтерфейси з параметром типу: **`interface List<E>`**
* **Bounded Generics** — обмеження типів: **`<T extends Number>`**
* **Wildcard Generics** — шаблони: **`<?>, <? extends T>, <? super T>`**
* **Raw Types** — сирі типи (небажано): **`List list = new ArrayList();`**
* **Type Erasure** — стирання типу під час компіляції

---

## **Пояснення на пальцях (💡)**

Уяви коробку (**`Box<T>`**), яка може містити будь-що, але ти вказуєш, що хочеш зберігати в ній, наприклад, яблука. Якщо хтось намагається покласти туди цвях — Java одразу скаже: “стоп, тут тільки яблука”.

Без дженеріків ти би щоразу відкривав коробку і сподівався, що там саме яблуко (і міг зламати зуб, якщо там цвях). **Дженеріки** — це етикетка на коробці з написом, що всередині

---

## **Корисні приклади (🧪)**

**Приклад без дженеріків (raw type):**

```java
List list = new ArrayList();
list.add("hello");
Integer num = (Integer) list.get(0); // ClassCastException!
```

**Той самий приклад з дженеріками:**

```java
List<String> list = new ArrayList<>();
list.add("hello");
String msg = list.get(0); // OK, без кастів
```

**Generic клас:**

```java
public class Box<T> {
  private T content;
  public void setContent(T content) { this.content = content; }
  public T getContent() { return content; }
}
Box<Integer> intBox = new Box<>();
intBox.setContent(123);
```

---

## **Порівняння (⚖️)**

| Без дженеріків (Raw Types) | З дженеріками |
| ----- | ----- |
| Потенційні помилки типу під час виконання | Перевірка типу на етапі компіляції |
| Потрібен кастинг (**`(Type) obj`**) | Кастинг не потрібен |
| Менш читабельний код | Більш зрозумілий і безпечний код |
| Спадок з pre-JDK 5.0 | Стандарт після JDK 5.0 |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔹 "T" — як Type (тип)

🔹 "Коли сумніваєшся — параметризуй"

🔹 "\<\> — твій страховий поліс від помилок типів"

🔹 PECS — Producer extends, Consumer super (для wildcard'ів)

---

## **🟩 Підсумок:**

**Дженеріки** — це один з найпотужніших інструментів Java, який дозволяє писати безпечний, багаторазовий і зрозумілий код. Вони зменшують кількість кастів, роблять типи очевидними, і допомагають виявити помилки ще на етапі компіляції. Вивчивши дженеріки — ти вже на рівень ближче до “Senior Java Wizard”.

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Oracle по Generics**](https://docs.oracle.com/javase/tutorial/java/generics/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке дженеріки (Generics) в Java і яка їхня основна мета?

> #### **💬 Відповідь:**
>
> **Дженеріки (Generics)** — це механізм у Java (введений у версії 5.0), який дозволяє створювати класи, інтерфейси та методи, що можуть працювати з **різними типами даних**, зберігаючи при цьому **типобезпечність на етапі компіляції**.
>
> **Основна мета:**
>
> 1. **Типобезпечність (Type Safety):** Дозволяють компілятору перевіряти, що ви використовуєте правильні типи даних. Це запобігає ситуаціям, коли ви, наприклад, випадково додаєте `String` у список, призначений для `Integer`.
>
> 2. **Усунення потреби в явних приведеннях типів (Casting):** Коли ви отримуєте об'єкт з дженерик-колекції, ви вже знаєте його тип, і вам не потрібно вручну приводити його до потрібного класу, що робить код чистішим і безпечнішим.
>
> 3. **Повторне використання коду:** Ви можете написати один клас (наприклад, `ArrayList<T>`) або метод, який буде працювати з будь-яким типом даних, замість того, щоб писати окремі класи для `ArrayListOfString`, `ArrayListOfInteger` і т.д.

#### **📝 Аналогія:**

Дженерік — це як універсальний контейнер з етикеткою. Ви можете покласти в нього що завгодно, але ви зобов'язані написати на етикетці, що саме там лежить (наприклад, `Box<Apple>`). Після цього ви вже не зможете покласти туди апельсин, а коли будете діставати вміст, ви точно знатимете, що там яблуко.

#### **📌 Що перевіряють:**

*Розуміння основної проблеми, яку вирішують дженеріки, — боротьба з помилками типів під час виконання (`ClassCastException`).*

* * *

### **2 ❓ Питання:**

Наведіть приклад коду до появи дженеріків і після, щоб продемонструвати їхні переваги.

> #### **💬 Відповідь:**
>
> **До Java 5 (до дженеріків):**
>
> Колекції працювали з `Object`. Це вимагало постійних перевірок та приведення типів.
>
> ```java
> // Використовувався "сирий" тип (raw type)
> List list = new ArrayList();
> list.add("hello");
> list.add(123); // Помилка не виявляється на етапі компіляції!
>
> // ... десь далі в коді
> for (Object obj : list) {
>     // Потрібне явне і небезпечне приведення типу
>     String str = (String) obj; // На другому елементі буде ClassCastException
>     System.out.println(str.toUpperCase());
> }
> ```
>
> **Після Java 5 (з дженеріками):**
>
> Ми вказуємо тип колекції, і компілятор контролює типобезпечність.
>
> ```java
> // Ми вказуємо, що список містить тільки рядки
> List<String> list = new ArrayList<>();
> list.add("hello");
> // list.add(123); // Помилка компіляції! Не можна додати Integer в List<String>
>
> for (String str : list) {
>     // Приведення типу не потрібне, компілятор вже знає тип
>     System.out.println(str.toUpperCase());
> }
> ```
>
> **Переваги очевидні:**
>
> 1. Помилка виявлена на етапі компіляції, а не під час роботи програми.
> 2. Код став чистішим, оскільки зникла потреба в явних приведеннях типів.

#### **📌 Що перевіряють:**

*Здатність на практичному прикладі продемонструвати ключові переваги дженеріків: типобезпечність та відсутність потреби в кастах.*

* * *

### **3 ❓ Питання:**

Що таке параметризований тип (parameterized type) та "сирий" тип (raw type)?

> #### **💬 Відповідь:**
>
> * **Параметризований тип (Parameterized Type):**
>   * Це дженерик-клас або інтерфейс, для якого вказано конкретний **тип-аргумент**.
>   * Наприклад: `List<String>`, `Map<Integer, User>`, `Box<Boolean>`.
>   * Використання параметризованих типів — це сучасна і рекомендована практика.
>
> * **"Сирий" тип (Raw Type):**
>   * Це дженерик-клас або інтерфейс, який використовується **без вказання типу-аргумента**.
>   * Наприклад: `List`, `Map`, `Box`.
>   * Компілятор видає попередження (`warning`) при використанні сирих типів. Вони були залишені в мові переважно для **зворотної сумісності** зі старим кодом, написаним до Java 5.
>   * Використання сирих типів є **небезпечним**, оскільки воно вимикає всі переваги типобезпечності дженеріків.

#### **📝 Коментар:**

Правило просте: **уникайте використання сирих типів** у новому коді за будь-яку ціну.

#### **📌 Що перевіряють:**

*Знання базової термінології та розуміння різниці між сучасним (параметризованим) та застарілим (сирим) підходами.*

* * *

### **4 ❓ Питання:**

Що таке "стирання типів" (Type Erasure)?

> #### **💬 Відповідь:**
>
> **Стирання типів** — це процес, під час якого компілятор Java **видаляє** всю інформацію про дженерик-типи після перевірки їхньої коректності і замінює її на відповідні `Object` або обмежені типи (`bound`).
>
> **Як це працює:**
>
> 1. `List<String>` на етапі компіляції перевіряється на типобезпечність.
> 2. Після перевірки компілятор "стирає" тип `<String>`, і в байт-коді цей клас виглядає як звичайний `List`, що працює з `Object`.
> 3. У місцях, де ви отримуєте дані з колекції (наприклад, `list.get(0)`), компілятор автоматично додає неявне приведення типу `(String)`.
>
> **Чому це було зроблено?**
> Основна причина — **зворотна сумісність**. Це дозволило новому коду з дженеріками працювати зі старими бібліотеками без них, і навпаки. Якби типи не стиралися, JVM довелося б кардинально змінити, і старий код перестав би працювати.
>
> **Наслідки:** Інформація про дженерик-тип **недоступна під час виконання (runtime)**. Наприклад, ви не можете зробити `new T()`, `T.class` або `obj instanceof List<String>`.

#### **📌 Що перевіряють:**

*Розуміння ключового механізму реалізації дженеріків в Java та його наслідків (недоступність типів у runtime).*

* * *

### **5 ❓ Питання:**

Чи можна створити екземпляр об'єкта дженерик-типу `T` всередині класу? Наприклад, `new T()`?

> #### **💬 Відповідь:**
>
> Ні, напряму це зробити **не можна**.
>
> Причина — **стирання типів (type erasure)**. Під час виконання (runtime) інформація про конкретний тип `T` (наприклад, `String` або `Integer`) видаляється. JVM не знає, конструктор якого саме класу їй потрібно викликати.
>
> **Як цю проблему можна обійти?**
>
> 1. **Передача `Class<T>` об'єкта (Type Token):**
>     * Ви можете передати об'єкт `Class` для типу `T` у конструктор і використовувати його для створення екземпляра через рефлексію.
>
>     ```java
>     public class Factory<T> {
>         private Class<T> type;
>         public Factory(Class<T> type) { this.type = type; }
>         public T createInstance() throws ... {
>             return type.getDeclaredConstructor().newInstance();
>         }
>     }
>     // Використання: new Factory<>(User.class).createInstance();
>     ```
>
> 2. **Передача фабрики ( `Supplier<T>`):**
>     * Можна передати лямбда-вираз, який знає, як створювати об'єкт.
>
>     ```java
>     public class Factory<T> {
>         private Supplier<T> supplier;
>         public Factory(Supplier<T> supplier) { this.supplier = supplier; }
>         public T createInstance() { return supplier.get(); }
>     }
>     // Використання: new Factory<>(User::new).createInstance();
>     ```

#### **📌 Що перевіряють:**

*Розуміння наслідків стирання типів та знання стандартних "обхідних шляхів" (workarounds) для цієї проблеми.*

* * *

### **6 ❓ Питання:**

Що таке дженерик-методи (Generic Methods) і чим вони відрізняються від методів у дженерик-класах? ⭐️

> #### **💬 Відповідь:**
>
> **Дженерик-метод** — це метод, який має власний **параметр типу**, незалежний від параметрів типу класу.
>
> **Оголошення:**
> `public <T> void myGenericMethod(T item) { ... }`
> Кутові дужки `<T>` **перед** типом повернення вказують на те, що це дженерик-метод.
>
> **Чим він відрізняється:**
>
> * **Дженерик-метод:** Може існувати всередині **як дженерик, так і не-дженерик** класу. Його параметр типу `T` "живе" тільки в межах цього методу.
> * **Метод у дженерик-класі:** Використовує параметр типу, оголошений на рівні **класу** (`class Box<T>`).
>
> **Приклад:**
>
> ```java
> public class Utils { // Не-дженерик клас
>     // Дженерик-метод
>     public static <T> void printArray(T[] array) {
>         for (T element : array) {
>             System.out.print(element + " ");
>         }
>         System.out.println();
>     }
> }
> // Utils.<Integer>printArray(...) - тип виводиться автоматично
> ```
>
> Цей метод `printArray` можна викликати з масивом будь-якого типу (`Integer[]`, `String[]`), і компілятор автоматично визначить тип `T`.

#### **📌 Що перевіряють:**

*Здатність розрізняти параметризацію на рівні класу та на рівні методу. Розуміння області видимості параметрів типу.*

* * *

### **7 ❓ Питання:**

Що таке оператор "діамант" (`<>`) і коли він з'явився?

> #### **💬 Відповідь:**
>
> **Оператор "діамант" (`<>`)** — це синтаксичний цукор, введений у **Java 7**, який дозволяє не дублювати тип-аргумент при створенні екземпляра дженерик-класу.
>
> Компілятор сам **виводить (infers)** потрібний тип з контексту.
>
> **До Java 7:**
>
> ```java
> Map<String, List<User>> userMap = new HashMap<String, List<User>>();
> ```
>
> **З Java 7:**
>
> ```java
> Map<String, List<User>> userMap = new HashMap<>(); // Набагато коротше
> ```
>
> Це робить код значно менш громіздким і більш читабельним. Цей процес називається **виведенням типів (type inference)**.

#### **📌 Що перевіряють:**

*Знання еволюції мови Java та синтаксичних покращень, що спрощують роботу з дженеріками.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати примітивні типи ( `int`, `double` ) як тип-аргументи для дженеріків?

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Дженеріки в Java працюють **тільки з об'єктними (посилальними) типами**.
>
> **Причина:**
> Знову ж таки, це пов'язано зі **стиранням типів (type erasure)**. Під час виконання дженерик-типи замінюються на `Object` (або інший обмежуючий тип). Примітивні типи не є нащадками `Object` і не можуть бути присвоєні змінній типу `Object`.
>
> **Рішення:**
> Використовувати відповідні **класи-обгортки (wrapper classes)**: `Integer` для `int`, `Double` для `double`, `Boolean` для `boolean` і т.д.
>
> `List<int> list; // ПОМИЛКА КОМПІЛЯЦІЇ`
> `List<Integer> list; // ПРАВИЛЬНО`
>
> Завдяки механізму **автобоксингу/анбоксингу (autoboxing/unboxing)** робота з ними виглядає майже так само, як з примітивами.

#### **📌 Що перевіряють:**

*Знання фундаментального обмеження дженеріків та ролі класів-обгорток.*

* * *

### **9 ❓ Питання:**

Чи можна створити масив дженерик-типу, наприклад, `new T[]` або `new List<String>[]`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, створення масивів дженерик-типу **напряму заборонено** в Java.
>
> `T[] array = new T[10]; // ПОМИЛКА КОМПІЛЯЦІЇ`
> `List<String>[] lists = new ArrayList<String>[10]; // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Причина:**
>
> Це пов'язано з тим, що дженеріки та масиви мають **різну реалізацію типобезпечності**.
>
> * **Дженеріки:** Типи стираються під час компіляції (`List<String>` стає `List`). Вони **не є типобезпечними під час виконання**.
> * **Масиви:** Типи перевіряються **під час виконання**. Об'єкт масиву знає свій тип (`String[]` знає, що він масив рядків), і спроба покласти в нього `Integer` призведе до `ArrayStoreException` під час виконання.
>
> Якби можна було створити `List<String>[]`, це створило б "діру" в системі типів. Ви могли б присвоїти його змінній `Object[]`, покласти туди `List<Integer>`, а потім при спробі дістати елемент отримати `ClassCastException`.
>
> **Що робити натомість?**
> Використовувати `List<List<String>>` або інші колекції.

#### **📌 Що перевіряють:**

*Глибоке розуміння конфлікту між стиранням типів у дженеріках та перевіркою типів під час виконання у масивах.*

* * *

### **10 ❓ Питання:**

Поясніть, що означає `instanceof List<String>`. Чи є такий код валідним? ⭐️

> #### **💬 Відповідь:**
>
> Ні, такий код **не є валідним** і не скомпілюється.
>
> `if (obj instanceof List<String>) { ... } // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Причина:**
>
> Знову ж таки — **стирання типів (type erasure)**. Під час виконання вся інформація про тип-аргумент (`<String>`) видаляється. JVM бачить тільки `List`. Вона не може перевірити, чи належить об'єкт до `List<String>` або `List<Integer>`, бо для неї це все просто `List`.
>
> **Як правильно перевірити:**
>
> Ви можете перевірити тільки **"сирий" тип**:
> `if (obj instanceof List) { ... }`
>
> Це означає, що ви можете перевірити, що об'єкт є списком, але ви **не можете перевірити тип елементів цього списку** за допомогою `instanceof`.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів обмежує можливості оператора `instanceof`.*

* * *

### **11 ❓ Питання:**

Для чого потрібні маски (wildcards) в дженеріках? Поясніть на прикладі `<?>`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Маски (Wildcards)** використовуються для створення більш гнучких API, коли вам потрібно працювати з дженерик-типами, але вам не важливий (або невідомий) точний тип-аргумент.
>
> **Необмежена маска (`?` - Unbounded Wildcard)**:
>
> Позначається знаком питання `?` і означає **"будь-який невідомий тип"**.
>
> **Приклад:** `List<?>` — це список, елементи якого належать до якогось невідомого типу.
>
> **Коли використовується `<?>`?**
>
> * Коли метод працює з дженерик-класом, але логіка методу **не залежить від конкретного типу** цього класу.
>
> **Приклад методу:**
>
> ```java
> public static void printList(List<?> list) { // Працює для List<String>, List<Integer>...
>     for (Object item : list) {
>         System.out.println(item);
>     }
> }
> ```
>
> Цей метод може роздрукувати будь-який список, бо `toString()` є у `Object`, і йому не потрібна інформація про конкретний тип.
>
> **Обмеження `List<?>`:**
> Ви **не можете додавати** нові елементи в такий список (крім `null`), тому що компілятор не знає, який саме тип є безпечним для додавання. Ви не можете додати `Integer` в `List<String>`, і навпаки.

#### **📌 Що перевіряють:**

*Розуміння концепції wildcards та призначення `<?>` для написання гнучких, але безпечних методів.*

* * *

### **12 ❓ Питання:**

Поясніть обмежені маски (bounded wildcards): `<? extends T>` та `<? super T>`. Коли яку використовувати (принцип PECS)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Обмежені маски** використовуються для того, щоб накласти обмеження на невідомий тип.
>
> * **`<? extends T>` (Upper Bounded Wildcard — обмеження зверху):**
>   * Означає "будь-який невідомий тип, що є нащадком `T` (або самим `T`)".
>   * **Приклад:** `List<? extends Number>` може бути `List<Integer>`, `List<Double>`, але не `List<String>`.
>   * **Обмеження:** Ви **не можете додавати** елементи в таку колекцію (крім `null`), тому що не знаєте точного типу. Зате ви можете **безпечно читати** з неї елементи як тип `T` (`Number` в нашому прикладі).
>
> * **`<? super T>` (Lower Bounded Wildcard — обмеження знизу):**
>   * Означає "будь-який невідомий тип, що є предком `T` (або самим `T`)".
>   * **Приклад:** `List<? super Integer>` може бути `List<Integer>`, `List<Number>`, `List<Object>`.
>   * **Обмеження:** Ви можете **безпечно додавати** в таку колекцію елементи типу `T` (`Integer`), бо вони сумісні з будь-яким супертипом. Але при читанні ви можете отримати лише `Object`.
>
> **Принцип PECS (Producer Extends, Consumer Super):**
>
> Це мнемонічне правило, яке допомагає вибрати правильну маску:
>
> * **"Producer Extends"**: Якщо ваша структура даних є **виробником (producer)**, тобто ви з неї переважно **читаєте** дані, використовуйте `<? extends T>`.
> * **"Consumer Super"**: Якщо ваша структура даних є **споживачем (consumer)**, тобто ви в неї переважно **записуєте** дані, використовуйте `<? super T>`.
>
> **Приклад:**
>
> ```java
> // `src` - виробник, ми з нього читаємо, тому extends
> // `dest` - споживач, ми в нього пишемо, тому super
> public static <T> void copy(List<? extends T> src, List<? super T> dest) {
>     for (T item : src) {
>         dest.add(item);
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння дженеріків, зокрема, коваріантності та контрваріантності. Знання принципу PECS є ознакою Senior-рівня.*

* * *

### **13 ❓ Питання:**

Що таке рекурсивне обмеження типів (recursive type bound) і де воно використовується? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Рекурсивне обмеження типів** — це конструкція, де тип-параметр обмежується типом, який залежить від самого себе.
>
> `class MyClass<T extends Comparable<T>> { ... }`
>
> **Що це означає:**
>
> Це означає, що `T` має бути типом, який можна **порівнювати з об'єктами свого ж типу**. Інтерфейс `Comparable<T>` сам по собі є дженериком.
>
> * `String implements Comparable<String>` — підходить.
> * `Integer implements Comparable<Integer>` — підходить.
> * `Date implements Comparable<Date>` — підходить.
>
> **Де це використовується:**
>
> Це широко використовується в методах, яким потрібно порівнювати елементи, наприклад, для пошуку максимуму або для сортування.
>
> **Приклад:**
>
> ```java
> public static <T extends Comparable<T>> T max(Collection<T> coll) {
>     T candidate = coll.iterator().next();
>     for (T item : coll) {
>         if (item.compareTo(candidate) > 0) { // Метод compareTo доступний завдяки обмеженню
>             candidate = item;
>         }
>     }
>     return candidate;
> }
> ```
>
> Це гарантує, що будь-який тип `T`, переданий у цей метод, матиме метод `compareTo(T)`, що робить код типобезпечним.

#### **📌 Що перевіряють:**

*Знання просунутих технік дженеріків, таких як рекурсивні обмеження, та розуміння їх застосування для забезпечення можливості порівняння.*

* * *

### **14 ❓ Питання:**

У чому різниця між `List<?>` та `List<Object>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже важлива відмінність, яку часто неправильно розуміють.
>
> * **`List<Object>`:**
>   * Це параметризований тип, який означає, що ви маєте список, в який можна покласти **об'єкти абсолютно будь-якого класу**, бо всі класи успадковують `Object`.
>   * `List<Object> list = new ArrayList<>(); list.add("string"); list.add(123); list.add(new User());` — все це валідний код.
>
> * **`List<?>`:**
>   * Це маска (wildcard), яка означає, що ви маєте список, який містить елементи **якогось конкретного, але невідомого типу**.
>   * Наприклад, це може бути `List<String>` або `List<Integer>`, але ви не знаєте, який саме.
>
> **Ключова різниця в типобезпечності:**
>
> * Ви **не можете** безпечно присвоїти `List<String>` змінній типу `List<Object>`. Це нетипобезпечно, бо після цього ви могли б додати `Integer` у список рядків.
>     `List<Object> list = new ArrayList<String>(); // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> * Але ви **можете** присвоїти `List<String>` змінній типу `List<?>`. Це безпечно.
>     `List<?> list = new ArrayList<String>(); // OK`
>
> **Правило:**
> `List<Object>` є більш специфічним (це список, який може містити що завгодно).
> `List<?>` є більш загальним (це може бути список будь-чого, але одного конкретного типу).
> Використовуйте `List<?>`, коли логіці неважливий тип, але `List<Object>` — коли вам дійсно потрібна гетерогенна колекція.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантики wildcards та їх відмінності від використання `Object` як типу-аргумента. Це одна з ключових концепцій поліморфізму в дженеріках.*

* * *

### **15 ❓ Питання:**

Що таке `super type tokens` і яку проблему дженеріків вони вирішують? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Super Type Token** — це ідіома (патерн), яка дозволяє **обійти стирання типів** і отримати інформацію про дженерик-тип під час виконання (runtime).
>
> **Проблема:**
>
> Через стирання типів, ви не можете отримати `Class`-об'єкт для параметризованого типу. `List<String>.class` — це невалідний синтаксис.
>
> **Рішення (Super Type Token):**
>
> Ідея полягає в тому, щоб створити **анонімний підклас** дженерик-класу. Інформація про дженерик-тип його **батьківського класу зберігається в байт-коді** і її можна отримати через рефлексію.
>
> **Приклад реалізації (як у бібліотеці Guava):**
>
> ```java
> public abstract class TypeToken<T> {
>     private final Type type;
>
>     protected TypeToken() {
>         // Отримуємо тип через рефлексію
>         Type superclass = getClass().getGenericSuperclass();
>         this.type = ((ParameterizedType) superclass).getActualTypeArguments()[0];
>     }
>     // ...
> }
> ```
>
> **Використання:**
>
> ```java
> // Створюємо анонімний підклас, щоб зберегти тип
> TypeToken<List<String>> stringListToken = new TypeToken<>() {};
>
> // Тепер ми можемо отримати тип List<String> під час виконання
> Type myType = stringListToken.getType();
> ```
>
> Ця ідіома є дуже потужною і використовується в бібліотеках для серіалізації/десеріалізації (Jackson, Gson) та фреймворках для впровадження залежностей (Guice), щоб працювати з дженерик-типами під час виконання.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про рефлексію, стирання типів та просунуті патерни, які використовуються для вирішення фундаментальних проблем платформи. Це питання рівня Senior+.*
