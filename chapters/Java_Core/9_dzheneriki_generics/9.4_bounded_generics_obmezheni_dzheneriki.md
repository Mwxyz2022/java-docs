---
layout: default
title: "Bounded Generics (Обмежені дженеріки)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 4
---

# Bounded Generics (Обмежені дженеріки)

* короткий опис: Bounded Generics – це дженеріки, де параметр типу обмежується певним класом або інтерфейсом за допомогою ключового слова `extends`. Це дозволяє використовувати лише ті типи, які є підтипами (або реалізують) вказаної межі.

* поняття: Bounded Generics, `<T extends Animal>`, `<T extends Cat & Swimmable>` (множинне обмеження).

* приклади: `Box<T extends Number>` (обмеження числовими типами), `SwimmingBox<T extends Cat & Swimmable>`.

* ключові моменти: Дозволяють викликати методи, визначені у межі (`T.swim()`, `T.compareTo()`). При множинних обмеженнях клас повинен бути першим.

* що важливо знати на співбесіді: Синтаксис, множинні обмеження, їх переваги.

### **✅ Офіційне визначення:**

**Bounded Generics** — це дженеріки, в яких параметр типу обмежується певним класом або інтерфейсом за допомогою ключового слова **`extends`**. Це дозволяє використовувати лише ті типи, які задовольняють це обмеження.

### **🧠 Простими словами:**

Це коли ти кажеш: “Я дозволяю використовувати цей клас тільки з певними типами — наприклад, лише з тваринами 🐾, або з тими, хто вміє плавати 🏊‍♂️”. У цьому випадку Java перевіряє, щоб типи відповідали вказаним вимогам.

---

## **📊 Основні моменти:**

* **Синтаксис простого обмеження:**

```java
class Box<T extends Animal> { ... }
```

* **Множинні обмеження:**

```java
class Box<T extends Cat & Swimmable> { ... }
```

🔸 Важливо: якщо є клас і інтерфейси, **клас має бути першим**, інакше буде помилка компіляції.

* **Тип можна обмежити інтерфейсом без класу:**

```java
class Box<T extends Swimmable> { ... }
```

---

## **Пояснення на пальцях (💡)**

Уяви, що ти орендуєш басейн — але тільки для тварин, які вміють плавати. Ти не пустиш туди бегемота, якщо він боїться води. Обмежені дженеріки — це твій спосіб сказати Java: "Я працюю лише з тим, хто є **`Cat`** і реалізує **`Swimmable`**". Інші типи навіть не будуть скомпільовані, якщо вони не задовольняють ці умови
---

## **Корисні приклади (🧪)**

### **🔸 Клас `Animal` і його нащадки:**

```java
public class Animal { }

public class Cat extends Animal { }

public class Dog extends Animal { }

public interface Swimmable {
    void swim();
}

public class Bengal extends Cat implements Swimmable {
    public void swim() {
        System.out.println("Bengal is swimming!");
    }
}

public class Persian extends Cat { }
```

### **🔸 Обмежений generic-клас:**

```java
public class Box<T extends Animal> {
    private T content;
    public void setContent(T content) { this.content = content; }
    public T getContent() { return content; }
}

// Використання:
Box<Cat> catBox = new Box<>();
Box<Dog> dogBox = new Box<>();
Box<String> errorBox = new Box<>(); // ❌ Помилка компіляції
```

### **🔸 Множинне обмеження:**

```java
public class SwimmingBox<T extends Cat & Swimmable> {
    public void letItSwim(T cat) {
        cat.swim();
    }
}

SwimmingBox<Bengal> b1 = new SwimmingBox<>(); // ✅
SwimmingBox<Persian> p1 = new SwimmingBox<>(); // ❌ Persian не реалізує Swimmable
```

---

## **Порівняння (⚖️)**

| Звичайний дженерик | Обмежений дженерик |
| :---: | :---: |
| Може приймати будь-який тип | Приймає лише ті, що відповідають обмеженню |
| Більше гнучкості, але менше безпеки | Менше гнучкості, більше гарантій |
| Не можна викликати специфічні методи типу | Можна викликати методи межі (**`T.swim()`** тощо) |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **"extends" \= "має бути схожим на"**
* **Клас перший, інтерфейси — на десерт 🍰**
* **`&` \= "і те, і те": `T extends Cat & Swimmable` \= кіт, що плаває**

---

## **🟩 Підсумок:**

Обмежені дженеріки дозволяють чітко контролювати, з якими типами може працювати клас або метод. Вони особливо корисні, коли потрібно забезпечити виконання певних методів або гарантувати спадкування. Пам’ятай про правильний порядок: клас — першим, інтерфейси — далі. Це додає безпеки і передбачуваності твоєму коду.

---

### **🔗 Додаткові джерела:**

* [**Java Generics \- Bounded Types (Oracle Docs)**](https://docs.oracle.com/javase/tutorial/java/generics/bounded.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке обмежені дженеріки (Bounded Generics) і яка їхня головна мета?

> #### **💬 Відповідь:**
>
> **Обмежені дженеріки** — це механізм у Java, який дозволяє накласти **обмеження** на тип-параметр, вказуючи, що він повинен бути **підтипом (нащадком)** певного класу або **реалізовувати** певний інтерфейс.
>
> Обмеження встановлюється за допомогою ключового слова **`extends`**.
>
> `public class MyClass<T extends UpperBound> { ... }`
>
> **Головна мета:**
>
> 1. **Гарантувати наявність певної поведінки:** Дозволити всередині дженерик-класу або методу викликати методи, визначені в класі/інтерфейсі-обмеженні. Наприклад, якщо `<T extends Comparable>`, ви гарантовано можете викликати `t.compareTo()`.
> 2. **Підвищити типобезпечність:** Обмежити використання вашого дженерик-класу/методу тільки тими типами, для яких його логіка має сенс.
> 3. **Покращити читабельність коду:** Обмеження служить додатковою документацією, яка чітко вказує, з якими типами призначений працювати даний код.

#### **📝 Аналогія:**

Уявіть, що ви організовуєте змагання з плавання (`SwimmingCompetition<T>`). Вам байдуже, хто бере участь — людина, собака чи риба. Але ви висуваєте одну вимогу: учасник `T` повинен реалізовувати інтерфейс `Swimmable`. `public class SwimmingCompetition<T extends Swimmable> { ... }`. Це гарантує, що у будь-якого учасника можна буде викликати метод `swim()`.

#### **📌 Що перевіряють:**

*Розуміння основної мети обмежених дженеріків — забезпечення наявності певного API у типів-параметрів.*

* * *

### **2 ❓ Питання:**

Як оголосити дженерик-клас з простим обмеженням (наприклад, тільки для чисел)?

> #### **💬 Відповідь:**
>
> Для цього використовується синтаксис `<T extends ClassName>`. Це означає, що тип `T` може бути або самим класом `ClassName`, або будь-яким його нащадком.
>
> **Приклад: клас для обчислення статистики для будь-яких числових типів.**
>
> ```java
> // Оголошуємо, що T має бути нащадком класу Number
> public class Stats<T extends Number> {
>     private T[] data;
>
>     public Stats(T[] data) {
>         this.data = data;
>     }
>
>     public double getAverage() {
>         double sum = 0.0;
>         for (T item : data) {
>             // Ми можемо безпечно викликати doubleValue(), бо він є у Number
>             sum += item.doubleValue();
>         }
>         return sum / data.length;
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> Integer[] ints = {1, 2, 3, 4};
> Stats<Integer> intStats = new Stats<>(ints); // OK, Integer extends Number
>
> Double[] doubles = {1.1, 2.2, 3.3};
> Stats<Double> doubleStats = new Stats<>(doubles); // OK, Double extends Number
>
> String[] strings = {"a", "b"};
> // Stats<String> stringStats = new Stats<>(strings); // ПОМИЛКА КОМПІЛЯЦІЇ
> // String не є нащадком Number
> ```

#### **📌 Що перевіряють:**

*Практичні навички написання дженерик-класів з обмеженнями та розуміння, як це дозволяє використовувати методи базового класу.*

* * *

### **3 ❓ Питання:**

Що таке множинні обмеження (multiple bounds) і який у них синтаксис?

> #### **💬 Відповідь:**
>
> **Множинні обмеження** дозволяють вимагати, щоб тип-параметр одночасно був нащадком певного класу **і** реалізовував один або кілька інтерфейсів.
>
> **Синтаксис:**
>
> Типи перераховуються через амперсанд (`&`).
> `public class MyClass<T extends ClassA & InterfaceB & InterfaceC> { ... }`
>
> **Важливе правило:** Якщо в обмеженні є клас, він **завжди** повинен стояти **першим** у списку. Клас може бути тільки один, а інтерфейсів — кілька.
>
> **Приклад:**
>
> Уявімо, що нам потрібен клас для роботи з об'єктами, які є нащадками `SomeBaseClass` і одночасно є `Serializable` та `Runnable`.
>
> ```java
> class MultiBound<T extends SomeBaseClass & Serializable & Runnable> {
>     private T item;
>
>     public void process() {
>         item.someBaseClassMethod(); // Метод з класу
>         item.run();                 // Метод з Runnable
>         // ... можемо серіалізувати item
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Знання синтаксису множинних обмежень та ключового правила про порядок класу та інтерфейсів.*

* * *

### **4 ❓ Питання:**

Чому клас в множинних обмеженнях завжди має йти першим? ⭐️

> #### **💬 Відповідь:**
>
> Це вимога специфікації мови Java. `public class MyClass<T extends InterfaceA & ClassB>` призведе до **помилки компіляції**.
>
> **Причина** цього правила, ймовірно, пов'язана зі **спрощенням роботи компілятора** та уникненням неоднозначностей.
>
> 1. Java дозволяє успадковувати лише **від одного класу**, але реалізовувати **багато інтерфейсів**. Розміщення класу на першому місці робить цю структуру чіткою та однозначною. Компілятору не потрібно перебирати весь список, щоб знайти клас, якщо він там є.
>
> 2. Це створює **стандартний і передбачуваний порядок** для всіх оголошень обмежень.
>
> Таким чином, правило "клас завжди перший" є синтаксичною вимогою, яка робить дизайн дженериків більш строгим і послідовним.

#### **📌 Що перевіряють:**

*Уважність до деталей специфікації мови. Розуміння того, що деякі правила є просто синтаксичними конвенціями для забезпечення порядку.*

* * *

### **5 ❓ Питання:**

Чи можна використовувати `super` для обмеження типів у дженерик-класах (`<T super MyClass>`)?

> #### **💬 Відповідь:**
>
> Ні, **не можна**.
>
> Ключове слово `super` використовується **тільки** для обмеження **масок (wildcards)**, а не для типів-параметрів класу або методу.
>
> * **`<T extends MyClass>` (Bounded Type Parameter):** **Валідно.** Використовується при оголошенні дженерик-класу або методу. `T` — це конкретний тип, який ми визначаємо.
>
> * **`List<? super MyClass>` (Lower Bounded Wildcard):** **Валідно.** Використовується для параметрів методів або полів. `?` — це невідомий тип, який є предком `MyClass`.
>
> * **`<T super MyClass>`:** **НЕВАЛІДНИЙ СИНТАКСИС**. Специфікація мови не передбачає такої конструкції.
>
> **Чому?**
>
> Обмеження `<T extends ...>` призначене для того, щоб гарантувати компілятору, що об'єкти типу `T` матимуть певне API. Обмеження знизу (`super`) не дає такої гарантії (ми знаємо, що `T` є предком `MyClass`, але ми нічого не можемо сказати про саме `T` — це може бути `Object`). Тому така конструкція не має практичного сенсу на рівні оголошення типу.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між `type parameters` та `wildcards`, що є поширеною темою для плутанини. Знання того, де саме можна використовувати `super` та `extends`.*

* * *

### **6 ❓ Питання:**

Розглянемо метод `Collections.max(Collection<? extends T> coll)`. Поясніть обмеження, що використовуються в його сигнатурі `public static <T extends Comparable<? super T>> T max(...)`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Повна сигнатура трохи відрізняється: `public static <T extends Object & Comparable<? super T>> T max(...)`. Розберемо її частини:
>
> 1. **`<T extends Object & Comparable<? super T>>`**: Це обмеження на тип `T`.
>     * **`extends Object &`**: Це надлишкова, але допустима частина. Будь-який об'єкт є `Object`.
>     * **`Comparable<? super T>`**: Це **рекурсивне обмеження з wildcard**. Воно є ключовим і дуже гнучким. Воно означає, що `T` має реалізовувати `Comparable`, але він може бути порівнюваним не обов'язково з самим собою, а з **будь-яким своїм супертипом**.
>
>         **Приклад:** Уявімо, є `class Student extends Person`, і `Person` реалізує `Comparable<Person>`.
>         `Student` сам по собі не реалізує `Comparable<Student>`, але він може бути порівняний як `Person`. Сигнатура `Comparable<? super Student>` дозволяє це. `Person` є супертипом для `Student`, тому `Student` підходить під це обмеження. Це робить метод `max` більш гнучким.
>
> 2. **`Collection<? extends T> coll`**: Це обмеження на колекцію.
>     * Означає, що метод може приймати колекцію, яка містить елементи типу `T` **або будь-якого його нащадка**.
>     * Це слідує принципу **PECS (Producer Extends)**: колекція `coll` є **виробником**, ми з неї тільки читаємо, тому використовуємо `extends`.
>
> Ця складна сигнатура робить метод `max` максимально гнучким і типобезпечним.

#### **📌 Що перевіряють:**

*Дуже глибокі знання дженеріків. Здатність читати, розуміти та пояснювати складні дженерик-сигнатури з JDK. Це питання для рівня Senior.*

* * *

### **7 ❓ Питання:**

У чому різниця між `<T extends Number>` і `Number` як типом поля? ⭐️

> #### **💬 Відповідь:**
>
> * **`<T extends Number>`**: Це оголошення **типу-параметра**.
>   * Ви створюєте дженерик-клас або метод, який може працювати з **конкретним** числовим типом, що був переданий. Наприклад, якщо ви створили `MyClass<Integer>`, то `T` всередині буде `Integer`.
>   * Це дозволяє зберігати типобезпечність. Наприклад, метод `T get()` поверне `Integer`, а не узагальнений `Number`.
>   * **Приклад:** `public class Holder<T extends Number> { private T value; public T get() { return value; } }`
>     * `new Holder<Integer>().get()` поверне `Integer`.
>
> * **`Number` як тип поля**:
>   * Це використання **конкретного класу `Number`** як типу.
>   * Ви створюєте не-дженерик клас, який може працювати з **будь-яким** нащадком `Number`, але при цьому **втрачається інформація** про точний тип.
>   * **Приклад:** `public class Holder { private Number value; public Number get() { return value; } }`
>     * Якщо ви поклали туди `Integer`, `get()` все одно поверне `Number`, і вам доведеться робити `instanceof` та приведення типів, щоб працювати з ним як з `Integer`.
>
> **Висновок:** `<T extends Number>` є значно кращим, оскільки він зберігає точну інформацію про тип, забезпечуючи типобезпечність і уникаючи потреби в приведенні типів.

#### **📌 Що перевіряють:**

*Розуміння різниці між параметризацією типу (зберігає інформацію) та використанням поліморфізму через базовий клас (втрачає інформацію).*

* * *

### **8 ❓ Питання:**

Припустимо, у вас є метод `<T> void process(List<T> list)`. Чи можна всередині нього викликати `list.add(new Object())`? А якщо метод `<T extends Number> void process(List<T> list)`, чи можна викликати `list.add(new Integer(1))`? ⭐️

> #### **💬 Відповідь:**
>
> **Перша частина: `<T> void process(List<T> list)`**
>
> * `list.add(new Object())` — **Ні, не можна.**
> * **Причина:** Компілятор не знає, що таке `T`. Це може бути `String`. Якщо це так, то додавання `Object` у `List<String>` було б нетипобезпечним. Метод `add` для `List<T>` очікує аргумент типу `T`.
>
> **Друга частина: `<T extends Number> void process(List<T> list)`**
>
> * `list.add(new Integer(1))}` — **Ні, все ще не можна.**
> * **Причина:** Хоча ми знаємо, що `T` є нащадком `Number`, ми **не знаємо, яким саме**. Наприклад, `list` може бути `List<Double>`. Додавати `Integer` у `List<Double>` є нетипобезпечним.
>
> **Загальне правило:** Ви не можете додавати в колекцію `List<T>` або `List<? extends T>` нічого, крім `null` (і `T` у першому випадку), тому що компілятор не може гарантувати типобезпечність. Він не знає, що таке `T` насправді.

#### **📌 Що перевіряють:**

*Глибоке розуміння обмежень, які накладає невідомий тип-параметр, навіть якщо він обмежений. Це ключова концепція для розуміння wildcards.*

* * *

### **9 ❓ Питання:**

Якби ви реалізовували метод `Collections.addAll`, яку дженерик-сигнатуру ви б використали? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `addAll` має додавати елементи з масиву у колекцію.
>
> `public static <T> boolean addAll(Collection<T> c, T... elements)` — це перша, наївна думка. Але її можна покращити.
>
> Розглянемо **принцип PECS (Producer Extends, Consumer Super)**.
>
> 1. Колекція `c` є **споживачем (Consumer)**. Ми в неї **записуємо** дані. Отже, для максимальної гнучкості слід використовувати `super`. Це дозволить додавати, наприклад, `Integer`-и в `List<Number>`. Сигнатура стає: `Collection<? super T>`.
>
> 2. Масив `elements` є **виробником (Producer)**. Ми з нього тільки **читаємо** дані. Тут можна було б використати `extends`, але з масивами це складніше. Стандартний `T...` є достатнім і працює, оскільки масиви в Java є коваріантними.
>
> **Реальна сигнатура в JDK:**
> `public static <T> boolean addAll(Collection<? super T> c, T... elements)`
>
> **Що це дає:**
>
> ```java
> List<Number> numbers = new ArrayList<>();
> Integer[] ints = {1, 2, 3};
>
> // Завдяки <? super T>, ми можемо додати масив Integer-ів у список Number-ів
> Collections.addAll(numbers, ints);
> ```
>
> Це демонструє, як правильне використання wildcards робить API більш гнучким.

#### **📌 Що перевіряють:**

*Здатність застосовувати принцип PECS для проектування власних API. Розуміння коваріантності та контрваріантності.*

* * *

### **10 ❓ Питання:**

Напишіть дженерик-метод, який знаходить перший елемент у `List`, що відповідає певному критерію, з використанням `Predicate`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> ```java
> import java.util.List;
> import java.util.function.Predicate;
>
> public class Finder {
>
>     // Метод приймає список List<T>
>     // і предикат, що працює з типом T або будь-яким його предком
>     public static <T> T findFirst(List<T> list, Predicate<? super T> predicate) {
>         for (T item : list) {
>             // predicate.test() приймає T, що є безпечним, оскільки
>             // предикат очікує T або його супертип
>             if (predicate.test(item)) {
>                 return item;
>             }
>         }
>         return null; // або кинути виняток
>     }
> }
> ```
>
> **Пояснення `Predicate<? super T>`:**
>
> * `Predicate` є **споживачем (Consumer)**. Його метод `test` "споживає" елемент типу `T`.
> * Згідно з PECS, для споживачів використовуємо `super`.
> * Це робить метод більш гнучким. Наприклад, ви можете шукати `Student` у `List<Student>`, використовуючи `Predicate<Person>`, якщо `Student extends Person`.
>
> **Приклад використання:**
> `List<String> names = List.of("A", "B", "LongName");`
> `Predicate<String> longNameCheck = s -> s.length() > 4;`
> `String found = Finder.findFirst(names, longNameCheck); // "LongName"`

#### **📌 Що перевіряють:**

*Вміння комбінувати дженеріки з функціональними інтерфейсами (як `Predicate`) та правильно застосовувати wildcards (PECS).*

* * *

### **11 ❓ Питання:**

Чи можна накласти обмеження на те, щоб тип `T` мав конструктор без аргументів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, в Java **немає** прямого синтаксичного способу накласти таке обмеження на тип-параметр.
>
> `public class Factory<T where T: new()> { ... }` — такого синтаксису (як у C#) в Java немає.
>
> Це одне з обмежень дженеріків, яке ускладнює створення екземплярів `T` (`new T()`).
>
> **Що робити натомість?**
>
> Якщо вашому дженерик-класу потрібно створювати екземпляри `T`, ви маєте використовувати обхідні шляхи:
>
> 1. **Передавати `Class<T>`:** Як вже згадувалося, ви передаєте "токен типу" і використовуєте рефлексію для виклику `clazz.getDeclaredConstructor().newInstance()`. Цей підхід кидатиме `Exception`, якщо конструктора без аргументів не існує, тобто перевірка відбудеться під час виконання.
>
> 2. **Передавати `Supplier<T>`:** Це більш безпечний і сучасний підхід. Ви передаєте "фабрику" для `T` у вигляді лямбда-виразу.
>
>     ```java
>     public static <T> T create(Supplier<T> factory) {
>         return factory.get();
>     }
>     // Виклик: User user = create(User::new);
>     ```
>
> Цей підхід гарантує наявність способу створення об'єкта вже на етапі компіляції.

#### **📌 Що перевіряють:**

*Знання про обмеження системи типів Java. Розуміння того, чому `new T()` не працює, та вміння пропонувати сучасні альтернативні рішення.*

* * *

### **12 ❓ Питання:**

Поясніть, що не так з цим кодом і як його виправити, щоб він скомпілювався. `public <T, E extends T> void process(T t, E e) { t = e; }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код **повністю валідний і скомпілюється без помилок**.
>
> Це може здатися контрінтуїтивним на перший погляд, але давайте розберемося.
>
> 1. Оголошено два типи: `T` і `E`.
> 2. `E` обмежений як нащадок `T`: `<T, E extends T>`.
> 3. Змінна `t` має тип `T`. Змінна `e` має тип `E`.
> 4. Виконується присвоєння `t = e;`.
>
> Це присвоєння є **типобезпечним**, тому що `E` є підтипом `T`. Згідно з принципами поліморфізму, ви завжди можете присвоїти об'єкт дочірнього класу змінній батьківського класу.
>
> **А от зворотне присвоєння було б невалідним:**
>
> `public <T, E extends T> void process(T t, E e) { e = t; } // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> Тому що `t` (батьківський тип) не обов'язково є екземпляром `E` (дочірнього типу). Для цього потрібне було б явне і небезпечне приведення типу.

#### **📌 Що перевіряють:**

*Уважність та глибоке розуміння того, як працює поліморфізм у поєднанні з дженериками. Питання створено, щоб "підловити" кандидата, який може поспішити і сказати, що код невалідний.*

* * *

### **13 ❓ Питання:**

Що буде, якщо спробувати обмежити тип `final`-класом, наприклад `<T extends String>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Такий код **є валідним**, але практично **не має сенсу**.
>
> Оголошення `<T extends String>` означає, що тип `T` може бути **або самим `String`**, або **його нащадком**.
>
> Оскільки клас `String` є **`final`**, у нього не може бути нащадків.
>
> Тому **єдиний** тип, який може задовольнити це обмеження, — це сам `String`.
>
> **Що це означає на практиці:**
>
> Дженерик `<T extends String>` стає еквівалентним простому використанню `String` напряму.
>
> `public <T extends String> void process(T value)`
>
> Функціонально є таким самим, як:
>
> `public void process(String value)`
>
> Компілятор просто замінить `T` на `String` і все буде працювати.
>
> Таким чином, хоча це синтаксично коректно, це надлишково і ускладнює код без жодної користі. Це ознака того, що розробник не до кінця зрозумів ідею обмежень.

#### **📌 Що перевіряють:**

*Здатність комбінувати знання з різних тем (дженеріки, `final`-класи) і робити логічні висновки.*

* * *

### **14 ❓ Питання:**

Як обмежені дженеріки допомагають вирішити проблеми при приведенні типів після стирання? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Коли компілятор стирає типи, він замінює тип-параметр `T` на його **межу (bound)**, а не просто на `Object`.
>
> **Без обмеження:**
>
> * `class Box<T> { private T item; }`
> * Після стирання -> `class Box { private Object item; }`
>
> **З обмеженням:**
>
> * `class Box<T extends Number> { private T item; }`
> * Після стирання -> `class Box { private Number item; }`
>
> **Як це допомагає:**
>
> 1. **Більш точна інформація в байт-коді:** JVM під час виконання знає, що поле `item` має тип `Number`, а не просто `Object`. Це дозволяє уникнути деяких неперевірених попереджень (unchecked warnings).
>
> 2. **Гарантія наявності методів:** Компілятор додає неявне приведення типу до `Number` при доступі до `item`. Це дозволяє вам безпечно викликати методи, визначені в `Number`, такі як `doubleValue()`, оскільки компілятор знає, що це приведення завжди буде успішним.
>
> ```java
> public double getAsDouble(Box<T extends Number> box) {
>     T item = box.get();
>     // Компілятор додає неявний каст: Number num = (Number) item;
>     // І виклик стає безпечним
>     return item.doubleValue();
> }
> ```
>
> Таким чином, обмеження надають компілятору і JVM більше інформації, що робить код більш надійним і безпечним навіть після стирання типів.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння механізму стирання типів і того, як обмеження впливають на згенерований байт-код.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви проектуєте API. В одному випадку ви можете використати дженерик-метод, а в іншому — два перевантажених методи. Коли який підхід кращий? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про вибір між гнучкістю та простотою/очевидністю.
>
> **Використовуйте дженерик-метод, коли:**
>
> * **Логіка для різних типів абсолютно ідентична.** Ви хочете уникнути дублювання коду.
>
>     ```java
>     // Ідеально для дженерика
>     public <T> void log(T item) { System.out.println(item.toString()); }
>     ```
>
> * **Вам потрібно забезпечити зв'язок між типами аргументів.** Наприклад, що два аргументи мають бути одного типу.
>
>     ```java
>     // Гарантує, що a і b одного типу
>     public <T> T pick(T a, T b) { ... }
>     ```
>
> **Використовуйте перевантажені методи, коли:**
>
> * **Логіка для різних типів суттєво відрізняється.** Дженерик-метод з купою `if (obj instanceof ...)` всередині — це антипатерн.
>
>     ```java
>     // Краще, ніж один дженерик з if'ами
>     public void process(String s) { /* логіка для рядка */ }
>     public void process(Integer i) { /* логіка для числа */ }
>     ```
>
> * **Ви хочете надати різні реалізації для примітивів та їхніх обгорток.** Дженерік не працює з примітивами, тому перевантаження `process(int i)` та `process(Integer i)` може бути корисним.
> * **Ви хочете зробити API більш очевидним.** Іноді краще мати два чітких, конкретних методи, ніж один абстрактний дженерик-метод, призначення якого потрібно "розшифровувати" через його сигнатуру.
>
> **Загальне правило:** Якщо логіка однакова, використовуйте дженеріки. Якщо логіка різна — перевантаження.

#### **📌 Що перевіряють:**

*Здатність приймати обґрунтовані рішення щодо дизайну API, зважаючи на читабельність, підтримуваність та гнучкість.*
