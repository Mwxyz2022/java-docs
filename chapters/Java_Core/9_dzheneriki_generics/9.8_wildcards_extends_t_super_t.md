---
layout: default
title: "Wildcards (?, ? extends T, ? super T)"
parent: "Дженеріки (Generics)"
grand_parent: "Java Core"
nav_order: 8
---

# Wildcards (?, ? extends T, ? super T)

* короткий опис: `Wildcards` – це символи підстановки (`?`) у дженеріках, що дозволяють працювати з різними, але сумісними типами. Вони бувають трьох видів: `unbounded` (`<?>`), `upper-bounded` (`? extends T`), та `lower-bounded` (`? super T`).

* поняття: Wildcard, `unbounded wildcard` (`<?>`), `upper-bounded` (`? extends T`), `lower-bounded` (`? super T`).

* приклади: `List<?>` (будь-який тип), `List<? extends Number>` (читання чисел), `List<? super Integer>` (запис цілих).

* ключові моменти: Правило PECS (Producer Extends, Consumer Super) допомагає вибрати правильний `wildcard`. `extends` – для читання, `super` – для запису.

* що важливо знати на співбесіді: Коли використовувати `extends` (тільки читання), `super` (тільки запис), `<?>` (читання як `Object`).

### **✅ Офіційне визначення:**

Wildcards — це спеціальні позначення (символи підстановки) в дженеріках Java, які дозволяють працювати з різними, але сумісними типами, не вказуючи конкретний тип. Використовуються через символ **`?`**.

### **🧠 Простими словами:**

Wildcards — це "джокери" у дженеріках. Коли ти не знаєш точно, який тип буде в списку, але знаєш — це має бути або якийсь нащадок певного типу, або предок. Наприклад, список будь-яких чисел (**`List<? extends Number>`**) або будь-яких типів, які можуть прийняти **`Integer`** (**`List<? super Integer>`**).

---

## **📊 Основні типи wildcards:**

| Синтаксис | Назва | Значення / що дозволяє |
| :---: | :---: | :---: |
| **`List<?>`** | Unbounded wildcard | Список чогось, тип не важливий |
| **`List<? extends Number>`** | Upper bound | Список **`Number`** або його підкласів |
| **`List<? super Integer>`** | Lower bound | Список **`Integer`** або його батьків |

📌 **Правило PECS:**

* **Producer — Extends** -> якщо ми **читаємо** **`(get) -> ? extends`**
* **Consumer — Super** -> якщо ми **пишемо** **`(add) -> ? super`**

---

## **Пояснення на пальцях (💡)**

Уяви полицю з ящиками (дженерік-колекція).

* Якщо ти не знаєш, що всередині, але тобі треба лише глянути — **`? extends T`**.
* Якщо тобі треба щось покласти в ящик — але ти не знаєш, що він точно приймає — **`? super T.`**
* А якщо тобі просто все одно — **`<?>`**.

🔸 PECS \= **"Продавець — Extends, Споживач — Super"**

---

## **Корисні приклади (🧪)**

### **🔸 `List<?>` — будь-який тип:**

```java
List<?> list = List.of("hello", 42, true);
// можна читати
Object value = list.get(0);
// не можна додати елемент -- тип не відомий
// list.add("something"); // ❌
```

### **🔸 `List<? extends Number>` — тільки для читання:**

```java
List<? extends Number> numbers = List.of(1, 2.5, 3L);
Number n = numbers.get(0); // ✅
// numbers.add(5); // ❌ -- не можна додавати
```

### **🔸 `List<? super Integer>` — тільки для запису:**

```java
List<? super Integer> list = new ArrayList<Number>();
list.add(10); // ✅ можна додати Integer
// Integer i = list.get(0); // ❌ -- повертається Object
```

---

## **Порівняння (⚖️)**

| Синтаксис | Читання (`get`) | Запис (`add`) | Для чого підходить |
| :---: | :---: | :---: | :---: |
| **`List<?>`** | ✅ Object | ❌ | Якщо не важливо, що всередині |
| **`List<? extends T>`** | ✅ T | ❌ | Якщо ти тільки читаєш |
| **`List<? super T>`** | ❌ (Object) | ✅ T | Якщо ти тільки записуєш |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **PECS** \= *Producer Extends, Consumer Super*
* **? \= хто-зна-що**
* **`extends`** \= я читач (джерело)
* **`super`** \= я писач (приймач)
* **`?`** \= “дай мені, що завгодно, я обережно подивлюсь” 👀

---

## **🟩 Підсумок:**

Wildcards у Java — це гнучкий спосіб роботи з колекціями, коли ти не знаєш або не хочеш вказувати точний тип. Вони дозволяють створювати універсальні API, які не прив'язані до конкретного типу. Ключове правило — **PECS** — допоможе тобі завжди пам’ятати, коли який wildcard доречний.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Wildcards**](https://docs.oracle.com/javase/tutorial/extra/generics/wildcards.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке маски (wildcards) у дженеріках і навіщо вони потрібні?

> #### **💬 Відповідь:**
>
> **Маска (Wildcard)**, що позначається знаком питання (`?`), — це спеціальний синтаксис у дженеріках, який представляє **невідомий тип**. Він використовується для створення більш гнучких методів та API, які можуть працювати з різними параметризаціями одного й того ж дженерик-класу.
>
> **Навіщо вони потрібні:**
>
> Дженеріки в Java є **інваріантними**. Це означає, що `List<String>` не є підтипом `List<Object>`. Wildcards дозволяють "пом'якшити" це правило і зробити дженеріки **коваріантними** або **контрваріантними**, що робить ваш код більш гнучким.
>
> **Приклад проблеми, яку вирішують wildcards:**
>
> ```java
> void printList(List<Object> list) { ... }
>
> List<String> stringList = new ArrayList<>();
> // printList(stringList); // ПОМИЛКА КОМПІЛЯЦІЇ
> ```
>
> Ви не можете передати `List<String>` туди, де очікується `List<Object>`.
>
> **Рішення з wildcard:**
>
> ```java
> void printList(List<?> list) { ... }
> printList(stringList); // OK!
> ```

#### **📌 Що перевіряють:**

*Розуміння того, що wildcards є вирішенням проблеми інваріантності дженеріків, та їхня основна мета — підвищення гнучкості API.*

* * *

### **2 ❓ Питання:**

Назвіть три типи масок (wildcards) та коротко їх опишіть.

> #### **💬 Відповідь:**
>
> Існує три типи масок:
>
> 1. **Необмежена маска (`?` - Unbounded Wildcard):**
>     * **Синтаксис:** `List<?>`
>     * **Значення:** "Список якогось невідомого типу".
>     * **Використання:** Коли логіка методу не залежить від типу елементів колекції (наприклад, метод `size()` або `print()`).
>
> 2. **Маска з верхньою межею (`? extends T` - Upper Bounded Wildcard):**
>     * **Синтаксис:** `List<? extends Number>`
>     * **Значення:** "Список, елементи якого є `Number` або будь-яким його нащадком (`Integer`, `Double` тощо)".
>     * **Використання:** Коли вам потрібно **читати** елементи з колекції та працювати з ними через API базового типу `T`.
>
> 3. **Маска з нижньою межею (`? super T` - Lower Bounded Wildcard):**
>     * **Синтаксис:** `List<? super Integer>`
>     * **Значення:** "Список, елементи якого є `Integer` або будь-яким його предком (`Number`, `Object`)".
>     * **Використання:** Коли вам потрібно **записувати** елементи типу `T` у колекцію.

#### **📌 Що перевіряють:**

*Знання трьох видів wildcards та їхнього основного призначення.*

* * *

### **3 ❓ Питання:**

Що означає правило PECS (Producer Extends, Consumer Super)? Поясніть на прикладі.

> #### **💬 Відповідь:**
>
> **PECS** — це мнемонічне правило, яке допомагає вибрати правильний тип маски (`extends` чи `super`) для параметрів методів.
>
> * **`Producer Extends` (Виробник використовує `extends`):**
>   * Якщо ваша структура даних (наприклад, `List`) виступає в ролі **виробника (producer)**, тобто ви з неї переважно **читаєте (отримуєте)** дані, використовуйте `? extends T`.
>   * Це гарантує, що ви можете безпечно отримати з неї об'єкти як тип `T`.
>
> * **`Consumer Super` (Споживач використовує `super`):**
>   * Якщо ваша структура даних виступає в ролі **споживача (consumer)**, тобто ви в неї переважно **записуєте (додаєте)** дані, використовуйте `? super T`.
>   * Це гарантує, що ви можете безпечно додати в неї об'єкти типу `T`.
>
> **Класичний приклад — копіювання:**
>
> ```java
> public static <T> void copy(List<? extends T> src, List<? super T> dest) {
>     for (T item : src) { // `src` - виробник, ми читаємо з нього
>         dest.add(item);   // `dest` - споживач, ми пишемо в нього
>     }
> }
> ```
>
> Ця сигнатура є максимально гнучкою. Наприклад, ви можете копіювати `List<Integer>` ( `src`) у `List<Number>` (`dest`).

#### **📌 Що перевіряють:**

*Знання та розуміння принципу PECS — це ключовий індикатор глибокого володіння дженеріками. Це питання часто ставлять на співбесідах.*

* * *

### **4 ❓ Питання:**

У чому обмеження використання `List<?>`? Чому в нього не можна нічого додати (крім `null`)?

> #### **💬 Відповідь:**
>
> **Обмеження:** Ви не можете викликати методи, які приймають тип-параметр як аргумент, наприклад `add(E element)`.
>
> `List<?> list = new ArrayList<String>();`
> `list.add("hello"); // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Причина:**
>
> `List<?>` означає "список **якогось конкретного, але невідомого** типу". Компілятор не знає, що це за тип.
>
> * Можливо, це `List<String>`.
> * Можливо, це `List<Integer>`.
> * Можливо, це `List<User>`.
>
> Оскільки компілятор не знає точного типу, він не може гарантувати типобезпечність при додаванні елемента. Якби він дозволив вам додати `"hello"` (`String`), а насправді це був `List<Integer>`, це призвело б до помилки типу.
>
> Щоб запобігти таким ситуаціям, компілятор **забороняє** додавати будь-що в `List<?>`, крім **`null`**, бо `null` є сумісним з будь-яким посилальним типом.
>
> Читати з `List<?>` можна, але ви отримаєте елементи тільки як `Object`.

#### **📌 Що перевіряють:**

*Розуміння того, як "невідомий" тип `?` обмежує операції запису для забезпечення типобезпечності.*

* * *

### **5 ❓ Питання:**

Що можна робити з `List<? extends Number>`, а що ні? ⭐️

> #### **💬 Відповідь:**
>
> `List<? extends Number>` означає список, що містить об'єкти типу `Number` або будь-якого його нащадка.
>
> **Що МОЖНА робити (читання):**
>
> * **Безпечно читати (отримувати)** елементи з колекції. Компілятор гарантує, що будь-який елемент, який ви отримаєте, є як мінімум `Number`.
>
>     ```java
>     List<Integer> intList = new ArrayList<>();
>     List<? extends Number> numberList = intList;
>
>     Number n = numberList.get(0); // OK
>     System.out.println(n.doubleValue()); // Можна викликати методи класу Number
>     ```
>
>
> **Що НЕ МОЖНА робити (запис):**
>
> * **Не можна додавати** елементи в колекцію (крім `null`). Причина та ж, що і для `List<?>`: компілятор не знає точного типу списку. Він може бути `List<Integer>`, `List<Double>` тощо. Додавання `new Integer(1)` у `List<Double>` було б небезпечним. Щоб уникнути цього, компілятор забороняє будь-які операції `add`.

#### **📌 Що перевіряють:**

*Розуміння того, що `extends`-wildcard робить колекцію фактично "тільки для читання".*

* * *

### **6 ❓ Питання:**

Що можна робити з `List<? super Integer>`, а що ні? ⭐️

> #### **💬 Відповідь:**
>
> `List<? super Integer>` означає список, що містить об'єкти типу `Integer` або будь-якого його предка (`Number`, `Object`).
>
> **Що МОЖНА робити (запис):**
>
> * **Безпечно додавати (записувати)** елементи типу `Integer` (та його нащадків) у колекцію.
> * **Причина:** Будь-який `Integer` можна безпечно "розширити" до його предка. Якщо це `List<Number>`, `Integer` туди підійде. Якщо це `List<Object>`, `Integer` туди теж підійде.
>
>     ```java
>     List<Number> numberList = new ArrayList<>();
>     List<? super Integer> consumerList = numberList;
>
>     consumerList.add(10); // OK. 10 - це Integer, який можна покласти в List<Number>
>     ```
>
> **Що НЕ МОЖНА робити (читання):**
>
> * Ви **не можете** безпечно прочитати елемент як `Integer`. Коли ви викликаєте `get()`, ви не знаєте, що саме отримаєте. Це може бути `Number` або `Object`, але не обов'язково `Integer`. Єдине, що гарантується — це те, що ви отримаєте об'єкт, який є предком `Integer`, тобто, в найзагальнішому випадку, це `Object`.
>
>     `Object obj = consumerList.get(0); // OK`
>     `Integer i = consumerList.get(0); // ПОМИЛКА КОМПІЛЯЦІЇ`

#### **📌 Що перевіряють:**

*Розуміння того, що `super`-wildcard робить колекцію фактично "тільки для запису" (типу `T`).*

* * *

### **7 ❓ Питання:**

Поясніть, у чому різниця між `List<?>` та `List<Object>`. ⭐️

> #### **💬 Відповідь:**
>
> Це дуже важлива відмінність, яку часто плутають.
>
> * **`List<Object>`:**
>   * Це параметризований тип, що означає "список, який може зберігати об'єкти **абсолютно будь-якого типу**". Ви можете одночасно додати в нього `String`, `Integer` та `User`.
>   * Це дуже конкретний тип.
>
> * **`List<?>`:**
>   * Це маска (wildcard), що означає "список, що містить елементи **якогось одного конкретного, але невідомого** типу".
>   * Це може бути `List<String>` або `List<Integer>`, але не те й інше одночасно.
>
> **Ключова відмінність у присвоєнні:**
>
> `List<String>` **не є** підтипом `List<Object>`, тому таке присвоєння заборонено.
> `List<Object> objList = new ArrayList<String>(); // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> Але `List<String>` **можна** присвоїти `List<?>`, тому що `?` може представляти `String`.
> `List<?> wildcardList = new ArrayList<String>(); // OK`
>
> `List<Object>` є менш гнучким, але дозволяє записувати в себе будь-що. `List<?>` є більш гнучким для параметрів методів, але забороняє запис.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантики wildcards. Здатність пояснити, чому `List<Object>` не є "супертипом" для всіх `List<T>`.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати wildcards в оголошенні класу, наприклад `public class Box<?>`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**.
>
> Wildcards (`?`, `? extends T`, `? super T`) призначені для використання тільки як **тип-аргументи** для **змінних, полів, параметрів методів та типів повернення**.
>
> Ви **не можете** використовувати wildcard в оголошенні **типу-параметра** самого дженерик-класу або методу.
>
> * `public class Box<?> { ... } // ПОМИЛКА КОМПІЛЯЦІЇ`
> * `public <?> T myMethod() { ... } // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Причина:**
> Коли ви оголошуєте дженерик-клас `<T>`, ви даєте ім'я цьому типу-параметру (`T`), щоб потім могти його використовувати всередині класу ( `private T item;`, `public T get()`).
>
> Знак `?` є анонімним, він не дає імені типу. Тому ви не змогли б на нього посилатися всередині класу.
>
> Отже, wildcards використовуються для роботи з **існуючими** дженерик-типами, а не для їхнього **оголошення**.

#### **📌 Що перевіряють:**

*Знання синтаксичних обмежень та розуміння різниці між оголошенням типу-параметра та використанням типу-аргумента.*

* * *

### **9 ❓ Питання:**

Припустимо, у вас є метод: `public void process(Set<? extends Number> numbers)`. Чи можете ви передати в нього `Set<Integer>`? Чи можете ви всередині методу викликати `numbers.add(new Double(1.0))`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **1. Чи можна передати `Set<Integer>`?**
>
> * **Так, можна.** `Integer` є нащадком `Number`, тому `Set<Integer>` задовольняє умову `Set<? extends Number>`. Це приклад **коваріантності**, яку надає `extends`.
>
> **2. Чи можна викликати `numbers.add(new Double(1.0))` всередині методу?**
>
> * **Ні, не можна.**
> * **Причина:** Компілятор знає, що `numbers` — це `Set` з нащадками `Number`, але він **не знає, з якими саме**.
> * Це може бути `Set<Integer>`. Додавати `Double` у `Set<Integer>` було б нетипобезпечним.
> * Це може бути `Set<Float>`. Додавати `Double` у `Set<Float>` також небезпечно.
>
> Оскільки компілятор не може гарантувати безпеку операції `add` для **будь-якого** можливого типу, що задовольняє `? extends Number`, він **забороняє** її.
>
> **Правило:** у колекцію з `? extends T` не можна додавати нічого, крім `null`.

#### **📌 Що перевіряють:**

*Практичне застосування знань про `extends`-wildcards та їхні обмеження на операції запису.*

* * *

### **10 ❓ Питання:**

Припустимо, у вас є метод `public void process(List<? super Integer> numbers)`. Які з цих викликів будуть валідними:

1. `numbers.add(10);`
2. `Integer i = numbers.get(0);`
3. `Number n = numbers.get(0);`
4. `Object o = numbers.get(0);`

Поясніть кожен випадок. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Розглянемо кожен випадок:
>
> 1. **`numbers.add(10);` — ВАЛІДНО.**
>     * `List<? super Integer>` означає "список `Integer` або будь-якого його предка (`Number`, `Object`)". В будь-який з цих списків можна безпечно додати `Integer`, оскільки `Integer` є сумісним з ними.
>
> 2. **`Integer i = numbers.get(0);` — НЕВАЛІДНО.**
>     * Ви не можете бути впевнені, що з `get()` ви отримаєте саме `Integer`. Цей список може бути `List<Number>`, і містити, наприклад, `Double`. Присвоїти `Double` до `Integer` не можна.
>
> 3. **`Number n = numbers.get(0);` — НЕВАЛІДНО.**
>     * Та сама причина. Список може бути `List<Object>`, і містити, наприклад, `String`. Присвоїти `String` до `Number` не можна.
>
> 4. **`Object o = numbers.get(0);` — ВАЛІДНО.**
>     * Єдине, що гарантує `List<? super Integer>`, — це те, що його елементи є предками `Integer`. У найгіршому випадку це `Object`. Тому ви завжди можете безпечно отримати будь-який елемент як `Object`.
>
> **Коротко:** `? super T` — безпечний для запису (`add`) елементів типу `T`, але безпечний для читання (`get`) тільки як `Object`.

#### **📌 Що перевіряють:**

*Детальне розуміння обмежень, які накладає `super`-wildcard на операції читання.*

* * *

### **11 ❓ Питання:**

Як wildcards допомагають вирішити проблему, пов'язану з неможливістю перевантажити методи через стирання типів (`public void m(List<String> l)` та `public void m(List<Integer> l)`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Wildcards не дозволяють **напряму вирішити** цю проблему (ви все ще не можете створити два такі методи), але вони дозволяють **обійти** її, створивши один, більш гнучкий метод.
>
> Замість двох методів ви можете створити **один метод з wildcard**:
>
> ```java
> public void processList(List<?> list) {
>     if (list.isEmpty()) {
>         // нічого не робимо
>         return;
>     }
>
>     // Перевіряємо тип першого елемента під час виконання
>     Object first = list.get(0);
>     if (first instanceof String) {
>         // Тут можна безпечно привести тип і працювати з List<String>
>         processStringList((List<String>) list);
>     } else if (first instanceof Integer) {
>         processIntegerList((List<Integer>) list);
>     }
> }
>
> private void processStringList(List<String> list) { /* ... */ }
> private void processIntegerList(List<Integer> list) { /* ... */ }
> ```
>
> Це є патерном "ручної диспетчеризації типів". Ви приймаєте будь-який список, а потім за допомогою `instanceof` визначаєте, з чим саме ви працюєте, і викликаєте відповідний приватний метод.
>
> Це не таке елегантне рішення, як перевантаження, але це один із способів впоратися з обмеженнями, накладеними стиранням типів.

#### **📌 Що перевіряють:**

*Здатність застосовувати wildcards та перевірку типів у рантаймі як "обхідний шлях" для емуляції перевантаження.*

* * *

### **12 ❓ Питання:**

Що таке захоплення маски (wildcard capture)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Захоплення маски** — це процес, під час якого компілятор Java **"захоплює"** невідомий тип, представлений wildcard (`?`), і дає йому **тимчасове, приховане ім'я**. Це дозволяє компілятору виконувати перевірку типів так, ніби цей невідомий тип є звичайним дженерик-параметром.
>
> **Коли це відбувається?**
>
> Розглянемо приклад:
>
> ```java
> public void swap(List<?> list, int i, int j) {
>     // ... як поміняти елементи місцями?
> }
> ```
>
> Нам потрібно дістати елемент з `list.get(i)` і покласти його в `list.set(j, ...)`. Але ми не можемо цього зробити, бо не знаємо типу.
>
> **Рішення:** Створити **приватний дженерик-метод-помічник**.
>
> ```java
> public static void swap(List<?> list, int i, int j) {
>     // Компілятор захоплює '?' і передає його як 'T' в swapHelper
>     swapHelper(list, i, j);
> }
>
> // Приватний метод, де невідомий тип отримав ім'я 'T'
> private static <T> void swapHelper(List<T> list, int i, int j) {
>     T temp = list.get(i);
>     list.set(i, list.get(j));
>     list.set(j, temp);
> }
> ```
>
> Коли викликається `swapHelper`, компілятор "захоплює" невідомий тип з `List<?>` і ставиться до нього як до конкретного `T` всередині `swapHelper`. Це гарантує, що типи елементів, які ми міняємо місцями, є однаковими.

#### **📌 Що перевіряють:**

*Дуже глибоке знання внутрішніх механізмів компілятора. Це просунута тема, яка демонструє експертний рівень.*

* * *

### **13 ❓ Питання:**

Чи може тип з `extends`-wildcard бути використаний у `return` statement? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, може. Це типовий сценарій для **виробників (producers)**.
>
> Ви можете оголосити метод, який повертає, наприклад, `List<? extends Number>`.
>
> **Коли це корисно?**
>
> Коли ви хочете надати клієнту **коваріантний доступ "тільки для читання"** до внутрішньої колекції. Це дає гнучкість, не порушуючи інкапсуляцію.
>
> **Приклад:**
>
> ```java
> public class NumberStorage {
>     private List<Integer> internalList = new ArrayList<>();
>
>     // ...
>
>     // Метод повертає список, який можна читати як List<Number>,
>     // але в який не можна нічого додати.
>     public List<? extends Number> getNumbers() {
>         return internalList;
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> NumberStorage storage = new NumberStorage();
> List<? extends Number> numbers = storage.getNumbers();
> Number n = numbers.get(0); // OK
> // numbers.add(3.14); // ПОМИЛКА КОМПІЛЯЦІЇ
> ```
>
> Це захищає внутрішню `List<Integer>` від додавання в неї, наприклад, `Double`, через посилання `List<? extends Number>`.

#### **📌 Що перевіряють:**

*Розуміння того, як wildcards застосовуються не тільки до параметрів, але й до типів повернення для забезпечення гнучкості та безпеки API.*

* * *

### **14 ❓ Питання:**

Яка різниця у використанні wildcards та дженерик-методів? Коли краще одне, а коли інше? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вибір залежить від того, чи потрібен вам **зв'язок між типами** в сигнатурі методу.
>
> **Використовуйте wildcards, коли:**
>
> * Вам потрібна гнучкість для **одного** параметра.
> * Логіка методу не потребує знати точного типу.
> * Немає зв'язку між типом одного параметра та типом іншого, або типом повернення.
>   * `void print(List<?> list);` — ідеально для wildcard.
>
> **Використовуйте дженерик-метод, коли:**
>
> * Вам потрібне **посилання на конкретний, хоч і невідомий, тип `T`**. Наприклад, щоб оголосити локальну змінну цього типу або повернути результат цього типу.
>   * `<T> T getFirst(List<T> list) { return list.get(0); }`
> * Вам потрібно встановити **зв'язок між кількома аргументами або між аргументом та типом повернення**.
>   * `<T> void copy(List<T> src, List<T> dest)` — гарантує, що типи обох списків однакові.
>   * `<T, U> U convert(T from)` — тип повернення залежить від типу `from`.
>
> **Загальне правило:** якщо тип-параметр використовується в сигнатурі **лише один раз**, і його не потрібно повертати, зазвичай можна обійтися **wildcard**. Якщо ж він використовується кілька разів для встановлення залежностей, вам потрібен **дженерик-метод**.

#### **📌 Що перевіряють:**

*Здатність приймати обґрунтоване рішення щодо дизайну методу, аналізуючи зв'язки між типами в його сигнатурі.*

* * *

### **15 ❓ Питання:**

Чому такий код є небезпечним, незважаючи на те, що він компілюється (з попередженням)?
`static void addToList(List<Object> list, Object item) { list.add(item); }`
`addToList(new ArrayList<String>(), 123);` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання демонструє, що джерелом небезпеки є не `List<Object>`, а саме те, як він використовується.
>
> Код `addToList(new ArrayList<String>(), 123);` **не скомпілюється**. Тому що `ArrayList<String>` **не є** підтипом `List<Object>`. Компілятор запобігає цій помилці.
>
> **Але давайте змінимо питання, щоб зробити його небезпечним**, використовуючи **сирий тип** як посередника.
>
> ```java
> // метод з сирим типом
> static void unsafeAdd(List list, Object item) {
>     list.add(item);
> }
>
> public static void main(String[] args) {
>     List<String> strings = new ArrayList<>();
>     // Викликаємо небезпечний метод
>     unsafeAdd(strings, 123); // Компілюється з попередженням!
>
>     // Тепер в `strings` лежить Integer, і це призведе до ClassCastException
>     String s = strings.get(0);
> }
> ```
>
> **Чому це небезпечно?**
>
> Проблема не в `List<Object>`, а в сирому `List`, який дозволяє обійти перевірки типів.
>
> **Початкове питання було сформульовано з підступом.** Воно змушує думати, що `ArrayList<String>` можна передати в `List<Object>`, що не так. Це перевірка на чітке знання інваріантності. Справжня небезпека виникає тільки при взаємодії з сирими типами.

#### **📌 Що перевіряють:**

*Дуже уважне читання коду. Глибоке знання інваріантності дженеріків і ролі сирих типів як джерела небезпеки.*
