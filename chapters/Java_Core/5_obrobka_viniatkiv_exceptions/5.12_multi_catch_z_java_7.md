---
layout: default
title: "Multi-catch (з Java 7)"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 12
---

# Multi-catch (з Java 7)

* короткий опис: Синтаксична можливість (з Java 7), що дозволяє обробити кілька типів винятків в одному `catch`-блоці, використовуючи символ `|` для розділення типів.

* поняття: `multi-catch`, `|` (оператор), `effectively final` (для змінної винятку).

* приклади: `catch (IOException | SQLException e) { ... }`.

* ключові моменти: Зменшує дублювання коду, змінна винятку `e` є `effectively final`. Не можна комбінувати типи, що мають спадкові відносини (наприклад, `Exception | IOException`).

* що важливо знати на співбесіді: Синтаксис, переваги, обмеження.

### **✅ Офіційне визначення:**

Multi-catch — це синтаксична можливість Java, яка дозволяє обробити кілька типів винятків в одному **`catch`**\-блоці, використовуючи символ `|` для розділення типів.

### **🧠 Простими словами:**

Якщо у **`try`**\-блоці може статись кілька помилок, і ти хочеш обробити їх однаково — не треба писати багато **`catch`**. Можна написати один, вказавши всі типи винятків через `|`

---

## **Основне**

* Усі **checked** або **unchecked** винятки, які **не мають спадковості між собою**.
* Не можна змішувати винятки, де один є підтипом іншого.

✔️ **Правильний приклад:**

```java
catch (IOException | SQLException | NumberFormatException e)
```

❌ **Неправильний приклад:**

```java
catch (Exception | IOException e) // IOException -- підтип Exception
```

---

## **Пояснення на пальцях (💡)**

Це як один сачок для кількох видів риб: якщо тобі байдуже, кого спіймав (IOException чи SQLException), — головне, щоб обробити помилку однаково, то multi-catch — ідеальний інструмент

---

## **Корисні приклади (🧪)**

```java
public class MultiCatchDemo {
    public static void main(String[] args) {
        try {
            if (Math.random() > 0.5) {
                throw new IOException("Файл не знайдено");
            } else {
                throw new SQLException("Помилка запиту");
            }
        } catch (IOException | SQLException e) {
            System.out.println("Помилка: " + e.getMessage());

            // e -- effectively final
            // e = new Exception(); ❌ не можна
        }
    }
}
```

---

## **Порівняння (⚖️)**

| Було до Java 7 | Стало з Java 7 |
| :---: | :---: |
| Кілька **`catch`**, дублювання коду | Один **`catch`**, спрощена обробка |
| Складніше підтримувати | Простіше і чистіше |
| DRY порушується | DRY дотримується |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"catch (A | B | C e) -> багато типів -> e фіксований (final)"**

Або по-народному:

**`catch`** — як коробка-сюрприз: не знаєш, що всередині, тому не можна міняти те, що вже спіймав.

---

## **🟩 Підсумок:**

* **Multi-catch** дозволяє в одному **`catch`** обробити **кілька типів винятків**.

* **Синтаксис:** **`catch`** `(Type1 | Type2 e)`
* **Змінну `e` не можна переназначити** — вона *effectively final*.
* **Не можна комбінувати спадкові типи** (наприклад, **`Exception`** `| IOException`).
* Використовуй **multi-catch**, якщо **логіка обробки винятків однакова** — це скорочує код і покращує читабельність.

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Tutorial – Catching Multiple Exception Types**](https://docs.oracle.com/javase/tutorial/essential/exceptions/catch.html)  

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `multi-catch` блок і яку проблему він вирішує?

> #### **💬 Відповідь:**
>
> **`Multi-catch`** — це синтаксична можливість, додана в **Java 7**, яка дозволяє обробляти **кілька різних типів винятків в одному `catch` блоці**.
>
> **Проблема, яку він вирішує:**
>
> Він усуває **дублювання коду**. До Java 7, якщо логіка обробки для кількох типів винятків була однаковою, доводилося писати окремий `catch` блок для кожного з них і копіювати туди той самий код.
>
> **Приклад:**
>
> **До Java 7 (з дублюванням):**
>
> ```java
> try {
>     // ... код, що кидає IOException або SQLException ...
> } catch (IOException e) {
>     logErrorAndNotifyAdmin(e);
> } catch (SQLException e) {
>     logErrorAndNotifyAdmin(e);
> }
> ```
>
> **З Java 7 (`multi-catch`):**
>
> ```java
> try {
>     // ...
> } catch (IOException | SQLException e) {
>     // Один блок для обох типів винятків
>     logErrorAndNotifyAdmin(e);
> }
> ```
>
> `Multi-catch` робить код значно коротшим, чистішим та легшим у підтримці.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `multi-catch` як інструменту для уникнення дублювання коду.*

* * *

### **2 ❓ Питання:**

Який синтаксис використовується для `multi-catch` і які основні правила його використання?

> #### **💬 Відповідь:**
>
> **Синтаксис:**
> Типи винятків перераховуються всередині `catch` через **вертикальну риску (`|`)**, а потім оголошується одна змінна для винятку.
>
> `catch (ExceptionType1 | ExceptionType2 | ... e)`
>
> **Основні правила:**
>
> 1. **Типи не повинні бути пов'язані наслідуванням:** У `multi-catch` блоці не можна перераховувати винятки, якщо один з них є підкласом іншого. Це призведе до **помилки компіляції**, оскільки більш специфічний виняток вже буде перехоплено більш загальним.
>     `catch (Exception | IOException e)` — **не валідно**.
>
> 2. **Змінна винятку є `final`:** Змінна `e` у `multi-catch` блоці є **неявно `final`**. Ви не можете переприсвоїти їй нове значення всередині `catch` блоку. Це зроблено для забезпечення типобезпечності.
>
> 3. Тип змінної `e` — це **найближчий спільний супертип** усіх перерахованих винятків.

#### **📌 Що перевіряють:**

*Знання синтаксису та ключових обмежень, які накладає ця конструкція.*

* * *

### **3 ❓ Питання:**

Чому цей код не скомпілюється?
`catch (IOException | FileNotFoundException e) { ... }`

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється, тому що він **порушує правило `multi-catch` щодо ієрархії винятків**.
>
> **Пояснення:**
>
> 1. `FileNotFoundException` є **підкласом (нащадком)** `IOException`.
>
> 2. Правило `multi-catch` забороняє перераховувати типи, де один є супертипом іншого.
>
> 3. Компілятор розглядає це як **надлишковість**. Оскільки `catch (IOException e)` вже перехоплює і всі `IOException`, і всі його нащадки (включаючи `FileNotFoundException`), то додавання `FileNotFoundException` до цього ж блоку не має сенсу.
>
> **Помилка компіляції буде такою:**
> `The exception FileNotFoundException is already caught by the alternative IOException`
>
> **Як виправити:**
> Достатньо залишити лише батьківський тип:
> `catch (IOException e) { ... }`
> Або, якщо потрібна різна логіка, використовувати окремі `catch`-блоки у правильному порядку.

#### **📌 Що перевіряють:**

*Чітке розуміння правил ієрархії типів у `multi-catch` та вміння виявляти помилки компіляції.*

* * *

### **4 ❓ Питання:**

Якого типу буде змінна `e` у цьому `catch`-блоці, і які методи можна на ній викликати?
`catch (IOException | SQLException e)`

> #### **💬 Відповідь:**
>
> Тип змінної `e` буде визначено як **найближчий спільний супертип** усіх перерахованих винятків.
>
> * `IOException` успадковує `Exception`.
> * `SQLException` також успадковує `Exception`.
>
> Тому найближчим спільним супертипом для них є **`Exception`**.
>
> **Які методи можна викликати:**
>
> * Всередині цього `catch` блоку ви можете **гарантовано** викликати лише ті методи, які **оголошені в класі `Exception`** та його батьківських класах (тобто `Throwable` та `Object`).
> * Це, наприклад, `getMessage()`, `printStackTrace()`, `getCause()`.
> * Ви **не можете** викликати методи, специфічні для `IOException` або `SQLException` (наприклад, `getErrorCode()` з `SQLException`), без явного `instanceof` та приведення типів.
>
> **Приклад:**
>
> ```java
> catch (IOException | SQLException e) {
>     System.out.println(e.getMessage()); // ОК
>     if (e instanceof SQLException) {
>         int errorCode = ((SQLException) e).getErrorCode(); // Потрібен кастинг
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння того, як компілятор виводить тип змінної у `multi-catch`, та як це обмежує доступ до специфічних методів.*

* * *

### **5 ❓ Питання:**

Чому змінна `e` в `multi-catch` блоці є неявно `final`?

> #### **💬 Відповідь:**
>
> Змінна `e` у `multi-catch` є **неявно `final`** для забезпечення **типобезпечності (type safety)**.
>
> **Пояснення:**
>
> 1. Як було сказано, тип змінної `e` є **спільним супертипом**. Наприклад, для `IOException | SQLException` це `Exception`.
> 2. Уявімо, що ви могли б переприсвоїти `e`:
>     `e = new Exception();`
> 3. Якби `multi-catch` використовувався для перехоплення та повторного кидання винятків, це могло б призвести до **неузгодженості типів**.
>
>     ```java
>     // Гіпотетичний небезпечний код
>     catch (IOException | SQLException e) {
>         // Уявимо, що нам дозволено це робити
>         e = new IllegalArgumentException(); // Змінили тип винятку
>         throw e; // Що тут кидати? Метод може не оголошувати IAE
>     }
>     ```
>
> 4. Компілятору було б дуже складно відстежити, якого реального типу є `e` в кожний момент часу.
>
> Зробивши `e` неявно `final`, розробники Java спростили правила і гарантували, що тип винятку, з яким ви працюєте всередині `catch` блоку, залишається тим, що був "зловлений", без несподіваних змін. Це робить поведінку більш передбачуваною.

#### **📌 Що перевіряють:**

*Глибоке розуміння дизайнерських рішень мови та того, як вони забезпечують типобезпечність.*

* * *

### **6 ❓ Питання:**

Коли варто використовувати `multi-catch`, а коли — кілька окремих `catch` блоків?

> #### **💬 Відповідь:**
>
> Вибір залежить від того, чи **однакова логіка обробки** для різних винятків.
>
> **Використовуйте `multi-catch`, коли:**
>
> * Логіка обробки для кількох типів винятків **ідентична**.
> * Наприклад, для `IOException` та `SQLException` ви хочете зробити одне й те саме: залогувати повідомлення про помилку і повернути `null` або кинути єдиний `ServiceException`.
> * Це дозволяє уникнути дублювання коду та робить його чистішим.
>
> **Використовуйте кілька окремих `catch` блоків, коли:**
>
> * Вам потрібна **специфічна, різна логіка** для кожного типу помилки.
> * Наприклад:
>   * Для `FileNotFoundException` ви хочете створити файл.
>   * Для `SocketTimeoutException` ви хочете повторити спробу з'єднання.
>   * Для `SQLException` ви хочете відкотити транзакцію.
>
> Змішувати їх в один `multi-catch`, а потім використовувати `instanceof` для розрізнення — це **антипатерн**.
>
> ```java
> // ПОГАНО:
> catch (FileNotFoundException | SocketTimeoutException e) {
>     if (e instanceof FileNotFoundException) { ... }
>     else if (e instanceof SocketTimeoutException) { ... }
> }
> ```
>
> Якщо логіка різна, використовуйте окремі `catch`-блоки.

#### **📌 Що перевіряють:**

*Практичне розуміння того, коли кожна з конструкцій є найбільш доречною. Уміння писати чистий код без зайвих перевірок.*

* * *

### **7 ❓ Питання:**

Розгляньте такий код. Чи скомпілюється він? Якщо ні, то чому?
`catch(NullPointerException | IllegalArgumentException | IllegalStateException e)`
⭐️

> #### **💬 Відповідь:**
>
> **Так, цей код скомпілюється без проблем.**
>
> **Пояснення:**
>
> Головне правило `multi-catch` — не можна перераховувати винятки, якщо один є нащадком іншого. Давайте перевіримо ієрархію:
>
> * `NullPointerException` -> `RuntimeException` -> `Exception`
> * `IllegalArgumentException` -> `RuntimeException` -> `Exception`
> * `IllegalStateException` -> `RuntimeException` -> `Exception`
>
> Всі три є **"братніми" класами** — вони всі успадковують `RuntimeException`, але жоден з них не є нащадком іншого. Тому жодна з альтернатив не "поглинає" іншу, і компілятор не бачить тут жодної надлишковості чи недосяжного коду.
>
> Типом змінної `e` в цьому випадку буде їхній найближчий спільний супертип, тобто **`RuntimeException`**.

#### **📌 Що перевіряють:**

*Знання ієрархаті стандартних `RuntimeException`-ів та правил `multi-catch`.*

* * *

### **8 ❓ Питання:**

Покажіть, як `multi-catch` допомагає уникнути "checked exception hell" у деяких випадках. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча головний спосіб боротьби з "checked exception hell" — це загортання винятків, `multi-catch` може допомогти зробити код більш лаконічним на межі шарів, де відбувається це загортання.
>
> **Приклад:**
>
> Уявімо метод, який працює з конфігурацією, що може бути у файлі або в базі даних. Він може кинути `IOException` або `SQLException`.
>
> **До `multi-catch`:**
>
> ```java
> public Config loadConfig() {
>     try {
>         // ... логіка, що може кинути IOException або SQLException
>     } catch (IOException e) {
>         throw new ConfigLoadException("Помилка конфігурації", e);
>     } catch (SQLException e) {
>         throw new ConfigLoadException("Помилка конфігурації", e);
>     }
> }
> ```
>
> **З `multi-catch`:**
>
> ```java
> public Config loadConfig() {
>     try {
>         // ...
>     } catch (IOException | SQLException e) {
>         // Один блок для загортання обох checked винятків у наш unchecked
>         throw new ConfigLoadException("Помилка конфігурації", e);
>     }
> }
> ```
>
> Це робить код значно коротшим і дозволяє елегантно обробити цілу групу `checked` винятків, перетворивши їх на один `unchecked` виняток. Це зменшує багатослівність, яка є одним з проявів "checked exception hell".

#### **📌 Що перевіряють:**

*Здатність застосовувати мовні конструкції для реалізації архітектурних патернів.*

* * *

### **9 ❓ Питання:**

Чи можна використовувати `final` перед змінною `e` в `multi-catch` блоці? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, це заборонено.**
>
> Якщо спробувати написати:
> `catch (IOException | SQLException final e)`
>
> То це призведе до **помилки компіляції**.
>
> **Причина:**
>
> Специфікація мови Java вже визначає, що параметр у `multi-catch` блоці є **неявно `final` (implicitly final)**. Його не можна переприсвоїти.
>
> Оскільки ця "фінальність" є **вбудованим правилом**, явне додавання модифікатора `final` було визнано **зайвим та непотрібним**. Щоб уникнути надлишковості та потенційної плутанини, компілятор просто забороняє його писати.
>
> **Поведінка `e` всередині блоку все одно буде такою, ніби вона `final`.**

#### **📌 Що перевіряють:**

*Дуже точне знання синтаксичних правил та їхніх неявних властивостей. Це питання на уважність.*

* * *

### **10 ❓ Питання:**

Якщо у вас є три винятки, і два з них ви хочете обробити однаково, а третій — інакше. Як буде виглядати така конструкція? ⭐️

> #### **💬 Відповідь:**
>
> Для цього слід скомбінувати **`multi-catch` блок** з **окремим `catch` блоком**.
>
> **Приклад:**
> У нас є метод, що працює з мережею та парсить дані, який може кинути `SocketTimeoutException`, `UnknownHostException` та `ParseException`. Ми хочемо обробити обидві мережеві помилки однаково, а помилку парсингу — інакше.
>
> ```java
> try {
>     // ... код, що робить запит і парсить відповідь ...
> } catch (SocketTimeoutException | UnknownHostException e) {
>     // 1. Спільна обробка для мережевих помилок
>     log.error("Мережева помилка. Спробуйте пізніше.", e);
>     showNetworkErrorToUser();
> } catch (ParseException e) {
>     // 2. Специфічна обробка для помилки парсингу
>     log.error("Некоректний формат відповіді.", e);
>     showDataFormatErrorToUser();
> }
> ```
>
> Цей підхід є чистим, читабельним і дозволяє гнучко групувати обробку помилок залежно від їхньої природи.

#### **📌 Що перевіряють:**

*Здатність комбінувати різні конструкції для побудови гнучкої логіки обробки помилок.*

* * *

### **11 ❓ Питання:**

Поясніть, чому не можна зловити `Throwable` в `multi-catch` блоці разом з будь-яким іншим винятком. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Не можна, тому що це **порушить основне правило `multi-catch`**, згідно з яким жоден з перерахованих типів не може бути **супертипом** іншого.
>
> * `Throwable` є **кореневим класом** для всієї ієрархаті винятків та помилок.
> * Будь-який інший виняток (наприклад, `IOException`, `RuntimeException`, `Error`) є його **нащадком**.
>
> Тому вираз `catch (Throwable | IOException e)` є невалідним.
>
> `catch (Throwable t)` сам по собі **перехоплює абсолютно все**. Додавання будь-якого іншого типу є **надлишковим**. Компілятор це виявить і видасть помилку.
>
> Це правило є логічним продовженням правила для звичайних `catch`-блоків, де більш загальний тип не може стояти перед більш специфічним. У `multi-catch` вони знаходяться "на одному рівні", тому взаємне наслідування заборонено.

#### **📌 Що перевіряють:**

*Глибоке розуміння ієрархаті `Throwable` та правил, що регулюють `multi-catch`.*

* * *

### **12 ❓ Питання:**

Що станеться, якщо у вас є `try-with-resources` з кількома ресурсами, і ви хочете по-різному обробляти винятки, кинуті різними методами `close()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це **дуже складне завдання**, і стандартними засобами `try-catch` це зробити **практично неможливо**.
>
> **Проблема:**
> Механізм `try-with-resources` обробляє винятки при закритті **прозоро** для розробника.
>
> * Якщо виняток виникає в `try` блоці, то будь-які винятки з `close()` стають **придушеними**. `catch` блок ловить лише основний виняток.
> * Якщо виняток в `try` не виник, то **перший** виняток з `close()` стає основним, а наступні — придушеними.
>
> Ви **не можете** написати `catch(IOExceptionFromClose1 e)` і `catch(IOExceptionFromClose2 e)`. `catch` не розрізняє, виник виняток з `try` блоку чи з методу `close()`.
>
> **Можливий (але дуже незграбний) обхідний шлях:**
>
> Можна проаналізувати **придушені винятки** всередині `catch` блоку.
>
> ```java
> catch (Exception primary) {
>     for (Throwable suppressed : primary.getSuppressed()) {
>         if (suppressed instanceof CloseExceptionFromResource1) {
>             //...
>         } else if (suppressed instanceof CloseExceptionFromResource2) {
>             //...
>         }
>     }
> }
> ```
>
> Це дуже складний, крихкий і нечитабельний код.
>
> **Правильне рішення:**
> Якщо вам потрібна така тонка логіка, то, ймовірно, `try-with-resources` не є найкращим інструментом. У такому випадку варто повернутися до класичної конструкції **`try-finally`**, де ви можете обернути кожен виклик `close()` у свій власний `try-catch` і реалізувати потрібну логіку обробки.

#### **📌 Що перевіряють:**

*Експертне розуміння обмежень `try-with-resources`. Кандидат повинен знати, коли цей "синтаксичний цукор" не підходить, і потрібно повертатися до більш явних конструкцій.*

* * *

### **13 ❓ Питання:**

Якщо `multi-catch` не існував би, як би ви реалізували обробку кількох винятків з однаковою логікою, уникнувши дублювання коду? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> До появи `multi-catch` у Java 7 існувало кілька способів вирішення цієї проблеми.
>
> **1. Винесення логіки в окремий метод:**
>
> * Це найкращий підхід, що дотримується принципу **DRY (Don't Repeat Yourself)**.
> * Створюється приватний метод, який приймає `Exception` і виконує всю логіку обробки.
> * Кожен `catch` блок просто викликає цей метод.
>
>     ```java
>     try {
>         // ...
>     } catch (IOException e) {
>         handleException(e);
>     } catch (SQLException e) {
>         handleException(e);
>     }
>
>     private void handleException(Exception e) {
>         // вся логіка тут
>         log.error("Error occurred", e);
>     }
>     ```
>
>
> **2. "Падіння" через `case` (Fall-through) — антипатерн:**
>
> Деякі розробники (особливо з C/C++ досвідом) робили так, але це **погана практика**:
>
> ```java
> catch (IOException e) {
>     // немає break, виконання "провалюється" далі
> }
> catch (SQLException e) {
>     // Спільна логіка
> }
> ```
>
> **Проблема:** змінна `e` у другому `catch` не бачить винятку з першого. Це працює некоректно.
>
> **3. Ловля загального супертипу:**
> Можна було б зловити `Exception`, але це погано, бо ловить і `RuntimeException`-и. Якщо у винятків є спільний, більш специфічний батько (наприклад, `MyCustomException`), то можна ловити його.
>
> Отже, винесення логіки в окремий метод було і залишається найкращою альтернативою.

#### **📌 Що перевіряють:**

*Навички рефакторингу та здатність писати чистий код, навіть якщо мова не надає для цього зручного синтаксису.*

* * *

### **14 ❓ Питання:**

Поясніть, що станеться з винятком `e` в цьому коді:
`catch (RuntimeException e) { throw e; }`
⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця конструкція відома як **"повторне кидання" (rethrowing)** винятку.
>
> **Що станеться:**
>
> 1. `catch` блок перехоплює `RuntimeException`.
> 2. Інструкція `throw e;` кидає **той самий екземпляр** винятку далі по стеку викликів.
> 3. **Важливо:** При повторному киданні оригінального екземпляра винятку, його **початковий стек-трейс (stack trace) не змінюється**. Він зберігає інформацію про місце, де виняток виник **спочатку**, а не про `catch` блок, з якого його було повторно кинуто.
>
> **Навіщо це може бути потрібно:**
>
> Ця ідіома використовується, коли вам потрібно **виконати якусь дію** при виникненні винятку (наприклад, залогувати його, відкотити транзакцію), але при цьому ви **не хочете його "ковтати"** і бажаєте, щоб код вищого рівня також про нього дізнався і відреагував.
>
> ```java
> catch (RuntimeException e) {
>     log.error("Неочікувана помилка!", e); // Логуємо
>     rollbackTransaction(); // Виконуємо дію
>     throw e; // Прокидаємо далі, щоб повідомити викличний код
> }
> ```
>
> Це дозволяє реалізувати наскрізну логіку обробки помилок, не втрачаючи інформацію про них.

#### **📌 Що перевіряють:**

*Знання патерну rethrowing та розуміння того, як він зберігає оригінальний контекст помилки.*

* * *

### **15 ❓ Питання:**

Що таке **rethrowing exceptions з покращеним аналізом типів** (з Java 7), і як це пов'язано з `multi-catch`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це ще одне важливе покращення, введене в Java 7, яке робить обробку `checked` винятків більш гнучкою.
>
> **Проблема до Java 7:**
>
> ```java
> // Цей метод змушений оголошувати throws Exception,
> // хоча насправді кидає лише IOException або SQLException
> public void myMethod() throws Exception {
>     try {
>         // ... може кинути IOException або SQLException
>     } catch (Exception e) {
>         log(e);
>         throw e; // Компілятор бачить тут лише тип Exception
>     }
> }
> ```
>
> **Рішення з Java 7 (Improved Type Analysis):**
>
> Компілятор став "розумнішим". Він аналізує `try` блок і **точно знає**, які саме типи `checked` винятків можуть бути кинуті.
>
> Якщо `catch(Exception e)` ловить ці винятки і просто прокидає їх далі (`throw e`), компілятор розуміє, що `e` насправді може бути лише одним із тих типів, що кидаються в `try`.
>
> ```java
> // З Java 7 цей метод може оголошувати конкретні типи
> public void myMethod() throws IOException, SQLException {
>     try {
>         // ...
>     } catch (Exception e) {
>         // Компілятор знає, що 'e' може бути лише IOException або SQLException
>         throw e;
>     }
> }
> ```
>
> **Зв'язок з `multi-catch`:**
> `multi-catch` — це інша сторона тієї ж медалі. `multi-catch` дозволяє об'єднати кілька `catch` блоків в один, а "rethrowing з аналізом типів" дозволяє одному `catch(Exception e)` поводитися так, ніби він є `multi-catch` для всіх можливих винятків з `try` блоку. Обидві фічі базуються на покращеному аналізі типів винятків з боку компілятора.

#### **📌 Що перевіряють:**

*Експертне знання сучасних можливостей Java. Розуміння того, як компілятор став "розумнішим" у роботі з винятками.*
