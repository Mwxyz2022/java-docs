---
layout: default
title: "Multiple catch blocks (кілька catch-блоків)"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 11
---

# Multiple catch blocks (кілька catch-блоків)

* короткий опис: Механізм, що дозволяє розміщувати кілька `catch`-блоків після одного `try`, кожен з яких обробляє винятки певного типу.

* поняття: Кілька `catch`-блоків, порядок `catch`-блоків, `unreachable code`.

* приклади: `catch (FileNotFoundException e) { ... } catch (IOException e) { ... } catch (Exception e) { ... }`.

* ключові моменти: `catch`-блоки перевіряються зверху вниз. Важливо розташовувати їх від найспецифічнішого до найзагальнішого, щоб уникнути помилок компіляції (`unreachable catch block`).

* що важливо знати на співбесіді: Правила порядку `catch`-блоків, помилка `unreachable catch block`.

### **✅ Офіційне визначення:**

**Multiple catch blocks** — це механізм у Java, що дозволяє обробляти різні типи винятків окремими блоками **`catch`** після одного **`try`**.

### **🧠 Простими словами:**

Коли в **`try`** може виникнути кілька різних винятків, ми можемо перехопити кожен із них окремим **`catch`**\-блоком і прописати власну логіку для кожного типу помилки.

---

## **📊 Типові шаблони multiple catch:**

* **`try`** \+ кілька **`catch`** для різних типів винятків.
* Найбільш специфічні винятки — зверху.
* Найбільш загальні (**`Exception`**, **`Throwable`**) — внизу.

**Синтаксис:**

```java
try {
    // код, що може кидати винятки
} catch (IOException e) {
    System.out.println("Файл не знайдено або не читається");
} catch (NumberFormatException e) {
    System.out.println("Некоректне число");
} catch (Exception e) {
    System.out.println("Інша помилка");
}
```

📌 Java перевіряє **`catch`**\-блоки зверху вниз. Коли знаходить відповідний — інші ігноруються.

---

## **Пояснення на пальцях (💡)**

Уяви, що ти ловиш м’ячі різного розміру: маленький, середній і великий. Якщо спочатку стоїть гігантська сітка (catch Exception), вона впіймає все, і дрібніші сітки нижче вже не мають значення — Java так і скаже: *“Цей блок недосяжний”*

---

## **Корисні приклади (🧪)**

```java
import java.io.*;

public class MultiCatchExample {
    public static void main(String[] args) {
        try {
            BufferedReader reader = new BufferedReader(new FileReader("data.txt"));
            int number = Integer.parseInt(reader.readLine());
            reader.close();
        } catch (FileNotFoundException e) {
            System.out.println("Файл не знайдено");
        } catch (IOException e) {
            System.out.println("Помилка при читанні файлу");
        } catch (NumberFormatException e) {
            System.out.println("Некоректне число у файлі");
        } catch (Exception e) {
            System.out.println("Щось пішло не так");
        }
    }
}
```

---

## **Порівняння (⚖️)**

| Варіант | Опис | Коментар |
| :---: | :---: | :---: |
| Один **`catch`** з **`Exception`** | Обробляє все | Низька деталізація |
| Кілька **`catch`** | Кожен тип — окрема логіка | Гнучкіше, читабельніше |
| Порушений порядок | Загальний виняток перед конкретним | Помилка компіляції |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Від вузького до широкого — як воронка"**

**`catch`** спочатку ловить "рибку по імені", потім "усіх підряд"

---

## **🟩 Підсумок:**

* Multiple catch blocks дозволяють обробляти кожен виняток окремо з відповідною логікою.
* Порядок catch-блоків має значення: спочатку конкретні, потім загальні.
* Неправильний порядок призведе до помилки компіляції (**`Unreachable catch block`**).
* Покращує читабельність і контроль над обробкою помилок.

---

### **🔗 Додаткові джерела:**

* [**Java Documentation: try-catch**](https://docs.oracle.com/javase/tutorial/essential/exceptions/catch.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке кілька `catch`-блоків (multiple catch blocks) і яка їхня основна мета?

> #### **💬 Відповідь:**
>
> **Multiple catch blocks** — це можливість у Java мати кілька `catch` блоків після одного `try` блоку. Кожен з цих `catch` блоків призначений для перехоплення та обробки **різного типу винятку**.
>
> **Основна мета:**
>
> Дозволити програмі **по-різному реагувати на різні типи помилок**. Це забезпечує більш **точну та специфічну обробку** виняткових ситуацій.
>
> Замість того, щоб ловити загальний `Exception` і потім аналізувати його тип за допомогою `instanceof`, ви можете одразу надати окрему логіку для кожної очікуваної помилки.
>
> **Приклад:**
>
> ```java
> try {
>     // ... робота з файлами та мережею ...
> } catch (FileNotFoundException e) {
>     // Логіка для випадку, коли файл не знайдено
>     System.out.println("Файл не існує. Спробуйте інший шлях.");
> } catch (SocketTimeoutException e) {
>     // Логіка для випадку, коли закінчився час очікування мережі
>     System.out.println("Сервер не відповідає. Повторіть спробу пізніше.");
> }
> ```
>
> Це робить код більш читабельним та підтримуваним.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `multiple catch` як інструменту для диференційованої обробки помилок.*

* * *

### **2 ❓ Питання:**

Яке найважливіше правило щодо порядку `catch`-блоків? Що станеться, якщо його порушити?

> #### **💬 Відповідь:**
>
> **Найважливіше правило:** `catch` блоки повинні бути впорядковані **від більш специфічного (дочірнього) типу винятку до більш загального (батьківського)**.
>
> **Чому так:**
> JVM перевіряє `catch` блоки послідовно, зверху вниз. Вона виконає **перший `catch` блок**, тип якого є сумісним з кинутим винятком (є тим самим класом або його суперкласом).
>
> **Що станеться, якщо порушити порядок:**
>
> Це призведе до **помилки компіляції**. Компілятор виявить, що більш специфічний `catch` блок є **недосяжним кодом (unreachable code)**.
>
> **Приклад помилки:**
>
> ```java
> try {
>     // ...
> } catch (IOException e) { // Загальний батько
>     // ...
> } catch (FileNotFoundException e) { // Його специфічний нащадок
>     // ПОМИЛКА КОМПІЛЯЦІЇ: Exception 'FileNotFoundException'
>     // has already been caught by the alternative IOException.
> }
> ```
>
> Оскільки `FileNotFoundException` є підкласом `IOException`, він завжди буде перехоплений першим блоком `catch (IOException e)`, і другий блок ніколи не виконається.

#### **📝 Аналогія:**

Це як ловити рибу сітками. Ви спочатку ставите сітку з малими отворами (для конкретних винятків), а вже за нею — сітку з великими (для загальних). Якщо зробити навпаки, велика сітка зловить всю рибу, і до маленької нічого не дійде.

#### **📌 Що перевіряють:**

*Знання строгих правил компілятора та їх зв'язок з ієрархією винятків.*

* * *

### **3 ❓ Питання:**

Поясніть, як працює multi-catch блок (з Java 7). Які обмеження накладаються на параметр винятку в такому блоці?

> #### **💬 Відповідь:**
>
> **Multi-catch блок** — це можливість, додана в Java 7, яка дозволяє обробляти **кілька різних типів винятків в одному `catch` блоці**.
>
> **Синтаксис:**
> Типи винятків перераховуються через вертикальну риску (`|`).
> `catch (IOException | SQLException | ParseException e)`
>
> **Призначення:**
> Це значно **зменшує дублювання коду**, якщо логіка обробки для кількох типів винятків є ідентичною.
>
> **Обмеження на параметр `e`:**
>
> У `multi-catch` блоці параметр винятку (`e`) є неявно **`final`**.
>
> * Це означає, що **ви не можете переприсвоїти** йому нове значення всередині `catch` блоку. `e = new Exception();` призведе до помилки компіляції.
> * Це зроблено для забезпечення типобезпечності. Тип змінної `e` є **спільним супертипом** усіх перерахованих винятків (наприклад, `Exception` для `IOException | SQLException`), і компілятор не дозволяє присвоювати їй значення, яке може не відповідати всім можливим типам, що перехоплюються.

#### **📌 Що перевіряють:**

*Знання сучасних (з Java 7) можливостей обробки винятків та їхніх нюансів.*

* * *

### **4 ❓ Питання:**

Що виведе цей код і чому?

```java
try {
    throw new ArrayIndexOutOfBoundsException();
} catch (IndexOutOfBoundsException e) {
    System.out.println("A");
} catch (RuntimeException e) {
    System.out.println("B");
} catch (Exception e) {
    System.out.println("C");
}
```

> #### **💬 Відповідь:**
>
> Цей код виведе **`A`**.
>
> **Пояснення:**
>
> 1. `try` блок кидає виняток `ArrayIndexOutOfBoundsException`.
> 2. JVM починає шукати відповідний `catch` блок зверху вниз.
> 3. **Перший `catch` блок:** `catch (IndexOutOfBoundsException e)`
>     * Клас `ArrayIndexOutOfBoundsException` є **підкласом** `IndexOutOfBoundsException`.
>     * Тому цей блок **є сумісним** і буде виконаний.
>     * Виводиться "A".
> 4. **Процес пошуку зупиняється.** Оскільки відповідний обробник знайдено, інші `catch` блоки (`RuntimeException`, `Exception`) будуть **проігноровані**.
>
> Хоча `ArrayIndexOutOfBoundsException` також є і `RuntimeException`, і `Exception`, буде виконано **перший** відповідний блок у ланцюжку.

#### **📌 Що перевіряють:**

*Розуміння того, як поліморфізм працює в `catch` блоках і як виконується лише перший відповідний блок.*

* * *

### **5 ❓ Питання:**

Напишіть код з `try-catch`, де один блок `catch` обробляє `FileNotFoundException`, а інший — будь-який інший `IOException`.

> #### **💬 Відповідь:**
>
> Для цього потрібно використати правило "від специфічного до загального", розмістивши `catch` для `FileNotFoundException` (який є підкласом `IOException`) **перед** `catch` для `IOException`.
>
> ```java
> import java.io.FileNotFoundException;
> import java.io.FileReader;
> import java.io.IOException;
>
> public class FileProcessor {
>
>     public void processFile(String path) {
>         try (FileReader reader = new FileReader(path)) {
>             // ... логіка читання ...
>             System.out.println("Файл успішно оброблено.");
>         } catch (FileNotFoundException e) {
>             // Специфічна обробка: файл не знайдено
>             System.err.println("Помилка: файл не знайдено за шляхом: " + path);
>         } catch (IOException e) {
>             // Загальна обробка для всіх інших помилок вводу/виводу
>             // Наприклад, помилка при читанні або закритті
>             System.err.println("Сталася помилка вводу/виводу: " + e.getMessage());
>         }
>     }
> }
> ```
>
> Це дозволяє реалізувати різну логіку для різних типів помилок, пов'язаних з однією операцією.

#### **📌 Що перевіряють:**

*Практичні навички написання коду з диференційованою обробкою помилок.*

* * *

### **6 ❓ Питання:**

Що краще: один `catch(Exception e)` чи кілька специфічних `catch` блоків? ⭐️

> #### **💬 Відповідь:**
>
> **Кілька специфічних `catch` блоків майже завжди є кращим вибором.**
>
> **Переваги кількох `catch` блоків:**
>
> 1. **Точна обробка помилок:** Ви можете написати різну логіку відновлення для різних проблем. Наприклад, реакція на `FileNotFoundException` (можна запропонувати користувачеві вказати інший шлях) відрізняється від реакції на `SocketTimeoutException` (можна запропонувати повторити спробу).
>
> 2. **Читабельність та ясність наміру:** Код стає самодокументованим. Відразу зрозуміло, які саме типи помилок ви очікуєте та як на них реагуєте.
>
> **Недоліки `catch(Exception e)`:**
>
> * **Приховування реальної проблеми:** Цей блок ловить **всі** `Exception`-и, включаючи несподівані `RuntimeException`'и (як `NullPointerException`). Це може замаскувати баг у вашому коді. Ви можете думати, що обробляєте помилку з файлом, а насправді "проковтнули" `NPE`.
> * **Узагальнена логіка:** Ви змушені писати одну логіку для всіх типів помилок, що рідко є коректним.
>
> `catch(Exception e)` виправданий лише на найвищому рівні програми для логування будь-яких непередбачуваних помилок перед завершенням роботи.

#### **📌 Що перевіряють:**

*Знання найкращих практик. Кандидат повинен розуміти, чому "ловити" загальний `Exception` є поганим стилем.*

* * *

### **7 ❓ Питання:**

Чи може `catch` блок кинути новий виняток? Якщо так, то що станеться з оригінальним? ⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Це є основою патерну **"загортання винятків" (exception wrapping)**.
>
> ```java
> try {
>     // ...
> } catch (SQLException e) {
>     throw new DataAccessException("Помилка БД", e);
> }
> ```
>
> **Що станеться з оригінальним винятком:**
>
> * Коли `catch` блок кидає новий виняток, оригінальний виняток (`SQLException e` у прикладі) **не прокидається далі** по стеку викликів.
> * Новий виняток (`DataAccessException`) займає його місце.
> * **Однак, інформація про оригінальний виняток не втрачається**, якщо ви передаєте його як **`cause`** (причину) в конструктор нового винятку. Це дозволяє при аналізі стек-трейсу бачити весь ланцюжок помилок.
>
> Якщо ж ви просто зробите `throw new MyException();` без передачі `e`, то інформація про першопричину буде втрачена.

#### **📌 Що перевіряють:**

*Розуміння патерну `exception wrapping` та механізму `cause`.*

* * *

### **8 ❓ Питання:**

Яким буде тип змінної `e` у `multi-catch` блоці, наприклад, `catch(IOException | SQLException e)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тип змінної `e` буде **найближчим спільним супертипом (common supertype)** усіх винятків, перерахованих у `catch`.
>
> **У випадку `catch(IOException | SQLException e)`:**
>
> 1. `IOException` успадковується від `Exception`.
> 2. `SQLException` також успадковується від `Exception`.
>
> 3. Найближчим спільним супертипом для них є `Exception`. Тому змінна `e` матиме тип **`Exception`**.
>
> Це означає, що всередині цього блоку ви можете викликати лише ті методи, які є в класі `Exception` (наприклад, `getMessage()`). Ви не можете викликати методи, специфічні для `IOException` або `SQLException`, без явного приведення типів (яке, у свою чергу, вимагало б `instanceof`).
>
> **Крім того, така змінна `e` є неявно `final`.**

#### **📌 Що перевіряють:**

*Глибокі знання про роботу `multi-catch` та правил виведення типів компілятором.*

* * *

### **9 ❓ Питання:**

Що станеться, якщо у вас є вкладений `try-catch` і виняток виникає у внутрішньому `try`, але внутрішній `catch` його не ловить? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У цьому випадку виняток буде **"спливати" назовні** і шукати обробник у **зовнішньому `catch` блоці**.
>
> **Приклад:**
>
> ```java
> try {
>     System.out.println("Зовнішній try");
>     try {
>         System.out.println("Внутрішній try");
>         throw new ArithmeticException();
>     } catch (IOException e) { // Цей блок не підходить
>         System.out.println("Внутрішній catch");
>     }
> } catch (Exception e) { // Цей блок підходить
>     System.out.println("Зовнішній catch");
> }
> ```
>
> **Порядок виконання:**
>
> 1. Виконується зовнішній `try`. Виводиться "Зовнішній try".
> 2. Виконується внутрішній `try`. Виводиться "Внутрішній try".
> 3. Кидається `ArithmeticException`.
> 4. JVM перевіряє внутрішній `catch` блок. Він ловить `IOException`, тому не підходить.
> 5. Внутрішня `try-catch` конструкція завершується, прокинувши виняток назовні.
> 6. Тепер JVM перевіряє `catch` блоки для зовнішнього `try`.
> 7. Зовнішній `catch (Exception e)` є сумісним, тому він виконується. Виводиться "Зовнішній catch".
>
> Це демонструє, як винятки "подорожують" вгору по стеку в пошуках відповідного обробника.

#### **📌 Що перевіряють:**

*Розуміння того, як працює розповсюдження винятків у вкладених конструкціях.*

* * *

### **10 ❓ Питання:**

Розгляньте такий код. Чи скомпілюється він? Чому?
`try {} catch (Exception e) {} catch (RuntimeException e) {}`
⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не скомпілюється.**
>
> Це є **порушенням правила порядку `catch`-блоків**.
>
> **Пояснення:**
>
> 1. Клас `RuntimeException` є **нащадком** класу `Exception`.
> 2. Перший `catch(Exception e)` є **більш загальним** і буде перехоплювати всі нащадки `Exception`, включаючи `RuntimeException`.
> 3. Тому другий блок, `catch(RuntimeException e)`, є **недосяжним кодом (unreachable code)**. Він ніколи не зможе виконатися.
> 4. Компілятор Java виявить цю логічну помилку і видасть помилку компіляції: `Exception 'java.lang.RuntimeException' has already been caught`.
>
> Щоб виправити це, потрібно поміняти блоки місцями: спочатку ловити `RuntimeException`, а потім — `Exception`.

#### **📌 Що перевіряють:**

*Повторна перевірка на знання правил порядку `catch`-блоків. Це питання-пастка.*

* * *

### **11 ❓ Питання:**

Навіщо в Java взагалі існують `checked` винятки, якщо багато сучасних мов та фреймворків від них відмовляються? Який був первісний задум? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Первісний задум творців Java був у тому, щоб створити **більш надійні програми**.
>
> **Філософія `checked` винятків:**
>
> * Існують певні типи помилок, які **не є багами програміста**, а є **очікуваними ризиками** при роботі із зовнішнім світом (файлова система, мережа, БД). Наприклад, файл може бути відсутнім, а мережеве з'єднання — розірватися.
> * Компілятор, вимагаючи обробку `checked` винятків, **змушує розробника замислитися** над цими ризиками і написати код для їх обробки.
> * Це робить **API самодокументованим**: сигнатура методу `throws IOException` чітко говорить клієнту, яка помилка може статися.
>
> На папері це виглядало як чудова ідея для підвищення надійності.
>
> **Чому від них відмовляються:**
>
> * На практиці це призвело до "checked exception hell", багатослівності та "ковтання" винятків, що часто приносило більше шкоди, ніж користі.
> * Сучасна філософія полягає в тому, щоб надати розробнику більше свободи, покладаючись на хороші практики, документацію та тестування, а не на жорсткі обмеження компілятора.
>
> Хоча `checked` винятки залишаються суперечливою темою, розуміння їхнього початкового призначення є важливим.

#### **📌 Що перевіряють:**

*Історичний та архітектурний контекст. Кандидат повинен розуміти "чому" ця фіча була створена, а не тільки "як" вона працює.*

* * *

### **12 ❓ Питання:**

Який виняток буде в кінцевому підсумку кинуто з цього методу?

```java
public static void test() {
    try {
        throw new IOException("IO");
    } catch (IOException e) {
        throw new RuntimeException("Runtime", e);
    } finally {
        // немає return або throw
    }
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Буде кинуто **`RuntimeException`**.
>
> **Послідовність виконання:**
>
> 1. `try` блок кидає `IOException`.
> 2. Керування переходить до `catch (IOException e)` блоку.
> 3. `catch` блок, у свою чергу, кидає новий виняток — `RuntimeException`, загортаючи оригінальний `IOException` як його причину (`cause`).
> 4. Потік виконання переривається, і програма готується прокинути `RuntimeException` далі.
> 5. Перед цим виконується блок `finally`.
> 6. Оскільки `finally` **не містить** ані `return`, ані власного `throw`, він просто завершує свою роботу.
> 7. JVM продовжує прокидати виняток, який був "заготовлений" раніше — тобто, **`RuntimeException`** з `catch` блоку.

#### **📌 Що перевіряють:**

*Уміння прослідкувати складний потік керування з перетворенням винятків.*

* * *

### **13 ❓ Питання:**

Чи можна зловити `Error`? Якщо так, чи варто це робити? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, технічно `Error` можна зловити**, використовуючи `catch(Error e)` або `catch(Throwable t)`.
>
> **Чи варто це робити?**
> **Ні, практично ніколи.** Це вважається дуже поганою практикою.
>
> **Чому:**
>
> Класи, що успадковують `Error`, сигналізують про **фатальні, невідворотні збої** в роботі JVM. Це не програмні помилки, які можна виправити.
>
> * `OutOfMemoryError`: Пам'ять закінчилася. Ви не можете надійно виділити нову пам'ять для обробки.
> * `StackOverflowError`: Стек переповнено. Ви не можете надійно викликати нові методи для обробки.
>
> Спроба "зловити" `Error` і продовжити роботу схожа на спробу ігнорувати той факт, що двигун літака відвалився. Програма знаходиться в критично нестабільному стані.
>
> Єдиний можливий сценарій — це мати `catch (Throwable t)` на найвищому рівні, щоб зробити останню спробу щось залогувати перед неминучим завершенням програми.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, які проблеми є фатальними для JVM і чому їхня обробка не має сенсу.*

* * *

### **14 ❓ Питання:**

Що буде, якщо один з типів у `multi-catch` блоці є нащадком іншого? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **помилки компіляції**.
>
> **Правило:**
> В `multi-catch` блоці (`catch (ExceptionA | ExceptionB e)`) жоден з типів винятків **не може бути підтипом (нащадком) іншого**.
>
> **Приклад:**
>
> ```java
> try {
>     // ...
> } catch (IOException | FileNotFoundException e) {
>     // ПОМИЛКА КОМПІЛЯЦІЇ
> }
> ```
>
> **Пояснення:**
>
> * `FileNotFoundException` є підкласом `IOException`.
> * Це означає, що частина `IOException` у `catch` блоці **вже перехоплює** всі `FileNotFoundException`.
> * Тому, додавання `FileNotFoundException` до цього ж блоку є **надлишковим**.
> * Компілятор виявляє цю надлишковість і повідомляє про помилку: `The exception FileNotFoundException is already caught by the alternative IOException`.
>
> Це правило допомагає уникнути плутанини і змушує писати більш чистий код, де достатньо вказати лише батьківський тип.

#### **📌 Що перевіряють:**

*Дуже точне знання правил компілятора для `multi-catch` та їх зв'язок з ієрархією винятків.*

* * *

### **15 ❓ Питання:**

Ви працюєте з методом, що кидає `checked` виняток `IOException`, але в вашому контексті ця помилка є фатальною, і програма не може від неї оговтатися. Який найкращий спосіб обробити цей виняток? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це поширена ситуація, коли `checked` виняток сигналізує про помилку, яка за бізнес-логікою є невідворотною (наприклад, неможливість прочитати основний конфігураційний файл при старті програми).
>
> Найкращий спосіб обробити таку ситуацію — це **"загорнути" `checked` виняток у відповідний `unchecked` виняток і кинути його далі.**
>
> **Чому це краще, ніж `throws IOException`:**
>
> * Це дозволяє не "забруднювати" весь стек викликів оголошенням `throws IOException`. Ви сигналізуєте, що ця помилка є фатальною для потоку виконання, а не просто очікуваною проблемою вводу/виводу.
>
> **Який `unchecked` виняток обрати:**
>
> * **`IllegalStateException`**: Якщо помилка виникає через те, що система знаходиться в некоректному стані (наприклад, очікувала на файл, а його немає).
> * **Кастомний `RuntimeException`**: Найкращий варіант. Ви можете створити власний виняток, наприклад `FatalConfigurationException extends RuntimeException`, який буде більш семантично точним.
>
> **Приклад:**
>
> ```java
> public class ConfigLoader {
>     public Config load() {
>         try (InputStream in = ...) {
>             // ... читання файлу ...
>             return new Config();
>         } catch (IOException e) {
>             // Загортаємо, бо без конфігурації додаток працювати не може
>             throw new IllegalStateException("Не вдалося завантажити конфігураційний файл", e);
>         }
>     }
> }
> ```
>
> Це дозволить програмі "впасти швидко" (fail-fast), а на найвищому рівні можна буде зловити цей `IllegalStateException`, залогувати його і коректно завершити роботу.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен вміти приймати обґрунтоване рішення, коли порушити парадигму `checked` винятків і перетворити їх на `unchecked` для кращого дизайну.*
