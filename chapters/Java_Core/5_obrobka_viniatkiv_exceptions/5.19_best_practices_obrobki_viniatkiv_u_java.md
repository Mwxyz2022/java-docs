---
layout: default
title: "Best practices обробки винятків у Java"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 19
---

# Best practices обробки винятків у Java

* короткий опис: Рекомендовані підходи для написання надійного, чистого та зрозумілого коду обробки помилок, що включають правильне логування, використання специфічних винятків, управління ресурсами та уникнення "ковтання" помилок.

* поняття: "Ковтання" винятків (порожній `catch`), логування, конкретні винятки, `try-with-resources`, `printStackTrace()`, `getCause()`.

* приклади: Завжди логувати винятки, використовувати `try-with-resources` для автоматичного закриття, не залишати порожні `catch`-блоки.

* ключові моменти: Не ковтай помилки, логуй, створюй конкретні винятки, завжди використовуй `try-with-resources`.

* що важливо знати на співбесіді: Правила "хорошого тону" в обробці винятків.

### **✅ Офіційне визначення:**

**Best practices** — це рекомендовані підходи до обробки винятків, які дозволяють писати надійний, чистий і зрозумілий код.

### **🧠 Простими словами:**

Це набір правил: що можна, що не можна, що бажано робити з помилками. Щоб твоя програма не “падала”, не “мовчала” і не лякала користувача

---

## **Основні правила**

📊 **Список правил з поясненням:**

* ❌ **Не ловити Throwable або Exception**  
  Занадто загально — можна прихопити й системні помилки, які не слід ловити.

* ❌ **Не залишати `catch` порожнім**  
  Повідомлення про помилку зникне, ти ніколи не дізнаєшся, що сталося.s

* ✅ **Завжди логувати винятки**  
  Щоб можна було легко розслідувати причину збою.

* ✅ **Кидати максимально конкретні винятки**  
  Це підвищує читаємість коду і полегшує налагодження.

* ✅ **Додавати опис у виняток**  
  Робить помилку зрозумілішою для людини (і майбутнього тебе).

* ✅ **Створювати свої винятки, коли потрібно**  
  Для розмежування логіки домену/бізнесу від технічних винятків.

* ✅ **Повторно використовувати `cause`**  
  Щоб зберегти першопричину помилки.

* ✅ **Завжди закривати ресурси (try-with-resources)**  
  Для запобігання витокам памʼяті, файлових дескрипторів, потоків.

---

## **Пояснення на пальцях (💡)**

🔻 **Так робити не треба:**

```java
try {
    // небезпечний код
} catch (Exception e) {
    // нічого не роблю
}
```

🙅‍♂️ -> Виняток проковтнуто — проблема залишилась невидимою.

✅ **Так правильно:**

```java
catch (IOException e) {
    logger.error("Не вдалося прочитати файл: " + e.getMessage(), e);
}
```

👍 -> Є лог, є контекст, є шанс вирішити.

---

## **Корисні приклади (🧪)**

* ❌ **Поганий catch:**

```java
catch (Exception e) { } // все ігнорується
```

* ✅ **Хороший catch з обгорткою:**

```java
catch (SQLException e) {
    throw new DataAccessException("Помилка при збереженні в БД", e);
}
```

* ✅ **Приклад конкретного винятку:**

```java
if (password.length() < 6) {
    throw new PasswordTooShortException("Пароль повинен мати щонайменше 6 символів");
}
```

---

## **Порівняння (⚖️)**

| Поганий підхід | Хороший підхід |
| :---: | :---: |
| **`catch (Exception e) {}`** | **`catch (IOException e) { log.error(...); }`** |
| **`throw new Exception("помилка")`** | **`throw new UserNotFoundException("ID: 123")`** |
| **`throw new SomeException()`** | **`throw new SomeException("Причина", cause)`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

Не лови все -> не ковтай нічого -> завжди логуй -> кидай по суті

**Try -> Catch -> Log -> Explain -> Throw**

---

## **🟩 Підсумок:**

* Не лови **`Throwable`** чи **`Exception`** без потреби
* Не залишай порожні **`catch`**\-блоки
* Логуй винятки: повідомлення \+ stacktrace
* Використовуй специфічні винятки
* Створюй власні типи винятків, коли помилка має бізнес-сенс
* Завжди додавай **`cause`**, якщо обгортаєш іншу помилку
* Закривай ресурси через **`try-with-resources`**

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Docs — Exceptions**](https://docs.oracle.com/javase/tutorial/essential/exceptions/index.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які три найважливіші "золоті правила" обробки винятків ви можете назвати?

> #### **💬 Відповідь:**
>
> 1. **Не "ковтайте" винятки (Don't Swallow Exceptions):**
>     * **Що це:** Ніколи не залишайте `catch` блок порожнім або лише з коментарем `//TODO`.
>     * **Чому:** Це приховує проблему. Програма продовжить працювати в непередбачуваному стані, а ви ніколи не дізнаєтеся про помилку, доки не станеться щось гірше. Мінімум, що потрібно зробити — залогувати виняток.
>
> 2. **Використовуйте `try-with-resources` для керування ресурсами:**
>     * **Що це:** Завжди використовуйте конструкцію `try-with-resources` для об'єктів, що реалізують `AutoCloseable` (потоки, з'єднання, сокети).
>     * **Чому:** Це найнадійніший спосіб гарантувати, що ресурси будуть закриті, навіть якщо виникнуть винятки, і це запобігає витокам ресурсів. Він коротший і безпечніший за ручне закриття в `finally`.
>
> 3. **Кидайте специфічні винятки:**
>     * **Що це:** Замість того, щоб кидати загальний `new Exception("Error")`, створюйте і кидайте специфічні кастомні винятки (`UserNotFoundException`, `InvalidInputException`).
>     * **Чому:** Це робить ваш API зрозумілим, дозволяє клієнтському коду диференційовано обробляти помилки і значно спрощує налагодження.

#### **📌 Що перевіряють:**

*Здатність виділити ключові, найбільш критичні аспекти обробки помилок.*

* * *

### **2 ❓ Питання:**

Чому "ковтання" винятків (порожній `catch` блок) є таким небезпечним?

> #### **💬 Відповідь:**
>
> "Ковтання" винятків — це один з найсерйозніших антипатернів, який може призвести до катастрофічних наслідків.
>
> **Чому це небезпечно:**
>
> 1. **Приховування помилок:** Якщо в коді виникає проблема, вона залишається повністю непоміченою. Немає жодного сліду про те, що щось пішло не так.
>
> 2. **Непередбачуваний стан програми:** Програма продовжує виконуватися, але її стан може бути пошкодженим або непослідовним. Це може призвести до:
>     * **Тихого псування даних:** Наприклад, дані не будуть збережені в базу даних, але програма буде вважати, що все добре.
>     * **Неправильних розрахунків:** Логіка може працювати з `null` або некоректними значеннями.
>
> 3. **Неможливість налагодження:** Знайти причину дивної поведінки стає майже неможливо, бо відсутні будь-які логи чи стек-трейси, які б вказали на джерело проблеми. Це перетворює налагодження на "пошук голки в копиці сіна".
>
> **Прийнятний випадок (дуже рідкісний):**
> Єдиний випадок, коли це може бути прийнятним — це при закритті ресурсу у `finally` блоці, де ви свідомо вирішили проігнорувати `IOException`, бо нічого не можете з ним зробити, і оригінальний виняток є важливішим. Але навіть тоді краще залогувати цю помилку.

#### **📌 Що перевіряють:**

*Розуміння довгострокових наслідків "лінивої" обробки помилок. Це перевірка на професійну зрілість.*

* * *

### **3 ❓ Питання:**

Чому `e.printStackTrace()` не слід використовувати в продакшн-коді?

> #### **💬 Відповідь:**
>
> `e.printStackTrace()` — це інструмент для **швидкого налагодження під час розробки**, але в продакшн-середовищі його використання є поганою практикою з кількох причин:
>
> 1. **Пише у `System.err`, а не в лог-файли:**
>     * Його вивід йде у стандартний потік помилок, що зазвичай є консоллю сервера. Цей вивід **не потрапляє** у стандартні лог-файли, керовані вашим лог-фреймворком (Log4j, Logback). Ви просто втрачаєте інформацію про помилку в централізованих логах.
>
> 2. **Відсутність гнучкості та форматування:**
>     * Ви не можете контролювати формат виводу, додати контекст (ID користувача, час) або встановити рівень логування (ERROR, WARN).
>
> 3. **Проблеми з багатопотоковістю:**
>     * У багатопотоковому середовищі вивід з `System.out` та `System.err` може хаотично перемішуватися. Стек-трейси з різних потоків будуть накладатися один на одного, і розібрати їх буде неможливо.
>
> **Як правильно:**
>
> Завжди використовуйте спеціалізований **логгер** (наприклад, SLF4J), який надає повний контроль над форматом, рівнем та місцем призначення логів.
>
> **Правильно:** `log.error("Сталася помилка при обробці замовлення {}", orderId, e);`
>
> Тут `e` передається як останній аргумент, і логгер сам коректно відформатує повний стек-трейс.

#### **📌 Що перевіряють:**

*Знання найкращих практик логування та розуміння обмежень `printStackTrace()`.*

* * *

### **4 ❓ Питання:**

Коли доцільно прокидати виняток (`throws`), а коли — обробляти його (`try-catch`)?

> #### **💬 Відповідь:**
>
> Це архітектурне рішення, яке залежить від рівня абстракції та відповідальності вашого коду.
>
> **Коли використовувати `throws` (делегувати відповідальність):**
>
> * **Якщо ваш метод не має достатньо контексту для обробки помилки.** Низькорівневий метод, що читає з мережі, не знає, чи повинен додаток спробувати ще раз, чи повідомити користувача. Це рішення має приймати код вищого рівня.
> * **При розробці бібліотеки або API.** Ви повинні надати клієнтам можливість самим вирішувати, як реагувати на помилки, а не нав'язувати їм свою логіку.
>
> **Коли використовувати `try-catch` (обробляти на місці):**
>
> * **Якщо ви можете коректно відновитися після помилки.** Наприклад, зловити `SocketTimeoutException` і повторити запит.
> * **На межі вашого API або на найвищому рівні програми.** Наприклад, у `Controller`'і ви повинні зловити всі винятки і перетворити їх на відповідний HTTP-статус та відповідь. `main`-метод повинен ловити помилки, щоб залогувати їх і коректно завершити роботу.
> * **Щоб "загорнути" виняток** в інший, більш специфічний для вашого домену.

#### **📌 Що перевіряють:**

*Здатність проектувати стратегію обробки помилок для багатошарових додатків.*

* * *

### **5 ❓ Питання:**

Чому краще ловити специфічні винятки (`FileNotFoundException`), а не загальний `Exception`?

> #### **💬 Відповідь:**
>
> Ловити загальний `Exception` — це погана практика, яка може приховати неочікувані помилки.
>
> **Проблеми з `catch (Exception e)`:**
>
> 1. **Ловить занадто багато:** Цей блок перехопить **усі `checked` і всі `unchecked`** винятки. Це означає, що ви можете випадково зловити **баг** у вашому коді (наприклад, `NullPointerException`), думаючи, що ви обробляєте помилку вводу/виводу.
>
> 2. **Неправильна обробка:** Ваша логіка обробки, написана для `FileNotFoundException`, ймовірно, буде некоректною для `NullPointerException`.
>
> 3. **Порушення читабельності:** Код стає менш зрозумілим, бо неясно, які саме помилки ви очікуєте обробити.
>
> **Переваги специфічних `catch`:**
>
> * **Точна обробка:** Ви пишете код, призначений для вирішення конкретної проблеми.
> * **Надійність:** Ви не ризикуєте "проковтнути" непередбачуваний `RuntimeException`.
> * **Чистота коду:** Ваш код чітко документує, які саме помилки він очікує і вміє обробляти.
>
> `catch (Exception e)` виправданий лише на найвищому рівні системи для "останнього рубежу" логування непередбачених помилок.

#### **📌 Що перевіряють:**

*Розуміння того, як узагальнена обробка помилок може маскувати реальні проблеми в коді.*

* * *

### **6 ❓ Питання:**

Поясніть патерн "загортання винятків" (exception wrapping) і навіщо передавати оригінальний виняток як `cause`. ⭐️

> #### **💬 Відповідь:**
>
> **Загортання винятків** — це патерн, коли ви ловите низькорівневий виняток і кидаєте натомість свій власний, високорівневий, **зберігаючи оригінальний виняток як його причину (`cause`)**.
>
> **Навіщо це потрібно:**
>
> Це робиться для **абстрагування від деталей реалізації**. Наприклад, ваш сервісний шар не повинен знати, що шар даних використовує JDBC і кидає `SQLException`. Замість цього, шар даних "загортає" `SQLException` у ваш кастомний `DataAccessException`.
>
> **Чому важливо передавати `cause`:**
>
> Якщо не передати оригінальний виняток, ви **втратите повний стек-трейс** і, як наслідок, інформацію про першопричину проблеми.
>
> `throw new DataAccessException("Помилка...", e);`
>
> Завдяки передачі `e` як `cause`, при виведенні стек-трейсу ви побачите **ланцюжок винятків** з секціями `Caused by: ...`. Це дозволяє відстежити проблему від високорівневої бізнес-помилки до її низькорівневого технічного джерела, що є критично важливим для налагодження.

#### **📌 Що перевіряють:**

*Знання архітектурного патерну для обробки помилок у багатошарових додатках.*

* * *

### **7 ❓ Питання:**

Які переваги надає `try-with-resources` у порівнянні з `try-catch-finally`?

> #### **💬 Відповідь:**
>
> `try-with-resources` (з Java 7) є абсолютно кращим підходом для роботи з ресурсами.
>
> **Основні переваги:**
>
> 1. **Автоматичне закриття ресурсів:** Він гарантує, що метод `close()` буде викликано для ресурсу, усуваючи ризик людської помилки та витоків ресурсів.
>
> 2. **Код значно коротший і чистіший:** Немає потреби писати громіздкий та схильний до помилок `finally` блок.
>
> 3. **Коректна обробка "придушених винятків":** Це ключова перевага. Якщо виняток виникає і в `try`, і при закритті ресурсу, `try-finally` втратить інформацію про першу помилку. `try-with-resources` збереже її, додавши помилку з `close()` як "придушену" до основної. Це робить діагностику набагато надійнішою.

#### **📌 Що перевіряють:**

*Знання сучасних найкращих практик мови та їхніх переваг.*

* * *

### **8 ❓ Питання:**

Що робити, якщо метод, який ви викликаєте, кидає `checked` виняток, але у вашому контексті ця помилка є невідворотною і вважається багом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це поширений випадок, коли контракт низькорівневого API (з `checked` винятком) не відповідає семантиці вашого бізнес-процесу.
>
> Найкраща практика в такій ситуації — **загорнути `checked` виняток у відповідний `unchecked` виняток** (зазвичай, `RuntimeException` або його кастомний підклас).
>
> **Приклад:**
> Ваша програма при старті повинна прочитати критично важливий конфігураційний файл. Метод `Files.readString()` кидає `IOException` (`checked`). Але якщо цього файлу немає, ваш додаток не може працювати взагалі. Це фатальна помилка конфігурації.
>
> ```java
> public class ConfigLoader {
>     public String loadConfig() {
>         try {
>             return Files.readString(Path.of("config.json"));
>         } catch (IOException e) {
>             // Ми не можемо відновитися. Це фатальна помилка.
>             // "Загортаємо" її в unchecked виняток.
>             throw new IllegalStateException("Не вдалося завантажити критичний конфігураційний файл", e);
>         }
>     }
> }
> ```
>
> Це дозволяє не "забруднювати" сигнатури методів вищого рівня `throws IOException`, оскільки вони нічого не зможуть з цим зробити. Програма просто "впаде" швидко (fail-fast), що є правильною поведінкою в цьому випадку.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен вміти приймати обґрунтоване рішення, коли порушити парадигму `checked` винятків для кращого дизайну.*

* * *

### **9 ❓ Питання:**

Коли варто кидати `IllegalArgumentException` vs `IllegalStateException`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидва є стандартними `unchecked` винятками, але використовуються для сигналізації про різні типи програмних помилок.
>
> **`IllegalArgumentException`:**
>
> * **Коли:** Кидається, коли в метод було передано **некоректний або недопустимий АРГУМЕНТ**. Проблема в тому, **ЩО** передали.
> * **Приклад:** `user.setAge(-5);`. Вік не може бути від'ємним, тому аргумент невалідний.
> * **Хто винен:** Клієнтський код, який викликав метод.
>
> **`IllegalStateException`:**
>
> * **Коли:** Кидається, коли метод викликається у **непідходящий час** або коли об'єкт знаходиться в **некоректному СТАНІ** для цього виклику. Проблема не в аргументах, а в тому, **КОЛИ** метод було викликано.
> * **Приклад:** Викликати `iterator.remove()` до виклику `iterator.next()`. Або спробувати підключитися до БД, коли об'єкт `Connection` вже закрито.
>
> **Проста мнемоніка:**
>
> * `IllegalArgumentException`: "Ти дав мені погані дані".
> * `IllegalStateException`: "Ти викликав мене в поганий момент".

#### **📌 Що перевіряють:**

*Знання стандартних винятків та вміння обирати семантично правильний тип винятку для сигналізації про помилку.*

* * *

### **10 ❓ Питання:**

Чому краще уникати розміщення складної логіки в `finally` блоці? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Розміщення складної логіки в `finally` є поганою практикою з кількох причин:
>
> 1. **Ризик нових винятків:** Якщо код у `finally` сам може кинути виняток, він **"перекриє"** будь-який оригінальний виняток з `try` або `catch` блоку. Ви втратите інформацію про початкову проблему.
>
> 2. **Заплутана логіка виконання:** `finally` блок не призначений для бізнес-логіки. Його єдина мета — **гарантоване очищення ресурсів**. Розміщення там логіки робить потік виконання програми неінтуїтивним і важким для розуміння.
>
> 3. **Неможливість контролювати потік:** Інструкції `return`, `break` або `continue` всередині `finally` мають несподівану поведінку і можуть "проковтнути" винятки.
>
> `finally` блок повинен бути максимально **простим, коротким та надійним**. В ідеалі, він має містити лише виклики `close()` або схожі операції очищення. Для цього найкраще використовувати `try-with-resources`.

#### **📌 Що перевіряють:**

*Розуміння того, що кожен блок (`try`, `catch`, `finally`) має своє чітке призначення, і змішування їхніх обов'язків веде до поганого коду.*

* * *

### **11 ❓ Питання:**

Що таке **fail-fast** підхід до обробки помилок і як `RuntimeException` допомагає його реалізувати? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Fail-Fast** — це принцип проектування, згідно з яким система повинна **негайно повідомляти про помилку**, як тільки її виявлено, а не продовжувати роботу в потенційно пошкодженому стані.
>
> **Як `RuntimeException` допомагає це реалізувати:**
>
> * `RuntimeException` є основним інструментом для цього. Коли метод виявляє порушення контракту або внутрішню помилку, він кидає `RuntimeException` (наприклад, `IllegalArgumentException`, `IllegalStateException`).
> * Оскільки це `unchecked` виняток, він **негайно зупиняє поточний потік виконання** і "спливає" вгору по стеку, доки не буде перехоплений (зазвичай, глобальним обробником) або не завершить роботу потоку.
> * Це запобігає подальшому виконанню коду з некоректними даними або в неправильному стані, що могло б призвести до пошкодження даних.
>
> **Приклад:**
> Валідація параметрів на початку методу.
>
> ```java
> public void process(User user) {
>     if (user == null) {
>         throw new IllegalArgumentException("User cannot be null");
>     }
>     // ... подальша логіка ...
> }
> ```
>
> Це fail-fast: ми не намагаємося працювати з `null`, а одразу "падаємо", сигналізуючи про баг у коді, що нас викликав.

#### **📌 Що перевіряють:**

*Знання архітектурних принципів побудови надійних систем та ролі винятків у їх реалізації.*

* * *

### **12 ❓ Питання:**

Як реалізувати централізовану обробку винятків у веб-додатку (наприклад, Spring MVC)? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> У сучасних фреймворках, як Spring, найкращою практикою є створення **глобального обробника винятків**. Це дозволяє тримати контролери чистими від `try-catch` блоків.
>
> У Spring це реалізується за допомогою анотації **`@RestControllerAdvice`** або **`@ControllerAdvice`**.
>
> **Як це працює:**
>
> 1. Ви створюєте клас, позначений `@RestControllerAdvice`.
>
> 2. Всередині нього ви створюєте методи, позначені **`@ExceptionHandler`**. Кожен такий метод відповідає за обробку певного типу винятку.
>
> **Приклад:**
>
> ```java
> @RestControllerAdvice
> public class GlobalExceptionHandler {
>
>     // Цей метод зловить наш кастомний виняток і поверне 404
>     @ExceptionHandler(UserNotFoundException.class)
>     @ResponseStatus(HttpStatus.NOT_FOUND)
>     public ErrorResponse handleUserNotFound(UserNotFoundException ex) {
>         return new ErrorResponse(ex.getMessage());
>     }
>
>     // Цей метод зловить усі інші помилки і поверне 500
>     @ExceptionHandler(Exception.class)
>     @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
>     public ErrorResponse handleGenericException(Exception ex) {
>         log.error("Unhandled exception", ex);
>         return new ErrorResponse("Внутрішня помилка сервера");
>     }
> }
> ```
>
> **Переваги:**
>
> * **Централізація:** Вся логіка обробки помилок знаходиться в одному місці.
> * **Чистота контролерів:** Контролери містять лише бізнес-логіку.
> * **Послідовність:** Усі відповіді про помилки мають однаковий формат.

#### **📌 Що перевіряють:**

*Знання практик побудови REST API та інструментів, які надають сучасні фреймворки.*

* * *

### **13 ❓ Питання:**

Чому цей код може призвести до `StackOverflowError`?
`catch (Exception e) { throw e; }`
⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код **сам по собі не призведе** до `StackOverflowError`. Інструкція `throw e;` просто **повторно кидає** існуючий виняток вгору по стеку.
>
> Однак, **`StackOverflowError` може виникнути**, якщо цей `try-catch` блок знаходиться всередині **рекурсивного методу**, який не має правильної умови виходу.
>
> **Приклад, що призводить до `StackOverflowError`:**
>
> ```java
> public void recursiveMethod() {
>     try {
>         // Умова виходу ніколи не досягається
>         recursiveMethod();
>     } catch (Exception e) {
>         // Це марно. Виняток буде постійно кидатися і ловитися.
>         throw e;
>     }
> }
> ```
>
> Але тут першопричина — це **нескінченна рекурсія**, а не сам `try-catch`.
>
> **Сценарій, де сам `catch` може бути причиною:**
>
> ```java
> try {
>     // ... кинути виняток ...
> } catch (Exception e) {
>     // Спроба обробити помилку призводить до тієї ж помилки
>     someMethodThatAlsoThrowsException();
> }
> ```
>
> Якщо `someMethodThatAlsoThrowsException()` викликає сам себе через ланцюжок винятків, це також може призвести до переповнення стека.
>
> Це питання з підступом, яке перевіряє здатність аналізувати першопричину, а не лише синтаксис.

#### **📌 Що перевіряють:**

*Уважність та вміння відрізняти причину від наслідку.*

* * *

### **14 ❓ Питання:**

Поясніть концепцію `Lombok`'s `@SneakyThrows`. Як вона працює і які її ризики? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **`@SneakyThrows`** — це анотація з бібліотеки Lombok, яка дозволяє **"прокинути" `checked` виняток**, **не оголошуючи його** у `throws` клаузі методу.
>
> **Як вона працює:**
>
> Вона є хаком, який "обманює" компілятор Java. Lombok на етапі компіляції генерує байткод, який кидає `checked` виняток так, ніби він є `unchecked`. Він по суті "стирає" перевірку компілятора для цього конкретного винятку.
>
> **Приклад:**
>
> ```java
> @SneakyThrows(IOException.class)
> public void readFile() {
>     // Не потрібно писати 'throws IOException'
>     Files.readAllBytes(...);
> }
> ```
>
> **Ризики та недоліки:**
>
> 1. **Порушення контракту:** Головний недолік. Метод кидає `checked` виняток, але **не попереджає про це** у своїй сигнатурі. Це руйнує ідею `checked` винятків і робить API непередбачуваним для клієнтів, які не знають про використання `@SneakyThrows`.
>
> 2. **Ускладнення обробки:** Клієнтський код не знає, що він повинен ловити `IOException`, і не зможе цього зробити без явного `catch(Throwable t)` або `catch(Exception e)`, що є поганою практикою.
>
> **Коли це може бути (обережно) виправдано:**
>
> * Всередині лямбда-виразів або стрімів, де робота з `checked` винятками дуже незручна, і ви впевнені, що на вищому рівні є загальний обробник, який зловить цю помилку.
>
> Загалом, `@SneakyThrows` — це потужний, але **небезпечний інструмент**, який слід використовувати дуже обережно, оскільки він робить помилки неявними.

#### **📌 Що перевіряють:**

*Знання сучасних інструментів та їхніх компромісів. Розуміння того, коли "синтаксичний цукор" може бути шкідливим.*

* * *

### **15 ❓ Питання:**

Як ви ставитеся до використання `checked` винятків у новому проекті? Яку б стратегію обрали? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про архітектурний вибір, і моя стратегія була б прагматичною.
>
> **Моя стратегія:** **Мінімізувати використання `checked` винятків у внутрішній бізнес-логіці, але визнавати їхню корисність на межі із зовнішніми системами.**
>
> **1. Шар доступу до даних (Repository Layer):**
>
> * Тут я б дозволив `checked` винятки від низькорівневих API (як `SQLException` від JDBC).
> * Але на **межі** цього шару (у публічних методах репозиторію) я б **загортав** ці `checked` винятки у власні **`unchecked` доменні винятки** (наприклад, `DataAccessException`).
>
> **2. Сервісний шар (Service Layer):**
>
> * Тут я б **повністю уникав** `checked` винятків. Уся бізнес-логіка працювала б з `RuntimeException`-ами. Це робить код чистішим, легшим для тестування і не "забруднює" сигнатури.
> * Помилки валідації або бізнес-правил кидалися б як специфічні `unchecked` винятки (наприклад, `InvalidUserDataException`).
>
> **3. API/Контролерний шар (Controller Layer):**
>
> * Цей шар не повинен мати `try-catch` блоків. Замість цього я б використовував **глобальний обробник винятків (`@RestControllerAdvice`)**, який би перехоплював мої кастомні `unchecked` винятки та перетворював їх на відповідні HTTP-статуси та відповіді.
>
> **Переваги такої стратегії:**
>
> * **Всередині додатку:** ми маємо чистий, гнучкий код без "checked exception hell".
> * **На межі з зовнішнім світом:** ми не ігноруємо `checked` винятки, а коректно їх обробляємо і перетворюємо на відповідні нашій архітектурі.
>
> Цей підхід є де-факто стандартом у сучасних фреймворках, як-от Spring.

#### **📌 Що перевіряють:**

*Архітектурну зрілість. Кандидат повинен мати власну, обґрунтовану думку про те, як будувати надійну та підтримувану систему обробки помилок.*
