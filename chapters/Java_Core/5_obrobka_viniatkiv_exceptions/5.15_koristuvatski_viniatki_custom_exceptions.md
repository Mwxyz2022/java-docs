---
layout: default
title: "Користувацькі винятки (Custom exceptions)"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 15
---

# Користувацькі винятки (Custom exceptions)

* короткий опис: Власні класи помилок, створені розробником шляхом розширення `Exception` (для `Checked Exceptions`) або `RuntimeException` (для `Unchecked Exceptions`), щоб описати специфічні бізнес-ситуації.

* поняття: Custom exceptions, `extends Exception`, `extends RuntimeException`.

* приклади: `UserNotFoundException`, `InvalidPasswordException`.

* ключові моменти: Підвищують читабельність коду, дозволяють чіткіше розмежовувати бізнес-логіку помилок, спрощують логування.

* що важливо знати на співбесіді: Коли створювати, як зробити checked/unchecked.

### **✅ Офіційне визначення:**

**Користувацький виняток** — це власний клас помилки, який розробник створює сам, розширюючи **`Exception`** або **`RuntimeException`**, щоб описати специфічну ситуацію, не покриту стандартними винятками Java.

### **🧠 Простими словами:**

Java має багато вбудованих помилок (**`NullPointerException`**, **`IOException`**), але іноді треба свою — наприклад, **`InvalidPasswordException`** або **`UserNotFoundException`**. Це допомагає чітко розрізняти власні помилки від системних.

---

## **Основне**

| Тип винятку | Наслідує | Характеристика |
| :---: | :---: | :---: |
| **Checked Exception** | **`extends Exception`** | Вимагає обробки (**try-catch** або **throws**) |
| **Unchecked Exception** | **`extends RuntimeException`** | Не вимагає обробки (можна не ловити) |

---

## **Пояснення на пальцях (💡)**

Якщо у тебе є унікальна логіка (перевірка паролю, доступу), краще створити свій виняток, а не кидати загальний **`IllegalArgumentException`**.

Це:

* полегшує читання коду,
* спрощує логування,
* дозволяє точніше ловити специфічні помилки.

---

## **Корисні приклади (🧪)**

##### **Checked виняток:**

```java
public class UserNotFoundException extends Exception {
    public UserNotFoundException(String message) {
        super(message);
    }
}

public void findUser(String username) throws UserNotFoundException {
    if (!database.contains(username)) {
        throw new UserNotFoundException("Користувач " + username + " не знайдений");
    }
}
```

##### **Unchecked виняток:**

```java
public class InvalidPasswordException extends RuntimeException {
    public InvalidPasswordException(String message) {
        super(message);
    }
}

public void login(String password) {
    if (!password.equals("1234")) {
        throw new InvalidPasswordException("Неправильний пароль");
    }
}
```

##### **Клас із кількома конструкторами:**

```java
public class AppException extends Exception {
    public AppException(String message) {
        super(message);
    }

    public AppException(String message, Throwable cause) {
        super(message, cause);
    }

    public AppException(Throwable cause) {
        super(cause);
    }
}
```

---

## **Порівняння (⚖️)**

| Виняток | Потрібна обробка | Призначення |
| :---: | :---: | :---: |
| **`extends Exception`** | ✅ Так | Очікувана, передбачувана проблема |
| **`extends RuntimeException`** | ❌ Не обов’язково | Помилки часу виконання |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Exception -> ловимо (checked)**
* **RuntimeException -> просто кидаємо (unchecked)**

або:  

**Свій виняток \= свої правила -> кидай свій клас**

---

## **🟩 Підсумок:**

* Користувацькі винятки — це свої класи, що розширюють **`Exception`** або **`RuntimeException`**.
* Зручно для опису бізнес-логіки, як **`UserNotFoundException`**, **`InvalidOrderStateException`**.
* Дають чистоту в коді, зрозумілий API та кращу обробку помилок.
* Обирай checked або unchecked винятки залежно від того, чи має бути обробка обов’язковою.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке користувацькі винятки (custom exceptions) і навіщо вони потрібні?

> #### **💬 Відповідь:**
>
> **Користувацькі винятки** — це класи, створені розробником шляхом наслідування від стандартних класів винятків (`Exception` або `RuntimeException`), які представляють **специфічні помилкові ситуації для конкретної доменної області або додатка**.
>
> **Навіщо вони потрібні:**
>
> 1. **Підвищення читабельності та ясності коду:** `UserNotFoundException` є набагато більш інформативним, ніж загальний `Exception("User not found")`. Це робить код самодокументованим.
>
> 2. **Точна обробка помилок:** Дозволяє створювати окремі `catch` блоки для різних типів бізнес-помилок. Ви можете по-різному реагувати на `InvalidPasswordException` та `AccountLockedException`.
>
> 3. **Додавання додаткового контексту:** Кастомний виняток може мати власні поля для зберігання додаткової інформації про помилку (наприклад, ID замовлення, ім'я користувача, поточний баланс).
>
> 4. **Абстрагування від деталей реалізації:** Дозволяє "загортати" низькорівневі технічні винятки (наприклад, `SQLException`) у високорівневі доменні винятки (`DataAccessException`), приховуючи деталі.
>
> По суті, вони дозволяють виражати помилки мовою вашої бізнес-логіки, а не мовою технічних збоїв.

#### **📝 Аналогія:**

Це як спеціалізовані попереджувальні знаки на дорозі. Замість одного загального знаку "Небезпека!" (`Exception`), ви маєте конкретні: "Обережно, слизька дорога!" (`SlipperyRoadException`), "Дикі тварини!" (`WildAnimalOnRoadException`), що дозволяє водієві реагувати більш точно.

#### **📌 Що перевіряють:**

*Розуміння того, що кастомні винятки — це інструмент для покращення семантики та архітектури обробки помилок.*

* * *

### **2 ❓ Питання:**

Як створити власний `checked` та `unchecked` виняток?

> #### **💬 Відповідь:**
>
> Створення кастомного винятку полягає в наслідуванні від відповідного базового класу.
>
> **1. Створення власного `checked` винятку:**
>
> * Клас повинен **успадковувати `Exception`** (або будь-якого його нащадка, що не є `RuntimeException`).
>
> ```java
> // Клас успадковує Exception, тому він checked
> public class FileParsingException extends Exception {
>     public FileParsingException(String message) {
>         super(message);
>     }
>
>     public FileParsingException(String message, Throwable cause) {
>         super(message, cause);
>     }
> }
> ```
>
> **2. Створення власного `unchecked` винятку:**
>
> * Клас повинен **успадковувати `RuntimeException`** (або будь-якого його нащадка).
>
> ```java
> // Клас успадковує RuntimeException, тому він unchecked
> public class InvalidCredentialsException extends RuntimeException {
>     public InvalidCredentialsException(String message) {
>         super(message);
>     }
> }
> ```
>
> За найкращою практикою, завжди слід надавати принаймні два конструктори: один, що приймає `String message`, і другий — `String message` та `Throwable cause` для підтримки ланцюжків винятків.

#### **📌 Що перевіряють:**

*Практичні навички створення класів-винятків та знання, який базовий клас обрати для `checked` та `unchecked` версій.*

* * *

### **3 ❓ Питання:**

У якому випадку ваш кастомний виняток повинен бути `checked`, а в якому — `unchecked`?

> #### **💬 Відповідь:**
>
> Це важливе архітектурне рішення, яке залежить від того, чи є помилка **очікуваною** і чи може клієнтський код **відновитися** після неї.
>
> **Робити виняток `checked` (наслідувати `Exception`), коли:**
>
> * Помилка є **очікуваним результатом** операції, на яку клієнт може і **повинен відреагувати**.
> * Це, як правило, стосується взаємодії з зовнішніми системами, де збій є ймовірним.
> * **Приклад:** `InsufficientFundsException`. Клієнтський код може зловити цей виняток і запропонувати користувачеві поповнити рахунок. Це частина бізнес-процесу.
>
> **Робити виняток `unchecked` (наслідувати `RuntimeException`), коли:**
>
> * Помилка свідчить про **програмерський баг**: порушення контракту, передача невалідних аргументів, некоректний стан системи.
> * **Клієнтський код нічого не може зробити**, окрім як залогувати помилку і дати програмі "впасти". Виправляти потрібно не клієнта, а сам код, що призвів до помилки.
> * **Приклад:** `UserNotFoundException`, кинутий у методі `updateUser(userId, data)`, де `userId` мав би бути валідним за логікою попередніх кроків. Його відсутність — це помилка в потоці виконання.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен вміти обґрунтувати свій вибір між `checked` та `unchecked` для конкретної бізнес-ситуації.*

* * *

### **4 ❓ Питання:**

Як додати додаткову інформацію (контекст) до кастомного винятку? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Оскільки кастомний виняток — це звичайний клас, ви можете додавати до нього **власні поля**, щоб зберігати додаткову інформацію про помилку.
>
> Це робить ваш виняток набагато кориснішим для налагодження та обробки, ніж просто рядок з повідомленням.
>
> **Приклад: виняток для невдалої платіжної транзакції.**
>
> ```java
> public class PaymentProcessingException extends Exception {
>     private final long transactionId;
>     private final String errorCode;
>
>     public PaymentProcessingException(String message, long transactionId, String errorCode, Throwable cause) {
>         super(message, cause);
>         this.transactionId = transactionId;
>         this.errorCode = errorCode;
>     }
>
>     public long getTransactionId() {
>         return transactionId;
>     }
>
>     public String getErrorCode() {
>         return errorCode;
>     }
>
>     @Override
>     public String getMessage() {
>         return super.getMessage() + " [Transaction ID: " + transactionId + ", Error Code: " + errorCode + "]";
>     }
> }
> ```
>
> Тепер `catch` блок, який перехоплює цей виняток, може отримати доступ до `transactionId` та `errorCode` і, наприклад, спробувати повторити транзакцію або показати користувачеві більш специфічне повідомлення.

#### **📌 Що перевіряють:**

*Здатність проектувати власні винятки, які несуть багатий контекст, а не лише повідомлення про помилку.*

* * *

### **5 ❓ Питання:**

Чому важливо передавати `cause` (причину) при "загортанні" одного винятку в інший?

> #### **💬 Відповідь:**
>
> Передача оригінального винятку як **`cause`** є **критично важливою** для збереження повної діагностичної інформації.
>
> Якщо ви не передаєте `cause`, ви **втрачаєте оригінальний стек-трейс**, і налагодження помилки стає майже неможливим.
>
> **Сценарій без `cause` (погано):**
>
> `catch (SQLException e) { throw new DataAccessException("Помилка БД"); }`
>
> * **Стек-трейс:** буде починатися з рядка, де ви кинули `DataAccessException`. Вся інформація про те, **де саме в JDBC-драйвері** сталася `SQLException` і яка була її причина, **буде втрачена**.
>
> **Сценарій з `cause` (добре):**
>
> `catch (SQLException e) { throw new DataAccessException("Помилка БД", e); }`
>
> * **Стек-трейс:** буде містити повний ланцюжок. Спочатку буде показано стек-трейс `DataAccessException`, а під ним — секція **`Caused by: java.sql.SQLException...`** з повним стек-трейсом оригінальної помилки.
>
> Це дозволяє розробнику прослідкувати шлях помилки через усі шари архітектури до її першоджерела.

#### **📌 Що перевіряють:**

*Розуміння того, як працює механізм `chained exceptions`, та важливості збереження контексту помилки.*

* * *

### **6 ❓ Питання:**

Навіщо в кастомному винятку перевизначати `getMessage()`? ⭐️

> #### **💬 Відповідь:**
>
> Перевизначення `getMessage()` дозволяє створити **більш інформативне та структуроване повідомлення про помилку**, яке може включати в себе значення додаткових полів кастомного винятку.
>
> За замовчуванням `getMessage()` просто повертає рядок, переданий у конструктор `super()`. Але якщо ваш виняток містить додатковий контекст, ви можете додати його до повідомлення.
>
> **Приклад:**
>
> ```java
> public class InvalidFieldException extends Exception {
>     private final String fieldName;
>
>     public InvalidFieldException(String message, String fieldName) {
>         super(message);
>         this.fieldName = fieldName;
>     }
>
>     // Перевизначаємо, щоб додати контекст
>     @Override
>     public String getMessage() {
>         return super.getMessage() + " [Field: '" + fieldName + "']";
>     }
> }
> ```
>
> Тепер, коли цей виняток буде залоговано (`log.error("...", e)`), у лог автоматично потрапить повне, збагачене повідомлення, наприклад: `"Значення не може бути порожнім [Field: 'username']"`.
>
> Це значно полегшує аналіз логів та налагодження.

#### **📌 Що перевіряють:**

*Увагу до деталей та розуміння, як зробити кастомні винятки ще більш корисними для діагностики.*

* * *

### **7 ❓ Питання:**

Чи можна створити кастомний `Error`? Якщо так, то в якій ситуації це може бути виправдано? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, технічно можна** створити власний клас, що успадковує `java.lang.Error`.
>
> **Але робити цього майже ніколи не варто.**
>
> `Error`-и призначені для сигналізації про **фатальні, невідворотні збої в роботі JVM**. Це не ті помилки, якими повинен керувати ваш додаток.
>
> **Ситуація, де це могло б бути виправдано (дуже рідкісний, специфічний випадок):**
>
> * При розробці **дуже низькорівневих систем, фреймворків або самої JVM**.
> * Наприклад, якщо ви створюєте власний `ClassLoader` і він виявляє, що визначення класу є **фундаментально пошкодженим**, і подальша робота неможлива, ви могли б кинути кастомний `MyLinkageError`.
>
> Однак, у 99.99% випадків, якщо ваш код зіткнувся з помилкою, якою б фатальною вона не здавалася для вашого додатка (наприклад, неможливість підключитися до бази даних при старті), її слід моделювати як **`RuntimeException`** (наприклад, `FatalApplicationStartupException extends RuntimeException`), а не як `Error`.
>
> `Error` — це "територія" JVM.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантики `Error` vs `Exception`. Кандидат повинен розуміти, чому кидати `Error` — це майже завжди неправильно.*

* * *

### **8 ❓ Питання:**

Які конструктори найкраще завжди додавати до кастомного класу винятків?

> #### **💬 Відповідь:**
>
> За найкращою практикою, для забезпечення максимальної гнучкості ваш кастомний виняток повинен надавати **чотири стандартні конструктори**, які успадковують від `Exception` (або `RuntimeException`):
>
> 1. **Конструктор без аргументів:**
>     `public MyException() { super(); }`
>
> 2. **Конструктор з повідомленням:**
>     `public MyException(String message) { super(message); }`
>
> 3. **Конструктор з повідомленням та причиною (`cause`):**
>     `public MyException(String message, Throwable cause) { super(message, cause); }`
>     Це найважливіший конструктор для підтримки ланцюжків винятків.
>
> 4. **Конструктор лише з причиною:**
>     `public MyException(Throwable cause) { super(cause); }`
>
> **Чому це важливо:**
>
> * Надання повного набору конструкторів робить ваш клас винятків **універсальним та зручним** для використання у різних ситуаціях.
> * Наприклад, іноді вам потрібно просто "загорнути" інший виняток без додавання нового повідомлення, і для цього ідеально підходить конструктор, що приймає лише `cause`.
>
> Це стандарт, якого дотримуються більшість винятків у JDK.

#### **📌 Що перевіряють:**

*Знання найкращих практик при проектуванні власних класів. Кандидат повинен думати про те, як його клас будуть використовувати інші.*

* * *

### **9 ❓ Питання:**

У чому різниця між тим, щоб кинути `new IllegalArgumentException("Invalid ID")` і `new MyCustomInvalidIdException("Invalid ID")`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає у **специфічності та можливостях обробки**.
>
> * **`new IllegalArgumentException(...)`:**
>   * **Що це:** Використання стандартного, **загального** винятку. Він каже: "було передано невалідний аргумент".
>   * **Недолік:** Він **неспецифічний**. Якщо ваш метод може кинути `IllegalArgumentException` з кількох причин (наприклад, і ID, і email невалідні), то в `catch` блоці вам буде **важко розрізнити**, яка саме помилка сталася, не аналізуючи рядок повідомлення (що є поганою практикою).
>
> * **`new MyCustomInvalidIdException(...)`:**
>   * **Що це:** Використання власного, **специфічного для домену** винятку. Він каже: "саме ID був невалідним".
>   * **Перевага:** Це дозволяє створювати **окремі `catch` блоки** для різних типів помилок, роблячи логіку обробки набагато чистішою та надійнішою.
>
>         ```java
>         try {
>             // ...
>         } catch (MyCustomInvalidIdException e) {
>             // логіка для невалідного ID
>         } catch (MyCustomInvalidEmailException e) {
>             // логіка для невалідного email
>         }
>         ```
>
> **Висновок:** Створення кастомних винятків, навіть простих нащадків `IllegalArgumentException`, значно підвищує **виразність та підтримуваність** коду, особливо у складній бізнес-логіці.

#### **📌 Що перевіряють:**

*Здатність бачити переваги у створенні більш специфічних типів для кращого моделювання помилок.*

* * *

### **10 ❓ Питання:**

Чи можна додати `enum` всередину класу винятку для категоризації помилок? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, це дуже потужний і елегантний патерн**, який дозволяє уникнути створення десятків маленьких класів винятків.
>
> **Ідея:**
> Ви створюєте один кастомний виняток (наприклад, `ValidationException`), а всередині нього — **`enum`**, який перераховує всі можливі **типи помилок валідації**.
>
> **Приклад:**
>
> ```java
> public class ValidationException extends RuntimeException {
>
>     public enum ErrorType {
>         USERNAME_TOO_SHORT,
>         EMAIL_INVALID_FORMAT,
>         PASSWORD_WEAK
>     }
>
>     private final ErrorType errorType;
>
>     public ValidationException(ErrorType errorType, String message) {
>         super(message);
>         this.errorType = errorType;
>     }
>
>     public ErrorType getErrorType() {
>         return errorType;
>     }
> }
> ```
>
> **Як це використовується:**
>
> * При виявленні помилки, ви кидаєте `new ValidationException(ErrorType.EMAIL_INVALID_FORMAT, "...")`.
> * У `catch` блоці ви ловите один тип — `ValidationException`, а потім за допомогою `switch` по `getErrorType()` реалізуєте різну логіку для кожного типу помилки.
>
> ```java
> catch (ValidationException e) {
>     switch (e.getErrorType()) {
>         case USERNAME_TOO_SHORT: ...
>         case EMAIL_INVALID_FORMAT: ...
>     }
> }
> ```
>
> Цей підхід є дуже гнучким, структурованим і дозволяє уникнути "вибуху" кількості класів винятків.

#### **📌 Що перевіряють:**

*Навички проектування складних систем обробки помилок та знання просунутих патернів.*

* * *

### **11 ❓ Питання:**

Як вплине на ієрархію те, що мій кастомний `checked` виняток успадковує інший `checked` виняток? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це стандартна практика, яка дозволяє будувати **ієрархії винятків**.
>
> **Приклад:**
>
> ```java
> // Базовий checked виняток для нашого модуля
> public class ModuleException extends Exception { ... }
>
> // Більш специфічні винятки
> public class NetworkException extends ModuleException { ... }
> public class DataFormatException extends ModuleException { ... }
> ```
>
> **Наслідки:**
>
> 1. Це дозволяє клієнтському коду бути більш **гнучким** в обробці. Клієнт може:
>     * **Ловити конкретний виняток:** `catch (NetworkException e)`
>     * Або **ловити всі винятки з цього модуля**, перехоплюючи батьківський: `catch (ModuleException e)`
>
> 2. Це також впливає на **`throws` клаузи** при перевизначенні. Якщо батьківський метод оголошує `throws ModuleException`, дочірній може звузити це до `throws NetworkException`, що є валідним.
>
> Створення ієрархій винятків дозволяє групувати помилки за їхньою природою і надає клієнтам вибір, наскільки детальною має бути їхня логіка обробки.

#### **📌 Що перевіряють:**

*Розуміння того, як принципи наслідування та поліморфізму застосовуються до ієрархаті винятків.*

* * *

### **12 ❓ Питання:**

Чи можна реалізувати кастомний виняток за допомогою `record`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Це призведе до **помилки компіляції**.
>
> **Причина:**
>
> Правила мови Java містять строге обмеження: **`record` не може явно успадковувати (`extends`) жоден інший клас**.
>
> Кожен `record` **неявно успадковує** спеціальний клас `java.lang.Record`, подібно до того, як `enum` успадковує `java.lang.Enum`. Оскільки Java не підтримує множинне наслідування класів, `record` не може успадкувати ще й `Exception` або `RuntimeException`.
>
> ```java
> // ПОМИЛКА КОМПІЛЯЦІЇ: Cannot extend 'java.lang.Exception'
> // public record MyExceptionRecord(String message) extends Exception { }
> ```
>
> **Однак, `record` може реалізовувати інтерфейси.** Але це не допомагає у створенні винятку, оскільки `Throwable` та його нащадки є класами.
>
> Тому для створення кастомних винятків потрібно використовувати традиційні **класи**.

#### **📌 Що перевіряють:**

*Дуже глибоке знання обмежень сучасних мовних конструкцій, таких як `record`.*

* * *

### **13 ❓ Питання:**

Поясніть, що означає фраза "кидати винятки на правильному рівні абстракції". Наведіть приклад. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це важливий архітектурний принцип, який означає, що **винятки, які "просочуються" з певного шару архітектури, повинні бути зрозумілими для вищого шару і відповідати його рівню абстракції.**
>
> **Порушення принципу:**
>
> * `UserService` (бізнес-логіка) кидає `SQLException`.
> * `Controller` (веб-шар), який викликає `UserService`, змушений обробляти `SQLException`.
>
> **Проблема:** Контролер нічого не повинен знати про те, яка база даних використовується (`SQL`). Це **деталь реалізації**, яка "просочилася" на вищий рівень.
>
> **Правильний підхід:**
>
> 1. `UserRepository` ловить `SQLException` і **"загортає"** його в `DataAccessException` (`RuntimeException`).
> 2. `UserService`, в свою чергу, може зловити `DataAccessException` і **перетворити** його на **доменний виняток**, зрозумілий для UI.
>
>     ```java
>     public User register(UserDto dto) throws RegistrationException {
>         try {
>             // ... userRepository.save(user); ...
>         } catch(DataAccessException e) {
>             // Перетворюємо технічну помилку на бізнес-помилку
>             throw new RegistrationException("Не вдалося зареєструвати користувача", e);
>         }
>     }
>     ```
>
> 3. Тепер `Controller` ловить `RegistrationException` і повертає користувачу зрозуміле повідомлення.
>
> Кожен шар "перекладає" низькорівневі помилки на мову своєї власної абстракції. Це є основою **слабкої зв'язності** та чистої архітектури.

#### **📌 Що перевіряють:**

*Архітектурне мислення та вміння проектувати багатошарові системи з коректною обробкою помилок.*

* * *

### **14 ❓ Питання:**

Чому клас `Exception` сам є `checked`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це **фундаментальне дизайнерське рішення** в Java, яке визначає, які винятки перевіряються за замовчуванням.
>
> Зробивши сам `Exception` перевірюваним, творці Java встановили **"безпеку за замовчуванням"**.
>
> **Логіка така:**
>
> * Будь-яка нова виняткова ситуація, для якої розробник створює власний виняток, за замовчуванням буде **успадковувати `Exception`**.
> * Це автоматично робить цей новий виняток **`checked`**.
> * Це змушує розробника **свідомо прийняти рішення**: залишити його `checked` (якщо клієнт повинен його обробляти) або явно зробити його `unchecked` шляхом успадкування від `RuntimeException`.
>
> Якби `Exception` був `unchecked`, то всі нові кастомні винятки за замовчуванням також були б `unchecked`. Це могло б призвести до того, що розробники забували б обробляти важливі очікувані помилки.
>
> По суті, дизайн Java каже: **"Всі помилки за замовчуванням є важливими і вимагають вашої уваги, якщо ви свідомо не вкажете, що це програмний баг (успадкувавши від `RuntimeException`)".**

#### **📌 Що перевіряють:**

*Розуміння філософії та дизайнерських рішень, що лежать в основі системи винятків Java.*

* * *

### **15 ❓ Питання:**

Розробіть ієрархію кастомних винятків для простої банківської системи. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Хороша ієрархія винятків дозволить диференційовано обробляти різні помилкові сценарії.
>
> **1. Базовий `checked` виняток для бізнес-операцій:**
>
> * `public class BankingException extends Exception`
>   * Це буде корінь для всіх очікуваних бізнес-помилок.
>
> **2. Більш специфічні `checked` нащадки:**
>
> * `public class InsufficientFundsException extends BankingException`
>   * Кидається, коли на рахунку недостатньо коштів. Клієнт може обробити і запропонувати поповнити рахунок.
>
> * `public class AccountNotFoundException extends BankingException`
>   * Кидається при спробі знайти неіснуючий рахунок. Клієнт може запропонувати створити новий.
>
> * `public class DailyLimitExceededException extends BankingException`
>   * Кидається, коли перевищено денний ліміт операцій.
>
> **3. Базовий `unchecked` виняток для системних помилок:**
>
> * `public class BankingSystemException extends RuntimeException`
>   * Використовується для загортання низькорівневих помилок (наприклад, з БД), від яких система не може оговтатися.
>
> **Приклад використання в коді:**
>
> ```java
> public void transfer(long from, long to, double amount)
>     throws InsufficientFundsException, AccountNotFoundException {
>     //...
>     if (balance < amount) throw new InsufficientFundsException(...);
>     //...
> }
>
> catch(InsufficientFundsException e) {
>     // UI: "Недостатньо коштів"
> } catch(AccountNotFoundException e) {
>     // UI: "Рахунок не знайдено"
> } catch(BankingException e) {
>     // UI: "Загальна бізнес-помилка"
> } catch(BankingSystemException e) {
>     // UI: "Сервіс тимчасово недоступний"
> }
> ```
>
> Така ієрархія робить код структурованим, читабельним і дозволяє гнучко обробляти помилки на різних рівнях.

#### **📌 Що перевіряють:**

*Навички проектування та здатність створювати чисті та розширювані ієрархаті для обробки помилок у конкретній доменній області.*
