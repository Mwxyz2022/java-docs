---
layout: default
title: "Checked vs Unchecked Exceptions"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 3
---

# Checked vs Unchecked Exceptions

* короткий опис: `Checked Exceptions` перевіряються компілятором і вимагають обробки (`try-catch`) або оголошення (`throws`). `Unchecked Exceptions` (підкласи `RuntimeException`) не перевіряються компілятором і не вимагають обов'язкової обробки.

* поняття: Checked Exception, Unchecked Exception, `RuntimeException`.

* приклади: `IOException`, `FileNotFoundException` (Checked); `NullPointerException`, `IllegalArgumentException` (Unchecked).

* ключові моменти: Checked – для очікуваних ризиків, Unchecked – для логічних помилок, які краще уникати.

* що важливо знати на співбесіді: Чітка різниця, коли що використовувати.

### **✅ Офіційне визначення:**

**Checked Exceptions** — винятки, які перевіряються компілятором, і ти зобовʼязаний або обробити їх (try-catch), або вказати в сигнатурі методу (throws).

**Unchecked Exceptions** — це винятки, які не перевіряються компілятором, і ти не зобов’язаний їх обробляти.

### **🧠 Простими словами:**

**`Checked`** \= Java каже: “Це може зламатися — оброби\!”

**`Unchecked`** \= Java каже: “Можеш не ловити, але будь уважний\!”

---

## **Основне**

| Тип | Клас | Сценарій |
| :---: | :---: | :---: |
| **Checked** | **IOException** | Відкриваєш файл, але його нема |
|  | **SQLException** | Проблеми з базою даних |
|  | **ParseException** | Помилка розбору дати |
|  | **ClassNotFoundException** | Клас не знайдено при завантаженні |
| **Unchecked** | **NullPointerException** | Звернення до null |
|  | **ArithmeticException** | Поділ на 0 |
|  | **IllegalArgumentException** | Метод отримав неправильні дані |
|  | **IndexOutOfBoundsException** | Індекс за межами масиву/стрічки |

---

## **Чому компілятор вимагає обробити тільки Checked Exceptions?**

📌 **Причина:**  
Це очікувані ризики, які можна передбачити наперед:

* робота з файлами
* робота з мережею
* робота з базою даних

🔁 Тобто ті речі, що зовнішні для програми (I/O, база) — потребують явної обробки.

⚠️ **Unchecked Exceptions** — це програмні помилки, викликані твоїм кодом (null, неправильний індекс) — їх треба уникати, а не ловити.

---

## **Корисні приклади (🧪)**

| Тип | Коли використовувати | Як обробляти |
| :---: | :---: | :---: |
| **Checked** | При взаємодії з зовнішнім світом (I/O, DB, мережа) | try-catch або throws у методі |
| **Unchecked** | Коли проблема в твоїй логіці | Обробка не обовʼязкова, але бажана (через валідацію або try-catch) |

---

## **Порівняння (⚖️)**

✔️ **Checked Exception:**

```java
import java.io.*;

public class CheckedExample {
    public static void main(String[] args) throws IOException {
        FileReader reader = new FileReader("file.txt"); // може викинути IOException
    }
}
```

✔️ **Unchecked Exception:**

```java
public class UncheckedExample {
    public static void main(String[] args) {
        String text = null;
        System.out.println(text.length()); // NullPointerException -- unchecked
    }
}
```

---

## **🧠 Мнемоніка для запам’ятовування:**

**Checked** -> Контролюється компілятором  
        -> Зовнішні ризики (файл, БД)  
        -> Обов'язково: try/catch або throws

**Unchecked** -> Помилка в коді  
        -> RuntimeException і діти  
        -> Обробка — не обов'язкова

Або скорочено:  
**`try or die`** — для Checked  
**`bug = your fault`** — для Unchecked

---

## **🟩 Підсумок:**

* **Checked Exception** \= Java зобов'язує тебе обробити (зовнішні проблеми).
* **Unchecked Exception** \= програмні помилки, які ти маєш попереджати сам.
* Обидва типи наслідують **`Exception`**, але лише checked контролюються компілятором.

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Tutorials — Exceptions**](https://docs.oracle.com/javase/tutorial/essential/exceptions/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка ключова різниця між `checked` та `unchecked` винятками в Java?

> #### **💬 Відповідь:**
>
> Ключова різниця полягає в **перевірці з боку компілятора**.
>
> **`Checked Exceptions` (Перевірювані винятки):**
>
> * Це винятки, які **перевіряються на етапі компіляції**.
> * **Правило:** Якщо метод може кинути `checked` виняток, він **зобов'язаний** або обробити його за допомогою `try-catch`, або оголосити у своїй сигнатурі за допомогою ключового слова `throws`. Якщо цього не зробити, код **не скомпілюється**.
> * **Призначення:** Сигналізують про **очікувані помилкові ситуації**, від яких клієнтський код може оговтатися (наприклад, робота з зовнішніми ресурсами).
> * **Ієрархія:** Усі нащадки `Exception`, крім `RuntimeException` та його нащадків.
>
> **`Unchecked Exceptions` (Неперевірювані винятки):**
>
> * Це винятки, які **не перевіряються компілятором**.
> * **Правило:** Ви **не зобов'язані** їх обробляти чи оголошувати.
> * **Призначення:** Зазвичай сигналізують про **програмні помилки (баги)**: порушення контрактів, логічні помилки, неправильне використання API.
> * **Ієрархія:** `RuntimeException`, `Error` та всі їхні нащадки.

#### **📝 Аналогія:**

* **Checked:** Поїздка на авто. Ви **знаєте**, що бензин може закінчитися, тому ви **перевіряєте** датчик палива і маєте план (заїхати на заправку).
* **Unchecked:** Ви забули прикрутити колесо. Це баг у підготовці. Програма (машина) просто "впаде", і ніяка попередня перевірка вас не врятує — треба виправляти саму помилку.

#### **📌 Що перевіряють:**

*Це одне з фундаментальних питань Java Core. Кандидат повинен чітко розмежувати ці дві категорії за їхнім призначенням та правилами обробки.*

* * *

### **2 ❓ Питання:**

Які класи є базовими для `checked` та `unchecked` винятків?

> #### **💬 Відповідь:**
>
> Розмежування проходить по лінії класу `RuntimeException`.
>
> * **`checked` винятки:**
>   * Їхнім спільним предком є клас **`java.lang.Exception`**.
>   * Будь-який клас, що успадковує `Exception`, але **не** `RuntimeException`, є перевірюваним.
>   * **Приклади:** `IOException`, `SQLException`, `ClassNotFoundException`.
>
> * **`unchecked` винятки:**
>   * Мають два кореневих класи:
>         1. **`java.lang.RuntimeException`**: Це основний клас для всіх неперевірюваних помилок, пов'язаних з логікою програми.
>         2. **`java.lang.Error`**: Це клас для фатальних помилок JVM, які також є неперевірюваними.
>   * **Приклади:** `NullPointerException`, `IllegalArgumentException` (нащадки `RuntimeException`); `StackOverflowError` (нащадок `Error`).

#### **📌 Що перевіряють:**

*Знання ієрархії винятків та ключової ролі класу `RuntimeException` у поділі на `checked` та `unchecked`.*

* * *

### **3 ❓ Питання:**

Коли на практиці слід створювати власний `checked` виняток, а коли — `unchecked`?

> #### **💬 Відповідь:**
>
> Це важливе архітектурне рішення. Правило великого пальця: **"Кидайте `checked`, якщо клієнт може щось зробити з помилкою, і `unchecked` — якщо ні"**.
>
> **Створюйте `checked` виняток, коли:**
>
> * Помилка є **очікуваною та ймовірною** (наприклад, при роботі з мережею або файлами).
> * **Клієнтський код може розумно відреагувати** на цю помилку та відновити роботу.
> * **Приклад:** `AccountBalanceException`. Якщо на рахунку недостатньо коштів, UI може показати повідомлення користувачеві "Будь ласка, поповніть рахунок". Це очікувана бізнес-ситуація.
>
> **Створюйте `unchecked` (`RuntimeException`) виняток, коли:**
>
> * Помилка вказує на **програмерський баг**: порушення передумов, неправильний стан, невалідовані дані, що не мали потрапити в цей метод.
> * **Клієнтський код не може нічого вдіяти**, окрім як залогувати помилку і дати програмі "впасти" (або обробити на найвищому рівні). Виправляти потрібно сам код, а не ловити виняток.
> * **Приклад:** `UserNotFoundException`, кинутий у внутрішньому методі, який за логікою завжди повинен отримувати існуючого користувача. Його відсутність — це помилка в логіці вищого рівня.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Здатність проектувати API, який чітко комунікує типи помилок, що можуть виникнути.*

* * *

### **4 ❓ Питання:**

Чому такі винятки, як `NullPointerException` та `ArrayIndexOutOfBoundsException`, є `unchecked`?

> #### **💬 Відповідь:**
>
> Вони є `unchecked` (нащадками `RuntimeException`), тому що вони майже завжди сигналізують про **логічну помилку (баг) у коді**.
>
> * **`NullPointerException`:** Виникає при спробі звернутися до `null`-посилання. Це означає, що програміст не виконав необхідну перевірку на `null` (`if (obj != null)`), або допустив ситуацію, коли змінна, яка не повинна бути `null`, нею стала.
>
> * **`ArrayIndexOutOfBoundsException`:** Виникає при зверненні до індексу масиву, який знаходиться поза його межами. Це означає, що логіка роботи з індексами є неправильною (наприклад, помилка "off-by-one" у циклі).
>
> Якби ці винятки були `checked`, код був би неймовірно засмічений `try-catch` блоками навколо практично кожного звернення до об'єкта чи масиву:
>
> ```java
> // Якби NPE був checked (жахливий код)
> try {
>     obj.doSomething();
> } catch (NullPointerException e) { /* ... */ }
> ```
>
> Це робить код нечитабельним. Філософія Java полягає в тому, що ці помилки слід **попереджати** (писати коректний код, додавати перевірки), а не **ловити**.

#### **📌 Що перевіряють:**

*Розуміння філософії, що стоїть за `unchecked` винятками — це про баги, які треба виправляти, а не обробляти.*

* * *

### **5 ❓ Питання:**

Що таке "прокидання" (`throws`) та "загортання" (`wrapping`) `checked` винятків? Коли який підхід краще? ⭐️

> #### **💬 Відповідь:**
>
> Це два основні способи роботи з `checked` винятками, які метод не може обробити самостійно.
>
> **Прокидання (`throws`):**
>
> * **Що це:** Метод просто "перекидає відповідальність" за обробку винятку на той код, що його викликав. Це робиться за допомогою ключового слова `throws` у сигнатурі методу.
>     `public void readFile() throws IOException { ... }`
> * **Коли краще:** Коли метод є низькорівневим, і він не має достатнього контексту, щоб коректно обробити помилку. Логіка обробки повинна бути на більш високому рівні.
>
> **Загортання (`wrapping`):**
>
> * **Що це:** Метод перехоплює низькорівневий `checked` виняток і "загортає" його у новий, зазвичай `unchecked` (`RuntimeException`) виняток, зберігаючи оригінальний як причину (`cause`).
>
>     ```java
>     catch (SQLException e) {
>         throw new DataAccessException("Помилка...", e);
>     }
>     ```
>
> * **Коли краще:** Коли ви хочете **абстрагуватися від деталей реалізації**. Ви не хочете, щоб ваш сервісний шар знав про `SQLException`. Замість цього, ви кидаєте більш загальний `DataAccessException`, приховуючи деталі. Це допомагає уникнути **"checked exception hell"**.
>
> **Висновок:** Якщо виняток є частиною контракту вашого API, **прокидайте** його. Якщо це деталь реалізації, яку ви хочете приховати, — **загортайте**.

#### **📌 Що перевіряють:**

*Розуміння двох основних стратегій обробки помилок у багатошарових додатках.*

* * *

### **6 ❓ Питання:**

Чому Stream API в Java 8 переважно працює з `unchecked` винятками? Як обробляти `checked` винятки всередині лямбда-виразів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Стандартні функціональні інтерфейси, що використовуються в Stream API (як `Function`, `Predicate`), **не оголошують `checked` винятки** у своїх сигнатурах. Це зроблено для того, щоб зробити код з потоками **більш лаконічним і читабельним**.
>
> Якби кожен `map` чи `filter` вимагав `try-catch`, функціональний стиль втратив би свою елегантність.
>
> **Як обробляти `checked` винятки всередині лямбди:**
>
> **Проблема:**
>
> ```java
> // ПОМИЛКА: IOException - це checked, а Function.apply не кидає його
> // list.stream().map(path -> Files.readString(path));
> ```
>
> **Рішення:**
>
> 1. **Обгортка `try-catch` всередині лямбди:**
>     * Це найпростіший, але багатослівний спосіб. Ви ловите `checked` виняток і кидаєте `unchecked` (або повертаєте якесь значення за замовчуванням).
>
>     ```java
>     list.stream().map(path -> {
>         try {
>             return Files.readString(path);
>         } catch (IOException e) {
>             throw new RuntimeException(e); // "Загортаємо" виняток
>         }
>     });
>     ```
>
> 2. **Винесення логіки в окремий метод:**
>     * Створюється приватний метод, який містить `try-catch` блок, а в лямбді викликається вже він. Це робить код чистішим.
>
>     ```java
>     private String readFileUnchecked(Path path) { ... }
>     list.stream().map(this::readFileUnchecked);
>     ```
>
> 3. **Створення власного "кидаючого" функціонального інтерфейсу:**
>     * Просунутий підхід для створення власних API, але не застосовується до стандартного Stream API.

#### **📌 Що перевіряють:**

*Практичні навички роботи зі Stream API та обробки винятків у функціональному стилі.*

* * *

### **7 ❓ Питання:**

Яка є головна критика концепції `checked` винятків? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Checked` винятки, хоч і були введені з добрими намірами, є однією з **найбільш суперечливих** особливостей Java.
>
> **Головна критика:**
>
> 1. **"Вірусний" характер:** Вони змушують "прокидати" їх вгору по всьому стеку викликів. Якщо низькорівневий метод починає кидати новий `checked` виняток, це може вимагати змін у десятках методів вище по ланцюжку.
>
> 2. **Забруднення сигнатур методів:** `throws IOException, SQLException, MyException, ...` робить сигнатури громіздкими і прив'язує їх до деталей реалізації.
>
> 3. **Провокування поганих практик:** Розробники, щоб уникнути багатослівності, часто вдаються до поганих практик:
>     * **"Ковтання" винятків:** `catch (Exception e) {}`
>     * **Оголошення `throws Exception`:** Це повністю руйнує ідею типізованих винятків.
>     * **"Загортання" у `RuntimeException`:** Це часто є виправданим, але, по суті, є способом обійти механізм `checked` винятків.
>
> 4. **Проблеми з лямбдами та стрімами:** Як було сказано, стандартні функціональні інтерфейси з ними не працюють, що вимагає додаткового шаблонного коду.
>
> Багато сучасних JVM-мов (Kotlin, Scala) **відмовилися** від `checked` винятків, вважаючи, що відповідальність за обробку помилок має лежати на розробнику, а не нав'язуватися компілятором.

#### **📌 Що перевіряють:**

*Критичне мислення та розуміння архітектурних дебатів навколо дизайну мови.*

* * *

### **8 ❓ Питання:**

Розглянемо блок `catch(Exception e)`. Чи перехопить він `StackOverflowError`? А `NullPointerException`?

> #### **💬 Відповідь:**
>
> * `StackOverflowError` — **не перехопить**.
> * `NullPointerException` — **перехопить**.
>
> **Пояснення:**
>
> Блок `catch (Exception e)` перехоплює лише об'єкти, які є екземплярами класу `Exception` або його нащадків.
>
> 1. **`StackOverflowError`:**
>     * `StackOverflowError` успадковується від **`Error`**, а не від `Exception`.
>     * `Error` і `Exception` є "братніми" класами, що успадковуються від `Throwable`, але не один від одного.
>     * Тому `catch(Exception e)` не може перехопити `Error`. Для цього потрібен був би `catch(Error e)` або `catch(Throwable t)`.
>
> 2. **`NullPointerException`:**
>     * `NullPointerException` успадковується від **`RuntimeException`**, який, у свою чергу, успадковується від **`Exception`**.
>     * Ієрархія: `Throwable` → `Exception` → `RuntimeException` → `NullPointerException`.
>     * Оскільки `NPE` є нащадком `Exception`, блок `catch (Exception e)` його перехопить.

#### **📌 Що перевіряють:**

*Чітке знання ієрархаті `Throwable` та правил роботи поліморфізму в `catch`-блоках.*

* * *

### **9 ❓ Питання:**

Які `Exception` та `Error` пов'язані з переповненням пам'яті і в чому між ними різниця? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> З переповненням пам'яті пов'язані дві основні помилки, які часто плутають: `StackOverflowError` та `OutOfMemoryError`. Обидві є нащадками `Error`, тобто фатальними.
>
> **`StackOverflowError`:**
>
> * **Причина:** Виникає, коли переповнюється **стекова пам'ять (Stack)**. Кожен потік має свій стек, куди поміщаються фрейми при виклику методів. Якщо ланцюжок викликів стає занадто глибоким, місце в стеку закінчується.
> * **Типовий сценарій:** Нескінченна або дуже глибока **рекурсія**.
>     `void myMethod() { myMethod(); }`
>
> **`OutOfMemoryError`:**
>
> * **Причина:** Виникає, коли закінчується пам'ять у **купі (Heap)**. Це відбувається, коли програма намагається створити новий об'єкт, але збирач сміття (GC) не може звільнити достатньо місця.
> * **Типовий сценарій:** **Витік пам'яті**, коли створюється величезна кількість об'єктів, на які продовжують існувати посилання, і GC не може їх видалити. Або при спробі завантажити в пам'ять занадто великий файл/масив.
>
> **Різниця:**
>
> * `StackOverflowError` — про **глибину викликів**.
> * `OutOfMemoryError` — про **обсяг даних (об'єктів)**.

#### **📌 Що перевіряють:**

*Розуміння різниці між пам'яттю Stack та Heap та помилками, що їм відповідають.*

* * *

### **10 ❓ Питання:**

Що станеться, якщо `throws` клауза перевизначеного методу в дочірньому класі буде ширшою, ніж у батьківському? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **помилки компіляції**.
>
> Існує строге правило для `checked` винятків: **перевизначений метод не може оголошувати `checked` виняток, який є ширшим (є суперкласом)** за той, що оголошено в батьківському методі.
>
> **Правила:**
> Перевизначений метод може:
>
> * Кидати **той самий `checked` виняток**.
> * Кидати його **підклас**.
> * **Не кидати** `checked` винятків взагалі.
>
> **Приклад помилки:**
>
> ```java
> class Parent {
>     void process() throws java.io.FileNotFoundException {}
> }
> class Child extends Parent {
>     // ПОМИЛКА: IOException є батьком для FileNotFoundException,
>     // тому це "розширення" контракту
>     @Override
>     void process() throws java.io.IOException {}
> }
> ```
>
> **Чому так:**
>
> Це правило підтримує **принцип підстановки Лісков (LSP)**. Клієнтський код, написаний для роботи з `Parent`, готовий обробляти лише `FileNotFoundException`. Якби `Child` почав кидати більш загальний `IOException`, цей код "зламався" б.

#### **📌 Що перевіряють:**

*Знання правил перевизначення, пов'язаних з обробкою винятків.*

* * *

### **11 ❓ Питання:**

Наведіть приклад, де логіку `try-catch` можна замінити на `Optional` (з Java 8). ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Optional` є чудовою функціональною альтернативою для обробки ситуацій, де **результат може бути відсутнім**, і для яких раніше часто використовували `null` або кидали виняток.
>
> **Типовий сценарій: пошук, який може нічого не знайти.**
>
> **Старий підхід (з винятком):**
>
> ```java
> public User findUserById(long id) throws UserNotFoundException {
>     User user = //... логіка пошуку
>     if (user == null) {
>         throw new UserNotFoundException("User not found");
>     }
>     return user;
> }
> ```
>
> **Клієнтський код:**
> `try { User user = repo.findUserById(1); ... } catch (UserNotFoundException e) { ... }`
>
> **Сучасний підхід (з `Optional`):**
>
> ```java
> public Optional<User> findUserById(long id) {
>     User user = //... логіка пошуку
>     return Optional.ofNullable(user);
> }
> ```
>
> **Клієнтський код:**
>
> ```java
> repo.findUserById(1)
>     .ifPresent(user -> System.out.println(user.getName()));
>
> User user = repo.findUserById(1)
>     .orElse(new User("Default"));
> ```
>
> **Переваги `Optional`:**
>
> * **Явність API:** Сигнатура методу `Optional<User>` **чітко говорить**, що результат може бути відсутнім. Це краще, ніж покладатися на `null` або `checked` винятки.
> * **Функціональний стиль:** Дозволяє писати лаконічний, декларативний код без `if(user != null)` або `try-catch`.
>
> `Optional` не призначений для заміни всіх винятків, а лише тих, що пов'язані з **очікуваною відсутністю значення**.

#### **📌 Що перевіряють:**

*Знання сучасного API Java та функціональних підходів до обробки помилок.*

* * *

### **12 ❓ Питання:**

Чи можна мати блок `try-finally` без `catch`? Коли це доцільно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, конструкція `try-finally` є абсолютно валідною.**
>
> **Коли це доцільно:**
>
> Вона використовується, коли вам потрібно **гарантовано виконати якусь дію по очищенню** (наприклад, закрити ресурс), але ви **не хочете обробляти виняток на цьому рівні**, а хочете, щоб він **був прокинутий далі** вгору по стеку викликів.
>
> **Сценарій:**
>
> ```java
> public void processFile() throws IOException {
>     FileInputStream input = new FileInputStream("file.txt");
>     try {
>         // Робота з файлом, яка може кинути IOException
>         // ...
>     } finally {
>         // Цей код виконається, навіть якщо виникне IOException.
>         // Ми гарантовано закриємо ресурс.
>         System.out.println("Закриваємо файл.");
>         input.close();
>         // Але IOException з `try` не буде перехоплено і полетить далі.
>     }
> }
> ```
>
> **Сучасна альтернатива:**
>
> Цей патерн був дуже поширений до Java 7. Сьогодні для таких задач найкраще використовувати **`try-with-resources`**, який робить те ж саме, але більш коротко, безпечно і автоматично.

#### **📌 Що перевіряють:**

*Знання всіх можливих комбінацій блоків `try-catch-finally` та їхнього призначення.*

* * *

### **13 ❓ Питання:**

Чому `AssertionError` є `Error`, а не `Exception`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> `AssertionError` є нащадком `Error`, тому що він сигналізує про **катастрофічну помилку в логіці програми** — порушення інваріанту або припущення, яке **ніколи** не повинно було статися.
>
> **Ідея `assert`:**
>
> Конструкція `assert` використовується для перевірки внутрішніх припущень розробника під час розробки та тестування. Наприклад: `assert x > 0;`.
>
> * Якщо це припущення хибне, це означає, що програма знаходиться в **абсолютно некоректному, непередбачуваному стані**.
> * Це не є "очікуваною" помилкою, яку клієнтський код повинен намагатися обробити. Це **баг**.
> * Відновлення після `AssertionError` не має сенсу. Єдиний правильний шлях — **негайно зупинити програму**, щоб запобігти подальшому пошкодженню даних, і **виправити баг**.
>
> Зробивши `AssertionError` нащадком `Error`, а не `Exception`, творці мови підкреслили, що це **фатальна програмна помилка**, яку **не слід ловити (`catch`)**.

#### **📌 Що перевіряють:**

*Розуміння філософії, що лежить в основі механізму `assert`, та його місця в ієрархії `Throwable`.*

* * *

### **14 ❓ Питання:**

Що таке "придушені винятки" (`suppressed exceptions`) і яку проблему `try-with-resources` вони вирішують? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Придушені винятки** — це механізм, введений у Java 7, який дозволяє **не втрачати інформацію про помилки**, що виникають під час закриття ресурсів, якщо в `try` блоці вже виник інший виняток.
>
> **Проблема, яку вони вирішують (у старій конструкції `try-finally`):**
>
> 1. В `try` блоці кидається `ExceptionA`.
> 2. В `finally` блоці при закритті ресурсу кидається `ExceptionB`.
> 3. **`ExceptionB` "перекриває" `ExceptionA`**. Інформація про початкову проблему **втрачається**.
>
> **Як працює `try-with-resources` з придушеними винятками:**
>
> 1. В `try` блоці кидається `ExceptionA` (це буде **основний** виняток).
> 2. При автоматичному закритті ресурсу `close()` кидає `ExceptionB`.
> 3. `ExceptionB` **не перекриває** `ExceptionA`, а **додається до нього** як "придушений".
>
> Обробивши основний виняток, можна отримати список придушених за допомогою методу `e.getSuppressed()`. Це гарантує, що жодна інформація про помилки не буде втрачена.

#### **📌 Що перевіряють:**

*Дуже глибоке знання `try-with-resources` та механізму придушення, який робить його набагато надійнішим за `try-finally`.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви проектуєте API. Як би ви вирішили, чи повинен ваш метод кидати `checked` виняток, чи повертати `Optional<T>`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це важливе дизайнерське рішення, і вибір залежить від **семантики** "невдачі".
>
> **Обирайте `Optional<T>`, коли:**
>
> * **Відсутність значення є очікуваним, нормальним результатом**, а не помилкою.
> * Метод виконує **пошук або запит**, який може просто нічого не знайти.
> * Ви хочете надати клієнту гнучкий, **функціональний API** для обробки "порожнього" результату (`map`, `ifPresent`, `orElse` тощо).
>
> **Приклад:** `userRepository.findById(id)` — відсутність користувача є нормальним сценарієм, а не помилкою.
>
> **Кидайте `checked` виняток, коли:**
>
> * Ситуація є справді **винятковою**, і вона **перешкоджає нормальному виконанню** операції.
> * Ви хочете змусити клієнта **обов'язково звернути увагу** на цю проблему і обробити її.
> * Вам потрібно передати **багато контексту** про помилку (що неможливо з `Optional`).
>
> **Приклад:** `paymentGateway.processPayment(amount)` кидає `InsufficientFundsException`. Недостатньо коштів — це **помилковий результат операції**, а не просто "відсутність результату". Цей виняток може нести інформацію про поточний баланс, суму, якої не вистачає, тощо.
>
> **Висновок:** `Optional` — для **"нічого не знайдено"**. `Exception` — для **"щось пішло не так"**. Неправильно використовувати винятки для звичайної логіки керування потоком, і так само неправильно використовувати `Optional` для сигналізації про справжні помилки.

#### **📌 Що перевіряють:**

*Архітектурне мислення та здатність обирати правильний інструмент для моделювання різних типів "невдач" в API.*
