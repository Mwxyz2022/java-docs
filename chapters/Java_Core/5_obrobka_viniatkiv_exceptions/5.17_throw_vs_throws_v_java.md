---
layout: default
title: "throw vs throws в Java"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 17
---

# throw vs throws в Java

* короткий опис: `throw` – це оператор, що використовується в тілі методу для створення та фактичного "кидання" винятку. `throws` – це ключове слово в сигнатурі методу, що оголошує, які `Checked Exceptions` метод може згенерувати та передати далі.

* поняття: `throw`, `throws`.

* приклади: `throw new IllegalArgumentException("Invalid");` (дія), `public void f() throws IOException;` (декларація).

* ключові моменти: `throw` – це дія (кидає один виняток), `throws` – це декларація (оголошує можливі винятки). `throws` використовується лише для `Checked Exceptions`.

* що важливо знати на співбесіді: Чітка різниця, коли що використовувати.

### **✅ Офіційне визначення:**

* **`throw`** — оператор для створення і **"кидання" винятку вручну**.
* **`throws`** — ключове слово в сигнатурі методу, що вказує: **цей метод може згенерувати виняток**.

### **🧠 Простими словами:**

* **`throw`** — це дія: **"кинути помилку тут і зараз"**.
* **`throws`** — це попередження: **"цей метод може кинути помилку пізніше"**.

---

## **Основне**

| Ключове слово | Де використовується |     Для чого     |                     Приклад                      |
|:-------------:|:-------------------:|:----------------:|:------------------------------------------------:|
|  **`throw`**  |    В тілі методу    |  Кидає виняток   | **`throw new IllegalArgumentException("...");`** |
| **`throws`**  | В сигнатурі методу  | Оголошує винятки |     **`public void x() throws IOException`**     |

---

## **Пояснення на пальцях (💡)**

* `throw` — коли хочеш **кинути виняток прямо зараз**:

```java
if ( user == null ) { 
    throw new IllegalArgumentException("Користувач не знайдений");
}
```

* `throws` — коли метод **може кинути виняток, але не обробляє його, а передає "вгору"**:

```java
public void readFile() throws IOException {
    Files.readAllLines(Path.of("file.txt"));
}
```

---

## **Корисні приклади (🧪)**

* **`throw` — кидаємо виняток вручну:**

```java
public void validateAge(int age) {
    if (age < 18) {
        throw new IllegalArgumentException("Недостатньо років");
    }
}
```

* **`throws` — метод попереджає, що може кинути виняток:**

```java
public void loadData() throws IOException {
    BufferedReader reader = new BufferedReader(new FileReader("data.txt"));
}
```

* **Разом: `throws` \+ `throw`**

```java
public void connect() throws IOException {
    throw new IOException("Підключення не вдалося");
}
```

---

## **Порівняння (⚖️)**

|     Параметр     |            `throw`             |                    `throws`                     |
|:----------------:|:------------------------------:|:-----------------------------------------------:|
|  **Що робить?**  |         Кидає виняток          |    Повідомляє, що метод може кинути виняток     |
| **Де пишеться?** |       В середині методу        |               В сигнатурі методу                |
|  **Кількість**   |      Один виняток за раз       |         Можна вказати кілька через кому         |
|   **Приклад**    | **`throw new IOException();`** | **`void x() throws IOException, SQLException`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **`throw`** -> **кидає (action)**
* **`throws`** -> **попереджає (signature)**

або просто:  
**`throw`** \= тут і зараз  
**`throws`** \= попереду можливо

---

## **🟩 Підсумок:**

* **`throw`** — використовується в тілі методу для кидання конкретного винятку
* **`throws`** — використовується в сигнатурі методу для оголошення можливих винятків
* Обидва ключові для контролю винятків і безпечного кодування

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка фундаментальна різниця між ключовими словами `throw` та `throws` в Java?

> #### **💬 Відповідь:**
>
> `throw` та `throws` виконують абсолютно різні, хоч і пов'язані, функції в механізмі обробки винятків.
>
> **`throw` (ДІЯ):**
>
> * Це **оператор**, який використовується для **фактичного "кидання"** екземпляра винятку.
> * Він **перериває** нормальний потік виконання програми в тому місці, де він викликається.
> * Використовується **всередині тіла методу** або конструктора.
> * За один раз може кинути **лише один** об'єкт винятку.
> * **Синтаксис:** `throw new MyException("Повідомлення");`
>
> **`throws` (ОГОЛОШЕННЯ):**
>
> * Це **ключове слово**, яке є частиною **сигнатури (заголовка) методу**.
> * Воно **не кидає** виняток, а лише **декларує (попереджає)**, що цей метод може кинути один або кілька **`checked` винятків**.
> * Це механізм, який **делегує відповідальність** за обробку винятків коду, що викликає цей метод.
> * **Синтаксис:** `public void myMethod() throws IOException, SQLException { ... }`

#### **📝 Аналогія:**

* **`throw`:** Це як гравець, що кидає м'яч на поле. Це сама дія кидка.
* **`throws`:** Це як напис на табличці біля стадіону: "Обережно, звідси можуть вилітати м'ячі!". Це лише попередження.

#### **📌 Що перевіряють:**

*Чітке розмежування між дією (`throw`) та декларацією (`throws`). Це базове питання-фільтр.*

* * *

### **2 ❓ Питання:**

Наведіть простий приклад використання `throw` та `throws` в одному методі.

> #### **💬 Відповідь:**
>
> Звісно. Розглянемо метод валідації, який перевіряє вік користувача.
>
> ```java
> public class UserValidator {
>
>     // 1. Метод ОГОЛОШУЄ 'throws', що він може кинути checked виняток
>     public void validateAge(int age) throws InvalidAgeException {
>
>         if (age < 18) {
>             // 2. Метод фактично КИДАЄ 'throw' екземпляр винятку
>             throw new InvalidAgeException("Користувач занадто молодий. Вік: " + age);
>         }
>         // ... інша логіка
>         System.out.println("Вік валідний.");
>     }
> }
>
> // Наш кастомний checked виняток
> class InvalidAgeException extends Exception {
>     public InvalidAgeException(String message) {
>         super(message);
>     }
> }
> ```
>
> **Як це працює:**
>
> * Метод `validateAge` обіцяє (`throws`), що може виникнути помилка `InvalidAgeException`.
> * Всередині, за певної умови (`age < 18`), він виконує цю обіцянку і кидає (`throw`) новий екземпляр цієї помилки.
> * Код, що викликає `validateAge()`, тепер зобов'язаний обробити `InvalidAgeException` за допомогою `try-catch`.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як ці два ключових слова працюють разом для реалізації обробки `checked` винятків.*

* * *

### **3 ❓ Питання:**

Чи можна використовувати `throw` для кидання `unchecked` винятків (нащадків `RuntimeException`)? Чи потрібно їх оголошувати у `throws`?

> #### **💬 Відповідь:**
>
> **Так, `throw` можна використовувати для кидання абсолютно будь-якого** нащадка `Throwable`, включаючи `RuntimeException` та `Error`.
>
> **Чи потрібно оголошувати їх у `throws`?**
>
> **Ні, не потрібно.** Компілятор **не вимагає** оголошувати `unchecked` винятки (усі нащадки `RuntimeException` та `Error`) у `throws` клаузі.
>
> **Приклад:**
>
> ```java
> public void setUsername(String username) {
>     if (username == null || username.isBlank()) {
>         // Кидаємо unchecked виняток. 'throws' не потрібен.
>         throw new IllegalArgumentException("Ім'я користувача не може бути порожнім");
>     }
>     this.username = username;
> }
> ```
>
> **Чому так?**
>
> Філософія `unchecked` винятків полягає в тому, що вони сигналізують про **програмерські баги**. Такі помилки повинні виправлятися в коді, а не оброблятися. Змушувати оголошувати їх усюди зробило б код надзвичайно зашумленим.

#### **📌 Що перевіряють:**

*Розуміння того, що `throw` універсальний, а `throws` — вибірковий (лише для `checked`)*.

* * *

### **4 ❓ Питання:**

Яка основна відмінність між киданням існуючого винятку та створенням нового?
`catch (IOException e) { throw e; }`
vs
`catch (IOException e) { throw new RuntimeException(e); }`

> #### **💬 Відповідь:**
>
> Це два різних підходи: **повторне кидання (rethrowing)** та **загортання (wrapping)**.
>
> **1. `throw e;` (Повторне кидання):**
>
> * **Що робить:** Кидає **той самий, оригінальний екземпляр** винятку, який був зловлений.
> * **Тип винятку:** Тип залишається **незмінним**. Якщо зловили `IOException`, то і кидаємо `IOException`.
> * **Стек-трейс:** **Зберігається повністю**. Стек-трейс не змінюється, він як і раніше вказує на місце, де виняток виник спочатку.
> * **Коли використовувати:** Коли потрібно виконати якусь дію (наприклад, логування) і дозволити помилці продовжити свій шлях вгору без змін.
>
> **2. `throw new RuntimeException(e);` (Загортання):**
>
> * **Що робить:** Створює **новий виняток** (`RuntimeException`) і "загортає" в нього оригінальний виняток `e` як **причину (`cause`)**.
> * **Тип винятку:** Тип **змінюється**. Тепер це `RuntimeException`.
> * **Стек-трейс:** Формується **ланцюжок винятків**. Стек-трейс буде показувати спочатку місце, де кинули `RuntimeException`, а під ним — секцію `Caused by:` з оригінальним стек-трейсом `IOException`.
> * **Коли використовувати:** Коли потрібно **абстрагуватися** від низькорівневих деталей або перетворити `checked` виняток в `unchecked`.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між rethrowing та wrapping та їхніх наслідків для стек-трейсу та типу винятку.*

* * *

### **5 ❓ Питання:**

Що станеться, якщо спробувати кинути `null`? `throw null;`

> #### **💬 Відповідь:**
>
> Це призведе до викидання **`NullPointerException`**, згенерованого самою JVM.
>
> **Пояснення:**
>
> * Оператор `throw` очікує отримати екземпляр об'єкта, що успадковується від `Throwable`.
> * Коли ви передаєте йому `null`, JVM намагається розіменувати це посилання, щоб отримати об'єкт винятку для подальшої обробки.
> * Оскільки посилання `null`, ця спроба розіменування негайно призводить до `NullPointerException`.
>
> Важливо, що це не те ж саме, що `throw new NullPointerException()`. У цьому випадку NPE виникає через саму некоректну спробу "кинути ніщо". Стек-трейс буде вказувати саме на рядок `throw null;`.

#### **📌 Що перевіряють:**

*Знання граничних випадків та того, як JVM реагує на некоректне використання мовних конструкцій.*

* * *

### **6 ❓ Питання:**

Як `throws` клауза працює при перевизначенні методів? ⭐️

> #### **💬 Відповідь:**
>
> Існує строге правило щодо `checked` винятків, яке гарантує дотримання **принципу підстановки Лісков**.
>
> **Правило:** Перевизначений метод у дочірньому класі **не може оголошувати `checked` виняток, який є ширшим або новим** порівняно з тим, що оголошено в батьківському методі.
>
> **Дочірній метод може:**
>
> * Не оголошувати жодних `checked` винятків.
> * Оголошувати той самий `checked` виняток.
> * Оголошувати **підклас** `checked` винятку.
>
> **Приклад:**
>
> ```java
> class Parent {
>     public void doWork() throws java.io.IOException {}
> }
> class Child extends Parent {
>     // OK: звужуємо до підкласу
>     @Override
>     public void doWork() throws java.io.FileNotFoundException {}
> }
>
> class BadChild extends Parent {
>     // ПОМИЛКА: розширюємо контракт, Exception - батько для IOException
>     // @Override
>     // public void doWork() throws Exception {}
> }
> ```
>
> Це гарантує, що клієнт, написаний для роботи з `Parent`, не "зламається", якщо йому передати об'єкт `Child`.

#### **📌 Що перевіряють:**

*Знання правил перевизначення методів, що стосуються винятків, та їхній зв'язок з LSP.*

* * *

### **7 ❓ Питання:**

Чому цей код не скомпілюється? Як його виправити?

```java
public void process() {
    throw new java.io.IOException();
}
```

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється, тому що **`IOException` — це `checked` виняток**.
>
> Згідно з правилами Java, якщо метод кидає `checked` виняток, він **зобов'язаний** повідомити про це.
>
> Компілятор видасть помилку: `unreported exception java.io.IOException; must be caught or declared to be thrown`.
>
> **Як виправити (два способи):**
>
> 1. **Обробка винятку за допомогою `try-catch`:**
>
>     ```java
>     public void process() {
>         try {
>             throw new java.io.IOException();
>         } catch (java.io.IOException e) {
>             // ... обробити виняток ...
>         }
>     }
>     ```
>
> 2. **Оголошення винятку за допомогою `throws`:**
>
>     ```java
>     public void process() throws java.io.IOException {
>         throw new java.io.IOException();
>     }
>     ```
>
> Вибір залежить від того, чи може метод `process()` сам впоратися з помилкою, чи відповідальність за це лежить на тому, хто його викликає.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як компілятор змушує обробляти `checked` винятки.*

* * *

### **8 ❓ Питання:**

Чи можна кидати (`throw`) `Error`? Якщо так, чи є це гарною практикою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Технічно так, `Error` можна кинути**, оскільки він є нащадком `Throwable`.
> `throw new OutOfMemoryError();` — це валідний код.
>
> **Чи є це гарною практикою?**
>
> **Ні, категорично ні.** Це дуже погана практика.
>
> **Причина:**
> Класи, що успадковують `Error`, зарезервовані для сигналізації про **фатальні, невідворотні помилки в роботі JVM**. Це не ті помилки, якими повинен керувати код додатка.
>
> Кидаючи `Error` вручну, ви:
>
> * **Вводите в оману** інших розробників та системи моніторингу. Вони будуть думати, що проблема в самій JVM, а не у вашій бізнес-логіці.
> * **Сигналізуєте про неправильний тип проблеми.** Наприклад, `AssertionError` має кидати сам механізм `assert`, а не ви вручну.
>
> Якщо у вашому коді виникла помилка, яка є "фатальною" для вашого додатка, найкращою практикою є кинути відповідний **`RuntimeException`**.

#### **📌 Що перевіряють:**

*Розуміння семантичного призначення `Error` та того, чому не слід зловживати цим механізмом.*

* * *

### **9 ❓ Питання:**

Як `throws` клауза взаємодіє з перевантаженими методами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `throws` клауза **не є частиною сигнатури методу** для цілей перевантаження.
>
> Це означає, що ви **не можете** мати два методи, які відрізняються лише своїми `throws` клаузами.
>
> **Приклад помилки компіляції:**
>
> ```java
> public class Test {
>     // ПОМИЛКА: 'doWork()' is already defined in 'Test'
>     public void doWork() throws IOException {}
>     public void doWork() throws SQLException {}
> }
> ```
>
> Компілятор вважатиме ці два методи однаковими, оскільки їхня назва та список параметрів збігаються.
>
> Список винятків у `throws` є лише **додатковою інформацією (контрактом)** про те, що може кинути метод, а не частиною його унікального ідентифікатора.

#### **📌 Що перевіряють:**

*Точне знання того, з чого складається сигнатура методу з точки зору перевантаження.*

* * *

### **10 ❓ Питання:**

Чому `throw` є виразом, який може бути використаний у тернарному операторі? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже цікавий аспект специфікації мови Java. Хоча `throw` перериває потік виконання, він вважається **виразом, тип якого є "ніщо" (`nothing`)**. Цей спеціальний тип може бути приведений до **будь-якого іншого типу**.
>
> Це дозволяє використовувати `throw` у місцях, де очікується значення, наприклад, у тернарному операторі або як тіло лямбда-виразу.
>
> **Приклад з тернарним оператором:**
>
> ```java
> public String getProperty(String key) {
>     String value = properties.get(key);
>     return (value != null) ? value :
>            throw new IllegalArgumentException("Property not found: " + key);
> }
> ```
>
> **Як це працює:**
>
> * Компілятор аналізує тернарний оператор.
> * Перша гілка повертає `String`.
> * Друга гілка "повертає" виняток. Оскільки тип виразу `throw` може бути приведений до будь-якого, компілятор вважає його сумісним з `String`.
> * Це дозволяє уникнути написання `if-else` і зробити код більш лаконічним.
>
> Така ж логіка працює і для лямбда-виразів:
> `() -> throw new RuntimeException()` — є валідною реалізацією `Supplier<String>`.

#### **📌 Що перевіряють:**

*Дуже глибоке знання специфікації мови Java (JLS). Це питання на рівень Senior.*

* * *

### **11 ❓ Питання:**

Чи можна кидати (`throw`) анонімний клас винятку? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.**
>
> Ви можете створити екземпляр анонімного внутрішнього класу, що успадковує будь-який клас винятку, і кинути його.
>
> **Приклад:**
>
> ```java
> public void process() {
>     throw new RuntimeException("Помилка з додатковим контекстом") {
>         // Це тіло анонімного класу
>         @Override
>         public String getMessage() {
>             return "АНІНІМНА ПОМИЛКА: " + super.getMessage();
>         }
>     };
> }
> ```
>
> **Навіщо це може бути потрібно:**
>
> * Для швидкого створення "одноразового" винятку з унікальною поведінкою, не створюючи для цього окремий `.java` файл.
> * Щоб додати певну логіку або поля до стандартного винятку "на льоту".
>
> **Недоліки:**
>
> * Такий виняток **не можна буде зловити за його унікальним типом**, оскільки він анонімний. Його можна зловити лише за типом суперкласу (у нашому прикладі, `RuntimeException`).
> * Код стає менш читабельним.
>
> На практиці це використовується дуже рідко, оскільки створення власного іменованого класу винятку зазвичай є кращим рішенням.

#### **📌 Що перевіряють:**

*Знання гнучких, хоча і не завжди поширених, можливостей мови.*

* * *

### **12 ❓ Питання:**

Що станеться, якщо `catch` блок перехопить `checked` виняток і повторно кине його (`throw e`)? Чи повинен метод, в якому це відбувається, оголошувати цей виняток у `throws`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, повинен.**
>
> `catch` блок "ловить" виняток і дає вам можливість його обробити. Але якщо ви **повторно кидаєте** той самий `checked` виняток, ви, по суті, відмовляєтеся від його повної обробки на цьому рівні.
>
> **Приклад:**
>
> ```java
> // Цей код не скомпілюється без throws IOException
> public void processFile() throws IOException {
>     try {
>         // ...
>     } catch (IOException e) {
>         log.error("Помилка!", e); // Виконали якусь дію
>         throw e; // Але вирішили прокинути помилку далі
>     }
> }
> ```
>
> Компілятор бачить, що з `catch` блоку може "вилетіти" `checked` виняток `IOException`, і вимагає, щоб метод `processFile` оголосив це у своїй сигнатурі.
>
> Це логічно, оскільки `throw e` знову робить виняток "необробленим" у межах цього методу, і відповідальність за його обробку переходить до коду, що викликає `processFile`.

#### **📌 Що перевіряють:**

*Точне розуміння того, що "оброблений" виняток — це той, який не "вилітає" з `try-catch` конструкції. Повторне кидання робить його знову "необробленим".*

* * *

### **13 ❓ Питання:**

Яким чином `throws` може ускладнити використання лямбда-виразів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `throws` для `checked` винятків створює проблему, оскільки більшість стандартних **функціональних інтерфейсів** (`Function`, `Consumer`, `Predicate` тощо) **не оголошують жодних `checked` винятків** у своїх абстрактних методах.
>
> **Проблема:**
>
> Це означає, що лямбда-вираз, який ви реалізуєте, **не може кидати `checked` виняток**.
>
> ```java
> public String readFile(String path) throws IOException {
>     return Files.readString(Path.of(path));
> }
>
> List<String> paths = ...;
> // ПОМИЛКА КОМПІЛЯЦІЇ: не можна використовувати посилання на метод,
> // який кидає checked виняток, з лямбдою, яка цього не робить
> // paths.stream().map(this::readFile).collect(Collectors.toList());
> ```
>
> `map` очікує `Function<String, String>`, а його метод `apply` не має `throws IOException`.
>
> **Рішення:**
> Потрібно "обгортати" `checked` виняток в `unchecked` всередині лямбди, що робить код більш багатослівним і менш елегантним, ніж він міг би бути.

#### **📌 Що перевіряють:**

*Розуміння однієї з основних проблем інтеграції старого "checked" світу з новим функціональним стилем в Java 8.*

* * *

### **14 ❓ Питання:**

Чи можна використовувати `throw` без створення нового екземпляра винятку, наприклад: `throw new MyException;`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, дужки `()` обов'язкові.** `throw new MyException();`
>
> **Пояснення:**
>
> * Оператор `throw` очікує як операнд **екземпляр об'єкта**, що успадковується від `Throwable`.
> * Вираз `new MyException` сам по собі ще не є повноцінним викликом, що створює об'єкт.
> * `new MyException()` — це вираз, що **викликає конструктор** і створює екземпляр.
>
> Спроба написати `throw new MyException;` призведе до **помилки компіляції** через неповний синтаксис.
>
> Це питання перевіряє уважність до деталей синтаксису мови.

#### **📌 Що перевіряють:**

*Точність і уважність до синтаксису Java.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви проектуєте API. Як би ви вирішили: кидати з методу `checked` виняток через `throws` чи `unchecked` через `throw`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це одне з ключових архітектурних рішень. Мій вибір залежав би від **природи помилки** та **очікуваної реакції клієнта API**.
>
> **Я б використав `throws` (для `checked` винятку), якщо:**
>
> * Помилка є **очікуваною, можливою і не є помилкою програміста**. Наприклад, `DocumentNotFoundException`.
> * Я очікую, що **клієнт може і повинен осмислено відреагувати** на цю ситуацію (наприклад, показати повідомлення "документ не знайдено" або запропонувати створити новий).
> * Виняток є частиною **стабільного публічного контракту** мого API.
>
> **Я б використав `throw` (для `unchecked` винятку), якщо:**
>
> * Помилка вказує на **неправильне використання API** з боку клієнта (порушення передумов). Наприклад, `throw new IllegalArgumentException("ID не може бути null")`. Клієнт не повинен ловити це, а повинен виправити свій код, щоб не передавати `null`.
> * Помилка свідчить про **внутрішній збій** у моєму сервісі, від якого клієнт не може оговтатися (наприклад, "загортання" `SQLException`). Для клієнта це несподівана проблема, і він може лише показати загальне повідомлення про помилку.
>
> **Основна ідея:** `throws` — для **контрольованого потоку бізнес-помилок**, `throw` (unchecked) — для **сигналізації про баги** або **невідворотні збої**.

#### **📌 Що перевіряють:**

*Здатність до проектування API. Кандидат повинен вміти приймати обґрунтовані рішення, які впливають на користувачів його коду.*
