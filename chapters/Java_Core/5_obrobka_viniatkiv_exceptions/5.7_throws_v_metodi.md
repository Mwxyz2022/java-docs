---
layout: default
title: "throws в методі"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 7
---

# throws в методі

* короткий опис: Ключове слово `throws` у сигнатурі методу вказує, що метод може викинути один або кілька `Checked Exceptions`, але не обробляє їх сам, а делегує обробку викликаючому коду.

* поняття: `throws`, сигнатура методу.

* приклади: `public void readFile() throws IOException`.

* ключові моменти: Використовується для `Checked Exceptions`, передає відповідальність за обробку далі по стеку викликів.

* що важливо знати на співбесіді: Різниця між `throw` та `throws`, коли використовувати `throws`.

### **✅ Офіційне визначення:**

**`throws`** — це ключове слово, яке вказує, що метод може викинути виняток, але не обробляє його сам, а передає далі (у стек викликів).

### **🧠 Простими словами:**

Метод каже: “У мене тут може статися помилка, я її не ловлю — розбирайся ти, хто мене викликає.”

---

## **Як зазначити винятки, які може кинути метод?**

### **✔️ Синтаксис:**

```java
public void метод() throws ExceptionType1, ExceptionType2 {
    // код, який може кидати винятки
}
```

### **✔️ Приклад:**

```java
public void readFile() throws IOException {
    FileReader reader = new FileReader("test.txt");
    reader.read();
    reader.close();
}
```

📌 У цьому методі не обробляється **`IOException`**, але ми попереджаємо компілятор, що така помилка може виникнути — тому метод, який викликає **`readFile()`**, повинен або зловити виняток, або передати далі.

---

## **Коли варто використовувати throws, а не try-catch? (💡)**

| Ситуація | Що краще? | Чому |
| :---: | :---: | :---: |
| Метод у бібліотеці, який викликають інші | **`throws`** | Обробку залишаємо клієнту (той, хто викликає) |
| Метод виконує маленьку дію з ризиком | **`throws`** | Код чистіший, відповідальність передається |
| В **`main()`** або в точці входу | **`try-catch`** | Виняток треба показати або обробити тут |
| Якщо виняток потрібно обробити одразу | **`try-catch`** | Наприклад, щоб вивести повідомлення |

---

## **Корисні приклади (🧪)**

```java
import java.io.*;

public class Example {

    // метод передає виняток далі
    public static void loadFile() throws IOException {
        FileReader reader = new FileReader("file.txt");
        reader.read();
        reader.close();
    }

    public static void main(String[] args) {
        try {
            loadFile();
        } catch (IOException e) {
            System.out.println("Помилка при читанні файлу: " + e.getMessage());
        }
    }
}
```

---

## **🧠 Мнемоніка для запам’ятовування:**

* **`throw`** -> кидаю
* **`throws`** -> попереджаю

Або:  
**`throw`** \= кинути виняток  
**`throws`** \= я можу кинути, але ловити не буду

---

## **🟩 Підсумок:**

* **`throws`** каже: “Я не обробляю — ти ловиш.”
* Застосовується для передачі checked exceptions.
* Дозволяє залишити метод чистим і відповідальним.
* Але не знімає зобов’язання обробити виняток десь далі.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке ключове слово `throws` і яка його основна мета в сигнатурі методу?

> #### **💬 Відповідь:**
>
> Ключове слово **`throws`** — це частина сигнатури методу, яка використовується для **оголошення** типів **`checked` винятків**, які цей метод може потенційно кинути під час свого виконання, але не обробляє їх самостійно.
>
> **Основна мета:**
>
> * **Делегування відповідальності:** Метод "попереджає" клієнтський код, який його викликає, що певні помилки можуть виникнути, і відповідальність за їхню обробку перекладається на цей клієнтський код.
> * **Забезпечення перевірки компілятором:** Це є частиною механізму `checked` винятків. Компілятор бачить `throws` клаузу і змушує код, що викликає цей метод, або обробити оголошені винятки за допомогою `try-catch`, або також прокинути їх далі.
>
> **Приклад:**
>
> ```java
> import java.io.IOException;
>
> public void readFile(String path) throws IOException {
>     // ... код, який працює з файлом і може кинути IOException ...
> }
> ```
>
> Ця сигнатура каже: "Я можу кинути `IOException`, і я не буду його тут ловити. Якщо ти мене викликаєш, будь готовий його обробити".

#### **📝 Аналогія:**

`throws` — це як попереджувальний знак на дорозі: "Обережно, можливий обвал каміння!". Ви можете проігнорувати його і ризикнути (що неможливо в Java для `checked` винятків), або підготуватися — одягнути каску (`try-catch`) або обрати інший маршрут (обробити помилку).

#### **📌 Що перевіряють:**

*Базове розуміння призначення `throws` як механізму делегування обробки `checked` винятків.*

* * *

### **2 ❓ Питання:**

У чому фундаментальна різниця між `throw` та `throws`?

> #### **💬 Відповідь:**
>
> Це два абсолютно різних ключових слова, які часто плутають початківці.
>
> **`throw` (ключове слово для дії):**
>
> * **Що робить:** **Явно кидає** екземпляр винятку в конкретному місці коду. Це дія, що перериває нормальний потік виконання.
> * **Де використовується:** Всередині **тіла методу** або конструктора.
> * **Синтаксис:** `throw new MyException("Повідомлення");`
> * **Приклад:**
>
>     ```java
>     if (age < 0) {
>         throw new IllegalArgumentException("Вік не може бути від'ємним");
>     }
>     ```
>
> **`throws` (ключове слово для оголошення):**
>
> * **Що робить:** **Оголошує (попереджає)**, які `checked` винятки можуть бути кинуті цим методом, але не обробляються ним.
>
> * **Де використовується:** У **сигнатурі (заголовку) методу**.
> * **Синтаксис:** `public void myMethod() throws IOException, SQLException { ... }`
> * **Приклад:**
>     `public void saveFile() throws IOException { ... }`
>
> **Коротко:** `throw` — це **"кинути м'яч"**, а `throws` — це **"попередити, що м'яч може прилетіти"**.

#### **📌 Що перевіряють:**

*Точне знання термінології та синтаксису. Це класичне питання на розрізнення понять.*

* * *

### **3 ❓ Питання:**

Коли доцільно використовувати `throws` замість `try-catch`?

> #### **💬 Відповідь:**
>
> Вибір між обробкою (`try-catch`) та прокиданням (`throws`) — це архітектурне рішення.
>
> **Доцільно використовувати `throws`, коли:**
>
> 1. **Метод не має достатньо контексту для обробки помилки:** Низькорівневий метод, що працює з файлами, не знає, чи повинен додаток завершити роботу, чи спробувати інший файл, чи повідомити користувача. Це рішення має приймати код вищого рівня, який має більше інформації про бізнес-логіку.
>
> 2. **Ви розробляєте бібліотеку або API:** Надайте клієнтам вашого API свободу самим вирішувати, як реагувати на помилки. "Жорстка" обробка всередині бібліотеки може бути негнучкою.
>
> 3. **Ви хочете зберегти код методу чистим та сфокусованим на "щасливому шляху":** Винесення обробки помилок на вищий рівень дозволяє методу зосередитися на своїй основній відповідальності.
>
> **Коли краще `try-catch`:**
>
> * Якщо ви **можете коректно відреагувати** на помилку на поточному рівні.
> * На **межі архітектурних шарів** або на найвищому рівні додатка (наприклад, у контролері, щоб повернути HTTP-статус помилки).
> * Коли ви хочете "загорнути" `checked` виняток в `unchecked`, щоб не "забруднювати" сигнатури.

#### **📌 Що перевіряють:**

*Розуміння того, як проектувати стратегію обробки помилок та розподіляти відповідальність між різними частинами програми.*

* * *

### **4 ❓ Питання:**

Чи потрібно оголошувати `RuntimeException`-и (неперевірювані винятки) у `throws` клаузі? Чому?

> #### **💬 Відповідь:**
>
> **Ні, не потрібно, і зазвичай це вважається поганою практикою.**
>
> * **Технічно це можливо:** `public void myMethod() throws NullPointerException { ... }` — такий код скомпілюється.
>
> * **Чому це погано:**
>     1. **Це не має ефекту:** Компілятор **ігнорує** `unchecked` винятки у `throws` клаузі. Він **не змусить** клієнтський код обробляти їх.
>     2. **Забруднення API:** Це додає "шум" до сигнатури методу без будь-якої практичної користі. Це може ввести в оману інших розробників, змушуючи їх думати, що цей виняток потрібно ловити.
>
> **Як правильно:**
>
> * `RuntimeException`'и (та їх нащадки) **не оголошуються** у `throws` клаузі.
> * Якщо метод може кинути такий виняток за певних умов, про це слід чітко написати в **документації методу (Javadoc)**, використовуючи тег `@throws`.
>
> ```java
> /**
>  * @param user a non-null user object
>  * @throws IllegalArgumentException if user's name is blank
>  */
> public void processUser(User user) { ... }
> ```
>
> `throws` клауза призначена виключно для **`checked` винятків**.

#### **📌 Що перевіряють:**

*Чітке розуміння різниці між `checked` та `unchecked` та правил роботи компілятора з кожним з них.*

* * *

### **5 ❓ Питання:**

Які правила існують для `throws` клаузи при перевизначенні методів? ⭐️

> #### **💬 Відповідь:**
>
> При перевизначенні методу існують строгі правила щодо `checked` винятків, які може оголошувати дочірній метод, щоб не "зламати" контракт батька.
>
> **Правило:** Перевизначений метод **не може розширювати** список `checked` винятків.
>
> **Дочірній метод може:**
>
> 1. **Не кидати** жодних `checked` винятків взагалі.
> 2. Кидати **той самий** `checked` виняток, що й батьківський.
> 3. Кидати **підклас** `checked` винятку, оголошеного в батька.
>
> **Чого він не може:**
>
> * Кидати `checked` виняток, який є **ширшим (суперкласом)** за батьківський.
> * Кидати **абсолютно новий `checked` виняток**, якого не було в сигнатурі батька.
>
> **Приклад:**
>
> ```java
> class Parent {
>     void process() throws java.io.IOException {}
> }
> class Child extends Parent {
>     // OK: звужуємо до підкласу
>     @Override void process() throws java.io.FileNotFoundException {}
> }
> ```
>
> **На `unchecked` винятки ці правила не поширюються.**

#### **📌 Що перевіряють:**

*Знання правил наслідування та їх зв'язок з обробкою винятків. Розуміння принципу підстановки Лісков у цьому контексті.*

* * *

### **6 ❓ Питання:**

Чи можна зловити виняток, який не оголошено у `throws` клаузі?

> #### **💬 Відповідь:**
>
> **Так, можна.** Це стосується **`unchecked` винятків** (`RuntimeException` та `Error`).
>
> * `throws` клауза є обов'язковою **лише для `checked` винятків**.
> * `unchecked` винятки можуть виникнути практично в будь-якому місці коду (`NullPointerException`, `ArithmeticException`), і ви не зобов'язані їх оголошувати.
>
> Тому, якщо метод не має `throws` клаузи, це не означає, що він не може кинути виняток. Це лише означає, що він **не кидає жодного `checked` винятку**.
>
> **Приклад:**
>
> ```java
> // Цей метод не має 'throws', але може кинути RuntimeException
> public void processName(String name) {
>     System.out.println(name.length()); // Може кинути NullPointerException
> }
>
> public void clientMethod() {
>     try {
>         processName(null);
>     } catch (NullPointerException e) {
>         // Ми успішно зловили unchecked виняток
>         System.out.println("Помилка: ім'я було null");
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння того, що `throws` стосується лише `checked` винятків, а `unchecked` можуть виникнути будь-де.*

* * *

### **7 ❓ Питання:**

Чому цей код не скомпілюється? Як його виправити?

```java
public void process() {
    new java.io.File("test.txt").createNewFile();
}
```

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється, тому що метод `java.io.File.createNewFile()` **оголошує**, що він може кинути **`checked` виняток — `java.io.IOException`**.
>
> Згідно з правилами Java, оскільки ми викликаємо метод, що може кинути `checked` виняток, ми **зобов'язані** його обробити.
>
> **Помилка компілятора буде:** `unreported exception java.io.IOException; must be caught or declared to be thrown`.
>
> **Як виправити (два способи):**
>
> **1. Обробити за допомогою `try-catch`:**
>
> ```java
> public void process() {
>     try {
>         new java.io.File("test.txt").createNewFile();
>     } catch (java.io.IOException e) {
>         e.printStackTrace(); // або інша логіка обробки
>     }
> }
> ```
>
> **2. Прокинути виняток далі за допомогою `throws`:**
>
> ```java
> public void process() throws java.io.IOException {
>     new java.io.File("test.txt").createNewFile();
> }
> ```
>
> Вибір залежить від того, чи може метод `process` самостійно впоратися з помилкою.

#### **📌 Що перевіряють:**

*Практичні навички роботи з `checked` винятками та знання API стандартної бібліотеки.*

* * *

### **8 ❓ Питання:**

Що таке "checked exception hell" і як `throws` клауза сприяє його виникненню? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"Checked exception hell"** — це ситуація, коли розробники змушені **"протягувати" (`throws`)** один або кілька `checked` винятків через **багато шарів архітектури**, що робить код багатослівним, крихким та погано підтримуваним.
>
> **Як `throws` сприяє цьому:**
>
> 1. Низькорівневий метод (наприклад, у DAO шарі) оголошує `throws SQLException`.
> 2. Метод у репозиторії, що його викликає, тепер теж змушений оголошувати `throws SQLException`.
> 3. Метод у сервісному шарі, що викликає репозиторій, теж змушений оголошувати `throws SQLException`.
> 4. І так далі, аж до контролера.
>
> **Проблеми:**
>
> * **Забруднення API:** Сигнатури всіх методів стають перевантаженими деталями реалізації низького рівня (`SQLException`).
> * **Порушення абстракції:** Сервісний шар не повинен знати, що під ним використовується саме SQL.
> * **Крихкість:** Якщо низькорівневий метод додасть ще один `checked` виняток, це викличе каскад змін угору по всьому стеку викликів.
>
> **Як цього уникнути:**
> На межі шарів слід **"загортати" (wrap)** `checked` винятки у власні, специфічні для домену, `unchecked` винятки (`RuntimeException`), розриваючи цей ланцюжок.

#### **📌 Що перевіряють:**

*Розуміння недоліків `checked` винятків та архітектурних патернів для їхньої правильної обробки.*

* * *

### **9 ❓ Питання:**

Чи можна оголосити `throws Exception` або `throws Throwable`? Чому це вважається поганою практикою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Технічно так, це можливо.** Але це є **дуже поганою практикою**.
>
> **Чому це погано:**
>
> 1. **Втрата специфічності:** `throws Exception` — це "лінивий" спосіб позбутися перевірки компілятора. Він говорить клієнтському коду: "Мій метод може кинути... будь-яку перевірювану помилку. Вгадай, яку саме, і обробляй все підряд".
>
> 2. **Ускладнення для клієнта:** Код, що викликає такий метод, змушений ловити загальний `Exception`, втрачаючи можливість реагувати по-різному на різні типи помилок (наприклад, на `FileNotFoundException` та `SocketTimeoutException`).
>
> 3. **Приховування реальних проблем:** Замість того, щоб чітко вказати на конкретні ризики, розробник просто "відмахується" від них.
>
> 4. **`throws Throwable` — ще гірше.** Це означає, що метод може кинути не тільки будь-який `Exception`, а й `Error`. Це приховує потенційні фатальні збої JVM.
>
> **Найкраща практика:**
> Завжди оголошуйте **найбільш специфічні** `checked` винятки, які ваш метод може кинути. Це робить ваш API чесним, прозорим та зручним для використання.

#### **📌 Що перевіряють:**

*Знання найкращих практик проектування API та вміння писати чистий, самодокументований код.*

* * *

### **10 ❓ Питання:**

Поясніть, що станеться, якщо `main` метод оголошено з `throws Exception`.

> #### **💬 Відповідь:**
>
> Це **валідна** з точки зору компілятора сигнатура:
> `public static void main(String[] args) throws Exception`
>
> **Що станеться при виконанні:**
>
> * Якщо всередині `main` буде кинуто будь-який `checked` або `unchecked` виняток, він **не буде оброблений**.
> * Відповідальність за його обробку перейде до **обробника за замовчуванням JVM**.
> * JVM просто **зупинить програму** і **виведе стек-трейс** винятку в стандартний потік помилок (зазвичай, у консоль).
>
> По суті, такий `throws Exception` у `main` є просто скороченим способом **уникнути написання `try-catch`** всередині `main`. Це часто використовується в:
>
> * Невеликих утилітах або скриптах.
> * Демонстраційному коді.
> * На змаганнях з програмування, де важлива швидкість написання, а не надійна обробка помилок.
>
> Для реальних продакшн-додатків краще мати централізований `try-catch` у `main`, щоб залогувати помилку більш осмислено перед завершенням.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як працює обробник винятків за замовчуванням у JVM.*

* * *

### **11 ❓ Питання:**

Як `throws` клауза взаємодіє з функціональними інтерфейсами, такими як `Runnable`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це є джерелом частих проблем при роботі з лямбдами.
>
> **Проблема:**
>
> Стандартні функціональні інтерфейси, як-от `Runnable`, **не оголошують `checked` винятки** у своїх абстрактних методах.
> `public interface Runnable { void run(); }`
>
> **Наслідок:**
> Лямбда-вираз або посилання на метод, які ви передаєте в якості реалізації `Runnable`, **не можуть кидати `checked` винятки**.
>
> **Приклад помилки:**
>
> ```java
> Runnable r = () -> {
>     Thread.sleep(1000); // ПОМИЛКА: Unhandled exception: java.lang.InterruptedException
> };
> ```
>
> `Thread.sleep()` кидає `InterruptedException` (checked), а сигнатура `run()` не має `throws`.
>
> **Як вирішити:**
>
> 1. **Обгорнути в `try-catch`:**
>
>     ```java
>     Runnable r = () -> {
>         try {
>             Thread.sleep(1000);
>         } catch (InterruptedException e) {
>             // "Загортаємо" в unchecked або обробляємо інакше
>             Thread.currentThread().interrupt(); // Правильна обробка
>             throw new RuntimeException(e);
>         }
>     };
>     ```
>
> 2. **Створити власний "кидаючий" інтерфейс:**
>     Це підхід для власних API, а не для `Thread`.

#### **📌 Що перевіряють:**

*Знання обмежень стандартних функціональних інтерфейсів та вміння працювати з `checked` винятками у функціональному стилі.*

* * *

### **12 ❓ Питання:**

Розгляньте такий код. Чи скомпілюється він? Чому?
`try { throw new Exception(); } catch (RuntimeException e) {}`
⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, цей код скомпілюється**, але він демонструє цікавий випадок обробки.
>
> **Пояснення:**
>
> * `throw new Exception();` — тут ми кидаємо **`checked`** виняток `Exception`.
> * `catch (RuntimeException e)` — цей блок ловить лише **`unchecked`** винятки, що є нащадками `RuntimeException`.
> * `Exception` **не є** нащадком `RuntimeException`, навпаки, `RuntimeException` є нащадком `Exception`.
>
> Тому кинутий `Exception` **не буде перехоплений** цим `catch` блоком.
>
> **Чому ж код компілюється?**
>
> * Виняток, що кидається **всередині `try` блоку**, вважається "обробленим", якщо для нього є **хоча б один потенційно відповідний `catch` блок**.
> * Компілятор тут "заплутаний". Це вважається валідною конструкцією, хоча логічно зрозуміло, що `catch` ніколи не виконається.
> * Оскільки виняток не буде перехоплено, він буде **прокинутий далі** з методу.
>
> **Фінальна вимога:**
> Метод, в якому знаходиться цей `try-catch`, **повинен мати `throws Exception` у своїй сигнатурі**, інакше виникне помилка "unreported exception".
>
> ```java
> void myMethod() throws Exception { // Ця клауза обов'язкова
>     try {
>         throw new Exception();
>     } catch (RuntimeException e) {
>         // Цей блок ніколи не буде виконаний
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Експертне розуміння правил обробки винятків. Кандидат повинен зрозуміти, що виняток не буде перехоплений і його треба прокидати далі.*

* * *

### **13 ❓ Питання:**

Поясніть, що таке `ExceptionInInitializerError`, і чому він є `Error`, а не `Exception`. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **`ExceptionInInitializerError`** — це `Error`, який кидається JVM, коли під час виконання **статичного блоку ініціалізації (`static {}`)** або ініціалізації **статичного поля** виникає неперехоплений виняток.
>
> **Чому це `Error`, а не `Exception`:**
>
> Це вважається **фатальною, невідворотною помилкою** ініціалізації класу.
>
> * Статична ініціалізація виконується лише один раз. Якщо вона провалилася, клас залишається у **"зламаному", неініціалізованому стані**.
> * Немає ніякого надійного способу "повторити" ініціализацію або "виправити" клас під час виконання.
> * Подальше використання цього класу є небезпечним і, швидше за все, призведе до непередбачуваної поведінки.
>
> Роблячи його `Error`, JVM сигналізує, що це **серйозний збій на рівні завантаження класу**, від якого програма не повинна намагатися оговтатися.
>
> Будь-яка наступна спроба доступу до цього класу призведе до `NoClassDefFoundError`.

#### **📌 Що перевіряють:**

*Знання помилок, пов'язаних з життєвим циклом класів, та їхнє місце в ієрархії `Throwable`.*

* * *

### **14 ❓ Питання:**

Що станеться, якщо `main` метод кидає `checked` виняток?
`public static void main(String[] args) throws IOException { ... }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Код з такою сигнатурою є **валідною** з точки зору компілятора.
>
> **Що станеться при виконанні:**
>
> 1. `throws` клауза у `main` методі означає, що якщо всередині `main` виникне `IOException`, то цей виняток **не буде оброблений** у самому методі.
>
> 2. Відповідальність за обробку переходить до **викликаючої сторони**, якою є **JVM**.
>
> 3. Оскільки JVM не має спеціальної логіки для обробки доменних `checked` винятків, її **обробник за замовчуванням** просто:
>     * **Зупиняє** виконання програми.
>     * **Виводить стек-трейс** винятку в консоль (`System.err`).
>
> Отже, з точки зору кінцевого результату, це працює так само, якби з `main` "вилетів" `unchecked` виняток. Програма просто "впаде".
>
> Це зручно для написання швидких скриптів або в освітніх цілях, щоб не писати `try-catch` у `main`, але для надійних додатків краще мати централізовану обробку помилок перед завершенням програми.

#### **📌 Що перевіряють:**

*Практичне розуміння того, хто є "клієнтом" для `main` методу і як JVM обробляє винятки "останньої інстанції".*

* * *

### **15 ❓ Питання:**

Поясніть, що таке патерн "загортання `checked` винятків у `unchecked`" і наведіть приклад коду, де це виправдано. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це поширений архітектурний патерн, який дозволяє уникнути "checked exception hell" у багатошарових додатках.
>
> **Ідея:**
> На нижніх шарах архітектури (наприклад, шар доступу до даних) ви ловите специфічні `checked` винятки (наприклад, `SQLException`), але замість того, щоб прокидати їх далі через `throws`, ви **"загортаєте"** їх у **кастомний `unchecked` виняток** (`RuntimeException`) і кидаєте його.
>
> **Коли це виправдано:**
>
> * Коли виняток сигналізує про помилку, від якої **клієнтський код вищого рівня все одно не зможе оговтатися**. Наприклад, якщо база даних недоступна, для більшості бізнес-операцій це фатальна помилка, і немає сенсу змушувати кожен сервіс обробляти `SQLException`.
>
> **Приклад реалізації:**
>
> 1. Створюємо власний `unchecked` виняток:
>
>     ```java
>     public class DataAccessException extends RuntimeException {
>         public DataAccessException(String message, Throwable cause) {
>             super(message, cause);
>         }
>     }
>     ```
>
> 2. Використовуємо його в репозиторії:
>
>     ```java
>     public class UserRepository {
>         public Optional<User> findById(long id) {
>             // метод getConnection() кидає SQLException
>             try (Connection conn = dataSource.getConnection()) {
>                 // ...
>                 return Optional.of(...);
>             } catch (SQLException e) {
>                 // Ловимо checked і кидаємо unchecked
>                 throw new DataAccessException("Помилка доступу до даних", e);
>             }
>         }
>     }
>     ```
>
> **Переваги:**
>
> * Сигнатури методів у сервісному шарі (`UserService`) залишаються чистими, без `throws SQLException`.
>
> * Відбувається **абстрагування від деталей реалізації**: сервісний шар не знає, що під ним використовується JDBC.
> * Кінцевий обробник (наприклад, у контролері) може зловити `DataAccessException` і повернути 500-ту помилку.

#### **📌 Що перевіряють:**

*Архітектурне мислення та знання найкращих практик для проектування надійної та гнучкої системи обробки помилок.*
