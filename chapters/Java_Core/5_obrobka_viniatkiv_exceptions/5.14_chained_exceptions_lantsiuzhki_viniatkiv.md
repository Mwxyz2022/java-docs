---
layout: default
title: "Chained exceptions (ланцюжки винятків)"
parent: "Обробка винятків (Exceptions)"
grand_parent: "Java Core"
nav_order: 14
---

# Chained exceptions (ланцюжки винятків)

* короткий опис: Механізм, який дозволяє прикріпити один виняток як першопричину (`cause`) іншого винятку, використовуючи конструктор `new Exception(message, cause)` або метод `Throwable.initCause()`.

* поняття: Chained exceptions, `Throwable.getCause()`.

* приклади: `throw new DataLoadException("Failed", originalIOException);` (де `originalIOException` є причиною).

* ключові моменти: Зберігає повний контекст помилки та історію викликів, допомагає в дебагінгу. `printStackTrace()` відображає весь ланцюг за допомогою "Caused by".

* що важливо знати на співбесіді: Навіщо потрібні, як отримати причину.

### **✅ Офіційне визначення:**

**Chained exceptions** — це механізм, який дозволяє прикріпити одну помилку як причину іншої, використовуючи метод **`Throwable.getCause().`**

### **🧠 Простими словами:**

Це як "вкласти одну помилку в іншу", щоб показати справжню причину збою. Наприклад, метод кидає загальну помилку, але всередині зберігає точну першопричину — типу **`IOException`**

---

## **Основні категорії / типи**

| Компонент | Опис |
| :---: | :---: |
| **Основний виняток** | Те, що бачить користувач/виконавець |
| **Вкладений (cause)** | Реальна причина помилки всередині |

---

## **Пояснення на пальцях (💡)**

* Уяви, що ловиш помилку в методі **`loadData()`**, але вона виникла через **`IOException`** у методі **`readFile()`**.
* Ти не хочеш втратити першопричину, тому вкладаєш **`IOException`** у свій виняток **`DataLoadException`**.
* Це зберігає контекст, не втрачає деталі і допомагає в логах побачити справжню причину — не просто "загальна помилка", а конкретний **`IOException`**.

---

## **Корисні приклади (🧪)**

```java
public class DataLoadException extends Exception {
    public DataLoadException(String message, Throwable cause) {
        super(message, cause); // викликає конструктор з ланцюгом винятків
    }
}

public class ChainedDemo {
    public static void readFile() throws IOException {
        throw new IOException("Файл не знайдено");
    }

    public static void loadData() throws DataLoadException {
        try {
            readFile();
        } catch (IOException e) {
            throw new DataLoadException("Не вдалося завантажити дані", e);
        }
    }

    public static void main(String[] args) {
        try {
            loadData();
        } catch (DataLoadException e) {
            System.out.println("Головна помилка: " + e.getMessage());
            System.out.println("Причина: " + e.getCause().getMessage());
        }
    }
}
```

📥 Вивід:

```java
Головна помилка: Не вдалося завантажити дані  
Причина: Файл не знайдено
```

---

## **Порівняння (⚖️)**

| Без cause | З cause (chained) |
| :---: | :---: |
| **Втрачаєш реальну причину** | Зберігаєш повну історію винятків |
| **Складно дебажити** | Лог бачить і зовнішню, і внутрішню помилку |
| **Тільки одне повідомлення** | Повна картина через **`.getCause()`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Chained" \= на ланцюжку — один виняток тягне іншого за собою\!**

Приклад: **`DataLoadException`** -> бо **`IOException`** -> бо файл зник

---

## **🟩 Підсумок:**

* **Chained exceptions** — спосіб передати одну помилку всередині іншої, не гублячи першопричину.
* Дають змогу не втратити інформацію при повторному киданні винятків.
* Доступ до вкладеної помилки — через **`getCause()`**.
* Полегшують глибоке логування і кращий дебаг реальних причин помилок.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs \- Throwable.getCause()**](https://docs.oracle.com/javase/8/docs/api/java/lang/Throwable.html#getCause--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке ланцюжок винятків (chained exceptions) і яка його головна мета?

> #### **💬 Відповідь:**
>
> **Ланцюжок винятків** — це механізм у Java, який дозволяє одному винятку "нести" в собі інший виняток, що був його **першопричиною (`cause`)**. Це створює логічний ланцюжок, який показує повну історію того, як виникла помилка.
>
> **Головна мета:**
>
> 1. **Не втрачати інформацію про першопричину:** Коли ви ловите низькорівневий виняток (наприклад, `SQLException`) і кидаєте натомість свій власний, високорівневий (`DataAccessException`), ланцюжок дозволяє зберегти оригінальний `SQLException`. Це критично важливо для налагодження.
>
> 2. **Підтримка абстракції:** Дозволяє приховувати низькорівневі деталі реалізації від клієнтського коду, не втрачаючи при цьому важливої діагностичної інформації. Сервісний шар може не знати про `SQLException`, але при аналізі логів розробник побачить повну картину.
>
> Цей механізм є основою патерну **"загортання винятків" (exception wrapping)**.

#### **📝 Аналогія:**

Це як медичний висновок.

* **Високорівневий виняток:** `Пацієнт не може працювати`.
* **Причина (`cause`):** `Бо у нього зламана нога`.
* **Причина причини:** `Бо він впав зі сходів`.
Ланцюжок `не може працювати ← зламана нога ← впав` дає повне розуміння ситуації.

#### **📌 Що перевіряють:**

*Базове розуміння концепції як інструменту для збереження контексту помилки.*

* * *

### **2 ❓ Питання:**

Як технічно реалізувати ланцюжок винятків у Java?

> #### **💬 Відповідь:**
>
> Це робиться дуже просто за допомогою спеціальних **конструкторів**, які є у більшості стандартних класів винятків (і які слід додавати у власні кастомні винятки).
>
> **Основний спосіб:**
> Використовувати конструктор, який приймає повідомлення та `Throwable` причину.
>
> `public Exception(String message, Throwable cause)`
>
> **Приклад:**
>
> ```java
> public void processUserData() {
>     try {
>         // Метод, що кидає низькорівневий виняток
>         readUserDataFromFile();
>     } catch (IOException originalException) {
>         // Ловимо низькорівневий виняток
>         // і кидаємо новий, високорівневий,
>         // передаючи оригінальний як "причину".
>         throw new UserProcessingException("Помилка обробки користувача", originalException);
>     }
> }
> ```
>
> Тепер об'єкт `UserProcessingException` "несе" всередині себе `IOException`, який його спричинив.

#### **📌 Що перевіряють:**

*Знання стандартних конструкторів винятків та вміння написати код, що реалізує exception wrapping.*

* * *

### **3 ❓ Питання:**

Які методи з класу `Throwable` використовуються для роботи з ланцюжками винятків?

> #### **💬 Відповідь:**
>
> Клас `Throwable` надає кілька методів для роботи з причиною винятку:
>
> 1. **`Throwable(String message, Throwable cause)` та `Throwable(Throwable cause)`:**
>     * Це **конструктори**, які дозволяють встановити причину одразу при створенні винятку. Це найкращий підхід.
>
> 2. **`public Throwable getCause()`:**
>     * **Повертає** об'єкт `Throwable`, який є причиною поточного винятку.
>     * Якщо причини немає, повертає `null`.
>
> 3. **`public Throwable initCause(Throwable cause)`:**
>     * Дозволяє **встановити причину постфактум**, якщо вона не була задана в конструкторі.
>     * Може бути викликаний **лише один раз**. Повторний виклик призведе до `IllegalStateException`.
>     * Використовується рідко, зазвичай коли виняток створюється у фреймворку, а причина стає відомою пізніше.

#### **📌 Що перевіряють:**

*Знання API класу `Throwable`, що є основою для роботи з винятками.*

* * *

### **4 ❓ Питання:**

Як ланцюжок винятків відображається у стек-трейсі (stack trace)?

> #### **💬 Відповідь:**
>
> Це одна з найкорисніших властивостей цього механізму. При виведенні стек-трейсу (наприклад, через `e.printStackTrace()`), JVM рекурсивно виводить інформацію не тільки про основний виняток, а й про всю його "історію".
>
> **Стек-трейс виглядає так:**
>
> 1. Спочатку йде стек-трейс **основного** винятку.
> 2. Потім іде ключове слово **`Caused by:`**.
> 3. А після нього — **стек-трейс винятку-причини**.
>
> Цей процес повторюється, якщо є глибший ланцюжок.
>
> **Приклад виводу:**
>
> ```
> com.example.MyServiceException: Не вдалося обробити запит
>     at com.example.MyService.process(MyService.java:10)
>     ... 15 more
> Caused by: com.example.DataAccessException: Помилка доступу до даних
>     at com.example.MyRepository.find(MyRepository.java:25)
>     at com.example.MyService.process(MyService.java:8)
>     ... 15 more
> Caused by: java.sql.SQLException: Connection timed out
>     at com.mysql.cj.jdbc.ConnectionImpl.create(ConnectionImpl.java:50)
>     ... 20 more
> ```
>
> Такий вивід є надзвичайно інформативним і дозволяє легко відстежити шлях помилки від високого рівня до її низькорівневого джерела.

#### **📌 Що перевіряють:**

*Практичний досвід налагодження програм. Кандидат повинен знати, як читати та інтерпретувати складні стек-трейси.*

* * *

### **5 ❓ Питання:**

Чому цей код є поганою практикою і як його виправити?

```java
catch (IOException e) {
    throw new RuntimeException("Сталася помилка");
}
```

> #### **💬 Відповідь:**
>
> Це дуже погана практика, оскільки тут відбувається **втрата інформації про першопричину помилки**.
>
> **Проблема:**
>
> * Ви ловите специфічний `IOException`, який несе в собі цінну інформацію (наприклад, який саме файл не було знайдено).
> * Але замість того, щоб зберегти цю інформацію, ви кидаєте новий `RuntimeException` з узагальненим повідомленням.
> * **Оригінальний виняток `e` просто "проковтується"**. Стек-трейс почнеться з місця `throw new RuntimeException(...)`, і ви ніколи не дізнаєтеся, що справжньою причиною була `IOException`.
>
> **Як виправити:**
>
> **Завжди** передавайте оригінальний виняток як **`cause`** у конструктор нового.
>
> **Правильний код:**
>
> ```java
> catch (IOException e) {
>     throw new RuntimeException("Сталася помилка", e);
> }
> ```
>
> Це єдиний правильний спосіб "загорнути" виняток без втрати контексту.

#### **📌 Що перевіряють:**

*Розуміння важливості збереження стек-трейсу. Це ключова навичка для написання коду, який легко налагоджувати.*

* * *

### **6 ❓ Питання:**

У контексті багатошарової архітектури, на межі яких шарів найчастіше використовується загортання винятків? ⭐️

> #### **💬 Відповідь:**
>
> Загортання винятків найчастіше відбувається на **межах між архітектурними шарами**, щоб **абстрагувати** вищий шар від деталей реалізації нижчого.
>
> **Типові місця:**
>
> **1. На межі `Repository` (Data Access Layer) та `Service` (Business Logic Layer):**
>
> * Це найпоширеніший випадок.
> * `Repository` ловить низькорівневі `checked` винятки, специфічні для технології доступу до даних (наприклад, `SQLException` для JDBC, `MongoException` для MongoDB).
> * Він "загортає" їх у власні, більш загальні, **`unchecked`** винятки (наприклад, `DataAccessException`), і кидає їх далі.
> * В результаті, `Service` шар залежить лише від `DataAccessException` і нічого не знає про те, яка саме база даних використовується під ним.
>
> **2. На межі `Service` та `Controller` (Presentation Layer):**
>
> * Сервіс може кидати специфічні для бізнес-логіки винятки (наприклад, `InsufficientFundsException`).
> * `Controller` може ловити їх і перетворювати на відповідні HTTP-статуси, або це може робити глобальний обробник винятків. Зазвичай тут не "загортають", а **трансформують** виняток у відповідь API.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Знання того, як застосовувати патерн exception wrapping для побудови слабкозв'язаних систем.*

* * *

### **7 ❓ Питання:**

Поясніть, що таке придушені винятки (`suppressed exceptions`) і чим вони відрізняються від ланцюжкових (`chained exceptions`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва механізми дозволяють прикріпити один виняток до іншого, вони вирішують **різні проблеми** і використовуються в **різних сценаріях**.
>
> **Ланцюжок винятків (`chained`):**
>
> * **Відношення:** Описує **причинно-наслідковий зв'язок ("caused by")**. Виняток `А` стався **тому, що** стався виняток `B`.
> * **Коли використовується:** Коли ви **ловите** один виняток і **кидаєте** інший, більш високорівневий. `try-catch`.
> * **Кількість:** Один виняток може мати лише **одну** безпосередню причину.
>
> **Придушені винятки (`suppressed`):**
>
> * **Відношення:** Описує **дві або більше незалежних помилок**, що сталися під час однієї операції. Одна помилка **не є причиною** іншої, вони просто сталися майже одночасно.
> * **Коли використовується:** Практично ексклюзивно в конструкції **`try-with-resources`**. Основна помилка виникає в `try` блоці, а придушені — під час автоматичного закриття ресурсів.
> * **Кількість:** Один виняток може мати **багато** придушених винятків (зберігаються в масиві).
>
> **Коротко:** `Chained` — це "А через Б". `Suppressed` — це "сталося А, а потім ще й Б".

#### **📌 Що перевіряють:**

*Дуже точне розмежування двох схожих, але різних механізмів обробки помилок.*

* * *

### **8 ❓ Питання:**

Чи можна створити власний `checked` виняток, який підтримує ланцюжки? Як це зробити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, це є найкращою практикою** при створенні кастомних винятків.
>
> Щоб ваш виняток підтримував ланцюжки, вам потрібно надати в ньому **конструктори**, які приймають `Throwable cause` як параметр, і передати його в конструктор суперкласу `Exception`.
>
> **Приклад реалізації:**
>
> ```java
> // Оголошуємо власний checked виняток
> public class CustomDataProcessingException extends Exception {
>
>     // Конструктор з повідомленням та причиною
>     public CustomDataProcessingException(String message, Throwable cause) {
>         // Викликаємо відповідний конструктор батьківського класу
>         super(message, cause);
>     }
>
>     // Конструктор лише з причиною
>     public CustomDataProcessingException(Throwable cause) {
>         super(cause);
>     }
> }
> ```
>
> **Використання:**
>
> ```java
> catch (IOException e) {
>     throw new CustomDataProcessingException("Помилка обробки", e);
> }
> ```
>
> Завдяки виклику `super(message, cause)`, ми передаємо відповідальність за збереження "причини" базовому класу `Throwable`, який має для цього всю необхідну логіку.

#### **📌 Що перевіряють:**

*Навички написання власних класів винятків, що відповідають найкращим практикам.*

* * *

### **9 ❓ Питання:**

Як можна вручну додати придушений виняток до іншого? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це можна зробити за допомогою публічного методу `Throwable.addSuppressed(Throwable exception)`.
>
> Хоча цей механізм зазвичай використовується автоматично в `try-with-resources`, ви можете викликати його і вручну, якщо вам потрібно зібрати кілька незалежних помилок.
>
> **Сценарій:**
>
> У вас є метод, який повинен виконати кілька незалежних дій по очищенню, і ви хочете, щоб всі вони були виконані, навіть якщо деякі з них кинуть винятки.
>
> ```java
> public void closeAllResources(List<Resource> resources) throws CleanupException {
>     CleanupException mainException = null;
>
>     for (Resource r : resources) {
>         try {
>             r.close();
>         } catch (Exception e) {
>             if (mainException == null) {
>                 mainException = new CleanupException("Помилка очищення", e);
>             } else {
>                 mainException.addSuppressed(e);
>             }
>         }
>     }
>
>     if (mainException != null) {
>         throw mainException;
>     }
> }
> ```
>
> Тут перший виняток, що виник, стає "причиною" для нашого `CleanupException`, а всі наступні помилки при закритті додаються до нього як **придушені**. Це гарантує, що жодна помилка не буде втрачена.

#### **📌 Що перевіряють:**

*Глибокі знання API `Throwable` та здатність реалізувати складну логіку агрегації помилок.*

* * *

### **10 ❓ Питання:**

Чи можна мати ланцюжок з `Error`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, технічно можна.**
>
> * `Error` успадковується від `Throwable`, тому він також має конструктори, що приймають `cause`, і методи `getCause()` та `addSuppressed()`.
> * Ви можете зловити `Exception` і кинути `Error`, передавши `Exception` як причину.
>
> **Приклад:**
>
> ```java
> try {
>     throw new IOException("Some IO problem");
> } catch (IOException e) {
>     // Хоча технічно можливо, це дуже погана ідея!
>     throw new AssertionError("Цього ніколи не мало статися!", e);
> }
> ```
>
> **Чи варто це робити?**
>
> **Практично ніколи.** Кидати `Error` власноруч — це дуже екстремальна дія. Це сигналізує про невідворотну помилку, зазвичай у самій JVM або у фундаментальних припущеннях програми. Загортати звичайний `Exception` в `Error` — це зловживання механізмом, оскільки ви перетворюєте потенційно оброблювану помилку на фатальну.
>
> Єдиним винятком може бути `AssertionError`, але його слід використовувати для перевірки інваріантів, а не для обробки звичайних помилок.

#### **📌 Що перевіряють:**

*Розуміння семантики та призначення різних типів `Throwable`. Кандидат повинен розуміти, чому певні технічно можливі дії є концептуально неправильними.*

* * *

### **11 ❓ Питання:**

Що виведе цей код? Поясніть ланцюжок.

```java
try {
    try {
        throw new IllegalArgumentException("Bottom");
    } catch (IllegalArgumentException e) {
        throw new IllegalStateException("Middle", e);
    }
} catch (IllegalStateException e) {
    throw new RuntimeException("Top", e);
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Фінальним кинутим винятком буде **`RuntimeException`**. При виведенні його стек-трейсу ми побачимо повний ланцюжок.
>
> **Ланцюжок:**
> `RuntimeException` → `IllegalStateException` → `IllegalArgumentException`
>
> **Стек-трейс буде виглядати приблизно так:**
>
> ```
> java.lang.RuntimeException: Top
>     at ...
> Caused by: java.lang.IllegalStateException: Middle
>     at ...
> Caused by: java.lang.IllegalArgumentException: Bottom
>     at ...
> ```
>
> **Пояснення:**
>
> 1. Внутрішній `try` кидає `IllegalArgumentException`.
> 2. Внутрішній `catch` ловить його і кидає **новий** `IllegalStateException`, передаючи `IllegalArgumentException` як `cause`.
> 3. Зовнішній `try-catch` ловить `IllegalStateException`.
> 4. Зовнішній `catch` кидає **ще один новий** виняток `RuntimeException`, передаючи `IllegalStateException` як `cause`.
>
> Це гарний приклад побудови ланцюжка з трьох рівнів.

#### **📌 Що перевіряють:**

*Здатність прослідкувати складний потік виконання з вкладеними `try-catch` та загортанням винятків.*

* * *

### **12 ❓ Питання:**

Що буде, якщо причина (`cause`) винятку сама має причину? Як `getCause()` буде працювати? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це абсолютно нормальна ситуація, яка і створює "ланцюжок". `getCause()` просто поверне наступний виняток у ланцюжку.
>
> Щоб пройтися по **всьому** ланцюжку причин, вам потрібно робити це **в циклі**, доки `getCause()` не поверне `null`.
>
> **Приклад коду для розкрутки ланцюжка:**
>
> ```java
> public void printFullCauseTrace(Throwable t) {
>     System.out.println("Початковий виняток: " + t);
>     Throwable cause = t.getCause();
>     int level = 1;
>     while (cause != null) {
>         System.out.println("  Причина " + level + ": " + cause);
>         cause = cause.getCause(); // Переходимо до наступної причини
>         level++;
>     }
> }
> ```
>
> `e.printStackTrace()` робить щось подібне "під капотом", коли виводить "Caused by:".

#### **📌 Що перевіряють:**

*Знання того, що ланцюжок може бути будь-якої довжини, та вміння написати код для його аналізу.*

* * *

### **13 ❓ Питання:**

У чому різниця між `throw e` та `throw new MyException(e)` з точки зору типу винятку, що кидається? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це два абсолютно різні підходи.
>
> **`throw e;` (rethrowing):**
>
> * **Що робить:** **Повторно кидає той самий екземпляр** винятку `e`, який був зловлений.
> * **Тип:** Тип винятку, що кидається, є **точно таким же**, яким був тип `e`.
> * **Контекст:** Використовується, коли ви хочете виконати якусь дію (наприклад, логування), але дозволити винятку продовжити свій шлях вгору по стеку в його **первісному вигляді**.
>
> **`throw new MyException(e);` (wrapping):**
>
> * **Що робить:** Створює **новий екземпляр** вашого кастомного винятку `MyException`, передаючи оригінальний виняток `e` як причину.
> * **Тип:** Тип винятку, що кидається, — це **`MyException`**. Оригінальний виняток стає "запакованим" всередину.
> * **Контекст:** Використовується, коли ви хочете **абстрагувати** код вищого рівня від деталей низькорівневих помилок, замінюючи, наприклад, `SQLException` на `DataAccessException`.
>
> **Ключова відмінність:** у першому випадку сигнатура методу повинна буде оголошувати `throws` для типу `e`, а у другому — для `MyException`.

#### **📌 Що перевіряють:**

*Чітке розуміння різниці між "повторним киданням" та "загортанням". Це важливо для проектування стратегії обробки помилок.*

* * *

### **14 ❓ Питання:**

Як ланцюжок винятків може допомогти у налагодженні багатошарової програми, де кожен шар має свій доменний виняток? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це саме той сценарій, для якого ланцюжки винятків є найбільш корисними. Вони надають повну **"трасуваність"** помилки крізь шари архітектури.
>
> **Сценарій:**
>
> 1. `Repository` намагається виконати SQL-запит і отримує `SQLException`. Він "загортає" його в `DataAccessException`.
> 2. `Service` викликає репозиторій. `DataAccessException` — це `RuntimeException`, тому сервіс його не ловить, але його власна логіка (наприклад, розрахунок знижки) зазнає невдачі через відсутність даних. Сервіс кидає свій виняток, наприклад, `BusinessLogicException`, загортаючи в нього `DataAccessException`.
> 3. `Controller` викликає сервіс і отримує `BusinessLogicException`. Він не знає, що робити, тому дозволяє глобальному обробнику помилок його зловити.
>
> **Що бачить розробник у логах:**
>
> Він бачить повну картину:
>
> * **Верхній рівень:** `BusinessLogicException: Помилка при розрахунку знижки...`
> * **`Caused by`:** `DataAccessException: Помилка отримання даних для користувача...`
> * **`Caused by`:** `SQLException: Table 'users' not found...`
>
> Розробник одразу бачить:
>
> * **Де** проблема проявилася на бізнес-рівні (у розрахунку знижки).
> * **Що** спричинило її на рівні даних (не вдалося отримати дані).
> * **Яка** була першопричина (немає таблиці в БД).
>
> Без ланцюжка винятків, він би побачив лише "Помилка при розрахунку знижки", і йому довелося б довго шукати справжню причину.

#### **📌 Що перевіряють:**

*Здатність мислити на рівні архітектури та пояснювати, як конкретні технічні механізми допомагають у підтримці та налагодженні складних систем.*

* * *

### **15 ❓ Питання:**

Що станеться, якщо спробувати встановити причину (`cause`) для винятку, у якого причина вже встановлена? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Спроба викликати метод `initCause(Throwable cause)` **більше одного разу** для одного й того ж екземпляра винятку призведе до викидання **`IllegalStateException`**.
>
> **Правило:** Причина винятку може бути встановлена **лише один раз**.
>
> Це правило є логічним, оскільки причина помилки має бути унікальною та незмінною.
>
> **Як це працює:**
>
> * **Найкращий спосіб** встановити причину — це передати її **в конструктор**. У цьому випадку вона встановлюється одразу і є `final` (з точки зору логіки).
>
> * Метод `initCause()` був доданий для рідкісних випадків, коли потрібно створити виняток, а його причина стає відомою лише пізніше (наприклад, у старих API, що вимагають конструктор без аргументів).
>
> **Приклад:**
>
> ```java
> IOException cause = new IOException("Причина");
> MyException ex = new MyException("Основна");
>
> ex.initCause(cause); // ОК
> ex.initCause(cause); // Кине IllegalStateException!
> ```
>
> Так само, якщо причина була встановлена в конструкторі, `initCause()` також кине виняток.

#### **📌 Що перевіряють:**

*Дуже глибокі знання API `Throwable` та його контрактів. Це питання на знання рідкісних, але важливих аспектів.*
