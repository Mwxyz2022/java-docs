---
layout: default
title: "Контракт equals() і hashCode()"
parent: "`equals()`, `hashCode()`, `clone()`"
grand_parent: "Java Core"
nav_order: 5
---

# Контракт equals() і hashCode()

* короткий опис: Головне правило контракту: якщо `a.equals(b)` повертає `true`, то `a.hashCode() == b.hashCode()` зобов'язано повертати `true`. Це критично важливо для коректної роботи хеш-структур (`HashMap`, `HashSet`), оскільки вони спочатку використовують `hashCode()` для розміщення/пошуку, а потім `equals()` для точного порівняння.

* поняття: Контракт, `HashMap`, `HashSet`, колізія.

* приклади: Порушення контракту може призвести до "втрати" об'єктів у колекціях або їх дублювання.

* ключові моменти: Критично важливий для хеш-колекцій, гарантує правильний пошук. Якщо `hashCode()` різні, `equals()` точно `false`.

* що важливо знати на співбесіді: Детальний контракт, наслідки порушення.

### **✅ Офіційне визначення:**

Контракт між методами **`equals()`** і **`hashCode()`** у Java визначає, що **якщо два об’єкти рівні згідно з `equals()`**, то **вони зобов’язані мати однаковий `hashCode()`**. Це критично важливо для коректної роботи хеш-структур (наприклад, **`HashMap`**, **`HashSet`**, **`Hashtable`**).

### **🧠 Простими словами:**

Java очікує, що ти дотримуєшся правил гри:

якщо два об’єкти однакові (**`equals`** каже “**`так`**”) — вони мають лежати в одному ящику (**`hashCode`** теж має бути однаковим).

Якщо ні — Java не зможе знайти об'єкт у хеш-колекції.

---

## **📊 Основні правила контракту:**

* Якщо **`a.equals(b) -> a.hashCode() == b.hashCode()`** ✅ **зобов'язано `true`**
* Якщо **`a.hashCode() != b.hashCode() -> a.equals(b)` точно `false`** ✅ **гарантовано**
* Якщо **`a.hashCode() == b.hashCode() -> a.equals(b)` може бути `true` або `false`** (це називається **`колізією`**)

---

## **Пояснення на пальцях (💡)**

* Хеш-контейнер (наприклад **`HashSet`**) зберігає обʼєкти у “відрах” на основі хеш-коду.
* Коли ти щось шукаєш — Java йде в правильне відро (**`hashCode()`**), а потім перевіряє, чи знайдений об’єкт дійсно той (**`equals()`**).
* Якщо **`hashCode()`** помилковий або не відповідає **`equals()`**, Java піде в “неправильне відро” — і обʼєкта там не буде.
* Результат: дублікати в **`Set`**, відсутність даних у **`Map`**, або інші неприємності.

---

## **Корисні приклади (🧪)**

```java
User u1 = new User("Max", 25);
User u2 = new User("Max", 25);

System.out.println(u1.equals(u2));    // true
System.out.println(u1.hashCode());    // 12345
System.out.println(u2.hashCode());    // 54321 ❌ контракт порушено!
```

**Якщо додати їх в HashSet:**

```java
Set<User> users = new HashSet<>();
users.add(u1);
System.out.println(users.contains(u2));  // false! Хоча equals повертає true
```

---

## **Порівняння (⚖️)**

| Умова | hashCode() | equals() | Результат у HashSet |
| ----- | ----- | ----- | ----- |
| Однакові значення полів | однаковий | **`true`** | OK ✅ |
| Різні значення, однаковий хеш | однаковий (колізія) | **`false`** | Колекція спрацює правильно |
| Однакові значення, різний хеш | ❌ різний | **`true`** | ❌ Контракт порушено, не знайдеться |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔁 **“Equals -> HashCode”**

* **`equals`** -> обов'язково має бути однаковий **`hashCode`**
* **`hashCode`** однаковий -> **`equals`** необов'язково **`true`**
* **`equals`** без **`hashCode`** \= **пастка**

---

## **🟩 Підсумок:**

Контракт між **`equals()`** і **`hashCode()`** — основа правильної роботи об’єктів у хеш-колекціях.

Якщо два об'єкти рівні за **`equals()`**, вони обов'язково мають мати однаковий **`hashCode()`**. Інакше Java може не знайти об'єкт або зберігати його двічі. Це одна з найпоширеніших помилок у Java, і її часто перевіряють на співбесідах.

---

### **🔗 Додаткові джерела:**

* [**Java equals/hashCode Contract — Oracle**](https://docs.oracle.com/javase/8/docs/api/java/lang/Object.html#hashCode--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Сформулюйте головне правило контракту між `equals()` та `hashCode()`.

> #### **💬 Відповідь:**
>
> Головне та найсуворіше правило контракту звучить так:
>
> **Якщо два об'єкти рівні один одному за методом `equals(Object obj)`, то їхні методи `hashCode()` зобов'язані повертати одне й те саме ціле число.**
>
> Формально: якщо `a.equals(b) == true`, то `a.hashCode() == b.hashCode()` також має бути `true`.
>
> Зворотне твердження **не є обов'язковим**: якщо хеш-коди двох об'єктів однакові, самі об'єкти не зобов'язані бути рівними.

#### **📝 Аналогія:**</h4>

Уявіть, що у всіх людей з однаковими іменами (`equals()`) мають бути однакові номери телефонів (`hashCode()`). Якщо у двох людей з іменем "Іван" будуть різні номери, то, знаючи тільки ім'я, ви не зможете додзвонитися до потрібного. Але якщо у двох людей з різними іменами випадково виявився однаковий номер (колізія), це нормально, і ви просто уточните, з ким розмовляєте.

#### **📌 Що перевіряють:**

*Чітке знання основного правила контракту, яке є фундаментом для всіх наступних питань.*

* * *

### **2 ❓ Питання:**

Чому цей контракт такий важливий саме для хеш-колекцій (`HashMap`, `HashSet`)?

> #### **💬 Відповідь:**
>
> Хеш-колекції використовують двоступеневий механізм для швидкого пошуку об'єктів:
>
> 1. **Крок 1: Швидкий пошук "кошика" (bucket) за допомогою `hashCode()`**. Колекція викликає `key.hashCode()` і на основі цього значення миттєво визначає, в якому внутрішньому масиві (кошику) може знаходитися об'єкт. Це дуже швидка операція (O(1)).
>
> 2. **Крок 2: Точний пошук усередині кошика за допомогою `equals()`**. Оскільки в одному кошику через колізії може бути кілька об'єктів, колекція послідовно перебирає їх і порівнює з шуканим ключем за допомогою `equals()`, щоб знайти точний збіг.
>
> Якщо контракт порушено (рівні за `equals` об'єкти мають різні хеш-коди), то на першому кроці колекція просто **піде не в той кошик** і ніколи не знайде потрібний об'єкт, навіть не дійшовши до виклику `equals()`.

#### **📌 Що перевіряють:**

*Розуміння внутрішнього алгоритму роботи хеш-колекцій і ролі кожного методу в цьому алгоритмі.*

* * *

### **3 ❓ Питання:**

Що станеться, якщо додати об'єкт у `HashSet`, а потім змінити його стан так, що зміниться `hashCode()`?

> #### **💬 Відповідь:**
>
> Це призведе до того, що об'єкт буде "втрачено" всередині `HashSet`.
>
> **Сценарій:**
>
> 1. Ви створюєте об'єкт, наприклад `MutableKey key = new MutableKey("initial_state")`.
> 2. `HashSet` обчислює його хеш-код, скажімо, `H1`, і розміщує `key` у кошик, що відповідає `H1`.
> 3. Потім ви змінюєте стан об'єкта: `key.setState("new_state")`.
> 4. Тепер `key.hashCode()` повертає нове значення, наприклад, `H2`.
> 5. Коли ви викликаєте `set.contains(key)`, `HashSet` обчислює поточний хеш-код `H2`, шукає у відповідному йому кошику, нічого там не знаходить і повертає `false`.
>
> Хоча об'єкт все ще знаходиться в множині (у кошику, що відповідає `H1`), ви більше не можете отримати до нього доступ або видалити його стандартними методами.
>
> **Висновок:** Елементи в `HashSet` (і ключі в `HashMap`) повинні бути **імутабельними (незмінними)** або принаймні не змінювати ті поля, які беруть участь в `equals` та `hashCode`.

#### **📌 Що перевіряють:**

*Розуміння небезпеки використання мутабельних об'єктів у хеш-колекціях і наслідків зміни хеш-коду для вже доданого елемента.*

* * *

### **4 ❓ Питання:**

Наведіть приклад коду, який порушує контракт `equals-hashCode` і продемонструйте наслідки.

> #### **💬 Відповідь:**
>
> Ось класичний приклад порушення, де `equals` перевизначено, а `hashCode` — ні.
>
> ```java
> import java.util.HashSet;
> import java.util.Set;
>
> class BrokenUser {
>     private String name;
>
>     public BrokenUser(String name) { this.name = name; }
>
>     @Override
>     public boolean equals(Object obj) { // Правильно перевизначено
>         if (this == obj) return true;
>         if (obj == null || getClass() != obj.getClass()) return false;
>         return name.equals(((BrokenUser) obj).name);
>     }
>
>     // hashCode() НЕ перевизначено, використовується стандартний з Object
> }
> ```
>
> **Демонстрація наслідків:**
>
> ```java
> public class Demo {
>     public static void main(String[] args) {
>         BrokenUser user1 = new BrokenUser("admin");
>         BrokenUser user2 = new BrokenUser("admin");
>
>         System.out.println("user1.equals(user2): " + user1.equals(user2)); // true
>         System.out.println("user1.hashCode(): " + user1.hashCode());
>         System.out.println("user2.hashCode(): " + user2.hashCode()); // Скоріш за все, буде іншим
>
>         Set<BrokenUser> users = new HashSet<>();
>         users.add(user1);
>
>         System.out.println("users.contains(user1): " + users.contains(user1)); // true
>         // Ось тут проблема: HashSet шукає user2 в іншому "кошику"
>         System.out.println("users.contains(user2): " + users.contains(user2)); // false!
>     }
> }
> ```
>
> В результаті ми не можемо знайти об'єкт, який логічно вже є у множині.

#### **📌 Що перевіряють:**

*Здатність написати код, що ілюструє проблему, і пояснити, чому він працює некоректно.*

* * *

### **5 ❓ Питання:**

Чи можна реалізувати `hashCode()`, щоб він завжди повертав одну й ту саму константу, наприклад, 0? Чи буде це порушувати контракт?

> #### **💬 Відповідь:**
>
> * **Чи порушує це контракт?**
>     Ні, формально **контракт не буде порушено**. Якщо хеш-код для всіх об'єктів однаковий (наприклад, 0), то для будь-яких двох рівних за `equals` об'єктів їхні хеш-коди також будуть рівні (оскільки `0 == 0`).
>
> * **Які будуть наслідки?**
>     Наслідки будуть катастрофічними для **продуктивності** хеш-колекцій. Всі без винятку об'єкти потраплять в один "кошик" (bucket).
>
>     Це призведе до того, що `HashMap` чи `HashSet` перетворяться на звичайний зв'язаний список (або дерево, з Java 8), і час виконання операцій (`get`, `put`, `contains`) деградує з O(1) до **O(n)**.
>
> **Висновок:** Хоча така реалізація є "легальною" з точки зору контракту, вона є вкрай неефективною і нівелює всю ідею хешування.

#### **📌 Що перевіряють:**

*Розуміння різниці між формальною коректністю (дотримання контракту) і практичною ефективністю (хороший розподіл хешів).*

* * *

### **6 ❓ Питання:**

Якщо `a.hashCode() != b.hashCode()`, що можна сказати про `a.equals(b)`?

> #### **💬 Відповідь:**
>
> Якщо `a.hashCode() != b.hashCode()`, то можна гарантовано стверджувати, що `a.equals(b)` поверне **`false`**.
>
> Це логічний наслідок основного правила (`a.equals(b) => a.hashCode() == b.hashCode()`). Це називається **контрапозицією**.
>
> Ця властивість активно використовується для оптимізації в хеш-колекціях. Якщо хеш-коди не збігаються, немає жодного сенсу викликати потенційно "дорогий" метод `equals()`. `HashMap` відразу розуміє, що об'єкти різні.

#### **📌 Що перевіряють:**

*Розуміння логічних наслідків основного правила контракту та його практичного застосування для оптимізації.*

* * *

### **7 ❓ Питання:**

Що таке колізія хешування і як `HashMap` з нею справляється?

> #### **💬 Відповідь:**
>
> **Колізія** — це ситуація, коли два **нерівних** за `equals` об'єкти мають **однаковий** `hashCode`.
>
> `if (a.hashCode() == b.hashCode() && !a.equals(b)) { // Це колізія }`
>
> Колізії є неминучими, тому хеш-колекції розроблені для їх коректної обробки.
>
> **Як `HashMap` з ними справляється:**
>
> Коли два або більше об'єктів з однаковим хеш-кодом потрапляють в один і той самий "кошик", `HashMap` зберігає їх там у вигляді структури даних.
>
> * **До Java 8:** це був **зв'язаний список (LinkedList)**.
> * **Починаючи з Java 8:** якщо кількість елементів у кошику перевищує певний поріг (за замовчуванням 8), зв'язаний список **перетворюється на збалансоване червоно-чорне дерево**. Це покращує продуктивність пошуку в межах "переповненого" кошика з O(n) до O(log n).
>
> Коли потрібно знайти елемент, `HashMap` спочатку знаходить кошик за `hashCode`, а потім послідовно перебирає елементи в цьому списку/дереві, викликаючи `equals()` для кожного, доки не знайде точний збіг.

#### **📌 Що перевіряють:**

*Знання про колізії та внутрішню будову `HashMap`, включаючи еволюцію цього механізму в Java 8.*

* * *

### **8 ❓ Питання:**

Чи потрібно включати в розрахунок `hashCode()` і `equals()` поля, які є унікальними для кожного екземпляра, наприклад, `id` з бази даних? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від того, що ви вважаєте "логічною рівністю" об'єкта. Є два підходи:
>
> 1. **Рівність за ідентифікатором (Entity equality):** Якщо два об'єкти представляють один і той самий запис у базі даних, вони вважаються рівними. У цьому випадку `equals` та `hashCode` повинні базуватися **лише на полі `id`**.
>
> 2. **Рівність за змістом (Value equality):** Якщо два об'єкти вважаються рівними, коли їхні атрибути (ім'я, дата, категорія) однакові, навіть якщо вони ще не збережені в БД (і `id` дорівнює `null` або 0). У цьому випадку `equals` та `hashCode` повинні базуватися на полях, що визначають вміст, а `id` **не слід включати**.
>
> **Проблема з `id`**: Якщо ви включаєте `id` у порівняння, то новий, ще не збережений об'єкт (`id=null`) ніколи не буде рівним такому ж за вмістом об'єкту, завантаженому з БД (`id=123`).
>
> **Поширена практика для JPA/Hibernate Entities:**
> Реалізовувати `equals` та `hashCode` **виключно на основі `id`**, але з перевіркою, чи об'єкти не є тимчасовими (transient). Це забезпечує стабільну поведінку об'єктів після того, як вони були збережені.

#### **📌 Що перевіряють:**

*Розуміння різниці між рівністю за ідентичністю та за значенням, особливо в контексті роботи з базами даних та ORM.*

* * *

### **9 ❓ Питання:**

Чому хороший `hashCode` повинен прагнути до рівномірного розподілу? ⭐️

> #### **💬 Відповідь:**
>
> **Рівномірний розподіл** означає, що хеш-функція генерує різні хеш-коди для різних об'єктів з однаковою ймовірністю, розкидаючи їх по всьому діапазону значень `int`.
>
> Це важливо, бо це **мінімізує кількість колізій**.
>
> * **Якщо розподіл рівномірний**, об'єкти будуть рівномірно розподілені по всіх "кошиках" хеш-таблиці. Розмір кожного кошика буде невеликим (в ідеалі 1), і продуктивність `HashMap` буде близькою до O(1).
> * **Якщо розподіл поганий**, багато об'єктів будуть скупчуватися в невеликій кількості кошиків, створюючи довгі списки або дерева. Це призведе до деградації продуктивності до O(n) або O(log n).
>
> Таким чином, якість хеш-функції напряму впливає на ефективність роботи хеш-колекцій.

#### **📌 Що перевіряють:**

*Розуміння теоретичних основ хешування і зв'язку між якістю хеш-функції та продуктивністю структур даних, що на ній базуються.*

* * *

### **10 ❓ Питання:**

Розглянемо `HashSet`. Якщо об'єкт успішно додано (`set.add(obj)` повернуло `true`), чи означає це, що `set.contains(obj)` одразу після цього поверне `true`? Чи завжди це так? ⭐️

> #### **💬 Відповідь:**
>
> Так, якщо `set.add(obj)` повернуло `true`, то негайний виклик `set.contains(obj)` з тим самим об'єктом **завжди** поверне `true`.
>
> **Однак, є підступ:** Це гарантовано тільки в тому випадку, якщо:
>
> 1. Клас об'єкта `obj` **коректно дотримується контракту `equals-hashCode`**.
> 2. Стан об'єкта `obj` **не змінюється** між викликами `add()` та `contains()` таким чином, щоб це вплинуло на його `hashCode()`.
>
> Якщо ж об'єкт мутабельний і його `hashCode()` зміниться після додавання, то `set.contains(obj)` може повернути `false`. Це порушує очікувану поведінку, але причина не в `HashSet`, а в некоректному використанні мутабельного об'єкта як елемента множини.
>
> Отже, в рамках коректного використання, відповідь — так, це завжди так.

#### **📌 Що перевіряють:**

*Уважність до деталей і здатність враховувати "крайові випадки", пов'язані з мутабельністю об'єктів.*

* * *

### **11 ❓ Питання:**

У класі `String` кешується хеш-код. Якби `String` був мутабельним (змінним), до яких проблем це могло б призвести? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якби `String` був мутабельним, а `hashCode` продовжував би кешуватися, це призвело б до катастрофи і повного порушення контракту `equals-hashCode`.
>
> **Сценарій проблеми:**
>
> 1. Створюється рядок `String s = new String("abc");`.
> 2. Викликається `s.hashCode()`. Він обчислюється і кешується. Нехай це буде `H1`.
> 3. Рядок `s` додається як ключ у `HashMap`: `map.put(s, "value")`. Він потрапляє в кошик, що відповідає `H1`.
> 4. Оскільки рядок мутабельний, ми змінюємо його вміст, наприклад, `s.changeContent("xyz")`.
> 5. Але кешований хеш-код **залишається старим** (`H1`), оскільки логіка кешування не знає про зміну.
>
> **Проблеми:**
>
> 1. **Порушення консистентності `hashCode()`**: Хеш-код об'єкта більше не відповідає його поточному стану.
> 2. **Порушення контракту `equals-hashCode`**:
>     * Створюємо новий рядок `String s2 = "xyz"`.
>     * `s.equals(s2)` поверне `true` (бо вміст однаковий).
>     * Але `s.hashCode()` поверне старе кешоване значення `H1`, а `s2.hashCode()` — правильне значення `H2`. Таким чином, `s.hashCode() != s2.hashCode()`. **Контракт порушено**.
> 3. **Некоректна робота `HashMap`**: Об'єкт `s` буде "втрачено" в мапі.
>
> Саме тому **імутабельність `String` є ключовою** для безпечного та ефективного кешування його хеш-коду.

#### **📌 Що перевіряють:**

*Глибоке розуміння переваг імутабельності та її нерозривного зв'язку з коректним хешуванням та використанням у колекціях.*

* * *

### **12 ❓ Питання:**

Чи може `equals` залежати від полів, які не входять у `hashCode`? Які наслідки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, не може. Це прямо **порушує контракт**.
>
> Якщо `equals` залежить від поля, яке не включено в `hashCode`, виникає ситуація:
>
> 1. Два об'єкти `obj1` та `obj2` однакові за всіма полями, що входять в `hashCode`, тому `obj1.hashCode() == obj2.hashCode()`.
> 2. Але вони відрізняються полем, яке є в `equals`, але немає в `hashCode`.
> 3. `obj1.equals(obj2)` поверне `false`.
>
> Це не порушує головного правила (`equals => hashCode`), тому здається, що все добре. Але є інша проблема: **продуктивність**.
>
> Якщо у вас буде багато об'єктів, які відрізняються лише цим "ігнорованим" полем, вони всі матимуть однаковий хеш-код і потраплять в один кошик. Це призведе до **масових колізій** і деградації продуктивності хеш-колекції до O(n).
>
> **Правильний підхід:** `hashCode()` повинен базуватися на тому самому наборі полів, що й `equals()`.

#### **📌 Що перевіряють:**

*Розуміння того, що `hashCode` має відображати логічний стан об'єкта настільки повно, наскільки це робить `equals`, щоб забезпечити хороший розподіл.*

* * *

### **13 ❓ Питання:**

Припустимо, `equals` порівнює поля без урахування регістру (case-insensitive), а `hashCode` — з урахуванням регістру. Чи порушено контракт? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, **контракт порушено**.
>
> **Приклад:**
>
> * `User user1 = new User("Tom");`
> * `User user2 = new User("tom");`
>
> **Аналіз:**
>
> 1. **`equals`**: Припустимо, реалізація `user1.equals(user2)` використовує `equalsIgnoreCase()` і повертає `true`.
> 2. **`hashCode`**: Припустимо, реалізація `hashCode()` використовує стандартний `name.hashCode()`.
>     * `"Tom".hashCode()` поверне одне значення.
>     * `"tom".hashCode()` поверне інше.
>
> В результаті ми маємо ситуацію, коли `user1.equals(user2)` є `true`, але `user1.hashCode() != user2.hashCode()`.
>
> **Як це виправити:**
> Якщо `equals` є case-insensitive, то і `hashCode` має бути таким самим. Поля, що порівнюються без урахування регістру, повинні бути приведені до одного регістру (наприклад, нижнього) перед обчисленням їхнього хеш-коду.
>
> ```java
> @Override
> public int hashCode() {
>     return Objects.hash(name.toLowerCase());
> }
>
> @Override
> public boolean equals(Object obj) {
>     // ...
>     return this.name.equalsIgnoreCase(other.name);
> }
> ```

#### **📌 Що перевіряють:**

*Уважність до деталей та здатність аналізувати узгодженість логіки в обох методах. Розуміння того, що спосіб порівняння має бути ідентичним.*

* * *

### **14 ❓ Питання:**

Чому клас `java.util.UUID` має таку складну реалізацію `hashCode`? Що вона робить? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Реалізація `hashCode()` в `UUID` виглядає складною, бо вона розроблена для забезпечення **максимально якісного розподілу хеш-кодів**, що критично важливо для об'єктів, які за своєю природою мають бути унікальними.
>
> `UUID` складається з двох 64-бітних значень: `mostSigBits` та `leastSigBits`. Проста комбінація їхніх хеш-кодів (як `Objects.hash(mostSigBits, leastSigBits)`) була б коректною, але неоптимальною.
>
> **Реалізація в JDK:**
>
> ```java
> public int hashCode() {
>     long hilo = mostSigBits ^ leastSigBits;
>     return ((int)(hilo >> 32)) ^ (int) hilo;
> }
> ```
>
> **Що тут відбувається:**
>
> 1. `mostSigBits ^ leastSigBits`: Два 64-бітні числа "змішуються" за допомогою операції XOR. Це робить так, що зміна бітів в будь-якій частині `UUID` вплине на результат.
> 2. `((int)(hilo >> 32)) ^ (int) hilo`: Отримане 64-бітне значення `hilo` перетворюється на 32-бітний `int` за вже знайомим нам правилом для `long`. Старша частина "накладається" на молодшу через XOR.
>
> Ця двоступенева операція XOR (`по-компонентна` та `внутрішня`) дозволяє максимально повно врахувати всі 128 біт інформації `UUID` при генерації 32-бітного хеш-коду, що дає дуже хороший розподіл і мінімізує колізії.

#### **📌 Що перевіряють:**

*Глибокі знання, включаючи вміння читати та аналізувати код зі стандартної бібліотеки. Розуміння бітових операцій та їх ролі у хешуванні.*

* * *

### **15 ❓ Питання:**

Що робити, якщо об'єкт повинен мати кілька різних "логік рівності" в залежності від контексту? Чи можна мати кілька пар `equals/hashCode`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, клас може мати лише одну реалізацію `equals()` та `hashCode()`, яка визначає його "природну" або "основну" логіку рівності.
>
> Якщо вам потрібні альтернативні логіки порівняння, стандартний підхід у Java — використовувати зовнішні стратегії.
>
> **Рішення:**
>
> 1. **Використання `Comparator`**: Для сортування або порівняння в структурах даних, які це підтримують (`TreeSet`, `TreeMap`), можна передати об'єкт `Comparator`.
>
> 2. **Для хеш-колекцій — `Wrapper` або сторонні бібліотеки**:
>     * **Патерн "Обгортка" (Wrapper):** Створюється клас-обгортка, який інкапсулює ваш об'єкт і реалізує потрібну пару `equals/hashCode`. Ви працюєте з колекцією цих обгорток. Це найбільш чистий підхід, хоча й трохи громіздкий.
>     * **Спеціалізовані колекції:** Деякі бібліотеки, наприклад Guava, надають можливість створювати хеш-колекції з кастомною стратегією рівності (через `Equivalence`).
>
> **Приклад з обгорткою для case-insensitive порівняння рядків:**
>
> ```java
> class CaseInsensitiveStringWrapper {
>     private final String str;
>     //...
>     @Override public boolean equals(Object o) { /* case-insensitive логіка */ }
>     @Override public int hashCode() { /* case-insensitive логіка */ }
> }
>
> Set<CaseInsensitiveStringWrapper> mySet = new HashSet<>();
> mySet.add(new CaseInsensitiveStringWrapper("Apple"));
> mySet.contains(new CaseInsensitiveStringWrapper("apple")); // true
> ```
>
> Це дозволяє використовувати один і той же об'єкт у різних контекстах з різною семантикою рівності.

#### **📌 Що перевіряють:**

*Вміння вирішувати складні дизайнерські проблеми за допомогою патернів. Розуміння того, як відокремити основну логіку класу від контекстно-залежної поведінки.*
