---
layout: default
title: "Метод hashCode() — призначення та контракт"
parent: "`equals()`, `hashCode()`, `clone()`"
grand_parent: "Java Core"
nav_order: 3
---

# Метод hashCode() — призначення та контракт

* короткий опис: Метод `hashCode()` з `java.lang.Object` повертає ціле число (хеш-код) об'єкта, що використовується для швидкого зберігання та пошуку об'єктів у хеш-структурах (`HashMap`, `HashSet`). Контракт вимагає: якщо `equals()` повертає `true`, то `hashCode()` має бути однаковим.

* поняття: `hashCode()`, хеш-код, хеш-таблиця, контракт `equals()/hashCode()`.

* приклади: `Objects.hash(name, age);` (для генерації хешу).

* ключові моменти: `hashCode()` є першим фільтром у хеш-колекціях. Завжди перевизначається разом з `equals()`.

* що важливо знати на співбесіді: Призначення `hashCode()`, його зв'язок з `equals()`, коли виникають колізії.

### **✅ Офіційне визначення:**

Метод **`hashCode()`** з класу **`java.lang.Object`** повертає ціле число (типу **`int`**), яке є хеш-кодом об’єкта. Його використовують для ефективного зберігання та пошуку об’єктів у структурах даних, що базуються на хешуванні (наприклад, **`HashMap`**, **`HashSet`**, **`Hashtable`**). За замовчуванням метод є **`native`** і реалізується платформою.

### **🧠 Простими словами:**

Метод **`hashCode()`** — це спосіб для Java швидко знайти об'єкт у колекції. Уяви шафу з 1000 ящиків — щоб не перевіряти кожен, Java бере об'єкт, рахує "номер ящика" (**`hashCode`**) і йде туди напряму. Якщо ти не перевизначиш цей метод правильно — Java може покласти твій об'єкт "не туди" і не знайде його потім.

---

## **📊 Поведінка:**

| Тип реалізації | Опис |
| :---: | :---: |
| За замовчуванням | **`native`** метод з **`Object`**, часто базується на адресі |
| Перевизначена | Ручна реалізація, яка враховує важливі поля класу |

---

## **Пояснення на пальцях (💡)**

* Якщо **`equals()`** каже, що обʼєкти рівні — **`hashCode()`** має бути однаковим. Це ключ до правильного пошуку в **`HashMap`** / **`HashSet`**
* Але зворотнє **не завжди вірно**: однакові хеші **`≠(не)`**однакові обʼєкти (може бути колізія)
* Якщо не перевизначити **`hashCode()`** — Java може покласти об'єкт у випадкове місце (залежно від реалізації **`JVM`**)
* Уяви, що ти поклав книжку в ящик №5, а шукаєш у №9 — ти її не знайдеш, навіть якщо вона там

---

## **Корисні приклади (🧪)**

```java
@Override
public int hashCode() {
    int result = 17;
    result = 31 * result + (name == null ? 0 : name.hashCode());
    result = 31 * result + age;
    return result;
}
```

**Альтернатива з Java 7+:**

```java
@Override
public int hashCode() {
    return Objects.hash(name, age);
}
```

---

## **Порівняння (⚖️)**

| Властивість | `equals()` | `hashCode()` |
| :---: | :---: | :---: |
| Тип повернення | **`boolean`** | **`int`** |
| Призначення | Чи обʼєкти однакові за суттю | У який "ящик" класти обʼєкт |
| Важливо для | Будь-якого порівняння | Хеш-структур (**`HashMap`**, **`HashSet`**) |
| Потрібно перевизначати разом | ✅ Так | ✅ Так |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧩 Якщо **`equals() = true -> hashCode()`** **має бути рівним**

🧩 Якщо **`hashCode() ≠ -> equals()`** **точно `false`**

🧩 Якщо **`hashCode() = -> equals()`** **може бути `true` або `false`**

---

## **🟩 Підсумок:**

Метод **`hashCode()`** — це основа роботи з хеш-колекціями. Він забезпечує швидкий пошук об’єктів у **`HashMap`**, **`HashSet`**, і його **завжди** слід перевизначати разом із **`equals()`**. Порушення контракту між ними призводить до багів і помилок, які важко відстежити. Найкраще — використовувати **`Objects.hash(...)`** для простоти і надійності.

---

### **🔗 Додаткові джерела:**

* [**Офіційна Java-документація: Object.hashCode()**](https://docs.oracle.com/javase/8/docs/api/java/lang/Object.html#hashCode--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке метод `hashCode()` і яке його основне призначення?

> #### **💬 Відповідь:**
>
> Метод `public int hashCode()` з класу `java.lang.Object` повертає цілочислове представлення об'єкта, відоме як **хеш-код**.
>
> **Основне призначення** — забезпечення швидкого пошуку та зберігання об'єктів у структурах даних, що базуються на хешуванні, таких як `HashMap`, `HashSet` та `Hashtable`.
>
> Замість того, щоб послідовно перебирати всі елементи колекції для пошуку потрібного (що довго, O(n)), хеш-колекції використовують хеш-код для миттєвого обчислення індексу "кошика" (bucket), де має знаходитися об'єкт. Це дозволяє виконувати операції додавання та пошуку в середньому за константний час (O(1)).

#### **📝 Аналогія:**

Хеш-код — це як номер глави в книзі. Щоб знайти потрібну інформацію, ви дивитеся у зміст (обчислюєте хеш-код), знаходите номер глави і відразу переходите на потрібну сторінку, замість того, щоб гортати всю книгу з початку.

#### **📌 Що перевіряють:**

*Розуміння базового призначення `hashCode()` як інструменту для оптимізації роботи з хеш-колекціями.*

* * *

### **2 ❓ Питання:**

Який контракт пов'язує методи `hashCode()` та `equals()`?

> #### **💬 Відповідь:**
>
> Існує суворий контракт, який нерозривно пов'язує ці два методи:
>
> 1. **Основне правило (one-way):** Якщо два об'єкти рівні за методом `equals(Object obj)`, то їхні методи `hashCode()` **зобов'язані** повертати однакове ціле число.
>     * `if (a.equals(b)) then (a.hashCode() == b.hashCode())` — має бути `true`.
>
> 2. **Зворотне твердження не є обов'язковим:** Якщо два об'єкти мають однаковий `hashCode()`, вони **не зобов'язані** бути рівними за `equals()`. Така ситуація називається **колізією**.
>     * `if (a.hashCode() == b.hashCode()) then (a.equals(b))` — може бути як `true`, так і `false`.
>
> 3. **Консистентність `hashCode()`:** Протягом одного виконання програми метод `hashCode()` має постійно повертати те саме значення для одного й того ж об'єкта, якщо поля, що беруть участь у розрахунку `equals()`, не змінювались.

#### **📝 Коментар:**</h4>

Найчастіше порушується перше правило. Якщо ви перевизначили `equals()`, але не `hashCode()`, два логічно рівних об'єкти можуть мати різні хеш-коди, що призведе до збою в роботі хеш-колекцій.

#### **📌 Що перевіряють:**

*Чітке знання офіційного контракту, який є фундаментом для коректної роботи з `HashMap` та `HashSet`.*

* * *

### **3 ❓ Питання:**

Що станеться, якщо перевизначити `equals()`, але не перевизначити `hashCode()`?

> #### **💬 Відповідь:**
>
> Це призведе до порушення контракту `equals()`-`hashCode()` і, як наслідок, до **некоректної роботи хеш-колекцій (`HashMap`, `HashSet`)**.
>
> Розглянемо сценарій:
>
> 1. Ви створюєте два логічно рівних об'єкти, наприклад, `User user1 = new User("admin");` та `User user2 = new User("admin");`.
> 2. `user1.equals(user2)` повертає `true`, тому що ви перевизначили `equals()`.
> 3. Але `user1.hashCode()` і `user2.hashCode()` повертають **різні значення**, тому що ви використовуєте реалізацію `hashCode()` за замовчуванням з класу `Object`, яка зазвичай базується на адресі об'єкта в пам'яті.
>
> **Наслідки:**
>
> ```java
> Set<User> userSet = new HashSet<>();
> userSet.add(user1);
>
> // Коли ви намагаєтесь перевірити наявність user2:
> // 1. HashSet обчислює hashCode() від user2 (він буде іншим).
> // 2. Шукає в іншому "кошику", де цього об'єкта немає.
> // 3. Метод equals() навіть не буде викликаний.
> System.out.println(userSet.contains(user2)); // false! Хоча логічно він там є.
> ```
>
> У `HashSet` можна буде додати обидва "однакові" об'єкти, що порушує семантику множини.

#### **📌 Що перевіряють:**

*Здатність пояснити конкретні наслідки порушення контракту на практичному прикладі роботи з `HashSet` або `HashMap`.*

* * *

### **4 ❓ Питання:**

Як реалізація `hashCode()` за замовчуванням (у класі `Object`) генерує хеш-код?

> #### **💬 Відповідь:**
>
> Метод `hashCode()` у класі `java.lang.Object` є `native` методом. Це означає, що його реалізація написана не на Java, а на іншій мові (зазвичай C/C++) і є частиною реалізації JVM.
>
> Конкретний алгоритм генерації **не стандартизований** і може відрізнятися в різних версіях і реалізаціях JVM (Oracle HotSpot, OpenJ9, GraalVM).
>
> Зазвичай реалізація за замовчуванням пов'язана з **внутрішньою адресою об'єкта в пам'яті**. Це не обов'язково є прямим перетворенням адреси, але гарантує, що два різні об'єкти, швидше за все, матимуть різні хеш-коди.
>
> **Важливо:** Не можна покладатися на конкретне значення, згенероване цим методом. Воно може змінюватися навіть між різними запусками однієї і тієї ж програми.

#### **📌 Що перевіряють:**

*Знання того, що `hashCode()` є `native` методом, та розуміння того, що його стандартна реалізація не є портативною і залежить від JVM.*

* * *

### **5 ❓ Питання:**

Які є загальні рекомендації та підходи до написання хорошого методу `hashCode()`?

> #### **💬 Відповідь:**
>
> Хороший `hashCode()` має відповідати двом критеріям: бути **коректним** (дотримуватися контракту) та **ефективним** (давати хороший розподіл, мінімізуючи колізії).
>
> **Рекомендації:**
>
> 1. **Включіть усі поля, що використовуються в `equals()`**: Якщо поле бере участь у порівнянні `equals()`, воно зобов'язане брати участь і в обчисленні `hashCode()`.
> 2. **Не включайте поля, що не використовуються в `equals()`**: Це зайве і може зашкодити продуктивності.
> 3. **Використовуйте `Objects.hash()` (з Java 7+)**: Це найпростіший, найбезпечніший і рекомендований спосіб. Він автоматично обробляє `null` та коректно комбінує хеш-коди полів.
> 4. **Класичний підхід (якщо `Objects.hash()` недоступний):**
>     * Почніть з ненульової константи (часто 17).
>     * Для кожного значущого поля `f` обчисліть `int` хеш-код `c`.
>     * Скомбінуйте хеш-коди: `result = 31 * result + c`.
>     * Використання непарного простого числа (31) дає кращий розподіл хеш-кодів.

#### **📝 Приклад з `Objects.hash()`:**

```java
@Override
public int hashCode() {
    // Просто, лаконічно і надійно
    return Objects.hash(field1, field2, field3);
}
```

#### **📌 Що перевіряють:**

*Практичні знання алгоритмів для створення `hashCode`. Розуміння, чому використовуються магічні числа (як 31) і переваги сучасного підходу з `Objects.hash()`.*

* * *

### **6 ❓ Питання:**

Що таке колізія в контексті `hashCode()` і чи є вона проблемою?

> #### **💬 Відповідь:**
>
> **Колізія** — це ситуація, коли два **нерівних** за `equals()` об'єкти мають **однаковий** `hashCode()`.
>
> `if (a.hashCode() == b.hashCode() && !a.equals(b)) { // Це колізія }`
>
> **Чи є це проблемою?**
> Ні, сама по собі колізія **не є помилкою**, це абсолютно нормальне і очікуване явище. Діапазон значень `int` обмежений, а кількість можливих об'єктів — нескінченна. Тому колізії неминучі (принцип Діріхле).
>
> Хеш-колекції, такі як `HashMap`, розроблені з урахуванням колізій. Коли кілька об'єктів потрапляють в один "кошик" (bucket), вони зберігаються там у вигляді списку (або збалансованого дерева, починаючи з Java 8, для підвищення продуктивності).
>
> **Коли колізії стають проблемою?**
> Якщо ваш метод `hashCode()` написаний погано і генерує **дуже багато колізій** (наприклад, завжди повертає одне й те саме число), то всі об'єкти будуть потрапляти в один кошик. Це вироджує хеш-таблицю у зв'язний список, і продуктивність операцій пошуку/вставки падає з O(1) до O(n).

#### **📌 Що перевіряють:**

*Розуміння того, що колізії є нормою, але їх надмірна кількість свідчить про погану реалізацію `hashCode()` і шкодить продуктивності.*

* * *

### **7 ❓ Питання:**

Чому число 31 часто використовується для обчислення хеш-коду?

> #### **💬 Відповідь:**
>
> Використання непарного простого числа, такого як 31, у формулі `result = 31 * result + c` є евристикою, яка допомагає досягти **кращого розподілу хеш-кодів** і зменшити кількість колізій.
>
> **Причини вибору саме 31:**
>
> 1. **Просте число:** Використання простого числа зменшує ймовірність повторюваних патернів, які можуть виникнути при множенні, якщо множник має спільні дільники з даними.
>
> 2. **Оптимізація компілятором:** Множення на 31 може бути легко оптимізоване JVM, оскільки `31 * i` еквівалентно `(i << 5) - i` (посув на 5 біт вліво мінус саме число). Бітові операції виконуються процесором значно швидше за звичайне множення.
>
> Джошуа Блох у "Effective Java" зазначає, що 31 було обрано як хороше непарне просте число, що демонструє хороші результати розподілу і піддається оптимізації. Хоча можна було б використати й інші прості числа (наприклад, 37, 41), 31 стало своєрідним стандартом де-факто.

#### **📌 Що перевіряють:**

*Глибоке розуміння деталей алгоритмів хешування. Знання про мікрооптимізації, які може виконувати JVM.*

* * *

### **8 ❓ Питання:**

Як розрахувати `hashCode` для поля-масиву? ⭐️

> #### **💬 Відповідь:**
>
> Не можна просто викликати `array.hashCode()`, оскільки цей метод не перевизначений для масивів і поверне хеш-код на основі посилання, а не вмісту.
>
> Для коректного розрахунку хеш-коду на основі **вмісту масиву** слід використовувати статичні методи з утилітарного класу `java.util.Arrays`:
>
> * `Arrays.hashCode(array)`: для одновимірних масивів примітивів (`int[]`, `byte[]` тощо) та об'єктів (`String[]`).
> * `Arrays.deepHashCode(objectArray)`: для багатовимірних масивів. Він рекурсивно обчислює хеш-код для вкладених масивів.

#### **📝 Приклад:**

```java
import java.util.Arrays;
import java.util.Objects;

public class DataPacket {
    private String type;
    private byte[] data;

    // ...

    @Override
    public int hashCode() {
        int result = Objects.hash(type);
        // Правильно комбінуємо хеш-код масиву
        result = 31 * result + Arrays.hashCode(data);
        return result;
    }

    // Альтернатива - передати масив у Objects.hash
    @Override
    public int hashCode() {
        return Objects.hash(type, Arrays.hashCode(data));
    }
}
```

#### **📌 Що перевіряють:**

*Уважність до деталей при роботі з масивами та знання відповідних утилітарних методів з класу `Arrays`.*

* * *

### **9 ❓ Питання:**

Припустимо, `hashCode()` для класу завжди повертає `42`. Чи буде такий клас коректно працювати з `HashMap`? Чи буде він порушувати контракт? ⭐️

> #### **💬 Відповідь:**
>
> * **Чи порушує це контракт?**
>     Ні, формально **контракт не порушено**. Основне правило вимагає, що якщо об'єкти рівні, їх хеш-коди мають бути рівні. Якщо хеш-код завжди `42`, то для будь-яких двох рівних об'єктів він теж буде рівним (42 == 42).
>
> * **Чи буде це коректно працювати?**
>     Так, `HashMap` та `HashSet` будуть **коректно** працювати в тому сенсі, що вони не загублять дані і будуть повертати правильні результати.
>
> * **Яка буде продуктивність?**
>     Продуктивність буде **жахливою**. Всі об'єкти потраплять в один і той самий "кошик" (bucket). Це перетворить `HashMap` на зв'язний список (або дерево). Операції `put`, `get`, `remove` будуть виконуватися за лінійний час **O(n)**, а не за константний **O(1)**.
>
> **Висновок:** Хоча такий `hashCode` є "легальним" з точки зору контракту, він є вкрай поганою практикою, оскільки зводить нанівець усі переваги хеш-таблиць.

#### **📌 Що перевіряють:**

*Здатність розрізняти "коректність" (дотримання контракту) та "ефективність" (хороший розподіл). Розуміння того, як якість реалізації `hashCode` впливає на асимптотичну складність операцій у хеш-колекціях.*

* * *

### **10 ❓ Питання:**

Яким буде результат `hashCode()` для рядка `"Aa"` і `"BB"` в Java і що це демонструє? ⭐️

> #### **💬 Відповідь:**
>
> Хеш-коди для цих рядків будуть **однаковими**.
> ` "Aa".hashCode() ` поверне `2112`.
> ` "BB".hashCode() ` поверне `2112`.
>
> Алгоритм розрахунку хеш-коду для `String` в Java має такий вигляд: `s[0]*31^(n-1) + s[1]*31^(n-2) + ... + s[n-1]`, де `s[i]` — це `char` код `i`-го символа.
>
> * Для `"Aa"`: `(int)'A' * 31 + (int)'a'` -> `65 * 31 + 97` = `2015 + 97` = `2112`.
> * Для `"BB"`: `(int)'B' * 31 + (int)'B'` -> `66 * 31 + 66` = `2046 + 66` = `2112`.
>
> Це класичний приклад **колізії**. Він демонструє, що навіть для дуже коротких і різних рядків хеш-коди можуть збігатися. Це ще раз підтверджує, що рівність хеш-кодів не означає рівність об'єктів, і тому після перевірки хешу хеш-колекція зобов'язана викликати `equals()`.

#### **📌 Що перевіряють:**

*Знання про існування колізій та, можливо, знання конкретного алгоритму хешування для `String` (хоча знати точні розрахунки не є обов'язковим).*

* * *

### **11 ❓ Питання:**

Чи можна використовувати поля `long`, `double` або `boolean` напряму при розрахунку `hashCode()`? Якщо ні, то як їх перетворити? ⭐️

> #### **💬 Відповідь:**
>
> Ні, напряму використовувати не можна, оскільки хеш-код є `int`. Потрібно виконати коректне перетворення.
>
> * **`boolean`**:
>     Перетворюється на `int`: `(b ? 1 : 0)`.
>
> * **`long`**:
>     Це 64-бітне число. Щоб отримати з нього 32-бітний `int`, який враховує всі біти, використовується операція XOR між старшою та молодшою частинами: `(int) (value ^ (value >>> 32))`. Це розподіляє біти старшої половини `long` на молодшу.
>
> * **`double`**:
>     Спочатку потрібно отримати його бітове представлення у вигляді `long` за допомогою `Double.doubleToLongBits(value)`, а потім застосувати до цього `long` той самий трюк з XOR, що й для звичайного `long`. Це необхідно для коректної обробки `NaN` та `-0.0`. Аналогічно для `float` використовується `Float.floatToIntBits(value)`.
>
> Усі ці перетворення автоматично і правильно виконує `Objects.hash()`, тому його використання значно спрощує життя.

#### **📌 Що перевіряють:**

*Розуміння того, як працювати з різними примітивними типами при побудові хеш-коду, та знання стандартних методів для їх коректного перетворення в `int`.*

* * *

### **12 ❓ Питання:**

Чому мутабельні об'єкти (змінні) є поганими кандидатами для ключів у `HashMap`? Як це пов'язано з `hashCode()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо мутабельний об'єкт використовується як ключ, і після додавання в `HashMap` його стан змінюється так, що це впливає на `hashCode()`, об'єкт буде "втрачено" всередині мапи.
>
> **Як це відбувається:**
>
> 1. Ви створюєте об'єкт-ключ, наприклад `MutableKey key = new MutableKey("initial")`.
> 2. `HashMap` обчислює його хеш-код (наприклад, `H1`) і розміщує запис у відповідний кошик `B1`.
> 3. Потім ви змінюєте стан ключа: `key.setData("changed")`.
> 4. Тепер `key.hashCode()` повертає нове значення (наприклад, `H2`).
> 5. Коли ви намагаєтеся отримати значення за цим ключем (`map.get(key)`), `HashMap` обчислює його поточний хеш-код `H2`, шукає кошик `B2` і, не знайшовши там нічого, повертає `null`.
>
> Об'єкт фізично залишається в кошику `B1`, але тепер до нього немає доступу, оскільки його поточний хеш-код вказує на зовсім інше місце. Це призводить до витоку пам'яті та непередбачуваної поведінки.
>
> **Висновок:** Ключі `HashMap` повинні бути імутабельними або, принаймні, їх стан, що впливає на `hashCode()` та `equals()`, не повинен змінюватися після додавання в мапу.

#### **📌 Що перевіряють:**

*Глибоке розуміння взаємодії стану об'єкта, його хеш-коду та внутрішньої роботи `HashMap`. Здатність передбачити наслідки використання мутабельних ключів.*

* * *

### **13 ❓ Питання:**

Що таке ідентифікаційний хеш-код (`identity hash code`) і чи можна його отримати для об'єкта з перевизначеним `hashCode()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ідентифікаційний хеш-код** — це хеш-код, який би згенерував для об'єкта оригінальний метод `Object.hashCode()` (зазвичай на основі адреси в пам'яті), незалежно від того, перевизначений метод `hashCode()` чи ні.
>
> Так, його можна отримати за допомогою статичного методу `System.identityHashCode(Object obj)`.
>
> **Коли це корисно?**
>
> 1. Коли вам потрібно використовувати об'єкт як ключ у хеш-мапі, яка повинна розрізняти об'єкти за їхньою ідентичністю (посиланням), а не за логічною рівністю. Наприклад, для зберігання метаданих про конкретні екземпляри об'єктів.
> 2. Для реалізації `IdentityHashMap` — спеціального типу мапи, яка використовує `==` для порівняння ключів та `System.identityHashCode()` для хешування, ігноруючи перевизначені `equals` та `hashCode`.
> 3. Для налагодження, щоб перевірити, чи два посилання вказують на той самий фізичний об'єкт.

#### **📌 Що перевіряють:**

*Знання про існування `System.identityHashCode()` та `IdentityHashMap`. Розуміння різниці між логічною рівністю та ідентичністю об'єкта.*

* * *

### **14 ❓ Питання:**

Як реалізація `String.hashCode()` змінилася з часом у Java і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Алгоритм хешування для `String` зазнав змін для покращення продуктивності та безпеки.
>
> **До Java 1.2:** `hashCode()` просто обходив рядок і додавав значення символів, що призводило до великої кількості колізій для рядків з однаковими наборами символів в іншому порядку (анаграми).
>
> **З Java 1.2:** Був введений знайомий нам алгоритм `s[0]*31^(n-1) + ...`. Він забезпечив значно кращий розподіл. Однак цей результат **не кешувався**. Кожен виклик `hashCode()` для незмінного рядка змушував проходити по ньому знову.
>
> **З Java 1.2, але пізніше (точніше, з `String` став імутабельним):** `hashCode()` **почав кешуватися**. При першому виклику хеш-код обчислюється і зберігається у приватному полі `hash`. Усі наступні виклики просто повертають це збережене значення. Оскільки `String` є імутабельним, його хеш-код ніколи не зміниться, тому кешування є безпечним та дуже ефективним.
>
> **З Java 7u6:** Для захисту від DoS-атак через "хеш-колізійні бомби" у `HashMap`/`HashSet` був введений альтернативний (додатковий) алгоритм хешування. Якщо виявляється велика кількість колізій для рядкових ключів в одній мапі, вона може переключитися на іншу хеш-функцію, менш передбачувану.
>
> Ці зміни демонструють еволюцію платформи у відповідь на проблеми продуктивності та безпеки.

#### **📌 Що перевіряють:**

*Глибокі знання історії та еволюції Java. Розуміння причин, що стоять за змінами в реалізації стандартних класів (кешування, безпека).*

* * *

### **15 ❓ Питання:**

Якщо `hashCode()` повертає випадкове число, чи порушує це контракт `equals()`/`hashCode()`? Які наслідки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання перевіряє розуміння відразу кількох правил контракту.
>
> **Чи порушує це контракт?**
> Так, це порушує відразу два правила:
>
> 1. **Контракт `hashCode()` і `equals()`:** Правило `a.equals(b) => a.hashCode() == b.hashCode()` буде порушено. Два рівних об'єкти майже напевно отримають різні випадкові хеш-коди.
> 2. **Контракт самого `hashCode()`:** Правило **консистентності** буде порушено. При кожному виклику `hashCode()` для того ж самого об'єкта він буде повертати нове випадкове число.
>
> **Які наслідки?**
>
> Це призведе до **повного хаосу** в роботі з хеш-колекціями.
>
> ```java
> Random random = new Random();
> public int hashCode() {
>     return random.nextInt();
> }
>
> Map<MyObject, String> map = new HashMap<>();
> MyObject key = new MyObject("data");
>
> map.put(key, "value"); // Об'єкт покладено в кошик, що відповідає hashCode()=H1
>
> // При спробі отримати:
> // 1. map.get(key) знову викликає key.hashCode().
> // 2. Він повертає нове випадкове число H2.
> // 3. HashMap шукає в іншому кошику і нічого не знаходить.
> String value = map.get(key); // Майже завжди поверне null!
> ```
>
> Ви не зможете навіть отримати щойно доданий елемент за тим самим ключем.
>
> **Висновок:** `hashCode()` повинен бути детермінованою функцією від незмінного стану об'єкта. Будь-яка випадковість або залежність від зовнішніх факторів є неприпустимою.

#### **📌 Що перевіряють:**

*Здатність аналізувати складні сценарії та їх наслідки, спираючись на фундаментальні правила. Розуміння поняття "детермінована функція" та його важливості для `hashCode()`.*
