---
layout: default
title: "Використання Objects.equals() і Objects.hash() (Java 7+)"
parent: "`equals()`, `hashCode()`, `clone()`"
grand_parent: "Java Core"
nav_order: 9
---

# Використання Objects.equals() і Objects.hash() (Java 7+)

* короткий опис: Утилітні методи з `java.util.Objects` (з Java 7+): `Objects.equals(a, b)` для безпечного порівняння об'єктів (`null`-safe) та `Objects.hash(...)` для генерації хеш-коду з кількох полів.

* поняття: `Objects.equals()`, `Objects.hash()`, `null`-safe, Java 7+.

* приклади: `Objects.equals(name, user.name);` (безпечне порівняння), `return Objects.hash(name, age);` (генерація хешу).

* ключові моменти: Спрощують написання `equals()` та `hashCode()`, підвищують читабельність, уникають `NullPointerException`.

* що важливо знати на співбесіді: Як спростити `equals()` та `hashCode()`, їхні переваги.

### **✅ Офіційне визначення:**

Методи **`Objects.equals(a, b)`** і **`Objects.hash(...)`** — це утилітні методи з класу **`java.util.Objects`**, що були додані у **`Java 7`**. Вони використовуються для **безпечного порівняння обʼєктів** та **генерації хеш-кодів**, зменшуючи ризик помилок і спрощуючи код.

### **🧠 Простими словами:**

Це зручні методи, щоб не писати вручну перевірки на **`null`** або обчислення хешу. **`Objects.equals()`** дозволяє порівнювати два значення, не боячись **`NullPointerException`**, а **`Objects.hash()`** — автоматично генерує хеш на основі потрібних полів

---

## **Основні методи та категорії**

📊 Основні методи класу **`Objects`** для **`equals()`** і **`hashCode()`**:

| Метод | Призначення |
| ----- | ----- |
| **`Objects.equals(a, b)`** | Безпечне порівняння, працює з **`null`** |
| **`Objects.hash(a, b, ...)`** | Генерація хешу з кількох полів |

---

## **Пояснення на пальцях (💡)**

Замість:

```java
if (name != null && name.equals(other.name)) ...
```

можеш просто написати:

```java
Objects.equals(name, other.name)
```

Це зручно, коли **може бути `null`** — метод сам перевіряє це всередині.

Так само, **`Objects.hash(...)`** — це аналог ручного хешування через **`31 * ...`** формулу, але коротше й читабельніше

---

## **Корисні приклади (🧪)**

**✅ equals() з `Objects.equals()`:**

```java
@Override
public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass()) return false;
    User user = (User) o;
    return age == user.age &&
           Objects.equals(name, user.name);
}
```

**✅ hashCode() з `Objects.hash()`:**

```java
@Override
public int hashCode() {
    return Objects.hash(name, age);
}
```

---

## **Порівняння (⚖️)**

| Порівняння | Стандартний підхід | `Objects`\-підхід |
| ----- | ----- | ----- |
| **`equals()`** | **`a != null && a.equals(b)`** | **`Objects.equals(a, b)`** |
| **`hashCode()`** | ручна формула з **`31 * ...`** | **`Objects.hash(a, b)`** |
| Null-безпечність | ❌ ні | ✅ так |
| Читабельність | 🟡 середня | ✅ висока |
| Продуктивність (похибка) | 🔼 швидше | 🔽 трохи повільніше (незначно) |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧠 **`Objects.equals(a, b) = null`**\-**safe equals**

🧠 **`Objects.hash(...)`** \= **easy multi-field hash**

---

## **🟩 Підсумок:**

**`Objects.equals()`** і **`Objects.hash()`** — це сучасний і зручний спосіб реалізувати методи **`equals()`** та **`hashCode()`** без перевірок на **`null`** і ручного множення. Вони підвищують **читабельність**, зменшують кількість помилок і роблять код чистішим. Обов'язково використовуй їх у реальному проекті, якщо не потрібна спеціалізована логіка.

---

### **🔗 Додаткові джерела:**

* [**Java 7: Objects.equals() — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/Objects.html#equals-java.lang.Object-java.lang.Object-)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке клас `java.util.Objects` і для чого він призначений?

> #### **💬 Відповідь:**
>
> `java.util.Objects` — це **утилітарний (utility)** клас, який був доданий в Java 7. Він містить набір **статичних методів-помічників** для роботи з об'єктами. Його основне призначення — спростити написання коду, зробити його більш надійним і читабельним.
>
> Він надає методи для:
>
> * Безпечного порівняння об'єктів ( `Objects.equals()`).
> * Безпечної генерації хеш-коду ( `Objects.hash()`, `Objects.hashCode()`).
> * Перевірки на `null` з викиданням винятку ( `Objects.requireNonNull()`).
> * Отримання рядкового представлення, безпечного до `null` ( `Objects.toString()`).
>
> Оскільки всі його методи статичні, він не призначений для створення екземплярів (його конструктор є приватним).

#### **📌 Що перевіряють:**

*Загальні знання стандартної бібліотеки Java, зокрема класів, що були додані в новіших версіях JDK для покращення зручності розробки.*

* * *

### **2 ❓ Питання:**

Яку проблему вирішує метод `Objects.equals(a, b)`?

> #### **💬 Відповідь:**
>
> `Objects.equals(a, b)` вирішує проблему **безпечного порівняння двох об'єктів, які можуть бути `null`**.
>
> **Проблема, яку він вирішує:**
>
> Якщо ви пишете `a.equals(b)`, і при цьому об'єкт `a` дорівнює `null`, ваш код кине `NullPointerException`.
> Щоб уникнути цього, раніше доводилося писати громіздкі перевірки:
> `(a == null) ? (b == null) : a.equals(b)`
>
> Метод `Objects.equals(a, b)` інкапсулює цю логіку всередині себе. Він повертає `true`, якщо обидва об'єкти `null` або якщо `a.equals(b)` повертає `true`, і `false` у всіх інших випадках, не кидаючи `NullPointerException`.
>
> **Реалізація `Objects.equals()`:**
>
> ```java
> public static boolean equals(Object a, Object b) {
>     return (a == b) || (a != null && a.equals(b));
> }
> ```
>
> Це робить код для порівняння полів в методі `equals()` значно чистішим та безпечнішим.

#### **📝 Коментар:**</h4>

Це ідеальний приклад того, як утилітарний метод забирає на себе шаблонну логіку, дозволяючи розробнику зосередитися на бізнес-завданнях.

#### **📌 Що перевіряють:**

*Розуміння проблеми `NullPointerException` при порівнянні об'єктів та знання стандартного, елегантного способу її вирішення.*

* * *

### **3 ❓ Питання:**

Яку проблему вирішує метод `Objects.hash(Object... values)`?

> #### **💬 Відповідь:**
>
> `Objects.hash()` вирішує проблему **простого, надійного та читабельного обчислення хеш-коду на основі кількох полів**.
>
> **Проблема, яку він вирішує:**
>
> Раніше для обчислення хеш-коду доводилося вручну писати "класичний" алгоритм, який виглядав приблизно так:
>
> ```java
> int result = 17;
> result = 31 * result + field1.hashCode();
> result = 31 * result + (field2 == null ? 0 : field2.hashCode());
> // ... і так далі для кожного поля
> ```
>
> Цей код був громіздким, схильним до помилок (можна було забути поле або перевірку на `null`) і менш читабельним.
>
> Метод `Objects.hash(field1, field2, ...)` автоматизує цей процес. Він приймає змінну кількість аргументів, коректно обробляє `null` для кожного з них і комбінує їхні хеш-коди, використовуючи доведений алгоритм. Це робить код реалізації `hashCode()` надзвичайно лаконічним: `return Objects.hash(field1, field2);`.

#### **📌 Що перевіряють:**

*Розуміння того, як `Objects.hash` спрощує реалізацію `hashCode` та усуває необхідність у ручному написанні "магічних" формул.*

* * *

### **4 ❓ Питання:**

Наведіть приклад реалізації методів `equals()` та `hashCode()` з використанням класу `Objects`.

> #### **💬 Відповідь:**
>
> Ось канонічний приклад для класу `User` з полями `name` та `age`.
>
> ```java
> import java.util.Objects;
>
> public class User {
>     private String name;
>     private int age;
>
>     // Конструктори, геттери...
>
>     @Override
>     public boolean equals(Object o) {
>         if (this == o) return true;
>         if (o == null || getClass() != o.getClass()) return false;
>         User user = (User) o;
>         // Порівнюємо примітив через ==
>         // Порівнюємо об'єкт через Objects.equals()
>         return age == user.age && Objects.equals(name, user.name);
>     }
>
>     @Override
>     public int hashCode() {
>         // Просто передаємо всі значущі поля в Objects.hash()
>         return Objects.hash(name, age);
>     }
> }
> ```
>
> Цей код є стандартним де-факто для реалізації цих методів у сучасній Java. Він чистий, безпечний і легко підтримується.

#### **📌 Що перевіряють:**

*Практичні навички застосування утиліт з класу `Objects` для написання ідіоматичного Java-коду.*

* * *

### **5 ❓ Питання:**

Чим відрізняються `Objects.hash(obj)` та `Objects.hashCode(obj)`? ⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом, оскільки ці два методи мають різне призначення.
>
> * **`Objects.hashCode(Object obj)`**:
>   * Приймає **один** об'єкт.
>   * Його єдина мета — повернути хеш-код об'єкта, будучи `null-safe`. Якщо `obj` є `null`, він повертає `0`. Якщо ні, він повертає `obj.hashCode()`.
>   * **Призначення:** Отримати хеш-код одного поля, не боячись `NullPointerException`.
>
> * **`Objects.hash(Object... values)`**:
>   * Приймає **змінну кількість** об'єктів (varargs).
>   * **Призначення:** **Скомбінувати** хеш-коди кількох полів в один фінальний хеш-код для всього об'єкта. Він всередині використовує алгоритм, подібний до ручного множення на 31.
>
> **Коли який використовувати:**
>
> * Викликайте `Objects.hash()` один раз у вашому методі `hashCode()`, передаючи туди всі потрібні поля.
> * `Objects.hashCode()` використовується неявно всередині `Objects.hash()`, але ви можете використовувати його і при ручній реалізації, якщо вам потрібно отримати хеш-код одного конкретного поля, яке може бути `null`.

#### **📌 Що перевіряють:**

*Уважність до деталей та знання сигнатур і призначення різних утилітарних методів.*

* * *

### **6 ❓ Питання:**

Чи є якісь недоліки у використанні `Objects.hash()` порівняно з ручною реалізацією? ⭐️

> #### **💬 Відповідь:**
>
> Так, є один потенційний недолік, пов'язаний з продуктивністю, хоча він рідко є суттєвим.
>
> **Недолік:**
>
> Метод `Objects.hash(Object... values)` приймає аргументи як varargs. "Під капотом" це означає, що перед викликом методу створюється **новий масив** (`Object[]`) для зберігання переданих аргументів. Також може відбуватися автобоксинг (autoboxing) примітивних типів (`int` -> `Integer`).
>
> **Наслідки:**
>
> * **Виділення пам'яті:** Створення цього тимчасового масиву призводить до невеликого додаткового виділення пам'яті при кожному виклику `hashCode()`.
> * **Продуктивність:** У дуже гарячих циклах (hot loops), де `hashCode()` викликається мільйони разів на секунду, це додаткове навантаження на збирач сміття та процесор може стати помітним.
>
> **Коли це важливо?**
>
> У 99.9% випадків ця різниця в продуктивності є несуттєвою і нею можна знехтувати заради читабельності та надійності коду. Ручну реалізацію `hashCode` варто розглядати тільки якщо профілювання показало, що `Objects.hash()` є реальним "вузьким місцем" у вашому застосунку.

#### **📌 Що перевіряють:**

*Розуміння того, як працюють varargs та автобоксинг, та їх впливу на продуктивність. Здатність аналізувати компроміси між зручністю та швидкодією.*

* * *

### **7 ❓ Питання:**

Як реалізація `Objects.equals(a, b)` гарантує відсутність `NullPointerException`?

> #### **💬 Відповідь:**
>
> Це гарантується порядком перевірок всередині методу. Подивимося на його реалізацію:
>
> ```java
> public static boolean equals(Object a, Object b) {
>     return (a == b) || (a != null && a.equals(b));
> }
> ```
>
> 1. **`a == b`**: Першою виконується ця перевірка.
>     * Якщо `a` і `b` — це те саме посилання, повертається `true`.
>     * Якщо і `a`, і `b` є `null`, то `null == null` повертає `true`. Це коректно.
>
> 2. **`a != null && a.equals(b)`**: Ця частина виконується, тільки якщо перша була `false`. Завдяки "короткому замиканню" (`short-circuiting`) оператора `&&`:
>     * Якщо `a` є `null`, то `a != null` повертає `false`, і друга частина `a.equals(b)` **взагалі не буде виконуватися**. Таким чином, `NullPointerException` не виникає.
>     * Якщо `a` не є `null`, тільки тоді викликається `a.equals(b)`, що тепер є безпечним.
>
> Таким чином, порядок і використання логічного "І" з коротким замиканням є ключем до `null`-безпеки цього методу.

#### **📌 Що перевіряють:**

*Розуміння роботи логічних операторів, зокрема "короткого замикання", та вміння аналізувати код.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати `Objects.hash()` для полів-масивів? Якщо так, то як правильно? ⭐️

> #### **💬 Відповідь:**
>
> Якщо ви просто передасте масив у `Objects.hash()`, наприклад `Objects.hash(myArray)`, то він буде оброблений як один об'єкт, і буде викликаний його стандартний `hashCode()`, який базується на посиланні, а не на вмісті. Це неправильно.
>
> **Правильний спосіб** — використовувати утилітарні методи з класу `java.util.Arrays` для обчислення хеш-коду на основі вмісту масиву, і передати вже цей `int` результат в `Objects.hash()` (або просто використовувати його напряму).
>
> **Приклад:**
>
> ```java
> @Override
> public int hashCode() {
>     // НЕПРАВИЛЬНО: Objects.hash(field1, myArray);
>
>     // ПРАВИЛЬНО:
>     // 1. Попередньо обчислити хеш-код масиву
>     int arrayHashCode = Arrays.hashCode(myArray);
>
>     // 2. Скомбінувати його з іншими полями
>     return Objects.hash(field1, arrayHashCode);
> }
> ```>
> Хоча `Objects.hash()` спрощує роботу з більшістю полів, для масивів все одно потрібно пам'ятати про використання `Arrays.hashCode()` або `Arrays.deepHashCode()`.

#### **📌 Що перевіряють:**

*Уважність до деталей при роботі з масивами та знання, що для них потрібні спеціальні утиліти, які не покриваються `Objects.hash` автоматично.*

* * *

### **9 ❓ Питання:**

Чому `Objects.equals` не є достатнім для реалізації `equals` у випадку успадкування? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Objects.equals()` є лише **утилітою для безпечного порівняння полів**. Він нічого не знає про контракт `equals`, зокрема, про перевірку типів класів.
>
> **Неправильна реалізація:**
>
> ```java
> @Override
> public boolean equals(Object obj) { // НЕПРАВИЛЬНО!
>     // Немає перевірок на this == obj, null та тип класу
>     User user = (User) obj; // Кине ClassCastException для іншого типу
>     return Objects.equals(name, user.name);
> }
> ```
>
> **Проблема:**
> Метод `equals` має обов'язково містити логіку перевірки типів (`getClass() != obj.getClass()`), щоб дотримуватися контракту (зокрема, симетричності). `Objects.equals()` цю перевірку не робить.
>
> **Правильний підхід:**
> `Objects.equals` використовується **всередині** правильно структурованого методу `equals` **після** всіх необхідних перевірок.
>
> ```java
> @Override
> public boolean equals(Object o) {
>     if (this == o) return true;
>     if (o == null || getClass() != o.getClass()) return false;
>     User user = (User) o;
>     return Objects.equals(name, user.name); // Ось тут він на своєму місці
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння того, що `Objects.equals` — це лише інструмент, а не повна заміна для реалізації методу. Здатність бачити повну картину реалізації, а не тільки її частини.*

* * *

### **10 ❓ Питання:**

Якщо у вашому класі тільки одне поле, чи є перевага у `Objects.hashCode()` над `field.hashCode()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, перевага є, і вона полягає в **обробці `null`**.
>
> * Якщо ви напишете `return field.hashCode();`, і `field` буде `null`, ви отримаєте `NullPointerException`.
> * Якщо ви напишете `return Objects.hashCode(field);`, він коректно обробить `null` і поверне `0`.
>
> З точки зору продуктивності, `Objects.hashCode()` буде трохи повільнішим через додаткову перевірку на `null`, але ця різниця є мізерною, а виграш у надійності — значним.
>
> **Щодо `Objects.hash()` (з `s` в кінці):**
> Для одного поля `return Objects.hash(field)` також буде працювати, але буде ще трохи повільнішим, оскільки він створює тимчасовий масив для одного елемента. Тому для одного поля, яке може бути `null`, **`Objects.hashCode(field)` є найбільш оптимальним та безпечним вибором**.

#### **📌 Що перевіряють:**

*Глибоке розуміння API `Objects` та здатність вибрати найбільш відповідний метод для конкретної ситуації (1 поле vs. N полів).*

* * *

### **11 ❓ Питання:**

Який інший корисний метод з класу `Objects`, крім `equals` та `hash`, часто використовується для написання надійного коду?

> #### **💬 Відповідь:**
>
> Дуже корисним є метод **`Objects.requireNonNull(T obj, String message)`**.
>
> **Його призначення** — перевіряти, що посилання не є `null`, і кидати `NullPointerException` з інформативним повідомленням, якщо це так.
>
> Це широко використовується для валідації аргументів у конструкторах та публічних методах (патерн "fail-fast").
>
> **Замість цього:**
>
> ```java
> public User(String name) {
>     if (name == null) {
>         throw new NullPointerException("Ім'я не може бути null");
>     }
>     this.name = name;
> }
> ```
>
> **Пишуть це:**
>
> ```java
> import static java.util.Objects.requireNonNull;
>
> public User(String name) {
>     this.name = requireNonNull(name, "Ім'я не може бути null");
> }
> ```
>
> Це робить код значно більш стислим і є стандартною практикою для перевірки інваріантів у конструкторах.

#### **📌 Що перевіряють:**

*Ширше знання класу `Objects` та сучасних підходів до валідації аргументів.*

* * *

### **12 ❓ Питання:**

Припустимо, клас `Objects` не існував би. Напишіть, як би виглядав еквівалент `Objects.hash("hello", null, 123)`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Еквівалент реалізовував би класичний алгоритм з ручною обробкою `null` та примітивів.
>
> ```java
> public int manualHash(String str, Object obj, int num) {
>     int result = 1; // або інша початкова константа
>
>     // 1. Обробка першого поля
>     result = 31 * result + (str == null ? 0 : str.hashCode());
>
>     // 2. Обробка другого поля
>     result = 31 * result + (obj == null ? 0 : obj.hashCode());
>
>     // 3. Обробка третього поля
>     result = 31 * result + num;
>
>     return result;
> }
> ```
>
> `Objects.hash` по суті робить те ж саме, що й `Arrays.hashCode(new Object[]{...})`, який реалізує подібний цикл. Цей приклад наочно демонструє, скільки шаблонного та схильного до помилок коду приховує за собою один виклик `Objects.hash()`.

#### **📌 Що перевіряють:**

*Здатність "розгорнути" утилітарний метод у його ручну реалізацію, що демонструє глибоке розуміння алгоритму, який лежить в його основі.*

* * *

### **13 ❓ Питання:**

Як IDE (наприклад, IntelliJ IDEA) використовує `Objects.equals()` та `Objects.hash()` при автоматичній генерації методів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Сучасні IDE є достатньо розумними, щоб генерувати код `equals()` та `hashCode()` відповідно до найкращих практик.
>
> **Коли ви використовуєте функцію "Generate `equals()` and `hashCode()`" в IntelliJ IDEA:**
>
> 1. IDE аналізує версію Java, налаштовану для вашого проекту.
>
> 2. **Якщо версія Java 7 або вище**, IDE за замовчуванням запропонує вам згенерувати методи, які використовують **`Objects.equals()` та `Objects.hash()`**. Це є шаблоном за замовчуванням.
>
> 3. **Якщо версія Java 6 або нижче**, IDE згенерує "класичну" реалізацію з ручними перевірками на `null` та формулою з множенням на 31.
>
> 4. **IDE також дозволяє налаштувати** шаблон генерації. Ви можете вибрати, які поля включити, і вибрати один з кількох шаблонів (наприклад, на основі `Objects`, `Arrays` або класичний).
>
> Це чудовий приклад того, як інструменти розробки сприяють поширенню найкращих практик. Розробнику не потрібно навіть пам'ятати точний синтаксис — IDE зробить все за нього, використовуючи найбільш сучасний та надійний підхід.

#### **📌 Що перевіряють:**

*Розуміння того, як інструменти розробки допомагають писати якісний код, та обізнаність у їхніх можливостях.*

* * *

### **14 ❓ Питання:**

Чи можна реалізувати `Objects.deepEquals(a,b)`? Якою була б його логіка? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна, і такий метод існує в `java.util.Objects`. Метод `Objects.deepEquals(a, b)` призначений для **глибокого порівняння** двох об'єктів.
>
> **Його логіка така:**
>
> 1. Він виконує ті самі початкові перевірки, що й `Objects.equals()` (`a == b`, перевірка на `null`).
>
> 2. **Ключова відмінність:** Якщо він бачить, що обидва об'єкти (`a` і `b`) є **масивами**, він не викликає `a.equals(b)` (який порівнює посилання). Замість цього він делегує роботу відповідному методу з `java.util.Arrays`:
>     * Якщо це масиви об'єктів — `Arrays.deepEquals(a, b)`.
>     * Якщо це масиви примітивів — `Arrays.equals(a, b)` для відповідного типу.
>
> 3. Якщо об'єкти не є масивами, він поводиться так само, як і `Objects.equals()`, викликаючи `a.equals(b)`.
>
> **Коли він корисний?**
> Коли вам потрібно порівняти два об'єкти, які **можуть бути масивами**. Це узагальнена версія `Arrays.equals` та `Arrays.deepEquals`, яка також коректно працює для не-масивів.

#### **📌 Що перевіряють:**

*Знання про існування `deepEquals` та розуміння різниці між ним та звичайним `equals`, особливо в контексті роботи з масивами.*

* * *

### **15 ❓ Питання:**

Чи гарантує `Objects.hash()` унікальність хеш-кодів для різних наборів аргументів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не гарантує**.
>
> `Objects.hash()` — це хеш-функція, а для будь-якої хеш-функції, яка відображає великий простір вхідних даних у менший простір вихідних (у даному випадку, нескінченну кількість можливих комбінацій об'єктів у 32-бітний `int`), **колізії є неминучими**.
>
> `Objects.hash()` лише прагне до того, щоб:
>
> * Дотримуватися контракту (`a.equals(b)` -> `hashCode(a) == hashCode(b)`).
> * Забезпечити хороший **розподіл** хеш-кодів, щоб мінімізувати кількість колізій на практиці.
>
> **Приклад колізії:**
> Навіть для стандартних типів можна знайти колізії. Наприклад, `Objects.hash("Aa")` та `Objects.hash("BB")` можуть дати однаковий результат, тому що `String.hashCode()` для цих рядків однаковий.
>
> **Висновок:** Унікальність не гарантується. Саме тому після перевірки на рівність хеш-кодів завжди слідує перевірка через `equals()` для вирішення колізій.

#### **📌 Що перевіряють:**

*Глибоке розуміння фундаментальних властивостей хешування, зокрема неминучості колізій. Здатність чітко розрізняти "мінімізацію колізій" та "гарантію унікальності".*
