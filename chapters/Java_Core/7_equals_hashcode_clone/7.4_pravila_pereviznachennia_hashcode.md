---
layout: default
title: "Правила перевизначення hashCode()"
parent: "`equals()`, `hashCode()`, `clone()`"
grand_parent: "Java Core"
nav_order: 4
---

# Правила перевизначення hashCode()

* короткий опис: `hashCode()` повинен повертати однакове значення для об'єктів, які вважаються рівними згідно з `equals()`. Він має бути консистентним (не змінюватися, якщо поля не змінюються) і враховувати ті ж поля, що й `equals()`.

* поняття: Контракт `hashCode()`, консистентність, колізії.

* приклади: Ручна реалізація з простими числами (`17`, `31`), використання `Objects.hash(...)`.

* ключові моменти: Завжди перевизначати разом з `equals()`, інакше хеш-колекції працюватимуть некоректно. Колізії допустимі, але їх слід мінімізувати.

* що важливо знати на співбесіді: Головне правило, типові помилки.

### **✅ Офіційне визначення:**

Метод **`hashCode()`** повинен повертати однакове значення для об'єктів, які вважаються рівними згідно з методом **`equals()`**. Він використовується у хеш-структурах (**`HashMap`**, **`HashSet`**) для обчислення "**`індексу`**" або "**`коробки`**", куди слід помістити об'єкт. Контракт між **`equals()`** і **`hashCode()`** є обов’язковим: **якщо `equals()` повертає true, то `hashCode()` має повертати однакове значення**.

### **🧠 Простими словами:**

Якщо Java вважає два обʼєкти однаковими (**`equals()`**), то вона **очікує**, що їхній **`hashCode()`** також буде однаковим. Інакше вона їх "розкладе" в різні ящики і ніколи не знайде. Це як ID — різні обʼєкти можуть мати однаковий ID (колізія), але однакові обʼєкти **повинні** мати той самий

---

## **Основні вимоги**

**📊 Що обов’язково має виконуватись при перевизначенні:**

* **Консистентність**: якщо поле, яке бере участь у **`equals()`**, не змінилося — **`hashCode()`** теж має залишатися таким самим.
* **equals() -> однаковий hashCode()**: якщо два обʼєкти рівні за **`equals()`**, вони **зобовʼязані** мати однаковий хеш.
* **Колізії допустимі**: два різні обʼєкти можуть мати однаковий **`hashCode()`** — це **нормально**, хоча й небажано.
* **Використовуй лише поля, що входять в `equals()`**: не включай у **`hashCode()`** ті поля, які не перевіряються у **`equals()`**.

---

## **Пояснення на пальцях (💡)**

* Пиши **`hashCode()`** так, щоб він **відображав ті самі поля**, що й **`equals()`**.
* Якщо ти зміниш поле, яке впливає на **`hashCode()`** — обʼєкт може "випасти" з мапи або множини.
* Колізії (однакові хеші у різних об’єктів) можливі, і Java з цим впорається — головне, щоб **`equals()`** після хешу визначив, що це різні обʼєкти.

---

## **Корисні приклади (🧪)**

✅ **Ручна реалізація з простими числами**:

```java
@Override
public int hashCode() {
    int result = 17;
    result = 31 * result + (name == null ? 0 : name.hashCode());
    result = 31 * result + age;
    return result;
}
```

📌 Пояснення:

* **`17`** — початкове просте число
* **`31`** — множник, ще одне просте
* **`name.hashCode()`** — безпечне врахування рядка
* **`age`** — int, враховується напряму

✅ **Сучасна альтернатива (Java 7+):**

```java
@Override
public int hashCode() {
    return Objects.hash(name, age);
}
```

**📌 Плюси:** менше коду, читається краще, обробляє **`null`**

---

## **Порівняння (⚖️)**

| Метод | Особливості |
| :---: | :---: |
| Ручне обчислення | Повний контроль, трохи більше коду |
| **`Objects.hash()`** | Простота, автоматична обробка **`null`**, повільніше |

---

## **🧠 Мнемоніка для запам’ятовування:**

📐 **equals -> hashCode**

* Якщо **`equals()`** \= **true** -> **`hashCode()`** **повинен** бути однаковим
* Але якщо **`hashCode()`** однаковий -> це **не гарантує** рівність за **`equals()`**

---

## **🟩 Підсумок:**

Метод **`hashCode()`** потрібно перевизначати завжди разом із **`equals().`** Він повинен враховувати ті самі поля, бути стабільним, не змінюватись випадково, і відповідати контракту. **Ідеальна реалізація** — зрозуміла, коротка, з мінімумом колізій. Можна використовувати як ручний підхід, так і **`Objects.hash(...)`** для простоти.

---

### **🔗 Додаткові джерела:**

* [**Java Object.hashCode() — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/lang/Object.html#hashCode--)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яке найголовніше правило, яке потрібно пам'ятати при перевизначенні `hashCode()`?

> #### **💬 Відповідь:**
>
> Найголовніше і найсуворіше правило, яке є частиною контракту `equals-hashCode`, звучить так:
>
> **"Якщо два об'єкти рівні за методом `equals()`, то їхні методи `hashCode()` зобов'язані повертати однакове ціле число."**
>
> `if (a.equals(b)) { assert a.hashCode() == b.hashCode(); }`
>
> Порушення цього правила призведе до того, що хеш-колекції (`HashMap`, `HashSet`) не зможуть коректно знайти об'єкти, навіть якщо вони логічно рівні, оскільки вони будуть шукати їх у різних "кошиках".

#### **📝 Аналогія:**</h4>

Це як правило для бібліотечного каталогу: якщо дві книги мають однаковий інвентарний номер (аналог `equals()`), то вони зобов'язані стояти на одній і тій самій полиці (мати однаковий `hashCode()`). Якщо це правило порушити, ви ніколи не знайдете другу копію книги, шукаючи її за інвентарним номером.

#### **📌 Що перевіряють:**

*Знання ключового правила контракту, яке є основою для розуміння всіх інших нюансів.*

* * *

### **2 ❓ Питання:**

Які поля класу слід включати в розрахунок `hashCode()`, а які ні?

> #### **💬 Відповідь:**
>
> **Слід включати:**
>
> * Тільки ті поля, які беруть участь у порівнянні в методі `equals()`. Якщо поле не впливає на логічну рівність об'єкта, воно не повинно впливати і на його хеш-код.
>
> **Не слід включати:**
>
> * **Поля, що не використовуються в `equals()`**: Це зайве і може призвести до порушення контракту (два рівні за `equals` об'єкти можуть отримати різний хеш-код, якщо це поле у них відрізняється).
> * **Обчислювані поля (кеш)**: Їх значення залежить від інших полів. Достатньо включити в хеш-код ті поля, на основі яких відбувається обчислення.
> * **Поля-ідентифікатори, які не є частиною логічного стану**: Наприклад, ID з бази даних, якщо `equals` порівнює об'єкти за змістом (ім'я, дата тощо). Включення ID призведе до того, що два логічно однакові об'єкти (ще не збережені в БД) будуть мати різний хеш-код.

#### **📝 Коментар:**

Вибір полів для `hashCode()` має повністю дзеркально відображати вибір полів для `equals()`. Це найпростіший спосіб гарантувати дотримання контракту.

#### **📌 Що перевіряють:**

*Розуміння, що `hashCode` має відображати логічний стан об'єкта так само, як і `equals`, і здатність правильно визначати значущі поля.*

* * *

### **3 ❓ Питання:**

Що таке правило консистентності для `hashCode()`?

> #### **💬 Відповідь:**
>
> Правило консистентності стверджує:
>
> **"Протягом одного виконання Java-застосунку, метод `hashCode()` повинен постійно повертати одне й те саме ціле число для одного й того ж об'єкта, за умови, що поля, які беруть участь у розрахунку `equals()`, не були змінені."**
>
> Це означає, що хеш-код не повинен залежати від випадкових значень, зовнішніх факторів або змінюватися з часом сам по собі.
>
> **Наслідки порушення:** Якщо хеш-код об'єкта, що вже знаходиться в `HashMap` (як ключ), раптом зміниться, цей об'єкт буде "втрачено". Колекція буде шукати його за новим хеш-кодом і не знайде, хоча фізично він залишився в "кошику", що відповідає старому хеш-коду.

#### **📝 Приклад порушення:**</h4>

```java
public class UnstableHash {
    // НЕПРАВИЛЬНО! Хеш-код залежить від випадкового числа
    @Override
    public int hashCode() {
        return new java.util.Random().nextInt();
    }
}
```

#### **📌 Що перевіряють:**

*Розуміння того, що `hashCode` має бути детермінованою функцією стану об'єкта.*

* * *

### **4 ❓ Питання:**

Чому `Objects.hash()` вважається кращим підходом для реалізації `hashCode()` у сучасному Java-коді?

> #### **💬 Відповідь:**
>
> Статичний метод `java.util.Objects.hash(Object... values)` (з'явився в Java 7) є кращим підходом з кількох причин:
>
> 1. **Простота і стислість**: Він замінює кілька рядків ручного обчислення одним лаконічним викликом: `return Objects.hash(field1, field2);`.
> 2. **`Null-safe`**: Він коректно обробляє `null` значення полів. Якщо поле дорівнює `null`, для нього використовується хеш-код `0`, що позбавляє необхідності писати тернарні оператори типу `(field == null ? 0 : field.hashCode())`.
> 3. **Зниження ризику помилок**: Розробнику не потрібно пам'ятати про вибір початкового числа та множника (17, 31). Алгоритм вже реалізовано всередині.
> 4. **Коректна обробка масивів**: Хоча `Objects.hash()` приймає `Object...`, і масив буде переданий як один об'єкт, для коректного хешування вмісту масиву його слід обертати в `Arrays.hashCode()`: `Objects.hash(field, Arrays.hashCode(arrayField))`.
>
> **Недолік:** Для дуже критичних до продуктивності ділянок коду `Objects.hash()` може бути трохи повільнішим за ручну реалізацію через створення тимчасового масиву для varargs та додаткові перевірки. Однак у 99% випадків ця різниця несуттєва.

#### **📌 Що перевіряють:**

*Знання сучасного API та вміння вибирати інструменти, що підвищують надійність та читабельність коду.*

* * *

### **5 ❓ Питання:**

Чи можна реалізувати `hashCode()`, просто повернувши константу, наприклад, `return 42;`? Чи порушує це контракт?

> #### **💬 Відповідь:**
>
> * **Чи порушує це контракт?**
>     Ні, формально **контракт не порушено**. Якщо хеш-код завжди однаковий (`42`), то для будь-яких двох рівних за `equals` об'єктів їхні хеш-коди також будуть рівні (оскільки `42 == 42`).
>
> * **Які наслідки?**
>     Наслідки будуть вкрай негативними для **продуктивності**. При такій реалізації всі об'єкти потраплять в один "кошик" (bucket) в хеш-таблиці.
>     Це вироджує структуру даних `HashMap` або `HashSet` у звичайний зв'язний список (або збалансоване дерево в Java 8+). В результаті час виконання операцій `put`, `get`, `contains`, `remove` деградує з O(1) до **O(n)**.
>
> **Висновок:** Такий `hashCode()` є "легальним", але абсолютно неефективним і зводить нанівець усі переваги хеш-колекцій.

#### **📌 Що перевіряють:**

*Здатність розрізняти формальну коректність (дотримання контракту) та практичну ефективність (хороший розподіл хеш-кодів).*

* * *

### **6 ❓ Питання:**

Як правильно розрахувати хеш-код для полів-примітивів, таких як `long`, `double` та `boolean`?

> #### **💬 Відповідь:**
>
> Оскільки `hashCode()` повертає `int`, примітиви інших типів потрібно коректно перетворити.
>
> * **Для `boolean`**: Перетворюється на `int` ( `true` -> 1, `false` -> 0).
>   * Код: `(myBoolean ? 1 : 0)`
>
> * **Для `long`**: Щоб врахувати всі 64 біти, старші 32 біти "змішуються" з молодшими за допомогою операції XOR.
>   * Код: `(int) (myLong ^ (myLong >>> 32))`
>
> * **Для `float`**: Перетворюється на бітове представлення `int` за допомогою `Float.floatToIntBits(myFloat)`.
>
> * **Для `double`**: Перетворюється на бітове представлення `long` (`Double.doubleToLongBits(myDouble)`), а потім до цього `long` застосовується та сама логіка, що й вище.
>
> Усі ці перетворення правильно реалізовані всередині `Objects.hash()` або відповідних класів-обгорток (`Long.hashCode(long)`, `Double.hashCode(double)`). Тому при ручній реалізації краще використовувати їх, а не писати бітові операції самостійно.

#### **📌 Що перевіряють:**

*Розуміння того, що для примітивів потрібні спеціальні перетворення, і знання, де шукати готові та надійні реалізації цих перетворень (класи-обгортки, `Objects`).*

* * *

### **7 ❓ Питання:**

Що краще: складний `hashCode()`, який дає мало колізій, але працює повільно, чи простий і швидкий, але з більшою кількістю колізій? ⭐️

> #### **💬 Відповідь:**
>
> Тут потрібен **баланс**. Немає єдиної правильної відповіді, вибір залежить від конкретного сценарію.
>
> 1. **Складний, але повільний `hashCode()`**:
>     * **Добре:** Якщо у вас величезна кількість об'єктів у хеш-колекції і ви хочете мінімізувати розмір "кошиків", щоб `equals()` викликався якомога рідше. Це може бути виправдано, якщо сам метод `equals()` є дуже "дорогим".
>     * **Погано:** Якщо обчислення хеш-коду саме по собі стає вузьким місцем у продуктивності.
>
> 2. **Простий і швидкий `hashCode()` з колізіями**:
>     * **Добре:** Якщо у вас не дуже багато елементів у колекції, або якщо метод `equals()` дуже швидкий. У цьому випадку невелика кількість колізій не сильно вплине на загальну продуктивність.
>     * **Погано:** Якщо він генерує занадто багато колізій, продуктивність впаде до O(n).
>
> **Загальна рекомендація:** Починайте з простої та надійної реалізації (як `Objects.hash()`). Оптимізуйте її тільки в тому випадку, якщо профілювання покаже, що `hashCode()` є реальною проблемою продуктивності. Зазвичай стандартні підходи є достатньо хорошими для 99% випадків.

#### **📌 Що перевіряють:**

*Здатність аналізувати компроміси (trade-offs) між різними підходами. Розуміння, що "ідеального" рішення не існує і вибір залежить від контексту.*

* * *

### **8 ❓ Питання:**

Поясніть на прикладі `HashMap`, як саме `hashCode()` та `equals()` працюють разом при виклику методу `put(K key, V value)`. ⭐️

> #### **💬 Відповідь:**
>
> Коли ви викликаєте `map.put(key, value)`, відбуваються наступні кроки:
>
> 1. **Обчислення хеш-коду:** `HashMap` викликає `key.hashCode()` для отримання хеш-коду.
>
> 2. **Обчислення індексу кошика:** На основі цього хеш-коду `HashMap` обчислює індекс "кошика" (bucket) у внутрішньому масиві, де має зберігатися запис. `index = hashCode % array.length`. (Реальна формула трохи складніша, але суть та сама).
>
> 3. **Перевірка кошика:**
>     * **Якщо кошик порожній:** Новий запис `(key, value)` просто додається туди.
>     * **Якщо в кошику вже є один або кілька записів (колізія):** `HashMap` починає ітерувати по елементах у цьому кошику. Для кожного елемента `existingEntry` виконується перевірка:
>         `if (existingEntry.hashCode() == key.hashCode() && existingEntry.getKey().equals(key))`
>
> 4. **Пошук та заміна:**
>     * **Якщо знайдено запис з таким самим ключем:** Його значення перезаписується новим `value`.
>     * **Якщо після перевірки всього кошика запис з таким ключем не знайдено:** Новий запис `(key, value)` додається в кінець списку (або дерева) в цьому кошику.
>
> Таким чином, `hashCode()` використовується для **швидкого "стрибка"** до потрібного кошика, а `equals()` — для **точного розрізнення** об'єктів усередині цього кошика.

#### **📌 Що перевіряють:**

*Детальне розуміння внутрішнього алгоритму роботи `HashMap`. Здатність покроково описати процес.*

* * *

### **9 ❓ Питання:**

Чи можна, щоб `hashCode()` повертав різні значення для рівних за `equals` об'єктів? Що це порушить? ⭐️

> #### **💬 Відповідь:**
>
> Ні, в жодному разі. Це прямо **порушує головне правило контракту**.
>
> `if (a.equals(b))` **має на увазі** `(a.hashCode() == b.hashCode())`.
>
> **Що це порушить?**
>
> Це повністю руйнує логіку роботи хеш-колекцій. Розглянемо `HashSet`:
>
> 1. Ви додаєте `obj1` в `HashSet`. Він потрапляє в кошик, що відповідає `obj1.hashCode()`.
> 2. Ви намагаєтеся перевірити наявність `obj2`, який рівний `obj1` за `equals`.
> 3. `HashSet` обчислює `obj2.hashCode()`. Оскільки він інший, `HashSet` буде шукати `obj2` у зовсім іншому кошику.
> 4. `set.contains(obj2)` поверне `false`, хоча з логічної точки зору `obj2` там має бути.
> 5. `set.add(obj2)` успішно додасть другий, "такий самий", об'єкт у множину, що порушить саму ідею `Set`.

#### **📌 Що перевіряють:**

*Уміння чітко артикулювати наслідки порушення основного правила контракту. Це питання може бути сформульоване трохи інакше, щоб перевірити уважність кандидата.*

* * *

### **10 ❓ Питання:**

Припустимо, ми не перевизначили ані `equals()`, ані `hashCode()`. Чи буде `HashMap` з такими об'єктами працювати коректно? ⭐️

> #### **💬 Відповідь:**
>
> Так, буде. Якщо ви не перевизначили жоден з методів, обидва успадковуються від класу `Object`.
>
> * `equals()` за замовчуванням порівнює посилання (`this == obj`).
> * `hashCode()` за замовчуванням генерує хеш-код, зазвичай на основі адреси в пам'яті.
>
> **Контракт при цьому дотримується:**
> Якщо `a.equals(b)` повертає `true`, це означає, що `a == b` (посилання рівні). А якщо посилання вказують на один і той самий об'єкт, то і їхні `hashCode()`, згенеровані на основі цих посилань, будуть однакові.
>
> **Як це буде працювати?**
> `HashMap` буде працювати в режимі "карти ідентичностей". Він буде розрізняти об'єкти за їхньою ідентичністю (посиланням), а не за логічним змістом. Ви зможете отримати значення з мапи, тільки якщо передасте в метод `get()` **точно той самий екземпляр об'єкта**, який використовувався як ключ при `put()`. Два різні об'єкти, навіть з однаковим вмістом, будуть вважатися різними ключами.
>
> Це така ж поведінка, як у `IdentityHashMap`.

#### **📌 Що перевіряють:**

*Розуміння того, що стандартна реалізація методів є самоузгодженою, хоча й не підходить для порівняння за вмістом.*

* * *

### **11 ❓ Питання:**

У яких випадках варто робити `hashCode()` кешованим і як це реалізувати? ⭐️

> #### **💬 Відповідь:**
>
> Кешування хеш-коду має сенс для **імутабельних (незмінних) об'єктів**, для яких обчислення `hashCode()` є "дорогою" операцією. Клас `java.lang.String` є яскравим прикладом.
>
> **Коли це виправдано:**
>
> * Клас є імутабельним (або принаймні його поля, що впливають на хеш, не змінюються).
> * Обчислення `hashCode` є складним (включає багато полів, цикли, складні обчислення).
> * Об'єкти цього класу часто використовуються в хеш-колекціях, і `hashCode()` викликається багато разів.
>
> **Як це реалізувати:**
>
> 1. Додається приватне поле для зберігання кешованого хешу (наприклад, `private int hashCodeCache;`).
> 2. Додається прапорець, щоб знати, чи хеш вже було обчислено, або використовується спеціальне значення (наприклад, 0), якщо 0 не може бути валідним хеш-кодом.
> 3. `hashCode()` реалізується з "лінивою ініціалізацією".

#### **📝 Приклад:**

```java
public final class ExpensiveImmutableObject { // Клас імутабельний
    private final String field1;
    private final byte[] data;

    private int hashCodeCache; // 0 означає, що кеш не обчислено

    public ExpensiveImmutableObject(String field1, byte[] data) {
        this.field1 = field1;
        this.data = data;
    }

    @Override
    public int hashCode() {
        int result = hashCodeCache;
        if (result == 0) { // Якщо кеш не ініціалізовано
            // "Дороге" обчислення
            result = Objects.hash(field1, Arrays.hashCode(data));
            hashCodeCache = result;
        }
        return result;
    }
}
```

#### **📌 Що перевіряють:**

*Знання технік оптимізації та вміння їх застосовувати для імутабельних класів.*

* * *

### **12 ❓ Питання:**

Що краще для хеш-коду поля `String`: `myString.hashCode()` чи якась своя хеш-функція? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Практично завжди краще використовувати стандартний `myString.hashCode()`.
>
> **Причини:**
>
> 1. **Надійність і коректність:** Реалізація `String.hashCode()` перевірена роками і мільйонами розробників. Вона коректно обробляє всі `Unicode`-символи. Написати свою хеш-функцію, яка буде настільки ж хорошою, дуже складно.
> 2. **Продуктивність:** Стандартна реалізація `hashCode()` в класі `String` **кешується**. При першому виклику хеш-код обчислюється і зберігається. Всі наступні виклики миттєво повертають це значення. Ваша власна хеш-функція, скоріш за все, буде обчислювати його щоразу, що буде значно повільніше.
> 3. **Захист від DoS-атак:** Сучасні версії JVM мають механізми захисту від атак, заснованих на колізіях хеш-кодів для рядків. Якщо ви використовуєте свою функцію, ви втрачаєте цей захист.
>
> **Коли може знадобитися своя функція?**
> Дуже рідко. Наприклад, якщо вам потрібен криптографічний хеш (MD5, SHA-256), але це вже зовсім інше завдання, яке не пов'язане з `Object.hashCode()`. Або якщо стандартна функція дає занадто багато колізій саме для вашого специфічного набору даних (що малоймовірно), і ви готові довести через профілювання, що ваша функція працює краще.
>
> **Висновок:** Не "винаходьте велосипед". Завжди використовуйте `myString.hashCode()`.

#### **📌 Що перевіряють:**

*Розуміння принципу "не робити передчасної оптимізації". Здатність оцінити надійність та ефективність стандартних рішень платформи.*

* * *

### **13 ❓ Питання:**

Чи можна реалізувати `hashCode()` так, щоб він завжди повертав результат `super.hashCode()`? Коли це має сенс? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна, але це має сенс лише в дуже специфічній ситуації.
>
> **`return super.hashCode();`** по суті означає, що ви делегуєте обчислення хеш-коду батьківському класу.
>
> **Коли це може мати сенс:**
>
> Якщо ви створюєте підклас, який **не додає жодних нових полів**, що впливають на стан `equals()`, але лише додає нову поведінку (нові методи). У цьому випадку логічна рівність вашого об'єкта повністю визначається станом батьківського класу.
>
> ```java
> public class User {
>     private String name;
>     // equals() і hashCode() реалізовані на основі name
> }
>
> public class PrintableUser extends User {
>     public void print() {
>         System.out.println("User: " + getName());
>     }
>
>     // Нових полів немає, тому логічна рівність така сама, як у User
>     @Override
>     public boolean equals(Object obj) {
>         return super.equals(obj);
>     }
>
>     @Override
>     public int hashCode() {
>         return super.hashCode();
>     }
> }
> ```
>
> У цьому випадку `PrintableUser` є "прозорим" розширенням `User` з точки зору рівності.
>
> Однак, якщо ваш підклас додає хоча б одне нове поле, яке бере участь в `equals`, то делегування `super.hashCode()` стане **некоректним** і порушить контракт.

#### **📌 Що перевіряють:**

*Розуміння тонкощів успадкування та його впливу на `equals`/`hashCode`. Здатність аналізувати, коли стан підкласу повністю визначається станом суперкласу.*

* * *

### **14 ❓ Питання:**

Як вплине на роботу `hashCode` оголошення поля як `transient`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Оголошення поля як `transient` **ніяк не впливає** на метод `hashCode()` автоматично.
>
> `transient` — це маркер для **механізму серіалізації**, який вказує, що це поле не потрібно зберігати. Метод `hashCode()` нічого не знає про цей маркер.
>
> **Однак тут є важливий непрямий зв'язок:**
>
> Якщо поле є частиною логічного стану об'єкта, воно має брати участь в `equals()` і `hashCode()`. Якщо ж ви вирішили зробити його `transient`, це, як правило, означає, що воно **не є** частиною постійного стану об'єкта (це може бути кеш, або чутливі дані, що не зберігаються).
>
> Якщо `transient`-поле **не є частиною логічної рівності**, то його **не слід включати** в розрахунок `hashCode()` (і `equals()`).
>
> Якщо ж ви з якоїсь причини включили `transient`-поле в `hashCode()`, ви ризикуєте отримати таку ситуацію:
>
> 1. Оригінальний об'єкт `obj1` має хеш-код `H1`.
> 2. Ви його серіалізуєте і десеріалізуєте, отримуючи `obj2`.
> 3. `transient`-поле в `obj2` буде мати значення за замовчуванням (`null`/`0`).
> 4. Якщо `hashCode` враховував це поле, то `obj2.hashCode()` буде іншим (`H2`).
>
> Це може призвести до проблем, якщо ви порівнюєте оригінальні та десеріалізовані об'єкти.

#### **📌 Що перевіряють:**

*Здатність розрізняти різні механізми Java (серіалізація, хешування) та їхню взаємодію. Розуміння того, що `transient` призначений для одного, а `hashCode` — для іншого.*

* * *

### **15 ❓ Питання:**

Клас `Thread` не перевизначає ані `equals()`, ані `hashCode()`. Чому, на вашу думку, це зроблено і які наслідки це має? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Клас `java.lang.Thread` не перевизначає `equals()` та `hashCode()` тому, що потік (thread) — це **унікальна сутність, яка представляє собою процес виконання, а не значення (value)**.
>
> Для потоків важлива їхня **ідентичність**, а не стан. Два об'єкти `Thread` ніколи не можуть бути "логічно рівними", навіть якщо вони створені для виконання одного й того ж коду `Runnable`. Кожен з них — це окремий, унікальний потік виконання в операційній системі.
>
> **Наслідки:**
>
> 1. **Порівняння потоків відбувається за посиланням.** `thread1.equals(thread2)` поверне `true` тільки якщо `thread1 == thread2`.
> 2. `hashCode()` для потоків генерується на основі їхньої ідентичності.
> 3. Це означає, що ви не можете використовувати об'єкти `Thread` у хеш-колекціях, розраховуючи на логічне порівняння. Кожен екземпляр буде унікальним ключем.
>
> Такий самий підхід використовується і для інших класів, що представляють унікальні ресурси або сутності, а не значення. Наприклад:
>
> * `StringBuilder` / `StringBuffer` (представляють мутабельний буфер символів).
> * Потоки вводу-виводу (`FileInputStream` тощо).
>
> Це дизайнерське рішення підкреслює, що ці класи є "об'єктами", а не "значеннями".

#### **📌 Що перевіряють:**

*Глибоке розуміння філософії дизайну класів. Здатність пояснити, чому для одних класів перевизначення `equals`/`hashCode` є обов'язковим, а для інших — контрпродуктивним.*
