---
layout: default
title: "Метод equals() в Java"
parent: "`equals()`, `hashCode()`, `clone()`"
grand_parent: "Java Core"
nav_order: 1
---

# Метод equals() в Java

* короткий опис: Метод `equals(Object obj)` з `java.lang.Object`, що використовується для логічного порівняння двох об'єктів. За замовчуванням порівнює посилання (`this == obj`), але його часто перевизначають для порівняння вмісту полів об'єкта.

* поняття: `equals()`, `==` (порівняння посилань), логічне порівняння, посилання.

* приклади: `User u1 = new User("Max", 25); User u2 = new User("Max", 25); System.out.println(u1.equals(u2));` (true, якщо перевизначено).

* ключові моменти: Необхідно перевизначувати для коректного порівняння вмісту, особливо якщо об'єкти використовуються у колекціях (`HashMap`, `HashSet`).

* що важливо знати на співбесіді: Різниця `==` і `equals()`, чому треба перевизначати.

### **✅ Офіційне визначення:**

Метод **`equals(Object obj)`** — це метод з класу **`java.lang.Object`**, який використовується для логічного порівняння двох об'єктів. За замовчуванням метод **`equals()`** повертає **`true`**, лише якщо два об'єктні посилання вказують на один і той самий об'єкт у памʼяті (**`this == obj`**). Його можна (і потрібно) перевизначити для порівняння сутності за вмістом (значеннями полів).

### **🧠 Простими словами:**

**`equals()`** — це як "чи однакові ці два об'єкти по суті?". Якщо в тебе два користувача з однаковим імʼям і віком, але це два окремі обʼєкти — **`==`** скаже false, а от перевизначений **`equals()`** може сказати true, якщо ти це правильно налаштуєш.

---

## **📊 Види порівняння:**

* **`==`** — перевіряє, чи посилання на той самий об'єкт
* **`equals()`** (за замовчуванням) — теж перевіряє **`this == obj`**
* Перевизначений **`equals()`** — порівнює значення полів обʼєкта

---

## **Пояснення на пальцях (💡)**

* Коли не перевизначений **`equals()`** — ти просто питаєш: “Це той самий об’єкт?”
* Коли перевизначений — ти питаєш: **`“Ці об’єкти виглядають однаково?”`**
* Якщо ти хочеш використовувати обʼєкти як ключі в **`HashMap`** або шукати в **`Set`**, тобі обов’язково треба перевизначити **`equals()`** разом з **`hashCode()`**
* Якщо не перевизначити — Java скаже, що два користувачі з однаковим імʼям — це різні обʼєкти

---

## **Корисні приклади (🧪)**

```java
public class User {
    private String name;
    private int age;

    @Override
    public boolean equals(Object obj) {
        if (this == obj) return true;
        if (obj == null || getClass() != obj.getClass()) return false;
        User other = (User) obj;
        return age == other.age && Objects.equals(name, other.name);
    }
}
```

#### **Використання:**

```java
User u1 = new User("Max", 25);
User u2 = new User("Max", 25);

System.out.println(u1.equals(u2)); // true, якщо метод перевизначено правильно
```

---

## **Порівняння (⚖️)**

| Порівняння | Поведінка | Пояснення |
| :---: | ----- | ----- |
| **`==`** | Порівнює посилання | Чи це один і той самий обʼєкт у памʼяті |
| **`equals()`** | Можна перевизначити | Порівнює поля, логіку, структуру |

---

## **🧠 Мнемоніка для запам’ятовування:**

🔹 Якщо перевизначаєш **`equals()`** -> завжди перевизначай **`hashCode()`**  
🔹 Контракт: **"equal objects must have equal hash codes"**  
🔹 **`==`** -> однакове посилання  
🔹 **`equals()`** -> однаковий вміст

---

## **🟩 Підсумок:**

Метод **`equals()`** — критично важливий для правильного порівняння обʼєктів у Java. Він дозволяє визначити, чи два різні об’єкти вважаються однаковими логічно, тобто за своїми полями. Якщо ти плануєш використовувати об'єкти в колекціях типу **`HashMap`**, **`HashSet`**, обов'язково перевизначай **`equals()`** разом із **`hashCode()`**.

### **🔗 Додаткові джерела:**

* [**Офіційна документація Java: Object.equals()**](https://docs.oracle.com/javase/8/docs/api/java/lang/Object.html#equals-java.lang.Object-)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому ключова різниця між оператором `==` та методом `equals()`?

> #### **💬 Відповідь:**
>
> Основна різниця полягає в тому, **що** вони порівнюють:
>
> * **Оператор `==`**:
>   * Для **примітивних типів** (`int`, `char`, `boolean` тощо) порівнює їхні **значення**.
>   * Для **об'єктів** (посилальних типів) порівнює **посилання на об'єкти**, тобто перевіряє, чи дві змінні вказують на **один і той самий об'єкт** у пам'яті.
>
> * **Метод `equals(Object obj)`**:
>   * Призначений для **логічного порівняння об'єктів** (порівняння за "сенсом", або за вмістом).
>   * За замовчуванням у класі `Object` його реалізація ідентична `==`.
>   * Його **потрібно перевизначати** у власних класах, щоб реалізувати логіку порівняння на основі значень полів.

#### **📝 Аналогія:**

Уявіть, що у вас є дві людини з однаковими іменами та датами народження, записаними у двох різних паспортах.

* `==` запитує: "Це один і той самий паспорт (фізичний документ)?" Відповідь: **ні**.
* `equals()` (правильно перевизначений) запитує: "Дані в цих паспортах описують одну й ту саму людину?" Відповідь: **так**.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння різниці між порівнянням посилань та порівнянням значень, що є однією з основ об'єктно-орієнтованого програмування в Java.*

* * *

### **2 ❓ Питання:**

Якою є поведінка методу `equals()` за замовчуванням (в класі `Object`)?

> #### **💬 Відповідь:**
>
> За замовчуванням метод `equals()` в класі `java.lang.Object` реалізований наступним чином:
>
> ```java
> public boolean equals(Object obj) {
>     return (this == obj);
> }
> ```
>
> Це означає, що без перевизначення `equals()` працює **точно так само, як і оператор `==`** для об'єктів. Він поверне `true` лише в тому випадку, якщо обидва посилання вказують на один і той же екземпляр об'єкта в купі (heap).

#### **📝 Коментар:**

Тому якщо ви створили два однакові за вмістом, але різні об'єкти (`new User("Tom")` і `new User("Tom")`), `equals()` за замовчуванням поверне `false`. Щоб отримати `true`, його потрібно перевизначити.

#### **📌 Що перевіряють:**

*Знання стандартної реалізації `equals()` та розуміння, чому її недостатньо для більшості користувацьких класів.*

* * *

### **3 ❓ Питання:**

Назвіть основні правила (контракт), яких потрібно дотримуватися при перевизначенні методу `equals()`.

> #### **💬 Відповідь:**
>
> Згідно з офіційною документацією Java, перевизначений метод `equals()` повинен відповідати п'яти властивостям (контракту):
>
> 1. **Рефлексивність (Reflexivity):** Об'єкт має бути рівним самому собі. Для будь-якого ненульового посилання `x`, вираз `x.equals(x)` має повертати `true`.
>
> 2. **Симетричність (Symmetry):** Якщо один об'єкт рівний іншому, то і другий має бути рівний першому. Для будь-яких ненульових посилань `x` і `y`, `x.equals(y)` має повертати `true` тоді й тільки тоді, коли `y.equals(x)` повертає `true`.
>
> 3. **Транзитивність (Transitivity):** Якщо перший об'єкт рівний другому, а другий — третьому, то перший має бути рівний третьому. Для будь-яких ненульових посилань `x`, `y` і `z`, якщо `x.equals(y)` повертає `true` і `y.equals(z)` повертає `true`, то `x.equals(z)` має повертати `true`.
>
> 4. **Консистентність (Consistency):** Повторні виклики `x.equals(y)` мають повертати одне й те саме значення, доки поля об'єктів `x` та `y`, що беруть участь у порівнянні, не були змінені.
>
> 5. **Порівняння з `null`:** Об'єкт не може бути рівним `null`. Для будь-якого ненульового посилання `x`, вираз `x.equals(null)` має повертати `false`.

#### **📝 Мнемоніка:**

**R**eflexive, **S**ymmetric, **T**ransitive, **C**onsistent, `null` -> **RSTCN**.

#### **📌 Що перевіряють:**

*Знання офіційного контракту `equals()`, що є ознакою професійного підходу до написання коду. Порушення контракту може призвести до непередбачуваної поведінки колекцій.*

* * *

### **4 ❓ Питання:**

Наведіть приклад правильного перевизначення `equals()` для простого класу.

> #### **💬 Відповідь:**
>
> Правильна реалізація `equals()` повинна включати кілька обов'язкових перевірок:
>
> 1. Перевірка на рівність посилань (`this == obj`) — це найшвидша оптимізація.
> 2. Перевірка на `null` (`obj == null`).
> 3. Перевірка на відповідність класів (`getClass() != obj.getClass()`).
> 4. Приведення типу `obj` до поточного класу.
> 5. Порівняння значущих полів. Для об'єктних полів слід використовувати `Objects.equals()`, щоб уникнути `NullPointerException`.

#### **📝 Приклад:**

```java
import java.util.Objects;

public class Person {
    private final String name;
    private final int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    @Override
    public boolean equals(Object obj) {
        // 1. Оптимізація: перевірка на рівність посилань
        if (this == obj) return true;

        // 2 & 3. Перевірка на null та на відповідність класів
        if (obj == null || getClass() != obj.getClass()) return false;

        // 4. Приведення типу
        Person person = (Person) obj;

        // 5. Порівняння значущих полів
        return age == person.age &&
               Objects.equals(name, person.name); // Objects.equals() коректно обробляє null
    }

    // Не забувайте перевизначати hashCode(), якщо перевизначили equals()
    @Override
    public int hashCode() {
        return Objects.hash(name, age);
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички написання надійного та коректного методу `equals()` з урахуванням усіх необхідних перевірок.*

* * *

### **5 ❓ Питання:**

Чому важливо перевизначати `hashCode()` разом з `equals()`?

> #### **💬 Відповідь:**
>
> Це одна з найважливіших вимог у Java. **Головний контракт `hashCode()`** говорить: "якщо два об'єкти рівні за `equals()`, то вони зобов'язані мати однаковий хеш-код".
>
> `hashCode()` використовується в хеш-колекціях, таких як `HashMap`, `HashSet` та `Hashtable`. Ці колекції використовують хеш-код для швидкого визначення "кошика" (bucket), в якому має зберігатися об'єкт.
>
> **Якщо ви перевизначите `equals()` але не `hashCode()`:**
>
> 1. Ви створите два логічно рівних об'єкти (`user1.equals(user2)` повертає `true`).
> 2. Але вони, ймовірно, матимуть різні хеш-коди (оскільки `hashCode()` за замовчуванням генерується на основі адреси в пам'яті).
> 3. При спробі додати їх у `HashSet`, колекція покладе їх у різні "кошики", вважаючи їх різними.
> 4. В результаті у вашому `HashSet` буде два "однакові" об'єкти, що порушує основний принцип множини. Пошук у `HashMap` за таким ключем також працюватиме некоректно.
>
> Тому порушення контракту `equals()`-`hashCode()` призводить до **непередбачуваної і некоректної поведінки хеш-колекцій**.

#### **📌 Що перевіряють:**

*Розуміння нерозривного зв'язку між `equals()` та `hashCode()` та усвідомлення наслідків порушення цього контракту.*

* * *

### **6 ❓ Питання:**

Наведіть приклади класів зі стандартної бібліотеки Java, які перевизначають `equals()`.

> #### **💬 Відповідь:**
>
> Більшість класів, що представляють значення (value objects), перевизначають `equals()` для логічного порівняння.
>
> * **`String`**: Порівнює вміст рядків символ за символом.
> * **Класи-обгортки (`Integer`, `Double` тощо)**: Порівнюють числові значення, які вони містять.
> * **`java.io.File` / `java.nio.file.Path`**: Порівнюють шляхи до файлів.
> * **Класи колекцій (`ArrayList`, `HashSet` тощо)**: Порівнюють вміст колекцій (кількість елементів та рівність кожного елемента через `equals()`).
> * **`java.util.Date` / `java.time.LocalDate`**: Порівнюють момент часу, який вони представляють.

#### **📝 Коментар:**</h4>

Навпаки, класи типу `StringBuilder` та `StringBuffer` **не** перевизначають `equals()`, оскільки вони є мутабельними і представляють об'єкт, а не значення. Для них важлива ідентичність посилання.

#### **📌 Що перевіряють:**

*Загальні знання стандартної бібліотеки Java та розуміння того, для яких типів об'єктів логічне порівняння є очікуваною поведінкою.*

* * *

### **7 ❓ Питання:**

Чим перевірка `obj instanceof MyClass` гірша за `getClass() != obj.getClass()` всередині `equals()`?

> #### **💬 Відповідь:**
>
> Використання `instanceof` може **порушити властивість симетричності** контракту `equals()` при успадкуванні.
>
> * `getClass() != obj.getClass()`: Ця перевірка вимагає, щоб об'єкти були **одного і того ж класу**. Вона не дозволяє порівнювати об'єкт базового класу з об'єктом дочірнього класу.
>
> * `obj instanceof MyClass`: Ця перевірка дозволяє порівнювати об'єкт `MyClass` з його нащадками. Наприклад, `childObject instanceof MyClass` поверне `true`.
>
> **Проблема з `instanceof`:**
> Уявімо, що є клас `Point` та його нащадок `ColorPoint`, який додає поле `color`.
>
> 1. `point.equals(colorPoint)` (де `point` є `Point`, `colorPoint` — `ColorPoint`) може повернути `true`, якщо реалізація `equals` в `Point` використовує `instanceof` і порівнює тільки `x` та `y`.
> 2. Але `colorPoint.equals(point)` поверне `false`, тому що реалізація в `ColorPoint` буде додатково порівнювати колір, а у `point` його немає.
>
> Таким чином, `a.equals(b)` є `true`, а `b.equals(a)` є `false`. **Симетричність порушена.**
>
> Через це, в переважній більшості випадків, використання `getClass() != obj.getClass()` є кращим та безпечнішим підходом.

#### **📌 Що перевіряють:**

*Глибоке розуміння тонкощів контракту `equals()`, зокрема властивості симетричності, та її зв'язку з успадкуванням.*

* * *

### **8 ❓ Питання:**

Чи можна сказати, що якщо `x.hashCode() != y.hashCode()`, то `x.equals(y)` гарантовано поверне `false`? А якщо `x.hashCode() == y.hashCode()`, що це означає? ⭐️

> #### **💬 Відповідь:**
>
> **Перша частина:** Так, можна. Якщо `x.hashCode() != y.hashCode()`, то з цього **випливає**, що `x.equals(y)` **обов'язково** поверне `false`. Це зворотне твердження до основного контракту "якщо об'єкти рівні, їх хеш-коди мають бути рівні". Це дуже корисна властивість для оптимізації в хеш-колекціях: якщо хеш-коди різні, `equals()` навіть не викликається.
>
> **Друга частина:** Якщо `x.hashCode() == y.hashCode()`, то це **нічого не гарантує** щодо `equals()`. Об'єкти можуть бути як рівними, так і нерівними. Така ситуація називається **колізією хешування**. Це означає, що два різні об'єкти випадково отримали однаковий хеш-код. У цьому випадку для остаточного визначення рівності хеш-колекція зобов'язана викликати метод `equals()`.

#### **📝 Аналогія:**</h4>

Хеш-код — це як номер кімнати в готелі, а об'єкти — це гості.

* Якщо номери кімнат різні, то гості точно різні.
* Якщо номери кімнат однакові, то це або один і той самий гість, або в кімнаті живуть кілька гостей (колізія), і потрібно перевіряти їхні паспорти (`equals()`).

#### **📌 Що перевіряють:**

*Розуміння співвідношення між `equals()` та `hashCode()` в обох напрямках та поняття колізій.*

* * *

### **9 ❓ Питання:**

Припустимо, у вас є мутабельний (змінний) об'єкт. Чи є хорошою ідеєю використовувати його як ключ у `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, це **дуже погана ідея** і поширене джерело помилок.
>
> Коли ви додаєте об'єкт як ключ у `HashMap`, колекція обчислює його хеш-код і розміщує об'єкт у відповідний "кошик" (bucket).
>
> Якщо після цього ви **зміните стан об'єкта** таким чином, що це вплине на його `hashCode()` (тобто зміните поле, що бере участь у розрахунку хеш-коду), то станеться наступне:
>
> 1. Новий хеш-код об'єкта буде іншим.
> 2. Але сам об'єкт **залишиться у старому кошику**, який відповідає старому хеш-коду.
> 3. Коли ви спробуєте знайти цей об'єкт у `HashMap` (`map.get(key)`), `HashMap` буде шукати його в новому кошику (відповідно до нового хеш-коду) і **не знайде** його там.
>
> В результаті об'єкт "губиться" всередині мапи. Ви не зможете отримати його за ключем, хоча він все ще фізично знаходиться в колекції.
>
> **Висновок:** Ключі в `HashMap` (та елементи в `HashSet`) мають бути **імутабельними (незмінними)** або, принаймні, не змінювати свій стан таким чином, що це впливає на `equals()` та `hashCode()`.

#### **📌 Що перевіряють:**

*Розуміння внутрішньої роботи хеш-колекцій та небезпеки використання мутабельних ключів.*

* * *

### **10 ❓ Питання:**

Для масивів краще використовувати `==` чи `equals()`? ⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом. Для масивів метод `equals()` **не перевизначений**, і він успадковує реалізацію від класу `Object`. Тому виклик `array1.equals(array2)` для двох різних масивів **працює так само, як і `==`** — він порівнює посилання.
>
> Навіть якщо масиви мають однаковий розмір і містять однакові елементи, `equals()` поверне `false`.
>
> ```java
> int[] a1 = {1, 2, 3};
> int[] a2 = {1, 2, 3};
> System.out.println(a1.equals(a2)); // false!
> ```
>
> Для коректного порівняння вмісту масивів слід використовувати статичний метод **`java.util.Arrays.equals(arr1, arr2)`**. Для багатовимірних масивів — **`java.util.Arrays.deepEquals(arr1, arr2)`**.

#### **📝 Коментар:**

Це одна з "пасток" мови Java. Інтуїтивно очікується, що `equals()` для масивів буде порівнювати вміст, але це не так.

#### **📌 Що перевіряють:**

*Уважність до деталей та знання специфічної поведінки `equals()` для масивів. Вміння використовувати правильні утиліти з класу `Arrays`.*

* * *

### **11 ❓ Питання:**

У класах-обгортках (`Integer`, `Long` тощо) `equals()` працює інакше, ніж у інших об'єктів. В чому особливість? ⭐️

> #### **💬 Відповідь:**
>
> Метод `equals()` у класах-обгортках (`Integer`, `Byte`, `Long` і т.д.) є особливим, оскільки він реалізований для порівняння не тільки з об'єктами того ж типу, але й **з об'єктами інших числових типів-обгорток**, якщо їхні значення можуть бути порівняні.
>
> Однак `Integer.equals()` поверне `true` **тільки** якщо на вхід подається об'єкт типу `Integer` з таким же числовим значенням. Він не намагається порівняти `Integer` з `Long`.
>
> Розглянемо приклад:
>
> ```java
> Integer a = 10;
> Long b = 10L;
> Double c = 10.0;
>
> a.equals(b); // false, тому що b не є Integer
> b.equals(c); // false, тому що c не є Long
> ```
>
> Це порушення принципу симетричності, якби `Integer(10).equals(Long(10))` було `true`.
>
> Тому розробники Java реалізували `equals` так, щоб він порівнював і тип, і значення.

#### **📌 Що перевіряють:**

*Знання специфіки реалізації `equals` у стандартних класах та увага до деталей роботи з числовими обгортками.*

* * *

### **12 ❓ Питання:**

Чи може `equals()` кидати винятки? Якщо так, то які і коли? ⭐️

> #### **💬 Відповідь:**
>
> Загалом, метод `equals()` **не повинен кидати перевіряємі винятки (checked exceptions)**, оскільки його сигнатура в `Object` не оголошує `throws`.
>
> Однак він може кидати неперевіряємі винятки (`RuntimeException`), хоча це і не є хорошою практикою. Найпоширеніші випадки:
>
> 1. **`NullPointerException`**: Якщо реалізація `equals()` не має перевірки на `null` і намагається викликати метод на переданому `null`-об'єкті. Наприклад, `obj.someField` замість `Objects.equals(this.field, obj.someField)`.
>
> 2. **`ClassCastException`**: Якщо замість перевірки `getClass() != obj.getClass()` або `instanceof` реалізація просто робить сліпе приведення типу `(MyClass) obj`, і на вхід передається об'єкт іншого, несумісного класу.
>
> Хороша, надійна реалізація `equals()` **не повинна кидати жодних винятків**, оскільки вона має обробляти всі можливі вхідні дані (включаючи `null` та об'єкти інших типів) і просто повертати `false`.

#### **📌 Що перевіряють:**

*Розуміння того, яким має бути надійний метод `equals()`, та здатність передбачати потенційні помилки у некоректних реалізаціях.*

* * *

### **13 ❓ Питання:**

Як реалізувати `equals()` для класу, поля якого можуть містити масиви (наприклад, `double[]`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо поля є масивами, не можна просто використовувати їхній метод `equals()`, оскільки він, як ми вже знаємо, порівнює посилання.
>
> Для порівняння вмісту таких полів потрібно використовувати відповідні методи з утилітарного класу `java.util.Arrays`:
>
> * `Arrays.equals(arr1, arr2)` — для одновимірних масивів.
> * `Arrays.deepEquals(objArr1, objArr2)` — для багатовимірних масивів.

#### **📝 Приклад:**

```java
import java.util.Arrays;
import java.util.Objects;

public class Vector {
    private final double[] coordinates;

    public Vector(double[] coordinates) {
        this.coordinates = coordinates;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        Vector vector = (Vector) o;
        // Використовуємо Arrays.equals для порівняння вмісту масивів
        return Arrays.equals(coordinates, vector.coordinates);
    }

    @Override
    public int hashCode() {
        // Arrays.hashCode для генерації хеш-коду на основі вмісту
        return Arrays.hashCode(coordinates);
    }
}
```

#### **📌 Що перевіряють:**

*Вміння працювати зі складними типами полів та знання відповідних утилітарних методів для їх порівняння.*

* * *

### **14 ❓ Питання:**

Уявімо, що ви перевизначили `equals()`, але забули про `hashCode()`. Ваш юніт-тест `assertEquals(obj1, obj2)` проходить успішно. Де саме і за яких умов проблема себе проявить? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Проблема проявить себе **тільки при використанні об'єкта в хеш-колекціях**, таких як `HashMap` або `HashSet`.
>
> Юніт-тест `assertEquals(obj1, obj2)` зазвичай просто викликає `obj1.equals(obj2)`, тому він не перевіряє контракт `hashCode()`. Тест пройде, бо `equals()` реалізовано коректно.
>
> **Проблема виникне тут:**
>
> ```java
> User user1 = new User("admin");
> User user2 = new User("admin");
>
> // equals повертає true
> System.out.println(user1.equals(user2)); // true
>
> Set<User> userSet = new HashSet<>();
> userSet.add(user1);
>
> // contains використовує hashCode() і потім equals()
> // Оскільки hashCode() не перевизначено, у user1 і user2 він різний
> // HashSet шукає user2 в неправильному "кошику" і не знаходить.
> System.out.println(userSet.contains(user2)); // false! - Ось проблема!
>
> userSet.add(user2);
> System.out.println(userSet.size()); // 2 - у множині два "однакових" об'єкти!
> ```
>
> Отже, проблема буде невидимою, доки об'єкт не потрапить у контекст, де `hashCode()` є критично важливим. Це класичний приклад прихованої помилки, яку важко знайти, якщо не знати про зв'язок `equals` та `hashCode`.

#### **📌 Що перевіряють:**

*Здатність аналізувати непрямі наслідки помилок у коді та розуміння того, в яких саме сценаріях порушення контракту призведе до збою.*

* * *

### **15 ❓ Питання:**

Припустимо, ми маємо такий код: `new BigDecimal("2.0").equals(new BigDecimal("2.00"))`. Що він поверне і чому це демонструє особливість реалізації `equals()` у `BigDecimal`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз поверне **`false`**.
>
> Це демонструє дуже важливу і часто несподівану особливість `equals()` у класі `java.math.BigDecimal`. Метод `equals()` для `BigDecimal` порівнює **не тільки числове значення, а й `scale` (кількість знаків після коми)**.
>
> * У `new BigDecimal("2.0")` значення є 2, а `scale` — 1.
> * У `new BigDecimal("2.00")` значення є 2, а `scale` — 2.
>
> Оскільки `scale` у них різний, `equals()` вважає їх нерівними.
>
> Для порівняння `BigDecimal` виключно за числовим значенням, ігноруючи `scale`, слід використовувати метод **`compareTo()`**:
>
> `new BigDecimal("2.0").compareTo(new BigDecimal("2.00"))` поверне `0`, що означає, що їхні числові значення рівні.
>
> **Чому так зроблено?** Це дозволяє зберігати точність, яка була вказана при створенні числа, що може бути критично важливим у фінансових та наукових розрахунках, де `2.0` і `2.00` можуть означати різний ступінь точності вимірювання.

#### **📌 Що перевіряють:**

*Дуже глибоке знання стандартної бібліотеки та уважність до деталей і "пасток" у реалізації `equals` для нетривіальних класів. Здатність пояснити, чому така "дивна" поведінка є виправданою.*
