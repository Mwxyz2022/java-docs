---
layout: default
title: "Символ підстановки ? (Wildcard)"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 2
---

# Символ підстановки ? (Wildcard)

* короткий опис: Символ підстановки `?` – це спеціальне позначення в дженеріках Java, що вказує на невідомий тип. Дозволяє писати методи, що працюють з різними, але сумісними параметризованими типами, підвищуючи гнучкість.

* поняття: Wildcard, `<?>` (unbounded), `<? extends T>` (upper-bounded), `<? super T>` (lower-bounded).

* приклади: `Collection<?>` (колекція невідомого типу), `List<? extends Number>` (читання), `List<? super Integer>` (запис).

* ключові моменти: `<?>` дозволяє лише читати елементи як `Object`, не можна додавати нічого (крім `null`). `Wildcards` допомагають дотримуватись PECS.

* що важливо знати на співбесіді: Що таке `?`, чим відрізняється від параметра типу `<T>`, що можна/не можна робити з `?`.

### **✅ Офіційне визначення:**

Символ підстановки (**`?`**) — це спеціальне позначення у дженеріках Java, яке вказує на **невідомий тип**. Він дозволяє писати методи або класи, які працюють з різними, але сумісними параметризованими типами.

### **🧠 Простими словами:**

**`?`** — це "я не знаю точно, який тип тут буде, але хочу, щоб код працював з будь-яким з них". Він дозволяє зробити код більш гнучким, наприклад: **`List<?>`** — це список **чогось**, ми не знаємо чого саме, але це точно список

---

## **Основне**

📊 **Типи підстановок:**

* **`<?>`** — будь-який тип, повністю невідомий.
* **`<? extends T>`** — будь-який тип, що є підтипом **`T`** або самим **`T.`**
* **`<? super T>`** — будь-який тип, що є надтипом **`T`** або самим **`T`**.

📦 **Колекції з wildcard:**

| Вираз | Інтерпретація | Можна додавати? | Можна читати? |
| :---: | :---: | :---: | :---: |
| **`Collection<?>`** | Колекція з елементами невідомого типу | ❌ (тільки **`null`**) | ✅ як **`Object`** |
| **`List<? extends T>`** | Читати об'єкти типу **`T`** або підтипу | ❌ | ✅ як **`T`** |
| **`List<? super T>`** | Додавати об'єкти типу **`T`** або підтипу | ✅ | ✅ як **`Object`** |

---

## **Пояснення на пальцях (💡)**

Уяви, що ти бариста ☕ і у тебе є контейнери для кави:

* **`List<?>`** — тобі дали коробку, але не сказали, що всередині. Можеш заглянути (тільки як Object), але нічого не додавай — раптом не той сорт\!
* **`List<? extends Beverage>`** — коробка з напоями: можеш налити каву чи чай, але додати новий напій не можна — бо не знаєш, який саме тип очікується.
* **`List<? super Coffee>`** — коробка, яка приймає каву або щось загальніше (наприклад, напої взагалі). Можеш додати каву, але не дізнаєшся точно, що вже лежить.

---

## **Корисні приклади (🧪)**

```java
public void printList(List<?> list) {
    for (Object obj : list) {
        System.out.println(obj);
    }
    // list.add("new item"); // ❌ compile-time error
}

public void addIntegers(List<? super Integer> list) {
    list.add(42); // ✅ OK
}

public void readNumbers(List<? extends Number> list) {
    Number num = list.get(0); // ✅ OK
    // list.add(10);          // ❌ compile-time error
}
```

---

## **Порівняння (⚖️)**

| Тип виразу | Читання | Запис | Тип даних при читанні |
| :---: | :---: | :---: | :---: |
| **`List<?>`** | ✅ | ❌ | **`Object`** |
| **`List<? extends T>`** | ✅ | ❌ | **`T`** |
| **`List<? super T>`** | ✅ | ✅ | **`Object`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**`?`** — **хто завгодно**

**`? extends T`** — **ті, хто походять від T**

**`? super T`** — **ті, хто старші за T**

**Wildcard — це коли тип не важливий або невідомий.**

---

## **🟩 Підсумок:**

* **`?`** — символ невідомого типу, корисний для універсальних API
* Дозволяє писати гнучкий і безпечний код
* Wildcards бувають трьох видів: **`?, ? extends T, ? super T`**
* З **`?`** не можна нічого додавати, лише читати (тип **`Object`**)
* Коли важливо знати тип — використовуй **`<T>`**, коли ні — **`?`**

**`🔗 Посилання на доповнення:`**

* [**`Java Tutorials – Wildcards`](https://docs.oracle.com/javase/tutorial/extra/generics/wildcards.html)**
* [**`Java Generics FAQ – Oracle`](https://docs.oracle.com/javase/tutorial/java/generics/faq.html)**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке символ підстановки `?` (wildcard) у дженеріках?

> #### **💬 Відповідь:**
>
> Символ підстановки **`?` (wildcard)**, який читається як "невідомий тип", — це спеціальний синтаксис у дженеріках, що дозволяє працювати з різними параметризаціями одного й того ж дженерик-класу, коли точний тип-аргумент невідомий або не важливий.
>
> Він використовується для підвищення **гнучкості** API, дозволяючи методам приймати або повертати колекції з різними, але сумісними типами.
>
> **Приклад:**
>
> Метод `public void printList(List<?> list)` може прийняти **будь-який** список — `List<String>`, `List<Integer>`, `List<User>` тощо.
>
> `?` завжди означає "якийсь конкретний, але невідомий нам тип".

#### **📝 Аналогія:**

`List<?>` — це як отримати посилку, на якій написано "ПОДАРУНОК". Ви знаєте, що всередині є якийсь подарунок, але не знаєте, який саме, доки не відкриєте.

#### **📌 Що перевіряють:**

*Розуміння того, що `?` представляє невідомий, але конкретний тип, і використовується для гнучкості.*

* * *

### **2 ❓ Питання:**

Які є три типи wildcards і яка між ними різниця?

> #### **💬 Відповідь:**
>
> Існує три типи:
>
> 1. **Необмежена маска (`?` - Unbounded Wildcard):**
>     * **Синтаксис:** `List<?>`
>     * **Значення:** "Список, елементи якого належать до будь-якого невідомого типу".
>     * **Обмеження:** В такий список не можна нічого **додавати** (крім `null`), а читати можна тільки як `Object`.
>
> 2. **Маска з верхньою межею (`? extends T` - Upper Bounded Wildcard):**
>     * **Синтаксис:** `List<? extends Number>`
>     * **Значення:** "Список невідомого типу, який є **`Number` або його нащадком**".
>     * **Обмеження:** Робить колекцію коваріантною. В такий список також не можна нічого **додавати** (крім `null`), але можна безпечно **читати** елементи як тип `T` ( `Number`).
>
> 3. **Маска з нижньою межею (`? super T` - Lower Bounded Wildcard):**
>     * **Синтаксис:** `List<? super Integer>`
>     * **Значення:** "Список невідомого типу, який є **`Integer` або його предком** (`Number`, `Object`)".
>     * **Обмеження:** Робить колекцію контраваріантною. В такий список можна безпечно **додавати** елементи типу `T` (`Integer`), але при читанні можна отримати елементи тільки як `Object`.

#### **📌 Що перевіряють:**

*Знання всіх трьох видів wildcards, їхнього синтаксису та ключових обмежень на читання/запис.*

* * *

### **3 ❓ Питання:**

Чому в `List<?>` не можна додавати елементи?

> #### **💬 Відповідь:**
>
> Тому що компілятор **не знає, який саме тип** представляє `?`, і не може гарантувати типобезпечність операції додавання.
>
> Розглянемо приклад:
>
> ```java
> public void processList(List<?> list) {
>     // Уявімо, що це дозволено:
>     // list.add("some string"); // ПОМИЛКА КОМПІЛЯЦІЇ
> }
>
> // ...
> List<Integer> numbers = new ArrayList<>();
> processList(numbers);
> ```
>
> 1. Ми передали `List<Integer>` у метод. `?` тепер "представляє" `Integer`.
> 2. Якби компілятор дозволив `list.add("some string")`, ми б додали рядок у список цілих чисел.
> 3. Це призвело б до **забруднення купи (heap pollution)** і до `ClassCastException` у майбутньому.
>
> Щоб запобігти таким ситуаціям, компілятор просто **забороняє** будь-який виклик методу `add()` на колекції типу `List<?>`, крім `add(null)`, оскільки `null` сумісний з будь-яким типом.
>
> Ця сама логіка стосується і `List<? extends T>`.

#### **📌 Що перевіряють:**

*Розуміння того, як компілятор забезпечує типобезпечність, забороняючи потенційно небезпечні операції.*

* * *

### **4 ❓ Питання:**

У чому різниця між дженерик-методом `<T> void method(List<T> list)` та методом з wildcard `void method(List<?> list)`? ⭐️

> #### **💬 Відповідь:**
>
> Вибір залежить від того, чи потрібен вам **зв'язок між типами** в сигнатурі.
>
> * **`void method(List<?> list)` (використовує wildcard):**
>   * **Коли використовувати:** Коли логіка методу **не залежить від конкретного типу** елементів і немає зв'язку між параметрами. Це більш простий і читабельний варіант, якщо вам просто потрібно, наприклад, роздрукувати список або отримати його розмір.
>
> * **`<T> void method(List<T> list)` (є дженерик-методом):**
>   * **Коли використовувати:** Коли вам потрібне **посилання на конкретний тип `T`** всередині методу або коли вам потрібно встановити **зв'язок** між кількома аргументами або типом повернення.
>
>     **Приклад зв'язку:**
>
>     ```java
>     // T пов'язує тип списку та тип елемента, що повертається
>     public <T> T getFirst(List<T> list) { return list.get(0); }
>
>     // Неможливо реалізувати з wildcard:
>     // public ? getFirst(List<?> list) - невалідний синтаксис
>     ```
>
> **Загальне правило:** Якщо тип-параметр використовується в сигнатурі **лише один раз** і тільки як тип аргументу, **використовуйте wildcard (`?`)**. В іншому випадку — дженерик-метод.

#### **📌 Що перевіряють:**

*Здатність вибрати правильний інструмент (`<T>` vs `?`) залежно від вимог до сигнатури методу.*

* * *

### **5 ❓ Питання:**

Що поверне такий код: `(new ArrayList<String>()) instanceof List<?>`? ⭐️

> #### **💬 Відповідь:**
>
> Такий код **не скомпілюється**.
>
> Оператор `instanceof` **не може** використовуватися з параметризованими типами праворуч, включаючи wildcards.
>
> **Причина** — **стирання типів (type erasure)**.
>
> Під час виконання JVM не має інформації про тип-аргумент, тому вона не може виконати таку перевірку. JVM бачить тільки "сирі" типи.
>
> **Як можна перевірити:**
> Ви можете перевірити тільки базовий тип:
>
> ```java
> if ((new ArrayList<String>()) instanceof List) {
>     // Ця перевірка пройде і поверне true
> }
> ```
>
> Таким чином, `instanceof` працює з сирими типами, але не з параметризованими.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів обмежує можливості оператора `instanceof`.*

* * *

### **6 ❓ Питання:**

Розшифруйте сигнатуру методу `Collections.copy(List<? super T> dest, List<? extends T> src)`. Чому саме такі wildcards? ⭐️

> #### **💬 Відповідь:**
>
> Ця сигнатура є канонічним прикладом застосування **принципу PECS (Producer Extends, Consumer Super)**.
>
> **Розбір:**
>
> * **`<T>`**: `copy` — це дженерик-метод, що працює з якимось типом `T`, який пов'язує вихідну та цільову колекції.
>
> * **`List<? extends T> src` (Джерело):**
>   * Колекція `src` є **виробником (producer)** — ми з неї **читаємо** дані.
>   * Згідно з PECS, для виробників використовується **`extends`**.
>   * Це дозволяє методу приймати список, що містить `T` або **будь-який його нащадок**. Наприклад, ви можете копіювати з `List<Integer>` (`src`) у `List<Number>` (`dest`).
>
> * **`List<? super T> dest` (Призначення):**
>   * Колекція `dest` є **споживачем (consumer)** — ми в неї **записуємо** дані.
>   * Згідно з PECS, для споживачів використовується **`super`**.
>   * Це дозволяє методу приймати список, який може зберігати `T` або **будь-який його предок**. Наприклад, можна копіювати елементи типу `Integer` у `List<Number>` або `List<Object>`.
>
> Ця комбінація робить метод `copy` максимально **гнучким та універсальним**.

#### **📌 Що перевіряють:**

*Практичне застосування правила PECS до аналізу сигнатур методів з JDK.*

* * *

### **7 ❓ Питання:**

Припустимо, є метод `public void addToList(List<? super String> list)`. Які з цих викликів валідні?

1. `list.add("hello");`
2. `list.add(new Object());`
3. `Object o = list.get(0);`
4. `String s = list.get(0);`

> #### **💬 Відповідь:**
>
> Розберемо кожен виклик:
>
> 1. **`list.add("hello");` — ВАЛІДНО.**
>     * `List<? super String>` може бути `List<String>`, `List<CharSequence>`, `List<Object>`. `String` є сумісним з усіма цими типами, тому додавати `String` безпечно.
>
> 2. **`list.add(new Object());` — НЕВАЛІДНО.**
>     * Ми не знаємо точного типу списку. Якщо це `List<String>`, ми не можемо додати туди `Object`. Компілятор забороняє це для забезпечення типобезпечності. Можна додавати тільки об'єкти типу `String` або його нащадків.
>
> 3. **`Object o = list.get(0);` — ВАЛІДНО.**
>     * При читанні з `List<? super String>`, єдине, що ми можемо гарантувати — це те, що отриманий елемент є `Object`, оскільки `Object` є супертипом для всіх можливих типів цього списку.
>
> 4. **`String s = list.get(0);` — НЕВАЛІДНО.**
>     * Ми не можемо бути впевнені, що отримаємо саме `String`. Список може бути, наприклад, `List<Object>` і містити `Integer`.

#### **📌 Що перевіряють:**

*Детальне розуміння правил читання та запису для `super`-wildcard.*

* * *

### **8 ❓ Питання:**

Що таке wildcards з кількома межами (multiple bounds)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Wildcards, на відміну від типів-параметрів (`<T>`), **не підтримують множинні обмеження** за допомогою `&`.
>
> **Наприклад, такий синтаксис є невалідним:**
>
> `List<? extends Runnable & Serializable> list; // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> Якщо вам потрібне обмеження відразу на кілька типів, ви **зобов'язані** використовувати **дженерик-метод** з оголошенням типу-параметра:
>
> ```java
> public <T extends Runnable & Serializable> void process(List<T> list) {
>     // ...
> }
> ```
>
> Це одне з ключових обмежень wildcards порівняно з повноцінними типами-параметрами. Wildcards призначені для більш простих сценаріїв, де достатньо одного обмеження.

#### **📌 Що перевіряють:**

*Знання синтаксичних обмежень wildcards. Здатність вибрати правильний інструмент (`wildcard` vs `дженерик-метод`) залежно від складності обмежень.*

* * *

### **9 ❓ Питання:**

Що таке "захоплення wildcards" (wildcard capture) і в якій ситуації це може знадобитися? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Захоплення wildcards** — це процес, під час якого компілятор неявно **"захоплює"** невідомий тип, представлений wildcard `?`, і надає йому **тимчасове ім'я**, щоб виконати перевірку типів. Це дозволяє працювати з wildcard так, ніби це звичайний тип-параметр.
>
> **Коли це потрібно?**
>
> Коли вам потрібно забезпечити зв'язок між типами, але сигнатура методу використовує wildcards.
>
> **Приклад:** Ви хочете написати метод, що міняє місцями два елементи в **будь-якому** списку.
>
> `public static void swap(List<?> list, int i, int j)` — як реалізувати тіло?
> `Object temp = list.get(i); list.set(i, list.get(j)); list.set(j, temp);`
> Такий код не скомпілюється, бо `list.set` очікує аргумент невідомого типу `?`, а ми передаємо `Object`.
>
> **Рішення:** Створити **приватний дженерик-метод-помічник**.
>
> ```java
> public static void swap(List<?> list, int i, int j) {
>     // Компілятор захоплює '?' і передає його як 'T'
>     swapHelper(list, i, j);
> }
>
> // Тут невідомий тип "отримав ім'я" T
> private static <T> void swapHelper(List<T> list, int i, int j) {
>     list.set(i, list.set(j, list.get(i))); // Елегантний swap
> }
> ```
>
> Коли викликається `swapHelper`, компілятор "захоплює" невідомий тип `?` і гарантує, що всередині `swapHelper` типи елементів будуть узгоджені, оскільки всі вони тепер мають однаковий тип `T`.

#### **📌 Що перевіряють:**

*Знання просунутих концепцій компілятора. Здатність вирішувати складні проблеми, пов'язані з обмеженнями wildcards, за допомогою патерну "дженерик-метод-помічник".*

* * *

### **10 ❓ Питання:**

Чи можна використовувати wildcard `?` у конструкції `new`? `new ArrayList<?>()` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Код `new ArrayList<?>()` призведе до **помилки компіляції**.
>
> **Причина:**
>
> Коли ви створюєте екземпляр класу (`new`), ви повинні надати **конкретний** тип, який буде використовуватися для цього екземпляра. Wildcard `?` за своєю природою представляє **невідомий** тип.
>
> Не можна створити об'єкт невідомого типу. Компілятор не знає, яку саме реалізацію `ArrayList` створювати, бо не знає типу елементів.
>
> Wildcards використовуються для **опису типів змінних, параметрів і значень, що повертаються**, щоб зробити їх більш гнучкими, але **не для створення екземплярів**.

#### **📌 Що перевіряють:**

*Розуміння того, що wildcards є концепцією для опису типів, а не для їх інстанціювання.*

* * *

### **11 ❓ Питання:**

Чому масиви не можуть безпечно працювати з wildcards? Поясніть `List<?>[]`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча створення дженерик-масиву, як-от `new List<String>[]`, заборонено, створення масиву з **параметризованим типом, що містить wildcard**, **дозволено** компілятором:
>
> `List<?>[] arrayOfLists = new List<?>[10]; // КОМПІЛЮЄТЬСЯ!`
>
> **Чому це небезпечно?**
>
> Ця конструкція, хоч і валідна, є **нетипобезпечною** і поєднує "найгірше з двох світів":
>
> * Від дженеріків ми отримуємо стирання типів.
> * Від масивів — коваріантність.
>
> **Приклад проблеми:**
>
> ```java
> List<?>[] arrayOfLists = new List<?>[1];
> List<Integer> intList = new ArrayList<>();
> arrayOfLists[0] = intList; // Все ще ОК
>
> Object[] objectArray = arrayOfLists; // Коваріантність
> List<String> strList = new ArrayList<>();
> objectArray[0] = strList; // Це теж валідно на рівні Object[]
>
> // Але тепер нашarrayOfLists[0] насправді є List<String>
> // Якщо десь буде спроба отримати його як List<Integer>,
> // може виникнути ClassCastException.
> ```
>
> **Висновок:** Хоча Java дозволяє створювати масиви з wildcard-типами (`List<?>[]`), це є небезпечною практикою, оскільки гарантії типобезпечності дженеріків ослаблюються. Найкраще правило — **не змішувати дженеріки та масиви**.

#### **📌 Що перевіряють:**

*Глибокі знання про взаємодію дженеріків, масивів та wildcards. Розуміння тонких аспектів типобезпечності.*

* * *

### **12 ❓ Питання:**

Поясніть, що таке `F-bounded polymorphism` і як wildcards допомагають його реалізувати. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `F-bounded polymorphism` або `F-bounded quantification` — це просунута концепція, яка використовує **рекурсивні обмеження типів**, щоб встановити зв'язок між типом та його власними підтипами.
>
> Канонічний приклад в Java — інтерфейс `Comparable`.
> `public interface Comparable<T> { int compareTo(T o); }`
>
> **Проблема:**
> Як написати дженерик-метод `max`, який працює з будь-якою колекцією `Comparable`-об'єктів?
>
> **Наївний підхід:**
> `<T extends Comparable<T>> T max(Collection<T> coll)`
>
> **Але що робити, якщо** `Student extends Person` і `Person implements Comparable<Person>`? Клас `Student` сам не реалізує `Comparable<Student>`, тому він не підійде під це обмеження.
>
> **Рішення з wildcards:**
>
> Використовувати wildcard **`super`** у рекурсивному обмеженні:
>
> `public static <T extends Comparable<? super T>> T max(Collection<T> coll)`
>
> **Що це означає:**
>
> * `T` має бути типом, який реалізує `Comparable`.
> * `Comparable` має бути параметризований **типом `T` або будь-яким його предком (`? super T`)**.
> * Це дозволяє `Student` задовольнити умову, оскільки він є `Comparable` зі своїм предком `Person`.
>
> Це робить API максимально гнучким для роботи з ієрархіями успадкування.

#### **📌 Що перевіряють:**

*Експертний рівень знань дженеріків. Розуміння просунутих патернів, таких як `F-bounded` типи, та ролі wildcards у їх реалізації.*

* * *

### **13 ❓ Питання:**

Чому іноді можна зустріти код з подвійним wildcard `List<List<?>>`? Що він означає? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тип `List<List<?>>` означає: **"Список, елементами якого є інші списки. При цьому тип елементів кожного внутрішнього списку є невідомим і може бути різним"**.
>
> **Приклад:**
>
> ```java
> List<String> strings = List.of("a", "b");
> List<Integer> integers = List.of(1, 2);
> List<Boolean> booleans = List.of(true);
>
> // Створюємо гетерогенний список списків
> List<List<?>> listOfLists = new ArrayList<>();
> listOfLists.add(strings);
> listOfLists.add(integers);
> listOfLists.add(booleans);
> ```
>
> **Що з цим можна робити:**
>
> * Ви можете безпечно **ітерувати** по зовнішньому списку.
> * Кожен елемент, який ви отримаєте, буде мати тип `List<?>`.
> * З цього внутрішнього списку ви зможете **читати** елементи тільки як `Object`.
>
> **Коли це корисно?**
>
> Такий тип може використовуватися в API, якому потрібно прийняти набір абсолютно різнорідних колекцій для виконання якоїсь загальної операції (наприклад, для отримання розміру кожної з них або для виводу).
>
> Це приклад максимального узагальнення, де ми відмовляємося від інформації про конкретні типи на користь гнучкості.

#### **📌 Що перевіряють:**

*Здатність читати та розуміти складні, вкладені дженерик-типи з wildcards.*

* * *

### **14 ❓ Питання:**

Що поверне такий метод і чому?
`public List<?> getList() { return new ArrayList<String>(); }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код є **повністю валідним**. Метод повертає екземпляр `ArrayList<String>`, але клієнтський код отримає його як `List<?>`.
>
> **Чому це працює:**
> `List<String>` є підтипом `List<?>`, тому присвоєння є безпечним.
>
> **Наслідки для клієнтського коду:**
>
> * Клієнт, який викликає цей метод, отримує **"тільки для читання"** представлення списку.
> * Він **не зможе додавати** в нього нові елементи (крім `null`), оскільки тип `?` є невідомим.
> * Він зможе **читати** з нього елементи, але тільки як `Object`.
>
> **Чому це може бути корисно?**
>
> Це може бути способом **захистити внутрішню колекцію** від модифікацій ззовні. Використовуючи `List<?>` як тип повернення, ви надаєте доступ до даних, але обмежуєте можливості їх зміни.
>
> Це менш суворий захист, ніж `Collections.unmodifiableList()`, але все ж створює певний бар'єр на етапі компіляції.

#### **📌 Що перевіряють:**

*Розуміння того, як wildcards можуть використовуватися для проектування API та контролю доступу до даних.*

* * *

### **15 ❓ Питання:**

Поясніть, що таке `capture of ?` в повідомленнях про помилки компіляції. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"capture of ?"** — це термін з повідомлень про помилки компілятора Java, який з'являється, коли ви намагаєтеся виконати небезпечну операцію з wildcard-типом.
>
> Він означає, що компілятор **"захопив"** невідомий тип `?` (надав йому внутрішнє ім'я, наприклад, `capture #1 of ?`), але операція, яку ви намагаєтеся виконати, не є безпечною для цього захопленого, але все ще невідомого типу.
>
> **Приклад:**
>
> ```java
> public void setFirst(List<?> list, Object value) {
>     list.set(0, value); // Помилка компіляції
> }
> ```
>
> **Повідомлення компілятора буде приблизно таким:**
>
> `The method set(int, capture#1-of-?) in the type List<capture#1-of-?> is not applicable for the arguments (int, Object)`
>
> **Що це означає:**
>
> 1. Компілятор "захопив" невідомий тип з `List<?>` і назвав його `capture#1-of-?`.
> 2. Метод `set` тепер очікує другий аргумент типу `capture#1-of-?`.
> 3. Ви намагаєтеся передати туди `Object`.
> 4. Компілятор не може довести, що `Object` є сумісним з `capture#1-of-?` (насправді, все навпаки).
> 5. Тому він видає помилку.
>
> Розуміння цього повідомлення допомагає діагностувати помилки, пов'язані з неправильним використанням wildcards.

#### **📌 Що перевіряють:**

*Здатність читати та інтерпретувати складні повідомлення про помилки від компілятора. Глибоке розуміння того, як компілятор внутрішньо працює з wildcards.*
