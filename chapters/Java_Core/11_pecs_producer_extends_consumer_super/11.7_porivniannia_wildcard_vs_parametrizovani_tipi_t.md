---
layout: default
title: "Порівняння wildcard vs параметризовані типи <T>"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 7
---

# Порівняння wildcard vs параметризовані типи <T>

* короткий опис: `Wildcard` (`?`) використовується для узагальнення типу в API, коли точний тип не важливий або невідомий. Параметризований тип (`<T>`) оголошується на рівні класу/методу, коли потрібно зберігати зв'язок між типами або повертати конкретний тип.

* поняття: Wildcard, параметризований тип `<T>`.

* приклади: `List<?>` (wildcard, обмежене використання), `public <T> void copyList(List<T> source, List<T> target)` (параметризований тип, для узагальнення логіки).

* ключові моменти: `Wildcard` для API-користувача (споживання), `<T>` для API-реалізації (створення логіки). `Wildcard` не дозволяє повний контроль над типом.

* що важливо знати на співбесіді: Коли краще використовувати `wildcard`, а коли `<T>`.

### **✅ Офіційне визначення:**

**Wildcard** (**`?, ? extends T, ? super T`**) — узагальнене обмеження типу **в середині** методу/інтерфейсу, **без введення нового типу**.

**Параметризований тип** (**`<T>`**) — **заміна змінної типу**, яка **оголошується явно** на рівні методу або класу для **узагальнення логіки**.

### **🧠 Простими словами:**

Wildcard — коли **ти не визначаєш новий тип**, просто кажеш: “я працюю з кимось схожим”.

**`<T>`** — коли **тобі потрібна логіка**, яка залежить від **конкретного типу**, і цей тип має бути **один і той же в кількох місцях**.

---

## 📊 **Порівняння wildcard vs параметризований тип**

| Характеристика | Wildcard (`?`) | Параметр типу (`<T>`) |
| :---: | :---: | :---: |
| **Декларація типу** | Не оголошується | Потрібно оголосити **`<T>`** |
| **Повторне використання** | Немає | Можна використовувати кілька раз |
| **Type Inference** | Обмежене | Повноцінне, працює з поверненням |
| **Читання / Запис** | **`extends`** -> тільки читати **`super`** -> тільки записувати | Читати і писати, повний контроль |
| **Призначення** | API користувача (використання) | API реалізації (універсальна логіка) |

---

## **Пояснення на пальцях (💡)**

Wildcard-и — це як "вхід без пароля": можеш подивитися, що всередині, але не можеш нічого змінити (або навпаки).

**`<T>`** — це твоя персональна карта доступу: можеш користуватись типом як хочеш, бо ти його повністю контролюєш.

---

## **Корисні приклади (🧪)**

### **Wildcard: API з невизначеним типом**

```java
public void printList(List<?> list) {
    for (Object obj : list) {
        System.out.println(obj);
    }
}
```

### **Параметризований тип: узагальнення логіки**

```java
public <T> void copyList(List<T> source, List<T> target) {
    for (T item : source) {
        target.add(item);
    }
}
```

### **Де wildcard НЕ спрацює:**

```java
public void copy(List<?> from, List<?> to) {
    // to.add(from.get(0)); ❌ - типи незалежні, навіть якщо обидва ?!
}
```

---

## **Порівняння (⚖️)**

| Сценарій | Wildcard ✅ | `<T>` ✅ |
| :---: | :---: | :---: |
| **Просто читати список** | ✅ | ✅ |
| **Додати елемент у список** | 🔶 **`super`** | ✅ |
| **Працювати з двома списками одного типу** | ❌ | ✅ |
| **Повертати тип з методу** | ❌ (тільки **`Object`**) | ✅ |
| **Повторно використовувати тип у кількох параметрах** | ❌ | ✅ |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“Wildcard для користувача, T для творця”**

* **`?`** — гостьовий режим
* **`<T>`** — повноправний адміністратор

---

## **🟩 Підсумок:**

* Wildcard (**`?, ? extends, ? super`**) — для **використання** типів, без потреби в оголошенні логіки.
* Параметризований тип **`<T>`** — для **повторного використання типу** в кількох місцях, передачі, повернення значень, узагальненої логіки.
* Правило PECS теж працює: **`extends`** — читаємо, **`super`** — додаємо.
* На співбесіді часто запитують: **"А чим це відрізняється? Коли що краще?"**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому ключова різниця між використанням wildcards (`?`) та параметризованих типів (`<T>`) у сигнатурах методів?

> #### **💬 Відповідь:**
>
> Ключова різниця полягає в **області видимості** та **призначенні**.
>
> * **Параметризований тип (`<T>`)**:
>   * Ви **оголошуєте новий тип-змінну** (`T`), яка "живе" в межах цього методу (або класу).
>   * Це дозволяє **посилатися на цей самий тип `T` кілька разів** у сигнатурі (наприклад, для кількох параметрів або для типу, що повертається). Це створює зв'язок між ними.
>   * **Приклад:** `public <T> void copy(List<T> src, List<T> dest)`. Тут `T` гарантує, що `src` і `dest` мають однаковий тип.
>
> * **Wildcard (`?`)**:
>   * Ви **не оголошуєте новий тип-змінну**. Ви просто кажете "тут може бути будь-який тип, що задовольняє певним умовам".
>   * Кожен `?` у сигнатурі є **незалежним**. Ви не можете встановити зв'язок між двома `?`.
>   * **Приклад:** `public void process(List<?> list)`. Це гнучкий спосіб прийняти будь-який список.
>
> **Коротко:** `<T>` — для створення **зв'язків між типами** та універсальної логіки. `?` — для створення **гнучких API**, яким не важливий точний тип або не потрібен зв'язок.

#### **📝 Аналогія:**

* **`<T>`:** Це як змінна `x` у математиці. `f(x, x)` означає, що обидва аргументи однакові.
* **`?`:** Це як "будь-яке число". `f(будь-яке_число, будь-яке_число)` не означає, що аргументи однакові.

#### **📌 Що перевіряють:**

*Розуміння того, що `<T>` вводить іменовану змінну типу, а `?` — анонімну.*

* * *

### **2 ❓ Питання:**

Коли варто використовувати параметризований тип (`<T>`), а коли — wildcard (`?`)?

> #### **💬 Відповідь:**
>
> Існує просте правило, запропоноване у книзі "Effective Java":
>
> **Використовуйте wildcard (`?`), якщо тип-параметр використовується у сигнатурі методу тільки ОДИН РАЗ.**
>
> **Приклад:**
>
> * `public void printAll(Collection<?> c)` — `?` використовується один раз. Тут `wildcard` є кращим, бо він простіший і не "засмічує" простір імен новим типом `T`.
>
> **Використовуйте параметризований тип (`<T>`), якщо:**
>
> * **Тип використовується кілька разів.**
>
>   * `public <T> void copy(List<T> src, List<T> dest)` — `T` використовується двічі, щоб гарантувати, що типи джерела і призначення однакові.
> * **Тип використовується як значення, що повертається.**
>
>   * `public <T> T getFirst(List<T> list)` — `T` потрібен для визначення типу повернення.
> * **Вам потрібна складна сигнатура з кількома типами.**
>
>   * `public <T, U> U convert(T from)` — потрібні імена `T` та `U` для вираження залежності.

#### **📌 Що перевіряють:**

*Практичне вміння застосовувати просте правило для вибору між `<T>` та `?`, що є ознакою написання чистого коду.*

* * *

### **3 ❓ Питання:**

Чому цей метод не скомпілюється, і як його виправити, використовуючи параметризований тип?
`public void copy(List<?> dest, List<?> src)`

> #### **💬 Відповідь:**
>
> Цей метод **скомпілюється**, але **не дозволить вам реалізувати копіювання** всередині.
>
> **Проблема:**
> Кожен `?` є **незалежним та невідомим**.
>
> ```java
> public void copy(List<?> dest, List<?> src) {
>     Object item = src.get(0); // Читаємо як Object, це ОК
>     // dest.add(item); // ПОМИЛКА КОМПІЛЯЦІЇ!
> }
> ```
>
> **Чому `dest.add()` не працює:**
> Компілятор не знає, що таке `?` для `dest`. Це може бути `List<String>`. Додавати `Object` в `List<String>` не можна. Компілятор не знає, що `?` в `src` та `?` в `dest` якось пов'язані.
>
> **Як виправити (з параметризованим типом):**
>
> Потрібно ввести тип-параметр `<T>`, щоб встановити **зв'язок** між списками.
>
> ```java
> public <T> void copy(List<T> dest, List<T> src) {
>     for (T item : src) {
>         dest.add(item);
>     }
>     // або просто dest.addAll(src);
> }
> ```
>
> Тепер компілятор знає, що обидва списки мають **один і той самий тип `T`**, тому операція `dest.add(item)` є типобезпечною.
>
> *Для максимальної гнучкості можна було б використати PECS: `<T> void copy(List<? super T> dest, List<? extends T> src)`.*

#### **📌 Що перевіряють:**

*Розуміння того, що wildcards є незалежними, та як параметризовані типи використовуються для встановлення залежностей між параметрами.*

* * *

### **4 ❓ Питання:**

Який підхід є кращим для наступного методу і чому?

1. `public <T extends Number> void processNumbers(List<T> list)`
2. `public void processNumbers(List<? extends Number> list)`

> #### **💬 Відповідь:**
>
> **Другий варіант — `public void processNumbers(List<? extends Number> list)` — є кращим.**
>
> **Чому?**
>
> Обидва методи досягають однакової мети: дозволяють передавати список з нащадками `Number`.
>
> Однак другий варіант (`? extends Number`) є **простішим та більш читабельним**. Тут тип-параметр використовується тільки **один раз** (як тип параметра `list`). Згідно з правилом "використовуй wildcard, якщо тип згадується один раз", це ідіоматичний вибір.
>
> Перший варіант (`<T extends Number> ... List<T>`) вводить **непотрібну складність**. Ми оголошуємо тип-параметр `T`, але потім **ніде його більше не використовуємо** (наприклад, для зв'язку з іншими параметрами).
>
> Отже, якщо вам не потрібно посилатися на тип `T` в іншому місці сигнатури, wildcard є більш лаконічним і кращим вибором.

#### **📌 Що перевіряють:**

*Вміння вибрати більш простий та читабельний варіант, коли обидва підходи є функціонально еквівалентними. Це свідчить про розуміння "чистого коду".*

* * *

### **5 ❓ Питання:**

Чи можна використовувати wildcard `?` для повернення значення з методу? ⭐️

> #### **💬 Відповідь:**
>
> **Технічно**, так, ви можете оголосити, що метод повертає `List<?>`.
>
> **Практично**, це зазвичай **погана ідея**, тому що це створює **проблеми для клієнтського коду**.
>
> Якщо метод повертає `List<?>`, то клієнт, який отримав цей список:
>
> * Не зможе безпечно **додати** в нього нічого (крім `null`).
> * Зможе **читати** з нього елементи тільки як `Object`, що вимагатиме небезпечних приведень типів.
>
> **Приклад:**
>
> ```java
> public List<?> getItems() {
>     return List.of("a", "b");
> }
>
> // ...
> List<?> items = getItems();
> // String s = items.get(0); // ПОМИЛКА КОМПІЛЯЦІЇ. get() повертає Object.
> ```
>
> **На противагу цьому**, дженерик-метод може повернути конкретний тип:
>
> `<T> T getFirst(List<T> list) { ... }`
>
> Цей метод повертає результат, тип якого відомий і може бути використаний без кастингу.
>
> **Висновок:** Для повернення значень краще використовувати параметризовані методи (`<T>`), а не wildcards.

#### **📌 Що перевіряють:**

*Розуміння того, як дизайн методу впливає на зручність його використання клієнтом. Хороший API не повинен змушувати клієнта робити небезпечні операції.*

* * *

### **6 ❓ Питання:**

Що таке "захоплення wildcards" (wildcard capture) і як параметризовані методи-помічники допомагають його вирішити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Захоплення wildcards** — це процес, коли компілятор **"захоплює" невідомий тип `?`** і дає йому тимчасове ім'я для виконання перевірок. Проблема виникає, коли нам потрібно виконати операцію, яка вимагає знати, що два невідомі типи однакові.
>
> **Приклад проблеми:**
> `public static void swap(List<?> list, int i, int j)`
> Ми не можемо просто написати `list.set(i, list.get(j))`, бо тип елемента з `get()` може не відповідати типу, який очікує `set()`.
>
> **Рішення: параметризований метод-помічник:**
>
> Створюється **приватний дженерик-метод**, якому передається список з wildcard.
>
> ```java
> public static void swap(List<?> list, int i, int j) {
>     // Компілятор "захоплює" тип '?' і передає його як 'T'
>     swapHelper(list, i, j);
> }
>
> // Цей метод тепер працює з іменованим типом T,
> // і компілятор знає, що всі елементи мають один тип
> private static <T> void swapHelper(List<T> list, int i, int j) {
>     list.set(i, list.set(j, list.get(i)));
> }
> ```
>
> Всередині `swapHelper` компілятор впевнений, що тип елемента, отриманого з `get(i)`, є тим самим типом `T`, який очікує `set()`. Таким чином, параметризований метод-помічник "дає ім'я" невідомому типу і дозволяє виконати типобезпечні операції.

#### **📌 Що перевіряють:**

*Знання просунутого патерну "helper method" для роботи з обмеженнями wildcards. Це демонструє експертний рівень.*

* * *

### **7 ❓ Питання:**

Яка основна відмінність між `List<T>` та `List<Object>` у контексті використання? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`List<T>` (у дженерик-класі або методі):**
>   * Означає, що список працює з **конкретним, але невідомим на етапі написання, типом `T`**.
>   * `T` визначається при створенні екземпляра класу (`new Box<String>()`) або при виклику методу.
>   * Всередині класу/методу **зберігається типобезпечність**. Компілятор знає, що всі елементи мають бути типу `T`, і не дозволить покласти об'єкт іншого типу.
>
> * **`List<Object>`:**
>   * Це **конкретний** тип, який означає, що список може зберігати об'єкти **будь-якого типу**.
>   * Це **гетерогенний** список. Ви можете одночасно додати туди `String`, `Integer`, `User`.
>   * **Недолік:** При читанні з такого списку ви отримуєте `Object`, і вам потрібні `instanceof` та ручні приведення типів, щоб працювати з конкретним об'єктом.
>
> **Головна відмінність:**
> `List<T>` забезпечує **гомогенність (однорідність)** і типобезпечність для **майбутнього** типу. `List<Object>` забезпечує **гетерогенність (різнорідність)** з втратою інформації про конкретний тип при читанні.

#### **📌 Що перевіряють:**

*Чітке розуміння того, що `<T>` — це про типобезпечну універсальність, а `<Object>` — про роботу з гетерогенними даними.*

* * *

### **8 ❓ Питання:**

Чи можна створити дженерик-масив з wildcard? Наприклад, `List<?>[]`. Чим це відрізняється від `List<String>[]`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * Створити масив конкретного параметризованого типу (`new List<String>[]`) **заборонено**.
> * Але створити масив параметризованого типу, що містить wildcard, **дозволено** компілятором: `List<?>[] arrayOfLists = new List<?>[10];`.
>
> **Чому така різниця?**
>
> Це вважається "дірою" в системі типів Java. Хоча компілятор дозволяє це, така конструкція є **нетипобезпечною** і не рекомендується до використання.
>
> **Чим це відрізняється?**
>
> * При `new List<String>[]` (якби було дозволено), JVM мала б знати, що тип компонента масиву — це `List<String>`.
> * При `new List<?>[10]`, через стирання типів, JVM знає, що тип компонента — це просто сирий `List`.
>
> Однак, оскільки масиви коваріантні, ви все одно можете потрапити в ситуацію, коли порушується типобезпечність. Тому загальне правило залишається незмінним: **уникайте змішування дженеріків та масивів**.

#### **📌 Що перевіряють:**

*Глибокі знання про взаємодію дженеріків, масивів та wildcards. Розуміння тонких аспектів типобезпечності.*

* * *

### **9 ❓ Питання:**

Якщо ваш метод приймає `List<?>`, чи можете ви використовувати для нього `Stream API`? ⭐️

> #### **💬 Відповідь:**
>
> Так, можете.
>
> Коли ви викликаєте `list.stream()`, ви отримаєте потік типу `Stream<?>`.
>
> Це означає, що кожен елемент у потоці буде розглядатися як `Object`. Ви можете виконувати будь-які операції `Stream API`, які працюють з `Object`.
>
> **Приклад:**
>
> ```java
> public void processWithStream(List<?> list) {
>     long count = list.stream() // Отримуємо Stream<?>
>                       .filter(obj -> obj != null) // Лямбда приймає Object
>                       .map(Object::toString) // Викликаємо методи Object
>                       .filter(s -> s.length() > 5)
>                       .count();
>     System.out.println("Кількість елементів з довжиною > 5: " + count);
> }
> ```
>
> Таким чином, `List<?>` повністю сумісний зі `Stream API`, але ви обмежені в роботі з елементами через API класу `Object`.

#### **📌 Що перевіряють:**

*Вміння комбінувати знання про дженеріки та `Stream API`.*

* * *

### **10 ❓ Питання:**

Чому цей метод кращий з wildcard, ніж без нього?

1. `public void addAll(List<Number> list)`
2. `public void addAll(List<? extends Number> list)`

Поясніть, що саме не так у постановці питання.

> #### **💬 Відповідь:**
>
> Постановка питання є **некоректною** або, скоріше, містить "пастку".
>
> * Метод, який приймає `List<? extends Number>`, **не може додавати** елементи (`add` заборонено), тому сама назва `addAll` для такої сигнатури є безглуздою.
>
> * Якщо мета — **читати** дані, то друга сигнатура є кращою.
>
> * Якщо ж мета — **додавати** дані, то правильною гнучкою сигнатурою була б третя, з `super`:
>     **3. `public void addAll(List<? super Number> list)`**
>
> Давайте порівняємо 1 і 3:
>
> * `addAll(List<Number> list)` може прийняти тільки `List<Number>`.
> * `addAll(List<? super Number> list)` може прийняти `List<Number>` та `List<Object>`, що робить його більш гнучким.
>
> Отже, в питанні порівнюється негнучкий метод для запису з гнучким, але непридатним для запису методом. Це демонструє важливість вибору **правильного wildcard** для поставленої задачі. Для `addAll` правильним вибором був би `super`.

#### **📌 Що перевіряють:**

*Уважність, здатність аналізувати не тільки сигнатуру, а й назву методу. Глибоке розуміння PECS.*

* * *

### **11 ❓ Питання:**

У яких випадках дженерик-метод `<T> void method(T arg)` може бути замінений на метод `void method(Object arg)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Технічно**, будь-який необмежений дженерик-метод можна замінити на метод, що працює з `Object`, оскільки після стирання типів `T` перетворюється на `Object`.
>
> Однак, **семантично** вони відрізняються.
>
> **Коли `void method(Object arg)` є прийнятним:**
>
> * Коли метод справді призначений для роботи з **будь-яким** об'єктом і використовує тільки методи класу `Object` ( `toString`, `hashCode`, `equals`).
>     `public void print(Object obj) { System.out.println(obj); }` — це цілком нормально.
>
> **Коли `<T> void method(...)` є кращим:**
>
> * **Коли потрібно встановити зв'язок між типами.**
>
>     `public <T> T identity(T arg) { return arg; }`
>     Тут `<T>` гарантує, що тип, що повертається, є тим самим, що і тип аргументу. `public Object identity(Object arg)` такої гарантії не дає.
>
> * **Коли є кілька аргументів, і вони мають бути одного типу.**
>
>     `<T> void processPair(T a, T b)`
>
> **Висновок:** Дженерик-метод використовується для забезпечення **типобезпечних зв'язків**, а `Object` — для роботи з дійсно гетерогенними даними. Якщо зв'язок не потрібен, `Object` є простішим варіантом.

#### **📌 Що перевіряють:**

*Розуміння семантичної різниці між узагальненням через `<T>` та поліморфізмом через `Object`.*

* * *

### **12 ❓ Питання:**

Поясніть, що таке "bridge method" в контексті порівняння параметризованого успадкування. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Bridge method (мостиковий метод)** — це синтетичний (згенерований компілятором) метод, який створюється для збереження **поліморфізму** після **стирання типів** у дженериках.
>
> **Проблема:**
>
> ```java
> class Node<T> { public void setData(T data) { /* ... */ } }
> class MyNode extends Node<String> {
>     @Override
>     public void setData(String data) { /* ... */ } // Перевизначення
> }
> ```
>
> **Після стирання типів:**
> `class Node { public void setData(Object data); }`
> `class MyNode extends Node { public void setData(String data); }`
>
> З точки зору JVM, метод `setData(String)` **не є** перевизначенням `setData(Object)`, оскільки у них різні сигнатури. Це перевантаження. Поліморфний виклик через посилання `Node` не спрацював би.
>
> **Рішення (Bridge method):**
>
> Компілятор додає в `MyNode` **невидимий "міст"**:
>
> ```java
> public void setData(Object data) { // Сигнатура, як у батька
>     setData((String) data); // Викликає ваш, типізований метод
> }
> ```
>
> Цей згенерований метод перевизначає батьківський, задовольняє вимоги поліморфізму і делегує виклик вашому, специфічному методу.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішні механізми роботи компілятора та JVM для підтримки дженеріків.*

* * *

### **13 ❓ Питання:**

Які обмеження `T` і `?` накладають на використання `new`, `instanceof` та статичних членів? Підсумуйте. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Всі ці обмеження є прямим наслідком **стирання типів**.
>
> * **`new` (створення екземпляра):**
>   * `new T()` — **Заборонено**. JVM не знає, що таке `T`.
>   * `new ArrayList<?>()` — **Заборонено**. `?` не є типом.
>
> * **`instanceof` (перевірка типу):**
>   * `obj instanceof T` — **Заборонено**. JVM не знає, що таке `T`.
>   * `obj instanceof List<T>` або `List<?>` — **Заборонено**. Інформація про тип-аргумент стирається.
>
> * **`static` (статичні члени):**
>   * `static T myField;` — **Заборонено**. Статичні члени належать класу, а не екземпляру, тому не можуть залежати від типу-параметра `T` екземпляра.
>
> Ці три "НЕ МОЖНА" є фундаментальними обмеженнями дженеріків, які випливають з їх реалізації через стирання.

#### **📌 Що перевіряють:**

*Систематичне розуміння всіх ключових обмежень, які накладає стирання типів.*

* * *

### **14 ❓ Питання:**

Розглянемо `Comparator<T>`. Чому краще оголошувати параметр як `Comparator<? super T>`, а не `Comparator<T>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використання `Comparator<? super T>` є кращим, оскільки це робить API значно **гнучкішим** і слідує принципу PECS.
>
> `Comparator` є **споживачем (Consumer)**. Його метод `compare(o1, o2)` "споживає" два об'єкти для порівняння. Згідно з PECS, для споживачів використовується `super`.
>
> **Практична перевага:**
>
> Це дозволяє вам використовувати **компаратор для батьківського класу** для сортування колекції **дочірніх класів**.
>
> ```java
> // У нас є компаратор, що порівнює будь-яких співробітників за зарплатою
> Comparator<Employee> salaryComparator = Comparator.comparing(Employee::getSalary);
>
> List<Manager> managers = new ArrayList<>(); // Manager extends Employee
>
> // Ми можемо сортувати список менеджерів, використовуючи компаратор для співробітників!
> managers.sort(salaryComparator);
> ```
>
> Це можливо, тому що метод `sort` має сигнатуру `sort(Comparator<? super E> c)`. Для `List<Manager>` `E` є `Manager`, тому метод очікує `Comparator<? super Manager>`. `Comparator<Employee>` задовольняє цю умову, оскільки `Employee` є супертипом `Manager`.
>
> Компаратор, що вміє порівнювати `Employee`, тим більше вміє порівнювати `Manager`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як контраваріантність (`super`) підвищує гнучкість функціональних інтерфейсів.*

* * *

### **15 ❓ Питання:**

Назвіть ситуацію, де параметризований метод є єдиним рішенням, і wildcard не може бути використаний. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Параметризований метод (`<T>`) є єдиним рішенням тоді, коли **тип повернення залежить від типу одного або кількох вхідних аргументів**, або коли потрібно забезпечити, що **кілька аргументів мають один і той самий невідомий тип**.
>
> **Приклад 1: Тип повернення залежить від аргументу**
>
> ```java
> // Цей метод повинен повернути елемент того ж типу, що й у списку.
> public static <T> T getFirst(List<T> list) {
>     return list.isEmpty() ? null : list.get(0);
> }
> ```
>
> Реалізувати це з wildcard неможливо, бо ви не можете повернути `?`. `Object` повернути можна, але тоді втрачається типобезпечність.
>
> **Приклад 2: Зв'язок між аргументами**
>
> ```java
> // Цей метод створює мапу з двох колекцій, гарантуючи, що елементи з них
> // мають один тип.
> public static <T> Map<T, T> createPairs(List<T> keys, List<T> values) {
>     // ... логіка
> }
> ```
>
> `T` тут пов'язує `keys` та `values`, а також тип ключів і значень у мапі. З wildcard (`List<?> keys, List<?> values`) такого зв'язку встановити неможливо.
>
> У цих випадках здатність `<T>` **давати ім'я типу** і використовувати це ім'я в різних частинах сигнатури є незамінною.

#### **📌 Що перевіряють:**

*Чітке розуміння сценаріїв, де іменований тип-параметр є незамінним інструментом, на відміну від анонімного wildcard.*
