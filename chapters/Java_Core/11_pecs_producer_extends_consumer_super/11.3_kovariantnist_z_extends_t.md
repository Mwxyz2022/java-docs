---
layout: default
title: "Коваріантність з ? extends T"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 3
---

# Коваріантність з ? extends T

* короткий опис: `? extends T` – це форма `upper-bounded wildcard` (`коваріантність`), що обмежує тип параметра типом `T` або будь-яким його підтипом. Дозволяє безпечно читати значення з колекції.

* поняття: Коваріантність, `? extends T`.

* приклади: `List<? extends Number>` (можна читати `Number` або `Integer`/`Double`), але не можна додавати елементи (крім `null`).

* ключові моменти: Правило "тільки читання" (Producer Extends). Компілятор забороняє додавання, щоб уникнути порушення типобезпеки.

* що важливо знати на співбесіді: Коли використовувати `? extends`, чому не можна додавати елементи, пояснення “коваріантність — тільки читання”.

### **✅ Офіційне визначення:**

Коваріантність з **`? extends T`** — це форма узагальнення (generic wildcard), яка обмежує тип параметра лише **типом `T` або його підтипами**. Вона дозволяє **читати значення** з параметризованої колекції без ризику типобезпеки.

### **🧠 Простими словами:**

Це як сказати: "Я приймаю будь-який тип, який є `T` або походить від **`T`** — я не знаю точно який, тому нічого нового туди не додаю. Просто читаю."

---

## **Основне**

📊 **Синтаксис і приклади:**

* **`List<? extends Number>`** — список, який містить **Number** або підтипи (**`Integer`**, **`Double`**, тощо)
* **`Collection<? extends Animal>`** — колекція з тваринами: **котами, собаками, коровами** — усі вони підтипи **`Animal`**

📦 **Правила використання:**

* ✅ Можна **зчитувати** елементи (тип буде **`T`**)
* ❌ Не можна **додавати** нові елементи (окрім **`null`**)
* ✅ Добре підходить для **API, які тільки читають дані**

---

## **Пояснення на пальцях (💡)**

Уяви собі:

* Ти — ветеринар 🐾
* Тобі приносять клітку з підписом: **`List<? extends Animal>`**
* Ти **можеш обстежити** всіх тварин (бо всі вони — точно тварини)
* Але ти **не можеш підселити нову** — раптом там усі коти, а ти підселиш корову?

Отже, **коваріантність \= тільки читання**. Бо не знаєш точний підтип усередині.

---

## **Корисні приклади (🧪)**

```java
public void printNumbers(List<? extends Number> numbers) {
    for (Number num : numbers) {
        System.out.println(num); // ✅ читаємо значення
    }
    // numbers.add(10); // ❌ помилка компіляції
    numbers.add(null); // ✅ null дозволяється
}

List<Integer> ints = List.of(1, 2, 3);
List<Double> doubles = List.of(3.14, 2.71);

printNumbers(ints);
printNumbers(doubles);
```

---

## **Порівняння (⚖️)**

| Тип виразу | Читання (get) | Запис (add) | Для чого |
| :---: | :---: | :---: | :---: |
| **`List<T>`** | ✅ | ✅ | Універсальне використання |
| **`List<? extends T>`** | ✅ (як **`T`**) | ❌ | Тільки читання |
| **`List<? super T>`** | ✅ (як **`Object`**) | ✅ (як `T`) | Тільки запис |

---

## **🧠 Мнемоніка для запам’ятовування:**

**PECS**:  
**P**roducer -> **E**xtends -> **C**hild -> **S**afe to read  
Якщо об'єкт **produce’иться** (з нього щось береться), то використовуй **`extends`**.

🧠 Think: **Extends -> Only Extract**

---

## **🟩 Підсумок:**

* **`? extends T`** — це **коваріантна** підстановка, означає "будь-який підтип `T`"
* Дозволяє **читати**, але **забороняє додавати**
* Дуже зручно для API, які читають дані, не змінюючи їх
* Важливо на співбесіді пам’ятати: **"extends — тільки читати"**
* Під час додавання — компілятор не дозволить, щоб уникнути проблем типів

---

**🔗 Додаткові джерела:**

* [**Oracle — Generics: Wildcards**](https://docs.oracle.com/javase/tutorial/java/generics/wildcards.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке коваріантність у дженеріках Java і як її досягти?

> #### **💬 Відповідь:**
>
> **Коваріантність** — це властивість системи типів, яка дозволяє вважати параметризований тип `Generic<A>` підтипом `Generic<B>`, якщо `A` є підтипом `B`.
>
> **Простими словами:** Якщо `Integer` є `Number`, то коваріантність дозволяє вважати `List<Integer>` "свого роду" `List<Number>`.
>
> За замовчуванням дженеріки в Java є **інваріантними**, тобто `List<Integer>` не є підтипом `List<Number>`.
>
> **Як досягти коваріантності:**
>
> Коваріантність досягається за допомогою **маски з верхньою межею (upper-bounded wildcard)** — **`? extends T`**.
>
> ```java
> List<Integer> integers = new ArrayList<>();
> // Присвоєння стає валідним, оскільки List<Integer>
> // є підтипом List<? extends Number>
> List<? extends Number> numbers = integers;
> ```
>
> Ця конструкція дозволяє створювати гнучкі API, які можуть працювати з цілими ієрархіями типів.

#### **📝 Аналогія:**

Якщо ви просите принести вам "кошик з будь-якими фруктами" (`List<? extends Fruit>`), вам можуть принести кошик яблук (`List<Apple>`) або кошик апельсинів (`List<Orange>`), і це буде коректно.

#### **📌 Що перевіряють:**

*Розуміння того, що `? extends T` є механізмом для досягнення коваріантності та вирішення проблеми інваріантності дженеріків.*

* * *

### **2 ❓ Питання:**

Які операції дозволені, а які заборонені для колекції типу `List<? extends Number>`?

> #### **💬 Відповідь:**
>
> | Операція | Чи дозволена? | Пояснення |
> |---|---|---|
> | **Читання (`get`)** | **Так, дозволено.** | Ви можете безпечно отримати будь-який елемент з колекції. Компілятор гарантує, що все, що ви отримаєте, є як мінімум `Number`. Тому результат `get()` можна присвоїти змінній типу `Number`. `Number n = list.get(0);` — валідно. |
> | **Запис (`add`)** | **Ні, заборонено** (крім `null`). | Ви **не можете додавати** нові елементи. Компілятор не знає, який **конкретний** тип (`?`) зберігається у списку. Це може бути `List<Integer>` або `List<Double>`. Спроба додати `Integer` у `List<Double>` (і навпаки) є нетипобезпечною. Щоб запобігти цьому, компілятор забороняє `add` для будь-якого об'єкта. |
>
> **Коротко:** `extends`-колекція є "виробником" (producer) або, фактично, **колекцією "тільки для читання"**.

#### **📌 Що перевіряють:**

*Чітке розуміння головного правила `extends`: читати можна, записувати — ні.*

* * *

### **3 ❓ Питання:**

Поясніть, чому такий код не скомпілюється:
`List<? extends Number> numbers = new ArrayList<Integer>();`
`numbers.add(new Integer(10));`

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється через те, що операція `add` на коваріантній колекції є нетипобезпечною, і компілятор її забороняє.
>
> **Покрокове пояснення:**
>
> 1. `List<? extends Number> numbers` означає: " `numbers` — це список, що містить елементи **якогось конкретного, але невідомого нам типу**, який є нащадком `Number`".
> 2. Ми присвоюємо йому `new ArrayList<Integer>()`. На цьому етапі цей "невідомий тип" (`?`) стає `Integer`.
> 3. **Але** змінна `numbers` все ще має тип `List<? extends Number>`. З точки зору компілятора, на неї все ще може посилатися, наприклад, `List<Double>`.
>
>     ```java
>     // Уявімо, що numbers може вказувати і на таке:
>     // numbers = new ArrayList<Double>();
>     ```
>
> 4. Коли ви намагаєтеся викликати `numbers.add(new Integer(10))`, компілятор задається питанням: "Що якщо `numbers` насправді є `List<Double>`? Тоді додавання `Integer` буде помилкою".
>
> 5. Оскільки компілятор **не може гарантувати** безпеку цієї операції для **всіх можливих** підтипів, що задовольняють `? extends Number`, він **забороняє** її.
>
> Це захисний механізм, який запобігає забрудненню купи (heap pollution).

#### **📌 Що перевіряють:**

*Здатність пояснити логіку компілятора: якщо безпеку не можна гарантувати, операція забороняється.*

* * *

### **4 ❓ Питання:**

Як правило PECS ("Producer Extends, Consumer Super") стосується коваріантності?

> #### **💬 Відповідь:**
>
> Коваріантність ( `? extends T`) відповідає за першу частину цього правила — **Producer Extends**.
>
> **Що це означає:**
>
> * Якщо у вашому методі структура даних (колекція, потік) є **виробником (producer)**, тобто ви з неї **тільки отримуєте (читаєте)** дані, ви повинні оголосити її параметр з `? extends T`.
>
> **Приклад:**
> Метод, що обчислює суму чисел у списку. Список тут є виробником.
>
> **Погана сигнатура (негнучка):**
> `public double sum(List<Number> list) { ... }` // Прийме тільки List<Number>
>
> **Хороша сигнатура (гнучка, з PECS):**
> `public double sum(List<? extends Number> list) { ... }` // Прийме і List<Integer>, і List<Double>
>
> `extends` розширює можливості вашого API, дозволяючи приймати колекції з більш специфічними типами (нащадками).

#### **📌 Що перевіряють:**

*Розуміння практичного застосування коваріантності для створення гнучкого, але безпечного API, згідно з правилом PECS.*

* * *

### **5 ❓ Питання:**

Чим коваріантність дженеріків (`List<? extends Number>`) відрізняється від коваріантності масивів (`Number[]`)? ⭐️

> #### **💬 Відповідь:**
>
> Це ключова відмінність у дизайні системи типів Java.
>
> * **Коваріантність масивів (`Number[]`):**
>   * **Типобезпечність:** **Не є типобезпечною на етапі компіляції**.
>   * **Перевірка:** Відбувається **під час виконання (runtime)**.
>   * **Наслідки:** Ви можете присвоїти `Integer[]` до `Number[]` і спробувати додати туди `Double`, що призведе до **`ArrayStoreException` в рантаймі**.
>
> * **Коваріантність дженеріків (`List<? extends Number>`):**
>   * **Типобезпечність:** **Гарантується на етапі компіляції**.
>   * **Перевірка:** Відбувається **під час компіляції**.
>   * **Наслідки:** Компілятор **забороняє** будь-які операції запису (`add`) в таку колекцію. Це запобігає можливості виникнення `ClassCastException` через невідповідність типів.
>
> **Висновок:** Дженеріки є більш безпечним механізмом. Вони виявляють помилки **рано** (на етапі компіляції), тоді як масиви дозволяють писати потенційно небезпечний код, який "падає" вже під час роботи.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що дженеріки були створені для виправлення недоліків (небезпечної коваріантності) масивів.*

* * *

### **6 ❓ Питання:**

Наведіть приклад з JDK, де `extends`-wildcard активно використовується в сигнатурі методу. ⭐️

> #### **💬 Відповідь:**
>
> Один з найпростіших і найпоширеніших прикладів — конструктор `ArrayList`, що приймає іншу колекцію:
>
> **`public ArrayList(Collection<? extends E> c)`**
>
> **Пояснення:**
>
> 1. Цей конструктор створює новий `ArrayList`, копіюючи елементи з іншої колекції `c`.
> 2. Колекція `c` тут є **виробником (producer)** — ми з неї тільки читаємо елементи для копіювання.
> 3. Завдяки `? extends E`, ви можете створити `ArrayList<Number>`, передавши в нього, наприклад, `List<Integer>` або `Set<Double>`.
>
>     ```java
>     List<Integer> integers = List.of(1, 2, 3);
>     // Створюємо ArrayList<Number> з List<Integer>
>     // Це можливо завдяки `? extends Number` (E тут - Number)
>     List<Number> numbers = new ArrayList<>(integers);
>     ```
>
>
> Це робить конструктор максимально гнучким і корисним.

#### **📌 Що перевіряють:**

*Здатність впізнавати застосування wildcards у коді стандартної бібліотеки.*

* * *

### **7 ❓ Питання:**

Якщо у вас є `List<? extends Animal>`, чи можна викликати на його елементах методи, визначені в класі `Animal`? ⭐️

> #### **💬 Відповідь:**
>
> Так, **можна**.
>
> Обмеження `? extends Animal` гарантує компілятору, що кожен елемент у цій колекції є або екземпляром класу `Animal`, або екземпляром **його нащадка**.
>
> Згідно з принципами поліморфізму, будь-який нащадок `Animal` має всі публічні та `protected` методи класу `Animal`.
>
> Тому ви можете безпечно отримати елемент і працювати з ним як з `Animal`:
>
> ```java
> public void feedAll(List<? extends Animal> animals) {
>     for (Animal animal : animals) {
>         // Ми можемо викликати метод, який є у Animal
>         animal.eat();
>     }
> }
> ```
>
> Саме в цьому і полягає головна мета `extends`-wildcard — дозволити **безпечне читання та використання** елементів через API базового типу.

#### **📌 Що перевіряють:**

*Розуміння того, як обмежені дженеріки взаємодіють з поліморфізмом.*

* * *

### **8 ❓ Питання:**

Уявіть, що є два методи. Який з них буде більш гнучким і чому?

1. `public void process(ArrayList<Number> list)`
2. `public void process(List<? extends Number> list)`

> #### **💬 Відповідь:**
>
> **Другий метод — `public void process(List<? extends Number> list)` — є значно більш гнучким.**
>
> **Чому:**
>
> * **Перший метод (`ArrayList<Number>`):**
>   * Приймає **тільки** екземпляри конкретного класу `ArrayList`, параметризовані **тільки** типом `Number`. Ви не зможете передати в нього `LinkedList<Number>` або `ArrayList<Integer>`. Він є дуже обмежуючим.
>
> * **Другий метод (`List<? extends Number>`):**
>   * **По-перше**, він приймає **будь-яку реалізацію `List`**, а не тільки `ArrayList`.
>   * **По-друге**, завдяки **коваріантності (`? extends Number`)**, він може прийняти список, параметризований **`Number` або будь-яким його нащадком** (`Integer`, `Double` тощо).
>
> **Правило хорошого дизайну API:** **"Будьте гнучкими у тому, що приймаєте, і конкретними у тому, що повертаєте"**. Другий метод слідує цьому принципу.

#### **📌 Що перевіряють:**

*Розуміння того, як проектувати гнучкі API, використовуючи інтерфейси та wildcards замість конкретних класів.*

* * *

### **9 ❓ Питання:**

Чи можна присвоїти `null` змінній `List<? extends Number>`? А викликати `add(null)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Присвоїти `null` змінній:** **Так, можна.**
>
>     ```java
>     List<? extends Number> list = null; // Валідно
>     ```
>
>     `null` можна присвоїти будь-якій змінній посилального типу.
>
> * **Викликати `add(null)`:** **Ні, не можна** (зазвичай).
>
>     Хоча в теорії `null` можна "привести" до будь-якого типу, і це, здавалося б, не повинно порушувати типобезпечність, **реалізація компілятора забороняє це**. Метод `add` для типу з `extends` не приймає жодних аргументів.
>
>     **Але** якщо бути точним, є деякі специфічні сценарії, де компілятор може дозволити це (пов'язано з `capture of ?`), але загальне правило, яке працює на практиці: `add` для `? extends T` не працює ні для чого.

#### **📝 Важливий нюанс:**</h4>

Думки про `add(null)` часто розходяться, але для більшості практичних цілей простіше запам'ятати: **`extends` — не для запису.**

#### **📌 Що перевіряють:**

*Уважність до крайових випадків (робота з `null`) та глибокі знання про реалізацію перевірок компілятором.*

* * *

### **10 ❓ Питання:**

Чому цей код не скомпілюється, хоча він виглядає логічним?
`List<Number> nums = new ArrayList<>();`
`List<? extends Number> ints = List.of(1,2);`
`nums.addAll(ints);` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється через сигнатуру методу `Collection.addAll(Collection<? extends E> c)`.
>
> Розберемо виклик `nums.addAll(ints)`:
>
> 1. `nums` має тип `List<Number>`, тому тип `E` для цього екземпляра — `Number`.
> 2. Метод `addAll` для `nums` очікує аргумент типу `Collection<? extends Number>`.
> 3. `ints` має тип `List<? extends Number>`.
>
> Проблема в тому, що **`List<? extends Number>` не є підтипом `Collection<? extends Number>`**.
>
> **Чому?**
> Невідомий тип (`?`) в `ints` може бути чимось, що не є сумісним з `Number`. Наприклад, гіпотетично `List<NotANumber>` може бути присвоєний `List<?>`, що є сумісним з `List<? extends Number>`, якщо компілятор втрачає точність.
>
> **Як це виправити:**
>
> ```java
> List<Number> nums = new ArrayList<>();
> List<Integer> ints = List.of(1,2); // Використовувати конкретний тип
> nums.addAll(ints); // Тепер OK, бо List<Integer> є Collection<? extends Number>
> ```
>
> Ця проблема є одним з найскладніших аспектів wildcards, який демонструє, що "невідомий нащадок" не може бути присвоєний "невідомому нащадку".

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як wildcards взаємодіють між собою. Це питання рівня Senior.*

* * *

### **11 ❓ Питання:**

Якщо у вас є `List<Object>`, чи можете ви присвоїти їй `List<Integer>`? А `List<? extends Number>`?

> #### **💬 Відповідь:**
>
> * **`List<Object> list = new ArrayList<Integer>();` — НЕ СКОМПІЛЮЄТЬСЯ.**
>   * Дженеріки є **інваріантними**. `List<Integer>` не є підтипом `List<Object>`.
>
> * **`List<? extends Number> list = new ArrayList<Integer>();` — СКОМПІЛЮЄТЬСЯ.**
>   * `extends` робить тип **коваріантним**. Оскільки `Integer` є нащадком `Number`, `List<Integer>` є підтипом `List<? extends Number>`.

#### **📌 Що перевіряють:**

*Повторення і закріплення знань про інваріантність та коваріантність.*

* * *

### **12 ❓ Питання:**

Чи може `? extends T` бути використаний в оголошенні класу? Наприклад, `class MyClass<? extends Number> { ... }`.

> #### **💬 Відповідь:**
>
> Ні, **не може**. Wildcard `?` **не може використовуватися** для оголошення **типів-параметрів** класу або методу.
>
> `public class MyClass<?> { ... } // ПОМИЛКА`
>
> Wildcard використовується тільки як **тип-аргумент** для вже існуючих дженерик-типів у змінних, полях, параметрах або значеннях, що повертаються.
>
> `private List<? extends Number> numbers; // OK`
>
> Щоб обмежити тип для класу, потрібно використовувати іменований тип-параметр:
> `public class MyClass<T extends Number> { ... } // ПРАВИЛЬНО`

#### **📌 Що перевіряють:**

*Знання синтаксичних правил та розрізнення між оголошенням типу-параметра (`<T>`) і використанням типу-аргумента з wildcard (`<?>`).*

* * *

### **13 ❓ Питання:**

Поясніть, що відбувається зі стиранням типів (Type Erasure) для `List<? extends Number>`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> При стиранні типів, тип-параметр або wildcard замінюється на свою **межу**.
>
> * Для необмежених типів (`List<T>`, `List<?>`) межею є **`Object`**.
> * Для обмежених типів (`List<T extends Number>`, `List<? extends Number>`) межею є **`Number`**.
>
> **Отже, після стирання типів, `List<? extends Number>` у байт-коді стає просто сирим `List`, який компілятор та JVM "вважають" таким, що працює з `Number`.**
>
> **Приклад:**
>
> **Код до стирання:**
> `List<? extends Number> list = ...; Number n = list.get(0);`
>
> **Як це виглядає після стирання (спрощено):**
> `List list = ...; Number n = (Number) list.get(0);`
>
> Компілятор знає, що межею є `Number`, тому він вставляє безпечний каст до `Number`, а не до `Object`.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів працює з `bounded` типами.*

* * *

### **14 ❓ Питання:**

Чому коваріантність корисна в основному для API, які приймають дані (параметри методів)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Коваріантність (`? extends T`) є корисною для параметрів методів, оскільки вона робить API **максимально гнучким для клієнта**.
>
> Уявіть, що у вас є метод: `processNumbers(Collection<Number> numbers)`.
> Клієнт, у якого є `List<Integer>`, не може викликати ваш метод. Йому доведеться створювати нову колекцію `List<Number>` і копіювати туди дані, що неефективно.
>
> Якщо ж ви оголосите метод як `processNumbers(Collection<? extends Number> numbers)`, то клієнт зможе **напряму передати свою `List<Integer>`**, `Set<Double>` або будь-яку іншу колекцію з нащадками `Number`.
>
> Це покращує **юзабіліті** та **ефективність** вашого API, оскільки клієнтам не потрібно робити зайвих перетворень. Ви "приймаєте" все, що є сумісним, замість того, щоб вимагати точного типу.

#### **📌 Що перевіряють:**

*Здатність думати з точки зору дизайнера API та розуміти, як гнучкість параметрів покращує досвід використання бібліотеки/методу.*

* * *

### **15 ❓ Питання:**

Розглянемо `Comparator.naturalOrder()`. Його сигнатура `public static <T extends Comparable<? super T>> Comparator<T> naturalOrder()`. Поясніть її. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця сигнатура використовує **F-bounded polymorphism**, щоб повернути максимально гнучкий та типобезпечний `Comparator`.
>
> Розберемо її:
>
> 1. **`<T extends Comparable<? super T>>`**: Це обмеження на тип `T`.
>     * Метод може працювати тільки з тими типами `T`, які самі по собі є порівнюваними.
>     * `Comparable<? super T>` — це гнучке обмеження. Воно означає, що `T` може бути порівнюваним не тільки з `T`, але і з будь-яким своїм **предком**. Це дозволяє коректно працювати з успадкуванням.
>
> 2. **`Comparator<T>`**:
>     * Метод повертає `Comparator`, який вміє порівнювати об'єкти типу `T`.
>
> **Як це працює на практиці:**
>
> Цей метод повертає компаратор, який просто викликає `o1.compareTo(o2)`. Завдяки складним обмеженням, компілятор впевнений, що будь-який тип `T`, для якого ви викликаєте `naturalOrder()`, матиме відповідний метод `compareTo`.
>
> **Приклад:**
>
> `List<String> list = ...; list.sort(Comparator.naturalOrder());`
>
> * Компілятор бачить, що `String implements Comparable<String>`.
> * `String` задовольняє умову `T extends Comparable<? super T>`, бо `? super String` включає `String`.
> * Тому `Comparator.naturalOrder()` повертає `Comparator<String>`.
>
> Це чудовий приклад того, як складні дженерики використовуються для створення простого та потужного API.

#### **📌 Що перевіряють:**

*Експертний рівень володіння дженеріками. Здатність аналізувати і пояснювати навіть найскладніші сигнатури з JDK.*
