---
layout: default
title: "Як обирати варіантність у власному API"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 10
---

# Як обирати варіантність у власному API

* короткий опис: При дизайні власного API слід чітко обирати варіантність (`? extends T`, `? super T`, або `<T>`) залежно від того, чи метод споживає дані (пише), продукує дані (читає), чи робить і те, і те.

* поняття: Варіантність, `? extends T` (для читання), `? super T` (для запису), `<T>` (для читання та запису).

* приклади: `void logAll(List<? super String> messages)` (метод лише записує), `List<? extends Config> loadSettings()` (метод лише читає).

* ключові моменти: Правильний вибір варіантності робить API гнучким, безпечним та інтуїтивно зрозумілим. Дотримання PECS.

* що важливо знати на співбесіді: Як обрати між `extends`/`super`/`T` для методів, типові помилки початківців.

### **✅ Офіційне визначення:**

Варіантність (covariance/contravariance) у Java через wildcard-и (**`? extends T, ? super T`**) — це спосіб контролю допустимих операцій над дженериками при дизайні API. Власний API повинен чітко вказувати: дані **споживаються**, **продукуються** чи і **те, і те**.

### **🧠 Простими словами:**

Потрібно обрати правильну варіантність, щоб користувач твого API не випадково порушив типову безпеку. Якщо API читає — **`extends`**, якщо пише — **`super`**, якщо обидва — параметризований тип **`<T>`**.

---

## **📊 Таблиця прийняття рішень:**

| Ситуація | Вибір | Чому? |
| :---: | :---: | :---: |
| Читання з колекції/потоку | **`? extends T`** | Дані приходять звідкись |
| Запис у колекцію/буфер | **`? super T`** | Дані треба покласти |
| І читання, і запис | **`<T>`** | Обидва напрями, потрібна симетрія |
| Тимчасове зберігання/обробка | **`<T>`** | Повний контроль над типом |
| API працює тільки з **`T`** | **`<T>`** | Не потрібна варіантність |

---

## **Пояснення на пальцях (💡)**

API — як шлагбаум: відкриваєш тільки в один бік або двосторонній рух.

* Якщо дозволиш і читати, і писати через wildcard — користувач легко отримає **`Compile-time error`**.
* Тому:
  * **`? extends T`** \= "Обережно\! Лише вхід\! (тільки читати)"
  * **`? super T`** \= "Обережно\! Лише вихід\! (тільки писати)"
  * **`<T>`** \= "Можна все — головне, щоб ти розумів, що робиш"

---

## **Корисні приклади (🧪)**

### **📤 API для логування (тільки пише):**

```java
void logAll(List<? super String> messages)
```

— ми передаємо повідомлення, але не читаємо їх

---

### **📥 API для читання конфігів:**

```java
void printSettings(List<? extends Config>)
```

— налаштування читаються, не модифікуються

---

### **🔄 API для буферу:**

```java
class Buffer<T> {
    void add(T item)
    T get()
}
```

— і додаємо, і читаємо -> потрібен конкретний **`<T>`**

---

## **Порівняння (⚖️)**

| Варіантність | Сценарій | Плюси | Мінуси |
| :---: | :---: | :---: | :---: |
| **`? extends T`** | Читання | Гнучко, безпечно | Неможливо додати елемент |
| **`? super T`** | Запис | Гнучко для запису | Не можна безпечно читати |
| **`<T>`** | Читання \+ Запис | Повний контроль | Менше гнучкості з типами |

---

## **🧠 Мнемоніка для запам’ятовування:**

❓ Читаєш? -> **Extends**  
❗ Пишеш? -> **Super**  
🌀 І те, і те? -> **T**

**ЧЕС** — Читання -> Extends, Супер — Писання -> Super

---

## **🟩 Підсумок:**

* Дизайн варіантності — критично важливий для гнучкого та безпечного API.
* Золоті правила:
  * **`? extends T`** — для **джерел**
  * **`? super T`** — для **приймачів**
  * **`<T>`** — для **повного контролю**
* Правильний вибір варіантності зменшує кількість багів і підвищує читабельність.
* Не бійся wildcard-ів — вони твої союзники в дизайні, а не вороги\!

---

**🔗 Додаткові джерела:**

* [**Oracle: Generics \- Guidelines**](https://docs.oracle.com/javase/tutorial/java/generics/generics-methods.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Який основний принцип слід використовувати при виборі варіантності для параметрів методів у власному API?

> #### **💬 Відповідь:**
>
> Основний принцип, який слід використовувати — це **PECS (Producer Extends, Consumer Super)**. Він допомагає проектувати максимально гнучкий та водночас типобезпечний API.
>
> **Правила такі:**
>
> 1. **"Producer Extends"**: Якщо параметр-колекція у вашому методі є **виробником (producer)**, тобто ви з нього **тільки читаєте** дані, використовуйте **`? extends T`**. Це дозволить клієнтам передавати колекції з підтипами `T`.
>
> 2. **"Consumer Super"**: Якщо параметр-колекція є **споживачем (consumer)**, тобто ви в нього **тільки записуєте** дані, використовуйте **`? super T`**. Це дозволить клієнтам передавати колекції з супертипами `T`.
>
> 3. **Без wildcards:** Якщо ваш метод одночасно і читає, і записує в колекцію, не використовуйте wildcards, а використовуйте точний тип (`List<T>`).
>
> Слідування цьому принципу робить ваш API зручним для використання, оскільки він не накладає на клієнтів непотрібних обмежень.

#### **📝 Аналогія:**</h4>

Проектуючи роз'єм, подумайте, для чого він:

* Якщо для отримання сигналу (прослуховування музики) — робіть його сумісним з будь-якими навушниками (`extends`).
* Якщо для передачі сигналу (підключення мікрофона) — робіть його сумісним з будь-яким підсилювачем (`super`).

#### **📌 Що перевіряють:**

*Знання та розуміння принципу PECS як основи для дизайну гнучких API.*

* * *

### **2 ❓ Питання:**

Припустимо, ви пишете метод для логування, який приймає список повідомлень і виводить їх у консоль. Яку сигнатуру ви оберете і чому?

1. `void logMessages(List<String> messages)`
2. `void logMessages(List<?> messages)`
3. `void logMessages(List<? extends Object> messages)`
4. `void logMessages(Iterable<?> messages)`

> #### **💬 Відповідь:**
>
> Найкращим вибором є **варіант 4: `void logMessages(Iterable<?> messages)`**.
>
> **Пояснення:**
>
> 1. **Аналізуємо роль параметра:** Метод тільки **читає** дані з колекції, щоб викликати для них `toString()`. Отже, він є **споживачем** самої колекції, але **виробником** її елементів (в термінах доступу).
>
> 2. **Аналізуємо гнучкість:**
>     * `List<String>` — занадто обмежуюче. Метод не зможе прийняти `List<Object>` або `Set<Integer>`.
>     * `List<?>` та `List<? extends Object>` є еквівалентними, але вони вимагають саме `List`. Чому не `Set`?
>
> 3. **Вибір найкращого інтерфейсу:**
>     Для виведення в консоль нам потрібна лише можливість **ітерації** по елементах. Інтерфейс **`Iterable`** є найбільш загальним, який надає таку можливість. Він є супертипом і для `List`, і для `Set`.
>
>     **`Iterable<?>`** є найкращим, бо:
>     * Він **максимально гнучкий** для клієнта (прийме і `List`, і `Set`).
>     * Він **чітко виражає намір**: "мені від цієї колекції потрібна лише можливість її обійти".
>
> **Правило хорошого API:** **"Будьте максимально гнучкими у тому, що приймаєте".**

#### **📌 Що перевіряють:**

*Здатність проектувати API з мінімально необхідними вимогами. Розуміння того, коли використовувати `Iterable`, `Collection` чи `List`/`Set`.*

* * *

### **3 ❓ Питання:**

Ви проектуєте метод, що об'єднує два списки. Яка з цих сигнатур є найкращою?

1. `<T> void union(List<T> list1, List<T> list2, List<T> dest)`
2. `<T> void union(List<? extends T> list1, List<? extends T> list2, List<? super T> dest)`

> #### **💬 Відповідь:**
>
> **Друга сигнатура є значно кращою**, оскільки вона слідує принципу PECS і надає максимальну гнучкість.
>
> `<T> void union(List<? extends T> list1, List<? extends T> list2, List<? super T> dest)`
>
> **Аналіз:**
>
> * `List<? extends T> list1`, `List<? extends T> list2` (**Producers**):
>   * Списки `list1` та `list2` є джерелами (виробниками) даних. Ми з них тільки **читаємо**.
>   * `extends` дозволяє клієнту передавати сюди списки з підтипами `T`. Наприклад, якщо `T` — `Number`, клієнт може передати `List<Integer>` і `List<Double>`.
>
> * `List<? super T> dest` (**Consumer**):
>   * Список `dest` є приймачем (споживачем) даних. Ми в нього тільки **записуємо**.
>   * `super` дозволяє клієнту передати список, типізований `T` або будь-яким його предком. Наприклад, елементи типу `Number` можна записати у `List<Object>`.
>
> Перша сигнатура змусила б усі три списки бути одного й того ж точного типу, що є дуже незручним і негнучким.

#### **📌 Що перевіряють:**

*Вміння застосувати PECS до методу з кількома параметрами-колекціями.*

* * *

### **4 ❓ Питання:**

Якщо ваш метод має повернути колекцію, чи варто використовувати wildcard у типі, що повертається? Наприклад, `List<?> getItems()`.

> #### **💬 Відповідь:**
>
> **Ні, зазвичай не варто.** Використання wildcards у типах, що повертаються, є **поганою практикою**, оскільки це створює **проблеми для клієнтського коду**.
>
> Якщо метод повертає `List<?>`, клієнт:
>
> * **Не може нічого додати** у цей список.
> * **Не може безпечно прочитати** елементи, окрім як `Object`.
>
> ```java
> List<?> items = myApi.getItems();
> // String s = items.get(0); // ПОМИЛКА КОМПІЛЯЦІЇ. get() повертає Object
> ```
>
> Це змушує клієнта робити небезпечні приведення типів, що нівелює всі переваги дженеріків.
>
> **Що робити натомість:**
>
> * Повертайте **конкретний параметризований тип** (`List<String>`).
> * Якщо вам потрібно заборонити модифікацію, повертайте **незмінну обгортку**: `Collections.unmodifiableList(myList)`. Це значно краще, ніж покладатися на обмеження wildcards.
>
> **Правило:** "Будьте конкретними у тому, що повертаєте".

#### **📌 Що перевіряють:**

*Розуміння того, як проектувати API, зручний для використання. Wildcard-и у значеннях, що повертаються, роблять API незручним.*

* * *

### **5 ❓ Питання:**

Коли варто використовувати точний тип `List<T>` у параметрах методу, ігноруючи PECS? ⭐️

> #### **💬 Відповідь:**
>
> Використовувати точний тип `List<T>` (без wildcards) потрібно тоді, коли ваш метод має **одночасно і читати, і записувати** дані в одну й ту саму колекцію.
>
> **PECS не застосовується**, оскільки колекція є і **виробником**, і **споживачем**.
>
> **Приклад:** Метод, який замінює всі входження одного елемента на інший.
>
> ```java
> public <T> void replace(List<T> list, T from, T to) {
>     for (int i = 0; i < list.size(); i++) {
>         // Читання (get)
>         if (list.get(i).equals(from)) {
>             // Запис (set)
>             list.set(i, to);
>         }
>     }
> }
> ```
>
> * `? extends T` не підійде, бо заборонить `set()`.
> * `? super T` не підійде, бо `get()` поверне `Object`.
>
> Тому в таких випадках, коли потрібен повний контроль над операціями, використовується точна параметризація `List<T>`.

#### **📌 Що перевіряють:**

*Розуміння того, коли PECS не застосовується, і чому в таких випадках потрібен точний тип.*

* * *

### **6 ❓ Питання:**

При проектуванні методу ви бачите, що тип-параметр використовується лише один раз у сигнатурі. Що це говорить вам про вибір між `<T>` та `?`? ⭐️

> #### **💬 Відповідь:**
>
> Це чіткий сигнал, що краще використовувати **wildcard (`?`)**.
>
> **Правило:** **Якщо тип-параметр використовується у сигнатурі методу тільки один раз, замініть його на wildcard.**
>
> **Погано (надлишково):**
> `<T> void printCollection(Collection<T> collection)`
>
> **Краще:**
> `void printCollection(Collection<?> collection)`
>
> **Чому так?**
>
> 1. **Простота та читабельність:** Сигнатура з wildcard є коротшою і простішою. Вона не вводить новий тип-змінну (`T`), який ніде більше не використовується.
> 2. **Ясність наміру:** `Collection<?>` чітко говорить: "Я приймаю будь-яку колекцію, і мені не важливий тип її елементів".
>
> Оголошення `<T>` має сенс тільки тоді, коли вам потрібно це `T` десь ще в сигнатурі (в іншому параметрі, у значенні, що повертається) для встановлення зв'язку.

#### **📌 Що перевіряють:**

*Знання цієї важливої евристики з "Effective Java" для написання чистого API.*

* * *

### **7 ❓ Питання:**

Який тип (`?` чи `<T>`) ви оберете для приватного методу-помічника, і чи відрізняється цей вибір від публічного API? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вибір може відрізнятися.
>
> * Для **публічного API** головним пріоритетом є **гнучкість для клієнта**. Тому тут PECS та wildcards є дуже важливими. `public void process(List<? extends Number> list)`
>
> * Для **приватного методу-помічника** пріоритетом може бути **простота реалізації** та **зручність для розробника**.
>
> Часто **параметризований тип `<T>`** є зручнішим для приватних методів, оскільки він дає більше контролю всередині методу.
>
> Класичний приклад — "захоплення wildcards":
>
> ```java
> public static void reverse(List<?> list) {
>     // Публічний API гнучкий.
>     // Всередині викликаємо приватний метод з <T>
>     reverseHelper(list);
> }
>
> // Приватний метод використовує <T> для зручності реалізації
> private static <T> void reverseHelper(List<T> list) {
>     // ... логіка, яка тепер може безпечно працювати з T
> }
> ```
>
> Таким чином, публічний API може використовувати wildcards, щоб бути гнучким, а його внутрішня реалізація може використовувати дженерик-метод, щоб спростити логіку.

#### **📌 Що перевіряють:**

*Здатність думати про різні контексти (публічний API vs приватна реалізація) та вибирати відповідні інструменти.*

* * *

### **8 ❓ Питання:**

Припустимо, ви пишете утилітарний клас `StackUtils`. Як ви спроектуєте сигнатуру методу `popAll`, який перекладає всі елементи з одного стеку в інший? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використовуємо стек, реалізований через `Deque`. І застосовуємо PECS.
>
> `public static <T> void popAll(Deque<? extends T> src, Collection<? super T> dest)`
>
> **Аналіз:**
>
> 1. `Deque<? extends T> src`:
>     * Вихідний стек `src` є **виробником (producer)**. Ми будемо з нього тільки "виштовхувати" (`pop`) елементи, тобто читати.
>     * `extends` дозволяє передати `Deque<Integer>` для `T`, встановленого як `Number`.
>
> 2. `Collection<? super T> dest`:
>     * Цільова колекція `dest` є **споживачем (consumer)**. Ми в неї тільки додаємо (`add`) елементи.
>     * `super` дозволяє нам перекладати елементи з `Deque<Integer>` у, наприклад, `List<Number>`.
>     * Тут використовується `Collection` замість `Deque`, бо приймаюча сторона може бути будь-якою колекцією, не обов'язково стеком, що робить API ще більш гнучким.
>
> **Реалізація:**
>
> ```java
> public static <T> void popAll(Deque<? extends T> src, Collection<? super T> dest) {
>     while (!src.isEmpty()) {
>         dest.add(src.pop());
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Практичне вміння застосувати PECS до нетривіального сценарію, вибираючи при цьому найбільш загальні типи для гнучкості.*

* * *

### **9 ❓ Питання:**

Ви проектуєте метод, що приймає `Function<A, B>`. Як можна зробити його більш гнучким за допомогою wildcards? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Потрібно застосувати PECS до обох параметрів `Function`: до її вхідного аргументу і до значення, що повертається.
>
> **Початкова сигнатура:**
> `public <A, B> B applyFunction(A input, Function<A, B> func)`
>
> **Гнучка сигнатура:**
> `public <A, B> B applyFunction(A input, Function<? super A, ? extends B> func)`
>
> **Пояснення:**
>
> * `? super A` (вхідний аргумент): `Function` є **споживачем** свого входу. Контраваріантність (`super`) дозволяє передати функцію, яка приймає `A` або будь-якого його предка. (Наприклад, передати `Function<Object, ...>` для обробки `String`).
>
> * `? extends B` (значення, що повертається): `Function` є **виробником** свого результату. Коваріантність (`extends`) дозволяє функції повернути значення типу `B` або будь-якого його нащадка. (Наприклад, якщо очікується `Number`, функція може повернути `Integer`).
>
> Цей патерн робить метод максимально гнучким, як і в `Stream.map`.

#### **📌 Що перевіряють:**

*Здатність застосувати PECS до функціональних інтерфейсів.*

* * *

### **10 ❓ Питання:**

Чому цей метод має проблеми, і як wildcard це виправить?
`public boolean containsAll(Collection<T> c1, Collection<T> c2)`

> #### **💬 Відповідь:**
>
> Цей метод перевіряє, чи містить `c1` усі елементи `c2`.
>
> **Проблема:**
> Він вимагає, щоб обидві колекції мали **точно той самий тип `T`**. Ви не зможете перевірити, чи містить `Collection<Object>` усі елементи `Collection<String>`.
>
> **Як wildcard це виправить:**
>
> Обом параметрам не важливий їхній точний тип. Нам потрібно лише мати можливість ітерувати по `c2` і викликати `contains` для `c1`.
>
> `public boolean containsAll(Collection<?> c1, Collection<?> c2)`
>
> **Аналіз:**
>
> * `c1.contains(obj)` очікує `Object`, що ми можемо отримати з `c2`.
> * Ми можемо ітерувати по `c2` і отримувати `Object`.
>
> ```java
> public static boolean containsAll(Collection<?> c1, Collection<?> c2) {
>     for (Object o : c2) {
>         if (!c1.contains(o)) {
>             return false;
>         }
>     }
>     return true;
> }
> ```
>
> Стандартна реалізація `Collection.containsAll` має сигнатуру `containsAll(Collection<?> c)`, що підтверджує цей підхід.

#### **📌 Що перевіряють:**

*Вміння аналізувати вимоги до операцій і вибирати найменш обмежуючу сигнатуру. Розуміння, коли потрібен `<?>`.*

* * *

### **11 ❓ Питання:**

У чому може бути помилка в дизайні, якщо ваш метод має таку сигнатуру: `List<? extends T> method(List<? super T> list)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже дивна і, скоріш за все, помилкова сигнатура, хоча й валідна.
>
> **Аналіз:**
>
> 1. **Вхідний параметр `List<? super T> list`**:
>     * Це "споживач". Ви можете **додавати** в нього об'єкти типу `T`, але **читати** можете тільки як `Object`.
>
> 2. **Тип, що повертається `List<? extends T>`**:
>     * Це "виробник". Клієнтський код зможе **читати** з нього об'єкти як `T`, але **не зможе додавати**.
>
> **Проблема:**
>
> Всередині цього методу ви не зможете ефективно **перекласти** елементи з `list` у список, що повертається, бо при читанні з `list` ви отримуєте `Object`, а повернути вам потрібно `List<? extends T>`. Доведеться робити небезпечні касти.
>
> ```java
> public <T> List<? extends T> method(List<? super T> list) {
>     List<T> result = new ArrayList<>();
>     for(Object o : list) {
>         // Потрібен небезпечний каст!
>         result.add((T) o);
>     }
>     return result;
> }
> ```
>
> Це сигнатура, яка **створює більше проблем, ніж вирішує**. Вона вказує на те, що, ймовірно, розробник не до кінця зрозумів принципи PECS або намагається зробити щось дуже нетипове.
>
> API має бути простим і зрозумілим. Така сигнатура не є ні тим, ні іншим.

#### **📌 Що перевіряють:**

*Здатність аналізувати API на "кодові запахи". Розуміння того, що валідний синтаксично код не завжди є хорошим з точки зору дизайну.*

* * *

### **12 ❓ Питання:**

Чому краще уникати wildcards у типах, що повертаються, з точки зору клієнта API? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що це змушує **клієнта** вирішувати проблеми з типами, які мав би вирішити **автор API**.
>
> Якщо ваш метод повертає `List<?>`, ви, як автор, кажете клієнту: "Ось тобі список, я не знаю, що в ньому, розбирайся сам".
>
> **Наслідки для клієнта:**
>
> * Він не може нічого додати в цей список.
> * Він може отримати з нього елементи тільки як `Object`.
> * Це змушує клієнта писати код з **неперевіреними приведеннями типів (`unchecked casts`)**, щоб працювати з даними, що є небезпечним.
>
> **Правильний дизайн:**
>
> Метод має повертати **найбільш конкретний тип**, який він може гарантувати.
>
> * Якщо це список рядків, повертайте `List<String>`.
> * Якщо ви хочете заборонити модифікацію, повертайте `Collections.unmodifiableList(...)` або імутабельний `List` ( `List.of`).
>
> Це перекладає відповідальність за типобезпечність на API, а не на клієнта, що робить API значно якіснішим та зручнішим.

#### **📌 Що перевіряють:**

*Розуміння принципів проектування API з фокусом на досвіді клієнта (API usability).*

* * *

### **13 ❓ Питання:**

Поясніть, що таке `Type Argument Inference`. Як це працює з `Collections.emptyList()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Type Argument Inference` (виведення типу-аргумента)** — це механізм компілятора, який дозволяє йому **автоматично визначити** тип-параметр для дженерик-методу з контексту, без його явного вказування.
>
> **Приклад з `Collections.emptyList()`:**
>
> Сигнатура цього методу: `public static <T> List<T> emptyList()`.
>
> Коли ви пишете:
> `List<String> myList = Collections.emptyList();`
>
> Відбувається наступне:
>
> 1. Компілятор бачить, що ви присвоюєте результат змінній типу `List<String>`.
> 2. На основі цього **цільового типу (target type)** він **виводить**, що тип `T` у цьому виклику має бути `String`.
> 3. Він "вважає", що ви викликали `Collections.<String>emptyList()`.
>
> Це працює, навіть якщо метод не має аргументів, з яких можна було б вивести тип. Компілятор аналізує, куди присвоюється результат.
>
> Цей механізм був значно покращений в Java 8, що зробило роботу з дженерик-методами набагато зручнішою.

#### **📌 Що перевіряють:**

*Знання про механізми "type inference" та "target typing" в Java.*

* * *

### **14 ❓ Питання:**

Припустимо, ваш метод повинен приймати тільки `List<Integer>`, `List<Double>` та `List<Long>`. Як ви можете обмежити тип, не використовуючи `if/instanceof`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> На жаль, стандартні дженеріки Java **не дозволяють** обмежити тип набором конкретних класів. `extends` та `super` працюють з ієрархією успадкування, а не з переліком.
>
> `public <T extends Integer | Double | Long> void method(...) // Такого синтаксису не існує`
>
> **Як це можна реалізувати "обхідними" шляхами:**
>
> 1. **Перевантаження методу (найпростіший підхід):**
>     Створити три окремі перевантажені версії методу.
>
>     ```java
>     public void process(List<Integer> list) { ... }
>     public void process(List<Double> list) { ... }
>     public void process(List<Long> list) { ... }
>     ```
>
> 2. **Підхід з "маркером" (більш складний):**
>     * Створити власний маркерний інтерфейс `AllowedType`.
>     * Змусити класи-обгортки "реалізовувати" його, що неможливо для стандартних класів. Можна було б створити свої обгортки.
>
> 3. **Перевірка під час виконання (не дуже елегантно):**
>
>     ```java
>     public <T extends Number> void process(List<T> list) {
>         Class<?> type = list.get(0).getClass();
>         if (type != Integer.class && type != Double.class && ...) {
>             throw new IllegalArgumentException("Непідтримуваний тип");
>         }
>         // ...
>     }
>     ```
>
> **Висновок:** Найпростішим і найчистішим рішенням у цьому випадку є **перевантаження методів**. Система типів дженеріків не підтримує такі "або-або" обмеження.

#### **📌 Що перевіряють:**

*Розуміння обмежень системи типів Java. Здатність пропонувати альтернативні рішення (як перевантаження), коли пряме рішення через дженеріки неможливе.*

* * *

### **15 ❓ Питання:**

Чому цей код небезпечний і як wildcard міг би запобігти помилці?
`public void addPositiveNumbers(List<Number> list, List<Integer> numbers)`

> #### **💬 Відповідь:**
>
> Питання з підступом. Ця сигнатура сама по собі **не є небезпечною**, а є просто **негнучкою**. Код всередині, якщо він просто додає елементи з `numbers` в `list`, буде безпечним.
>
> **Але давайте припустимо, що всередині відбувається щось більш складне**, і розглянемо, як wildcards могли б покращити ситуацію.
>
> Припустимо, ми пишемо так:
>
> `public void process(List<Number> numbers)`
> і викликаємо з `List<Integer>`: `process(myIntegerList)`. Це не скомпілюється.
>
> **Як wildcard запобігає помилкам:**
>
> Принцип PECS змушує нас замислитися про **намір**.
>
> * Якщо метод **тільки читає**, ми б написали `process(List<? extends Number> numbers)`. Це б дозволило передати `List<Integer>` і було б безпечно, бо `add` заборонено.
> * Якщо метод **тільки пише**, ми б написали `process(List<? super Integer> numbers)`. Це дозволило б передати `List<Number>`, куди можна безпечно додати `Integer`.
>
> Повертаючись до початкового питання `addPositiveNumbers(List<Number> list, List<Integer> numbers)`:
>
> Ця сигнатура безпечна, але обмежує клієнта. Якби ми написали її згідно з PECS, вона була б **і безпечною, і гнучкою**:
>
> `addPositiveNumbers(List<? super Number> dest, List<? extends Integer> src)`
>
> Це дозволило б, наприклад, додавати позитивні `MySpecialPositiveInteger extends Integer` у `List<Object>`.

#### **📌 Що перевіряють:**

*Здатність аналізувати API, бачити його обмеження і пропонувати покращення на основі принципів, як-от PECS.*
