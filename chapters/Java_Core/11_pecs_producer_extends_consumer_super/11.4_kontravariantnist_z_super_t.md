---
layout: default
title: "Контраваріантність з ? super T"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 4
---

# Контраваріантність з ? super T

* короткий опис: `? super T` – це форма `lower-bounded wildcard` (`контраваріантність`), що обмежує тип параметра типом `T` або будь-яким його надтипом. Дозволяє безпечно додавати значення типу `T` або його підтипів до колекції.

* поняття: Контраваріантність, `? super T`.

* приклади: `List<? super Integer>` (можна додавати `Integer` або його підтипи), але зчитувати елементи можна лише як `Object`.

* ключові моменти: Правило "тільки запис" (Consumer Super). Компілятор дозволяє додавання, але обмежує читання, оскільки точний тип елемента невідомий.

* що важливо знати на співбесіді: Коли використовувати `? super`, що можна додавати, що не можна читати, принцип “контраваріантність — тільки запис”.

### **✅ Офіційне визначення:**

Контраваріантність з **`? super T`** — це форма узагальнення (generic wildcard), яка обмежує тип параметра типом **`T`** або **будь-яким його надтипом**. Це дозволяє **безпечно додавати** значення типу **`T`** або його підтипів до колекції.

### **🧠 Простими словами:**

Це як сказати: "Я не знаю, хто саме — може **`T`**, може його прадід **`Object`**, але я впевнений, що можу покласти в це місце об'єкт типу **`T`**."

---

## **Основне**

📊 **Синтаксис і приклади:**

* **`List<? super Integer>`** — список, який може бути **`List<Integer>, List<Number>`** або **`List<Object>`**
* **`Consumer<? super Apple>`** — споживач, що може спожити яблуко або будь-який тип, вищий у ієрархії

📦 **Правила використання:**

* ✅ Можна **додавати** елементи типу **`T`** або його підтипів
* ❌ Не можна **безпечно зчитувати**, крім як **`Object`** (найбезпечніший супертип)
* ✅ Добре підходить для **API, які приймають/записують дані**

---

## **Пояснення на пальцях (💡)**

Уяви:

* Ти кур’єр 📦, що приносить **Integer’и**
* У тебе є коробка з позначкою **`List<? super Integer>`** — ти не знаєш, чи це **`List<Integer>, List<Number>`** чи **`List<Object>`**
* Але ти впевнений, що **можеш покласти в неї Integer**
* Проте, ти **не знаєш, що вже там є**, і не можеш читати безпечно — раптом там **`Object`**, і ти не знатимеш, що з ним робити

Контраваріантність \= **тільки запис** (safe insert, unsafe extract)

---

## **Корисні приклади (🧪)**

```java
public void addNumbers(List<? super Integer> list) {
    list.add(42); // ✅ Додаємо Integer
    list.add(new Integer(100)); // ✅ Можна додати Integer або підтип (якщо був)
    // Integer i = list.get(0); // ❌ Compile error
    Object o = list.get(0); // ✅ Але можна прочитати як Object
}
```

Виклик методу:

```java
List<Number> numbers = new ArrayList<>();
addNumbers(numbers);

List<Object> objects = new ArrayList<>();
addNumbers(objects);
```

---

## **Порівняння (⚖️)**

| Тип виразу | Читання (get) | Запис (add) | Для чого |
| :---: | :---: | :---: | :---: |
| **`List<T>`** | ✅ (як **`T`**) | ✅ (як **`T`**) | Універсальне використання |
| **`List<? extends T>`** | ✅ (як **`T`**) | ❌ | Тільки читання |
| **`List<? super T>`** | ✅ (як **`Object`**) | ✅ (як **`T`**) | Тільки запис |

---

## **🧠 Мнемоніка для запам’ятовування:**

**PECS**:  
**C**onsumer -> **S**uper -> **Parent -> Safe to add**  
Якщо об'єкт **споживається** (щось у нього передається), використовуй **`super`**.

🧠 Think: **Super -> Only Supply**

---

## **🟩 Підсумок:**

* **`? super T`** — це **контраваріантна** підстановка: дозволяє використовувати будь-який **супертип T**
* Дозволяє **додавати** елементи типу **`T`** до колекції
* Читати значення можна лише як **`Object`**, оскільки точний тип невідомий
* Добре підходить для API, які **приймають дані**
* Важливо на співбесіді пам’ятати: **"super — тільки запис"**

---

**🔗 Додаткові джерела:**

* [**Oracle — Generics: Wildcards**](https://docs.oracle.com/javase/tutorial/java/generics/wildcards.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке контраваріантність у дженеріках Java і як її досягти?

> #### **💬 Відповідь:**
>
> **Контраваріантність (Contravariance)** — це властивість системи типів, де відношення успадкування є "зворотним". Якщо `A` є підтипом `B`, то `Generic<B>` вважається підтипом `Generic<A>`.
>
> **Як досягти для дженеріків:**
> Контраваріантність досягається за допомогою **маски з нижньою межею (lower-bounded wildcard)** — **`? super T`**.
>
> Ця конструкція означає "будь-який невідомий тип, що є **супертипом (предком) `T`** або самим `T`".
>
> **Приклад:**
> `Integer` є підтипом `Number`.
> Згідно з контраваріантністю, `List<Number>` є підтипом `List<? super Integer>`.
>
> ```java
> List<Number> numbers = new ArrayList<>();
> // Присвоєння валідне, тому що Number є супертипом Integer
> List<? super Integer> integerConsumer = numbers;
> ```
>
> Це дозволяє писати гнучкі методи, які можуть приймати колекції більш загальних типів для запису в них більш конкретних даних.

#### **📝 Аналогія:**

`List<? super Apple>` — це як "кошик, куди можна класти яблука". Це може бути кошик спеціально для яблук, або кошик для будь-яких фруктів (`Fruit` є супертипом `Apple`), або навіть кошик для будь-якої їжі. В будь-який з них ви можете безпечно покласти яблуко.

#### **📌 Що перевіряють:**

*Розуміння того, що `? super T` "перевертає" відношення успадкування і використовується для створення гнучких "споживачів" даних.*

* * *

### **2 ❓ Питання:**

Які операції дозволені, а які заборонені для колекції типу `List<? super Integer>`?

> #### **💬 Відповідь:**
>
> | Операція | Чи дозволена? | Пояснення |
> |---|---|---|
> | **Запис (`add`)** | **Так, дозволено** (для `Integer` та його нащадків). | Ви можете безпечно додати `Integer` (або об'єкт його підкласу, якби такий був) у колекцію. Компілятор знає, що список може приймати `Integer` або його предків (`Number`, `Object`). В будь-якому з цих випадків `Integer` є сумісним. `list.add(10)` — валідно. |
> | **Читання (`get`)** | **Ні, заборонено** (крім як `Object`). | Ви **не можете** безпечно прочитати елемент як `Integer`. Коли ви викликаєте `get()`, ви не знаєте, який саме тип зберігається в списку. Це може бути `List<Number>` і містити `Double`, або `List<Object>` і містити `String`. Єдине, що гарантується — що отриманий об'єкт є `Object`. |
>
> **Коротко:** `super`-колекція є "споживачем" (consumer) або, фактично, **колекцією "тільки для запису"** (якщо говорити про тип `T`).

#### **📌 Що перевіряють:**

*Чітке розуміння головного правила `super`: записувати можна, безпечно читати — ні (тільки як `Object`).*

* * *

### **3 ❓ Питання:**

Поясніть, чому такий код валідний:
`List<? super Integer> list = new ArrayList<Number>();`
`list.add(123);`

> #### **💬 Відповідь:**
>
> Цей код валідний, оскільки він повністю відповідає правилам контраваріантності.
>
> **Покрокове пояснення:**
>
> 1. **`List<? super Integer> list = new ArrayList<Number>();`**
>     * `List<? super Integer>` означає "список, типізований `Integer` або будь-яким його супертипом".
>     * `Number` є супертипом для `Integer`.
>     * Тому `List<Number>` є підтипом `List<? super Integer>`, і присвоєння є **валідим**.
>
> 2. **`list.add(123);`**
>     * Ми намагаємося додати `Integer` у список, який ми знаємо, є "споживачем" `Integer`-ів.
>     * З точки зору компілятора, змінна `list` може посилатися на `List<Integer>`, `List<Number>` або `List<Object>`.
>     * **В усі ці списки можна безпечно додати `Integer`**.
>         * У `List<Integer>` — очевидно.
>         * У `List<Number>` — можна, бо `Integer` є `Number`.
>         * У `List<Object>` — можна, бо `Integer` є `Object`.
>     * Оскільки ця операція є безпечною для **всіх можливих** типів, які задовольняють `? super Integer`, компілятор **дозволяє** її.

#### **📌 Що перевіряють:**

*Здатність застосувати правила контраваріантності до конкретного прикладу коду і логічно обґрунтувати, чому він працює.*

* * *

### **4 ❓ Питання:**

Як правило PECS ("Producer Extends, Consumer Super") стосується контраваріантності?

> #### **💬 Відповідь:**
>
> Контраваріантність (`? super T`) відповідає за другу частину цього правила — **Consumer Super**.
>
> **Що це означає:**
>
> * Якщо у вашому методі структура даних (колекція, функціональний інтерфейс) є **споживачем (consumer)**, тобто ваш метод **тільки записує** дані в неї, ви повинні оголосити її параметр з `? super T`.
>
> **Приклад:**
> Метод, що додає кілька `Integer`-ів у список. Список тут є споживачем.
>
> **Погана сигнатура (негнучка):**
> `public void addNumbers(List<Integer> list) { ... }` // Прийме тільки `List<Integer>`
>
> **Хороша сигнатура (гнучка, з PECS):**
> `public void addNumbers(List<? super Integer> list) { ... }` // Прийме `List<Integer>`, `List<Number>`, `List<Object>`
>
> Це робить ваш API значно гнучкішим, дозволяючи клієнту передавати більш загальні колекції для заповнення.

#### **📌 Що перевіряють:**

*Розуміння практичного застосування контраваріантності для створення гнучких API-споживачів даних, згідно з правилом PECS.*

* * *

### **5 ❓ Питання:**

У чому різниця між сигнатурами методів `void add(List<T> list)` та `void add(List<? super T> list)`? Коли яку використовувати? ⭐️

> #### **💬 Відповідь:**
>
> * **`void add(List<T> list)`:**
>   * **Значення:** Цей метод приймає список, елементи якого мають **точно тип `T`**. Ви не можете передати `List` з супертипом `T`.
>   * **Можливості всередині:** Всередині методу ви можете як **читати** елементи як `T`, так і **записувати** елементи типу `T`.
>
> * **`void add(List<? super T> list)`:**
>   * **Значення:** Цей метод може приймати список, елементи якого мають тип **`T` або будь-який його предок**.
>   * **Можливості всередині:** Ви можете **тільки записувати** елементи типу `T`, але **не можете безпечно читати** їх (тільки як `Object`).
>
> **Коли яку використовувати:**
>
> * Використовуйте **`List<T>`**, якщо ваш метод одночасно і **читає, і записує** в колекцію. Це найтиповіший випадок для внутрішньої логіки.
> * Використовуйте **`List<? super T>`** для **публічних API**, які виступають в ролі **"споживачів"**. Це робить ваш API максимально гнучким для клієнтів. Наприклад, якщо ваш метод лише додає `String`-и в список, зробіть його таким, щоб він приймав `List<? super String>`, і тоді клієнт зможе передати туди `List<Object>`.

#### **📌 Що перевіряють:**

*Здатність вибрати правильну сигнатуру залежно від того, які операції (читання, запис або обидві) виконуються над колекцією.*

* * *

### **6 ❓ Питання:**

Чому з `List<? super Integer>` можна безпечно читати елементи тільки як `Object`? ⭐️

> #### **💬 Відповідь:**
>
> Тому що компілятор **не знає точного типу** елементів у списку. Він знає лише, що це **супертип** `Integer`.
>
> Розглянемо `List<? super Integer> list`. Ця змінна може посилатися на:
>
> * `List<Integer>`
> * `List<Number>`
> * `List<Object>`
>
> Уявімо, що ми намагаємося прочитати елемент: `? item = list.get(0);`
>
> * Якщо `list` є `List<Integer>`, то `item` — `Integer`.
> * Якщо `list` є `List<Number>`, то `item` — `Number` (і може бути, наприклад, `Double`).
> * Якщо `list` є `List<Object>`, то `item` — `Object` (і може бути `String`).
>
> Оскільки ми не знаємо, який з цих випадків є істинним, компілятор може гарантувати лише те, що є спільним для всіх них. **Єдиний тип, до якого можна безпечно привести будь-який елемент з будь-якого з цих списків — це `Object`**.
>
> Будь-яка спроба прочитати як `Integer` або `Number` може призвести до `ClassCastException`, тому компілятор забороняє це.

#### **📌 Що перевіряють:**

*Логічне обґрунтування того, чому читання з контраваріантної колекції обмежене типом `Object`.*

* * *

### **7 ❓ Питання:**

Наведіть приклад з JDK, де `super`-wildcard активно використовується в сигнатурі методу. ⭐️

> #### **💬 Відповідь:**
>
> Класичний приклад — метод `sort` в інтерфейсі `List` (з Java 8) або `Collections.sort`.
>
> **`void sort(Comparator<? super E> c)`**
>
> **Пояснення:**
>
> 1. `E` — це тип елементів самого списку (наприклад, `String`).
> 2. Метод приймає `Comparator`, який є **споживачем (consumer)**. Його метод `compare` "споживає" два об'єкти для порівняння.
> 3. Згідно з PECS, для споживачів використовується **`super`**.
>
> **Що це дає на практиці?**
>
> Це дозволяє вам сортувати список дочірніх об'єктів, використовуючи компаратор для батьківського класу.
>
> ```java
> class Person { /* ... */ }
> class Student extends Person { /* ... */ }
>
> List<Student> students = new ArrayList<>();
> // Створюємо компаратор для Person
> Comparator<Person> personComparator = Comparator.comparing(Person::getName);
>
> // Ми можемо сортувати List<Student>, використовуючи Comparator<Person>!
> students.sort(personComparator);
> ```
>
> Компаратор, який вміє порівнювати `Person`, тим більше вміє порівнювати і `Student`. `super`-wildcard робить такий код валідним і гнучким.

#### **📌 Що перевіряють:**

*Здатність впізнавати та пояснювати використання `super`-wildcards у важливих методах стандартної бібліотеки.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати `super` в обмеженнях для типів-параметрів (`<T super Integer>`)? ⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Ключове слово `super` використовується **тільки для обмеження wildcards (`?`)**.
>
> * `<T extends Number>` — **Валідно**. Це обмеження типу-параметра.
> * `List<? super Number>` — **Валідно**. Це обмеження wildcard.
> * `<T super Number>` — **НЕВАЛІДНИЙ СИНТАКСИС**.
>
> **Причина:** Обмеження для типів-параметрів `<T ...>` призначене для того, щоб гарантувати, що `T` має певні методи. Верхня межа (`extends`) дає таку гарантію (`T` матиме методи `Number`). Нижня межа (`super`) не дає такої інформації — ми знаємо, що `T` є предком `Number`, але не знаємо, яким саме (це може бути `Object`), тому не можемо гарантувати наявності якихось специфічних методів.

#### **📌 Що перевіряють:**

*Розуміння синтаксичної різниці між обмеженням типу-параметра та обмеженням wildcard. Це питання дозволяє відсіяти тих, хто плутає ці поняття.*

* * *

### **9 ❓ Питання:**

Створіть метод, який додає елемент `Number` у будь-який список, що може його прийняти. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Щоб список міг прийняти `Number`, він має бути типізований або самим `Number`, або будь-яким його **супертипом** (`Object`).
>
> Для цього ідеально підходить **`? super Number`**.
>
> ```java
> import java.util.ArrayList;
> import java.util.List;
>
> public class NumberUtils {
>
>     // Список є споживачем (consumer), тому `super`
>     public static void addNumber(List<? super Number> list, Number number) {
>         list.add(number);
>     }
>
>     public static void main(String[] args) {
>         List<Number> numberList = new ArrayList<>();
>         List<Object> objectList = new ArrayList<>();
>         List<Integer> integerList = new ArrayList<>();
>
>         addNumber(numberList, 10);      // OK
>         addNumber(numberList, 3.14);    // OK
>         addNumber(objectList, 20);      // OK
>
>         // addNumber(integerList, 30); // ПОМИЛКА КОМПІЛЯЦІЇ
>         // List<Integer> не є супертипом Number
>     }
> }
> ```
>
> Цей метод є гнучким, оскільки він дозволяє клієнту передати будь-яку колекцію, яка здатна зберігати `Number`.

#### **📌 Що перевіряють:**

*Практичні навички проектування гнучких API з використанням `super`-wildcards.*

* * *

### **10 ❓ Питання:**

Чому `Function<? super T, ? extends R>` є стандартним патерном для функцій-маперів (наприклад, у `Stream.map`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Сигнатура `Stream.map` виглядає так: `<R> Stream<R> map(Function<? super T, ? extends R> mapper)`. Вона є ідеальним прикладом застосування PECS для функціональних інтерфейсів.
>
> 1. **Вхідний параметр `Function<? super T, ...>`:**
>     * `Function` є **споживачем (Consumer)**. Вона "споживає" елемент типу `T` зі вхідного потоку.
>     * Використання `super` робить її контраваріантною по входу. Це дозволяє вам передати функцію, яка працює з `T` або будь-яким його **предком**. Наприклад, обробити `Stream<Integer>` за допомогою `Function<Number, ...>`, бо функція, що працює з `Number`, тим більше вміє працювати і з `Integer`.
>
> 2. **Вихідний параметр `Function<..., ? extends R>`:**
>     * `Function` є **виробником (Producer)** результату типу `R`.
>     * Використання `extends` робить її коваріантною по виходу. Це дозволяє функції повернути об'єкт типу `R` або будь-якого його **нащадка**. Наприклад, якщо `map` очікує `Stream<Number>`, функція може повернути `Integer`, і це буде валідно.
>
> Ця комбінація робить метод `map` **максимально гнучким**: він приймає "найбільш загальну можливу функцію" і дозволяє їй повертати "найбільш конкретний можливий результат".

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як PECS застосовується до функціональних інтерфейсів, що є ключем до розуміння дизайну `Stream API`.*

* * *

### **11 ❓ Питання:**

Розглянемо `Comparator.thenComparing(Comparator<? super T> other)`. Чому тут використовується `super`, а не `extends`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тут використовується `super` з тієї ж причини, що і в `List.sort`: `Comparator` є **споживачем (consumer)**.
>
> * `Comparator<? super T>` означає: "компаратор, який вміє порівнювати об'єкти типу `T` **або будь-якого його предка**".
>
> **Навіщо це потрібно?**
>
> Уявіть, що у вас є ланцюжок порівнянь:
> `Comparator<Student> c = Comparator.comparing(Student::getFaculty)`
> `.thenComparing(Student::getLastName);`
>
> Припустимо, ви хочете додати третє порівняння, але у вас є тільки `Comparator<Person>`.
> `Comparator<Person> personComparator = Comparator.comparing(Person::getAge);`
>
> `c = c.thenComparing(personComparator); // Цей код валідний!`
>
> Це працює, тому що `thenComparing` приймає `Comparator<? super Student>`, а `Comparator<Person>` задовольняє цю умову, бо `Person` є супертипом `Student`. Компаратор, що вміє порівнювати людей за віком, тим більше вміє порівнювати студентів за віком.
>
> Якби тут був `extends`, такий гнучкий ланцюжок був би неможливий.

#### **📌 Що перевіряють:**

*Здатність аналізувати API і застосовувати PECS до функціональних інтерфейсів.*

* * *

### **12 ❓ Питання:**

Що буде зі стиранням типів (Type Erasure) для `List<? super Integer>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для `extends`-wildcards тип стирається до його **верхньої межі**. А для `super`-wildcards — ні.
>
> Для `List<? super Integer>` тип **завжди стирається до `Object`**.
>
> **Чому?**
>
> * Компілятор не знає, який саме супертип використовується. Це може бути `Integer`, `Number`, `Serializable` або `Object`.
> * Найбільш "загальним" та безпечним типом, до якого можна стерти будь-який супертип, є **`Object`**.
>
> Тому, після стирання типів, і `List<?>`, і `List<? extends SomeClass>`, і `List<? super SomeClass>` — усі вони на рівні байт-коду виглядають як сирий `List`, але компілятор вставляє різні типи кастів, базуючись на межах.
>
> Для `? super Integer`, при читанні, компілятор дозволить привести елемент тільки до `Object`.

#### **📌 Що перевіряють:**

*Розуміння того, як стирання типів працює з `super`-wildcards.*

* * *

### **13 ❓ Питання:**

Чи може `? super T` використовуватися в оголошенні класу, як `class MyClass<? super String> { ... }`?

> #### **💬 Відповідь:**
>
> Ні, **не може**. Wildcard `?` (в будь-якій його формі) **не може** використовуватися для оголошення **типів-параметрів** класу або методу.
>
> Він може бути використаний тільки як **тип-аргумент** для вже існуючих дженерик-типів.
>
> * `private List<? super String> myList;` — **OK**.
> * `public void method(Consumer<? super String> consumer)` — **OK**.
> * `public class MyClass<? super String>` — **ПОМИЛКА КОМПІЛЯЦІЇ**.

#### **📌 Що перевіряють:**

*Знання синтаксичних правил та розмежування між оголошенням типів та їх використанням.*

* * *

### **14 ❓ Питання:**

Які є альтернативи `List<? super T>` для методів-споживачів, якщо ви хочете уникнути wildcards? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча `List<? super T>` є ідіоматичним рішенням, можна обійтися і без нього, хоча це зробить API менш гнучким.
>
> **Альтернатива: Дженерик-метод.**
>
> Ви можете створити дженерик-метод, який приймає два типи-параметри.
>
> **Замість цього (з wildcard):**
> `public static <T> void addAll(List<? super T> list, T... elements)`
>
> **Можна написати так (з дженерик-методом):**
>
> ```java
> public static <E, T extends E> void addAll(List<E> list, T... elements) {
>     for (T element : elements) {
>         list.add(element);
>     }
> }
> ```
>
> **Як це працює:**
>
> * `E` — тип елементів списку.
> * `T extends E` — тип елементів, які ми додаємо. Він має бути підтипом `E`.
>
> Це також дозволяє додати `Integer` (`T`) у `List<Number>` (`E`), але є значно менш читабельним і складнішим для розуміння.
>
> **Висновок:** `? super T` було введено саме для того, щоб спростити такі сценарії і зробити сигнатури методів більш зрозумілими.

#### **📌 Що перевіряють:**

*Здатність мислити про альтернативні способи реалізації та глибоко розуміти, як працює виведення типів у дженерик-методах.*

* * *

### **15 ❓ Питання:**

Чому `? super T` забороняє читати елементи як щось більш конкретне, ніж `Object`, але дозволяє викликати `toString()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> 1. **Заборона на читання:** Як ми вже обговорювали, ми не можемо безпечно прочитати елемент як `T`, бо колекція може бути типізована супертипом `T`. Наприклад, з `List<Object>` ми не можемо безпечно отримати `Integer`.
>
> 2. **Дозвіл на виклик `toString()`:**
>     Коли ви викликаєте `list.get(0).toString()`, відбувається наступне:
>     * `list.get(0)` повертає посилання, статичний тип якого — `Object`.
>     * У класу `Object` є публічний метод `toString()`.
>     * Тому компілятор дозволяє цей виклик.
>     * Під час виконання буде викликано **перевизначений** `toString()` реального об'єкта завдяки **поліморфізму**.
>
> Це правило стосується **будь-яких публічних методів класу `Object`**, таких як `hashCode()` та `equals()`. Ви можете їх викликати для елементів, отриманих з будь-якої колекції (`<?>`, `<? extends T>`, `<? super T>`), оскільки кожен об'єкт гарантовано має ці методи.

#### **📌 Що перевіряють:**

*Розуміння того, як поліморфізм та успадкування від `Object` взаємодіють з обмеженнями, які накладають дженеріки.*
