---
layout: default
title: "Практика: методи з wildcard-ами"
parent: "PECS (Producer Extends, Consumer Super)"
grand_parent: "Java Core"
nav_order: 6
---

# Практика: методи з wildcard-ами

* короткий опис: Застосування wildcard-ів (`?`, `? extends T`, `? super T`) у сигнатурах Java-методів для забезпечення гнучкості та типобезпеки. Дозволяє методам працювати з різними, але сумісними параметризованими типами, обмежуючи операції.

* поняття: Wildcard, `<?>`, `<? extends T>`, `<? super T>`.

* приклади: `void printList(List<?> list)` (тільки читання як `Object`), `double sum(List<? extends Number> list)` (читання чисел), `void addIntegers(List<? super Integer> list)` (запис цілих).

* ключові моменти: Вибір `wildcard` залежить від наміру методу (тільки читання, тільки запис, або універсальна обробка).

* що важливо знати на співбесіді: Порівняння `<?>`, `? extends T`, `? super T` у методах, типові завдання та помилки.

### **✅ Офіційне визначення:**

У Java-методах з дженеріками можна використовувати wildcard-и (**`?`**, **`? extends T, ? super T`**) для визначення меж допустимих типів параметрів. Це забезпечує гнучкість і безпечність типів.

### **🧠 Простими словами:**

Це способи сказати компілятору:

* «Будь-який тип, я не вдаюсь у подробиці» (**`<?>`**)
* «Щось, що є **`T`** або підтипом» (**`? extends T`**)
* «Щось, що є **`T`** або надтипом» (**`? super T`**)

---

## 📊 **Типи wildcard-ів у методах:**

| Вираз | Опис | Типовий сценарій |
| :---: | :---: | :---: |
| **`<?>`** | Будь-який тип | Загальна обробка без запису |
| **`? extends T`** | **`T`** або підтип | **Читання** (producer) |
| **`? super T`** | **`T`** або надтип | **Запис** (consumer) |

---

## **Пояснення на пальцях (💡)**

Уяви метод як віконце у світ типів:

* **`<?>`** — ти визираєш, але нічого не знаєш про тип.
* **`? extends Animal`** — ти точно знаєш, що це якийсь Animal: пес, кіт або тигр. Можна **дивитись**, не можна **годувати**.
* **`? super Dog`** — ти знаєш, що можеш дати пса, бо це або Dog, або Object. Можна **годувати**, але не знаєш, що там усередині.

---

## **Корисні приклади (🧪)**

```java
// 1. Загальний метод, читає все, але нічого не додає
public void printList(List<?> list) {
    for (Object obj : list) {
        System.out.println(obj);
    }
    // list.add("test"); ❌ Заборонено
}

// 2. Читає тільки числа
public double sum(List<? extends Number> list) {
    double total = 0;
    for (Number n : list) {
        total += n.doubleValue();
    }
    return total;
    // list.add(1); ❌ Заборонено
}

// 3. Метод додає тільки Integer або його підтип
public void addNumbers(List<? super Integer> list) {
    list.add(10);   // ✅ OK
    list.add(0);    // ✅ OK
    // Integer i = list.get(0); ❌ Тип занадто загальний
}
```

---

## **Порівняння (⚖️)**

| Метод | Призначення | Можна читати? | Можна додавати? |
| :---: | :---: | :---: | :---: |
| **`printList(List<?>)`** | Просто читає | ✅ Object | ❌ |
| **`sum(List<? extends Number>)`** | Читання чисел | ✅ Number | ❌ |
| **`addNumbers(List<? super Integer>)`** | Додавання чисел | ❌ (тільки Object) | ✅ Integer |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Wildcards \= читання або запис, але не обидва одразу\!**

* **`extends = export`** -> читаємо з колекції
* **`super = submit`** -> записуємо в колекцію
* **`<?> = whatever`** -> нічого не додаємо

---

## **🟩 Підсумок:**

* Wildcards (**`?`**, **`?`** `extends`, **`? super`**) — це ключ до безпечної та гнучкої роботи з дженеріками.
* Вибір залежить від **наміру** методу: читати (**`extends`**), писати (**`super`**), або просто обробляти (**`<?>`**).
* Поширена помилка: намагатись додати в **`?`** `extends` або прочитати з **`?`** `super`.
* Часто використовуються у бібліотеках, API, фреймворках — і це класика для технічної співбесіди.

---

**🔗 Додаткові джерела:**

* [**Java Generics: Wildcards — Oracle Docs**](https://docs.oracle.com/javase/tutorial/java/generics/wildcards.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яку проблему вирішують методи з wildcard-ами?

> #### **💬 Відповідь:**
>
> Методи з wildcard-ами вирішують проблему **відсутності гнучкості**, яка виникає через **інваріантність** дженеріків.
>
> **Проблема:**
>
> Через інваріантність, `List<String>` не є підтипом `List<Object>`. Це означає, що якщо у вас є метод `public void print(List<Object> list)`, ви не зможете передати в нього `List<String>`.
>
> **Рішення з wildcard-ами:**
>
> Wildcards дозволяють "пом'якшити" це правило і створити метод, який може приймати колекції з різними, але сумісними типами.
>
> `public void print(List<?> list)`
>
> Цей метод тепер може прийняти будь-який список: `List<String>`, `List<Integer>` тощо. Таким чином, wildcard-и використовуються для написання **універсальних та гнучких API**, які працюють з цілими ієрархіями типів.

#### **📌 Що перевіряють:**

*Розуміння того, що wildcard-и — це інструмент для досягнення гнучкості, втраченої через інваріантність дженеріків.*

* * *

### **2 ❓ Питання:**

Поясніть на прикладі, коли доцільно використовувати необмежений wildcard `<?>` у сигнатурі методу.

> #### **💬 Відповідь:**
>
> Необмежений wildcard `<?>` доцільно використовувати тоді, коли логіка методу **не залежить від конкретного типу** елементів у колекції, і метод працює з ними через API класу `Object`.
>
> **Типові сценарії:**
>
> * Отримання розміру колекції.
> * Виклик методів, доступних у `Object`, таких як `toString()`, `hashCode()`.
> * Очищення колекції (`clear()`).
>
> **Приклад: метод, що друкує будь-яку колекцію:**
>
> ```java
> public void printCollection(Collection<?> collection) {
>     if (collection == null) {
>         return;
>     }
>     System.out.println("Collection has " + collection.size() + " elements:");
>     for (Object item : collection) {
>         // Працюємо з елементом як з Object
>         System.out.println(item.toString());
>     }
> }
> ```
>
> Цей метод може прийняти `List<String>`, `Set<Integer>` або будь-яку іншу колекцію, оскільки `size()` і `toString()` є у всіх них.
>
> **Важливо:** В таку колекцію не можна нічого додавати (крім `null`), оскільки компілятор не знає, який тип є безпечним.

#### **📌 Що перевіряють:**

*Розуміння того, коли інформація про конкретний тип не важлива, і як `<?>` використовується для вираження цього наміру.*

* * *

### **3 ❓ Питання:**

Напишіть метод, який обчислює суму всіх елементів у списку чисел. Який wildcard слід використати? ⭐️

> #### **💬 Відповідь:**
>
> Для цієї задачі слід використовувати **`? extends Number`**.
>
> **Чому?**
>
> 1. Нам потрібно приймати списки будь-яких чисел: `List<Integer>`, `List<Double>`, `List<Float>` тощо. Всі ці типи є нащадками `Number`.
> 2. Нам потрібно **читати** дані зі списку, щоб отримати їхнє числове значення. Список тут є **виробником (producer)**.
> 3. Згідно з правилом **PECS**, для виробників використовується `extends`.
>
> **Приклад методу:**
>
> ```java
> import java.util.List;
>
> public class Calculator {
>
>     public static double sum(List<? extends Number> numbers) {
>         double total = 0.0;
>         for (Number num : numbers) {
>             // Ми можемо безпечно викликати методи класу Number
>             total += num.doubleValue();
>         }
>         return total;
>     }
> }
> ```
>
> Цей метод є гнучким і типобезпечним. Спроба додати в нього щось (`numbers.add(...)`) призведе до помилки компіляції.

#### **📌 Що перевіряють:**

*Практичне вміння застосовувати правило "Producer Extends" для написання корисного утилітарного методу.*

* * *

### **4 ❓ Питання:**

Напишіть метод, який додає кілька цілих чисел (`Integer`) у список. Який wildcard слід використати? ⭐️

> #### **💬 Відповідь:**
>
> Для цієї задачі слід використовувати **`? super Integer`**.
>
> **Чому?**
>
> 1. Нам потрібно мати можливість додавати `Integer` у список. Це означає, що список має бути типізований або самим `Integer`, або його **предком** (`Number`, `Object`).
> 2. Список тут є **споживачем (consumer)**. Ми в нього **записуємо** дані.
> 3. Згідно з правилом **PECS**, для споживачів використовується `super`.
>
> **Приклад методу:**
>
> ```java
> import java.util.List;
>
> public class NumberUtils {
>
>     public static void addIntegers(List<? super Integer> list) {
>         list.add(1);
>         list.add(2);
>         list.add(3);
>     }
> }
>
> // Використання:
> List<Integer> integerList = new ArrayList<>();
> List<Number> numberList = new ArrayList<>();
> List<Object> objectList = new ArrayList<>();
>
> addIntegers(integerList); // OK
> addIntegers(numberList); // OK
> addIntegers(objectList); // OK
> ```

#### **📌 Що перевіряють:**

*Практичне вміння застосовувати правило "Consumer Super" для проектування гнучкого API.*

* * *

### **5 ❓ Питання:**

Чому цей метод не скомпілюється, і як його виправити?
`public <T> void copyFirst(List<T> src, List<T> dest) { dest.add(src.get(0)); }`
коли його викликають так:
`List<Integer> ints = ...; List<Number> nums = ...; copyFirst(ints, nums);`

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється при виклику**, а не при оголошенні.
>
> **Проблема:**
> Метод `copyFirst(List<T> src, List<T> dest)` вимагає, щоб обидва списки були **одного й того ж типу `T`**.
>
> Коли ви викликаєте `copyFirst(ints, nums)`, компілятор намагається **вивести (infer)** тип `T`.
>
> * З першого аргументу (`ints`, тип `List<Integer>`) він робить висновок, що `T` — це `Integer`.
> * З другого аргументу (`nums`, тип `List<Number>`) він робить висновок, що `T` — це `Number`.
>
> Компілятор не може одночасно задовольнити обидві умови, тому виникає **помилка виведення типів**.
>
> **Як це виправити:**
>
> Потрібно використати **PECS**, щоб зробити сигнатуру гнучкою.
>
> ```java
> // src - producer (extends), dest - consumer (super)
> public <T> void copyFirst(List<? extends T> src, List<? super T> dest) {
>     dest.add(src.get(0));
> }
> ```
>
> Тепер при виклику `copyFirst(ints, nums)` компілятор виведе `T` як `Integer` (з `ints`). Сигнатура `dest` стане `List<? super Integer>`, і `List<Number>` задовольняє цю умову.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працює виведення типів та як обмеження точним типом (`List<T>`) робить API менш гнучким.*

* * *

### **6 ❓ Питання:**

Розглянемо `Collections.fill(List<? super T> list, T obj)`. Поясніть її сигнатуру з точки зору wildcards. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Сигнатура:** `public static <T> void fill(List<? super T> list, T obj)`
>
> **Розбір:**
>
> 1. **`<T>`**: `fill` — це дженерик-метод, що працює з якимось типом `T`.
>
> 2. **`List<? super T> list`**:
>     * Список `list` тут є **споживачем (consumer)**. Ми в нього **записуємо** дані (заповнюємо його).
>     * Використання `super` дозволяє цьому методу бути максимально гнучким. Наприклад, ви можете заповнити `List<Number>` значеннями типу `Integer`.
>
> 3. **`T obj`**:
>     * Це об'єкт, яким ми заповнюємо список. Його тип `T` пов'язаний з типом списку.
>
> **Приклад використання гнучкості:**
>
> ```java
> List<Number> numbers = new ArrayList<>(List.of(0.0, 0.0, 0.0));
> Integer fillValue = 1;
>
> // Ми можемо заповнити List<Number> значенням Integer завдяки <? super Integer>
> Collections.fill(numbers, fillValue); // Т буде виведено як Number, <? super Number> включає Number.
> // Або якщо точніше, Т = Integer, і List<Number> підходить під List<? super Integer>
>
> System.out.println(numbers); // [1, 1, 1] - відбудеться автобоксинг
> ```
>
> Це демонструє, як `super` дозволяє працювати з ієрархіями типів "знизу вгору".

#### **📌 Що перевіряють:**

*Здатність аналізувати сигнатури методів з JDK, застосовуючи до них правило PECS.*

* * *

### **7 ❓ Питання:**

Що краще використовувати в публічному API: `Set<?>` чи `Collection<?>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про вибір **найбільш загального типу**, який все ще задовольняє потреби методу.
>
> **Загальне правило:** Використовуйте **найбільш абстрактний тип**, який можливий.
>
> 1. Якщо вашому методу потрібно тільки **ітеруватися** по елементах, найкращий вибір — `Iterable<?>`.
>
> 2. Якщо вам потрібні методи, як-от `size()`, `isEmpty()`, `contains()`, але не потрібна семантика `Set` (унікальність) чи `List` (індексація), тоді **`Collection<?>` є кращим вибором**.
>
> 3. Використовуйте `Set<?>` тільки в тому випадку, якщо для вашої логіки **важливий контракт `Set`** — унікальність елементів.
>
> **Приклад:**
> Метод, що просто друкує колекцію.
>
> ```java
> // Добре, але можна краще
> public void print(Set<?> set) { ... }
>
> // Краще, бо більш гнучко (прийме і Set, і List)
> public void print(Collection<?> collection) { ... }
>
> // Ще краще, якщо потрібна тільки ітерація
> public void print(Iterable<?> iterable) { ... }
> ```
>
> Використання більш загального типу (`Collection` замість `Set`) робить ваш API більш гнучким для клієнтів.

#### **📌 Що перевіряють:**

*Розуміння принципів проектування API. Вміння вибирати правильний рівень абстракції для параметрів.*

* * *

### **8 ❓ Питання:**

Чому `? extends T` називають **коваріантним**, а `? super T` — **контраваріантним**? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ці терміни описують, як зберігається (або інвертується) відношення успадкування.
>
> * **Коваріантність (`extends`):**
>   * Напрямок успадкування **зберігається**.
>   * Якщо `Integer` є підтипом `Number`, то `List<Integer>` також є підтипом `List<? extends Number>`. Відношення `A -> B` перетворюється на `F<A> -> F<B>`. Напрямок "стрілки" той самий.
>
> * **Контраваріантність (`super`):**
>   * Напрямок успадкування **інвертується (змінюється на протилежний)**.
>   * Якщо `Integer` є підтипом `Number`, то `List<Number>` стає підтипом `List<? super Integer>`. Відношення `A -> B` перетворюється на `F<B> -> F<A>`. Напрямок "стрілки" змінюється.
>
> **Мнемоніка:**
>
> * `Co`-variant: відношення йде *разом* (co-).
> * `Contra`-variant: відношення йде *проти* (contra-).

#### **📌 Що перевіряють:**

*Знання теоретичних основ, що лежать за wildcards. Це питання з теорії систем типів.*

* * *

### **9 ❓ Питання:**

Припустимо, у вас є метод: `public void process(List<? super Number> list)`. Який з цих викликів `list.add(...)` буде валідним?

1. `list.add(new Integer(1));`
2. `list.add(new Double(1.0));`
3. `list.add(new Object());`
4. `list.add(null);`

> #### **💬 Відповідь:**
>
> Розберемо кожен виклик:
>
> 1. `list.add(new Integer(1));` — **ВАЛІДНО.**
>     * `Integer` є підтипом `Number`, тому його можна безпечно додати в колекцію, що приймає `Number` або його предків.
>
> 2. `list.add(new Double(1.0));` — **ВАЛІДНО.**
>     * `Double` також є підтипом `Number`.
>
> 3. `list.add(new Object());` — **НЕВАЛІДНО.**
>     * Ми не можемо додати `Object`, тому що `list` може бути, наприклад, `List<Number>`. Додавати `Object` в `List<Number>` не можна. Можна додавати тільки об'єкти, які є типом `Number` або його **нащадками**.
>
> 4. `list.add(null);` — **ВАЛІДНО.**
>     * `null` можна додати в будь-яку колекцію об'єктів.
>
> **Правило:** в `List<? super T>` можна додавати об'єкти типу **`T` та його підтипів**.

#### **📌 Що перевіряють:**

*Уважність до деталей та чітке розуміння правил додавання для `super`-wildcard.*

* * *

### **10 ❓ Питання:**

Чому wildcards зазвичай не використовуються для типів, що повертаються з методів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використовувати wildcards у типах, що повертаються, зазвичай є **поганою практикою**, оскільки це **створює проблеми для клієнтського коду**.
>
> Розглянемо метод `public List<?> getItems()`.
>
> * Клієнт, який викликає цей метод, отримує `List<?>`.
> * Він **не може додавати** елементи в цей список.
> * Він **не може навіть безпечно прочитати** елементи, окрім як `Object`.
>
> ```java
> List<?> items = getItems();
> items.add("a"); // Помилка
> String s = items.get(0); // Помилка, повертається Object
> ```
>
> Такий API є дуже незручним для клієнта. Він змушує його робити небезпечні приведення типів, що нівелює переваги дженеріків.
>
> **Краще** повернути конкретний параметризований тип (наприклад, `List<String>`) або, якщо вміст не має змінюватися, — `Collection<String>`, або імутабельну версію.
>
> **Виняток:** `return Collections.emptyList();` є винятком. Цей метод повертає `List<T>`, але всередині повертається один статичний `List<?>` з безпечним кастом, оскільки список порожній.

#### **📌 Що перевіряють:**

*Розуміння того, як проектувати API, зручні для використання. "Не змушуй свого клієнта страждати".*

* * *

### **11 ❓ Питання:**

Якщо у вас є `List<List<? extends Number>>`, що це означає? Чи можете ви додати `List<Integer>` до нього? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Що це означає:**
>
> Це "список, кожен елемент якого є іншим списком, що містить щось, що є нащадком `Number`".
>
> * Зовнішній список — це звичайний `List`, параметризований типом `List<? extends Number>`.
> * Кожен внутрішній список може бути `List<Integer>`, `List<Double>`, `List<BigDecimal>` тощо.
>
> **Чи можна додати `List<Integer>` до нього?**
>
> **Так, можна.**
>
> ```java
> List<List<? extends Number>> listOfLists = new ArrayList<>();
> List<Integer> integerList = List.of(1, 2, 3);
>
> listOfLists.add(integerList); // Валідно!
> ```
>
> Це працює, тому що `List<Integer>` **є підтипом** `List<? extends Number>` завдяки коваріантності. Тому додавання `List<Integer>` у `List`, який очікує `List<? extends Number>`, є типобезпечним.

#### **📌 Що перевіряють:**

*Здатність читати та розуміти вкладені дженерики та те, як варіантність застосовується на кожному рівні.*

* * *

### **12 ❓ Питання:**

Що не так з цим методом, і як wildcard допоможе це виправити?
`public <T extends Comparable<T>> T max(List<T> list)`

> #### **💬 Відповідь:**
>
> Проблема цього методу в тому, що його обмеження **надто суворе** і негнучке при роботі з ієрархіями успадкування.
>
> **Проблема:**
>
> Розглянемо ієрархію `class Student extends Person`, де `Person implements Comparable<Person>`.
> `Student` тепер є порівнюваним, але він **не реалізує `Comparable<Student>`**. Він реалізує `Comparable<Person>`.
> Тому ви **не зможете** викликати `max(listOfStudents)`.
>
> **Як wildcard допоможе це виправити:**
>
> Потрібно використовувати **рекурсивне обмеження з `super`**, щоб "пом'якшити" вимогу.
>
> **Правильна сигнатура:**
>
> `public <T extends Comparable<? super T>> T max(List<T> list)`
>
> **Що це означає:** " `T` має бути типом, який можна порівнювати з **самим собою АБО з будь-яким своїм предком**".
>
> Тепер `Student` задовольняє цю умову, тому що `Student` можна порівняти з `Person`, а `Person` є супертипом для `Student`.
> Це робить метод значно більш гнучким.

#### **📌 Що перевіряють:**

*Розуміння `F-bounded polymorphism` та ролі `super`-wildcard у вирішенні проблем з успадкуванням для `Comparable` та інших подібних інтерфейсів.*

* * *

### **13 ❓ Питання:**

Припустимо, у вас є `List<String>`. Який з цих методів ви можете викликати:

1. `void method1(List<?> list)`
2. `void method2(List<Object> list)`
3. `void method3(List<? extends CharSequence> list)`
4. `void method4(List<? super String> list)`

> #### **💬 Відповідь:**
>
> * **1. `method1(List<?> list)` — МОЖНА.**
>     `List<String>` є підтипом `List<?>`.
> * **2. `method2(List<Object> list)` — НЕ МОЖНА.**
>     Дженеріки інваріантні. `List<String>` не є підтипом `List<Object>`.
> * **3. `method3(List<? extends CharSequence> list)` — МОЖНА.**
>     `String` реалізує інтерфейс `CharSequence`, тому він є його підтипом. Завдяки коваріантності (`extends`), `List<String>` є підтипом `List<? extends CharSequence>`.
> * **4. `method4(List<? super String> list)` — МОЖНА.**
>     Завдяки контраваріантності (`super`), цей метод може приймати список, типізований `String` або будь-яким його предком. `String` задовольняє цю умову (є рівним `String`).

#### **📌 Що перевіряють:**

*Практичне вміння застосовувати правила варіантності для визначення сумісності типів.*

* * *

### **14 ❓ Питання:**

Які ключові відмінності між `Т[]` (дженерик-масив, якби він був можливий) та `List<? extends T>`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це порівняння двох підходів до коваріантності.
>
> * **`T[]` (якби `new T[]` було валідно, але для існуючих масивів):**
>   * **Типобезпечність:** **Небезпечний**. Перевірка типів відбувається **під час виконання**. Може кинути `ArrayStoreException`.
>   * **Операції:** **Дозволяє і читання, і запис**. Саме запис є джерелом небезпеки.
>     `Object[] arr = new String[1]; arr[0] = 1; // ArrayStoreException в runtime`
>
> * **`List<? extends T>`:**
>   * **Типобезпечність:** **Безпечний**. Перевірка типів відбувається **під час компіляції**.
>   * **Операції:** Дозволяє **тільки читання**. Операції запису (`add`) заборонені компілятором, що усуває ризик `ClassCastException`.
>     `List<? extends Number> list = new ArrayList<Integer>(); // list.add(3.14) - помилка компіляції`
>
> **Висновок:** `List<? extends T>` є **безпечною реалізацією коваріантності**, яка досягається шляхом накладення обмежень на операції. Масиви надають "небезпечну" коваріантність, яка покладається на перевірки в рантаймі.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як дженеріки з `extends`-wildcard вирішують фундаментальну проблему небезпечної коваріантності масивів.*

* * *

### **15 ❓ Питання:**

Чи можна написати метод, що приймає `List<List<?>>`, і щось корисне з ним зробити? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна. `List<List<?>>` означає "список, елементами якого є інші списки невідомих типів". Хоча ми не знаємо типів внутрішніх списків, ми все ще можемо виконувати операції, що не залежать від типу.
>
> **Приклад корисного методу:** Об'єднати всі елементи з усіх внутрішніх списків в один `List<Object>`.
>
> ```java
> public static List<Object> flatten(List<List<?>> listOfLists) {
>     List<Object> result = new ArrayList<>();
>     // Ітеруємо по зовнішньому списку. Кожен елемент `list` - це List<?>
>     for (List<?> list : listOfLists) {
>         // Ітеруємо по внутрішньому списку. Кожен елемент `item` - це Object
>         for (Object item : list) {
>             result.add(item);
>         }
>         // або просто: result.addAll(list);
>     }
>     return result;
> }
>
> public static void main(String[] args) {
>     List<String> strings = List.of("a", "b");
>     List<Integer> integers = List.of(1, 2);
>     List<List<?>> data = List.of(strings, integers);
>
>     List<Object> flattened = flatten(data);
>     System.out.println(flattened); // Виведе: [a, b, 1, 2]
> }
> ```
>
> Цей метод є гнучким, оскільки приймає набір гетерогенних колекцій, і корисним, оскільки він виконує значущу операцію — їх об'єднання.

#### **📌 Що перевіряють:**

*Здатність працювати зі складними, вкладеними дженерик-типами та застосовувати знання про wildcards для реалізації практичних завдань.*
