---
layout: default
title: "Метод main()"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 13
---

# Метод main()

* короткий опис: Метод `main()` – це стартова точка будь-якої Java-програми, яку JVM шукає і викликає при запуску. Його сигнатура є фіксованою: `public static void main(String[] args)`.

* поняття: `main()`, `public`, `static`, `void`, `String[] args`.

* приклади: Запуск програми з аргументами командного рядка (`java MyApp arg1 arg2`).

* ключові моменти: Фіксована сигнатура, `static` дозволяє викликати без об'єкта, `args` для передачі аргументів.

* що важливо знати на співбесіді: Чому `public static void`, що таке `String[] args`.

### **✅ Офіційне визначення:**

Метод **`main()`** — це стартова точка будь-якої Java-програми. JVM (Java Virtual Machine) шукає цей метод і викликає його при запуску програми:

```java
public static void main(String[] args)
```

### **🧠 Простими словами:**

**`main()`** — це як кнопка "Старт" на твоєму улюбленому пульті. Без неї твоя Java-програма — просто набір класів, але не запускається. Вона — «вхідні двері» у світ твоєї логіки

---

## **Основне**

| Частина | Значення |
| :---: | :---: |
| **`public`** | Видимий і доступний JVM (ззовні класу) |
| **`static`** | Не треба створювати обʼєкт класу |
| **`void`** | Не повертає жодного значення |
| **`main`** | Стандартна назва методу, яку шукає JVM |
| **`String[] args`** | Масив рядкових аргументів командного рядка |

---

## **Пояснення на пальцях (💡)**

* **Чому `public`?**  
  JVM не твоє приватне друге «я» — вона стороння. Щоб подзвонити, треба відкрити двері.

* **Чому `static`?**  
  Уяви, що ти запускаєш фільм, але ще немає каси з квитками (обʼєктів). JVM заходить напряму — без чекання черги.

* **Чому `void`?**  
  Цей метод — не калькулятор. Він запускає код, а не повертає «результат».

* **Що таке `String[] args`?**  
  Це «пошта» для твоєї програми — можна передати командні параметри, наприклад:

```java
java MyApp hello 123
 -- args[0] = "hello", args[1] = "123".
```

---

## **Корисні приклади (🧪)**

### **🔹 Стандартний main:**

```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Привіт, Java!");
    }
}
```

### **🔹 Обробка аргументів:**

```java
public class ArgsExample {
    public static void main(String[] args) {
        for (int i = 0; i < args.length; i++) {
            System.out.println("Аргумент " + i + ": " + args[i]);
        }
    }
}
```

Запуск у консолі:

```java
java ArgsExample cat dog fish
```

Вивід:

```java
Аргумент 0: cat  
Аргумент 1: dog  
Аргумент 2: fish
```

---

## **Порівняння (⚖️)**

| Сигнатура | Працює? | Коментар |
| :---: | :---: | :---: |
| **`public static void main(String[] args)`** | ✅ | Стандарт |
| **`static public void main(String[] args)`** | ✅ | Порядок не має значення |
| **`public static void main(String args[])`** | ✅ | \[\] після імені теж варіант |
| **`void main(String[] args)`** | ❌ | Без **`public`** `static` — ні |
| **`public void main(String[] args)`** | ❌ | Без **`static`** — ні |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **M** — Момент запуску
* **public** — Видимий для JVM
* **static** — Без обʼєкта
* **void** — Не повертає значення
* **String\[\] args** — Аргументи командного рядка

---

## **🟩 Підсумок:**

Метод **`main()`** — це стандартна точка входу в Java-додаток. Він має бути **`public static void main(String[] args)`** для коректного запуску JVM. Цей метод дозволяє стартувати програму і приймати зовнішні параметри через **`args`**.

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Tutorials: The main() Method**](https://docs.oracle.com/javase/tutorial/getStarted/application/index.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке метод `main()` в Java і яка його основна функція?

> #### **💬 Відповідь:**
>
> Метод **`main()`** — це спеціальний метод, який є **точкою входу** (entry point) для будь-якої Java-програми, що запускається з консолі або операційної системи.
>
> **Основна функція:**
> Коли ви даєте команду на запуск Java-додатка (наприклад, `java MyProgram`), Java Virtual Machine (JVM) шукає у вказаному класі метод із сигнатурою `public static void main(String[] args)`. Знайшовши його, JVM викликає цей метод, і з нього починається виконання всього коду вашої програми.
>
> Простими словами, це "вхідні двері", через які JVM заходить у ваш код, щоб почати його виконувати. Без цього методу програма не знатиме, з чого почати свою роботу.

#### **📝 Аналогія:**

Метод `main()` — це як інструкція "Почати з кроку 1" у посібнику. Без цієї інструкції ви просто маєте набір правил, але не знаєте, з якого почати.

#### **📌 Що перевіряють:**

*Базове розуміння того, що таке точка входу в програму і яка роль методу `main()` у цьому процесі.*

* * *

### **2 ❓ Питання:**

Поясніть кожне ключове слово в сигнатурі `public static void main(String[] args)`.

> #### **💬 Відповідь:**
>
> Кожне слово в цій сигнатурі є критично важливим для правильного запуску програми:
>
> * **`public` (модифікатор доступу):**
>   * Робить метод видимим і доступним для виклику з будь-якого місця. **JVM** не є частиною вашого коду чи пакету, тому їй потрібен публічний доступ, щоб викликати цей метод ззовні.
>
> * **`static` (модифікатор):**
>   * Дозволяє JVM викликати метод `main` **без створення екземпляра (об'єкта) класу**. Метод належить самому класу. Якби `main` не був статичним, JVM довелося б створювати об'єкт, що створило б проблему "курки та яйця": як створити об'єкт, якщо виконання коду ще не почалося?
>
> * **`void` (тип повернення):**
>   * Означає, що метод `main` **не повертає жодного значення** після свого завершення. Програма сигналізує про успішне або помилкове завершення іншими способами (наприклад, кодами виходу `System.exit()`).
>
> * **`main` (назва методу):**
>   * Це стандартна, узгоджена назва, яку JVM **шукає за замовчуванням** як точку входу.
>
> * **`String[] args` (параметр):**
>   * Це масив рядків, який дозволяє передавати **аргументи командного рядка** у вашу програму. Кожне слово, написане в консолі після `java ClassName`, потрапляє в цей масив як окремий рядок.

#### **📝 Коментар:**

Запам'ятовування цих п'яти компонентів та їх призначення є фундаментальним для будь-якого Java-розробника.

#### **📌 Що перевіряють:**

*Здатність розкласти по полицях одну з найважливіших конструкцій Java та пояснити призначення кожної її частини.*

* * *

### **3 ❓ Питання:**

Чи можна змінити сигнатуру методу `main()`? Наприклад, змінити назву параметра або порядок модифікаторів?

> #### **💬 Відповідь:**
>
> Деякі зміни дозволені, а деякі — ні.
>
> **Що МОЖНА змінити:**
>
> * **Порядок модифікаторів `public` та `static`:** `static public void main...` є абсолютно валідним, оскільки їх порядок не має значення.
> * **Назва параметра масиву:** Замість `args` можна використовувати будь-яку іншу валідну назву, наприклад, `params`, `arguments`: `main(String[] params)`.
> * **Синтаксис оголошення масиву:** Квадратні дужки можна поставити після назви параметра: `main(String args[])`.
> * **Змінна кількість аргументів (Varargs):** Замість масиву можна використати varargs: `main(String... args)`.
>
> **Що НЕ МОЖНА змінити (це призведе до того, що JVM не знайде метод):**
>
> * **Видалити `public` або `static`:** Метод перестане бути точкою входу.
> * **Змінити тип повернення з `void`:** Наприклад, `int main(...)` не буде точкою входу.
> * **Змінити назву методу `main`:** JVM шукає саме `main`.
> * **Змінити тип параметра:** Він повинен бути `String[]` або `String...`. Наприклад, `main(int[] args)` не працюватиме.

#### **Приклади валідних сигнатур:**

```java
public static void main(String[] arguments) { /* ... */ }
static public void main(String... args) { /* ... */ }
```

#### **📌 Що перевіряють:**

*Увагу до деталей специфікації мови. Кандидат повинен знати, що є обов'язковим контрактом, а що — лише синтаксичною варіацією.*

* * *

### **4 ❓ Питання:**

Для чого потрібен параметр `String[] args`? Наведіть приклад використання.

> #### **💬 Відповідь:**
>
> Параметр `String[] args` призначений для передачі **аргументів командного рядка** у вашу Java-програму. Це дозволяє налаштовувати поведінку програми при її запуску без необхідності змінювати код.
>
> Коли ви запускаєте програму з консолі, все, що ви пишете після імені класу, розбивається по пробілах і передається в масив `args`.
>
> **Приклад використання:**
>
> Створимо програму, яка вітається з користувачем, ім'я якого передано як аргумент.
>
> ```java
> public class Greeter {
>     public static void main(String[] args) {
>         if (args.length > 0) {
>             String name = args[0];
>             System.out.println("Hello, " + name + "!");
>         } else {
>             System.out.println("Hello, World!");
>         }
>     }
> }
> ```
>
> **Як це запустити:**
>
> 1. Компілюємо: `javac Greeter.java`
> 2. Запускаємо з аргументом: `java Greeter John`
>
> **Вивід:**
>
> ```
> Hello, John!
> ```
>
> Якщо запустити без аргументів (`java Greeter`), вивід буде:
>
> ```
> Hello, World!
> ```
>
> Таким чином, `args` буде `"John"`. Якщо аргументів декілька, вони потраплять в `args`, `args` і так далі.

#### **📌 Що перевіряють:**

*Практичні навички роботи з консоллю та розуміння механізму передачі параметрів у програму.*

* * *

### **5 ❓ Питання:**

Чи може бути кілька методів `main()` в одному класі або в одній програмі?

> #### **💬 Відповідь:**
>
> Так, може, але з важливими нюансами.
>
> **1. Кілька методів `main()` в одному класі:**
>
> * Ви можете мати **кілька методів з назвою `main`** в одному класі, якщо вони мають **різні сигнатури** (тобто відбувається **перевантаження (overloading)**).
> * Однак **лише один з них**, який має канонічну сигнатуру `public static void main(String[] args)`, буде **точкою входу**, яку знайде JVM.
>
> ```java
> public class MultipleMains {
>     // ЦЕ ТОЧКА ВХОДУ
>     public static void main(String[] args) {
>         System.out.println("Головний main викликано");
>         main(10); // Виклик перевантаженого методу
>     }
>     // Це просто звичайний перевантажений метод
>     public static void main(int number) {
>         System.out.println("Перевантажений main з числом: " + number);
>     }
> }
> ```
>
> **2. Кілька методів `main()` в одній програмі (в різних класах):**
>
> * **Так, це абсолютно нормально.** Будь-який клас у вашому проекті може мати валідний `main()` метод.
> * При запуску програми ви **явно вказуєте**, `main` якого саме класу ви хочете виконати:
>     `java com.example.MyWebApp` (викликає `main` у класі `MyWebApp`)
>     `java com.example.utils.DbTool` (викликає `main` у класі `DbTool`)
>
> Це дуже зручно для створення невеликих утиліт або тестових запусків всередині великого проекту.

#### **📌 Що перевіряють:**

*Розуміння перевантаження методів та того, як JVM ідентифікує конкретну точку входу при запуску.*

* * *

### **6 ❓ Питання:**

Що буде, якщо метод `main()` викине виняток?

> #### **💬 Відповідь:**
>
> Якщо метод `main()` викидає неперехоплений виняток (`uncaught exception`), то:
>
> 1. **Потік `main` зупиняє свою роботу:** Виконання коду в методі `main` негайно припиняється.
> 2. **Програма аварійно завершується:** JVM завершує роботу всього додатка.
> 3. **Стек-трейс винятку виводиться в консоль:** Інформація про виняток, його тип, повідомлення та ланцюжок викликів (stack trace) буде надрукована в стандартний потік помилок (`System.err`), що зазвичай є консоллю.
>
> **Приклад:**
>
> ```java
> public class ExceptionInMain {
>     public static void main(String[] args) {
>         System.out.println("Початок");
>         int result = 10 / 0; // Це викине ArithmeticException
>         System.out.println("Кінець"); // Цей рядок ніколи не виконається
>     }
> }
> ```
>
> **Вивід у консоль:**
>
> ```
> Початок
> Exception in thread "main" java.lang.ArithmeticException: / by zero
>         at ExceptionInMain.main(ExceptionInMain.java:4)
> ```
>
> Для коректної обробки помилок такі потенційно небезпечні операції слід обгортати в блок `try-catch`.

#### **📌 Що перевіряють:**

*Знання механізму обробки винятків та життєвого циклу програми. Кандидат повинен розуміти, що неперехоплений виняток у `main` є фатальним для програми.*

* * *

### **7 ❓ Питання:**

Чи можна викликати метод `main()` з іншого методу?

> #### **💬 Відповідь:**
>
> **Так, звісно.** Метод `main()` є звичайним статичним методом, і його можна викликати з будь-якого іншого методу (статичного чи ні), як і будь-який інший статичний метод.
>
> **Приклад:**
>
> ```java
> public class CallingMain {
>
>     public static void main(String[] args) {
>         System.out.println("Викликано з JVM з аргументами: " + java.util.Arrays.toString(args));
>     }
>
>     public void someOtherMethod() {
>         System.out.println("Збираюся викликати main вручну...");
>         // Створюємо власний масив аргументів
>         String[] customArgs = {"arg1", "arg2"};
>         // Викликаємо main як звичайний статичний метод
>         main(customArgs);
>         System.out.println("main було викликано вручну.");
>     }
> }
> ```
>
> При запуску цього коду (наприклад, через тест, що викликає `someOtherMethod`), JVM не братиме участі у другому виклику `main`. Це буде просто звичайний виклик методу всередині вашої програми.
>
> Однак, на практиці це робиться дуже рідко, оскільки зазвичай логіку виділяють в окремі методи, а `main` залишають лише як точку входу.

#### **📌 Що перевіряють:**

*Розуміння того, що `main` не є "магічним" з точки зору викликів всередині програми. Це звичайний `static` метод, що підкоряється тим самим правилам.*

* * *

### **8 ❓ Питання:**

Поясніть, що таке коди виходу (exit codes) і як можна повернути код виходу з `main()` методу? ⭐️

> #### **💬 Відповідь:**
>
> **Код виходу** — це ціле число, яке процес (у нашому випадку, Java-програма) повертає операційній системі після свого завершення. Це стандартний спосіб для програми повідомити, чи вона завершилася успішно.
>
> * **`0`** — зазвичай означає **успішне завершення**.
> * **Будь-яке інше ненульове число** (зазвичай `1` або більше) — означає, що сталася **помилка**. Конкретне значення може вказувати на тип помилки.
>
> Оскільки метод `main` має тип повернення `void`, він не може повернути код виходу безпосередньо через `return`.
>
> **Для цього використовується статичний метод `System.exit(int status)`:**
>
> ```java
> public class ExitCodeExample {
>     public static void main(String[] args) {
>         if (args.length == 0) {
>             System.err.println("Помилка: потрібно передати ім'я файлу.");
>             System.exit(1); // Завершення з кодом помилки
>         }
>
>         // ... логіка програми ...
>
>         System.out.println("Програма успішно завершена.");
>         System.exit(0); // Завершення з кодом успіху (хоча це необов'язково)
>     }
> }
> ```
>
> Коли викликається `System.exit()`, JVM негайно припиняє роботу і повертає вказаний код ОС. Це корисно для написання скриптів та автоматизації, де наступний крок залежить від успішності попереднього.

#### **📌 Що перевіряють:**

*Знання механізмів взаємодії Java-програми з операційною системою та інструментів для керування життєвим циклом програми.*

* * *

### **9 ❓ Питання:**

Чи виконується щось перед методом `main()`? ⭐️

> #### **💬 Відповідь:**
>
> **Так, виконується.** Перед тим, як JVM викличе метод `main()`, відбувається процес **завантаження та ініціалізації класу**.
>
> Під час цього процесу виконуються:
>
> 1. **Ініціалізація статичних полів:** Всі `static` поля класу отримують свої значення.
> 2. **Виконання статичних блоків ініціалізації:** Всі блоки `static { ... }` виконуються в тому порядку, в якому вони оголошені в класі.
>
> **Приклад:**
>
> ```java
> public class BeforeMain {
>     private static String message = initializeMessage();
>
>     static {
>         System.out.println("Виконується статичний блок.");
>     }
>
>     public static String initializeMessage() {
>         System.out.println("Ініціалізується статичне поле.");
>         return "Hello from static field!";
>     }
>
>     public static void main(String[] args) {
>         System.out.println("Виконується main. Повідомлення: " + message);
>     }
> }
> ```
>
> **Порядок виводу:**
>
> 1. `Ініціалізується статичне поле.`
> 2. `Виконується статичний блок.`
> 3. `Виконується main. Повідомлення: Hello from static field!`
>
> Це демонструє, що статична частина класу готується до роботи ще до того, як буде зроблено перший крок у методі `main`.

#### **📌 Що перевіряють:**

*Розуміння життєвого циклу класу в JVM, зокрема процесу завантаження та ініціалізації.*

* * *

### **10 ❓ Питання:**

Чи можна запустити Java-програму без методу `main()`? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від того, що мається на увазі під "програмою".
>
> * **Консольний додаток:** **Ні.** Для стандартного запуску консольної програми (`java ClassName`) метод `main()` є обов'язковою точкою входу.
>
> * **Веб-додаток (наприклад, сервлет):** **Так.** У веб-додатках, які розгортаються на сервері додатків (наприклад, Tomcat), точкою входу є не `main()`, а сам **контейнер сервлетів**. Він завантажує ваші класи, створює екземпляри сервлетів та викликає їхні методи життєвого циклу (`init()`, `service()`, `doGet()`, `doPost()`) у відповідь на HTTP-запити. `main()` метод у такому проекті зазвичай відсутній або використовується лише для вбудованого запуску (наприклад, у Spring Boot).
>
> * **Аплети (застарілі):** **Так.** Раніше аплети в браузері також не використовували `main`, а мали свої методи життєвого циклу (`init()`, `start()`).
>
> * **Юніт-тести (JUnit):** **Так.** Фреймворк для тестування (наприклад, JUnit) сам знаходить і викликає методи, позначені анотаціями `@Test`. Йому не потрібен `main`.
>
> **Висновок:** `main()` є точкою входу для **стандартних standalone-додатків**, але не для всіх типів Java-програм.

#### **📌 Що перевіряють:**

*Широту кругозору кандидата. Він повинен знати, що існують різні середовища виконання Java, кожне зі своїм життєвим циклом.*

* * *

### **11 ❓ Питання:**

Як можна зупинити виконання програми з методу `main`, не чекаючи його природного завершення? Чи виконається блок `finally` в цьому випадку? ⭐️

> #### **💬 Відповідь:**
>
> Можна примусово завершити роботу всієї JVM за допомогою методу **`System.exit(int status)`**.
>
> Цей метод негайно припиняє роботу програми та повертає вказаний `status` код операційній системі.
>
> **Чи виконається блок `finally`?**
>
> **Ні, не виконається.** Виклик `System.exit()` — це радикальний спосіб завершення. Він зупиняє всю віртуальну машину, і жоден подальший код Java, включаючи блоки `finally`, не виконується.
>
> **Приклад:**
>
> ```java
> public class FinallyAndExit {
>     public static void main(String[] args) {
>         try {
>             System.out.println("Всередині try");
>             System.exit(0);
>         } finally {
>             // Цей код ніколи не виконається
>             System.out.println("Всередині finally");
>         }
>     }
> }
> ```
>
> **Вивід:**
>
> ```
> Всередині try
> ```
>
> Саме тому `System.exit()` слід використовувати з великою обережністю, оскільки він може перешкодити коректному звільненню ресурсів (закриттю файлів, з'єднань), логіка якого зазвичай розміщується у `finally`.

#### **📌 Що перевіряють:**

*Знання тонких моментів роботи з винятками та життєвим циклом. Кандидат повинен знати, в яких рідкісних випадках `finally` може не виконатися.*

* * *

### **12 ❓ Питання:**

Чому цей код, який раніше працював у Java 6, перестав бути валідним способом "запуску" програми без `main` в Java 7 та новіших версіях?

```java
public class NoMain {
    static {
        System.out.println("Hello from static block!");
        System.exit(0);
    }
}
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код демонструє цікаву зміну в поведінці JVM, яка відбулася в Java 7.
>
> * **У Java 6 та раніше:** JVM спочатку завантажувала клас, щоб перевірити, чи є в ньому метод `main()`. Під час цього завантаження виконувався статичний блок. Цей код успішно виводив "Hello from static block!" і завершував роботу через `System.exit(0)`, так і не дійшовши до помилки про відсутність `main`. Це було популярним трюком.
>
> * **Починаючи з Java 7:** Порядок перевірки було змінено. Тепер JVM **спочатку перевіряє наявність валідного методу `main()`** у класі і **тільки після цього** завантажує та ініціалізує його.
>
> Оскільки в класі `NoMain` немає методу `main()`, JVM в Java 7+ одразу викидає помилку, не доходячи до виконання статичного блоку.
>
> **Помилка в Java 7+:**
>
> ```
> Error: Main method not found in class NoMain, please define the main method as:
>    public static void main(String[] args)
> or a JavaFX application class must extend javafx.application.Application
> ```

#### **📌 Що перевіряють:**

*Знання історичних змін у поведінці JVM. Це питання відрізняє досвідченого розробника, який пам'ятає, як платформа еволюціонувала.*

* * *

### **13 ❓ Питання:**

Яким буде результат виконання цього коду? Що станеться, якщо змінити тип посилання?

```java
public class MainHiding {
    public static void main(String[] args) {
        Parent p = new Child();
        p.sayHello();
    }
}
class Parent {
    public static void sayHello() {
        System.out.println("Hello from Parent");
    }
}
class Child extends Parent {
    public static void sayHello() {
        System.out.println("Hello from Child");
    }
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код виведе на екран:
>
> ```
> Hello from Parent
> ```
>
> **Пояснення:**
>
> Це класичний приклад **приховування методів (method hiding)**, а не перевизначення.
>
> 1. Метод `sayHello()` є **статичним**.
> 2. Вибір статичного методу для виклику відбувається **на етапі компіляції** і базується на **типі посилання**, а не на фактичному типі об'єкта.
> 3. Змінна `p` має тип посилання **`Parent`**.
> 4. Тому компілятор "прив'язує" виклик `p.sayHello()` до статичного методу `sayHello()` з класу `Parent`. Той факт, що `p` насправді вказує на об'єкт `Child`, **ігнорується**.
>
> **Якщо змінити тип посилання:**
>
> Якби ми написали:
>
> ```java
> Child c = new Child();
> c.sayHello();
> ```
>
> Тоді вивід був би:
>
> ```
> Hello from Child
> ```
>
> Тому що тип посилання тепер `Child`, і компілятор прив'язує виклик до методу класу `Child`.
>
> Це демонструє, що поліморфізм часу виконання не застосовується до статичних методів.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між статичним та динамічним зв'язуванням, а також знання концепції приховування методів.*

* * *

### **14 ❓ Питання:**

Ви намагаєтесь запустити програму, але отримуєте помилку `Could not find or load main class`. Назвіть щонайменше три можливі причини цієї помилки. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Помилка `Error: Could not find or load main class com.example.MyClass` є однією з найпоширеніших і може бути спричинена кількома причинами:
>
> 1. **Неправильний classpath:** Це найчастіша причина. JVM не може знайти файл `MyClass.class`.
>     * **Причина:** Ви запускаєте програму з неправильної директорії або не вказали `classpath` (`-cp` або `-classpath`) для файлів `.jar` або директорій, де лежить ваш клас.
>     * **Рішення:** Переконайтеся, що ви запускаєте команду `java` з кореневої директорії пакетів (наприклад, з `src` або `target/classes`), і правильно використовуєте `-cp`. Наприклад: `java -cp . com.example.MyClass`.
>
> 2. **Неправильне повне ім'я класу:** Ви могли вказати неправильне ім'я класу, яке не збігається з шляхом до файлу.
>     * **Причина:** Якщо ваш клас знаходиться в пакеті `com.example`, ви повинні запускати його з повним кваліфікованим іменем `com.example.MyClass`, а не просто `MyClass`.
>     * **Рішення:** Перевірте назву пакета в `.java` файлі та використовуйте повне ім'я класу.
>
> 3. **Пошкоджений або відсутній `.class` файл:** Клас може бути відсутнім у вказаному місці, або файл `.class` може бути пошкодженим.
>     * **Причина:** Ви забули скомпілювати `.java` файл, видалили `.class` файл, або він був пошкоджений під час копіювання.
>     * **Рішення:** Перекомпілюйте ваш проект (`javac` або `mvn clean install`).
>
> **Додаткова причина:**
>
> * **Помилки в статичних блоках:** Якщо в статичному блоці класу виникає виняток, це призведе до `ExceptionInInitializerError`, що також може завадити завантаженню класу і, як наслідок, призвести до схожої помилки.

#### **📌 Що перевіряють:**

*Практичний досвід та навички у налагодженні проблем із запуском додатків. Це питання, яке перевіряє, чи кандидат стикався з реальними проблемами, а не просто знає теорію.*

* * *

### **15 ❓ Питання:**

Як Java-програми, написані на сучасних фреймворках типу Spring Boot, "ховають" метод `main()`, і як вони насправді запускаються? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони його не "ховають", а **генерують стандартний, але дуже простий `main()` метод**, переносячи всю складну логіку запуску всередину фреймворку.
>
> **Як це працює в Spring Boot:**
>
> Коли ви створюєте Spring Boot додаток, стартовий клас зазвичай виглядає так:
>
> ```java
> @SpringBootApplication
> public class MyApplication {
>     public static void main(String[] args) {
>         SpringApplication.run(MyApplication.class, args);
>     }
> }
> ```
>
> Тут відбувається наступне:
>
> 1. **`main()` залишається точкою входу:** JVM, як і раніше, викликає цей метод `main()`. Він є обов'язковим.
> 2. **Делегування запуску фреймворку:** Уся робота делегується одному статичному методу — `SpringApplication.run()`. Розробнику не потрібно писати складну логіку.
> 3. **Що робить `SpringApplication.run()`:**
>     * Створює **контекст додатка (ApplicationContext)**.
>     * Сканує `classpath` на наявність класів, позначених анотаціями Spring ( `@Component`, `@Service` тощо).
>     * Створює та налаштовує всі необхідні біни (об'єкти).
>     * Залежно від конфігурації та бібліотек у `classpath`, запускає **вбудований веб-сервер** (наприклад, Tomcat) і розгортає на ньому додаток.
>     * Обробляє аргументи командного рядка (`args`), щоб налаштувати програму.
>
> **Чому це так зручно:**
> Розробнику не потрібно вручну налаштовувати сервер, керувати контекстом, сканувати компоненти. Він просто пише один рядок у `main`, а фреймворк бере на себе всю складну і рутинну роботу по запуску та конфігурації програми. Це є ключовою перевагою Spring Boot та схожих фреймворків.

#### **📌 Що перевіряють:**

*Розуміння того, як сучасні фреймворки спрощують розробку. Кандидат повинен знати, що за "магією" стоїть добре спроектований код, який все одно покладається на стандартні механізми Java, як-от `main()`.*
