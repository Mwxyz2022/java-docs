---
layout: default
title: "Wrapper класи"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 5
---

# Wrapper класи

* короткий опис: Класи-обгортки (Integer, Double, Character тощо) для примітивних типів, що дозволяють використовувати їх як об'єкти, необхідні для колекцій та generic-типів.

* поняття: Wrapper, примітиви, об'єкт, `Integer`, `Double`, `Character`.

* приклади: `int` vs `Integer`, `char` vs `Character`.

* ключові моменти: Immutable, final, потрібні для роботи з колекціями, `==` порівнює посилання, `equals()` порівнює значення.

* що важливо знати на співбесіді: Навіщо потрібні, їхні властивості.

### **✅ Офіційне визначення:**

**Wrapper клас** — це клас із пакету **`java.lang`**, який "обгортає" значення примітивного типу у відповідний обʼєкт. Наприклад: **`int`** -> **`Integer`**, **`double`** -> **`Double`** і т.д.

Усі wrapper класи є **immutable** і **final**.

### **🧠 Простими словами:**

Уяви, що примітив — це просто число, а wrapper — це коробочка для цього числа. Якщо потрібно покласти це число у колекцію або передати як обʼєкт — запаковуємо його у коробочку.

---

## **📊 Таблиця відповідності:**

| Примітивний тип | Wrapper клас | Приклад |
| :---: | :---: | :---: |
| **`byte`** | `Byte` | `Byte b = 1;` |
| **`short`** | `Short` | `Short s = 2;` |
| **`int`** | `Integer` | `Integer i = 3;` |
| **`long`** | `Long` | `Long l = 4L;` |
| **`float`** | `Float` | `Float f = 5.5f;` |
| **`double`** | `Double` | `Double d = 6.6;` |
| **`char`** | `Character` | `Character c = 'A';` |
| **`boolean`** | `Boolean` | `Boolean b = true;` |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

Java-колекції (**`List`**, **`Map`**, тощо) не підтримують примітиви напряму. Тобто **`List<int>`** — **не працює**, але **`List<Integer>`** — **ок**. От і потрібні wrapper-и.

### **У чому особливість?**

* Wrapper-и — це **обʼєкти**, не значення.
* Вони мають **методи**: наприклад, **`Integer.parseInt(), Double.valueOf().`**
* Всі wrapper-и є **immutable** та **final**.

### **Типові граблі:**

* ❗ **\== vs equals()**: `==` порівнює посилання, **`equals()`** — значення.
* ❗ **Integer кешування**: значення між `-128` і **`127`** кешуються -> дивна поведінка при порівнянні через **`==.`**
* ❗ **NumberFormatException** при неправильному парсингу.

### **Коли застосовувати?**

* Робота з колекціями: **`List<Integer>, Set<Boolean>`**
* Generics: **`Optional<Double>, Map<Character, Boolean>`**
* Парсинг рядків: **`Integer.parseInt(userInput)`**

---

## **Корисні приклади (🧪)**

```java
// Автоматичне обгортання (Autoboxing)
List<Integer> numbers = new ArrayList<>();
numbers.add(10); // int -> Integer автоматично

// Розпакування (Unboxing)
int x = numbers.get(0); // Integer -> int

// Ручне створення
Integer i = Integer.valueOf("123");

// Парсинг
double d = Double.parseDouble("3.14");

```

---

## **Порівняння (⚖️)**

| Характеристика | Примітиви (`int`) | Wrapper класи (`Integer`) |
| :---: | :---: | :---: |
| **Зберігаються у Stack** | ✅ | ❌ (у Heap) |
| **Значення за замовчуванням** | **`0`** | **`null`** |
| **Підтримка у Generics** | ❌ | ✅ |
| **Швидкодія** | Вища | Нижча |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧠 **`int`** -> **`Integer`**, **`char`** -> **`Character`** (**єдина з великої літери\!**)

📦 **"Box the value — unwrap when needed\!"**

💡 **Integer кешується** у діапазоні **`[-128..127]`** -> **`==`** може працювати неочікувано

---

## **🟩 Підсумок:**

* **Wrapper класи** дозволяють використовувати **примітиви як обʼєкти**.
* Необхідні для **колекцій**, **generics** та **API**, які працюють тільки з обʼєктами.
* Кожен примітив має відповідний wrapper (int -> Integer, boolean -> Boolean і т.д.)
* **Памʼятай:** **`==`** не для обʼєктів. Використовуй **`equals()`**.
* Будь уважний із кешуванням **`Integer`**, особливо при тестуванні.

---

### **🔗 Додаткові джерела:**

* [**Java SE Docs: Wrapper Classes**](https://docs.oracle.com/javase/tutorial/java/data/wrapperclasses.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Для чого в Java потрібні класи-обгортки (wrapper classes), якщо є ефективніші примітивні типи?

> #### **💬 Відповідь:**
>
> Класи-обгортки необхідні для подолання обмежень примітивних типів і інтеграції їх у об'єктно-орієнтовану екосистему Java. Основні причини їх існування:
>
> * **Робота з колекціями та Generics:** Java Collections Framework (наприклад, `List`, `Set`, `Map`) та Generics працюють лише з об'єктами. Ви не можете створити `List<int>`, тому що `int` — це не об'єкт. Для цього потрібен клас-обгортка: `List<Integer>`.
>
> * **Можливість мати значення `null`:** Примітивні типи не можуть мати значення `null`. Наприклад, `int` за замовчуванням дорівнює `0`, а `boolean` — `false`. `Integer` може бути `null`, що корисно для представлення відсутності значення (наприклад, у базах даних чи API-відповідях).
>
> * **Наявність корисних методів:** Класи-обгортки надають безліч статичних методів для роботи з відповідними примітивами. Наприклад, `Integer.parseInt("123")` для перетворення рядка в число, або `Character.isDigit('5')` для перевірки, чи є символ цифрою.
>
> * **Використання в об'єктно-орієнтованих концепціях:** Деякі патерни проектування або API вимагають передачі об'єктів. Класи-обгортки дозволяють "запакувати" примітивне значення в об'єкт.

#### **📝 Коментар:**

Головна ідея: wrapper класи — це міст між світом примітивних значень та світом об'єктів у Java.

#### **📌 Що перевіряють:**

*Розуміння фундаментальних обмежень примітивних типів та усвідомлення того, які проблеми вирішують класи-обгортки.*

* * *

### **2 ❓ Питання:**

Що таке autoboxing та unboxing (автоупаковка та авторозпаковка)? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Це механізм автоматичного перетворення між примітивними типами та їхніми класами-обгортками, введений у Java 5 для спрощення коду.
>
> * **Autoboxing (Автоупаковка):** Це автоматичне перетворення примітивного типу в об'єкт відповідного класу-обгортки. Компілятор робить це за вас.
>
>     **Приклад:**
>
>     ```java
>     // Замість ручного створення: Integer i = Integer.valueOf(10);
>     Integer i = 10; // Компілятор перетворює int 10 на new Integer(10)
>     List<Integer> list = new ArrayList<>();
>     list.add(5); // Autoboxing: int 5 автоматично перетворюється на Integer
>     ```
>
> * **Unboxing (Авторозпаковка):** Це автоматичне перетворення об'єкта класу-обгортки назад у відповідний примітивний тип.
>
>     **Приклад:**
>
>     ```java
>     Integer number = Integer.valueOf(100);
>     // Замість ручного виклику: int value = number.intValue();
>     int value = number; // Unboxing: об'єкт Integer розпаковується в int
>
>     // Приклад в виразі:
>     Integer a = 5;
>     int b = a + 2; // 'a' автоматично розпаковується для виконання арифметичної операції
>     ```

#### **📝 Коментар:**

Хоча автоупаковка/розпаковка робить код чистішим, вона може приховувати створення об'єктів і призводити до несподіваних `NullPointerException`, якщо ви намагаєтеся розпакувати `null`-обгортку.

#### **📌 Що перевіряють:**

*Знання синтаксичного цукру Java та здатність пояснити, що компілятор робить "під капотом".*

* * *

### **3 ❓ Питання:**

У чому різниця між `int` та `Integer`?

> #### **💬 Відповідь:**
>
> Це одні з найпоширеніших представників примітиву та його обгортки. Різниця між ними фундаментальна:
>
| Характеристика | `int` (примітив) | `Integer` (об'єкт) |
| :---: | :---: | :---: |
| **Тип** | Примітивний тип даних. | Клас, тобто є об'єктом. |
| **Пам'ять** | Зберігається в **Stack** (якщо локальна змінна) або є частиною об'єкта в Heap. Дуже легко-ваговий (4 байти). | Посилання зберігається в **Stack**, сам об'єкт - в **Heap**. Має додаткові накладні витрати на заголовки об'єкта. |
| **Значення `null`** | Не може бути `null`. За замовчуванням — `0`. | Може мати значення `null`. |
| **Використання** | Для простих арифметичних операцій. | Для колекцій, generics, API, що працюють з об'єктами. |
| **Методи** | Не має методів. | Надає багато корисних утилітарних методів (наприклад, `parseInt`, `toString`, `compare`). |

#### **📝 Коментар:**

Коротко кажучи, `int` — це просте значення, а `Integer` — повноцінний об'єкт, який "обгортає" це значення.

#### **📌 Що перевіряють:**

*Чітке розуміння відмінностей між примітивом та об'єктом, включно з аспектами пам'яті, значень за замовчуванням та сценаріїв використання.*

* * *

### **4 ❓ Питання:**

Що виведе на екран наступний код і чому?

```java
Integer a = 100;
Integer b = 100;
Integer c = 1000;
Integer d = 1000;

System.out.println(a == b);
System.out.println(c == d);
```

> #### **💬 Відповідь:**
>
> Результат буде таким:
>
> ```
> true
> false
> ```
>
> **Пояснення:**
>
> Це пов'язано з **кешуванням об'єктів `Integer`**. Специфікація Java вимагає, щоб об'єкти `Integer`, створені через автоупаковку (або `Integer.valueOf()`) в діапазоні від **-128 до 127** (включно), завжди були одними й тими ж об'єктами з пулу (кешу).
>
> 1. `System.out.println(a == b);` -> `true`
>     * Значення `100` потрапляє у кешований діапазон.
>     * При створенні `a` та `b` JVM повертає посилання на **один і той самий об'єкт** з кешу.
>     * Оскільки `==` порівнює посилання на об'єкти, а посилання однакові, результат — `true`.
>
> 2. `System.out.println(c == d);` -> `false`
>     * Значення `1000` **не потрапляє** у кешований діапазон.
>     * Для `c` та `d` JVM створює **два різних, незалежних об'єкти** `Integer` у купі.
>     * Оскільки `c` та `d` посилаються на різні об'єкти, порівняння посилань за допомогою `==` дає `false`.

#### **📝 Коментар:**

Це класичне питання-пастка. **Мораль:** **ніколи** не порівнюйте об'єкти-обгортки за допомогою `==`. **Завжди** використовуйте метод `equals()`, щоб порівнювати їхні значення: `c.equals(d)` повернуло б `true`.

#### **📌 Що перевіряють:**

*Знання неочевидних, але важливих аспектів реалізації класів-обгорток, таких як кешування. Це відрізняє досвідченого розробника від початківця.*

* * *

### **5 ❓ Питання:**

Що таке "immutability" (незмінність) класів-обгорток і чому це важливо?

> #### **💬 Відповідь:**
>
> **Незмінність (immutability)** означає, що після створення об'єкта класу-обгортки його внутрішній стан (значення) неможливо змінити. Всі класи-обгортки в Java (`Integer`, `Double` і т.д.) є незмінними (`immutable`) і фінальними (`final`).
>
> Коли ви виконуєте операцію, яка, здавалося б, змінює значення обгортки (наприклад, `Integer i = 5; i++;`), насправді відбувається наступне:
>
> 1. Поточний об'єкт `Integer` розпаковується в `int`.
> 2. Виконується операція інкременту (`5 + 1 = 6`).
> 3. Створюється **новий** об'єкт `Integer` зі значенням `6`.
> 4. Посилання `i` починає вказувати на цей новий об'єкт.
>
> **Чому це важливо:**
>
> * **Потокобезпечність (Thread Safety):** Незмінні об'єкти можна безпечно передавати між потоками без ризику, що один потік змінить дані, які використовує інший.
> * **Кешування:** Незмінність дозволяє безпечно реалізувати кеш (як у випадку з `Integer` від -128 до 127). Якби об'єкти були змінними, один потік міг би змінити значення в кешованому об'єкті, що призвело б до хаосу в інших частинах програми.
> * **Надійність:** Використання незмінних об'єктів як ключів у `HashMap` або елементів у `HashSet` є безпечним, оскільки їхній хеш-код не зміниться після створення.

#### **📌 Що перевіряють:**

*Розуміння концепції незмінності, її переваг та того, як вона реалізована в класах-обгортках.*

* * *

### **6 ❓ Питання:**

Напишіть код для перетворення рядка "123.45" на `double` та `Double`. Що може піти не так?

> #### **💬 Відповідь:**
>
> Перетворення можна виконати за допомогою методів класу `Double`.
>
> ```java
> String str = "123.45";
> 
> // 1. Перетворення на примітив double
> double primitiveDouble = Double.parseDouble(str);
> 
> // 2. Перетворення на об'єкт Double
> Double wrapperDouble = Double.valueOf(str);
> ```
>
> **Що може піти не так:**
>
> Якщо рядок `str` має некоректний формат, обидва методи викинуть виняток **`NumberFormatException`**.
>
> **Приклади некоректних форматів:**
>
> * `"123,45"` (кома замість крапки як десятковий роздільник у стандартній локалі).
> * `"abc"` (містить нецифрові символи).
> * `""` (порожній рядок).
>
> Тому при роботі з введенням від користувача такий код завжди слід обгортати в блок `try-catch`.

#### **📝 Приклад:**

```java
public Double parseWithSafety(String input) {
    try {
        return Double.valueOf(input);
    } catch (NumberFormatException e) {
        System.err.println("Помилка формату числа: " + input);
        return null; // або повернути значення за замовчуванням
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички роботи з класами-обгортками та їхніми утилітарними методами, а також розуміння потенційних виняткових ситуацій.*

* * *

### **7 ❓ Питання:**

Які значення за замовчуванням у примітивів та їхніх класів-обгорток, якщо вони є полями класу?

> #### **💬 Відповідь:**
>
> Значення за замовчуванням у них різні, що є важливою відмінністю.
>
| Примітивний тип | Значення за замовчуванням | Клас-обгортка | Значення за замовчуванням |
| :---: | :---: | :---: | :---: |
| `int`, `short`, `byte`, `long` | `0` | `Integer` і т.д. | **`null`** |
| `float`, `double` | `0.0` | `Float`, `Double` | **`null`** |
| `char` | `'\u0000'` | `Character` | **`null`** |
| `boolean` | `false` | `Boolean` | **`null`** |

#### **📝 Приклад:**

```java
public class DefaultValues {
    int primitiveInt;
    Integer wrapperInt;

    public void printDefaults() {
        System.out.println("int: " + primitiveInt);     // Виведе: int: 0
        System.out.println("Integer: " + wrapperInt); // Виведе: Integer: null
    }
}
```

**Наслідок:** спроба викликати метод на неініціалізованій обгортці (`wrapperInt.intValue()`) призведе до `NullPointerException`.

#### **📌 Що перевіряють:**

*Уважність до деталей та розуміння наслідків того, що обгортки є об'єктами і за замовчуванням посилаються на `null`.*

* * *

### **8 ❓ Питання:**

Чому клас `Character` відрізняється від інших обгорток (`Integer`, `Long` і т.д.) у плані кешування? ⭐️

> #### **💬 Відповідь:**
>
> Хоча `Integer` та інші числові обгортки кешують об'єкти у фіксованому діапазоні (наприклад, -128..127), **`Character`** використовує інший підхід.
>
> `Character` також кешує об'єкти, але його кеш прив'язаний до **перших 128 ASCII-символів**, тобто символів з кодами від **0 до 127** (`'\u0000'` до `'\u007f'`).
>
> Це означає, що для будь-якого символу з цього діапазону (латинські літери, цифри, знаки пунктуації) метод `Character.valueOf(char)` завжди повертатиме один і той самий об'єкт із кешу.
>
> **Приклад:**
>
> ```java
> Character c1 = 'A'; // ASCII 65
> Character c2 = 'A';
> Character c3 = 'Ї'; // Код > 127
> Character c4 = 'Ї';
>
> System.out.println(c1 == c2); // true, бо 'A' входить до кешу
> System.out.println(c3 == c4); // false, бо 'Ї' не входить до кешу
> ```
>
> Інші обгортки, такі як `Boolean`, кешують обидва свої можливі об'єкти (`Boolean.TRUE` та `Boolean.FALSE`).

#### **📝 Коментар:**

Це логічно, оскільки символи з першої половини таблиці ASCII використовуються найчастіше, і кешування дозволяє економити пам'ять.

#### **📌 Що перевіряють:**

*Глибокі знання специфіки реалізації різних класів-обгорток, а не лише найвідомішого прикладу з `Integer`.*

* * *

### **9 ❓ Питання:**

Чи можна створити власний клас-обгортку? Наприклад, для `String`? Чи є в цьому сенс? ⭐️

> #### **💬 Відповідь:**
>
> Технічно **так, можна створити** клас, який "обгортає" інший об'єкт, наприклад `String`.
>
> ```java
> public final class MyStringWrapper {
>     private final String value;
>
>     public MyStringWrapper(String value) {
>         this.value = value;
>     }
>
>     public String getValue() {
>         return value;
>     }
>     // ... інші методи ...
> }
> ```
>
> **Чи є в цьому сенс?**
> У випадку зі `String` — **практично ніякого**, тому що `String` вже є незмінним об'єктом.
>
> Сенс у створенні власної обгортки може бути, якщо ви хочете:
>
> * **Додати додаткову логіку або поведінку:** Наприклад, обгортка для `String`, яка автоматично виконує валідацію або санітизацію даних при створенні.
> * **Створити типізовану обгортку для підвищення безпеки типів (Value Object pattern):** Замість того, щоб передавати всюди `String` для ID, email, phone number, можна створити класи `UserId`, `Email`, `PhoneNumber`, які обгортають `String`. Це запобігає помилкам, коли ви випадково передаєте ID замість email.
> * **Обгорнути змінюваний (mutable) об'єкт, щоб зробити його незмінним (immutable):** Наприклад, обгортка навколо `Date`, яка надає тільки методи для читання, але не для зміни.
>
> Але створення обгортки для примітиву (наприклад, власного `MyInteger` замість `java.lang.Integer`) є безглуздим, оскільки стандартні класи вже добре оптимізовані JVM.

#### **📌 Що перевіряють:**

*Розуміння патернів проектування та архітектурного мислення. Кандидат повинен розуміти не тільки "як", але і "навіщо" можна використовувати концепцію обгортання.*

* * *

### **10 ❓ Питання:**

Що станеться при спробі розпакувати (`unboxing`) `null`-посилання? Поясніть на прикладі. ⭐️

> #### **💬 Відповідь:**
>
> При спробі автоматичної розпаковки (`unboxing`) об'єкта-обгортки, який має значення `null`, буде викинуто виняток **`NullPointerException`**.
>
> Це відбувається тому, що компілятор "під капотом" перетворює unboxing на виклик відповідного методу, наприклад, `intValue()`. Виклик будь-якого методу на `null`-посиланні призводить до `NPE`.
>
> **Приклад:**
>
> ```java
> Integer wrapperInt = null;
> int primitiveInt = wrapperInt; // Тут виникне NullPointerException
> ```
>
> **Що насправді робить компілятор:**
>
> ```java
> Integer wrapperInt = null;
> int primitiveInt = wrapperInt.intValue(); // Виклик методу на null!
> ```
>
> Це одна з найпоширеніших "прихованих" помилок, пов'язаних з autoboxing/unboxing.
>
> Особливо небезпечним це стає в умовних операторах або циклах:
>
> ```java
> public void process(Boolean flag) {
>     if (flag) { // Unboxing: flag.booleanValue()
>         // ...
>     }
> }
> // Якщо викликати process(null), отримаємо NPE.
> ```

#### **📌 Що перевіряють:**

*Знання потенційних пасток та небезпек, пов'язаних з автоупаковкою. Досвідчений розробник завжди пам'ятає про можливість `null` і пише захищений код.*

* * *

### **11 ❓ Питання:**

Чому класи-обгортки оголошені як `final`? Які були б наслідки, якби вони не були `final`? ⭐️

> #### **💬 Відповідь:**
>
> Класи-обгортки оголошені як `final`, щоб **заборонити успадкування** від них. Це зроблено з кількох ключових причин, тісно пов'язаних з їхньою незмінністю (immutability) та безпекою.
>
> **Наслідки, якби вони не були `final`:**
>
> 1. **Порушення незмінності:** Можна було б створити дочірній клас, який би змінив поведінку та порушив контракт незмінності. Наприклад, перевизначити метод так, щоб він змінював внутрішній стан.
>
>     ```java
>     // Уявний код, якби Integer не був final
>     class MutableInteger extends Integer {
>         public void setValue(int newValue) { this.value = newValue; } // Порушення!
>     }
>     ```
>
> 2. **Проблеми з безпекою та кешуванням:** Якби хтось міг створити `MutableInteger`, то він міг би отримати кешований об'єкт `Integer` і змінити його значення, що поламало б роботу всіх частин програми, які використовують цей кешований об'єкт.
>
> 3. **Некоректна робота `equals()` і `hashCode()`:** Можна було б успадкувати, наприклад, від `Integer` і `String` (якби було множинне успадкування) і створити об'єкт, який є одночасно і числом, і рядком, що порушує контракт симетричності `equals()`.
>
> По суті, `final` є гарантією того, що поведінка та властивості класів-обгорток (незмінність, кешування) залишаться такими, якими їх задумали творці мови.

#### **📌 Що перевіряють:**

*Глибоке розуміння ключових принципів ООП (успадкування) та дизайну класів. Кандидат повинен розуміти, чому обмеження іноді є благом для стабільності та безпеки системи.*

* * *

### **12 ❓ Питання:**

Як впливає використання класів-обгорток замість примітивів на продуктивність? Коли варто свідомо йти на це, а коли ні? ⭐️

> #### **💬 Відповідь:**
>
> Використання обгорток замість примітивів має **негативний вплив на продуктивність**, особливо в обчислювально інтенсивних частинах коду.
>
> **Причини падіння продуктивності:**
>
> * **Витрати на пам'ять (Memory Overhead):** Об'єкт `Integer` займає значно більше пам'яті (16 байт на 64-бітній JVM), ніж `int` (4 байти), через заголовок об'єкта.
> * **Створення об'єктів:** Постійне створення об'єктів-обгорток (особливо в циклах поза кешованим діапазоном) створює навантаження на Garbage Collector.
> * **Непрямий доступ (Indirection):** Щоб отримати значення з обгортки, потрібно виконати додатковий крок — "перейти" за посиланням з Stack до Heap, що повільніше, ніж прямий доступ до значення в Stack.
>
> **Коли варто використовувати примітиви:**
>
> * У масивних обчисленнях, циклах, лічильниках.
> * Як поля в об'єктах, що масово створюються, для економії пам'яті (`int` замість `Integer`).
> * У коді, критичному до продуктивності (High-Performance Computing).
>
> **Коли варто (і необхідно) використовувати обгортки:**
>
> * Для зберігання даних у колекціях (`List<Integer>`).
> * Коли потрібне значення `null` для позначення відсутності даних.
> * В API, що вимагають об'єкти (наприклад, Generics).
>
> **Висновок:** Для бізнес-логіки та роботи з колекціями переваги обгорток переважують їхні недоліки в продуктивності. Але для "гарячих" шляхів виконання та математичних обчислень завжди слід надавати перевагу примітивам.

#### **📌 Що перевіряють:**

*Здатність кандидата мислити в термінах компромісів (trade-offs) між зручністю коду та продуктивністю.*

* * *

### **13 ❓ Питання:**

Подивіться на цей код. Чи буде він працювати? Якщо так, то що станеться? Якщо ні, то чому? ⭐️⭐️

```java
List<Integer> list = new ArrayList<>();
list.add(1);
list.add(2);
list.add(null);
list.add(4);

int sum = 0;
for (Integer i : list) {
    sum += i;
}
System.out.println(sum);
```

> #### **💬 Відповідь:**
>
> **Цей код не завершить роботу успішно.** Він скомпілюється, але під час виконання на третій ітерації циклу буде викинуто виняток **`NullPointerException`**.
>
> **Пояснення крок за кроком:**
>
> 1. Цикл `for-each` ітерується по списку `list`.
> 2. На кожній ітерації відбувається спроба виконати операцію `sum += i;`.
> 3. Ця операція розкривається компілятором у `sum = sum + i;`.
> 4. Щоб виконати арифметичне додавання, об'єкт `Integer i` повинен бути автоматично розпакований (`unboxing`) в примітив `int`. Фактично виконується `sum = sum + i.intValue();`.
>
> **Проблемна ітерація:**
>
> * Коли `i` дорівнює `1`, все добре.
> * Коли `i` дорівнює `2`, все добре.
> * Коли `i` дорівнює **`null`**, JVM намагається виконати `i.intValue()`, тобто `null.intValue()`.
> * Виклик будь-якого методу на `null`-посиланні призводить до `NullPointerException`, і програма аварійно завершує роботу.

#### **📝 Коментар:**

Це ідеальний приклад "тихої" міни, закладеної авторозпаковкою. Код виглядає безневинно, але наявність `null` у колекції може призвести до падіння. Перед виконанням таких операцій завжди слід додавати перевірку `if (i != null)`.

#### **📌 Що перевіряють:**

*Здатність "бачити" прихований unboxing, аналізувати покрокове виконання коду та передбачати runtime-помилки, особливо NPE.*

* * *

### **14 ❓ Питання:**

Як Java реалізує кешування `Integer`? Чи можна змінити діапазон кешування? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Кешування `Integer` реалізовано всередині самого класу `java.lang.Integer` за допомогою **внутрішнього статичного масиву**.
>
> Приблизна реалізація всередині JDK виглядає так:
>
> * Існує приватний статичний вкладений клас `IntegerCache`.
> * При першому зверненні до нього він створює та заповнює масив `Integer` об'єктами для всіх значень у діапазоні від `-128` до `127`.
> * Статичний метод `Integer.valueOf(int i)` перед створенням нового об'єкта перевіряє, чи входить `i` в діапазон кешу. Якщо так, він повертає об'єкт з цього масиву; якщо ні — створює новий об'єкт `new Integer(i)`.
>
> **Чи можна змінити діапазон?**
>
> **Так, верхню межу діапазону кешування можна змінити**, але тільки верхню і тільки при запуску JVM. Нижня межа `-128` є фіксованою.
>
> Це робиться за допомогою системної властивості JVM:
> `java -Djava.lang.Integer.IntegerCache.high=<new_upper_bound> MyProgram`
>
> Наприклад, `java -Djava.lang.Integer.IntegerCache.high=1000 MyProgram` змусить JVM кешувати `Integer` від -128 до 1000. Це може бути корисним у специфічних додатках, де часто використовуються числа з певного, вузького діапазону поза стандартним, щоб зменшити створення об'єктів.

#### **📝 Коментар:**

Можливість налаштовувати кеш — це потужний інструмент для тюнінгу, але його слід використовувати обережно і лише після профілювання, оскільки збільшення кешу потребує більше постійної пам'яті (у Old Generation Heap).

#### **📌 Що перевіряють:**

*Експертні знання про внутрішню реалізацію JDK та можливості тонкого налаштування JVM. Це питання для Senior-рівня.*

* * *

### **15 ❓ Питання:**

Яка основна відмінність між `Integer.parseInt()` та `Integer.valueOf()`? У яких ситуаціях варто використовувати кожен з них? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва методи перетворюють рядок на числове значення, їхні **типи повернення** та **семантика** є різними.
>
> * `public static int parseInt(String s)`:
>   * **Повертає:** Примітивний тип **`int`**.
>   * **Семантика:** "Дай мені просте числове значення з цього рядка".
>   * **Продуктивність:** Дуже висока, оскільки не створює жодних об'єктів.
>
> * `public static Integer valueOf(String s)`:
>   * **Повертає:** Об'єкт **`Integer`**.
>   * **Семантика:** "Дай мені об'єкт-обгортку, що представляє число з цього рядка".
>   * **Продуктивність:** Потенційно нижча, оскільки може створювати новий об'єкт `Integer`. Однак цей метод **використовує кеш**: якщо рядок представляє число в діапазоні від -128 до 127, він поверне об'єкт із кешу, уникаючи створення нового.
>
> **Коли який використовувати:**
>
> * Використовуйте **`parseInt()`**, коли вам потрібне **лише примітивне значення `int`** для обчислень. Це найефективніший варіант.
>
>     ```java
>     int age = Integer.parseInt(userInput);
>     if (age < 18) { ... }
>     ```
>
> * Використовуйте **`valueOf()`**, коли вам потрібен **об'єкт `Integer`**, наприклад, для додавання в колекцію, для присвоєння полю типу `Integer`, або коли ви хочете скористатися перевагами кешування.
>
>     ```java
>     List<Integer> numbers = new ArrayList<>();
>     numbers.add(Integer.valueOf(userInput));
>     ```
>
> **Коротка мнемоніка:** `parse` -> `primitive`, `valueOf` -> `object`.

#### **📌 Що перевіряють:**

*Увагу до деталей API стандартної бібліотеки. Кандидат повинен розуміти різницю між отриманням значення та отриманням об'єкта, а також усвідомлювати наслідки для продуктивності та використання пам'яті.*
