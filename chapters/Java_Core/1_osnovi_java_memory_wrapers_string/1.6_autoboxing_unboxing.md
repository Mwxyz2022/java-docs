---
layout: default
title: "Autoboxing / Unboxing"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 6
---

# Autoboxing / Unboxing

* короткий опис: Автоматичне перетворення примітивів у wrapper-об'єкти (Autoboxing) та навпаки (Unboxing), що спрощує код, але може призвести до `NullPointerException` або додаткових витрат ресурсів.

* поняття: Autoboxing, Unboxing.

* приклади: `List<Integer> list = new ArrayList<>(); list.add(5);` (autoboxing), `int x = list.get(0);` (unboxing).

* ключові моменти: Зручність у використанні, але можливі `NullPointerException` при розпакуванні `null` та додаткові витрати пам'яті/продуктивності.

* що важливо знати на співбесіді: Як працює, типові помилки.

### **✅ Офіційне визначення:**

**Autoboxing** — це автоматичне перетворення примітивного типу (**`int`**, **`double`**, тощо) у відповідний wrapper клас (**`Integer`**, **`Double`**, …) під час компіляції.

**Unboxing** — це автоматичне перетворення обʼєкта wrapper-класу назад у примітив.

### **🧠 Простими словами:**

Java сама "запаковує" число у коробочку (wrapper), коли це потрібно, і розпаковує назад, коли очікується примітив. Це виглядає зручно — але бувають **"сюрпризи"**.

---

## **📊 Види перетворень:**

| Операція | Приклад | Що відбувається |
| :---: | :---: | :---: |
| **Autoboxing** | **`Integer x = 5;`** | **`int -> Integer`** |
| **Unboxing** | **`int y = new Integer(5);`** | **`Integer -> int`** |
| **Колекції** | **`list.add(3);`** | **`int -> Integer`** (авто) |

🔍 **Java сама вирішує**, коли треба зробити обгортку або розпакувати, залежно від контексту.

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

Щоб можна було:

* працювати з примітивами у **колекціях**, **generics**, **Stream API**;
* уникнути ручного виклику **`valueOf()`** / **`xxxValue()`** у простих випадках.

### **У чому особливість?**

* Дуже зручно — **менше коду**, чистіше API.
* Але **обʼєкти створюються непомітно** -> це має ціну (памʼять, швидкодія).

### **Типові граблі:**

* ❗ **`==`** між wrapper-обʼєктами — порівнює **посилання**, а не значення.
* ❗ **`NullPointerException`**, коли **`Integer x = null; int y = x;`** — Java намагається розпакувати **`null`**.

### **Коли застосовувати?**

* Там, де API очікує **обʼєкти**, але зручно працювати **з примітивами**.
* Наприклад, **`List<Integer>`** або **`Map<Character, Boolean>.`**

---

## **Корисні приклади (🧪)**

```java
// Autoboxing: int -> Integer
List<Integer> list = new ArrayList<>();
list.add(42); // автоматично box'иться

// Unboxing: Integer -> int
int num = list.get(0); // автоматично unbox'иться

// ❌ NPE при розпакуванні null
Integer a = null;
int b = a; // ⚠️ NullPointerException
```

---

## **Порівняння (⚖️)**

| Ознака | Autoboxing | Unboxing |
| :---: | :---: | :---: |
| **Напрямок** | **`int -> Integer`** | **`Integer -> int`** |
| **Коли виконується** | Коли потрібен обʼєкт | Коли потрібен примітив |
| **Можливі ризики** | Зайва алокація | **`NullPointerException`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Auto -> box** (поклади в коробку)
* **Un -> box** (дістань з коробки)

📦 “**Box me up when I go to collections. Unbox me when I do math.**”

---

## **🟩 Підсумок:**

Autoboxing та Unboxing — це автоматичні перетворення між **примітивами** і **wrapper-класами**.  
Це зручно, але має побічні ефекти:

* **NullPointerException** при розпакуванні **`null`**
* **Зайві обʼєкти** -> **гірша продуктивність**
* **`==`** не працює як очікуєш — завжди використовуй **`equals()`** для обʼєктів.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs: Autoboxing and Unboxing**](https://docs.oracle.com/javase/tutorial/java/data/autoboxing.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Autoboxing та Unboxing і для чого їх додали в Java?

> #### **💬 Відповідь:**
>
> Це механізм компілятора, який автоматизує перетворення між примітивними типами та їхніми відповідними класами-обгортками.
>
> * **Autoboxing (Автоупаковка):** Це автоматичне перетворення **примітивного типу** (наприклад, `int`) в об'єкт відповідного **класу-обгортки** (наприклад, `Integer`). Це еквівалентно неявному виклику `Integer.valueOf(int)`.
>
> * **Unboxing (Авторозпаковка):** Це зворотний процес — автоматичне перетворення **об'єкта класу-обгортки** назад у відповідний **примітивний тип**. Це еквівалентно неявному виклику `integer.intValue()`.
>
> **Головна мета**, з якою їх додали в Java 5, — це **спрощення коду та підвищення його читабельності**. Завдяки autoboxing/unboxing розробники можуть писати більш природний код, особливо при роботі з колекціями, без необхідності постійно робити ручні перетворення.

#### **📝 Приклад:**

До Java 5 код виглядав так:

```java
List list = new ArrayList();
list.add(Integer.valueOf(5)); // Ручна упаковка
int i = ((Integer)list.get(0)).intValue(); // Ручна розпаковка
```

Після Java 5:

```java
List<Integer> list = new ArrayList<>();
list.add(5);      // Autoboxing
int i = list.get(0); // Unboxing
```

#### **📌 Що перевіряють:**

*Базове розуміння концепції, її мети та здатності навести простий приклад "до" і "після".*

* * *

### **2 ❓ Питання:**

В яких ситуаціях компілятор виконує Autoboxing, а в яких — Unboxing?

> #### **💬 Відповідь:**
>
> Компілятор застосовує ці перетворення в чітко визначених контекстах, коли тип даних не збігається з очікуваним.
>
> **Autoboxing (примітив → об'єкт) відбувається, коли:**
>
> * Примітивне значення присвоюється змінній типу класу-обгортки.
>     `Integer i = 10;`
> * Примітивне значення передається як параметр у метод, що очікує об'єкт-обгортку.
>     `list.add(5); // Метод add очікує Integer, передаємо int`
>
> **Unboxing (об'єкт → примітив) відбувається, коли:**
>
> * Об'єкт-обгортка присвоюється змінній примітивного типу.
>     `int i = Integer.valueOf(10);`
> * Об'єкт-обгортка використовується в арифметичних операціях.
>     `Integer a = 5; int b = a + 2;`
> * Об'єкт-обгортка використовується в операціях порівняння, що вимагають примітиви.
>     `Integer c = 10; if (c > 5) { ... }`
> * Об'єкт-обгортка використовується в `switch` операторі (з Java 7).

#### **📝 Коментар:**

Найчастіше це відбувається "прозоро" для розробника, що є одночасно і зручністю, і потенційним джерелом помилок.

#### **📌 Що перевіряють:**

*Знання контекстів, у яких застосовується механізм, що свідчить про розуміння роботи компілятора Java.*

* * *

### **3 ❓ Питання:**

Яка основна небезпека, пов'язана з Unboxing? Наведіть код, який її демонструє.

> #### **💬 Відповідь:**
>
> Основна і найпоширеніша небезпека Unboxing — це виникнення **`NullPointerException` (NPE)**, коли програма намагається розпакувати об'єкт-обгортку, який має значення `null`.
>
> Це відбувається тому, що Unboxing, по суті, є викликом методу (`.intValue()`, `.doubleValue()` і т.д.) на об'єкті. Виклик будь-якого методу на `null`-посиланні призводить до NPE.
>
> **Код, що демонструє проблему:**
>
> ```java
> public class UnboxingDanger {
>
>     public static Integer getCount() {
>         // Уявімо, що метод може повернути null,
>         // якщо, наприклад, дані не знайдено в базі
>         return null;
>     }
>
>     public static void main(String[] args) {
>         // Спроба розпакувати null
>         int count = getCount(); // Тут виникне NullPointerException
>         System.out.println("Кількість: " + count);
>     }
> }
> ```
>
> Під час виконання рядка `int count = getCount();` компілятор намагається виконати `getCount().intValue()`, що в цьому випадку еквівалентно `null.intValue()`, і це спричиняє `NullPointerException`.

#### **📝 Коментар:**

Правило №1 при роботі з обгортками: перед тим, як розпакувати об'єкт (явно чи неявно), завжди перевіряйте його на `null`, якщо є хоч найменший сумнів у тому, що він може бути `null`.

#### **📌 Що перевіряють:**

*Знання головної пастки авторозпаковки. Це одне з найважливіших практичних знань для написання надійного коду.*

* * *

### **4 ❓ Питання:**

Що виведе цей код і чому?

```java
Integer i1 = 1;
Integer i2 = 1;
Long l1 = 2L;

System.out.println(i1 == i2);
System.out.println(i1 + 1 == l1);
System.out.println(i1.equals(l1 - 1));
```

> #### **💬 Відповідь:**
>
> Результат буде таким:
>
> ```
> true
> true
> false
> ```
>
> **Пояснення:**
>
> 1. **`System.out.println(i1 == i2);` → `true`**
>     * Значення `1` потрапляє в кешований діапазон `Integer` (-128 до 127). `i1` та `i2` посилаються на один і той же об'єкт з кешу. `==` порівнює посилання, які є однаковими.
>
> 2. **`System.out.println(i1 + 1 == l1);` → `true`**
>     * Тут відбуваються кілька неявних перетворень. Вираз `i1 + 1` призводить до **Unboxing** `i1` в `int` (стає `1 + 1 = 2`).
>     * Для порівняння `int` та `Long`, JVM розширює типи до найбільшого. Отже, `l1` **розпаковується** в `long` (стає `2L`), а результат `2` (`int`) розширюється до `long`.
>     * Порівнюються два примітиви: `2L == 2L`, що є `true`.
>
> 3. **`System.out.println(i1.equals(l1 - 1));` → `false`**
>     * Вираз `l1 - 1` призводить до **Unboxing** `l1` в `long`, результат обчислення — `1L`.
>     * Метод `Integer.equals(Object obj)` **перевіряє тип** об'єкта. Він очікує `Integer`, а не `Long`. Оскільки `1L` - це `long`, він буде **автоупакований** в `Long`, а не `Integer`.
>     * Усередині `equals` відбувається перевірка `obj instanceof Integer`, яка повертає `false`, оскільки передано об'єкт типу `Long`. Тому загальний результат `false`.

#### **📌 Що перевіряють:**

*Уміння розбиратися в складних ланцюжках неявних перетворень (Unboxing, Autoboxing, розширення примітивів) та знання роботи методу `equals()` для класів-обгорток.*

* * *

### **5 ❓ Питання:**

Як Autoboxing та Unboxing впливають на продуктивність? Наведіть приклад коду, де це може стати проблемою.

> #### **💬 Відповідь:**
>
> Вони негативно впливають на продуктивність, оскільки створюють "приховані" витрати:
>
> * **Autoboxing** призводить до створення нових об'єктів у купі (якщо значення не кешується), що вимагає виділення пам'яті та згодом створює роботу для Garbage Collector.
> * **Unboxing** вимагає додаткових операцій для отримання примітивного значення з об'єкта.
>
> **Приклад коду, де це є проблемою:**
>
> ```java
> public class PerformanceIssue {
>     public static void main(String[] args) {
>         long startTime = System.currentTimeMillis();
>
>         Long sum = 0L; // Використовуємо обгортку Long замість long
>         for (long i = 0; i < Integer.MAX_VALUE; i++) {
>             sum += i; // ПРОБЛЕМА ТУТ
>         }
>
>         long endTime = System.currentTimeMillis();
>         System.out.println("Сума: " + sum);
>         System.out.println("Час виконання: " + (endTime - startTime) + " мс");
>     }
> }
> ```
>
> **Пояснення проблеми:**
> У рядку `sum += i;` на кожній ітерації циклу відбувається:
>
> 1. **Unboxing:** Об'єкт `sum` розпаковується в `long`.
> 2. Виконується додавання.
> 3. **Autoboxing:** Результат упаковується в **новий об'єкт `Long`**, на який починає посилатися `sum`.
>
> Це призводить до створення понад 2 мільярдів короткоживучих об'єктів `Long`, що спричиняє величезне навантаження на GC і значно сповільнює виконання програми. Якщо замінити `Long sum` на `long sum`, програма виконається в десятки, а то й сотні разів швидше.

#### **📌 Що перевіряють:**

*Здатність ідентифікувати "гарячі" ділянки коду, де неявні перетворення можуть спричинити серйозні проблеми з продуктивністю.*

* * *

### **6 ❓ Питання:**

Чи можна повністю вимкнути Autoboxing/Unboxing в Java?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Autoboxing та Unboxing — це фундаментальна частина специфікації мови Java, починаючи з версії 5. Не існує жодного прапора компілятора чи налаштування JVM, яке б дозволило їх вимкнути.
>
> Вони є частиною синтаксису мови, і компілятор `javac` завжди буде застосовувати ці перетворення у відповідних контекстах. Єдиний спосіб їх "уникнути" — це писати код так, щоб не створювати ситуацій для їх застосування:
>
> * Використовувати примітиви там, де це можливо.
> * Використовувати явні виклики `Integer.valueOf()` та `.intValue()` замість неявних перетворень.
>
> Однак, на практиці це зазвичай не потрібно і робить код менш читабельним. Краще розуміти, як цей механізм працює, і уникати його потенційних пасток, аніж намагатися його "обійти".

#### **📝 Коментар:**

Це питання на розуміння статусу механізму в мові. Autoboxing/Unboxing — це не опція, а частина граматики мови.

#### **📌 Що перевіряють:**

*Глибину знань про специфікацію мови Java та розуміння того, що є її невід'ємною частиною, а що — опціональною функцією.*

* * *

### **7 ❓ Питання:**

Який зв'язок між Autoboxing та перевантаженням методів? Який метод буде викликано і чому?

```java
public class OverloadingTest {
    public static void method(int i) {
        System.out.println("method(int)");
    }
    public static void method(Integer i) {
        System.out.println("method(Integer)");
    }
    public static void main(String[] args) {
        int i = 5;
        method(i);
    }
}
```

> #### **💬 Відповідь:**
>
> Буде викликано метод `method(int)`.
>
> **Пояснення:**
>
> Компілятор Java при виборі перевантаженого методу дотримується наступного правила пріоритету:
>
> 1. **Точна відповідність:** Спочатку він шукає метод, сигнатура якого точно відповідає переданим типам.
> 2. **Розширення примітивів (Widening):** Якщо точної відповідності немає, він намагається застосувати розширення примітивного типу (наприклад, `int` до `long`).
> 3. **Autoboxing/Unboxing:** Якщо і розширення неможливе, він намагається застосувати автоупаковку або авторозпаковку.
> 4. **Змінна кількість аргументів (Varargs):** В останню чергу він розглядає методи зі змінною кількістю аргументів.
>
> У даному випадку змінна `i` має тип `int`. Оскільки існує метод `method(int)`, який є **точною відповідністю**, компілятор обере саме його. Autoboxing до `Integer` буде проігноровано, оскільки він має нижчий пріоритет.
>
> Якби методу `method(int)` не існувало, тоді був би викликаний `method(Integer)` через autoboxing.

#### **📌 Що перевіряють:**

*Розуміння правил вирішення перевантажень (overload resolution) в Java та місця, яке в цій ієрархії займає autoboxing.*

* * *

### **8 ❓ Питання:**

Розгляньте такий код. Чи скомпілюється він? Якщо так, що станеться під час виконання?

```java
public static void main(String[] args) {
    Integer i = new Integer(1);
    Long l = new Long(1);
    
    // System.out.println(i == l); // Помилка компіляції!
    System.out.println(i.equals(l));
}
```

> #### **💬 Відповідь:**
>
> Рядок `System.out.println(i == l);` **не скомпілюється**. Компілятор Java видасть помилку `incomparable types: Integer and Long`.
>
> Причина в тому, що `==` для об'єктних типів можна застосовувати, лише якщо один тип може бути приведений до іншого (наприклад, один є підкласом іншого). `Integer` та `Long` не мають такого зв'язку в ієрархії класів (вони обидва спадкоємці `Number`), тому компілятор одразу відкидає таке порівняння як безглузде.
>
> **Друга частина коду:**
>
> Рядок `System.out.println(i.equals(l));` **скомпілюється і виконається**, але виведе **`false`**.
>
> **Пояснення:**
> Метод `Integer.equals(Object o)` реалізований так, що він спочатку перевіряє тип переданого об'єкта.
>
> ```java
> public boolean equals(Object obj) {
>     if (obj instanceof Integer) {
>         return value == ((Integer)obj).intValue();
>     }
>     return false;
> }
> ```
>
> Оскільки `l` є об'єктом типу `Long`, а не `Integer`, перевірка `obj instanceof Integer` поверне `false`, і весь метод `equals` одразу поверне `false`, навіть не доходячи до порівняння значень. Unboxing в цьому випадку взагалі не відбувається.

#### **📌 Що перевіряють:**

*Увагу до деталей роботи оператора `==` та методу `equals` для різних типів обгорток. Розуміння того, що компілятор може запобігати логічно некоректним порівнянням.*

* * *

### **9 ❓ Питання:**

Що виведе цей код і чому?

```java
List<Integer> list = new ArrayList<>();
list.add(10);
list.add(20);
list.remove(10); // Видалення елемента
System.out.println(list);
```

⭐️

> #### **💬 Відповідь:**
>
> Це питання-пастка. Код **не скомпілюється**. Причина полягає в неоднозначності виклику методу `remove`.
>
> У класі `ArrayList` є два перевантажених методи `remove`:
>
> 1. `public E remove(int index)`: Видаляє елемент за **індексом**.
> 2. `public boolean remove(Object o)`: Видаляє **перше входження вказаного об'єкта**.
>
> Коли ми викликаємо `list.remove(10)`, компілятор Java бачить `10` як примітив `int`. Згідно з правилами перевантаження, він обирає метод з **точною відповідністю типів**, тобто `remove(int index)`.
>
> Отже, Java намагатиметься видалити елемент за **індексом 10**. Але в нашому списку всього 2 елементи (з індексами 0 та 1). Тому під час виконання буде викинуто виняток **`IndexOutOfBoundsException`**.
>
> Ой, перепрошую, це моя неуважність. Це правильне пояснення, якби код запустився. Насправді, код `list.remove(10)` *скомпілюється*, але спричинить `IndexOutOfBoundsException` під час виконання. Виправлю відповідь.

* * *

#### **Змінена відповідь на питання №9:**
>
> #### **💬 Відповідь:**
>
> Цей код **скомпілюється**, але під час виконання буде викинуто виняток **`IndexOutOfBoundsException`**.
>
> **Пояснення:**
>
> Це питання-пастка, пов'язана з перевантаженням методу `remove` в `ArrayList`. Цей клас має два методи `remove`:
>
> 1. `public E remove(int index)` — видаляє елемент за **індексом**.
> 2. `public boolean remove(Object o)` — видаляє **перше входження вказаного об'єкта**.
>
> Коли ми викликаємо `list.remove(10)`, компілятор Java бачить `10` як примітив `int`. Згідно з правилами вирішення перевантажень, він віддає перевагу методу з точною відповідністю типів — `remove(int index)`. Він не буде виконувати **Autoboxing** `int` до `Integer`, якщо є більш відповідний метод.
>
> Таким чином, Java намагається видалити елемент з **індексом 10**. Оскільки наш список має лише два елементи (індекси 0 та 1), виникне `IndexOutOfBoundsException`.
>
> **Щоб видалити об'єкт зі значенням 10, потрібно явно вказати, що ми хочемо викликати `remove(Object o)`:**
>
> ```java
> list.remove(Integer.valueOf(10)); // Явна упаковка, викликає remove(Object)
> // або
> list.remove((Integer)10);         // Приведення типу
> ```

#### **📌 Що перевіряють:**

*Розуміння, як autoboxing взаємодіє з перевантаженням методів у стандартній бібліотеці. Це дуже поширений підводний камінь при роботі з колекціями чисел.*

* * *

### **10 ❓ Питання:**

Поясніть, що відбувається "під капотом" під час виконання цього тернарного оператора:
`Integer result = (someBoolean) ? 1 : null;`
Чому цей код, на відміну від `Integer result = null;`, може викликати проблеми? ⭐️

> #### **💬 Відповідь:**
>
> Цей код, хоч і виглядає безневинним, може призвести до `NullPointerException` через правила, які компілятор застосовує до тернарного оператора.
>
> Специфікація мови Java (JLS) вимагає, щоб тип результату тернарного оператора був єдиним. Коли другий та третій операнди мають різні, але сумісні типи (у нашому випадку `int` для `1` та тип `null`), компілятор намагається звести їх до спільного знаменника.
>
> **Що відбувається:**
>
> 1. Компілятор бачить, що один з можливих результатів — `int` (`1`), а інший — `null`.
> 2. Щоб узгодити типи, він визначає, що тип виразу має бути `Integer` (об'єкт).
> 3. Однак, через наявність `int` у виразі, він застосовує **Unboxing** до результату. Фактично, компілятор намагається зробити результат виразу примітивним `int`.
> 4. Це означає, що якщо `someBoolean` буде `false`, `null` буде розпаковано, що призведе до `NullPointerException`.
>
> Таким чином, вираз еквівалентний:
>
> ```java
> Integer temp;
> if (someBoolean) {
>     temp = Integer.valueOf(1);
> } else {
>     temp = null;
> }
> int resultValue = temp.intValue(); // Неявний Unboxing! NPE, якщо temp == null
> Integer result = resultValue;     // Неявний Autoboxing
> ```
>
> Проблеми не виникне, тільки якщо `someBoolean` завжди буде `true`.

#### **📌 Що перевіряють:**

*Дуже глибокі знання специфікації мови Java. Це складний випадок, який показує, як неявні механізми (autoboxing/unboxing) у поєднанні з іншими правилами мови (тернарний оператор) можуть призвести до неочевидної поведінки.*

* * *

### **11 ❓ Питання:**

Чи можна сказати, що Autoboxing є просто "синтаксичним цукром"? Аргументуйте свою відповідь. ⭐️

> #### **💬 Відповідь:**
>
> **Так, значною мірою можна.** Термін **"синтаксичний цукор"** означає синтаксичну конструкцію в мові, яка робить код легшим для читання або написання, і яку можна перетворити на більш базові конструкції, що вже існують у мові.
>
> Autoboxing ідеально підпадає під це визначення:
>
> * **Спрощує написання:** Він дозволяє писати `Integer i = 5;` замість `Integer i = Integer.valueOf(5);`.
> * **Перетворюється на існуючі конструкції:** Компілятор `javac` повністю переписує код з автоупаковкою/розпаковкою на явні виклики методів (`.valueOf()`, `.intValue()`). В згенерованому байткоді немає жодних спеціальних інструкцій для autoboxing — там просто викликаються звичайні методи.
>
> **Аргументи "проти" (чому це трохи більше, ніж просто цукор):**
> Деякі можуть стверджувати, що оскільки цей механізм так тісно інтегрований у правила мови (наприклад, у вирішення перевантажень, тернарні оператори), він має глибший вплив на семантику, ніж просто поверхнева заміна. Він змінює те, як розробник думає про взаємодію примітивів та об'єктів.
>
> Проте, з технічної точки зору, оскільки все це вирішується на етапі компіляції без змін у JVM, визначення "синтаксичний цукор" є цілком коректним.

#### **📌 Що перевіряють:**

*Розуміння концепції "синтаксичного цукру" та здатність аналізувати фічі мови з точки зору їх реалізації (етап компіляції vs. етап виконання).*

* * *

### **12 ❓ Питання:**

Чому цей код з використанням `Boolean` працює без `NullPointerException`?

```java
Boolean b = null;
System.out.println(b ? "True" : "False");
```

> #### **💬 Відповідь:**
>
> Цей код **спричинить `NullPointerException`**. Це чергова пастка, пов'язана з авторозпаковкою.
>
> **Пояснення:**
>
> Умовна частина тернарного оператора (`умова ? вираз1 : вираз2`) очікує значення типу `boolean`.
>
> 1. Змінна `b` має тип `Boolean` і значення `null`.
> 2. Щоб перевірити умову, JVM намагається **розпакувати (`Unboxing`)** об'єкт `b` до примітиву `boolean`.
> 3. Це еквівалентно виклику `b.booleanValue()`.
> 4. Оскільки `b` дорівнює `null`, виклик `null.booleanValue()` призводить до **`NullPointerException`**.
>
> Цей код спрацював би, якби вираз був написаний інакше, щоб уникнути розпакування:
>
> ```java
> System.out.println(Boolean.TRUE.equals(b) ? "True" : "False");
> // або
> System.out.println(b != null && b ? "True" : "False");
> ```

#### **📌 Що перевіряють:**

*Уважність та вміння застосовувати знання про небезпеку Unboxing до різних контекстів (у цьому випадку — до тернарного оператора). Розпізнавання того, де саме відбувається неявна дія.*

* * *

### **13 ❓ Питання:**

Якби ви проектували новий фреймворк, у якому випадку API вашого фреймворку використовувало б примітиви, а в якому — класи-обгортки в публічних методах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> При проектуванні API вибір між примітивами та обгортками є важливим архітектурним рішенням, яке залежить від семантики методу.
>
> **Я б використовував класи-обгортки (`Integer`, `Boolean`) у таких випадках:**
>
> 1. **Коли відсутність значення є легальним станом (необов'язкові параметри):** Якщо параметр може бути не заданий, використання обгортки дозволяє передати `null` для позначення "відсутності значення" або "використовуй значення за замовчуванням".
>     `public void setSearchFilter(String query, Integer limit, Boolean activeOnly)`
>
> 2. **У методах, що повертають дані, які можуть не існувати:** Наприклад, пошук об'єкта в базі даних. Метод `findUserById(long id)` повинен повертати `User`, а не `user`. Повернення `null` є чітким сигналом "не знайдено". Те ж саме стосується, наприклад, `Integer getLatestScore(User u)`, який може повернути `null`, якщо у користувача немає жодного результату.
>
> 3. **При роботі з Generics:** Тут вибору немає, API, що працює з генериками, зобов'язане використовувати обгортки (`List<Double>`).
>
> **Я б використовував примітиви (`int`, `boolean`):**
>
> 1. **Для обов'язкових параметрів, які не можуть бути відсутніми:** Якщо метод вимагає, наприклад, ідентифікатор або лічильник, використання примітиву `long id` або `int count` є самодокументуючим. Це змушує клієнта API завжди надавати значення і захищає метод від `NullPointerException` при розпаковці.
>
> 2. **У високопродуктивних частинах API:** Якщо фреймворк містить математичну бібліотеку або утиліти для обробки великих об'ємів даних, використання примітивів у внутрішніх циклах і критичних методах є обов'язковим для уникнення накладних витрат.
>     `public double calculateSum(double[] numbers)`
>
> **Золоте правило:** Використовувати примітиви за замовчуванням, а обгортки — лише тоді, коли їхні властивості (можливість `null`) є семантично важливими для логіки API.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен продемонструвати здатність приймати обґрунтовані проектні рішення, зважуючи зручність, безпеку та продуктивність.*

* * *

### **14 ❓ Питання:**

Чи існує різниця у згенерованому байткоді для `Integer i = 10;` та `Integer i = Integer.valueOf(10);`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, згенерований байткод для цих двох рядків буде абсолютно ідентичним.**
>
> **Autoboxing** — це функція **компілятора**. Коли компілятор `javac` зустрічає рядок `Integer i = 10;`, він розпізнає це як ситуацію для автоупаковки і сам перетворює цей рядок на еквівалентний виклик `Integer i = Integer.valueOf(10);`.
>
> Отже, в обох випадках у згенерований `.class` файл потрапить одна і та сама інструкція байткоду — виклик статичного методу `Integer.valueOf(int)`.
>
> **Що ми побачимо в байткоді (спрощено):**
>
> ```
> bipush 10                 // Покласти константу 10 на стек
> invokestatic  #2          // Викликати статичний метод java/lang/Integer.valueOf(I)Ljava/lang/Integer;
> astore_1                  // Зберегти результат (посилання на об'єкт) у локальну змінну 1
> ```
>
> Це демонструє, що Autoboxing є саме синтаксичним цукром, який не вводить нових механізмів на рівні JVM, а лише спрощує вихідний код, трансформуючи його у вже існуючі конструкції на етапі компіляції.

#### **📝 Коментар:**

Це можна легко перевірити, скомпілювавши `.java` файл і дизасемблювавши його за допомогою утиліти `javap -c`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, на якому етапі (компіляція чи виконання) працює Autoboxing, та вміння мислити на рівні згенерованого коду.*

* * *

### **15 ❓ Питання:**

Видаліть зі списку `list` усі непарні числа. Чому цей код викине `ConcurrentModificationException` і як його виправити, не забуваючи про Autoboxing/Unboxing?

```java
List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5, 6));
for (Integer i : list) {
    if (i % 2 != 0) {
        list.remove(i);
    }
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код викине **`ConcurrentModificationException`** через спробу модифікувати колекцію під час ітерації по ній за допомогою `for-each` циклу.
>
> **Чому це відбувається:**
> `for-each` цикл "під капотом" використовує **ітератор (`Iterator`)**. Ітератор має внутрішній лічильник модифікацій (`modCount`), який він порівнює з лічильником модифікацій у колекції. Коли викликається метод `list.remove(i)`, лічильник колекції змінюється. На наступній ітерації ітератор бачить, що лічильники не збігаються, і негайно викидає `ConcurrentModificationException` для запобігання непередбачуваної поведінки.
>
> **Виправлення проблеми:**
>
> Правильний спосіб видаляти елементи під час ітерації — використовувати **метод `remove()` самого ітератора**.
>
> Однак тут є ще один **підводний камінь, пов'язаний з Unboxing**. Вираз `i % 2 != 0` є неявним Unboxing.
>
> **Правильне і безпечне рішення:**
>
> ```java
> List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5, 6, null));
>
> Iterator<Integer> iterator = list.iterator();
> while (iterator.hasNext()) {
>     Integer i = iterator.next();
>
>     // Захист від NPE при розпакуванні
>     if (i != null && i % 2 != 0) {
>         iterator.remove(); // Використовуємо метод ітератора
>     }
> }
> ```
>
> **Ще краще та сучасніше рішення (з Java 8):**
> Використовувати метод `removeIf()`, який інкапсулює логіку ітерації та безпечного видалення.
>
> ```java
> List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5, 6, null));
> // removeIf використовує Predicate, який також може працювати з null
> list.removeIf(i -> i != null && i % 2 != 0);
> ```
>
> Це рішення є найбільш коротким, читабельним та безпечним, оскільки воно також коректно обробляє небезпеку Unboxing для `null`-елементів.

#### **📌 Що перевіряють:**

*Здатність комбінувати знання з кількох тем: робота з колекціями, ітератори, винятки (`ConcurrentModificationException`) та небезпеки, пов'язані з Autoboxing/Unboxing (`NullPointerException`). Це комплексне питання, що демонструє досвід кандидата.*
