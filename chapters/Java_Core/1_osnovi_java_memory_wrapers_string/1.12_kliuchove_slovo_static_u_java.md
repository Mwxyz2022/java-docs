---
layout: default
title: "Ключове слово static у Java"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 12
---

# Ключове слово static у Java

* короткий опис: Ключове слово `static` означає, що член (поле, метод, блок, вкладений клас) належить класу, а не конкретному екземпляру, і є спільним для всіх об'єктів.

* поняття: `static`, поле, метод, блок, вкладений клас.

* приклади: `Math.random()`, `System.out.println()`, `Counter.count`.

* ключові моменти: Викликаються без створення об'єкта, не мають доступу до `this`, ініціалізуються один раз.

* що важливо знати на співбесіді: Різниця між `static` та instance членами, коли використовувати.

### **✅ Офіційне визначення:**

**`static`** — це ключове слово в Java, що означає **приналежність до класу**, а не до обʼєкта. Воно може застосовуватись до змінних, методів, блоків і вкладених класів.

### **🧠 Простими словами:**

**`static`** \= **"загальний для всіх"**

* Належить класу, а не конкретному обʼєкту
* Може бути викликаний **без створення обʼєкта**
* Корисний для спільних ресурсів, утиліт, констант

---

## **Основне**

| Що | Де | Що це означає | Приклад |
| :---: | :---: | :---: | :---: |
| **Змінна** | **`static int count`** | Спільна для всіх обʼєктів | **`ClassName.count`** |
| **Метод** | **`static void print()`** | Можна викликати без обʼєкта | **`ClassName.print()`** |
| **Блок** | **`static {}`** | Статичний ініціалізатор | Виконується один раз |
| **Клас** | **`static class Inner {}`** | Вкладений клас, незалежний від зовнішнього | Не потребує екземпляру зовнішнього класу |

---

## **Пояснення на пальцях (💡)**

🔹 **Статична змінна** -> Ніби загальний рахунок для всіх обʼєктів. Наприклад, лічильник створених екземплярів.

🔹 **Статичний метод** -> Можна викликати напряму з класу — як **`Math.sqrt()`** або **`System.out.println()`**.

🔹 **Статичний блок** -> Блок коду, який виконується один раз при завантаженні класу. Корисний для ініціалізації.

🔹 **Статичний клас** -> Вкладений клас, який не має доступу до нестатичних елементів зовнішнього класу.

---

## **Корисні приклади (🧪)**

```java
// 🔹 Статична змінна:
class Counter {
    static int count = 0;
    Counter() {
        count++;
    }
}

// 🔹 Статичний метод:
class Utils {
    static void sayHi() {
        System.out.println("Привіт!");
    }
}

// 🔹 Статичний блок:
class Config {
    static {
        System.out.println("Конфігурація завантажена!");
    }
}

// 🔹 Статичний клас:
class Outer {
    static class Inner {
        void show() {
            System.out.println("Привіт з Inner!");
        }
    }
}
```

---

## **Порівняння (⚖️)**

| Характеристика | `static` | `non-static` |
| :---: | :---: | :---: |
| **Приналежність** | До класу | До обʼєкта |
| **Виклик без обʼєкта** | ✅ | ❌ |
| **Памʼять** | Спільна для всіх екземплярів | Кожен обʼєкт має своє поле |
| **Контекст** | Не має доступу до this | Має доступ до this |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **S.T.A.T.I.C.**

* **S**hareable — спільне
* **T**ied to class — належить класу
* **A**ccessible без обʼєкта
* **T**ools — утиліти типу **`Math`**
* **I**nit only once (static block)
* **C**onstants — часто **`static`** `final`

---

## **🟩 Підсумок:**

Ключове слово **`static`** в Java дозволяє створювати змінні та методи, які належать класу, а не обʼєкту. Це корисно для спільних значень, утиліт, констант, а також для точки входу програми (**`main`**). Правильне використання **`static`** дозволяє зменшити кількість обʼєктів і зробити код ефективнішим.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — static**](https://docs.oracle.com/javase/tutorial/java/javaOO/classvars.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що означає ключове слово `static` в Java? У чому головна відмінність між `static` та не-`static` (instance) членами класу?

> #### **💬 Відповідь:**
>
> Ключове слово **`static`** вказує на те, що член класу (поле, метод, блок або вкладений клас) належить **самому класу**, а не конкретному екземпляру (об'єкту) цього класу.
>
> **Головна відмінність:**
>
> * **`static` члени (члени класу):**
>   * Існують в **єдиному екземплярі** для всього класу. Вони спільні для всіх об'єктів.
>   * Ініціалізуються один раз, коли клас завантажується в JVM.
>   * Доступ до них можна отримати **напряму через ім'я класу**, без створення об'єкта (`ClassName.staticMember`).
>
> * **Не-`static` члени (члени екземпляра, instance members):**
>   * Існують **окремо для кожного об'єкта** (екземпляра) класу.
>   * Ініціалізуються при кожному створенні нового об'єкта за допомогою `new`.
>   * Доступ до них можливий **лише через посилання на конкретний об'єкт** (`objectReference.instanceMember`).

#### **📝 Аналогія:**

Уявіть, що клас `Car` — це креслення автомобіля.

* Не-`static` поле `String color` — це колір конкретного автомобіля. Кожен автомобіль має свій колір.
* `static` поле `int totalCarsProduced` — це загальний лічильник усіх вироблених автомобілів. Він один на всіх і належить самому "кресленню", а не конкретній машині.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння різниці між рівнем класу та рівнем об'єкта, що є основою об'єктно-орієнтованого програмування.*

* * *

### **2 ❓ Питання:**

Чому метод `main()` в Java оголошується як `public static void`?

> #### **💬 Відповідь:**
>
> Кожен з модифікаторів у `public static void main(String[] args)` має своє важливе призначення:
>
> * **`public`:** Робить метод `main` доступним для виклику з будь-якого місця. Зокрема, це дозволяє **JVM** викликати його для запуску програми. JVM не є частиною вашого пакету, тому доступ має бути публічним.
>
> * **`static`:** Це найважливіша частина. JVM повинна мати можливість викликати метод `main` **без необхідності створювати екземпляр класу**. Якби метод `main` не був статичним, JVM довелося б вирішувати, який конструктор викликати, а якщо конструктор має параметри, то як їх передавати. `static` усуває цю проблему: метод належить класу, і його можна викликати напряму.
>
> * **`void`:** Метод `main` не повертає жодного значення. Завершення програми в Java сигналізується не поверненням значення з `main` (як у C++), а його нормальним завершенням або викликом `System.exit()`.
>
> * **`String[] args`:** Дозволяє передавати аргументи командного рядка в програму при її запуску.

#### **📝 Коментар:**

Це класичне питання, яке перевіряє, чи кандидат розуміє причини, а не просто завчив сигнатуру методу.

#### **📌 Що перевіряють:**

*Здатність пояснити кожне ключове слово в одній з найважливіших конструкцій мови, особливо роль `static` у точці входу в програму.*

* * *

### **3 ❓ Питання:**

Чи можна з `static` методу викликати не-`static` метод або звернутися до не-`static` поля? Чому?

> #### **💬 Відповідь:**
>
> **Ні, напряму — не можна.** Спроба зробити це призведе до **помилки компіляції**.
>
> **Пояснення:**
>
> * `static` методи та поля належать **класу** і існують незалежно від будь-яких об'єктів. Вони завантажуються разом з класом.
> * Не-`static` методи та поля належать **конкретному екземпляру (об'єкту)** і існують, тільки коли об'єкт створено.
>
> Статичний метод не має доступу до ключового слова **`this`**, оскільки він не прив'язаний до жодного конкретного об'єкта. Він просто не знає, для **якого саме** об'єкта викликати не-`static` метод або до поля якого об'єкта звертатися.
>
> **Однак, це можна зробити опосередковано:**
> Ви можете викликати не-`static` метод зі `static` методу, якщо ви **явно створите екземпляр класу** і викличете метод через посилання на цей об'єкт.
>
> ```java
> public class Example {
>     private String instanceField = "Instance Data";
>
>     public void instanceMethod() {
>         System.out.println(instanceField);
>     }
>
>     public static void staticMethod() {
>         // Помилка компіляції: Немає доступу до this
>         // instanceMethod();
>
>         // ПРАВИЛЬНО: через створення об'єкта
>         Example obj = new Example();
>         obj.instanceMethod();
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння контексту виконання. Кандидат повинен чітко розрізняти контекст класу (статичний) та контекст об'єкта (нестатичний) і правила взаємодії між ними.*

* * *

### **4 ❓ Питання:**

Що таке статичний блок ініціалізації (`static` block) і коли він виконується?

> #### **💬 Відповідь:**
>
> **Статичний блок ініціалізації** — це блок коду, позначений ключовим словом `static`, який використовується для ініціалізації **статичних полів** класу.
>
> ```java
> class MyConfig {
>     public static final Map<String, String> CONFIG_MAP;
>
>     static {
>         System.out.println("Статичний блок виконується...");
>         CONFIG_MAP = new HashMap<>();
>         CONFIG_MAP.put("url", "jdbc:mysql://localhost/db");
>         CONFIG_MAP.put("user", "root");
>     }
> }
> ```
>
> **Коли він виконується:**
>
> Статичний блок виконується **один раз** за весь час життя програми — у момент, коли **JVM завантажує клас** у пам'ять. Це відбувається при першому зверненні до класу, наприклад, при створенні його екземпляра (`new MyConfig()`), виклику його статичного методу або зверненні до його статичного поля.
>
> Якщо в класі є кілька статичних блоків, вони виконуються в тому порядку, в якому вони оголошені в коді.
>
> Він ідеально підходить для складної ініціалізації статичних полів, яка вимагає більше ніж одного рядка коду (наприклад, завантаження даних з файлу, заповнення колекції).

#### **📌 Що перевіряють:**

*Знання механізму ініціалізації класів у JVM та інструментів для ініціалізації статичних полів.*

* * *

### **5 ❓ Питання:**

Наведіть приклади, коли використання `static` полів та методів є доцільним.

> #### **💬 Відповідь:**
>
> Використання `static` є доцільним тоді, коли певна функціональність або дані логічно пов'язані з класом в цілому, а не з його окремими екземплярами.
>
> **Доцільне використання `static` полів:**
>
> 1. **Константи (`public static final`):** Для визначення публічних констант, які є незмінними і спільними для всієї програми.
>     `public static final int MAX_CONNECTIONS = 10;`
>
> 2. **Лічильники або спільні ресурси:** Коли потрібно відстежувати інформацію, спільну для всіх об'єктів.
>     `private static int userCount = 0;` (лічильник створених користувачів).
>
> 3. **Кеш або пул об'єктів:** Створення спільного кешу, доступ до якого мають усі екземпляри.
>
> **Доцільне використання `static` методів:**
>
> 1. **Утилітарні методи (Utility methods):** Методи, які виконують операції над переданими їм параметрами і не залежать від стану будь-якого об'єкта. Клас `java.lang.Math` є ідеальним прикладом (`Math.sqrt()`, `Math.max()`).
>
> 2. **Фабричні методи (Factory methods):** Статичні методи, які створюють та повертають екземпляри свого або іншого класу. Це дає більше контролю над створенням об'єктів, ніж конструктори.
>     `Integer number = Integer.valueOf("123");`
>
> 3. **Реалізація патерну Singleton:** Метод `getInstance()`, який повертає єдиний екземпляр класу.

#### **📌 Що перевіряють:**

*Практичне розуміння, де і чому застосовується `static` в реальних проектах. Здатність навести приклади з JDK.*

* * *

### **6 ❓ Питання:**

Що таке `static` вкладений клас? Чим він відрізняється від внутрішнього (не-`static`) класу?

> #### **💬 Відповідь:**
>
> Це два типи вкладених класів, і різниця між ними фундаментальна.
>
> * **`static` вкладений клас (static nested class):**
>   * Оголошується з ключовим словом `static` всередині іншого класу.
>   * **Не має** неявного посилання на екземпляр зовнішнього класу. Він не пов'язаний з об'єктом зовнішнього класу.
>   * Може звертатися **тільки до `static` членів** зовнішнього класу.
>   * Його екземпляр можна створити **незалежно** від зовнішнього класу: `new OuterClass.StaticNestedClass();`.
>
> * **Внутрішній клас (inner class, не-`static`):**
>   * Оголошується без `static`.
>   * **Має** неявне посилання на екземпляр зовнішнього класу, в якому він був створений.
>   * Може звертатися до **всіх членів** зовнішнього класу (включаючи `private`).
>   * Його екземпляр можна створити **тільки через екземпляр зовнішнього класу**: `outerObject.new InnerClass();`.
>
> **Приклад:**
>
> ```java
> class Outer {
>     private int instanceVar = 10;
>     private static int staticVar = 20;
>
>     static class StaticNested {
>         void show() { System.out.println(staticVar); /* ОК */ }
>     }
>
>     class Inner {
>         void show() { System.out.println(instanceVar); /* ОК */ }
>     }
> }
> ```
>
> **Коли який використовувати:** Використовуйте `static` вкладений клас, якщо йому не потрібен доступ до стану екземпляра зовнішнього класу. Це запобігає витокам пам'яті і є більш поширеним. Внутрішній клас потрібен, тільки якщо він тісно пов'язаний зі станом об'єкта-власника.

#### **📌 Що перевіряють:**

*Знання про вкладені та внутрішні класи, що є просунутою темою ООП в Java.*

* * *

### **7 ❓ Питання:**

Чи можна успадкувати `static` метод? А перевизначити (`override`)?

> #### **💬 Відповідь:**
>
> * **Успадкувати — так, можна.** Дочірній клас "успадковує" статичні методи батьківського, і їх можна викликати через ім'я дочірнього класу.
>
>     ```java
>     class Parent { static void greet() { System.out.println("Hi"); } }
>     class Child extends Parent {}
>     // Child.greet(); // ✔️ Працює
>     ```
>
> * **Перевизначити (`@Override`) — ні, не можна.** Статичні методи **прив'язані до класу**, а не до об'єкта, тому концепція поліморфізму часу виконання до них незастосовна.
>
> Якщо ви оголосите в дочірньому класі статичний метод з такою ж сигнатурою, як у батьківському, це буде не перевизначення, а **приховування (hiding)**.
>
> **Приклад приховування:**
>
> ```java
> class Parent {
>     static void sayHello() { System.out.println("Hello from Parent"); }
> }
> class Child extends Parent {
>     // Це НЕ @Override, а приховування методу батька
>     static void sayHello() { System.out.println("Hello from Child"); }
> }
>
> Parent.sayHello(); // Виведе "Hello from Parent"
> Child.sayHello();  // Виведе "Hello from Child"
>
> Parent p = new Child();
> p.sayHello(); // Виведе "Hello from Parent"!
> ```
>
> В останньому рядку, оскільки вибір статичного методу відбувається **на етапі компіляції** на основі **типу посилання** (`Parent`), буде викликано метод класу `Parent`.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між статичним та динамічним зв'язуванням. Кандидат повинен знати про приховування методів.*

* * *

### **8 ❓ Питання:**

Де зберігаються `static` поля в пам'яті JVM? ⭐️

> #### **💬 Відповідь:**
>
> Розташування `static` полів, як і інших даних, змінювалося з еволюцією JVM.
>
> * **До Java 8 (у Java 7 і раніше):** `static` поля зберігалися в спеціальній області пам'яті, яка називалася **PermGen (Permanent Generation)**. Ця область була окремою від основної купи (Heap) і мала фіксований розмір.
>
> * **Починаючи з Java 8:** Область PermGen була повністю **видалена**. Тепер `static` поля, як і об'єкти, зберігаються в **основній купі (Heap)**.
>
> А метадані класів (інформація про структуру класу, методи, поля), які раніше також були в PermGen, були переміщені в нову область під назвою **Metaspace**, яка є частиною нативної пам'яті, а не купи.
>
> Отже, в сучасній Java відповідь така: `static` поля зберігаються **в Heap**. Вони прив'язані до об'єкта `java.lang.Class`, який представляє завантажений клас.

#### **📌 Що перевіряють:**

*Знання внутрішньої структури пам'яті JVM та її змін у сучасних версіях. Це важливе питання для діагностики проблем з пам'яттю.*

* * *

### **9 ❓ Питання:**

Чи може `static` метод бути `abstract`? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.** Спроба оголосити метод одночасно як `static` та `abstract` призведе до **помилки компіляції**.
>
> **Причина** — це фундаментальне логічне протиріччя:
>
> * **`abstract`** метод **не має реалізації** і **повинен бути перевизначений (`override`)** у дочірньому класі.
> * **`static`** метод **не може бути перевизначений** у дочірньому класі (його можна лише приховати). Він прив'язаний до класу, а не до об'єкта, тому поліморфізм для нього не працює.
>
> Оскільки абстрактний метод за своєю природою покладається на механізм перевизначення, а статичний метод цей механізм ігнорує, їх поєднання є неможливим.
>
> **Починаючи з Java 8**, в інтерфейсах можна мати **`static` методи**, але вони **повинні мати тіло реалізації**. Вони не можуть бути `abstract` чи `default`.

#### **📌 Що перевіряють:**

*Здатність виявляти логічні суперечності в комбінаціях ключових слів Java.*

* * *

### **10 ❓ Питання:**

Чи може конструктор бути оголошений як `static`? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.** Спроба оголосити конструктор як `static` призведе до **помилки компіляції**.
>
> **Пояснення:**
>
> 1. **Призначення конструктора:** Конструктор призначений для **ініціалізації екземпляра (об'єкта)** класу. Його робота тісно пов'язана зі створенням нового об'єкта, і він має доступ до ключового слова `this`.
>
> 2. **Призначення `static`:** `static` методи та поля, навпаки, **не прив'язані до конкретного екземпляра**. Вони належать класу і не мають доступу до `this`.
>
> Ці два поняття є взаємовиключними. Конструктор за своєю суттю є **не-статичним**.
>
> **Чим замінити?**
> Якщо вам потрібна "статична" логіка створення об'єктів, для цього використовують **статичні фабричні методи**. Це звичайні `static` методи, які всередині себе викликають приватний конструктор і повертають готовий об'єкт. Цей підхід дає більше гнучкості.

#### **📌 Що перевіряють:**

*Розуміння призначення конструкторів та логічних обмежень, що на них накладаються.*

* * *

### **11 ❓ Питання:**

Що виведе цей код і чому?

```java
class Test {
    public static void main(String[] args) {
        Test obj = null;
        obj.doSomething();
    }
    public static void doSomething() {
        System.out.println("Doing something...");
    }
}
```

> #### **💬 Відповідь:**
>
> Цей код **скомпілюється і виконається без помилок**, і на екран буде виведено:
>
> ```
> Doing something...
> ```
>
> **Пояснення (це питання-пастка):**
>
> Хоча ми викликаємо метод на `null`-посиланні (`obj`), **метод `doSomething()` є статичним (`static`)**.
>
> Компілятор Java визначає, який статичний метод викликати, на основі **типу посилання**, а не на основі самого об'єкта.
>
> 1. Компілятор бачить виклик `obj.doSomething()`.
> 2. Він дивиться на тип змінної `obj`, який є `Test`.
> 3. Він бачить, що `doSomething()` є `static` методом класу `Test`.
> 4. Тому компілятор перетворює цей виклик на `Test.doSomething()`.
>
> Оскільки для виклику статичного методу об'єкт не потрібен, той факт, що `obj` дорівнює `null`, **не має жодного значення**. Програма не намагається розіменувати `null`-посилання, тому `NullPointerException` не виникає.
>
> **Примітка:** Сучасні IDE попередять вас, що статичні методи слід викликати через ім'я класу, а не через посилання на об'єкт, щоб уникнути такої плутанини.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як відбувається вибір `static` методу (на етапі компіляції), і як це відрізняється від виклику instance-методу (на етапі виконання).*

* * *

### **12 ❓ Питання:**

У якому порядку будуть викликані конструктор та статичні/нестатичні блоки цього класу?

```java
class InitializationOrder {
    static { System.out.println("Static block 1"); }
    { System.out.println("Instance block 1"); }
    InitializationOrder() { System.out.println("Constructor"); }
    static { System.out.println("Static block 2"); }
    { System.out.println("Instance block 2"); }

    public static void main(String[] args) {
        new InitializationOrder();
    }
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Порядок виконання буде таким:
>
> ```
> Static block 1
> Static block 2
> Instance block 1
> Instance block 2
> Constructor
> ```
>
> **Пояснення послідовності:**
>
> 1. **Завантаження класу (виконується один раз):**
>     * Коли JVM зустрічає `new InitializationOrder()`, вона спочатку завантажує клас `InitializationOrder`.
>     * Під час завантаження класу виконуються **всі статичні блоки** в тому порядку, в якому вони написані в коді.
>     * Тому спочатку виконається `Static block 1`, а потім `Static block 2`.
>
> 2. **Створення екземпляра (виконується щоразу):**
>     * Після завантаження класу починається процес створення об'єкта.
>     * Спочатку виконуються **всі не-статичні блоки ініціалізації** в тому порядку, в якому вони написані.
>     * Тому виконається `Instance block 1`, а потім `Instance block 2`.
>     * І лише **після** всіх блоків ініціалізації викликається **конструктор**.

#### **📌 Що перевіряють:**

*Детальне знання процесу ініціалізації об'єктів у Java. Це важливе питання для розуміння життєвого циклу об'єктів та можливих проблем при складній ініціалізації.*

* * *

### **13 ❓ Питання:**

Чому надмірне використання `static` вважається поганою практикою і може свідчити про проблеми в дизайні? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Надмірне використання `static` членів може бути ознакою процедурного стилю програмування в об'єктно-орієнтованій мові та призводить до низки проблем у дизайні:
>
> 1. **Глобальний стан (Global State):** Статичні поля діють як глобальні змінні. Будь-яка частина коду може змінити їх, що робить стан програми важко передбачуваним та складним для відстеження. Це ускладнює розуміння коду, його налагодження та призводить до неочевидних побічних ефектів.
>
> 2. **Тісна зв'язність (Tight Coupling):** Код, який напряму залежить від статичних методів інших класів (`Utils.doSomething()`), стає тісно пов'язаним з цими конкретними реалізаціями. Це ускладнює заміну реалізацій, гнучкість та розширюваність системи.
>
> 3. **Погана тестованість (Poor Testability):** Класи з великою кількістю статичних залежностей дуже важко тестувати ізольовано. Ви не можете легко "підмінити" (замокати) статичний метод за допомогою популярних mock-фреймворків (як Mockito), оскільки він не є частиною об'єкта. Це вимагає використання більш складних інструментів (як PowerMock), що є ознакою поганого дизайну.
>
> 4. **Порушення принципів ООП:** `static` не пов'язаний з об'єктами і, відповідно, не підтримує поліморфізм. Зловживання `static` може перетворити об'єктно-орієнтовану програму на набір статичних функцій, втрачаючи переваги ООП.
>
> **Висновок:** `static` — це корисний інструмент, але його слід використовувати обережно: для справжніх констант, утилітарних функцій та фабричних методів, а не для зберігання стану програми чи реалізації бізнес-логіки.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти принципи хорошого дизайну ПО (низька зв'язність, висока тестованість) та те, як зловживання певними мовними конструкціями може їх порушувати.*

* * *

### **14 ❓ Питання:**

Розгляньте патерн Singleton. Яку роль відіграє `static` у його класичній реалізації? Які є проблеми з такою реалізацією? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У класичній реалізації патерну Singleton `static` відіграє **ключову роль**.
>
> **Роль `static`:**
>
> 1. **Статичне поле (`private static Singleton instance`):** Створюється **єдиний екземпляр** синглтона, який зберігається в статичному полі. Оскільки поле `static`, воно належить класу, а не об'єкту, і існує в одному екземплярі.
>
> 2. **Статичний метод (`public static Singleton getInstance()`):** Надає **глобальну точку доступу** до цього єдиного екземпляра. Оскільки метод `static`, його можна викликати напряму через клас (`Singleton.getInstance()`) без створення об'єкта.
>
> **Приклад класичної реалізації:**
>
> ```java
> public class Singleton {
>     private static Singleton instance;
>
>     private Singleton() {} // Приватний конструктор
>
>     public static Singleton getInstance() {
>         if (instance == null) { // Лінива ініціалізація
>             instance = new Singleton();
>         }
>         return instance;
>     }
> }
> ```
>
> **Проблеми з цією класичною реалізацією:**
>
> 1. **Не потокобезпечна:** У багатопотоковому середовищі два потоки можуть одночасно увійти в блок `if (instance == null)`, що призведе до створення двох екземплярів. Це можна виправити за допомогою `synchronized` або double-checked locking, але це ускладнює код.
>
> 2. **Проблеми з рефлексією:** За допомогою рефлексії можна викликати приватний конструктор і створити другий екземпляр, порушивши патерн.
>
> 3. **Проблеми з серіалізацією:** Десеріалізація за замовчуванням завжди створює новий об'єкт.
>
> **Сучасне рішення:**
> Найкращий спосіб реалізації Singleton в Java сьогодні — це використання **`enum`**:
>
> ```java
> public enum Singleton {
>     INSTANCE;
>     // методи
> }
> ```
>
> Це потокобезпечно, захищено від рефлексії та серіалізації "з коробки".

#### **📌 Що перевіряють:**

*Знання класичних патернів проектування, їх реалізації, слабких місць та сучасних альтернатив.*

* * *

### **15 ❓ Питання:**

У чому різниця між статичним імпортом (`import static`) та звичайним імпортом (`import`)? Коли доцільно використовувати статичний імпорт? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає в тому, **що саме** імпортується і **як** до нього можна звертатися.
>
> * **Звичайний імпорт (`import`):**
>   * **Що імпортує:** **Класи, інтерфейси, enum-и.**
>   * **Приклад:** `import java.util.ArrayList;`
>   * **Як використовується:** Дозволяє звертатися до типу за його коротким іменем (`ArrayList`) замість повного (`java.util.ArrayList`).
>
> * **Статичний імпорт (`import static`):**
>   * **Що імпортує:** **Статичні члени** (поля та методи) класу.
>   * **Приклад:** `import static java.lang.Math.PI;` або `import static java.lang.Math.*;`
>   * **Як використовується:** Дозволяє звертатися до статичних членів **без вказування імені класу**.
>
> **Приклад використання:**
>
> ```java
> import java.util.Arrays;
> import static java.lang.Math.PI;
> import static java.lang.Math.sqrt;
> import static java.util.Arrays.asList; // Статичний фабричний метод
>
> public class StaticImportExample {
>     public void doCalc() {
>         double radius = 2.0;
>         double area = PI * sqrt(radius); // Замість Math.PI * Math.sqrt(radius)
>         List<String> list = asList("a", "b"); // Замість Arrays.asList(...)
>     }
> }
> ```
>
> **Коли доцільно використовувати `import static`:**
>
> 1. **При роботі з константами:** Якщо ви часто використовуєте статичні константи з одного класу (наприклад, `TimeUnit.SECONDS`), статичний імпорт може зробити код чистішим.
> 2. **Для утилітарних методів:** При частому використанні статичних утилітарних методів, як-от з `Math` або `Arrays`.
> 3. **У тестах:** Дуже часто використовується для імпорту assert-методів з бібліотек типу JUnit або AssertJ (`import static org.junit.Assert.assertEquals;`).
>
> **Застереження:**
> Слід уникати "зіркового" статичного імпорту (`import static com.example.Utils.*;`), якщо це може призвести до конфлікту імен (name ambiguity) і зробити код менш читабельним, оскільки стає незрозуміло, з якого класу викликається метод.

#### **📌 Що перевіряють:**

*Знання просунутих можливостей мови та вміння застосовувати їх для покращення читабельності коду, а також розуміння потенційних проблем, які вони можуть створити.*
