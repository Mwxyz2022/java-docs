---
layout: default
title: "Тернарний оператор (?:) в Java"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 14
---

# Тернарний оператор (?:) в Java

* короткий опис: Тернарний оператор (`?:`) – це умовний оператор, що дозволяє скоротити конструкцію `if-else` до одного рядка для вибору значення за умовою.

* поняття: Тернарний оператор, `умова ? якщо_так : якщо_ні`.

* приклади: `int max = (a > b) ? a : b;`.

* ключові моменти: Ідеально для простих перевірок у змінних, повертає значення, що може бути присвоєне.

* що важливо знати на співбесіді: Синтаксис, коли доцільно використовувати, обмеження (читабельність для складних умов).

### **✅ Офіційне визначення:**

Тернарний оператор (?:) в Java — це умовний оператор, який дозволяє скоротити конструкцію if-else до одного рядка. Його синтаксис:

```java
умова ? значення_якщо_true : значення_якщо_false;
```

### **🧠 Простими словами:**

Це як коротка форма if-else: якщо умова вірна — виконай перше, інакше — друге. Ідеально для простих перевірок і присвоєння значень

---

## **Основне**

| Елемент | Пояснення |
| :---: | :---: |
| **умова** | Логічне вираження ( **`true`** / **`false`** ) |
| **?** | Якщо умова **`true`** |
| **:** | Якщо умова **`false`** |
| **Тип значення** | Обидва значення мають бути сумісні за типом |

---

## **Пояснення на пальцях (💡)**

### **Навіщо він?**  

  Щоб писати коротше, коли треба просто щось вибрати за умовою.

### **У чому зручність?**  

  1 рядок замість 4-х — компактність у дії.

### **Де застосовують?**  

  Підходить для логіки в UI, повідомленнях, змінних.

### **Граблі:**  

  Не використовуй, якщо умова складна — втратиш читабельність.  
  Не можна робити "кілька дій" — лише одне значення.

---

## **Корисні приклади (🧪)**

```java
// Парне чи непарне:
int number = 7;
String result = (number % 2 == 0) ? "парне" : "непарне";
System.out.println(result); // непарне
```

```java
// Повідомлення про статус:
boolean isLoggedIn = true;
String message = isLoggedIn ? "Ласкаво просимо!" : "Будь ласка, увійдіть.";
System.out.println(message);
```

```java
// Максимум із двох:
int a = 10, b = 20;
int max = (a > b) ? a : b;
System.out.println(max); // 20
```

```java
// Вкладені тернарні (⚠️ обережно!):
int score = 85;
String grade = (score >= 90) ? "A" :
               (score >= 80) ? "B" : "C";
System.out.println(grade); // B
```

---

## **Порівняння (⚖️)**

| Підхід | Код | Коментар |
| :---: | :---: | :---: |
| **if-else** | **`if (x > 0) res = "+"; else res = "-";`** | Більше коду |
| **Тернарний** | **`res = (x > 0) ? "+" : "-";`** | Коротше, зручніше |
| **Вкладений тернарний** | ⚠️ | Читабельність падає при ускладненні |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **умова ? так : ні**  
  -> логіка в одному рядку

* Мнемо: "Якщо так — перше, якщо ні — друге"

---

## **🟩 Підсумок:**

Тернарний оператор **`(?:)`** — це коротка форма if-else, яка дозволяє обрати значення за умовою. Ідеально підходить для простих перевірок і присвоєнь. Але не використовуй при складній логіці — краще звичайний if. Запамʼятай структуру:

```java
умова ? якщо_так : якщо_ні;
```

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Tutorials: Conditional Operator**](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/op2.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке тернарний оператор в Java і який його синтаксис?

> #### **💬 Відповідь:**
>
> **Тернарний оператор (`?:`)** — це єдиний оператор у Java, який приймає три операнди. Він є компактною заміною для простої конструкції `if-else` і дозволяє вибрати одне з двох значень залежно від результату логічної умови.
>
> Його основна функція — повернути значення, яке можна присвоїти змінній або використати у виразі.
>
> **Синтаксис:**
>
> ```java
> умова ? вираз1_якщо_true : вираз2_якщо_false
> ```
>
> * **`умова`:** Будь-який вираз, що повертає `boolean` (`true` або `false`).
> * **`вираз1_якщо_true`:** Значення, яке буде повернуто, якщо умова істинна.
> * **`вираз2_якщо_false`:** Значення, яке буде повернуто, якщо умова хибна.

#### **📝 Аналогія:**

Це як запитання на перехресті: "Чи зелене світло?"

* `?` (так) → "Йди вперед".
* `:` (ні) → "Стій".
Результатом буде одна з двох дій.

#### **📌 Що перевіряють:**

*Базове знання синтаксису та призначення тернарного оператора як альтернативи `if-else`.*

* * *

### **2 ❓ Питання:**

Наведіть простий приклад використання тернарного оператора і покажіть, як би той самий код виглядав з `if-else`.

> #### **💬 Відповідь:**
>
> Завдання: знайти максимальне з двох чисел.
>
> **Приклад з тернарним оператором:**
>
> ```java
> int a = 15;
> int b = 10;
> int max = (a > b) ? a : b;
> System.out.println("Максимальне число: " + max); // Виведе 15
> ```
>
> **Той самий код з `if-else`:**
>
> ```java
> int a = 15;
> int b = 10;
> int max;
>
> if (a > b) {
>     max = a;
> } else {
>     max = b;
> }
> System.out.println("Максимальне число: " + max);
> ```
>
> Як видно, тернарний оператор дозволяє виконати ту саму логіку в одному рядку замість кількох, що робить код більш компактним для простих завдань присвоєння.

#### **📝 Коментар:**

Це класичний приклад, який демонструє основну перевагу тернарного оператора — лаконічність.

#### **📌 Що перевіряють:**

*Здатність перетворити просту `if-else` конструкцію на тернарний вираз і навпаки. Це показує практичне розуміння оператора.*

* * *

### **3 ❓ Питання:**

Коли доцільно використовувати тернарний оператор, а коли краще надати перевагу `if-else`?

> #### **💬 Відповідь:**
>
> Вибір між тернарним оператором та `if-else` — це питання **читабельності** та **складності логіки**.
>
> **Коли доцільно використовувати тернарний оператор:**
>
> * **Для простих присвоєнь:** Коли потрібно присвоїти змінній одне з двох значень залежно від простої умови.
>     `String status = isActive ? "Online" : "Offline";`
> * **У `return` стейтментах:** Коли потрібно повернути одне з двох значень.
>     `return (user != null) ? user.getName() : "Guest";`
> * **В ініціалізації `final` змінних:**
>     `final int discount = (isPremiumMember) ? 20 : 5;`
>
> **Коли краще використовувати `if-else`:**
>
> * **Якщо потрібні кілька дій:** Тернарний оператор може повернути лише одне значення. Якщо в кожній гілці потрібно виконати кілька операцій (наприклад, викликати метод і змінити змінну), `if-else` є єдиним варіантом.
> * **Якщо умови складні та вкладені:** Вкладені тернарні оператори (`умова1 ? значення1 : (умова2 ? значення2 : значення3)`) дуже швидко стають нечитабельними. Для такої логіки `if-else-if` набагато зрозуміліший.
> * **Якщо одна з гілок не потрібна:** Наприклад, `if (condition) { doSomething(); }`. Таке неможливо виразити через тернарний оператор.
>
> **Золоте правило:** Якщо ви дивитесь на тернарний оператор і вам потрібно більше кількох секунд, щоб зрозуміти його логіку, — перепишіть його на `if-else`. Читабельність завжди важливіша за компактність.

#### **📌 Що перевіряють:**

*Здатність кандидата мислити в термінах чистоти коду (clean code). Розуміння того, що короткий код — не завжди хороший код.*

* * *

### **4 ❓ Питання:**

Яким повинен бути тип виразів, що повертаються тернарним оператором? Наведіть приклад з несумісними типами.

> #### **💬 Відповідь:**
>
> Типи `вираз1_якщо_true` та `вираз2_якщо_false` повинні бути **сумісними**. Компілятор Java повинен мати можливість визначити єдиний тип для всього тернарного виразу.
>
> **Правила сумісності:**
>
> 1. Якщо обидва вирази мають **однаковий тип**, то тип результату буде таким самим.
>     `(умова) ? 10 : 20` → тип `int`
>
> 2. Якщо один тип може бути **розширений (widened)** до іншого, то тип результату буде розширеним типом.
>     `(умова) ? 10 : 20.5` → `10` буде розширено до `double`, тип результату `double`.
>
> 3. Якщо типи **несумісні**, це призведе до **помилки компіляції**.
>
> **Приклад з несумісними типами:**
>
> ```java
> // ПОМИЛКА КОМПІЛЯЦІЇ: Incompatible types. String cannot be converted to int
> // int result = (isSuccess) ? "Success" : 1;
> ```
>
> Тут компілятор не може визначити спільний тип для `String` та `int`.
>
> Щоб це спрацювало, обидва результати потрібно було б привести до спільного предка, наприклад, `Object`:
> `Object result = (isSuccess) ? "Success" : 1;` (але `1` буде автоупаковано в `Integer`).

#### **📌 Що перевіряють:**

*Знання правил приведення типів у Java та того, як компілятор визначає тип виразу.*

* * *

### **5 ❓ Питання:**

Чи можна використовувати тернарний оператор без присвоєння його результату змінній?

> #### **💬 Відповідь:**
>
> **Так, можна.** Результат тернарного оператора — це вираз, який можна використовувати в будь-якому місці, де очікується значення.
>
> Його не обов'язково присвоювати змінній. Наприклад, його можна одразу передати як аргумент методу.
>
> **Приклад:**
>
> ```java
> boolean useDefaultGreeting = false;
> String customName = "John";
>
> // Результат тернарного оператора передається напряму в метод
> System.out.println(
>     useDefaultGreeting ? "Hello, World!" : "Hello, " + customName
> );
> ```
>
> **Однак, не можна використовувати його просто як самостійну інструкцію.**
>
> ```java
> // ПОМИЛКА КОМПІЛЯЦІЇ: Not a statement
> // (isReady) ? start() : stop();
> ```
>
> Вищенаведений код не скомпілюється, тому що тернарний оператор — це **вираз (expression)**, що повертає значення, а не **інструкція (statement)**, що виконує дію. Для виконання дій потрібно використовувати `if-else`.

#### **📌 Що перевіряють:**

*Розуміння різниці між виразом та інструкцією в Java.*

* * *

### **6 ❓ Питання:**

Що виведе цей код і чому?

```java
Object result;
if (true) {
    result = new Integer(10);
} else {
    result = new Double(20.5);
}
System.out.println(result);

System.out.println(true ? new Integer(10) : new Double(20.5));
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код продемонструє неочевидну, але важливу поведінку тернарного оператора, пов'язану з приведенням типів.
>
> **Вивід буде таким:**
>
> ```
> 10
> 10.0
> ```
>
> **Пояснення:**
>
> 1. **Блок `if-else`:**
>     * Умова `true` істинна, тому змінній `result` типу `Object` присвоюється об'єкт `Integer` зі значенням 10.
>     * `System.out.println(result)` викликає `toString()` на цьому `Integer`, і виводиться `10`. Все логічно.
>
> 2. **Тернарний оператор:**
>     * `true ? new Integer(10) : new Double(20.5)`
>     * Тут компілятор Java бачить два можливих типи результату: `Integer` та `Double`. Згідно зі специфікацією мови, він намагається знайти **найбільш конкретний спільний супертип**. Для числових обгорток це не просто `Object` або `Number`.
>     * JVM застосовує **двійкове числове розширення (binary numeric promotion)**. Тип `Integer` може бути розширений до `Double` (через unboxing до `int`, потім розширення до `double`, потім boxing до `Double`).
>     * Отже, тип **всього тернарного виразу** визначається як `Double`.
>     * Оскільки умова `true` істинна, вибирається `new Integer(10)`. Але для того, щоб відповідати типу виразу (`Double`), `Integer(10)` **приводиться до `Double`**.
>     * В результаті на друк іде об'єкт `Double` зі значенням `10.0`.

#### **📌 Що перевіряють:**

*Глибокі знання специфікації мови (JLS) щодо правил приведення типів у тернарних виразах. Це питання-пастка.*

* * *

### **7 ❓ Питання:**

Чи можна використовувати `void` методи всередині тернарного оператора?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Це призведе до **помилки компіляції**.
>
> Тернарний оператор є **виразом (expression)**, що означає, що він **повинен повернути значення**. Обидві його гілки (`вираз1` і `вираз2`) повинні бути виразами, що повертають значення.
>
> Метод, який оголошений як `void`, **не повертає ніякого значення**. Його виклик є **інструкцією (statement)**, а не виразом. Тому він не може бути частиною тернарного оператора.
>
> **Приклад коду, що не скомпілюється:**
>
> ```java
> public void start() { System.out.println("Start"); }
> public void stop() { System.out.println("Stop"); }
>
> public void control(boolean shouldStart) {
>     // ПОМИЛКА КОМПІЛЯЦІЇ: "void type not allowed here"
>     // shouldStart ? start() : stop();
> }
> ```
>
> Для виконання дій (виклику `void` методів) потрібно використовувати конструкцію `if-else`.

#### **📌 Що перевіряють:**

*Чітке розуміння, що таке "вираз, що повертає значення", та його відмінність від `void`.*

* * *

### **8 ❓ Питання:**

Що виведе цей код, і чи є тут проблема?

```java
Integer value = null;
String result = (value == null) ? "null" : value.toString();
```

> #### **💬 Відповідь:**
>
> Цей код **скомпілюється і виконається без помилок**.
>
> На екран буде виведено: **`null`**.
>
> **Чи є тут проблема?**
>
> У даному конкретному випадку — **ні, проблеми немає**. Це є поширеним і правильним ідіоматичним використанням тернарного оператора для безпечної обробки `null`.
>
> **Пояснення логіки:**
>
> 1. Перевіряється умова `value == null`.
> 2. Оскільки змінна `value` дійсно `null`, умова повертає `true`.
> 3. Тернарний оператор вибирає перший вираз: `"null"`.
> 4. Це значення присвоюється змінній `result`.
> 5. `value.toString()` (друга частина) **ніколи не виконується**, тому `NullPointerException` не виникає.
>
> Цей підхід є компактним та безпечним способом надати значення за замовчуванням, якщо об'єкт `null`.

#### **📝 Коментар:**

Це приклад правильного та корисного використання тернарного оператора для захисту від NPE.

#### **📌 Що перевіряють:**

*Здатність проаналізувати потік виконання та переконатися, що небезпечний код не буде виконано. Розуміння ідіом мови.*

* * *

### **9 ❓ Питання:**

Що таке вкладені тернарні оператори? Наведіть приклад і поясніть, чому їх використання не завжди є гарною ідеєю. ⭐️

> #### **💬 Відповідь:**
>
> **Вкладений тернарний оператор** — це тернарний оператор, в якому одна або обидві гілки (`вираз1` або `вираз2`) також є тернарними операторами.
>
> Це дозволяє реалізувати логіку, подібну до `if-else-if-else`.
>
> **Приклад (визначення знака числа):**
>
> ```java
> int number = -10;
> String sign = (number > 0) ? "positive" :
>               (number < 0) ? "negative" : "zero";
>
> System.out.println(sign); // Виведе "negative"
> ```
>
> **Чому це не завжди гарна ідея:**
>
> Головний недолік — **погана читабельність**. Навіть один рівень вкладеності, як у прикладі вище, може вимагати додаткових зусиль для розуміння. З двома або трьома рівнями вкладеності код стає майже неможливим для швидкого аналізу, і його легко зламати при змінах.
>
> Наприклад:
> `int result = a > 0 ? (b > 0 ? 1 : 2) : (c > 0 ? 3 : 4);`
>
> Такий код значно важче читати, ніж еквівалентну конструкцію з `if-else`. Читабельність і простота обслуговування коду зазвичай важливіші за максимальну компактність.
>
> **Висновок:** Один рівень вкладеності може бути прийнятним для дуже простої логіки. Більше одного рівня — це майже завжди ознака поганого стилю, і такий код слід рефакторити з використанням `if-else-if`.

#### **📌 Що перевіряють:**

*Принципи написання чистого коду (clean code). Кандидат повинен усвідомлювати, що можливості мови потрібно використовувати з розумом.*

* * *

### **10 ❓ Питання:**

Що виведе цей код і чому?
`System.out.println(true ? "A" : "B" + "C");`
`System.out.println(false ? "A" : "B" + "C");` ⭐️

> #### **💬 Відповідь:**
>
> Результат може бути неочевидним через пріоритет операторів.
>
> **Вивід буде таким:**
>
> ```
> A
> BC
> ```
>
> **Пояснення:**
>
> **Тернарний оператор (`?:`) має нижчий пріоритет**, ніж оператор додавання (`+`). Однак його структура `умова ? вираз1 : вираз2` є цілісною.
>
> 1. `System.out.println(true ? "A" : "B" + "C");`
>     * Спочатку обробляється весь тернарний оператор.
>     * Умова `true` істинна.
>     * Оператор повертає перший вираз — `"A"`.
>     * Вираз `"B" + "C"` **ніколи не обчислюється**.
>     * На друк іде `"A"`.
>
> 2. `System.out.println(false ? "A" : "B" + "C");`
>     * Спочатку обробляється весь тернарний оператор.
>     * Умова `false` хибна.
>     * Оператор повертає другий вираз — `"B" + "C"`.
>     * Тепер обчислюється вираз `"B" + "C"`, який дає результат `"BC"`.
>     * На друк іде `"BC"`.
>
> Пріоритет операторів тут вплинув би, якби вираз був, наприклад, `(умова ? a : b) + c`.

#### **📌 Що перевіряють:**

*Знання пріоритету операторів та правил обчислення виразів у Java.*

* * *

### **11 ❓ Питання:**

Що станеться при спробі використати тернарний оператор для присвоєння `null` примітивній змінній?

```java
int value = (condition) ? 10 : null;
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код призведе до **`NullPointerException`**, якщо умова `condition` буде `false`. Це один із підступних випадків, де Unboxing призводить до помилки.
>
> **Пояснення механізму:**
>
> 1. Компілятор аналізує два можливі результати: `int` (для `10`) та тип `null`.
> 2. Щоб узгодити типи, він визначає, що спільним типом має бути клас-обгортка `Integer`.
> 3. Отже, `10` буде автоматично **упаковано (Autoboxing)** в `Integer.valueOf(10)`.
> 4. Тепер тернарний оператор працює з двома об'єктами: `Integer` та `null`. Тип всього виразу стає `Integer`.
> 5. Але результат потрібно присвоїти змінній примітивного типу `int`. Це вимагає **розпаковки (Unboxing)**.
>
> **Що відбувається, якщо `condition` є `false`:**
>
> * Тернарний оператор повертає `null`.
> * JVM намагається розпакувати цей `null` в `int` (викликати `.intValue()`).
> * Це негайно призводить до `NullPointerException`.
>
> Ця ситуація є гарним прикладом того, як ланцюжок неявних перетворень (boxing → unboxing) може приховати потенційну помилку.

#### **📌 Що перевіряють:**

*Глибоке розуміння взаємодії тернарного оператора, автоупаковки/розпаковки та обробки `null`.*

* * *

### **12 ❓ Питання:**

Як тернарний оператор може бути пов'язаний з типом `void` через лямбда-вирази? ⭐️

> #### **💬 Відповідь:**
>
> Хоча `void` методи безпосередньо використовувати не можна, ситуація стає цікавішою з функціональними інтерфейсами, які представляють `void` методи, як-от `Runnable`.
>
> Справа в тому, що лямбда-вираз, який нічого не повертає (наприклад, `() -> System.out.println("Hi")`), може бути присвоєний змінній функціонального інтерфейсу, метод якого повертає об'єкт.
>
> **Приклад:**
>
> ```java
> // Callable<Void> очікує, що метод call() поверне Void
> Callable<Void> callable = isTrue ?
>     () -> { System.out.println("True"); return null; } :
>     () -> { System.out.println("False"); return null; };
>
> callable.call();
> ```
>
> Тут ми змушені явно повертати `null`, тому що сигнатура `Callable` вимагає повернення значення.
>
> **Але унікальна ситуація виникає, коли обидві гілки тернарного оператора — це методи, які повертають однакові значення (хоч і через побічний ефект).**
>
> ```java
> // ПОМИЛКА КОМПІЛЯЦІЇ. Обидві гілки повинні повертати значення.
> // Object obj = someCondition ? System.out.println("A") : System.out.println("B");
> ```
>
> Таким чином, навіть через лямбди тернарний оператор залишається виразом, який **вимагає повернення значення** з кожної гілки. `void`-сумісність досягається поверненням `null` у функціональних інтерфейсах.

#### **📌 Що перевіряють:**

*Розуміння того, як лямбди та функціональні інтерфейси взаємодіють з іншими конструкціями мови.*

* * *

### **13 ❓ Питання:**

Чи можна використовувати `break`, `continue` або `return` всередині тернарного оператора? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> **Причина:**
> `break`, `continue` та `return` — це **керуючі інструкції (control flow statements)**. Вони не повертають значення, а змінюють потік виконання програми (виходять з циклу, переходять до наступної ітерації, виходять з методу).
>
> Тернарний оператор, як було сказано раніше, є **виразом (expression)**. Його гілки повинні бути виразами, що обчислюються і повертають значення.
>
> **Спроба використати ці інструкції призведе до помилки компіляції.**
>
> ```java
> for (int i = 0; i < 10; i++) {
>     // ПОМИЛКА КОМПІЛЯЦІЇ
>     // int x = (i < 5) ? i : break;
> }
> ```
>
> Вони просто синтаксично несумісні. Тернарний оператор призначений для **даних**, а `if-else` — для керування **потоком**.

#### **📌 Що перевіряють:**

*Фундаментальну різницю між виразами (expressions) та інструкціями (statements) у Java. Це питання-пастка, яке перевіряє базові концепції мови.*

* * *

### **14 ❓ Питання:**

Які переваги та недоліки асоціативності тернарного оператора справа наліво (right-to-left associativity)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Асоціативність справа наліво** означає, що вкладені тернарні оператори групуються справа наліво.
>
> Тобто, вираз `a ? b : c ? d : e` інтерпретується як `a ? b : (c ? d : e)`, а не як `(a ? b : c) ? d : e`.
>
> **Переваги:**
>
> * **Логічна структура:** Така асоціативність дозволяє створювати ланцюжки, які є природною заміною для конструкції `if-else if-else`.
>
>     ```java
>     // if (a) { b } else if (c) { d } else { e }
>     a ? b : c ? d : e;
>     ```
>
>     Це дозволяє лаконічно записувати послідовні перевірки.
>
> **Недоліки:**
>
> * **Погана читабельність:** Головний недолік. Хоча компілятор розуміє це однозначно, для людини такий код може бути складним для аналізу. Без дужок легко заплутатися, що до чого відноситься.
> * **Високий ризик помилок:** При зміні або рефакторингу такого коду дуже легко зробити помилку, яка буде синтаксично правильною, але логічно невірною.
>
> **Приклад:**
>
> Припустимо, ми хочемо реалізувати логіку:
>
> ```
> if (score > 90) "A" else "Not A"
> ```
>
> І хтось "розширює" це:
> `score > 90 ? "A" : "Not A" == "Not A" ? "Fail" : "Pass"`
>
> Через асоціативність та пріоритет операторів, це буде працювати зовсім не так, як очікувалося.
>
> **Висновок:** Хоча асоціативність справа наліво є логічно послідовною, її практичні недоліки в читабельності та підтримці коду переважують будь-які переваги в компактності. Завжди краще використовувати дужки для явної вказівки порядку або просто перейти на `if-else`.

#### **📌 Що перевіряють:**

*Експертне знання синтаксису мови, включаючи такі деталі, як асоціативність операторів. Здатність оцінити, як синтаксичні правила впливають на читабельність коду.*

* * *

### **15 ❓ Питання:**

Напишіть код для присвоєння змінній `х` значення 10, використовуючи тернарний оператор, але так, щоб це виглядало максимально заплутано або неочевидно. Поясніть, чому він працює. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ось приклад, що демонструє складну взаємодію приведення типів та unboxing:
>
> ```java
> public static void main(String[] args) {
>     char a = 'X'; // ASCII-код 88
>     Double b = 10.0;
>     Object obj = true ? a : b;
>
>     Integer x = (obj instanceof Character) ?
>         (int)(Character)obj - 78 :
>         ((Double)obj).intValue();
>
>     System.out.println(x);
> }
> ```
>
> **Результат виконання:** `10`
>
> **Пояснення, чому це працює:**
>
> 1. `Object obj = true ? a : b;`
>     * Розглядаються типи `char` ('X') та `Double` (10.0).
>     * Відбувається **двійкове числове розширення**. `char` розпаковується до `int` (88), потім розширюється до `double` (88.0).
>     * Типом всього тернарного виразу стає `Double`.
>     * Оскільки умова `true` істинна, вибирається `char 'X'`, але для відповідності типу він перетворюється на `Double` зі значенням `88.0`.
>     * Отже, `obj` містить `Double` об'єкт зі значенням `88.0`.
>
> 2. `Integer x = ...`
>     * Перевіряється умова `obj instanceof Character`. Оскільки `obj` є `Double`, ця умова **хибна**.
>     * Виконується друга гілка тернарного оператора: `((Double)obj).intValue()`.
>     * Об'єкт `obj` приводиться до `Double`, з нього витягується `double` значення `88.0`, і потім викликається `intValue()`, який відкидає дробову частину, повертаючи `int` `88`.
>     * Тут мала бути помилка в моєму заплутаному коді. Давайте виправимо, щоб отримати 10.
>
> **Виправлений та більш заплутаний приклад:**
>
> ```java
> // мета - отримати 10
> Number value = true ? new Integer(10) : new Double(20.0);
> // через розширення типів value стане Double(10.0)
>
> Integer x = value instanceof Double && value.doubleValue() < 15.0 ?
>             (int)value.doubleValue() : 0;
> System.out.println(x); // виведе 10
> ```
>
> Тут ми знову використовуємо numeric promotion, щоб `value` стало `Double(10.0)`, а потім перевіряємо його тип і значення, щоб виконати потрібну гілку і привести `double` до `int`.
>
> Цей приклад показує, як неявні перетворення типів, боксинг та робота з ієрархією класів (`Number`, `Double`, `Integer`) можуть зробити простий, на перший погляд, код дуже складним для розуміння.

#### **📌 Що перевіряють:**

*Глибоке, експертне розуміння правил приведення типів (Type Promotion Rules) у Java, а також творчий підхід. Це питання на перевірку, наскільки глибоко кандидат знає "підводні камені" мови.*
