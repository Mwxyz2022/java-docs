---
layout: default
title: "Байткод у Java"
parent: "Основи Java: Memory, Wrappers, String"
grand_parent: "Java Core"
nav_order: 2
---

# Байткод у Java

* короткий опис: Байткод – це проміжне представлення Java-програми (`.class` файл), створене компілятором `javac` для виконання на JVM, що забезпечує платформонезалежність.

* поняття: Байткод, `.class` файл, `javac`, JVM.

* приклади: Процес компіляції (`javac`) та виконання (`java`) Java-файлу, що створює та запускає байткод.

* ключові моменти: Байткод є платформонезалежним кодом, що виконується JVM, дозволяючи принципу "Write Once, Run Anywhere".

* що важливо знати на співбесіді: Що таке байткод, навіщо він потрібен, його роль у кросплатформенності.

### **✅ Офіційне визначення:**

**Байткод (Bytecode)** — це проміжне представлення Java-програми у вигляді інструкцій для **Java Virtual Machine** `JVM`

Формально — це набір команд у **`.class`** файлі, який створюється компілятором **`javac`** із вихідного **`.java`** коду.

### **🧠 Простими словами:**

Java не перетворюється одразу у машинний код. Вона трансформується у байткод — універсальну мову, яку розуміє **`JVM`**

🎮 Уяви: байткод — це «універсальний картридж», а **`JVM`** — це «універсальна консоль», яка вміє запускати ці картриджі на будь-якій ОС

---

## 📊 **Стадії обробки Java-коду:**

|     Етап     |                       Опис                       |
|:------------:|:------------------------------------------------:|
| **`.java`**  |         Вихідний код, написаний людиною          |
| **`javac`**  | Компілятор перетворює **`.java`** -> **`.class`** |
| **`.class`** |       Містить байткод (інструкції для JVM)       |
|  **`java`**  |       JVM виконує байткод на конкретній ОС       |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

— Щоб один і той самий код міг запускатись на різних ОС без змін.

### **Особливість?**

— Байткод **незалежний від платформи**, на відміну від **`.exe`** чи **`.out`** файлів.

### **Типові граблі:**

* Забув скомпілювати після змін у **`.java`** — запускається стара версія **`.class`**.
* Несумісність **`.class`** файлів між версіями Java (наприклад, створив у Java 17 — не запускається на JVM 8).

### **Коли застосовувати?**

— Завжди. Уся Java завжди спочатку транслюється у байткод.

---

## **Корисні приклади (🧪)**

```java
// HelloWorld.java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Привіт, Java!");
    }
}
```

```java
//# Компіляція
javac HelloWorld.java  //# створює HelloWorld.class

//# Виконання
java HelloWorld       //  # JVM читає байткод із HelloWorld.class
```

**`javac`** створює байткод,
**`java`** — запускає його через JVM

---

## **Порівняння (⚖️)**

|     Параметр      |     Байткод (.class)      |   Машинний код (.exe/.out)    |
|:-----------------:|:-------------------------:|:-----------------------------:|
| **Незалежність**  | ✅ Працює на будь-якій JVM | ❌ Прив’язаний до ОС/процесора |
| **Виконується у** |            JVM            |     Безпосередньо на CPU      |
|  **Призначення**  |   Платформонезалежність   |  Висока швидкість виконання   |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
.java -> javac -> .class -> java -> JVM
```

🔁 Людський код -> Компіляція -> Байткод -> Виконання -> Результат

**📌 Фраза для запам’ятовування:**

**`"Java -> Class -> Run Everywhere" 🌍`**

---

## **🟩 Підсумок:**

* Байткод — це універсальна інструкція, яку розуміє JVM.
* Він генерується з **`.java`** файлу за допомогою компілятора **`javac`**.
* Зберігається у **`.class`** файлі.
* Саме завдяки байткоду Java має гасло: **`Write Once, Run Anywhere`**.
* Він забезпечує переносимість, безпечність і стабільність Java-додатків.

---

### **🔗 Додаткові джерела:**

* [**Java Bytecode у документації Oracle**](https://docs.oracle.com/javase/specs/)
* [**Bytecode Overview — Wikipedia**](https://en.wikipedia.org/wiki/Java_bytecode)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Java Байткод і яка його основна мета?

> #### **💬 Відповідь:**
>
> **Java Байткод** — це набір інструкцій, у який компілятор Java (`javac`) перетворює вихідний код, написаний програмістом (`.java` файл). Цей код зберігається у `.class` файлах і не є машинним кодом, зрозумілим процесору, а є проміжним, низькорівневим форматом, розробленим спеціально для виконання **Java Virtual Machine (JVM)**.
>
> **Основна мета байткоду** — забезпечити **платформонезалежність** Java. Завдяки тому, що Java-програми компілюються в єдиний універсальний байткод, їх можна запускати на будь-якій операційній системі (Windows, macOS, Linux) без перекомпіляції. Єдина вимога — на цій системі має бути встановлена відповідна реалізація JVM, яка "перекладе" цей байткод на мову інструкцій, зрозумілу локальному процесору.

#### **📝 Аналогія:**

Уявіть, що байткод — це універсальний рецепт страви, написаний спеціальною мовою для кухарів. JVM — це професійний шеф-кухар, який може працювати в будь-якій країні. Цей шеф-кухар бере універсальний рецепт (байткод) і готує страву, використовуючи місцеві продукти та обладнання (ресурси операційної системи). Рецепт один, а кухарі та кухні різні.

#### **📌 Що перевіряють:**

*Базове розуміння ключової концепції Java — платформної незалежності — і ролі байткоду в її реалізації.*

* * *

### **2 ❓ Питання:**

Як відбувається процес перетворення вихідного `.java` файлу на виконувану програму?

> #### **💬 Відповідь:**
>
> Процес складається з двох основних етапів: компіляції та виконання.
>
> 1. **Компіляція:**
>     * Розробник пише код у файлі з розширенням `.java` (наприклад, `MyProgram.java`).
>     * За допомогою **компілятора Java (`javac`)**, цей файл перетворюється на один або кілька файлів з розширенням `.class` (наприклад, `MyProgram.class`).
>     * Кожен `.class` файл містить **байткод** — інструкції для JVM.
>
> 2. **Виконання:**
>     * За допомогою **команди `java`**, ми запускаємо **JVM**.
>     * JVM завантажує `.class` файл, верифікує байткод (перевіряє його на безпечність) та починає його виконувати, інтерпретуючи або компілюючи "на льоту" (JIT) в машинний код.

#### **📝 Приклад:**

Процес у командному рядку:

```bash
# 1. Створюємо вихідний код у файлі HelloWorld.java
# (вміст файлу як у вашому конспекті)

# 2. Компілюємо .java файл у .class файл
# Ця команда створить файл HelloWorld.class
javac HelloWorld.java

# 3. Запускаємо програму, передаючи ім'я класу JVM
# JVM знайде HelloWorld.class, завантажить його і виконає метод main
java HelloWorld
```

#### **📌 Що перевіряють:**

*Практичне розуміння робочого циклу Java-розробника. Кандидат повинен чітко розрізняти етапи компіляції та виконання та інструменти, що використовуються (`javac` та `java`).*

* * *

### **3 ❓ Питання:**

Чим байткод принципово відрізняється від машинного коду?

> #### **💬 Відповідь:**
>
> Головні відмінності полягають у **платформеній залежності** та **середовищі виконання**.
>
> * **Машинний код (`.exe`, `.out`):**
>   * Це набір інструкцій, зрозумілий **конкретному типу процесора** (наприклад, x86-64 або ARM).
>   * Він є **платформозалежним**. Програма, скомпільована для Windows x64, не запуститься на macOS з ARM-процесором.
>   * Виконується безпосередньо центральним процесором (CPU), що робить його дуже швидким.
>
> * **Байткод (`.class`):**
>   * Це набір інструкцій, зрозумілий **віртуальній машині (JVM)**.
>   * Він є **платформонезалежним**. Один і той самий `.class` файл може виконуватися на будь-якій системі, де є JVM.
>   * Виконується всередині JVM, яка діє як проміжний шар-абстракція між програмою та операційною системою.

#### **📝 Коментар:**

Байткод є компромісом між швидкістю виконання машинного коду та портативністю інтерпретованих мов. Завдяки JIT-компіляції сучасні JVM можуть досягати продуктивності, близької до нативних програм.

#### **📌 Що перевіряють:**

*Чітке розуміння ключової архітектурної відмінності Java від компільованих мов на кшталт C++ чи Go, і чому Java отримала таку широку кросплатформеність.*

* * *

### **4 ❓ Питання:**

Чи можна сказати, що байткод — це те саме, що і `.class` файл?

> #### **💬 Відповідь:**
>
> Не зовсім, хоча ці поняття тісно пов'язані. Говорити так — це спрощення.
>
> * **Байткод** — це *набір інструкцій*, тобто сам код, який виконує JVM.
>
> * **`.class` файл** — це *контейнер*, структурований двійковий файл, який містить цей байткод. Окрім самого байткоду для методів, `.class` файл містить багато іншої **мета-інформації**:
>   * "Магічне число" `0xCAFEBABE` для ідентифікації.
>   * Версію формату `.class` файлу (версія JDK, якою його скомпільовано).
>   * **Пул констант (Constant Pool):** Таблиця всіх констант, імен класів, полів і методів, що використовуються.
>   * Опис самого класу, його батьківських класів та інтерфейсів.
>   * Описи полів та методів класу.
>
> Отже, **байткод є основною частиною вмісту `.class` файлу**, але не єдиною.

#### **📝 Коментар:**

Це питання на точність формулювань. Хороший кандидат розуміє, що `.class` файл — це структурований формат, який інкапсулює байткод та необхідні для його виконання метадані.

#### **📌 Що перевіряють:**

*Увагу до деталей і глибину розуміння структури артефактів Java. Кандидат повинен знати, що `.class` файл – це більше, ніж просто набір команд.*

* * *

### **5 ❓ Питання:**

Що таке `stack-based` архітектура JVM і як це пов'язано з байткодом?

> #### **💬 Відповідь:**
>
> Архітектура JVM є **стековою (stack-based)**. Це означає, що для виконання операцій вона використовує структуру даних, яка називається **стек операндів (operand stack)**. Кожен потік виконання має свій стек, де відбуваються всі обчислення.
>
> Це безпосередньо пов'язано з байткодом: інструкції байткоду (опкоди) призначені для маніпуляції цим стеком.
>
> Наприклад, щоб додати два числа, JVM виконає такі операції:
>
> 1. Інструкція `iload_1` кладе перше число (змінну) на вершину стека.
> 2. Інструкція `iload_2` кладе друге число на вершину стека.
> 3. Інструкція `iadd` бере два верхні елементи зі стека, додає їх і кладе результат назад на стек.
>
> На противагу цьому, багато фізичних процесорів мають **регістрову (register-based)** архітектуру, де дані перед обробкою завантажуються в невеликий набір швидких регістрів. Стекова архітектура робить інструкції байткоду більш компактними та простішими, що сприяє платформній незалежності.

#### **📝 Коментар:**

Розуміння стекової природи JVM є ключем до аналізу байткоду. Це пояснює, чому команди виглядають як `load`, `store`, `add`, `pop` — усі вони маніпулюють даними у стеку.

#### **📌 Що перевіряють:**

*Розуміння низькорівневих аспектів роботи JVM. Це показує, що кандидат не просто пише код, а має уявлення про модель виконання цього коду.*

* * *

### **6 ❓ Питання:**

Чи є байткод людськочитаним?

> #### **💬 Відповідь:**
>
> **Ні, безпосередньо байткод у `.class` файлі не є людськочитаним.** Це бінарний формат. Якщо відкрити `.class` файл у текстовому редакторі, ви побачите набір незрозумілих символів.
>
> Однак його можна **дизасемблювати** у людськочитаний вигляд за допомогою спеціальних інструментів. Стандартною утилітою, що входить до складу JDK, є `javap` (Java Disassembler). Вона перетворює бінарні опкоди байткоду на текстові мнемоніки, такі як `iload`, `iadd`, `invokevirtual`.

#### **📝 Приклад:**

```bash
# Дизасемблюємо скомпільований клас HelloWorld
javap -c HelloWorld
```

Результат буде приблизно таким:

```java
// ...
public static void main(java.lang.String[]);
    Code:
       0: getstatic     #2                  // Field java/lang/System.out:Ljava/io/PrintStream;
       3: ldc           #3                  // String Привіт, Java!
       5: invokevirtual #4                  // Method java/io/PrintStream.println:(Ljava/lang/String;)V
       8: return
// ...
```

Це вже можна аналізувати, щоб зрозуміти, які саме інструкції виконує JVM.

#### **📌 Що перевіряють:**

*Знання інструментарію JDK та практичних способів аналізу роботи Java-програм на низькому рівні.*

* * *

### **7 ❓ Питання:**

Чому байткод вважається більш безпечним, ніж машинний код?

> #### **💬 Відповідь:**
>
> Безпека байткоду забезпечується завдяки контролю з боку **JVM**. Перш ніж виконати будь-який код, JVM проводить його через **верифікатор байткоду (Bytecode Verifier)**.
>
> Верифікатор виконує кілька перевірок:
>
> * **Коректність форматів:** Перевіряє, чи `.class` файл має правильну структуру.
> * **Переповнення/спустошення стека:** Гарантує, що операції з байткодом не призведуть до помилок стека операндів.
> * **Типізація:** Слідкує за тим, щоб змінні використовувалися згідно з їхніми типами (наприклад, не можна ціле число використовувати як посилання на об'єкт).
> * **Контроль доступу:** Забороняє коду напряму маніпулювати пам'яттю (наприклад, через вказівники, як у C++) або отримувати несанкціонований доступ до приватних полів інших класів.
>
> Оскільки байткод виконується у "пісочниці" JVM, він ізольований від прямого доступу до ресурсів операційної системи. Машинний код, навпаки, виконується напряму і може робити практично все, що дозволено користувачу.

#### **📝 Коментар:**

Це одна з ключових причин, чому Java стала популярною для мережевих додатків та аплетів на зорі інтернету. Верифікатор був гарантією того, що код, завантажений з неперевіреного джерела, не зможе зашкодити системі.

#### **📌 Що перевіряють:**

*Розуміння вбудованих у платформу Java механізмів безпеки та ролі JVM як захисного шару.*

* * *

### **8 ❓ Питання:**

Чи буде `.class` файл, скомпільований у Java 17, працювати на JVM для Java 8? Чому? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не буде.** Java підтримує **зворотну сумісність**, але не **пряму сумісність**.
>
> * **Зворотна сумісність:** Код, скомпільований на старій версії Java (наприклад, Java 8), буде працювати на новій JVM (наприклад, Java 17).
> * **Пряма сумісність:** Код, скомпільований на новій версії Java (17), **не буде** працювати на старій JVM (8).
>
> **Причина** полягає у версії формату `.class` файлу. Кожен JDK генерує байткод, позначений певною версією. JVM вміє читати байткод своєї версії та всіх попередніх, але не майбутніх.
>
> При спробі запустити такий файл на старій JVM ви отримаєте помилку `java.lang.UnsupportedClassVersionError`. У повідомленні буде вказано, що клас було скомпільовано новішою версією Java, ніж та, яку підтримує середовище виконання.

#### **📝 Приклад:**

Щоб скомпілювати код на Java 17, але зробити його сумісним з JVM 8, можна використовувати прапори компілятора:

```bash
# Компільєумо код так, щоб він міг запускатись на JVM версії 8
javac --release 8 MyClass.java
```

#### **📌 Що перевіряють:**

*Розуміння практичних аспектів версійності в Java. Це типова проблема, з якою стикаються розробники у великих проектах з різними середовищами.*

* * *

### **9 ❓ Питання:**

Що таке `Constant Pool` (пул констант) у байткоді і яка його роль? ⭐️

> #### **💬 Відповідь:**
>
> **Пул констант (Constant Pool)** — це одна з найважливіших частин `.class` файлу. Це таблиця-довідник, яка зберігає всі літерали, імена та символічні посилання, що використовуються в класі.
>
> У ньому зберігаються:
>
> * **Числові та рядкові літерали:** `123`, `3.14f`, `"Hello, World!"`.
> * **Символічні посилання на класи та інтерфейси:** Наприклад, ім'я класу `java.lang.String`.
> * **Символічні посилання на поля:** Імена та типи полів.
> * **Символічні посилання на методи:** Імена, сигнатури (параметри та тип повернення) методів.
>
> **Роль пулу констант** полягає в тому, щоб зробити байткод компактним і полегшити процес зв'язування (linking) під час завантаження класу. Замість того, щоб вбудовувати повні імена методів або рядки прямо в інструкції, байткод містить лише індекси (посилання) на відповідні записи в пулі констант. JVM під час виконання знаходить потрібні дані за цими індексами.

#### **📝 Коментар:**

Це схоже на зміст у книзі. Замість того, щоб повторювати повну назву розділу кожного разу, ви просто посилаєтесь на номер сторінки. Пул констант є "змістом" для `.class` файлу.

#### **📌 Що перевіряють:**

*Більш глибокі знання про внутрішню структуру `.class` файлів. Це свідчить про те, що кандидат цікавився тим, як Java працює "під капотом".*

* * *

### **10 ❓ Питання:**

Що таке опкоди байткоду? Наведіть кілька прикладів. ⭐️

> #### **💬 Відповідь:**
>
> **Опкод (код операції)** — це основна одиниця інструкції байткоду, яка зазвичай займає один байт. Кожен опкод вказує JVM, яку операцію потрібно виконати. На сьогодні існує близько 200 різних опкодів.
>
> Опкоди можна згрупувати за їхнім призначенням:
>
> * **Завантаження і збереження (Load/Store):**
>   * `iload_0`: Завантажити цілочисельну змінну з локального масиву змінних (індекс 0) на стек операндів.
>   * `astore_1`: Зберегти посилання на об'єкт з вершини стека в локальну змінну (індекс 1).
>
> * **Арифметичні операції:**
>   * `iadd`: Додати два цілих числа з вершини стека.
>   * `fmul`: Помножити два числа з плаваючою комою (float) з вершини стека.
>
> * **Виклик методів:**
>   * `invokevirtual`: Виклик екземплярного методу (найпоширеніший виклик).
>   * `invokestatic`: Виклик статичного методу.
>
> * **Створення об'єктів:**
>   * `new`: Створити новий об'єкт.

#### **📝 Коментар:**

На співбесіді не вимагають знати всі опкоди напам'ять. Важливо розуміти сам принцип: байткод складається з однобайтових команд, які описують низькорівневі операції зі стеком, локальними змінними та пулом констант.

#### **📌 Що перевіряють:**

*Ступінь заглиблення у внутрішню реалізацію мови. Здатність навести кілька прикладів опкодів показує, що кандидат не просто чув про байткод, а має уявлення про його структуру.*

* * *

### **11 ❓ Питання:**

Якщо байткод інтерпретується, чому кажуть, що Java є швидкою мовою? ⭐️

> #### **💬 Відповідь:**
>
> Стверджувати, що байткод *тільки* інтерпретується, некоректно для сучасних JVM. Сучасні JVM (як-от HotSpot) використовують гібридний підхід:
>
> 1. **Інтерпретація:** Спочатку, коли програма запускається, JVM виконує байткод за допомогою **інтерпретатора**. Це дозволяє програмі швидко стартувати, оскільки не витрачається час на компіляцію.
>
> 2. **Профілювання:** Під час інтерпретації, JVM збирає статистику про те, які методи та цикли виконуються найчастіше. Ці частини коду називаються **"гарячими точками" (hotspots)**.
>
> 3. **JIT-компіляція (Just-In-Time):** Коли певна "гаряча точка" досягає порогового значення викликів, **JIT-компілятор** компілює її байткод у високооптимізований **нативний машинний код** для цільової платформи.
>
> Усі подальші виклики цієї частини коду будуть виконувати вже скомпільований машинний код, що значно швидше за інтерпретацію. Ця фаза "розігріву" є причиною, чому довготривалі Java-додатки з часом починають працювати дуже швидко, досягаючи продуктивності, порівнянної з C++.

#### **📝 Коментар:**

Саме через це Java іноді повільно "стартує", але потім демонструє чудову продуктивність. Вона динамічно адаптується до навантаження під час виконання.

#### **📌 Що перевіряють:**

*Розуміння сучасних механізмів оптимізації в JVM. Кандидат повинен знати про JIT-компіляцію та її роль у підвищенні продуктивності Java-додатків.*

* * *

### **12 ❓ Питання:**

Чи зберігає байткод імена локальних змінних і коментарі з вихідного коду? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від параметрів компіляції:
>
> * **Коментарі:** **Ні, ніколи.** Компілятор `javac` повністю ігнорує і видаляє всі коментарі з вихідного коду (`//` та `/* ... */`). Вони не потрапляють у байткод і призначені виключно для розробників.
>
> * **Імена локальних змінних:** **За замовчуванням — так.** Сучасні компілятори Java (`javac`) включають у `.class` файл спеціальну таблицю з іменами локальних змінних (Local Variable Table). Це робиться в першу чергу для зручності **налагодження (дебагу)**. Налагоджувач використовує цю інформацію, щоб показувати вам імена змінних під час покрокового виконання.
>
> Однак цю інформацію можна видалити, скомпілювавши код зі спеціальним прапором `javac -g:none MyClass.java`. В такому випадку в байткоді залишаться тільки типи змінних та їхні індекси, але не імена. Це трохи зменшить розмір `.class` файлу, але ускладнить налагодження.

#### **📝 Коментар:**

Це хороше питання, щоб перевірити практичний досвід кандидата. Розуміння того, яка інформація з коду "доживає" до байткоду, важливе для налагодження та аналізу продуктивності.

#### **📌 Що перевіряють:**

*Знання того, яка саме інформація з вихідного коду є важливою для виконання/налагодження, а яка — лише для людини. Показує досвід роботи з налагоджувачами.*

* * *

### **13 ❓ Питання:**

Чи можна створити або змінити байткод безпосередньо, оминувши `.java` файл і компілятор `javac`? Якщо так, то як і навіщо? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Це називається **програмною маніпуляцією байткодом**. Це просунута техніка, яка використовується для модифікації `.class` файлів на льоту (під час завантаження) або після компіляції.
>
> **Як це зробити?**
> Існують спеціалізовані бібліотеки для роботи з байткодом:
>
> * **ASM:** Низькорівнева, дуже швидка і потужна бібліотека. Вимагає глибокого розуміння структури байткоду.
> * **Javassist:** Високорівнева бібліотека, яка надає простіший API, дозволяючи змінювати код, використовуючи синтаксис, схожий на Java.
> * **Byte Buddy:** Сучасна бібліотека, яка вважається золотим стандартом. Її API дуже гнучкий і потужний.
>
> **Навіщо це потрібно?**
> Це використовується у багатьох фреймворках та інструментах для реалізації функціоналу, який важко або неможливо реалізувати на рівні мови Java:
>
> * **AOP (Аспектно-орієнтоване програмування):** Фреймворки як Spring AOP та AspectJ додають логіку (наприклад, логування, транзакції) у методи класів, змінюючи їх байткод.
> * **ORM-фреймворки (Hibernate):** Реалізують механізм "лінивої" ініціалізації (`lazy loading`) шляхом створення проксі-класів на льоту.
> * **Інструменти для моніторингу (APM):** Агенти на кшталт `Java Agent` динамічно додають у код вимірювання часу виконання методів.
> * **Моking-фреймворки (Mockito):** Створюють "заглушки" (моки) для класів під час тестування.

#### **📌 Що перевіряють:**

*Знання просунутих технік та екосистеми Java. Це питання відрізняє досвідченого розробника, який розуміє, як працюють сучасні фреймворки "під капотом", від того, хто просто ними користується.*

* * *

### **14 ❓ Питання:**

Припустимо, ви аналізуєте байткод і бачите інструкції `invokevirtual`, `invokespecial`, `invokestatic` та `invokedynamic`. Поясніть різницю між ними. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ці чотири інструкції відповідають за різні типи викликів методів і є ключовими для розуміння поліморфізму та динамічності в Java.
>
> * **`invokevirtual`:**
>   * Це найпоширеніша інструкція. Вона використовується для виклику **публічних екземплярних (нестатичних) методів**.
>   * Це динамічний виклик. Конкретна реалізація методу, яка буде викликана, визначається під час виконання (runtime) на основі **фактичного типу об'єкта**, а не типу посилання. Це і є основа поліморфізму.
>
> * **`invokestatic`:**
>   * Використовується для виклику **статичних методів**.
>   * Виклик є статичним, оскільки реалізація методу відома вже на етапі компіляції (завжди викликається метод певного класу). Він не залежить від екземпляра об'єкта.
>
> * **`invokespecial`:**
>   * Використовується для особливих випадків:
>         1. Виклик **приватних методів** екземпляра.
>         2. Виклик **конструкторів** (`<init>`).
>         3. Виклик методів батьківського класу (через `super`).
>   * Виклик також є статично зв'язаним, оскільки компілятор точно знає, який метод потрібно викликати.
>
> * **`invokedynamic`:**
>   * Найновіша і найскладніша інструкція, додана в Java 7.
>   * Вона дозволяє відкласти зв'язування виклику до самого моменту виконання. Спочатку була введена для підтримки динамічних мов на JVM (JRuby, Jython).
>   * Сьогодні вона є основою для реалізації **лямбда-виразів** у Java 8 та новіших версіях. Вона дозволяє JVM обирати найефективнішу стратегію реалізації лямбди під час виконання.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як різні концепції мови Java (поліморфізм, статичні методи, конструктори, лямбди) транслюються у конкретні інструкції байткоду. Це питання для Senior-рівня.*

* * *

### **15 ❓ Питання:**

Поясніть, що відбувається під час етапу **верифікації байткоду**. Наведіть приклад атаки, якій може запобігти верифікатор. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Верифікація байткоду** — це критично важливий етап безпеки, який відбувається, коли ClassLoader завантажує `.class` файл, але перед тим, як він буде виконаний. Мета верифікатора — переконатися, що байткод є коректним, безпечним і не порушить стабільність JVM.
>
> Процес включає чотири основні стадії перевірок:
>
> 1. **Структурна перевірка:** Перевірка базової структури `.class` файлу (наявність `CAFEBABE`, коректність пулу констант і т.д.).
> 2. **Типова перевірка:** Аналіз потоку коду без його виконання. Верифікатор перевіряє кожну інструкцію, моделюючи стан стека операндів, і гарантує, що типи аргументів завжди відповідають тому, що очікує інструкція.
> 3. **Зв'язування:** Перевірка коректності символічних посилань на класи, поля та методи.
> 4. **Під час виконання:** Деякі перевірки (наприклад, тип об'єкта при `invokevirtual`) відбуваються вже в runtime.
>
> **Приклад атаки, якій запобігає верифікатор: переповнення стека операндів.**
>
> Уявіть, що зловмисник вручну створює шкідливий `.class` файл. Його мета — викликати метод, який очікує один параметр (наприклад, `int`), але перед цим покласти на стек два параметри (наприклад, посилання на об'єкт і `int`).
>
> * **Зловмисний байткод (псевдокод):**
>     `aload_0 // покласти на стек`this`
>     `iconst_5 // покласти на стек `5`
>     `invokevirtual someMethod(I)V` // викликати метод, що чекає один int.
>
> * **Що робить верифікатор:**
>     Під час аналізу потоку коду верифікатор побачить, що перед викликом `invokevirtual` на стеку лежить два значення (`this` і `5`), тоді як метод `someMethod` очікує лише одне. Це є порушенням цілісності стека. Верифікатор відхилить такий клас із помилкою `java.lang.VerifyError`.
>
> Якби цієї перевірки не було, зайве посилання на об'єкт могло б бути інтерпретоване як частина адреси повернення, що потенційно дозволило б виконати довільний код.

#### **📌 Що перевіряють:**

*Експертне розуміння механізмів безпеки платформи Java. Кандидат повинен не просто знати, що верифікатор існує, але й розуміти, як він працює на концептуальному рівні та які конкретні загрози він нейтралізує.*
