---
layout: default
title: "Статичні поля та методи (static)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 21
---

# Статичні поля та методи (static)

* короткий опис: Ключове слово `static` означає, що член (поле або метод) належить класу, а не конкретному екземпляру, і є спільним для всіх об'єктів.

* поняття: `static`, поле, метод, блок, вкладений клас.

* приклади: `static int count;`, `static void printInfo();`, `static {}`.

* ключові моменти: Належить класу, викликається без створення об'єкта, не має доступу до `this`. Використовується для спільних ресурсів, утиліт.

* що важливо знати на співбесіді: Різниця між `static` та instance членами, коли використовувати.

### **✅ Офіційне визначення:**

Ключове слово **`static`** в Java означає, що поле або метод належить класу, а не конкретному об’єкту. Такі члени існують незалежно від екземплярів класу й спільні для всіх об’єктів.

### **🧠 Простими словами:**

**`static`** — це як спільна змінна або метод для всього класу. Уяви клас **`Car`**, у якого є лічильник усіх створених машин — **`static`** `int count`. Він буде один на всіх машин.

---

## **Основне**

| Елемент класу | Можна зробити static | Приклад |
| :---: | :---: | :---: |
| **Поле** | ✅ | **`static int count;`** |
| **Метод** | ✅ | **`static void printInfo()`** |
| **Блок ініціалізації** | ✅ | **`static { ... }`** |
| **Вкладений клас** | ✅ | **`static class Inner { ... }`** |
| **Конструктор** | ❌ | *Java не дозволяє static конструктори* |

---

## **Пояснення на пальцях (💡)**

* Звичайне поле — у кожного об’єкта своє.
* Статичне поле — одне для всіх. Якщо один об’єкт змінив значення — це побачать усі.
* Статичний метод — викликається без створення об’єкта, напряму через ім’я класу.

---

## **Корисні приклади (🧪)**

🔹 **Статичне поле та метод:**

```java
public class Counter {
    static int count = 0;

    public Counter() {
        count++;
    }

    static void printCount() {
        System.out.println("Total objects: " + count);
    }
}

Counter c1 = new Counter();
Counter c2 = new Counter();

Counter.printCount(); // Виведе: Total objects: 2
```

🔹 **Використання в утилітарному класі:**

```java
public class MathUtils {
    public static int square(int x) {
        return x * x;
    }
}

int result = MathUtils.square(5); // 25
```

🔹 **Статичний блок (одноразова ініціалізація):**

```java
class App {
    static {
        System.out.println("App loaded!");
    }
}
```

---

## **Порівняння (⚖️)**

| Властивість | Звичайне поле/метод | Статичне поле/метод (static) |
| :---: | :---: | :---: |
| **Прив’язка** | До об’єкта | До класу |
| **Існування** | Коли створено об’єкт | Після завантаження класу |
| **Виклик** | Через об’єкт | Через ім’я класу |
| **Контекст доступу** | Має доступ до **`this`** | ❌ Немає доступу до **`this`** / нестатичних полів |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘  
**`static = спільне = доступне напряму через клас`**  
**`static int count`** — один лічильник на всіх

**`static void main`** — головний метод без об’єкта

---

## **🟩 Підсумок:**

Ключове слово **`static`** дозволяє створювати спільні для всіх об’єктів поля та методи. Воно широко використовується для утилітарних функцій, лічильників, фабрик, констант. Статичні методи не мають доступу до нестатичних частин класу і викликаються напряму через ім’я класу — це дуже потужний інструмент у Java.

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Java: static**](https://docs.oracle.com/javase/tutorial/java/javaOO/classvars.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому фундаментальна різниця між `static` (статичним) полем та `instance` (не-статичним) полем?

> #### **💬 Відповідь:**
>
> Фундаментальна різниця полягає в **приналежності**, **кількості екземплярів** та **життєвому циклі**.
>
> **Instance (не-статичне) поле:**
>
> * **Приналежність:** Належить **конкретному екземпляру (об'єкту)** класу.
> * **Кількість екземплярів:** Кожен об'єкт має **свою власну копію** цього поля. Зміна значення в одному об'єкті не впливає на інші.
> * **Життєвий цикл:** Створюється разом з об'єктом і знищується, коли об'єкт стає доступним для збирача сміття.
>
> **Static (статичне) поле:**
>
> * **Приналежність:** Належить **самому класу**, а не його об'єктам.
> * **Кількість екземплярів:** Існує в **єдиному екземплярі**, який є **спільним для всіх об'єктів** цього класу. Якщо один об'єкт змінює його значення, ця зміна видима для всіх інших.
> * **Життєвий цикл:** Створюється при завантаженні класу в JVM і існує доти, доки клас не буде вивантажено (що трапляється дуже рідко).

#### **📝 Аналогія:**

* **Instance поле (`String name` у класі `Student`):** це як особистий щоденник студента. Кожен має свій.
* **Static поле (`String universityName` у класі `Student`):** це як назва університету, написана на дошці оголошень. Вона одна для всіх студентів цього навчального закладу.

#### **📌 Що перевіряють:**

*Розуміння базової концепції рівня класу та рівня об'єкта. Це основа ООП.*

* * *

### **2 ❓ Питання:**

Чому статичні методи не можуть звертатися до не-статичних полів або викликати не-статичні методи напряму?

> #### **💬 Відповідь:**
>
> Вони не можуть цього робити, тому що **статичний контекст не прив'язаний до конкретного екземпляра (об'єкта)**.
>
> **Пояснення:**
>
> 1. **Не-статичні** поля та методи **належать об'єкту**. Щоб звернутися до `instanceField` або викликати `instanceMethod()`, потрібно знати, **про який саме об'єкт** іде мова. Ця інформація надається через неявне посилання `this`.
>
> 2. **Статичні** методи та блоки належать **класу**. Вони існують незалежно від того, чи було створено хоча б один об'єкт. В статичному контексті **немає посилання `this`**.
>
> Оскільки статичний метод не знає, для **якого саме екземпляра** викликати не-статичний метод (для `obj1`? `obj2`? чи якогось іншого?), компілятор забороняє такий прямий виклик.
>
> **Як це можна зробити опосередковано:**
> Потрібно явно створити об'єкт і звернутися до його членів через посилання.
>
> ```java
> public static void staticMethod() {
>     MyClass instance = new MyClass();
>     instance.instanceMethod(); // Тепер ми знаємо, для якого об'єкта викликати
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння поняття "контекст виконання" (`static` vs `instance`) та ролі `this`.*

* * *

### **3 ❓ Питання:**

Що таке статичний блок ініціалізації (`static {}`) і в якому порядку він виконується?

> #### **💬 Відповідь:**
>
> **Статичний блок ініціалізації** — це спеціальний блок коду в класі, позначений ключовим словом `static`, який використовується для ініціалізації **статичних полів**.
>
> **Коли він виконується:**
>
> * Він виконується **один раз** за весь час роботи програми, у момент **першого завантаження класу** JVM. Це відбувається, коли до класу відбувається перше звернення (створення об'єкта, виклик статичного методу тощо).
>
> **Порядок виконання:**
>
> 1. Спочатку ініціалізуються статичні поля батьківського класу (і виконуються його статичні блоки).
> 2. Потім ініціалізуються статичні поля поточного класу та виконуються його статичні блоки **в тому порядку, в якому вони написані в коді**.
>
> **Приклад:**
>
> ```java
> public class StaticOrder {
>     static { System.out.println("Static block 1"); }
>     public static String a = "A";
>     static { System.out.println("Static block 2, a = " + a); }
> }
> ```
>
> **Вивід при завантаженні класу:**
>
> ```
>
> Static block 1
> Static block 2, a = A
>
> ```
>
> Це робить їх ідеальними для складної статичної ініціалізації, наприклад, для завантаження конфігурації або реєстрації драйверів.

#### **📌 Що перевіряють:**

*Знання життєвого циклу класу та механізмів статичної ініціалізації.*

* * *

### **4 ❓ Питання:**

Наведіть приклади, коли використання статичних методів є доцільним і відповідає хорошому дизайну.

> #### **💬 Відповідь:**
>
> `static` методи доцільно використовувати тоді, коли поведінка логічно пов'язана з класом в цілому, а не зі станом його конкретного екземпляра.
>
> **Типові сценарії:**
>
> 1. **Утилітарні методи (Utility/Helper Methods):**
>     * Це методи, які приймають параметри, виконують над ними операцію і повертають результат, не покладаючись на стан жодного об'єкта.
>     * **Приклад:** Клас `java.lang.Math`. Усі його методи, як-от `Math.sqrt(double)`, `Math.max(int, int)`, є статичними, бо для їх роботи не потрібен об'єкт `Math`. Те ж саме стосується класів-помічників, як `Arrays` або `Collections`.
>
> 2. **Фабричні методи (Factory Methods):**
>     * Це статичні методи, які створюють та повертають екземпляри свого або інших класів. Вони надають більше контролю над створенням об'єктів, ніж конструктори (наприклад, можуть повертати об'єкти з кешу).
>     * **Приклад:** `Integer.valueOf("123")` або `LocalDate.of(2023, 12, 25)`.
>
> 3. **Реалізація патерну Singleton:**
>     * Публічний статичний метод `getInstance()` є стандартним способом надання глобальної точки доступу до єдиного екземпляра синглтона.

#### **📌 Що перевіряють:**

*Практичне розуміння, де `static` є потужним інструментом, а не антипатерном. Кандидат повинен навести приклади з JDK.*

* * *

### **5 ❓ Питання:**

Чому надмірне використання статичних методів та полів вважається поганою практикою або "кодовим запахом"?

> #### **💬 Відповідь:**
>
> Зловживання `static` може перетворити об'єктно-орієнтований код на процедурний і створює низку проблем з дизайном:
>
> 1. **Погана тестованість:** `static` методи важко "підмінити" (замокати) в юніт-тестах. Якщо клас `A` напряму викликає `SomeUtil.doWork()`, ви не можете легко ізолювати `A` від `SomeUtil` і змусити `doWork()` повертати тестове значення. Це призводить до тісної зв'язності та ускладнює тестування.
>
> 2. **Глобальний стан:** Статичні поля є, по суті, глобальними змінними. Будь-яка частина програми може змінити їх у будь-який час, що робить стан програми **непередбачуваним** і **важким для відстеження**. Це є джерелом складних для налагодження помилок, особливо у багатопотоковому середовищі.
>
> 3. **Тісна зв'язність (Tight Coupling):** Код, що залежить від статичних членів, жорстко прив'язується до **конкретних реалізацій**, а не до абстракцій. Це суперечить принципу інверсії залежностей і ускладнює заміну компонентів системи.
>
> 4. **Порушення принципів ООП:** `static` не підтримує поліморфізм. Якщо ваша бізнес-логіка реалізована через статичні методи, ви втрачаєте гнучкість і розширюваність, які дає ООП.
>
> **Висновок:** `static` слід використовувати для справжніх утиліт та констант, а не для реалізації основної бізнес-логіки чи зберігання стану програми.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Розуміння того, як `static` може зашкодити таким важливим характеристикам коду, як тестованість та гнучкість.*

* * *

### **6 ❓ Питання:**

Чи можна перевизначити (`@Override`) `static` метод у дочірньому класі? Поясніть, що таке "приховування методів".

> #### **💬 Відповідь:**
>
> **Ні, `static` метод не можна перевизначити.** Механізм перевизначення пов'язаний з динамічним поліморфізмом та об'єктами.
>
> Якщо ви оголосите в дочірньому класі статичний метод з такою ж сигнатурою, як у батьківському, це буде не перевизначення, а **приховування методів (method hiding)**.
>
> **Як працює приховування:**
>
> * Вибір статичного методу для виклику відбувається **на етапі компіляції** і базується на **типі посилання**, а не на фактичному типі об'єкта.
>
> **Приклад:**
>
> ```java
> class Parent {
>     static void greet() { System.out.println("Hello from Parent"); }
> }
> class Child extends Parent {
>     static void greet() { System.out.println("Hello from Child"); }
> }
>
> Parent p = new Child();
> p.greet(); // Викличе метод з Parent, оскільки тип посилання - Parent
>
> Child c = new Child();
> c.greet(); // Викличе метод з Child, оскільки тип посилання - Child
> ```
>
> У першому виклику `p.greet()` поліморфізм не спрацював. Сучасні IDE попередять про виклик статичного методу через посилання на об'єкт, рекомендуючи викликати його через ім'я класу.

#### **📌 Що перевіряють:**

*Розуміння різниці між статичним та динамічним зв'язуванням. Це класичне питання для перевірки знань наслідування та поліморфізму.*

* * *

### **7 ❓ Питання:**

У якому порядку виконуються статичні та не-статичні блоки ініціалізації та конструктори? ⭐️

> #### **💬 Відповідь:**
>
> Порядок виконання є чітко визначеним і відбувається у два етапи: завантаження класу та створення екземпляра.
>
> **Етап 1: Завантаження класу (виконується ОДИН раз)**
>
> 1. Виконуються статичні ініціалізатори **батьківського** класу (поля та `static` блоки).
> 2. Виконуються статичні ініціалізатори **поточного** класу (поля та `static` блоки) у порядку їх оголошення.
>
> **Етап 2: Створення екземпляра (виконується КОЖНОГО разу при `new`)**
> 3.  Виконується ланцюжок виклику конструкторів **зверху вниз**.
> 4.  Всередині кожного конструктора, **перед** його кодом, виконуються не-статичні ініціалізатори (поля та `{}` блоки) **батьківського** класу.
> 5.  Потім виконується код конструктора **батьківського** класу.
> 6.  Потім виконуються не-статичні ініціалізатори **поточного** класу.
> 7.  Нарешті, виконується код конструктора **поточного** класу.
>
> **Коротка схема:**
> `Статика батька -> Статика нащадка -> (для new:) Блоки+Конструктор батька -> Блоки+Конструктор нащадка`

#### **📌 Що перевіряють:**

*Дуже детальне знання життєвого циклу об'єкта та класу. Це просунуте питання, що вимагає точності.*

* * *

### **8 ❓ Питання:**

Чи може `final` змінна бути не ініціалізована в статичному блоці, якщо вона `static`? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.** Компілятор Java вимагає, щоб **`static final` змінна** була **гарантовано ініціалізована рівно один раз** до того, як клас буде готовий до використання.
>
> Для `static final` змінної є лише **два можливих місця для ініціалізації**:
>
> 1. **Безпосередньо при оголошенні:**
>     `public static final int MAX_USERS = 100;`
>
> 2. **У статичному блоці ініціалізації:**
>
>     ```java
>     public static final Map<String, String> CONFIG;
>     static {
>         CONFIG = new HashMap<>();
>         CONFIG.put("key", "value");
>     }
>     ```
>
> Якщо ви оголосите `static final` змінну і не ініціалізуєте її ні в одному з цих місць, ви отримаєте **помилку компіляції**. Це гарантує, що константа завжди матиме значення.

#### **📌 Що перевіряють:**

*Знання правил ініціалізації `final` змінних у статичному контексті.*

* * *

### **9 ❓ Питання:**

Що таке статичний імпорт (`import static`) і коли його доцільно використовувати? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Статичний імпорт** — це конструкція, яка дозволяє імпортувати **статичні члени** (поля та методи) класу безпосередньо у простір імен вашого класу. Це дозволяє звертатися до них **без вказування імені класу**.
>
> **Приклад:**
>
> Замість:
> `double area = Math.PI * Math.pow(radius, 2);`
>
> Можна написати:
>
> ```java
> import static java.lang.Math.PI;
> import static java.lang.Math.pow;
> // ...
> double area = PI * pow(radius, 2);
> ```
>
> **Коли це доцільно:**
>
> * **При роботі з константами:** Якщо ви часто використовуєте константи з одного класу (наприклад, `TimeUnit.SECONDS`, `TimeUnit.MILLISECONDS`).
> * **У юніт-тестах:** Це дуже поширена практика для імпорту **assert-методів** з бібліотек типу JUnit або AssertJ.
>     `import static org.junit.jupiter.api.Assertions.assertEquals;`
>     Це дозволяє писати `assertEquals(expected, actual)` замість `Assertions.assertEquals(...)`.
>
> **Застереження:**
> Слід уникати зловживання статичним імпортом, особливо з "зірочкою" (`import static ....*;`), оскільки це може **зробити код менш читабельним** (незрозуміло, звідки прийшов метод) та призвести до **конфлікту імен**.

#### **📌 Що перевіряють:**

*Знання сучасних синтаксичних можливостей мови та вміння оцінювати, коли їхнє використання покращує, а коли — погіршує читабельність коду.*

* * *

### **10 ❓ Питання:**

Як `static` поля впливають на потокобезпечність? Чи є вони безпечними для використання у багатопотокових додатках? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `static` поля є одним з **найбільших джерел проблем з потокобезпечністю**.
>
> **Проблема:**
>
> * Статичне поле є **спільним ресурсом** для всіх потоків, що мають доступ до цього класу.
> * Якщо кілька потоків одночасно намагаються **читати і записувати** в це поле, виникає **стан гонитви (race condition)**.
> * Зміни, зроблені одним потоком, можуть бути невидимими для іншого через кешування значень у регістрах процесора.
>
> **Приклад небезпечного лічильника:**
>
> ```java
> public class UnsafeCounter {
>     private static int count = 0;
>     public void increment() {
>         count++; // НЕАТОМАРНА операція!
>     }
> }
> ```
>
> Якщо кілька потоків викличуть `increment()`, результат буде непередбачуваним, і фінальне значення `count` буде меншим, ніж очікувалося.
>
> **Як забезпечити безпеку:**
>
> * **Зробити поле `final`:** Якщо статичне поле є константою, воно потокобезпечне.
> * **Використовувати синхронізацію:** Захищати доступ до статичного поля за допомогою `synchronized` блоків або методів.
> * **Використовувати атомарні класи:** Для лічильників та простих значень найкращим вибором є класи з пакета `java.util.concurrent.atomic`, наприклад, `AtomicInteger`.
>     `private static AtomicInteger count = new AtomicInteger(0);`
>
> `static` поля можна використовувати безпечно, але вони вимагають особливої уваги до синхронізації.

#### **📌 Що перевіряють:**

*Розуміння того, як `static` створює спільний стан, і знання інструментів для забезпечення потокобезпечності.*

* * *

### **11 ❓ Питання:**

Що таке `static` вкладений клас? Чим він принципово відрізняється від внутрішнього класу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Статичний вкладений клас (static nested class)** — це клас, оголошений всередині іншого класу з модифікатором `static`.
>
> **Принципова відмінність:**
>
> * **Зв'язок із зовнішнім класом:**
>   * **`static` вкладений клас** — це просто клас, "запакований" всередину іншого для логічного групування. Він **не має** неявного посилання на екземпляр зовнішнього класу. Він повністю незалежний.
>   * **Внутрішній клас (inner class)** — навпаки, **має** неявне посилання на екземпляр зовнішнього класу, в якому він був створений.
>
> **Наслідки цієї відмінності:**
>
> 1. **Доступ до членів:**
>     * `static` клас може звертатися **лише до `static` членів** зовнішнього класу.
>     * `inner` клас може звертатися до **всіх членів** зовнішнього класу, включаючи `private` поля та методи.
>
> 2. **Створення екземпляра:**
>     * `new Outer.StaticNested()` — можна створити незалежно.
>     * `outerInstance.new Inner()` — можна створити лише через екземпляр зовнішнього класу.
>
> 3. **Витоки пам'яті:**
>     * Внутрішні класи можуть легко призвести до витоків пам'яті, оскільки утримують посилання на зовнішній об'єкт, не даючи йому бути прибраним збирачем сміття.
>
> **Рекомендація:** Завжди надавайте перевагу `static` вкладеним класам, якщо їм не потрібен доступ до стану екземпляра зовнішнього класу.

#### **📌 Що перевіряють:**

*Глибокі знання про вкладені/внутрішні класи та їхні наслідки для дизайну та управління пам'яттю.*

* * *

### **12 ❓ Питання:**

Розгляньте патерн "Фабричний метод". Чому фабричні методи зазвичай роблять статичними? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Фабричний метод** — це патерн, який надає інтерфейс для створення об'єктів, дозволяючи підкласам вирішувати, який саме клас інстанціювати. У простішій формі, це просто метод, що інкапсулює логіку створення об'єкта.
>
> **Фабричні методи зазвичай роблять статичними з кількох причин:**
>
> 1. **Не потрібен екземпляр фабрики:** Головна мета — створити об'єкт. Немає сенсу створювати один об'єкт (фабрику) лише для того, щоб створити інший. Статичний метод дозволяє викликати логіку створення напряму через клас: `MyObject obj = MyObjectFactory.create();`.
>
> 2. **Простота та зручність API:** Це надає єдину, зрозумілу точку входу для створення об'єктів. `Integer.valueOf()` є значно кращим API, ніж необхідність створювати `new IntegerFactory().create(...)`.
>
> 3. **Інкапсуляція конструкторів:** Фабричний метод часто використовується разом з **приватним конструктором**. Це змушує клієнтів створювати об'єкти виключно через фабрику, що дає повний контроль над процесом.
>
> **Переваги використання статичного фабричного методу над публічним конструктором:**
>
> * **Можна дати методу зрозуміле ім'я** (наприклад, `createFromCache` або `createEmpty`).
> * **Можна повернути підтип** оголошеного типу.
> * **Можна повернути кешований екземпляр** замість створення нового (як це робить `Integer.valueOf()`).
> * **Не обов'язково створювати новий об'єкт** при кожному виклику.

#### **📌 Що перевіряють:**

*Знання патернів проектування та розуміння архітектурних переваг статичних фабричних методів над публічними конструкторами.*

* * *

### **13 ❓ Питання:**

Чи можна отримати доступ до `static` полів через `null` посилання? Поясніть чому. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Код, який звертається до статичного поля (або методу) через `null`-посилання, **скомпілюється і виконається без `NullPointerException`**.
>
> **Приклад:**
>
> ```java
> public class StaticTest {
>     public static String MESSAGE = "Hello";
>
>     public static void main(String[] args) {
>         StaticTest obj = null;
>         // Це працює!
>         System.out.println(obj.MESSAGE);
>     }
> }
> ```
>
> **Пояснення (це питання-пастка):**
>
> 1. Статичні члени **належать класу**, а не об'єкту.
> 2. Коли компілятор бачить вираз `obj.MESSAGE`, він дивиться на **тип посилання** `obj`, а не на його значення.
> 3. Тип `obj` — це `StaticTest`.
> 4. Компілятор "розуміє", що `MESSAGE` — це статичне поле цього класу.
> 5. Тому він **перетворює** цей виклик на **прямий доступ через ім'я класу**: `StaticTest.MESSAGE`.
> 6. Оскільки фактично звернення до об'єкта не відбувається, той факт, що `obj` дорівнює `null`, не має жодного значення.
>
> **Важливо:**
> Це є **дуже поганою практикою**, оскільки такий код заплутує і створює хибне враження, що відбувається робота з об'єктом. Сучасні IDE та аналізатори коду завжди попереджають про такий стиль і рекомендують використовувати прямий доступ через ім'я класу.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як компілятор обробляє доступ до статичних членів. Це перевірка на уважність та знання того, як не варто писати код.*

* * *

### **14 ❓ Питання:**

Уявіть собі клас `Parent` зі статичним блоком та клас `Child extends Parent` також зі статичним блоком. Що буде виведено, якщо створити об'єкт `new Child()`? А якщо після цього звернутися до `Child.someStaticField`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Розглянемо послідовність дій.
>
> ```java
> class Parent {
>     static { System.out.println("Parent static block"); }
> }
> class Child extends Parent {
>     static { System.out.println("Child static block"); }
>     public static int someStaticField = 1;
> }
>
> // 1. new Child()
> // 2. Child.someStaticField
> ```
>
> **Послідовність виконання для `new Child()`:**
>
> Статичні блоки виконуються лише **один раз** при першому завантаженні класу. Створення об'єкта `Child` ініціює завантаження `Child`, а той, у свою чергу, — `Parent`.
>
> 1. Завантажується клас `Parent`. Виконується його статичний блок.
>     **Вивід:** `Parent static block`
> 2. Завантажується клас `Child`. Виконується його статичний блок.
>     **Вивід:** `Child static block`
> 3. Після цього виконуються конструктори (батька, потім нащадка).
>
> **Якщо після цього звернутися до `Child.someStaticField`:**
>
> **Нічого додатково не буде виведено.**
> Класи `Parent` та `Child` **вже завантажені та ініціалізовані**. Повторного виконання статичних блоків не буде. Програма просто отримає доступ до вже ініціалізованого поля.
>
> **Якщо б ми спочатку звернулися до `Child.someStaticField`:**
>
> Результат був би **точно таким же**. Доступ до статичного поля класу також ініціює його завантаження. Спочатку виконався б статичний блок `Parent`, потім `Child`, а потім було б отримано значення поля.

#### **📌 Що перевіряють:**

*Детальне знання життєвого циклу класів та розуміння того, що статична ініціалізація є одноразовим процесом.*

* * *

### **15 ❓ Питання:**

Як можна ініціалізувати `final static` поле, значення якого залежить від результату, що може кинути `checked` виняток? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема, оскільки при ініціалізації поля напряму або в статичному блоці ви не можете використовувати `try-catch` для обробки `checked` винятку стандартним способом.
>
> **Проблема:**
>
> ```java
> class Config {
>     // ПОМИЛКА КОМПІЛЯЦІЇ: Unhandled exception
>     private static final String CONFIG_VALUE = loadConfig();
>
>     private static String loadConfig() throws IOException {
>         // ... читання з файлу
>     }
> }
> ```
>
> **Рішення:**
>
> **Ініціалізацію потрібно проводити всередині статичного блоку**, де можна використовувати конструкцію `try-catch`.
>
> 1. Оголосіть поле.
> 2. У `static` блоці оберніть виклик методу в `try-catch`.
> 3. У `catch` блоці у вас є два варіанти:
>     * **Найкращий варіант:** Перетворити `checked` виняток на `unchecked` (наприклад, `RuntimeException` або, що ще краще, `ExceptionInInitializerError`). Це призведе до "падіння" програми при старті, що є правильною поведінкою, якщо конфігурація не може бути завантажена.
>     * Присвоїти полю значення за замовчуванням (менш бажано).
>
> **Правильна реалізація:**
>
> ```java
> public class Config {
>     public static final String CONFIG_VALUE;
>
>     static {
>         try {
>             CONFIG_VALUE = loadConfigFromFile();
>         } catch (IOException e) {
>             // "Загортаємо" виняток. Якщо конфігурації немає, додаток не може працювати.
>             throw new ExceptionInInitializerError(e);
>         }
>     }
>
>     private static String loadConfigFromFile() throws IOException {
>         // ... логіка читання ...
>     }
> }
> ```
>
> Таким чином, статичний блок дозволяє виконати складну та потенційно небезпечну логіку для ініціалізації статичних констант.

#### **📌 Що перевіряють:**

*Навички обробки винятків у складних сценаріях, таких як статична ініціалізація. Це питання на знання найкращих практик для написання надійного коду.*
