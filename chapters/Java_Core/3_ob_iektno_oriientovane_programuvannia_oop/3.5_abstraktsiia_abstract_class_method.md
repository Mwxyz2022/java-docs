---
layout: default
title: "Абстракція (abstract class, method)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 5
---

# Абстракція (abstract class, method)

* короткий опис: Принцип ООП, який дозволяє приховати деталі реалізації, показуючи лише суттєві характеристики об'єкта. Реалізується через абстрактні класи (`abstract class`) та абстрактні методи (`abstract method`).

* поняття: Абстракція, `abstract class`, `abstract method`, `interface`.

* приклади: `abstract class Animal { abstract void makeSound(); }`.

* ключові моменти: Створення шаблонів поведінки, примус підкласів до реалізації конкретних методів, неможливість прямого створення об'єкта абстрактного класу.

* що важливо знати на співбесіді: Визначення, як реалізується, різниця з інкапсуляцією.

### **✅ Офіційне визначення:**

Абстракція — це принцип ООП, який дозволяє приховати деталі реалізації й показати тільки суттєві характеристики об’єкта.
У Java реалізується через абстрактні класи, абстрактні методи та інтерфейси.

### **🧠 Простими словами:**

Абстракція — як кермо в авто: ти крутиш його, але не знаєш і не хочеш знати, що там під капотом. Важливо тільки, **що воно робить**, а не **як саме**.

---

## **📊 Засоби абстракції в Java:**

|          Тип          |                  Що це                  |                    Особливість                     |
|:---------------------:|:---------------------------------------:|:--------------------------------------------------:|
| **`abstract class`**  |   Клас, що не можна створити напряму    |     Може мати реалізовані і абстрактні методи      |
| **`abstract method`** |    Метод без тіла (тільки сигнатура)    |        Реалізується обов’язково в підкласі         |
|    **`interface`**    | Повна абстракція (до Java 8 — без тіла) | Усі методи публічні та абстрактні за замовчуванням |

---

## **Пояснення на пальцях (💡)**

Абстрактний клас — це як інструкція без деталей. Каже: "Хто мене наслідує — має зробити це по-своєму."

* Екземпляр абстрактного класу створити не можна — тільки через конкретні нащадки.
* Абстракція допомагає:
  * створити єдиний інтерфейс,
  * змусити підкласи реалізувати поведінку,
  * приховати складність.

---

## **Корисні приклади (🧪)**

```java
// Абстрактний клас
abstract class Animal {
    abstract void makeSound();  // Абстрактний метод

    void breathe() {
        System.out.println("Breathing...");
    }
}

// Підкласи реалізують абстрактні методи
class Dog extends Animal {
    void makeSound() {
        System.out.println("Bark");
    }
}

class Cat extends Animal {
    void makeSound() {
        System.out.println("Meow");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal dog = new Dog();
        dog.makeSound();  // Bark
        dog.breathe();    // Breathing...
    }
}
```

---

## **Порівняння (⚖️)**

|           Особливість            |    abstract class     |              interface               |
|:--------------------------------:|:---------------------:|:------------------------------------:|
|        **Може мати поля**        |         ✅ так         |     ✅ тільки **`static final`**      |
| **Може мати реалізовані методи** |           ✅           |    ✅ (з Java 8 — default methods)    |
|         **Наслідування**         | Один абстрактний клас | Можна реалізувати багато інтерфейсів |
|         **Конструктор**          |           ✅           |        ❌ (немає конструктора)        |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘 **"Контракт без тіла"**

* Абстрактний метод — лише заголовок (без тіла)
* Абстрактний клас — частково готовий шаблон
* Інтерфейс — чистий контракт без деталей (до Java 8\)

---

## **🟩 Підсумок:**

Абстракція дозволяє працювати з об'єктами на рівні **"що робити"**, а не **"як саме"**. Вона створює уніфіковану
поведінку через абстрактні класи та інтерфейси, що є ключем для гнучких і масштабованих архітектур.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке абстракція в ООП і яку проблему вона вирішує?

> #### **💬 Відповідь:**
>
> **Абстракція** — це один з чотирьох основних принципів ООП, суть якого полягає у **приховуванні складних деталей реалізації** та наданні користувачеві лише **необхідної, суттєвої інформації**.
>
> **Головна ідея:** фокусуватися на тому, **"ЩО"** об'єкт може робити, а не на тому, **"ЯК"** він це робить.
>
> **Яку проблему вона вирішує:**
>
> 1. **Зменшення складності:** Абстракція дозволяє працювати зі складними системами через простий та зрозумілий інтерфейс, не заглиблюючись у внутрішню логіку.
>
> 2. **Підвищення гнучкості та слабка зв'язність:** Коли код залежить від абстракції (інтерфейсу або абстрактного класу), а не від конкретної реалізації, цю реалізацію можна легко замінити іншою без зміни коду, що її використовує.
>
> 3. **Визначення контракту:** Абстракція дозволяє визначити обов'язковий набір методів (контракт), які повинні реалізувати всі дочірні класи, що забезпечує узгодженість поведінки.

#### **📝 Аналогія:**

Пульт від телевізора — це ідеальна абстракція. Він надає простий інтерфейс (кнопки "Гучність +", "Канал →"), приховуючи всю складність інфрачервоних передавачів, мікросхем та обробки сигналів всередині телевізора. Ви знаєте, **що** кнопка робить, але не **як** вона це робить.

#### **📌 Що перевіряють:**

*Здатність пояснити концепцію простими словами та навести приклад з реального життя. Розуміння, що абстракція — це в першу чергу про спрощення.*

* * *

### **2 ❓ Питання:**

Які інструменти для реалізації абстракції існують у Java?

> #### **💬 Відповідь:**
>
> У Java є два основних інструменти для реалізації принципу абстракції:
>
> 1. **Абстрактні класи (`abstract class`):**
>     * Це класи, які не можна інстанціювати (створити об'єкт). Вони служать як **шаблони** для інших класів.
>     * Можуть містити як **абстрактні методи** (без реалізації), так і **конкретні методи** (з реалізацією) та поля.
>     * Клас, що успадковує абстрактний клас, **зобов'язаний** реалізувати всі його абстрактні методи.
>
> 2. **Інтерфейси (`interface`):**
>     * Це повністю абстрактна конструкція, яка визначає **контракт поведінки**.
>     * Традиційно, інтерфейс містить лише сигнатури публічних абстрактних методів та константи (`public static final`).
>     * З Java 8 інтерфейси також можуть містити `default` та `static` методи з реалізацією.
>     * Клас, що реалізує інтерфейс (`implements`), **зобов'язаний** реалізувати всі його абстрактні методи.
>
> Обидва інструменти дозволяють писати код, що працює з абстрактними типами, досягаючи поліморфізму та слабкої зв'язності.

#### **📌 Що перевіряють:**

*Знання базових мовних конструкцій, що використовуються для реалізації абстракції.*

* * *

### **3 ❓ Питання:**

Що таке абстрактний метод?

> #### **💬 Відповідь:**
>
> **Абстрактний метод** — це метод, який оголошено з ключовим словом **`abstract`** і **не має тіла реалізації**. Він складається лише з сигнатури (назви, списку параметрів та типу повернення) і завершується крапкою з комою.
>
> **Приклад:**
> `public abstract void makeSound();`
>
> **Ключові правила:**
>
> * Абстрактні методи можуть існувати **тільки всередині абстрактних класів або інтерфейсів**.
> * Якщо клас містить хоча б один абстрактний метод, він сам **повинен бути оголошений як `abstract`**.
> * Будь-який не-абстрактний дочірній клас **зобов'язаний перевизначити (`@Override`)** і надати реалізацію для всіх успадкованих абстрактних методів.
>
> По суті, абстрактний метод — це "обіцянка" або вимога, яку суперклас накладає на своїх спадкоємців, змушуючи їх надати власну версію певної поведінки.

#### **📌 Що перевіряють:**

*Знання синтаксису та правил, пов'язаних з абстрактними методами.*

* * *

### **4 ❓ Питання:**

У чому основні відмінності між абстрактним класом та інтерфейсом?

> #### **💬 Відповідь:**
>
> Це одне з найчастіших питань на співбесідах, яке перевіряє розуміння основ ООП.
>
| Характеристика | Абстрактний клас (`abstract class`) | Інтерфейс (`interface`) |
| :---: | :---: | :---: |
| **Призначення** | Надати **спільну базову функціональність та стан** для тісно пов'язаних класів (відношення "is-a"). | Визначити **спільний контракт поведінки** для класів, які можуть бути абсолютно непов'язаними (відношення "can-do").|
| **Методи** | Може містити як абстрактні, так і **конкретні методи**. | До Java 8 — лише абстрактні. З Java 8+ — може містити `default` та `static` методи. |
| **Поля** | Може містити будь-які поля (`final`, `static`, змінювані). | Лише **`public static final`** константи. |
| **Наслідування** | Клас може успадкувати **лише один** абстрактний клас. | Клас може реалізувати **багато** інтерфейсів. |
| **Конструктор** | **Має конструктор** (викликається через `super()` у дочірніх класах). | **Не має конструктора.** |
| **Контроль доступу**| Може використовувати всі модифікатори доступу (`public`, `protected`, `default`, `private`). | Всі члени є неявно `public` (до Java 9 для `private` методів).|

#### **📌 Що перевіряють:**

*Чітке знання технічних та концептуальних відмінностей між двома ключовими інструментами абстракції.*

* * *

### **5 ❓ Питання:**

Коли слід використовувати абстрактний клас, а коли — інтерфейс? Наведіть сценарії.

> #### **💬 Відповідь:**
>
> Вибір залежить від мети, яку ви переслідуєте.
>
> **Використовуйте абстрактний клас, коли:**
>
> 1. **Ви хочете поділитися кодом між тісно пов'язаними класами.** Якщо кілька класів мають спільні поля та реалізації методів, логічно винести їх у спільний абстрактний суперклас.
>     * **Сценарій:** Класи `CheckingAccount` та `SavingAccount`. Обидва є різновидами `BankAccount` і можуть мати спільні поля (`balance`, `owner`) та методи (`getBalance()`).
> 2. **Вам потрібно контролювати стан.** Абстрактні класи можуть мати поля стану (`non-final`, `non-static`), що дозволяє створити базовий стан для всіх спадкоємців.
>
> **Використовуйте інтерфейс, коли:**
>
> 1. **Ви хочете визначити поведінку (контракт), яку можуть реалізовувати абсолютно непов'язані класи.**
>     * **Сценарій:** Інтерфейс `Serializable`, `Comparable`, `Runnable`. Класи `String`, `Date`, `Thread` дуже різні, але всі можуть бути серіалізованими, порівнюваними або виконуваними.
> 2. **Вам потрібне множинне наслідування типів.** Оскільки клас може реалізувати багато інтерфейсів, це дозволяє об'єкту мати багато різних "ролей".
>     * **Сценарій:** `class Bird extends Animal implements Flyable, Serializable` — Птах є твариною, може літати і може бути серіалізованим.
>
> **Золоте правило:** Якщо ви можете описати зв'язок як "є різновидом" і є спільна реалізація, можливо, це абстрактний клас. Якщо ви описуєте здатність "може робити щось", це майже завжди інтерфейс. **Надавайте перевагу інтерфейсам**, якщо це можливо, бо вони більш гнучкі.

#### **📌 Що перевіряють:**

*Здатність приймати архітектурні рішення. Кандидат повинен розуміти, який інструмент є кращим для вирішення конкретної задачі дизайну.*

* * *

### **6 ❓ Питання:**

Чи може абстрактний клас не містити жодного абстрактного методу? Якщо так, то який у цьому сенс? ⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Абсолютно валідним є оголосити клас як `abstract`, навіть якщо він не має жодного абстрактного методу.
>
> **Сенс у цьому є, і основна мета — заборонити створення екземплярів (об'єктів) цього класу.**
>
> **Сценарії використання:**
>
> 1. **Створення базового утилітарного класу, призначеного лише для успадкування.** Уявіть, що у вас є клас `BaseController`, який надає деякі спільні, готові до використання `protected` методи для всіх контролерів у вашому веб-додатку. Ви не хочете, щоб хтось випадково створив об'єкт `new BaseController()`, оскільки він не має самостійного сенсу. Роблячи його `abstract`, ви змушуєте використовувати лише його спадкоємців.
>
> 2. **Застаріла практика для утилітарних класів:** Раніше, щоб створити клас, що містить лише `static` методи (наприклад, `MathUtils`), іноді його робили абстрактним, щоб заборонити його інстанціювання. Сьогодні для цього краще використовувати `private` конструктор.

#### **📌 Що перевіряють:**

*Знання нюансів використання `abstract`. Розуміння того, що `abstract` на рівні класу — це в першу чергу про заборону інстанціювання.*

* * *

### **7 ❓ Питання:**

Яка різниця між абстракцією та інкапсуляцією?

> #### **💬 Відповідь:**
>
> Ці два принципи часто плутають, але вони описують різні аспекти проектування об'єктів.
>
> * **Абстракція** стосується **спрощення складності** та фокусується на **зовнішньому інтерфейсі** об'єкта. Вона відповідає на питання **"ЩО?"** (що об'єкт може робити?).
>   * Це про **приховування непотрібних деталей**.
>   * *Приклад:* Ви бачите кнопку "Пуск" на пральній машині. Це абстракція.
>
> * **Інкапсуляція** стосується **приховування даних** та об'єднання їх з логікою в єдине ціле. Вона фокусується на **внутрішній реалізації**. Вона відповідає на питання **"ЯК?"** (як об'єкт реалізує свою поведінку?).
>   * Це про **захист даних та контроль доступу**.
>   * *Приклад:* Те, що відбувається всередині пральної машини (робота двигуна, нагрівача води), приховано від вас. Це інкапсуляція.
>
> **Як вони пов'язані:**
> **Інкапсуляція є одним із способів досягнення абстракції.** Використовуючи модифікатори доступу (`private`), ми приховуємо внутрішню реалізацію (інкапсуляція), щоб надати користувачеві лише простий публічний інтерфейс (абстракція).

#### **📌 Що перевіряють:**

*Чітке розмежування двох фундаментальних принципів ООП.*

* * *

### **8 ❓ Питання:**

Чи може абстрактний клас реалізовувати інтерфейс? Чи може інтерфейс розширювати інший інтерфейс? ⭐️

> #### **💬 Відповідь:**
>
> **Так, обидва сценарії можливі** і є поширеною практикою.
>
> **1. Абстрактний клас реалізує інтерфейс (`implements`):**
>
> * Абстрактний клас може реалізовувати інтерфейс. При цьому він **не зобов'язаний** надавати реалізацію для всіх методів інтерфейсу.
> * Він може залишити деякі (або всі) методи інтерфейсу абстрактними, **перекладаючи обов'язок їх реалізації** на своїх конкретних спадкоємців.
> * Це корисно для створення "проміжної" абстракції, яка може надати базову реалізацію для деяких методів інтерфейсу.
>
>     ```java
>     interface MyInterface { void methodA(); void methodB(); }
>     abstract class MyAbstractClass implements MyInterface {
>         @Override
>         public void methodA() { /* Базова реалізація */ }
>         // methodB() залишається абстрактним для реалізації в підкласах
>     }
>     ```
>
> **2. Інтерфейс розширює інший інтерфейс (`extends`):**
>
> * Інтерфейс може **успадковувати** від одного або **кількох** інших інтерфейсів. Для цього використовується ключове слово `extends`.
> * Це дозволяє створювати ієрархії контрактів, де дочірній інтерфейс успадковує всі методи батьківського і може додавати свої власні.
> * Це єдина форма **множинного наслідування**, яка повністю підтримується в Java.
>
>     ```java
>     interface Flyable { void fly(); }
>     interface Swimmable { void swim(); }
>     interface SuperBird extends Flyable, Swimmable {} // Успадковує обидва методи
>     ```

#### **📌 Що перевіряють:**

*Знання гнучких можливостей побудови ієрархій за допомогою абстрактних класів та інтерфейсів.*

* * *

### **9 ❓ Питання:**

Чи може абстрактний клас мати конструктор? Якщо так, то навіщо, адже його об'єкт не можна створити? ⭐️

> #### **💬 Відповідь:**
>
> **Так, абстрактний клас може ( і часто має) конструктор.**
>
> Хоча ви не можете створити екземпляр абстрактного класу напряму (`new MyAbstractClass()`), його конструктор відіграє важливу роль в ієрархії наслідування.
>
> **Навіщо він потрібен:**
>
> 1. **Для ініціалізації полів абстрактного класу:** Абстрактний клас може мати власні поля (стан), і конструктор є єдиним правильним місцем для їх ініціалізації.
>
> 2. **Для виклику з конструкторів дочірніх класів:** Конструктор дочірнього класу **повинен** викликати конструктор свого батьківського класу за допомогою `super()`. Це гарантує, що вся успадкована частина об'єкта буде коректно ініціалізована перед тим, як дочірній клас почне ініціалізувати свої власні поля.
>
> **Приклад:**
>
> ```java
> abstract class Shape {
>     private String color;
>
>     public Shape(String color) {
>         System.out.println("Конструктор Shape викликано");
>         this.color = color;
>     }
>     // ...
> }
>
> class Circle extends Shape {
>     public Circle(String color) {
>         super(color); // Виклик конструктора батька для ініціалізації поля 'color'
>         System.out.println("Конструктор Circle викликано");
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння ланцюжка виклику конструкторів та процесу ініціалізації об'єктів в ієрархії наслідування.*

* * *

### **10 ❓ Питання:**

Чи може `abstract` метод бути `private`, `static` або `final`? Поясніть чому для кожного. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, `abstract` метод не може мати жоден з цих модифікаторів.** Це призведе до **помилки компіляції** через логічні протиріччя.
>
> 1. **`private` та `abstract`:**
>     * **`abstract`** означає, що метод **повинен бути перевизначений** у дочірньому класі.
>     * **`private`** означає, що метод **невидимий** для дочірніх класів і, відповідно, **не може бути перевизначений**.
>     * Це пряме протиріччя.
>
> 2. **`static` та `abstract`:**
>     * **`abstract`** покладається на **поліморфізм часу виконання** та механізм перевизначення.
>     * **`static`** методи **не можуть бути перевизначені** (лише приховані) і зв'язуються на етапі компіляції. Механізм поліморфізму до них незастосовний.
>     * Знову ж таки, це протиріччя.
>
> 3. **`final` та `abstract`:**
>     * **`abstract`** вимагає, щоб метод **був перевизначений**.
>     * **`final`** **забороняє** перевизначення.
>     * Це найбільш очевидне логічне протиріччя.
>
> Таким чином, `abstract` є несумісним з усіма модифікаторами, які якимось чином обмежують можливість його реалізації в дочірньому класі.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантики кожного ключового слова та здатність аналізувати їх сумісність.*

* * *

### **11 ❓ Питання:**

Який зв'язок між абстракцією та принципом відкритості/закритості (Open/Closed Principle) з SOLID? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Принцип відкритості/закритості** стверджує, що програмні сутності (класи, модулі) повинні бути **відкритими для розширення**, але **закритими для модифікації**.
>
> **Абстракція** є ключовим інструментом для досягнення цього принципу.
>
> **Як це пов'язано:**
>
> 1. Ми створюємо **абстракцію** (інтерфейс або абстрактний клас), яка визначає стабільний контракт. Ця абстракція є **"закритою" для модифікації**. Ми намагаємося не змінювати її після того, як вона була визначена.
>
> 2. Нова функціональність додається шляхом створення **нових конкретних реалізацій** цієї абстракції. Система є **"відкритою" для розширення**, оскільки ми можемо додати скільки завгодно нових класів, що реалізують цей інтерфейс.
>
> **Приклад:**
>
> У нас є інтерфейс `PaymentGateway` з методом `processPayment()`.
>
> * Цей інтерфейс є **закритим** — ми його не змінюємо.
> * Спочатку ми маємо реалізацію `CreditCardGateway`.
> * Коли нам потрібно додати підтримку PayPal, ми не змінюємо існуючі класи, а створюємо **новий клас** `PayPalGateway`, що реалізує `PaymentGateway`. Система **відкрита** для такого розширення.
>
> Код, що використовує `PaymentGateway`, буде працювати з новою реалізацією без будь-яких змін, завдяки поліморфізму.

#### **📌 Що перевіряють:**

*Розуміння того, як принципи ООП взаємодіють з принципами SOLID. Здатність бачити абстракцію як основу для побудови гнучкої архітектури.*

* * *

### **12 ❓ Питання:**

Що таке `default` методи в інтерфейсах (з Java 8) і як вони впливають на концепцію абстракції? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`default` методи** — це методи, оголошені в інтерфейсі з ключовим словом `default`, які **мають тіло реалізації**.
>
> Вони були додані в Java 8 з однією головною метою: дозволити **еволюцію існуючих інтерфейсів**, не ламаючи класи, що їх реалізують.
>
> **Як вони впливають на абстракцію:**
>
> * Вони **трохи "розмивають"** ідею інтерфейсу як чистої абстракції. Тепер інтерфейс може містити не тільки контракт, але й певну поведінку за замовчуванням.
> * Це робить інтерфейси **більш схожими на абстрактні класи**, оскільки тепер обидва можуть надавати реалізацію.
>
> **Приклад:**
>
> ```java
> interface Logger {
>     void log(String message); // Абстрактний метод
>
>     // Default метод, доданий пізніше
>     default void logError(String message) {
>         log("ERROR: " + message);
>     }
> }
> ```
>
> Тепер всі існуючі класи, що реалізують `Logger`, автоматично отримають метод `logError()` без необхідності його реалізовувати. Це дозволило, наприклад, додати метод `forEach` до інтерфейсу `Iterable` в Java 8, не зламавши мільйони існуючих колекцій.
>
> Однак, ключові відмінності між інтерфейсом та абстрактним класом (одиничне наслідування, стан) залишаються, тому вони все ще служать різним цілям.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови та розуміння причин їх введення та впливу на класичні концепції.*

* * *

### **13 ❓ Питання:**

Що станеться, якщо клас успадковує абстрактний клас, але не реалізує всі його абстрактні методи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У такому випадку дочірній клас **сам повинен бути оголошений як `abstract`**.
>
> **Правило:**
> Будь-який клас, який не надає реалізації для **всіх** успадкованих ним абстрактних методів (чи то від абстрактного класу, чи то від інтерфейсу), не є "повним" або "конкретним". Він також залишається абстрактним.
>
> Спроба оголосити такий клас як конкретний призведе до **помилки компіляції**. Компілятор повідомить, що "class must either be declared abstract or implement abstract method ...".
>
> **Приклад:**
>
> ```java
> abstract class Shape {
>     abstract double getArea();
>     abstract double getPerimeter();
> }
>
> // Цей клас надає лише одну реалізацію.
> // Тому він теж повинен бути абстрактним.
> abstract class FourSidedShape extends Shape {
>     @Override
>     public double getArea() {
>         // ... якась реалізація
>         return 0;
>     }
>     // Метод getPerimeter() залишається нереалізованим
> }
>
> // Лише конкретний клас реалізує все
> class Rectangle extends FourSidedShape {
>     @Override
>     public double getPerimeter() {
>         return 0; //...
>     }
> }
> ```
>
> Цей механізм дозволяє створювати проміжні рівні абстракції в ієрархії.

#### **📌 Що перевіряють:**

*Знання правил роботи з абстрактними класами в ієрархії успадкування.*

* * *

### **14 ❓ Питання:**

Що таке маркерний інтерфейс (Marker Interface)? Як він пов'язаний з абстракцією? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Маркерний інтерфейс** — це інтерфейс, який **не містить жодних методів або констант**. Він є "порожнім".
>
> Його єдина мета — **"позначити" (`mark`)** клас, який його реалізує, надавши йому певну спеціальну властивість або вказавши, що він повинен оброблятися певним чином системними механізмами або фреймворками.
>
> **Класичний приклад у JDK:**
>
> * `java.io.Serializable`: Клас, що реалізує цей інтерфейс, сигналізує JVM, що його об'єкти можна серіалізувати (перетворити в потік байт).
> * `java.lang.Cloneable`: Позначає, що об'єкти цього класу можна клонувати за допомогою методу `Object.clone()`.
> * `java.rmi.Remote`: Використовується в RMI для позначення об'єктів, які можуть бути доступні віддалено.
>
> **Зв'язок з абстракцією:**
>
> Маркерний інтерфейс є крайнім випадком **абстракції поведінки**. Він абстрагує певну **здатність або властивість** до самого її існування, не визначаючи ніяких конкретних методів. Оператор `instanceof` потім використовується для перевірки наявності цієї властивості.
>
> ```java
> if (obj instanceof Serializable) {
>     // ... логіка серіалізації
> }
> ```
>
> **Примітка:** В сучасній Java перевага надається використанню **анотацій** замість маркерних інтерфейсів, оскільки анотації є більш гнучкими і можуть містити метадані.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та їх реалізації в стандартній бібліотеці Java.*

* * *

### **15 ❓ Питання:**

Розгляньте таку ситуацію: у вас є інтерфейс `A` з `default` методом `doWork()`, інтерфейс `B` з таким же `default` методом, і клас `C implements A, B`. Що станеться? Як вирішити проблему? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця ситуація відома як **"проблема ромба" для `default` методів**. Вона виникає, коли клас реалізує два інтерфейси, що мають `default` методи з однаковою сигнатурою.
>
> **Що станеться:**
>
> Цей код **не скомпілюється**. Компілятор Java видасть помилку, повідомляючи про неоднозначність (ambiguity), оскільки він не знає, яку з двох реалізацій `doWork()` (з інтерфейсу `A` чи `B`) клас `C` повинен успадкувати.
>
> **Як вирішити проблему:**
>
> Java **вимагає від розробника явно вирішити цей конфлікт**. Клас `C` **зобов'язаний перевизначити (`@Override`)** конфліктний метод `doWork()`.
>
> Усередині перевизначеного методу у вас є кілька варіантів:
>
> 1. **Надати абсолютно нову, власну реалізацію:**
>
>     ```java
>     @Override
>     public void doWork() {
>         System.out.println("C's implementation of doWork");
>     }
>     ```
>
> 2. **Явно викликати одну з батьківських реалізацій:**
>     Для цього використовується спеціальний синтаксис `super`:
>
>     ```java
>     @Override
>     public void doWork() {
>         // Викликати реалізацію з інтерфейсу A
>         A.super.doWork();
>     }
>     ```
>
> 3. **Комбінувати обидві реалізації:**
>
>     ```java
>     @Override
>     public void doWork() {
>         A.super.doWork();
>         B.super.doWork();
>     }
>     ```
>
>
> Таким чином, на відміну від C++, де проблема ромба могла призводити до непередбачуваної поведінки, Java вирішує її на етапі компіляції, змушуючи розробника приймати свідоме рішення.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як працюють `default` методи та механізми вирішення конфліктів у сучасній Java.*
