---
layout: default
title: "POJO / DTO (Data Holder)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 25
---

# POJO / DTO (Data Holder)

* короткий опис: POJO (Plain Old Java Object) – простий Java-клас, який не залежить від фреймворків, містить поля, геттери/сеттери. DTO (Data Transfer Object) – це POJO, призначений для перенесення даних між шарами системи, без бізнес-логіки.

* поняття: POJO, DTO, Data Holder, Геттери, Сеттери.

* приклади: Клас `User` (POJO) з полями `name`, `age`. `UserDto` для передачі даних в API.

* ключові моменти: POJO – загального призначення, DTO – для транспортування даних. Обидва мінімалістичні.

* що важливо знати на співбесіді: Різниця між POJO та DTO, їх призначення.

### **✅ Офіційне визначення:**

* **POJO (Plain Old Java Object)** — простий Java-клас, який не залежить від специфічних фреймворків чи бібліотек, містить лише поля, геттери/сеттери, конструктори та, можливо, методи **`toString(), equals(), hashCode().`**
* **DTO (Data Transfer Object)** — об’єкт для перенесення даних між шарами системи, зазвичай схожий на POJO, але з фокусом саме на транспортування, без бізнес-логіки.

### **🧠 Простими словами:**

* **POJO** — це "тупа коробка з полями" — просто зберігає дані, без логіки.
* **DTO** — це "посилка" з даними, яку ти передаєш з сервісу в контролер, з БД у клієнт тощо.

---

## **Основне**

| Ознака | POJO | DTO |
| :---: | :---: | :---: |
| **Структура** | Поля \+ геттери/сеттери | Те саме |
| **Призначення** | Загального призначення | Передача даних між шарами |
| **Поведінка/логіка** | Може містити легку логіку | Не повинно містити логіку |
| **Залежність від фреймворків** | ❌ Немає | ❌ Теж немає |

---

## **Пояснення на пальцях (💡)**

* Якщо тобі треба просто тримати дані в пам’яті — використовуй POJO.
* Якщо ти передаєш ці дані між частинами програми або по мережі — DTO.
* DTO і POJO часто виглядають однаково, різниця — в контексті використання.

---

## **Корисні приклади (🧪)**

🔹 **POJO (Plain Old Java Object):**

```java
public class User {
    private String name;
    private int age;

    public User() {}

    public User(String name, int age) {
        this.name = name;
        this.age = age;
    }

    public String getName() { return name; }
    public void setName(String name) { this.name = name; }

    public int getAge() { return age; }
    public void setAge(int age) { this.age = age; }

    @Override
    public String toString() {
        return "User{name='" + name + "', age=" + age + "}";
    }
}
```

---

🔹 **DTO (для передачі в API):**

```java
public class UserDto {
    private String name;
    private int age;

    public UserDto() {}

    public UserDto(String name, int age) {
        this.name = name;
        this.age = age;
    }

    // Тільки геттери/сеттери, без логіки
}
```

---

## **Порівняння (⚖️)**

| Параметр | POJO | DTO |
| :---: | :---: | :---: |
| **Призначення** | Загальне, для зберігання | Передача між шарами |
| **Логіка** | Може мати | Ні, повинна бути "тупою" |
| **Взаємодія** | Сервіси, моделі | API, контролери, транспорт |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘

* **POJO \= Просто Обʼєкт**
* **DTO \= Data To Others** — «дані для передачі іншим»

---

## **🟩 Підсумок:**

POJO та DTO — це прості класи без логіки, які використовуються для зберігання або передачі даних.

* POJO — універсальний, може мати легку логіку.
* DTO — строго для передачі між шарами або сервісами.

Це основа чистої архітектури, REST API, мікросервісів і зрозумілого коду.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке POJO (Plain Old Java Object)? Назвіть його основні характеристики.

> #### **💬 Відповідь:**
>
> **POJO (Plain Old Java Object)** — це термін, який використовується для опису "простого" Java-об'єкта. "Простота" полягає в тому, що він **не обмежений жодними спеціальними вимогами фреймворків** або специфікацій.
>
> **Основні характеристики POJO:**
>
> 1. **Незалежність:** Він не успадковує класи з якихось специфічних фреймворків (як `HttpServlet`), не реалізує "маркерні" інтерфейси (як `EJBHome`) і не вимагає спеціальних анотацій.
> 2. **Фокус на даних:** Головна мета POJO — зберігати дані у своїх полях.
> 3. **Інкапсуляція:** Зазвичай, поля є `private`, а доступ до них надається через `public` геттери та сеттери.
> 4. **Конструктор:** Часто має публічний конструктор без аргументів для зручного створення, а також може мати перевантажені конструктори.
>
> Простими словами, це звичайний, самодостатній клас, що представляє якусь бізнес-сутність або структуру даних, не прив'язаний до інфраструктури.

#### **📝 Аналогія:**

POJO — це як універсальна коробка. Ви можете покласти в неї що завгодно і використовувати де завгодно. Ви не прив'язані до правил конкретної поштової служби (фреймворку).

#### **📌 Що перевіряють:**

*Розуміння того, що POJO — це концепція, а не строгий технічний стандарт. Це про "незалежність від фреймворків".*

* * *

### **2 ❓ Питання:**

Що таке DTO (Data Transfer Object) і яка його головна мета?

> #### **💬 Відповідь:**
>
> **DTO (Data Transfer Object)** — це патерн проектування, який використовується для **передачі даних між різними шарами або процесами** в додатку. Це об'єкт, єдине призначення якого — бути **контейнером для даних**.
>
> **Головна мета DTO:**
>
> * **Зменшити кількість мережевих викликів:** Замість того, щоб робити багато дрібних запитів для отримання кожного поля, дані групуються в один DTO і передаються за один раз. Це особливо важливо в розподілених системах (наприклад, між клієнтом і сервером).
> * **Відокремити внутрішню модель від зовнішньої:** DTO дозволяє створити "фасад" для вашої внутрішньої доменної моделі. Ви можете віддавати клієнту лише ті дані, які йому потрібні, і в тому форматі, в якому йому зручно, приховуючи складність вашої бази даних чи внутрішньої логіки.
>
> **Характеристики DTO:**
>
> * Зазвичай це **POJO**.
> * **Не містить бізнес-логіки.** Його методи (якщо вони є) призначені лише для доступу до даних (геттери/сеттери).
> * Часто є **серіалізованим**, щоб його можна було легко перетворити в JSON, XML тощо для передачі по мережі.

#### **📌 Що перевіряють:**

*Розуміння того, що DTO — це спеціалізований POJO з чітким призначенням: бути "посилкою" для даних.*

* * *

### **3 ❓ Питання:**

У чому основна різниця між POJO та DTO? Чи може POJO бути DTO?

> #### **💬 Відповідь:**
>
> Основна різниця лежить у їхньому **призначенні та контексті використання**, а не в технічній реалізації.
>
> * **POJO (Plain Old Java Object):**
>   * Це **загальний термін** для простого Java-класу, що представляє будь-яку сутність.
>   * Може використовуватися на будь-якому шарі додатку. Наприклад, як частина доменної моделі, де він може містити певну бізнес-логіку.
>
> * **DTO (Data Transfer Object):**
>   * Це **специфічний патерн** для вирішення конкретної задачі — **передачі даних**.
>   * Це "тупий" об'єкт, який **не повинен мати жодної бізнес-логіки**. Його відповідальність — лише зберігати і переносити дані.
>
> **Чи може POJO бути DTO?**
>
> **Так.** Насправді, DTO — це **різновид POJO**. Будь-який DTO за своєю природою є POJO (бо він є простим об'єктом, не прив'язаним до фреймворків).
>
> **Але не кожен POJO є DTO.**
>
> Якщо POJO використовується в доменному шарі і містить бізнес-правила (наприклад, метод `user.calculateBonus()`), то він не є DTO.
>
> **Коротко:** DTO — це про роль, POJO — це про технічні характеристики.

#### **📌 Що перевіряють:**

*Здатність розрізняти концепції на основі їхнього семантичного призначення.*

* * *

### **4 ❓ Питання:**

Навіщо взагалі потрібен DTO, якщо можна просто передавати сутності (Entities) з бази даних напряму на клієнт?

> #### **💬 Відповідь:**
>
> Передавати сутності бази даних (наприклад, JPA/Hibernate Entities) напряму — це **дуже погана практика і антипатерн**. Використання DTO натомість вирішує кілька важливих проблем:
>
> 1. **Приховування внутрішньої структури:** Ваша структура бази даних може бути складною і з часом змінюватися. Віддаючи Entity назовні, ви "виставляєте" деталі своєї реалізації. DTO дозволяє створити стабільний, незалежний від бази даних контракт для клієнта.
>
> 2. **Захист від надмірної передачі даних (Over-posting/Over-fetching):** Entity може містити багато полів, включаючи службові або чутливі дані (наприклад, хеш пароля, внутрішні прапори), які не повинні потрапляти на клієнт. DTO дозволяє включити лише ті поля, які необхідні для конкретного сценарію.
>
> 3. **Уникнення проблем з "лінивою" ініціалізацією:** JPA-сутності часто використовують ліниве завантаження (`LAZY loading`) для пов'язаних сутностей. Якщо ви віддасте таку сутність, а сесія Hibernate вже закрита, спроба клієнта (наприклад, серіалізатора JSON) отримати доступ до цих полів призведе до `LazyInitializationException`. DTO заповнюється даними, поки сесія ще відкрита, і є "від'єднаним" об'єктом.
>
> 4. **Оптимізація та агрегація:** DTO може бути агрегацією даних з кількох сутностей, надаючи клієнту вже готове, зручне представлення, щоб йому не доводилося робити кілька запитів.
>
> Таким чином, DTO є важливим шаром абстракції між доменною моделлю та зовнішнім світом.

#### **📌 Що перевіряють:**

*Розуміння архітектури багатошарових додатків та проблем, які виникають при змішуванні різних шарів (Data Layer, Business Layer, Presentation Layer).*

* * *

### **5 ❓ Питання:**

Чим відрізняються DTO та Value Object (VO)?

> #### **💬 Відповідь:**
>
> Ці два патерни часто плутають, але вони служать різним цілям.
>
> * **DTO (Data Transfer Object):**
>   * **Призначення:** Перенесення даних.
>   * **Змінність:** Зазвичай **змінний (mutable)**. Має геттери та сеттери для заповнення даними (наприклад, серіалізатором).
>   * **Ідентичність:** Ідентичність DTO зазвичай не важлива. Два DTO з однаковими даними є взаємозамінними, але не обов'язково рівними за `.equals()`.
>
> * **Value Object (VO, Об'єкт-значення):**
>   * **Призначення:** Представляє **просте значення** або характеристику в доменній моделі, яка не має власної ідентичності.
>   * **Змінність:** **Незмінний (immutable).** Його значення встановлюється в конструкторі і не може бути змінене.
>   * **Ідентичність:** Два VO **вважаються рівними**, якщо всі їхні поля рівні. Тому для VO **завжди** потрібно перевизначати `.equals()` та `hashCode()`.
>   * **Приклад:** `Money` (сума та валюта), `Color` (RGB), `DateRange` (початок та кінець). Два об'єкти `Money(100, "USD")` є абсолютно однаковими.
>
> **Коротко:** DTO — це про "пересилку" даних. VO — це про "значення". `record` в Java є ідеальним кандидатом для створення Value Object.

#### **📌 Що перевіряють:**

*Знання просунутих патернів Domain-Driven Design (DDD). Це питання на рівень Middle+.*

* * *

### **6 ❓ Питання:**

Що таке Anemic Domain Model ("Анемічна доменна модель") і як вона пов'язана з POJO? ⭐️

> #### **💬 Відповідь:**
>
> **Анемічна доменна модель** — це антипатерн, при якому об'єкти доменної моделі (які мали б представляти бізнес-сутності) є **"анемічними"**, тобто не містять **жодної бізнес-логіки**.
>
> Такі об'єкти є просто набором полів з геттерами та сеттерами — по суті, "тупими" **POJO-контейнерами для даних**.
>
> Уся бізнес-логіка, яка мала б належати цим об'єктам (наприклад, `order.calculateTotal()`, `user.deactivate()`), виноситься назовні, зазвичай у "сервісні" класи.
>
> **Зв'язок з POJO:**
>
> * При анемічній моделі, доменні об'єкти вироджуються до рівня простих POJO/DTO.
>
> **Проблеми анемічної моделі:**
>
> * **Порушення інкапсуляції:** Логіка та дані існують окремо, що є ознакою **процедурного програмування**, а не ООП.
> * **"Багаті" сервіси:** Сервісні класи стають громіздкими, оскільки містять логіку, що мала б належати доменним об'єктам.
> * **Менша читабельність:** Складніше зрозуміти, які правила застосовуються до конкретної бізнес-сутності, оскільки вони розкидані по різних сервісах.
>
> На противагу цьому, **"багата" доменна модель (Rich Domain Model)** заохочує розміщувати бізнес-логіку безпосередньо в доменних об'єктах, що робить їх справжніми об'єктами в сенсі ООП.

#### **📌 Що перевіряють:**

*Розуміння принципів Domain-Driven Design та вміння критично оцінювати архітектуру. Здатність бачити, коли POJO використовується недоречно.*

* * *

### **7 ❓ Питання:**

Яким чином `record`'и, введені в Java 16, спрощують створення DTO та Value Objects? ⭐️

> #### **💬 Відповідь:**
>
> **`record`'и** — це ідеальний інструмент для створення DTO та, особливо, Value Objects, оскільки вони значно **зменшують кількість шаблонного коду (boilerplate)**.
>
> `public record UserDto(String name, int age) {}`
>
> Цей один рядок коду компілятор автоматично перетворює на **незмінний (`immutable`) `final` клас**, який вже містить:
>
> 1. **`private final` поля** для всіх компонентів (`name` та `age`).
> 2. **Канонічний конструктор**, що ініціалізує всі поля.
> 3. **Публічні методи доступу** (акцесори) для кожного поля (`name()` та `age()`).
> 4. **Коректно перевизначені** `equals()`, `hashCode()` та `toString()`.
>
> **Переваги для DTO та VO:**
>
> * **Компактність:** Не потрібно писати десятки рядків коду вручну.
> * **Незмінність "з коробки":** `record` за замовчуванням є незмінним, що є ідеальною властивістю для DTO та обов'язковою для Value Objects.
> * **Надійність:** Автоматично згенеровані `equals()` та `hashCode()` гарантують коректну роботу в колекціях та порівняння за значенням.
>
> Використання `record`'ів є сучасною найкращою практикою для створення простих незмінних носіїв даних.

#### **📌 Що перевіряють:**

*Знання сучасних фіч Java та того, як вони вирішують давні проблеми (наприклад, багатослівність POJO).*

* * *

### **8 ❓ Питання:**

Як реалізувати перетворення (мапінг) між Entity, DTO та моделлю для UI? Які інструменти для цього існують? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Перетворення між різними представленнями даних є рутинною задачею у багатошарових додатках.
>
> **1. Ручний мапінг:**
>
> * Створюються методи-конвертери або окремі `Mapper` класи, які вручну копіюють дані з одного об'єкта в інший.
>
>     ```java
>     public UserDto toDto(User user) {
>         UserDto dto = new UserDto();
>         dto.setName(user.getName());
>         // ...
>         return dto;
>     }
>     ```> *   **Плюси:** Повний контроль, прозорість.
> * **Мінуси:** Багато шаблонного коду, схильність до помилок (можна забути змапити нове поле).
>
> **2. Використання бібліотек для мапінгу:**
>
> * Існують спеціалізовані бібліотеки, які автоматизують цей процес за допомогою рефлексії або генерації коду.
> * **Популярні бібліотеки:**
>   * **MapStruct:** Дуже швидка бібліотека, яка **генерує код маперів на етапі компіляції**. Вона не використовує рефлексію в runtime. Це найкращий вибір з точки зору продуктивності та безпеки типів.
>   * **ModelMapper:** Більш гнучка бібліотека, що використовує **рефлексію**. Вона може "вгадувати" відповідність полів за їхніми іменами. Легша у налаштуванні, але повільніша і менш безпечна.
>
> Використання таких бібліотек значно зменшує кількість шаблонного коду і робить процес мапінгу більш надійним.

#### **📌 Що перевіряють:**

*Знання екосистеми Java та інструментів, що полегшують розробку. Розуміння переваг та недоліків різних підходів (ручний, рефлексія, генерація коду).*

* * *

### **9 ❓ Питання:**

Які поля `final` та `private` у звичайного POJO? Чому так? ⭐️

> #### **💬 Відповідь:**
>
> За класичним визначенням JavaBean/POJO, поля робляться **`private`**, щоб дотримуватися принципу **інкапсуляції**.
>
> ```java
> public class User {
>     private String name;
>     // ...
> }
> ```
>
> Це дозволяє контролювати доступ до даних через геттери та сеттери.
>
> **Чи повинні поля бути `final`?**
>
> * **Ні, не обов'язково.** POJO може бути як **змінним (mutable)**, так і **незмінним (immutable)**.
> * Якщо POJO є **змінним**, поля не будуть `final`, і для них будуть надані сеттери.
>     `private String name;`
> * Якщо ж ви проектуєте **незмінний POJO**, тоді його поля **повинні бути `private final`**, а значення для них задаватися лише через конструктор. Сеттерів у такому випадку не буде.
>     `private final String name;`
>
> Отже, `private` — це практично завжди стандарт для хорошого POJO. `final` — це вибір дизайну, який залежить від того, чи потрібен вам змінний чи незмінний об'єкт.

#### **📌 Що перевіряють:**

*Розуміння того, що POJO — це гнучка концепція, яка може бути реалізована і як змінний, і як незмінний клас.*

* * *

### **10 ❓ Питання:**

Чому DTO часто роблять серіалізованими (`implements Serializable`)? ⭐️

> #### **💬 Відповідь:**
>
> DTO роблять серіалізованими, тому що їхнє основне призначення — **передача даних між різними процесами або шарами**.
>
> **Серіалізація** — це процес перетворення об'єкта в потік байтів, який можна зберегти у файл, передати по мережі тощо.
>
> **Сценарії, де потрібна серіалізація DTO:**
>
> 1. **REST API:** Коли ви відправляєте відповідь з сервера на клієнт, об'єкт DTO серіалізується в **JSON** або **XML** (наприклад, бібліотеками Jackson або JAXB). `Serializable` для цього не завжди потрібен, але є гарною практикою.
> 2. **Remote Method Invocation (RMI) або інші RPC-фреймворки:** При віддаленому виклику методів об'єкти-параметри та результати повинні бути серіалізовані для передачі по мережі.
> 3. **Кешування:** При кешуванні об'єктів (наприклад, в Redis або Memcached) їх часто серіалізують перед збереженням.
> 4. **Збереження стану сесії:** Веб-сервери можуть серіалізувати об'єкти в сесії для реплікації між вузлами кластера.
>
> Реалізація `Serializable` є "маркером", який вказує, що об'єкт призначений для такої передачі або збереження.

#### **📌 Що перевіряють:**

*Розуміння механізмів взаємодії між різними частинами розподіленої системи.*

* * *

### **11 ❓ Питання:**

У багатошаровій архітектурі (Controller-Service-Repository), на якому шарі зазвичай відбувається перетворення Entity в DTO? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Зазвичай, перетворення з доменної сутності (Entity) в DTO відбувається на **сервісному шарі (Service Layer)**.
>
> **Пояснення потоку даних:**
>
> 1. **Repository Layer:** Його відповідальність — робота з базою даних. Репозиторій отримує з БД **`Entity`** об'єкти і передає їх вище.
>
> 2. **Service Layer:**
>     * Це шар, що містить основну **бізнес-логіку**. Він отримує `Entity` від репозиторію.
>     * Він виконує бізнес-операції.
>     * Після цього, перед тим як повернути результат на шар контролера, він **перетворює (мапить)** `Entity` (або кілька `Entity`) у **`DTO`**.
>     * Таким чином, шар контролера **ніколи не працює з доменними сутностями**, а отримує лише ті дані, які потрібні для відображення.
>
> 3. **Controller Layer:**
>     * Його відповідальність — обробка HTTP-запитів.
>     * Він отримує `DTO` від сервісного шару.
>     * Він передає `DTO` для серіалізації в JSON/XML і відправляє його як HTTP-відповідь.
>
> Цей підхід забезпечує чітке розділення відповідальності та приховує доменну модель від зовнішнього API.

#### **📌 Що перевіряють:**

*Розуміння принципів проектування багатошарової архітектури. Це стандартне питання для Java-розробників, що працюють з веб-додатками.*

* * *

### **12 ❓ Питання:**

Чи може DTO містити інший DTO? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, звісно.** Це поширена практика для представлення складних, вкладених структур даних.
>
> **Приклад:**
> Уявімо, що нам потрібно повернути інформацію про **замовлення**, яке містить інформацію про **клієнта** та список **товарів**.
>
> **DTO-структура:**
>
> ```java
> public record OrderItemDto(String productName, int quantity, BigDecimal price) { }
>
> public record CustomerDto(long id, String fullName) { }
>
> public record OrderDto(long orderId,
>                        LocalDateTime orderDate,
>                        CustomerDto customer,
>                        List<OrderItemDto> items) {
> }
> ```
>
> У цьому прикладі:
>
> * `OrderDto` **містить (`HAS-A`)** один об'єкт `CustomerDto`.
> * `OrderDto` **містить (`HAS-A`)** список об'єктів `OrderItemDto`.
>
> Це дозволяє побудувати складну, але чітко структуровану "посилку" з даними для передачі клієнту в одному API-відповіді, точно відображаючи зв'язки між сутностями.

#### **📌 Що перевіряють:**

*Здатність проектувати складні структури даних для API, використовуючи композицію DTO.*

* * *

### **13 ❓ Питання:**

Чому в деяких проектах можна зустріти і `UserDto`, і `UserRequestDto`, і `UserResponseDto`? У чому різниця? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використання різних DTO для запитів та відповідей є **гарною архітектурною практикою**, яка підвищує безпеку та гнучкість API.
>
> **`UserRequestDto` (або `UserCreateDto`, `UserUpdateDto`):**
>
> * **Призначення:** Представляє дані, які клієнт **надсилає на сервер** для створення або оновлення ресурсу.
> * **Склад:** Містить лише ті поля, які клієнт може і повинен задавати. Наприклад, `name`, `email`, `password`. Не містить `id`, `creationDate`, `role` (якщо роль не можна задавати напряму).
> * Також часто містить валідаційні анотації (`@NotNull`, `@Size` тощо).
>
> **`UserResponseDto` (або просто `UserDto`):**
>
> * **Призначення:** Представляє дані, які **сервер відправляє клієнту** у відповідь.
> * **Склад:** Містить лише ті поля, які клієнт повинен бачити. Наприклад, `id`, `name`, `email`, `creationDate`. **Ніколи не містить чутливих даних**, як-от хеш пароля.
>
> **Чому їх розділяють:**
>
> 1. **Безпека:** Щоб ніколи випадково не "виставити" назовні чутливі поля (пароль) і не дозволити клієнту змінювати поля, які не призначені для зміни (`id`, `role`).
> 2. **Різні набори полів:** Дані, що потрібні для створення об'єкта, і дані, що потрібні для його відображення, часто відрізняються.
> 3. **Різна валідація:** Вимоги до даних у запиті (наприклад, "пароль не може бути порожнім") відрізняються від даних у відповіді.
>
> Розділення DTO на запит та відповідь є ознакою добре спроектованого REST API.

#### **📌 Що перевіряють:**

*Розуміння найкращих практик проектування REST API. Це питання для розробників рівня Middle+.*

* * *

### **14 ❓ Питання:**

Чи повинен DTO мати `equals()` та `hashCode()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це залежить від **сценарію використання**.
>
> * **Зазвичай, ні:** Якщо DTO використовується виключно як "одноразовий" контейнер для передачі даних з сервісу в контролер, а потім у JSON-відповідь, то йому не потрібні `equals()` та `hashCode()`. Ніхто не буде порівнювати ці об'єкти або класти їх у `HashSet`.
>
> * **Коли так:** Потреба в них виникає, якщо ви **обробляєте DTO у колекціях** або **використовуєте їх у юніт-тестах** для порівняння очікуваного результату з фактичним.
>
>     ```java
>     // У юніт-тесті
>     UserDto expected = new UserDto("John", 30);
>     UserDto actual = userService.getUser(1);
>
>     // Це спрацює коректно, лише якщо equals() перевизначено
>     assertEquals(expected, actual);
>     ```
>
> * **Щодо `HashSet<UserDto>` або `HashMap<UserDto, ...>`:** Якщо ви кладете DTO у хеш-колекції, то перевизначення `equals()` та `hashCode()` є **обов'язковим**, інакше колекція буде працювати некоректно (на основі посилань).
>
> **Найкраща практика:**
> Сьогодні, з появою `record`, які автоматично генерують коректні `equals()` та `hashCode()`, найкращим рішенням є **просто використовувати `record` для DTO**. Це вирішує проблему без зайвих зусиль і робить ваші DTO одразу готовими до використання в тестах та колекціях.

#### **📌 Що перевіряють:**

*Прагматичне мислення. Кандидат повинен розуміти, що не потрібно додавати код, який не використовується, але також знати, коли реалізація цих методів є критичною.*

* * *

### **15 ❓ Питання:**

Як патерн Builder може бути використаний для створення DTO? Коли це доцільно? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча для простих DTO з 2-3 полями Builder може бути надлишковим, він стає дуже корисним, коли DTO є **складним і має багато опціональних полів**.
>
> **Коли це доцільно:**
>
> 1. **Багато опціональних полів:** Коли DTO має, скажімо, 10 полів, з яких 3 обов'язкові, а 7 — ні. Використання перевантажених конструкторів призведе до "телескопічного антипатерну". Builder дозволяє задавати лише потрібні поля в зручному форматі.
>
> 2. **Під час тестування:** Builder робить створення тестових DTO-об'єктів значно чистішим та більш читабельним, ніж виклик конструктора з великою кількістю `null` значень.
>
> 3. **Створення DTO по частинах:** Іноді дані для одного DTO збираються з кількох джерел. Builder дозволяє поступово "наповнювати" його, і лише в кінці викликати `.build()`.
>
> **Приклад:**
>
> ```java
> public class SearchRequestDto {
>     private final String query;
>     private final int limit;
>     private final String sortBy;
>     private final boolean ascending;
>
>     // приватний конструктор
>
>     // public static class Builder { ... }
> }
>
> // Використання
> SearchRequestDto request = new SearchRequestDto.Builder("java")
>                             .limit(20)
>                             .sortBy("date")
>                             .build();
> ```
>
> Тут ми не вказали `ascending`, і білдер може використати для нього значення за замовчуванням.
>
> В сучасному коді `record` часто є достатнім, але для дуже складних DTO з кастомною логікою створення, Builder все ще залишається потужним інструментом.

#### **📌 Що перевіряють:**

*Знання просунутих патернів проектування та здатність оцінити, коли складність певного патерну є виправданою.*
