---
layout: default
title: "Модифікатори доступу: public, private, protected, default"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 12
---

# Модифікатори доступу: public, private, protected, default

* короткий опис: Ключові слова (`public`, `private`, `protected`, `default`/`package-private`), що визначають рівень видимості членів класу (полів, методів, вкладених класів) для інших частин програми.

* поняття: `public`, `private`, `protected`, `default` (package-private).

* приклади: `public String name;`, `private int age;`.

* ключові моменти: Контроль видимості є основою інкапсуляції. `private` – найсуворіший, `public` – найвідкритіший.

* що важливо знати на співбесіді: Таблиця доступу для кожного модифікатора.

### **✅ Офіційне визначення:**

Модифікатори доступу (access modifiers) — це ключові слова в Java, які визначають рівень видимості полів, методів,
класів та інших членів класу для інших частин програми. Вони контролюють, хто може звертатись до певних елементів коду.

### **🧠 Простими словами:**

Уяви двері з різними замками:

* **`private`** — тільки для власника кімнати (класу);
* **`default`** (package-private) — для сусідів у тій же «будівлі» (пакеті);
* **`protected`** — для сусідів і дітей (підкласів), навіть якщо вони живуть в інших будівлях;
* **`public`** — двері відкриті для всіх.

---

## **📊 Порівняння модифікаторів доступу:**

|   Модифікатор   | Той самий клас | Той самий пакет | Підклас |          Інші пакети          |
|:---------------:|:--------------:|:---------------:|:-------:|:-----------------------------:|
|  **`public`**   |       ✅        |        ✅        |    ✅    |               ✅               |
| **`protected`** |       ✅        |        ✅        |    ✅    | ❌ (тільки через наслідування) |
|  **`default`**  |       ✅        |        ✅        |    ❌    |               ❌               |
|  **`private`**  |       ✅        |        ❌        |    ❌    |               ❌               |

---

## **Пояснення на пальцях (💡)**

* **private** — «Тільки я сам». Доступ є лише в межах цього класу.
* **default** (package-private) — «Для своїх». Видимість обмежена пакетом (папкою).
* **protected** — «Для своїх і дітей». Доступ у тому ж пакеті та в підкласах, навіть якщо вони в інших пакетах.
* **public** — «Відчинені двері». Доступ будь-де, звідки завгодно.

---

## **Корисні приклади (🧪)**

```java
public class Person {
    public String name;         // доступний всюди
    private int age;            // доступ лише в цьому класі
    protected String email;     // доступ у пакеті та підкласах
    String phoneNumber;         // default -- доступний лише в пакеті

    private void saySecret() {
        System.out.println("My age is " + age);
    }

    public void sayHello() {
        System.out.println("Hi, my name is " + name);
    }
}
```

---

## **Порівняння (⚖️)**

|   Модифікатор   | Доступ ззовні |  Доступ у підкласах  |         Коментар         |
|:---------------:|:-------------:|:--------------------:|:------------------------:|
|  **`private`**  |       ❌       |          ❌           | Максимальне приховування |
|  **`default`**  |       ❌       |          ❌           |   Лише в межах пакету    |
| **`protected`** |       ❌       | ✅ (при наслідуванні) |    Частково публічний    |
|  **`public`**   |       ✅       |          ✅           |    Повністю відкритий    |

---

## **🧠 Мнемоніка для запам’ятовування:**

📦 P \- D \- P \- P

**Private \< Default \< Protected \< Public**

(Чим правіше — тим ширший доступ)

* * *

## **🟩 Підсумок:**

Модифікатори доступу — це основа інкапсуляції в Java. Вони дозволяють контролювати, хто і як може працювати з даними та
логікою класу. Правильний вибір модифікатора — це як встановити надійні двері й замки в коді, які захищають від
випадкових або небажаних змін і роблять програму більш підтримуваною.

* * *

### **🔗 Корисні посилання для глибшого занурення:**

* [**Oracle Docs \- Access Control**](https://docs.oracle.com/javase/tutorial/java/javaOO/accesscontrol.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке модифікатори доступу в Java і яку основну проблему вони вирішують?

> #### **💬 Відповідь:**
>
> **Модифікатори доступу** — це ключові слова (`public`, `private`, `protected` та відсутність модифікатора, що означає `default`), які визначають **рівень видимості** членів класу (полів, методів, конструкторів) та самих класів.
>
> **Основна проблема, яку вони вирішують, — це реалізація принципу інкапсуляції.**
>
> Вони дозволяють:
>
> 1. **Приховувати деталі реалізації:** Робити внутрішні поля та допоміжні методи недоступними ззовні (`private`), щоб захистити стан об'єкта.
> 2. **Створювати стабільний публічний API:** Визначати, які методи є "публічним контрактом" класу (`public`), а які — його внутрішньою "кухнею". Це дозволяє змінювати внутрішню логіку, не ламаючи клієнтський код.
> 3. **Керувати доступом в ієрархії наслідування:** Дозволяти дочірнім класам доступ до певної функціональності батька (`protected`), яка при цьому залишається прихованою від решти світу.
>
> Таким чином, модифікатори доступу — це ключовий інструмент для побудови надійних, гнучких та безпечних програм.

#### **📝 Аналогія:**

Це як різні типи дверей у будівлі:

* `public` — головний вхід, відкритий для всіх.
* `protected` — двері для персоналу та їхніх сімей (спадкоємців).
* `default` — двері між кабінетами на одному поверсі (в одному пакеті).
* `private` — двері особистого сейфа всередині кабінету.

#### **📌 Що перевіряють:**

*Розуміння того, що модифікатори доступу — це не просто обмеження, а потужний інструмент дизайну.*

* * *

### **2 ❓ Питання:**

Назвіть усі чотири модифікатори доступу в порядку від найменш до найбільш обмежувального.

> #### **💬 Відповідь:**
>
> Рівні доступу в Java, в порядку від найширшого (найменш обмежувального) до найсуворішого (найбільш обмежувального), розташовуються так:
>
> 1. **`public` (найбільш відкритий):**
>     * Член доступний з будь-якого місця: з будь-якого іншого класу в будь-якому іншому пакеті.
>
> 2. **`protected`:**
>     * Член доступний для класів у **тому ж пакеті**, а також для **класів-спадкоємців** (навіть якщо вони знаходяться в інших пакетах).
>
> 3. **`default` (package-private):**
>     * Це рівень доступу, якщо **жодного модифікатора не вказано**.
>     * Член доступний лише для класів, що знаходяться у **тому самому пакеті**.
>
> 4. **`private` (найбільш закритий):**
>     * Член доступний **лише всередині самого класу**, в якому він оголошений.

#### **Мнемоніка:**

(Від суворого до відкритого): **P**rivate **D**ogs **P**rotect **P**ublicly - `private`, `default`, `protected`, `public`.

#### **📌 Що перевіряють:**

*Чітке знання ієрархії модифікаторів доступу, що є базовою вимогою.*

* * *

### **3 ❓ Питання:**

Поясніть, що таке `default` (package-private) доступ. Коли його доцільно використовувати?

> #### **💬 Відповідь:**
>
> **`default` (або `package-private`)** — це рівень доступу, який застосовується, якщо перед членом класу не вказано жодного модифікатора доступу (`public`, `protected` чи `private`).
>
> **Що він означає:**
>
> Член з таким доступом видимий **лише для класів, що знаходяться в тому самому пакеті (`package`)**. Він невидимий для класів з інших пакетів, навіть для класів-спадкоємців.
>
> **Коли його доцільно використовувати:**
>
> Це дуже корисний інструмент для **інкапсуляції на рівні пакета**. Він дозволяє створювати **допоміжні (helper) класи або методи**, які потрібні для роботи компонентів всередині пакета, але не повинні бути частиною публічного API цього пакета.
>
> **Сценарій:**
> У вас є пакет `com.example.service`.
>
> * Інтерфейс `UserService` та його реалізація `UserServiceImpl` є **`public`**.
> * Але для роботи сервісу потрібні допоміжні класи, наприклад, `UserMapper` (для перетворення DTO в Entity) або `ValidationUtils`. Ці класи не призначені для використання ззовні пакета `service`.
> * Роблячи їх **`default`**, ви приховуєте деталі реалізації цілого сервісного шару, роблячи його публічний API чистішим і зменшуючи зв'язність між пакетами.

#### **📌 Що перевіряють:**

*Розуміння `default` доступу як інструменту для архітектури та дизайну, а не просто як "те, що є, коли нічого не вказано".*

* * *

### **4 ❓ Питання:**

У чому основна відмінність між `protected` та `default` доступом?

> #### **💬 Відповідь:**
>
> Ці два модифікатори часто плутають, але вони вирішують різні завдання, пов'язані з наслідуванням та пакетною структурою.
>
> **Головна відмінність** — це те, як вони поводяться з **класами-спадкоємцями в інших пакетах**.
>
> * **`default` (package-private):**
>   * **Обмежений лише пакетом.**
>   * Член **не буде** видимим для дочірнього класу, якщо цей дочірній клас знаходиться в **іншому пакеті**.
>
> * **`protected`:**
>   * **Дозволяє доступ спадкоємцям, незалежно від пакета.**
>   * Член буде видимим і для класів у тому ж пакеті, **і для дочірніх класів**, навіть якщо вони розташовані в інших пакетах.
>
> **Коли використовувати `protected`:**
> `protected` спеціально створений для **проектування класів, призначених для розширення (наслідування)**. Він дозволяє базовому класу надавати своїм спадкоємцям доступ до внутрішньої функціональності, яка при цьому залишається прихованою від решти світу.

#### **📝 Коментар:**

Коротко: `default` — це "видимий для сусідів по поверху", `protected` — це "видимий для сусідів І для всіх моїх дітей, де б вони не жили".

#### **📌 Що перевіряють:**

*Чітке розмежування двох схожих, але різних рівнів видимості. Розуміння ролі `protected` у наслідуванні.*

* * *

### **5 ❓ Питання:**

Які модифікатори доступу можна застосовувати до класів верхнього рівня (top-level classes)?

> #### **💬 Відповідь:**
>
> До класів верхнього рівня (тобто, тих, що не є вкладеними) можна застосовувати **лише два** модифікатори доступу:
>
> 1. **`public`:**
>     * Клас є видимим і доступним **з будь-якого іншого класу в будь-якому пакеті**.
>
> 2. **`default` (package-private):**
>     * Якщо модифікатор не вказано (`class MyClass { ... }`), клас є видимим **лише всередині свого власного пакета**.
>
> **Модифікатори `private` та `protected` не можна застосовувати до класів верхнього рівня.** Спроба зробити це призведе до помилки компіляції.
>
> * `private class` — безглуздо, оскільки такий клас був би нікому невидимий.
> * `protected class` — також не має сенсу, оскільки `protected` пов'язаний з наслідуванням, а щоб успадкувати клас, до нього спочатку треба отримати доступ.
>
> Модифікатори `private` та `protected` можна застосовувати лише до **вкладених (nested) класів**.

#### **📌 Що перевіряють:**

*Знання того, як модифікатори доступу застосовуються до різних типів сутностей (не тільки до полів/методів, але й до класів).*

* * *

### **6 ❓ Питання:**

Як правило наслідування впливає на модифікатори доступу перевизначених методів? ⭐️

> #### **💬 Відповідь:**
>
> Правило таке: рівень доступу перевизначеного методу в дочірньому класі **не може бути більш обмежувальним**, ніж рівень доступу методу в батьківському класі. Він може бути лише **таким самим або менш обмежувальним (більш відкритим)**.
>
> **Ієрархія доступу:**
> `private` < `default` < `protected` < `public`
>
> **Приклади:**
>
> * `protected` метод можна перевизначити як `protected` або `public`.
> * `public` метод можна перевизначити **лише як `public`**.
> * **Не можна** перевизначити `public` метод як `protected`.
>
> **Чому так зроблено:**
>
> Це правило є критично важливим для дотримання **принципу підстановки Лісков (LSP)** та поліморфізму.
>
> Якщо б дочірній клас міг "звузити" доступ до успадкованого методу, то код, який раніше міг викликати цей метод через посилання на батьківський клас, "зламався" б при підстановці об'єкта дочірнього класу. Заборона звуження доступу гарантує, що контракт, визначений батьком, буде дотримано.

#### **📌 Що перевіряють:**

*Знання правил перевизначення та їх зв'язку з фундаментальними принципами ООП.*

* * *

### **7 ❓ Питання:**

Чи може `private` метод бути перевизначеним (`overridden`) у дочірньому класі? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.**
>
> **Пояснення:**
>
> 1. Модифікатор `private` робить метод **невидимим** за межами його власного класу.
> 2. Дочірній клас **не "бачить"** `private` методів свого батька, тому він **не може** їх перевизначити.
>
> Якщо ви створите в дочірньому класі метод з такою ж назвою і сигнатурою, як у `private` методу батька, це **не буде перевизначенням**. Це буде просто **новий, абсолютно незалежний метод**, який належить лише дочірньому класу і випадково має таку ж назву.
>
> **Приклад:**
>
> ```java
> class Parent {
>     private void doWork() { System.out.println("Parent work"); }
>     public void execute() { doWork(); }
> }
> class Child extends Parent {
>     // Це НЕ перевизначення. Це просто новий private метод в Child.
>     private void doWork() { System.out.println("Child work"); }
> }
>
> new Child().execute(); // Виведе "Parent work"
> ```
>
> `execute()`, успадкований з `Parent`, викликає `doWork()` зі свого власного контексту, тобто з `Parent`.

#### **📌 Що перевіряють:**

*Розуміння того, як `private` впливає на видимість і наслідування. Це перевірка на чітке розмежування між перевизначенням і просто створенням нового методу.*

* * *

### **8 ❓ Питання:**

Чому `public` поля вважаються поганою практикою з точки зору ООП? ⭐️

> #### **💬 Відповідь:**
>
> Публічні поля (`public`) є **прямим порушенням принципу інкапсуляції** і вважаються серйозним антипатерном.
>
> **Проблеми, які вони створюють:**
>
> 1. **Відсутність контролю та валідації:** Будь-яка частина коду може змінити значення поля напряму, присвоївши йому будь-що, в тому числі некоректні дані (наприклад, від'ємну ціну або `null` туди, де він не очікується). Об'єкт не може захистити свій стан.
>
> 2. **Жорстка зв'язність (Tight Coupling):** Зовнішній код стає жорстко прив'язаним до **імені та типу** цього поля. Якщо ви захочете змінити його назву або тип (наприклад, з `int` на `long`), вам доведеться змінити код **у всіх місцях**, де до цього поля був прямий доступ. Це робить рефакторинг надзвичайно складним.
>
> 3. **Негнучкість:** Ви не можете додати якусь логіку (наприклад, логування, нотифікацію) при зміні значення поля, як це можна було б зробити в сеттері.
>
> **Виняток:** Єдиний випадок, коли публічні поля є прийнятними, — це для **публічних статичних констант (`public static final`)**. Оскільки вони `final`, їх не можна змінити, і тому загрози для стану об'єкта немає.

#### **📌 Що перевіряють:**

*Розуміння того, чому інкапсуляція є важливою і які практичні проблеми виникають при її ігноруванні.*

* * *

### **9 ❓ Питання:**

Поясніть, як модифікатори доступу взаємодіють з конструкторами. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Конструктори також можуть мати модифікатори доступу, і це є важливим інструментом контролю над створенням об'єктів.
>
> * **`public` конструктор:**
>   * Це стандартний випадок. Дозволяє створювати об'єкти класу з будь-якого місця.
>
> * **`protected` конструктор:**
>   * Дозволяє створювати об'єкти лише зсередини того самого пакета або в дочірніх класах. Це часто використовується для класів, призначених для розширення, але не для прямого інстанціювання ззовні.
>
> * **`default` (package-private) конструктор:**
>   * Дозволяє створювати об'єкти лише з класів того самого пакета. Корисно для допоміжних класів, які не повинні використовуватися за межами свого пакета.
>
> * **`private` конструктор:**
>   * **Забороняє** створення об'єктів класу ззовні. Це дуже потужний інструмент, що використовується для реалізації:
>         1. **Патерну Singleton:** гарантує, що існує лише один екземпляр класу.
>         2. **Патерну Factory Method:** змушує клієнтів створювати об'єкти через спеціальні статичні методи, а не напряму.
>         3. **Утилітарних класів:** забороняє створювати екземпляри класів, що містять лише `static` методи.
>
> Таким чином, керуючи доступом до конструкторів, ми керуємо життєвим циклом об'єктів.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та розуміння того, як модифікатори доступу допомагають в їх реалізації.*

* * *

### **10 ❓ Питання:**

Чому доступ `protected` у Java іноді вважається "проблемним"? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча `protected` є корисним інструментом, його часто критикують, оскільки він може **порушувати інкапсуляцію** і створювати **неочевидні залежності**.
>
> **Проблеми з `protected`:**
>
> 1. **Слабша інкапсуляція:** `protected` робить члени класу частиною його **публічного API для спадкоємців**. Це означає, що ви більше не можете вільно змінювати ці `protected` члени, не ризикуючи "зламати" невідому кількість дочірніх класів, які можуть існувати в інших частинах програми або навіть у сторонніх бібліотеках.
>
> 2. **Зв'язність на рівні пакета:** Окрім спадкоємців, `protected` члени видимі для **всіх класів у тому самому пакеті**. Це створює тісну зв'язність всередині пакета, яка може бути небажаною. Будь-який клас у пакеті може отримати доступ до деталей реалізації, призначених лише для наслідування.
>
> 3. **Неочевидність:** Його семантика є складнішою, ніж у `public` або `private`. `default` (package-private) доступ є простішим для розуміння (просто видимість в межах пакета).
>
> **Рекомендація:**
> Багато сучасних посібників зі стилю (наприклад, від Google) рекомендують **мінімізувати використання `protected`**. Замість нього краще надавати добре спроектовані `public` або `default` API і використовувати композицію замість глибоких ієрархій наслідування. `protected` слід використовувати свідомо, лише коли ви проектуєте клас спеціально для розширення.

#### **📌 Що перевіряють:**

*Критичне мислення та розуміння архітектурних компромісів. Кандидат повинен знати не тільки, як інструмент працює, але і які проблеми він може створювати.*

* * *

### **11 ❓ Питання:**

Які модифікатори доступу можуть мати члени `interface` (поля та методи)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Рівні доступу в інтерфейсах мають строгі **неявні правила**.
>
> **Для полів:**
>
> * Будь-яке поле, оголошене в інтерфейсі, є **неявно `public static final`**.
> * Тобто, це завжди публічна статична константа. Ви не можете оголосити поле з іншим модифікатором доступу (наприклад, `private` чи `protected`).
>     `int MAX_SIZE = 100; // Автоматично стає public static final int MAX_SIZE = 100;`
>
> **Для методів:**
>
> * **До Java 8:** Всі методи були неявно **`public abstract`**.
>
> * **Починаючи з Java 8:**
>   * Абстрактні методи все ще є неявно **`public`**.
>   * Додано **`default` методи**, які є неявно **`public`** і мають реалізацію.
>   * Додано **`static` методи**, які **завжди є `public`** і мають реалізацію.
>
> * **Починаючи з Java 9:**
>   * Додано **`private` методи** (як статичні, так і нестатичні). Вони використовуються як допоміжні методи для уникнення дублювання коду всередині `default` та `static` методів самого інтерфейсу.
>
> Отже, в сучасних інтерфейсах методи можуть бути `public` або `private`.

#### **📌 Що перевіряють:**

*Знання еволюції мови Java та сучасних можливостей інтерфейсів.*

* * *

### **12 ❓ Питання:**

У вас є два класи в різних пакетах: `package1.Parent` та `package2.Child extends Parent`. Які `protected` члени `Parent` будуть доступні в `Child`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання на знання тонкощів `protected` доступу.
>
> У класі `Child` з пакета `package2` будуть доступні **лише ті `protected` члени `Parent`**, до яких `Child` звертається **через посилання на `Child` або його підкласи**.
>
> **Приклад:**
>
> ```java
> // У пакеті package1
> package package1;
> public class Parent {
>     protected void protectedMethod() {}
> }
> ```
>
> ```java
> // У пакеті package2
> package package2;
> import package1.Parent;
>
> public class Child extends Parent {
>     public void testAccess() {
>         // 1. ОК: доступ до успадкованого методу через 'this'
>         this.protectedMethod();
>         super.protectedMethod();
>
>         // 2. ОК: доступ через посилання типу Child
>         Child anotherChild = new Child();
>         anotherChild.protectedMethod();
>
>         // 3. ПОМИЛКА КОМПІЛЯЦІЇ!
>         Parent parentRef = new Parent();
>         // parentRef.protectedMethod(); // Помилка!
>     }
> }
> ```
>
> **Пояснення помилки №3:**
> Хоча `Child` успадковує від `Parent`, він не має права доступу до `protected` членів **довільного екземпляра `Parent`**, який не є екземпляром самого `Child` або його підкласу. `protected` захищає від доступу "збоку", дозволяючи доступ лише "по вертикалі" (в ієрархаті).
>
> Таким чином, `protected` означає "доступний спадкоємцям, але лише до тієї частини, яка стосується їх самих".

#### **📌 Що перевіряють:**

*Дуже глибоке та точне знання специфікації Java щодо модифікатора `protected`. Це просунуте питання.*

* * *

### **13 ❓ Питання:**

Як вибір модифікатора доступу може вплинути на продуктивність? Чи існує різниця? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> У сучасних JVM, різниця в продуктивності між викликами методів з різними модифікаторами доступу (`public`, `protected`, `default`) **практично відсутня або нехтувано мала**.
>
> Однак, є один виняток, де модифікатор може мати значний вплив — це **`private`**.
>
> **Чому `private` методи можуть бути швидшими:**
>
> * `private` методи **не можуть бути перевизначені** і є неявно `final`.
> * Це означає, що **JIT-компілятор** може застосувати до них агресивні оптимізації на етапі компіляції:
>     1. **Статичне зв'язування:** Замість повільнішого динамічного виклику через `vtable`, компілятор може використовувати прямий, статичний виклик.
>     2. **Інлайнінг (Inlining):** JIT може повністю "вбудувати" код `private` методу безпосередньо в місце його виклику, усунувши накладні витрати на виклик методу взагалі. Це найважливіша оптимізація.
>
> **Висновок:**
> З точки зору продуктивності, єдина суттєва різниця може бути між `private` методами та всіма іншими (`public`, `protected`, `default`). Однак, вибір модифікатора доступу **ніколи не повинен** базуватися на міркуваннях мікро-оптимізації. Він повинен диктуватися виключно **принципами хорошого дизайну та інкапсуляції**. Переваги від чистої архітектури завжди будуть значнішими, ніж можливий виграш у наносекундах.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як працює JIT-компілятор, та здатність відокремити реальні оптимізації від передчасних мікро-оптимізацій.*

* * *

### **14 ❓ Питання:**

Поясніть, як `public static final` створює ефективну константу в Java. Чому всі три модифікатори важливі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Комбінація **`public static final`** є стандартним ідіоматичним способом створення констант у Java. Кожен модифікатор відіграє свою ключову роль:
>
> * **`public`:**
>   * Робить константу **доступною з будь-якого місця** в програмі. Це логічно, оскільки константи часто представляють загальновідомі значення (наприклад, `Math.PI`).
>
> * **`static`:**
>   * Робить поле **членом класу, а не об'єкта**. Це гарантує, що константа існує в **єдиному екземплярі**, а не створюється копія для кожного об'єкта. Це економить пам'ять.
>   * Дозволяє звертатися до неї напряму через ім'я класу (`MyConstants.MY_CONSTANT`) без створення об'єкта.
>
> * **`final`:**
>   * Гарантує, що значення константи може бути **присвоєне лише один раз** і не може бути змінене. Це забезпечує її незмінність.
>
> **Разом вони створюють:** публічно доступне, єдине на всю програму, незмінне значення.
>
> Якщо прибрати хоча б один з модифікаторів, властивості константи будуть порушені:
>
> * Без `public` — вона не буде глобально доступною.
> * Без `static` — кожен об'єкт матиме свою копію константи, що безглуздо.
> * Без `final` — це буде просто глобальна змінна, яку будь-хто може змінити.

#### **📌 Що перевіряють:**

*Здатність пояснити синергію між різними ключовими словами та обґрунтувати, чому стандартна ідіома є саме такою.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви проектуєте бібліотеку. Як ви будете обирати модифікатори доступу для її класів та методів, щоб забезпечити гнучкість для користувачів і стабільність API? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Проектування API бібліотеки — це мистецтво балансу між наданням потужності та захистом від неправильного використання. Мій підхід базувався б на принципі **"мінімально необхідного доступу"**.
>
> 1. **Все, що можна, — `private`:**
>     * За замовчуванням, всі поля та допоміжні методи класів я б робив **`private`**. Це дає максимальну свободу для майбутніх змін внутрішньої реалізації, не ламаючи API.
>
> 2. **`public` лише для головного API:**
>     * Тільки ті класи та методи, які є **частиною офіційного публічного контракту** бібліотеки, були б `public`. Це те, з чим повинні взаємодіяти користувачі. Інтерфейси тут є ідеальним інструментом.
>
> 3. **Використання `default` (package-private) для внутрішньої взаємодії:**
>     * Якщо бібліотека складається з кількох пакетів, класи, які повинні взаємодіяти всередині одного пакета, але не мають бути видимими ззовні, я б робив **`default`**. Наприклад, `UserService` (public) може використовувати `UserValidator` (default). Це приховує внутрішню структуру пакета.
>
> 4. **`protected` — дуже обережно:**
>     * Я б використовував `protected` **лише для класів, які спеціально спроектовані для розширення (наслідування)** користувачами бібліотеки. Це явний контракт, що "ось ці методи ви можете перевизначати для кастомізації поведінки".
>
> 5. **Класи та пакети:**
>     * Ключові класи API були б `public`. Всі допоміжні класи, які не є частиною API, були б `default`. Це дозволяє мати чистий, мінімалістичний публічний інтерфейс.
>
> Такий підхід робить API **стабільним** (користувачі не залежать від деталей реалізації, які можуть змінитися) і **зрозумілим** (вони бачать лише те, що їм потрібно), а також дає мені як автору бібліотеки максимальну **гнучкість** для майбутніх рефакторингів.

#### **📌 Що перевіряють:**

*Архітектурне мислення та розуміння принципів проектування API. Кандидат повинен продемонструвати зрілий підхід до створення коду, який буде використовуватися іншими.*
