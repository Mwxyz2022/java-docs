---
layout: default
title: "Наслідування (extends)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 4
---

# Наслідування (extends)

* короткий опис: Механізм ООП, що дозволяє одному класу (підкласу) успадковувати властивості та поведінку іншого класу (суперкласу) через ключове слово `extends`.

* поняття: Наслідування, `extends`, суперклас, підклас, перевизначення.

* приклади: `class Dog extends Animal`.

* ключові моменти: Сприяє повторному використанню коду, встановлює `IS-A` відношення, Java підтримує лише одиничне наслідування класів.

* що важливо знати на співбесіді: Визначення, як працює, обмеження Java (множинне наслідування класів).

### **✅ Офіційне визначення:**

Наслідування — це принцип ООП, за яким один клас (підклас) отримує (успадковує) властивості та поведінку іншого класу (
суперкласу). У Java реалізується через ключове слово **`extends`**.

### **🧠 Простими словами:**

Наслідування — це як передати ДНК від батьків до дітей. Новий клас автоматично отримує все, що має батьківський, але може додати свої “фішки” або змінити старі.

---

## **📊 Типи учасників наслідування:**

|      Тип      |             Опис              |              Java-приклад              |
|:-------------:|:-----------------------------:|:--------------------------------------:|
| **Суперклас** | Батьківський клас, що передає |       **`class Animal { ... }`**       |
|  **Підклас**  | Дочірній клас, що успадковує  | **`class Dog extends Animal { ... }`** |

📌 Важливо:

Java підтримує **лише одиничне наслідування класів**, але може реалізовувати багато інтерфейсів (**`implements`**)
---

## **Пояснення на пальцях (💡)**

Уяви, є клас **`Animal`** з методом **`eat()`**. Якщо створити клас **`Dog`**, що наслідує **`Animal`**, то **`Dog`**
автоматично вміє їсти (**`eat()`**).

Переваги:

* Не треба дублювати код у кожному класі — економія часу й нервів.
* Підклас може:
  * Використовувати методи суперкласу
  * Додавати власні методи
  * Перевизначати (override) успадковані методи

---

## **Корисні приклади (🧪)**

```java
class Animal {
    void eat() {
        System.out.println("This animal eats food.");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("The dog barks.");
    }
}

public class Main {
    public static void main(String[] args) {
        Dog d = new Dog();
        d.eat();   // Успадкований метод
        d.bark();  // Власний метод
    }
}
```

---

## **Порівняння (⚖️)**

|        Підхід        |               Пояснення               |
|:--------------------:|:-------------------------------------:|
| **Без наслідування** |    Кожен клас реалізує все з нуля     |
| **З наслідуванням**  | Загальні методи в батьківському класі |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“Dog -> Animal”**  
**`Dog extends Animal`** — собака є твариною, отже має все, що має тварина, \+ своє.

Або коротко:  
**`extends` \= розширює функціональність**

---

## **🟩 Підсумок:**

Наслідування — це механізм створення нових класів на основі існуючих, що дає змогу повторно використовувати код і
покращувати структуру програми. Це фундаментальна концепція ООП, яка дозволяє ефективно будувати ієрархії і масштабувати
проєкти.

---

**🔗 Ресурси для глибшого занурення:**

* [**Oracle Java Tutorials: Inheritance**](https://docs.oracle.com/javase/tutorial/java/IandI/subclasses.html)
* [**Wikipedia: Inheritance (object-oriented programming)**](https://en.wikipedia.org/wiki/Inheritance_\(object-oriented_programming\))

Добре, я ознайомився з конспектом. На співбесідах на цю тему дуже важливо показати розуміння не тільки того, **що таке наслідування**, але й **коли його слід використовувати**, яких помилок уникати (наприклад, "проблема ромба") та як воно пов'язане з іншими принципами ООП.

* * *

# **FAQ — Часті запитання (❓)**

* * *

### **1 ❓ Питання:**

Що таке наслідування в Java і яке ключове слово для цього використовується?

> #### **💬 Відповідь:**
>
> **Наслідування (Inheritance)** — це один з чотирьох основних принципів ООП, який дозволяє одному класу (який називають **підкласом** або **дочірнім класом**) успадковувати **не-`private`** поля та методи іншого класу (який називають **суперкласом** або **батьківським класом**).
>
> **Головна ідея:** створити новий клас на основі існуючого, розширивши або змінивши його функціональність. Це встановлює між класами відношення **"є різновидом" ("is-a")**.
>
> Для реалізації наслідування в Java використовується ключове слово **`extends`**.
>
> **Приклад:**
>
> ```java
> // Суперклас
> class Vehicle {
>     public void startEngine() {
>         System.out.println("Двигун запущено.");
>     }
> }
>
> // Підклас успадковує метод startEngine()
> class Car extends Vehicle {
>     public void drive() {
>         System.out.println("Автомобіль їде.");
>     }
> }
> ```
>
> Тепер об'єкт `Car` матиме як свій власний метод `drive()`, так і успадкований `startEngine()`.

#### **📝 Аналогія:**

Наслідування — це як відносини між видами у біології. Клас `Кіт` успадковує властивості класу `Ссавець` (наприклад, теплокровність), який, у свою чергу, успадковує властивості класу `Тварина` (наприклад, здатність рухатися).

#### **📌 Що перевіряють:**

*Базове розуміння концепції та знання ключового слова `extends`.*

* * *

### **2 ❓ Питання:**

Які переваги надає наслідування?

> #### **💬 Відповідь:**
>
> Наслідування надає кілька важливих переваг у проектуванні програм:
>
> 1. **Повторне використання коду (Code Reusability):** Це головна перевага. Спільну логіку та дані можна винести в один батьківський клас, і всі дочірні класи зможуть їх використовувати, уникаючи дублювання коду.
>
> 2. **Створення ієрархій:** Дозволяє будувати логічні та зрозумілі ієрархії класів (наприклад, `Shape` → `Circle`, `Rectangle`), що відображає структуру предметної області.
>
> 3. **Поліморфізм:** Наслідування є основою для реалізації поліморфізму часу виконання. Воно дозволяє працювати з об'єктами різних дочірніх класів через єдиний інтерфейс їхнього спільного батьківського класу.
>
> 4. **Розширюваність:** Легко додавати нові класи з новою функціональністю, успадковуючи від існуючих, не змінюючи при цьому старий, перевірений код. Це відповідає принципу відкритості/закритості.

#### **📌 Що перевіряють:**

*Розуміння практичної користі наслідування для побудови гнучких та масштабованих систем.*

* * *

### **3 ❓ Питання:**

Які члени класу (поля, методи) успадковуються, а які ні?

> #### **💬 Відповідь:**
>
> Дочірній клас успадковує більшість членів батьківського класу, але з певними обмеженнями, що визначаються **модифікаторами доступу**.
>
> **Що успадковується:**
>
> * **`public`** поля та методи.
> * **`protected`** поля та методи.
>
> **Що не успадковується:**
>
> * **`private`** поля та методи. Вони є видимими лише всередині самого батьківського класу. Дочірній клас не має до них прямого доступу.
> * **Конструктори.** Конструктори не успадковуються, але конструктор дочірнього класу **повинен** викликати один з конструкторів батьківського класу (явно через `super()` або неявно).
>
> **Члени з доступом `default` (package-private):**
>
> * Успадковуються, **тільки якщо** дочірній клас знаходиться в **тому ж пакеті**, що і батьківський.

#### **📝 Коментар:**

Обмеження на успадкування `private` членів є важливою частиною механізму інкапсуляції.

#### **📌 Що перевіряють:**

*Знання правил видимості та того, як модифікатори доступу впливають на механізм наслідування.*

* * *

### **4 ❓ Питання:**

Що таке одиничне наслідування, і чому Java не підтримує множинне наслідування класів?

> #### **💬 Відповідь:**
>
> **Одиничне наслідування (Single Inheritance)** — це правило, згідно з яким клас може мати **лише одного** безпосереднього батьківського класу. Це означає, що конструкція `class C extends A, B { ... }` є невалідною в Java.
>
> **Чому Java не підтримує множинне наслідування класів:**
>
> Основна причина — це **"проблема ромба" (Diamond Problem)**.
>
> Уявімо таку ієрархію:
>
> ```
>      A (має метод doWork())
>     / \
>    B   C  (обидва перевизначають doWork())
>     \ /
>      D   (успадковує від B і C)
> ```
>
> Якщо б клас `D` міг успадкувати і від `B`, і від `C`, виникає неоднозначність: **яку версію методу `doWork()`** повинен викликати об'єкт `D`? Версію з `B` чи з `C`?
>
> Щоб уникнути цієї складної проблеми та пов'язаної з нею неоднозначності, творці Java вирішили повністю заборонити множинне наслідування класів (реалізації).
>
> **Альтернатива:**
> Для досягнення множинного наслідування **типів (контрактів)**, в Java використовується **реалізація багатьох інтерфейсів** (`class MyClass implements InterfaceA, InterfaceB`). Починаючи з Java 8, `default` методи в інтерфейсах частково повертають "проблему ромба", але вона вирішується вимогою до класу явно перевизначити конфліктний метод.

#### **📌 Що перевіряють:**

*Розуміння дизайнерських рішень Java, знання проблеми ромба та того, як інтерфейси використовуються для обходу обмежень наслідування.*

* * *

### **5 ❓ Питання:**

Що таке ключове слово `super` і в яких двох випадках воно використовується?

> #### **💬 Відповідь:**
>
> Ключове слово `super` використовується в дочірньому класі для звернення до членів його безпосереднього батьківського класу.
>
> **Два основні випадки використання:**
>
> 1. **Виклик конструктора батьківського класу:**
>     * Використовується у формі `super(...)` для виклику одного з конструкторів суперкласу.
>     * Цей виклик **повинен бути першим рядком** у тілі конструктора дочірнього класу.
>     * Якщо виклик `super()` не зроблено явно, компілятор автоматично додає неявний виклик `super()` (конструктора без аргументів) батьківського класу.
>
>     ```java
>     class Child extends Parent {
>         public Child(int value) {
>             super(value); // Явний виклик конструктора Parent(int)
>         }
>     }
>     ```>
> 2. **Звернення до полів та методів батьківського класу:**
>     * Використовується у формі `super.methodName()` або `super.fieldName` для доступу до методу або поля суперкласу, особливо якщо вони були **перевизначені (overridden)** або **приховані (hidden)** у дочірньому класі.
>
>     ```java
>     class Child extends Parent {
>         @Override
>         public void doWork() {
>             super.doWork(); // Викликаємо реалізацію з Parent
>             System.out.println("...а потім робимо додаткову роботу.");
>         }
>     }
>     ```
>

#### **📌 Що перевіряють:**

*Практичні знання про те, як відбувається взаємодія між дочірнім та батьківським класами.*

* * *

### **6 ❓ Питання:**

У чому різниця між перевизначенням (`Overriding`) та перевантаженням (`Overloading`) методів?

> #### **💬 Відповідь:**
>
> Це два абсолютно різних механізми, хоч і звучать схоже.
>
> **Перевизначення (Method Overriding):**
>
> * **Де відбувається:** У **дочірньому класі**.
> * **Що робить:** Дочірній клас надає **нову реалізацію** для методу, який вже існує в батьківському класі.
> * **Вимоги:** Назва методу, тип повернення (або його підтип — коваріантність) та список параметрів **повинні повністю збігатися** з методом батьківського класу.
> * **Зв'язок:** Пов'язано з **наслідуванням** та **поліморфізмом часу виконання (runtime)**.
> * **Анотація:** Використовується анотація `@Override`, щоб компілятор перевірив коректність перевизначення.
>
> **Перевантаження (Method Overloading):**
>
> * **Де відбувається:** В **одному класі**.
> * **Що робить:** Клас має кілька методів з **однаковою назвою**, але **різними списками параметрів** (різна кількість, типи або порядок параметрів).
> * **Вимоги:** Різні сигнатури параметрів. Тип повернення може відрізнятися.
> * **Зв'язок:** Це форма **статичного поліморфізму (compile-time)**. Компілятор вирішує, яку версію методу викликати, на основі переданих аргументів.
>
> **Приклад:**
> `System.out.println()` — класичний приклад перевантаження (для `int`, `String`, `double` і т.д.).
> `toString()` в класах — приклад перевизначення методу з класу `Object`.

#### **📌 Що перевіряють:**

*Здатність чітко розмежувати ці дві фундаментальні концепції ООП.*

* * *

### **7 ❓ Питання:**

Які правила та обмеження існують для перевизначення методів (`@Override`)? ⭐️

> #### **💬 Відповідь:**
>
> Існує кілька строгих правил:
>
> 1. **Сигнатура методу:** Назва методу та типи/порядок параметрів у дочірньому класі повинні **точно збігатися** з методом батьківського класу.
>
> 2. **Тип повернення:**
>     * Повинен бути **таким самим**, як у батьківському методі.
>     * Або (з Java 5) він може бути **коваріантним**, тобто підтипом типу, що повертається батьківським методом. Наприклад, якщо батьківський метод повертає `Animal`, дочірній може повертати `Dog`.
>
> 3. **Модифікатор доступу:** Модифікатор доступу у перевизначеному методі **не може бути більш суворим**, ніж у батьківському. Він може бути лише таким самим або менш суворим.
>     * `protected` можна перевизначити як `protected` або `public`.
>     * `public` можна перевизначити лише як `public`.
>     * Не можна `public` метод перевизначити як `protected`.
>
> 4. **Винятки (`throws`):** Перевизначений метод не може кидати **нові перевірювані (checked) винятки**, яких не було в сигнатурі батьківського методу. Він може кидати ті самі винятки, їхні підкласи, або не кидати взагалі.
>
> 5. **`final` та `static` методи:**
>     * `final` методи **не можна** перевизначити.
>     * `static` методи **не можна** перевизначити (їх можна лише приховати).

#### **📌 Що перевіряють:**

*Знання технічних деталей та контрактів мови Java щодо перевизначення методів.*

* * *

### **8 ❓ Питання:**

Як клас `Object` пов'язаний з наслідуванням у Java? ⭐️

> #### **💬 Відповідь:**
>
> Клас **`java.lang.Object`** є коренем усієї ієрархії класів у Java.
>
> **Ключовий зв'язок:**
> **Кожен клас у Java неявно успадковує клас `Object`**, якщо він явно не успадковує інший клас.
>
> Якщо ви пишете:
> `class MyClass { ... }`
>
> то компілятор розглядає це як:
> `class MyClass extends java.lang.Object { ... }`
>
> **Наслідки:**
>
> 1. **Спільна базова функціональність:** Усі об'єкти в Java автоматично успадковують базові методи з `Object`, такі як:
>     * `toString()`
>     * `equals(Object obj)`
>     * `hashCode()`
>     * `getClass()`
>     * `clone()`, `finalize()` (хоч і застарілі) та методи для роботи з потоками (`wait`, `notify`, `notifyAll`).
> 2. **Поліморфізм:** Оскільки всі класи є нащадками `Object`, можна створити змінну типу `Object`, яка може посилатися на об'єкт будь-якого класу. Це забезпечує максимальний рівень узагальнення.
>
> Таким чином, `Object` є спільною відправною точкою для всієї системи типів Java.

#### **📌 Що перевіряють:**

*Знання основи ієрархії класів Java та розуміння, звідки у всіх об'єктів з'являються базові методи.*

* * *

### **9 ❓ Питання:**

Що таке "надавати перевагу композиції над наслідуванням" і чому це є гарною практикою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це фундаментальний принцип об'єктно-орієнтованого дизайну, який рекомендує досягати **повторного використання коду** за допомогою **композиції**, а до **наслідування** вдаватися обережніше.
>
> * **Наслідування** реалізує відношення **"is-a" (є різновидом)**.
> * **Композиція** реалізує відношення **"has-a" (має)**, коли один об'єкт містить посилання на інший.
>
> **Чому перевага надається композиції:**
>
> 1. **Гнучкість:** Композиція є набагато гнучкішою. Ви можете змінювати поведінку об'єкта **під час виконання**, підставляючи різні реалізації вкладених об'єктів. З наслідуванням тип об'єкта жорстко визначається на етапі компіляції.
>
> 2. **Слабка зв'язність:** Наслідування створює тісний зв'язок між батьківським і дочірнім класами. Дочірній клас залежить від **деталей реалізації** батька. Композиція, навпаки, залежить лише від **публічного інтерфейсу** вкладеного об'єкта.
>
> 3. **Краща інкапсуляція:** При композиції ви можете приховати існування вкладеного об'єкта і надати лише ті методи, які потрібні, не "виставляючи" весь його API назовні.
>
> 4. **Уникнення "проблеми ромба":** Композиція дозволяє "успадковувати" поведінку від кількох джерел, тоді як класове наслідування в Java одиничне.
>
> **Коли використовувати наслідування:**
> Його слід використовувати, коли між класами дійсно існує ієрархічне відношення "is-a", і ви плануєте використовувати поліморфізм (наприклад, `List<Animal>`).
>
> Для простого повторного використання коду композиція майже завжди є кращим вибором.

#### **📌 Що перевіряють:**

*Розуміння просунутих принципів дизайну та здатність оцінювати архітектурні компроміси. Це питання для розробників рівня Middle+.*

* * *

### **10 ❓ Питання:**

Що таке коваріантні типи повернення і як вони пов'язані з наслідуванням? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Коваріантні типи повернення (Covariant return types)** — це можливість, додана в Java 5, яка дозволяє перевизначеному методу в дочірньому класі повертати **підтип** того типу, що повертає батьківський метод.
>
> **До Java 5:**
> Тип повернення перевизначеного методу повинен був **точно збігатися** з типом повернення батьківського методу.
>
> **З Java 5 і пізніше:**
> Це обмеження було послаблено.
>
> **Приклад:**
>
> ```java
> class Animal {
>     public Animal reproduce() {
>         System.out.println("Тварина розмножується...");
>         return new Animal();
>     }
> }
>
> class Dog extends Animal {
>     @Override
>     // Повертаємо Dog, який є підтипом Animal
>     public Dog reproduce() {
>         System.out.println("Собака народжує цуценят...");
>         return new Dog();
>     }
> }
>
> public class Main {
>     public static void main(String[] args) {
>         Dog myDog = new Dog();
>         Dog puppy = myDog.reproduce(); // Не потрібне приведення типів (Dog)!
>     }
> }
> ```
>
> Без коваріантних типів повернення метод `reproduce()` у класі `Dog` мав би повертати `Animal`, і для отримання `Dog` знадобилося б явне приведення типів: `Dog puppy = (Dog) myDog.reproduce();`.
>
> Коваріантні типи роблять код більш **типізованим, безпечним та усувають необхідність у зайвому приведенні типів**.

#### **📌 Що перевіряють:**

*Знання сучасних (починаючи з Java 5) можливостей мови та їхньої практичної користі.*

* * *

### **11 ❓ Питання:**

Яка послідовність виклику конструкторів у ієрархії наслідування? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Послідовність виклику конструкторів завжди йде **"згори вниз"** — від найвищого суперкласу (`Object`) до самого дочірнього класу.
>
> **Правило:**
>
> 1. Першим рядком у будь-якому конструкторі є неявний або явний виклик конструктора батьківського класу (`super()`).
> 2. Тому, коли створюється об'єкт дочірнього класу, спочатку буде викликано конструктор його батька, який, у свою чергу, викличе конструктор свого батька, і так далі, аж до класу `Object`.
> 3. Лише після того, як всі конструктори батьківських класів завершать свою роботу, почне виконуватися код конструктора самого дочірнього класу.
>
> **Приклад:**
>
> ```java
> class A { A() { System.out.println("Конструктор A"); } }
> class B extends A { B() { System.out.println("Конструктор B"); } }
> class C extends B { C() { System.out.println("Конструктор C"); } }
>
> new C();
> ```
>
> **Вивід:**
>
> ```
>
> Конструктор A
> Конструктор B
> Конструктор C
>
> ```
>
> Це гарантує, що на момент ініціалізації дочірнього класу всі успадковані ним частини від батьківських класів вже будуть повністю ініціалізовані.

#### **📌 Що перевіряють:**

*Знання процесу ініціалізації об'єктів у Java. Це важливе питання для розуміння життєвого циклу об'єктів.*

* * *

### **12 ❓ Питання:**

Що станеться, якщо конструктор батьківського класу є приватним (`private`)? ⭐️

> #### **💬 Відповідь:**
>
> Якщо конструктор батьківського класу є **`private`**, то від цього класу **не можна буде успадковувати** (з певними винятками).
>
> **Пояснення:**
>
> 1. Як ми знаємо, конструктор дочірнього класу **повинен** викликати конструктор батьківського класу через `super()`.
> 2. Модифікатор `private` робить конструктор доступним **лише всередині самого батьківського класу**.
> 3. Тому з дочірнього класу викликати `private` конструктор батька неможливо.
>
> Компілятор видасть помилку: "There is no default constructor available in 'ParentClass'".
>
> Таким чином, роблячи всі конструктори класу приватними, ви фактично робите клас **неявно `final`** — його неможливо розширити. Це один із способів заборонити наслідування, альтернативний до ключового слова `final`.

#### **Виняток:**

* Вкладені (nested) класи всередині батьківського класу можуть викликати його `private` конструктор.

#### **📌 Що перевіряють:**

*Розуміння того, як модифікатори доступу для конструкторів впливають на можливість наслідування.*

* * *

### **13 ❓ Питання:**

Як анотація `@Override` допомагає уникнути помилок при наслідуванні? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Анотація `@Override` є інструментом **компілятора**, який допомагає забезпечити, що ви дійсно перевизначаєте метод, а не випадково створюєте новий.
>
> **Як вона працює:**
>
> Коли ви позначаєте метод анотацією `@Override`, компілятор виконує перевірку: чи існує в батьківському класі (або реалізованому інтерфейсі) метод з **точно такою ж сигнатурою**.
>
> **Чим вона допомагає:**
>
> Вона захищає від поширених помилок:
>
> 1. **Опечатки в назві методу:**
>
>     ```java
>     // У батька є `public void doWork()`
>     // Ви помилилися:
>     // @Override // <- тут компілятор видасть ПОМИЛКУ!
>     public void dowork() { ... }
>     ```>     Без `@Override` ви б просто створили новий, ніяк не пов'язаний метод `dowork()`, а поліморфізм не спрацював би.
>
> 2. **Помилки в параметрах:**
>
>     ```java
>     // У батька є `public void process(String s)`
>     // Ви помилилися:
>     // @Override // <- ПОМИЛКА КОМПІЛЯЦІЇ
>     public void process(Object s) { ... }
>     ```>     Без анотації це було б **перевантаження (overloading)**, а не перевизначення.
>
> Використання `@Override` є **найкращою практикою** і обов'язкове для написання надійного коду, оскільки переносить перевірку коректності наслідування з runtime (коли ви дивуєтесь, чому не викликається потрібний метод) на **compile-time**.

#### **📌 Що перевіряють:**

*Знання інструментів, які допомагають писати більш безпечний та надійний код. Розуміння ролі анотацій.*

* * *

### **14 ❓ Питання:**

Чи можна наслідувати кілька `abstract` класів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> Правило **одиничного наслідування класів** у Java застосовується як до конкретних, так і до **абстрактних** класів.
>
> Клас може використовувати `extends` лише **один раз**.
>
> ```java
> abstract class Base1 { ... }
> abstract class Base2 { ... }
>
> // ПОМИЛКА КОМПІЛЯЦІЇ
> // class MyClass extends Base1, Base2 { ... }
> ```
>
> **Пояснення:**
> Хоча абстрактні класи не можна інстанціювати, вони все ще є **класами**. Вони можуть містити поля та реалізації методів. Якби було дозволено успадковувати кілька абстрактних класів, це могло б призвести до тих самих проблем, що й множинне наслідування:
>
> * **Конфлікти полів:** Що робити, якщо в `Base1` і `Base2` є поле з однаковою назвою?
> * **Конфлікти реалізацій:** Що робити, якщо обидва класи надають різну реалізацію одного й того ж методу? (Аналог "проблеми ромба").
>
> Тому, для отримання "поведінки" з кількох джерел, слід використовувати **реалізацію багатьох інтерфейсів**. Клас може успадкувати один абстрактний клас і реалізувати кілька інтерфейсів:
> `class MyClass extends Base1 implements InterfaceA, InterfaceB { ... }`

#### **📌 Що перевіряють:**

*Точне розуміння обмежень наслідування в Java, зокрема те, що `abstract class` підкоряється тим же правилам.*

* * *

### **15 ❓ Питання:**

Що таке "приховування полів" (Field Hiding) при наслідуванні, і чому це є поганою практикою? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Приховування полів** — це ситуація, коли дочірній клас оголошує поле з **такою ж назвою**, як у батьківського класу.
>
> На відміну від методів, поля в Java **не перевизначаються (`override`)**. Замість цього поле дочірнього класу просто "приховує" поле батьківського.
>
> **Як це працює:**
>
> * Доступ до поля визначається **статично**, на основі **типу посилання**, а не фактичного типу об'єкта.
>
> **Приклад:**
>
> ```java
> class Parent {
>     public String name = "Parent";
> }
>
> class Child extends Parent {
>     public String name = "Child"; // Приховує поле 'name' з Parent
> }
>
> public class Main {
>     public static void main(String[] args) {
>         Child child = new Child();
>         System.out.println(child.name); // Виведе "Child"
>
>         Parent parent = child; // Посилання типу Parent вказує на об'єкт Child
>         System.out.println(parent.name); // Виведе "Parent"!
>     }
> }
> ```
>
> Останній рядок виводить `"Parent"`, тому що тип посилання — `Parent`, і компілятор звертається до поля `name` саме цього класу, ігноруючи поле в `Child`.
>
> **Чому це погана практика:**
>
> * **Плутанина та помилки:** Це створює надзвичайно заплутаний та неінтуїтивний код. Поведінка програми починає залежати від типу посилання, що є джерелом важковиловлюваних багів.
> * **Порушення принципу найменшого здивування:** Код поводиться не так, як від нього очікуєш.
>
> Тому слід **завжди уникати** оголошення полів у дочірніх класах з тими ж іменами, що й у батьківських. Поля завжди краще робити `private` і працювати з ними через методи.

#### **📌 Що перевіряють:**

*Експертні знання про те, як наслідування працює для полів, на відміну від методів. Розуміння концепції статичного зв'язування для полів.*
