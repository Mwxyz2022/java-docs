---
layout: default
title: "Перевантаження методів (Method Overloading)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 7
---

# Перевантаження методів (Method Overloading)

* короткий опис: Можливість оголосити в одному класі кілька методів з однаковою назвою, але з різною кількістю, типами або порядком параметрів. Це форма статичного (compile-time) поліморфізму.

* поняття: Перевантаження, `compile-time polymorphism`.

* приклади: `add(int a, int b)`, `add(double a, double b)`, `add(int a, int b, int c)`.

* ключові моменти: Вибір методу відбувається під час компіляції, тип повернення не враховується.

* що важливо знати на співбесіді: Визначення, правила, відмінність від перевизначення.

### **✅ Офіційне визначення:**

Перевантаження методу (Overloading) — це можливість оголосити в одному класі кілька методів з однаковою назвою, але з різною кількістю або типами параметрів. Остаточний вибір методу відбувається на етапі компіляції (compile-time polymorphism).

### **🧠 Простими словами:**

У тебе є метод **`print()`**, і ти хочеш друкувати як текст, так і число. Замість того, щоб робити **`printText()`** і **`printNumber()`**, ти просто перевантажуєш один метод **`print(...)`** для різних випадків. Модерн-версія “Кнопка ‘Друк’ — натискай, що хочеш\!” 😎

---

## **📊 Способи перевантаження методу за параметрами:**

| Вид зміни параметрів | Приклад |
| :---: | ----- |
| **Зміна кількості** | **`sum(int a, int b)`** vs **`sum(int a, int b, int c)`** |
| **Зміна типу** | **`sum(int a, double b)`** vs **`sum(double a, double b)`** |
| **Зміна порядку параметрів** | **`sum(int a, String b)`** vs **`sum(String b, int a)`** |

---

## **Пояснення на пальцях (💡)**

Метод — це як кнопка «друк». Але ти можеш натиснути її з різним вмістом: текстом, числом, датою...

Java вибирає потрібний метод, ґрунтуючись на параметрах, а не на імені. І тут без сюрпризів — усе це статичний поліморфізм: вибір методу відбувається ще до запуску програми.

---

## **Корисні приклади (🧪)**

```java

public class Calculator {

    public int add(int a, int b) {
        return a + b;
    }

    public double add(double a, double b) {
        return a + b;
    }

    public int add(int a, int b, int c) {
        return a + b + c;
    }
}

public class Main {
    public static void main(String[] args) {
        Calculator calc = new Calculator();

        System.out.println(calc.add(2, 3));         // 5
        System.out.println(calc.add(2.5, 3.1));     // 5.6
        System.out.println(calc.add(1, 2, 3));      // 6
    }
}

```

---

## **Порівняння (⚖️)**

| Параметри | Вибраний метод |
| :---: | :---: |
| **`add(2, 3)`** | **`int add(int, int)`** |
| **`add(2.0, 3.0)`** | **`double add(double, double)`** |
| **`add(1, 2, 3)`** | **`int add(int, int, int)`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧠 **`Overloading`** \= Один метод, багато варіантів

Запам’ятай: ім’я одне — параметри різні
---

## **🟩 Підсумок:**

Перевантаження методів робить код чистішим і зручнішим. Замість десятків методів із різними назвами маєш одну дію — **`print(), add(), save()`** — яка працює з різними даними. Це полегшує читання, підтримку і розширення коду.

---

### **🔗 Корисні посилання для глибшого вивчення:**

* [**Oracle Docs: Method Overloading**](https://docs.oracle.com/javase/tutorial/java/javaOO/methods.html) **— офіційна документація**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке перевантаження методів (Method Overloading)? Яка його головна мета?

> #### **💬 Відповідь:**
>
> **Перевантаження методів (Method Overloading)** — це можливість мати в одному класі **кілька методів з однаковою назвою**, але з **різними списками параметрів**. Різниця в параметрах може бути в їхній:
>
> * **кількості**,
> * **типах**,
> * або **порядку**.
>
> **Головна мета** — підвищити **читабельність та зручність** використання API. Замість того, щоб створювати багато методів з різними іменами для схожих операцій (наприклад, `addInts`, `addDoubles`), ми можемо використовувати одне й те саме ім'я (`add`), а компілятор сам обере правильну версію залежно від переданих аргументів.
>
> Це є формою **статичного (compile-time) поліморфізму**.

#### **📝 Приклад:**

```java
public class Printer {
    void print(String s) { /* ... */ }
    void print(int i) { /* ... */ }
    void print(String s, int copies) { /* ... */ }
}
```

Тут метод `print` перевантажено, щоб він міг працювати з різними типами та кількістю аргументів.

#### **📌 Що перевіряють:**

*Базове розуміння концепції, її мети та знання правил, за якими методи вважаються перевантаженими.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між перевантаженням (`Overloading`) та перевизначенням (`Overriding`) методів?

> #### **💬 Відповідь:**
>
> Це одне з найчастіших і найважливіших питань для розрізнення двох основних концепцій поліморфізму.
>
| Характеристика | Перевантаження (`Overloading`) | Перевизначення (`Overriding`) |
| :---: | :---: | :---: |
| **Де відбувається** | В **одному класі** (або між батьківським і дочірнім). | Лише в **дочірньому класі**. |
| **Сигнатура методу** | Назва та сама, **список параметрів різний**. | Назва, список параметрів, тип повернення (або коваріантний) **повинні бути однаковими**. |
| **Призначення** | Надати той самий метод для роботи з **різними даними**. | Надати **нову, специфічну реалізацію** для методу, успадкованого від батька. |
| **Тип поліморфізму** | **Статичний** (вирішується на етапі компіляції). | **Динамічний** (вирішується на етапі виконання). |
| **Приклад** | `System.out.println()` для `int`, `String`, `boolean`... | Метод `toString()` в вашому класі, що перевизначає `toString()` з `Object`. |

#### **📌 Що перевіряють:**

*Чітке розмежування цих двох фундаментальних концепцій. Це класичне питання для будь-якого рівня співбесіди.*

* * *

### **3 ❓ Питання:**

Чи можна перевантажити метод, змінивши лише тип повернення? Чому?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Спроба оголосити два методи з однаковою назвою та однаковим списком параметрів, але різними типами повернення, призведе до **помилки компіляції**.
>
> **Приклад коду, що не скомпілюється:**
>
> ```java
> public class Test {
>     public int doSomething() { return 1; }
>     // ПОМИЛКА: 'doSomething()' is already defined in 'Test'
>     // public String doSomething() { return "1"; }
> }
> ```
>
> **Чому так?**
>
> Компілятор Java визначає, яку версію методу викликати, **лише на основі назви методу та типів переданих аргументів**. Тип повернення **не є частиною сигнатури** для цілей перевантаження.
>
> Якби це було дозволено, виникла б неоднозначність у таких викликах:
>
> `doSomething();`
>
> Компілятор не знав би, яку версію методу викликати — ту, що повертає `int`, чи ту, що повертає `String`, оскільки результат нікуди не присвоюється. Щоб уникнути такої плутанини, це правило є строгим.

#### **📌 Що перевіряють:**

*Знання правил, що визначають унікальність сигнатури методу для перевантаження. Розуміння того, що тип повернення не бере в цьому участі.*

* * *

### **4 ❓ Питання:**

Поясніть, як компілятор обирає правильну версію перевантаженого методу. Що станеться, якщо виникне неоднозначність?

> #### **💬 Відповідь:**
>
> Компілятор проходить через трьохетапний процес, щоб знайти найбільш специфічний метод для виклику:
>
> **Етап 1: Пошук точної відповідності та розширення примітивів (Widening)**
>
> 1. Компілятор шукає метод, сигнатура якого **точно відповідає** типам переданих аргументів.
> 2. Якщо точної відповідності немає, він намагається знайти метод, куди аргументи можуть бути передані через **розширення примітивних типів** (наприклад, `int` до `long`, `float` до `double`).
>
> **Етап 2: Автоупаковка та авторозпаковка (Autoboxing/Unboxing)**
>
> 1. Якщо попередній крок не дав результату, компілятор намагається застосувати **автоупаковку** (наприклад, `int` до `Integer`) або **авторозпаковку** (`Integer` до `int`).
> 2. Він може поєднувати це з розширенням (наприклад, `int` → `Integer` → `Object`).
>
> **Етап 3: Змінна кількість аргументів (Varargs)**
>
> 1. В останню чергу компілятор шукає метод зі змінною кількістю аргументів (`String...`), куди можна "запакувати" передані аргументи.
>
> **Неоднозначність (Ambiguity):**
>
> Якщо на будь-якому з етапів компілятор знаходить **два або більше методів**, які є однаково "хорошими" кандидатами, він не може зробити вибір і видає **помилку компіляції** через неоднозначність виклику (ambiguous method call).
>
> **Приклад неоднозначності:**
>
> ```java
> void test(int i, long l) { ... }
> void test(long l, int i) { ... }
> // test(10, 10); // ПОМИЛКА! Незрозуміло, який метод викликати.
> ```

#### **📌 Що перевіряють:**

*Розуміння алгоритму вибору методу (method resolution). Це просунуте питання, що вимагає знання про numeric promotion та autoboxing.*

* * *

### **5 ❓ Питання:**

Чи можна перевантажити конструктори? Якщо так, то як і навіщо?

> #### **💬 Відповідь:**
>
> **Так, можна, і це дуже поширена практика.**
>
> **Як перевантажити:**
> Конструктори перевантажуються за тими ж правилами, що й звичайні методи: вони повинні мати **різні списки параметрів**.
>
> **Навіщо це потрібно:**
>
> Перевантаження конструкторів надає **різні способи створення та ініціалізації об'єкта**, що робить клас більш гнучким.
>
> **Типові сценарії:**
>
> 1. **Конструктор без аргументів (за замовчуванням):** Створює об'єкт зі стандартними значеннями.
> 2. **Конструктор з основними параметрами:** Дозволяє створити об'єкт з найважливішими даними.
> 3. **Конструктор копіювання:** Приймає інший об'єкт того ж класу для створення копії.
> 4. **Повний конструктор:** Дозволяє ініціалізувати всі поля класу.
>
> **Приклад:**
>
> ```java
> public class User {
>     private String name;
>     private int age;
>
>     public User() { // 1. За замовчуванням
>         this("Guest", 0);
>     }
>
>     public User(String name) { // 2. Лише з іменем
>         this(name, 0);
>     }
>
>     public User(String name, int age) { // 3. Повний
>         this.name = name;
>         this.age = age;
>     }
> }
> ```
>
> Зверніть увагу на використання `this(...)` для виклику іншого конструктора в тому ж класі. Це допомагає уникнути дублювання коду ініціалізації.

#### **📌 Що перевіряють:**

*Знання правил роботи з конструкторами та розуміння практичної користі їх перевантаження.*

* * *

### **6 ❓ Питання:**

Який з методів буде викликано і чому?

```java
public void test(Object o) { System.out.println("Object"); }
public void test(String s) { System.out.println("String"); }
//...
test(null);
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється**, оскільки виклик є **неоднозначним (ambiguous)**.
>
> **Пояснення:**
>
> 1. Літерал `null` не має власного типу. Він може бути присвоєний будь-якому посилальному типу.
> 2. Коли компілятор бачить виклик `test(null)`, він знаходить двох потенційних кандидатів: `test(Object o)` та `test(String s)`.
> 3. З точки зору ієрархії успадкування, `String` є **більш специфічним типом**, ніж `Object`. Зазвичай, компілятор віддає перевагу більш специфічному методу.
> 4. **Однак**, в даному випадку, оскільки `null` може представляти і `String`, і `Object`, і багато інших типів, компілятор не може зробити однозначний вибір між двома перевантаженими версіями.
> 5. В результаті він видає помилку "reference to test is ambiguous".
>
> **Щоб виправити це, потрібно явно вказати тип:**
>
> ```java
> test((String) null); // Викличе test(String)
> test((Object) null); // Викличе test(Object)
> ```

#### **📌 Що перевіряють:**

*Розуміння того, як перевантаження працює з `null` і що таке неоднозначність виклику.*

* * *

### **7 ❓ Питання:**

Що виведе цей код? Який метод буде викликано?

```java
public void method(long l) { System.out.println("long"); }
public void method(Integer i) { System.out.println("Integer"); }
//...
int i = 5;
method(i);
```

⭐️

> #### **💬 Відповідь:**
>
> Буде виведено: **`long`**.
>
> **Пояснення процесу вибору (Method Resolution):**
>
> Компілятор аналізує можливі шляхи перетворення аргументу `int i` до типів параметрів:
>
> * **Кандидат 1: `method(long l)`**
>   * Це вимагає одного перетворення: **розширення примітивного типу (widening)** з `int` до `long`.
>
> * **Кандидат 2: `method(Integer i)`**
>   * Це вимагає одного перетворення: **автоупаковка (autoboxing)** з `int` до `Integer`.
>
> Згідно зі специфікацією мови Java, **розширення примітивних типів має вищий пріоритет**, ніж автоупаковка.
>
> Оскільки існує валідний шлях через розширення (`int` → `long`), компілятор обере саме його і не буде розглядати варіант з автоупаковкою.
>
> Тому буде викликано метод `method(long l)`.

#### **📌 Що перевіряють:**

*Глибокі знання правил вирішення перевантажень, зокрема пріоритету розширення над автоупаковкою.*

* * *

### **8 ❓ Питання:**

Поясніть, як перевантаження взаємодіє з наслідуванням.

> #### **💬 Відповідь:**
>
> Методи, успадковані від батьківського класу, також беруть участь у процесі вирішення перевантаження.
>
> Це означає, що дочірній клас може:
>
> * **Перевантажити успадкований метод:** Створити в дочірньому класі метод з такою ж назвою, як у батька, але з іншими параметрами.
> * **Використовувати успадковані перевантажені методи:** Усі `public` та `protected` перевантажені версії методу з батьківського класу доступні в дочірньому.
>
> **Приклад:**
>
> ```java
> class Parent {
>     public void print(String s) { System.out.println("Parent: " + s); }
> }
>
> class Child extends Parent {
>     // Перевантажуємо метод з Parent
>     public void print(int i) {
>         System.out.println("Child: " + i);
>     }
> }
>
> //...
> Child c = new Child();
> c.print("Hello"); // Викликає успадкований метод з Parent
> c.print(100);    // Викликає власний перевантажений метод
> ```
>
> Коли компілятор шукає відповідний метод для виклику, він розглядає **всі доступні методи** — як власні, так і успадковані — і обирає найбільш специфічний.

#### **📌 Що перевіряють:**

*Розуміння того, як перевантаження працює в межах ієрархії класів.*

* * *

### **9 ❓ Питання:**

Чи можна перевантажити `private` методи? ⭐️

> #### **💬 Відповідь:**
>
> **Так, звісно.**
>
> Правила перевантаження застосовуються до будь-яких методів усередині класу, незалежно від їхнього модифікатора доступу. `private` методи — не виняток.
>
> Ви можете мати кілька `private` методів з однаковою назвою, доки в них різні списки параметрів.
>
> **Приклад:**
>
> ```java
> public class DataProcessor {
>
>     public void process() {
>         // ... якась логіка ...
>         log("Процес завершено", true);
>     }
>
>     // Приватний перевантажений метод
>     private void log(String message) {
>         System.out.println(message);
>     }
>
>     // Ще одна версія
>     private void log(String message, boolean withTimestamp) {
>         if (withTimestamp) {
>             // ... додаємо час
>         }
>         log(message);
>     }
> }
> ```
>
> Це цілком валідний і корисний підхід для структурування внутрішньої логіки класу.
>
> Головне пам'ятати, що `private` методи не беруть участі у наслідуванні чи поліморфізмі, тому їхнє перевантаження стосується лише коду всередині цього класу.

#### **📌 Що перевіряють:**

*Уважність до деталей та розуміння того, що перевантаження — це суто внутрішньокласовий механізм (на рівні компіляції), не пов'язаний з доступом ззовні.*

* * *

### **10 ❓ Питання:**

Що таке змінна кількість аргументів (varargs) і як вона пов'язана з перевантаженням? ⭐️

> #### **💬 Відповідь:**
>
> **Varargs (`...`)** — це синтаксис, що дозволяє передавати в метод довільну кількість аргументів одного типу. "Під капотом" компілятор перетворює їх на масив.
>
> **Приклад:**
> `public void printNumbers(int... numbers) { ... }`
>
> **Зв'язок з перевантаженням:**
>
> * Метод з varargs **може бути перевантажений**.
>
>     ```java
>     void process(String s) { ... }
>     void process(String... s) { ... }
>     ```
>
> * **Але є важливий нюанс у пріоритеті:**
>     При виборі перевантаженого методу, варіант з varargs має **найнижчий пріоритет**. Компілятор обере його, тільки якщо не знайде жодної іншої, більш точної відповідності (через точне співпадіння, розширення або автоупаковку).
>
> **Приклад пріоритету:**
>
> ```java
> void test(int i) { System.out.println("int"); }
> void test(int... i) { System.out.println("int..."); }
>
> test(10); // Викличе test(int), бо це точна відповідність
> ```
>
> **Неоднозначність з varargs:**
> Створення двох перевантажених методів, де обидва могли б прийняти varargs, може призвести до неоднозначності.
> `void method(int... i)` та `void method(int i, int... j)` — виклик `method(1)` буде неоднозначним.

#### **📌 Що перевіряють:**

*Знання про varargs та їхнє місце в ієрархії правил вирішення перевантажень.*

* * *

### **11 ❓ Питання:**

Чому цей код викликає помилку компіляції через неоднозначність?

```java
void method(Integer i, Long l) { ... }
void method(Long l, Integer i) { ... }
// ...
method(null, null);
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код призведе до **помилки компіляції** `reference to method is ambiguous`.
>
> **Причина:**
>
> 1. Літерал `null` є "сумісним" з будь-яким посилальним типом.
> 2. Коли компілятор аналізує виклик `method(null, null)`, він бачить, що **обидві** версії методу є однаково валідними кандидатами:
>     * **`method(Integer i, Long l)`:** `null` можна передати і як `Integer`, і як `Long`.
>     * **`method(Long l, Integer i)`:** Так само, `null` можна передати в обидва параметри.
> 3. Компілятор не може визначити, яка з версій є **більш специфічною (more specific)**. Оскільки `Integer` і `Long` не знаходяться в ієрархії успадкування один одного, жоден з методів не має пріоритету.
>
> Через те, що компілятор не може зробити однозначний вибір, він повідомляє про помилку неоднозначності.
>
> **Для вирішення** потрібно явно вказати тип хоча б одного з аргументів:
> `method((Integer) null, null);`

#### **📌 Що перевіряють:**

*Розуміння того, як `null` взаємодіє з правилами вирішення перевантаження, та концепції "більш специфічного" методу.*

* * *

### **12 ❓ Питання:**

Чи можна перевантажити `final` метод? А `static`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, обидва можна перевантажити без проблем.**
>
> Обмеження, що накладають `final` та `static`, стосуються **перевизначення (`Overriding`)**, а не перевантаження (`Overloading`).
>
> **Перевантаження `final` методу:**
> `final` забороняє змінювати реалізацію методу в дочірніх класах, але ніяк не заважає мати в тому ж класі інший метод з такою ж назвою, але іншими параметрами.
>
> ```java
> public class Calculator {
>     public final int sum(int a, int b) { return a + b; }
>     // Перевантажуємо final метод - все добре
>     public final int sum(int a, int b, int c) { return a + b + c; }
> }
> ```
>
> **Перевантаження `static` методу:**
> Так само, можна мати кілька статичних методів з однаковою назвою.
>
> ```java
> public class Utils {
>     public static String format(int i) { return String.valueOf(i); }
>     public static String format(double d) { return String.valueOf(d); }
> }
> ```
>
> Перевантаження — це механізм, що вирішується компілятором на основі сигнатур, і йому байдуже, чи є метод `final` або `static`.

#### **📌 Що перевіряють:**

*Дуже чітке розмежування між правилами для Overloading та Overriding. Кандидат не повинен плутати обмеження, що стосуються наслідування, з правилами оголошення методів усередині одного класу.*

* * *

### **13 ❓ Питання:**

Який метод буде викликано, якщо існує точна відповідність і можливість розширення через varargs?

```java
void process(int a, int b) { System.out.println("int, int"); }
void process(int... a) { System.out.println("int..."); }
// ...
process(1, 2);
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Буде викликано метод **`process(int a, int b)`**.
>
> **Пояснення:**
>
> Компілятор Java завжди обирає **найбільш специфічний** метод.
>
> 1. Виклик `process(1, 2)` відповідає **точно** сигнатурі `(int, int)`.
> 2. Він також відповідає сигнатурі `(int...)`, оскільки `1` і `2` можуть бути "запаковані" у масив.
>
> Однак, версія з **фіксованою кількістю аргументів завжди є більш специфічною**, ніж версія зі змінною кількістю аргументів (varargs). Тому компілятор надасть перевагу `process(int, int)`.
>
> Варіант з varargs буде обрано, тільки якщо немає жодної іншої підходящої версії, наприклад:
>
> * `process(1)`
> * `process(1, 2, 3)`
> * `process()`

#### **📌 Що перевіряють:**

*Знання місця varargs у ієрархії правил вирішення перевантажень.*

* * *

### **14 ❓ Питання:**

Як дженерики (Generics) взаємодіють з перевантаженням? Чому цей код не скомпілюється?

```java
public void print(List<String> list) {}
public void print(List<Integer> list) {}
```

⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється** через механізм **стирання типів (type erasure)**, який використовують дженерики в Java.
>
> **Пояснення:**
>
> 1. **Стирання типів:** На етапі компіляції вся інформація про параметризовані типи (як `<String>` або `<Integer>`) **стирається**, і вони замінюються на їхню верхню межу (зазвичай, `Object`).
>
> 2. **Що бачить JVM:** Після стирання типів, обидва методи для JVM виглядають так:
>
>     ```java
>     public void print(List list) {}
>     public void print(List list) {}
>     ```
>
> 3. В результаті, з точки зору JVM, ці два методи мають **абсолютно однакову сигнатуру**, що є **помилкою** (дублювання методу). Компілятор виявить цю проблему і повідомить, що обидва методи мають однакову сигнатуру після стирання типів (`both methods have same erasure`).
>
> **Висновок:** Не можна перевантажувати методи, які відрізняються лише параметрами типу дженерика, оскільки ця інформація втрачається на етапі виконання.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про роботу дженериків у Java, зокрема про стирання типів. Це просунуте питання, яке перевіряє, чи кандидат розуміє, як дженерики реалізовані "під капотом".*

* * *

### **15 ❓ Питання:**

Яким буде результат цього коду? Поясніть логіку вибору.

```java
void method(int i, double d) { System.out.println("int, double"); }
void method(double d, int i) { System.out.println("double, int"); }
//...
method(1, 2);
```

⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код викличе **помилку компіляції** через **неоднозначність виклику (ambiguous method call)**.
>
> **Пояснення логіки компілятора:**
>
> Компілятор аналізує виклик `method(1, 2)`, де обидва аргументи є `int` літералами.
>
> **Кандидат 1: `method(int i, double d)`**
>
> * Щоб викликати цей метод, другий аргумент (`2`) потрібно розширити з `int` до `double`.
> * Це вимагає **одного перетворення** (widening).
>
> **Кандидат 2: `method(double d, int i)`**
>
> * Щоб викликати цей метод, перший аргумент (`1`) потрібно розширити з `int` до `double`.
> * Це також вимагає **одного перетворення** (widening).
>
> **Проблема:**
>
> Обидва методи є однаково "хорошими" кандидатами. Кожен вимагає рівно одного перетворення-розширення. Компілятор не може визначити, який з них є **більш специфічним**. У нього немає правил, щоб надати перевагу перетворенню другого аргумента над першим чи навпаки.
>
> Через цю неоднозначність, компілятор відмовляється робити вибір і повідомляє про помилку.
>
> **Для вирішення** потрібно явно вказати тип одного з аргументів:
> `method(1, 2.0);` // Викличе `method(int, double)`
> `method(1.0, 2);` // Викличе `method(double, int)`

#### **📌 Що перевіряють:**

*Експертне розуміння правил вирішення перевантажень у складних, неоднозначних випадках. Кандидат повинен вміти "думати, як компілятор".*
