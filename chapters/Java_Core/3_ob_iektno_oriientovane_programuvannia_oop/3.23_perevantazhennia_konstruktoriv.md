---
layout: default
title: "Перевантаження конструкторів"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 23
---

# Перевантаження конструкторів

* короткий опис: Можливість мати декілька конструкторів з однаковою назвою, але різними наборами параметрів (кількість, тип, порядок), що дозволяє гнучко створювати об'єкти.

* поняття: Перевантаження конструкторів, Сигнатура конструктора.

* приклади: `Book()`, `Book(String title)`, `Book(String title, int pages)`.

* ключові моменти: Зручно для гнучкої ініціалізації, часто використовує `this()` для уникнення дублювання коду.

* що важливо знати на співбесіді: Призначення, як реалізується, переваги.

### **✅ Офіційне визначення:**

Перевантаження конструктора в Java — це механізм, який дозволяє створювати кілька конструкторів у одному класі з різними наборами параметрів. JVM розрізняє їх за кількістю, типом і порядком аргументів.

### **🧠 Простими словами:**

Ти можеш створити кілька версій конструктора, які ініціалізують об’єкт по-різному: з усіма даними, лише з іменем, або взагалі без нічого. Все залежить від того, що передають при створенні об’єкта.

---

## **Основне**

| Сигнатура конструктора | Для чого використовується |
| :---: | :---: |
| **`User()`** | Створити з дефолтними значеннями |
| **`User(String name)`** | Створити користувача з іменем |
| **`User(String name, int age)`** | Створити користувача з іменем і віком |

---

## **Пояснення на пальцях (💡)**

Уяви, що ти хочеш створити **`User`**\-об’єкти різними способами:

* один раз із усіма даними,
* інший — лише з ім’ям,
* іноді — взагалі без параметрів.

Замість одного конструктора з багатьма **`null`** або **`0`**, краще створити кілька конструкторів і передавати тільки те, що потрібно.

Для уникнення дублювання коду між конструкторами часто використовують **`this(...)`**.

---

## **Корисні приклади (🧪)**

```java
public class User {
    String name;
    int age;

    // Конструктор без параметрів
    public User() {
        this("Anonymous", 0); // викликає інший конструктор
    }

    // Конструктор з 1 параметром
    public User(String name) {
        this(name, 0); // також викликає основний
    }

    // Конструктор з 2 параметрами
    public User(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
java
Копіювати
Редагувати
// Використання:
User u1 = new User();           // Anonymous, 0
User u2 = new User("Alice");    // Alice, 0
User u3 = new User("Bob", 25);  // Bob, 25
```

---

## **Порівняння (⚖️)**

| Підхід | Плюси | Мінуси |
| :---: | :---: | :---: |
| **Один великий конструктор** | Один вхід — легко тестувати | Потрібно передавати все |
| **Перевантаження** | Гнучкість створення об’єкта | Можна загубити логіку без **`this()`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘

**`this(...)`** \= "виклик іншої версії себе"

Overloading \= Over Options — багато способів створення одного об’єкта

---

## **🟩 Підсумок:**

Перевантаження конструкторів — це зручний спосіб зробити клас гнучким у створенні об’єктів. Замість одного “монстра” з десятком параметрів, краще мати кілька простих конструкторів, які викликають один одного через **`this(...)`** і дозволяють створювати об’єкти з різними наборами вхідних даних. Це підвищує читабельність і зручність використання класу.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке перевантаження конструкторів і яка його основна мета?

> #### **💬 Відповідь:**
>
> **Перевантаження конструкторів (Constructor Overloading)** — це можливість мати в одному класі **кілька конструкторів з різними списками параметрів**. Як і при перевантаженні методів, компілятор розрізняє їх за **кількістю, типами або порядком** параметрів.
>
> **Основна мета:**
>
> Надати **гнучкість при створенні об'єктів**. Це дозволяє створювати екземпляри класу різними способами, надаючи різний набір початкових даних. Наприклад:
>
> * Створити об'єкт зі значеннями за замовчуванням (через конструктор без параметрів).
> * Створити об'єкт, ініціалізувавши лише найважливіші поля.
> * Створити об'єкт, ініціалізувавши всі поля.
>
> **Приклад:**
>
> ```java
> class Pizza {
>     // Конструктори перевантажені для різних способів "замовлення" піци
>     Pizza() { /*... Маргарита за замовчуванням ...*/ }
>     Pizza(String topping) { /*... піца з однією добавкою ...*/ }
>     Pizza(String topping1, String topping2) { /*... з двома ...*/ }
> }
> ```
>
> Таким чином, клієнтський код може обрати найзручніший спосіб створення об'єкта `Pizza`.

#### **📌 Що перевіряють:**

*Базове розуміння концепції як інструменту для гнучкої ініціалізації об'єктів.*

* * *

### **2 ❓ Питання:**

Як використовується ключове слово `this()` у перевантажених конструкторах? Наведіть приклад.

> #### **💬 Відповідь:**
>
> **`this()`** — це спеціальний синтаксис для **виклику іншого конструктора того ж класу** зсередини конструктора.
>
> Це основний інструмент для реалізації **"ланцюжка конструкторів" (constructor chaining)** в межах одного класу, що дозволяє **уникнути дублювання коду ініціалізації**.
>
> **Правила використання:**
>
> * Виклик `this(...)` повинен бути **першим рядком** у тілі конструктора.
> * В одному конструкторі можна використати лише один виклик `this()`.
>
> **Приклад (із загальним "основним" конструктором):**
>
> ```java
> public class User {
>     private String username;
>     private String role;
>
>     // Конструктор за замовчуванням
>     public User() {
>         // 1. Викликаємо конструктор з одним параметром
>         this("guest");
>     }
>
>     // Конструктор з одним параметром
>     public User(String username) {
>         // 2. Викликаємо основний конструктор з двома параметрами
>         this(username, "USER");
>     }
>
>     // Основний конструктор, який містить всю логіку ініціалізації
>     public User(String username, String role) {
>         this.username = username;
>         this.role = role;
>     }
> }
> ```
>
> У цьому прикладі перші два конструктори делегують свою роботу основному конструктору, передаючи значення за замовчуванням. Це дозволяє централізувати логіку ініціалізації.

#### **📌 Що перевіряють:**

*Знання стандартного патерну для уникнення дублювання коду в конструкторах за допомогою `this()`.*

* * *

### **3 ❓ Питання:**

Що станеться, якщо я створю в класі власний конструктор з параметрами, але не створю конструктор без параметрів?

> #### **💬 Відповідь:**
>
> Якщо ви оголосите в класі **хоча б один власний конструктор**, компілятор Java **не буде автоматично генерувати конструктор за замовчуванням** (public-конструктор без параметрів).
>
> **Наслідок:**
> Ви **втратите можливість** створювати об'єкти цього класу без передачі аргументів. Спроба зробити це призведе до **помилки компіляції**.
>
> **Приклад:**
>
> ```java
> public class Book {
>     private String title;
>
>     // Єдиний конструктор вимагає назву
>     public Book(String title) {
>         this.title = title;
>     }
> }
>
> public class Main {
>     public static void main(String[] args) {
>         Book b1 = new Book("Java 101"); // ✔️ ОК
>         // Book b2 = new Book(); // ❌ ПОМИЛКА КОМПІЛЯЦІЇ!
>                                   // "cannot find symbol constructor Book()"
>     }
> }
> ```
>
> Це є важливим механізмом, який дозволяє "змусити" користувачів вашого класу надавати необхідні дані при створенні об'єкта, роблячи його стан більш прогнозованим.

#### **📌 Що перевіряють:**

*Знання правил автоматичної генерації конструктора. Це дуже поширене джерело помилок для початківців.*

* * *

### **4 ❓ Питання:**

Чи може один конструктор викликати і `this()`, і `super()` одночасно?

> #### **💬 Відповідь:**
>
> **Ні, не може.** Це призведе до **помилки компіляції**.
>
> **Причина:**
>
> У Java існує строге правило: і виклик `this(...)`, і виклик `super(...)` **повинні бути самою першою інструкцією** в тілі конструктора.
>
> Оскільки в конструкторі може бути лише одна "перша інструкція", неможливо задовольнити цю вимогу для обох викликів одночасно.
>
> **Як працює ланцюжок ініціалізації:**
>
> * Або конструктор делегує роботу іншому конструктору **того ж класу** (`this()`).
> * Або він делегує роботу конструктору **батьківського класу** (`super()`).
>
> В кінці будь-якого ланцюжка `this()`-викликів завжди стоятиме конструктор, який містить явний або неявний виклик `super()`. Це гарантує, що ієрархія ініціалізується правильно — від самого верхнього предка до поточного класу.

#### **📌 Що перевіряють:**

*Знання строгих синтаксичних правил для конструкторів.*

* * *

### **5 ❓ Питання:**

Чи можна перевантажити `private` конструктор?

> #### **💬 Відповідь:**
>
> **Так, звісно.**
>
> Правила перевантаження (різні списки параметрів) застосовуються до будь-яких конструкторів, незалежно від їхнього модифікатора доступу.
>
> Це може бути корисно в деяких патернах проектування, де клас повністю контролює процес створення своїх екземплярів.
>
> **Приклад з патерном Builder:**
>
> ```java
> public class Config {
>     private final String host;
>     private final int port;
>
>     // Приватний конструктор, доступний лише для білдера
>     private Config(String host, int port) {
>         this.host = host;
>         this.port = port;
>     }
>
>     // Інший приватний конструктор
>     private Config() {
>         this("localhost", 8080);
>     }
>
>     public static Config createDefault() {
>         return new Config(); // Можемо викликати приватний конструктор зсередини
>     }
>
>     // ... логіка білдера ...
> }
> ```
>
> Перевантаження приватних конструкторів дозволяє гнучко керувати внутрішньою логікою створення об'єктів.

#### **📌 Що перевіряють:**

*Розуміння, що перевантаження — це механізм, не пов'язаний з доступом ззовні, і його можна застосовувати до будь-яких методів чи конструкторів.*

* * *

### **6 ❓ Питання:**

Як перевантаження конструкторів пов'язане з ініціалізацією `final` полів?

> #### **💬 Відповідь:**
>
> **`blank final` поля** (ті, що не ініціалізовані при оголошенні) **повинні бути ініціалізовані рівно один раз** до завершення роботи будь-якого конструктора.
>
> Перевантажені конструктори та `this()` відіграють у цьому ключову роль:
>
> * Якщо ви використовуєте ланцюжок викликів `this()`, достатньо, щоб **лише останній (основний) конструктор** у цьому ланцюжку ініціалізував усі `final` поля.
>
> **Приклад:**
>
> ```java
> public class Connection {
>     private final String url;
>     private final int timeout;
>
>     public Connection(String url) {
>         // Делегуємо ініціалізацію, надаючи значення за замовчуванням
>         this(url, 5000);
>     }
>
>     // Лише цей конструктор реально ініціалізує final поля
>     public Connection(String url, int timeout) {
>         this.url = url;
>         this.timeout = timeout;
>     }
> }
> ```
>
> Якби конструктор `Connection(String url)` не викликав `this(...)` і не ініціалізував би поле `timeout` сам, виникла б **помилка компіляції**, оскільки `final` поле `timeout` залишилося б неініціалізованим.
>
> Таким чином, `this()` допомагає централізувати логіку ініціалізації `final` полів в одному місці.

#### **📌 Що перевіряють:**

*Знання правил ініціалізації `final` полів та того, як вони взаємодіють з ланцюжками конструкторів.*

* * *

### **7 ❓ Питання:**

Навіщо потрібен конструктор копіювання (copy constructor) і як він пов'язаний з перевантаженням? ⭐️

> #### **💬 Відповідь:**
>
> **Конструктор копіювання** — це перевантажений конструктор, який приймає як параметр **інший об'єкт того ж самого класу**. Його мета — створити новий об'єкт як копію існуючого.
>
> `public MyClass(MyClass other) { ... }`
>
> **Навіщо він потрібен:**
>
> 1. **Простий спосіб клонування:** Це більш безпечна та гнучка альтернатива використанню `Object.clone()`.
> 2. **Контроль над копіюванням:** Дозволяє явно визначити, яке копіювання буде виконуватися — **поверхневе (shallow copy)** чи **глибоке (deep copy)**.
>
> **Зв'язок з перевантаженням:**
>
> Він є просто ще однією версією перевантаженого конструктора. Це дозволяє мати різні способи створення об'єкта: з нуля або на основі існуючого.
>
> **Приклад:**
>
> ```java
> class Email {
>     private String to;
>     private String body;
>
>     public Email(String to, String body) { this.to = to; this.body = body; }
>
>     // Конструктор копіювання
>     public Email(Email other) {
>         this.to = other.to;
>         this.body = other.body;
>     }
> }
>
> Email original = new Email("a@b.c", "Hello");
> Email copy = new Email(original); // Створюємо копію
> ```

#### **📌 Що перевіряють:**

*Знання поширених патернів, як-от конструктор копіювання, та їх переваг над іншими підходами (наприклад, `clone()`).*

* * *

### **8 ❓ Питання:**

У якому порядку виконуються ініціалізація полів, блоки ініціалізації та конструктори (батьківського та дочірнього класів)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Порядок ініціалізації є строгим і відбувається "зверху вниз" по ієрархії:
>
> **Крок 1: Ініціалізація `static` членів (один раз при завантаженні класу)**
>
> 1. Статичні ініціалізатори батьківського класу.
> 2. Статичні ініціалізатори дочірнього класу.
>
> **Крок 2: Ініціалізація екземпляра (при кожному `new`)**
> 3. **Виклик конструктора дочірнього класу.**
> 4. Цей конструктор неявно або явно викликає **`super()`**.
> 5. **Перед** виконанням тіла конструктора **батьківського** класу, виконуються його **не-статичні ініціалізатори** (поля та блоки `{}`) у порядку оголошення.
> 6. Виконується тіло конструктора **батьківського** класу.
> 7. **Перед** виконанням тіла конструктора **дочірнього** класу, виконуються його **не-статичні ініціалізатори**.
> 8. Виконується тіло конструктора **дочірнього** класу.
>
> **Коротка схема:**
> `статика батька -> статика нащадка -> ініціалізатори батька -> конструктор батька -> ініціализатори нащадка -> конструктор нащадка.`

#### **📌 Що перевіряють:**

*Дуже детальне знання життєвого циклу об'єкта. Це просунуте питання, що вимагає точності.*

* * *

### **9 ❓ Питання:**

Який конструктор буде викликано для цього коду, і чому?
`new Child(10.5);`

```java
class Parent {
    Parent(double d) { System.out.println("Parent double"); }
}
class Child extends Parent {
    Child(int i) {
        super(i);
        System.out.println("Child int");
    }
}
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється**. Компілятор видасть помилку: `constructor Child in class Child cannot be applied to given types`.
>
> **Пояснення:**
>
> 1. Виклик `new Child(10.5)` вимагає знайти конструктор у класі `Child`, який може прийняти аргумент типу `double`.
>
> 2. У класі `Child` є **лише один** конструктор: `Child(int i)`.
>
> 3. Компілятор перевіряє, чи можна передати `double` (`10.5`) у параметр типу `int`. Це є **звужуючим перетворенням (narrowing conversion)**, яке може призвести до втрати даних (`10.5` → `10`).
>
> 4. Java **не виконує звужуючі перетворення автоматично** при виборі перевантаженого методу чи конструктора.
>
> 5. Оскільки немає жодного відповідного конструктора в класі `Child` (ні `Child(double)`, ні такого, куди `double` може бути розширено), компілятор не може знайти кандидата для виклику і повідомляє про помилку.
>
> **Як виправити:**
> Потрібно або додати конструктор `Child(double d)`, або явно привести тип при виклику: `new Child((int) 10.5);`.

#### **📌 Що перевіряють:**

*Знання правил вирішення перевантажень, зокрема того, що звужуючі перетворення не застосовуються автоматично.*

* * *

### **10 ❓ Питання:**

Уявіть, що ви створюєте складний об'єкт з багатьма опціональними параметрами. Яка є краща альтернатива перевантаженню конструкторів у цьому випадку? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Коли кількість параметрів, особливо опціональних, стає великою, перевантаження конструкторів призводить до **антипатерну "Телескопічний конструктор"**. Вам доводиться створювати безліч конструкторів, які викликають один одного, що робить код громіздким і важким для читання.
>
> **Кращою альтернативою** в цьому випадку є патерн проектування **Builder**.
>
> **Суть патерну Builder:**
>
> 1. Ви створюєте окремий **вкладений статичний клас `Builder`** всередині вашого основного класу.
> 2. Білдер має методи для поступового налаштування кожного параметра (наприклад, `withName()`, `withAge()`). Кожен такий метод повертає сам білдер (`return this;`) для ланцюжкових викликів.
> 3. Основний клас має **`private` конструктор**, який приймає білдер як аргумент.
> 4. Білдер має фінальний метод `build()`, який викликає цей приватний конструктор і повертає готовий, ініціалізований об'єкт.
>
> **Переваги Builder над перевантаженням:**
>
> * **Висока читабельність:** Код створення об'єкта стає самодокументованим: `new User.Builder().withName("John").withAge(30).build();`.
> * **Гнучкість:** Легко ініціалізувати лише ті поля, які потрібні, і в будь-якому порядку.
> * **Незмінність (Immutability):** Дуже добре підходить для створення незмінних об'єктів.
>
> Builder є де-факто стандартом для створення складних об'єктів у Java.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та розуміння, які проблеми вони вирішують порівняно з базовими підходами.*

* * *

### **11 ❓ Питання:**

Чи можна викликати не-`static` метод всередині конструктора? Чи безпечно це? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Конструктор виконується в контексті об'єкта, тому він має доступ до всіх його не-статичних членів, включаючи методи.
>
> **Чи безпечно це?**
> **Не завжди.** Це може бути небезпечно, якщо цей метод **не є `private` або `final`**, тобто, якщо його **можна перевизначити** в дочірньому класі.
>
> **Проблема (виклик поліморфного методу з конструктора):**
>
> * Коли конструктор батьківського класу викликає такий метод, через поліморфізм буде викликано **реалізацію з дочірнього класу**.
> * Але це відбувається **до того**, як конструктор дочірнього класу встиг ініціалізувати власні поля.
> * В результаті, метод дочірнього класу буде працювати з **не повністю ініціалізованим об'єктом**, що може призвести до `NullPointerException` або некоректної поведінки.
>
> **Приклад:**
>
> ```java
> abstract class Parent {
>     Parent() { init(); } // Небезпечний виклик
>     abstract void init();
> }
> class Child extends Parent {
>     private String name = "Default";
>     void init() {
>         // `name` тут ще дорівнює null, бо конструктор Child не почав роботу
>         System.out.println(name.length()); // NPE!
>     }
> }
> ```
>
> **Висновок:** Викликати з конструктора можна лише **`private`** або **`final`** методи, оскільки їхню поведінку не можна змінити в дочірніх класах.

#### **📌 Що перевіряють:**

*Глибоке розуміння порядку ініціалізації та небезпек, пов'язаних з викликом поліморфних методів у конструкторах.*

* * *

### **12 ❓ Питання:**

Поясніть, що станеться, якщо конструктор викине виняток. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо конструктор викидає виняток, процес створення об'єкта **переривається**, і **об'єкт не буде створено**.
>
> **Послідовність подій:**
>
> 1. JVM виділяє пам'ять для об'єкта.
> 2. Починається ланцюжок виклику конструкторів (від `Object` і до поточного класу).
> 3. Якщо в якомусь із конструкторів виникає виняток:
>     * Виконання цього конструктора негайно припиняється.
>     * Конструктори, що чекають у ланцюжку виклику, не будуть виконані.
>     * Оператор `new` не зможе повернути посилання на об'єкт.
> 4. Оскільки на щойно виділену пам'ять не буде жодних посилань, вона **стане доступною для збирача сміття (Garbage Collector)**.
>
> **Обробка винятків з конструктора:**
>
> * Якщо конструктор кидає `checked` виняток, це повинно бути оголошено в його `throws` клаузі.
> * Код, що викликає `new MyClass()`, повинен обробляти цей виняток у `try-catch`.
>
> **Важливий нюанс з `finally`:**
> Якщо в блоці `try`, де створюється об'єкт, є блок `finally`, то `finally` **буде виконано**, навіть якщо конструктор кинув виняток.

#### **📌 Що перевіряють:**

*Розуміння того, як обробка винятків взаємодіє з процесом створення об'єкта, та знання наслідків помилок у конструкторах.*

* * *

### **13 ❓ Питання:**

Яким чином `record`'и, введені в Java 16, спрощують створення конструкторів для класів-даних? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `record`'и кардинально спрощують створення **незмінних (immutable) класів-носіїв даних**, автоматично генеруючи багато шаблонного коду, включаючи конструктори.
>
> Для `public record Point(int x, int y) {}`, компілятор автоматично генерує:
>
> **1. Канонічний конструктор (`canonical constructor`):**
>
> * Це **публічний конструктор**, який приймає параметри для **всіх компонентів** запису (`public Point(int x, int y)`).
> * Його тіло автоматично ініціалізує відповідні `private final` поля (`this.x = x; this.y = y;`).
> * Це позбавляє від необхідності писати цю рутинну логіку вручну.
>
> **2. Компактний конструктор (`compact constructor`):**
>
> * Це спеціальна, коротка форма конструктора, яка дозволяє додати **лише логіку валідації або нормалізації**, не переписуючи присвоєння.
> * Він не має списку параметрів.
>
>     ```java
>     public record Range(int start, int end) {
>         // Компактний конструктор для валідації
>         public Range {
>             if (start > end) {
>                 throw new IllegalArgumentException("Start cannot be after end");
>             }
>         }
>     }
>     ```
> Присвоєння `this.start = start;` і `this.end = end;` будуть автоматично додані компілятором **після** виконання цього блоку.
>
> Таким чином, `record`'и забезпечують інкапсуляцію та ініціалізацію "з коробки", слідуючи найкращим практикам.

#### **📌 Що перевіряють:**

*Знання сучасних фіч Java та їхніх переваг у порівнянні з традиційними підходами.*

* * *

### **14 ❓ Питання:**

Чи може `abstract` клас мати конструктор? Поясніть свою відповідь. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може і часто повинен.**
>
> Хоча ви не можете створити екземпляр абстрактного класу напряму (`new MyAbstractClass()`), його конструктор виконує критично важливу роль в ієрархії наслідування.
>
> **Навіщо він потрібен:**
>
> * **Для ініціалізації успадкованих полів:** Абстрактний клас може мати власні `private` або `protected` поля (стан). Конструктор є єдиним правильним місцем для їх ініціалізації.
>
> * **Для виклику з конструкторів дочірніх класів (`super()`):** Будь-який дочірній клас **зобов'язаний** викликати конструктор свого батьківського класу. Це гарантує, що вся успадкована частина об'єкта буде коректно сконструйована.
>
> **Приклад:**
>
> ```java
> abstract class Animal {
>     protected final String name;
>
>     // Конструктор для ініціалізації стану
>     protected Animal(String name) {
>         this.name = name;
>     }
>     abstract void makeSound();
> }
>
> class Dog extends Animal {
>     public Dog(String name) {
>         super(name); // Викликаємо конструктор батька
>     }
>     // ...
> }
> ```
>
> Без конструктора в `Animal` було б неможливо ініціалізувати поле `name`.

#### **📌 Що перевіряють:**

*Розуміння того, що конструктори потрібні не тільки для прямого створення об'єктів, а й для участі в ланцюжку ініціалізації в ієрархії.*

* * *

### **15 ❓ Питання:**

Що станеться, якщо `super()` або `this()` викликати не першим рядком у конструкторі? Чому існує таке строге правило? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Спроба викликати `super()` або `this()` не першим рядком призведе до **помилки компіляції**: `Call to 'super()' must be first statement in constructor body`.
>
> **Чому це правило є таким строгим:**
>
> Це правило є фундаментальним для гарантування **коректного та передбачуваного порядку ініціалізації об'єкта**.
>
> 1. **Порядок ініціалізації:** Java гарантує, що об'єкт ініціалізується **"зверху вниз"** по ієрархії. Спочатку повинна бути повністю створена та ініціалізована **батьківська частина** об'єкта, і лише потім — **дочірня**. `super()` є механізмом, що запускає цей процес.
>
> 2. **Захист від роботи з неініціалізованим станом:** Якби було дозволено виконувати код до виклику `super()`, цей код міг би спробувати отримати доступ до успадкованих полів або викликати методи, які залежать від стану батька, **до того, як цей стан було ініціалізовано**. Це призвело б до непередбачуваної поведінки та помилок.
>
>     ```java
>     public Child() {
>         // Якби цей код був дозволений...
>         System.out.println(super.someParentField); // Читаємо неініціалізоване поле!
>         super();
>     }
>     ```
>
> 3. **Виклик `this()`:** `this()` передає керування іншому конструктору. Якщо б перед цим було виконано якийсь код, цей код міг би бути виконано кілька разів, що призвело б до подвійної ініціалізації або некоректних побічних ефектів.
>
> Отже, це строге правило є механізмом захисту, що забезпечує логічний і безпечний порядок створення об'єктів.

#### **📌 Що перевіряють:**

*Глибоке розуміння філософії дизайну мови Java та причин, що лежать в основі її синтаксичних правил. Це питання на розуміння "чому", а не тільки "як".*
