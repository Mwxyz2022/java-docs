---
layout: default
title: "Перевизначення методів (Overriding)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 8
---

# Перевизначення методів (Overriding)

* короткий опис: Процес, за якого підклас надає власну реалізацію методу, що вже був оголошений у суперкласі, з ідентичною сигнатурою. Це форма динамічного (runtime) поліморфізму.

* поняття: Перевизначення, `@Override` (анотація), `runtime polymorphism`.

* приклади: `Dog` перевизначає метод `speak()` з `Animal` для виводу "Bark".

* ключові моменти: Вибір методу відбувається під час виконання програми. `final`, `static`, `private` методи не можуть бути перевизначені.

* що важливо знати на співбесіді: Визначення, правила, відмінність від перевантаження.

### **✅ Офіційне визначення:**

Перевизначення методу — це процес, за якого підклас надає власну реалізацію методу, що вже був оголошений у суперкласі. Сигнатура методу (назва, параметри, тип повернення) має бути ідентичною, і в Java його слід позначати анотацією **`@Override`**.

### **🧠 Простими словами:**

У тебе є батьківський клас **`Animal`** з методом **`speak()`**, який каже "звук". У класі **`Dog`** ти перевизначаєш цей метод, щоб він казав "гав". Тобто поведінка змінюється, але назва методу — та сама.

---

## **📊 Що потрібно для перевизначення:**

| Умова | Пояснення |
| :---: | :---: |
| **Та сама сигнатура** | Ім’я методу, кількість і типи параметрів мають співпадати |
| **Спільний або ширший доступ** | Наприклад: **`protected`** -> **`public`** можна, **`public`** -> **`private`** — ні |
| **Заборонено перевизначати:** | **`final`**, **`static`**, **`private`** методи (не наслідуються або не можна змінювати) |
| **Анотація `@Override`** | Не обов’язкова, але дуже рекомендована для контролю правильності |

---

## **Пояснення на пальцях (💡)**

Ти наслідуєш клас і отримуєш метод, який уже щось робить. Але тобі потрібно, щоб він працював інакше. Перевизначення дозволяє переписати логіку.

JVM під час виконання визначає тип обʼєкта і викликає метод саме дочірнього класу — це динамічний поліморфізм (runtime polymorphism)
---

## **Корисні приклади (🧪)**

```java
class Animal {
    void speak() {
        System.out.println("Some sound");
    }
}

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Bark");
    }
}

class Cat extends Animal {
    @Override
    void speak() {
        System.out.println("Meow");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal a1 = new Dog();
        Animal a2 = new Cat();
        
        a1.speak();  // Bark
        a2.speak();  // Meow
    }
}
```

---

## **Порівняння (⚖️)**

| Критерій | Overloading | Overriding |
| :---: | :---: | :---: |
| **Назва методу** | Та сама | Та сама |
| **Сигнатура** | Різна (параметри) | Та сама |
| **Поліморфізм** | Статичний (compile-time) | Динамічний (runtime) |
| **Зв’язок із батьком** | Не обов’язковий | Працює тільки з наслідуванням |

---

## **🧠 Мнемоніка для запам’ятовування:**

“Override — перезапиши, як тобі треба”

🔁 Ти не змінюєш ім’я методу — ти переписуєш його поведінку в контексті нового класу
---

## **🟩 Підсумок:**

Перевизначення — ключ до динамічного поліморфізму. Воно дозволяє підкласам змінювати поведінку методів батьківських класів без зміни їх сигнатури. Завдяки цьому принципу створюють гнучкі, розширювані та легко підтримувані програми.

---

### **🔗 Корисні посилання для глибшого занурення:**

* [**Oracle Docs: Method Overriding**](https://docs.oracle.com/javase/tutorial/java/IandI/override.html) **— офіційна документація**

Добре, я ознайомився з конспектом. На співбесідах на цю тему важливо не тільки розрізняти **перевизначення від перевантаження**, але й знати **всі правила та обмеження**, розуміти зв'язок з **динамічним поліморфізмом**, `@Override`, **коваріантними типами** та ключовими словами `final`, `static`, `private`.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке перевизначення методів (Method Overriding) в Java? Яка його головна мета?

> #### **💬 Відповідь:**
>
> **Перевизначення методів** — це механізм ООП, при якому **дочірній клас (підклас)** надає **власну, специфічну реалізацію** для методу, який вже існує в його **батьківському класі (суперкласі)**.
>
> Сигнатура методу (назва та параметри) в дочірньому класі повинна бути ідентичною сигнатурі методу в батьківському.
>
> **Головна мета:**
>
> * Дозволити підкласу **змінити або розширити** успадковану поведінку.
> * Це є основою для **динамічного поліморфізму (runtime polymorphism)**, що дозволяє об'єктам різних класів по-різному реагувати на один і той самий виклик методу.
>
> **Приклад:**
> Батьківський клас `Vehicle` може мати метод `startEngine()`, а його дочірні класи `Car` та `Motorcycle` можуть перевизначити цей метод, щоб додати специфічну для них логіку запуску.

#### **📝 Аналогія:**

Це як успадкувати від батька рецепт борщу. Ви знаєте основні інгредієнти та кроки (це успадкований метод), але ви можете "перевизначити" його, додавши, наприклад, гриби або гострий перець, щоб зробити його унікальним. Назва "борщ" та основна ідея залишаються, але реалізація стає вашою.

#### **📌 Що перевіряють:**

*Базове розуміння концепції як способу надання специфічної реалізації для успадкованої поведінки.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між перевизначенням (`Overriding`) та перевантаженням (`Overloading`)?

> #### **💬 Відповідь:**
>
> Це одне з найважливіших порівняльних питань в ООП.
>
| Характеристика | Перевизначення (`Overriding`) | Перевантаження (`Overloading`) |
| :---: | :---: | :---: |
| **Призначення** | Змінити поведінку успадкованого методу. | Надати методи з однаковою назвою для роботи з різними даними. |
| **Де відбувається** | У дочірньому класі (відносно батьківського).| В одному класі. |
| **Сигнатура методу** | Назва, кількість і типи параметрів **повинні бути однаковими**. | Назва однакова, а **список параметрів різний** (за кількістю, типами або порядком).|
| **Тип поліморфізму** | **Динамічний (Runtime)**. Вирішується JVM під час виконання. | **Статичний (Compile-time)**. Вирішується компілятором під час компіляції. |
| **Ключовий зв'язок** | Тісно пов'язане з **наслідуванням**. | Не пов'язане з наслідуванням. |
| **Анотація** | `@Override` (рекомендовано). | Немає. |

#### **📌 Що перевіряють:**

*Здатність чітко розмежувати ці дві фундаментальні концепції, що є обов'язковою для будь-якого Java-розробника.*

* * *

### **3 ❓ Питання:**

Які правила необхідно дотримуватися при перевизначенні методу?

> #### **💬 Відповідь:**
>
> Існує кілька строгих правил, які перевіряються компілятором:
>
> 1. **Назва та параметри:** Назва методу та список параметрів (кількість, типи, порядок) повинні **точно збігатися** з методом батьківського класу.
>
> 2. **Тип повернення:** Повинен бути **таким самим**, як у батьківському методі, або **коваріантним** (тобто, бути підтипом типу, що повертає батьківський метод). Наприклад, якщо батьківський повертає `Number`, дочірній може повертати `Integer`.
>
> 3. **Модифікатор доступу:** Рівень доступу перевизначеного методу **не може бути більш суворим**, ніж у батька. Він може бути лише таким самим або більш відкритим (наприклад, `protected` можна розширити до `public`, але не навпаки).
>
> 4. **`final`, `static`, `private` методи:**
>     * `final` методи **не можна** перевизначити.
>     * `static` методи **не можна** перевизначити (їх можна лише приховати).
>     * `private` методи **не можна** перевизначити, оскільки вони невидимі для дочірнього класу.
>
> 5. **Винятки (`throws`):** Перевизначений метод може не кидати винятки, кидати ті самі `checked` винятки, їхні підкласи, або будь-які `unchecked` винятки. Він **не може кидати нові `checked` винятки**, яких не було в сигнатурі батьківського методу.

#### **📌 Що перевіряють:**

*Знання технічних деталей та контрактів мови, що регулюють механізм перевизначення.*

* * *

### **4 ❓ Питання:**

Для чого потрібна анотація `@Override`? Чи є вона обов'язковою?

> #### **💬 Відповідь:**
>
> Анотація **`@Override`** використовується для того, щоб явно вказати компілятору, що даний метод **має намір перевизначити** метод із суперкласу (або реалізувати метод з інтерфейсу).
>
> **Чи є вона обов'язковою?**
>
> **Ні, вона не є обов'язковою** з точки зору синтаксису. Код скомпілюється і буде працювати і без неї, якщо всі правила перевизначення дотримано.
>
> **Чому її використання є найкращою практикою і вкрай рекомендовано:**
>
> `@Override` діє як **запобіжник**. Вона змушує компілятор виконати додаткову перевірку:
>
> * Чи дійсно існує метод з такою ж сигнатурою в батьківському класі.
>
> Це захищає від поширених помилок:
>
> * **Опечатки в назві:** Наприклад, `tostring()` замість `toString()`.
> * **Помилки в параметрах:** Наприклад, `equals(String s)` замість `equals(Object o)`.
>
> Без анотації `@Override` в таких випадках ви б просто створили новий, ніяк не пов'язаний (перевантажений) метод, і поліморфізм не спрацював би, що призвело б до важковиловлюваної логічної помилки. З анотацією ви отримаєте **помилку на етапі компіляції**.

#### **📌 Що перевіряють:**

*Розуміння того, як інструменти мови (анотації) допомагають писати більш безпечний та надійний код.*

* * *

### **5 ❓ Питання:**

Чи можна перевизначити `static` метод? Що таке приховування методів?

> #### **💬 Відповідь:**
>
> **Ні, `static` метод не можна перевизначити.** Перевизначення — це концепція, пов'язана з об'єктами та поліморфізмом часу виконання, тоді як `static` методи належать класу.
>
> Якщо в дочірньому класі створити статичний метод з такою ж сигнатурою, як у батьківському, це буде не перевизначення, а **приховування методів (method hiding)**.
>
> **Приховування працює так:**
>
> * Вибір статичного методу для виклику відбувається **на етапі компіляції** і базується на **типі посилання**, а не на фактичному типі об'єкта.
>
> **Приклад:**
>
> ```java
> class Parent {
>     static void whoAmI() { System.out.println("I am Parent"); }
> }
> class Child extends Parent {
>     static void whoAmI() { System.out.println("I am Child"); } // Приховування
> }
>
> Parent p = new Child();
> p.whoAmI(); // Виведе "I am Parent"!
> ```
>
> Виклик `p.whoAmI()` прив'язується до методу з класу `Parent`, оскільки тип посилання `p` — `Parent`. Поліморфізм тут не працює.

#### **📌 Що перевіряють:**

*Розуміння відмінностей у зв'язуванні для `static` та instance-методів. Знання концепції "приховування".*

* * *

### **6 ❓ Питання:**

Як викликати перевизначений метод батьківського класу з дочірнього?

> #### **💬 Відповідь:**
>
> Для цього використовується ключове слово **`super`**.
>
> `super` дозволяє звернутися до членів безпосереднього батьківського класу.
>
> **Синтаксис:** `super.methodName(...);`
>
> Це дуже поширена практика, коли потрібно не повністю замінити поведінку батьківського методу, а лише **розширити** її.
>
> **Приклад:**
>
> ```java
> class Logger {
>     public void log(String message) {
>         // Базова логіка - запис у консоль
>         System.out.println(message);
>     }
> }
>
> class FileLogger extends Logger {
>     @Override
>     public void log(String message) {
>         // 1. Спочатку виконуємо логіку батьківського класу
>         super.log(message);
>
>         // 2. Потім додаємо власну, розширену логіку
>         // ... код для запису 'message' у файл ...
>         System.out.println("Повідомлення також записано у файл.");
>     }
> }
> ```
>
> Таким чином, `FileLogger` повторно використовує базову функціональність і доповнює її.

#### **📌 Що перевіряють:**

*Практичні знання про взаємодію між дочірнім та батьківським класами.*

* * *

### **7 ❓ Питання:**

Поясніть, що таке коваріантні типи повернення. Наведіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> **Коваріантні типи повернення** — це функціональність Java (з версії 5), яка дозволяє перевизначеному методу повертати **підтип** того типу, який повертав метод у батьківському класі.
>
> Це послаблення класичного правила, за яким тип повернення повинен був точно збігатися.
>
> **Приклад (Патерн "Фабричний метод"):**
>
> ```java
> class Animal {
>     public Animal createChild() {
>         return new Animal();
>     }
> }
>
> class Cat extends Animal {
>     @Override
>     // Повертаємо Cat, який є підтипом Animal. Це коваріантний тип.
>     public Cat createChild() {
>         return new Cat();
>     }
> }
> ```
>
> **Перевага:**
> Це робить код більш типізованим та усуває необхідність у зайвому **приведенні типів**.
>
> ```java
> Cat myCat = new Cat();
> // Без коваріантності: Cat child = (Cat) myCat.createChild();
> // З коваріантністю: Cat child = myCat.createChild(); // Приведення не потрібне
> ```

#### **📌 Що перевіряють:**

*Знання просунутих можливостей мови, що стосуються наслідування та поліморфізму.*

* * *

### **8 ❓ Питання:**

Який рівень доступу може мати перевизначений метод порівняно з батьківським? ⭐️

> #### **💬 Відповідь:**
>
> Правило таке: рівень доступу перевизначеного методу **не може бути більш обмежувальним (суворим)**, ніж у батьківському методі. Він може бути лише таким самим або менш обмежувальним (більш відкритим).
>
> Ієрархія доступу (від найбільш до найменш обмежувального):
> `private` → `default` → `protected` → `public`
>
> **Правила:**
>
> * `public` метод може бути перевизначений **лише як `public`**.
> * `protected` метод може бути перевизначений як **`protected`** або **`public`**.
> * `default` метод може бути перевизначений як **`default`**, **`protected`** або **`public`** (за умови, що класи в одному пакеті).
> * `private` метод **не можна** перевизначити взагалі.
>
> **Навіщо це потрібно:**
> Це правило підтримує **принцип підстановки Лісков**. Якщо об'єкт дочірнього класу підставляється замість об'єкта батьківського, він повинен задовольняти його контракту. Якби дочірній клас міг "звузити" доступ до методу (наприклад, з `public` до `private`), то код, який раніше міг викликати цей метод через посилання на батька, "зламався" б.

#### **📌 Що перевіряють:**

*Знання правил видимості при наслідуванні та їх зв'язок з принципами ООП.*

* * *

### **9 ❓ Питання:**

Як `final` впливає на перевизначення методів та класів? ⭐️

> #### **💬 Відповідь:**
>
> Ключове слово **`final`** діє як "заборона" для розширення та перевизначення.
>
> **`final` метод:**
>
> * Метод, оголошений як `final`, **не можна перевизначити (`@Override`)** у жодному з дочірніх класів.
> * Це робиться тоді, коли потрібно гарантувати, що конкретна реалізація методу є остаточною і незмінною в усій ієрархаті класів. Це важливо для критичних операцій або для безпеки.
>
>     ```java
>     class SuperAlgorithm {
>         public final void execute() { /* важлива незмінна логіка */ }
>     }
>     class SubAlgorithm extends SuperAlgorithm {
>         // @Override public void execute() { ... } // ПОМИЛКА КОМПІЛЯЦІЇ
>     }
>     ```
>
>
> **`final` клас:**
>
> * Клас, оголошений як `final`, **не можна успадкувати**. Від нього не може бути дочірніх класів.
> * Оскільки наслідування неможливе, то і питання перевизначення його методів не виникає. Всі методи такого класу є неявно `final` з точки зору наслідування.
> * Це використовується для класів, чия реалізація є повною та не призначена для змін, наприклад, `String` або `Integer`.

#### **📌 Що перевіряють:**

*Знання ролі `final` як механізму обмеження наслідування та поліморфізму.*

* * *

### **10 ❓ Питання:**

Поясніть на прикладі, як перевизначення методів `equals()` та `hashCode()` реалізує поліморфізм. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Перевизначення `equals()` та `hashCode()` — це класичний приклад поліморфізму, який дозволяє об'єктам визначати власну логіку рівності, що потім використовується в поліморфному коді, наприклад, у колекціях.
>
> **Приклад:**
>
> У нас є `List<Object>`. Ми хочемо перевірити, чи містить вона певний об'єкт `User`.
>
> ```java
> class User {
>     private int id;
>     // ...
>     @Override
>     public boolean equals(Object o) { /* порівняння за id */ }
>     @Override
>     public int hashCode() { /* хеш-код на основі id */ }
> }
>
> List<Object> items = new ArrayList<>();
> items.add(new User(1));
>
> boolean contains = items.contains(new User(1)); // поверне true
> ```
>
> **Як поліморфізм тут працює:**
>
> 1. `ArrayList.contains(Object o)` всередині себе викликає метод `o.equals(element)` для кожного елемента списку.
> 2. Коли `contains` доходить до нашого об'єкта `User`, викликається `o.equals(...)`, де `o` — це `new User(1)`.
> 3. Хоча `contains` нічого не знає про клас `User` (він працює з `Object`), завдяки **динамічному поліморфізму** JVM викликає **перевизначену** версію `.equals()` з класу `User`, яка порівнює об'єкти за `id`.
> 4. Те ж саме стосується `HashSet` та `HashMap`, які використовують `hashCode()` та `equals()` для визначення унікальності та пошуку елементів. Вони поліморфно викликають ці методи для будь-яких об'єктів, що зберігаються.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як колекції та інші фреймворки покладаються на поліморфну поведінку базових методів `Object`.*

* * *

### **11 ❓ Питання:**

Які обмеження накладаються на перевизначення методу щодо `checked` та `unchecked` винятків? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Правила щодо винятків у перевизначених методах відрізняються для `checked` та `unchecked` exceptions.
>
> * **`Checked Exceptions` (перевірювані):**
>   * Це винятки, які компілятор змушує обробляти (наприклад, `IOException`).
>   * **Правило:** Перевизначений метод **не може кидати `checked` виняток, який є ширшим (є суперкласом) або новим** порівняно з тим, що кидає батьківський метод.
>   * Він може:
>         1. Не кидати жодних `checked` винятків.
>         2. Кидати той самий `checked` виняток.
>         3. Кидати **підклас** `checked` винятку.
>
> * **`Unchecked Exceptions` (неперевірювані):**
>   * Це винятки, що успадковують `RuntimeException` або `Error` (наприклад, `NullPointerException`, `IllegalArgumentException`).
>   * **Правило:** На `unchecked` винятки **немає жодних обмежень**. Перевизначений метод може кидати будь-які нові `unchecked` винятки, навіть якщо батьківський метод не кидав нічого.
>
> **Навіщо такі правила для `checked` exceptions:**
> Це підтримує принцип підстановки Лісков. Клієнтський код, написаний для роботи з батьківським класом, готовий обробляти лише ті `checked` винятки, які оголошені у батька. Якби дочірній клас почав кидати нові, несподівані `checked` винятки, це б зламало клієнтський код.

#### **📌 Що перевіряють:**

*Знання правил обробки винятків у контексті наслідування. Це важлива деталь для написання надійних API.*

* * *

### **12 ❓ Питання:**

Що таке приховування полів (Field Hiding)? Чому поля не перевизначаються, як методи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Приховування полів** виникає, коли дочірній клас оголошує поле з такою ж назвою, як у батьківського. На відміну від методів, поля в Java **не підтримують поліморфізм** і не перевизначаються.
>
> **Чому поля не перевизначаються:**
>
> * Вибір поля для доступу відбувається **на етапі компіляції (статичне зв'язування)** і базується на **типі посилання**, а не на фактичному типі об'єкта.
> * Методи ж використовують **динамічне зв'язування** (для не-`final`/`static`).
>
> **Приклад:**
>
> ```java
> class Parent { public int value = 10; }
> class Child extends Parent { public int value = 20; }
>
> Parent p = new Child();
> System.out.println(p.value); // Виведе 10!
> ```
>
> `p` має тип `Parent`, тому компілятор прив'язує доступ до поля `value` з класу `Parent`.
>
> **Причина такого дизайну:**
> Якби поля можна було перевизначати поліморфно, це б ускладнило логіку і могло б порушити інкапсуляцію та інваріанти батьківського класу. Загальноприйнята практика ООП полягає в тому, щоб **робити поля `private`** і надавати доступ до них через **методи (геттери)**, які, у свою чергу, можуть бути поліморфно перевизначені. Це значно більш гнучкий і безпечний підхід.

#### **📌 Що перевіряють:**

*Експертне розуміння відмінностей між статичним і динамічним зв'язуванням та їх застосування до полів та методів.*

* * *

### **13 ❓ Питання:**

Як `super.method()` працює у глибокій ієрархії наслідування? (наприклад, C extends B, B extends A). ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ключове слово `super` завжди посилається на **безпосереднього батька** класу, в якому воно викликається. Воно **не дозволяє "перестрибувати" через рівні** ієрархії.
>
> Розглянемо ієрархію: `A ← B ← C`.
>
> ```java
> class A { public void doWork() { System.out.println("A"); } }
> class B extends A { @Override public void doWork() { System.out.println("B"); } }
> class C extends B {
>     @Override
>     public void doWork() {
>         System.out.println("C");
>         // Цей виклик звертається до методу з класу B
>         super.doWork();
>
>         // НЕМОЖЛИВО зробити так, щоб викликати метод з A напряму:
>         // super.super.doWork(); // Помилка компіляції!
>     }
> }
> ```
>
> **Що відбувається:**
>
> * Коли `doWork()` викликається для об'єкта `C`, він виводить `"C"`.
> * Потім `super.doWork()` викликає реалізацію з **найближчого батька**, тобто з класу **`B`**. Він виводить `"B"`.
> * Якби метод `doWork()` у класі `B` також містив `super.doWork()`, то він, у свою чергу, викликав би реалізацію з класу `A`.
>
> **Чому так зроблено:**
>
> Цей механізм підтримує **інкапсуляцію**. Дочірній клас `C` не повинен знати про реалізацію "дідуся" (`A`). Він залежить лише від свого безпосереднього батька (`B`). Це запобігає тісній зв'язності у глибоких ієрархіях.

#### **📌 Що перевіряють:**

*Точне розуміння того, як працює `super`, та його обмежень. Знання принципів дизайну, що лежать в основі цих обмежень.*

* * *

### **14 ❓ Питання:**

Якби вам довелося обирати між успадкуванням від абстрактного класу та реалізацією інтерфейсу для досягнення поліморфізму, чому ви б надали перевагу в більшості випадків? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> У більшості випадків **я б надав перевагу реалізації інтерфейсу**. Це більш гнучкий і масштабований підхід, який краще відповідає сучасним принципам дизайну ПО.
>
> **Аргументи на користь інтерфейсів:**
>
> 1. **Множинне наслідування типів:** Клас може реалізувати багато інтерфейсів. Це дозволяє створювати об'єкти, що поєднують різні "ролі" або контракти, що неможливо з одиничним наслідуванням класів.
>
> 2. **Слабка зв'язність:** Інтерфейс визначає лише **чистий контракт**, не нав'язуючи жодної реалізації чи стану. Класи, що його реалізують, залишаються незалежними один від одного і від самого інтерфейсу, крім сигнатур методів. Це значно спрощує заміну реалізацій.
>
> 3. **Розділення "Що" від "Як":** Інтерфейси ідеально реалізують принцип абстракції, відокремлюючи "що" об'єкт робить (`API`) від того, "як" він це робить (`Implementation`).
>
> **Коли абстрактний клас може бути кращим:**
>
> * Коли існує тісний зв'язок **"is-a"**, і ви хочете надати **спільну базову реалізацію** та/або **спільний стан (поля)** для всіх дочірніх класів. Наприклад, абстрактний клас `AbstractList` у JDK надає базову логіку для багатьох методів, залишаючи лише кілька ключових для реалізації в підкласах.
>
> **Висновок:**
> Починати слід з інтерфейсу. Якщо ви бачите, що кілька реалізацій цього інтерфейсу дублюють багато коду, тоді можна розглянути створення проміжного абстрактного класу, який реалізує цей інтерфейс і виносить спільний код. Це поєднує гнучкість інтерфейсів з повторним використанням коду від абстрактних класів.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен вміти приймати обґрунтовані дизайнерські рішення та розуміти довгострокові наслідки свого вибору.*

* * *

### **15 ❓ Питання:**

Поясніть, що таке **принцип підстановки Лісков (LSP)** простими словами, і як неправильне перевизначення може його порушити. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Принцип підстановки Лісков** — це один з принципів SOLID, який стверджує:
>
> > "Об'єкти дочірнього класу повинні мати можливість підставлятися замість об'єктів батьківського класу без зміни коректності та очікуваної поведінки програми".
>
> Простими словами: якщо ваш код працює з `Animal`, він повинен так само коректно працювати, якщо ви передасте йому `Dog` або `Cat`, і нічого не "зламається".
>
> **Як неправильне перевизначення може його порушити:**
>
> 1. **Посилення передумов (Preconditions):**
>     * Дочірній клас висуває більш строгі вимоги до вхідних даних, ніж батьківський.
>     * *Приклад:* Метод `parent.process(int age)` працює для будь-якого віку, а дочірній `@Override process(int age)` кидає виняток, якщо `age < 18`. Код, який очікував, що метод працює для всіх, "зламається".
>
> 2. **Послаблення постумов (Postconditions):**
>     * Дочірній клас не гарантує виконання того, що обіцяв батьківський.
>     * *Приклад:* `Database.save()` гарантує, що дані збережені. Дочірній `CacheableDatabase.save()` зберігає дані лише в кеші і не гарантує запис на диск.
>
> 3. **Порушення інваріантів батька:**
>     * Найвідоміший приклад — **проблема "Коло-Еліпс" або "Квадрат-Прямокутник"**. Квадрат є різновидом прямокутника, але він порушує інваріант прямокутника про те, що ширина і висота є незалежними. Якщо змінити ширину квадрата, його висота теж зміниться, що не очікується від прямокутника.
>
> Дотримання LSP є критично важливим для створення надійних і передбачуваних ієрархій наслідування і є основою для коректного поліморфізму.

#### **📌 Що перевіряють:**

*Глибоке розуміння принципів SOLID та їх зв'язку з ООП. Кандидат повинен вміти аналізувати ієрархії класів на відповідність цим принципам.*
