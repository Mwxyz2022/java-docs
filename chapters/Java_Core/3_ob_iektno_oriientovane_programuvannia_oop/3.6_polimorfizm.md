---
layout: default
title: "Поліморфізм"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 6
---

# Поліморфізм

* короткий опис: Принцип ООП, що дозволяє об'єктам одного типу мати різну поведінку залежно від їхньої конкретної реалізації. Реалізується через перевизначення (runtime) та перевантаження (compile-time) методів.

* поняття: Поліморфізм, `IS-A` відношення, динамічний поліморфізм (runtime), статичний поліморфізм (compile-time).

* приклади: `Animal a = new Dog(); a.speak();` (динамічний), `add(int, int)` та `add(double, double)` (статичний).

* ключові моменти: Одинакова сигнатура методу, але різна поведінка. Дозволяє гнучке розширення системи.

* що важливо знати на співбесіді: Визначення, види (overloading/overriding), як реалізується.

### **✅ Офіційне визначення:**

Поліморфізм — це принцип ООП, який дозволяє об’єктам одного типу мати різну поведінку залежно від їхньої конкретної реалізації. У Java поліморфізм реалізується через перевизначення методів (runtime/dynamic polymorphism) і перевантаження методів (compile-time/static polymorphism).

### **🧠 Простими словами:**

Поліморфізм — це коли один і той самий метод викликається на різних об’єктах, і кожен обʼєкт виконує його по-своєму. Тип — один, а результат — різний. Це як слово “говори” для собаки, кішки і папуги — кожен «говорить» по-своєму.

---

## **📊 Типи поліморфізму в Java:**

| Тип | Назва | Приклад |
| :---: | :---: | :---: |
| **Compile-time** | Статичний (перевантаження) | **`add(int, int) / add(double, double)`** |
| **Runtime** | Динамічний (перевизначення) | **`animal.speak() -> bark / meow`** |

---

## **Пояснення на пальцях (💡)**

* **Статичний поліморфізм:**  
  Методи з однаковою назвою працюють з різною кількістю або типом аргументів — Java знає, який метод викликати ще під час компіляції.

* **Динамічний поліморфізм:**  
  Методи перевизначаються в дочірніх класах, і JVM вирішує, яку саме реалізацію викликати під час виконання.

* **Головне:** IS-A відношення — кожен Dog є Animal, тому ми можемо посилатися на Dog як на Animal.

---

## **Корисні приклади (🧪)**

##### **✅ Статичний (compile-time) поліморфізм:**

```java
class Calculator {
    int add(int a, int b) {
        return a + b;
    }
    double add(double a, double b) {
        return a + b;
    }
}
```

##### **✅ Динамічний (runtime) поліморфізм:**

```java
class Animal {
    void speak() {
        System.out.println("Some sound");
    }
}

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Bark");
    }
}

class Cat extends Animal {
    @Override
    void speak() {
        System.out.println("Meow");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal a1 = new Dog();
        Animal a2 = new Cat();

        a1.speak(); // Bark
        a2.speak(); // Meow
    }
}
```

---

## **Порівняння (⚖️)**

| Тип поліморфізму | Коли визначається реалізація | Як реалізується |
| :---: | :---: | :---: |
| **Статичний** | Під час компіляції | Перевантаження методів |
| **Динамічний** | Під час виконання програми | Перевизначення методів \+ **`IS-A`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 "Один метод — багато поведінок"  
🧠 Polymorphism \= Many Forms  
poly — багато  
morph — форм

---

## **🟩 Підсумок:**

Поліморфізм — це можливість писати код на високому рівні абстракції. Ми використовуємо один тип (суперклас) для роботи з багатьма конкретними реалізаціями. Це дозволяє створювати гнучкий, розширюваний і чистий код, особливо при роботі з колекціями, шаблонами, фреймворками.

---

**🔗 Для глибшого вивчення:**

* [**Oracle Java Tutorials — Polymorphism**](https://docs.oracle.com/javase/tutorial/java/IandI/polymorphism.html)

Добре, я ознайомився з конспектом. На співбесідах на тему "Поліморфізм" ключове — це вміти чітко розмежувати **статичний та динамічний поліморфізм**, пояснити, як **динамічне зв'язування** реалізує поліморфізм часу виконання, і навести **практичні переваги** цього принципу. Я складу FAQ, фокусуючись на цих аспектах.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке поліморфізм своїми словами? Яку головну проблему він вирішує?

> #### **💬 Відповідь:**
>
> **Поліморфізм** — це принцип ООП, який дозволяє працювати з об'єктами різних класів через **єдиний спільний інтерфейс (тип)**, при цьому кожен об'єкт буде виконувати дії **по-своєму**.
>
> **Головна ідея** полягає в тому, щоб один і той самий виклик методу призводив до різної поведінки залежно від **фактичного типу об'єкта** під час виконання програми. Це можна описати фразою "один інтерфейс, багато реалізацій".
>
> **Яку проблему він вирішує:**
> Він усуває необхідність у великій кількості умовних конструкцій (`if-else` або `switch`), які б перевіряли тип об'єкта перед тим, як викликати для нього відповідний метод. Завдяки поліморфізму, JVM робить цю роботу за нас, роблячи код **більш гнучким, розширюваним та легким у підтримці**.

#### **📝 Аналогія:**

Уявіть, у вас є USB-порт (це інтерфейс `Connectable`). Ви можете підключити до нього флешку, мишу або зарядний пристрій (це об'єкти `FlashDrive`, `Mouse`, `Charger`). Хоча ви взаємодієте з ними через один і той самий порт, кожен пристрій виконує свою унікальну функцію. Вам не потрібно знати, що саме підключено, — система розпізнає це сама.

#### **📌 Що перевіряють:**

*Здатність пояснити концепцію простими словами та, що важливіше, визначити її основну практичну користь — уникнення громіздких умовних перевірок типу.*

* * *

### **2 ❓ Питання:**

Які два основні види поліморфізму існують в Java? Поясніть різницю.

> #### **💬 Відповідь:**
>
> У Java існує два основних види поліморфізму, які розрізняються за тим, коли саме вирішується, який метод буде викликано.
>
> **1. Поліморфізм часу компіляції (Compile-time Polymorphism або Статичний поліморфізм):**
>
> * **Реалізація:** За допомогою **перевантаження методів (method overloading)**.
> * **Як працює:** Компілятор вирішує, яку версію методу викликати, **на етапі компіляції**, аналізуючи кількість, типи та порядок аргументів, переданих у виклик.
> * **Приклад:** У класі `Calculator` можуть бути методи `add(int, int)` та `add(double, double)`. Компілятор точно знає, який з них викликати, залежно від переданих чисел.
>
> **2. Поліморфізм часу виконання (Runtime Polymorphism або Динамічний поліморфізм):**
>
> * **Реалізація:** За допомогою **перевизначення методів (method overriding)**.
> * **Як працює:** Вирішення, яку версію методу викликати, відбувається **під час виконання програми** JVM. JVM дивиться на **фактичний тип об'єкта**, на який вказує посилання, і викликає його перевизначений метод.
> * **Приклад:** `Animal animal = new Dog(); animal.makeSound();` — хоча тип посилання `Animal`, JVM викличе метод `makeSound()` з класу `Dog`.

#### **📝 Коментар:**

Коли в контексті ООП говорять "поліморфізм", зазвичай мають на увазі саме динамічний поліморфізм часу виконання, оскільки він є більш потужним принципом.

#### **📌 Що перевіряють:**

*Чітке розмежування між двома видами поліморфізму, їхніми механізмами реалізації та часом зв'язування.*

* * *

### **3 ❓ Питання:**

Напишіть простий приклад коду, що демонструє динамічний поліморфізм.

> #### **💬 Відповідь:**
>
> Звісно. Класичний приклад — це ієрархія фігур.
>
> ```java
> // 1. Абстракція (батьківський клас)
> abstract class Shape {
>     abstract void draw(); // Абстрактний метод
> }
>
> // 2. Конкретні реалізації (дочірні класи)
> class Circle extends Shape {
>     @Override
>     void draw() {
>         System.out.println("Малюємо коло.");
>     }
> }
>
> class Square extends Shape {
>     @Override
>     void draw() {
>         System.out.println("Малюємо квадрат.");
>     }
> }
>
> public class DrawingApp {
>     public static void main(String[] args) {
>         // Створюємо масив, що може зберігати різні фігури
>         Shape[] shapes = new Shape[2];
>         shapes[0] = new Circle(); // Об'єкт Circle
>         shapes[1] = new Square(); // Об'єкт Square
>
>         // 3. Поліморфний виклик
>         for (Shape shape : shapes) {
>             // Один і той самий виклик методу, але різна поведінка
>             shape.draw();
>         }
>     }
> }
> ```
>
> **Вивід:**
>
> ```
>
> Малюємо коло.
> Малюємо квадрат.
>
> ```
>
> Тут ми ітеруємо по масиву типу `Shape` і викликаємо один і той самий метод `draw()`. Завдяки поліморфізму, JVM під час виконання дивиться на фактичний тип об'єкта (`Circle` або `Square`) і викликає відповідну перевизначену версію методу.

#### **📌 Що перевіряють:**

*Практичні навички написання коду, що використовує ключові елементи поліморфізму: абстракцію, наслідування та перевизначення.*

* * *

### **4 ❓ Питання:**

Що таке динамічне зв'язування (Dynamic Method Dispatch) і як воно реалізує поліморфізм?

> #### **💬 Відповідь:**
>
> **Динамічне зв'язування (Dynamic Binding або Late Binding)** — це механізм, за допомогою якого вибір конкретної реалізації методу для виклику відбувається **під час виконання програми (runtime)**, а не на етапі компіляції.
>
> Саме цей механізм є основою динамічного поліморфізму в Java.
>
> **Як це працює:**
>
> 1. Коли ви викликаєте перевизначений метод через посилання на суперклас (наприклад, `Animal animal = new Dog(); animal.makeSound();`), компілятор лише перевіряє, чи існує такий метод (`makeSound()`) у типі посилання (`Animal`).
>
> 2. Однак, замість того, щоб жорстко "зашити" виклик методу з `Animal`, компілятор генерує байткод зі спеціальною інструкцією (наприклад, `invokevirtual`), яка каже JVM: "Знайди реалізацію цього методу під час виконання".
>
> 3. Під час виконання, **JVM** дивиться на **фактичний тип об'єкта**, на який вказує посилання `animal` (у нашому випадку, це `Dog`).
>
> 4. JVM шукає відповідний метод в таблиці віртуальних методів цього об'єкта і викликає **конкретну реалізацію** з класу `Dog`.
>
> Цей процес дозволяє програмі бути гнучкою: ви можете додати новий клас `Fish` до ієрархії `Animal`, і існуючий код, який працює з `Animal`, буде коректно викликати `makeSound()` для `Fish` без будь-яких змін.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як JVM реалізує поліморфізм "під капотом". Це питання на розуміння різниці між compile-time та runtime.*

* * *

### **5 ❓ Питання:**

Які методи в Java не можуть бути поліморфними (у сенсі динамічного поліморфізму)?

> #### **💬 Відповідь:**
>
> Поліморфна поведінка (вибір реалізації під час виконання) можлива лише для методів, що використовують динамічне зв'язування. Тому методи, для яких компілятор може визначити кінцеву реалізацію **на етапі компіляції (static binding)**, не є поліморфними.
>
> До таких методів належать:
>
> 1. **`static` методи:**
>     * Вони належать класу, а не об'єкту. Компілятор завжди викликає версію методу на основі **типу посилання**, а не фактичного типу об'єкта.
>
> 2. **`final` методи:**
>     * Їх **не можна перевизначити**. Оскільки існує лише одна версія цього методу, компілятор точно знає, яку реалізацію викликати.
>
> 3. **`private` методи:**
>     * Вони невидимі для дочірніх класів і, відповідно, **не можуть бути перевизначені**. Вони також є неявно `final`.
>
> По суті, динамічний поліморфізм працює лише для **публічних або захищених нестатичних і нефінальних** методів екземпляра.

#### **📌 Що перевіряють:**

*Знання обмежень поліморфізму та того, як різні модифікатори (`static`, `final`, `private`) впливають на механізм зв'язування.*

* * *

### **6 ❓ Питання:**

Що таке коваріантні типи повернення і як вони розширюють можливості поліморфізму? ⭐️

> #### **💬 Відповідь:**
>
> **Коваріантні типи повернення** — це можливість у Java (з версії 5.0), яка дозволяє перевизначеному методу в дочірньому класі повертати **підтип** того типу, що повертає батьківський метод.
>
> **Приклад:**
>
> ```java
> class VehicleFactory {
>     public Vehicle createVehicle() {
>         return new Vehicle();
>     }
> }
>
> class CarFactory extends VehicleFactory {
>     @Override
>     // Повертаємо Car, який є підтипом Vehicle. Це коваріантність.
>     public Car createVehicle() {
>         return new Car();
>     }
> }
> ```
>
> **Як це розширює поліморфізм:**
>
> * **Більша типізація та безпека:** Це дозволяє створювати більш точні ієрархії. Наприклад, фабрика автомобілів логічно повинна створювати саме автомобілі.
> * **Усунення необхідності в явному приведенні типів (`cast`):** Без цієї можливості, `createVehicle` у `CarFactory` мав би повертати `Vehicle`, і клієнтському коду довелося б писати `Car myCar = (Car) carFactory.createVehicle();`. З коваріантними типами приведення не потрібне: `Car myCar = carFactory.createVehicle();`.
>
> Це робить код більш чистим, безпечним та інтуїтивно зрозумілим, розширюючи гнучкість поліморфізму і на типи повернення.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови та їхньої практичної користі для написання більш якісного коду.*

* * *

### **7 ❓ Питання:**

Як поліморфізм пов'язаний з принципом "програмуйте на рівні інтерфейсу, а не реалізації"?

> #### **💬 Відповідь:**
>
> Вони пов'язані нерозривно. **Поліморфізм є ключовим механізмом, який робить цей принцип можливим і корисним.**
>
> **Принцип "програмуйте на рівні інтерфейсу"** стверджує, що ваш код повинен залежати від **абстракцій (інтерфейсів)**, а не від **конкретних класів (реалізацій)**.
>
> **Приклад:**
>
> * **Погано (залежність від реалізації):**
>     `ArrayList<String> list = new ArrayList<>();`
>     В цьому випадку змінна `list` жорстко прив'язана до типу `ArrayList`. Якщо ви захочете замінити реалізацію на `LinkedList`, вам доведеться змінювати тип змінної.
>
> * **Добре (залежність від інтерфейсу):**
>     `List<String> list = new ArrayList<>();`
>     Тут змінна `list` має тип інтерфейсу `List`. Це означає, що їй можна присвоїти об'єкт будь-якого класу, що реалізує `List`.
>
> **Як поліморфізм це забезпечує:**
>
> Коли ви викликаєте методи через посилання `list` (`list.add()`, `list.get()`), саме завдяки **поліморфізму** JVM під час виконання викличе правильну реалізацію методу — ту, що належить об'єкту `ArrayList`. Якщо ви пізніше зміните код на `List<String> list = new LinkedList<>();`, решта коду продовжить працювати без змін, бо поліморфізм "подбає" про виклик методів з `LinkedList`.
>
> Таким чином, програмування на рівні інтерфейсів дозволяє досягти гнучкості, а поліморфізм — це "двигун", який змушує цю гнучкість працювати.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти, як принципи ООП використовуються для досягнення таких архітектурних цілей, як низька зв'язність.*

* * *

### **8 ❓ Питання:**

Що таке перевантаження методів і чому його називають статичним поліморфізмом? ⭐️

> #### **💬 Відповідь:**
>
> **Перевантаження методів (Method Overloading)** — це можливість мати в одному класі кілька методів з **однаковою назвою**, але **різними списками параметрів** (різна кількість, типи або порядок параметрів).
>
> **Приклад:**
>
> ```java
> public class Logger {
>     public void log(String message) { /* ... */ }
>     public void log(String message, int level) { /* ... */ }
>     public void log(Exception e) { /* ... */ }
> }
> ```
>
> **Чому це статичний поліморфізм:**
>
> Його називають **статичним** (або поліморфізмом часу компіляції), тому що вибір конкретної версії методу для виклику робить **компілятор** на **етапі компіляції**.
>
> Компілятор аналізує типи та кількість аргументів, які ви передали у виклик методу, і жорстко "прив'язує" цей виклик до однієї з існуючих перевантажених версій.
>
> У згенерованому байткоді вже буде вказано, яку саме версію методу `log` потрібно викликати. На відміну від динамічного поліморфізму, тут немає ніякого вибору під час виконання.

#### **📌 Що перевіряють:**

*Здатність чітко розмежувати Overloading та Overriding та пояснити, чому їх відносять до різних типів поліморфізму.*

* * *

### **9 ❓ Питання:**

Як оператор `instanceof` може "зламати" поліморфізм? Коли його використання є ознакою поганого дизайну? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Оператор **`instanceof`** дозволяє перевірити, чи є об'єкт екземпляром певного класу.
>
> Хоча іноді він буває корисним (наприклад, у реалізації `equals()`), його надмірне використання у бізнес-логіці вважається **антипатерном**, який **руйнує переваги поліморфізму**.
>
> **Як він "ламає" поліморфізм:**
>
> Поліморфізм покликаний **усунути** необхідність перевіряти тип об'єкта. Замість цього ми просто викликаємо метод, а об'єкт сам знає, як його виконати.
>
> Код, що використовує `instanceof`, робить прямо протилежне:
>
> **Поганий дизайн (з `instanceof`):**
>
> ```java
> public void processAnimal(Animal animal) {
>     if (animal instanceof Dog) {
>         // спеціальна логіка для собаки
>         ((Dog) animal).wagTail();
>     } else if (animal instanceof Cat) {
>         // спеціальна логіка для кота
>         ((Cat) animal).purr();
>     }
> }
> ```
>
> **Проблеми такого підходу:**
>
> 1. **Порушення принципу відкритості/закритості:** Якщо ми додамо новий клас `Bird`, нам доведеться **змінювати** цей `if-else` блок у всіх місцях, де він використовується. Система не є розширюваною.
> 2. **Зайва зв'язність:** Метод `processAnimal` тепер знає про всі конкретні підкласи `Animal`.
>
> **Правильний поліморфний підхід:**
>
> Слід додати абстрактний метод `performAction()` до класу `Animal` і перевизначити його в кожному дочірньому класі.
>
> ```java
> public void processAnimal(Animal animal) {
>     animal.performAction(); // JVM сама викличе потрібну версію
> }
> ```
>
> Цей код є простим, гнучким і легко розширюваним.
>
> **Висновок:** Використання `instanceof` з `if-else` часто є ознакою того, що ви боретеся з ООП, а не використовуєте його переваги.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, навіщо потрібен поліморфізм. Кандидат повинен бачити `instanceof` як "червоний прапор" у дизайні коду.*

* * *

### **10 ❓ Питання:**

Що таке ad-hoc поліморфізм та параметричний поліморфізм? До яких типів поліморфізму в Java вони відносяться? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це терміни з теорії мов програмування для класифікації видів поліморфізму.
>
> 1. **Ad-hoc поліморфізм (або спеціальний поліморфізм):**
>     * **Ідея:** Коли одна й та сама функція або оператор має **різні реалізації** для різних типів, і ці реалізації можуть бути абсолютно непов'язаними.
>     * **Приклад у Java:** **Перевантаження методів (Method Overloading)**. Метод `add(int, int)` та `add(String, String)` мають однакову назву, але їхні реалізації (додавання чисел та конкатенація рядків) є логічно різними. Це випадок ad-hoc поліморфізму. Оператор `+` також є прикладом.
>     * **Тип в Java:** Статичний поліморфізм.
>
> 2. **Параметричний поліморфізм:**
>     * **Ідея:** Коли код може працювати з різними типами, не знаючи їхньої конкретної структури. Функціональність є однаковою для всіх типів, що задовольняють певному контракту.
>     * **Приклад у Java:** **Дженерики (Generics)**. Клас `ArrayList<T>` може зберігати об'єкти будь-якого типу `T`. Його методи (`add`, `get`) працюють однаково, незалежно від того, чи це `ArrayList<String>`, чи `ArrayList<Integer>`. Логіка не залежить від конкретного типу.
>
> Таким чином:
>
> * **Перевантаження** — це ad-hoc поліморфізм.
> * **Перевизначення (subtype polymorphism)** — це класичний динамічний поліморфізм.
> * **Дженерики** — це параметричний поліморфізм.

#### **📌 Що перевіряють:**

*Теоретичні знання з комп'ютерних наук та теорії мов програмування. Це питання для перевірки академічної підготовки та широти кругозору.*

* * *

### **11 ❓ Питання:**

Поясніть, що таке `vtable` (таблиця віртуальних методів) і яку роль вона відіграє в реалізації динамічного поліморфізму. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Таблиця віртуальних методів (vtable)** — це внутрішня структура даних, яку використовує JVM (і багато інших компіляторів для ООП-мов) для ефективної реалізації **динамічного зв'язування**.
>
> **Як це працює:**
>
> 1. **Створення `vtable`:** Для кожного **класу**, що завантажується, JVM створює `vtable`. Це, по суті, масив **покажчиків на реалізації методів**.
>     * Якщо клас перевизначає метод батька, у відповідному записі `vtable` буде стояти покажчик на нову реалізацію.
>     * Якщо не перевизначає, то буде успадковано покажчик з `vtable` батьківського класу.
>
> 2. **Прив'язка до об'єкта:** Кожен **об'єкт**, що створюється в купі, має прихований внутрішній покажчик на `vtable` свого класу.
>
> 3. **Виклик методу:** Коли відбувається поліморфний виклик `obj.doWork()`:
>     * JVM через покажчик об'єкта `obj` знаходить його `vtable`.
>     * Вона знає, що метод `doWork()`, наприклад, завжди знаходиться на 5-й позиції в `vtable` для всієї ієрархії.
>     * JVM бере покажчик з 5-ї позиції `vtable` об'єкта `obj` і переходить за ним, викликаючи правильну реалізацію методу.
>
> **Чому це ефективно:**
>
> Це перетворює пошук методу в дуже швидку операцію: кілька звернень до пам'яті за покажчиками (`obj` → `vtable` → `метод`). Це набагато швидше, ніж шукати потрібний метод, проходячись по всій ієрархаті класів кожного разу.
>
> Саме завдяки `vtable` динамічний поліморфізм у Java є дуже швидким.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішню роботу JVM. Це питання для Senior-рівня, що вимагає розуміння низькорівневих механізмів реалізації ООП.*

* * *

### **12 ❓ Питання:**

Розгляньте такий код. Чи буде він працювати поліморфно? Поясніть чому.

```java
class Parent {
    public String name = "Parent";
}
class Child extends Parent {
    public String name = "Child";
}
//...
Parent p = new Child();
System.out.println(p.name);
```

⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код виведе **`Parent`**.
>
> **Він не буде працювати поліморфно**, тому що **поліморфізм у Java застосовується лише до методів екземпляра, але не до полів**.
>
> **Пояснення:**
>
> 1. **Приховування полів (Field Hiding):** Коли дочірній клас оголошує поле з такою ж назвою, як у батьківського, він не "перевизначає" його, а "приховує". Об'єкт `Child` буде містити **обидва поля `name`**: одне успадковане від `Parent`, інше — власне.
>
> 2. **Статичне зв'язування для полів:** Вибір, до якого поля звертатися, відбувається **на етапі компіляції** на основі **типу посилання**.
>
> 3. **Аналіз коду:**
>     * `Parent p = new Child();` — посилання `p` має тип `Parent`.
>     * Коли компілятор бачить вираз `p.name`, він "прив'язує" цей доступ до поля `name`, оголошеного в класі **`Parent`**.
>     * Тому, незважаючи на те, що `p` вказує на об'єкт `Child`, буде отримано значення поля з `Parent`.
>
> Це класичний приклад того, чому приховування полів є поганою практикою і чому доступ до стану завжди слід робити через інкапсульовані методи (геттери), які, на відміну від полів, можуть бути поліморфними.

#### **📌 Що перевіряють:**

*Експертне розуміння того, що саме в Java є поліморфним, а що — ні. Знання концепції приховування полів.*

* * *

### **13 ❓ Питання:**

Поясніть на прикладі, як можна "зламати" контракт Лісков (Liskov Substitution Principle), і як це пов'язано з поліморфізмом. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Принцип підстановки Лісков (LSP)** стверджує, що об'єкти дочірніх класів повинні мати можливість підставлятися замість об'єктів батьківських класів, **не змінюючи коректності програми**.
>
> Це означає, що дочірній клас не повинен "звужувати" контракт батьківського або порушувати його інваріанти.
>
> **Приклад, що "ламає" LSP (класична проблема Прямокутника та Квадрата):**
>
> ```java
> class Rectangle {
>     protected int width, height;
>     public void setWidth(int width) { this.width = width; }
>     public void setHeight(int height) { this.height = height; }
>     public int getArea() { return width * height; }
> }
>
> // З точки зору геометрії, Квадрат "є" Прямокутником
> class Square extends Rectangle {
>     @Override
>     public void setWidth(int width) {
>         super.setWidth(width);
>         super.setHeight(width); // Порушуємо інваріант: тепер висота теж змінюється!
>     }
>     @Override
>     public void setHeight(int height) {
>         super.setWidth(height);
>         super.setHeight(height); // Порушуємо інваріант!
>     }
> }
> ```
>
> **Тест, який "ламається":**
>
> ```java
> public void testArea(Rectangle r) {
>     r.setWidth(5);
>     r.setHeight(4);
>     // Ми очікуємо, що площа буде 5 * 4 = 20
>     assert r.getArea() == 20;
> }
> // testArea(new Rectangle()); // Пройде
> // testArea(new Square());    // Впаде! Площа буде 16 (4*4).
> ```
>
> **Зв'язок з поліморфізмом:**
> Тут ми не можемо безпечно використовувати `Square` там, де очікується `Rectangle`, тому що `Square` порушує контракт свого батька (інваріант, що ширина та висота незалежні). Це означає, що **поліморфна підстановка не працює**, і принцип Лісков порушено.

#### **📌 Що перевіряють:**

*Глибоке розуміння принципів SOLID та того, як неправильна ієрархія наслідування може призвести до тонких логічних помилок.*

* * *

### **14 ❓ Питання:**

Як `private` метод в батьківському класі та метод з такою ж сигнатурою в дочірньому взаємодіють між собою? Чи є це поліморфізмом? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони **ніяк не взаємодіють**. Це **не є** ані перевизначенням, ані перевантаженням, ані приховуванням. Це просто **два абсолютно незалежних методи**, які випадково мають однакові назви.
>
> **Пояснення:**
>
> 1. `private` метод у батьківському класі **невидимий** для дочірнього класу. Він не є частиною контракту, що успадковується.
> 2. Коли ви створюєте в дочірньому класі метод з такою ж сигнатурою, він не має жодного уявлення про існування `private` методу у батька. Він не може його "перевизначити", бо не бачить.
>
> **Приклад:**
>
> ```java
> class Parent {
>     private void doWork() {
>         System.out.println("Parent's private work");
>     }
>     public void execute() {
>         doWork(); // Викличе свій private метод
>     }
> }
>
> class Child extends Parent {
>     // Це НЕ перевизначення. Це просто новий метод,
>     // що належить тільки Child.
>     private void doWork() {
>         System.out.println("Child's private work");
>     }
> }
>
> public class Main {
>     public static void main(String[] args) {
>         Parent p = new Child();
>         p.execute();
>     }
> }
> ```
>
> **Вивід:**
>
> ```
> Parent's private work
> ```
>
> Оскільки метод `execute` успадковано з `Parent`, він викликає `private`-метод `doWork()` зі свого власного класу (`Parent`), до якого він має доступ. Він нічого не знає про `private` метод у `Child`.
>
> Це не є поліморфізмом, оскільки немає динамічного зв'язування. Зв'язування виклику `private`-методу відбувається на етапі компіляції.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як модифікатори доступу впливають на механізми ООП. Здатність розрізняти незалежні методи та методи, що беруть участь у наслідуванні.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви працюєте з колекцією `List<Number>`. Чому ви можете додати в неї `Integer` та `Double`, але не можете присвоїти `List<Integer>` змінній типу `List<Number>`? Поясніть з точки зору поліморфізму та дженериків. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання стосується **коваріантності та інваріантності** в системі типів Java.
>
> **Чому можна додати `Integer` в `List<Number>`:**
>
> * `Integer` є підкласом `Number`.
> * Метод `list.add(element)` для `List<Number>` очікує елемент типу `Number`.
> * Завдяки **поліморфізму**, ви можете передати об'єкт підкласу (`Integer`) туди, де очікується об'єкт суперкласу (`Number`). Це класичний поліморфізм.
>
> **Чому не можна присвоїти `List<Integer>` змінній `List<Number>`:**
> `List<Number> nums = new ArrayList<Integer>(); // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Дженерики в Java є інваріантними.** Це означає, що якщо `Integer` є підтипом `Number`, то `List<Integer>` **не є** підтипом `List<Number>`.
>
> **Причина такої поведінки — безпека типів.**
>
> Уявімо на мить, що таке присвоєння було б можливим:
>
> ```java
> List<Integer> integers = new ArrayList<>();
> // Припустимо, це можливо
> List<Number> numbers = integers;
>
> // Тепер через посилання numbers ми можемо додати в список Double
> numbers.add(10.5); // Логічно коректно, бо Double є підтипом Number
>
> // А тепер виникає катастрофа...
> // Початкове посилання integers вказує на той же список!
> Integer value = integers.get(0); // Спроба присвоїти Double до Integer -> ClassCastException!
> ```
>
> Щоб запобігти такій **забрудненості купи (heap pollution)**, система типів Java забороняє таке присвоєння на етапі компіляції.
>
> Для гнучкої роботи з ієрархією типів у дженериках існують **wildcards**, наприклад:
> `List<? extends Number> nums = new ArrayList<Integer>();`
> Це посилання дозволяє читати елементи як `Number`, але забороняє додавати нові (окрім `null`), що забезпечує безпеку типів.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння системи типів Java, включаючи взаємодію поліморфізму з дженериками та концепцію інваріантності. Це питання для рівня Senior.*
