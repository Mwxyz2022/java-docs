---
layout: default
title: "Принципи SOLID"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 28
---

# Принципи SOLID

* короткий опис: SOLID – це набір з 5 архітектурних принципів (Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion) для проектування чистого, гнучкого та масштабованого ООП-коду.

* поняття: SRP, OCP, LSP, ISP, DIP.

* приклади: Відокремлення відповідальності (SRP), розширення поведінки без зміни коду (OCP).

* ключові моменти: Фундамент якісного дизайну, покращення підтримуваності, тестування та масштабованості коду.

* що важливо знати на співбесіді: Назвати та пояснити кожен принцип, його практичне застосування.

### **✅ Офіційне визначення:**

**SOLID** — це акронім із п’яти принципів об’єктно-орієнтованого програмування, які допомагають проектувати зрозумілий,
гнучкий і легко підтримуваний код. Запропоновані Робертом Мартіном ("Дядько Боб").

### **🧠 Простими словами:**

**SOLID
** — це 5 золотих правил, як писати код, щоб його було легко розвивати без ламання, без "злипань", і щоб команди працювали з ним без страху
---

## **Основні принципи SOLID**

| Літера  |                  Принцип                  |                                Суть                                |
|:-------:|:-----------------------------------------:|:------------------------------------------------------------------:|
| **`S`** | Single Responsibility Principle (**SRP**) |       Клас має одну причину для змін (одну відповідальність)       |
| **`O`** |      Open/Closed Principle (**OCP**)      |        Клас відкритий для розширення, але закритий для змін        |
| **`L`** |  Liskov Substitution Principle (**LSP**)  |           Підкласи мають працювати замість батьківських            |
| **`I`** | Interface Segregation Principle (**ISP**) | Не змушуй реалізовувати зайве — краще кілька маленьких інтерфейсів |
| **`D`** | Dependency Inversion Principle (**DIP**)  |            Залежності на абстракції, а не на реалізації            |

---

## **Пояснення на пальцях (💡)**

* **S — Один клас \= одна відповідальність**  
  Не треба змішувати зберігання, друк і валідацію в одному класі — кожен клас робить щось одне.

* **O — Краще додавати, ніж змінювати**  
  Не редагуй старий код — додай новий через успадкування або делегацію.

* **L — Підкласи працюють без сюрпризів**  
  Якщо замість **`Animal`** передаєш **`Dog`**, програма має працювати як і раніше.

* **I — Маленькі інтерфейси краще**  
  Не змушуй клас реалізовувати методи, які йому не потрібні.

* **D — Залежність від абстракцій**  
  Клас має залежати від інтерфейсів, а не від конкретних класів.

---

## **Корисні приклади (🧪)**

**🔸 SRP (Single Responsibility):**

```java
class User {
    String name;
    // ❌ Не варто тут зберігати або друкувати user
}

class UserRepository {
    void save(User user) { ...}
}

class UserPrinter {
    void print(User user) { ...}
}
```

**🔸 OCP (Open/Closed):**

```java
interface Payment {
    void pay();
}

class CreditCardPayment implements Payment { ...
}

class PayPalPayment implements Payment { ...
}

void processPayment(Payment p) {
    p.pay(); // нові типи оплат додаються, а не змінюють логіку
}
```

**🔸 LSP (Liskov):**

```java
class Bird {
    void fly() {
    }
}

class Penguin extends Bird {
    // ❌ penguin не літає -- LSP порушено
}
```

**🔸 ISP (Interface Segregation):**

```java
interface Printer {
    void print();

    void fax(); // ❌ не всі принтери можуть факсувати
}

// Краще:
interface Printable {
    void print();
}

interface Faxable {
    void fax();
}
```

**🔸 DIP (Dependency Inversion):**

```java
class AuthService {
    private UserRepository repo;

    AuthService(UserRepository repo) {
        this.repo = repo;
    }
}
```

-> залежність від абстракції **`UserRepository`**, а не від конкретної реалізації **`DatabaseUserRepository`**.

---

## **Порівняння (⚖️)**

| Принцип |           Основна мета           |         Ключова вигода          |
|:-------:|:--------------------------------:|:-------------------------------:|
| **SRP** |      Відокремити обов’язки       |  Простіше тестувати, змінювати  |
| **OCP** |     Додавати — не змінювати      | Менше шансів зламати старий код |
| **LSP** |    Безпечна заміна підкласом     |     Поведінка передбачувана     |
| **ISP** | Не змушувати реалізовувати зайве |       Менше зайвої логіки       |
| **DIP** |     Залежати від абстракцій      | Легка заміна реалізацій, моків  |

---

## **🧠 Мнемоніка для запам’ятовування:**

| Літера |      Розшифровка      |
|:------:|:---------------------:|
| **S**  | Single responsibility |
| **O**  |      Open/closed      |
| **L**  |  Liskov substitution  |
| **I**  | Interface segregation |
| **D**  | Dependency inversion  |

**Абревіатура:** SOLID \= твердий фундамент ООП-дизайну

---

## **🟩 Підсумок:**

Принципи SOLID — основа якісного ООП-коду. Вони допомагають створювати системи, які не розвалюються при зміні, легко
розширюються, тестуються та зручні для командної роботи. Дотримуйся SOLID — і твій код буде як LEGO: надійний і гнучкий.

Добре, я ознайомився з конспектом. На співбесідах на цю тему важливо не тільки розшифрувати акронім, а й **пояснити суть кожного принципу своїми словами**, навести **практичні приклади порушення та дотримання**, і розуміти, як ці принципи **взаємопов'язані** та допомагають створювати гнучку та підтримувану архітектуру.

* * *

# **FAQ — Часті запитання (❓)**

* * *

### **1 ❓ Питання:**

Що таке принципи SOLID і яка їхня головна мета?

> #### **💬 Відповідь:**
>
> **SOLID** — це акронім, що позначає п'ять фундаментальних принципів об'єктно-орієнтованого дизайну, сформульованих Робертом Мартіном ("Дядько Боб"). Ці принципи є рекомендаціями щодо того, як проектувати класи та їхні взаємозв'язки.
>
> **Головна мета SOLID** — створити систему, яка є:
>
> * **Зрозумілою:** код легко читати та аналізувати.
> * **Гнучкою:** легко вносити зміни та додавати нову функціональність.
> * **Підтримуваною:** легко виправляти помилки та розвивати проект у довгостроковій перспективі.
> * **Тестованою:** компоненти системи можна легко тестувати в ізоляції.
>
> Простими словами, SOLID допомагає уникнути "спагеті-коду" та побудувати міцну, надійну архітектуру, яка не "розвалиться" при першій же зміні вимог.

#### **📝 Розшифровка акроніму:**

* **S** - Single Responsibility Principle (Принцип єдиної відповідальності)
* **O** - Open/Closed Principle (Принцип відкритості/закритості)
* **L** - Liskov Substitution Principle (Принцип підстановки Барбари Лісков)
* **I** - Interface Segregation Principle (Принцип розділення інтерфейсу)
* **D** - Dependency Inversion Principle (Принцип інверсії залежностей)

#### **📌 Що перевіряють:**

*Загальне розуміння, що SOLID — це не просто правила, а інструменти для досягнення якості програмного забезпечення.*

* * *

### **2 ❓ Питання:**

Поясніть принцип єдиної відповідальності (Single Responsibility Principle - SRP). Наведіть приклад порушення.

> #### **💬 Відповідь:**
>
> **Принцип єдиної відповідальності (SRP)** стверджує, що **клас повинен мати лише одну причину для зміни**. Це означає, що клас повинен мати лише одну чітко визначену відповідальність.
>
> **Порушення цього принципу** відбувається, коли один клас бере на себе кілька різних завдань. Такі класи називають "божими об'єктами" (God Objects), і їх дуже важко підтримувати.
>
> **Приклад порушення:**
>
> Уявімо клас `User`, який робить усе: і зберігає дані, і валідує їх, і відправляє на сервер.
>
> ```java
> // ПОГАНО: Клас має 3 відповідальності
> public class User {
>     private String name;
>     private String email;
>
>     // Відповідальність №1: Зберігання даних
>     public String getName() { ... }
>     public void setName(String name) { ... }
>
>     // Відповідальність №2: Валідація
>     public boolean validateEmail() {
>         return email.contains("@");
>     }
>
>     // Відповідальність №3: Робота з базою даних
>     public void saveToDatabase() {
>         // ... логіка збереження ...
>     }
> }
> ```
>
> **Проблема:** Якщо зміниться логіка валідації, вам доведеться змінювати клас `User`. Якщо зміниться спосіб збереження в БД, ви знову змінюєте `User`. Це порушення SRP.
>
> **Як виправити:**
> Розділити відповідальності на три окремі класи:
>
> 1. `User` (простий POJO для зберігання даних).
> 2. `UserValidator` (для валідації).
> 3. `UserRepository` (для збереження в БД).
>
> Тепер кожен клас має лише одну причину для зміни.

#### **📌 Що перевіряють:**

*Розуміння того, що "відповідальність" — це про "причину для зміни". Здатність ідентифікувати класи, що роблять занадто багато.*

* * *

### **3 ❓ Питання:**

Що таке принцип відкритості/закритості (Open/Closed Principle - OCP)? Як його досягти?

> #### **💬 Відповідь:**
>
> **Принцип відкритості/закритості (OCP)** стверджує, що **програмні сутності (класи, модулі) повинні бути відкритими для розширення, але закритими для модифікації**.
>
> Це означає, що ви повинні мати можливість **додавати нову функціональність**, **не змінюючи** існуючий, вже протестований код.
>
> **Як його досягти:**
>
> Цей принцип досягається переважно за допомогою **абстракцій (інтерфейсів та абстрактних класів)** та **поліморфізму**.
>
> **Приклад:**
>
> Уявімо, нам потрібно розрахувати площу різних фігур.
>
> **Погано (порушення OCP):**
>
> ```java
> public class AreaCalculator {
>     // Щоразу при додаванні нової фігури, нам доведеться змінювати цей метод
>     public double calculate(Object shape) {
>         if (shape instanceof Circle) { /* ... */ }
>         if (shape instanceof Square) { /* ... */ }
>         // якщо додати Triangle, треба дописувати if
>     }
> }
> ```
>
> **Добре (дотримання OCP):**
>
> 1. Створюємо абстракцію:
>
>     ```java
>     interface Shape {
>         double getArea();
>     }
>     ```> 2.  Конкретні класи реалізують її:
>     ```java
>     class Circle implements Shape { ... }
>     class Square implements Shape { ... }
>     ```
>
> 3. Головний клас працює з абстракцією:
>
>     ```java
>     public class AreaCalculator {
>         public double calculate(Shape shape) {
>             return shape.getArea();
>         }
>     }
>     ```> Тепер, щоб додати `Triangle`, ми просто створюємо новий клас `class Triangle implements Shape`. Клас `AreaCalculator` **залишається незмінним** — він "закритий" для модифікації, але система "відкрита" для розширення новими фігурами.

#### **📌 Що перевіряють:**

*Розуміння того, як абстракція та поліморфізм є ключем до створення коду, який не потрібно постійно переписувати.*

* * *

### **4 ❓ Питання:**

Що означає принцип підстановки Лісков (Liskov Substitution Principle - LSP)? Наведіть приклад порушення.

> #### **💬 Відповідь:**
>
> **Принцип підстановки Лісков (LSP)** стверджує, що **об'єкти дочірнього класу повинні мати можливість підставлятися замість об'єктів батьківського класу без зміни коректності та очікуваної поведінки програми**.
>
> Простими словами, дочірній клас не повинен "ламати" контракт, встановлений батьківським класом. Він може розширювати функціональність, але не змінювати базову поведінку несподіваним чином.
>
> **Класичний приклад порушення — проблема "Прямокутник та Квадрат":**
>
> Математично квадрат є різновидом прямокутника. Спробуємо змоделювати це:
>
> ```java
> class Rectangle {
>     protected int width, height;
>     public void setWidth(int w) { this.width = w; }
>     public void setHeight(int h) { this.height = h; }
>     public int getArea() { return width * height; }
> }
>
> // Здається логічним...
> class Square extends Rectangle {
>     @Override
>     public void setWidth(int w) { // Щоб зберегти властивість квадрата
>         super.setWidth(w);
>         super.setHeight(w); // ...ми змушені змінювати і висоту!
>     }
>     @Override
>     public void setHeight(int h) { /* аналогічно */ }
> }
> ```
>
> **Чому це порушення LSP:**
>
> Код, який працює з `Rectangle`, очікує, що зміна ширини не вплине на висоту.
>
> ```java
> void resizeTest(Rectangle r) {
>     r.setWidth(5);
>     r.setHeight(4);
>     assert r.getArea() == 20; // Це твердження впаде, якщо передати Square!
> }
> ```
>
> Якщо передати об'єкт `Square`, `r.setHeight(4)` змінить і ширину на 4, і площа буде `16`, а не `20`. Ми не можемо безпечно підставити `Square` замість `Rectangle`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що наслідування в ООП — це про **поведінковий контракт**, а не лише про класифікацію з реального світу.*

* * *

### **5 ❓ Питання:**

Поясніть принцип розділення інтерфейсу (Interface Segregation Principle - ISP). Чому "товсті" інтерфейси — це погано?

> #### **💬 Відповідь:**
>
> **Принцип розділення інтерфейсу (ISP)** стверджує, що **краще мати багато невеликих, специфічних для клієнта інтерфейсів, ніж один великий універсальний інтерфейс**.
>
> **"Товстий" інтерфейс** — це інтерфейс, що містить багато методів, які обслуговують різні групи клієнтів. Проблема в тому, що клас, якому потрібна лише частина функціональності, змушений реалізовувати **всі методи**, навіть ті, які йому не потрібні.
>
> **Приклад порушення:**
>
> ```java
> // "Товстий" інтерфейс
> interface Worker {
>     void work();
>     void eat();
> }
>
> class HumanWorker implements Worker {
>     public void work() { /* ... */ }
>     public void eat() { /* ... */ }
> }
>
> class RobotWorker implements Worker {
>     public void work() { /* ... */ }
>     // Робот не їсть! Ми змушені залишити метод порожнім
>     // або кидати виняток, що є поганою практикою.
>     public void eat() { }
> }
> ```
>
> **Як виправити (дотримуючись ISP):**
>
> Розділити "товстий" інтерфейс на два менших, більш специфічних.
>
> ```java
> interface Workable { void work(); }
> interface Eatable { void eat(); }
>
> class HumanWorker implements Workable, Eatable { ... }
> class RobotWorker implements Workable { ... }
> ```
>
> Тепер кожен клас реалізує лише ту поведінку, яка йому властива. Код стає чистішим, гнучкішим і більш логічним.

#### **📌 Що перевіряють:**

*Здатність проектувати гнучкі та цілеспрямовані інтерфейси. Розуміння, чому реалізація непотрібних методів є ознакою поганого дизайну.*

* * *

### **6 ❓ Питання:**

Що таке принцип інверсії залежностей (Dependency Inversion Principle - DIP)? Поясніть на прикладі. ⭐️

> #### **💬 Відповідь:**
>
> **Принцип інверсії залежностей (DIP)** — це один з найважливіших принципів для побудови слабкозв'язаних систем. Він складається з двох частин:
>
> 1. **Високорівневі модулі не повинні залежати від низькорівневих. Обидва повинні залежати від абстракцій.**
> 2. **Абстракції не повинні залежати від деталей. Деталі повинні залежати від абстракцій.**
>
> Простими словами, замість того, щоб бізнес-логіка напряму залежала від конкретних інструментів (наприклад, конкретної бази даних), вона повинна залежати від **інтерфейсу (контракту)**, а вже конкретний інструмент буде реалізовувати цей інтерфейс.
>
> **Приклад порушення (без DIP):**
>
> ```java
> // Високорівневий модуль
> class ReportGenerator {
>     // Жорстка залежність від конкретного класу!
>     private MySqlDatabaseReader dbReader = new MySqlDatabaseReader();
>
>     public Report generate() {
>         List<String> data = dbReader.readData();
>         // ...
>     }
> }
>
> // Низькорівневий модуль
> class MySqlDatabaseReader {
>     public List<String> readData() { /* ... */ }
> }
> ```
>
> **Як виправити (з DIP):**
>
> 1. Створюємо абстракцію:
>    `interface DataReader { List<String> readData(); }`
>
> 2. Низькорівневий модуль реалізує її:
>    `class MySqlDatabaseReader implements DataReader { ... }`
>
> 3. Високорівневий модуль залежить від абстракції:
>
>    ```java
>    class ReportGenerator {
>        private final DataReader reader; // Залежність від інтерфейсу
>
>        public ReportGenerator(DataReader reader) { // Залежність передається ззовні
>            this.reader = reader;
>        }
>        // ...
>    }
>    ```> Тепер `ReportGenerator` не знає про `MySqlDatabaseReader`. Він працює з будь-яким `DataReader`. Ми **інвертували** напрямок залежності. Це основа для **Dependency Injection**.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Це ключовий принцип для розуміння сучасних фреймворків (як Spring) та побудови тестованого, гнучкого коду.*

* * *

### **7 ❓ Питання:**

Як чотири принципи ООП (інкапсуляція, наслідування, поліморфізм, абстракція) співвідносяться з принципами SOLID? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **SOLID — це, по суті, набір практичних рекомендацій, як правильно і ефективно використовувати чотири основні принципи ООП.**
>
> * **Інкапсуляція** є основою для **SRP**. Розділяючи відповідальності, ми краще інкапсулюємо логіку кожного класу.
>
> * **Абстракція та Поліморфізм** є ключовими інструментами для реалізації **OCP** та **DIP**. Ми створюємо абстракції (інтерфейси), щоб "закрити" існуючий код від змін, але "відкрити" його для розширення новими поліморфними реалізаціями.
>
> * **Наслідування** регулюється **LSP**. Принцип Лісков дає нам чіткі правила, як правильно будувати ієрархії наслідування, щоб поліморфізм не "зламався".
>
> * **Абстракція** також є основою для **ISP**. Замість однієї великої, "товстої" абстракції, ми створюємо кілька менших і більш сфокусованих.
>
> Можна сказати, що ООП дає вам **інструменти** (класи, наслідування, інтерфейси), а SOLID дає вам **інструкцію**, як цими інструментами користуватися, щоб не "побудувати кривий будинок".

#### **📌 Що перевіряють:**

*Системне мислення. Кандидат повинен бачити зв'язок між фундаментальними принципами мови та високорівневими принципами дизайну.*

* * *

### **8 ❓ Питання:**

Наведіть приклад, де дотримання одного принципу SOLID може допомогти дотриматися іншого. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Дотримання **Принципу інверсії залежностей (DIP)** часто автоматично допомагає дотриматися **Принципу відкритості/закритості (OCP)**.
>
> **Розглянемо приклад:**
>
> Припустимо, у нас є клас `NotificationService`, який відправляє сповіщення.
>
> **Без DIP:**
>
> ```java
> class NotificationService {
>     public void sendNotification(String user, String message) {
>         // Жорстка прив'язка до EmailSender
>         EmailSender sender = new EmailSender();
>         sender.sendEmail(user, message);
>     }
> }
> ```
>
> Цей клас порушує **OCP**. Якщо нам знадобиться додати відправку через SMS, нам доведеться **змінити** цей метод, додавши `if-else` або іншу логіку.
>
> **Застосуємо DIP:**
>
> 1. Створимо абстракцію:
>     `interface MessageSender { void send(String recipient, String message); }`
>
> 2. Зробимо `EmailSender` її реалізацією:
>     `class EmailSender implements MessageSender { ... }`
>
> 3. `NotificationService` тепер залежить від абстракції:
>
>     ```java
>     class NotificationService {
>         private final MessageSender sender;
>         public NotificationService(MessageSender sender) { this.sender = sender; }
>         public void sendNotification(String user, String message) {
>             sender.send(user, message);
>         }
>     }
>     ```> **Як це допомогло OCP:**
>
> Тепер клас `NotificationService` **закритий для модифікації**. Щоб додати відправку SMS, ми просто створюємо **новий клас** `class SmsSender implements MessageSender`. А потім при конфігурації передаємо в `NotificationService` потрібну реалізацію.
>
> Таким чином, дотримання DIP зробило наш код відповідним OCP.

#### **📌 Що перевіряють:**

*Розуміння синергії між різними принципами SOLID.*

* * *

### **9 ❓ Питання:**

Який принцип SOLID найчастіше порушується початківцями і чому? ⭐️

> #### **💬 Відповідь:**
>
> Найчастіше початківці порушують **Принцип єдиної відповідальності (Single Responsibility Principle - SRP)**.
>
> **Чому так відбувається:**
>
> 1. **Простота на перший погляд:** Найпростіший і найшвидший спосіб додати нову функціональність — це додати ще один метод до вже існуючого класу. Спочатку це здається зручним.
>
> 2. **Нерозуміння поняття "відповідальність":** Визначення "єдиної відповідальності" є досить суб'єктивним. Початківці можуть вважати "управління користувачем" однією відповідальністю, тоді як досвідчений розробник розіб'є її на валідацію, персистенцію, представлення даних тощо.
>
> 3. **Ефект "снігової кулі":** Порушення починається з малого. До класу `User` додають один "неправильний" метод. Потім ще один. З часом клас перетворюється на "божий об'єкт", який знає про все — від логіки бази даних до форматування для UI.
>
> 4. **Відсутність фокусу на дизайні:** Початківці часто фокусуються на тому, "щоб код просто запрацював", і не думають про майбутню підтримку, тестованість та гнучкість, які забезпечує SRP.
>
> Другим за частотою порушення, ймовірно, є **DIP** — пряма залежність від конкретних класів (`new MySqlConnection()`) замість залежності від інтерфейсів.

#### **📌 Що перевіряють:**

*Здатність аналізувати поширені помилки та розуміти їхні причини. Це також показує рівень досвіду кандидата.*

* * *

### **10 ❓ Питання:**

Як `private` конструктори допомагають дотримуватися принципів SOLID? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `private` конструктори, хоч і є простою мовною конструкцією, допомагають у реалізації кількох принципів SOLID.
>
> **1. Підтримка SRP та OCP через патерн Factory Method:**
> Приватний конструктор змушує створювати об'єкти через статичний фабричний метод. Ця фабрика може інкапсулювати складну логіку створення. Наприклад, повертати різні підкласи залежно від параметрів, що підтримує **OCP**. Крім того, логіка створення виноситься в окремий метод, що відповідає **SRP**.
>
> **2. Гарантування SRP через утилітарні класи:**
> Клас, що містить лише утилітарні `static` методи (наприклад, `MathUtils`), не повинен мати стану і його не можна інстанціювати. `private` конструктор запобігає цьому, забезпечуючи, що цей клас виконує **лише одну роль** — надає функції.
>
> **3. Підтримка DIP:**
> Фабричні методи, які часто використовуються разом з приватними конструкторами, зазвичай повертають **абстракцію (інтерфейс)**, а не конкретний клас. Це прямо підтримує **DIP**, оскільки клієнтський код залежить від інтерфейсу, а не від реалізації.
>
> Таким чином, `private` конструктор є важливим інструментом для контролю над створенням об'єктів, що, у свою чергу, допомагає будувати архітектуру відповідно до принципів SOLID.

#### **📌 Що перевіряють:**

*Здатність бачити, як низькорівневі мовні конструкції впливають на високорівневий дизайн коду.*

* * *

### **11 ❓ Питання:**

Який принцип SOLID найбільше стосується патерну "Адаптер"? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн "Адаптер" найбільше пов'язаний з **Принципом відкритості/закритості (Open/Closed Principle - OCP)** та **Принципом інверсії залежностей (DIP)**.
>
> **Як це працює:**
>
> 1. **Дотримання OCP:**
>     * Уявімо, наша система працює з інтерфейсом `MyTargetInterface`. Ми хочемо інтегрувати сторонню бібліотеку з несумісним інтерфейсом `Adaptee`.
>     * Замість того, щоб **змінювати** наш існуючий код, щоб він працював з `Adaptee`, ми створюємо новий клас `Adapter`, який реалізує `MyTargetInterface` і "обгортає" `Adaptee`.
>     * Таким чином, наша система залишається **закритою для модифікації**, але **відкритою для розширення** новою функціональністю через адаптер.
>
> 2. **Дотримання DIP:**
>     * Наш клієнтський код залежить від **абстракції** (`MyTargetInterface`).
>     * Адаптер дозволяє "підлаштувати" деталь реалізації (`Adaptee`) під цю абстракцію.
>     * Це зміцнює залежність від абстракцій, а не від деталей.
>
> Адаптер є класичним прикладом того, як можна інтегрувати старий або несумісний код у нову систему, не порушуючи при цьому принципи хорошого дизайну.

#### **📌 Що перевіряють:**

*Знання патернів проектування та їх зв'язку з принципами SOLID.*

* * *

### **12 ❓ Питання:**

Поясніть, як можна порушити принцип підстановки Лісков, неправильно перевизначивши `equals()` в ієрархії наслідування. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний приклад, де порушується властивість **симетричності**.
>
> Розглянемо ієрархію `Point -> ColorPoint`.
>
> ```java
> class Point {
>     private int x, y;
>     @Override
>     public boolean equals(Object o) {
>         if (!(o instanceof Point)) return false;
>         Point p = (Point) o;
>         return p.x == x && p.y == y;
>     }
> }
> class ColorPoint extends Point {
>     private Color color;
>     @Override
>     public boolean equals(Object o) {
>         if (!(o instanceof ColorPoint)) return false;
>         // Порівнюємо і координати, і колір
>         return super.equals(o) && ((ColorPoint)o).color == color;
>     }
> }
> ```
>
> **Проблема:**
>
> ```java
> Point p = new Point(1, 2);
> ColorPoint cp = new ColorPoint(1, 2, Color.RED);
>
> p.equals(cp); // -> true
> // `cp instanceof Point` поверне `true`, і будуть порівняні лише x та y.
>
> cp.equals(p); // -> false
> // `p instanceof ColorPoint` поверне `false`, і метод одразу поверне `false`.
> ```
>
> Ми отримали `p.equals(cp) != cp.equals(p)`, що є прямим **порушенням симетричності** і, як наслідок, **порушенням LSP**. Ми не можемо безпечно підставити `ColorPoint` туди, де очікується `Point`, оскільки поведінка `equals` стає непередбачуваною.
>
> **Як вирішити:**
> Найчастіше це вирішується або тим, що `equals` робиться `final` у батька, або перевіркою `getClass() == o.getClass()` замість `instanceof`, або взагалі використанням **композиції замість наслідування**.

#### **📌 Що перевіряють:**

*Експертне розуміння тонкощів реалізації `equals` та того, як воно перетинається з LSP.*

* * *

### **13 ❓ Питання:**

Назвіть антипатерни, які є протилежностями до принципів SOLID. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Для кожного принципу SOLID можна виділити відповідний антипатерн.
>
> * **Протилежність SRP:**
>   * **Божий об'єкт (God Object) / Кровоточивий клас (Blob):** Один клас, який знає занадто багато і робить занадто багато. Він концентрує в собі величезну кількість непов'язаних відповідальностей.
>
> * **Протилежність OCP:**
>   * **Модифікація замість розширення:** Постійна необхідність змінювати існуючий код для додавання нової функціональності, що призводить до каскадних змін та регресійних помилок. Часто проявляється у великих `if-else` або `switch` конструкціях.
>
> * **Протилежність LSP:**
>   * **Некоректна ієрархія наслідування:** Дочірній клас "ламає" контракт батьківського (наприклад, кидає `UnsupportedOperationException` в перевизначеному методі).
>
> * **Протилежність ISP:**
>   * **"Товстий" інтерфейс (Fat Interface):** Інтерфейс з великою кількістю методів, який змушує клієнтів реалізовувати функціональність, яка їм не потрібна.
>
> * **Протилежність DIP:**
>   * **Жорстке кодування залежностей (Hardcoding dependencies):** Високорівневі класи напряму створюють екземпляри низькорівневих (`new MySqlRepository()`), що призводить до тісної зв'язності та поганої тестованості.
>
> Розуміння цих антипатернів допомагає краще розпізнавати проблеми в дизайні коду.

#### **📌 Що перевіряють:**

*Досвід та вміння бачити "кодові запахи". Здатність ідентифікувати не тільки, "як треба", але й "як не треба".*

* * *

### **14 ❓ Питання:**

Чому кажуть, що дотримання принципів SOLID робить код більш тестованим? Поясніть на прикладі одного з принципів. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Дотримання SOLID напряму веде до покращення **тестованості**, оскільки створює маленькі, ізольовані та слабозв'язані компоненти.
>
> Розглянемо **Принцип інверсії залежностей (DIP)**.
>
> **Без DIP (погана тестованість):**
>
> ```java
> class UserService {
>     private final UserRepository repository = new DatabaseUserRepository(); // Жорстка залежність
>
>     public User findUser(long id) {
>         // ...
>         return repository.findById(id);
>     }
> }
> ```
>
> * **Проблема:** Щоб протестувати `UserService`, вам потрібна **реальна база даних**. Ви не можете ізолювати сервіс від репозиторію. Тест буде повільним, складним у налаштуванні та нестабільним (залежатиме від стану БД).
>
> **З DIP (хороша тестованість):**
>
> ```java
> interface UserRepository { User findById(long id); }
> class UserService {
>     private final UserRepository repository; // Залежність від інтерфейсу
>     public UserService(UserRepository repository) { this.repository = repository; }
>     // ...
> }
> ```
>
> **Як це допомагає в юніт-тесті:**
> Тепер ми можемо легко **"підмінити" реальну залежність на mock-об'єкт**, використовуючи фреймворки, як Mockito.
>
> ```java
> @Test
> public void testUserService() {
>     // 1. Створюємо mock (заглушку)
>     UserRepository mockRepo = Mockito.mock(UserRepository.class);
>     // 2. Налаштовуємо поведінку mock'а
>     Mockito.when(mockRepo.findById(1L)).thenReturn(new User("Test"));
>
>     // 3. Впроваджуємо mock в наш сервіс
>     UserService userService = new UserService(mockRepo);
>
>     // 4. Тестуємо логіку сервісу в повній ізоляції
>     User user = userService.findUser(1L);
>     assertEquals("Test", user.getName());
> }
> ```
>
> Завдяки DIP, ми можемо протестувати `UserService`, не маючи жодної бази даних, що робить тест швидким, надійним та ізольованим. Аналогічні переваги дають й інші принципи SOLID.

#### **📌 Що перевіряють:**

*Розуміння практичної важливості принципів SOLID. Здатність пояснити, як архітектура впливає на такий ключовий аспект розробки, як тестування.*

* * *

### **15 ❓ Питання:**

Як `default` методи в інтерфейсах (Java 8+) можуть допомогти дотримуватися, а як — потенційно порушити принципи SOLID? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> `Default` методи — це потужний, але двосічний інструмент у контексті SOLID.
>
> **Як вони допомагають:**
>
> * **OCP (Принцип відкритості/закритості):** Це їхнє головне призначення. Вони дозволяють **розширювати** інтерфейси новими методами, **не змінюючи** існуючі класи-реалізатори. Це ідеальна реалізація OCP для API.
>
> **Як вони можуть порушити:**
>
> * **SRP (Принцип єдиної відповідальності):** Існує спокуса додати занадто багато `default` методів в один інтерфейс, перетворюючи його з чистого контракту на "смітник" для утилітарних функцій. Інтерфейс починає мати кілька відповідальностей.
>
> * **ISP (Принцип розділення інтерфейсу):** Якщо `default` метод додає значну логіку, яка потрібна не всім реалізаторам, це може зробити інтерфейс "товстішим", ніж потрібно. Класи, яким не потрібна ця `default`-реалізація, все одно її "отримують".
>
> * **LSP (Принцип підстановки Лісков):** Якщо `default` метод покладається на певні неявні інваріанти або викликає інші методи інтерфейсу, то клас-реалізатор, який не очікує такої поведінки, може "зламатися" або поводитися непередбачувано. Це може порушити контракт, який клієнти очікують від реалізацій цього інтерфейсу.
>
> **Висновок:** `Default` методи є потужним інструментом для еволюції API (OCP), але їх слід використовувати обережно, щоб не перетворити чисті інтерфейси на заплутані класи з кількома відповідальностями, що порушує інші принципи.

#### **📌 Що перевіряють:**

*Здатність до критичного аналізу нових можливостей мови. Кандидат повинен розуміти не тільки переваги, а й потенційні ризики та антипатерни, пов'язані з новими фічами.*
