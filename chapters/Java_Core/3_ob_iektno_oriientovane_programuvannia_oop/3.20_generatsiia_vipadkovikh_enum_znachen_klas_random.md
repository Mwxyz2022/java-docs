---
layout: default
title: "Генерація випадкових enum значень, клас Random"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 20
---

# Генерація випадкових enum значень, клас Random

* короткий опис: Використання класу `java.util.Random` для генерації випадкових чисел у поєднанні з методом `enum.values()` для отримання випадкового елемента з перерахування.

* поняття: `java.util.Random`, `nextInt()`, `enum.values()`, `ThreadLocalRandom`.

* приклади: `Color[] colors = Color.values(); int index = random.nextInt(colors.length); Color randomColor = colors[index];`.

* ключові моменти: `Random` для псевдовипадкових чисел. `values()` повертає масив констант `enum`. `ThreadLocalRandom` для потокобезпечної генерації.

* що важливо знати на співбесіді: Як отримати випадкове значення `enum`, відмінності між `Random` і `ThreadLocalRandom`.

### **✅ Офіційне визначення:**

Клас **`java.util.Random`** дозволяє генерувати випадкові числа, boolean, double тощо. У поєднанні з **`Enum.values()`**
можна обрати випадковий елемент із перерахування (**`enum`**).

### **🧠 Простими словами:**

**`Random`** — це як кубик-генератор. Ти кидаєш його і отримуєш випадкове число. Якщо у тебе є **`enum`
**, ти просто береш випадковий індекс зі списку значень **`enum`** — і отримуєш випадковий варіант.

---

## **Основне**

|        Метод         |       Повертає       |           Приклад            |
|:--------------------:|:--------------------:|:----------------------------:|
|   **`nextInt()`**    | випадкове ціле (int) |     **`rand.nextInt()`**     |
| **`nextInt(bound)`** | від 0 до bound \- 1  | **`rand.nextInt(5)`** -> 0..4 |
|  **`nextDouble()`**  | число від 0.0 до 1.0 |   **`rand.nextDouble()`**    |
| **`nextBoolean()`**  |    true або false    |   **`rand.nextBoolean()`**   |

---

## **Пояснення на пальцях (💡)**

Щоб вибрати випадковий елемент enum, потрібно:

1. Отримати всі значення enum через **`.values()`**.
2. Згенерувати випадковий індекс у межах **`0 .. length - 1`**.
3. Взяти елемент із масиву за цим індексом.

---

## **Корисні приклади (🧪)**

🔹 **Приклад enum:**

```java
public enum Color {
    RED, GREEN, BLUE, YELLOW, ORANGE
}
```

🔹 **Отримання випадкового значення:**

```java
import java.util.Random;

public class RandomColorPicker {
    public static void main(String[] args) {
        Random random = new Random();
        Color[] colors = Color.values();

        int index = random.nextInt(colors.length);
        Color randomColor = colors[index];

        System.out.println("Random color: " + randomColor);
    }
}
```

🔹 **Інкапсуляція в метод:**

```java
public static Color getRandomColor() {
    return Color.values()[new Random().nextInt(Color.values().length)];
}
```

---

## **Порівняння (⚖️)**

|         Підхід          |     Гнучкість      |                  Коментар                  |
|:-----------------------:|:------------------:|:------------------------------------------:|
| **`Random.nextInt()`**  |  ✅ Універсальний   |          Працює з будь-якими enum          |
| **Власний enum-метод**  |      ✅ Зручно      | Можна додати метод **`Color.getRandom()`** |
| **`ThreadLocalRandom`** | ✅ Більш ефективний |        Для багатопотокових додатків        |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧠

**`Random + values() + індекс = випадкове enum значення`**

📦

**`Color.values()[Random.nextInt(...)]`**
---

## **🟩 Підсумок:**

Щоб отримати випадкове значення enum, використай **`.values()`**, згенеруй випадковий індекс через **`Random`**, і
звернись до масиву. Це проста, ефективна практика, що часто використовується в іграх, симуляціях, генераторах даних.

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація java.util.Random**](https://docs.oracle.com/javase/8/docs/api/java/util/Random.html)
* [**StackOverflow — How to get a random enum value**](https://stackoverflow.com/questions/1972392/how-to-get-a-random-value-from-an-enum)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Як отримати випадкове значення з `enum` в Java? Опишіть покроковий алгоритм.

> #### **💬 Відповідь:**
>
> Щоб отримати випадкову `enum`-константу, потрібно поєднати можливості `enum`-типів та класу для генерації випадкових чисел, наприклад, `java.util.Random`.
>
> **Покроковий алгоритм:**
>
> 1. **Отримати всі можливі значення `enum`:** Використовуйте статичний метод `values()`, який повертає масив, що містить усі константи `enum` у порядку їх оголошення.
>
> 2. **Створити екземпляр генератора випадкових чисел:** `Random random = new Random();`.
>
> 3. **Згенерувати випадковий індекс:** Використовуйте метод `random.nextInt(int bound)`, де `bound` — це кількість елементів у масиві (`values().length`). Цей метод поверне випадкове ціле число в діапазоні від `0` (включно) до `bound` (виключно).
>
> 4. **Отримати елемент за індексом:** Використовуйте згенерований індекс, щоб отримати відповідний елемент з масиву `values()`.
>
> **Приклад коду:**
>
> ```java
> public enum Status {
>     NEW, IN_PROGRESS, DONE, FAILED
> }
>
> Random random = new Random();
> Status[] allStatuses = Status.values(); // Отримуємо масив [NEW, IN_PROGRESS, DONE, FAILED]
> int randomIndex = random.nextInt(allStatuses.length); // Генеруємо індекс від 0 до 3
> Status randomStatus = allStatuses[randomIndex]; // Отримуємо випадковий статус
>
> System.out.println("Випадковий статус: " + randomStatus);
> ```

#### **📌 Що перевіряють:**

*Знання базового алгоритму та основних методів `enum` (`values()`) та `Random` (`nextInt()`). Це основне питання на дану тему.*

* * *

### **2 ❓ Питання:**

Як інкапсулювати логіку отримання випадкового `enum`-значення всередині самого `enum`? Напишіть приклад.

> #### **💬 Відповідь:**
>
> Це дуже гарна практика, що відповідає принципам ООП. Ми можемо додати **статичний фабричний метод** безпосередньо в `enum`, який буде приховувати деталі реалізації.
>
> **Як це зробити:**
>
> 1. Створіть `private static final` екземпляр `Random` всередині `enum`, щоб не створювати його кожного разу.
> 2. Створіть `private static final` масив значень `enum` (результат `values()`), щоб уникнути постійних викликів цього методу.
> 3. Створіть `public static` метод (наприклад, `random()`), який буде виконувати логіку вибору і повертати випадкову константу.
>
> **Приклад реалізації:**
>
> ```java
> import java.util.Random;
>
> public enum Direction {
>     NORTH, EAST, SOUTH, WEST;
>
>     private static final Random PRNG = new Random();
>     private static final Direction[] VALUES = values();
>
>     public static Direction random() {
>         int index = PRNG.nextInt(VALUES.length);
>         return VALUES[index];
>     }
> }
> ```
>
> **Як використовувати:**
> `Direction randomDirection = Direction.random();`
>
> Це робить код значно чистішим та більш семантичним, а також ефективнішим, оскільки `Random` і масив створюються лише один раз.

#### **📌 Що перевіряють:**

*Навички дизайну. Кандидат повинен вміти інкапсулювати логіку у відповідний клас, а не залишати її в клієнтському коді.*

* * *

### **3 ❓ Питання:**

Які є альтернативи `java.util.Random` для генерації випадкових чисел? У чому їхні переваги та недоліки?

> #### **💬 Відповідь:**
>
> Хоча `java.util.Random` є класичним варіантом, у Java існують більш сучасні та ефективні альтернативи.
>
> **1. `java.util.concurrent.ThreadLocalRandom` (з Java 7):**
>
> * **Переваги:** Це **найкращий вибір для багатопотокових додатків**. Кожен потік отримує свій власний генератор, що усуває **конкуренцію (contention)** за один спільний екземпляр `Random`. Це значно підвищує продуктивність у висококонкурентних сценаріях. Також він генерується без `seed`, що робить його менш передбачуваним.
> * **Недоліки:** Не можна створити екземпляр напряму; доступ отримується через `ThreadLocalRandom.current()`.
> * **Приклад:** `ThreadLocalRandom.current().nextInt(bound);`
>
> **2. `java.security.SecureRandom`:**
>
> * **Переваги:** Генерує **криптографічно стійкі** випадкові числа. Він збирає ентропію з операційної системи, що робить його результати значно менш передбачуваними, ніж у `Random` (який є псевдовипадковим).
> * **Недоліки:** **Значно повільніший** за `Random` або `ThreadLocalRandom`.
> * **Використання:** Необхідний для завдань, пов'язаних з безпекою: генерація ключів, солей для паролів, токенів сесій.
>
> **3. Інтерфейс `RandomGenerator` (з Java 17):**
>
> * Це новий інтерфейс, який уніфікує API для різних генераторів випадкових чисел. Надає доступ до різних алгоритмів (наприклад, `Xoshiro256PlusPlus`) через фабричні методи. Це більш гнучкий та сучасний підхід.
>
> **Висновок:**
>
> * Для простих однопотокових завдань — `Random`.
> * Для багатопотокових завдань — **`ThreadLocalRandom`**.
> * Для безпеки — **`SecureRandom`**.

#### **📌 Що перевіряють:**

*Знання сучасних API для генерації випадкових чисел та розуміння компромісів між продуктивністю, потокобезпечністю та криптографічною стійкістю.*

* * *

### **4 ❓ Питання:**

Чому клас `java.util.Random` вважається потокобезпечним, але його не рекомендують використовувати в багатопотоковому середовищі?

> #### **💬 Відповідь:**
>
> `java.util.Random` дійсно є **потокобезпечним (thread-safe)**. Його внутрішній стан (змінна `seed`) захищений від стану гонитви за допомогою **атомарних операцій (CAS - Compare-And-Swap)**.
>
> **Проблема:**
>
> Його не рекомендують для висококонкурентних додатків через **"вузьке місце" (bottleneck) продуктивності**.
>
> * Всі потоки, які використовують один і той самий екземпляр `Random`, **змушені конкурувати** за доступ до цього єдиного атомарного `seed`.
> * Коли багато потоків одночасно намагаються отримати наступне випадкове число, вони шикуються в чергу, щоб оновити цей `seed`. Це призводить до серйозного **падіння продуктивності**, оскільки потоки блокують один одного.
>
> **Рішення:**
>
> Саме для вирішення цієї проблеми в Java 7 було введено клас **`ThreadLocalRandom`**. Він надає кожному потоку **свій власний, ізольований генератор**, усуваючи будь-яку конкуренцію. Тому для багатопотокових систем `ThreadLocalRandom.current()` є значно кращим вибором.

#### **📌 Що перевіряють:**

*Глибоке розуміння багатопотоковості. Кандидат повинен розрізняти "потокобезпечність" та "високу продуктивність у багатопотоковому середовищі".*

* * *

### **5 ❓ Питання:**

Як отримати випадковий елемент `enum` з виключенням деяких значень? ⭐️

> #### **💬 Відповідь:**
>
> Стандартний підхід тут не підійде. Потрібно спочатку відфільтрувати значення, які ми хочемо виключити, а потім обирати випадковий елемент з решти.
>
> **Підхід з `List`:**
>
> 1. Отримати всі значення `enum` через `values()`.
> 2. Створити новий `List` і додати в нього лише ті значення, які не входять до списку виключень.
> 3. Згенерувати випадковий індекс в межах розміру цього нового списку.
>
> **Приклад коду (з використанням Java 8 Stream API):**
>
> ```java
> public enum Level { BEGINNER, INTERMEDIATE, ADVANCED, EXPERT }
>
> public static Level getRandomLevelExcluding(Set<Level> excluded) {
>     List<Level> availableLevels = Arrays.stream(Level.values())
>             .filter(level -> !excluded.contains(level))
>             .collect(Collectors.toList());
>
>     if (availableLevels.isEmpty()) {
>         return null; // або кинути виняток
>     }
>
>     int randomIndex = new Random().nextInt(availableLevels.size());
>     return availableLevels.get(randomIndex);
> }
>
> // Використання:
> Level random = getRandomLevelExcluding(Set.of(Level.EXPERT));
> ```
>
> Цей підхід є гнучким, але може бути не дуже ефективним, якщо ця операція виконується дуже часто, оскільки кожен раз створюється нова колекція.

#### **📌 Що перевіряють:**

*Здатність вирішувати нестандартні задачі та використовувати Java 8 Stream API для маніпуляцій з колекціями.*

* * *

### **6 ❓ Питання:**

Чому `enum.values()` не рекомендується викликати в "гарячих" циклах? ⭐️

> #### **💬 Відповідь:**
>
> `enum.values()` не рекомендується для частих викликів, тому що цей метод **кожен раз створює і повертає новий масив**.
>
> Хоча `enum`-константи є синглтонами, метод `values()` для безпеки **клонує** внутрішній масив констант перед тим, як його повернути. Це робиться для того, щоб клієнтський код не міг випадково змінити цей внутрішній масив (хоча це було б можливо лише через рефлексію).
>
> **Наслідки:**
>
> * **Створення об'єктів:** Постійні виклики `values()` в циклі призводять до створення безлічі короткоживучих масивів.
> * **Навантаження на Garbage Collector:** Ці масиви потрібно буде постійно прибирати збирачем сміття.
>
> **Як правильно:**
>
> Якщо вам потрібно часто звертатися до всіх значень `enum`, **кешуйте результат виклику `values()`** у статичному полі.
>
> ```java
> public enum Direction {
>     NORTH, EAST, SOUTH, WEST;
>
>     public static final Direction[] ALL_DIRECTIONS = values();
> }
>
> // Тепер в циклі можна використовувати ALL_DIRECTIONS
> for (Direction dir : Direction.ALL_DIRECTIONS) { /* ... */ }
> ```
>
> Цей підхід створює масив лише один раз при завантаженні класу.

#### **📌 Що перевіряють:**

*Розуміння того, як працюють методи "під капотом" та здатність писати оптимізований код, уникаючи непотрібного створення об'єктів.*

* * *

### **7 ❓ Питання:**

Уявіть, що ваші `enum`-константи мають різну "вагу" або ймовірність випадіння. Як реалізувати випадковий вибір з урахуванням ваги? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична задача на **зважений випадковий вибір (weighted random selection)**. Простий вибір за індексом тут не підійде.
>
> **Алгоритм:**
>
> 1. Додайте до `enum` поле `weight`.
> 2. Обчисліть **загальну суму** всіх ваг.
> 3. Згенеруйте випадкове число в діапазоні від 0 до (загальна_сума - 1).
> 4. Ітеруйте по `enum`-константах, послідовно віднімаючи їхню вагу від випадкового числа. Та константа, на якій число стане меншим або рівним нулю, і є обраною.
>
> **Приклад коду:**
>
> ```java
> public enum LootType {
>     COMMON(70), // 70% шанс
>     RARE(25),   // 25% шанс
>     EPIC(5);    // 5% шанс
>
>     private final int weight;
>     private static final int TOTAL_WEIGHT = 100;
>     private static final Random RND = new Random();
>
>     LootType(int weight) { this.weight = weight; }
>
>     public static LootType getRandom() {
>         int randomNum = RND.nextInt(TOTAL_WEIGHT);
>
>         for (LootType type : values()) {
>             randomNum -= type.weight;
>             if (randomNum < 0) {
>                 return type;
>             }
>         }
>         return null; // Не повинно статися
>     }
> }
> ```
>
> Цей метод забезпечить, що `COMMON` буде випадати набагато частіше, ніж `EPIC`, відповідно до їхніх ваг.

#### **📌 Що перевіряють:**

*Алгоритмічне мислення, здатність реалізувати нетривіальну логіку, яка виходить за рамки простих бібліотечних викликів.*

* * *

### **8 ❓ Питання:**

Чим відрізняється створення `new Random()` від `new Random(long seed)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає в **передбачуваності** послідовності випадкових чисел.
>
> * **`new Random()` (Конструктор без аргументів):**
>   * При створенні `Random` таким чином, його початкове значення (**seed**) обчислюється на основі **поточного часу** (`System.nanoTime()`) та унікального системного значення.
>   * Оскільки початкове значення кожного разу різне, послідовність згенерованих чисел також буде **різною при кожному запуску** програми.
>   * Це підходить для більшості випадків, коли вам потрібні просто непередбачувані числа.
>
> * **`new Random(long seed)` (Конструктор з початковим значенням):**
>   * Ви **явно задаєте** початкове значення.
>   * **Головна властивість:** якщо створити два екземпляри `Random` з **однаковим `seed`**, вони будуть генерувати **абсолютно ідентичну послідовність** "випадкових" чисел.
>   * `Random` є **псевдовипадковим** генератором. Алгоритм, який він використовує, є детермінованим.
>
> **Коли використовувати `seed`:**
>
> Це корисно для **тестування** (коли вам потрібні відтворювані результати) або для **процедурної генерації** (наприклад, в іграх для створення однакових світів за тим самим "сідом").

#### **📌 Що перевіряють:**

*Розуміння того, що `Random` — це псевдовипадковий генератор, та знання, як керувати його відтворюваністю за допомогою `seed`.*

* * *

### **9 ❓ Питання:**

Який клас слід використовувати, якщо вам потрібен випадковий вибір елемента з `List`? ⭐️

> #### **💬 Відповідь:**
>
> Для випадкового вибору елемента з `List` так само використовується генератор випадкових чисел для отримання індексу.
>
> **Загальний підхід:**
>
> ```java
> public <T> T getRandomElement(List<T> list) {
>     if (list == null || list.isEmpty()) {
>         return null;
>     }
>     Random random = new Random();
>     int randomIndex = random.nextInt(list.size());
>     return list.get(randomIndex);
> }
> ```
>
> Якщо потрібно обрати кілька унікальних елементів, краще "перетасувати" список і взяти перші N елементів.
>
> **Як зробити це ще простіше:**
>
> Іноді для таких задач простіше отримати масив з колекції і працювати з ним.
>
> ```java
> List<String> myList = ...;
> String[] myArray = myList.toArray(new String[0]);
>
> Random rnd = new Random();
> String randomElement = myArray[rnd.nextInt(myArray.length)];
> ```
>
> Логіка залишається тією ж: **генеруємо індекс, отримуємо елемент**. Для `enum` це спрощено тим, що метод `.values()` одразу дає нам масив.

#### **📌 Що перевіряють:**

*Здатність застосувати той самий базовий алгоритм до іншої структури даних (`List`).*

* * *

### **10 ❓ Питання:**

Що виведе цей код і чому?

```java
public static void main(String[] args) {
    System.out.println(Status.valueOf("new"));
}
```

(Припускаючи, що `enum Status { NEW, IN_PROGRESS, DONE }`) ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код викине виняток **`IllegalArgumentException`**.
>
> **Пояснення:**
>
> Метод **`Enum.valueOf(Class<T> enumType, String name)`** (який викликається "під капотом" `Status.valueOf("new")`) є **чутливим до регістру**.
>
> * Він очікує, що переданий рядок `name` **точно** відповідає назві однієї з `enum`-констант.
> * У нашому `enum` константа оголошена як `NEW` (у верхньому регістрі).
> * Спроба знайти константу за рядком `"new"` (у нижньому регістрі) зазнає невдачі.
>
> Оскільки константи з такою назвою не існує, метод викидає `IllegalArgumentException` з повідомленням "No enum constant Status.new".
>
> **Як це виправити:**
>
> Щоб зробити пошук нечутливим до регістру, потрібно написати власну допоміжну функцію.
>
> ```java
> public static Status fromString(String text) {
>     for (Status s : Status.values()) {
>         if (s.name().equalsIgnoreCase(text)) {
>             return s;
>         }
>     }
>     throw new IllegalArgumentException("Немає статусу для: " + text);
> }
> ```

#### **📌 Що перевіряють:**

*Знання тонкощів роботи стандартних методів `enum` та вміння писати більш надійний код для парсингу.*

* * *

### **11 ❓ Питання:**

У чому різниця між `random.nextInt()` та `random.nextInt(100)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає в **діапазоні** згенерованих значень.
>
> * **`random.nextInt()` (без аргументів):**
>   * Повертає псевдовипадкове ціле число в **усьому діапазоні `int`**.
>   * Результат може бути як **додатним**, так і **від'ємним**: від `Integer.MIN_VALUE` до `Integer.MAX_VALUE`.
>
> * **`random.nextInt(int bound)` (з аргументом):**
>   * Повертає псевдовипадкове ціле число в діапазоні від **`0` (включно)** до **`bound` (виключно)**.
>   * `bound` повинен бути додатним.
>   * **Приклад:** `random.nextInt(100)` поверне число від `0` до `99`.
>
> Ця різниця є дуже важливою. Перша форма використовується рідко. Друга форма (`nextInt(bound)`) є надзвичайно поширеною, оскільки саме вона потрібна для генерації випадкових індексів для масивів або списків.
>
> Якщо вам потрібне випадкове число у довільному діапазоні (наприклад, від 10 до 20), можна використати таку формулу:
> `random.nextInt(max - min + 1) + min;`

#### **📌 Що перевіряють:**

*Точне знання API класу `Random`, зокрема відмінностей у перевантажених методах.*

* * *

### **12 ❓ Питання:**

Чому цей код є поганою ідеєю, навіть якщо він працює?
`return MyEnum.values()[new Random().nextInt(MyEnum.values().length)];` ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код, хоч і коректний, є **дуже неефективним** і є поганою ідеєю з кількох причин:
>
> 1. **Повторний виклик `values()`:** Метод `MyEnum.values()` викликається **двічі** на кожен виклик. Як ми знаємо, `values()` **кожен раз створює новий масив**. Це подвійне марнування ресурсів.
>
> 2. **Постійне створення `new Random()`:** Екземпляр `Random` створюється кожного разу, коли викликається цей рядок коду. `Random` є відносно "важким" об'єктом, і немає жодної потреби створювати його знову і знову.
>
> **Наслідки:**
>
> * **Створення зайвих об'єктів:** Призводить до зайвого навантаження на пам'ять.
> * **Навантаження на Garbage Collector:** Всі ці короткоживучі об'єкти (`Random` та два масиви) доведеться прибирати.
>
> **Набагато кращим підходом** було б винести `values()` та `Random` у `static final` поля, як було показано в питанні №2, щоб вони ініціалізувалися лише один раз.

#### **📌 Що перевіряють:**

*Здатність бачити проблеми з продуктивністю у, на перший погляд, простому коді. Розуміння вартості створення об'єктів.*

* * *

### **13 ❓ Питання:**

Які потокобезпечні альтернативи `java.util.Random` ви знаєте, і коли їх використовувати? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Для роботи у багатопотоковому середовищі існують кращі альтернативи, ніж спільний екземпляр `java.util.Random`.
>
> **1. `java.util.concurrent.ThreadLocalRandom`:**
>
> * **Найкращий вибір** для більшості багатопотокових завдань, де потрібна висока продуктивність.
> * **Як працює:** Надає кожному потоку **свій власний, ізольований генератор**. Це повністю усуває конкуренцію за спільний ресурс.
> * **Як використовувати:** Доступ отримується через статичний метод `ThreadLocalRandom.current()`.
>     `int r = ThreadLocalRandom.current().nextInt(100);`
>
> **2. `java.security.SecureRandom`:**
>
> * **Також потокобезпечний**, але значно **повільніший**.
> * Його основна мета — **криптографічна стійкість**, а не швидкість.
> * **Коли використовувати:** Тільки тоді, коли вам потрібні випадкові числа, які неможливо передбачити, наприклад, для генерації ключів шифрування, токенів безпеки, солей для паролів тощо. Для звичайних симуляцій або ігор він є надлишковим.
>
> **Висновок:** Для продуктивності — `ThreadLocalRandom`. Для безпеки — `SecureRandom`. Спільний `new Random()` — зазвичай уникати в багатопотоковому коді.

#### **📌 Що перевіряють:**

*Експертні знання бібліотек для роботи з багатопотоковістю та безпекою. Кандидат повинен вміти обирати правильний інструмент для завдання.*

* * *

### **14 ❓ Питання:**

Поясніть, що таке `seed` в контексті `Random`, і як він дозволяє отримати відтворювану послідовність. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **`seed` (початкове значення)** — це число, яке використовується для ініціалізації внутрішнього стану псевдовипадкового генератора чисел (PRNG), яким є `java.util.Random`.
>
> **Псевдовипадковість:**
> Генератор насправді не створює "справжніх" випадкових чисел. Він використовує **детермінований математичний алгоритм**, який на основі поточного стану генерує наступне число, а потім оновлює свій стан. `seed` — це початкова точка для цього алгоритму.
>
> **Як він дозволяє отримати відтворювану послідовність:**
>
> * Якщо ви ініціалізуєте два екземпляри `Random` **однаковим `seed`**, їхні внутрішні алгоритми почнуть працювати з однакового початкового стану.
> * Оскільки алгоритм детермінований, послідовні виклики `nextInt()`, `nextDouble()` тощо будуть генерувати **абсолютно ідентичну послідовність** "випадкових" чисел для обох екземплярів.
>
> ```java
> Random r1 = new Random(42L); // Однаковий seed
> Random r2 = new Random(42L);
>
> System.out.println(r1.nextInt()); // Наприклад, -1157793070
> System.out.println(r2.nextInt()); // Точно таке ж число
>
> System.out.println(r1.nextInt()); // Наприклад, 1913984760
> System.out.println(r2.nextInt()); // Знову те ж саме
> ```
>
> **Навіщо це потрібно:**
>
> * **Відтворюваність у тестах:** Дозволяє писати юніт-тести для коду, що залежить від випадковості, отримуючи кожного разу однаковий результат.
> * **Процедурна генерація:** В іграх для генерації однакових світів/рівнів за однаковим `seed`.
> * **Симуляції:** Для відтворення симуляцій з тими ж вхідними даними.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння того, що таке псевдовипадковість та як керувати нею за допомогою `seed`.*

* * *

### **15 ❓ Питання:**

Як, використовуючи Stream API (Java 8), можна отримати 5 унікальних випадкових значень з `enum`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання елегантно вирішується за допомогою `Stream API` та `Collections.shuffle`.
>
> **Ідея:**
>
> 1. Взяти всі значення `enum` як список.
> 2. Перемішати цей список у випадковому порядку.
> 3. Взяти перші 5 елементів з перемішаного списку.
>
> **Реалізація:**
>
> ```java
> public enum CardSuit { DIAMONDS, CLUBS, HEARTS, SPADES, ... } // Припустимо, тут багато значень
>
> public static List<CardSuit> getFiveRandomSuits() {
>     // 1. Отримуємо всі значення enum як список
>     List<CardSuit> allSuits = new ArrayList<>(Arrays.asList(CardSuit.values()));
>
>     // 2. Перемішуємо список у випадковому порядку
>     Collections.shuffle(allSuits);
>
>     // 3. Використовуємо Stream API, щоб взяти перші 5 елементів
>     return allSuits.stream()
>             .limit(5)
>             .collect(Collectors.toList());
> }
> ```
>
> **Альтернативний підхід з `random.ints`:**
>
> ```java
> public static List<CardSuit> getFiveRandomSuits2() {
>     CardSuit[] allSuits = CardSuit.values();
>     return new Random().ints(0, allSuits.length) // Генеруємо потік випадкових індексів
>             .distinct() // Робимо їх унікальними
>             .limit(5) // Беремо 5 штук
>             .mapToObj(i -> allSuits[i]) // Перетворюємо індекси на об'єкти
>             .collect(Collectors.toList());
> }
> ```
>
> Другий підхід є більш "функціональним" і може бути ефективнішим для дуже великих `enum`, оскільки не створює копію всього списку і не перемішує його повністю.

#### **📌 Що перевіряють:**

*Знання Java 8 Stream API та здатність комбінувати його з іншими інструментами (як `Random` чи `Collections`) для вирішення складних задач обробки даних.*
