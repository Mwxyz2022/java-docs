---
layout: default
title: "Інтерфейси (interface, implements) — Контракт поведінки, реалізація методів у класах"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 15
---

# Інтерфейси (interface, implements) — Контракт поведінки, реалізація методів у класах

* короткий опис: Інтерфейс у Java — це абстрактний тип, який визначає контракт поведінки: набір методів, які мають бути реалізовані в класі, що його імплементує.

* поняття: `interface`, `implements`, Абстрактні методи, `default` методи, `static` методи, `private` методи.

* приклади: `interface Flyable { void fly(); }`, `class Bird implements Flyable`.

* ключові моменти: Визначає `що` клас повинен робити, а не `як`. Множинна реалізація. З Java 8+ можуть мати реалізацію.

* що важливо знати на співбесіді: Визначення, як реалізуються, відмінності від абстрактних класів.

### **✅ Офіційне визначення:**

Інтерфейс в Java — це **абстрактний тип**, який визначає **контракт поведінки**: набір методів, які мають бути
реалізовані в класі, що його імплементує. Він описує **що треба зробити**, але не **як**.

### **🧠 Простими словами:**

Інтерфейс — це **чек-ліст**: клас, який "підписується" на нього, зобов’язується реалізувати всі методи.

Це як **правила гри** — "хочеш грати — дотримуйся"
---

## **Основні елементи інтерфейсів**

|       Елемент        |               Приклад               |                  Коментар                   |
|:--------------------:|:-----------------------------------:|:-------------------------------------------:|
|   **`interface`**    |   **`interface Flyable { ... }`**   |            Оголошення інтерфейсу            |
|   **`implements`**   | **`class Bird implements Flyable`** |           Клас реалізує інтерфейс           |
|  Абстрактні методи   |          **`void fly();`**          | Метод без тіла, обов’язковий для реалізації |
| **`default`** методи |     **`default void log() {}`**     |        Метод з реалізацією (Java 8+)        |
| **`static`** методи  |     **`static void info() {}`**     |    Можна викликати без створення обʼєкта    |
| **`private`** методи |   **`private void helper() {}`**    |    Внутрішня логіка інтерфейсу (Java 9+)    |

---

## **Пояснення на пальцях (💡)**

* **Інтерфейс \= контракт**: ти кажеш, що клас **вміє** щось робити (**`Flyable`**), але **не описуєш як**.
* Один клас може **реалізовувати декілька інтерфейсів** (на відміну від **`extends`**, яке одне).
* **Писати код проти інтерфейсів** — це хороша архітектурна практика для гнучкості й тестованості.

---

## **Корисні приклади (🧪)**

### **🔹 Оголошення та реалізація інтерфейсу:**

```java
interface Flyable {
    void fly();
}

class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("Bird flies");
    }
}
```

### **🔹 Клас реалізує кілька інтерфейсів:**

```java
interface Swimmable {
    void swim();
}

class Duck implements Flyable, Swimmable {
    public void fly() {
        System.out.println("Duck flies");
    }

    public void swim() {
        System.out.println("Duck swims");
    }
}
```

### **🔹 Інтерфейс із `default` методом:**

```java
interface Logger {
    default void log(String message) {
        System.out.println("LOG: " + message);
    }
}
```

---

## **Порівняння: Інтерфейс vs Абстрактний клас ⚖️**

|          Ознака          |                Інтерфейс                |          Абстрактний клас          |
|:------------------------:|:---------------------------------------:|:----------------------------------:|
|     **Наслідування**     |       Можна реалізовувати кілька        |   Наслідування лише одного класу   |
|         **Поля**         | Тільки **`static`** `final` (константи) |       Може мати змінні поля        |
| **Методи з реалізацією** | **`default`**, **`static`** (з Java 8\) |        Так, звичайні методи        |
|    **Підходить для**     |        Контрактів / можливостей         | Спільної логіки, базової поведінки |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘 **interface \= що вмієш?**

* Якщо клас вміє літати -> **`implements`** `Flyable`
* Якщо вміє плавати -> **`implements`** `Swimmable`

📌 Більшість інтерфейсів закінчуються на **`-able:`**  
**`Flyable`**, **`Drawable, Serializable, Runnable`**

---

## **🟩 Підсумок:**

Інтерфейси в Java — це **контракти**, які дозволяють:

* Розділяти **що має бути реалізовано** без опису **як**.
* Створювати **гнучку архітектуру** через програмування проти інтерфейсів.
* Використовувати **поліморфізм** і **множинну реалізацію**.
* Уніфікувати API через **`implements`**.

🔑 **Інтерфейси — це серце гнучких і масштабованих Java-додатків.**

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Oracle**](https://docs.oracle.com/javase/tutorial/java/IandI/createinterface.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс в Java і яка його основна мета?

> #### **💬 Відповідь:**
>
> **Інтерфейс** в Java — це повністю **абстрактний тип**, який визначає **контракт поведінки**. Він описує, **ЩО** клас повинен робити, але не каже, **ЯК** він має це робити.
>
> **Основна мета інтерфейсу:**
>
> 1. **Визначення контракту:** Інтерфейс містить набір сигнатур методів, які будь-який клас, що його реалізує (`implements`), зобов'язаний надати. Це гарантує, що всі класи, що реалізують інтерфейс, будуть мати узгоджену поведінку.
>
> 2. **Досягнення повної абстракції:** Він дозволяє повністю відокремити визначення методу від його реалізації.
>
> 3. **Реалізація поліморфізму та множинного наслідування типів:** Клас може реалізовувати багато інтерфейсів, що дозволяє об'єкту мати багато різних типів. Це є основою для побудови гнучких та слабкозв'язаних систем.
>
> **Приклад:**
> Інтерфейс `Runnable` має один метод `run()`. Будь-який клас, що реалізує `Runnable`, гарантує, що його можна виконати, викликавши метод `run()`.

#### **📝 Аналогія:**

Інтерфейс — це як розетка в стіні. Вона визначає **контракт**: форму, напругу, частоту. Ви можете підключити до неї будь-який прилад (зарядний пристрій, лампу, пилосос), який дотримується цього контракту. Розетці байдуже, що саме робить прилад, головне — щоб він мав правильну вилку.

#### **📌 Що перевіряють:**

*Розуміння того, що інтерфейс — це в першу чергу контракт поведінки, а не просто набір методів.*

* * *

### **2 ❓ Питання:**

У чому ключові відмінності між інтерфейсом та абстрактним класом?

> #### **💬 Відповідь:**
>
> Це одне з найфундаментальніших питань, що перевіряє розуміння інструментів абстракції.
>
| Характеристика | Інтерфейс (`interface`) | Абстрактний клас (`abstract class`) |
| :---: | :---: | :---: |
| **Призначення** | Визначає **контракт (поведінку)**, "can-do". | Надає **базовий скелет (стан та поведінку)**, "is-a". |
| **Наслідування** | Клас може реалізувати **багато** інтерфейсів. | Клас може успадкувати **лише один** абстрактний клас. |
| **Поля** | Лише **`public static final`** константи. | Може містити будь-які поля (включаючи `private`, `non-final`).|
| **Методи** | До Java 8 — лише абстрактні. З Java 8+ — може містити `default` та `static` методи. | Може містити як абстрактні, так і конкретні (реалізовані) методи.|
| **Конструктор** | **Немає** конструктора. | **Має** конструктор для ініціалізації стану, який викликається з дочірніх класів. |

#### **📌 Що перевіряють:**

*Чітке знання технічних та концептуальних відмінностей. Кандидат повинен уміти пояснити, коли і чому обирати кожен з них.*

* * *

### **3 ❓ Питання:**

Що таке `default` методи в інтерфейсах (з Java 8) і яку проблему вони вирішують?

> #### **💬 Відповідь:**
>
> **`default` метод** — це метод в інтерфейсі, який має **тіло реалізації** і оголошений з ключовим словом `default`.
>
> **Проблему, яку вони вирішують:**
>
> * Вони дозволяють **додавати нові методи до існуючих інтерфейсів, не ламаючи** всі класи, що їх реалізують.
> * До Java 8, якщо ви додавали новий абстрактний метод в інтерфейс, вам потрібно було змінити всі класи, що його реалізують, і додати реалізацію цього нового методу. Це було величезною проблемою для розвитку стандартної бібліотеки (наприклад, `Collections API`).
>
> **Приклад:**
> В Java 8 до інтерфейсу `Iterable` було додано метод `forEach`. Якби він був абстрактним, це "зламало" б тисячі існуючих реалізацій колекцій. Замість цього його додали як `default` метод:
>
> ```java
> public interface Iterable<T> {
>     Iterator<T> iterator();
>
>     default void forEach(Consumer<? super T> action) {
>         // ... реалізація за замовчуванням ...
>     }
> }
> ```
>
> Тепер всі класи, що реалізують `Iterable`, автоматично "отримують" цей метод, і їхній код не ламається. При бажанні вони можуть його перевизначити.
>
> Таким чином, `default` методи забезпечують **зворотну сумісність та еволюцію API**.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей Java (з версії 8) та розуміння причин їхнього впровадження.*

* * *

### **4 ❓ Питання:**

Чи може клас реалізувати два інтерфейси, в яких є `default` методи з однаковою сигнатурою? Якщо так, то як вирішується конфлікт?

> #### **💬 Відповідь:**
>
> Якщо клас реалізує два інтерфейси, які мають `default` методи з однаковою назвою та параметрами, виникає **конфлікт, відомий як "проблема ромба"**.
>
> Цей код **не скомпілюється**. Компілятор Java не знає, яку з двох успадкованих реалізацій обрати, і видасть помилку.
>
> **Як вирішити конфлікт:**
>
> Клас-реалізатор **зобов'язаний явно вирішити** цю неоднозначність, **перевизначивши (`@Override`)** конфліктний метод. Усередині перевизначеного методу розробник може:
>
> 1. **Надати абсолютно нову, власну реалізацію.**
> 2. **Викликати одну з батьківських реалізацій**, використовуючи спеціальний синтаксис `InterfaceName.super.methodName()`.
> 3. **Комбінувати обидві реалізації.**
>
> **Приклад:**
>
> ```java
> interface Speaker {
>     default void speak() { System.out.println("Speaking..."); }
> }
> interface Actor {
>     default void speak() { System.out.println("Acting..."); }
> }
>
> class Performer implements Speaker, Actor {
>     @Override
>     public void speak() {
>         // Явно обираємо реалізацію з Speaker
>         Speaker.super.speak();
>     }
> }
> ```
>
> Таким чином, Java вирішує проблему на етапі компіляції, змушуючи програміста прийняти свідоме рішення.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють `default` методи та механізми вирішення конфліктів у сучасній Java.*

* * *

### **5 ❓ Питання:**

Що таке маркерний інтерфейс? Наведіть приклади з JDK.

> #### **💬 Відповідь:**
>
> **Маркерний інтерфейс (Marker Interface)** — це інтерфейс, який **не містить жодних методів або констант**. Він є порожнім.
>
> **Його призначення** — "позначити" (`mark`) клас, який його реалізує, надавши йому певну семантичну властивість або вказавши JVM, фреймворку чи інструменту, що цей клас повинен оброблятися особливим чином.
>
> **Приклади з JDK:**
>
> * `java.io.Serializable`: Клас, що реалізує цей інтерфейс, сигналізує механізму серіалізації, що його об'єкти можна безпечно перетворити в потік байт. Якщо клас не реалізує цей інтерфейс, спроба серіалізації призведе до `NotSerializableException`.
>
> * `java.lang.Cloneable`: Позначає, що об'єкти цього класу підтримують клонування за допомогою методу `Object.clone()`. Без цього інтерфейсу `Object.clone()` викине `CloneNotSupportedException`.
>
> * `java.util.RandomAccess`: Цим інтерфейсом позначають реалізації `List` (наприклад, `ArrayList`), які підтримують швидкий довільний доступ до елементів за індексом. Алгоритми, як `Collections.sort`, можуть використовувати цю інформацію для вибору більш ефективного алгоритму.
>
> В сучасній Java роль маркерних інтерфейсів часто виконують **анотації**, які є більш гнучкими.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та їх реалізації в стандартній бібліотеці.*

* * *

### **6 ❓ Питання:**

Навіщо в інтерфейсах потрібні `static` та `private` методи? ⭐️

> #### **💬 Відповідь:**
>
> Ці методи були додані в Java 8 та 9 відповідно, щоб зробити інтерфейси більш потужними та уникнути дублювання коду.
>
> **`static` методи (з Java 8):**
>
> * **Призначення:** Дозволяють розміщувати **утилітарні (helper) методи** безпосередньо в інтерфейсі, до якого вони логічно відносяться.
> * **Перевага:** Раніше такі методи доводилося виносити в окремі `...Utils` класи (наприклад, `Collections` для `Collection`). Тепер вони можуть бути частиною самого інтерфейсу.
> * Вони належать інтерфейсу, а не об'єкту, і викликаються через `InterfaceName.methodName()`.
>
>     ```java
>     interface MyInterface {
>         static void helperMethod() { /* ... */ }
>     }
>     // Виклик: MyInterface.helperMethod();
>     ```
>
>
> **`private` методи (з Java 9):**
>
> * **Призначення:** Дозволяють **уникати дублювання коду** всередині самого інтерфейсу, якщо кілька `default` або `static` методів використовують однакову складну логіку.
> * **Перевага:** Ця логіка може бути винесена в `private` метод, який не буде частиною публічного API інтерфейсу.
>
>     ```java
>     interface MyInterface {
>         default void methodA() { commonLogic("A"); }
>         default void methodB() { commonLogic("B"); }
>         // Приватний допоміжний метод
>         private void commonLogic(String s) { /* ... */ }
>     }
>     ```
>
>
#### **📌 Що перевіряють:**

*Знання сучасних фіч Java та розуміння, які проблеми вони вирішують.*

* * *

### **7 ❓ Питання:**

Чому поля в інтерфейсі є неявно `public static final`?

> #### **💬 Відповідь:**
>
> Це випливає з самої концепції інтерфейсу як **чистого контракту**, який не повинен мати **власного стану**.
>
> 1. **`public`:** Інтерфейс визначає публічний контракт, тому все, що в ньому є, повинно бути доступним для реалізаторів та клієнтів.
>
> 2. **`static`:** Поля належать самому інтерфейсу, а не екземпляру класу, що його реалізує. Якби вони не були статичними, це б означало, що інтерфейс може мати стан екземпляра, що суперечить його ідеї як контракту поведінки.
>
> 3. **`final`:** Поля повинні бути константами, бо якби їх можна було змінювати, це знову ж таки було б формою стану. Інтерфейс не повинен зберігати дані, що змінюються, а лише визначати константи, пов'язані з його контрактом (наприклад, `public static final int DEFAULT_PORT = 8080;`).
>
> Коли ви пишете:
> `int MAX_SIZE = 100;`
>
> Компілятор автоматично перетворює це на:
> `public static final int MAX_SIZE = 100;`
>
> Це зроблено для лаконічності та для того, щоб підкреслити природу інтерфейсу.

#### **📌 Що перевіряють:**

*Розуміння семантики інтерфейсів та причин, чому на їхні члени накладено такі строгі неявні правила.*

* * *

### **8 ❓ Питання:**

Поясніть принцип "програмуйте на рівні інтерфейсу, а не реалізації". Чому це важливо? ⭐️

> #### **💬 Відповідь:**
>
> Це один з найважливіших принципів об'єктно-орієнтованого дизайну. Він означає, що ваш код (змінні, параметри методів, типи повернення) повинен посилатися на **абстрактний тип (інтерфейс)**, а не на його **конкретний клас (реалізацію)**.
>
> **Приклад:**
>
> * **Погано:** `ArrayList<String> list = new ArrayList<>();`
> * **Добре:** `List<String> list = new ArrayList<>();`
>
> **Чому це важливо:**
>
> 1. **Гнучкість та слабка зв'язність (Loose Coupling):** Коли ваш код залежить від інтерфейсу `List`, ви можете легко замінити реалізацію, наприклад, `ArrayList` на `LinkedList`, **не змінюючи решту коду**. Програма залишається робочою, бо обидва класи дотримуються контракту `List`.
>     `List<String> list = new LinkedList<>(); // Єдина зміна`
>
> 2. **Розширюваність:** Легко додавати нові реалізації. Якщо ви створите власний `MySuperFastList implements List`, ваш існуючий код буде з ним працювати.
>
> 3. **Тестованість:** Це спрощує тестування, оскільки ви можете легко "підмінити" реальну реалізацію на mock-об'єкт (заглушку) для ізольованого тестування.
>
> Цей принцип є основою для патерну **Dependency Injection** та побудови модульних, легко підтримуваних систем.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Це питання на розуміння ключового принципу створення гнучкого коду.*

* * *

### **9 ❓ Питання:**

Що таке функціональний інтерфейс? Яка його основна вимога? ⭐️

> #### **💬 Відповідь:**
>
> **Функціональний інтерфейс** — це інтерфейс, який містить **рівно один абстрактний метод**.
>
> Це ключова концепція, що лежить в основі **лямбда-виразів** у Java 8.
>
> **Основна вимога:** Наявність **лише одного** абстрактного методу. Інтерфейс при цьому може містити скільки завгодно `default` або `static` методів.
>
> **Анотація `@FunctionalInterface`:**
> Хоча ця анотація не є обов'язковою, її використання є **найкращою практикою**. Вона дає команду компілятору перевірити, чи дійсно інтерфейс відповідає вимогам функціонального. Якщо ви спробуєте додати другий абстрактний метод, компілятор видасть помилку.
>
> **Приклад:**
>
> ```java
> @FunctionalInterface
> interface Calculator {
>     int calculate(int a, int b); // Єдиний абстрактний метод
> }
> ```
>
> **Навіщо вони потрібні:**
> Функціональні інтерфейси виступають як **тип для лямбда-виразів**. Будь-яка лямбда, сигнатура якої збігається з абстрактним методом, може бути присвоєна змінній цього інтерфейсу.
>
> ```java
> Calculator adder = (x, y) -> x + y;
> Calculator multiplier = (x, y) -> x * y;
> ```

#### **📌 Що перевіряють:**

*Знання основ функціонального програмування в Java 8, зокрема ролі функціональних інтерфейсів та анотації `@FunctionalInterface`.*

* * *

### **10 ❓ Питання:**

Як клас, що реалізує інтерфейс, може викликати `default` метод, якщо він його перевизначив? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо клас перевизначив `default` метод, він може викликати оригінальну реалізацію з інтерфейсу за допомогою спеціального синтаксису **`super`**, який вказує ім'я інтерфейсу.
>
> **Синтаксис:** `InterfaceName.super.methodName();`
>
> **Приклад:**
>
> ```java
> interface Greeter {
>     default void greet() {
>         System.out.println("Hello from Greeter!");
>     }
> }
>
> class MyGreeter implements Greeter {
>     @Override
>     public void greet() {
>         // 1. Спочатку робимо щось своє
>         System.out.println("Custom greeting is about to happen...");
>
>         // 2. Викликаємо реалізацію за замовчуванням з інтерфейсу
>         Greeter.super.greet();
>
>         // 3. Потім можемо додати ще щось
>         System.out.println("Greeting finished.");
>     }
> }
> ```
>
> Цей механізм схожий на `super.method()` для класів і дозволяє **розширювати**, а не повністю замінювати `default`-поведінку.

#### **📌 Що перевіряють:**

*Знання повного синтаксису для роботи з `default` методами, включаючи виклик батьківської реалізації.*

* * *

### **11 ❓ Питання:**

Що таке **SAM-інтерфейс** і чим він відрізняється від функціонального інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **SAM-інтерфейс (Single Abstract Method Interface)** — це і є **функціональний інтерфейс**.
>
> Ці два терміни є **синонімами**.
>
> * Термін **SAM-інтерфейс** був поширений до появи Java 8. Він дослівно означає "інтерфейс з одним абстрактним методом".
> * З виходом Java 8 і лямбда-виразів був введений офіційний, більш сучасний термін — **функціональний інтерфейс**, який закріплено анотацією `@FunctionalInterface`.
>
> Отже, якщо на співбесіді вас запитують про різницю, правильна відповідь — що **різниці немає**, це просто стара і нова назви для одного й того самого поняття. Використання терміну "функціональний інтерфейс" є більш сучасним і правильним у контексті Java 8+.

#### **📌 Що перевіряють:**

*Знання термінології та її еволюції. Це питання на перевірку, чи кандидат знайомий як з класичними, так і з сучасними поняттями.*

* * *

### **12 ❓ Питання:**

Чи може інтерфейс розширювати клас? А клас реалізовувати клас? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, в обох випадках.**
>
> **1. Чи може інтерфейс розширювати клас (`interface A extends B` де `B` - клас)?**
>
> * **Ні, не може.** Це призведе до помилки компіляції.
> * **Причина:** Це порушує фундаментальну логіку. Інтерфейс — це чистий контракт, він не може успадковувати стан або конкретну реалізацію від класу. Інтерфейс може розширювати **лише інші інтерфейси**.
>
> **2. Чи може клас реалізовувати клас (`class A implements B` де `B` - клас)?**
>
> * **Ні, не може.** Це також помилка компіляції.
> * **Причина:** Ключове слово `implements` використовується **лише для реалізації інтерфейсів**. Для отримання функціональності від іншого класу використовується наслідування за допомогою `extends`.
>
> **Правильні відношення в Java:**
>
> * Клас **`extends`** клас.
> * Клас **`implements`** інтерфейс.
> * Інтерфейс **`extends`** інтерфейс.

#### **📌 Що перевіряють:**

*Чітке знання синтаксису та семантики `extends` та `implements` і правил їх застосування.*

* * *

### **13 ❓ Питання:**

Чому інтерфейси не можуть мати `protected` методів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з основною метою інтерфейсів та семантикою модифікатора `protected`.
>
> * **Призначення інтерфейсу:** Визначити **публічний контракт**, який може бути реалізований будь-яким класом у будь-якому пакеті. Все в інтерфейсі за замовчуванням `public`, бо це API для зовнішнього світу.
>
> * **Призначення `protected`:** Надати доступ **дочірнім класам** (або класам у тому ж пакеті) до деталей реалізації, які приховані від решти. Це інструмент для **ієрархії наслідування реалізації**.
>
> **Чому вони несумісні:**
>
> 1. Інтерфейс не має власної реалізації (за винятком `default`/`static` методів, які мають свої правила), тому немає "внутрішньої логіки", яку потрібно було б захищати за допомогою `protected`.
> 2. Якби `protected` метод був дозволений в інтерфейсі, це б створило дивну ситуацію. Будь-який клас, що реалізує інтерфейс, повинен був би надати `public` реалізацію для цього методу, щоб задовольнити контракт. Тоді сам `protected` в інтерфейсі втратив би будь-який сенс.
>
> `default` методи також є `public`, бо вони є частиною контракту.
> З Java 9 з'явилися `private` методи, але вони служать лише для внутрішньої декомпозиції коду `default` та `static` методів і не є частиною API інтерфейсу.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, чому певні мовні конструкції спроектовані саме так, а не інакше.*

* * *

### **14 ❓ Питання:**

Чи можна створити екземпляр інтерфейсу? Наприклад, `new Runnable()`. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Напряму — ні. Але опосередковано — так.**
>
> **1. Напряму — не можна:**
>
> * Інтерфейс є абстрактним типом. Ви не можете написати `new Runnable();`, тому що інтерфейс не має конструктора і не містить повної реалізації. Це призведе до помилки компіляції.
>
> **2. Опосередковано — можна (за допомогою анонімних класів):**
>
> * Конструкція, яка виглядає як `new Runnable()`, насправді є створенням **екземпляра анонімного внутрішнього класу**, який **реалізує** інтерфейс `Runnable`.
>
> ```java
> Runnable r = new Runnable() {
>     @Override
>     public void run() {
>         System.out.println("Running...");
>     }
> };
> ```
>
> Тут ми не створюємо об'єкт `Runnable`, а створюємо об'єкт "безіменного" класу, який імплементує `Runnable` і одразу надає реалізацію для методу `run()`.
>
> **3. З лямбда-виразами (сучасний підхід):**
>
> В Java 8+ для функціональних інтерфейсів це робиться ще простіше за допомогою лямбди:
>
> ```java
> Runnable r = () -> System.out.println("Running...");
> ```
>
> "Під капотом" цей лямбда-вираз також перетворюється на об'єкт, що реалізує інтерфейс `Runnable`.
>
> Отже, ви створюєте екземпляр **класу-реалізатора**, а не самого інтерфейсу.

#### **📌 Що перевіряють:**

*Дуже точне розуміння того, що насправді відбувається при виклику `new` для інтерфейсу. Розмежування між інтерфейсом і його реалізацією.*

* * *

### **15 ❓ Питання:**

Поясніть на прикладі патерну "Стратегія", чому програмування на рівні інтерфейсів є таким потужним. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн **"Стратегія"** є ідеальною демонстрацією потужності інтерфейсів та поліморфізму.
>
> **Суть патерну:** Дозволити змінювати алгоритм (стратегію) під час виконання програми, не змінюючи клас, який його використовує.
>
> **Як це реалізується:**
>
> 1. **Створюється інтерфейс "Стратегії"**, який визначає єдиний метод для виконання алгоритму.
>
>     ```java
>     // Абстракція (інтерфейс)
>     interface SortingStrategy {
>         void sort(List<Integer> list);
>     }
>     ```
>
> 2. **Створюються конкретні реалізації** цього інтерфейсу, кожна з яких інкапсулює свій власний алгоритм.
>
>     ```java
>     // Конкретні стратегії
>     class BubbleSortStrategy implements SortingStrategy {
>         public void sort(List<Integer> list) { /* логіка бульбашкового сортування */ }
>     }
>     class QuickSortStrategy implements SortingStrategy {
>         public void sort(List<Integer> list) { /* логіка швидкого сортування */ }
>     }
>     ```
>
>
> 3. **Клас-"контекст"** (який використовує стратегію) залежить **лише від інтерфейсу**.
>
>     ```java
>     // Контекст
>     class Sorter {
>         private SortingStrategy strategy;
>
>         public void setStrategy(SortingStrategy strategy) { // Можна змінювати стратегію в runtime!
>             this.strategy = strategy;
>         }
>
>         public void performSort(List<Integer> list) {
>             // Делегуємо роботу обраній стратегії
>             strategy.sort(list);
>         }
>     }
>     ```
>
>
> **У чому сила:**
>
> * `Sorter` нічого не знає про те, як саме відбувається сортування. Він повністю **відокремлений** від конкретних алгоритмів.
> * Ми можемо **динамічно змінювати** поведінку `Sorter`'а, просто передаючи йому інший об'єкт-стратегію: `sorter.setStrategy(new QuickSortStrategy());`.
> * Легко додавати **нові стратегії** (наприклад, `MergeSortStrategy`), не змінюючи жодного рядка в класі `Sorter`.
>
> Це робить систему надзвичайно гнучкою, розширюваною та тестованою, що і є головною перевагою програмування на рівні інтерфейсів.

#### **📌 Що перевіряють:**

*Знання патернів проектування та здатність продемонструвати на їхньому прикладі, як фундаментальні принципи ООП (абстракція, поліморфізм) вирішують реальні архітектурні задачі.*
