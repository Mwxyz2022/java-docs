---
layout: default
title: "IS-A vs HAS-A (Наслідування проти композиції)"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 13
---

# IS-A vs HAS-A (Наслідування проти композиції)

* короткий опис: `IS-A` відношення (наслідування, `extends`) означає, що один клас є різновидом іншого (наприклад, `Dog IS-A Animal`). `HAS-A` відношення (композиція/агрегація) означає, що один клас містить екземпляр іншого класу як частину своєї структури (наприклад, `Car HAS-A Engine`).

* поняття: `IS-A` (inheritance), `HAS-A` (composition/aggregation), наслідування, композиція.

* приклади: `Dog extends Animal`, `Car` з полем `Engine`.

* ключові моменти: `IS-A` – жорсткий зв'язок; `HAS-A` – гнучкий зв'язок. Композиція часто є кращою альтернативою наслідуванню для повторного використання коду.

* що важливо знати на співбесіді: Різниця, коли що використовувати, переваги композиції.

### **✅ Офіційне визначення:**

**IS-A відношення** — це наслідування (`inheritance`), коли один клас є спеціалізованим типом іншого, реалізуючи його
поведінку та властивості (наприклад, `Dog extends Animal`).

**HAS-A відношення** — це композиція або агрегація (`composition`/`aggregation`), коли один клас містить у собі
екземпляр іншого класу як частину своєї структури (наприклад, `Car` має `Engine`).

### **🧠 Простими словами:**

**IS-A означає «є»**, наприклад собака — це тварина.
**HAS-A означає «має»**, наприклад машина має двигун.

## **Основне**

* **IS-A (наслідування)**

  * Використання ключового слова `extends` у Java

  * Вказує на жорсткий зв’язок між класами

  * Перевага: повторне використання коду, поліморфізм

  * Недолік: тісне зв’язування, складність підтримки при великій ієрархії

* **HAS-A (композиція/агрегація)**

  * Поля класу, які є об’єктами інших класів

  * Гнучкіший зв’язок, зменшує залежності

  * Композиція — сильніший зв’язок, коли частина належить цілому (агрегація — слабший зв’язок)

  * Перевага: краща масштабованість, простіше змінювати компоненти

## **Пояснення на пальцях (💡)**

Уяви, що IS-A — це як сімейне дерево: ти — це твій батько, син — це батько для онука. Жорстка ієрархія.  
HAS-A — це як машина і її двигун: двигун можна замінити, а машина все одно залишиться машиною. Зручніше, якщо хочеш
змінювати частини без переписування всього.

## **Корисні приклади (🧪)**

* **IS-A:**

```java
class Animal {
    void eat() {
        System.out.println("Eating");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("Barking");
    }
}
```

* **HAS-A (композиція):**

```java
class Engine {
    void start() {
        System.out.println("Engine started");
    }
}

class Car {
    private Engine engine;

    public Car() {
        this.engine = new Engine();
    }

    void startCar() {
        engine.start();
    }
}
```

## **Порівняння (⚖️)**

| Критерій              | IS-A (Наслідування)                | HAS-A (Композиція/Агрегація)              |
|-----------------------|------------------------------------|-------------------------------------------|
| Тип зв’язку           | Жорсткий                           | Гнучкий                                   |
| Взаємозалежність      | Висока                             | Низька                                    |
| Спадкування поведінки | Автоматично                        | Потрібно явно делегувати                  |
| Заміна компонента     | Складно                            | Легко (через заміну об’єкта)              |
| Переваги              | Поліморфізм, повторне використання | Легка підтримка, гнучкість                |
| Недоліки              | Жорстка ієрархія, складність змін  | Потрібна додаткова логіка для делегування |

---

## **🧠 Мнемоніка для запам’ятовування**

* **`IS-A`** — Ієрархія (Inheritance — Ієрархія) \= "Я є"

* **`HAS-A`** — Композиція (Has-a \= Має) \= "Я маю"

**Уяви собаку: "Я є тварина" (IS-A) і "Я маю хвіст" (HAS-A).**

---

## **🟩 Підсумок:**

* **`IS-A і HAS-A`** — фундаментальні концепції ООП для моделювання зв’язків між класами.

* **`IS-A`** означає наслідування і жорсткий зв’язок «є типом».

* **`HAS-A`** — композиція або агрегація, гнучкий зв’язок «має частину».

* Композиція зазвичай більш гнучка та підтримувана, рекомендується використовувати її, коли це можливо.

* Вміння чітко розрізняти і застосовувати ці концепції — ключове на співбесідах з ООП та розробки.

**🔗 Для поглиблення:**

* [**Oracle Java Tutorials \- Inheritance**](https://docs.oracle.com/javase/tutorial/java/IandI/subclasses.html)

* [**Stack Overflow discussion about IS-A vs HAS-A**](https://stackoverflow.com/questions/48448/is-a-vs-has-a-inheritance-vs-composition)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Поясніть, що таке відношення IS-A та HAS-A в ООП.

> #### **💬 Відповідь:**
>
> **IS-A** та **HAS-A** — це два фундаментальні типи відношень між класами, які використовуються для моделювання реального світу та повторного використання коду.
>
> 1. **IS-A (є різновидом):**
>     * **Що це:** Це відношення **наслідування**. Воно означає, що один клас є спеціалізованою версією іншого.
>     * **Реалізація в Java:** За допомогою ключового слова `extends` для класів або `implements` для інтерфейсів.
>     * **Приклад:** `Dog` **IS-A** `Animal` (`class Dog extends Animal {}`). Собака є різновидом тварини.
>     * **Перевірка:** Просте речення "Об'єкт А є Б" має звучати логічно.
>
> 2. **HAS-A (має):**
>     * **Що це:** Це відношення **асоціації**, яке реалізується через **композицію** або **агрегацію**. Воно означає, що один об'єкт містить у собі посилання на інший об'єкт як на свою частину.
>     * **Реалізація в Java:** Шляхом оголошення поля, тип якого є іншим класом.
>     * **Приклад:** `Car` **HAS-A** `Engine` (`class Car { private Engine engine; }`). Автомобіль має двигун.
>     * **Перевірка:** Просте речення "Об'єкт А має Б" звучить логічно.

#### **📝 Аналогія:**

Ви, як людина:

* `Ви` **IS-A** `Ссавець` (наслідування властивостей, як-от теплокровність).
* `Ви` **HAS-A** `Серце` (композиція; серце — невід'ємна частина вас).

#### **📌 Що перевіряють:**

*Базове розуміння двох основних способів побудови зв'язків між класами.*

* * *

### **2 ❓ Питання:**

Назвіть головні переваги та недоліки наслідування (IS-A).

> #### **💬 Відповідь:**
>
> **Переваги:**
>
> * **Повторне використання коду:** Це найочевидніша перевага. Спільний код виноситься в суперклас і успадковується підкласами, що уникає дублювання.
> * **Поліморфізм:** Наслідування є основою для динамічного поліморфізму, що дозволяє працювати з різними об'єктами через єдиний інтерфейс суперкласу.
> * **Логічна ієрархія:** Дозволяє будувати зрозумілі ієрархії класів, що відображають предметну область.
>
> **Недоліки:**
>
> * **Жорсткий зв'язок (Tight Coupling):** Дочірній клас сильно залежить від реалізації батьківського. Зміна в батьківському класі може несподівано "зламати" всі дочірні. Це відомо як **"проблема крихкого базового класу"**.
> * **Обмежена гнучкість:** Java підтримує лише одиничне наслідування класів, тому ви не можете успадкувати поведінку від кількох класів одночасно.
> * **Розкриття реалізації:** Дочірній клас успадковує всі `public` та `protected` члени батька, навіть ті, які йому логічно не потрібні, що може порушувати інкапсуляцію.
> * **Неправильне використання:** Часто початківці використовують наслідування там, де краще підійшла б композиція, просто для повторного використання коду, створюючи нелогічні ієрархії (наприклад, `MyList extends ArrayList` замість `class MyList { private List list; }`).

#### **📌 Що перевіряють:**

*Збалансоване розуміння наслідування. Кандидат повинен усвідомлювати, що це потужний, але іноді небезпечний інструмент.*

* * *

### **3 ❓ Питання:**

Назвіть головні переваги та недоліки композиції (HAS-A).

> #### **💬 Відповідь:**
>
> **Переваги:**
>
> * **Гнучкість:** Це найважливіша перевага. Ви можете змінювати поведінку об'єкта **під час виконання**, підставляючи різні реалізації вкладених об'єктів (наприклад, змінюючи "двигун" у "автомобіля"). З наслідуванням тип жорстко зафіксований.
> * **Слабка зв'язність (Loose Coupling):** Клас залежить лише від **публічного інтерфейсу** вкладеного об'єкта, а не від деталей його реалізації. Це полегшує заміну компонентів.
> * **Краща інкапсуляція:** Ви повністю контролюєте, який доступ до вкладеного об'єкта надавати зовнішньому світу. Можна приховати його повністю або делегувати лише деякі методи.
> * **Дозволяє "множинне наслідування" поведінки:** Клас може містити посилання на багато різних об'єктів, комбінуючи їхню функціональність.
>
> **Недоліки:**
>
> * **Більше шаблонного коду:** Для того, щоб "виставити" назовні поведінку вкладеного об'єкта, часто доводиться писати **методи-делегати**, які просто викликають методи внутрішнього об'єкта.
>
>     ```java
>     class Car {
>         private Engine engine;
>         public void start() { this.engine.start(); } // Делегування
>     }
>     ```
>
> * **Немає переваг поліморфізму IS-A:** Об'єкт "контейнер" не є підтипом об'єктів, які він містить, тому ви не можете поліморфно підставити `Car` туди, де очікується `Engine`.

#### **📌 Що перевіряють:**

*Розуміння того, чому композиція вважається більш гнучким інструментом, та здатність оцінити її компроміси.*

* * *

### **4 ❓ Питання:**

Поясніть принцип "надавати перевагу композиції над наслідуванням" ("favor composition over inheritance"). Коли все ж таки слід використовувати наслідування?

> #### **💬 Відповідь:**
>
> Цей відомий принцип дизайну означає, що для досягнення **повторного використання коду** та гнучкості системи **композиція (HAS-A)** є, як правило, **кращим вибором**, ніж наслідування (IS-A).
>
> * **Композицію** слід використовувати, коли вам потрібна **гнучкість** і ви хочете **використовувати функціональність** іншого класу. Це створює більш слабкий зв'язок.
>
> * **Наслідування** слід використовувати набагато обережніше, переважно тоді, коли між класами існує **справжнє відношення "є різновидом" (is-a)** і ви плануєте використовувати переваги **поліморфізму**.
>
> **Коли наслідування є хорошим вибором:**
>
> 1. **Коли обидва класи знаходяться в одному пакеті та під вашим повним контролем,** і ви впевнені, що зміни в батьківському класі не зламають дочірні.
> 2. **Коли дочірній клас дійсно є спеціалізацією батьківського**, і його можна безпечно підставити замість батьківського (дотримання принципу Лісков). Приклад: `HashMap` та `LinkedHashMap`. `LinkedHashMap` дійсно **є** `HashMap`, але з доданою поведінкою (збереження порядку).
> 3. **При роботі з фреймворками, які вимагають наслідування**, наприклад, розширення `HttpServlet` для створення сервлетів.
>
> У всіх інших випадках, особливо якщо вам просто потрібна "одна-дві функції" з іншого класу, композиція буде набагато безпечнішим та гнучкішим рішенням.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен вміти обґрунтовано обирати між двома підходами, розуміючи їхні довгострокові наслідки.*

* * *

### **5 ❓ Питання:**

Наведіть приклад, де наслідування було б поганим дизайнерським рішенням, а композиція — хорошим.

> #### **💬 Відповідь:**
>
> Класичний приклад — це спроба повторно використати функціональність `ArrayList`, успадкувавши від нього.
>
> Уявімо, ми хочемо створити стек за допомогою `ArrayList`.
>
> **Погане рішення (IS-A):**
>
> ```java
> // Здається, що Stack IS-A List... але це не так.
> public class MyStack<T> extends ArrayList<T> {
>     public void push(T item) {
>         this.add(item);
>     }
>     public T pop() {
>         return this.remove(this.size() - 1);
>     }
> }
> ```
>
> **Проблеми:**
>
> 1. `MyStack` тепер успадковує **всі** публічні методи `ArrayList`, такі як `add(int index, T element)`, `remove(int index)` тощо.
> 2. Це **порушує логіку стека (LIFO)**. Користувач може випадково викликати `myStack.add(0, item)` і додати елемент на початок, "зламавши" стек.
> 3. Ми розкрили занадто багато деталей реалізації.
>
> **Хороше рішення (HAS-A):**
>
> ```java
> public class MyStack<T> {
>     // MyStack "має" (HAS-A) список для зберігання даних
>     private List<T> list = new ArrayList<>();
>
>     public void push(T item) {
>         list.add(item);
>     }
>
>     public T pop() {
>         if (list.isEmpty()) { return null; }
>         return list.remove(list.size() - 1);
>     }
> }
> ```
>
> **Переваги:**
>
> 1. Клас `MyStack` надає лише ті методи, які йому потрібні (`push`, `pop`), ідеально реалізуючи **інкапсуляцію**.
> 2. Він не розкриває жодних зайвих методів з `ArrayList`.
> 3. Внутрішню реалізацію можна легко замінити (наприклад, на `LinkedList`), і це не вплине на код, що використовує `MyStack`.

#### **📌 Що перевіряють:**

*Здатність застосовувати принципи дизайну до конкретної задачі та пояснити, чому один підхід кращий за інший.*

* * *

### **6 ❓ Питання:**

Що таке агрегація та композиція? Чим вони відрізняються? ⭐️

> #### **💬 Відповідь:**
>
> І агрегація, і композиція є **підтипами відношення асоціації "HAS-A"**. Вони обидві означають, що один об'єкт містить інший, але відрізняються **силою зв'язку** та **життєвим циклом**.
>
> * **Агрегація (Aggregation):**
>   * Це **слабкий** зв'язок "має".
>   * Об'єкти, що є "частинами", можуть існувати **незалежно** від об'єкта, що є "цілим".
>   * "Ціле" не керує життєвим циклом "частин".
>   * **Приклад:** `Університет` та `Студенти`. Якщо університет закриється, студенти продовжать існувати.
>
> * **Композиція (Composition):**
>   * Це **сильний** зв'язок "має".
>   * "Частина" **не може існувати** без "цілого". Життєвий цикл частини повністю залежить від цілого.
>   * Зазвичай "ціле" відповідає за створення та знищення своїх "частин".
>   * **Приклад:** `Людина` та її `Серце`. Серце не може існувати окремо від людини. Якщо людина помирає, її серце також припиняє функціонувати як частина цієї системи.
>
> В коді це часто не так чітко розрізняється, але концептуальна різниця полягає у **володінні та життєвому циклі**.

#### **📌 Що перевіряють:**

*Знання просунутих концепцій ОО-аналізу та дизайну.*

* * *

### **7 ❓ Питання:**

Як Java реалізує відношення "is-a-kind-of" та "has-a-part-of"?

> #### **💬 Відповідь:**
>
> Java надає конкретні мовні конструкції для реалізації цих двох фундаментальних відношень.
>
> **Для "is-a-kind-of" (IS-A):**
>
> * Використовується **наслідування** за допомогою ключового слова `extends` для класів.
>     `class Dog extends Animal {} // Dog - це різновид Animal`
> * Також використовується **реалізація інтерфейсів** за допомогою ключового слова `implements`.
>     `class Car implements Movable {} // Car має здатність рухатись (Movable)`
>
> **Для "has-a-part-of" (HAS-A):**
>
> * Використовується **включення одного об'єкта в інший** як поле класу.
>     `class Car { private Engine engine; } // Car "має" Engine`
> * Цей підхід і є реалізацією **композиції** або **агрегації**.

#### **📌 Що перевіряють:**

*Здатність пов'язати теоретичні концепції (IS-A, HAS-A) з конкретними мовними конструкціями (`extends`, поля класу).*

* * *

### **8 ❓ Питання:**

Чи може дочірній клас обмежити доступ до успадкованого public методу, зробивши його private? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.**
>
> У Java існує строге правило щодо видимості при перевизначенні методів: **модифікатор доступу в дочірньому класі не може бути більш обмежувальним**, ніж у батьківському.
>
> Це означає, що якщо метод у суперкласі `public`, то в підкласі він також повинен бути `public`. Ви не можете "звузити" його видимість до `protected`, `default` чи `private`.
>
> **Причина:**
> Це правило підтримує **принцип підстановки Лісков (LSP)**. Якщо ви маєте посилання на батьківський тип `Parent p = new Child();`, ви очікуєте, що можете викликати всі `public` методи, які є у `Parent`. Якби `Child` міг зробити один з цих методів `private`, виклик `p.someMethod()` призвів би до помилки, порушуючи контракт батьківського класу.

#### **📌 Що перевіряють:**

*Знання правил перевизначення методів, пов'язаних з модифікаторами доступу.*

* * *

### **9 ❓ Питання:**

Розгляньте таку ієрархію: `Employee` -> `Manager` -> `Director`. Це приклад відношення IS-A чи HAS-A?

> #### **💬 Відповідь:**
>
> Це класичний приклад відношення **IS-A (є різновидом)**, реалізованого через **наслідування**.
>
> * `Manager` **IS-A** `Employee` (Менеджер є працівником).
> * `Director` **IS-A** `Manager` (Директор є різновидом менеджера).
>
> Код виглядав би так:
>
> ```java
> class Employee { ... }
> class Manager extends Employee { ... }
> class Director extends Manager { ... }
> ```
>
> Тут `Manager` успадковує всі властивості та поведінку `Employee` (наприклад, `name`, `salary`, `work()`) і додає власні (наприклад, `manageTeam()`). `Director` успадковує все від `Manager` і додає ще щось своє.
>
> Ця ієрархія дозволяє поліморфно працювати з усіма працівниками, наприклад, в `List<Employee>`.

#### **📌 Що перевіряють:**

*Здатність аналізувати предметну область та обирати правильний тип відношення.*

* * *

### **10 ❓ Питання:**

Коли варто обирати наслідування від абстрактного класу замість реалізації інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча інтерфейси є більш гнучкими, існують сценарії, де **наслідування від абстрактного класу** є кращим вибором.
>
> Основна причина — коли вам потрібно **поділитися спільною реалізацією та станом** між тісно пов'язаними класами.
>
> **Використовуйте абстрактний клас, коли:**
>
> 1. Ви хочете надати **спільну базову логіку**. Якщо багато дочірніх класів мають однаковий код у деяких методах, логічно винести цей код у конкретний метод абстрактного класу, щоб уникнути дублювання.
>
> 2. Вам потрібно мати **спільні поля (стан)**. Абстрактні класи можуть мати поля екземпляра (`private int state;`), тоді як інтерфейси — лише `public static final` константи.
>
> 3. Вам потрібно контролювати доступ до методів (використовувати `protected` або `default`). В інтерфейсах всі методи (крім `private` з Java 9) є `public`.
>
> **Приклад з JDK:** `java.util.AbstractList`. Цей абстрактний клас реалізує інтерфейс `List` і надає готову реалізацію для багатьох методів (`indexOf`, `hashCode`, `equals` тощо), покладаючись лише на кілька абстрактних методів (`get(int index)`, `size()`), які повинні реалізувати підкласи. Це значно спрощує створення власної реалізації `List`.

#### **📌 Що перевіряють:**

*Розуміння, коли кожен з інструментів абстракції є найбільш доречним.*

* * *

### **11 ❓ Питання:**

Чому кажуть, що наслідування порушує інкапсуляцію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Наслідування може **порушувати або ослаблювати інкапсуляцію**, оскільки створює дуже тісний зв'язок між дочірнім та батьківським класами.
>
> **Як це відбувається:**
>
> 1. **Залежність від деталей реалізації:** Дочірній клас залежить не тільки від публічного інтерфейсу батька, а й від його **деталей реалізації**, особливо якщо він використовує `protected` члени або викликає методи, які, у свою чергу, залежать від внутрішнього стану батька.
>
> 2. **Проблема крихкого базового класу:** Розробник батьківського класу може змінити його внутрішню реалізацію, вважаючи, що це безпечно, оскільки не змінюється публічний API. Але ця зміна може несподівано "зламати" логіку в дочірньому класі, який покладався на попередню поведінку.
>
> **Приклад:**
> `HashSet` у JDK мав метод `addAll`, який внутрішньо викликав свій метод `add` для кожного елемента. Розробники, що створювали свій клас, який успадковував `HashSet` і перевизначав `add` для підрахунку доданих елементів, отримували подвійний підрахунок, коли викликали `addAll`.
>
> **Композиція**, навпаки, краще зберігає інкапсуляцію, оскільки залежить лише від публічного контракту об'єкта, а не від його внутрішньої логіки.

#### **📌 Що перевіряють:**

*Глибоке розуміння взаємодії принципів ООП та їхніх компромісів. Це питання для розробників рівня Middle+.*

* * *

### **12 ❓ Питання:**

Як реалізувати відношення "багато-до-багатьох" (наприклад, `Студент` та `Курс`) за допомогою HAS-A відношення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Відношення "багато-до-багатьох" означає, що один студент може бути записаний на багато курсів, і один курс може мати багато студентів.
>
> Це реалізується за допомогою **колекцій** в обох класах.
>
> **Реалізація:**
>
> * Клас `Студент` **"має" (`HAS-A`)** список курсів.
> * Клас `Курс` **"має" (`HAS-A`)** список студентів.
>
> ```java
> public class Student {
>     private String name;
>     private List<Course> courses = new ArrayList<>();
>
>     public void enroll(Course course) {
>         if (!courses.contains(course)) {
>             courses.add(course);
>             course.addStudent(this); // Підтримуємо двосторонній зв'язок
>         }
>     }
>     // ...
> }
>
> public class Course {
>     private String title;
>     private List<Student> students = new ArrayList<>();
>
>     public void addStudent(Student student) {
>         if (!students.contains(student)) {
>             students.add(student);
>             student.enroll(this); // Підтримуємо двосторонній зв'язок
>         }
>     }
>     // ...
> }
> ```
>
> Тут ми маємо **двостороннє відношення агрегації**. Життєвий цикл студентів та курсів не пов'язаний напряму (якщо видалити курс, студенти залишаться). Важливою частиною реалізації є підтримка **консистентності** цього зв'язку: при додаванні курсу студенту, ми додаємо студента до курсу.

#### **📌 Що перевіряють:**

*Навички моделювання даних та реалізації складних відношень між об'єктами.*

* * *

### **13 ❓ Питання:**

Чи можна використовувати `final` разом з `extends`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом. Залежить від того, де саме застосовується `final`.
>
> * **Якщо `final` застосовано до класу:**
>     `public final class MyClass extends Parent { ... }`
>     **Так, це абсолютно валідно.** Це означає, що `MyClass` успадковує від `Parent`, але від самого `MyClass` **вже не можна успадкувати**. `MyClass` є фінальною ланкою в ієрархаті.
>
> * **Якщо `final` застосовано до батьківського класу:**
>
>     ```java
>     final class Parent { ... }
>     // public class Child extends Parent { ... } // ПОМИЛКА КОМПІЛЯЦІЇ
>     ```
>     **Ні, це неможливо.** Модифікатор `final` на класі **забороняє** будь-яке наслідування від нього.
>
> Отже, ключове слово `final` саме по собі не конфліктує з `extends`, але його розміщення визначає, чи є наслідування можливим.

#### **📌 Що перевіряють:**

*Дуже точне розуміння синтаксису мови та ролі ключового слова `final` у керуванні ієрархіями наслідування.*

* * *

### **14 ❓ Питання:**

Чи є наслідування від `java.lang.Object` прикладом IS-A відношення? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Технічно так, але з семантичної точки зору це дуже узагальнене твердження.**
>
> **Технічно:** Кожен клас, який ви створюєте, неявно успадковує `Object`, тому `YourClass` **IS-A** `Object`. Це дозволяє, наприклад, помістити будь-який об'єкт у `List<Object>`.
>
> **Семантично (з точки зору дизайну):**
>
> Відношення IS-A зазвичай передбачає **спеціалізацію**. `Dog IS-A Animal` означає, що Собака — це більш конкретний, спеціалізований тип Тварини.
>
> Відношення `YourClass IS-A Object` є настільки **загальним**, що воно практично не несе ніякої семантичної інформації про ваш клас, крім того, що це "щось, що є об'єктом у Java".
>
> Успадкування від `Object` — це в першу чергу **технічна вимога платформи** для забезпечення спільної базової функціональності (як `equals()`, `hashCode()`, `toString()`) для всіх об'єктів та можливості роботи з ними через єдиний кореневий тип.
>
> Отже, хоча формально це IS-A, на практиці, говорячи про дизайн, ми маємо на увазі більш значущі IS-A зв'язки у нашій власній доменній моделі.

#### **📌 Що перевіряють:**

*Здатність розрізняти технічні аспекти реалізації мови та семантичні аспекти проектування програмного забезпечення. Глибоке розуміння ролі класу `Object`.*

* * *

### **15 ❓ Питання:**

Розгляньте проблему "Коло-Еліпс". Чому успадкування `Circle` від `Ellipse` може бути поганою ідеєю, незважаючи на те, що математично коло є різновидом еліпса? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична ілюстрація **порушення Принципу підстановки Лісков (LSP)**, яка показує, що ієрархія наслідування в програмуванні повинна базуватися на **поведінці**, а не тільки на математичній чи логічній класифікації.
>
> **Проблема:**
>
> * `Еліпс` має дві незалежні осі (наприклад, `radiusX` та `radiusY`). Його інваріант: `setRadiusX()` не повинно змінювати `radiusY`, і навпаки.
> * `Коло` має лише один радіус. Його інваріант: `radiusX` завжди дорівнює `radiusY`.
>
> **Якщо ми робимо `class Circle extends Ellipse`:**
>
> 1. Клас `Circle` повинен якось забезпечити свій інваріант (`width == height`).
> 2. Для цього він буде змушений перевизначити сеттери:
>
>     ```java
>     @Override
>     public void setRadiusX(int radiusX) {
>         super.setRadiusX(radiusX);
>         super.setRadiusY(radiusX); // Порушуємо поведінку батька!
>     }
>     ```
>
>
> **Чому це ламає LSP:**
>
> * Код, який працює з посиланням на `Ellipse`, очікує, що, викликавши `ellipse.setRadiusX(10)` та `ellipse.setRadiusY(20)`, він отримає еліпс з осями 10 і 20.
> * Але якщо ми підставимо туди об'єкт `Circle`, після виклику `setRadiusY(20)` його ширина теж стане 20. Поведінка змінилася, і програма, що покладалася на незалежність осей, може працювати некоректно.
>
> **Висновок:**
> Незважаючи на математичне IS-A, `Circle` **не може бути поведінковим підтипом** `Ellipse`, оскільки він накладає нові обмеження, що порушують контракт батька. У цьому випадку наслідування є поганим вибором. Кращим рішенням було б мати спільний інтерфейс `Shape` або взагалі не пов'язувати їх наслідуванням.

#### **📌 Що перевіряють:**

*Експертне розуміння Принципу підстановки Лісков та того, що наслідування в ООП — це про **поведінковий контракт**, а не лише про класифікацію з реального світу.*
