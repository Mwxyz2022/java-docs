---
layout: default
title: "Ключове слово this"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 10
---

# Ключове слово this

* короткий опис: Спеціальне ключове слово, що посилається на поточний об'єкт класу, зсередини якого викликається метод або доступне поле.

* поняття: `this`, `this()`, `this.fieldName`.

* приклади: `this.name = name;` (розрізнення поля і параметра), `this("param")` (виклик іншого конструктора в тому ж класі).

* ключові моменти: `this()` має бути першим рядком у конструкторі. Використовується для доступу до власних членів об'єкта.

* що важливо знати на співбесіді: Призначення `this`, відмінність від `super`.

### **✅ Офіційне визначення:**

Ключове слово **`this`** — це спеціальне посилання в Java, яке вказує на поточний об’єкт класу, зсередини якого викликається метод або доступне поле. Його можна використовувати для розрізнення параметрів і полів, виклику методів поточного обʼєкта, або передачі самого об’єкта як аргументу.

### **🧠 Простими словами:**

**`this`** — це як "я". Усередині обʼєкта **`User`** воно означає "цей конкретний користувач". Коли Java бачить **`this.name`**, вона знає: "мається на увазі поле **`name`** цього об'єкта".

---

## **📊 Використання `this` в Java:**

| Ситуація | Приклад | Опис |
| :---: | :---: | :---: |
| Доступ до полів обʼєкта | **`this.name = name;`** | Задаємо поле класу через поточний обʼєкт |
| Виклик методу поточного обʼєкта | **`this.printInfo();`** | Викликаємо метод у тому ж обʼєкті |
| Виклик конструктора класу | **`this("John", 25);`** | Викликаємо інший конструктор того ж класу |
| Передача поточного обʼєкта | **`otherMethod(this);`** | Передаємо поточний обʼєкт як аргумент |

---

## **Пояснення на пальцях (💡)**

* Якщо параметр конструктора (або методу) називається **`name`**, а у класі є поле з таким самим ім’ям, то **`this.name`** — це поле класу, а просто **`name`** — параметр.
* **`this()`** — викликає інший конструктор цього ж класу. Це зручно для повторного використання коду, особливо у конструкторах.
* **`this`** часто застосовують у патернах з ланцюжками викликів (fluent interface, builder pattern).

---

## **Корисні приклади (🧪)**

🔹 **Розрізнення полів і параметрів:**

```java
public class Person {
    private String name;

    public Person(String name) {
        this.name = name; // this.name -- поле класу, name -- параметр конструктора
    }
}
```

🔹 **Виклик іншого конструктора:**

```java
public class User {
    private String name;
    private int age;

    public User(String name) {
        this(name, 0); // виклик другого конструктора цього ж класу
    }

    public User(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```

🔹 **Передача поточного обʼєкта як аргумент:**

```java
class Printer {
    void print(Person person) {
        System.out.println(person.getName());
    }
}

class Person {
    private String name = "Alex";

    String getName() {
        return name;
    }

    void show() {
        Printer p = new Printer();
        p.print(this); // передаємо сам об'єкт
    }
}
```

---

## **Порівняння (⚖️)**

| Ключове слово | Вказує на… | Доступ до |
| :---: | :---: | :---: |
| **`this`** | Поточний обʼєкт | Поля, методи, інші конструктори того ж класу |
| **`super`** | Батьківський обʼєкт | Поля, методи, конструктор суперкласу |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **`this`** \= "я" — поточний обʼєкт

* **`this.name`** -> моє поле **`name`**
* **`this()`** -> мій інший конструктор
* **`this.print()`** -> мій метод **`print`**

---

## **🟩 Підсумок:**

Ключове слово **`this`** дозволяє працювати з поточним екземпляром класу: викликати його методи, отримувати доступ до полів, передавати себе як аргумент. Воно зручно і необхідне, особливо коли потрібно розрізняти поля і параметри, або будувати ланцюжки викликів у конструкторах чи методах.

---

### **🔗 Перевірені джерела для глибшого вивчення:**

* [**Oracle Docs: The this keyword**](https://docs.oracle.com/javase/tutorial/java/javaOO/this.html)

Добре, я ознайомився з конспектом. На співбесідах на цю тему важливо чітко розрізняти **дві форми `this`**, розуміти **різницю між `this` та `super`**, знати про **неможливість використання `this` у статичному контексті** та вміти пояснити, як `this` використовується для вирішення практичних завдань.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке ключове слово `this` в Java і на що воно посилається?

> #### **💬 Відповідь:**
>
> Ключове слово **`this`** — це спеціальне посилання, яке вказує на **поточний екземпляр (об'єкт) класу**, всередині якого воно використовується.
>
> Простими словами, коли метод або конструктор виконується, `this` — це посилання на той самий об'єкт, для якого цей метод або конструктор був викликаний.
>
> Воно використовується для того, щоб код всередині класу міг отримати доступ до своїх власних полів та методів, а також передавати посилання на самого себе в інші методи. `this` є доступним лише у не-статичному контексті.

#### **📝 Аналогія:**

Якщо ви говорите про себе: "Я йду в магазин", слово "Я" — це і є `this`. Воно вказує на конкретну людину (об'єкт), яка зараз говорить. Кожна людина, кажучи "я", має на увазі себе.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `this` як посилання на поточний об'єкт.*

* * *

### **2 ❓ Питання:**

Назвіть основні сценарії використання `this`.

> #### **💬 Відповідь:**
>
> Існує три основні сценарії використання `this` в Java:
>
> 1. **Розрізнення полів класу та параметрів методу (або локальних змінних):**
>     * Це найпоширеніший випадок. Якщо параметр методу або конструктора має таку саму назву, як і поле класу, `this` використовується для явного звернення саме до поля класу.
>     * **Приклад:** `this.name = name;`
>
> 2. **Виклик іншого конструктора того ж класу (`this()`):**
>     * Використовується у формі `this(...)` з конструктора для виклику іншого перевантаженого конструктора цього ж класу. Це дозволяє уникнути дублювання коду ініціалізації.
>
> 3. **Передача посилання на поточний об'єкт:**
>     * Об'єкт може передати посилання на самого себе як аргумент в інший метод або повернути його з методу.
>     * **Приклад:** `someExternalService.register(this);`

#### **📌 Що перевіряють:**

*Знання основних практичних застосувань `this`. Кандидат повинен назвати щонайменше перші два сценарії.*

* * *

### **3 ❓ Питання:**

Поясніть, навіщо потрібен синтаксис `this.name = name;` у конструкторах або сеттерах.

> #### **💬 Відповідь:**
>
> Цей синтаксис необхідний для вирішення **конфлікту імен (name shadowing)**, коли ім'я параметра методу або конструктора збігається з іменем поля класу.
>
> **Проблема:**
> Якщо не використовувати `this`, компілятор буде вважати, що ви працюєте з **найближчою за областю видимості змінною**, якою є параметр методу.
>
> **Приклад без `this`:**
>
> ```java
> public class Person {
>     private String name;
>     public Person(String name) {
>         name = name; // Ви присвоюєте параметр самому собі!
>                       // Поле класу 'this.name' залишається null.
>     }
> }
> ```
>
> Цей код є логічною помилкою.
>
> **Правильне рішення з `this`:**
>
> ```java
> public class Person {
>     private String name;
>     public Person(String name) {
>         // this.name - посилається на поле класу
>         // name - посилається на параметр конструктора
>         this.name = name;
>     }
> }
> ```
>
> Використання `this.` явно вказує компілятору, що ліва частина присвоєння — це **поле поточного об'єкта**, а права — параметр.

#### **📌 Що перевіряють:**

*Розуміння того, як Java вирішує конфлікти імен, та практичне застосування `this` для розрізнення полів та параметрів.*

* * *

### **4 ❓ Питання:**

Що таке виклик `this()` і які правила його використання?

> #### **💬 Відповідь:**
>
> `this()` — це спеціальний синтаксис для виклику **іншого конструктора того ж класу** зсередини конструктора. Це називається **ланцюжком конструкторів (constructor chaining)**.
>
> **Правила використання:**
>
> 1. Виклик `this(...)` повинен бути **першим рядком** у тілі конструктора. Перед ним не може бути жодної іншої інструкції.
> 2. В одному конструкторі можна використати лише **один** виклик `this()`.
>
> **Навіщо це потрібно:**
>
> Це дозволяє **уникнути дублювання коду** ініціалізації. Замість того, щоб копіювати логіку, менш специфічні конструктори можуть викликати більш специфічний, передаючи значення за замовчуванням.
>
> **Приклад:**
>
> ```java
> public class Rectangle {
>     private int width, height;
>
>     public Rectangle() {
>         this(1, 1); // Викликає другий конструктор, створюючи квадрат 1x1 за замовчуванням
>     }
>
>     public Rectangle(int width, int height) {
>         this.width = width;
>         this.height = height;
>     }
> }
> ```
>
> Тут конструктор без аргументів повторно використовує логіку повного конструктора.

#### **📌 Що перевіряють:**

*Знання правил виклику конструкторів та вміння використовувати `this()` для написання чистого коду без повторень.*

* * *

### **5 ❓ Питання:**

У чому різниця між `this` та `super`?

> #### **💬 Відповідь:**
>
> Обидва є ключовими словами, що посилаються на об'єкти, але на різні в контексті наслідування.
>
> | Характеристика | `this` | `super` |
> | :---: | :---: | :---: |
> | **На що посилається** | На **поточний екземпляр** класу. | На **батьківську частину** поточного екземпляра (на його **суперклас**). |
> | **Конструктори (`()`)** | `this()` викликає **інший конструктор того ж класу**. | `super()` викликає **конструктор батьківського класу**. |
>
| **Доступ до членів** | `this.field` або `this.method()` звертається до членів **поточного класу**. | `super.field` або `super.method()` звертається до **успадкованих** членів батьківського класу, навіть якщо вони перевизначені/приховані.|
| **Де доступне** | У будь-якому не-статичному методі або конструкторі.| Лише у не-статичних методах та конструкторах **дочірнього класу**. |

#### **📌 Що перевіряють:**

*Чітке розмежування двох ключових понять, що регулюють взаємодію між об'єктами в ієрархії наслідування.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати `this` у статичному методі або статичному блоці? Чому?

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Спроба використати `this` у статичному контексті призведе до **помилки компіляції**: `non-static variable this cannot be referenced from a static context`.
>
> **Пояснення:**
>
> * Ключове слово `this` посилається на **поточний екземпляр (об'єкт)** класу.
>
> * **Статичні методи та блоки** належать **класу**, а не конкретному об'єкту. Вони виконуються в контексті класу і **не прив'язані до жодного екземпляра**. Вони існують навіть тоді, коли не створено жодного об'єкта цього класу.
>
> Оскільки в статичному контексті немає поняття "поточного об'єкта", то і посилання `this` просто не існує і не має сенсу.
>
> **Аналогічно:** `super` також не можна використовувати в статичному контексті, бо він теж пов'язаний з екземпляром.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між `static` (клас) та `instance` (об'єкт) контекстами.*

* * *

### **7 ❓ Питання:**

Чи можна одночасно використовувати `this()` і `super()` в одному конструкторі? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.** Це призведе до **помилки компіляції**.
>
> **Причина:**
>
> І `this(...)`, і `super(...)` мають строге правило: вони **повинні бути першою інструкцією** в тілі конструктора.
>
> Оскільки в конструкторі може бути лише одна "перша інструкція", неможливо задовольнити цю вимогу для обох викликів одночасно.
>
> **Навіщо таке правило:**
> Це правило гарантує, що об'єкт ініціалізується в чітко визначеному порядку. Спочатку повинна бути повністю ініціалізована батьківська частина об'єкта (через `super()`), або має бути викликаний інший конструктор, який, у свою чергу, в кінці свого ланцюжка викличе `super()`. Не можна починати ініціалізацію полів поточного класу, поки батьківська частина ще не готова.

#### **📌 Що перевіряють:**

*Знання строгих правил ініціалізації в конструкторах.*

* * *

### **8 ❓ Питання:**

Наведіть приклад, коли `this` використовується для передачі посилання на поточний об'єкт в інший метод.

> #### **💬 Відповідь:**
>
> Це поширений сценарій, коли об'єкт потрібно "зареєструвати" десь або коли інший об'єкт повинен знати про нього.
>
> **Приклад: Патерн "Спостерігач" (Observer)**
>
> У нас є `EventSource` (джерело подій) та `EventListener` (слухач). Слухач повинен зареєструвати сам себе у джерела.
>
> ```java
> interface Listener {
>     void onEvent(String event);
> }
>
> class EventSource {
>     private List<Listener> listeners = new ArrayList<>();
>
>     public void registerListener(Listener listener) {
>         listeners.add(listener);
>     }
>     // ...
> }
>
> class MyListener implements Listener {
>     private EventSource source;
>
>     public MyListener(EventSource source) {
>         this.source = source;
>         // Передаємо посилання на поточний об'єкт MyListener
>         // для реєстрації у EventSource
>         source.registerListener(this);
>     }
>
>     @Override
>     public void onEvent(String event) {
>         System.out.println("Подію отримано: " + event);
>     }
> }
> ```
>
> У конструкторі `MyListener` ключове слово `this` використовується для передачі посилання на щойно створений екземпляр `MyListener` у метод `registerListener()` об'єкта `EventSource`.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та практичних сценаріїв, де потрібно передавати посилання на поточний об'єкт.*

* * *

### **9 ❓ Питання:**

Як `this` використовується для реалізації патерну "Builder" або "Fluent Interface"? ⭐️

> #### **💬 Відповідь:**
>
> У цих патернах `this` використовується для створення **ланцюжків викликів методів (method chaining)**.
>
> **Ідея:**
> Кожен метод, який налаштовує стан об'єкта (у випадку білдера) або виконує дію, **повертає посилання на поточний об'єкт** (`return this;`).
>
> Це дозволяє викликати наступний метод на результаті попереднього, створюючи лаконічний та читабельний код.
>
> **Приклад реалізації Builder:**
>
> ```java
> public class User {
>     // ... поля ...
>
>     public static class Builder {
>         private String name;
>         private int age;
>
>         public Builder withName(String name) {
>             this.name = name;
>             return this; // Повертаємо поточний білдер
>         }
>
>         public Builder withAge(int age) {
>             this.age = age;
>             return this; // Повертаємо поточний білдер
>         }
>
>         public User build() {
>             return new User(this);
>         }
>     }
> }
> ```
>
> **Як це використовується:**
>
> ```java
> User user = new User.Builder()
>                 .withName("John")
>                 .withAge(30)
>                 .build();
> ```
>
> Кожен виклик `.with...()` повертає той самий об'єкт `Builder`, дозволяючи продовжувати ланцюжок.

#### **📌 Що перевіряють:**

*Знання сучасних патернів проектування та того, як ключове слово `this` є їхньою невід'ємною частиною.*

* * *

### **10 ❓ Питання:**

Чи завжди `this` вказує на об'єкт того класу, в якому він написаний? Розгляньте випадок наслідування. ⭐️

> #### **💬 Відповідь:**
>
> **Так, `this` завжди вказує на об'єкт того класу, екземпляр якого був створений**, а не на клас, в якому код з `this` було написано. Це ключова властивість для поліморфізму.
>
> **Приклад з наслідуванням:**
>
> ```java
> class Parent {
>     public void whoAmI() {
>         // getClass() викликається на поточнному об'єкті (this)
>         System.out.println("this is an instance of: " + this.getClass().getName());
>     }
> }
>
> class Child extends Parent {
> }
>
> public class Main {
>     public static void main(String[] args) {
>         Parent p = new Parent();
>         p.whoAmI();
>
>         Child c = new Child();
>         c.whoAmI();
>     }
> }
> ```
>
> **Вивід:**
>
> ```
> this is an instance of: Parent
> this is an instance of: Child
> ```
>
> У другому випадку, хоча метод `whoAmI()` реалізовано в класі `Parent`, він викликається для об'єкта `Child`. Тому всередині цього методу `this` посилається на об'єкт `Child`, і `this.getClass()` повертає `Child.class`.
>
> `this` завжди вказує на фактичний об'єкт часу виконання.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як `this` працює в ієрархії наслідування і що воно пов'язане з об'єктом, а не з класом, де було написано код.*

* * *

### **11 ❓ Питання:**

Чи можна присвоїти `this` значення `null`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> `this` є **фінальним (final)** посиланням. Компілятор не дозволяє змінювати значення `this`.
>
> `this` — це не звичайна змінна, а ключове слово, яке JVM автоматично ініціалізує посиланням на поточний об'єкт під час його створення. Ви не можете втручатися в цей процес і переприсвоювати `this`.
>
> Спроба написати `this = null;` або `this = someOtherObject;` призведе до **помилки компіляції**.
>
> Це логічно: `this` представляє сам об'єкт, з якого виконується код. Об'єкт не може "перестати бути самим собою".

#### **📌 Що перевіряють:**

*Розуміння `this` як спеціального, незмінного посилання.*

* * *

### **12 ❓ Питання:**

Яким чином `this` використовується в анонімних внутрішніх класах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> В анонімних класах існує **два різних `this`**:
>
> 1. **`this` (простий):** Посилається на **екземпляр самого анонімного класу**.
>
> 2. **`OuterClassName.this`:** Посилається на **екземпляр зовнішнього (оточуючого) класу**, в методі якого було створено анонімний клас.
>
> Це дозволяє зсередини анонімного класу отримати доступ як до його власних членів (якщо вони є), так і до членів об'єкта, що його створив.
>
> **Приклад:**
>
> ```java
> public class Outer {
>     private String outerField = "I am outer";
>
>     public void createInner() {
>         // Створюємо анонімний Runnable
>         new Thread(new Runnable() {
>             private String innerField = "I am inner";
>
>             @Override
>             public void run() {
>                 // this посилається на об'єкт Runnable
>                 System.out.println(this.innerField);
>
>                 // Outer.this посилається на об'єкт Outer
>                 System.out.println(Outer.this.outerField);
>             }
>         }).start();
>     }
> }
> ```
>
> Ця можливість є однією з причин, чому не-`static` внутрішні класи можуть призводити до витоків пам'яті: вони утримують неявне посилання на свій зовнішній екземпляр.

#### **📌 Що перевіряють:**

*Просунуті знання про роботу з внутрішніми та анонімними класами, включаючи особливості `this` у цьому контексті.*

* * *

### **13 ❓ Питання:**

Яка небезпека пов'язана з передачею `this` з конструктора до того, як об'єкт буде повністю ініціалізовано? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це є **небезпечною практикою**, відомою як **"втеча `this`" (escaping `this`)**. Вона може призвести до того, що інший потік отримає посилання на **не повністю ініціалізований об'єкт**.
>
> **Проблема:**
>
> 1. Ви передаєте `this` з конструктора в інший об'єкт (наприклад, реєструєте його як слухача).
> 2. Конструктор вашого класу ще не завершив свою роботу, і деякі його поля ще не отримали свої фінальні значення.
> 3. Інший об'єкт в іншому потоці одразу ж отримує посилання і починає викликати методи вашого, ще "недоствореного" об'єкта.
> 4. Це може призвести до читання значень за замовчуванням (`0` або `null`) замість ініціалізованих, що спричиняє `NullPointerException` або некоректну поведінку.
>
> **Приклад:**
>
> ```java
> public class LeakingThis {
>     private final int value;
>
>     public LeakingThis(EventSource source) {
>         // НЕБЕЗПЕЧНО: передаємо this до того, як поле 'value' буде ініціалізовано
>         source.registerListener(this);
>
>         // Ініціалізація відбувається ПІСЛЯ передачі посилання
>         this.value = 100;
>     }
> }
> ```
>
> Якщо інший потік одразу після `registerListener` викличе метод, що читає `value`, він побачить `0`.
>
> **Як уникнути:**
>
> * Не передавайте `this` з конструктора.
> * Використовуйте статичний фабричний метод, який спочатку створить об'єкт, а потім зареєструє його.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння проблем багатопотоковості та життєвого циклу об'єкта. Це питання для перевірки досвіду розробки надійних багатопотокових систем.*

* * *

### **14 ❓ Питання:**

У чому різниця в семантиці `super.getClass()` та `this.getClass()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання-пастка. Незважаючи на різний синтаксис, **результат `super.getClass()` та `this.getClass()` буде абсолютно ідентичним.**
>
> **Пояснення:**
>
> 1. Метод `getClass()` оголошений у класі `Object` як **`public final`**.
>
> 2. Оскільки він `final`, його **не можна перевизначити**.
>
> 3. `getClass()` — це не-статичний метод, який завжди повертає **фактичний тип об'єкта часу виконання (runtime type)**.
>
> 4. І `this`, і `super` стосуються **одного й того ж об'єкта**, що виконується. `this` — це весь об'єкт, `super` — його батьківська частина. Але об'єкт — один.
>
> 5. Коли ви викликаєте `super.getClass()`, це не означає "дай мені клас батька". Оскільки `getClass()` не може бути перевизначено, виклик через `super` все одно "проходить" до єдиної фінальної реалізації цього методу, яка успадкована від `Object` і працює з поточним об'єктом.
>
> **Приклад:**
>
> ```java
> class Parent {}
> class Child extends Parent {}
>
> // всередині методів Child:
> this.getClass() // поверне Child.class
> super.getClass() // також поверне Child.class
> ```
>
> Отримати клас батька можна лише за допомогою `this.getClass().getSuperclass()`.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як `final` методи та динамічне зв'язування взаємодіють з `super`. Це дуже тонкий нюанс, що відрізняє експерта.*

* * *

### **15 ❓ Питання:**

Поясніть, що таке `this` у контексті лямбда-виразів. На що він посилається? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Поведінка `this` у лямбда-виразах кардинально **відрізняється** від його поведінки в анонімних внутрішніх класах.
>
> **У лямбда-виразі `this` є лексичним.**
>
> Це означає, що `this` всередині лямбди посилається на **`this` оточуючого (зовнішнього) контексту**. Лямбда **не створює** власну область видимості для `this`. Вона "захоплює" `this` з того місця, де вона була оголошена.
>
> **Порівняння:**
>
> * **Анонімний клас:**
>     `this` всередині анонімного класу посилається на **екземпляр самого анонімного класу**.
>
> * **Лямбда-вираз:**
>     `this` всередині лямбди посилається на **екземпляр класу, що містить лямбду**.
>
> **Приклад:**
>
> ```java
> public class LambdaThisExample {
>     private String name = "Outer Class";
>
>     public Runnable createRunnable() {
>         // У цьому контексті `this` вказує на об'єкт LambdaThisExample
>
>         // Лямбда "захоплює" цей `this`
>         return () -> System.out.println("this.name = " + this.name);
>     }
>
>     public static void main(String[] args) {
>         LambdaThisExample obj = new LambdaThisExample();
>         Runnable r = obj.createRunnable();
>         r.run(); // Виведе "this.name = Outer Class"
>     }
> }
> ```
> Ця відмінність є однією з ключових при виборі між лямбдою та анонімним класом. Якщо вам потрібно створити новий "я", який можна передати (як у слухачах), ви будете використовувати анонімний клас. Якщо вам потрібен доступ до стану зовнішнього об'єкта, лямбда є більш лаконічним варіантом.

#### **📌 Що перевіряють:**

*Глибокі знання про лямбда-вирази, введені в Java 8, та їхню семантичну відмінність від старих конструкцій, як-от анонімні класи.*
