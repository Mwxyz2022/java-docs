---
layout: default
title: "Інтерфейси Comparable, Comparator — Сортування, кастомна логіка порівняння"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 17
---

# Інтерфейси Comparable, Comparator — Сортування, кастомна логіка порівняння

* короткий опис: Розбір інтерфейсів `Comparable` (для природного порядку об'єкта) та `Comparator` (для зовнішньої, кастомної логіки сортування).

* поняття: `Comparable`, `compareTo()`, `Comparator`, `compare()`, Природний порядок, Кастомний порядок.

* приклади: Реалізація `compareTo()` для сортування за віком, використання `Comparator.comparing()` для сортування за ім'ям.

* ключові моменти: `Comparable` – об'єкт сортує себе; `Comparator` – зовнішній об'єкт сортує інші. Використовуються для `TreeSet`, `Collections.sort()`.

* що важливо знати на співбесіді: Різниця, коли що використовувати, контракт `compareTo()`.

### **✅ Офіційне визначення:**

* **Comparable\<T\>** — інтерфейс, який дозволяє об'єктам самостійно визначати логіку свого порівняння (природний порядок).
* **Comparator\<T\>** — інтерфейс для створення зовнішньої логіки порівняння обʼєктів, незалежно від їх класу.

### **🧠 Простими словами:**

* **Comparable** — обʼєкт сам знає, як себе порівнювати: "Я хочу бути впорядкованим за імʼям".
* **Comparator** — сторонній сортувальник: ти задаєш логіку порівняння без змін у класі.

---

## **📊 Порівняння Comparable vs Comparator:**

| Ознака | Comparable | Comparator |
| :---: | :---: | :---: |
| **Місце реалізації** | У самому класі | Окремий клас або лямбда |
| **Метод** | **`compareTo(T other)`** | **`compare(T o1, T o2)`** |
| **Кількість стратегій** | Лише одна (вбудована) | Можна створити багато різних |
| **Collections.sort()** | Без аргументу | З компаратором |
| **TreeSet / TreeMap** | Автоматично використовує | Можна явно задати |

---

## **Пояснення на пальцях (💡)**

* **Comparable**: клас сам вирішує, як його екземпляри порівнюються (наприклад, сортувати людей за віком).
* **Comparator**: створюється ззовні й дозволяє порівнювати одним способом сьогодні, іншим — завтра, не чіпаючи сам клас.

---

## **Корисні приклади (🧪)**

### **🔹 Comparable:**

```java
class Person implements Comparable<Person> {
    String name;
    int age;
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
    @Override
    public int compareTo(Person other) {
        return this.age - other.age;
    }
}
```

### **🔹 Comparator:**

```java
class PersonNameComparator implements Comparator<Person> {
    public int compare(Person p1, Person p2) {
        return p1.name.compareTo(p2.name);
    }
}
```

### **🔹 Використання:**

```java
List<Person> people = new ArrayList<>();
people.add(new Person("Alice", 30));
people.add(new Person("Bob", 25));

Collections.sort(people); // Сортує за age (Comparable)
Collections.sort(people, new PersonNameComparator()); // За name

// Або лямбдою:
Collections.sort(people, (p1, p2) -> p1.name.compareTo(p2.name));
```

---

## **Порівняння (⚖️)**

| Критерій | Comparable | Comparator |
| :---: | :---: | :---: |
| **Впроваджується** | Класом | Окремо |
| **Гнучкість** | ❌ Лише один спосіб | ✅ Будь-яка кількість |
| **Порівняння** | **`this.compareTo(other)`** | **`compare(obj1, obj2)`** |
| **TreeSet / TreeMap** | Працює напряму | Передається зовні |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Comparable** \= "Я сам знаю, як себе порівнювати"
* **Comparator** \= "Ти скажи, як мене порівнювати"

📘 Підказка:

* **`compareTo()`** — “я проти нього”
* **`compare()`** — “він проти нього”

---

## **🟩 Підсумок:**

* **Comparable** визначає "природний" порядок об'єкта (всередині класу).
* **Comparator** дозволяє створювати кілька різних стратегій порівняння ззовні.
* Вони — фундамент для сортування в Java, і обидва мають своє місце у хорошому дизайні.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs: Comparable**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/lang/Comparable.html)
* [**Oracle Docs: Comparator**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Comparator.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейси `Comparable` та `Comparator` і яка їхня головна мета?

> #### **💬 Відповідь:**
>
> `Comparable` та `Comparator` — це два ключових функціональних інтерфейси в Java, які визначають **стратегію порівняння об'єктів**. Вони є основою для сортування об'єктів у колекціях (`Collections.sort()`, `Arrays.sort()`) та для роботи впорядкованих структур даних (`TreeSet`, `TreeMap`).
>
> * **`Comparable<T>`:**
>   * **Призначення:** Визначає **"природний порядок"** для об'єктів певного класу. Логіка порівняння є **внутрішньою** властивістю самого об'єкта.
>   * **Як реалізується:** Клас **сам реалізує** цей інтерфейс (`implements Comparable<MyClass>`) і надає реалізацію методу `compareTo()`.
>
> * **`Comparator<T>`:**
>   * **Призначення:** Визначає **зовнішню, кастомну логіку** порівняння. Логіка порівняння є **окремою** від об'єктів, які вона порівнює.
>   * **Як реалізується:** Створюється **окремий клас** або **лямбда-вираз**, що реалізує інтерфейс `Comparator<MyClass>` і надає реалізацію методу `compare()`.

#### **📝 Аналогія:**

* **`Comparable`:** Люди вміють самі порівнювати себе за віком. Це їхня "природна" властивість.
* **`Comparator`:** Ви запрошуєте зовнішнього експерта, який може порівняти тих самих людей, але за іншим критерієм — наприклад, за зростом, прізвищем або зарплатнею. Ви можете мати багато таких "експертів".

#### **📌 Що перевіряють:**

*Базове розуміння того, що `Comparable` — це про внутрішній порядок, а `Comparator` — про зовнішній. Це фундаментальна відмінність.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між методами `compareTo()` та `compare()`?

> #### **💬 Відповідь:**
>
> Хоча вони обидва служать для порівняння, їхні сигнатури та контекст виклику різні.
>
> **`int compareTo(T other)` (з інтерфейсу `Comparable`):**
>
> * **Аргументи:** Приймає **один** аргумент — інший об'єкт для порівняння.
> * **Як викликається:** Викликається на **поточному об'єкті** (`this`). Логіка: "порівняй мене (`this`) з іншим (`other`)".
> * **Реалізація:** Знаходиться всередині класу, об'єкти якого порівнюються.
>
> **`int compare(T o1, T o2)` (з інтерфейсу `Comparator`):**
>
> * **Аргументи:** Приймає **два** аргументи — об'єкти, які потрібно порівняти.
> * **Як викликається:** Викликається на об'єкті-компараторі, а об'єкти для порівняння передаються як параметри. Логіка: "порівняй `o1` з `o2`".
> * **Реалізація:** Знаходиться в окремому класі або лямбда-виразі.

#### **📝 Мнемоніка:**

* `compareTo()` → **Я проти НЬОГО**.
* `compare()` → **ВІН проти НЬОГО**.

#### **📌 Що перевіряють:**

*Знання сигнатур та контексту виклику ключових методів. Кандидат не повинен їх плутати.*

* * *

### **3 ❓ Питання:**

Що означає контракт методу `compareTo()`? Що повинні повертати значення < 0, 0, > 0?

> #### **💬 Відповідь:**
>
> Контракт методу `compareTo(T other)` є строгим і важливим для коректної роботи алгоритмів сортування. Він порівнює поточний об'єкт (`this`) з іншим (`other`).
>
> **Метод повинен повернути:**
>
> * **Від'ємне ціле число (наприклад, -1):** якщо поточний об'єкт (`this`) є **меншим** за `other`.
> * **Нуль (`0`):** якщо об'єкти **рівні** (з точки зору порядку сортування).
> * **Додатне ціле число (наприклад, 1):** якщо поточний об'єкт (`this`) є **більшим** за `other`.
>
> **Важливе правило:**
> `sgn(x.compareTo(y)) == -sgn(y.compareTo(x))` (де `sgn` — це знак числа). Якщо поміняти об'єкти місцями, знак результату має змінитися.
>
> **Типова реалізація:**
>
> ```java
> // Сортування за цілим числом
> return this.age - other.age;
>
> // Сортування за рядком (делегування методу compareTo() класу String)
> return this.name.compareTo(other.name);
> ```

#### **📌 Що перевіряють:**

*Знання контракту `compareTo()`. Кандидат повинен чітко розуміти, що означають різні значення, які повертає метод.*

* * *

### **4 ❓ Питання:**

Коли слід використовувати `Comparable`, а коли — `Comparator`?

> #### **💬 Відповідь:**
>
> Вибір залежить від ситуації та вимог до гнучкості.
>
> **Використовуйте `Comparable`, коли:**
>
> 1. Існує один, очевидний **"природний порядок"** для об'єктів цього класу.
>     * **Приклад:** Для класу `Person` сортування за прізвищем або ID є природним. Для `Date` — хронологічний порядок.
> 2. Цей порядок є основним і буде використовуватися найчастіше.
> 3. Ви **можете змінювати** вихідний код класу, щоб додати `implements Comparable`.
>
> **Використовуйте `Comparator`, коли:**
>
> 1. Вам потрібно **кілька різних стратегій сортування** для одного й того ж класу.
>     * **Приклад:** Сортувати об'єкти `Person` і за іменем, і за віком, і за зарплатою.
> 2. Ви **не можете змінити** вихідний код класу, об'єкти якого ви хочете сортувати (наприклад, це клас зі сторонньої бібліотеки).
> 3. Вам потрібна "одноразова" логіка сортування, яку зручно визначити за допомогою **лямбда-виразу** або анонімного класу.
>
> **Загальна рекомендація:** Якщо є очевидний природний порядок, реалізуйте `Comparable`. Для всіх інших кастомних логік сортування використовуйте `Comparator`.

#### **📌 Що перевіряють:**

*Здатність приймати архітектурні рішення та обирати правильний інструмент для конкретної задачі.*

* * *

### **5 ❓ Питання:**

Як відсортувати список об'єктів, якщо їхній клас не реалізує `Comparable`? Напишіть приклад з використанням лямбда-виразу.

> #### **💬 Відповідь:**
>
> Якщо клас не реалізує `Comparable`, спроба викликати `Collections.sort(list)` призведе до помилки компіляції (або runtime, якщо список не типізований).
>
> У цьому випадку потрібно надати **зовнішню логіку сортування**, передавши `Comparator` як другий аргумент у метод `Collections.sort()`.
>
> Починаючи з Java 8, найзручніше це робити за допомогою **лямбда-виразу**.
>
> **Приклад:**
>
> У нас є клас `Car`, який не реалізує `Comparable`, і ми хочемо відсортувати список машин за роком випуску.
>
> ```java
> class Car {
>     private String model;
>     private int year;
>     // конструктор, геттери...
> }
>
> List<Car> cars = new ArrayList<>();
> cars.add(new Car("Audi", 2018));
> cars.add(new Car("BMW", 2015));
> cars.add(new Car("Tesla", 2021));
>
> // Використовуємо лямбда-вираз, який реалізує інтерфейс Comparator<Car>
> // (c1, c2) -> c1.getYear() - c2.getYear() є реалізацією методу compare(Car c1, Car c2)
> Collections.sort(cars, (c1, c2) -> c1.getYear() - c2.getYear());
>
> // або, що ще краще, використовуючи статичні методи Comparator
> cars.sort(Comparator.comparingInt(Car::getYear));
> ```
>
> Використання `Comparator.comparingInt` є більш читабельним і безпечним щодо переповнення, ніж пряма різниця.

#### **📌 Що перевіряють:**

*Практичні навички роботи з `Comparator` та сучасним синтаксисом Java 8 (лямбди, посилання на методи).*

* * *

### **6 ❓ Питання:**

Що означає "узгодженість `compareTo()` з `equals()`"? Чому це важливо? ⭐️

> #### **💬 Відповідь:**
>
> Узгодженість `compareTo()` з `equals()` означає дотримання такого правила:
>
> **`(x.compareTo(y) == 0)` повинно бути еквівалентним `(x.equals(y))`**.
>
> Простими словами, якщо два об'єкти рівні з точки зору порядку сортування, вони повинні бути рівними і з логічної точки зору, і навпаки.
>
> **Чому це важливо:**
>
> Хоча це не є строгою вимогою, її **настійно рекомендується дотримуватися**, оскільки впорядковані колекції, такі як **`TreeSet` та `TreeMap`**, покладаються на цей контракт.
>
> * `TreeSet` та `TreeMap` використовують `compareTo()` (або `compare()`) для визначення **унікальності** та **розташування** елементів, а **не** `.equals()`.
> * Якщо цей контракт порушено, поведінка цих колекцій стає **непередбачуваною**.
>
> **Приклад порушення:**
> Клас `Person` реалізує `compareTo()` на основі `age`, а `equals()`/`hashCode()` — на основі `id`.
>
> ```java
> Person p1 = new Person(1, "John", 25);
> Person p2 = new Person(2, "Jane", 25);
> // p1.equals(p2) -> false (різні id)
> // p1.compareTo(p2) -> 0 (однаковий вік)
>
> Set<Person> set = new TreeSet<>();
> set.add(p1);
> set.add(p2);
>
> System.out.println(set.size()); // Виведе 1!
> ```
>
> `TreeSet` вважатиме `p2` дублікатом `p1`, оскільки їх `compareTo()` повернув 0, і не додасть другий об'єкт.

#### **📌 Що перевіряють:**

*Розуміння тонких аспектів взаємодії між різними контрактами (`compareTo`, `equals`) та їхній вплив на роботу стандартних колекцій.*

* * *

### **7 ❓ Питання:**

Назвіть переваги використання статичних методів-помічників `Comparator`, таких як `comparing()`, `thenComparing()`, `nullsFirst()`. ⭐️

> #### **💬 Відповідь:**
>
> Починаючи з Java 8, інтерфейс `Comparator` було збагачено багатьма статичними та `default` методами, які значно спрощують створення складних компараторів.
>
> **Переваги:**
>
> 1. **Читабельність:** Код стає значно чистішим та більш декларативним.
>     `Comparator.comparing(Person::getName)` є набагато зрозумілішим, ніж `(p1, p2) -> p1.getName().compareTo(p2.getName())`.
>
> 2. **Ланцюжковість та композиція (`thenComparing()`):** Легко створювати компаратори з кількома рівнями сортування.
>
>     ```java
>     // Сортувати за прізвищем, а потім за іменем
>     Comparator.comparing(Person::getLastName).thenComparing(Person::getFirstName);
>     ```> Це значно простіше, ніж писати громіздку логіку з `if-else` всередині одного `compare`.
>
> 3. **Безпечна обробка `null` (`nullsFirst()` / `nullsLast()`):** Надають простий спосіб визначити, де повинні знаходитися `null` елементи — на початку чи в кінці, уникаючи `NullPointerException`.
>
>     `Comparator.comparing(Person::getName, Comparator.nullsFirst(String::compareTo))`
>
> 4. **Спеціалізовані компаратори (`comparingInt`, `comparingDouble`, `comparingLong`):** Вони уникають автоупаковки (autoboxing), що може дати невеликий приріст продуктивності.
>
> 5. **Оборотне сортування (`reversed()`):** Легко отримати компаратор, що сортує у зворотному порядку.
>
> Ці методи є прикладом сучасного Fluent API і роблять роботу з компараторами значно приємнішою.

#### **📌 Що перевіряють:**

*Знання сучасного Java 8+ API. Кандидат повинен продемонструвати, що він вміє писати чистий, функціональний та ідіоматичний код.*

* * *

### **8 ❓ Питання:**

Як відсортувати список у зворотному порядку? Назвіть кілька способів. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Існує кілька способів, вибір залежить від контексту.
>
> **1. Використання `Collections.reverseOrder()`:**
>
> * Це найпростіший спосіб, якщо об'єкти реалізують `Comparable`. Метод `reverseOrder()` повертає готовий компаратор, що сортує у зворотному до природного порядку.
>
> ```java
> List<String> list = Arrays.asList("B", "A", "C");
> list.sort(Collections.reverseOrder()); // [C, B, A]
> ```
>
> **2. Використання методу `Comparator.reversed()` (з Java 8):**
>
> * Якщо у вас уже є компаратор, ви можете легко отримати його "зворотну" версію.
>
> ```java
> Comparator<Person> byName = Comparator.comparing(Person::getName);
> // ...
> people.sort(byName.reversed()); // Сортувати за іменем у зворотному порядку
> ```
>
> **3. "Обгортання" результату `compareTo`:**
>
> * Якщо ви пишете власний компаратор, можна просто поміняти операнди місцями або помножити результат на -1.
>
> ```java
> // Варіант A: міняємо місцями
> people.sort((p1, p2) -> p2.getName().compareTo(p1.getName()));
> // Варіант Б: множимо (менш рекомендовано)
> // people.sort((p1, p2) -> -p1.getName().compareTo(p2.getName()));
> ```
>
> **4. Використання `Collections.sort()` і потім `Collections.reverse()`:**
>
> * Можна спочатку відсортувати список у прямому порядку, а потім просто перевернути його.
>     `Collections.sort(list); Collections.reverse(list);`
> * Це менш ефективно, оскільки вимагає двох проходів по списку.

#### **📌 Що перевіряють:**

*Повноту знань стандартної бібліотеки та здатність обрати найефективніший та найчитабельніший спосіб для конкретної задачі.*

* * *

### **9 ❓ Питання:**

Уявіть, що ви реалізуєте `compareTo` для класу, що має кілька полів (наприклад, сортування за прізвищем, потім за іменем). Як би ви це реалізували ефективно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо потрібно реалізувати таку логіку всередині `compareTo`, важливо робити це послідовно і зупинятися, як тільки буде знайдено різницю.
>
> **Ефективна реалізація:**
>
> ```java
> public class Person implements Comparable<Person> {
>     private String lastName;
>     private String firstName;
>
>     // ...
>
>     @Override
>     public int compareTo(Person other) {
>         // 1. Спочатку порівнюємо за першим критерієм (прізвище)
>         int lastNameCompare = this.lastName.compareTo(other.lastName);
>
>         // 2. Якщо прізвища різні, одразу повертаємо результат
>         if (lastNameCompare != 0) {
>             return lastNameCompare;
>         }
>
>         // 3. Якщо прізвища однакові, порівнюємо за другим критерієм (ім'я)
>         return this.firstName.compareTo(other.firstName);
>     }
> }
> ```
>
> **Сучасний підхід з `Comparator` (з Java 8):**
>
> Хоча `compareTo` може мати лише одну реалізацію, якщо таку ж логіку потрібно було б реалізувати в `Comparator`, найкращим способом було б використання ланцюжка `thenComparing`:
>
> `Comparator.comparing(Person::getLastName).thenComparing(Person::getFirstName);`
>
> Це значно чистіше і менш схильне до помилок.

#### **📌 Що перевіряють:**

*Навички написання чистого та ефективного коду для реалізації складних правил порівняння.*

* * *

### **10 ❓ Питання:**

Чому реалізація `(o1, o2) -> o1.getAge() - o2.getAge()` може бути небезпечною? Коли вона може дати неправильний результат? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця реалізація, хоч і дуже поширена, може бути небезпечною через ризик **цілочисельного переповнення (integer overflow)**.
>
> **Проблема:**
>
> Уявіть, що ви порівнюєте два числа, одне з яких дуже велике додатне, а інше — дуже велике від'ємне.
>
> * `o1.getAge()` = `2_000_000_000` (`Integer.MAX_VALUE` близько 2.1 млрд)
> * `o2.getAge()` = `-1_000_000_000`
>
> Спроба обчислити `o1.getAge() - o2.getAge()`:
> `2_000_000_000 - (-1_000_000_000)` = `3_000_000_000`
>
> Цей результат **перевищує `Integer.MAX_VALUE`**, і відбудеться **переповнення**. Результат стане **від'ємним числом**, хоча насправді `o1` більше за `o2`, і ми очікували додатне число.
>
> Це "зламає" логіку сортування, і алгоритм буде поводитися некоректно.
>
> **Безпечні альтернативи:**
>
> 1. **Використання `if-else` (найнадійніший ручний спосіб):**
>
>     ```java
>     if (o1.getAge() > o2.getAge()) return 1;
>     if (o1.getAge() < o2.getAge()) return -1;
>     return 0;
>     ```>
> 2. **Використання статичних методів `Integer.compare()` або `Long.compare()`:**
>
>     ```java
>     return Integer.compare(o1.getAge(), o2.getAge());
>     ```>
> 3. **Використання `Comparator.comparingInt()`:**
>
>     `Comparator.comparingInt(Person::getAge)`
>
> Ці підходи є безпечними щодо переповнення.

#### **📌 Що перевіряють:**

*Глибокі знання про обмеження примітивних типів та написання надійного коду, що враховує граничні випадки.*

* * *

### **11 ❓ Питання:**

Поясніть, що таке `Comparator.naturalOrder()` та `Comparator.reverseOrder()`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це два статичні методи-помічники, додані в Java 8, які повертають готові компаратори для сортування об'єктів, що реалізують `Comparable`.
>
> * **`Comparator.naturalOrder()`:**
>   * Повертає компаратор, який сортує об'єкти в їхньому **природному порядку**, визначеному методом `compareTo()`.
>   * Це корисно, коли вам потрібен `Comparator`, але ви хочете використати вже існуючу логіку `Comparable`. Наприклад, для передачі в методи, які очікують саме `Comparator`.
>     `list.stream().sorted(Comparator.naturalOrder())...`
>
> * **`Comparator.reverseOrder()`:**
>   * Повертає компаратор, який сортує об'єкти у **зворотному до їхнього природного порядку**.
>   * Це більш сучасна та елегантна заміна для `Collections.reverseOrder()`.
>     `list.stream().sorted(Comparator.reverseOrder())...`
>
> Обидва методи корисні при роботі зі `Stream API`, де для методів типу `sorted()` може знадобитися явний `Comparator`.

#### **📌 Що перевіряють:**

*Знання сучасного Java 8 Stream API та його інтеграції з `Comparator`.*

* * *

### **12 ❓ Питання:**

Що таке **стабільне сортування (stable sort)**, і чи гарантують `Collections.sort()` та `Arrays.sort()` стабільність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Стабільне сортування** — це властивість алгоритму сортування, яка гарантує, що **порядок рівних елементів** (тих, для яких `compare()` або `compareTo()` повертає `0`) **не буде змінено** після сортування.
>
> **Приклад:**
> У вас є список об'єктів: `(A, 5), (C, 3), (B, 5)`.
> Ви сортуєте його за другим полем (числом).
>
> * **Стабільне сортування** гарантовано дасть: `(C, 3), (A, 5), (B, 5)` (порядок A і B зберігся).
> * **Нестабільне сортування** може дати і `(C, 3), (A, 5), (B, 5)`, і `(C, 3), (B, 5), (A, 5)`.
>
> **Чи гарантують це методи в Java?**
>
> **Так.**
> Специфікація Java для `Collections.sort()` та `Arrays.sort()` **вимагає**, щоб реалізація алгоритму сортування була **стабільною**.
>
> Внутрішньо вони зазвичай використовують **MergeSort** або **Timsort** (гібрид MergeSort та Insertion Sort), які є стабільними алгоритмами.
>
> Ця властивість є важливою, особливо при багатопрохідному сортуванні. Наприклад, ви можете відсортувати список спочатку за іменем, а потім (стабільно) за відділом. У результаті співробітники в кожному відділі залишаться відсортованими за іменем.

#### **📌 Що перевіряють:**

*Глибокі знання про властивості алгоритмів сортування, які використовуються в стандартній бібліотеці.*

* * *

### **13 ❓ Питання:**

Що робити, якщо потрібно відсортувати об'єкти, які можуть бути `null`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Спроба викликати `compareTo()` або `compare()` для `null` значень призведе до `NullPointerException`. Для безпечної обробки таких випадків існують спеціалізовані рішення.
>
> Найкращий спосіб — використовувати статичні методи-адаптери з `Comparator` (з Java 8):
>
> 1. **`Comparator.nullsFirst(Comparator<? super T> comparator)`:**
>     * Створює компаратор-обгортку, яка вважає `null` значення **меншими** за всі не-`null` значення.
>     * Усі `null` елементи опиняться на початку відсортованого списку.
>
> 2. **`Comparator.nullsLast(Comparator<? super T> comparator)`:**
>     * Створює компаратор-обгортку, яка вважає `null` значення **більшими** за всі не-`null` значення.
>     * Усі `null` елементи опиняться в кінці списку.
>
> **Приклад:**
>
> ```java
> List<String> names = new ArrayList<>(Arrays.asList("C", null, "A", "B"));
>
> // Сортуємо за природним порядком, з null на початку
> names.sort(Comparator.nullsFirst(Comparator.naturalOrder()));
>
> // Результат: [null, A, B, C]
> ```
>
> **Ручна реалізація (до Java 8):**
>
> Довелося б писати складну логіку з перевірками на `null` всередині `compare()`:
>
> ```java
> if (o1 == o2) return 0;
> if (o1 == null) return -1; // null менший
> if (o2 == null) return 1;
> return o1.compareTo(o2);
> ```
>
> Методи з Java 8 значно спрощують цю задачу.

#### **📌 Що перевіряють:**

*Навички написання надійного коду, що обробляє `null`, та знання сучасних API, що спрощують цю задачу.*

* * *

### **14 ❓ Питання:**

Чи можна реалізувати `Comparator` для порівняння об'єктів різних, але пов'язаних типів (наприклад, з спільного суперкласу)? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Це досягається за допомогою **wildcard'ів (`?`)** у визначенні типу компаратора.
>
> **Приклад:**
>
> Припустимо, у нас є ієрархія `Animal` ← `Dog`, `Cat`. Ми хочемо мати один компаратор, що може порівнювати будь-яких тварин (наприклад, за вагою).
>
> ```java
> // Comparator<? super T> означає, що компаратор може приймати
> // будь-який тип, що є T або його суперкласом.
>
> // Цей компаратор може порівнювати Dog, Cat, Animal - будь-що,
> // що має метод getWeight()
> Comparator<Animal> byWeight = Comparator.comparingInt(Animal::getWeight);
>
> List<Dog> dogs = ...;
> List<Cat> cats = ...;
>
> dogs.sort(byWeight); // Працює
> cats.sort(byWeight); // Працює
> ```
>
> Аналогічно, можна використовувати **wildcards** для створення компаратора, що може порівнювати елементи, які є підтипами чогось.
>
> `Comparator<Shape>` може бути використаний для сортування `List<Circle>`, якщо `Circle extends Shape`.
>
> Це важливий аспект для написання **гнучких та універсальних** компараторів, які можна повторно використовувати в різних частинах коду.

#### **📌 Що перевіряють:**

*Глибокі знання дженериків, включаючи wildcards, та їхнє застосування разом з `Comparable`/`Comparator`.*

* * *

### **15 ❓ Питання:**

Чому клас, який реалізує `Comparable`, повинен перевизначати `equals()`, щоб забезпечити узгодженість, але клас, що реалізує `Comparator`, не повинен? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про **відповідальність та семантику**.
>
> **`Comparable`:**
>
> * Його метод `compareTo()` визначає **"природний порядок"**, який є **внутрішньою властивістю** об'єкта.
> * Якщо `a.compareTo(b) == 0` (об'єкти рівні для сортування), але `a.equals(b)` є `false` (об'єкти нерівні логічно), це створює **семантичний конфлікт**. Поведінка впорядкованих колекцій, як `TreeSet`, стає непередбачуваною. Тому контракт `(a.compareTo(b) == 0) == a.equals(b)` є **дуже бажаним**.
>
> **`Comparator`:**
>
> * Його метод `compare(a, b)` визначає **зовнішню, довільну стратегію** порівняння. Компаратор **не є частиною стану** об'єктів, які він порівнює.
> * Ми можемо мати багато різних компараторів для одного класу, і вони можуть вважати різні об'єкти "рівними" для цілей сортування.
> * Наприклад, `Comparator` за роком випуску для `Car` вважатиме всі машини 2020 року рівними. Це не означає, що ці об'єкти рівні за `equals()`.
> * Специфікація `Comparator.equals(Object obj)` чітко говорить, що він повинен повертати `true` **тільки тоді, коли інший об'єкт є компаратором, що накладає такий самий порядок**. Це про рівність **компараторів**, а не об'єктів, які вони порівнюють.
> * **Перевизначати `equals` для `Comparator` майже ніколи не потрібно**. Особливо для лямбда-виразів, у яких немає власного поняття рівності.
>
> **Висновок:** Узгодженість `compareTo` та `equals` стосується **внутрішньої логіки класу**. `Comparator` є **зовнішньою утилітою**, і його логіка `equals` не пов'язана з об'єктами, які він сортує.

#### **📌 Що перевіряють:**

*Експертне розуміння семантики та контрактів цих двох інтерфейсів. Кандидат повинен розуміти, "хто за що відповідає".*
