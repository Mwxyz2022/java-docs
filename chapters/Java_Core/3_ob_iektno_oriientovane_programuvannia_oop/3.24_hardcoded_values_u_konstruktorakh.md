---
layout: default
title: "Hardcoded values у конструкторах"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 24
---

# Hardcoded values у конструкторах

* короткий опис: Hardcoded values – це жорстко зафіксовані значення (рядки, числа тощо), які безпосередньо прописані в коді конструктора чи методу. Це антипатерн, що знижує гнучкість.

* поняття: Hardcoded, Антипатерн, Гнучкість, Тестування.

* приклади: `this.port = 8080;` (значення `8080` є hardcoded).

* ключові моменти: Погана практика, оскільки для зміни значення потрібна зміна та перекомпіляція коду. Порушує OCP.

* що важливо знати на співбесіді: Чому це антипатерн, як уникнути (використання параметрів, конфігурації).

### **✅ Офіційне визначення:**

Hardcoded values — це жорстко зафіксовані значення (рядки, числа тощо), які безпосередньо прописані в коді конструктора чи методу. У контексті конструкторів — це коли певні значення зашиті прямо в логіку ініціалізації об’єкта.

### **🧠 Простими словами:**

Це коли ти прямо в конструкторі прописуєш **`"admin"`** або **`8080`** чи **`"https://api.com"`** — і потім не можеш це легко змінити. Це як якщо твій чайник завжди гріє воду до 100°C — і ти не можеш це налаштувати.

---

## **Основне**

| Тип значення | Приклад у коді | Коментар |
| :---: | :---: | :---: |
| **Рядок (`String`)** | **`this.role = "ADMIN";`** | Неможливо змінити без зміни коду |
| **Число (`int`, `long`)** | **`this.port = 8080;`** | Не масштабується |
| **URL / ключ** | **`this.url = "https://api.example.com"`** | Порушення безпеки / ускладнення тестування |

---

## **Пояснення на пальцях (💡)**

Якщо ти жорстко прописав значення, ти створив залежність від коду.

Ти не можеш легко протестувати інші варіанти, не змінивши код класу.

У великому проєкті це перетворюється на хаос: замість одного місця для налаштувань — їх 50, і кожне треба редагувати вручну.

---

## **Корисні приклади (🧪)**

🔻 **Погано (hardcoded):**

```java
public class Server {
    private int port;

    public Server() {
        this.port = 8080; // ❌ жорстко зашито
    }
}
```

✅ **Добре (гнучко через параметр):**

```java
public class Server {
    private int port;

    public Server(int port) {
        this.port = port;
    }
}
```

✅ **І ще краще — через конфігурацію:**

```java
public class Config {
    public static final int DEFAULT_PORT = 8080;
}

new Server(Config.DEFAULT_PORT);
```

---

## **Порівняння (⚖️)**

| Підхід | Плюси | Мінуси |
| :---: | :---: | :---: |
| **Hardcoded у конструкторі** | Простий код | ❌ Неможливо змінити без редагування коду |
| **Параметри конструктора** | ✅ Гнучкий | Потрібно писати більше коду |
| **Зовнішня конфігурація** | ✅ Легке тестування, масштабування | Потрібна правильна організація |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘

**Hardcoding \= Hard to change**

Якщо значення "зашите" в коді, то його важко буде "витягти" без болю

---

## **🟩 Підсумок:**

Жорстко прописані значення в конструкторах — це антипатерн, який ускладнює масштабування, тестування та підтримку коду. Краще використовувати параметри, конфігурації або DI. Простіше змінити значення — легше оновити програму без ризику.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке "hardcoded values" (жорстко закодовані значення), і чому це вважається антипатерном?

> #### **💬 Відповідь:**
>
> **Hardcoded values** — це константні значення (числа, рядки, булеві значення тощо), які прописані безпосередньо у вихідному коді, замість того, щоб завантажуватися з зовнішньої конфігурації, передаватися як параметри або визначатися в одному централізованому місці.
>
> У контексті конструкторів це виглядає так:
>
> ```java
> public class DatabaseConnector {
>     private String url;
>     public DatabaseConnector() {
>         this.url = "jdbc:mysql://localhost:3306/prod_db"; // Hardcoded value
>     }
> }
> ```
>
> **Чому це антипатерн:**
>
> 1. **Низька гнучкість:** Для зміни такого значення (наприклад, для запуску на іншому середовищі) потрібно **змінювати вихідний код** і **перекомпілювати** програму.
> 2. **Погана тестованість:** Важко тестувати компонент в ізоляції. Наприклад, ви не можете легко підключити його до тестової бази даних, не змінивши клас.
> 3. **Дублювання коду та помилки:** Одне й те саме "магічне число" або рядок може бути розкидано по всьому проекту. Якщо його потрібно змінити, є ризик забути оновити його в якомусь місці.
> 4. **Порушення принципів дизайну:** Це порушує принцип **відкритості/закритості**, оскільки клас не можна розширити або змінити його поведінку без модифікації.

#### **📝 Аналогія:**

Це як припаяти кабель живлення вашого ноутбука безпосередньо до розетки в стіні. Він буде працювати, але тільки в цій одній кімнаті. Ви не зможете взяти його з собою, не "ламаючи стіну".

#### **📌 Що перевіряють:**

*Базове розуміння, що таке hardcode і чому це призводить до негнучкого коду.*

* * *

### **2 ❓ Питання:**

Які є альтернативи жорсткому кодуванню значень у конструкторах?

> #### **💬 Відповідь:**
>
> Існує кілька значно кращих підходів, які роблять код більш гнучким та підтримуваним.
>
> 1. **Передача значень через параметри конструктора:**
>     * Це найпростіший і найпоширеніший спосіб. Об'єкт отримує всі необхідні залежності та конфігурації під час свого створення.
>     `public Server(int port) { this.port = port; }`
>
> 2. **Використання констант:**
>     * Якщо значення є константою в межах програми, його слід винести в **`public static final`** поле, бажано в окремому конфігураційному класі. Це централізує константу в одному місці.
>     `private static final int DEFAULT_TIMEOUT = 5000;`
>
> 3. **Зовнішня конфігурація (найкращий варіант для середовищ):**
>     * Значення (URL-и, порти, ключі API) зберігаються у зовнішніх файлах (наприклад, `.properties`, `.yml`, `.xml`).
>     * Додаток під час запуску читає ці файли і передає значення у відповідні об'єкти.
>
> 4. **Впровадження залежностей (Dependency Injection):**
>     * Це просунутий підхід, коли спеціальний фреймворк (наприклад, Spring) сам читає конфігурацію і автоматично "впроваджує" потрібні значення в поля або конструктори об'єктів.
>
> Вибір залежить від контексту, але будь-який з цих підходів кращий за hardcode.

#### **📌 Що перевіряють:**

*Знання основних патернів конфігурації додатків. Кандидат повинен розуміти, як зробити компоненти гнучкими.*

* * *

### **3 ❓ Питання:**

Як жорстке кодування впливає на юніт-тестування? Наведіть приклад.

> #### **💬 Відповідь:**
>
> Жорстке кодування робить **юніт-тестування в ізоляції майже неможливим**.
>
> Юніт-тест повинен перевіряти один "юніт" (зазвичай, один клас) ізольовано від його зовнішніх залежностей (бази даних, файлової системи, мережевих сервісів). Hardcode жорстко прив'язує клас до реальних, "важких" залежностей.
>
> **Приклад:**
>
> У нас є сервіс, який працює з реальним платіжним шлюзом.
>
> ```java
> public class PaymentService {
>     private final ApiClient apiClient;
>
>     public PaymentService() {
>         // Hardcoded URL прив'язує нас до реального сервісу
>         this.apiClient = new ApiClient("https://api.realsystem.com/pay");
>     }
>
>     public boolean processPayment(double amount) {
>         // ... цей метод відправляє реальний HTTP-запит
>         return apiClient.sendPayment(amount);
>     }
> }
> ```
>
> **Проблема при тестуванні:**
>
> * Ви не можете протестувати `PaymentService`, не відправляючи реальні запити на `realsystem.com`.
> * Тест стане повільним, ненадійним (залежить від мережі) і може коштувати реальні гроші.
> * Ви не можете легко протестувати сценарії помилок (наприклад, коли API недоступне).
>
> **Правильний підхід (з DI):**
>
> ```java
> public PaymentService(ApiClient apiClient) {
>     this.apiClient = apiClient;
> }
> ```
>
> Тепер у юніт-тесті ми можемо передати **mock-об'єкт** (`Mockito.mock(ApiClient.class)`) і повністю контролювати його поведінку, тестуючи логіку `PaymentService` в ізоляції.

#### **📌 Що перевіряють:**

*Розуміння того, як дизайн коду впливає на його тестованість. Це критично важлива навичка для сучасного розробника.*

* * *

### **4 ❓ Питання:**

Що таке "магічні числа" (magic numbers) і як вони пов'язані з hardcoding?

> #### **💬 Відповідь:**
>
> **"Магічне число"** — це числовий літерал, який з'являється в коді без будь-якого пояснення свого походження чи сенсу. Це є формою **hardcoding**.
>
> **Проблема:**
>
> Коли інший розробник (або ви самі через місяць) дивиться на код, незрозуміло, що це число означає.
>
> **Поганий приклад:**
>
> ```java
> if (user.getStatus() == 2) { // Що означає 2? "Активний"? "Заблокований"? "На модерації"?
>     // ...
> }
> double finalPrice = price * 1.2; // Що таке 1.2? Це ПДВ?
> ```
>
> **Як виправити:**
>
> Магічні числа слід замінювати на **іменовані константи** (`public static final`), що робить код самодокументованим.
>
> **Хороший приклад:**
>
> ```java
> public static final int STATUS_ACTIVE = 2;
> public static final double VAT_RATE = 1.2;
>
> if (user.getStatus() == STATUS_ACTIVE) {
>     // Тепер логіка зрозуміла
> }
> double finalPrice = price * VAT_RATE;
> ```
>
> Ще кращою альтернативою для статусів є використання **`enum`**.

#### **📌 Що перевіряють:**

*Знання поширених "кодових запахів" (code smells) та способів написання чистого, самодокументованого коду.*

* * *

### **5 ❓ Питання:**

Чому цей конструктор вважається погано спроектованим? Як його покращити?
`public User() { this.role = "USER"; this.active = true; }`

> #### **💬 Відповідь:**
>
> Цей конструктор є погано спроектованим через використання **hardcoded значень за замовчуванням**.
>
> **Проблеми:**
>
> * **Негнучкість:** Усі користувачі, створені через цей конструктор, завжди будуть мати роль "USER" та статус "активний". Немає ніякого способу створити, наприклад, адміністратора або неактивного користувача.
> * **Приховані залежності:** Логіка, що використовує цей клас, неявно очікує, що роль завжди буде "USER". Це ускладнює підтримку.
>
> **Як покращити:**
>
> 1. **Використовувати параметризований конструктор:**
>
>     ```java
>     public User(String role, boolean active) {
>         this.role = role;
>         this.active = active;
>     }
>     ```>     Це робить залежності явними та дозволяє створювати різні конфігурації об'єктів.
>
> 2. **Використовувати перевантаження та `this()` для значень за замовчуванням:**
>
>     Якщо значення за замовчуванням все ж потрібні, їх слід реалізувати через виклик більш загального конструктора.
>
>     ```java
>     public User() {
>         this("USER", true); // Делегування основному конструктору
>     }
>
>     public User(String role, boolean active) {
>         this.role = role;
>         this.active = active;
>     }
>     ```>
> 3. **Винести константи:** Значення `"USER"` краще винести у `public static final String` константу. Ще краще — використовувати `enum` для ролей.

#### **📌 Що перевіряють:**

*Здатність аналізувати код та пропонувати кращі дизайнерські рішення, що підвищують гнучкість.*

* * *

### **6 ❓ Питання:**

Коли використання hardcoded значень може бути прийнятним? ⭐️

> #### **💬 Відповідь:**
>
> Хоча в більшості випадків hardcode є антипатерном, існують ситуації, де він може бути прийнятним або навіть бажаним.
>
> **Сценарії прийнятного використання:**
>
> 1. **Справжні, незмінні математичні або фізичні константи:**
>     * Значення числа Пі, кількість днів у тижні.
>     `double circleArea = 3.14159 * r * r;` — хоча навіть тут краще використати `Math.PI`.
>
> 2. **Значення, які є частиною самого алгоритму:**
>     * Наприклад, значення за замовчуванням у внутрішній логіці, які не призначені для зміни. Розмір початкового буфера `new StringBuilder(16)`.
>
> 3. **Логи та повідомлення для налагодження:**
>     * Рядки, що використовуються лише для внутрішнього логування або виводу на консоль під час розробки, рідко потребують конфігурації.
>     `log.debug("User successfully authenticated");`
>
> 4. **Код у прототипах, скриптах або юніт-тестах:**
>     * У коді, який не є частиною основного додатка (наприклад, простий скрипт для одноразової задачі або тестові дані в юніт-тесті), hardcode може бути виправданим для спрощення.
>     `User testUser = new User("test", "test@test.com");`
>
> Головний критерій — **якщо значення є фундаментально незмінним і не залежить від середовища виконання, його можна жорстко закодувати**, але все одно краще винести в іменовану константу.

#### **📌 Що перевіряють:**

*Здатність до прагматичного мислення. Кандидат повинен розуміти, що не всі правила є абсолютними, і вміти розрізняти контексти.*

* * *

### **7 ❓ Питання:**

Розгляньте такий конструктор: `public Circle() { this.radius = 1.0; }`. Чи можна вважати `1.0` "магічним числом"? ⭐️

> #### **💬 Відповідь:**
>
> Так, формально `1.0` тут є **магічним числом**, оскільки його призначення не є очевидним без додаткового контексту.
>
> * Чому саме 1.0? Це одиничне коло за замовчуванням? Чи це мінімально можливий радіус?
>
> **Як це можна покращити:**
>
> Найкращим рішенням було б винести це значення в іменовану **приватну статичну константу**, яка б пояснювала його призначення.
>
> ```java
> public class Circle {
>     private static final double DEFAULT_RADIUS = 1.0;
>     private double radius;
>
>     public Circle() {
>         // Тепер намір очевидний
>         this.radius = DEFAULT_RADIUS;
>     }
>
>     public Circle(double radius) {
>         this.radius = radius;
>     }
> }
> ```
>
> Цей підхід має кілька переваг:
>
> * **Читабельність:** Код стає самодокументованим.
> * **Легкість зміни:** Якщо знадобиться змінити значення за замовчуванням, це робиться в одному місці.
> * **Уникнення помилок:** Ви не зможете випадково написати `1.0` в одному місці і `1` в іншому, створюючи непослідовність.
>
> Таким чином, навіть для простих значень за замовчуванням, використання іменованих констант є кращою практикою.

#### **📌 Що перевіряють:**

*Увагу до чистоти коду (clean code). Здатність побачити можливість для покращення навіть у, на перший погляд, простому коді.*

* * *

### **8 ❓ Питання:**

Як принцип інверсії контролю (IoC) допомагає уникнути hardcoding у конструкторах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Інверсія контролю (Inversion of Control)** — це принцип, згідно з яким створення та управління залежностями об'єкта виноситься **назовні**, у зовнішній контейнер або фреймворк. **Впровадження залежностей (Dependency Injection - DI)** є реалізацією цього принципу.
>
> **Як це допомагає уникнути hardcoding:**
>
> Замість того, щоб клас сам створював свої залежності або конфігурації всередині конструктора (що є формою hardcoding), він отримує їх **готовими** ззовні.
>
> * **Без IoC (hardcoding):**
>
>     ```java
>     public class MyService {
>         private final DatabaseConnection dbConnection;
>         public MyService() {
>             // Клас сам контролює свою залежність
>             this.dbConnection = new DatabaseConnection("jdbc:mysql://prod:3306");
>         }
>     }
>     ```>
> * **З IoC/DI (гнучко):**
>
>     ```java
>     public class MyService {
>         private final DatabaseConnection dbConnection;
>
>         // Контроль інвертовано: залежність приходить ззовні
>         public MyService(DatabaseConnection dbConnection) {
>             this.dbConnection = dbConnection;
>         }
>     }
>     ```
>
> Тепер конфігурація `DatabaseConnection` (включаючи її URL) знаходиться за межами `MyService`. Її може створювати і налаштовувати DI-фреймворк (наприклад, Spring), читаючи дані з файлів `.properties` або `.yml`. `MyService` стає незалежним від деталей конфігурації, що робить його гнучким та легким для тестування.

#### **📌 Що перевіряють:**

*Знання сучасних архітектурних патернів та їхню роль у побудові гнучких додатків. Розуміння того, як DI вирішує проблему hardcoding.*

* * *

### **9 ❓ Питання:**

Якби ви побачили `new User("admin", "root")` у коді, що проходить code review, які б ваші коментарі були? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Я б залишив кілька коментарів, оскільки цей рядок містить кілька "кодових запахів" та потенційних ризиків.
>
> 1. **Жорстко закодовані рядки:** `"admin"` та `"root"` — це магічні рядки.
>     * **Коментар:** "Ці значення є жорстко закодованими. Будь ласка, винесіть їх у константи (`public static final`) в окремий конфігураційний клас (наприклад, `AppConstants.DEFAULT_ADMIN_USERNAME`). Це централізує константи і зробить код більш читабельним."
>
> 2. **Потенційна проблема безпеки:**
>     * Якщо це облікові дані адміністратора, зберігати їх у вигляді відкритого тексту у вихідному коді — **серйозна вразливість**.
>     * **Коментар:** "Зберігання облікових даних у коді є небезпечним. Ці значення повинні завантажуватися із зовнішніх, захищених джерел (наприклад, environment variables, secret management systems), а паролі не повинні зберігатися у відкритому вигляді."
>
> 3. **Тестованість:**
>     * Код, що створює об'єкти з такими значеннями напряму, важко тестувати.
>     * **Коментар:** "Розгляньте можливість створення цього користувача через фабричний метод або отримання з DI-контейнера. Це дозволить легше підмінити його в юніт-тестах."
>
> В цілому, я б порекомендував повністю позбутися цього рядка, замінивши його на отримання конфігурації ззовні.

#### **📌 Що перевіряють:**

*Здатність аналізувати код на наявність проблем з безпекою, підтримкою та тестованістю. Практичні навички проведення code review.*

* * *

### **10 ❓ Питання:**

Яким чином `final` поля можуть неявно стимулювати кращий дизайн і зменшувати hardcoding у конструкторах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використання `final` для полів змушує розробника приймати рішення про ініціалізацію, що часто призводить до більш гнучкого дизайну.
>
> Якщо ви оголошуєте поле як `private final`, ви зобов'язані його ініціалізувати **рівно один раз**: або при оголошенні, або в конструкторі.
>
> Розглянемо `private final int port;`.
>
> * **Варіант 1 (Погано):** Ви можете зробити `this.port = 8080;` в конструкторі. Це буде `final`, але `hardcoded`.
>
> * **Варіант 2 (Краще):** Дуже часто, оскільки значення може знадобитися різне, розробник інтуїтивно приходить до того, щоб **передати його через параметр конструктора**, що є значно кращим дизайном.
>
>     ```java
>     public class Server {
>         private final int port;
>
>         // `final` змушує нас ініціалізувати порт тут
>         public Server(int port) {
>             this.port = port;
>         }
>     }
>     ```> Таким чином, `final` підштовхує до **"ін'єкції" залежностей через конструктор**, що є одним з найкращих способів уникнути hardcoding і створити незмінні або частково незмінні об'єкти. Клас стає менш відповідальним за отримання своїх залежностей і більше фокусується на своїй основній логіці.

#### **📌 Що перевіряють:**

*Розуміння неявних переваг використання `final` для покращення дизайну. Кандидат повинен бачити, як одні мовні конструкції заохочують використання інших, більш правильних.*

* * *

### **11 ❓ Питання:**

Чому цей код може вважатися крихким?
`public class ApiClient { private String baseUrl = "http://api.myservice.com/v1"; }`
Які проблеми можуть виникнути на різних етапах життєвого циклу продукту? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код є крихким, бо він жорстко прив'язує реалізацію до конкретного середовища та версії API.
>
> **Проблеми на різних етапах:**
>
> 1. **Розробка та тестування:**
>     * **Проблема:** Розробники, запускаючи код локально, будуть надсилати запити на реальний продакшн-сервер. Це може призвести до "забруднення" прод-даних тестовими записами. Юніт-тести, як вже було сказано, стають неможливими.
>
> 2. **CI/CD та середовища (Staging, UAT):**
>     * **Проблема:** Щоб розгорнути додаток на staging-середовищі, потрібно мати **іншу версію коду** з `baseUrl = "http://api.staging.myservice.com"`. Це означає необхідність підтримувати різні гілки коду або складні скрипти для заміни рядків під час білду, що є дуже поганою практикою.
>
> 3. **Продакшн та оновлення:**
>     * **Проблема:** Якщо команда, що розробляє API, випустить нову версію (`/v2`), вам доведеться **змінювати код, перекомпілювати та перезапускати** весь додаток. Це робить оновлення повільними та ризикованими.
>
> **Рішення:** `baseUrl` повинен бути винесений у **зовнішній конфігураційний файл**, який може бути різним для кожного середовища. Додаток повинен читати це значення під час старту.

#### **📌 Що перевіряють:**

*Розуміння повного життєвого циклу програмного забезпечення. Кандидат повинен мислити не тільки про написання коду, але й про його розгортання, тестування та підтримку.*

* * *

### **12 ❓ Питання:**

Чи можна вважати `this()` формою hardcoding? Поясніть. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, у певному сенсі `this()` з літералами **можна вважати формою hardcoding**, але більш **контрольованою та локалізованою**.
>
> Розглянемо цей приклад:
>
> ```java
> public User() {
>     this("Default User", 18);
> }
>
> public User(String name, int age) {
>     this.name = name;
>     this.age = age;
> }
> ```
>
> **В чому полягає hardcoding:**
>
> * Значення `"Default User"` та `18` є жорстко закодованими. Вони знаходяться прямо у вихідному коді.
>
> **Чому це краще, ніж пряме присвоєння:**
>
> * **Централізація:** Логіка ініціалізації не розкидана по кількох конструкторах, а зібрана в одному "основному" конструкторі (`User(String, int)`).
> * **Явний намір:** Виклик `this(...)` чітко показує, що це створення об'єкта зі **значеннями за замовчуванням**.
>
> **Як це можна покращити ще більше:**
> Винести ці "дефолтні" значення в **приватні статичні константи**.
>
> ```java
> private static final String DEFAULT_NAME = "Default User";
> private static final int DEFAULT_AGE = 18;
>
> public User() {
>     this(DEFAULT_NAME, DEFAULT_AGE);
> }
> ```
>
> Цей підхід є найкращим, оскільки він зберігає гнучкість `this()`, але усуває "магічні" значення, роблячи код самодокументованим і легким для зміни.
>
> Отже, хоча `this()` з літералами і є hardcoding'ом, це значно менше зло, ніж присвоєння `this.name = "Default User"` безпосередньо.

#### **📌 Що перевіряють:**

*Здатність бачити різні рівні "поганої практики". Кандидат повинен розуміти, що існують відтінки, і вміти пропонувати ідеальне рішення.*

* * *

### **13 ❓ Питання:**

Яким чином `switch` вирази (з Java 14) допомагають уникнути "магічних значень" у коді порівняно з `if-else`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це непрямий зв'язок, але він стосується чистоти коду. `switch` вирази, особливо при роботі з `enum`, роблять код більш декларативним і усувають необхідність у "магічних" значеннях, які часто з'являються в `if-else` ланцюжках.
>
> **Приклад з `if-else` та "магічними" рядками:**
>
> ```java
> String getGreeting(String role) {
>     if (role.equals("ADMIN")) {
>         return "Hello, Admin!";
>     } else if (role.equals("USER")) {
>         return "Hello, User!";
>     } else {
>         return "Hello, Guest!";
>     }
> }
> ```
>
> Тут `"ADMIN"` та `"USER"` — це hardcoded магічні рядки.
>
> **З `enum` та `switch`-виразом:**
>
> ```java
> enum Role { ADMIN, USER, GUEST }
>
> String getGreeting(Role role) {
>     return switch (role) {
>         case ADMIN -> "Hello, Admin!";
>         case USER -> "Hello, User!";
>         case GUEST -> "Hello, Guest!";
>         // `default` не потрібен, якщо всі enum покриті
>     };
> }
> ```
>
> **Як це допомагає:**
>
> 1. **Типобезпечність:** `enum` усуває магічні рядки, замінюючи їх на типізовані константи.
> 2. **Вичерпність:** `switch`-вираз **вимагає** від вас обробити всі можливі значення `enum`. Компілятор перевірить, що ви не забули жодної гілки. Це захищає від логічних помилок, якщо в майбутньому до `enum` буде додано нову роль. `if-else` такої перевірки не надає.
>
> Таким чином, `switch` вирази в поєднанні з `enum` є потужним інструментом проти hardcoding'у, оскільки вони роблять усі можливі варіанти явними та перевіреними на етапі компіляції.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови та того, як вони допомагають писати більш безпечний та виразний код.*

* * *

### **14 ❓ Питання:**

Як анотації `@Value` (наприклад, у Spring Framework) вирішують проблему hardcoding? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Анотація `@Value` у фреймворку Spring — це потужний механізм **впровадження залежностей (Dependency Injection)**, призначений спеціально для того, щоб **позбутися hardcoding конфігураційних значень**.
>
> **Як це працює:**
>
> 1. Ви оголошуєте поле у вашому класі (наприклад, `private String apiUrl;`).
> 2. Замість того, щоб присвоювати йому значення в коді, ви позначаєте його анотацією `@Value`.
> 3. В анотації ви вказуєте **ключ (placeholder)**, за яким потрібно знайти значення у зовнішній конфігурації.
>
>     ```java
>     @Value("${api.baseUrl}")
>     private String apiUrl;
>
>     @Value("${server.port:8080}") // Можна вказати значення за замовчуванням
>     private int port;
>     ```>
> 4. При запуску додатку Spring IoC-контейнер:
>     * Читає зовнішні конфігураційні файли (`application.properties` або `application.yml`).
>     * Знаходить значення за ключем (`api.baseUrl`).
>     * Автоматично "впроваджує" (injects) це значення у відповідне поле.
>
> **Переваги:**
>
> * **Повне відокремлення коду від конфігурації:** Код нічого не знає про конкретні значення.
> * **Гнучкість для середовищ:** Ви можете мати різні `.properties` файли для розробки, тестування та продакшену. Додаток автоматично підхопить потрібну конфігурацію.
> * **Централізоване управління:** Всі налаштування знаходяться в одному місці, а не розкидані по коду.
>
> Це є сучасною, найкращою практикою для управління конфігураціями в Java-додатках.

#### **📌 Що перевіряють:**

*Знання принципів роботи популярних фреймворків (як Spring) та того, як вони вирішують класичні проблеми дизайну.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви не можете змінити клас, який має hardcoded значення в конструкторі. Який патерн проектування ви могли б використати, щоб "обійти" цю проблему і зробити використання цього класу більш гнучким? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний сценарій, коли ми маємо справу зі сторонньою або застарілою бібліотекою. Для вирішення цієї проблеми ідеально підходить патерн **Адаптер (Adapter)**.
>
> **Ідея патерну Адаптер:**
> Створити клас-обгортку (адаптер), який "адаптує" незручний інтерфейс одного класу до іншого, більш зручного для нас.
>
> **Як реалізувати:**
>
> 1. Припустимо, у нас є "проблемний" клас:
>
>     ```java
>     public final class LegacyApiClient {
>         public LegacyApiClient() {
>             // Незручний hardcode!
>             connect("http://legacy.api.com", 80);
>         }
>         // ...
>     }
>     ```> 2.  Ми створюємо наш власний **інтерфейс**, який визначає потрібний нам контракт.
>     ```java
>     public interface ModernApiClient {
>         void sendRequest(String data);
>     }
>     ```> 3.  Створюємо **клас-адаптер**, який реалізує наш інтерфейс і всередині себе **використовує (через композицію)** застарілий клас.
>     ```java
>     public class LegacyApiAdapter implements ModernApiClient {
>         // Проблема в тому, що ми не можемо змінити URL...
>         // В цьому випадку адаптер мало чим допоможе,
>         // якщо URL зашито в конструкторі без параметрів.
>     }
>     ```>
>
> *Виправлення:* Цей сценарій не вирішується Адаптером, якщо конструктор без параметрів. Адаптер допомагає, коли інтерфейси не збігаються.
>
> **Кращим рішенням буде патерн Декоратор або Проксі, але лише якщо ми можемо якось вплинути на поведінку. Якщо ж `LegacyApiClient` повністю закритий, і ми не можемо ні успадкувати, ні змінити його залежності, то рішення знаходиться поза кодом — наприклад, перенаправлення запитів на рівні мережі (DNS, проксі-сервер).**
>
> **Якщо ж клас такий:**
> `public LegacyApiClient(String url) { ... }`, а ми хочемо використовувати його з `default`-значенням, але не жорстко, то допоможе **Фабрика** або **Білдер**, які приховують цей дефолт, але можуть бути сконфігуровані для тестування.

#### **Остаточна відповідь:**

> **Найкращим рішенням для інкапсуляції роботи з таким негнучким класом буде патерн "Фабрика" (Factory Method) або "Абстрактна Фабрика" (Abstract Factory).**
>
> 1. Ми створюємо **інтерфейс** для клієнта, з яким хочемо працювати.
>     `interface ApiClient { void send(); }`
>
> 2. Створюємо **Фабрику**, яка буде відповідати за створення об'єктів.
>
>     ```java
>     public class ApiClientFactory {
>         public ApiClient createApiClient(String env) {
>             if ("test".equals(env)) {
>                 return new MockApiClient(); // для тестів
>             } else {
>                 // Якщо нам потрібно "обійти" hardcoded LegacyApiClient,
>                 // можливо, адаптер або проксі не допоможе.
>                 // Тут ми визнаємо, що для prod буде створено негнучкий клієнт
>                 // Але сам вибір відбувається гнучко!
>                 return new LegacyApiAdapter(new LegacyApiClient());
>             }
>         }
>     }
>     ```> В класі `LegacyApiAdapter` ми обгортаємо `LegacyApiClient` і реалізуємо інтерфейс `ApiClient`.
>
> **Суть:**
> Ми **ізолюємо** створення негнучкого об'єкта всередині фабрики. Наш основний код залежить від фабрики та інтерфейсу. Це дозволяє нам **підміняти реалізацію** для тестів (`MockApiClient`) або для різних середовищ, навіть якщо одна з реалізацій є "проблемною".
>
> Таким чином, ми не "виправляємо" hardcode, а **обмежуємо його вплив** на решту системи.

#### **📌 Що перевіряють:**

*Здатність застосовувати патерни проектування для вирішення реальних архітектурних проблем, таких як робота з успадкованим або негнучким кодом.*
