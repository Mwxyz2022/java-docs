---
layout: default
title: "Конструктори: default, з параметрами, this(), super()"
parent: "Об'єктно-орієнтоване програмування (ООП)"
grand_parent: "Java Core"
nav_order: 22
---

# Конструктори: default, з параметрами, this(), super()

* короткий опис: Конструктори – спеціальні методи для ініціалізації об'єкта під час його створення. Можуть бути `default` (без параметрів), з параметрами, використовувати `this()` для виклику іншого конструктора того ж класу, або `super()` для виклику конструктора батьківського класу.

* поняття: Конструктор, `default` конструктор, конструктор з параметрами, `this()`, `super()`.

* приклади: `User()`, `User(String name)`, `this("name", 0)`, `super("animal")`.

* ключові моменти: Конструктори не мають типу повернення. `this()` та `super()` повинні бути першим рядком.

* що важливо знати на співбесіді: Типи конструкторів, їх призначення, використання `this()` та `super()`.

### **✅ Офіційне визначення:**

**`Конструктор`** — це спеціальний метод класу, який викликається під час створення об’єкта і використовується для ініціалізації його стану (змінних). Конструктор має назву класу і не має типу повернення.

### **🧠 Простими словами:**

**`Конструктор`** — це “інструкція зі збирання” об’єкта. Коли ти створюєш новий **`User`**, Java спочатку викликає конструктор, щоб заповнити поля — ім’я, вік, email...

---

## **📊 Типи конструкторів у Java:**

| Тип | Приклад | Опис |
| :---: | :---: | :---: |
| **Default (за замовчуванням)** | **`public User() {}`** | Без параметрів. Java створює сама, якщо немає жодного свого конструктора |
| **З параметрами** | **`public User(String name) { ... }`** | Ініціалізує поля через аргументи |
| **Перевантаження** | Декілька конструкторів з різними параметрами | Дозволяє створювати об’єкти з різними даними |
| **`this()`** | **`this(значення);`** | Викликає інший конструктор цього ж класу |
| **`super()`** | **`super(значення);`** | Викликає конструктор батьківського класу |

---

## **Пояснення на пальцях (💡)**

* Якщо ти не створюєш конструктор — Java дає default constructor (порожній).
* Якщо створив хоч один конструктор — Java вже не додасть default.
* **`this()`** — дозволяє викликати інший конструктор у цьому ж класі.
* **`super()`** — викликає конструктор з суперкласу і має бути першим рядком у конструкторі підкласу.

---

## **Корисні приклади (🧪)**

🔹 **Конструктор за замовчуванням:**

```java
public class User {
    public User() {
        System.out.println("User created");
    }
}
```

🔹 **Конструктор з параметрами:**

```java
public class User {
    String name;

    public User(String name) {
        this.name = name;
    }
}
```

🔹 **`this()` для виклику іншого конструктора:**

```java
public class User {
    String name;
    int age;

    public User(String name) {
        this(name, 0); // Виклик іншого конструктора
    }

    public User(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```

🔹 **`super()` у підкласі:**

```java
class Person {
    public Person(String name) {
        System.out.println("Person: " + name);
    }
}

class Student extends Person {
    public Student() {
        super("Student"); // виклик конструктора батьківського класу
    }
}
```

---

## **Порівняння (⚖️)**

| Конструкція | Що робить | Коментар |
| :---: | :---: | :---: |
| **`this()`** | Викликає інший конструктор цього ж класу | Має бути першим рядком конструктора |
| **`super()`** | Викликає конструктор батьківського класу | Також перший рядок конструктора |
| **`User()`** | Створює об’єкт з дефолтним станом | Якщо є власні конструктори — Java не створює автоматично default |

---

## **🧠 Мнемоніка для запам’ятовування:**

📘

* **`this()`** — ВИКЛИК свого конструктора
* **`super()`** — ВИКЛИК батьківського конструктора
* Не забувай: обидва мають бути першими у конструкторі\!

---

## **🟩 Підсумок:**

Конструктори — це ключ до створення і ініціалізації об’єктів. Вони бувають прості, з параметрами, з логікою. Використовуючи **`this()`** і **`super()`**, можна створювати чіткі, багаторазові шляхи ініціалізації. Це основа ООП-архітектури і фундаментальний блок для будь-якого Java-класу.

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Oracle по конструкторам**](https://docs.oracle.com/javase/tutorial/java/javaOO/constructors.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке конструктор у Java і яка його головна мета?

> #### **💬 Відповідь:**
>
> **Конструктор** — це спеціальний метод, який викликається автоматично в момент створення нового об'єкта класу за допомогою оператора `new`.
>
> **Головна мета конструктора:**
> **Ініціалізувати об'єкт**, тобто надати початкові значення його полям. Він гарантує, що об'єкт буде створено у валідному та узгодженому стані.
>
> **Основні характеристики конструктора:**
>
> * Його назва **завжди збігається** з назвою класу.
> * Він **не має типу повернення**, навіть `void`.
> * Він може бути **перевантажений** (тобто клас може мати кілька конструкторів з різними параметрами).

#### **📝 Аналогія:**

Конструктор — це як інструкція по збірці для нового товару. Коли ви купуєте новий смартфон (`new Smartphone()`), конструктор "встановлює" початкові налаштування: яскравість екрану, мову, гучність.

#### **📌 Що перевіряють:**

*Базове розуміння того, що конструктор — це механізм ініціалізації, а не звичайний метод.*

* * *

### **2 ❓ Питання:**

Що таке конструктор за замовчуванням (default constructor)? Коли він створюється автоматично?

> #### **💬 Відповідь:**
>
> **Конструктор за замовчуванням** — це конструктор **без параметрів**.
>
> **Коли він створюється автоматично:**
>
> Компілятор Java **автоматично генерує** публічний конструктор без параметрів, але **тільки якщо ви не оголосили в класі жодного іншого конструктора**.
>
> **Приклад автоматичної генерації:**
>
> ```java
> public class User { } // Немає конструкторів
>
> // Можна створити об'єкт: new User();
> // бо компілятор додасть: public User() { super(); }
> ```
>
> **Коли він НЕ створюється:**
>
> Якщо ви додали в клас **хоча б один власний конструктор** (наприклад, з параметрами), компілятор **вже не буде** генерувати конструктор за замовчуванням.
>
> ```java
> public class User {
>     public User(String name) { /* ... */ } // Є власний конструктор
> }
>
> // new User(); // ПОМИЛКА КОМПІЛЯЦІЇ!
> // Конструктора без параметрів більше не існує.
> ```
>
> У цьому випадку, якщо вам потрібен конструктор без параметрів, ви повинні оголосити його явно.

#### **📌 Що перевіряють:**

*Знання правил автоматичної генерації конструктора. Це дуже поширене джерело помилок для початківців.*

* * *

### **3 ❓ Питання:**

У чому різниця між `this()` та `super()`?

> #### **💬 Відповідь:**
>
> Хоча синтаксис схожий і обидва використовуються лише в конструкторах, їхнє призначення кардинально різне.
>
> * **`this()` (Виклик іншого конструктора того ж класу):**
>   * Використовується для **"ланцюгового" виклику** перевантажених конструкторів **всередині одного класу**.
>   * **Мета:** Уникнути дублювання коду ініціалізації. Зазвичай, менш специфічні конструктори викликають більш специфічний.
>   * **Приклад:** `this("Guest", 0);`
>
> * **`super()` (Виклик конструктора батьківського класу):**
>   * Використовується для виклику конструктора **безпосереднього суперкласу**.
>   * **Мета:** Ініціалізувати успадковану частину об'єкта.
>   * **Приклад:** `super(name);`
>
> **Спільні правила:**
>
> 1. Обидва виклики (`this()` і `super()`) **повинні бути першою інструкцією** в тілі конструктора.
> 2. Як наслідок, їх **не можна використовувати разом** в одному конструкторі.

#### **📝 Мнемоніка:**

* `this()` → дивиться на "цьому ж поверсі" (в тому ж класі).
* `super()` → дивиться на "поверх вище" (в батьківському класі).

#### **📌 Що перевіряють:**

*Чітке розмежування двох основних механізмів ланцюжкових викликів конструкторів.*

* * *

### **4 ❓ Питання:**

Чи може конструктор бути `private`? Якщо так, то навіщо?

> #### **💬 Відповідь:**
>
> **Так, може.** Приватний конструктор **забороняє створення об'єктів цього класу ззовні** за допомогою оператора `new`. Це потужний інструмент для контролю над інстанціюванням.
>
> **Основні сценарії використання `private` конструктора:**
>
> 1. **Реалізація патерну Singleton:** Це класичний випадок. Приватний конструктор гарантує, що екземпляр класу може бути створений лише всередині самого класу, зазвичай через `public static` метод `getInstance()`. Це дозволяє створити лише один екземпляр на всю JVM.
>
> 2. **Реалізація патерну Factory Method:** Забороняє пряме створення об'єктів і змушує клієнтів використовувати статичні фабричні методи. Це дає більше гнучкості (наприклад, фабрика може повернути об'єкт з кешу або підклас).
>
> 3. **Створення утилітарних класів (Utility Classes):** Для класів, що містять лише `static` методи (як `java.lang.Math`), немає сенсу створювати їх екземпляри. Приватний конструктор робить це неможливим і явно виражає намір розробника.
>
> 4. **Реалізація патерну Builder:** Конструктор основного класу робиться приватним і приймає як аргумент об'єкт-білдер. Це гарантує, що об'єкт можна створити лише через правильно сконфігурований білдер.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та розуміння того, як мовні конструкції використовуються для їх реалізації.*

* * *

### **5 ❓ Питання:**

Поясніть, що таке "ланцюжок конструкторів" (constructor chaining) і покажіть приклад.

> #### **💬 Відповідь:**
>
> **Ланцюжок конструкторів** — це процес, при якому один конструктор викликає інший. Існує два типи ланцюжків:
>
> 1. **В межах одного класу:** За допомогою **`this()`**.
> 2. **Між дочірнім та батьківським класами:** За допомогою **`super()`**.
>
> **Мета:** Повторне використання коду ініціалізації та забезпечення правильного порядку ініціалізації.
>
> **Приклад комбінованого ланцюжка:**
>
> ```java
> class Person {
>     String name;
>     Person(String name) { this.name = name; }
> }
>
> class Employee extends Person {
>     String position;
>
>     Employee() {
>         // 1. Цей конструктор викликає інший конструктор свого ж класу
>         this("Guest", "Intern");
>     }
>
>     Employee(String name, String position) {
>         // 2. Цей конструктор викликає конструктор батьківського класу
>         super(name);
>         this.position = position;
>     }
> }
> ```
>
> Тут при виклику `new Employee()` запускається ланцюжок:
> `Employee()` → `Employee(String, String)` → `Person(String)` → `Object()`.

#### **📌 Що перевіряють:**

*Розуміння того, як `this()` та `super()` використовуються разом для побудови послідовної та чистої логіки ініціалізації.*

* * *

### **6 ❓ Питання:**

Що станеться, якщо не викликати `super()` у конструкторі дочірнього класу, а у батьківського класу немає конструктора без аргументів? ⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **помилки компіляції**.
>
> **Пояснення:**
>
> 1. Якщо в конструкторі дочірнього класу ви **не робите явного виклику** `super(...)`, компілятор Java **автоматично вставляє** виклик конструктора батька без аргументів — **`super()`**.
>
> 2. Якщо в батьківському класі **немає доступного конструктора без аргументів** (наприклад, є лише конструктор з параметрами), то компілятор не може виконати неявний виклик `super()`.
>
> 3. В такій ситуації він видасть помилку: `There is no default constructor available in 'ParentClass'`.
>
> **Приклад помилки:**
>
> ```java
> class Parent {
>     public Parent(String name) {} // Є лише конструктор з параметром
> }
>
> class Child extends Parent {
>     // Помилка тут! Компілятор не може вставити super(), бо його немає в Parent.
>     public Child() {}
> }
> ```
>
> **Як виправити:**
> Потрібно **явно викликати** існуючий конструктор батька:
> `public Child() { super("defaultName"); }`

#### **📌 Що перевіряють:**

*Знання правил взаємодії конструкторів в ієрархії. Це дуже поширена проблема на практиці.*

* * *

### **7 ❓ Питання:**

Що таке конструктор копіювання (copy constructor)? Напишіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> **Конструктор копіювання** — це спеціальний конструктор, який приймає як аргумент **інший об'єкт того ж самого класу** і створює новий об'єкт, копіюючи значення полів з об'єкта-аргумента.
>
> **Навіщо він потрібен:**
>
> Це простий та інтуїтивно зрозумілий спосіб створити копію (клон) існуючого об'єкта.
>
> **Приклад:**
>
> ```java
> public class User {
>     private String name;
>     private int age;
>
>     public User(String name, int age) {
>         this.name = name;
>         this.age = age;
>     }
>
>     // Конструктор копіювання
>     public User(User other) {
>         this.name = other.name;
>         this.age = other.age;
>     }
> }
> ```
>
> **Як використовувати:**
>
> ```java
> User original = new User("John", 30);
> // Створюємо точну копію
> User copy = new User(original);
> ```
>
> Важливо пам'ятати, що такий конструктор за замовчуванням виконує **поверхневе копіювання (shallow copy)**. Якщо поля є посиланнями на змінні об'єкти, потрібно реалізовувати **глибоке копіювання (deep copy)**, створюючи копії і для цих об'єктів.

#### **📌 Що перевіряють:**

*Знання поширених патернів проектування та способів реалізації клонування об'єктів.*

* * *

### **8 ❓ Питання:**

Чи може конструктор мати модифікатори `final`, `static` або `abstract`? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не може.** Жоден з цих модифікаторів не можна застосувати до конструктора.
>
> **Пояснення:**
>
> * **`final`:** Забороняє перевизначення. Конструктори **ніколи не успадковуються**, тому концепція перевизначення до них незастосовна. Отже, `final` для них не має сенсу.
>
> * **`static`:** Прив'язує член до класу, а не до об'єкта. Конструктор за своєю суттю призначений для **ініціалізації об'єкта**, тому він не може бути статичним.
>
> * **`abstract`:** Вимагає, щоб метод був реалізований у дочірньому класі. Конструктор не успадковується і повинен мати тіло для ініціалізації, тому він не може бути абстрактним.
>
> Спроба використати будь-який з цих модифікаторів для конструктора призведе до **помилки компіляції**. Конструктори можуть мати лише модифікатори доступу (`public`, `protected`, `private`, `default`).

#### **📌 Що перевіряють:**

*Розуміння семантики конструкторів та логічних обмежень, що на них накладаються.*

* * *

### **9 ❓ Питання:**

Опишіть порядок ініціалізації полів екземпляра, блоків ініціалізації та конструктора при створенні об'єкта. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Порядок ініціалізації членів екземпляра є строгим і відбувається наступним чином після того, як ланцюжок виклику конструкторів батьківських класів (`super()`) завершено:
>
> 1. **Ініціалізація полів при оголошенні:** Спочатку виконується ініціалізація полів, яким значення присвоєно безпосередньо при їх оголошенні.
> 2. **Виконання блоків ініціалізації екземпляра:** Потім виконуються **всі не-статичні блоки ініціалізації `{}`**.
> 3. **Виконання коду конструктора:** В останню чергу виконується код, що знаходиться безпосередньо в тілі самого конструктора.
>
> Важливо, що ініціалізація полів та блоки ініціалізації виконуються **в тому порядку, в якому вони написані в коді**.
>
> **Приклад:**
>
> ```java
> class InitOrder {
>     { System.out.println("Instance block 1"); }
>     private String name = "Default Name";
>     InitOrder() {
>         System.out.println("Constructor. Name = " + name);
>     }
>     { System.out.println("Instance block 2. Name = " + name); }
> }
> ```
>
> **Вивід при `new InitOrder()`:**
>
> ```
> Instance block 1
> Instance block 2. Name = Default Name
> Constructor. Name = Default Name
> ```
>
> Спочатку виконались блоки (перший, потім другий, причому `name` вже було ініціалізовано). І лише потім — конструктор.

#### **📌 Що перевіряють:**

*Детальне знання процесу ініціалізації об'єктів. Це важливо для розуміння того, чому код може поводитися неочікувано.*

* * *

### **10 ❓ Питання:**

Що таке "втеча `this` з конструктора" і чому це небезпечно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"Втеча `this`" (leaking `this`)** — це небезпечна ситуація, коли посилання на об'єкт, що створюється, стає доступним для іншого потоку **до того, як конструктор завершив свою роботу**.
>
> Це може статися, якщо всередині конструктора:
>
> * Передати `this` як аргумент в інший об'єкт (наприклад, зареєструвати його як слухача).
> * Запустити новий потік, який використовує `this`.
>
> **Чому це небезпечно:**
>
> Це створює **race condition**. Інший потік може отримати посилання і почати викликати методи на **не повністю ініціалізованому об'єкті**.
>
> * Його поля ще можуть мати значення за замовчуванням (`0`, `null`, `false`).
> * Навіть **`final` поля** можуть бути невидимими для іншого потоку до завершення конструктора, що руйнує гарантії Java Memory Model.
>
> **Приклад:**
>
> ```java
> public class LeakyConstructor {
>     private final String message;
>
>     public LeakyConstructor() {
>         new Thread(() -> {
>             // Цей код може виконатися ДО завершення конструктора.
>             // this.message може бути null!
>             System.out.println(this.message.toUpperCase());
>         }).start();
>
>         // Ініціалізація відбувається після запуску потоку
>         this.message = "Hello";
>     }
> }
> ```
>
> Такий код майже гарантовано призведе до `NullPointerException`.
>
> **Як уникнути:** Не передавайте `this` з конструктора. Використовуйте фабричні методи або окремий метод ініціалізації для реєстрації об'єкта після його повного створення.

#### **📌 Що перевіряють:**

*Глибоке розуміння проблем багатопотоковості, пов'язаних з життєвим циклом об'єкта. Це питання для розробників рівня Middle+ та вище.*

* * *

### **11 ❓ Питання:**

Чому `public static final` константа, ініціалізована значенням, яке може кинути `checked` виняток, вимагає статичного блоку, а не конструктора? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, коли і як ініціалізуються статичні члени та члени екземпляра.
>
> 1. **`public static final`** константа є **статичним** полем. Статичні поля **ініціалізуються один раз при завантаженні класу**, до створення будь-якого об'єкта.
> 2. **Конструктор** викликається лише при **створенні екземпляра (`new`)**. На момент завантаження класу жоден конструктор ще не викликався.
>
> Отже, ініціалізувати статичне поле в конструкторі неможливо — це призведе до помилки компіляції.
>
> Якщо ініціалізація статичної константи може кинути `checked` виняток, єдиний спосіб його обробити — це використати **статичний блок ініціалізації `static {}`**.
>
> **Правильний підхід:**
>
> ```java
> public class Config {
>     public static final Properties CONFIG_PROPS;
>
>     static {
>         try {
>             // Ініціалізація в статичному блоці з обробкою винятку
>             CONFIG_PROPS = loadProperties();
>         } catch (IOException e) {
>             throw new ExceptionInInitializerError(e);
>         }
>     }
>
>     private static Properties loadProperties() throws IOException {
>         // ... логіка читання з файлу
>     }
> }
> ```
>
> Якщо при завантаженні конфігурації виникає помилка, `ExceptionInInitializerError` "вбиває" ініціалізацію класу, що є правильною поведінкою.

#### **📌 Що перевіряють:**

*Чітке розмежування статичного контексту та контексту екземпляра. Навички обробки винятків у статичних ініціалізаторах.*

* * *

### **12 ❓ Питання:**

Чи можна викликати `abstract` метод з конструктора абстрактного класу? Якщо так, які можуть бути ризики? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, технічно можна**, і компілятор це дозволить. Однак, це вважається **дуже ризикованою та поганою практикою**.
>
> **Ризики:**
>
> Проблема аналогічна "втечі `this`". Виклик `abstract` методу з конструктора батьківського класу призводить до того, що виконується **метод дочірнього класу** (через поліморфізм) **до того, як конструктор дочірнього класу завершив свою роботу**.
>
> Це означає, що цей перевизначений метод буде працювати з **не повністю ініціалізованим об'єктом**.
>
> **Приклад:**
>
> ```java
> abstract class Parent {
>     Parent() {
>         // Викликаємо абстрактний метод. Буде викликано реалізація з Child.
>         initialize();
>     }
>     abstract void initialize();
> }
>
> class Child extends Parent {
>     private List<String> data;
>
>     Child() {
>         super();
>         // 'data' ініціалізується ПІСЛЯ виклику super() (і, відповідно, initialize())
>         this.data = new ArrayList<>();
>     }
>
>     @Override
>     void initialize() {
>         // Спроба використати поле 'data', яке на цей момент ще null!
>         System.out.println("Data size: " + data.size()); // NullPointerException!
>     }
> }
> ```
>
> Оскільки `initialize()` викликається до того, як конструктор `Child` присвоїть `new ArrayList<>()` полю `data`, виникає `NullPointerException`.
>
> **Висновок:** Уникайте виклику не-`private`, не-`final` (тобто, поліморфних) методів з конструкторів.

#### **📌 Що перевіряють:**

*Експертне розуміння порядку ініціалізації та небезпек, пов'язаних з поліморфізмом у конструкторах.*

* * *

### **13 ❓ Питання:**

Що виведе цей код і чому?
`public class Test { public Test() { this(10); } public Test(int i) { System.out.println(i); } public static void main(String[] args) { new Test(); } }`
⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Код виведе на екран **`10`**.
>
> **Пояснення:**
>
> 1. `new Test()` викликає **конструктор без параметрів** `Test()`.
> 2. Першою ( і єдиною) інструкцією в `Test()` є `this(10);`.
> 3. **`this(10)`** — це виклик **іншого, перевантаженого конструктора** того ж класу, який приймає `int` як параметр.
> 4. Виконується конструктор `Test(int i)` з аргументом `10`.
> 5. Він виводить значення `i` на екран, тобто, `10`.
>
> Цей код є правильною реалізацією **ланцюжка конструкторів (constructor chaining)** за допомогою `this()`, де конструктор за замовчуванням делегує свою роботу більш специфічному конструктору.

#### **📌 Що перевіряють:**

*Уміння прослідкувати ланцюжок викликів конструкторів з `this()`.*

* * *

### **14 ❓ Питання:**

Яка відмінність у послідовності ініціалізації, коли об'єкт створюється шляхом **клонування** (`clone()`), а не через конструктор? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це ключова відмінність, яку важливо знати. При створенні об'єкта через `clone()` **конструктори не викликаються**.
>
> **Послідовність при `clone()`:**
>
> 1. Метод `Object.clone()` створює **побітову копію** об'єкта в пам'яті. Він копіює значення всіх полів з оригінального об'єкта в новий.
> 2. Це **поверхневе копіювання (shallow copy)**. Якщо поля є посиланнями, копіюються лише посилання, а не самі об'єкти.
> 3. **Жодні блоки ініціалізації (`{}`) чи конструктори** ні для цього класу, ні для його батьків **не виконуються**.
>
> **Наслідки:**
>
> * `clone()` може бути швидшим, оскільки він уникає логіки конструкторів.
> * Але це також **дуже небезпечно**, якщо логіка ініціалізації та встановлення інваріантів знаходиться саме в конструкторах. Клонований об'єкт може бути створений в некоректному стані.
> * Через ці та інші проблеми (поверхневе копіювання, складний контракт) використання `Cloneable` та `clone()` вважається **поганою практикою** в сучасній Java.
>
> Набагато кращою альтернативою для створення копій є **конструктори копіювання** або **статичні фабричні методи**. Вони є більш явними, гнучкими та безпечними.

#### **📌 Що перевіряють:**

*Розуміння різних способів створення об'єктів та їхніх фундаментальних відмінностей. Знання про те, чому `clone()` є проблемним.*

* * *

### **15 ❓ Питання:**

Як `super()` взаємодіє з `try-catch` блоком? Чи можна обернути `super()` в `try-catch`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> Спроба обернути виклик `super()` (або `this()`) у блок `try-catch` призведе до **помилки компіляції**.
>
> **Причина:**
>
> Існує строге правило: **виклик `super()` або `this()` повинен бути самою першою інструкцією в тілі конструктора.**
>
> Конструкція `try { ... }` сама по собі є інструкцією. Отже, якщо ви напишете:
>
> ```java
> try {
>     super(); // ПОМИЛКА!
> } catch (Exception e) {}
> ```
>
> `super()` вже не буде першою інструкцією — першою буде `try`.
>
> **Чому таке обмеження існує:**
>
> 1. Якщо конструктор батьківського класу кидає `checked` виняток, дочірній конструктор **зобов'язаний його або обробити, або оголосити у своїй `throws` клаузі**.
> 2. **Але** якщо виклик `super()` є першим рядком, а `try-catch` починається лише після нього, то як можна обробити виняток, що кидається `super()`? Ніяк.
>
> Це обмеження виглядає як дилема, але воно вирішується тим, що конструктор дочірнього класу просто **повинен оголосити**, що він теж може кидати винятки з конструктора батька.
>
> ```java
> class Parent {
>     Parent() throws IOException {}
> }
> class Child extends Parent {
>     // Правильно: прокидаємо виняток далі
>     Child() throws IOException {
>         super();
>     }
> }
> ```
>

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння синтаксичних правил та механізмів обробки винятків у конструкторах. Це питання на виявлення експертних знань.*
