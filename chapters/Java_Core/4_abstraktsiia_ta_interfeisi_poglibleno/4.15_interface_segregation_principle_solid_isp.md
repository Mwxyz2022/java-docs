---
layout: default
title: "Interface Segregation Principle (SOLID — ISP)"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 15
---

# Interface Segregation Principle (SOLID — ISP)

* короткий опис: Один з принципів SOLID, який стверджує, що клієнти не повинні залежати від інтерфейсів, які вони не використовують. Краще мати багато маленьких, вузькоспрямованих інтерфейсів, ніж один великий.

* поняття: ISP, SOLID, "fat interface", вузькоспрямовані інтерфейси.

* приклади: Розділення великого інтерфейсу `Machine` на `Printable`, `Scannable`, `Faxable`.

* ключові моменти: Підвищує гнучкість, тестування, модульність та підтримуваність коду.

* що важливо знати на співбесіді: Пояснення ISP, його переваги, як застосувати.

### **✅ Офіційне визначення:**

Клієнти не повинні залежати від інтерфейсів, які вони не використовують. Інтерфейс має бути вузько спрямованим, а не "монстром" з купою зайвих методів.

**🔗 [SOLID principles — ISP by Robert C. Martin](https://en.wikipedia.org/wiki/Interface_segregation_principle)**

### **🧠 Простими словами:**

Краще мати кілька маленьких інтерфейсів з чіткими ролями, ніж один великий, де доводиться писати пусті методи, які ніколи не використовуєш. Клас реалізує тільки те, що йому справді треба.

---

## **Основне**

| Концепт | Пояснення |
| :---: | :---: |
| **Великий інтерфейс** | Має багато методів, не всі потрібні |
| **Маленький інтерфейс** | Один чіткий функціонал, одна відповідальність |
| **Проблема** | Клас змушений реалізовувати непотрібні методи |
| **Рішення** | Розбити великий інтерфейс на кілька маленьких |

---

## **Пояснення на пальцях (💡)**

Уяви, що є інтерфейс **`Machine`** з трьома методами:

```java
public interface Machine {
    void print();
    void scan();
    void fax();
}
```

Але твій клас **`SimplePrinter`** може тільки друкувати:

```java
public class SimplePrinter implements Machine {
    public void print() { System.out.println("Printing"); }
    public void scan() { /* пусто */ }
    public void fax() { /* пусто */ }
}
```

Тут ти порушуєш ISP — доводиться писати пусті методи, що нікого не радує.

**Рішення:**  
Розбиваємо **`Machine`** на три інтерфейси:

```java
public interface Printable {
    void print();
}

public interface Scannable {
    void scan();
}

public interface Faxable {
    void fax();
}
```

І тоді **`SimplePrinter`** реалізує лише те, що потрібно:

```java
public class SimplePrinter implements Printable {
    public void print() { System.out.println("Printing"); }
}
```

---

## **Корисні приклади (🧪)**

**Погано (порушення ISP):**

```java
public interface Machine {
    void print();
    void scan();
    void fax();
}

public class OldPrinter implements Machine {
    public void print() { System.out.println("Printing"); }
    public void scan() { /* пусто */ }
    public void fax() { /* пусто */ }
}
```

**Добре (дотримання ISP):**

```java
public interface Printable {
    void print();
}

public interface Scannable {
    void scan();
}

public interface Faxable {
    void fax();
}

public class OldPrinter implements Printable {
    public void print() { System.out.println("Printing"); }
}
```

---

## **Порівняння: Порушення vs Дотримання ISP (⚖️)**

| Ознака | Порушення ISP (Machine) | Дотримання ISP (Printable, ...) |
| :---: | :---: | :---: |
| **Кількість методів** | Багато, не всі потрібні | Лише потрібні |
| **Реалізація непотрібних методів** | ✅ Часто є | ❌ Немає |
| **Гнучкість** | Низька | Висока |
| **Тестованість** | Складніше | Легше |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 "Інтерфейс не мусить знати все — хай знає тільки своє"

-> Один інтерфейс — одна відповідальність

---

## **🟩 Підсумок:**

**Interface Segregation Principle** каже: не треба створювати монстрів з інтерфейсів. Краще розділити на маленькі і чіткі, щоб кожен клас реалізовував лише те, що йому потрібно. Це робить код чистим, гнучким, тестованим і щасливим.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Принцип розділення інтерфейсу (Interface Segregation Principle - ISP)? Поясніть своїми словами.

> #### **💬 Відповідь:**
>
> **Принцип розділення інтерфейсу (ISP)** — це один з п'яти принципів SOLID, який стверджує, що **клієнти (класи, що реалізують інтерфейс) не повинні бути змушені залежати від методів, які вони не використовують**.
>
> Простими словами, це означає: **краще мати багато невеликих, спеціалізованих інтерфейсів, ніж один великий, універсальний ("товстий") інтерфейс**.
>
> Кожен інтерфейс повинен визначати один, чітко сфокусований набір поведінок.
>
> **Головна ідея:** клас, який реалізує інтерфейс, не повинен бути змушений писати "пустишки" або кидати `UnsupportedOperationException` для методів, які йому логічно не потрібні.

#### **📝 Аналогія:**

Це як меню в ресторані.

* **Порушення ISP ("товстий" інтерфейс):** Вам дають одне величезне меню, де є і сніданки, і обіди, і десерти, і алкогольні напої. Якщо ваш заклад — це кав'ярня, ви змушені пояснювати кожному клієнту, що більшість позицій з цього меню ви не готуєте.
* **Дотримання ISP:** У вас є окремі меню: "Кавова карта", "Десерти", "Сніданки". Ви надаєте клієнту лише те меню, яке відповідає вашому закладу.

#### **📌 Що перевіряють:**

*Розуміння того, що ISP — це про створення вузькоспрямованих та клієнтоорієнтованих контрактів.*

* * *

### **2 ❓ Питання:**

Що таке "товстий" інтерфейс (fat interface) і які проблеми він створює?

> #### **💬 Відповідь:**
>
> **"Товстий" інтерфейс** — це інтерфейс, який має занадто багато методів, що описують різні, часто непов'язані, аспекти поведінки. Він намагається бути "універсальним солдатом".
>
> **Проблеми, які він створює:**
>
> 1. **Примус до реалізації непотрібних методів:** Класи, яким потрібна лише частина функціональності інтерфейсу, змушені надавати реалізацію для всіх його методів. Це часто призводить до появи **порожніх методів** або методів, що кидають **`UnsupportedOperationException`**.
>
> 2. **Низька згуртованість (Low Cohesion):** Інтерфейс містить методи, які не є логічно пов'язаними, що робить його важким для розуміння та підтримки.
>
> 3. **Крихкість та висока зв'язність:** Зміна в одній частині "товстого" інтерфейсу може змусити **перекомпілювати та змінювати всі класи**, які його реалізують, навіть ті, які не використовували змінений метод.
>
> 4. **Порушення принципу підстановки Лісков (LSP):** Коли метод кидає `UnsupportedOperationException`, це означає, що об'єкт цього класу не може виконати те, що "обіцяв" контракт інтерфейсу. Такий об'єкт не можна безпечно підставити замість іншого.

#### **📌 Що перевіряють:**

*Здатність ідентифікувати антипатерни в дизайні API та пояснити їхні негативні наслідки.*

* * *

### **3 ❓ Питання:**

Наведіть приклад порушення ISP та покажіть, як його виправити.

> #### **💬 Відповідь:**
>
> **Приклад порушення: Універсальний інтерфейс для транспортних засобів.**
>
> **Погано:**
>
> ```java
> // "Товстий" інтерфейс
> interface Vehicle {
>     void drive();
>     void fly();
> }
>
> class Car implements Vehicle {
>     public void drive() { System.out.println("Машина їде"); }
>     // Машина не літає!
>     public void fly() { throw new UnsupportedOperationException(); }
> }
>
> class Airplane implements Vehicle {
>     // Літак не їздить по дорогах (у загальному сенсі)
>     public void drive() { throw new UnsupportedOperationException(); }
>     public void fly() { System.out.println("Літак летить"); }
> }
> ```
>
> `Car` змушений реалізовувати `fly()`, а `Airplane` — `drive()`.
>
> **Як виправити (дотримуючись ISP):**
> Розділити великий інтерфейс на два менших, специфічних.
>
> **Добре:**
>
> ```java
> interface Drivable {
>     void drive();
> }
>
> interface Flyable {
>     void fly();
> }
>
> // Тепер кожен клас реалізує лише те, що він вміє
> class Car implements Drivable {
>     public void drive() { System.out.println("Машина їде"); }
> }
>
> class Airplane implements Flyable {
>     public void fly() { System.out.println("Літак летить"); }
> }
>
> // А хтось може реалізувати обидва
> class FlyingCar implements Drivable, Flyable {
>     // ...
> }
> ```
>
> Цей підхід є гнучким, логічним і не змушує класи реалізовувати непотрібну поведінку.

#### **📌 Що перевіряють:**

*Практичні навички рефакторингу. Здатність перетворити "поганий" дизайн на "хороший", використовуючи принципи SOLID.*

* * *

### **4 ❓ Питання:**

Як Принцип розділення інтерфейсу пов'язаний з Принципом єдиної відповідальності (SRP)?

> #### **💬 Відповідь:**
>
> Ці два принципи дуже схожі та тісно пов'язані, але застосовуються на різних рівнях.
>
> * **SRP (Принцип єдиної відповідальності)** стосується **класів**. Він каже, що клас повинен мати лише одну причину для зміни (одну відповідальність).
>
> * **ISP (Принцип розділення інтерфейсу)** стосується **інтерфейсів (абстракцій)**. Він каже, що інтерфейс повинен бути сфокусованим і не змушувати клієнтів залежати від методів, які їм не потрібні.
>
> **Як вони пов'язані:**
> Можна сказати, що **ISP — це застосування SRP до інтерфейсів**.
>
> Якщо ви створюєте інтерфейс, який відповідає за багато речей (порушення ISP), то класи, що його реалізують, швидше за все, також будуть мати багато відповідальностей (порушення SRP).
>
> Коли ви розділяєте "товстий" інтерфейс на кілька менших, сфокусованих (дотримуючись ISP), ви заохочуєте створення класів, кожен з яких реалізує один або кілька з цих невеликих інтерфейсів, що, у свою чергу, допомагає дотримуватися і SRP.

#### **📌 Що перевіряють:**

*Здатність бачити зв'язок між різними принципами SOLID та розуміння, як вони доповнюють один одного.*

* * *

### **5 ❓ Питання:**

Які `default` методи в інтерфейсах (Java 8) можуть допомогти, якщо ви змушені працювати з "товстим" інтерфейсом, який не можна змінити? ⭐️

> #### **💬 Відповідь:**
>
> Це питання про те, як можна "пом'якшити" наслідки поганого дизайну, з яким ви змушені працювати.
>
> Якщо ви не можете змінити "товстий" інтерфейс, ви можете створити **абстрактний клас-адаптер (Skeletal Implementation)**, який надає **порожні реалізації** для всіх методів цього інтерфейсу.
>
> **`default` методи** з Java 8 дозволяють зробити подібну річ на рівні **допоміжного інтерфейсу**.
>
> **Приклад:**
>
> ```java
> // "Товстий" інтерфейс зі сторонньої бібліотеки, який ми не можемо змінити
> interface LegacyListener {
>     void onEventA();
>     void onEventB();
>     void onEventC();
>     void onEventD();
> }
> ```
>
> **Як `default` методи допомагають:**
>
> Ми можемо створити свій власний інтерфейс-адаптер, який успадковує від "товстого" і надає `default`-реалізації (порожні) для всіх його методів.
>
> ```java
> // Наш інтерфейс-адаптер
> interface LegacyListenerAdapter extends LegacyListener {
>     @Override
>     default void onEventA() {} // Порожня реалізація за замовчуванням
>
>     @Override
>     default void onEventB() {}
>
>     @Override
>     default void onEventC() {}
>
>     @Override
>     default void onEventD() {}
> }
> ```
>
> Тепер, нашому конкретному класу-слухачу, якому потрібен лише `onEventC`, не потрібно реалізовувати всі чотири методи. Він може реалізувати наш інтерфейс-адаптер і перевизначити лише те, що йому потрібно.
>
> `class MyListener implements LegacyListenerAdapter { @Override public void onEventC() { ... } }`
>
> Це імітує поведінку класів-адаптерів зі старої Java (наприклад, `WindowAdapter` у Swing).

#### **📌 Що перевіряють:**

*Кмітливість та здатність використовувати нові можливості мови для вирішення старих проблем.*

* * *

### **6 ❓ Питання:**

Поясніть на прикладі з `java.util.Collection`, чому цей інтерфейс, на перший погляд, може здатися "товстим", але насправді він дотримується принципів хорошого дизайну. ⭐️

> #### **💬 Відповідь:**
>
> Інтерфейс `Collection` містить досить багато методів (`add`, `remove`, `contains`, `size`, `iterator` тощо). На перший погляд, його можна було б вважати "товстим".
>
> **Однак він не порушує ISP, тому що всі його методи є високозгуртованими (cohesive) і служать одній-єдиній меті: визначити базовий контракт для структури даних, що є "контейнером елементів".**
>
> * Кожен метод у `Collection` описує фундаментальну операцію, яку ви очікуєте від будь-якої колекції.
> * Дуже важко уявити клас, якому потрібен, наприклад, метод `add()`, але не потрібен `size()` чи `iterator()`. Ці методи є логічно нерозривними.
>
> ISP порушується не тоді, коли в інтерфейсі багато методів, а тоді, коли ці методи **не пов'язані логічно** і обслуговують **різних клієнтів**.
>
> **До того ж, ієрархія Collections API сама по собі дотримується ISP:**
>
> * `List` додає методи, пов'язані з доступом за індексом.
> * `Set` додає контракт унікальності.
> * `Queue` додає методи для роботи з чергою (`offer`, `poll`).
>
> Кожен з них додає свою сфокусовану групу поведінок, що є прикладом хорошого дизайну.

#### **📌 Що перевіряють:**

*Здатність аналізувати API і відрізняти великий, але згуртований інтерфейс, від справді "товстого" інтерфейсу, що порушує SRP/ISP.*

* * *

### **7 ❓ Питання:**

Які патерни проектування напряму допомагають дотримуватися ISP? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Дотримання ISP часто є наслідком застосування інших архітектурних патернів, що сприяють розділенню обов'язків.
>
> 1. **Адаптер (Adapter):**
>     * Дозволяє "адаптувати" "товстий" або несумісний інтерфейс до більш вузького, який потрібен клієнту. Клієнт працює лише з потрібною йому частиною API, а адаптер приховує решту.
>
> 2. **Декоратор (Decorator):**
>     * Замість того, щоб додавати всю можливу функціональність в один інтерфейс, ви створюєте базовий інтерфейс, а потім "обгортаєте" його реалізації в декоратори, кожен з яких додає одну конкретну поведінку. Це сприяє створенню багатьох маленьких, сфокусованих класів.
>
> 3. **Фасад (Facade):**
>     * Фасад надає спрощений, високорівневий інтерфейс для складної підсистеми. Клієнти залежать від цього простого інтерфейсу, а не від безлічі інтерфейсів внутрішніх компонентів.
>
> По суті, будь-який патерн, що заохочує декомпозицію та створення невеликих, сфокусованих класів/інтерфейсів, опосередковано допомагає дотримуватися ISP.

#### **📌 Що перевіряють:**

*Знання патернів проектування та їхнього зв'язку з принципами SOLID.*

* * *

### **8 ❓ Питання:**

Як ISP пов'язаний з принципом підстановки Лісков (LSP)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони тісно пов'язані. Порушення **ISP** часто призводить до порушення **LSP**.
>
> * **ISP** каже, що клієнти не повинні реалізовувати методи, які їм не потрібні.
> * **LSP** каже, що підтип повинен мати можливість замінити супертип без зміни коректності програми.
>
> **Як порушення ISP веде до порушення LSP:**
>
> 1. Коли у вас є "товстий" інтерфейс (`Worker` з методами `work()` та `eat()`), клас, якому не потрібен один з методів (наприклад, `RobotWorker` та його метод `eat()`), змушений надати **"пустушку"** або, що гірше, кидати **`UnsupportedOperationException`**.
>
>     ```java
>     class RobotWorker implements Worker {
>         public void work() { ... }
>         public void eat() {
>             throw new UnsupportedOperationException("Роботи не їдять!");
>         }
>     }
>     ```
>
> 2. Тепер уявімо код, який працює з цим інтерфейсом:
>
>     ```java
>     public void manageWorker(Worker worker) {
>         worker.work();
>         worker.eat(); // Програма впаде, якщо сюди передати RobotWorker!
>     }
>     ```
> 3.  Ми більше **не можемо безпечно підставити** `RobotWorker` туди, де очікується `Worker`, оскільки він "ламає" контракт, кидаючи виняток. Це є прямим **порушенням LSP**.
>
> Розділення інтерфейсу (дотримання ISP) вирішує цю проблему, оскільки `RobotWorker` просто не буде реалізовувати `Eatable`.

#### **📌 Що перевіряють:**

*Розуміння синергії між принципами SOLID. Здатність побачити, як одна проблема в дизайні (порушення ISP) призводить до іншої (порушення LSP).*

* * *

### **9 ❓ Питання:**

Чому реалізація інтерфейсу, що має лише один метод, майже ніколи не є порушенням ISP? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що такий інтерфейс є **максимально згуртованим (cohesive) та атомарним**.
>
> **Функціональний інтерфейс** (SAM-інтерфейс), такий як `Runnable` або `Comparator`, є ідеальним прикладом дотримання ISP.
>
> * Він визначає **одну, єдину, чітко сфокусовану поведінку** (`run()` — вміння бути запущеним, `compare()` — вміння порівнювати).
> * Неможливо уявити ситуацію, коли класу потрібна була б лише "частина" цього інтерфейсу. Він або реалізує цю одну дію, або ні.
> * Він не може змусити клієнта реалізовувати щось зайве, бо в ньому просто немає нічого зайвого.
>
> Таким чином, розбиваючи функціональність на набір функціональних інтерфейсів, ви природним чином дотримуєтеся ISP.

#### **📌 Що перевіряють:**

*Зв'язок між ISP та іншими концепціями, як-от згуртованість та функціональні інтерфейси.*

* * *

### **10 ❓ Питання:**

Чи завжди клас з одним методом кращий, ніж клас з багатьма методами, з точки зору ISP? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Не завжди.** Важлива не кількість методів, а їхня **логічна згуртованість (cohesion)**.
>
> **ISP не говорить: "робіть інтерфейси з одним методом"**. Він говорить: **"не змушуйте клієнтів залежати від методів, які вони не використовують"**.
>
> * Якщо **всі методи** в інтерфейсі служать **одній меті** і, як правило, використовуються разом, то такий інтерфейс, навіть з багатьма методами, **не порушує ISP**.
> * **Приклад:** Інтерфейс `java.util.List`. Він має багато методів (`add`, `get`, `remove`, `size`, `clear`, `iterator`...), але всі вони логічно пов'язані з концепцією "список". Клас, що реалізує `List`, зазвичай потребує їх усіх. Розбивати `List` на `Addable`, `Gettable`, `Removable` було б надмірним і непрактичним.
>
> Порушення відбувається тоді, коли методи обслуговують **різні, незалежні абстракції**. Наприклад, `print()` та `saveToDatabase()` в одному інтерфейсі `DocumentProcessor`. Це дві різні відповідальності.

#### **📌 Що перевіряють:**

*Уміння мислити прагматично. Кандидат повинен розуміти, що принципи SOLID — це настанови, а не догми, і їх слід застосовувати з розумом.*

* * *

### **11 ❓ Питання:**

Як `private` методи в інтерфейсах (з Java 9) допомагають підтримувати ISP? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> `private` методи в інтерфейсах безпосередньо не впливають на те, як клієнти реалізують цей інтерфейс, але вони **опосередковано** допомагають дотримуватися ISP, роблячи `default` методи чистішими.
>
> **Як це працює:**
>
> 1. ISP заохочує створювати маленькі, сфокусовані інтерфейси.
> 2. Іноді, щоб уникнути "товстих" інтерфейсів, розробники використовували `default` методи для надання опціональної функціональності.
> 3. Якщо кілька `default` методів мали спільну логіку, до Java 9 цю логіку не можна було винести в окремий метод, не зробивши його також `public default`, що знову ж таки "забруднювало" API.
> 4. **`private` методи** дозволили **інкапсулювати** цю спільну логіку **всередині інтерфейсу**.
>
> Це робить `default` методи більш лаконічними та сфокусованими, і заохочує розробників краще структурувати логіку всередині інтерфейсів, не виставляючи зайвих деталей назовні. Це, у свою чергу, робить сам інтерфейс більш згуртованим, що є метою ISP.
>
> По суті, `private` методи — це інструмент для покращення **внутрішньої якості** інтерфейсу, що полегшує дотримання ISP на рівні його публічного API.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як різні можливості мови взаємодіють для досягнення цілей хорошого дизайну.*

* * *

### **12 ❓ Питання:**

Розгляньте таку ієрархію: `interface Animal { void eat(); }`, `interface Pet extends Animal { void play(); }`. Чи порушує це ISP? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не порушує.** Це є **правильним застосуванням розширення інтерфейсів** для створення більш специфічних контрактів.
>
> **Пояснення:**
>
> * ISP каже, що не слід змушувати клієнтів залежати від непотрібних методів.
>
> * У цій ієрархії ми **не змушуємо** всіх `Animal` вміти гратися. Ми створюємо новий, більш вузький тип `Pet`, який **додає** нову поведінку (`play`) до існуючої (`eat`).
>
> * **Це дає вибір:**
>   * Клас, який представляє дику тварину, може реалізувати лише `Animal`.
>   * Клас, який представляє домашню тварину, може реалізувати більш специфічний інтерфейс `Pet`.
>
> Цей підхід є **альтернативою розділенню інтерфейсів**. Замість створення окремих `Eatable` та `Playable`, ми будуємо ієрархію, де кожен наступний рівень є більш специфічним. Це повністю відповідає ідеї ISP — не надавати "зайвих" методів тим, кому вони не потрібні.

#### **📌 Що перевіряють:**

*Розуміння того, що ієрархії інтерфейсів є ще одним інструментом для досягнення цілей ISP.*

* * *

### **13 ❓ Питання:**

Що краще: клас, що реалізує один "товстий" інтерфейс, чи клас, що реалізує багато маленьких? Обґрунтуйте. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Однозначно краще, коли клас реалізує багато маленьких, сфокусованих інтерфейсів.** Це прямо відповідає Принципу розділення інтерфейсу (ISP) і дає багато переваг.
>
> **Клас, що реалізує один "товстий" інтерфейс:**
>
> * **Проблеми:**
>   * Змушений реалізовувати непотрібні методи.
>   * Низька згуртованість.
>   * Важко зрозуміти, яку саме роль виконує цей клас, оскільки він "вміє" все.
>
> **Клас, що реалізує багато маленьких інтерфейсів:**
>
> * **Переваги:**
>     1. **Ясність наміру:** `class MyClass implements Serializable, Comparable, Closeable` одразу говорить нам про три різні аспекти поведінки цього класу. Код стає самодокументованим.
>     2. **Гнучкість та поліморфізм:** Об'єкт цього класу може бути переданий у будь-який метод, що очікує `Serializable`, `Comparable` або `Closeable`. Він може грати багато різних ролей у різних контекстах.
>     3. **Висока згуртованість:** Кожен інтерфейс відповідає за одну конкретну поведінку.
>     4. **Краща тестованість:** Легше тестувати кожну "роль" окремо.
>
> **Приклад:**
> Клас `SuperHero implements Flyable, Bulletproof, FastRunnable`. Це значно краще, ніж один інтерфейс `ISuperHeroAbilities` з десятком методів.
>
> Цей підхід називається **композицією поведінки** і є однією з найпотужніших ідей в об'єктно-орієнтованому дизайні.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти, як дизайн на рівні інтерфейсів впливає на всю структуру програми.*

* * *

### **14 ❓ Питання:**

Уявіть, що ви працюєте зі сторонньою бібліотекою, яка надає "товстий" інтерфейс. Як ви можете спроектувати свій код, щоб дотримуватися ISP, не змінюючи цю бібліотеку? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема, яка вирішується за допомогою **патерну "Адаптер"**.
>
> **Алгоритм дій:**
>
> 1. **Визначте власні, вузькоспрямовані інтерфейси**, які потрібні вашому клієнтському коду. Ці інтерфейси будуть дотримуватися ISP.
>
>     ```java
>     // Нам потрібна лише функція друку
>     interface MyPrinter {
>         void printDocument(String doc);
>     }
>     ```
>
> 2. **Створіть клас-адаптер**, який буде **реалізовувати ваш вузький інтерфейс** і **використовувати ("обгортати") об'єкт "товстого" інтерфейсу**.
>
>     ```java
>     // "Товстий" інтерфейс з бібліотеки
>     interface LegacyMachine {
>         void print(String content, int copies);
>         void scan();
>     }
>
>     // Адаптер
>     class LegacyMachineAdapter implements MyPrinter {
>         private final LegacyMachine legacyMachine;
>
>         public LegacyMachineAdapter(LegacyMachine legacyMachine) {
>             this.legacyMachine = legacyMachine;
>         }
>
>         @Override
>         public void printDocument(String doc) {
>             // "Перекладаємо" виклик з нашого API на API бібліотеки
>             legacyMachine.print(doc, 1);
>         }
>     }
>     ```
>
> 3. **Ваш клієнтський код** тепер залежить лише від вашого чистого інтерфейсу `MyPrinter`.
>
> Таким чином, ви **ізолюєте** свій код від недосконалого дизайну сторонньої бібліотеки. Адаптер бере на себе всю "брудну" роботу по взаємодії з "товстим" інтерфейсом. Це дозволяє дотримуватися ISP у власному коді, навіть якщо залежності йому не відповідають.

#### **📌 Що перевіряють:**

*Знання патернів проектування та їх застосування для ізоляції та інтеграції зі сторонніми системами.*

* * *

### **15 ❓ Питання:**

Як `records` (Java 16) та `sealed` класи/інтерфейси (Java 17) допомагають краще дотримуватися принципів ISP та SRP? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ці сучасні можливості Java неявно сприяють дотриманню цих принципів.
>
> **Як допомагають `records`:**
>
> * `record` ідеально підходить для створення **DTO** або **Value Objects**.
> * Це заохочує дотримуватися **SRP**, оскільки `record` за своєю природою призначений бути **"тупим" носієм даних**. Розробнику стає психологічно важче "навішувати" на нього бізнес-логіку, оскільки це порушує його ідею. Це підштовхує до винесення логіки в окремі сервісні класи.
>
> **Як допомагають `sealed` класи/інтерфейси:**
>
> * `sealed`-інтерфейс дозволяє створити **закриту ієрархію типів**. Це є потужним інструментом для дотримання **ISP** у певних контекстах.
> * **Приклад:** Ви можете визначити `sealed interface Shape permits Circle, Square {}`. Це означає, що **лише `Circle` та `Square`** можуть реалізовувати цей інтерфейс.
> * Тепер, коли ви працюєте з цим інтерфейсом, ви можете використовувати **`switch`-вирази з pattern matching** і бути впевненими, що ви **обробили всі можливі випадки**.
>
>     ```java
>     double getArea(Shape shape) {
>         return switch(shape) {
>             case Circle c -> ... ;
>             case Square s -> ... ;
>             // `default` не потрібен, бо компілятор знає всі можливі типи!
>         }
>     }
>     ```
>
> Це робить код, що залежить від інтерфейсу, більш **надійним і вичерпним**, що є однією з цілей ISP — не змушувати клієнтів обробляти несподівані, невідомі їм реалізації.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як нові можливості мови змінюють підходи до проектування та допомагають писати більш безпечний та виразний код.*
