---
layout: default
title: "Статичні класи (внутрішні та утилітні)"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 9
---

# Статичні класи (внутрішні та утилітні)

* короткий опис: Вкладені статичні класи (`static nested class`) – це класи, оголошені всередині іншого класу, які не мають доступу до нестатичних членів зовнішнього класу. Утилітарні класи (`utility class`) – це класи з усіма методами та полями як `static`, що не потребують створення об'єкта.

* поняття: `static nested class`, `utility class`.

* приклади: `Outer.StaticNested` (вкладений static клас), `Math`, `Collections` (утилітарні класи).

* ключові моменти: Не потребують створення об'єкта зовнішнього класу, служать для логічного групування або надання утилітарних функцій.

* що важливо знати на співбесіді: Різниця між `static` та inner класами, `utility` класи.

### **✅ Офіційне визначення:**

У Java можна оголосити **static клас** всередині іншого класу (внутрішній **`static class`**), або створити клас з усіма методами/полями як **`static`** для утилітарних задач (**`utility class`**). Статичні класи не прив’язані до об’єкта зовнішнього класу.

**🔗 [JLS §8.1.1.1 Nested Classes](https://docs.oracle.com/javase/specs/jls/se17/html/jls-8.html#jls-8.1.1)**

### **🧠 Простими словами:**

**`static`** `class` — це клас, який живе сам по собі, не чекаючи, що хтось створить об’єкт зовнішнього класу. Це або набір корисних методів, як у **`Math`**, або допоміжний клас, що живе всередині іншого, але не лізе у його приватні речі

---

## **Типи static класів (📊)**

| Тип | Опис | Приклад |
| :---: | :---: | :---: |
| **Утилітарний (utility)** | Клас з усіма **`static`** методами, без стану | **`Math, Collections`** |
| **Внутрішній static клас** | Клас всередині іншого класу, без доступу до this зовнішнього класу | **`Outer.Inner`** |

---

## **Пояснення на пальцях (💡)**

🔸 **Utility-класи** — це набір корисних статичних методів, які не потребують створення об’єкта.  
Наприклад, **`Math.sqrt(16)`** — просто виклик функції, ніяких new.

🔸 **Внутрішні static класи** — як вкладені помічники. Вони лежать всередині іншого класу, але **не мають доступу до нестатичних полів зовнішнього класу**. Це зручно для логічного структурування.

🔸 **Типова помилка:**  
Плутати внутрішній static клас зі звичайним внутрішнім класом, який **має** доступ до **`this`** зовнішнього класу

---

## **Корисні приклади (🧪)**

🔻 **Utility-клас:**

```java
public class Calculator {
    public static int sum(int a, int b) {
        return a + b;
    }
}

int result = Calculator.sum(3, 4); // 7
```

🔻 **Внутрішній static клас:**

```java
public class Outer {
    private static String label = "STATIC";

    public static class Inner {
        public void print() {
            System.out.println("Inner class - " + label);
        }
    }
}

Outer.Inner inner = new Outer.Inner();
inner.print(); // Inner class - STATIC
```

---

## **Порівняння: звичайний vs static внутрішній клас (⚖️)**

| Ознака | Внутрішній звичайний клас | Внутрішній static клас |
| :---: | :---: | :---: |
| **Має доступ до `this.outer`** | ✅ Так | ❌ Ні |
| **Можна створити без об’єкта зовнішнього класу** | ❌ Ні | ✅ Так |
| **Часте використання** | Робота з об’єктом | Структурне вкладення без зв’язку |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **"static class \= не залежу ні від кого"**

* Утилітарний -> як **`Math`** чи **`Collections`**
* Внутрішній -> вкладений, але без доступу до зовнішнього **`this`**

---

## **🟩 Підсумок:**

**`static`** класи — це або утилітарні класи з набором корисних статичних методів, або вкладені внутрішні класи, які не залежать від об’єкта зовнішнього класу. Вони допомагають тримати код акуратним і не плодити зайві об’єкти.

---

### **🔗 Додаткові джерела:**

**🔗 [Oracle Java Tutorial: Nested Classes](https://docs.oracle.com/javase/tutorial/java/javaOO/nested.html)**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які є два основні типи "статичних класів" у Java? Поясніть кожен з них.

> #### **💬 Відповідь:**
>
> Термін "статичний клас" може означати дві різні концепції:
>
> 1. **Статичний вкладений клас (`static nested class`):**
>     * Це клас, оголошений **всередині іншого класу** з модифікатором `static`.
>     * Його головна особливість — він **не має** неявного посилання на екземпляр зовнішнього класу. Він поводиться як звичайний клас верхнього рівня, але для зручності та логічного групування "запакований" всередину іншого.
>     * **Приклад:** Патерн `Builder`, клас `Map.Entry` всередині інтерфейсу `Map`.
>
> 2. **Утилітарний клас (`utility class`):**
>     * Це не спеціальна мовна конструкція, а **патерн проектування**.
>     * Це клас, який містить **лише `static` методи та поля**. Він не призначений для створення екземплярів.
>     * Зазвичай має **`private` конструктор**, щоб заборонити його інстанціювання.
>     * **Приклад:** `java.lang.Math`, `java.util.Collections`.

#### **📌 Що перевіряють:**

*Чітке розмежування між мовною конструкцією (`static nested class`) та патерном проектування (`utility class`).*

* * *

### **2 ❓ Питання:**

У чому ключова відмінність між статичним вкладеним класом та звичайним внутрішнім класом (inner class)?

> #### **💬 Відповідь:**
>
> Ключова відмінність полягає у **наявності неявного посилання на екземпляр зовнішнього класу**.
>
> **Статичний вкладений клас (`static nested class`):**
>
> * **Не має** посилання на об'єкт зовнішнього класу.
> * Може звертатися **лише до `static` членів** зовнішнього класу.
> * Створюється незалежно: `new OuterClass.StaticNestedClass();`.
>
> **Звичайний внутрішній клас (`inner class`, не-`static`):**
>
> * **Має** неявне посилання на об'єкт зовнішнього класу, в якому він був створений.
> * Може звертатися до **всіх членів** зовнішнього класу, включаючи `private` поля та методи.
> * Створюється **тільки через екземпляр зовнішнього класу**: `outerObject.new InnerClass();`.
>
> **Коли який обирати:**
>
> * Використовуйте **`static` вкладений клас**, якщо вкладеному класу **не потрібен доступ до стану** екземпляра зовнішнього класу. Це **кращий вибір за замовчуванням**, оскільки він не створює зайвих посилань і запобігає витокам пам'яті.
> * Використовуйте **внутрішній клас**, тільки якщо він **тісно пов'язаний** зі станом зовнішнього об'єкта і потребує прямого доступу до його полів/методів.

#### **📝 Аналогія:**

* **`static` вкладений клас:** Це як окремий будинок на вашій земельній ділянці. Він знаходиться на вашій території, але живе своїм життям.
* **Внутрішній клас:** Це як кімната всередині вашого будинку. Вона є невід'ємною частиною будинку і має доступ до всього, що в ньому є.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння різниці між цими двома типами вкладених класів. Це класичне питання для співбесіди з ООП.*

* * *

### **3 ❓ Питання:**

Наведіть приклад, де статичний вкладений клас є найкращим рішенням.

> #### **💬 Відповідь:**
>
> Найкращим і найпоширенішим прикладом є реалізація патерну **Builder**.
>
> `Builder` — це клас, що допомагає конструювати складні об'єкти. Він логічно пов'язаний з класом, який він будує, тому його зручно розмістити всередині. Але йому **не потрібен доступ** до екземпляра основного класу (бо його ще не існує на момент конфігурації білдера).
>
> **Приклад:**
>
> ```java
> public class User {
>     private final String firstName;
>     private final String lastName;
>     private final int age;
>
>     // Приватний конструктор, який приймає білдер
>     private User(Builder builder) {
>         this.firstName = builder.firstName;
>         this.lastName = builder.lastName;
>         this.age = builder.age;
>     }
>
>     // Статичний вкладений клас Builder
>     public static class Builder {
>         private String firstName;
>         private String lastName;
>         private int age = 0; // Значення за замовчуванням
>
>         public Builder(String firstName, String lastName) { // Обов'язкові поля
>             this.firstName = firstName;
>             this.lastName = lastName;
>         }
>
>         public Builder age(int age) {
>             this.age = age;
>             return this;
>         }
>
>         public User build() {
>             return new User(this);
>         }
>     }
> }
>
> // Використання
> User user = new User.Builder("John", "Doe").age(30).build();
> ```
>
> Інший чудовий приклад — це `Map.Entry` в інтерфейсі `Map`. Він є статичним, бо запис (ключ-значення) є концепцією, що належить мапі в цілому, але не конкретному екземпляру `HashMap`.

#### **📌 Що перевіряють:**

*Знання патерну Builder та розуміння того, чому `static` клас є ідеальним вибором для його реалізації.*

* * *

### **4 ❓ Питання:**

Наведіть приклад утилітарного класу з JDK. Які його основні характеристики?

> #### **💬 Відповідь:**
>
> Класичними прикладами є **`java.lang.Math`** та **`java.util.Collections`**.
>
> **Характеристики на прикладі `java.lang.Math`:**
>
> 1. **Оголошений як `final`:**
>     `public final class Math`
>     Це забороняє успадкування, оскільки немає сенсу створювати "підклас" математичних функцій.
>
> 2. **Має `private` конструктор:**
>     `private Math() {}`
>     Це **забороняє створення екземплярів** класу (`new Math()`), оскільки всі його методи статичні і не потребують об'єкта.
>
> 3. **Містить лише `static` члени:**
>     * **Статичні методи:** `public static double sqrt(double a)`, `public static int max(int a, int b)`.
>     * **Статичні константи:** `public static final double PI`, `public static final double E`.
>
> Всі ці характеристики разом утворюють класичний **патерн утилітарного класу**: `final` клас з приватним конструктором і набором публічних статичних методів та полів.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки та здатність аналізувати її дизайн.*

* * *

### **5 ❓ Питання:**

Чи може статичний вкладений клас бути `private`? Якщо так, то коли це доцільно? ⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Статичний вкладений клас може мати будь-який з чотирьох модифікаторів доступу: `public`, `protected`, `default` або `private`.
>
> **Коли доцільно робити його `private`:**
>
> Це корисно, коли вам потрібен **допоміжний клас-структура**, який використовується **лише всередині зовнішнього класу** і не повинен бути видимим ззовні взагалі. Це посилює інкапсуляцію.
>
> **Приклад:**
> Уявімо, клас `Cache` реалізує складний механізм кешування. Для зберігання запису в кеші (значення + час життя) він може використовувати внутрішній `private static` клас.
>
> ```java
> public class Cache<K, V> {
>
>     // Приватний статичний вкладений клас
>     private static class CacheEntry<V> {
>         final V value;
>         final long expiryTime;
>
>         CacheEntry(V value, long ttl) {
>             this.value = value;
>             this.expiryTime = System.currentTimeMillis() + ttl;
>         }
>     }
>
>     private final Map<K, CacheEntry<V>> cacheMap = new ConcurrentHashMap<>();
>
>     public void put(K key, V value, long ttl) {
>         cacheMap.put(key, new CacheEntry<>(value, ttl));
>     }
>
>     // ...
> }
> ```
>
> Тут `CacheEntry` є деталлю реалізації самого `Cache`. Робити його видимим ззовні не потрібно і навіть шкідливо. Оскільки він не залежить від стану екземпляра `Cache`, він є `static`.

#### **📌 Що перевіряють:**

*Розуміння того, як модифікатори доступу застосовуються до вкладених класів для досягнення кращої інкапсуляції.*

* * *

### **6 ❓ Питання:**

Чим внутрішній клас (inner) може бути небезпечним з точки зору витоків пам'яті? Як статичний вкладений клас вирішує цю проблему? ⭐️

> #### **💬 Відповідь:**
>
> **Небезпека внутрішнього класу:**
>
> Не-статичний внутрішній клас утримує **неявне посилання** на екземпляр зовнішнього класу, який його створив. Це може призвести до **витоку пам'яті**, якщо життєвий цикл внутрішнього об'єкта довший, ніж у зовнішнього.
>
> **Типовий сценарій:**
>
> 1. Зовнішній об'єкт (наприклад, `Activity` в Android) створює внутрішній об'єкт (наприклад, `AsyncTask` для фонової роботи).
> 2. `Activity` має бути знищене (наприклад, при повороті екрану).
> 3. Але фонове завдання `AsyncTask` ще виконується. Оскільки воно має **сильне посилання** на `Activity`, збирач сміття **не може видалити** `Activity` з пам'яті.
> 4. `Activity` разом з усіма своїми ресурсами "зависає" в пам'яті — це і є витік.
>
> **Як статичний вкладений клас вирішує проблему:**
>
> Статичний вкладений клас **не має** неявного посилання на зовнішній екземпляр. Його життєвий цикл повністю незалежний.
>
> У вищенаведеному прикладі, якщо `AsyncTask` зробити **`static nested class`**, він більше не буде утримувати посилання на `Activity`. Якщо йому потрібен контекст, його слід передавати через **слабке посилання (`WeakReference`)**, що дозволить `Activity` бути знищеною збирачем сміття, коли це необхідно.
>
> **Правило:** Якщо вкладеному класу не потрібен прямий доступ до `this` зовнішнього класу, **завжди робіть його `static`**.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працює управління пам'яттю та GC. Здатність ідентифікувати потенційні витоки пам'яті.*

* * *

### **7 ❓ Питання:**

Що виведе цей код? Поясніть свою відповідь.

```java
public class Outer {
    private String instanceVar = "Instance";
    private static String staticVar = "Static";
    
    public static class Nested {
        public void print() {
            System.out.println(staticVar);
            // System.out.println(instanceVar);
        }
    }
}
```

Якщо розкоментувати другий `println`, що зміниться?
⭐️

> #### **💬 Відповідь:**
>
> **У поточному вигляді** код виведе:
>
> ```
> Static
> ```
>
> **Пояснення:**
> Статичний вкладений клас `Nested` **має доступ до `static` членів** зовнішнього класу, тому виклик `System.out.println(staticVar)` є абсолютно валідним.
>
> **Якщо розкоментувати другий `println`:**
>
> `// System.out.println(instanceVar);`
>
> Код **не скомпілюється**. Компілятор видасть помилку:
> `non-static variable 'instanceVar' cannot be referenced from a static context`.
>
> **Пояснення помилки:**
> `static` вкладений клас `Nested` **не асоційований** з жодним екземпляром `Outer`. Він не має посилання `this` на `Outer`. Тому він просто не знає, з якого саме об'єкта `Outer` брати значення для поля `instanceVar`. `instanceVar` належить об'єкту, а не класу.

#### **📌 Що перевіряють:**

*Розуміння правил доступу з статичного вкладеного класу.*

* * *

### **8 ❓ Питання:**

Які є переваги у використанні `enum` замість утилітарного класу для групування пов'язаних констант? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`enum`** є значно кращим та більш типобезпечним рішенням.
>
> **Підхід з утилітарним класом:**
>
> ```java
> public final class StatusConstants {
>     private StatusConstants() {}
>     public static final int NEW = 0;
>     public static final int IN_PROGRESS = 1;
>     public static final int DONE = 2;
> }
> ```
>
> **Проблеми цього підходу:**
>
> 1. **Відсутність типобезпечності:** Метод, що очікує статус, буде приймати `int`. Йому можна передати будь-яке число (`-1`, `500`), і компілятор не знайде помилки.
> 2. **Відсутність простору імен:** Якщо є інший набір констант (`UserType.ADMIN = 0`), їх можна легко переплутати.
> 3. **Крихкість:** Додавання або зміна значень може "зламати" код.
>
> **Переваги `enum`:**
>
> ```java
> public enum Status {
>     NEW, IN_PROGRESS, DONE
> }
> ```
>
> 1. **Типобезпечність:** Створюється новий тип `Status`. Метод, що приймає `Status`, не може прийняти нічого іншого.
> 2. **Чіткий простір імен:** Усі константи згруповані під типом `Status`.
> 3. **Читабельність:** `Status.IN_PROGRESS` набагато зрозуміліше, ніж просто `1`.
> 4. **Функціональність:** `enum` може мати методи та поля, що дозволяє інкапсулювати пов'язану з константою логіку.
>
> Отже, для представлення фіксованого набору пов'язаних констант `enum` є абсолютно кращим вибором. Утилітарний клас слід залишати для набору функцій, а не значень.

#### **📌 Що перевіряють:**

*Знання сучасних найкращих практик. Розуміння того, чому `enum` є кращою альтернативою "старому" підходу з константами.*

* * *

### **9 ❓ Питання:**

Як можна створити екземпляр статичного вкладеного класу? А не-статичного?

> #### **💬 Відповідь:**
>
> Синтаксис створення для них різний, оскільки відображає їхній зв'язок із зовнішнім класом.
>
> **Створення статичного вкладеного класу:**
>
> * Створюється як звичайний клас, але з префіксом імені зовнішнього класу. Екземпляр зовнішнього класу **не потрібен**.
>
>     ```java
>     OuterClass.StaticNestedClass nestedObj = new OuterClass.StaticNestedClass();
>     ```
> **Створення не-статичного (внутрішнього) класу:**
>
> * Створюється **тільки через екземпляр зовнішнього класу**, оскільки внутрішній клас прив'язаний до нього.
>
>     ```java
>     OuterClass outerObj = new OuterClass();
>     OuterClass.InnerClass innerObj = outerObj.new InnerClass();
>     ```
> Цей незвичайний синтаксис `outerObj.new` чітко показує, що створюється об'єкт, який "належить" `outerObj`.

#### **📌 Що перевіряють:**

*Знання точного синтаксису для роботи з різними типами вкладених класів.*

* * *

### **10 ❓ Питання:**

Чи можна оголосити інтерфейс всередині класу? Якщо так, то чи буде він неявно статичним? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Ви можете оголосити інтерфейс всередині класу.
>
> І, що найцікавіше, будь-який інтерфейс, оголошений як член класу, є **неявно `static`**.
>
> **Пояснення:**
>
> * Інтерфейси за своєю природою є **абстрактними** і **не можуть бути прив'язані до екземпляра**. Вони не мають стану (`this`).
> * Якби вкладений інтерфейс не був статичним, він мав би неявне посилання на `this` зовнішнього класу, що суперечить самій концепції інтерфейсу.
> * Тому специфікація мови Java чітко визначає, що будь-який член-інтерфейс (member interface) є `static`, навіть якщо ви не пишете це слово.
>
> **Приклад:**
>
> ```java
> public class DataHolder {
>     // Цей інтерфейс неявно є 'public static'
>     interface DataProcessor {
>         void process(String data);
>     }
> }
>
> // Звертатися до нього потрібно через ім'я зовнішнього класу
> DataHolder.DataProcessor processor = (data) -> System.out.println(data);
> ```
>
> Оголошення `static interface` є валідним, але слово `static` надлишкове.

#### **📌 Що перевіряють:**

*Дуже глибоке знання специфікації Java. Це питання-пастка, оскільки поведінка відрізняється від класів.*

* * *

### **11 ❓ Питання:**

Які є переваги винесення утилітарної логіки у `static` методи порівняно з створенням об'єкта-утиліти? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Переваги `static` методів для утиліт:**
>
> 1. **Простота та зручність виклику:** Не потрібно створювати екземпляр класу. Виклик є прямим та лаконічним: `MyUtils.doSomething()`.
>
> 2. **Ефективність з точки зору пам'яті:** Не створюються зайві об'єкти, що зменшує навантаження на Garbage Collector.
>
> 3. **Явність наміру:** `static` метод чітко сигналізує, що ця операція є **безстановою (stateless)** і її результат залежить лише від вхідних параметрів.
>
> **Переваги об'єкта-утиліти (не-статичного підходу):**
>
> 1. **Тестованість та гнучкість:** Це головна перевага. Якщо утилітарна логіка реалізована через інтерфейс та його конкретну реалізацію, ви можете легко **впровадити (inject)** її та **підмінити на mock-об'єкт** у тестах. Статичні методи важко мокати.
>
> 2. **Можливість мати стан:** Іноді утиліті може знадобитися якась конфігурація або стан (наприклад, форматер дати з певним патерном). Об'єкт може зберігати цей стан, статичний клас — ні (лише глобальний статичний стан, що погано).
>
> 3. **Відповідність принципам SOLID (DIP):** Залежність від інтерфейсу утиліти краща, ніж жорстка прив'язка до статичних методів конкретного класу.
>
> **Висновок:**
> Для **дуже простої, безстанової логіки** (`Math`, `StringUtils`) статичний підхід є прийнятним.
> Для будь-якої **складної або залежної від конфігурації** логіки, яку потрібно тестувати, завжди слід віддавати перевагу **не-статичному підходу** з інтерфейсом та впровадженням залежностей.

#### **📌 Що перевіряють:**

*Архітектурне мислення та розуміння компромісів між простим процедурним стилем та більш гнучким, але трохи більш багатослівним ОО-дизайном.*

* * *

### **12 ❓ Питання:**

Що виведе цей код і чому?
`new A().new B().print();`

```java
public class A {
    private String field = "A field";
    static class B { // Клас B - статичний
        void print() {
            // System.out.println(field);
        }
    }
}
```

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється**.
>
> **Проблема** у рядку: `new A().new B()`.
>
> **Пояснення:**
>
> * Клас `B` є **статичним вкладеним класом**.
> * Для створення його екземпляра **не потрібен** екземпляр зовнішнього класу `A`.
> * Синтаксис `outerInstance.new InnerClass()` використовується **лише для не-статичних (внутрішніх) класів**.
>
> Спроба використати такий синтаксис для статичного класу є помилкою.
>
> **Правильний спосіб створити екземпляр `B`:**
>
> ```java
> A.B b = new A.B();
> b.print();
> ```
>
> **Якщо розкоментувати `System.out.println(field)`:**
> Код також не скомпілюється з іншої причини: `static` клас `B` не має доступу до не-статичного поля `field` з `A`.

#### **📌 Що перевіряють:**

*Точне знання синтаксису створення різних типів вкладених класів. Це питання-пастка, яке перевіряє уважність.*

* * *

### **13 ❓ Питання:**

Чи може статичний вкладений клас реалізовувати інтерфейс або успадковувати інший клас? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, безперечно.**
>
> **Статичний вкладений клас** — це, по суті, звичайний клас верхнього рівня, який просто для зручності "запаковано" всередину іншого. Він підкоряється **всім тим самим правилам** ООП, що і звичайні класи.
>
> * **Успадкування:** Він може **успадкувати (`extends`)** будь-який інший клас (окрім `final`).
> * **Реалізація інтерфейсів:** Він може **реалізовувати (`implements`)** будь-яку кількість інтерфейсів.
>
> **Приклад:**
>
> ```java
> public class Manager {
>
>     // Цей клас представляє стратегію обчислення бонусів
>     public static class BonusCalculator implements Callable<Double> {
>
>         private double sales;
>
>         public BonusCalculator(double sales) { this.sales = sales; }
>
>         @Override
>         public Double call() throws Exception {
>             return sales * 0.1; // 10% бонус
>         }
>     }
> }
> ```
>
> Тут статичний вкладений клас `BonusCalculator` реалізує стандартний інтерфейс `Callable`. Це поширена практика для групування логічно пов'язаних класів.

#### **📌 Що перевіряють:**

*Розуміння того, що `static` на класі знімає обмеження, пов'язані з екземпляром зовнішнього класу, але не накладає нових обмежень на наслідування.*

* * *

### **14 ❓ Питання:**

Які є ризики при ініціалізації статичних полів з логікою, що залежить від порядку завантаження класів? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже небезпечна ситуація, яка може призвести до **непередбачуваної поведінки та важковиловлюваних помилок**.
>
> **Проблема:**
>
> JVM **не гарантує** строгий порядок завантаження класів, якщо вони не мають явних залежностей. Порядок може змінюватися залежно від реалізації JVM, версії Java, та шляху виконання коду.
>
> Якщо ініціалізація статичного поля в класі `A` залежить від значення статичного поля в класі `B`, а ініціалізація `B` — від `A` (циклічна залежність), або якщо їхній порядок завантаження може змінитися, ви можете отримати:
>
> * **`NullPointerException`:** Якщо ви звертаєтесь до статичного поля класу, який ще не був ініціалізований.
> * **Некоректні значення:** Поле може бути ініціалізоване значенням за замовчуванням (`0` або `null`), а не тим, яке очікувалося.
> * **`ExceptionInInitializerError`:** Якщо ініціалізація призводить до винятку.
>
> **Приклад:**
>
> ```java
> class A { public static int SIZE = B.DATA.length(); }
> class B { public static String DATA = "hello"; }
>
> // Якщо першим буде ініціалізовано A, він отримає NPE на B.DATA.
> // Якщо першим буде B, все буде добре.
> ```
>
> **Як уникнути:**
>
> 1. **Уникати статичних ініціалізаторів, що залежать від інших класів.**
> 2. Використовувати **ліниву ініціалізацію** через `static` метод (патерн Initialization-on-demand holder).
> 3. Переносити таку логіку на етап явного запуску додатку (наприклад, у `main` або в логіку старту фреймворку), де порядок можна контролювати.

#### **📌 Що перевіряють:**

*Розуміння того, що `static` створює глобальний стан, управління яким може бути дуже складним. Здатність бачити потенційні проблеми, пов'язані з порядком ініціалізації.*

* * *

### **15 ❓ Питання:**

Поясніть, що таке `static` метод в інтерфейсі (з Java 8) і як він відрізняється від `static` методу в утилітарному класі. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Статичний метод в інтерфейсі** — це метод з реалізацією, що належить самому інтерфейсу і викликається через `InterfaceName.methodName()`.
>
> Його введення **змінило** необхідність у створенні окремих утилітарних класів.
>
> **Відмінності:**
>
| Характеристика | `static` метод в інтерфейсі | `static` метод в утилітарному класі |
| :---: | :---: | :---: |
| **Призначення** | Надання утилітарної логіки, **тісно пов'язаної з контрактом** інтерфейсу. | Надання загальної утилітарної логіки. |
| **Розташування** | Логіка знаходиться **поруч з абстракцією**, до якої вона відноситься. | Логіка винесена в **окремий, непов'язаний клас**. |
| **Успадкування** | **Не успадковується** класами-реалізаторами. | Так само не успадковується. |
| **Гнучкість** | Дозволяє створювати **"самодостатні" інтерфейси**. Наприклад, `Comparator` містить і контракт `compare()`, і статичні методи-фабрики (`comparing()`).| Більш традиційний підхід. |
>
> **Приклад порівняння:**
>
> * **Старий підхід:** Інтерфейс `Collection`, утилітарний клас `Collections` з методом `sort()`.
> * **Сучасний підхід:** Інтерфейс `Comparator` з `static` методами `comparingInt()`, `naturalOrder()`.
>
> Статичні методи в інтерфейсах роблять API **більш згуртованим (cohesive)**, оскільки утилітарні функції тепер можуть жити поруч із самим контрактом. Це вважається більш сучасним та чистим дизайном.

#### **📌 Що перевіряють:**

*Розуміння того, як сучасні фічі Java дозволяють покращити дизайн коду і зробити його більш логічним та самодостатнім.*
