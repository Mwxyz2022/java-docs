---
layout: default
title: "Що таке інтерфейс (Interface)"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 1
---

# Що таке інтерфейс (Interface)

* короткий опис: Інтерфейс – це абстрактний тип, що визначає набір абстрактних методів (контракт), які мають бути реалізовані в класі, що його імплементує. До Java 8 інтерфейси містили лише абстрактні методи та константи.

* поняття: `interface`, `implements`, абстракція через контракт.

* приклади: `public interface Drawable { void draw(); }`, `class Circle implements Drawable`.

* ключові моменти: Визначає, що клас повинен робити, але не як. Клас зобов'язаний реалізувати всі абстрактні методи.

* що важливо знати на співбесіді: Визначення, основні властивості, як реалізується.

### **✅ Офіційне визначення:**

**`Інтерфейс`** в Java — це тип, який визначає набір абстрактних методів (контракт), які мають бути реалізовані класом,
що його імплементує. Оголошується за допомогою ключового слова **`interface`**.

### **🧠 Простими словами:**

Уяви інтерфейс як "чекліст". Якщо клас його реалізує, то зобов’язаний виконати всі пункти. Сам по собі інтерфейс не містить реалізацій — лише те,*що потрібно зробити*, а не *як*

---

## **Основне**

|        Компонент        |                      Пояснення                       |
|:-----------------------:|:----------------------------------------------------:|
|     **`interface`**     |       Ключове слово для оголошення інтерфейсу        |
|     **Абстракція**      |        Повністю абстрактний тип (до Java 8\)         |
|   **Метод без тіла**    |     Методи мають лише сигнатури, без реалізації      |
|    **`implements`**     |   Ключове слово для реалізації інтерфейсу у класі    |
|   **Обов'язковість**    | Усі методи інтерфейсу мають бути реалізовані у класі |
| **Множинна реалізація** | Клас може реалізовувати кілька інтерфейсів одночасно |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

Щоб задати, *що повинен робити клас*, не описуючи, *як саме*. Це підвищує гнучкість і тестованість.

🔸 **У чому особливість?**  
Інтерфейс — це набір вимог. Він не має стану або логіки. Це як "роль": будь-хто може бути **`Flyable`**, якщо реалізує
метод **`fly()`**.

🔸 **Які "граблі"?**  
Забув реалізувати хоча б один метод — отримаєш помилку компіляції. А ще не можна мати змінні стану (тільки **`public`**
`static final` константи).

🔸 **Коли застосовувати?**  
Коли треба абстрагувати поведінку: наприклад, і **`Кіт`**, і **`Робот`** можуть "говорити", але реалізують це
по-різному

---

## **Корисні приклади (🧪)**

```java
// 🔻 Оголошення інтерфейсу
public interface Drawable {
    void draw();
}

// 🔻 Клас реалізує інтерфейс
public class Circle implements Drawable {
    @Override
    public void draw() {
        System.out.println("Drawing a circle");
    }
}

// 🔻 Інший клас реалізує інтерфейс по-своєму
public class Square implements Drawable {
    @Override
    public void draw() {
        System.out.println("Drawing a square");
    }
}
```

---

## **Порівняння (⚖️)**

|        Параметр         |            Інтерфейс             |   Абстрактний клас   |
|:-----------------------:|:--------------------------------:|:--------------------:|
|    **Ключове слово**    |         **`interface`**          | **`abstract class`** |
| **Метод з реалізацією** |          ❌ (до Java 8\)          |          ✅           |
|        **Поля**         | Тільки **`public static final`** |       Будь-які       |
|    **Наслідування**     |    Можна реалізовувати багато    |    Лише один клас    |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
interface =інструкція
implements=реалізація
Не реалізував
метод --
буде біда!
```

👾 Асоціюй з театром: інтерфейс — це сценарій, клас — актор, що грає роль

---

## **🟩 Підсумок:**

**`Інтерфейс`** — це контракт, який описує, які методи має реалізовувати клас. Це механізм для досягнення абстракції
поведінки без реалізації. Клас, що реалізує інтерфейс, зобов’язаний реалізувати всі його методи. Це робить код гнучким,
масштабованим і легко тестованим. В Java інтерфейси — ключ до слабкого зв’язування та модульності.

---

### **🔗 Додаткові джерела:**

* [**Офіційна документація Oracle: Interface**](https://docs.oracle.com/javase/tutorial/java/IandI/createinterface.html)
* [**Java Language Specification §9.1**](https://docs.oracle.com/javase/specs/jls/se17/html/jls-9.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс в Java і яка його основна мета?

> #### **💬 Відповідь:**
>
> **Інтерфейс** в Java — це повністю **абстрактний тип**, який використовується для визначення **контракту поведінки**. Він описує набір методів, які клас, що його реалізує (`implements`), зобов'язаний надати.
>
> **Основна мета інтерфейсу:**
>
> 1. **Визначення API/контракту:** Інтерфейс говорить, **ЩО** об'єкт повинен вміти робити, але не каже, **ЯК** він це робить. Це дозволяє відокремити специфікацію від реалізації.
> 2. **Досягнення повної абстракції:** Він надає 100% абстрактний шаблон для поведінки.
> 3. **Підтримка поліморфізму:** Дозволяє працювати з об'єктами різних класів через єдиний тип (тип інтерфейсу), якщо вони всі реалізують цей інтерфейс.
> 4. **Реалізація множинного наслідування типів:** Клас в Java може реалізувати багато інтерфейсів, отримуючи таким чином багато "ролей" або типів.
>
> **Приклад:** `interface List` визначає, що будь-яка його реалізація (`ArrayList`, `LinkedList`) повинна мати методи `add()`, `get()`, `size()` тощо.

#### **📝 Аналогія:**

Інтерфейс — це як пульт від телевізора. Він має кнопки (`on()`, `setChannel(int)`, `setVolume(int)`), які визначають, що ви можете робити. Але йому байдуже, як саме телевізор (конкретний клас) реалізує ці команди — чи це плазма, чи LED, чи кінескопний.

#### **📌 Що перевіряють:**

*Розуміння того, що інтерфейс — це в першу чергу контракт, а не просто набір методів. Це фундаментальна ідея ОО-дизайну.*

* * *

### **2 ❓ Питання:**

Які члени може містити інтерфейс у сучасній Java (Java 8+)?

> #### **💬 Відповідь:**
>
> З еволюцією Java можливості інтерфейсів значно розширилися. У сучасній Java інтерфейс може містити:
>
> 1. **Абстрактні методи:** Це класичні методи без тіла, які є неявно `public` та `abstract`. Вони визначають основний контракт.
>     `void doSomething();`
>
> 2. **`default` методи (з Java 8):**
>     * Це **публічні** методи з тілом реалізації. Вони дозволяють додавати нову функціональність в інтерфейси, не ламаючи існуючі класи.
>     * `default void log(String message) { ... }`
>
> 3. **`static` методи (з Java 8):**
>     * Це **публічні** статичні методи, які належать самому інтерфейсу. Зазвичай це утилітарні (helper) методи, пов'язані з інтерфейсом.
>     * `static void utilityMethod() { ... }`
>
> 4. **Константи:**
>     * Будь-яке поле, оголошене в інтерфейсі, є неявно **`public static final`**.
>     * `int MAX_RETRIES = 5;`
>
> 5. **`private` методи (з Java 9):**
>     * Це допоміжні методи (як статичні, так і нестатичні) з тілом реалізації, які використовуються для уникнення дублювання коду всередині `default` та `static` методів самого інтерфейсу. Вони не є частиною публічного API.

#### **📌 Що перевіряють:**

*Знання сучасного синтаксису Java та еволюції інтерфейсів. Кандидат повинен знати про нововведення Java 8 та 9.*

* * *

### **3 ❓ Питання:**

Поясніть, що означає "програмувати на рівні інтерфейсу, а не реалізації". Чому це є гарною практикою?

> #### **💬 Відповідь:**
>
> Це один з найважливіших принципів об'єктно-орієнтованого дизайну. Він означає, що змінні, параметри методів та типи повернення повинні бути оголошені з типом **інтерфейсу**, а не конкретного класу.
>
> **Приклад:**
>
> * **Погано:** `ArrayList<User> users = new ArrayList<>();`
> * **Добре:** `List<User> users = new ArrayList<>();`
>
> **Чому це важливо:**
>
> 1. **Гнучкість та слабка зв'язність (Loose Coupling):** Ваш код залежить від **контракту (`List`)**, а не від **деталей реалізації (`ArrayList`)**. Це дозволяє вам у будь-який момент легко **замінити реалізацію**, наприклад, на `LinkedList`, **не змінюючи решту коду**, який працює зі змінною `users`.
>
>     `List<User> users = new LinkedList<>(); // Єдина зміна`
>
> 2. **Розширюваність:** Легко додавати нові класи, що реалізують той самий інтерфейс, і система буде з ними працювати без змін.
>
> 3. **Підвищення тестованості:** При тестуванні ви можете легко підставити mock-об'єкт, який реалізує той самий інтерфейс.
>
> Цей підхід є основою для побудови гнучких, підтримуваних та масштабованих систем.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти, як цей принцип допомагає досягти ключових цілей хорошого дизайну ПО.*

* * *

### **4 ❓ Питання:**

Чому клас може реалізовувати багато інтерфейсів, але успадковувати лише один клас?

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, **ЩО саме** успадковується, та з бажанням уникнути **"проблеми ромба"**.
>
> * **При успадкуванні від класу (`extends`):**
>   * Дочірній клас успадковує не тільки **контракт (сигнатури методів)**, але й **реалізацію (код методів)** та **стан (поля)**.
>   * Якби було дозволено множинне наслідування класів (наприклад, `class C extends A, B`), і `A` та `B` мали б метод з однаковою назвою, але різною реалізацією, виникла б неоднозначність — яку реалізацію обрати для `C`? Це і є "проблема ромба". Java забороняє це, щоб уникнути такої складності.
>
> * **При реалізації інтерфейсу (`implements`):**
>   * Клас успадковує лише **контракт** — зобов'язання реалізувати певні методи. Він **не успадковує реалізацію**.
>   * Клас, що реалізує інтерфейси, сам **надає власну реалізацію** для всіх абстрактних методів. Тому конфлікту реалізацій не виникає.
>
> Навіть з появою `default` методів, які додають реалізацію в інтерфейси, Java вирішує проблему ромба, вимагаючи від класу явно перевизначити конфліктний метод.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, чому множинне наслідування реалізації є проблемою, а множинне наслідування типів — ні.*

* * *

### **5 ❓ Питання:**

Чи може інтерфейс мати конструктор? Чому?

> #### **💬 Відповідь:**
>
> **Ні, інтерфейс не може мати конструктора.**
>
> **Причина:**
>
> * Призначення конструктора — **ініціалізувати стан (поля)** об'єкта.
> * Інтерфейси за своєю природою **не можуть мати полів стану екземпляра**. Вони можуть містити лише `public static final` константи, які належать самому інтерфейсу, а не об'єкту.
> * Оскільки немає стану для ініціалізації, то і конструктор не має сенсу.
>
> Крім того, ви **не можете створити екземпляр інтерфейсу** напряму за допомогою оператора `new`. Конструктор просто ніколи не був би викликаний у контексті самого інтерфейсу.
>
> Абстрактні класи, на відміну від інтерфейсів, можуть мати стан і тому мають конструктори, які викликаються їхніми спадкоємцями.

#### **📌 Що перевіряють:**

*Знання семантики інтерфейсів та розуміння, чому деякі мовні конструкції є для них неприпустимими.*

* * *

### **6 ❓ Питання:**

Що таке `default` метод в інтерфейсі і яку проблему він вирішив? ⭐️

> #### **💬 Відповідь:**
>
> **`default` метод** — це метод в інтерфейсі, який має **тіло реалізації** і оголошений з ключовим словом `default`.
>
> **Проблему, яку він вирішив:**
>
> Він вирішив проблему **еволюції інтерфейсів** у зворотно сумісний спосіб.
>
> * **До Java 8:** Якщо потрібно було додати новий метод до існуючого інтерфейсу (наприклад, до `List`), це "ламало" всі існуючі класи, що реалізовували цей інтерфейс, оскільки вони були зобов'язані надати реалізацію для нового методу.
> * **З Java 8:** Завдяки `default` методам, можна додати новий метод з **реалізацією за замовчуванням**. Усі існуючі класи автоматично "успадкують" цю `default`-реалізацію і їхній код не "зламається". При бажанні, вони можуть перевизначити цей метод.
>
> Це дозволило розробникам JDK додати нові методи, як `forEach` в `Iterable` або багато методів в `Stream API`, не порушуючи сумісність з мільйонами рядків існуючого коду.

#### **📌 Що перевіряють:**

*Знання сучасних фіч Java та розуміння архітектурних проблем, які вони вирішують.*

* * *

### **7 ❓ Питання:**

Поясніть призначення `static` методів в інтерфейсах. Чим вони відрізняються від `default` методів? ⭐️

> #### **💬 Відповідь:**
>
> **`static` методи** в інтерфейсах (з Java 8) — це методи, які належать **самому інтерфейсу**, а не екземплярам класів, що його реалізують.
>
> **Призначення:**
>
> Вони дозволяють групувати **утилітарні (helper) методи** безпосередньо в інтерфейсі, до якого вони логічно відносяться. Раніше для цього доводилося створювати окремі класи-помічники (наприклад, `Collections` для інтерфейсу `Collection`).
>
> **Чим вони відрізняються від `default` методів:**
>
| Характеристика | `static` метод | `default` метод |
| :---: | :---: | :---: |
| **Приналежність** | Належить **інтерфейсу**. | Належить **екземпляру** класу, що реалізує інтерфейс.|
| **Виклик** | Викликається через ім'я інтерфейсу: `MyInterface.staticMethod()`. | Викликається на об'єкті: `myObject.defaultMethod()`. |
| **Успадкування** | **Не успадковується** класами-реалізаторами. | **Успадковується** класами-реалізаторами. |
| **Перевизначення** | **Не можна** перевизначити. | **Можна** перевизначити. |

#### **📌 Що перевіряють:**

*Здатність чітко розрізняти `static` та `default` методи в інтерфейсах та їхні різні сценарії використання.*

* * *

### **8 ❓ Питання:**

Які модифікатори доступу можуть мати методи та поля в інтерфейсі?

> #### **💬 Відповідь:**
>
> В інтерфейсах існують строгі **неявні (implicit)** модифікатори для їхніх членів.
>
> **Для полів:**
>
> * Будь-яке поле є **неявно `public static final`**.
> * `int MAX_USERS = 100;` автоматично стає `public static final int MAX_USERS = 100;`. Інші модифікатори не дозволені.
>
> **Для методів:**
>
> * **Абстрактні методи:** неявно **`public`** (і `abstract`).
> * **`default` методи (з Java 8):** неявно **`public`**.
> * **`static` методи (з Java 8):** неявно **`public`**. Ви не можете зробити їх `protected` чи `private` (у сенсі видимості ззовні).
> * **`private` методи (з Java 9):** можна оголошувати методи як `private`. Вони використовуються лише для внутрішніх потреб інтерфейсу (як допоміжні для `default` або `static` методів) і не є частиною його публічного API.
>
> Отже, в сучасному інтерфейсі методи можуть бути `public` або `private`.

#### **📌 Що перевіряють:**

*Точне знання правил мови, що стосуються інтерфейсів, включаючи неявні модифікатори.*

* * *

### **9 ❓ Питання:**

Чи може інтерфейс розширювати інший інтерфейс? Якщо так, яке ключове слово використовується і скільки інтерфейсів можна розширити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може.**
>
> **Ключове слово:** Для розширення одного інтерфейсу іншим використовується те саме ключове слово, що і для класів — **`extends`**.
>
> **Кількість:** На відміну від класів, інтерфейс може успадковувати **кілька інших інтерфейсів** одночасно. Це є формою **множинного наслідування** в Java, яка є безпечною, оскільки успадковуються лише контракти.
>
> **Приклад:**
>
> ```java
> interface Movable { void move(); }
> interface Flyable { void fly(); }
> interface Swimmable { void swim(); }
>
> // Цей інтерфейс об'єднує три інші контракти
> interface SuperDrone extends Movable, Flyable, Swimmable {}
>
> // Клас, що реалізує SuperDrone, тепер зобов'язаний
> // надати реалізацію для всіх трьох методів.
> class MyDrone implements SuperDrone {
>     @Override public void move() { /* ... */ }
>     @Override public void fly() { /* ... */ }
>     @Override public void swim() { /* ... */ }
> }
> ```
>
> Це дозволяє створювати складні та гнучкі ієрархії контрактів.

#### **📌 Що перевіряють:**

*Знання того, як будуються ієрархії інтерфейсів, та розуміння ролі `extends` у цьому контексті.*

* * *

### **10 ❓ Питання:**

Як вирішується конфлікт, якщо клас реалізує два інтерфейси, які мають абстрактні методи з однаковою сигнатурою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У цьому випадку **конфлікту не виникає**.
>
> **Пояснення:**
>
> * Інтерфейси визначають лише **контракт**, тобто вимагають від класу надати реалізацію.
> * Якщо два інтерфейси (`A` та `B`) мають метод `void doWork()`, це просто означає, що клас, який їх реалізує, отримує **два однакові зобов'язання**.
> * Клас-реалізатор повинен надати **лише одну реалізацію** методу `doWork()`, яка задовольнить обидва контракти одночасно.
>
> **Приклад:**
>
> ```java
> interface Speaker { void speak(); }
> interface Announcer { void speak(); }
>
> // Конфлікту немає. Клас просто повинен реалізувати speak()
> class RadioHost implements Speaker, Announcer {
>     @Override
>     public void speak() {
>         System.out.println("Speaking on air...");
>     }
> }
> ```
>
> Проблема виникає лише тоді, коли обидва інтерфейси мають `default` методи з однаковою сигнатурою, оскільки тоді виникає конфлікт **реалізацій**.

#### **📌 Що перевіряють:**

*Здатність розрізняти конфлікт контрактів (якого немає) та конфлікт реалізацій.*

* * *

### **11 ❓ Питання:**

Поясніть різницю в призначенні `default` методу в інтерфейсі та методу в абстрактному класі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча і те, і інше надає готову реалізацію, їхні **семантичні призначення** різні.
>
> * **Метод в абстрактному класі:**
>   * **Призначення:** Надати **спільну базову логіку** для тісно пов'язаної ієрархаті класів, які мають **спільний стан**. Цей метод зазвичай покладається на поля або інші методи цього ж абстрактного класу. Він є частиною **скелету реалізації**.
>
> * **`default` метод в інтерфейсі:**
>   * **Призначення:** Надати **поведінку за замовчуванням** для класів, що реалізують інтерфейс, або забезпечити **зворотну сумісність** при додаванні нових методів.
>   * Він не може покладатися на стан екземпляра (бо інтерфейс його не має).
>   * Це скоріше **опціональна функціональність**, яку можна використати або перевизначити, а не фундаментальна частина базового класу.
>
> **Коротко:** метод в абстрактному класі — це про **спільне ядро**. `default` метод — це про **додаткову або опціональну поведінку**.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що схожі технічні можливості можуть слугувати різним дизайнерським цілям.*

* * *

### **12 ❓ Питання:**

Що таке функціональний інтерфейс і як анотація `@FunctionalInterface` допомагає в роботі з ним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Функціональний інтерфейс** (також відомий як SAM-інтерфейс) — це інтерфейс, який містить **рівно один абстрактний метод**.
>
> Він може містити будь-яку кількість `default` або `static` методів, але абстрактний метод має бути лише один.
>
> **Роль `@FunctionalInterface`:**
>
> * Це **інформаційна анотація** для компілятора та інших розробників.
> * Вона **не є обов'язковою** для того, щоб інтерфейс був функціональним, але її використання є **найкращою практикою**.
>
> **Чим вона допомагає:**
>
> * **Перевірка компілятором:** Якщо ви позначили інтерфейс як `@FunctionalInterface`, компілятор перевірить, чи він дійсно містить лише один абстрактний метод. Якщо ви випадково додасте другий, компілятор видасть **помилку**. Це захищає контракт інтерфейсу.
> * **Документація:** Явно показує намір, що цей інтерфейс призначений для використання з **лямбда-виразами**.
>
> Будь-який лямбда-вираз, сигнатура якого збігається з абстрактним методом функціонального інтерфейсу, може бути представлений цим інтерфейсом.

#### **📌 Що перевіряють:**

*Знання ключових концепцій функціонального програмування в Java 8, ролі анотацій та їхньої користі.*

* * *

### **13 ❓ Питання:**

Наведіть приклад патерну "Стратегія", реалізованого за допомогою інтерфейсів. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн **"Стратегія"** є ідеальною демонстрацією потужності інтерфейсів для створення гнучкої архітектури. Він дозволяє визначати сімейство алгоритмів, інкапсулювати кожен з них і робити їх взаємозамінними.
>
> **Кроки реалізації:**
>
> 1. **Створюється інтерфейс Стратегії**, який визначає єдиний метод.
>
>     ```java
>     // Інтерфейс - контракт для всіх стратегій
>     interface PaymentStrategy {
>         void pay(int amount);
>     }
>     ```
>
> 2. **Створюються конкретні реалізації-стратегії.**
>
>     ```java
>     class CreditCardStrategy implements PaymentStrategy {
>         public void pay(int amount) { /* логіка оплати карткою */ }
>     }
>     class PayPalStrategy implements PaymentStrategy {
>         public void pay(int amount) { /* логіка оплати через PayPal */ }
>     }
>     ```
>
> 3. **Клас-контекст** використовує інтерфейс і може динамічно змінювати стратегію.
>
>     ```java
>     class ShoppingCart {
>         private PaymentStrategy paymentStrategy;
>
>         // Стратегія передається ззовні
>         public void setPaymentStrategy(PaymentStrategy strategy) {
>             this.paymentStrategy = strategy;
>         }
>
>         public void checkout(int total) {
>             // Делегуємо виконання обраній стратегії
>             paymentStrategy.pay(total);
>         }
>     }
>     ```
> **Використання:**
>
> `cart.setPaymentStrategy(new PayPalStrategy()); cart.checkout(100);`
>
> Завдяки інтерфейсу `ShoppingCart` не знає нічого про конкретні способи оплати і може працювати з будь-якою новою стратегією, не змінюючи свій код.

#### **📌 Що перевіряють:**

*Знання патернів проектування та здатність продемонструвати на їхньому прикладі, як інтерфейси допомагають реалізувати принципи SOLID (зокрема, OCP та DIP).*

* * *

### **14 ❓ Питання:**

Чому клас `java.util.Collections` є класом, а не інтерфейсом, хоча містить лише статичні утилітарні методи? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання стосується **історичного дизайну Java**. Клас `Collections` був створений у **Java 1.2**, задовго до того, як в інтерфейсах з'явилася підтримка `static` методів (це сталося лише в **Java 8**).
>
> * **У ті часи**, єдиним способом згрупувати набір утилітарних статичних методів був **клас**.
> * Щоб заборонити створення екземплярів таких утилітарних класів, для них створювали **приватний конструктор**. Саме так і зроблено в `java.util.Collections`.
>
> **Якби `Collections API` проектувалося сьогодні (в епоху Java 8+):**
>
> Ймовірно, багато з цих методів були б **статичними методами** безпосередньо в інтерфейсах `Collection`, `List`, `Set`. Наприклад, замість `Collections.sort(list)` ми б мали `List.sort(list)`, або, як зараз, `list.sort(comparator)`.
>
> Частково це вже сталося:
>
> * Інтерфейс `List` отримав статичний метод `List.of(...)`.
> * Інтерфейс `Comparator` отримав безліч статичних методів-помічників.
>
> Отже, `Collections` є класом через історичні обмеження мови на момент його створення.

#### **📌 Що перевіряють:**

*Знання історії та еволюції Java. Розуміння того, чому певні дизайнерські рішення в JDK є такими, якими вони є.*

* * *

### **15 ❓ Питання:**

Розгляньте таку ієрархію: `interface A { default void m() {} }`, `interface B extends A {}`, `class C implements B {}`. Яка реалізація методу `m()` буде викликана для об'єкта класу `C`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Для об'єкта класу `C` буде викликана **`default`-реалізація методу `m()` з інтерфейсу `A`**.
>
> **Пояснення механізму наслідування `default`-методів:**
>
> 1. `Default` методи, як і звичайні, **успадковуються** по ієрархії.
>
> 2. Інтерфейс `B` **розширює (`extends`)** інтерфейс `A`. Це означає, що `B` **успадковує** всі члени `A`, включаючи `default` метод `m()`. Оскільки `B` не перевизначає цей метод, він просто "пропускає" його далі по ієрархії.
>
> 3. Клас `C` **реалізує (`implements`)** інтерфейс `B`. Оскільки `B` має (успадкований) `default` метод `m()`, клас `C` **не зобов'язаний** його реалізовувати. Він також "успадковує" цю `default`-реалізацію.
>
> 4. Тому, коли ми створюємо `new C()` і викликаємо `.m()`, буде використана найближча доступна реалізація в ієрархії, якою є `default`-метод з інтерфейсу `A`.
>
> **Якщо б `C` перевизначив метод `m()`**, то була б викликана його власна реалізація.
>
> **Якщо б `B` також мав `default` метод `m()`**, то клас `C` успадкував би версію з `B`, оскільки `B` є більш специфічним типом для `C`.

#### **📌 Що перевіряють:**

*Глибоке розуміння правил наслідування `default` методів в ієрархаті інтерфейсів. Це просунуте питання про сучасні можливості Java.*
