---
layout: default
title: "Порядок ініціалізації в Java"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 10
---

# Порядок ініціалізації в Java

* короткий опис: Суворий порядок, у якому JVM ініціалізує члени класу та об'єкти: спочатку статичні поля, потім статичні блоки, потім нестатичні поля, потім нестатичні блоки, і наприкінці – конструктор.

* поняття: Порядок ініціалізації, `static` поля/блоки, instance поля/блоки, конструктор.

* приклади: Демонстрація порядку виконання через `System.out.println()` у різних блоках.

* ключові моменти: `static` ініціалізація (один раз) відбувається до `instance` ініціалізації (при кожному `new`).

* що важливо знати на співбесіді: Детальний порядок ініціалізації, можливі помилки.

### **✅ Офіційне визначення:**

У Java є суворий порядок, у якому відбувається ініціалізація класу і його об’єктів. Спершу ініціалізуються **static** елементи класу, потім — нестатичні (instance), а наприкінці виконується конструктор.

**🔗 [JLS §12.4 Initialization of Classes](https://docs.oracle.com/javase/specs/jls/se17/html/jls-12.html#jls-12.4)**

### **🧠 Простими словами:**

🔹 Спершу запускаються статичні змінні та блоки (вони належать класу, як твій улюблений шеф)

🔹 Потім ініціалізуються екземплярні змінні та блоки (для кожного нового об’єкта своя історія)

🔹 І наостанок конструктор — це як фінальний акорд

---

## **Повна послідовність ініціалізації (📊)**

| Етап | Що виконується | Де це описується в коді |
|:----:| :---: | :---: |
|  1   | Ініціалізація **`static`** полів | **`static int x = 10;`** |
|  2   | Виконання **`static`** блоків | **`static { ... }`** |
|  3   | Ініціалізація **`instance`** полів | **`int y = 20;`** |
|  4   | Виконання ініціалізаційних блоків | **`{ ... }`** |
|  5   | Виклик конструктора | **`MyClass() { ... }`** |

---

## **Пояснення на пальцях (💡)**

🔸 Чому важливо знати?  
Бо можна натрапити на ***NullPointerException*** або непередбачувану поведінку, якщо змінні ще не проініціалізовані.

🔸 Що таке ініціалізаційний блок **`{ ... }`**?  
Це шматок коду, який виконується до конструктора, і його можна використовувати для спільної логіки для всіх конструкторів.

🔸 Коли виконується **`static { ... }`**?  
Він запускається один раз при завантаженні класу, а ініціалізаційні блоки для інстансу — кожного разу при створенні нового об'єкта
---

## **Корисні приклади (🧪)**

```java
public class InitializationOrder {

    // 1 Static field
    static int staticField = initStaticField();

    // 2 Static block
    static {
        System.out.println("2 Static block");
    }

    // 3 Instance field
    int instanceField = initInstanceField();

    // 4 Instance initialization block
    {
        System.out.println("4 Instance init block");
    }

    // 5 Constructor
    public InitializationOrder() {
        System.out.println("5 Constructor");
    }

    private static int initStaticField() {
        System.out.println("1 Static field init");
        return 100;
    }

    private int initInstanceField() {
        System.out.println("3 Instance field init");
        return 200;
    }

    public static void main(String[] args) {
        System.out.println("▶ Creating object 1:");
        new InitializationOrder();

        System.out.println("\n▶ Creating object 2:");
        new InitializationOrder();
    }
}
```

**🖨 Вивід програми:**

```java
1 Static field init  
2 Static block  
▶ Creating object 1:  
3 Instance field init  
4 Instance init block  
5 Constructor  

▶ Creating object 2:  
3 Instance field init  
4 Instance init block  
5 Constructor 
```

---

## **Візуальне порівняння (⚖️)**

| Етап | Статичний (static) | Нестатичний (instance) |
| :---: | :---: | :---: |
| **Поля** | **`static int x = …;`** | **`int y = …;`** |
| **Блоки** | **`static { ... }`** | **`{ ... }`** |
| **Виконується коли** | Один раз при завантаженні класу | Кожного разу при створенні об’єкта |
| **Має доступ до `this`** | ❌ Ні | ✅ Так |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **"S-S-I-I-C"** —

**Static field -> Static block -> Instance field -> Instance block -> Constructor**

---

## **🟩 Підсумок:**

Java ініціалізує все чітко і послідовно:  
**`static field -> static block -> instance field -> instance block -> constructor.`**  
Знання цієї послідовності допомагає уникати багів і робити код стабільним, як хороший old-school legacy.

---

### **🔗 Додаткові джерела:**

**🔗 [Java Tutorial: Initialization Blocks](https://docs.oracle.com/javase/tutorial/java/javaOO/initial.html)**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Який загальний порядок ініціалізації при створенні об'єкта в Java?

> #### **💬 Відповідь:**
>
> Процес ініціалізації можна розділити на два основні етапи: **ініціалізація класу** (виконується один раз) та **ініціалізація екземпляра** (виконується при кожному `new`).
>
> **Етап 1: Ініціалізація Класу (виконується лише при першому зверненні до класу)**
>
> 1. Спочатку ініціалізуються **`static` поля** класу.
> 2. Потім виконуються **`static {}` блоки ініціалізації**.
> *Якщо в класі є кілька статичних полів та блоків, вони виконуються в тому порядку, в якому вони написані в коді.*
>
> **Етап 2: Ініціалізація Екземпляра (виконується при кожному `new MyClass()`)**
> 3.  Спочатку ініціалізуються **не-`static` (instance) поля**.
> 4.  Потім виконуються **не-`static` `{}` блоки ініціалізації** (також відомі як блоки ініціалізації екземпляра).
> *Поля та блоки ініціалізації екземпляра також виконуються в тому порядку, в якому вони написані.*
> 5.  В останню чергу виконується **конструктор**.
>
> **Коротка формула:** `Static` ініціалізація → `Instance` ініціалізація → Конструктор.

#### **📝 Мнемоніка:**

* Спочатку готується **фабрика** (клас, `static` частина).
* Потім "з конвеєра" сходить **продукт** (об'єкт, `instance` частина).
* І в кінці на продукт ставиться **штамп** (конструктор).

#### **📌 Що перевіряють:**

*Базове знання послідовності кроків. Це фундаментальне питання для розуміння життєвого циклу об'єкта.*

* * *

### **2 ❓ Питання:**

Який порядок ініціалізації в ієрархії наслідування (батьківський та дочірній класи)?

> #### **💬 Відповідь:**
>
> Ініціалізація завжди відбувається **"згори вниз"** — від найвищого суперкласу до найнижчого підкласу.
>
> **Повна послідовність для `new Child()` (де `Child extends Parent`):**
>
> **Етап 1: Статична ініціалізація**
>
> 1. **Статичні ініціалізатори `Parent`** (поля та `static` блоки).
> 2. **Статичні ініціалізатори `Child`** (поля та `static` блоки).
>
> *Цей етап виконується лише один раз для кожного класу.*
>
> **Етап 2: Ініціалізація екземпляра**
> 3.  Виклик конструктора `Child()` призводить до неявного `super()`.
> 4.  **Не-статичні ініціалізатори `Parent`** (поля та `{}` блоки).
> 5.  Тіло конструктора **`Parent`**.
> 6.  **Не-статичні ініціалізатори `Child`** (поля та `{}` блоки).
> 7.  Тіло конструктора **`Child`**.
>
> Таким чином, JVM гарантує, що на момент, коли дочірній клас починає свою ініціалізацію, вся його успадкована частина від батька вже повністю готова.

#### **📌 Що перевіряють:**

*Розуміння того, як ланцюжок виклику `super()` впливає на порядок ініціалізації в ієрархії.*

* * *

### **3 ❓ Питання:**

Що таке блок ініціалізації екземпляра (`{}`)? Чим він відрізняється від конструктора і коли його доцільно використовувати?

> #### **💬 Відповідь:**
>
> **Блок ініціалізації екземпляра (`{ ... }`)** — це блок коду без назви, який розміщується всередині класу, поза будь-яким методом.
>
> **Чим відрізняється від конструктора:**
>
> * **Порядок виконання:** Він виконується **ДО** виконання тіла будь-якого конструктора.
> * **Застосування:** Код з цього блоку буде виконаний при виклику **БУДЬ-ЯКОГО** конструктора цього класу.
>
> **Коли його доцільно використовувати:**
>
> 1. **Для спільної логіки ініціалізації:** Якщо у вас є кілька перевантажених конструкторів, і всі вони повинні виконати якусь спільну логіку перед своєю основною роботою.
>
>     **Але цей випадок краще реалізувати через ланцюжок конструкторів `this()`**.
>
> 2. **Ініціалізація анонімних класів:** Це єдиний поширений та виправданий випадок його використання. Оскільки анонімні класи не можуть мати явних конструкторів, блок ініціалізації є єдиним способом виконати якусь логіку під час їх створення.
>
>     ```java
>     List<String> list = new ArrayList<>() {{
>         add("A");
>         add("B");
>     }};
>     ```
>
>     **Примітка:** Цей патерн ("double brace initialization") зараз вважається **антипатерном**, оскільки створює зайвий анонімний клас, може викликати витоки пам'яті і не працює з `final` класами.
>
> **Висновок:** На практиці, в сучасному коді блоки ініціалізації екземпляра майже не використовуються.

#### **📌 Що перевіряють:**

*Знання про існування цього менш поширеного механізму та розуміння, чому він втратив свою актуальність.*

* * *

### **4 ❓ Питання:**

Що буде виведено на екран? Поясніть покроково.

```java
class Parent {
    Parent() { System.out.println("Parent Constructor"); }
    { System.out.println("Parent Instance Block"); }
    static { System.out.println("Parent Static Block"); }
}

class Child extends Parent {
    Child() { System.out.println("Child Constructor"); }
    { System.out.println("Child Instance Block"); }
    static { System.out.println("Child Static Block"); }
}

public class Main {
    public static void main(String[] args) {
        new Child();
    }
}
```

> #### **💬 Відповідь:**
>
> **Вивід буде таким:**
>
> ```
> Parent Static Block
> Child Static Block
> Parent Instance Block
> Parent Constructor
> Child Instance Block
> Child Constructor
> ```
>
> **Покрокове пояснення:**
>
> 1. `new Child()` запускає процес. Перш за все, JVM потрібно ініціалізувати клас `Child`.
> 2. JVM бачить, що `Child` успадковує `Parent`, який ще не ініціалізований.
> 3. **Виконується статична ініціалізація `Parent`**. Виводиться `Parent Static Block`.
> 4. **Виконується статична ініціалізація `Child`**. Виводиться `Child Static Block`.
> 5. Тепер починається створення екземпляра. Конструктор `Child` викликає `super()`.
> 6. **Виконуються не-статичні ініціалізатори `Parent`**. Виводиться `Parent Instance Block`.
> 7. **Виконується тіло конструктора `Parent`**. Виводиться `Parent Constructor`.
> 8. Керування повертається до конструктора `Child`.
> 9. **Виконуються не-статичні ініціалізатори `Child`**. Виводиться `Child Instance Block`.
> 10. **Виконується тіло конструктора `Child`**. Виводиться `Child Constructor`.

#### **📌 Що перевіряють:**

*Практичне застосування знань про повний порядок ініціалізації в ієрархії класів.*

* * *

### **5 ❓ Питання:**

Що таке "illegal forward reference"? Коли виникає ця помилка? ⭐️

> #### **💬 Відповідь:**
>
> **"Illegal forward reference"** — це помилка компіляції, яка виникає, коли ви намагаєтеся **прочитати** статичне поле у статичному блоці або ініціалізаторі, який знаходиться у коді **перед** оголошенням цього поля.
>
> **Правило:** Ви не можете звертатися до статичного поля напряму до того, як компілятор "дійшов" до його оголошення.
>
> **Приклад, що не скомпілюється:**
>
> ```java
> public class ForwardReference {
>     static {
>         // ПОМИЛКА: Не можна читати 'value' до його оголошення
>         // System.out.println(value);
>
>         // Цікаво, що присвоювати можна, але це не ініціалізує оголошення
>         value = 10;
>     }
>
>     static int value = 5;
> }
> ```
>
> **Чому це заборонено:**
>
> Ця заборона допомагає уникнути логічних помилок. Якби читання було дозволено, ви б отримали значення поля за замовчуванням (`0`), а не те, яке присвоюється при оголошенні (`5`), що може призвести до неочевидної поведінки. Компілятор змушує вас дотримуватися чіткого порядку ініціалізації.

#### **📌 Що перевіряють:**

*Глибокі знання про строгі правила компілятора Java, пов'язані з порядком ініціалізації.*

* * *

### **6 ❓ Питання:**

Розгляньте такий код. Чому він може призвести до `NullPointerException`?

```java
public class Parent {
    public Parent() {
        doSomething();
    }
    public void doSomething() {
        System.out.println("Parent method");
    }
}
class Child extends Parent {
    private String name = "MyName";
    @Override
    public void doSomething() {
        System.out.println("Child method, name length: " + name.length());
    }
}
// main: new Child();
```

> #### **💬 Відповідь:**
>
> Цей код призведе до **`NullPointerException`** через те, що **поліморфний метод викликається з конструктора суперкласу**.
>
> **Послідовність виконання, що призводить до помилки:**
>
> 1. Викликається конструктор `new Child()`.
> 2. Першою інструкцією є неявний `super()`, який викликає конструктор `Parent()`.
> 3. Усередині конструктора `Parent` викликається метод `doSomething()`.
> 4. Завдяки **динамічному поліморфізму**, JVM викликає **перевизначену** версію `doSomething()` з класу **`Child`**.
> 5. **Проблема:** на цей момент, конструктор `Parent` ще не завершив роботу, а конструктор `Child` **навіть не починав** виконуватися. Це означає, що ініціалізація полів `Child` (рядок `private String name = "MyName";`) **ще не відбулася**.
> 6. Тому, коли метод `doSomething()` з `Child` намагається отримати доступ до `name`, це поле все ще має значення **`null`**.
> 7. Виклик `name.length()` на `null`-посиланні призводить до `NullPointerException`.
>
> **Висновок:** Це класичний приклад, чому **не можна викликати не-`final` та не-`private` методи з конструкторів**.

#### **📌 Що перевіряють:**

*Експертне розуміння порядку ініціалізації та небезпек, що виникають при взаємодії з поліморфізмом.*

* * *

### **7 ❓ Питання:**

Що станеться, якщо статичний блок ініціалізації кине виняток? ⭐️

> #### **💬 Відповідь:**
>
> Якщо `static` блок кидає неперехоплений виняток, це призводить до **фатальної помилки** ініціалізації класу.
>
> 1. JVM перехоплює будь-який виняток, що "вилітає" зі статичного блоку, і "загортає" його в **`ExceptionInInitializerError`**.
> 2. Ініціалізація класу **провалюється**. Клас позначається як такий, що не може бути ініціалізований.
> 3. **Будь-яка наступна спроба** використати цей клас у коді (наприклад, створити об'єкт або викликати статичний метод) негайно призведе до викидання **`NoClassDefFoundError`**.
>
> Це означає, що клас стає **непридатним для використання** до кінця роботи програми. Тому логіка в статичних блоках повинна бути надзвичайно надійною. Якщо операція може кинути `checked` виняток, його потрібно обробити в `try-catch`, і в `catch` блоці, можливо, кинути `ExceptionInInitializerError` вручну.

#### **📌 Що перевіряють:**

*Знання механізмів обробки помилок на рівні завантаження класів.*

* * *

### **8 ❓ Питання:**

Як порядок доступу до статичних полів може вплинути на порядок ініціалізації різних класів? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ініціалізація класів у JVM є **"лінивою"**. Клас ініціалізується тільки тоді, коли він стає потрібним. Порядок звернення до статичних членів може кардинально змінити послідовність ініціалізації.
>
> **Приклад з залежністю:**
>
> ```java
> class A {
>     static { System.out.println("A initialized"); }
> }
> class B {
>     // Ініціалізація B залежить від A
>     public static String MESSAGE = "Message from B using " + A.class.getName();
>     static { System.out.println("B initialized"); }
> }
> ```
>
> **Сценарій:**
>
> Якщо в `main` ми вперше звертаємося до класу `B`:
> `System.out.println(B.MESSAGE);`
>
> **Порядок буде таким:**
>
> 1. JVM починає ініціалізувати клас `B`.
> 2. Під час ініціалізації `static` поля `B.MESSAGE`, вона натрапляє на звернення до `A.class`. Це "активне використання" класу `A`.
> 3. JVM бачить, що клас `A` ще не ініціалізований. Вона **призупиняє ініціалізацію `B`** і починає ініціалізувати `A`.
> 4. Виконується статичний блок `A`. **Вивід:** `A initialized`.
> 5. Ініціалізація `A` завершена.
> 6. JVM повертається до ініціалізації `B`. Завершує ініціалізацію поля `MESSAGE`.
> 7. Виконується статичний блок `B`. **Вивід:** `B initialized`.
>
> Отже, хоча ми звернулися лише до `B`, клас `A` був ініціалізований раніше через залежність.

#### **📌 Що перевіряють:**

*Глибоке розуміння динамічного характеру завантаження та ініціалізації класів у JVM.*

* * *

### **9 ❓ Питання:**

Чому блок ініціалізації екземпляра (`{}`) використовується так рідко? Які є кращі альтернативи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Блок ініціалізації екземпляра використовується рідко, тому що його функціональність **майже завжди можна реалізувати кращими, більш читабельними способами**.
>
> **Сценарії використання та їх альтернативи:**
>
> 1. **Спільна логіка для кількох конструкторів:**
>     * **Проблема:** Блок `{}` дозволяє винести спільний код.
>     * **Краща альтернатива:** **Ланцюжок конструкторів (`this()`)**. Створюється один "основний" конструктор, який містить всю логіку, а інші просто викликають його. Це більш явно та контрольовано.
>
> 2. **Складна логіка ініціалізації полів:**
>     * **Проблема:** Якщо ініціалізація поля вимагає `try-catch` або кількох рядків.
>     * **Краща альтернатива:** Винести цю логіку в **`private` метод**, а потім викликати цей метод при ініціалізації поля. Це робить код чистішим та більш структурованим.
>     `private int value = initializeValue();`
>
> 3. **Ініціалізація анонімних класів:**
>     * Це був єдиний валідний сценарій, оскільки вони не мають назви для конструктора.
>     * **Сучасна альтернатива:** **Лямбда-вирази** (для функціональних інтерфейсів) та фабричні методи значно зменшили потребу в анонімних класах.
>
> Через наявність кращих альтернатив та неявний характер виконання, блоки ініціалізації екземпляра роблять код менш очевидним і тому їх уникають.

#### **📌 Що перевіряють:**

*Розуміння найкращих практик кодування. Кандидат повинен знати не тільки "як", але і "як краще".*

* * *

### **10 ❓ Питання:**

Розгляньте `enum`. Як і коли відбувається його статична ініціалізація? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `enum` — це спеціальний тип класу, і його ініціалізація відбувається за тими ж правилами, але з нюансами.
>
> **Коли відбувається ініціалізація:**
>
> Так само, як і для звичайного класу, при **першому активному зверненні**. Наприклад, коли ви вперше звертаєтеся до однієї з його констант: `MyEnum.CONSTANT_A`.
>
> **Порядок ініціалізації:**
>
> 1. Спочатку створюються **екземпляри кожної `enum`-константи**. По суті, кожен `MyEnum.A` — це `public static final` поле, і його створення викликає `private` конструктор `enum`. Вони створюються в тому порядку, в якому оголошені.
> 2. **Потім виконуються статичні блоки ініціалізації `static {}`**, якщо вони є.
>
> **Приклад:**
>
> ```java
> public enum TestEnum {
>     A, B; // Це еквівалентно public static final TestEnum A = new TestEnum();
>
>     TestEnum() {
>         System.out.println("Конструктор для " + this.name());
>     }
>
>     static {
>         System.out.println("Статичний блок");
>     }
> }
>
> // main: TestEnum.A;
> ```
>
> **Вивід:**
>
> ```
>
> Конструктор для A
> Конструктор для B
> Статичний блок
> ```
>
> Цікаво, що **конструктори викликаються до статичного блоку**. Це особливість `enum`-ів. JVM спочатку повинна створити всі екземпляри, які є статичними полями, і лише потім виконати решту статичної ініціалізації.

#### **📌 Що перевіряють:**

*Знання нюансів, пов'язаних з `enum`-типами, та їхнього специфічного порядку ініціалізації.*

* * *

### **11 ❓ Питання:**

Яким чином `final` поле може вплинути на порядок ініціалізації? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> `final` поля **не змінюють сам порядок ініціалізації**, але вони накладають **строгі вимоги** щодо того, **де і коли** ініціалізація повинна завершитися.
>
> **Вимоги `final`:**
>
> * `final` **поле екземпляра** повинно бути гарантовано ініціалізоване **до завершення кожного конструктора**.
> * `final static` **поле класу** повинно бути гарантовано ініціалізоване **до завершення статичної ініціалізації класу**.
>
> **Вплив на дизайн:**
>
> 1. Це змушує ініціалізувати поле або при оголошенні, або у відповідному ініціалізаційному блоці/конструкторі. Не можна залишити його неініціалізованим.
>
> 2. У випадку **циклічної залежності ініціалізації**, це може призвести до **помилки компіляції**.
>
> **Приклад циклічної залежності з `final`:**
>
> ```java
> class A {
>     // ПОМИЛКА: Не можна звернутися до 'B.VAL', бо B залежить від A
>     public static final int VAL = B.VAL + 1;
> }
> class B {
>     public static final int VAL = A.VAL + 1;
> }
> ```
>
> Компілятор виявить цю циклічну залежність між `final` статичними полями і не скомпілює код. Якби поля не були `final`, код міг би скомпілюватися, але впав би в рантаймі. `final` допомагає виявляти такі проблеми раніше.

#### **📌 Що перевіряють:**

*Розуміння того, як `final` впливає на вимоги компілятора та допомагає виявляти помилки дизайну на ранніх етапах.*

* * *

### **12 ❓ Питання:**

Поясніть патерн "ініціалізація на вимогу" (Initialization-on-demand holder idiom). Чому він залежить від правил ініціалізації класів? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це потокобезпечний, лінивий спосіб реалізації патерну **Singleton**, який повністю покладається на гарантії JVM щодо ініціалізації класів.
>
> **Реалізація:**
>
> ```java
> public class Singleton {
>     private Singleton() {}
>
>     // Статичний вкладений клас, що "тримає" екземпляр
>     private static class SingletonHolder {
>         private static final Singleton INSTANCE = new Singleton();
>     }
>
>     public static Singleton getInstance() {
>         return SingletonHolder.INSTANCE;
>     }
> }
> ```
>
> **Чому він працює і є потокобезпечним:**
>
> 1. **Лінива ініціалізація:**
>     * Клас `SingletonHolder` **не буде ініціалізований** доти, доки хтось **вперше не викличе** метод `Singleton.getInstance()`. Саме тоді відбудеться перше звернення до `SingletonHolder`.
>
> 2. **Потокобезпечність:**
>     * JVM **гарантує**, що ініціалізація класу (включаючи виконання його статичних полів та блоків) є **атомарною та потокобезпечною**.
>     * Навіть якщо 100 потоків одночасно викличуть `getInstance()`, лише один потік ініціалізує `SingletonHolder` (отримавши внутрішнє блокування), а інші будуть чекати. Після завершення вони просто отримають вже готовий екземпляр.
>
> Цей патерн є елегантним, бо не вимагає явних `synchronized` блокувань чи `volatile` змінних, а використовує вбудовані гарантії мови.

#### **📌 Що перевіряють:**

*Експертне знання патерну Singleton та розуміння того, як його найкращі реалізації покладаються на фундаментальні механізми JVM.*

* * *

### **13 ❓ Питання:**

Якщо в `main` методі просто оголосити `Parent p = null;`, чи будуть виконані статичні блоки класу `Parent`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не будуть.**
>
> **Пояснення:**
>
> Згідно з Java Language Specification, просте **оголошення посилання** на тип (`Parent p = null;`) **не є активним використанням** цього класу.
>
> Ініціалізація класу (і, відповідно, виконання статичних блоків) відбувається лише при першому *активному* використанні, такому як:
>
> * Створення екземпляра (`new Parent()`).
> * Виклик статичного методу.
> * Доступ до статичного поля (не compile-time constant).
> * Якщо це дочірній клас, що ініціалізується.
>
> Оголошення посилання `Parent p;` або присвоєння йому `null` лише повідомляє компілятору про тип змінної. Жодної взаємодії з класом `Parent` під час виконання на цьому етапі не відбувається, тому JVM не має причин його завантажувати та ініціалізувати.

#### **📌 Що перевіряють:**

*Дуже точне знання того, які саме дії призводять до ініціалізації класу, а які — ні.*

* * *

### **14 ❓ Питання:**

Як рефлексія (`Class.forName()`) впливає на порядок ініціалізації? У чому різниця між `Class.forName(className)` та `classLoader.loadClass(className)`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> І `Class.forName()` і `ClassLoader.loadClass()` завантажують клас у пам'ять, але вони відрізняються тим, чи **ініціалізують** вони його.
>
> * **`Class.forName(String className)`:**
>   * Цей метод **завантажує і одразу ж ініціалізує** клас. Тобто, після його виклику **будуть виконані всі статичні поля та блоки** цього класу.
>   * Саме тому його історично використовували для реєстрації JDBC-драйверів: виклик `Class.forName("com.mysql.cj.jdbc.Driver")` змушував виконатися статичний блок драйвера, який реєстрував себе в `DriverManager`.
>
> * **`ClassLoader.loadClass(String name)`:**
>   * Цей метод **лише завантажує** клас у пам'ять (завантажує байткод), але **не ініціалізує** його.
>   * Статичні блоки **не будуть виконані** одразу після виклику `loadClass()`. Вони виконаються пізніше, при першому активному використанні цього класу.
>
> Існує також перевантажена версія `Class.forName(String name, boolean initialize, ClassLoader loader)`, де другий параметр `initialize` дозволяє явно контролювати, чи потрібно ініціалізувати клас. `Class.forName(className)` за замовчуванням викликає її з `initialize = true`.

#### **📌 Що перевіряють:**

*Експертне розуміння життєвого циклу класів та нюансів роботи з `ClassLoader` та `Reflection API`.*

* * *

### **15 ❓ Питання:**

Уявіть собі циклічну залежність між статичними ініціалізаторами двох класів. До чого це призведе у багатопотоковому середовищі? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Циклічна залежність статичних ініціалізаторів у багатопотоковому середовищі може призвести до **дедлоку (deadlock)**.
>
> **Сценарій дедлоку:**
>
> ```java
> class A {
>     static {
>         // Потік А тут...
>         System.out.println("Initializing A...");
>         try { Thread.sleep(1000); } catch (InterruptedException e) {}
>         B.doSomething(); // Намагається ініціалізувати B
>     }
>     public static void doSomething() {}
> }
> class B {
>     static {
>         // Потік Б тут...
>         System.out.println("Initializing B...");
>         A.doSomething(); // Намагається ініціалізувати A
>     }
>     public static void doSomething() {}
> }
>
> // У Потоці А: new A();
> // У Потоці Б: new B();
> ```
>
> **Що відбувається:**
>
> 1. **Потік А** запускається і намагається ініціалізувати клас `A`. Він отримує **блокування на класі `A`**. Він друкує "Initializing A..." і засинає.
> 2. **Потік Б** запускається і намагається ініціалізувати клас `B`. Він отримує **блокування на класі `B`**. Він друкує "Initializing B...".
> 3. **Потік Б** намагається викликати `A.doSomething()`. Щоб це зробити, йому потрібно ініціалізувати клас `A`, але **блокування на `A` утримується Потоком А**. Потік Б **зависає** в очікуванні.
> 4. **Потік А** прокидається і намагається викликати `B.doSomething()`. Йому потрібно ініціалізувати клас `B`, але **блокування на `B` утримується Потоком Б**. Потік А також **зависає**.
>
> В результаті ми маємо **дедлок**: Потік А чекає на Б, а Б чекає на А. Програма "зависне" назавжди.
>
> Це одна з найскладніших проблем для діагностики, і вона демонструє, чому циклічні залежності між статичними ініціалізаторами є вкрай небезпечними.

#### **📌 Що перевіряють:**

*Експертний рівень розуміння багатопотоковості, механізмів блокування на рівні JVM та архітектурних антипатернів.*
