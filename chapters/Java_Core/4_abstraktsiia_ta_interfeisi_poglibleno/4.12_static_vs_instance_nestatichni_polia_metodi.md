---
layout: default
title: "Static vs Instance (нестатичні) поля/методи"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 12
---

# Static vs Instance (нестатичні) поля/методи

* короткий опис: `Static` члени належать класу і є спільними для всіх об'єктів, тоді як `instance` (нестатичні) члени належать конкретному об'єкту і є унікальними для кожного.

* поняття: `static`, `instance`, `this`.

* приклади: `ClassName.staticMethod()`, `object.instanceMethod()`. `static` методи не мають доступу до `this`.

* ключові моменти: `static` викликається без об'єкта, `instance` – через об'єкт. `static` члени ініціалізуються один раз, `instance` – при кожному створенні об'єкта.

* що важливо знати на співбесіді: Коли що використовувати, обмеження `static` методів.

### **✅ Офіційне визначення:**

У Java **`static`** поля та методи належать класу, а нестатичні (**instance**) — конкретному об'єкту.

**`static`** методи можна викликати без створення об'єкта, **`instance`** — тільки через об'єкт.

**🔗 [JLS §8.3.1.1 Static Fields](https://docs.oracle.com/javase/specs/jls/se17/html/jls-8.html#jls-8.3.1.1)**
**🔗 [JLS §8.4.3.2 Static Methods](https://docs.oracle.com/javase/specs/jls/se17/html/jls-8.html#jls-8.4.3.2)**

### **🧠 Простими словами:**

* **`static`** \= "належить класу" -> один на всіх, як однакові шкарпетки в офісі 😎
* без **`static`** \= "належить об'єкту" -> своя пара у кожного

---

## **Порівняння**

| Характеристика | `static` | Instance (нестатичне) |
| :---: | :---: | :---: |
| **Приналежність** | Класу | Об'єкту |
| **Виклик** | **`ClassName.method()`** | **`object.method()`** |
| **Пам’ять** | Спільна для всіх об’єктів | Окрема для кожного об’єкта |
| **Доступ до `this`** | ❌ Немає | ✅ Є |
| **Виклик без `new`** | ✅ Можна | ❌ Ні |
| **Типове використання** | Утиліти, лічильники, константи | Стан об’єкта, логіка поведінки |

---

## **Пояснення на пальцях (💡)**

### **Коли використовуєш `static`?**  

Якщо метод або змінна не залежать від конкретного об'єкта. Приклад: **`Math.sqrt(), Integer.parseInt().`**

### **Коли — без `static` (instance)?**  

Коли значення чи поведінка залежать від конкретних даних об'єкта, наприклад **`user.getName(), car.drive().`**

### **Типові граблі:**  

Спроба звернутись до нестатичних полів/методів з **`static`** методу — помилка. Бо **`this`** там немає — а без об’єкта ти як на самоті в космосі.

---

## **Корисні приклади (🧪)**

```java
public class Demo {
    public static int staticCount = 0;    // 🔹 спільне для всіх
    public int instanceCount = 0;         // 🔹 індивідуальне для кожного

    public Demo() {
        staticCount++;
        instanceCount++;
    }

    public static void staticMethod() {
        System.out.println("Static method called");
        // System.out.println(instanceCount); ❌ Помилка!
        // System.out.println(this); ❌ Помилка!
    }

    public void instanceMethod() {
        System.out.println("Instance method called");
        System.out.println("staticCount = " + staticCount);
        System.out.println("instanceCount = " + instanceCount);
    }
}

public class Main {
    public static void main(String[] args) {
        Demo.staticMethod();  // Можна без new!

        Demo d1 = new Demo();
        Demo d2 = new Demo();

        d1.instanceMethod();  // instanceCount = 1
        d2.instanceMethod();  // instanceCount = 1
        System.out.println("Total: " + Demo.staticCount); // Total: 2
    }
}
```

---

## **Порівняння (⚖️)**

| Виклик з `static` метода | Дозволено? | Коментар |
| :---: | :---: | :---: |
| **Інше `static` поле/метод** | ✅ Так | Всі статичні належать класу |
| **Instance поле/метод** | ❌ Ні | Потрібен конкретний об'єкт |
| **`this`** | ❌ Ні | **`this`** — це посилання на об'єкт, а об’єкта немає |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **"Static живе в класі — Instance живе в об’єкті"**

* 🧠 **`static`** -> один для всіх (як у всіх співробітників одна корпоративна пошта)
* 🧠 **`instance`** -> у кожного своя (як у кожного співробітника свій ноутбук)

---

## **🟩 Підсумок:**

**`static`** методи і поля — це спільні ресурси класу, які можна викликати без створення об’єкта, проте без доступу до **`this. Instance`** елементи — це індивідуальні властивості та поведінка об’єкта, що мають повний доступ до стану. Вибирай **`static`**, коли хочеш щось одне на всіх, і **`instance`** — коли потрібен індивідуальний стан.

---

### **🔗 Додаткові джерела:**

**🔗 [Oracle Java Tutorial: Understanding Class Members](https://docs.oracle.com/javase/tutorial/java/javaOO/classvars.html)**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка основна відмінність між `static` та `instance` (не-статичними) членами класу?

> #### **💬 Відповідь:**
>
> Основна відмінність полягає в **приналежності** та **кількості екземплярів**.
>
> **Instance члени (не-`static`):**
>
> * **Приналежність:** Належать **конкретному екземпляру (об'єкту)** класу.
> * **Екземпляри:** Кожен новий об'єкт, створений за допомогою `new`, отримує **свою власну, незалежну копію** цих полів.
> * **Доступ:** Звертатися до них можна лише через посилання на об'єкт (наприклад, `user.getName()`). Мають доступ до `this`.
>
> **`static` члени:**
>
> * **Приналежність:** Належать **самому класу**, а не його об'єктам.
> * **Екземпляри:** Існують в **єдиному екземплярі** на всю JVM, який є **спільним для всіх об'єктів** цього класу.
> * **Доступ:** Звертатися до них можна напряму через ім'я класу (наприклад, `Math.PI`), без створення об'єкта. Не мають доступу до `this`.
>
> Простими словами, `instance` — це про **"моє"** (кожного об'єкта), а `static` — це про **"наше"** (всього класу).

#### **📝 Аналогія:**

* **Instance:** У кожного студента (`об'єкт`) є свій особистий номер залікової книжки (`instance поле`).
* **`static`:** Назва університету (`static поле`) є однаковою для всіх студентів.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння різниці між рівнем класу та рівнем об'єкта. Це ключова концепція ООП.*

* * *

### **2 ❓ Питання:**

Чому `static` метод не може викликати `instance` метод напряму?

> #### **💬 Відповідь:**
>
> `static` метод не може викликати `instance` метод напряму, тому що **статичний контекст не пов'язаний з жодним конкретним об'єктом**.
>
> **Пояснення:**
>
> 1. `Instance` метод, за визначенням, працює зі станом конкретного екземпляра. Для цього йому потрібне неявне посилання `this`.
> 2. `static` метод належить класу і викликається без створення об'єкта. У нього **немає посилання `this`**.
> 3. Коли ви викликаєте `static` метод, він просто не знає, **для якого саме об'єкта** потрібно викликати `instance` метод.
>
> **Приклад помилки:**
>
> ```java
> public class Example {
>     private String message = "Hello";
>     public void printMessage() { System.out.println(message); }
>
>     public static void doStaticWork() {
>         // ПОМИЛКА КОМПІЛЯЦІЇ: для якого об'єкта викликати printMessage()?
>         // printMessage();
>     }
> }
> ```
>
> **Як це можна зробити:**
> Для виклику потрібно **явно створити об'єкт** і викликати метод через нього:
>
> `Example obj = new Example(); obj.printMessage();`

#### **📌 Що перевіряють:**

*Розуміння поняття "контекст виконання" (`static` vs `instance`) та ролі `this`.*

* * *

### **3 ❓ Питання:**

Чи може `instance` метод звертатися до `static` членів класу? Чому?

> #### **💬 Відповідь:**
>
> **Так, може.**
>
> **Пояснення:**
>
> * Статичні члени (поля та методи) належать **класу** і створюються **один раз** при завантаженні цього класу.
> * Коли створюється екземпляр об'єкта, всі статичні члени **вже існують** у пам'яті і є спільними.
> * Тому будь-який `instance` метод може вільно звертатися до будь-яких `static` членів свого класу, оскільки вони гарантовано ініціалізовані і доступні.
>
> **Приклад:**
>
> ```java
> public class User {
>     private static int userCount = 0;
>     private String name;
>
>     public User(String name) {
>         this.name = name;
>         userCount++; // Instance-метод (конструктор) звертається до static-поля
>     }
>
>     public void printTotalUsers() {
>         // Instance-метод звертається до static-поля
>         System.out.println("Total users: " + userCount);
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Знання правил видимості та життєвого циклу `static` та `instance` членів. Кандидат повинен розуміти односторонність доступу: `instance` бачить `static`, але не навпаки.*

* * *

### **4 ❓ Питання:**

Де в пам'яті JVM зберігаються `static` та `instance` поля?

> #### **💬 Відповідь:**
>
> Їхнє розташування різне і пов'язане з різними областями пам'яті.
>
> * **`instance` поля:**
>   * Зберігаються **всередині об'єкта** в **купі (Heap)**.
>   * Кожен новий об'єкт, створений за допомогою `new`, отримує в купі свою власну область пам'яті для зберігання своїх `instance`-полів.
>
> * **`static` поля:**
>   * Їхнє розташування змінювалося з версіями Java.
>   * **До Java 8:** Зберігалися в спеціальній області пам'яті, що називалася **PermGen (Permanent Generation)**.
>   * **Починаючи з Java 8:** PermGen було видалено. Статичні поля тепер зберігаються **в купі (Heap)**, разом з об'єктом `java.lang.Class`, який представляє завантажений клас. Метадані самого класу (опис структури) переїхали в `Metaspace`.
>
> Отже, в сучасних версіях Java і `static`, і `instance` поля знаходяться в купі, але `instance`-поля прив'язані до конкретного об'єкта, а `static`-поля — до об'єкта `Class`.

#### **📌 Що перевіряють:**

*Знання архітектури пам'яті JVM та її еволюції. Це просунуте питання.*

* * *

### **5 ❓ Питання:**

Поясніть на прикладі, коли доцільно використовувати `static` поле.

> #### **💬 Відповідь:**
>
> `static` поля доцільно використовувати тоді, коли дані є **спільними та єдиними для всіх екземплярів класу**, тобто не є властивістю конкретного об'єкта.
>
> **Класичні сценарії:**
>
> **1. Константи (`static final`):**
>
> * Для визначення значень, які незмінні і однакові для всієї програми.
>
>     ```java
>     public class MathConstants {
>         public static final double PI = 3.14159;
>     }
>     ```
> **2. Лічильники або спільний стан:**
> * Коли потрібно відстежувати інформацію, пов'язану з класом в цілому, наприклад, кількість створених об'єктів.
>
>     ```java
>     public class Widget {
>         private static int widgetsCreated = 0;
>         public Widget() {
>             widgetsCreated++;
>         }
>         public static int getWidgetsCreated() {
>             return widgetsCreated;
>         }
>     }
>     ```
> **3. Спільні ресурси:**
> * Наприклад, кеш, пул з'єднань або конфігураційний об'єкт, який має бути єдиним для всіх частин програми, що його використовують.
>     `private static final Logger LOGGER = LoggerFactory.getLogger(MyClass.class);`

#### **📌 Що перевіряють:**

*Практичне розуміння того, де і чому `static` поля є корисним інструментом дизайну.*

* * *

### **6 ❓ Питання:**

Чому надмірне використання `static` полів є ознакою поганого дизайну, особливо в багатопотоковому середовищі?

> #### **💬 Відповідь:**
>
> Надмірне використання `static` полів призводить до появи **глобального стану (global state)**, що є серйозним антипатерном.
>
> **Проблеми глобального стану:**
>
> 1. **Непередбачуваність:** Будь-яка частина коду може змінити значення статичного поля в будь-який момент. Це робить поведінку програми важкою для відстеження та налагодження.
>
> 2. **Погана тестованість:** Важко писати ізольовані юніт-тести, оскільки тести можуть впливати один на одного через зміну спільного статичного стану. Порядок виконання тестів може стати важливим, що є ознакою крихких тестів.
>
> 3. **Проблеми з багатопотоковістю:** Це головна небезпека.
>     * Статичне поле — це **спільний ресурс** для всіх потоків.
>     * Якщо це поле не є `final`, будь-яка операція запису в нього повинна бути **синхронізована**, інакше виникає **стан гонитви (race condition)**.
>     * Необхідність ручної синхронізації ускладнює код і може призвести до дедлоків.
>
> Набагато кращим підходом є використання **стану екземпляра** та передача залежностей через **Dependency Injection**, що робить компоненти незалежними та легкими для тестування.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти, чому глобальний стан є шкідливим і як його уникати.*

* * *

### **7 ❓ Питання:**

Як `static` та `instance` методи поводяться при наслідуванні? ⭐️

> #### **💬 Відповідь:**
>
> Вони поводяться абсолютно по-різному.
>
> * **`instance` методи (не-`static`):**
>   * Підтримують **перевизначення (`Overriding`)**.
>   * Вибір реалізації відбувається **під час виконання (runtime)** за допомогою динамічного поліморфізму. JVM дивиться на фактичний тип об'єкта.
>
> * **`static` методи:**
>   * **Не можуть бути перевизначені.**
>   * Якщо дочірній клас оголошує статичний метод з такою ж сигнатурою, це називається **приховуванням методів (method hiding)**.
>   * Вибір реалізації відбувається **на етапі компіляції (compile-time)** на основі **типу посилання**.
>
> **Приклад:**
>
> ```java
> class Parent {
>     void instanceMethod() { System.out.println("Parent instance"); }
>     static void staticMethod() { System.out.println("Parent static"); }
> }
> class Child extends Parent {
>     @Override void instanceMethod() { System.out.println("Child instance"); }
>     static void staticMethod() { System.out.println("Child static"); }
> }
>
> Parent p = new Child();
> p.instanceMethod(); // Виведе "Child instance" (поліморфізм)
> p.staticMethod();   // Виведе "Parent static"! (приховування)
> ```

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між перевизначенням та приховуванням, а також між динамічним та статичним зв'язуванням.*

* * *

### **8 ❓ Питання:**

Поясніть патерн Singleton. Яку роль у його класичній реалізації відіграють `static` поле та `static` метод? ⭐️

> #### **💬 Відповідь:**
>
> **Singleton** — це патерн проектування, який гарантує, що для певного класу існує **лише один екземпляр**, і надає до нього глобальну точку доступу.
>
> **`static` члени відіграють у цьому ключову роль:**
>
> 1. **`private static final MyClass INSTANCE = new MyClass();` (eager-ініціалізація) або `private static MyClass instance;` (lazy-ініціалізація):**
>     * **Статичне поле** використовується для зберігання **єдиного екземпляра**. Оскільки поле `static`, воно одне на весь клас. `private` забороняє доступ до нього ззовні.
>
> 2. **`public static MyClass getInstance()`:**
>     * **Статичний метод** слугує **глобальною точкою доступу**. Його можна викликати напряму (`MyClass.getInstance()`) без необхідності створювати об'єкт.
>     * Цей метод відповідає за створення екземпляра (якщо використовується лінива ініціалізація) та завжди повертає те саме посилання.
>
> 3. **`private MyClass()`:**
>     * **Приватний конструктор** забороняє створення екземплярів ззовні за допомогою `new`, гарантуючи, що єдиний екземпляр може бути створений лише всередині самого класу.
>
> `static` поле забезпечує єдиність, а `static` метод — глобальний доступ.

#### **📌 Що перевіряють:**

*Знання патерну Singleton та здатність пояснити, як кожен `static` елемент сприяє його реалізації.*

* * *

### **9 ❓ Питання:**

Що виведе цей код і чому?
`Test.doSomething();`

```java
public class Test {
    public void doSomething() {
        System.out.println("Instance method");
    }
    public static void main(String[] args) {
        // ...
    }
}
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється**. Компілятор видасть помилку: `non-static method 'doSomething()' cannot be referenced from a static context`.
>
> **Пояснення:**
>
> * `doSomething()` є **`instance` (не-статичним) методом**.
> * Щоб викликати `instance` метод, вам потрібен **екземпляр (об'єкт)** цього класу.
> * Виклик `Test.doSomething()` — це спроба звернутися до методу через **ім'я класу**, що дозволено лише для **`static`** методів.
>
> Оскільки метод не є статичним, компілятор не знає, для якого об'єкта його викликати, і повідомляє про помилку.
>
> **Як виправити:**
>
> Потрібно спочатку створити екземпляр класу:
>
> ```java
> Test obj = new Test();
> obj.doSomething();
> ```

#### **📌 Що перевіряють:**

*Здатність розпізнати некоректний виклик `instance` методу зі статичного контексту. Це перевірка на базові знання.*

* * *

### **10 ❓ Питання:**

У чому різниця в порядку ініціалізації між `static final` та просто `final` полем? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає у **часі та контексті** їхньої ініціалізації.
>
> * **`static final` поле:**
>   * Це **константа класу**.
>   * Ініціалізується **один раз** при **завантаженні класу**. Це відбувається або при оголошенні, або у статичному блоці. Його ініціалізація є частиною **статичної ініціалізації класу**.
>
> * **`final` поле (instance):**
>   * Це **константа екземпляра**.
>   * Ініціалізується **кожного разу** при створенні **нового об'єкта**. Це відбувається або при оголошенні, або у блоці ініціалізації екземпляра `{}`, або в **конструкторі**.
>   * Його ініціалізація є частиною **ініціалізації екземпляра**.
>
> **Приклад:**
>
> ```java
> class Demo {
>     private static final long CLASS_LOAD_TIME = System.currentTimeMillis();
>     private final long OBJECT_CREATION_TIME;
>
>     public Demo() {
>         this.OBJECT_CREATION_TIME = System.currentTimeMillis();
>     }
> }
> ```
>
> `CLASS_LOAD_TIME` буде однаковим для всіх об'єктів. `OBJECT_CREATION_TIME` буде унікальним для кожного.

#### **📌 Що перевіряють:**

*Розуміння, що `static` та `instance` ініціалізації — це два різних етапи в життєвому циклі об'єктів.*

* * *

### **11 ❓ Питання:**

Що краще використовувати для утилітарних функцій: `static` методи в класі чи `default` методи в інтерфейсі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це залежить від **контексту та призначення** утилітарної функції.
>
> **`static` методи в класі (наприклад, `MathUtils`):**
>
> * **Коли використовувати:** Для **загальних утиліт**, які не є частиною поведінки якогось конкретного типу. Вони є незалежними помічниками. Наприклад, `StringUtils` не є частиною контракту самого `String`.
> * **Перевага:** Логічне групування функцій, не пов'язаних з конкретним контрактом.
>
> **`default`/`static` методи в інтерфейсі:**
>
> * **Коли використовувати:** Для утилітарних функцій, які **тісно пов'язані з контрактом** самого інтерфейсу.
> * **Перевага:** Покращена **згуртованість (cohesion)**. Логіка знаходиться поруч з абстракцією, яку вона доповнює. Це робить API більш самодостатнім.
> * **Приклад:** `Comparator.comparing()` — це статичний метод, що створює компаратор. `Comparator.thenComparing()` — це `default` метод, що створює новий компаратор на основі існуючого. Обидва логічно належать до інтерфейсу `Comparator`.
>
> **Висновок:** Якщо функція є частиною "сім'ї" поведінок, визначених інтерфейсом, розміщуйте її в інтерфейсі. Якщо це абсолютно незалежна утиліта — використовуйте традиційний утилітарний клас.

#### **📌 Що перевіряють:**

*Розуміння сучасного дизайну API. Кандидат повинен знати, що утилітарні класи — не єдиний варіант.*

* * *

### **12 ❓ Питання:**

Як `static` поле може спричинити витік пам'яті (memory leak)? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Статичні поля є одним із **найпоширеніших джерел витоків пам'яті** в Java-додатках, особливо в тих, що мають довгий життєвий цикл (як-от веб-сервери).
>
> **Як виникає витік:**
>
> 1. Статичне поле живе, доки завантажений клас. Тобто, практично **завжди**.
> 2. Якщо ви додаєте в **статичну колекцію** (наприклад, `static List` або `static Map`) посилання на короткоживучі об'єкти і **ніколи їх звідти не видаляєте**, ці об'єкти **ніколи не будуть видалені збирачем сміття**.
> 3. **Збирач сміття** бачить, що на ці об'єкти є **сильне посилання** зі статичного кореня (поля класу), і вважає їх "живими".
>
> **Приклад:**
>
> ```java
> public class Cache {
>     private static final Map<String, BigObject> cache = new HashMap<>();
>
>     public void process(String key) {
>         BigObject obj = new BigObject(key);
>         // Кладемо об'єкт у статичний кеш
>         cache.put(key, obj);
>     }
> }
> ```
>
> Якщо ми ніколи не будемо очищати цей `cache`, то з кожним викликом `process` у пам'яті будуть накопичуватися `BigObject`, які ніколи не будуть видалені.
>
> **Як уникнути:**
>
> * Не використовувати статичні поля для зберігання стану екземплярів.
> * Використовувати `WeakHashMap` або сторонні бібліотеки кешування з політиками витіснення.
> * Явно очищати статичні колекції, коли дані стають непотрібними.

#### **📌 Що перевіряють:**

*Практичний досвід та знання про поширені проблеми управління пам'яттю.*

* * *

### **13 ❓ Питання:**

Які `instance` члени класу `Object` не мають сенсу, якщо їх викликати зі `static` контексту (через посилання на об'єкт `Class`)? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання на розуміння різниці між об'єктом `java.lang.Class` та екземплярами цього класу. Об'єкт `Class` сам є об'єктом, і на ньому можна викликати методи `Object`.
>
> Однак, методи `Object`, призначені для **багатопотоковості**, втрачають свій типовий сенс, коли викликаються на об'єкті `Class`:
>
> * **`wait()`, `notify()`, `notifyAll()`**
>
> **Пояснення:**
>
> * Ці методи використовуються для **координації потоків**, які працюють зі **спільним ресурсом (об'єктом)**. Потоки зазвичай синхронізуються на моніторі конкретного об'єкта-ресурсу.
> * Хоча технічно ви можете синхронізуватися на моніторі об'єкта `MyClass.class`:
>     `synchronized (MyClass.class) { MyClass.class.wait(); }`
> * Це вважається **дуже поганою практикою**. Ви використовуєте **глобальне блокування**, яке вплине на всі потоки, що намагаються отримати доступ до будь-яких `synchronized static` методів цього класу. Це може легко призвести до дедлоків і значно знижує продуктивність.
> * Використання `wait()`/`notify()` на `Class` об'єкті для координації є вкрай негнучким і заплутаним.
>
> Для координації потоків слід завжди використовувати спеціальні **об'єкти-блокування (`lock objects`)** або синхронізуватися на об'єктах, що представляють конкретний ресурс.

#### **📌 Що перевіряють:**

*Експертні знання з багатопотоковості. Кандидат повинен розуміти, чому певні технічно можливі дії є архітектурно неправильними.*

* * *

### **14 ❓ Питання:**

Чи можна використовувати посилання `this` всередині ініціалізатора `static` поля? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> Це призведе до **помилки компіляції**.
>
> `public static final SomeObject instance = new SomeObject(this); // ПОМИЛКА`
>
> **Причина:**
>
> 1. Ініціалізація **`static` полів** відбувається на етапі **ініціалізації класу**.
> 2. Цей етап виконується **до того**, як буде створено **будь-який екземпляр** цього класу.
> 3. Посилання `this` вказує на **поточний об'єкт**, але на етапі статичної ініціалізації **жодного поточного об'єкта ще не існує**.
>
> `this` отримує своє значення лише тоді, коли починається робота **конструктора** або **не-статичного методу**, тобто в контексті конкретного екземпляра. Статичний контекст і `this` є несумісними.

#### **📌 Що перевіряють:**

*Повторна перевірка на розуміння різниці між `static` та `instance` контекстами у складному сценарії.*

* * *

### **15 ❓ Питання:**

У чому різниця між викликом статичного методу через `null` посилання та через посилання на реальний об'єкт? Що відбувається в байткоді? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **З точки зору результату — жодної різниці. В обох випадках метод буде викликано успішно.**
>
> **Приклад:**
>
> ```java
> MyClass obj1 = null;
> MyClass obj2 = new MyClass();
>
> obj1.staticMethod(); // Працює
> obj2.staticMethod(); // Працює
> MyClass.staticMethod(); // Рекомендований спосіб
> ```
>
> **Що відбувається в байткоді:**
>
> Це найцікавіша частина. Незалежно від того, як ви пишете виклик (`obj1.staticMethod()` чи `MyClass.staticMethod()`), **компілятор генерує однаковий байткод**.
>
> Він використовує спеціальну інструкцію **`invokestatic`**.
>
> 1. Компілятор бачить, що ви намагаєтеся викликати `static` метод.
> 2. Він дивиться на **тип посилання** (`MyClass`), а не на його значення (`null` чи реальний об'єкт).
> 3. Він генерує байткод, який викликає статичний метод безпосередньо для класу `MyClass`: `invokestatic com/example/MyClass.staticMethod()V`.
>
> В байткоді **немає жодної операції завантаження посилання на об'єкт (`aload`)**, як це було б для `instance`-методу (де використовується `invokevirtual`).
>
> **Висновок:** Оскільки об'єкт у виклику статичного методу не бере участі на рівні байткоду, його значення (`null` чи ні) не має жодного значення. Саме тому `NullPointerException` не виникає. Виклик через посилання — це просто "синтаксичний цукор", який компілятор ігнорує.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як Java-код транслюється в байткод, і як працює статичне зв'язування.*
