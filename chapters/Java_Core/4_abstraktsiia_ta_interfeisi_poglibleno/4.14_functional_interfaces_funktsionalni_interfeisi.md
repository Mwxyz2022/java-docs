---
layout: default
title: "Functional interfaces (функціональні інтерфейси)"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 14
---

# Functional interfaces (функціональні інтерфейси)

* короткий опис: Інтерфейс з рівно одним абстрактним методом (SAM – Single Abstract Method), що слугує цільовим типом для лямбда-виразів. Може містити `default` та `static` методи.

* поняття: Функціональний інтерфейс, SAM, `@FunctionalInterface` (анотація).

* приклади: `Predicate<T>`, `Function<T, R>`, `Consumer<T>`, `Supplier<T>`.

* ключові моменти: Дозволяють писати компактні лямбда-вирази, є основою функціонального програмування в Java 8+.

* що важливо знати на співбесіді: Визначення, роль у Java 8+, стандартні ФІ.

### **✅ Офіційне визначення:**

**`Функціональний інтерфейс`** — це інтерфейс, який має рівно один абстрактний метод. Він може містити також **`default`** і **`static`** методи. Саме для таких інтерфейсів працюють лямбда-вирази і method reference.

**🔗 [Oracle Docs: Functional Interfaces](https://docs.oracle.com/javase/8/docs/api/java/lang/FunctionalInterface.html)**

### **🧠 Простими словами:**

**Функціональний інтерфейс** — це інтерфейс з одним єдиним методом, який можна подати як лямбду — тобто як функцію в обгортці Java

---

## **Основні ознаки (📊)**

| Ознака | Значення |
| :---: | :---: |
| Один абстрактний метод | ✅ Обов’язково |
| Default / Static методи | ✅ Можна додавати |
| Анотація **`@FunctionalInterface`** | ✅ Рекомендовано, але не обов’язково |
| Використовується з | Лямбда-виразами, method reference, Stream API |

---

## **Пояснення на пальцях (💡)**

🔸 **Що таке SAM?**  
Single Abstract Method — інтерфейс з одним абстрактним методом, «однометодник».

🔸 **Навіщо це?**  
Щоб писати коротко і красиво:

```java
button.setOnClick(() -> System.out.println("Clicked"));
```

а не колись радянський варіант:

```java
button.setOnClick(new OnClickListener() {
    public void onClick() {
        System.out.println("Clicked");
    }
});
```

🔸 **Чому `@FunctionalInterface`?**  
Щоб захистити себе від себе — компілятор підкаже, якщо раптом додаси другий абстрактний метод, ідея лямбди впаде в кому.

---

## **Корисні приклади (🧪)**

🔻 Власний функціональний інтерфейс:

```java
@FunctionalInterface
public interface Printer {
    void print(String message);
}
```

🔻 Лямбда-вираз із ним:

```java
Printer p = msg -> System.out.println("Print: " + msg);
p.print("Hello");
```

🔻 Стандартні функціональні інтерфейси Java 8+:

```java
Predicate<String> isEmpty = s -> s.isEmpty();
Consumer<String> printer = s -> System.out.println(s);
Function<Integer, String> toString = i -> "Number: " + i;
```

---

## **Популярні функціональні інтерфейси (⚖️)**

| Інтерфейс | Метод | Що робить |
| :---: | :---: | :---: |
| **Runnable** | **`void run()`** | Без параметрів, нічого не повертає |
| **Callable\<T\>** | **`T call()`** | Без параметрів, повертає значення |
| **Supplier\<T\>** | **`T get()`** | Постачає значення |
| **Consumer\<T\>** | **`void accept(T t)`** | Приймає значення, нічого не повертає |
| **Function\<T,R\>** | **`R apply(T t)`** | Приймає T, повертає R |
| **Predicate\<T\>** | **`boolean test(T t)`** | Перевіряє умову |
| **BiFunction\<T,U,R\>** | **`R apply(T,U)`** | Приймає два аргументи, повертає значення |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **SAM \= Single Abstract Method**

-> Один метод \= можна написати лямбду, як у справжніх функціональних мовах

---

## **🟩 Підсумок:**

**Функціональні інтерфейси** — це двері у світ лямбда-виразів і функціонального стилю в Java. Вони дозволяють писати компактний, зрозумілий і гнучкий код. Анотація **`@FunctionalInterface`** — це страховка від дурних помилок. Саме ці інтерфейси лежать в основі Stream API, подій і сучасного Java-коду.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке функціональний інтерфейс в Java і яка його головна вимога?

> #### **💬 Відповідь:**
>
> **Функціональний інтерфейс (Functional Interface)** — це інтерфейс, який містить **рівно один абстрактний метод**. Його також називають SAM-інтерфейсом (Single Abstract Method).
>
> **Головна вимога:**
> Наявність **лише одного** абстрактного методу. При цьому інтерфейс може містити будь-яку кількість `default` та `static` методів, це не порушує його статусу.
>
> **Основне призначення:**
> Функціональні інтерфейси слугують як **цільовий тип (target type)** для **лямбда-виразів** та **посилань на методи (method references)**, що є основою функціонального програмування в Java 8+.
>
> **Приклад:**
>
> ```java
> @FunctionalInterface
> interface MathOperation {
>     int operate(int a, int b);
> }
> ```
>
> Будь-яка лямбда, що приймає два `int` і повертає `int`, може бути представлена цим інтерфейсом:
> `MathOperation addition = (a, b) -> a + b;`

#### **📌 Що перевіряють:**

*Базове знання визначення та основної ролі функціонального інтерфейсу як основи для лямбда-виразів.*

* * *

### **2 ❓ Питання:**

Для чого потрібна анотація `@FunctionalInterface`? Чи є вона обов'язковою?

> #### **💬 Відповідь:**
>
> Анотація **`@FunctionalInterface`** — це інформаційна анотація, яка використовується для позначення функціонального інтерфейсу.
>
> **Чи є вона обов'язковою?**
>
> **Ні, не є.** Інтерфейс буде вважатися функціональним, якщо він просто відповідає вимозі (має один абстрактний метод), навіть без цієї анотації.
>
> **Навіщо вона потрібна (і чому її використання є найкращою практикою):**
>
> 1. **Перевірка компілятором:** Це головна перевага. Якщо ви позначили інтерфейс як `@FunctionalInterface`, компілятор перевірить, чи він дійсно містить лише один абстрактний метод. Якщо ви випадково додасте другий, компілятор видасть **помилку**. Це захищає контракт інтерфейсу від випадкових змін.
>
> 2. **Документація та ясність наміру:** Анотація чітко сигналізує іншим розробникам (та інструментам, як Javadoc), що цей інтерфейс був спроектований спеціально для використання з лямбда-виразами.
>
> 3. **Підказки IDE:** IDE використовують цю анотацію, щоб надавати кращі підказки та автодоповнення.

#### **📌 Що перевіряють:**

*Розуміння того, що анотація — це інструмент для забезпечення надійності та читабельності, а не обов'язкова синтаксична вимога.*

* * *

### **3 ❓ Питання:**

Назвіть та опишіть 4 основні функціональні інтерфейси з пакета `java.util.function`.

> #### **💬 Відповідь:**
>
> В Java 8 було додано пакет `java.util.function` з набором стандартних функціональних інтерфейсів для найпоширеніших випадків. Чотири основні:
>
> 1. **`Predicate<T>` (Предикат):**
>     * **Сигнатура:** `boolean test(T t)`
>     * **Призначення:** Приймає один аргумент і повертає `boolean` результат. Використовується для **перевірки умови**.
>     * **Приклад:** `Predicate<String> isEmpty = s -> s.isEmpty();`
>
> 2. **`Consumer<T>` (Споживач):**
>     * **Сигнатура:** `void accept(T t)`
>     * **Призначення:** Приймає один аргумент і **виконує з ним якусь дію**, нічого не повертаючи.
>     * **Приклад:** `Consumer<String> printer = s -> System.out.println(s);`
>
> 3. **`Function<T, R>` (Функція):**
>     * **Сигнатура:** `R apply(T t)`
>     * **Призначення:** Приймає один аргумент типу `T` і **перетворює його** на результат типу `R`.
>     * **Приклад:** `Function<String, Integer> lengthFunc = s -> s.length();`
>
> 4. **`Supplier<T>` (Постачальник):**
>     * **Сигнатура:** `T get()`
>     * **Призначення:** **Не приймає жодних аргументів**, але **повертає (постачає)** значення певного типу.
>     * **Приклад:** `Supplier<LocalDate> today = () -> LocalDate.now();`

#### **📝 Мнемоніка:**

* `Predicate` -

> тестує

* `Consumer` -

> споживає

* `Function` -

> трансформує

* `Supplier` -

> постачає

#### **📌 Що перевіряють:**

*Знання основних "будівельних блоків" Stream API та функціонального програмування в Java. Це базове питання для роботи з Java 8+.*

* * *

### **4 ❓ Питання:**

Поясніть, що таке посилання на метод (method reference) і як воно пов'язане з функціональними інтерфейсами. Наведіть приклад.

> #### **💬 Відповідь:**
>
> **Посилання на метод (Method Reference)** — це **короткий, читабельний синтаксис** для лямбда-виразу, який просто викликає один існуючий метод.
>
> Вони дозволяють повторно використовувати існуючі методи як реалізації функціональних інтерфейсів.
>
> **Зв'язок:**
>
> Посилання на метод, як і лямбда, може бути використано лише там, де очікується **функціональний інтерфейс**. Сигнатура методу, на який посилаються, повинна бути сумісною з абстрактним методом цього інтерфейсу.
>
> **Приклад:**
>
> ```java
> List<String> names = Arrays.asList("john", "jane", "peter");
> ```
>
> **Використання лямбди:**
> `names.forEach(s -> System.out.println(s));`
>
> **Використання посилання на метод (більш коротко і читабельно):**
> `names.forEach(System.out::println);`
>
> Тут `System.out::println` — це посилання на метод `println` об'єкта `System.out`. Компілятор розуміє, що це еквівалентно лямбді `s -> System.out.println(s)`, оскільки `forEach` очікує `Consumer<String>`, а метод `println` приймає `String` і нічого не повертає.

#### **📌 Що перевіряють:**

*Знання сучасного синтаксису Java 8+ та вміння писати більш лаконічний та виразний код.*

* * *

### **5 ❓ Питання:**

Чому `Comparator` є функціональним інтерфейсом, хоча він має багато методів?

> #### **💬 Відповідь:**
>
> Це питання перевіряє точне розуміння визначення.
>
> **`Comparator` є функціональним інтерфейсом**, тому що він містить **рівно один абстрактний метод** — `compare(T o1, T o2)`.
>
> Всі інші методи, які має `Comparator`, є:
>
> * **`default` методи:** `thenComparing()`, `reversed()`, тощо.
> * **`static` методи:** `comparing()`, `naturalOrder()`, тощо.
>
> Визначення функціонального інтерфейсу **не обмежує кількість `default` чи `static` методів**. Головна вимога — лише один абстрактний метод.
>
> Метод `equals(Object obj)` також присутній в інтерфейсі, але він має особливий статус: якщо інтерфейс оголошує абстрактний метод, який відповідає сигнатурі одного з публічних методів `Object`, то цей метод **не зараховується** до кількості абстрактних методів інтерфейсу. Це зроблено для того, щоб не "ламати" функціональність інтерфейсів, які природно містять такі методи, як `Comparator`.
>
> Тому, незважаючи на велике API, `Comparator` задовольняє вимозі SAM (Single Abstract Method) і може бути реалізований за допомогою лямбди.

#### **📌 Що перевіряють:**

*Точне знання визначення функціонального інтерфейсу та його взаємодії з `default`/`static` методами і методами з `Object`.*

* * *

### **6 ❓ Питання:**

Як `default` методи можуть бути використані для композиції функціональних інтерфейсів? Наведіть приклад з `Predicate`. ⭐️

> #### **💬 Відповідь:**
>
> `Default` методи надають потужний механізм для **комбінування або модифікації** поведінки функціональних інтерфейсів, створюючи нову функціональність на основі існуючої.
>
> Інтерфейс **`Predicate<T>`** є чудовим прикладом. Він має три `default` методи: `and()`, `or()`, та `negate()`.
>
> **Приклад:**
> У нас є два простих предиката:
>
> ```java
> Predicate<String> isLong = s -> s.length() > 5;
> Predicate<String> startsWithS = s -> s.startsWith("S");
> ```
>
> Тепер, використовуючи `default` методи, ми можемо легко їх комбінувати:
>
> * **Комбінація через `and()`:**
>     `Predicate<String> isLongAndStartsWithS = isLong.and(startsWithS);`
>
> * **Комбінація через `or()`:**
>     `Predicate<String> isLongOrStartsWithS = isLong.or(startsWithS);`
>
> * **Заперечення через `negate()`:**
>     `Predicate<String> isShort = isLong.negate();`
>
> **Використання:**
> `System.out.println(isLongAndStartsWithS.test("Something")); // true`
>
> Це дозволяє будувати складні перевірки з простих, декларативних "будівельних блоків", що є основою функціонального стилю.

#### **📌 Що перевіряють:**

*Знання композиційних можливостей стандартних функціональних інтерфейсів. Це показує глибоке розуміння Java 8.*

* * *

### **7 ❓ Питання:**

Назвіть кілька примітив-спеціалізованих версій основних функціональних інтерфейсів. Навіщо вони потрібні? ⭐️

> #### **💬 Відповідь:**
>
> Для роботи з примітивними типами (`int`, `long`, `double`) були створені спеціалізовані версії функціональних інтерфейсів, щоб **уникнути накладних витрат на автоупаковку (autoboxing) та авторозпаковку (unboxing)**.
>
> Використання, наприклад, `Predicate<Integer>` змусило б JVM постійно перетворювати `int` в `Integer` і навпаки.
>
> **Приклади спеціалізованих версій:**
>
> * Для **`Function`**:
>   * `IntFunction<R>` (приймає `int`, повертає `R`)
>   * `ToIntFunction<T>` (приймає `T`, повертає `int`)
>   * `IntToDoubleFunction` (приймає `int`, повертає `double`)
>
> * Для **`Predicate`**:
>   * `IntPredicate`, `LongPredicate`, `DoublePredicate`
>
> * Для **`Consumer`**:
>   * `IntConsumer`, `LongConsumer`, `DoubleConsumer`
>
> * Для **`Supplier`**:
>   * `IntSupplier`, `LongSupplier`, `DoubleSupplier`
>
> **Навіщо вони потрібні:**
> Вони значно **підвищують продуктивність** при роботі з потоками примітивних типів (`IntStream`, `DoubleStream`), оскільки всі операції відбуваються без створення зайвих об'єктів-обгорток. Для коду, критичного до продуктивності, завжди слід надавати перевагу цим спеціалізованим версіям.

#### **📌 Що перевіряють:**

*Розуміння того, як автоупаковка впливає на продуктивність, та знання інструментів стандартної бібліотеки для оптимізації роботи з примітивами.*

* * *

### **8 ❓ Питання:**

Що таке **Bi-функціональні інтерфейси**? Наведіть приклади. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Bi-функціональні інтерфейси** — це варіації стандартних функціональних інтерфейсів, які приймають **два аргументи** замість одного.
>
> Вони використовуються у сценаріях, де операція вимагає двох вхідних значень.
>
> **Основні приклади:**
>
> * **`BiFunction<T, U, R>`:**
>   * **Метод:** `R apply(T t, U u)`
>   * Приймає два аргументи (`T` та `U`) і повертає результат `R`.
>   * **Приклад:** `BiFunction<String, Integer, String> combine = (s, i) -> s + ": " + i;`
>
> * **`BiConsumer<T, U>`:**
>   * **Метод:** `void accept(T t, U u)`
>   * Приймає два аргументи і виконує дію, нічого не повертаючи.
>   * **Приклад:** Дуже часто використовується з `Map.forEach()`: `map.forEach((key, value) -> System.out.println(key + " -> " + value));`
>
> * **`BiPredicate<T, U>`:**
>   * **Метод:** `boolean test(T t, U u)`
>   * Приймає два аргументи і повертає `boolean` результат.
>   * **Приклад:** `BiPredicate<String, Integer> isLongerThan = (s, len) -> s.length() > len;`

#### **📌 Що перевіряють:**

*Повноту знань про пакет `java.util.function` та його інструменти для роботи з операціями, що приймають два аргументи.*

* * *

### **9 ❓ Питання:**

Чи може функціональний інтерфейс кидати `checked` винятки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Стандартні функціональні інтерфейси з пакета `java.util.function` (як `Function`, `Predicate`) **не оголошують `throws` клаузу** у своїх абстрактних методах.
>
> Це означає, що лямбда-вираз, який ви присвоюєте їм, **не може напряму кидати `checked` виняток**.
>
> **Проблема:**
>
> ```java
> Function<String, Integer> parseInt = (s) -> Integer.parseInt(s); // ОК, NumberFormatException - unchecked
>
> // ПОМИЛКА КОМПІЛЯЦІЇ: unhandled exception java.io.IOException
> Function<String, String> readFile = (path) -> Files.readString(Path.of(path));
> ```
>
> **Як це вирішити:**
>
> 1. **Обробка винятку всередині лямбди:**
>     Найпростіший спосіб — обгорнути код в `try-catch` і "загорнути" `checked` виняток у `unchecked`.
>
>     ```java
>     (path) -> {
>         try {
>             return Files.readString(Path.of(path));
>         } catch (IOException e) {
>             throw new RuntimeException(e);
>         }
>     }
>     ```
>
> 2. **Створення власного функціонального інтерфейсу:**
>     Ви можете створити власний функціональний інтерфейс, який оголошує `throws` клаузу.
>
>     ```java
>     @FunctionalInterface
>     interface ThrowingFunction<T, R> {
>         R apply(T t) throws Exception;
>     }
>     ```
> Це більш чистий підхід, але ви не зможете використовувати його безпосередньо у стандартному `Stream API`.

#### **📌 Що перевіряють:**

*Знання того, як обробка винятків взаємодіє з лямбда-виразами, та розуміння обмежень стандартних функціональних інтерфейсів.*

* * *

### **10 ❓ Питання:**

Що таке **Closure (замикання)** в контексті лямбда-виразів і функціональних інтерфейсів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Замикання (Closure)** — це концепція функціонального програмування, де функція (у нашому випадку, лямбда-вираз) "захоплює" і запам'ятовує змінні зі своєї **зовнішньої, лексичної області видимості**, навіть якщо ця область вже перестала існувати.
>
> У Java лямбда-вирази можуть захоплювати:
>
> * Поля екземпляра (`this.fieldName`).
> * Статичні поля.
> * Локальні змінні та параметри методів.
>
> **Важливе правило:**
> Локальні змінні, які "захоплюються" лямбдою, повинні бути **`final` або `effectively final`**. Це означає, що їхнє значення не повинно змінюватися після ініціалізації.
>
> **Приклад замикання:**
>
> ```java
> public Function<Integer, Integer> createAdder(int amount) {
>     // 'amount' - це локальна змінна
>
>     // Лямбда "захоплює" змінну 'amount'
>     return (number) -> number + amount;
> }
>
> // ...
> Function<Integer, Integer> add5 = createAdder(5);
> Function<Integer, Integer> add10 = createAdder(10);
>
> System.out.println(add5.apply(20)); // Виведе 25
> System.out.println(add10.apply(20)); // Виведе 30
> ```
>
> Тут лямбда, повернута з `createAdder`, "пам'ятає" значення `amount`, яке існувало на момент її створення, навіть після того, як метод `createAdder` завершив свою роботу.

#### **📌 Що перевіряють:**

*Глибоке розуміння концепцій функціонального програмування та того, як вони реалізовані в Java, включаючи правила для `effectively final`.*

* * *

### **11 ❓ Питання:**

Уявіть, що інтерфейс `Runnable` не існував би. Створіть свій власний функціональний інтерфейс, який міг би замінити його для запуску коду в новому потоці. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Runnable` — це класичний функціональний інтерфейс з одним методом `void run()`, який не приймає аргументів і нічого не повертає.
>
> **Створення власного аналога:**
>
> 1. Оголошуємо інтерфейс.
> 2. Позначаємо його анотацією `@FunctionalInterface` для надійності.
> 3. Оголошуємо один абстрактний метод з відповідною сигнатурою.
>
> ```java
> @FunctionalInterface
> public interface Executable {
>     void execute();
> }
> ```
>
> **Як його використовувати:**
>
> Тепер ми можемо створити `Thread`, передавши йому реалізацію нашого інтерфейсу через лямбду. Але конструктор `Thread` очікує саме `Runnable`. Тому для реального використання нам доведеться створити "адаптер".
>
> ```java
> public class CustomRunner {
>     public static void runTask(Executable task) {
>         // Створюємо анонімний Runnable, який викликає наш таск
>         new Thread(() -> task.execute()).start();
>     }
>
>     public static void main(String[] args) {
>         // Створюємо наш таск за допомогою лямбди
>         Executable myTask = () -> System.out.println("Виконується кастомний таск!");
>
>         runTask(myTask);
>     }
> }
> ```
>
> Це демонструє, як легко можна визначити власні функціональні інтерфейси для створення більш специфічних і читабельних API.

#### **📌 Що перевіряють:**

*Навички проектування власних API з використанням функціональних інтерфейсів.*

* * *

### **12 ❓ Питання:**

Поясніть призначення операторів `UnaryOperator<T>` та `BinaryOperator<T>`. Чим вони відрізняються від `Function` та `BiFunction`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `UnaryOperator` та `BinaryOperator` — це **спеціалізовані** версії `Function` та `BiFunction` для випадків, коли тип операндів і тип результату **однакові**.
>
> * **`UnaryOperator<T>`:**
>   * Розширює `Function<T, T>`.
>   * **Сигнатура:** `T apply(T t)`
>   * **Призначення:** Представляє унарну операцію, яка приймає один аргумент і повертає результат **того ж типу**.
>   * **Приклад:** Інкремент (`n -> n + 1`), перетворення рядка у верхній регістр (`String::toUpperCase`).
>
> * **`BinaryOperator<T>`:**
>   * Розширює `BiFunction<T, T, T>`.
>   * **Сигнатура:** `T apply(T t1, T t2)`
>   * **Призначення:** Представляє бінарну операцію, яка приймає два аргументи і повертає результат **того ж типу**.
>   * **Приклад:** Додавання (`(a, b) -> a + b`), знаходження максимуму (`Integer::max`).
>
> **Відмінність від `Function`/`BiFunction`:**
> `Function<T, R>` та `BiFunction<T, U, R>` є більш загальними, вони дозволяють типам аргументів і результату **відрізнятися**. `UnaryOperator` та `BinaryOperator` — це просто зручні скорочення для найпоширенішого випадку, коли типи збігаються. Їх використання робить намір коду більш **явним та семантично точним**.

#### **📌 Що перевіряють:**

*Повноту знань про пакет `java.util.function` та розуміння спеціалізованих інтерфейсів.*

* * *

### **13 ❓ Питання:**

Чи можна реалізувати один клас, який є функціональним інтерфейсом, і водночас успадкувати інший клас? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Це питання з підступом.** Сам клас не може бути функціональним інтерфейсом, бо `interface` і `class` — це різні сутності. Але **клас може реалізувати функціональний інтерфейс і успадкувати інший клас одночасно.**
>
> ```java
> @FunctionalInterface
> interface Task {
>     void run();
> }
>
> abstract class BaseWorker {
>     protected String id;
>     public BaseWorker(String id) { this.id = id; }
> }
>
> // Клас одночасно і успадковує, і реалізує
> class MyTaskWorker extends BaseWorker implements Task {
>
>     public MyTaskWorker(String id) {
>         super(id);
>     }
>
>     @Override
>     public void run() {
>         System.out.println("Завдання виконується працівником: " + id);
>     }
> }
> ```
>
> **Однак, якщо питання полягає в тому, чи може `інтерфейс` успадкувати клас, то відповідь — ні.**
>
> Інтерфейс може розширювати лише інші інтерфейси.
>
> Це питання перевіряє, наскільки точно кандидат розуміє термінологію та правила системи типів Java.

#### **📌 Що перевіряють:**

*Уважність до формулювань та точність у використанні термінології. Здатність розпізнати питання з підступом.*

* * *

### **14 ❓ Питання:**

Чому `java.lang.Comparable<T>` є функціональним інтерфейсом, а `java.util.Comparator<T>` — також, хоча `Comparator` має значно більше методів? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидва є функціональними, тому що **обидва задовольняють головній вимозі — мають рівно один абстрактний метод.**
>
> * **`Comparable<T>`:**
>   * Має лише один абстрактний метод: `int compareTo(T o)`.
>   * Тому він є функціональним інтерфейсом.
>
> * **`Comparator<T>`:**
>   * Також має лише **один абстрактний метод:** `int compare(T o1, T o2)`.
>   * Усі інші численні методи, як-от `thenComparing()`, `reversed()` є **`default` методами**, а `comparing()`, `naturalOrder()` — **`static` методами**.
>   * Метод `equals(Object obj)`, хоч і оголошений в інтерфейсі, не рахується, бо він є публічним методом `Object`.
>
> Цей приклад чудово ілюструє, що **кількість методів в інтерфейсі не є показником**. Важлива лише кількість **абстрактних** методів. `Comparator` був спеціально перероблений у Java 8 з використанням `default` та `static` методів, щоб надати багате API, залишаючись при цьому функціональним інтерфейсом, який можна створювати за допомогою лямбд.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як визначається функціональний інтерфейс, включаючи правило щодо методів з `Object`.*

* * *

### **15 ❓ Питання:**

Поясніть, що таке **каррінг (currying)** у функціональному програмуванні і як його можна імітувати в Java за допомогою функціональних інтерфейсів. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Каррінг** — це техніка у функціональному програмуванні, яка дозволяє перетворити функцію, що приймає **багато аргументів**, на **послідовність функцій, кожна з яких приймає лише один аргумент**.
>
> Замість функції `f(a, b, c)` ви створюєте функцію `g(a)`, яка повертає функцію `h(b)`, яка, у свою чергу, повертає функцію `i(c)`.
>
> **Як це імітувати в Java:**
>
> Для цього можна використовувати стандартний `java.util.function.Function`, який повертає інший `Function`.
>
> **Приклад: функція, що додає три числа.**
>
> * **Звичайна реалізація:**
>
>     ```java
>     // Потрібен свій інтерфейс
>     interface TriFunction<T, U, V, R> {
>         R apply(T t, U u, V v);
>     }
>     TriFunction<Integer, Integer, Integer, Integer> adder = (a, b, c) -> a + b + c;
>     adder.apply(1, 2, 3); // 6
>     ```
>
> * **Каррована реалізація:**
>
>     ```java
>     Function<Integer, Function<Integer, Function<Integer, Integer>>> curriedAdder =
>         a -> b -> c -> a + b + c;
>     ```
>
> **Як це працює:**
>
> 1. `curriedAdder.apply(1)`: ми "фіксуємо" перший аргумент `a=1` і отримуємо **нову функцію** `b -> c -> 1 + b + c`.
>
>    `Function<Integer, Function<Integer, Integer>> add1 = curriedAdder.apply(1);`
>
> 2. `add1.apply(2)`: ми фіксуємо другий аргумент `b=2` і отримуємо **ще одну нову функцію** `c -> 1 + 2 + c`.
>
>    `Function<Integer, Integer> add1And2 = add1.apply(2);`
>
> 3. `add1And2.apply(3)`: ми передаємо останній аргумент і отримуємо фінальний результат `6`.
>
> **Навіщо це потрібно:**
> Каррінг дозволяє створювати **частково застосовані функції (partial application)**. Ви можете "зафіксувати" частину аргументів і створити нову, більш спеціалізовану функцію, яку потім можна передавати далі. Це є потужним інструментом для композиції та повторного використання функцій.

#### **📌 Що перевіряють:**

*Експертні знання концепцій функціонального програмування та здатність реалізовувати їх за допомогою мовних конструкцій Java. Це дуже просунуте питання.*
