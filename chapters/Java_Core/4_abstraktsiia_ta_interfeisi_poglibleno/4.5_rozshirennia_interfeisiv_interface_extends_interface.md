---
layout: default
title: "Розширення інтерфейсів (interface extends interface)"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 5
---

# Розширення інтерфейсів (interface extends interface)

* короткий опис: Інтерфейс у Java може успадковувати один або декілька інших інтерфейсів за допомогою ключового слова `extends`. Це дозволяє новому інтерфейсу комбінувати всі абстрактні методи предків.

* поняття: `interface extends A, B`.

* приклади: `interface C extends A, B` (інтерфейс `C` успадковує методи `A` та `B`).

* ключові моменти: Підтримується множинне наслідування інтерфейсів. Клас, що реалізує такий інтерфейс, повинен реалізувати всі успадковані методи.

* що важливо знати на співбесіді: Як працює успадкування інтерфейсів, його переваги.

### **✅ Офіційне визначення:**

Інтерфейс у Java може розширювати один або декілька інших інтерфейсів, використовуючи ключове слово **`extends`**. Це дозволяє новому інтерфейсу успадкувати всі абстрактні методи з інших інтерфейсів.

🔗 [Java Language Specification §9.1.3](https://docs.oracle.com/javase/specs/jls/se17/html/jls-9.html#jls-9.1.3)

### **🧠 Простими словами:**

Інтерфейс може "об'єднати" інші інтерфейси — він не реалізує методи, лише успадковує їх сигнатури. Це дозволяє групувати поведінки без дублювання коду.

---

## **Основне**

| Параметр | Пояснення |
| :---: | :---: |
| **Ключове слово** | **`extends`** |
| **Множинне наслідування інтерфейсів** | Підтримується (**`interface C extends A, B`**) |
| **Наслідується** | Лише сигнатури методів |
| **Конфлікти реалізацій** | Відсутні, бо немає реалізацій і стану |
| **Взаємодія з `implements` у класах** | Клас, який реалізує такий інтерфейс, повинен реалізувати всі методи |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

Щоб об'єднувати кілька інтерфейсів у новий, ширший контракт. Це особливо зручно, коли об'єкт повинен мати декілька різних поведінок.

🔸 **У чому плюс?**  
Створюється гнучка структура без дублювання. Не потрібно вказувати всі інтерфейси в кожному класі знову.

🔸 **Типова помилка:**  
Клас реалізує похідний інтерфейс, але не реалізував методи з одного з батьків — отримаємо помилку компіляції
---

## **Корисні приклади (🧪)**

```java
public interface A {
    void methodA();
}

public interface B {
    void methodB();
}

// C успадковує A та B
public interface C extends A, B {
    void methodC();
}

// Клас реалізує C -- має реалізувати methodA(), methodB(), methodC()
public class MyClass implements C {
    @Override
    public void methodA() {
        System.out.println("A");
    }

    @Override
    public void methodB() {
        System.out.println("B");
    }

    @Override
    public void methodC() {
        System.out.println("C");
    }
}
```

---

## **Порівняння (⚖️)**

| Особливість | Класи (`extends`) | Інтерфейси (`extends`) |
| :---: | :---: | :---: |
| **Множинне наслідування** | ❌ | ✅ |
| **Що наслідується** | Логіка \+ стан | Тільки сигнатури методів |
| **Конфлікти реалізацій** | Можливі | Не виникають |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **"Інтерфейсів багато — реалізація одна"**

* **`interface extends A, B, C — ✅`**
* **`class extends A, B`** — ❌ (в Java не підтримується множинне наслідування класів)

---

## **🟩 Підсумок:**

Механізм розширення інтерфейсів у Java дозволяє створювати більш загальні абстракції, об'єднуючи поведінки з кількох джерел без дублювання. Це дає змогу легко будувати поведінкові ієрархії, зберігаючи чистоту коду та уникнення конфліктів реалізації.

---

### **🔗 Додаткові джерела:**

* [**Oracle Tutorial: Interface Inheritance**](https://docs.oracle.com/javase/tutorial/java/IandI/interfaceDef.html)
* [**Java SE 17 Language Specification: §9.1.3 Interface Declarations**](https://docs.oracle.com/javase/specs/jls/se17/html/jls-9.html#jls-9.1.3)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що означає "розширення інтерфейсів" у Java? Яке ключове слово для цього використовується?

> #### **💬 Відповідь:**
>
> **Розширення інтерфейсів** — це механізм, за яким один інтерфейс (дочірній) може **успадкувати** всі члени (абстрактні методи, `default` методи, `static` методи, константи) одного або декількох інших інтерфейсів (батьківських).
>
> Для цього використовується те саме ключове слово, що і для класів — **`extends`**.
>
> **Головна мета:**
> Створити **більш специфічний контракт**, об'єднавши вимоги кількох інших контрактів. Це дозволяє будувати **ієрархії типів** і уникати дублювання оголошень методів.
>
> **Приклад:**
>
> ```java
> interface Readable {
>     String read();
> }
>
> // CloseableReadable успадковує метод read() і додає свій власний
> interface CloseableReadable extends Readable {
>     void close();
> }
>
> // Клас, що реалізує CloseableReadable, зобов'язаний реалізувати обидва методи
> class FileReader implements CloseableReadable {
>     public String read() { /* ... */ }
>     public void close() { /* ... */ }
> }
> ```

#### **📌 Що перевіряють:**

*Базове розуміння того, що інтерфейси можуть утворювати ієрархії так само, як і класи, але зі своїми правилами.*

* * *

### **2 ❓ Питання:**

Чи підтримує Java множинне наслідування? Поясніть у контексті класів та інтерфейсів.

> #### **💬 Відповідь:**
>
> Це питання з нюансом. Правильна відповідь: **"залежить від того, про що йде мова"**.
>
> **Множинне наслідування класів (реалізації та стану):**
>
> * **Ні, Java не підтримує.** Клас може `extends` лише **один** інший клас. Це зроблено, щоб уникнути **"проблеми ромба" (Diamond Problem)**, пов'язаної з конфліктом реалізацій.
>
> **Множинне наслідування інтерфейсів (типів та контрактів):**
>
> * **Так, Java повністю підтримує.**
>   * Інтерфейс може `extends` **будь-яку кількість** інших інтерфейсів.
>   * Клас може `implements` **будь-яку кількість** інтерфейсів.
> * **Чому це безпечно:** Тому що успадковуються в першу чергу **контракти (абстрактні методи)**, а не реалізація. Клас-реалізатор сам надає кінцеву реалізацію, тому конфлікту не виникає.
>
> **Приклад:**
>
> ```java
> interface A {}
> interface B {}
>
> // Інтерфейс успадковує два інтерфейси
> interface C extends A, B {}
>
> // Клас успадковує клас і реалізує два інтерфейси
> class D extends MyBaseClass implements A, B {}
> ```

#### **📌 Що перевіряють:**

*Чітке розмежування між наслідуванням реалізації та наслідуванням типів. Це фундаментальна відмінність у дизайні Java.*

* * *

### **3 ❓ Питання:**

Що станеться, якщо клас реалізує інтерфейс `C`, який є `C extends A, B`, а в інтерфейсах `A` і `B` є абстрактні методи з однаковою сигнатурою?

> #### **💬 Відповідь:**
>
> **Жодного конфлікту не виникне.**
>
> **Пояснення:**
>
> * Інтерфейси визначають лише **контракт**. Якщо два або більше батьківських інтерфейсів мають абстрактний метод з однаковою сигнатурою, для класу-реалізатора це просто означає, що він отримує **одне й те саме зобов'язання** з кількох джерел.
> * Клас повинен надати **лише одну реалізацію** цього методу, яка автоматично задовольнить контракт усіх інтерфейсів.
>
> **Приклад:**
>
> ```java
> interface Clickable {
>     void onClick();
> }
> interface Submittable {
>     void onClick(); // така ж сигнатура
> }
>
> // SuperButtonContract успадковує одне й те саме зобов'язання
> interface SuperButtonContract extends Clickable, Submittable {}
>
> // Клас Button повинен реалізувати onClick() лише один раз
> class Button implements SuperButtonContract {
>     @Override
>     public void onClick() {
>         System.out.println("Button was clicked!");
>     }
> }
> ```
>
> Конфлікт виник би, якби ці методи мали **різні типи повернення** або якби обидва були **`default`-методами** з різною реалізацією.

#### **📌 Що перевіряють:**

*Розуміння того, що конфлікту контрактів при однакових сигнатурах немає.*

* * *

### **4 ❓ Питання:**

Як вирішується конфлікт, якщо інтерфейс успадковує два інтерфейси з `default`-методами з однаковою сигнатурою? ⭐️

> #### **💬 Відповідь:**
>
> Ситуація аналогічна до класу, що реалізує два інтерфейси з однаковими `default`-методами.
>
> **Інтерфейс-спадкоємець зобов'язаний вирішити цей конфлікт.** Він має два варіанти:
>
> 1. **Перевизначити конфліктний метод як `abstract`:**
>     * Це найпростіший спосіб. Інтерфейс-спадкоємець просто "скидає" `default`-реалізацію і перекладає обов'язок надання реалізації на кінцеві класи.
>
>     ```java
>     interface A { default void doWork() { System.out.println("A"); } }
>     interface B { default void doWork() { System.out.println("B"); } }
>     interface C extends A, B {
>         @Override
>         void doWork(); // Робимо метод знову абстрактним
>     }
>     ```
>
> 2. **Надати власну `default`-реалізацію:**
>
>     * Інтерфейс-спадкоємець може перевизначити метод і надати власну логіку, яка, в свою чергу, може викликати одну з батьківських реалізацій.
>
>     ```java
>     interface C extends A, B {
>         @Override
>         default void doWork() {
>             A.super.doWork(); // Викликаємо реалізацію з A
>         }
>     }
>     ```
>
> Таким чином, неоднозначність завжди вирішується явно, не дозволяючи компілятору робити припущень.

#### **📌 Що перевіряють:**

*Знання механізму вирішення конфліктів для `default` методів в ієрархіях інтерфейсів.*

* * *

### **5 ❓ Питання:**

Що станеться, якщо інтерфейс успадковує два інтерфейси з однаковими константами? ⭐️

> #### **💬 Відповідь:**
>
> Сам по собі інтерфейс, що розширює батьківські, **скомпілюється без проблем**. Проблема **неоднозначності** виникне лише тоді, коли якийсь код спробує **звернутися до цієї константи** через ім'я дочірнього інтерфейсу.
>
> **Приклад:**
>
> ```java
> interface A { String NAME = "A"; }
> interface B { String NAME = "B"; }
>
> // Цей інтерфейс компілюється нормально
> interface C extends A, B {}
>
> public class Main {
>     public static void main(String[] args) {
>         // ПОМИЛКА КОМПІЛЯЦІЇ: "reference to NAME is ambiguous"
>         // System.out.println(C.NAME);
>
>         // Як виправити: звернутися до константи через ім'я батьківського інтерфейсу
>         System.out.println(A.NAME); // "A"
>         System.out.println(B.NAME); // "B"
>     }
> }
> ```
>
> Це демонструє, що конфлікт виникає не при оголошенні, а при спробі доступу.

#### **📌 Що перевіряють:**

*Розуміння того, як Java обробляє конфлікти статичних членів в ієрархатії інтерфейсів.*

* * *

### **6 ❓ Питання:**

Наведіть приклад з JDK, де використовується розширення інтерфейсів.

> #### **💬 Відповідь:**
>
> **Java Collections Framework** є чудовим прикладом.
>
> 1. **`interface Collection<E>`** — це один з базових інтерфейсів, що визначає основні операції, як-от `add()`, `size()`, `iterator()`.
>
> 2. **`interface List<E>`** — **розширює `Collection<E>`**.
>     `public interface List<E> extends Collection<E>`
>     * `List` успадковує всі методи `Collection` і додає свої власні, специфічні для списків (упорядкованих колекцій): `get(int index)`, `set(int index, E element)`, `indexOf(Object o)`.
>
> 3. **`interface SortedSet<E>`** — **розширює `Set<E>`** (який, у свою чергу, розширює `Collection<E>`).
>     * Він успадковує контракт множини і додає методи, специфічні для впорядкованих множин: `first()`, `last()`, `subSet(...)`.
>
> Ця ієрархія дозволяє створювати більш спеціалізовані контракти на основі загальних, що робить API колекцій гнучким та добре структурованим.

#### **📌 Що перевіряють:**

*Знання структури стандартної бібліотеки Java. Кандидат повинен розуміти дизайн Java Collections Framework.*

* * *

### **7 ❓ Питання:**

Чи може клас реалізувати інтерфейс `A` і одночасно `B`, якщо `B extends A`? Чи потрібно реалізовувати методи з `A`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Але оголошення `implements A` у цьому випадку є **надлишковим**.
>
> ```java
> interface A { void methodA(); }
> interface B extends A { void methodB(); }
>
> // Замість 'implements A, B' достатньо написати 'implements B'
> class MyClass implements A, B {
>     public void methodA() { ... }
>     public void methodB() { ... }
> }
> ```
>
> **Пояснення:**
>
> * Коли ви пишете `implements B`, ви берете на себе зобов'язання реалізувати **всі методи**, які визначені в інтерфейсі `B`.
> * Оскільки `B` **розширює `A`**, він успадковує і контракт `A`. Таким чином, контракт `B` вже включає в себе контракт `A`.
> * Отже, реалізуючи `B`, ви **автоматично** задовольняєте і контракт `A`.
>
> Компілятор не видасть помилку, але IDE, скоріше за все, попередить про надлишковість `implements A`.
>
> **Правильний, більш лаконічний запис:**
> `class MyClass implements B { ... }`

#### **📌 Що перевіряють:**

*Розуміння транзитивності наслідування в інтерфейсах. Здатність писати чистий та ненадлишковий код.*

* * *

### **8 ❓ Питання:**

Як працює ключове слово `super` для виклику `default` методу з успадкованого інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для виклику `default` методу з батьківського інтерфейсу використовується спеціальний синтаксис **`InterfaceName.super.methodName()`**.
>
> Це необхідно у двох випадках:
>
> 1. **При вирішенні конфлікту**, коли клас реалізує два інтерфейси з однаковим `default` методом.
> 2. Коли клас **перевизначає `default` метод**, але хоче викликати оригінальну реалізацію з інтерфейсу, щоб розширити її.
>
> **Приклад розширення поведінки:**
>
> ```java
> interface Logger {
>     default void log(String message) {
>         System.out.println("[LOG]: " + message);
>     }
> }
>
> class FileLogger implements Logger {
>     @Override
>     public void log(String message) {
>         // 1. Спочатку викликаємо логіку за замовчуванням
>         Logger.super.log(message);
>
>         // 2. Потім додаємо свою
>         // ... код для запису у файл ...
>         System.out.println("Message also written to file.");
>     }
> }
> ```
>
> Важливо зазначити, що звичайний `super.log()` тут би не спрацював, оскільки `super` стосується батьківського **класу**, а не інтерфейсу.

#### **📌 Що перевіряють:**

*Знання сучасного синтаксису Java 8 для роботи з `default` методами.*

* * *

### **9 ❓ Питання:**

Чи може `default` метод з дочірнього інтерфейсу викликати `default` метод з батьківського інтерфейсу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може.**
>
> Це дозволяє створювати ієрархії `default`-поведінки. Синтаксис для цього такий самий, як і в класі — **`InterfaceName.super.methodName()`**.
>
> **Приклад:**
>
> ```java
> interface A {
>     default void process() {
>         System.out.println("Processing A");
>     }
> }
>
> interface B extends A {
>     @Override
>     default void process() {
>         // Викликаємо реалізацію з батьківського інтерфейсу A
>         A.super.process();
>         System.out.println("Processing B");
>     }
> }
>
> class MyImpl implements B {}
>
> new MyImpl().process();
> ```
>
> **Вивід:**
>
> ```
>
> Processing A
> Processing B
> ```
>
> Це показує, що `default` методи поводяться дуже схоже на звичайні методи при наслідуванні.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як `default` методи працюють в ієрархаті інтерфейсів.*

* * *

### **10 ❓ Питання:**

Чому кажуть, що множинна реалізація інтерфейсів є кращою за множинне наслідування класів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Множинна реалізація інтерфейсів вважається кращою, тому що вона надає **гнучкість** без **проблем**, властивих множинному наслідуванню класів.
>
> **Головні переваги:**
>
> 1. **Уникає "проблеми ромба":** Інтерфейси (традиційно) не несуть реалізації, тому конфлікту успадкованого коду не виникає. Клас сам відповідає за реалізацію.
>
> 2. **Слабка зв'язність:** Класи, що реалізують інтерфейс, залежать лише від його **контракту**, а не від деталей реалізації батьківських класів. Це полегшує заміну та тестування.
>
> 3. **Підтримка композиційного дизайну:** Множинна реалізація дозволяє класу "отримати" багато різних поведінок (`can-do`), що краще відповідає принципу "надавати перевагу композиції над наслідуванням".
>
> **Множинне наслідування класів**, навпаки, призвело б до:
>
> * **Жорсткої ієрархаті:** Важко змінювати та підтримувати.
> * **Конфліктів стану:** Що робити, якщо два батьківські класи мають поле з однаковою назвою?
>
> Тому модель Java (одиничне наслідування класів + множинна реалізація інтерфейсів) вважається більш безпечним та гнучким компромісом.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Кандидат повинен розуміти дизайнерські рішення, що лежать в основі мови.*

* * *

### **11 ❓ Питання:**

Що станеться, якщо інтерфейс `B extends A`, і обидва оголошують метод з однаковою сигнатурою, але різними `checked` винятками? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **помилки компіляції**.
>
> Правило перевизначення методів щодо `checked` винятків діє і для ієрархаті інтерфейсів: **перевизначений (дочірній) метод не може оголошувати новий або ширший `checked` виняток**, ніж батьківський.
>
> **Приклад:**
>
> ```java
> interface A {
>     void process() throws java.io.IOException;
> }
>
> // ПОМИЛКА КОМПІЛЯЦІЇ: 'process()' in B clashes with 'process()' in A;
> // attempting to use incompatible exception type
> interface B extends A {
>     @Override
>     void process() throws java.sql.SQLException;
> }
> ```
>
> Тут `SQLException` не є підкласом `IOException`, тому компілятор вважає це порушенням контракту.
>
> **Що дозволено:**
>
> * `@Override void process() throws java.io.FileNotFoundException;` (бо `FileNotFoundException` є підкласом `IOException`).
> * `@Override void process();` (не кидати `checked` винятки взагалі).
>
> Це правило гарантує, що клієнтський код, який працює з інтерфейсом `A`, не зіткнеться з несподіваними `checked` винятками при роботі з реалізаціями `B` або `C`.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про правила успадкування, що стосуються винятків.*

* * *

### **12 ❓ Питання:**

Який зв'язок між патерном "Міст" (Bridge) та розширенням інтерфейсів? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Патерн "Міст" — це структурний патерн, який **розділяє великий клас або набір тісно пов'язаних класів на дві окремі ієрархії — абстракцію та реалізацію**, що дозволяє їм розвиватися незалежно.
>
> **Розширення інтерфейсів** є одним з ключових інструментів для реалізації цього патерну.
>
> **Як це працює:**
>
> 1. **Створюється ієрархія абстракцій:** Зазвичай це `abstract class`, який містить посилання на **інтерфейс реалізації**.
>
> 2. **Створюється ієрархія реалізацій:** Це **інтерфейс** та його конкретні реалізації. Цей інтерфейс можна **розширювати** для створення нових типів реалізацій.
>
> **Приклад:**
> У нас є різні фігури (`Circle`, `Square`) і різні способи їх малювання (`VectorRenderer`, `RasterRenderer`).
>
> * **Ієрархія абстракцій (фігури):**
>     `abstract class Shape { protected Renderer renderer; ... }`
>
> * **Ієрархія реалізацій (рендерери):**
>     `interface Renderer { void renderCircle(...); }`
>     `interface AdvancedRenderer extends Renderer { void renderWithShadow(...); }`
>
> Тепер можна комбінувати будь-яку фігуру з будь-яким рендерером. **Розширення інтерфейсів** (`AdvancedRenderer extends Renderer`) дозволяє додавати нові можливості до реалізацій, не чіпаючи ієрархію фігур. Це і є "міст" між ними.

#### **📌 Що перевіряють:**

*Знання патернів проектування та здатність бачити, як конкретні мовні конструкції використовуються для їх реалізації.*

* * *

### **13 ❓ Питання:**

Чи можна реалізувати інтерфейс з дженеріком, вказавши конкретний тип? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, це стандартна і дуже поширена практика.**
>
> Коли ви реалізуєте параметризований (generic) інтерфейс, ви можете або:
>
> **1. Залишити клас-реалізатор також дженериком:**
>
> ```java
> // ArrayList також є дженериком
> public class ArrayList<T> implements List<T> { ... }
> ```
>
> **2. Вказати конкретний тип (зафіксувати його):**
>
> Це робиться, коли ваш клас призначений для роботи лише з одним конкретним типом.
>
> ```java
> // Наша власна реалізація списку, яка працює лише з рядками
> public class StringList implements List<String> {
>
>     // Тепер всі успадковані методи будуть працювати з String,
>     // а не з узагальненим T.
>     @Override
>     public boolean add(String e) { /* ... */ }
>
>     @Override
>     public String get(int index) { /* ... */ }
> }
> ```
>
> Цей підхід є абсолютно коректним і дозволяє створювати типізовані, спеціалізовані класи на основі узагальнених інтерфейсів.
>
> Інтерфейс `Comparable<T>` — ще один чудовий приклад. Коли клас його реалізує, він зазвичай фіксує тип: `class User implements Comparable<User>`.

#### **📌 Що перевіряють:**

*Знання дженериків та того, як вони застосовуються в контексті реалізації інтерфейсів.*

* * *

### **14 ❓ Питання:**

Що станеться, якщо `default` метод в інтерфейсі викликає метод, який згодом був доданий до класу `Object`? (Наприклад, `isEmpty()` до Java 15). ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про правила вирішення конфліктів та еволюцію JDK.
>
> **Правило:** **Методи з класу `Object` завжди мають вищий пріоритет**, ніж `default`-методи з інтерфейсів.
>
> **Сценарій:**
>
> 1. Ви пишете інтерфейс `MyInterface` з `default` методом `boolean isEmpty() { ... }`.
> 2. Ви пишете клас `MyClass implements MyInterface`, який успадковує цю `default`-реалізацію.
> 3. Ви оновлюєте JDK до версії, де в `String` (або іншому суперкласі) з'явився публічний метод `isEmpty()`. Або, теоретично, якби його додали в `Object`.
>
> **Що станеться:**
>
> * `default` метод з вашого інтерфейсу буде **проігноровано**.
> * Завжди буде викликана реалізація з суперкласу (зрештою, з `Object`).
> * Це може **несподівано змінити поведінку** вашої програми.
>
> Через цей ризик, **рекомендація** — **уникати створення `default`-методів, сигнатури яких збігаються з методами з `Object` (`equals`, `hashCode`, `toString` тощо)**. Хоча компілятор може видати помилку в таких випадках, намагатися перевизначити їх як `default` — це погана ідея.
>
> Поява `isEmpty()` як стандартного методу рядків та колекцій — приклад того, як еволюція API може створювати подібні потенційні колізії.

#### **📌 Що перевіряють:**

*Експертне розуміння правил вирішення конфліктів у Java, включаючи перевагу методів `Object`.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви проектуєте API. Як ієрархія розширення інтерфейсів може допомогти вам структурувати різні рівні функціональності вашого API? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ієрархія інтерфейсів — це потужний інструмент для проектування **багаторівневого та гнучкого API**.
>
> **Підхід "від базового до просунутого":**
>
> 1. **Базовий інтерфейс:** Створюється основний, мінімалістичний інтерфейс, що визначає абсолютно необхідну функціональність. Його реалізувати найпростіше.
>
>     `interface SimpleRepository<T> { T findById(long id); void save(T entity); }`
>
> 2. **Просунутий інтерфейс:** Створюється другий інтерфейс, який **розширює (`extends`)** базовий і додає нові, більш складні або опціональні можливості.
>
>     `interface SearchableRepository<T> extends SimpleRepository<T> { List<T> findByName(String name); }`
>
> 3. **Ще більш специфічний інтерфейс:**
>
>     `interface AuditableRepository<T> extends SearchableRepository<T> { List<AuditLog> getHistory(long id); }`
>
> **Переваги такого дизайну:**
>
> * **Модульність:** Кожен інтерфейс визначає один аспект функціональності.
> * **Гнучкість для клієнтів:**
>   * Клас, якому потрібен лише базовий функціонал, може реалізувати тільки `SimpleRepository`.
>   * Клас, якому потрібні всі можливості, може реалізувати одразу `AuditableRepository`.
> * **Чітка структура API:** Ієрархія інтерфейсів чітко документує різні рівні можливостей вашого API.
> * **Дотримання ISP:** Замість одного "товстого" інтерфейсу ми маємо кілька менших.
>
> Цей підхід часто використовується у великих фреймворках (наприклад, Spring Data), щоб надати розробникам гнучкість у виборі рівня функціональності, який їм потрібен.

#### **📌 Що перевіряють:**

*Архітектурне мислення. Здатність проектувати гнучкі та добре структуровані API, використовуючи можливості мови.*
