---
layout: default
title: "Різниця між інтерфейсом і абстрактним класом"
parent: "Абстракція та Інтерфейси (поглиблено)"
grand_parent: "Java Core"
nav_order: 6
---

# Різниця між інтерфейсом і абстрактним класом

* короткий опис: Інтерфейс – це контракт поведінки без стану, що підтримує множинну реалізацію. Абстрактний клас – це шаблон, який може містити часткову реалізацію та стан, але підтримує лише одиничне наслідування.

* поняття: `interface`, `abstract class`, `implements`, `extends`, множинне наслідування.

* приклади: Порівняльна таблиця за ключовими словами, можливостями та сценаріями використання.

* ключові моменти: Інтерфейси для "що робити", абстрактні класи для "що спільного мають". Інтерфейси не мають конструкторів, абстрактні класи можуть.

* що важливо знати на співбесіді: Детальні відмінності, коли що краще використовувати (shared fields vs shared behavior).

### **✅ Офіційне визначення:**

**Інтерфейс** — це абстрактний тип, що оголошує набір методів без реалізації (до Java 8).

**Абстрактний клас** — це клас, який може містити як абстрактні методи, так і методи з реалізацією, і не може бути створений напряму.

🔗 [JLS §8.1.1.1 – Abstract Classes](https://docs.oracle.com/javase/specs/jls/se21/html/jls-8.html#jls-8.1.1.1)  
🔗 [JLS §9.1 – Interfaces](https://docs.oracle.com/javase/specs/jls/se21/html/jls-9.html#jls-9.1)

### **🧠 Простими словами:**

* **Інтерфейс** — це контракт: опис того, що має робити клас.
* **Абстрактний клас** — це напівготовий шаблон: вже є частина логіки та полів, решту треба додати.

---

## **Основні відмінності (📊)**

|         Критерій         |            Інтерфейс (`interface`)             |           Абстрактний клас (`abstract class`)           |
|:------------------------:|:----------------------------------------------:|:-------------------------------------------------------:|
|    **Ключове слово**     |                **`interface`**                 |                     **`abstract`**                      |
|     **Наслідування**     |                **`implements`**                |                      **`extends`**                      |
|     **Множинність**      |        ✅ Так (можна реалізувати кілька)        |          ❌ Ні (тільки один батьківський клас)           |
| **Методи з реалізацією** |  ✅ З **`default`** або **`static`** з Java 8   |                ✅ Звичайні методи з тілом                |
|  **Абстрактні методи**   |       ✅ (завжди, до Java 8 — лише такі)        |                            ✅                            |
|         **Поля**         | Тільки **`public`** `static final` (константи) | Будь-які (змінні стану, **`private`**, **`protected`**) |
|     **Конструктори**     |                    ❌ Немає                     |       ✅ Є (але не можна створити об'єкт напряму)        |
|     **Інкапсуляція**     |              ❌ Немає полів стану               |                        ✅ Можлива                        |
|  **Коли застосовувати**  |            Для поведінки без стану             |             Для спільної поведінки \+ стану             |

---

## **Пояснення на пальцях (💡)**

🔸 **Інтерфейс — це “що треба вміти”**  
Наприклад, **`Printable`**, **`Walkable`**, **`Flyable`** — просто набір методів, які клас має реалізувати.

🔸 **Абстрактний клас — це “що спільного мають”**  
Може містити загальні поля (наприклад, **`name`**, **`age`**) і реалізацію деяких методів (**`log()`**), а абстрактні
методи (**`speak()`**) залишити для реалізації в нащадках.

🔸 **Типова помилка**  
Використовувати інтерфейс, коли потрібне спільне поле або логіка — наприклад, лічильник, ім’я, дата створення.

---

## **Корисні приклади (🧪)**

🔻 **Інтерфейс:**

```java
public interface Printable {
    void print();
}

```

🔻 **Абстрактний клас:**

```java
public abstract class Document {
    protected String title;

    public Document(String title) {
        this.title = title;
    }

    public void printTitle() {
        System.out.println("Title: " + title);
    }

    public abstract void print(); // абстрактний метод
}
```

🔻 **Клас, що реалізує обидва:**

```java
public class Report extends Document implements Printable {
    public Report(String title) {
        super(title);
    }

    @Override
    public void print() {
        System.out.println("Printing report...");
    }
}
```

---

## **Порівняння (⚖️)**

|                 Сценарій                 |     Краще використовувати     |
|:----------------------------------------:|:-----------------------------:|
|       **Треба описати поведінку**        | ✅ Інтерфейс (**`interface`**) |
|     **Треба спільні поля та методи**     |      ✅ Абстрактний клас       |
|   **Клас має вже часткову реалізацію**   |      ✅ Абстрактний клас       |
| **Клас реалізує кілька ролей/поведінок** |         ✅ Інтерфейси          |
| **Потрібна гнучкість і розширюваність**  |         ✅ Інтерфейси          |

---

## **🧠 Мнемоніка для запам’ятовування:**

📌 **"Інтерфейс — поведінка, абстрактний клас — поведінка \+ стан"**

* **`interface`** -> що клас має **вміти**
* **`abstract class`** -> що клас **має** та **як діє**

---

## **🟩 Підсумок:**

**`Інтерфейс`** — це інструмент для визначення поведінки без стану, чудово підходить для композиції та гнучкості.  
**`Абстрактний клас`** — це заготовка з частковою реалізацією та станом, підходить для спільної логіки.  
Після Java 8 інтерфейси стали ще потужнішими завдяки можливості мати **`default`**, **`static`**, а з Java 9 — і *
*`private`** методи.

---

### **🔗 Додаткові джерела:**

* **🔗 [Oracle Java Tutorial: Interface vs Abstract Class](https://docs.oracle.com/javase/tutorial/java/IandI/abstract.html)**

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка головна концептуальна різниця між абстрактним класом та інтерфейсом?

> #### **💬 Відповідь:**
>
> Головна відмінність полягає в їхньому **семантичному призначенні** та типі відношення, яке вони встановлюють.
>
> * **Абстрактний клас** встановлює відношення **IS-A (є різновидом)**.
>   * Він описує **ідентичність** та **фундаментальну природу** об'єкта.
>   * Використовується для створення ієрархії **тісно пов'язаних** класів, які мають **спільний стан (поля)** та/або **базову реалізацію**.
>   * **Приклад:** `Dog` та `Cat` — це різновиди `Animal`. Вони мають спільні властивості, як-от вік або вага.
>
> * **Інтерфейс** встановлює відношення **CAN-DO (може робити)**.
>   * Він описує **поведінку, контракт або здатність**, яку може мати об'єкт.
>   * Використовується для надання певної функціональності абсолютно **непов'язаним** класам.
>   * **Приклад:** `Airplane` та `Bird` — це абсолютно різні об'єкти, але обидва можуть реалізовувати інтерфейс `Flyable`.

#### **📌 Що перевіряють:**

*Здатність пояснити різницю не тільки технічно ("один `extends`, багато `implements`"), але й з точки зору моделювання предметної області.*

* * *

### **2 ❓ Питання:**

Назвіть три ключові технічні відмінності між абстрактним класом та інтерфейсом (у сучасній Java).

> #### **💬 Відповідь:**
>
> Навіть після нововведень у Java 8+, ключові відмінності залишаються.
>
> 1. **Наслідування:**
>     * Клас може успадкувати **лише один** абстрактний клас.
>     * Клас може реалізувати **багато** інтерфейсів.
>
> 2. **Стан (Поля екземпляра):**
>     * Абстрактний клас **може** мати поля стану (`non-static`, `non-final`). Це його ключова перевага для надання спільного стану спадкоємцям.
>     * Інтерфейс **не може** мати полів стану. Всі його поля є неявно `public static final` константами.
>
> 3. **Конструктори:**
>     * Абстрактний клас **може** (і часто має) конструктори для ініціалізації свого стану.
>     * Інтерфейс **не може** мати конструкторів.

#### **📝 Коментар:**

Відмінність у можливості мати **стан** є сьогодні головним критерієм при виборі між абстрактним класом та інтерфейсом.

#### **📌 Що перевіряють:**

*Чітке знання технічних обмежень та можливостей кожного інструменту.*

* * *

### **3 ❓ Питання:**

Коли на практиці слід обрати абстрактний клас, а коли — інтерфейс? Наведіть сценарії для обох.

> #### **💬 Відповідь:**
>
> Вибір залежить від задачі проектування.
>
> **Обирайте абстрактний клас, коли:**
>
> * **Ви хочете поділитися кодом між тісно пов'язаними класами.**
> * **Ви очікуєте, що класи в ієрархаті матимуть спільні поля (стан) або методи (реалізацію).**
> * **Ви хочете надати базовий каркас, залишивши для реалізації лише деякі деталі (патерн "Шаблонний метод").**
>
> **Сценарій:** Створення різних типів сповіщень. `AbstractNotification` може містити спільні поля, як-от `recipient` і `timestamp`, та спільну логіку логування, а підкласи `EmailNotification` та `SmsNotification` реалізують лише метод `send()`.
>
> **Обирайте інтерфейс, коли:**
>
> * **Ви очікуєте, що абсолютно непов'язані класи будуть реалізовувати цю поведінку.**
> * **Ви хочете визначити лише контракт поведінки, не нав'язуючи стан або реалізацію.**
> * **Ви хочете використати переваги множинного наслідування типів.**
>
> **Сценарій:** Інтерфейс `Serializable`. І `User`, і `Configuration`, і `Date` можуть бути серіалізованими, хоча між ними немає нічого спільного.

#### **📌 Що перевіряють:**

*Здатність аналізувати вимоги та приймати обґрунтоване архітектурне рішення.*

* * *

### **4 ❓ Питання:**

Чи можна інстанціювати абстрактний клас? А інтерфейс?

> #### **💬 Відповідь:**
>
> **Напряму — ні, в обох випадках.**
>
> 1. **Абстрактний клас:**
>     * Ви не можете написати `new MyAbstractClass()`. Це призведе до **помилки компіляції**.
>     * Ключове слово `abstract` спеціально для цього і призначене — заборонити створення екземплярів неповних класів.
>
> 2. **Інтерфейс:**
>     * Аналогічно, `new MyInterface()` — це помилка компіляції. Інтерфейс є чистим контрактом без реалізації.
>
> **Як можна створити об'єкт опосередковано:**
>
> В обох випадках ви можете створити екземпляр **анонімного внутрішнього класу**, який успадковує абстрактний клас або реалізує інтерфейс і одразу надає реалізацію для всіх абстрактних методів.
>
> ```java
> Runnable r = new Runnable() {
>     @Override
>     public void run() { /* ... */ }
> };
> ```
>
> Тут ми створюємо екземпляр **анонімного класу**, а не `Runnable`.

#### **📌 Що перевіряють:**

*Точне розуміння того, що `abstract` на рівні класу/інтерфейсу означає заборону на пряме створення об'єктів.*

* * *

### **5 ❓ Питання:**

Як `default` методи (з Java 8) "розмили" межу між інтерфейсами та абстрактними класами?

> #### **💬 Відповідь:**
>
> `Default` методи дозволили інтерфейсам мати **методи з реалізацією**, що раніше було прерогативою абстрактних (та конкретних) класів. Це дійсно "розмило" межу, але не стерло її повністю.
>
> * **Що стало схожим:**
>   * І інтерфейси, і абстрактні класи тепер можуть надавати **готову реалізацію поведінки**. Це зменшило потребу в абстрактних класах-скелетах, якщо не потрібен спільний стан.
>
> * **Які ключові відмінності залишилися:**
>     1. **Стан:** Абстрактні класи все ще єдині, хто може мати поля стану екземпляра (`private`, `protected`). Інтерфейси — ні. **Це головна відмінність.**
>     2. **Наслідування:** Клас може успадкувати лише один абстрактний клас, але реалізувати багато інтерфейсів.
>     3. **Конструктори:** Абстрактні класи мають конструктори, інтерфейси — ні.
>
> **Висновок:** `default` методи дали інтерфейсам можливість реалізовувати патерни, як-от "Шаблонний метод" (для безстанової логіки), що раніше було доступно лише абстрактним класам. Але фундаментальна різниця у можливості мати стан залишилася.

#### **📌 Що перевіряють:**

*Розуміння впливу `default` методів на класичне порівняння. Кандидат повинен знати, що змінилося, а що — ні.*

* * *

### **6 ❓ Питання:**

Наведіть приклад, де абстрактний клас реалізує інтерфейс. У чому сенс такого підходу? ⭐️

> #### **💬 Відповідь:**
>
> Це дуже поширений та корисний патерн, відомий як **"скелетна реалізація" (Skeletal Implementation)**.
>
> **Сенс:** поєднати гнучкість інтерфейсів з повторним використанням коду від абстрактних класів.
>
> **Приклад з JDK:**
>
> 1. Існує інтерфейс **`java.util.List`**. Він визначає **контракт**, якому повинні відповідати всі списки.
>
> 2. Існує абстрактний клас **`java.util.AbstractList`**, який `implements List`.
>     * Цей клас надає **готову ("скелетну") реалізацію** для більшості методів `List` (наприклад, `indexOf`, `iterator`, `equals`, `hashCode`).
>     * Ці реалізації базуються на кількох ключових методах, які `AbstractList` залишає **абстрактними**: `get(int index)` та `size()`.
>
> 3. **Що це дає:**
>     Якщо ви хочете створити власну реалізацію списку (наприклад, незмінну), вам не потрібно реалізовувати десятки методів з `List`. Ви можете просто **успадкувати від `AbstractList`** і реалізувати лише два обов'язкових методи (`get` і `size`). Решту функціональності ви отримаєте безкоштовно.
>
> **`ArrayList` та `LinkedList`** також використовують цей підхід для уникнення дублювання коду.

#### **📌 Що перевіряють:**

*Знання поширених патернів дизайну та їхньої реалізації в стандартній бібліотеці Java.*

* * *

### **7 ❓ Питання:**

Чому поля в інтерфейсі завжди `public static final`?

> #### **💬 Відповідь:**
>
> Це випливає з самої **концептуальної природи** інтерфейсу як контракту поведінки, який **не повинен мати власного стану**.
>
> * **`public`:** Інтерфейс визначає публічний контракт, тому все, що в ньому є, повинно бути доступним ззовні.
>
> * **`static`:** Поле належить самому інтерфейсу, а не екземпляру класу. Якби воно було не-статичним, це означало б, що інтерфейс має стан, що суперечить його ідеї.
>
> * **`final`:** Поле є константою і його не можна змінити. Знову ж таки, якби його можна було змінювати, це було б формою стану.
>
> Отже, інтерфейс може визначати лише **константи**, які логічно пов'язані з ним. Наприклад:
>
> ```java
> interface NetworkConstants {
>     int DEFAULT_TIMEOUT = 5000;
> }
> ```
>
> У той же час, **абстрактний клас** призначений для того, щоб мати **спільний стан**, тому його поля можуть бути будь-якими.

#### **📌 Що перевіряють:**

*Розуміння семантичних причин, що лежать в основі синтаксичних правил мови.*

* * *

### **8 ❓ Питання:**

Чи може абстрактний клас не містити жодного абстрактного методу? Якщо так, то яка його мета? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Абсолютно валідним є оголосити клас як `abstract`, навіть якщо всі його методи мають реалізацію.
>
> **Мета такого підходу** — **заборонити створення екземплярів цього класу**.
>
> Ключове слово `abstract` на рівні класу служить сигналом, що цей клас є **неповним** або **непризначеним для самостійного існування**. Він призначений лише для того, щоб бути **шаблоном або базою для наслідування**.
>
> **Приклад:**
> Ви можете створити `abstract class BaseController`, який надає набір готових, не-абстрактних методів для роботи з сесією, кешем тощо. Але ви не хочете, щоб хтось міг створити `new BaseController()`. Зробивши його `abstract`, ви змушуєте розробників створювати лише конкретні контролери (`UserController`, `ProductController`), які успадковують цю базову функціональність.

#### **📌 Що перевіряють:**

*Знання того, що `abstract` на класі — це перш за все про заборону інстанціювання.*

* * *

### **9 ❓ Питання:**

Який принцип SOLID легше дотримуватися, використовуючи інтерфейси, а не абстрактні класи, і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Використання інтерфейсів значно полегшує дотримання **Принципу інверсії залежностей (Dependency Inversion Principle - DIP)**.
>
> **Принцип DIP:** високорівневі модулі не повинні залежати від низькорівневих, обидва повинні залежати від **абстракцій**.
>
> * **З інтерфейсами:**
>   * Інтерфейс є **ідеальною абстракцією**. Він не несе в собі жодної реалізації чи стану.
>   * Це дозволяє легко "від'єднати" високорівневу логіку від конкретних реалізацій.
>     `private final DataRepository repository;` (де `DataRepository` — інтерфейс).
>
> * **З абстрактними класами:**
>   * Це теж можливо, але **менш гнучко**.
>   * Залежність від абстрактного класу є **сильнішою**, ніж від інтерфейсу, оскільки абстрактний клас може нав'язувати певний стан або базову реалізацію.
>   * Крім того, оскільки клас може успадкувати лише один абстрактний клас, це створює обмеження. Якщо ваш клас вже успадковує `AbstractController`, він не зможе успадкувати `AbstractRepository` для тестування.
>
> Інтерфейси надають **чисту абстракцію без побічних ефектів**, що робить їх кращим вибором для реалізації DIP та побудови слабкозв'язаних систем.

#### **📌 Що перевіряють:**

*Зв'язок між принципами ООП та SOLID. Розуміння того, як вибір інструментів впливає на архітектуру.*

* * *

### **10 ❓ Питання:**

Чи можна оголосити конструктор в абстрактному класі? А в інтерфейсі? Поясніть "чому" для обох. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **В абстрактному класі — так, можна і потрібно.**
>   * **Чому:** Абстрактний клас може мати **поля (стан)**. Конструктор необхідний для **ініціалізації цього стану**. Хоча ви не можете створити об'єкт абстрактного класу напряму, його конструктор викликається з конструкторів дочірніх класів через `super()` для того, щоб ініціалізувати успадковану частину об'єкта.
>
> * **В інтерфейсі — ні, не можна.**
>   * **Чому:** Інтерфейси **не мають полів стану екземпляра**. Їхнє призначення — визначати контракт, а не зберігати дані. Оскільки немає стану для ініціалізації, то і конструктор не має сенсу.
>
> Ця відмінність є однією з фундаментальних і випливає з різного призначення цих двох інструментів.

#### **📌 Що перевіряють:**

*Чітке розуміння того, що абстрактні класи можуть мати стан, а інтерфейси — ні, і як це впливає на наявність конструкторів.*

* * *

### **11 ❓ Питання:**

Як еволюція Java (зокрема, `default` та `private` методи) вплинула на класичний вибір "інтерфейс чи абстрактний клас"? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вона **змістила баланс на користь інтерфейсів**, зробивши їх ще більш потужними і зменшивши кількість випадків, коли потрібні абстрактні класи.
>
> * **`default` методи (Java 8):** Дозволили інтерфейсам надавати **реалізацію поведінки**. Це забрало одну з ексклюзивних переваг абстрактних класів. Тепер для надання базової *безстанової* логіки абстрактний клас більше не потрібен.
>
> * **`private` методи (Java 9):** Дозволили **інкапсулювати** цю логіку всередині інтерфейсу, зробивши код чистішим і усунувши необхідність виносити допоміжні методи в публічне API.
>
> **Що залишилося за абстрактними класами:**
>
> Головною і практично єдиною причиною обрати абстрактний клас сьогодні є необхідність мати **спільний стан (поля екземпляра)** або `protected` члени.
>
> Якщо вам потрібен лише контракт та/або спільна поведінка без стану, **інтерфейс є кращим і більш гнучким вибором** завдяки підтримці множинної реалізації.

#### **📌 Що перевіряють:**

*Здатність аналізувати еволюцію мови та робити висновки про зміну найкращих практик проектування.*

* * *

### **12 ❓ Питання:**

Що станеться, якщо клас реалізує інтерфейс, але не надасть реалізації для одного з його абстрактних методів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У такому випадку компілятор видасть **помилку компіляції**.
>
> Повідомлення буде приблизно таким: `MyClass is not abstract and does not override abstract method myMethod() in MyInterface`.
>
> Це означає, що клас порушив **контракт**, який він зобов'язався виконувати.
>
> **Є лише один спосіб уникнути цієї помилки:**
>
> Сам клас-реалізатор також **повинен бути оголошений як `abstract`**.
>
> ```java
> // Цей клас є "проміжною" реалізацією.
> abstract class PartialImplementation implements MyInterface {
>     // Він реалізує один метод...
>     public void methodA() { ... }
>     // ...але залишає інший для своїх нащадків.
>     // abstract void methodB(); - успадковується неявно
> }
> ```
>
> Цей підхід дозволяє створювати ієрархії реалізацій, де відповідальність за реалізацію методів розподіляється по кількох рівнях.

#### **📌 Що перевіряють:**

*Знання базових правил компіляції, пов'язаних з реалізацією інтерфейсів.*

* * *

### **13 ❓ Питання:**

Поясніть патерн "Шаблонний метод". Який інструмент — інтерфейс чи абстрактний клас — краще для його реалізації? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Патерн "Шаблонний метод"** визначає **скелет алгоритму** в методі суперкласу, делегуючи реалізацію деяких його кроків дочірнім класам.
>
> **Традиційно**, найкращим інструментом для його реалізації був **абстрактний клас**.
>
> * Він містив `public final` **шаблонний метод**, який визначав послідовність кроків.
> * Деякі кроки були реалізовані як `private` або `final` методи.
> * Інші кроки, які мали кастомізувати нащадки, оголошувалися як `protected abstract` методи.
>
> **З Java 8 ситуація змінилася.**
>
> Якщо алгоритм **не залежить від стану**, цей патерн **можна ефективно реалізувати і в інтерфейсі**:
>
> ```java
> interface DataProcessor {
>     // Шаблонний метод
>     default void process() {
>         byte[] data = readData(); // крок 1, для реалізації
>         byte[] processedData = processData(data); // крок 2, для реалізації
>         System.out.println("Processing done.");
>     }
>
>     // Абстрактні кроки, які клас повинен реалізувати
>     byte[] readData();
>     byte[] processData(byte[] data);
> }
> ```
>
> **Що ж краще?**
>
> * Якщо вам потрібен **спільний стан** для кроків алгоритму — **абстрактний клас** залишається кращим вибором.
> * Якщо ваш алгоритм є **безстановим (stateless)**, **інтерфейс** є більш гнучким, оскільки не займає слот наслідування.
>
> Отже, тепер вибір залежить від наявності стану.

#### **📌 Що перевіряють:**

*Знання патернів проектування та здатність оцінити, як нові можливості мови впливають на їхню реалізацію.*

* * *

### **14 ❓ Питання:**

Чому `public static void main(String[] args)` не може бути в інтерфейсі (до Java 8) і як це змінилося? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **До Java 8**, метод `main()` **не міг бути** в інтерфейсі.
>
> **Причина:**
>
> * `main`-метод є **статичним (`static`)**.
> * До Java 8 інтерфейси **не могли містити статичних методів**. Вони могли містити лише абстрактні методи та константи.
>
> **З Java 8 все змінилося.**
>
> * **Інтерфейси отримали можливість мати `static` методи.**
> * Тому, починаючи з Java 8, ви **можете** оголосити та запустити `public static void main(String[] args)` безпосередньо з інтерфейсу.
>
> ```java
> public interface MyApp {
>     public static void main(String[] args) {
>         System.out.println("Hello from an interface!");
>     }
> }
> ```
>
> Ви можете скомпілювати та запустити його: `java MyApp`.
>
> **Чи є в цьому практичний сенс?**
>
> * **Зазвичай ні.** Це скоріше цікавий побічний ефект нових можливостей.
> * Практика розміщення `main` методу в інтерфейсі є дуже незвичною і може заплутати інших розробників.
> * Однак, це може бути корисним для швидкого тестування або демонстрації `default`-методів самого інтерфейсу без створення окремого класу-реалізатора.
>     `public static void main(String[] args) { new MyInterface() {}.myDefaultMethod(); }`

#### **📌 Що перевіряють:**

*Дуже точне знання еволюції Java та її несподіваних наслідків. Це питання на перевірку, чи кандидат дійсно глибоко розуміє сучасні фічі.*

* * *

### **15 ❓ Питання:**

Як би ви обрали між абстрактним класом та інтерфейсом, проектуючи систему для роботи з різними типами документів (`Word`, `PDF`, `Excel`), які мають спільну властивість "версія" та спільну поведінку "відкрити" та "зберегти"? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична задача на вибір архітектури, і найкращим рішенням буде **комбінований підхід**.
>
> **1. Визначаємо контракт через інтерфейс:**
>
> * Головна поведінка — "можна відкрити" та "можна зберегти". Це чистий контракт. Створюємо для цього інтерфейс. Це зробить нашу систему максимально гнучкою, оскільки клієнтський код буде залежати саме від нього.
>
>     ```java
>     public interface Document {
>         void open();
>         void save();
>         int getVersion();
>         void setVersion(int version);
>     }
>     ```
> **2. Створюємо абстрактний клас для спільного стану:**
>
> * Усі документи мають спільну властивість — "версія". Логіку зберігання цієї версії логічно винести в одне місце, щоб не дублювати її. Для цього ідеально підходить абстрактний клас, який реалізує наш інтерфейс.
>
>     ```java
>     public abstract class AbstractDocument implements Document {
>         protected int version; // Спільний стан
>
>         @Override
>         public int getVersion() {
>             return version;
>         }
>
>         @Override
>         public void setVersion(int version) {
>             this.version = version;
>         }
>     }
>     ```
>
> **3. Створюємо конкретні реалізації:**
>
> * Тепер конкретні класи можуть **успадкувати** від `AbstractDocument`, щоб отримати готову логіку роботи з версією, і їм залишиться лише реалізувати специфічні методи `open()` та `save()`.
>
>     ```java
>     public class PdfDocument extends AbstractDocument {
>         @Override
>         public void open() { /* логіка для PDF */ }
>         @Override
>         public void save() { /* логіка для PDF */ }
>     }
>     ```
>
> **Переваги цього підходу:**
>
> * **Гнучкість:** Клієнти працюють з інтерфейсом `Document`.
> * **Повторне використання коду:** Спільна логіка версіонування знаходиться в `AbstractDocument`.
> * **Чистота:** Кожен клас робить свою справу.
>
> Це класичний патерн "Інтерфейс + Абстрактний скелет".

#### **📌 Що перевіряють:**

*Архітектурне мислення. Здатність декомпозувати проблему та застосувати правильні інструменти ООП для створення гнучкого, розширюваного та підтримуваного рішення.*
