---
layout: default
title: "Анотації JUnit — основні інструменти"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 5
---

# Анотації JUnit — основні інструменти

* короткий опис: Анотації JUnit – це спеціальні маркери (`@Test`, `@BeforeEach`, `@AfterAll` тощо), що використовуються для позначення методів у тестовому класі з метою визначення їх ролі та керування життєвим циклом тестування.

* поняття: `@Test`, `@BeforeEach`, `@AfterEach`, `@BeforeAll`, `@AfterAll`, `@Disabled`, `@DisplayName`.

* приклади: `@BeforeAll void setup() { ... }` (один раз на клас), `@BeforeEach void init() { ... }` (перед кожним тестом).

* ключові моменти: Керування життєвим циклом тестів (підготовка, виконання, очищення), порядок виконання анотацій. Допомагають уникнути дублювання коду.

* що важливо знати на співбесіді: Призначення кожної анотації, порядок виконання.

### **✅ Офіційне визначення:**

**Анотації в JUnit** — це спеціальні маркери, що використовуються для позначення методів у тестовому класі з метод визначення їх ролі (тест, підготовка, завершення, тощо) під час виконання тестів.

### **🧠 Простими словами:**

**JUnit анотації** — це як дорожні знаки для тестового фреймворку: вони вказують, де починати, що саме тестувати, коли прибрати за собою і т.д.

---

## **Основне**

|      Анотація      |                    Призначення                     |           Приклад застосування            |
|:------------------:|:--------------------------------------------------:|:-----------------------------------------:|
|    **`@Test`**     |               Позначає метод як тест               |   **`@Test void testAddition() {...}`**   |
| **`@BeforeEach`**  |     Метод виконується **перед кожним** тестом      |     Ініціалізація перед кожним тестом     |
|  **`@AfterEach`**  |     Метод виконується **після кожного** тесту      |        Очистка після кожного тесту        |
|  **`@BeforeAll`**  | Метод виконується **один раз перед усіма** тестами |         Статичний setup для класу         |
|  **`@AfterAll`**   |  Метод виконується **один раз після всіх** тестів  |             Закриття ресурсів             |
|  **`@Disabled`**   |                    Вимикає тест                    |   **`@Disabled("ще не реалізовано")`**    |
| **`@DisplayName`** |              Задає зручне ім’я тесту               | **`@DisplayName("Сума 2+2 дорівнює 4")`** |

---

## **Пояснення на пальцях (💡)**

Уяви, що твій тестовий клас — це пекарня.

* **`@BeforeAll`** — вмикаєш духовку один раз на початку робочого дня.
* **`@BeforeEach`** — чистиш форму перед кожною булочкою.
* **`@Test`** — випікаєш булочку.
* **`@AfterEach`** — миєш форму після кожної булочки.
* **`@AfterAll`** — вимикаєш духовку в кінці дня.

---

## **Корисні приклади (🧪)**

```java
import org.junit.jupiter.api.*;

@TestInstance(TestInstance.Lifecycle.PER_CLASS)
public class ExampleTest {

    @BeforeAll
    void globalSetup() {
        System.out.println("🔧 Перед усіма тестами");
    }

    @BeforeEach
    void init() {
        System.out.println("✨ Перед кожним тестом");
    }

    @Test
    void testOne() {
        System.out.println("✅ Тест 1");
    }

    @Test
    void testTwo() {
        System.out.println("✅ Тест 2");
    }

    @AfterEach
    void tearDown() {
        System.out.println("🧹 Після кожного тесту");
    }

    @AfterAll
    void globalCleanup() {
        System.out.println("🔥 Після всіх тестів");
    }
}
```

**Очікуваний порядок виконання:**

```java
🔧 Перед усіма тестами  
✨ Перед кожним тестом  
✅ Тест 1  
🧹 Після кожного тесту  
✨ Перед кожним тестом  
✅ Тест 2  
🧹 Після кожного тесту  
🔥 Після всіх тестів
```

---

## **Порівняння (⚖️)**

|     Анотація      |     Частота виклику     |              Статична?              | Тип підготовки |
|:-----------------:|:-----------------------:|:-----------------------------------:|:--------------:|
| **`@BeforeEach`** | Перед **кожним** тестом |                 Ні                  |    Локальна    |
| **`@AfterEach`**  | Після **кожного** тесту |                 Ні                  |    Локальна    |
| **`@BeforeAll`**  |  Один раз **на клас**   | Так (або через **`@TestInstance`**) |   Глобальна    |
|  **`@AfterAll`**  |  Один раз **на клас**   | Так (або через **`@TestInstance`**) |   Глобальна    |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"BABA T"** — порядок виконання:

* **B**eforeAll
* **A**fterAll
* **B**eforeEach
* **A**fterEach
* **T**est

Мнемоніка: "БАБа Тестить\!" — Баба краще за CI: знає порядок, тестить все, ще й посуд миє. 🧼

---

## **🟩 Підсумок:**

**`JUnit анотації`** — ключовий механізм управління життєвим циклом тестів. Вони дають можливість автоматизовано
підготувати тестове середовище, запускати тести, очищати ресурси та покращувати читаємість тестів. Порядок їх виконання
важливий для побудови передбачуваних і стабільних тестів. Знання цих анотацій — обов’язкова база для будь-якого
Java-розробника.

---

### **🔗 Додаткові джерела:**

* [**StackOverflow — порядок виконання JUnit анотацій**](https://stackoverflow.com/questions/22507383/order-of-execution-of-junit-annotations)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке анотації JUnit і яка їхня головна роль у тестуванні?

> #### **💬 Відповідь:**
>
> **Анотації JUnit** — це спеціальні метадані (маркери), які додаються до методів та класів у тестовому коді. Вони не змінюють логіку самого коду, але надають інструкції фреймворку JUnit, як саме виконувати тести.
>
> **Головна роль анотацій:**
>
> * **Визначення тестів:** Анотація `@Test` позначає метод як тестовий, який JUnit повинен виконати.
> * **Керування життєвим циклом:** Анотації, такі як `@BeforeEach`, `@AfterEach`, `@BeforeAll`, `@AfterAll`, дозволяють виконувати певний код для підготовки (setup) та очищення (teardown) тестового середовища до, після або навколо тестів.
> * **Конфігурація тестів:** Анотації, як-от `@Disabled` чи `@Tag`, дозволяють керувати виконанням тестів (наприклад, тимчасово вимкнути тест або згрупувати їх).
> * **Покращення читабельності:** Анотація `@DisplayName` дозволяє надати тестам людсько-зрозумілі імена, що покращує звітність.
>
> Простими словами, анотації перетворюють звичайний Java-клас на потужний тестовий набір, яким може керувати JUnit.

#### **📝 Аналогія:**

Анотації — це як дорожні знаки для фреймворку JUnit. `@Test` — це знак "головна дорога" (цей метод виконуємо). `@BeforeEach` — "обов'язкова зупинка перед перехрестям" (цей код виконати перед кожним тестом). `@Disabled` — "ремонтні роботи, об'їзд" (цей тест тимчасово не виконуємо).

#### **📌 Що перевіряють:**

*Базове розуміння призначення анотацій та їхньої ролі в автоматизації та структуруванні тестів.*

* * *

### **2 ❓ Питання:**

У чому різниця між анотаціями `@BeforeEach` та `@BeforeAll`?

> #### **💬 Відповідь:**
>
> Обидві анотації використовуються для підготовки тестового середовища (setup), але вони відрізняються **частотою та областю застосування**:
>
> * **`@BeforeEach` (Перед кожним):**
>   * **Частота:** Метод, позначений цією анотацією, виконується **ПЕРЕД КОЖНИМ** тестовим методом (`@Test`) у класі.
>   * **Призначення:** Ідеально підходить для створення "чистого" середовища для кожного тесту, щоб вони не впливали один на одного. Наприклад, ініціалізація нового об'єкта перед кожним тестом, скидання стану.
>   * **Метод:** має бути звичайним (не статичним).
>
> * **`@BeforeAll` (Перед усіма):**
>   * **Частота:** Метод виконується **ЛИШЕ ОДИН РАЗ** перед виконанням **УСІХ** тестів у класі.
>   * **Призначення:** Використовується для ініціалізації "дорогих" ресурсів, які можна розділити між усіма тестами в класі. Наприклад, встановлення з'єднання з базою даних, запуск Docker-контейнера.
>   * **Метод:** за замовчуванням має бути **статичним (`static`)**.

#### **📝 Приклад:**

Якщо у вас 2 тести в класі: `@BeforeEach`-метод виконається 2 рази, а `@BeforeAll`-метод — 1 раз.

#### **📌 Що перевіряють:**

*Чітке розуміння життєвого циклу тестів JUnit. Це одне з найпоширеніших питань для перевірки базових знань фреймворку.*

* * *

### **3 ❓ Питання:**

Поясніть повний порядок виконання анотацій життєвого циклу JUnit 5 для класу з двома тестами.

> #### **💬 Відповідь:**
>
> JUnit 5 дотримується чіткого і передбачуваного порядку виконання. Для класу з двома тестовими методами (`testA`, `testB`) порядок буде таким:
>
> 1. **`@BeforeAll`** — виконується один раз на початку.
> 2. **`@BeforeEach`** — виконується перед першим тестом.
> 3. **`@Test` (testA)** — виконується перший тест.
> 4. **`@AfterEach`** — виконується після першого тесту.
> 5. **`@BeforeEach`** — виконується перед другим тестом (знову).
> 6. **`@Test` (testB)** — виконується другий тест.
> 7. **`@AfterEach`** — виконується після другого тесту (знову).
> 8. **`@AfterAll`** — виконується один раз у самому кінці.

#### **📝 Аналогія (Пекарня):**

1. **`@BeforeAll`**: Увімкнули піч (один раз на день).
2. **`@BeforeEach`**: Помили форму для хліба.
3. **`@Test`**: Спекли перший хліб.
4. **`@AfterEach`**: Знову помили форму.
5. **`@BeforeEach`**: Знову помили форму.
6. **`@Test`**: Спекли другий хліб.
7. **`@AfterEach`**: Знову помили форму.
8. **`@AfterAll`**: Вимкнули піч (в кінці дня).

#### **📌 Що перевіряють:**

*Практичне розуміння життєвого циклу тестів. Кандидата можуть попросити написати на дошці очікуваний вивід коду з цими анотаціями.*

* * *

### **4 ❓ Питання:**

Для чого використовується анотація `@Disabled`?

> #### **💬 Відповідь:**
>
> Анотація **`@Disabled`** використовується для того, щоб **тимчасово вимкнути** виконання певного тестового методу або цілого тестового класу.
>
> **Коли це корисно:**
>
> * Коли функціонал, який тестується, тимчасово зламаний або ще не реалізований, і ви не хочете, щоб падаючі тести "засмічували" звіти та блокували CI/CD пайплайн.
> * Коли тест є нестабільним ("flaky") і вимагає часу на виправлення, але ви не хочете його видаляти.
> * Коли тест застарів, але ви хочете залишити його як нагадування про необхідність оновлення.
>
> При запуску JUnit **пропустить** тести, позначені `@Disabled`, і відобразить їх у звіті як "пропущені" (skipped), а не як "провалені".
>
> Анотація може приймати опціональний параметр-рядок для пояснення причини вимкнення, що є хорошою практикою.

#### **📝 Приклад:**

```java
@Disabled("Цей тест буде виправлено в задачі JIRA-123")
@Test
void someBrokenTest() {
    // ...
}
```

#### **📌 Що перевіряють:**

*Знання практичних інструментів для управління тестовим набором. Це показує, що кандидат має досвід роботи з реальними проектами, де тести іноді потрібно тимчасово вимикати.*

* * *

### **5 ❓ Питання:**

Як надати тесту більш інформативну та читабельну назву?

> #### **💬 Відповідь:**
>
> Для цього в JUnit 5 використовується анотація **`@DisplayName`**. Вона дозволяє замінити технічну назву методу (наприклад, `testLoginWithValidCredentials`) на людсько-зрозумілий рядок, який буде відображатися в IDE та звітах про тестування.
>
> **Переваги:**
>
> * **Читабельність:** Звіти стають набагато легшими для розуміння нетехнічними спеціалістами (менеджерами, аналітиками).
> * **Виразність:** Дозволяє описувати поведінку тесту природною мовою, використовуючи пробіли, розділові знаки та навіть емодзі.
> * **Документація:** Назва тесту стає частиною документації, яка пояснює, що саме перевіряється.

#### **📝 Приклад:**

```java
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class LoginTest {

    @Test
    @DisplayName("✅ Користувач може успішно увійти з правильним логіном та паролем")
    void testSuccessfulLogin() {
        // ...
    }

    @Test
    @DisplayName("❌ Система повинна показати помилку при введенні неправильного пароля")
    void testLoginWithInvalidPassword() {
        // ...
    }
}
```

#### **📌 Що перевіряють:**

*Вміння писати "чисті" тести, які легко читати та підтримувати. Це частина інженерної культури.*

* * *

### **6 ❓ Питання:**

Чому методи, позначені `@BeforeAll` та `@AfterAll`, за замовчуванням мають бути `static`?

> #### **💬 Відповідь:**
>
> Методи `@BeforeAll` та `@AfterAll` за замовчуванням мають бути `static`, тому що вони виконуються **один раз на рівні класу, а не екземпляра**.
>
> Життєвий цикл JUnit 5 за замовчуванням такий: **новий екземпляр тестового класу створюється для кожного тестового методу (`@Test`)**.
>
> Отже, коли JUnit готується виконати перший тест, він ще не має екземпляра класу, щоб викликати на ньому нестатичний метод `@BeforeAll`. Єдиний спосіб викликати метод без екземпляра класу — це зробити його **статичним**. Те ж саме стосується й `@AfterAll` — після виконання останнього тесту відповідний екземпляр класу вже знищено, тому для виклику методу знову потрібен `static`.

#### **📝 Коментар:**

Цю поведінку можна змінити, якщо анотувати клас `@TestInstance(Lifecycle.PER_CLASS)`. Тоді буде створено лише один екземпляр класу на всі тести, і методи `@BeforeAll`/`@AfterAll` можуть бути нестатичними.

#### **📌 Що перевіряють:**

*Глибоке розуміння життєвого циклу тестування в JUnit 5. Це питання дозволяє перевірити, чи кандидат знає не лише "що", а й "чому".*

* * *

### **7 ❓ Питання:**

У якому порядку виконуються методи `@BeforeEach` та `@AfterEach`, якщо тестовий клас успадковується від іншого класу, де також є ці анотації? ⭐️

> #### **💬 Відповідь:**
>
> JUnit дотримується чіткої ієрархічної логіки при виконанні методів життєвого циклу в успадкованих класах.
>
> * **`@BeforeEach`**: Виконання йде **"зверху вниз"** по ієрархії. Спочатку виконується `@BeforeEach` батьківського класу, а потім — `@BeforeEach` дочірнього класу.
>
> * **`@AfterEach`**: Виконання йде у зворотному порядку — **"знизу вгору"**. Спочатку виконується `@AfterEach` дочірнього класу, а потім — `@AfterEach` батьківського.
>
> Цей порядок гарантує, що середовище, підготовлене батьківським класом, буде доступне для дочірнього, а очищення відбудеться у зворотному порядку до створення.

#### **📝 Приклад:**

```java
class ParentTest {
    @BeforeEach void setupParent() { /* ... */ } // Виконається 1-м
    @AfterEach void tearDownParent() { /* ... */ } // Виконається 4-м
}
class ChildTest extends ParentTest {
    @BeforeEach void setupChild() { /* ... */ }  // Виконається 2-м
    @AfterEach void tearDownChild() { /* ... */ }   // Виконається 3-м
    @Test void myTest() { /* ... */ }
}
```

#### **📌 Що перевіряють:**

*Знання нюансів роботи JUnit з успадкуванням. Це може бути важливо в проектах зі складними ієрархіями тестових класів.*

* * *

### **8 ❓ Питання:**

Які анотації JUnit 4 відповідають анотаціям `@BeforeEach` та `@BeforeAll` з JUnit 5? ⭐️

> #### **💬 Відповідь:**
>
> При переході з JUnit 4 на JUnit 5 анотації життєвого циклу були перейменовані для більшої ясності.
>
> | **JUnit 5** | **JUnit 4** | **Призначення** |
> | :---: | :---: | :--- |
> | **`@BeforeEach`** | `@Before` | Виконується перед кожним тестом |
> | **`@AfterEach`** | `@After` | Виконується після кожного тесту |
> | **`@BeforeAll`** | `@BeforeClass` | Виконується один раз перед усіма тестами |
> | **`@AfterAll`** | `@AfterClass` | Виконується один раз після всіх тестів |
> | **`@Disabled`** | `@Ignore` | Вимикає тест |
>
> Головна мета перейменування — зробити назви більш явними та інтуїтивно зрозумілими (`Each` чітко вказує на виконання для кожного тесту, `All` — для всіх).

#### **📌 Що перевіряють:**

*Знання обох версій JUnit. Це важливо для розробників, яким доводиться працювати як з новими, так і з легасі-проектами.*

* * *

### **9 ❓ Питання:**

Що таке `@Nested` тести в JUnit 5 і для чого вони потрібні? ⭐️

> #### **💬 Відповідь:**
>
> Анотація **`@Nested`** дозволяє створювати **вкладені тестові класи** всередині основного тестового класу. Це дуже потужний інструмент для **групування та структурування тестів**, пов'язаних з певним станом або сценарієм.
>
> **Основні переваги:**
>
> 1. **Групування за контекстом:** Дозволяє згрупувати тести, що стосуються певної функціональності. Наприклад, в тесті для `Stack` можна створити вкладений клас "when new" (коли стек новий), "when not empty" (коли не порожній).
> 2. **Спільний setup/teardown:** Вкладений клас може мати власні методи `@BeforeEach`/`@AfterEach`, що дозволяє уникнути дублювання коду підготовки для групи тестів.
> 3. **Читабельність:** Назви вкладених класів у поєднанні з `@DisplayName` можуть створювати дуже читабельні речення, що описують поведінку.

#### **📝 Приклад:**

```java
@DisplayName("A stack")
class StackTest {
    // ...
    @Nested
    @DisplayName("when new")
    class WhenNew {
        @Test
        @DisplayName("is empty")
        void isEmpty() { /* ... */ }
    }

    @Nested
    @DisplayName("after pushing an element")
    class AfterPushing {
        // ... свій @BeforeEach ...
        @Test
        @DisplayName("is not empty")
        void isNotEmpty() { /* ... */ }
    }
}
```

#### **📌 Що перевіряють:**

*Знання розширених можливостей JUnit 5. Використання `@Nested` вказує на вміння писати добре структуровані та читабельні тести в стилі BDD (Behavior-Driven Development).*

* * *

### **10 ❓ Питання:**

Як анотація `@TestInstance` змінює життєвий цикл тестового класу? ⭐️

> #### **💬 Відповідь:**
>
> Анотація `@TestInstance` дозволяє керувати життєвим циклом екземплярів тестових класів. Вона приймає один з двох можливих режимів:
>
> 1. **`TestInstance.Lifecycle.PER_METHOD` (режим за замовчуванням):**
>     * **Поведінка:** JUnit створює **новий екземпляр** тестового класу **перед кожним тестовим методом**.
>     * **Перевага:** Гарантує повну ізоляцію тестів. Стан, змінений в одному тесті, не вплине на інший.
>     * **Наслідок:** `@BeforeAll`/`@AfterAll` методи мають бути статичними.
>
> 2. **`TestInstance.Lifecycle.PER_CLASS`:**
>     * **Поведінка:** JUnit створює **лише один екземпляр** тестового класу для **всіх тестів** у ньому.
>     * **Перевага:** Дозволяє робити методи `@BeforeAll`/`@AfterAll` **нестатичними**. Також дозволяє зберігати стан у полях класу між тестами (хоча це зазвичай є поганою практикою).
>     * **Ризик:** Порушує ізоляцію тестів. Якщо один тест змінює стан екземпляра, це може вплинути на результат наступних тестів.
>
> Анотація `@TestInstance(Lifecycle.PER_CLASS)` ставиться на рівні класу.

#### **📌 Що перевіряють:**

*Глибокі знання конфігурації JUnit. Кандидат повинен розуміти як стандартну поведінку, так і способи її зміни та пов'язані з цим ризики.*

* * *

### **11 ❓ Питання:**

Чи можна використовувати анотації JUnit 5 разом з анотаціями JUnit 4 в одному проекті? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Це можливо завдяки модульній архітектурі JUnit 5, а саме — модулю **JUnit Vintage Engine**.
>
> * **JUnit Vintage Engine** — це спеціальний рушій тестування, який є частиною платформи JUnit 5. Його єдина мета — **виявляти та запускати тести, написані на JUnit 3 та JUnit 4**.
> * Це дозволяє проектам **поступово мігрувати** зі старої версії JUnit на нову. Ви можете писати нові тести, використовуючи сучасний API JUnit 5, і в той же час залишати старі тести на JUnit 4 без змін.
> * При запуску тестів JUnit Platform автоматично задіє і Jupiter Engine (для тестів JUnit 5), і Vintage Engine (для тестів JUnit 4), а потім об'єднає результати в єдиний звіт.
>
> **Але в одному класі** змішувати анотації JUnit 4 (`@Before`) та JUnit 5 (`@BeforeEach`) не можна, оскільки це призведе до непередбачуваної поведінки.

#### **📌 Що перевіряють:**

*Розуміння архітектури JUnit 5 та її переваг, таких як зворотна сумісність. Це важливо для роботи з великими легасі-проектами.*

* * *

### **12 ❓ Питання:**

Чи гарантує JUnit певний порядок виконання `@Test` методів всередині класу? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, за замовчуванням JUnit не гарантує жодного конкретного порядку** виконання тестових методів. Порядок може бути непередбачуваним і залежати від реалізації JVM, що може призводити до тестів, які проходять на одній машині і падають на іншій.
>
> **Найкраща практика:** Тести повинні бути **незалежними** один від одного. Результат одного тесту не повинен залежати від того, чи був виконаний інший тест до або після нього.
>
> **Як можна вплинути на порядок (якщо це дійсно необхідно):**
>
> JUnit 5 надає анотацію **`@TestMethodOrder`** для керування порядком:
>
> * **`@TestMethodOrder(MethodOrderer.OrderAnnotation.class)`**: Дозволяє задати явний порядок за допомогою анотації `@Order(число)`.
>
>     ```java
>     @Test @Order(1) void firstTest() { ... }
>     @Test @Order(2) void secondTest() { ... }
>     ```
>
> * **`@TestMethodOrder(MethodOrderer.DisplayName.class)`**: Сортує за назвою з `@DisplayName`.
> * **`@TestMethodOrder(MethodOrderer.MethodName.class)`**: Сортує за назвою методу.
>
> Однак, явне задання порядку слід використовувати дуже обережно, оскільки це може вказувати на поганий дизайн тестів (залежність між ними).

#### **📌 Що перевіряють:**

*Знання важливого принципу незалежності тестів та способів керування порядком, коли це є вимушеною необхідністю.*

* * *

### **13 ❓ Питання:**

Поясніть, що таке `@ExtendWith` і чому цей механізм кращий за `@RunWith` з JUnit 4. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`@ExtendWith`** — це основна анотація в JUnit 5 для підключення **розширень (Extensions)**. Розширення дозволяють додавати власну логіку до життєвого циклу тестів.
>
> **Чим цей механізм кращий за `@RunWith` з JUnit 4:**
>
> 1. **Композиція замість наслідування:**
>     * У JUnit 4 анотація `@RunWith(SomeRunner.class)` дозволяла використовувати **лише один Runner** на тестовий клас. Якщо вам потрібно було скомбінувати функціональність від `MockitoJUnitRunner` та `SpringJUnit4ClassRunner`, це було неможливо.
>     * У JUnit 5 анотація `@ExtendWith` дозволяє реєструвати **скільки завгодно розширень** для одного класу. Вони будуть виконані по черзі.
>
>     ```java
>     @ExtendWith({MockitoExtension.class, SpringExtension.class})
>     class MyTest { ... }
>     ```
>
> 2. **Гнучкість та чистота:** Extension Model надає значно більше точок для втручання в життєвий цикл (до/після тесту, обробка винятків, розв'язання параметрів) через чітко визначені інтерфейси-коллбеки.
>
> **Висновок:** `@ExtendWith` пропонує значно більш гнучку, потужну та композитну модель розширення, ніж монолітні та обмежуючі `Runners` у JUnit 4.

#### **📌 Що перевіряють:**

*Експертне розуміння архітектури JUnit 5. Кандидат повинен розуміти переваги нової моделі розширень та проблеми, які вона вирішує.*

* * *

### **14 ❓ Питання:**

Як анотації `@Tag` та `@Disabled` впливають на CI/CD пайплайн? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидві анотації є інструментами для керування тим, які тести виконуються, що є критично важливим для побудови ефективних CI/CD пайплайнів.
>
> **`@Tag` (Теги):**
>
> * **Вплив на CI/CD:** Теги дозволяють створювати **різні набори тестів (test suites)** для різних етапів пайплайну.
>   * **На етапі Pull Request / Commit:** Можна запускати лише швидкі тести, позначені тегом `"unit"` або `"fast"`. Це забезпечує швидкий зворотний зв'язок розробнику.
>   * **На етапі нічної збірки (Nightly Build):** Можна запускати повний набір тестів, включаючи повільні, позначені тегом `"integration"` або `"e2e"`.
>   * **На етапі релізу:** Можна запускати окремий набір тестів з тегом `"smoke"`.
>
> **`@Disabled`:**
>
> * **Вплив на CI/CD:** Дозволяє тимчасово виключити з пайплайну нестабільний ("flaky") або падаючий тест **без необхідності його видаляти чи коментувати**.
>   * Це запобігає ситуації, коли один нестабільний тест "фарбує" весь пайплайн у червоний колір і блокує роботу команди.
>   * При цьому, тест залишається видимим у коді та звітах, що нагадує про необхідність його виправити (технічний борг).
>
> Разом ці анотації дають команді гнучкість у керуванні тестовим набором, балансуючи між швидкістю фідбеку та повнотою перевірок.

#### **📌 Що перевіряють:**

*Розуміння того, як інструменти тестування інтегруються в загальний процес розробки та доставки ПЗ. Це демонструє досвід роботи в сучасних DevOps-орієнтованих командах.*

* * *

### **15 ❓ Питання:**

Чи можна створити власну кастомну анотацію, яка буде комбінувати кілька анотацій JUnit? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** JUnit 5 підтримує створення **композитних анотацій (composed annotations)**. Це дозволяє створювати власні анотації, які поєднують в собі одну або декілька стандартних анотацій JUnit, роблячи код тестів більш стислим та виразним.
>
> Для цього потрібно створити власну анотацію і позначити її стандартними анотаціями JUnit.
>
> **Сценарій:** У нас є багато швидких інтеграційних тестів, які завжди вимагають `@Test`, `@Tag("integration")` та `@Tag("fast")`.
>
> **Рішення:** Створимо одну кастомну анотацію `@FastIntegrationTest`.

#### **📝 Приклад:**

```java
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * Кастомна композитна анотація, що поєднує @Test та два теги.
 */
@Target({ElementType.ANNOTATION_TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
@Test // Позначає метод як тестовий
@Tag("integration") // Додає тег "integration"
@Tag("fast") // Додає тег "fast"
public @interface FastIntegrationTest {
}
```

**Тепер використання в коді стає набагато чистішим:**

```java
class MyServiceIntegrationTest {
    
    // Замість трьох анотацій, використовуємо одну
    @FastIntegrationTest
    void testUserCreationFlow() {
        // ...
    }
}
```

Такий підхід дозволяє створювати власний предметно-орієнтований словник для тестів, що покращує їх читабельність та підтримку.

#### **📌 Що перевіряють:**

*Експертне знання JUnit 5. Здатність створювати композитні анотації свідчить про глибоке розуміння фреймворку та прагнення писати чистий та підтримуваний код (принцип DRY - Don't Repeat Yourself).*

* * *
