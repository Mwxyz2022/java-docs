---
layout: default
title: "Організація тестів та Test Suites"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 12
---

# Організація тестів та Test Suites

* короткий опис: Організація тестів – це групування тестів за функціональністю або компонентами для зручного запуску, аналізу та інтеграції в CI/CD пайплайни. `Test Suite` – це набір тестів, об'єднаних для спільного запуску.

* поняття: `Test Suite`, групування тестів, `@Tag` (для категоризації), Maven/Gradle (для запуску), CI/CD (для автоматизації).

* приклади: Структура папок (`src/test/java/unit/`, `integration/`), `@SelectPackages`, `@IncludeTags`, запуск `mvn test -Dgroups=unit`.

* ключові моменти: Зручність запуску груп тестів, інтеграція з системами збірки та CI/CD.

* що важливо знати на співбесіді: Як організовувати тести у проєкті, як запускати групи тестів.

### **✅ Офіційне визначення:**

**Test Suite** — це набір тестів, об’єднаних для спільного запуску, зазвичай згідно з функціональністю, компонентом або сценарієм.

### **🧠 Простими словами:**

Це як коробки з тестами, які зручно запускати разом — наприклад, всі тести на логін, або всі інтеграційні

---

## **Основне**

🗂 **Класифікація тестів**:

* **Unit tests** — тести на рівні окремих методів / класів
* **Integration tests** — перевірка взаємодії між модулями
* **End-to-End (E2E)** — перевірка роботи системи як цілого
* **Smoke tests** — базова перевірка, що система взагалі стартує
* **Regression tests** — перевірка, що старий код не зламався

🧩 **Test Suites за категоріями**:

* **`@Tag("unit"), @Tag("integration")`**
* Кастомні теги для групування (**`@Tag("db"), @Tag("api")`**)

🧰 **Інструменти**:

* **JUnit 5**: **`@SelectPackages, @IncludeTags, @ExcludeTags`**
* **Maven Surefire plugin**
* **Gradle Test Task**
* **CI/CD: Jenkins, GitHub Actions, GitLab CI**

---

## **Пояснення на пальцях (💡)**

* Уяви тести — як солдати в армії. Не завжди треба кликати весь батальйон. Іноді достатньо лише снайперів (**`unit`** `tests`), іноді — спецназ (**`integration`**).
* Test Suite — це твоя команда для конкретної місії.
* IDE — швидкий спосіб запуску. Але в CI важлива автоматизація через CLI або build tools.

---

## **Корисні приклади (🧪)**

📁 **Структура директорій** (за **`Maven`** **`convention`**):

```java
src/
├── main/java/...
└── test/java/
    ├── unit/
    ├── integration/
    └── e2e/
```

📦 **JUnit 5 — Suite запуску групи тестів:**

```java
@RunWith(JUnitPlatform.class)
@SelectPackages("com.myapp.tests.unit")
@IncludeTags("unit")
public class UnitTestSuite {}
```

🏃‍♂️ **Запуск із командного рядка (Maven):**

```java
mvn test -Dgroups=unit
```

🧩 **Gradle:**

```java
test {
    useJUnitPlatform {
        includeTags 'unit'
    }
}
```

---

## **Порівняння (⚖️)**

|  Платформа  |      Як групується       | Підтримка Suite? |       Як запускати?       |
|:-----------:|:------------------------:|:----------------:|:-------------------------:|
| **JUnit 5** | **`@Tag`**, **`@Suite`** |       ✅ Є        |  IDE, CLI, Maven, Gradle  |
|  **Maven**  |  **`Surefire`** config   |       ✅ Є        | **`mvn`** `test`, профілі |
| **Gradle**  |     **`Test`** task      |       ✅ Є        |    **`gradle`** `test`    |
|  **CI/CD**  |        Пайплайни         |       ✅ Є        |  Автоматично після пушу   |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"S.C.O.R.E."** — що має бути в організації тестів:

* **S** – Structure (структура папок)
* **C** – CI/CD інтеграція
* **O** – Organization by type (unit, integration)
* **R** – Run from anywhere (IDE, CLI, CI)
* **E** – Execution via suites

---

## **🟩 Підсумок:**

* Розділяй тести за типами: unit, integration, e2e.
* Групуй їх через **`@Tag`**, папки або suite-класи.
* Використовуй Maven або Gradle для фільтрації.
* Інтегруй запуск тестів у CI/CD пайплайни.
* На інтерв’ю поясни не лише як, а **навіщо** ти так організував — це ключ\!

---

### **🔗 Додаткові джерела:**

* [**Maven Surefire Plugin Docs**](https://maven.apache.org/surefire/maven-surefire-plugin/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Чому взагалі потрібно організовувати тести, а не просто писати їх в одному місці?

> #### **💬 Відповідь:**
>
> Організація тестів є критично важливою для підтримки, масштабування та ефективного використання тестового набору в будь-якому проекті, що є складнішим за "Hello, World".
>
> **Основні причини:**
>
> 1. **Масштабованість та підтримка:** Коли кількість тестів сягає сотень або тисяч, знайти потрібний тест або зрозуміти загальну картину без структури стає неможливо. Організація (напр., по папках `unit`, `integration`) робить кодову базу чистою та зрозумілою.
>
> 2. **Ефективність CI/CD:** Різні типи тестів мають різну швидкість виконання. Швидкі **unit-тести** можна запускати при кожному коміті, тоді як повільні **E2E** або **інтеграційні тести** — лише на нічних збірках. Без організації ви будете змушені запускати всі тести разом, що значно сповільнює процес зворотного зв'язку.
>
> 3. **Фокусований запуск:** Іноді потрібно запустити лише певну групу тестів (напр., всі тести, що стосуються модуля оплати, або лише "smoke-тести"). Правильна організація дозволяє це зробити легко.
>
> 4. **Ясність та комунікація:** Структура тестів слугує формою документації. Нова людина в команді, подивившись на організацію тестів, може швидше зрозуміти архітектуру та функціонал проекту.

#### **📝 Аналогія:**

Уявіть, що тести — це книги в бібліотеці. Без організації (по жанрах, авторах) це просто купа книг на підлозі. Знайти потрібну книгу буде майже неможливо. Організація тестів — це як створення каталогів та секцій, що дозволяє легко знайти будь-яку "книгу" і зрозуміти, що взагалі є в бібліотеці.

#### **📌 Що перевіряють:**

*Розуміння того, що управління тестами — це інженерна задача, а не хаотичний процес. Кандидат повинен думати про масштабованість та ефективність у контексті тестування.*

* * *

### **2 ❓ Питання:**

Що таке Test Suite і яка його основна мета?

> #### **💬 Відповідь:**
>
> **Test Suite (Тестовий набір)** — це колекція тестових класів або методів, які об'єднані для спільного запуску. Це механізм для групування тестів за певною ознакою.
>
> **Основна мета Test Suite:**
>
> * **Логічне групування:** Дозволяє об'єднувати тести, пов'язані з певною функціональністю (напр., `AuthenticationTestSuite`), типом тестування (`RegressionTestSuite`, `SmokeTestSuite`) або будь-якою іншою логічною категорією.
> * **Зручність запуску:** Замість того, щоб запускати кожен тест окремо, ви можете запустити весь набір однією командою. Це особливо важливо для CI/CD, де потрібно виконувати певні набори тестів на різних етапах.
>
> У **JUnit 4** Test Suites створювалися за допомогою анотацій `@RunWith(Suite.class)` та `@Suite.SuiteClasses`.
> У **JUnit 5** цей підхід було значно покращено за допомогою анотації `@Suite` та анотацій для вибірки тестів (`@SelectPackages`, `@IncludeTags` і т.д.).

#### **📝 Приклад:**

Можна створити `SmokeTestSuite`, який включатиме лише кілька найкритичніших тестів з різних модулів, щоб швидко перевірити працездатність системи після розгортання.

#### **📌 Що перевіряють:**

*Знання базової термінології та розуміння концепції групування тестів для подальшого управління ними.*

* * *

### **3 ❓ Питання:**

Яким є стандартний підхід до організації тестів у структурі папок проекту Maven/Gradle?

> #### **💬 Відповідь:**
>
> Стандартна конвенція, якої дотримуються Maven та Gradle, передбачає розміщення всіх тестів у директорії `src/test/java`. Для подальшої організації тести часто розділяють за їхнім типом.
>
> **Типова структура папок:**
>
> ```
> src/
> ├── main/
> │   └── java/
> │       └── com/example/myapp/   # Виробничий код
> └── test/
>     └── java/
>         └── com/example/myapp/   # Тести в тому ж пакеті
>             ├── unit/            # Папка для unit-тестів
>             │   └── UserServiceTest.java
>             └── integration/     # Папка для інтеграційних тестів
>                 └── UserIntegrationTest.java
> ```
>
> **Чому така структура є корисною:**
>
> * **Ізоляція:** Код тестів повністю відокремлений від виробничого коду.
> * **Дзеркальна структура пакетів:** Тестовий клас для `com.example.myapp.MyClass` знаходиться в `com.example.myapp.MyClassTest`, що полегшує навігацію.
> * **Розділення за типом:** Створення підпапок `unit` та `integration` дозволяє легко налаштувати системи збирання на запуск різних типів тестів. Наприклад, `mvn test` може запускати тільки тести з папки `unit`.

#### **📌 Що перевіряють:**

*Знання стандартних конвенцій побудови проектів. Це базова навичка для роботи в команді та використання інструментів збирання.*

* * *

### **4 ❓ Питання:**

Що таке теги (`@Tag`) в JUnit 5 і як вони допомагають організовувати тести?

> #### **💬 Відповідь:**
>
> **Теги (Tags)** — це механізм в JUnit 5, який дозволяє "маркувати" тестові методи або класи за допомогою довільних рядків. Це дуже гнучкий спосіб категоризації тестів.
>
> **Як вони допомагають:**
>
> 1. **Категоризація:** Ви можете позначити тести тегами, що описують їхній тип (`@Tag("unit")`, `@Tag("integration")`), функціональність (`@Tag("login")`, `@Tag("payment")`), або будь-яку іншу характеристику (`@Tag("slow")`, `@Tag("flaky")`).
> 2. **Гнучкий запуск:** За допомогою тегів ви можете дуже гнучко керувати, які тести запускати. Наприклад, "запустити всі тести з тегом `regression`, але виключити ті, що мають тег `slow`".
> 3. **Заміна Test Suites:** У багатьох випадках теги є більш гнучкою альтернативою старим Test Suites, оскільки один тест може мати кілька тегів, тобто належати до кількох груп одночасно.

#### **📝 Приклад:**

```java
class PaymentServiceTest {
    @Test
    @Tag("fast")
    @Tag("unit")
    void testPriceCalculation() { /*...*/ }

    @Test
    @Tag("slow")
    @Tag("integration")
    @Tag("database")
    void testPaymentGatewayIntegration() { /*...*/ }
}
```

#### **📌 Що перевіряють:**

*Знання сучасних інструментів JUnit 5 для організації тестів. Теги є ключовою фічею для гнучкого управління тестовими наборами.*

* * *

### **5 ❓ Питання:**

Як запустити лише певну групу тестів (наприклад, з тегом "unit") за допомогою Maven?

> #### **💬 Відповідь:**
>
> У Maven за запуск тестів відповідає плагін **Maven Surefire Plugin**. Він дозволяє фільтрувати тести, які потрібно виконати, на основі тегів (груп).
>
> Це можна зробити двома способами:
>
> **1. Через командний рядок (найпоширеніший спосіб на CI):**
> Ви можете передати властивість `groups` або `excludedGroups` під час запуску.
>
> ```bash
> # Запустити лише тести з тегом "unit"
> mvn test -Dgroups="unit"
> 
> # Запустити тести з тегом "regression", але виключити "slow"
> mvn test -Dgroups="regression" -DexcludedGroups="slow"
>    ```
>
> **2. Через конфігурацію в `pom.xml`:**
> Можна налаштувати плагін, щоб він за замовчуванням запускав або виключав певні групи.
>
> ```xml
> <plugin>
>     <artifactId>maven-surefire-plugin</artifactId>
>     <version>3.0.0-M5</version>
>     <configuration>
>         <groups>unit | smoke</groups>
>         <excludedGroups>integration</excludedGroups>
>     </configuration>
> </plugin>
>    ```

#### **📌 Що перевіряють:**

*Практичні навички роботи з інструментами збирання. Розробник повинен знати, як керувати виконанням тестів не тільки в IDE, а й у консолі та CI/CD.*

* * *

### **6 ❓ Питання:**

Порівняйте два підходи до організації тестів: за допомогою структури папок та за допомогою тегів (`@Tag`).

> #### **💬 Відповідь:**
>
> Обидва підходи служать для організації тестів, але мають різний рівень гнучкості.
>
> **Організація за папками (напр., `unit/`, `integration/`):**
>
> * **Переваги:**
>   * Дуже просто та візуально зрозуміло. Відразу видно, де який тип тесту знаходиться.
>   * Легко налаштовується в системах збирання для запуску (напр., "запустити все з папки `**/unit/*Test.java`").
> * **Недоліки:**
>   * **Негнучко.** Один тестовий клас може належати лише до однієї папки. Важко реалізувати більш складні групування (напр., "smoke-тести", які включають кілька unit та інтеграційних тестів).
>
> **Організація за тегами (`@Tag`):**
>
> * **Переваги:**
>   * **Дуже гнучко.** Один тест може мати кілька тегів, тобто належати до багатьох логічних груп одночасно (`@Tag("unit") @Tag("fast") @Tag("login")`).
>   * Дозволяє створювати складні правила для запуску (напр., запустити тести з тегом ("A" або "B"), але не "C").
> * **Недоліки:**
>   * Менш наочно. Щоб зрозуміти, до якої групи належить тест, потрібно дивитися на його анотації.
>
> **Найкращий підхід:**
> Зазвичай використовують **гібридний підхід**: базове розділення на `unit` та `integration` по папках, а всередині них — більш детальна категоризація за допомогою тегів.

#### **📌 Що перевіряють:**

*Здатність аналізувати переваги та недоліки різних підходів та обирати оптимальну комбінацію для проекту.*

* * *

### **7 ❓ Питання:**

Що таке Test Suite в JUnit 5 і як його створити?

> #### **💬 Відповідь:**
>
> У JUnit 5 **Test Suite** — це спеціальний клас, який служить "пусковим механізмом" для виконання набору обраних тестів. На відміну від JUnit 4, де потрібно було перераховувати класи, JUnit 5 пропонує значно більш гнучкий, декларативний підхід.
>
> Для створення Test Suite використовуються такі анотації:
>
> * **`@Suite`**: Основна анотація, що позначає клас як Test Suite.
> * **Анотації вибірки (Selectors):**
>   * `@SelectPackages("com.example.tests")`: Включити всі тести з вказаних пакетів.
>   * `@SelectClasses({LoginTest.class, RegistrationTest.class})`: Включити конкретні класи.
> * **Анотації фільтрації (Filters):**
>   * `@IncludeTags("smoke")`: Включити лише тести з вказаними тегами.
>   * `@ExcludeTags("slow")`: Виключити тести з вказаними тегами.
>
> Це дозволяє динамічно формувати набір тестів без необхідності вручну редагувати клас-сьют.

#### **📝 Приклад:**

```java
import org.junit.platform.suite.api.*;

// Цей сьют запустить всі тести в пакеті "com.example.regression",
// які мають тег "smoke", але не мають тегу "wip" (work in progress).

@Suite
@SelectPackages("com.example.regression")
@IncludeTags("smoke")
@ExcludeTags("wip")
public class SmokeTestSuite {
    // Тіло класу може бути порожнім
}
```

#### **📌 Що перевіряють:**

*Знання сучасного API JUnit 5 для створення та керування тестовими наборами.*

* * *

### **8 ❓ Питання:**

Як організація тестів допомагає впровадити стратегію "Піраміди тестування"? ⭐️

> #### **💬 Відповідь:**
>
> Правильна організація тестів є **необхідною умовою** для реалізації Піраміди тестування. Піраміда передбачає, що у вас є велика кількість швидких unit-тестів, менша кількість інтеграційних, і зовсім мало E2E тестів.
>
> **Як організація допомагає:**
>
> 1. **Класифікація:** Щоб слідувати піраміді, ви повинні спочатку класифікувати кожен тест: чи є він unit, інтеграційним, чи E2E. Це робиться за допомогою **структури папок** (`/unit`, `/integration`) та/або **тегів** (`@Tag("unit")`, `@Tag("integration")`).
>
> 2. **Візуальний контроль:** Правильна організація дозволяє візуально та за допомогою інструментів оцінити, чи відповідає ваш тестовий набір формі піраміди. Ви можете легко порахувати кількість тестів кожного типу.
>
> 3. **Вибірковий запуск в CI/CD:** Ви можете налаштувати CI-пайплайн так, щоб він відображав піраміду:
>     * **На кожен коміт:** запускаються лише `unit`-тести (швидкий фідбек).
>     * **Перед мержем в main:** запускаються `unit` та `integration` тести.
>     * **На нічній збірці:** запускаються всі тести, включаючи повільні `E2E`.
>
> Без чіткої організації всі тести змішані в одну купу, і реалізувати таку диференційовану стратегію запуску стає неможливо.

#### **📌 Що перевіряють:**

*Здатність пов'язати дві ключові концепції тестування — організацію та піраміду — і пояснити, як перша є інструментом для реалізації другої.*

* * *

### **9 ❓ Питання:**

Яким чином консистентне іменування тестів пов'язане з їхньою організацією? ⭐️

> #### **💬 Відповідь:**
>
> Консистентне іменування є **мікрорівнем** організації тестів, тоді як структура папок та теги — **макрорівнем**. Вони доповнюють один одного.
>
> **Як вони пов'язані:**
>
> * **Ієрархія розуміння:**
>     1. **Структура папок** дає загальне уявлення (`це інтеграційний тест`).
>     2. **Назва тестового класу** уточнює компонент (`це тест для UserService`).
>     3. **Назва тестового методу** (`createUser_WithExistingEmail_ShouldThrowException`) дає точний опис сценарію.
>
> * **Легкість пошуку та навігації:** Якщо вам потрібно знайти тест, пов'язаний з логіном, ви можете шукати за назвами файлів (`LoginTest.java`), або за назвами методів, що містять слово `login`. Консистентність робить пошук передбачуваним.
>
> * **Документація:** Обидва рівні організації працюють разом, створюючи живу документацію. По структурі папок і файлів ви можете зрозуміти архітектуру системи, а по назвах тестів — її поведінку.
>
> Погана організація на будь-якому з рівнів (наприклад, тести в хаотичних папках або тести з назвами `test1`, `test2`) робить весь тестовий набір важким для підтримки.

#### **📌 Що перевіряють:**

*Розуміння, що якість коду та його організація — це багатошарова концепція, де важливі як глобальна структура, так і локальні деталі.*

* * *

### **10 ❓ Питання:**

Ви працюєте над проектом з тисячами тестів. Як ви будете налаштовувати CI/CD пайплайн для ефективного запуску тестів? ⭐️

> #### **💬 Відповідь:**
>
> Для проекту з великою кількістю тестів ефективний CI/CD пайплайн має бути багатоступеневим і оптимізованим для швидкого зворотного зв'язку.
>
> **Моя стратегія:**
>
> 1. **Розділення на етапи (Staged Execution):** Я б розбив виконання тестів на кілька етапів:
>     * **Етап 1 (на кожен Push у гілку фічі):** Запуск лише **найшвидших тестів**.
>         * Статичний аналіз коду (Linting).
>         * Всі **unit-тести** (`@Tag("unit")`).
>         * Цей етап має завершуватися за 1-3 хвилини.
>     * **Етап 2 (при створенні Pull/Merge Request):** Запуск більш повного набору.
>         * Все з Етапу 1.
>         * **Інтеграційні тести** (`@Tag("integration")`), які не залежать від повільних зовнішніх систем.
>         * **Контрактні тести** (Pact, Spring Cloud Contract).
>         * Цей етап може тривати 5-15 хвилин.
>     * **Етап 3 (після мержу в головну гілку / на нічній збірці):** Запуск абсолютно всіх тестів.
>         * Все з попередніх етапів.
>         * **Повільні інтеграційні тести** (`@Tag("slow")`).
>         * **End-to-End (E2E) тести.**
>
> 2. **Паралелізація:** Налаштувати запуск тестів у кілька потоків, щоб скоротити час виконання кожного етапу. JUnit 5 підтримує паралелізацію, також її можна налаштувати на рівні Maven/Gradle.
>
> 3. **Кеш залежностей:** Налаштувати кешування на CI-сервері, щоб не завантажувати всі Maven/Gradle залежності при кожному запуску.

#### **📌 Що перевіряють:**

*Досвід роботи з CI/CD та здатність проектувати ефективні пайплайни. Кандидат повинен думати про оптимізацію процесу розробки в цілому.*

* * *

### **11 ❓ Питання:**

Які переваги та недоліки використання однієї великої Test Suite для всіх регресійних тестів? ⭐️

> #### **💬 Відповідь:**
>
> **Переваги:**
>
> * **Простота запуску:** Є одна точка входу для виконання всього регресійного набору. Легко налаштувати на CI для нічного запуску.
> * **Централізоване управління:** Легко бачити, які саме класи включені у повну регресію.
>
> **Недоліки (їх значно більше):**
>
> * **Відсутність гнучкості:** Ви не можете легко запустити лише частину регресії. Або все, або нічого.
> * **Повільний зворотний зв'язок:** Прогін такого сьюту може займати години, що робить його непридатним для частих запусків.
> * **Складність підтримки:** Якщо сьют визначений через явне перерахування класів (`@SelectClasses`), його потрібно постійно вручну оновлювати при додаванні нових тестових класів. Це дуже схильно до помилок.
> * **"Все або нічого":** Якщо один тест у сьюті нестабільний ("flaky"), він може заблокувати весь сьют і створити "шум" у звітах.
>
> **Кращий підхід:** Замість одного монолітного сьюту краще використовувати **теги**. Позначити всі регресійні тести тегом `@Tag("regression")` і запускати їх за цим тегом. Це значно гнучкіше, оскільки ви можете комбінувати теги (напр., запустити регресію для `payment` модуля: `regression & payment`).

#### **📌 Що перевіряють:**

*Критичне мислення та розуміння, що прості на перший погляд рішення можуть бути неефективними в довгостроковій перспективі. Кандидат повинен знати сучасні, більш гнучкі альтернативи.*

* * *

### **12 ❓ Питання:**

Чи може один тестовий клас належати до кількох Test Suites? Як це реалізувати? ⭐️

> #### **💬 Відповідь:**
>
> **Так, може.** Це одна з причин, чому Test Suites є потужним інструментом організації.
>
> **Як це реалізувати:**
>
> Оскільки сучасні сьюти в JUnit 5 базуються на селекторах (пакети, теги), один і той самий тестовий клас може бути включений у різні сьюти, якщо він відповідає їхнім критеріям вибірки.
>
> **Приклад:**
> Нехай є клас `PaymentServiceTest`, який містить різні тести:
>
> ```java
> class PaymentServiceTest {
>     @Test @Tag("smoke") @Tag("payment")
>     void testCreatePayment() { ... }
> 
>     @Test @Tag("regression") @Tag("payment")
>     void testRefundLogic() { ... }
> }
> ```
>
> Тепер ми можемо створити два сьюти:
>
> 1. `SmokeTestSuite`, який включає всі тести з тегом `smoke`:
>
>     ```java
>     @Suite @IncludeTags("smoke")
>     public class SmokeTestSuite {}
>     ```
>
> 2. `PaymentRegressionSuite`, який включає всі тести з тегом `payment`:
>
>     ```java
>     @Suite @IncludeTags("payment")
>     public class PaymentRegressionSuite {}
>     ```
>
> У результаті, `PaymentServiceTest` (а точніше, його методи) буде включений в обидва сьюти, оскільки він відповідає критеріям обох.

#### **📌 Що перевіряють:**

*Детальне розуміння того, як працюють механізми вибірки та фільтрації в JUnit 5, та гнучкість, яку вони надають.*

* * *

### **13 ❓ Питання:**

Чим Test Suite в JUnit 5 (`@Suite`) відрізняється від підходу в JUnit 4 (`@RunWith(Suite.class)`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони відрізняються кардинально, відображаючи зміну філософії JUnit від жорсткої структури до гнучкої композиції.
>
> **JUnit 4 (`@RunWith(Suite.class)`):**
>
> * **Принцип:** **Явне перерахування.** Ви повинні були явно перерахувати всі тестові класи, які входять до сьюту, за допомогою анотації `@Suite.SuiteClasses`.
> * **Проблеми:**
>   * **Ручне оновлення:** Потрібно було вручну додавати кожен новий тестовий клас у цей список. Це легко забути.
>   * **Негнучкість:** Не було простого способу відфільтрувати тести за тегами чи іншими критеріями.
>   * **Один Runner:** `@RunWith` не можна було комбінувати з іншими раннерами.
>
> **JUnit 5 (`@Suite`):**
>
> * **Принцип:** **Декларативна вибірка та фільтрація.** Ви не перераховуєте класи, а описуєте *правила*, за якими JUnit повинен знайти та відібрати тести.
> * **Переваги:**
>   * **Автоматичне виявлення:** Використовуючи `@SelectPackages`, сьют автоматично знайде всі нові тести, додані в пакет, без необхідності ручних змін.
>   * **Потужна фільтрація:** Можна комбінувати різні селектори (`@SelectPackages`) та фільтри (`@IncludeTags`, `@ExcludeTags`), щоб динамічно формувати набір тестів.
>   * **Незалежність від Runner'ів:** Працює на основі нової архітектури JUnit Platform.
>
> **Висновок:** Підхід JUnit 5 є набагато більш гнучким, автоматизованим та менш схильним до людських помилок, ніж застарілий механізм у JUnit 4.

#### **📌 Що перевіряють:**

*Глибоке розуміння еволюції JUnit. Кандидат повинен не просто знати API, а й розуміти, які проблеми вирішує новий підхід порівняно зі старим.*

* * *

### **14 ❓ Питання:**

Уявіть, що вам потрібно організувати тести для мікросервісного проекту. Як зміниться ваша стратегія організації тестів порівняно з монолітом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Організація тестів у мікросервісному проекті має свої особливості, оскільки акцент зміщується з тестування однієї великої системи на тестування багатьох маленьких, незалежних сервісів та їхньої взаємодії.
>
> **Основні зміни:**
>
> 1. **Акцент на тестах усередині сервісу:** Кожен мікросервіс є окремою кодовою базою і повинен мати власну **повну піраміду тестування** (unit, інтеграційні тести компонентів). Це найважливіший рівень.
>
> 2. **Інший зміст інтеграційних тестів:**
>     * З'являється новий, критично важливий тип тестування — **контрактне тестування (Contract Testing)**. Його мета — перевірити, чи дотримується один сервіс (провайдер) API-контракту, на який розраховує інший (споживач). Ці тести групуються окремо.
>
> 3. **Мінімізація E2E-тестів:** Повноцінні E2E-тести, що проходять через кілька сервісів, є надзвичайно складними, повільними та крихкими в мікросервісній архітектурі. Їх кількість намагаються **звести до абсолютного мінімуму**, покладаючись на контрактні тести для перевірки інтеграцій.
>
> 4. **Організація репозиторіїв та CI:**
>     * Кожен сервіс зазвичай має свій репозиторій та свій незалежний CI/CD пайплайн.
>     * Організація тестів відбувається **в межах кожного сервісу**.
>     * Існують окремі пайплайни для запуску контрактних та E2E-тестів, які координують розгортання кількох сервісів у тестовому середовищі.
>
> **Нова структура тегів може виглядати так:** `@Tag("unit")`, `@Tag("component-integration")`, `@Tag("contract")`, `@Tag("e2e")`.

#### **📌 Що перевіряють:**

*Здатність адаптувати класичні підходи до сучасних архітектурних патернів. Це демонструє архітектурне мислення та досвід роботи зі складними системами.*

* * *

### **15 ❓ Питання:**

Як можна використовувати профілі збирання (build profiles) в Maven або варіанти в Gradle для управління різними наборами тестів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Профілі в Maven та варіанти/задачі в Gradle — це потужні інструменти для створення різних конфігурацій збирання та виконання, які ідеально підходять для керування тестовими наборами.
>
> **Maven Profiles:**
>
> * **Що це:** Профіль — це набір конфігурацій, який можна активувати або деактивувати під час збирання.
> * **Як використовувати для тестів:**
>   * Можна створити кілька профілів, наприклад: `fast-tests` та `full-regression`.
>   * У кожному профілі можна переконфігурувати `maven-surefire-plugin`, щоб він включав або виключав певні теги (groups) або папки.
>
> #### **Приклад у `pom.xml`:**
>
> ```xml
> <profiles>
>     <profile>
>         <id>fast-tests</id>
>         <activation><activeByDefault>true</activeByDefault></activation>
>         <properties>
>             <test.groups>unit,smoke</test.groups>
>         </properties>
>     </profile>
>     <profile>
>         <id>full-regression</id>
>         <properties>
>             <test.groups>unit,integration,regression</test.groups>
>         </properties>
>     </profile>
> </profiles>
> <!-- В конфігурації Surefire: <groups>${test.groups}</groups> -->
> ```
>
> **Запуск:**
>
> * `mvn test` (запустить `fast-tests` за замовчуванням).
> * `mvn test -P full-regression` (активує профіль `full-regression`).
>
> **Gradle:**
> В Gradle це реалізується зазвичай через створення кастомних **Test завдань (tasks)**.
>
> ```groovy
> task integrationTest(type: Test) {
>     useJUnitPlatform {
>         includeTags 'integration'
>     }
> }
> test {
>     useJUnitPlatform {
>         excludeTags 'integration'
>     }
> }
> ```
>
> **Запуск:**
>
> * `gradle test` (запустить всі, крім інтеграційних).
> * `gradle integrationTest` (запустить лише інтеграційні).
>
> Цей підхід дозволяє створювати дуже гнучкі та потужні конфігурації для CI/CD, де різні етапи пайплайну викликають різні профілі або задачі.

#### **📌 Що перевіряють:**

*Експертне володіння інструментами збирання та здатність проектувати складні та гнучкі процеси CI/CD. Це ознака Senior-рівня.*

* * *
