---
layout: default
title: "Методи перевірки (Assertions) в JUnit"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 6
---

# Методи перевірки (Assertions) в JUnit

* короткий опис: `Assertions` – це статичні методи в JUnit (з класу `org.junit.jupiter.api.Assertions`), які перевіряють, чи виконується певна умова під час тесту. Якщо перевірка не проходить, тест вважається проваленим.

* поняття: `Assertions`, `assertEquals()`, `assertNotEquals()`, `assertTrue()`, `assertFalse()`, `assertNull()`, `assertNotNull()`, `assertThrows()`.

* приклади: `assertEquals(4, 2 + 2);`, `assertThrows(IllegalArgumentException.class, () -> someMethod());`.

* ключові моменти: Забезпечують перевірку очікуваної поведінки коду. Провалюють тест, якщо умова не виконується.

* що важливо знати на співбесіді: Призначення основних `assert`-методів, їх використання.

### **✅ Офіційне визначення:**

**Assertions** — це методи в JUnit, які перевіряють, чи виконується певна умова під час тесту. Якщо перевірка не
проходить, тест вважається проваленим.

### **🧠 Простими словами:**

Уяви, що **`assert`** — це охоронець тесту, який кричить "СТОП\!", коли щось іде не так

---

## **Основне**

|         Метод         |              Призначення               |                          Приклад                          |
|:---------------------:|:--------------------------------------:|:---------------------------------------------------------:|
|  **`assertEquals`**   | Очікуване значення дорівнює фактичному |               **`assertEquals(4, 2 + 2)`**                |
| **`assertNotEquals`** |  Значення не повинні бути однаковими   |              **`assertNotEquals(5, 2 + 2)`**              |
|   **`assertTrue`**    |      Умова має бути **істинною**       |           **`assertTrue(list.contains("A"))`**            |
|   **`assertFalse`**   |       Умова має бути **хибною**        |             **`assertFalse(obj.isEmpty())`**              |
|   **`assertNull`**    |         Об'єкт має бути `null`         |                 **`assertNull(result)`**                  |
|  **`assertNotNull`**  |     Об'єкт **не має** бути `null`      |                 **`assertNotNull(user)`**                 |
|  **`assertThrows`**   |       Метод має викинути виняток       | **`assertThrows(IllegalArg.class, () -> doSomething())`** |

---

## **Пояснення на пальцях (💡)**

Уяви тест — це суд. **`Assertions`** — це свідки, що перевіряють:

* "Це саме той підозрюваний?" --> **`assertEquals`**
* "Це не він?" -> **`assertNotEquals`**
* "Він тут?" -> **`assertNotNull`**
* "Він зник?" -> **`assertNull`**
* "Сказав правду?" -> **`assertTrue`**
* "Збрехав?" -> **`assertFalse`**
* "А чи був злочин?" -> **`assertThrows`**

---

## **Корисні приклади (🧪)**

```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class AssertionsExampleTest {

    @Test
    void testEquality() {
        assertEquals(4, 2 + 2);
        assertNotEquals(5, 2 + 2);
    }

    @Test
    void testBooleanConditions() {
        assertTrue("abc".startsWith("a"));
        assertFalse("abc".endsWith("z"));
    }

    @Test
    void testNullChecks() {
        String str = null;
        assertNull(str);
        
        str = "hello";
        assertNotNull(str);
    }

    @Test
    void testException() {
        assertThrows(IllegalArgumentException.class, () -> {
            throw new IllegalArgumentException("Oops!");
        });
    }
}
```

---

## **Порівняння (⚖️)**

|       Метод        | Для чого використовується |          Альтернатива          |
|:------------------:|:-------------------------:|:------------------------------:|
| **`assertEquals`** |     Перевірка значень     | **`assertTrue(a.equals(b))`**  |
|  **`assertNull`**  |      Чи є **`null`**      | **`assertTrue(obj == null)`**  |
| **`assertThrows`** |    Очікування винятку     | try-catch (але менш елегантно) |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"EN-TF-NN-T"** — памʼятай це як набір основних перевірок:

* **E**quals / NotEquals
* **T**rue / False
* **N**ull / NotNull
* **T**hrows

Мнемоніка: "**ЕНі ТеФе НуНе Троль**" — JUnit бореться з багами, а не з реальністю 😄

---

## **🟩 Підсумок:**

Методи перевірки (**`assert...`**) — це наріжний камінь тестування з JUnit. Вони перевіряють, що система поводиться
очікувано. Правильно обраний assert підвищує не лише точність тесту, а й його зрозумілість для інших. Використовуйте *
*`assertEquals`** для значень, **`assertTrue`** / **`False`** для логіки, **`assertNull`** / **`NotNull`** для об'єктів,
і **`assertThrows`** для винятків.

* * *

# **FAQ — Часті запита-ння (❓)**

### **1 ❓ Питання:**

Що таке твердження (Assertions) в JUnit і яка їхня основна функція в тестах?

> #### **💬 Відповідь:**
>
> **Твердження (Assertions)** — це набір статичних методів з класу `org.junit.jupiter.api.Assertions`, які є серцем будь-якого unit-тесту.
>
> **Основна функція** тверджень — **перевірити умову** всередині тестового методу. Вони порівнюють **фактичний результат**, отриманий від коду, що тестується, з **очікуваним результатом**.
>
> * Якщо умова, яку перевіряє твердження, виконується (тобто фактичний результат збігається з очікуваним), тест продовжує своє виконання.
> * Якщо умова **не виконується**, твердження кидає `AssertionFailedError`. Це негайно **провалює тест** і зупиняє виконання цього конкретного тестового методу.
>
> По суті, тест без тверджень є марним, оскільки він нічого не перевіряє — він може виконати код, але не скаже, чи є результат правильним.

#### **📝 Аналогія:**

Якщо тест — це науковий експеримент, то твердження — це момент вимірювання. Ви провели реакцію (виконали код) і тепер за допомогою приладів (тверджень) перевіряєте, чи отримали ви саме ту речовину (результат), на яку розраховували.

#### **📌 Що перевіряють:**

*Розуміння ключової ролі тверджень. Кандидат повинен знати, що твердження — це механізм, який визначає, успішний тест чи провальний.*

* * *

### **2 ❓ Питання:**

Які є найпоширеніші методи тверджень в JUnit 5? Наведіть приклади.

> #### **💬 Відповідь:**
>
> JUnit 5 надає багатий набір методів для різних типів перевірок. Ось найуживаніші з них:
>
> * **`assertEquals(expected, actual)`:**
>     Перевіряє, що два значення є рівними (використовуючи метод `equals()`). Найчастіше використовуваний метод.
>
>     ```java
>     assertEquals(4, calculator.add(2, 2));
>     ```
>
> * **`assertTrue(boolean condition)`:**
>     Перевіряє, що булева умова є `true`.
>
>     ```java
>     assertTrue(user.isActive());
>     ```
>
> * **`assertFalse(boolean condition)`:**
>     Перевіряє, що булева умова є `false`.
>
>     ```java
>     assertFalse(list.isEmpty());
>     ```
>
> * **`assertNull(Object actual)`:**
>     Перевіряє, що об'єкт є `null`.
>
>     ```java
>     assertNull(userRepository.findById(-1));
>     ```
>
> * **`assertNotNull(Object actual)`:**
>     Перевіряє, що об'єкт не є `null`.
>
>     ```java
>     assertNotNull(userService.getActiveUser());
>     ```
>
> * **`assertThrows(Class<? extends Throwable> expectedType, Executable executable)`:**
>     Перевіряє, що виконання коду (`executable`) кидає виняток очікуваного типу.
>
>     ```java
>     assertThrows(IllegalArgumentException.class, () -> list.get(99));
>     ```
>
> * **`assertSame(Object expected, Object actual)`:**
>     Перевіряє, що дві змінні посилаються на **той самий об'єкт** в пам'яті (перевірка через `==`).

#### **📌 Що перевіряють:**

*Практичні знання основного API JUnit. Кандидат повинен вільно оперувати цими методами та знати, який з них застосувати в конкретній ситуації.*

* * *

### **3 ❓ Питання:**

У чому різниця між `assertEquals` та `assertSame`?

> #### **💬 Відповідь:**
>
> Це два методи для перевірки на рівність, але вони перевіряють два абсолютно різних типи рівності.
>
> * **`assertEquals(expected, actual)`:**
>   * **Перевіряє на еквівалентність за значенням.**
>   * Внутрішньо цей метод викликає `expected.equals(actual)`. Він перевіряє, чи є об'єкти "логічно" однаковими, навіть якщо це два різні екземпляри в пам'яті.
>
> * **`assertSame(expected, actual)`:**
>   * **Перевіряє на ідентичність за посиланням.**
>   * Внутрішньо цей метод виконує перевірку `expected == actual`. Він перевіряє, чи вказують дві змінні на **один і той самий об'єкт** у купі (heap).
>
> Використовувати `assertSame` потрібно дуже рідко, наприклад, для перевірки роботи патерну Singleton або коли важливо переконатися, що метод повернув кешований екземпляр, а не новий.

#### **📝 Приклад:**

```java
String a = new String("hello");
String b = new String("hello");
String c = a;

assertEquals(a, b); // Успішно, бо їхні значення однакові
// assertSame(a, b);    // Провалиться, бо це два різні об'єкти в пам'яті

assertSame(a, c);   // Успішно, бо `a` і `c` посилаються на той самий об'єкт
```

#### **📌 Що перевіряють:**

*Чітке розуміння різниці між рівністю (`.equals()`) та ідентичністю (`==`) в Java, що є фундаментальним знанням ООП.*

* * *

### **4 ❓ Питання:**

Як протестувати, що метод кидає виняток певного типу?

> #### **💬 Відповідь:**
>
> У JUnit 5 для цього використовується спеціалізований та дуже зручний метод **`assertThrows()`**. Він не тільки перевіряє факт викидання винятку, але й дозволяє перевірити сам об'єкт винятку.
>
> **Як це працює:**
>
> 1. Ви викликаєте `assertThrows()`, передаючи йому клас очікуваного винятку та лямбда-вираз, який містить код, що має кинути цей виняток.
> 2. JUnit виконує лямбда-вираз у блоці `try-catch`.
> 3. Якщо був кинутий виняток потрібного типу, тест проходить. Метод повертає сам об'єкт винятку.
> 4. Якщо був кинутий виняток іншого типу, або винятку не було взагалі, тест провалюється.

#### **📝 Приклад:**

```java
@Test
void whenDividingByZero_thenThrowsIllegalArgumentException() {
    Calculator calculator = new Calculator();
    
    // Блок 1: Перевірка типу винятку
    IllegalArgumentException exception = assertThrows(IllegalArgumentException.class, () -> {
        calculator.divide(1, 0);
    });

    // Блок 2 (опційно): Перевірка повідомлення у винятку
    assertEquals("Divider cannot be zero", exception.getMessage());
}
```

#### **Коментар:**

Цей підхід набагато кращий, ніж `try-catch` з `fail()` або `@Test(expected=...)` у JUnit 4, оскільки він дозволяє зробити додаткові перевірки самого винятку.

#### **📌 Що перевіряють:**

*Знання API JUnit 5 для тестування негативних сценаріїв. Обробка помилок — важлива частина логіки, і її тестування є обов'язковим.*

* * *

### **5 ❓ Питання:**

Що таке `assertAll()` і в яких випадках його варто використовувати?

> #### **💬 Відповідь:**
>
> Метод **`assertAll()`** використовується для групування кількох тверджень, щоб **усі вони були виконані**, навіть якщо одне з перших провалиться.
>
> **Проблема, яку він вирішує:**
> При звичайному використанні, якщо перше ж твердження в тесті падає, виконання тесту зупиняється, і ви ніколи не дізнаєтесь про стан наступних тверджень.
>
> **Як працює `assertAll()`:**
> Він приймає набір лямбда-виразів, кожен з яких містить одне або кілька тверджень. `assertAll()` виконає **усі** ці лямбди, збереже інформацію про всі збої, і лише в кінці провалить тест, видавши звіт про всі невдалі перевірки.
>
> **Коли використовувати:**
>
> * Коли ви тестуєте один об'єкт і хочете перевірити кілька його властивостей незалежно одна від одної.
> * Це дозволяє отримати повну картину помилок за один запуск тесту, що значно прискорює процес налагодження.

#### **📝 Приклад:**

```java
@Test
void testPersonProperties() {
    Person person = new Person("John", "Doe");
    person.setAge(30);

    assertAll("person",
        () -> assertEquals("John", person.getFirstName()),
        () -> assertEquals("Doe", person.getLastName()),
        () -> assertEquals(30, person.getAge())
    );
}
```

#### **📌 Що перевіряють:**

*Знання розширених, але корисних функцій JUnit 5. Використання `assertAll` свідчить про прагнення писати ефективніші та інформативніші тести.*

* * *

### **6 ❓ Питання:**

Для чого потрібен останній необов'язковий параметр `message` або `Supplier<String> messageSupplier` у методах тверджень? ⭐️

> #### **💬 Відповідь:**
>
> Цей параметр призначений для надання **власного повідомлення про помилку**, яке буде відображене, якщо твердження провалиться. Це робить звіти про тестування набагато інформативнішими.
>
> * **`String message`:** Простий рядок з повідомленням.
> * **`Supplier<String> messageSupplier` (кращий варіант):** Лямбда-вираз, який повертає рядок.
>
> **Чому `Supplier` краще:**
> Якщо використовувати звичайний `String`, цей рядок буде створюватися (наприклад, через конкатенацію) **при кожному** запуску тесту, навіть якщо тест успішний. Це може призводити до зайвих витрат продуктивності, особливо якщо повідомлення складне.
>
> Якщо використовувати `Supplier<String>`, лямбда-вираз буде виконаний і рядок буде створено **тільки тоді, коли тест провалиться**. Це називається **"лінивим" (lazy) обчисленням** і є більш ефективним підходом.

#### **📝 Приклад:**

```java
// Повідомлення створюється завжди
assertEquals(expected, actual, "Помилка для користувача ID: " + user.getId());

// Повідомлення створюється тільки при збої
assertEquals(expected, actual, () -> "Помилка для користувача ID: " + user.getId());
```

#### **📌 Що перевіряють:**

*Увагу до деталей API та прагнення писати не тільки правильний, але й оптимізований код, навіть у тестах.*

* * *

### **7 ❓ Питання:**

Чому іноді краще використовувати сторонні бібліотеки для тверджень, як-от AssertJ, замість стандартних з JUnit? ⭐️

> #### **💬 Відповідь:**
>
> Хоча стандартні твердження JUnit є достатніми для більшості випадків, сторонні бібліотеки, як-от **AssertJ**, пропонують кілька значних переваг:
>
> 1. **"Плинний" (Fluent) API та краща читабельність:**
>     Твердження в AssertJ читаються майже як природна мова, що робить тести більш виразними.
>     * JUnit: `assertTrue(list.contains("A") && list.size() == 3);`
>     * AssertJ: `assertThat(list).contains("A").hasSize(3);`
>
> 2. **Більш інформативні повідомлення про помилки:**
>     Повідомлення про збої в AssertJ зазвичай є набагато детальнішими і зрозумілішими.
>
> 3. **Розширений набір тверджень:**
>     AssertJ надає безліч специфічних тверджень "з коробки" для роботи з колекціями, рядками, винятками, `Optional`, файлами тощо, яких немає в JUnit. Наприклад, `assertThat(list).containsExactlyInAnyOrder("A", "B");`
>
> 4. **Легка розширюваність:** Дозволяє легко створювати власні кастомні твердження для ваших доменних об'єктів.
>
> Spring Boot, наприклад, включає AssertJ до свого `spring-boot-starter-test` за замовчуванням, що підкреслює його популярність та корисність.

#### **📌 Що перевіряють:**

*Кругозір кандидата та знання популярних бібліотек в екосистемі Java. Вміння обирати найкращий інструмент для роботи.*

* * *

### **8 ❓ Питання:**

Як протестувати операції з числами з плаваючою комою (float, double) за допомогою `assertEquals`? ⭐️

> #### **💬 Відповідь:**
>
> Пряме порівняння чисел з плаваючою комою за допомогою `assertEquals(double expected, double actual)` є **поганою практикою**, оскільки через особливості їх представлення в пам'яті можуть виникати невеликі похибки обчислень. Наприклад, `0.1 + 0.2` не буде точно дорівнювати `0.3`.
>
> Для коректного порівняння JUnit надає перевантажену версію `assertEquals`, яка приймає третій параметр — **`delta` (допустиму похибку)**.
>
> **`assertEquals(double expected, double actual, double delta)`**
>
> Цей метод перевіряє, чи є різниця між `expected` та `actual` меншою або рівною `delta`. Твердження пройде, якщо `Math.abs(expected - actual) <= delta`.

#### **📝 Приклад:**

```java
@Test
void testDoubleAddition() {
    double result = 0.1 + 0.2;
    // assertEquals(0.3, result); // Може провалитися!

    // Правильний підхід з дельтою
    assertEquals(0.3, result, 0.00001);
}
```

#### **📌 Що перевіряють:**

*Знання специфіки роботи з числами з плаваючою комою та відповідних інструментів у JUnit для уникнення нестабільних тестів.*

* * *

### **9 ❓ Питання:**

Що таке `fail()` і в яких рідкісних випадках він може бути корисним? ⭐️

> #### **💬 Відповідь:**
>
> Метод **`fail()`** — це твердження, яке **завжди провалює тест** з заданим повідомленням.
> `fail("Цей код не мав бути виконаний");`
>
> **Коли він може бути корисним:**
>
> 1. **Позначення ще не реалізованої логіки:** Якщо ви створюєте тест для функціоналу, який ще не написано (у стилі TDD), ви можете тимчасово поставити `fail()` всередині, щоб тест був червоним, доки ви не напишете код.
>
> 2. **У складних умовних блоках:** Якщо у вас є `if-else` або `switch`, і тест за логікою ніколи не повинен потрапити в певну гілку (напр., в `else`), ви можете поставити там `fail()`. Якщо тест все ж таки потрапить туди, він провалиться, що вкаже на помилку в логіці.
>
> 3. **У блоці `catch` при тестуванні винятків (застарілий підхід):** У JUnit 4, якщо ви очікували, що виняток не буде кинуто, ви могли обгорнути виклик у `try-catch` і викликати `fail()` у блоці `catch`.
>
> У сучасному JUnit 5 використання `fail()` стало значно рідшим завдяки потужнішим інструментам, як-от `assertThrows` та параметризовані тести, але він все ще може бути корисним для деяких крайніх випадків.

#### **📌 Що перевіряють:**

*Повноту знань API JUnit, включаючи менш уживані, але іноді корисні методи.*

* * *

### **10 ❓ Питання:**

Які є альтернативи `assertTrue(myMap.containsKey("key"))`? ⭐️

> #### **💬 Відповідь:**
>
> Хоча `assertTrue(myMap.containsKey("key"))` є технічно коректним, існують кращі та більш виразні способи написати таку перевірку, які надають більше інформації при збої.
>
> **Стандартна альтернатива JUnit:**
>
> * На жаль, у стандартному API JUnit немає спеціального твердження `assertContainsKey`. Тому `assertTrue` є основним варіантом.
>
> **Краща альтернатива зі сторонніми бібліотеками (напр., AssertJ):**
>
> * Сторонні бібліотеки надають значно кращі можливості для роботи з колекціями.
>
>     ```java
>     import static org.assertj.core.api.Assertions.assertThat;
>
>     // AssertJ
>     assertThat(myMap).containsKey("key");
>     ```
>
> **Перевага AssertJ:**
> Якщо цей тест впаде, AssertJ видасть дуже інформативне повідомлення, наприклад:
> `Expecting map: { "anotherKey" -> 123 } to contain key: "key"`.
>
> JUnit `assertTrue` просто скаже: `expected: <true> but was: <false>`.
>
> **Висновок:** Для простих логічних перевірок `assertTrue` підходить. Для роботи з колекціями, мапами та іншими складними об'єктами краще використовувати AssertJ для підвищення читабельності тестів та інформативності звітів про помилки.

#### **📌 Що перевіряють:**

*Прагнення до написання чистого, читабельного та інформативного коду. Кандидат повинен розуміти, що не всі технічно правильні рішення є найкращими з точки зору підтримки.*

* * *

### **11 ❓ Питання:**

Як перевірити, що виконання методу не перевищує заданий час? ⭐️⭐️

> #### **💬 Відповідь:**
>
> В JUnit 5 для цього існує спеціальне твердження **`assertTimeout()`**. Воно дозволяє виконати блок коду і провалити тест, якщо його виконання займає більше часу, ніж вказаний ліміт.
>
> Існує дві версії цього методу:
>
> 1. **`assertTimeout(Duration timeout, Executable executable)`**:
>     * Ця версія **не перериває** виконання коду, якщо час вичерпано. Вона дочекається його завершення і лише потім провалить тест.
>
> 2. **`assertTimeoutPreemptively(Duration timeout, Executable executable)`**:
>     * Ця версія **перериває** виконання коду в іншому потоці, якщо час вичерпано. Цей підхід слід використовувати обережно, оскільки він може залишити систему в некоректному стані.
>
> Цей інструмент корисний для простих перевірок продуктивності, щоб переконатися, що метод не "зависає" або не працює аномально довго.

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assertions.assertTimeout;
import java.time.Duration;

@Test
void testPerformance() {
    // Перевіряємо, що метод виконається менш ніж за 100 мілісекунд
    assertTimeout(Duration.ofMillis(100), () -> {
        // ... якийсь довготривалий код ...
        Thread.sleep(50); // Симуляція роботи
    });
}
```

#### **📌 Що перевіряють:**

*Знання можливостей JUnit для нефункціонального тестування. Це показує, що кандидат думає не тільки про коректність, а й про продуктивність.*

* * *

### **12 ❓ Питання:**

Поясніть, що таке `Assumptions` в JUnit 5 і чим вони відрізняються від `Assertions`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Assumptions` (Припущення)** — це механізм у JUnit, який дозволяє **пропускати тест**, якщо певна умова **не виконується**.
>
> **Ключова відмінність від `Assertions`:**
>
> * **`Assertion` (Твердження):** Якщо умова не виконується, тест **провалюється (Failed)**. Це означає, що є **баг** у коді, який тестується.
> * **`Assumption` (Припущення):** Якщо умова не виконується, тест **пропускається (Aborted/Skipped)**. Це означає, що **зовнішнє середовище** не підходить для виконання цього тесту, але це **не є помилкою** в коді.
>
> **Коли використовувати `Assumptions`:**
>
> Коли тест має сенс виконувати тільки за певних зовнішніх умов. Наприклад:
>
> * Тест залежить від певної операційної системи.
> * Тест вимагає наявності Docker-середовища.
> * Тест вимагає доступу до зовнішнього API, який може бути недоступний.
>
> Використовуючи `assumingThat`, ви можете умовно виконати частину тесту.

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assumptions.*;

@Test
void testOnlyOnWindows() {
    // Якщо тест запущено не на Windows, він буде пропущений, а не провалений
    assumeTrue(System.getProperty("os.name").startsWith("Windows"));

    // ... сам тест, який буде виконано тільки на Windows ...
}

@Test
void testWithDocker() {
    boolean isDockerRunning = checkDocker();
    // Виконати блок тверджень, тільки якщо Docker запущено
    assumingThat(isDockerRunning, () -> {
        // ... перевірки, що залежать від Docker ...
    });
}
```

#### **📌 Що перевіряють:**

*Глибоке розуміння інструментів JUnit. Кандидат повинен розрізняти збій тесту через баг у коді і через невідповідне середовище.*

* * *

### **13 ❓ Питання:**

Як протестувати метод, який повертає `void`? На що фокусуються такі тести? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тестування `void`-методів — це **тестування поведінки (behavior testing)**, а не стану. Оскільки метод нічого не повертає, ми не можемо перевірити його результат за допомогою `assertEquals`. Замість цього, ми перевіряємо, чи **спричинив** метод очікувані **побічні ефекти (side effects)**.
>
> **На чому фокусуються тести:**
>
> 1. **Зміна стану самого об'єкта:** Ми перевіряємо, що після виклику `void`-методу змінилися внутрішні поля об'єкта.
>
>     ```java
>     @Test
>     void testActivateUser() {
>         User user = new User();
>         assertFalse(user.isActive()); // Передумова
>         user.activate(); // void-метод
>         assertTrue(user.isActive()); // Перевірка зміни стану
>     }
>     ```
>
> 2. **Взаємодія з іншими об'єктами (колабораторами):** Ми перевіряємо, що наш метод правильно викликав методи своїх залежностей. Це робиться за допомогою **моків (mocks)** та методу `Mockito.verify()`.
>
>     ```java
>     @Mock private EmailService mockEmailService;
>     // ...
>     @Test
>     void testRegisterUser() {
>         userService.register(newUser); // void-метод
>         // Перевіряємо, чи був викликаний метод відправки email
>         verify(mockEmailService).sendWelcomeEmail(newUser.getEmail());
>     }
>     ```
>
> Отже, для тестування `void`-методів ми перевіряємо не "що повернулося", а "що змінилося".

#### **📌 Що перевіряють:**

*Здатність мислити в категоріях "стану" та "поведінки". Це ключова навичка для написання unit-тестів для складних систем, де методи часто не повертають значень, а змінюють стан або взаємодіють з іншими компонентами.*

* * *

### **14 ❓ Питання:**

Як `assertEquals` працює для об'єктів кастомних класів? Що потрібно реалізувати в класі, щоб він працював коректно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `assertEquals(expected, actual)` для порівняння об'єктів (не примітивів) внутрішньо викликає метод **`expected.equals(actual)`**.
>
> Якщо ви не перевизначите метод `equals()` у вашому кастомному класі, він успадкує реалізацію з класу `Object`. Реалізація в `Object` — це просто перевірка за посиланням (`this == obj`), що еквівалентно `assertSame()`.
>
> **Що потрібно реалізувати:**
>
> Для того, щоб `assertEquals` працював коректно (порівнював об'єкти за їхніми внутрішніми полями, а не за посиланнями), необхідно **перевизначити метод `equals()`** у вашому класі.
>
> **Контракт `equals()` і `hashCode()`:**
> Крім `equals()`, також **обов'язково** потрібно перевизначити і метод `hashCode()`. Згідно з контрактом Java, якщо два об'єкти рівні за `equals()`, їхні хеш-коди також мають бути рівними (`a.equals(b) -> a.hashCode() == b.hashCode()`). Це критично для правильної роботи хеш-базованих колекцій (`HashMap`, `HashSet`).
>
> **Висновок:** Для коректної роботи `assertEquals` для вашого класу, ви повинні правильно перевизначити і `equals()`, і `hashCode()`.

#### **📝 Приклад:**

```java
class Point {
    int x, y;
    // ...
    @Override public boolean equals(Object o) { ... return this.x == other.x && this.y == other.y; }
    @Override public int hashCode() { ... return Objects.hash(x, y); }
}

Point p1 = new Point(1, 2);
Point p2 = new Point(1, 2);
assertEquals(p1, p2); // Пройде успішно, тільки якщо equals() перевизначено.
```

#### **📌 Що перевіряють:**

*Глибокі знання основ ООП в Java. Це питання поєднує знання про тестування (`assertEquals`) з фундаментальними концепціями мови (`equals` та `hashCode`).*

* * *

### **15 ❓ Питання:**

Як протестувати код, який залежить від поточного часу (наприклад, `LocalDateTime.now()`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Код, що напряму залежить від `LocalDateTime.now()` або `new Date()`, є **важким для тестування**, оскільки його результат змінюється щомиті. Це робить тести непередбачуваними та нестабільними.
>
> **Правильний підхід** — це уникнути прямої залежності від системного годинника і зробити час **зовнішньою залежністю**, яку можна контролювати в тестах.
>
> **Кроки:**
>
> 1. **Ввести абстракцію для часу:** Замість прямого виклику `LocalDateTime.now()`, використовувати інтерфейс `java.time.Clock`. Цей клас дозволяє отримувати поточний час, але його можна легко замінити в тестах.
>
> 2. **Ін'єкція залежності `Clock`:** Ваш сервіс повинен отримувати екземпляр `Clock` через конструктор. У виробничому коді ви будете передавати `Clock.systemUTC()`, а в тестах — фіксований годинник.
>
>     ```java
>     // Хороший, тестований код
>     public class MyService {
>         private final Clock clock;
>         public MyService(Clock clock) { this.clock = clock; }
>     
>         public void doSomethingWithTime() {
>             LocalDateTime now = LocalDateTime.now(this.clock);
>             // ...
>         }
>     }
>     ```
>
> 3. **Контроль часу в тесті:** У unit-тесті ви створюєте фіксований годинник і передаєте його у ваш сервіс. Це дозволяє зробити тест повністю детермінованим.
>
#### **📝 Приклад тесту:**

```java
@Test
void testWithFixedTime() {
    // Створюємо годинник, який завжди повертає один і той самий час
    String fixedInstant = "2023-10-27T10:00:00Z";
    Clock fixedClock = Clock.fixed(Instant.parse(fixedInstant), ZoneId.of("UTC"));
    
    MyService myService = new MyService(fixedClock);
    
    myService.doSomethingWithTime();
    
    // Тепер ми можемо перевірити логіку з точним, передбачуваним часом
    // assertEquals(...)
}
```

Такий підхід робить код, що залежить від часу, повністю тестованим і надійним.

#### **📌 Що перевіряють:**

*Вміння писати тестований код (testable code). Це питання демонструє здатність розробника застосовувати принципи дизайну (Dependency Inversion) для вирішення складних проблем у тестуванні.*

* * *
