---
layout: default
title: "Покриття коду тестами (Code Coverage)"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 11
---

# Покриття коду тестами (Code Coverage)

* короткий опис: `Code Coverage` – це метрика якості тестування, яка показує, яку частину коду програми було виконано (протестовано) під час запуску тестів.

* поняття: Code Coverage, Line Coverage, Branch Coverage, Method Coverage, Class Coverage, `JaCoCo` (інструмент).

* приклади: `if (number > 0) { ... } else { ... }` – перевірка покриття гілок.

* ключові моменти: 100% покриття не гарантує якість (важливі `assert`-и). 80-90% – типовий цільовий рівень. `Branch coverage` часто важливіший за `Line coverage`.

* що важливо знати на співбесіді: Що це, метрики, інструменти, як інтерпретувати.

### **✅ Офіційне визначення:**

**Code Coverage** — це метрика якості тестування, яка показує, яку частину коду програми було виконано (протестовано) під час запуску тестів.

### **🧠 Простими словами:**

Це відсоток коду, який «зачепили» тести. Чим більше — тим краще видно, що тестами щось дійсно перевіряється

---

## **Основне**

**📊 Основні метрики покриття:**

* **Line Coverage** – скільки рядків коду виконано
* **Branch Coverage** – скільки гілок (if/else, switch) пройдено
* **Method Coverage** – скільки методів було викликано
* **Class Coverage** – скільки класів було задіяно
* **Instruction Coverage** – кількість виконаних JVM-інструкцій

**🛠 Інструменти:**

* **JaCoCo** (найпопулярніший для Java)
* IntelliJ IDEA coverage tool
* Cobertura, Clover, SonarQube

---

## **Пояснення на пальцях (💡)**

* Уяви, що твій код — це лабіринт. Test coverage показує, скільки коридорів ти пройшов. Але пройти всі — не завжди
  означає, що лабіринт безпечний 😉
* 100% **`coverage`** може нічого не **`означати`**, якщо **`assert’`**`ів` немає або логіка не перевірена.
* Branch coverage важливіший, бо перевіряє розгалуження — тобто складну поведінку.

---

## **Корисні приклади (🧪)**

```java
public boolean isPositive(int number) {
    if (number > 0) {
        return true;
    } else {
        return false;
    }
}
```

**📈 Покриття:**

* Line coverage: 100% (якщо протестували і \>0, і ≤0)
* Branch coverage: 100% (if і else виконались)
* Але якщо протестували лише number \= 1, то branch \= 50%

**📦 JaCoCo у pom.xml:**

```java
xml
Копіювати
Редагувати
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.8</version>
    <executions>
        <execution>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
                <goal>report</goal>
            </goals>
        </execution>
    </executions>
</plugin>
```

---

## **Порівняння (⚖️)**

|       Метрика       |       Що перевіряє       |       Приклад       |
|:-------------------:|:------------------------:|:-------------------:|
|  **Line Coverage**  |     Виконання рядків     |  **`if (x > 0)`**   |
| **Branch Coverage** | Варіанти логічних шляхів |  **`if` і `else`**  |
| **Method Coverage** |    Чи викликано метод    | **`someMethod()`**  |
| **Class Coverage**  | Чи використовувався клас | **`new MyClass()`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"LAMBI"** — як маленька лампа, що світить на coverage:

* **L** — Line
* **A** — Assert
* **M** — Method
* **B** — Branch
* **I** — Instruction

---

## **🟩 Підсумок:**

* **Test Coverage** — це важлива метрика, але не абсолют.
* Зосереджуйся на логіці, не на **%**.
* **Branch \> Line** у більшості випадків.
* **JaCoCo** — зручний інструмент для вимірювання.
* **80–90%** **coverage** — хороший цільовий рівень, якщо покрито важливі гілки.

---

### **🔗 Додаткові джерела:**

* [**What is Code Coverage? (JetBrains Guide)**](https://www.jetbrains.com/help/idea/coverage.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке покриття коду (Code Coverage) і яка його головна мета?

> #### **💬 Відповідь:**
>
> **Покриття коду (Code Coverage)** — це метрика, яка вимірює, який відсоток вихідного коду програми був виконаний ("зачеплений") під час запуску автоматизованих тестів. Вона показує, які частини коду були перевірені, а які — залишилися без уваги.
>
> **Головна мета Code Coverage** — не довести, що код не має багів, а **знайти непротестовані частини коду**. Це інструмент для виявлення "сліпих плям" у вашому тестовому наборі. Аналізуючи звіт про покриття, розробники можуть побачити, які класи, методи або гілки логіки потребують додаткових тестів, тим самим підвищуючи загальну надійність продукту.

#### **📝 Аналогія:**

Уявіть, що ваш код — це карта доріг міста, а ваші тести — це маршрути, якими ви проїхали. Code Coverage показує, який відсоток доріг на карті ви відвідали. Якщо покриття 80%, це означає, що 20% доріг залишилися для вас незвіданими, і ви не знаєте, чи є там ями (баги).

#### **📌 Що перевіряють:**

*Базове розуміння, що таке Code Coverage, та його призначення. Кандидат повинен знати, що це метрика для аналізу тестів, а не показник якості самого коду.*

* * *

### **2 ❓ Питання:**

Які є основні типи (метрики) покриття коду?

> #### **💬 Відповідь:**
>
> Існує кілька метрик для вимірювання покриття, кожна з яких дивиться на код під різним кутом:
>
> * **Line Coverage (Покриття рядків):**
>     Найпростіша метрика. Показує відсоток виконаних рядків коду від загальної кількості.
>
> * **Branch Coverage (Покриття гілок):**
>     Більш просунута метрика. Відстежує, які гілки умовних операторів (`if/else`, `switch`, `while`) були виконані. Наприклад, для `if-else` є дві гілки: одна для `true`, інша для `false`.
>
> * **Method Coverage (Покриття методів):**
>     Показує відсоток викликаних методів від загальної кількості методів у класі.
>
> * **Class Coverage (Покриття класів):**
>     Показує відсоток класів, які були задіяні (завантажені та ініціалізовані) під час виконання тестів.
>
> * **Instruction Coverage (JaCoCo):**
>     Найбільш детальна метрика. Вимірює відсоток виконаних інструкцій байт-коду JVM.

#### **📝 Коментар:**

`Branch Coverage` зазвичай вважається більш важливою та інформативною метрикою, ніж `Line Coverage`, оскільки вона гарантує перевірку різних логічних шляхів.

#### **📌 Що перевіряють:**

*Знання основних метрик покриття та розуміння, що вони надають різний рівень деталізації аналізу.*

* * *

### **3 ❓ Питання:**

Чи гарантує 100% покриття коду відсутність багів? Чому?

> #### **💬 Відповідь:**
>
> **Ні, абсолютно не гарантує.** 100% покриття коду є небезпечною і часто оманливою метрикою.
>
> **Причини:**
>
> 1. **Покриття не дорівнює якості перевірки:** 100% покриття означає лише те, що кожен рядок коду був **виконаний** під час тестів. Це нічого не говорить про те, **наскільки добре** він був перевірений. Тест може мати 100% покриття і при цьому не містити жодного `assert`'у, що робить його марним.
>
> 2. **Пропущені сценарії:** Тест може покривати код, але не перевіряти всі важливі граничні випадки (edge cases). Наприклад, ви перевірили `sum(2, 3)`, але не `sum(Integer.MAX_VALUE, 1)`.
>
> 3. **Не враховує відсутність коду:** Покриття не може знайти баги, пов'язані з **нереалізованим** функціоналом (наприклад, відсутність валідації вхідних даних).
>
> **Висновок:** 100% покриття може створити хибне відчуття безпеки. Якість та продуманість тверджень (`assert`) набагато важливіша за відсоток покриття.

#### **📝 Аналогія:**

Ви можете пройтися по кожній кімнаті в будинку (100% покриття), але якщо ви не перевірили, чи зачиняються вікна і чи працюють крани (немає `assert`'ів), ви не можете сказати, що будинок у хорошому стані.

#### **📌 Що перевіряють:**

*Критичне мислення та розуміння обмежень інструментів. Кандидат не повинен сліпо гнатися за цифрами, а розуміти їхній реальний сенс.*

* * *

### **4 ❓ Питання:**

Який інструмент для вимірювання покриття коду є найпопулярнішим у Java-екосистемі?

> #### **💬 Відповідь:**
>
> Найпопулярнішим і стандартом де-факто для вимірювання покриття коду в Java є інструмент **JaCoCo (Java Code Coverage)**.
>
> **Основні характеристики JaCoCo:**
>
> * **Open-source:** Є безкоштовним та активно розвивається.
> * **Проста інтеграція:** Легко інтегрується з системами збирання, такими як **Maven** та **Gradle**, за допомогою плагінів.
> * **Гнучкі звіти:** Генерує детальні звіти у форматах HTML, XML та CSV. HTML-звіт візуально підсвічує покриті, частково покриті та непокриті рядки та гілки коду.
> * **Робота "на льоту":** JaCoCo працює шляхом інструментації байт-коду під час виконання тестів за допомогою Java-агента, що робить процес вимірювання прозорим.
>
> Інші інструменти, такі як Cobertura або Clover, існують, але JaCoCo є найбільш широко використовуваним сьогодні.

#### **📝 Приклад (конфігурація в Maven `pom.xml`):**

```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.8</version>
    <executions>
        <execution>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
                <goal>report</goal>
            </goals>
        </execution>
    </executions>
</plugin>
```

#### **📌 Що перевіряють:**

*Знання основного інструментарію для забезпечення якості в екосистемі Java. Очікується, що розробник чув про JaCoCo і має уявлення про його призначення.*

* * *

### **5 ❓ Питання:**

Який цільовий відсоток покриття коду зазвичай вважається хорошою метою для проекту?

> #### **💬 Відповідь:**
>
> Не існує єдиної "ідеальної" цифри, яка б підходила для всіх проектів. Однак, у індустрії склався певний консенсус щодо прагматичних цілей.
>
> Зазвичай хорошою метою вважається **80-90% покриття** (особливо **branch coverage**).
>
> **Чому не 100%?**
>
> * **Закон спадної віддачі:** Досягти перших 80% покриття відносно легко і дуже корисно — ви тестуєте ключову логіку. Кожен наступний відсоток вимагає все більше зусиль і приносить все менше користі. Тестування тривіальних геттерів, сеттерів або складних блоків обробки рідкісних помилок може бути невиправдано дорогим.
>
> **Важливіше, ніж загальний відсоток:**
>
> * Переконатися, що **вся критично важлива бізнес-логіка** має близьке до 100% покриття.
> * Використовувати метрику покриття не як жорсткий KPI, а як **інструмент для виявлення непротестованих областей** коду.
>
> Деякі команди встановлюють поріг (quality gate) на CI, наприклад, 80%, і збірка падає, якщо покриття стає нижчим.

#### **📌 Що перевіряють:**

*Прагматичний підхід та бізнес-орієнтоване мислення. Кандидат повинен розуміти, що потрібно шукати баланс між якістю та вартістю розробки.*

* * *

### **6 ❓ Питання:**

Розгляньте код: `if (a > 0 && b > 0) { ... }`. Скільки тестів потрібно для досягнення 100% Line Coverage і 100% Branch Coverage?

> #### **💬 Відповідь:**
>
> Давайте проаналізуємо цей випадок.
>
> ```java
> public void process(int a, int b) {
>     if (a > 0 && b > 0) {
>         System.out.println("Both positive");
>     }
> }
> ```
>
> * **Для 100% Line Coverage (Покриття рядків):**
>     Потрібен **лише один тест**, який змусить умову `if` виконатися. Наприклад:
>   * `process(5, 5)`
>     Цей тест виконає і рядок з `if`, і рядок з `System.out.println`.
>
> * **Для 100% Branch Coverage (Покриття гілок):**
>     Потрібно **два тести**, щоб покрити обидві гілки умови `if`:
>     1. Тест, коли умова `true` (виконується тіло `if`).
>         * Наприклад: `process(5, 5)`
>     2. Тест, коли умова `false` (тіло `if` не виконується).
>         * Наприклад: `process(-1, 5)` (або `process(5, -1)`, або `process(-1, -1)`)
>
> Цей приклад чудово демонструє, чому Branch Coverage є більш надійною метрикою — вона змушує нас тестувати негативні сценарії.

#### **📌 Що перевіряють:**

*Практичне розуміння різниці між Line та Branch coverage. Це питання часто дають на співбесідах, щоб перевірити, як кандидат аналізує код.*

* * *

### **7 ❓ Питання:**

Чому Branch Coverage вважається більш важливою та надійною метрикою, ніж Line Coverage? ⭐️

> #### **💬 Відповідь:**
>
> Branch Coverage є більш надійною метрикою, оскільки вона гарантує перевірку **різних логічних шляхів** у коді, тоді як Line Coverage цього не робить.
>
> **Ключова проблема Line Coverage:**
> Можна досягти 100% Line Coverage, але при цьому пропустити тестування важливих гілок логіки.
>
> **Приклад, що демонструє слабкість Line Coverage:**
>
> ```java
> public void someMethod(String input) {
>     if (input == null) return; // Guard clause
>     // ... 10 рядків складної логіки ...
> }
> ```
>
> Якщо написати лише один тест, який передає `input = null`, ми отримаємо:
>
> * **Line Coverage: 100%** (виконано 2 рядки з 2, які не є коментарями чи дужками).
> * **Branch Coverage: 50%** (ми перевірили лише одну гілку `if`, але не перевірили, що відбувається, коли `input` не `null`).
>
> Таким чином, `Line Coverage` створює хибне враження, що весь код протестований, хоча вся основна логіка методу залишилася без перевірки. `Branch Coverage` одразу ж покаже цю проблему.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що різні метрики дають різну цінність. Кандидат повинен вміти обґрунтувати, чому одна метрика є кращою за іншу.*

* * *

### **8 ❓ Питання:**

Як покриття коду інтегрується в процес CI/CD (Безперервної інтеграції та доставки)? ⭐️

> #### **💬 Відповідь:**
>
> Інтеграція вимірювання покриття коду в CI/CD пайплайн є ключовою практикою для підтримки високої якості.
>
> **Процес зазвичай виглядає так:**
>
> 1. **Налаштування збирання:** У `pom.xml` (Maven) або `build.gradle` (Gradle) додається плагін `JaCoCo`.
>
> 2. **Запуск тестів на CI-сервері:** Коли розробник робить коміт або створює pull request, CI-сервер (Jenkins, GitLab CI, GitHub Actions) автоматично запускає процес збирання, який включає виконання всіх unit та інтеграційних тестів.
>
> 3. **Генерація звіту:** Після виконання тестів, плагін `JaCoCo` генерує звіт про покриття.
>
> 4. **Встановлення "Брами якості" (Quality Gate):**
>     * Це найважливіший крок. Налаштовується правило, яке **провалює збірку**, якщо покриття коду опускається нижче певного порогу (наприклад, 80%).
>     * Також можна налаштувати правило, що покриття для **нового коду** не може бути нижчим за встановлену межу. Це запобігає погіршенню якості.
>
> 5. **Публікація звітів:** Звіти про покриття публікуються в інструментах, як-от SonarQube або безпосередньо в інтерфейсі CI-сервера, роблячи їх доступними для всієї команди.
>
> Це забезпечує постійний контроль за якістю тестів і не дозволяє "протягнути" в основну гілку код без належного покриття.

#### **📌 Що перевіряють:**

*Досвід роботи з сучасними DevOps-практиками. Кандидат повинен розуміти, як автоматизувати перевірку якості та інтегрувати її в процес розробки.*

* * *

### **9 ❓ Питання:**

Що таке часткове покриття гілки (partial branch coverage) у звітах JaCoCo (жовтий колір)? ⭐️

> #### **💬 Відповідь:**
>
> У звітах JaCoCo ромб, що позначає умовний оператор, може бути зафарбований трьома кольорами:
>
> * **Зелений:** 100% Branch Coverage. Всі гілки (і `true`, і `false`) були покриті тестами.
> * **Червоний:** 0% Branch Coverage. Жодна з гілок не була покрита (код узагалі не виконувався).
> * **Жовтий:** **Часткове покриття (Partial Branch Coverage)**.
>
> **Жовтий колір означає**, що під час тестів була виконана **лише одна з можливих гілок** умовної логіки.
>
> **Приклад:**
>
> ```java
> if (user != null && user.isActive()) {
>     // ...
> }
> ```
>
> Ромб буде жовтим, якщо:
>
> * Був тест, де `user` не `null` і `isActive` = `true` (гілка `true`).
> * Але **не було тесту**, де `user` = `null` (перша частина умови `false`), або `user` не `null` але `isActive` = `false` (друга частина умови `false`).
>
> Жовтий колір — це прямий сигнал для розробника, що потрібно додати тести, які перевіряють альтернативні логічні шляхи.

#### **📌 Що перевіряють:**

*Практичний досвід роботи з інструментами аналізу покриття та вміння правильно інтерпретувати їхні звіти.*

* * *

### **10 ❓ Питання:**

Які частини коду зазвичай виключають зі звіту про покриття і чому? ⭐️

> #### **💬 Відповідь:**
>
> Зазвичай зі звіту про покриття виключають код, який не містить бізнес-логіки і тестування якого не приносить великої цінності, а лише штучно завищує або занижує показники.
>
> **Типові кандидати на виключення:**
>
> 1. **Згенерований код:**
>     * Класи, згенеровані інструментами (напр., з WSDL/gRPC).
>     * Код, згенерований Lombok (`@Data`, `@Getter`, `@Setter`, `@EqualsAndHashCode`). Тестувати його немає сенсу, бо ви тестуєте бібліотеку, а не свій код.
>
> 2. **POJO / DTO / Моделі даних:** Прості класи, що містять лише поля та геттери/сеттери.
>
> 3. **Конфігураційні класи:** Класи, що містять конфігурацію Spring, біни тощо, де майже немає виконуваної логіки.
>
> 4. **Головний клас додатку:** Наприклад, клас з `public static void main` у Spring Boot додатку.
>
> **Чому це роблять:**
>
> * Щоб метрика покриття була **більш значущою**. Вона повинна відображати покриття **саме вашої бізнес-логіки**, а не шаблонного коду.
> * Виключення такого коду дозволяє зосередитися на дійсно важливих частинах системи.
>
> У JaCoCo це налаштовується через конфігурацію плагіна в Maven/Gradle, де можна вказати пакети або класи для виключення.

#### **📌 Що перевіряють:**

*Прагматичний підхід та досвід. Кандидат повинен розуміти, що важливо тестувати, а що — ні, і вміти налаштовувати інструменти відповідно.*

* * *

### **11 ❓ Питання:**

Який зв'язок між Code Coverage та рефакторингом? ⭐️

> #### **💬 Відповідь:**
>
> Code Coverage є **індикатором безпеки** для проведення рефакторингу.
>
> **Рефакторинг** — це зміна внутрішньої структури коду без зміни його зовнішньої поведінки.
>
> **Зв'язок:**
>
> 1. **Перед рефакторингом:** Аналіз покриття коду дозволяє оцінити, наскільки безпечно змінювати певну частину системи. Якщо критичний модуль має низьке покриття, рефакторинг є дуже ризикованим, оскільки ви можете щось зламати і не дізнатися про це. Першим кроком має бути **збільшення покриття** для цього модуля.
>
> 2. **Після рефакторингу:** Наявний набір тестів, що забезпечував високе покриття до рефакторингу, тепер слугує "сіткою безпеки". Ви можете запустити тести після змін. Якщо всі вони проходять (і покриття не впало), ви можете бути впевнені, що зовнішня поведінка коду не змінилася.
>
> Високе покриття дає розробникам **сміливість** проводити рефакторинг і покращувати якість коду, не боячись вносити регресійні баги.

#### **📌 Що перевіряють:**

*Розуміння, що інструменти якості (як покриття) не існують у вакуумі, а тісно пов'язані з іншими інженерними практиками (як рефакторинг).*

* * *

### **12 ❓ Питання:**

У вашому звіті JaCoCo ви бачите, що `catch` блок не покритий тестами. Що це означає і як це виправити? ⭐️

> #### **💬 Відповідь:**
>
> Непокритий `catch` блок означає, що **жоден з ваших тестів не спровокував виникнення того винятку**, який цей блок обробляє.
>
> Це може вказувати на одну з двох ситуацій:
>
> 1. **Прогалина в тестуванні:** Ви не написали тест на негативний сценарій, який би привів до цього винятку. Це найпоширеніша причина.
> 2. **"Мертвий" код:** Можливо, виняток, що обробляється в `catch`, вже не може виникнути через зміни в коді, і цей блок є "мертвим кодом", який можна видалити.
>
> **Як це виправити:**
>
> Потрібно **написати новий unit-тест**, який цілеспрямовано змусить код кинути очікуваний виняток.
>
> * **Якщо виняток приходить із залежності:** Використовуйте **Mockito**, щоб змусити мок кинути потрібний виняток.
>
>     ```java
>     when(myRepository.findById(1)).thenThrow(new SQLException());
>     ```
>
> * **Якщо виняток виникає через невалідні дані:** Напишіть тест, що передає ці дані.
>
>     ```java
>     assertThrows(IllegalArgumentException.class, () -> myService.process(null));
>     ```
>
> Після написання такого тесту, ваш `catch` блок стане покритим, і ви будете впевнені, що логіка обробки помилок працює правильно.

#### **📌 Що перевіряють:**

*Вміння аналізувати звіти про покриття та писати тести на негативні сценарії та обробку винятків.*

* * *

### **13 ❓ Питання:**

Чи може `private` метод бути покритий тестами? Чи потрібно це робити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, `private` метод може і повинен бути покритий тестами**, але **опосередковано**.
>
> **Як він покривається:**
> `private` метод покривається тоді, коли виконується публічний метод, який його викликає. JaCoCo не робить різниці між `public` та `private` кодом — він просто відстежує, які інструкції байт-коду були виконані.
>
> **Чи потрібно це робити:**
> Пряме тестування `private` методів (напр., за допомогою рефлексії) є **антипатерном**. Тести повинні перевіряти публічний контракт класу.
>
> Якщо ви бачите у звіті про покриття, що ваш `private` метод не покритий, це означає, що **жоден з тестів для публічних методів не викликав його**. Це сигнал про проблему:
>
> * Або у вас відсутній тест для того сценарію в публічному методі, який задіює цей `private` метод.
> * Або цей `private` метод є **"мертвим кодом"** і його можна безпечно видалити.
>
> **Висновок:** Ви не пишете тести *для* приватних методів. Ви пишете тести *для* публічних методів, які, в свою чергу, повинні забезпечувати покриття приватних.

#### **📌 Що перевіряють:**

*Глибоке розуміння принципів unit-тестування, інкапсуляції та того, як аналізувати покриття в контексті дизайну коду.*

* * *

### **14 ❓ Питання:**

Чому гонитва за 100% покриттям може призвести до написання гірших тестів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Коли 100% покриття стає **жорсткою ціллю (KPI)**, а не інструментом, це може призвести до низки негативних наслідків і погіршення якості тестів.
>
> 1. **Тестування тривіального коду:** Щоб "добити" відсотки, розробники починають писати тести для тривіальних геттерів, сеттерів, простих конструкторів або делегуючих методів. Такі тести не приносять жодної цінності, але збільшують обсяг коду та час виконання.
>
> 2. **Поява тестів без тверджень:** Найпростіший спосіб збільшити покриття — написати тест, який просто викликає метод, але нічого не перевіряє (`assert`). Такий тест є абсолютно марним, але він підвищує метрику.
>
> 3. **Ігнорування важливих, але складних сценаріїв:** Розробники можуть уникати тестування складної бізнес-логіки, тому що це вимагає часу, і натомість писати десяток простих тестів для геттерів, щоб швидко досягти цілі.
>
> 4. **Складні та нечитабельні тести:** Щоб "зачепити" якусь рідкісну гілку в `catch` блоці, розробники можуть писати дуже складні тести з великою кількістю моків, які важко зрозуміти та підтримувати.
>
> 5. **Втрата фокусу:** Команда починає думати про "зелені цифри у звіті", а не про реальну якість продукту та пошук багів. Це приклад **"gaming the metric"** (маніпуляція метрикою).

#### **📌 Що перевіряють:**

*Розуміння людського фактору та організаційних аспектів розробки ПЗ. Кандидат повинен усвідомлювати, що неправильно поставлені цілі можуть призвести до контрпродуктивних результатів.*

* * *

### **15 ❓ Питання:**

Розглянемо вираз: `boolean result = (a && b) || c;`. Скільки тестів мінімально потрібно для 100% **Branch Coverage**? Чи є цього достатньо для повної перевірки логіки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це складне питання, яке показує обмеженість навіть `Branch Coverage`.
>
> **Скільки тестів для 100% Branch Coverage:**
> `Branch Coverage` перевіряє, що вся умова в цілому була `true` хоча б раз і `false` хоча б раз.
> Для 100% покриття достатньо **двох тестів**:
>
> 1. **Для результату `true`**: наприклад, `c = true` (тоді `a` і `b` неважливі).
> 2. **Для результату `false`**: `a = false`, `b = false`, `c = false`.
>
> В обох цих випадках повний вираз був і `true`, і `false`, отже `Branch Coverage` буде 100%.
>
> **Чи достатньо цього для повної перевірки?**
>
> **Абсолютно ні.** Ми не перевірили безліч комбінацій. Наприклад, ми не перевірили випадок, коли `c` є `false`, але `a` і `b` є `true`. Якщо в логіці є помилка, пов'язана саме з оператором `&&`, ми її ніколи не знайдемо.
>
> **Що потрібно для кращої перевірки?**
>
> Для перевірки такої складної умови потрібно використовувати більш сильні критерії покриття, наприклад, **MC/DC (Modified Condition/Decision Coverage)**. Це критерій, який вимагає перевірити, що кожна **окрема умова** (`a`, `b`, `c`) незалежно впливає на кінцевий результат.
>
> Для нашого випадку, щоб задовольнити MC/DC, знадобиться більше тестів. Наприклад, для перевірки `a`:
>
> * `a=T, b=T, c=F` -> `result = T`
> * `a=F, b=T, c=F` -> `result = F`
>
> Ця пара тестів показує, що зміна лише `a` впливає на результат. Такі пари потрібно підібрати для кожної умови.
>
> **Висновок:** Навіть 100% `Branch Coverage` не гарантує повної перевірки складної логічної умови.

#### **📌 Що перевіряють:**

*Глибокі теоретичні знання в області тестування ПЗ. Кандидат повинен розуміти обмеження стандартних метрик та знати про існування більш суворих критеріїв, що використовуються у системах високої надійності.*

* * *
