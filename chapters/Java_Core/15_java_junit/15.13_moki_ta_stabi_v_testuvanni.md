---
layout: default
title: "Моки та Стаби в тестуванні"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 13
---

# Моки та Стаби в тестуванні

* короткий опис: `Stub` – це тестовий об'єкт, який повертає наперед визначені значення, призначений для надання даних. `Mock` – це тестовий об'єкт, який імітує поведінку реального об'єкта та дозволяє перевіряти взаємодію з ним (виклики методів, параметри, порядок).

* поняття: `Stub`, `Mock`, `Mockito` (популярний фреймворк), тестові дублери, `Spy` (частковий мок).

* приклади: `when(service.get()).thenReturn(value);` (stub), `verify(service).callMethod();` (mock).

* ключові моменти: Використовуються для ізоляції Unit-тестів від зовнішніх залежностей. `Mockito` – найпоширеніший фреймворк для мокінгу.

* що важливо знати на співбесіді: Різниця між `mock` та `stub`, коли використовувати `Mockito`.

### **✅ Офіційне визначення:**

* **Stub** — обʼєкт, який повертає наперед визначені значення. Його мета — **надати дані**.
* **Mock** — обʼєкт, який імітує поведінку справжнього обʼєкта і дозволяє **перевіряти взаємодію** з ним (виклики, параметри, порядок викликів).

### **🧠 Простими словами:**

* **Стаб** — це "актовий гість", який каже те, що ти йому прописав.
* **Мок** — це "детектив", який записує, хто, коли і як з ним говорив.

---

## **Основне**

📦 **Типи тестових обʼєктів** (за Мартіном Фаулером):

|    Тип    |                         Мета                          |
|:---------:|:-----------------------------------------------------:|
| **Dummy** |        Пустий обʼєкт, просто щоб не було null         |
| **Stub**  |         Повертає заздалегідь задану відповідь         |
| **Fake**  |      Має просту реалізацію (напр. in-memory DB)       |
| **Mock**  |              Валідує очікувану взаємодію              |
|  **Spy**  | Справжній обʼєкт, але з можливістю перевірки викликів |

🛠 **Основні фреймворки**:

* **Mockito** (найпопулярніший)
* EasyMock, JMock (менш популярні)
* PowerMock (для складних випадків, deprecated у JUnit 5\)
* JUnit 5 з інтеграцією **`@ExtendWith(MockitoExtension.class)`**

---

## **Пояснення на пальцях (💡)**

* Уяви, що тестуєш кавоварку. Замість справжньої бази даних чи апаратного блоку — підставляєш **мок**.
* Справжній unit test має перевіряти **лише одну одиницю логіки**. Якщо твій клас тягне HTTP-запит або лізе в базу —
  мокай\!
* **Stub** використовується, коли ти просто хочеш, щоб метод **`getTemperature()`** повернув 95 і все.
* **Mock** — коли важливо перевірити, що **`sendEmail()`** був викликаний з параметром **`"Warning"`**.

---

## **Корисні приклади (🧪)**

📌 **Mockito: мокінг і перевірка викликів**

```java
@ExtendWith(MockitoExtension.class)
class CoffeeMachineTest {

    @Mock
    WaterHeater heater;

    @InjectMocks
    CoffeeMachine machine;

    @Test
    void shouldHeatWater() {
        machine.makeCoffee();

        verify(heater).heatWater(); // перевірка виклику
    }
}
```

📌 **Stub-поведінка: повертаємо фіксоване значення**

```java
when(heater.getTemperature()).thenReturn(95);

assertEquals(95, machine.getCurrentTemperature());
```

📌 **Spy: частково справжній обʼєкт**

```java
@Spy
List<String> spyList = new ArrayList<>();

@Test
void testSpy() {
    spyList.add("Hello");
    verify(spyList).add("Hello");
}
```

---

## **Порівняння (⚖️)**

|     Характеристика     |             Stub             |            Mock             |
|:----------------------:|:----------------------------:|:---------------------------:|
|        **Мета**        | Повернути фіксоване значення |    Перевірити взаємодію     |
| **Перевірка викликів** |              ❌               |              ✅              |
| **Реалізація логіки**  |    Мінімальна / фіксована    | Повністю підміняє поведінку |
|      **Приклад**       |  **`when().thenReturn()`**   |       **`verify()`**        |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"SMaSH"** – моки, стаби і компанія:

* **S** — Stub --> Simple return
* **M** — Mock --> Monitor interaction
* **a** — and
* **S** — Spy --> Semi-real
* **H** — Helper --> test doubles as test helpers

---

## **🟩 Підсумок:**

* **Моки** дозволяють перевіряти взаємодію з об’єктами.
* **Стаби** підставляють фіксовані значення, щоб уникнути залежностей.
* Фреймворк №1 — **Mockito**, інтегрується з JUnit 5\.
* Unit-тести мають бути **ізольовані** — мокайте все, що не є "unit".
* На інтерв’ю обов’язково покажи, що знаєш **відмінність і мету** кожного типу замінника.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке "тестовий дублер" (Test Double), і чому в unit-тестуванні необхідні моки та стаби?

> #### **💬 Відповідь:**
>
> **Тестовий дублер** — це загальний термін для будь-якого об'єкта, який замінює справжній виробничий об'єкт у тесті.
>
> **Чому вони необхідні:**
> Головний принцип unit-тестування — це **ізоляція**. Ми хочемо перевірити лише один конкретний "юніт" (метод, клас) без впливу його залежностей. Справжні залежності (наприклад, база даних, мережевий клієнт, інший сервіс) роблять тести:
>
> * **Повільними:** Звернення до бази даних або по мережі займає багато часу.
> * **Нестабільними:** Тест може впасти через проблеми з мережею або недоступність БД, а не через помилку в коді.
> * **Непередбачуваними:** Результат тесту може залежати від даних у зовнішній системі.
>
> **Моки та стаби** — це два найпоширеніші типи тестових дублерів, які вирішують цю проблему. Вони замінюють справжні залежності і дозволяють повністю контролювати середовище, роблячи unit-тест швидким, надійним та ізольованим.

#### **📝 Аналогія:**

Уявіть, що ви тестуєте автомобільний двигун на стенді. Ви не ставите його в реальний автомобіль і не їдете по місту. Замість цього, ви підключаєте до нього симулятори, які імітують подачу палива та навантаження (це і є ваші моки та стаби). Це дозволяє перевірити двигун в ізоляції.

#### **📌 Що перевіряють:**

*Розуміння фундаментального принципу ізоляції в unit-тестуванні. Кандидат повинен знати, навіщо взагалі потрібні тестові дублери.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між моком (Mock) і стабом (Stub)?

> #### **💬 Відповідь:**
>
> Хоча обидва є тестовими дублерами, вони мають різну мету. Головна різниця полягає в тому, **що** саме ви перевіряєте: **стан** чи **поведінку**.
>
> * **Stub (Стаб):**
>   * **Призначення:** Надати тесту заздалегідь визначені дані (симулювати **стан**). Його головна задача — відповісти на виклики з фіксованими значеннями, щоб тестований код міг продовжити роботу.
>   * **Що перевіряється:** Ми не перевіряємо сам стаб. Ми використовуємо його, щоб перевірити результат роботи нашого об'єкта.
>   * **Фокус:** "Коли мій код запитає Х, дай йому У".
>
> * **Mock (Мок):**
>   * **Призначення:** Перевірити **взаємодію** між об'єктами (симулювати **поведінку**). Мок "запам'ятовує", які його методи були викликані, скільки разів і з якими параметрами.
>   * **Що перевіряється:** У кінці тесту ми робимо твердження (`verify`) щодо самого моку — чи відбулися очікувані виклики.
>   * **Фокус:** "Я очікую, що мій код викличе метод Х рівно один раз".
>
> **Коротко:** стаб допомагає перевірити результат (**стан**), мок — перевірити взаємодію (**поведінку**).

#### **📝 Аналогія:**

* **Стаб** — це актор, якому ви дали сценарій з однією фразою: "На питання 'хто ти?' відповідай 'я робот'". Ви використовуєте його, щоб перевірити реакцію головного героя на цю фразу.
* **Мок** — це шпигун, якого ви послали на зустріч. В кінці ви не питаєте його, що він сказав. Ви питаєте: "Скільки разів до тебе звернулися? Чи запитали тебе секретне кодове слово?".

#### **📌 Що перевіряють:**

*Чітке розуміння різниці між тестуванням стану (state-based) та тестуванням поведінки (behavior-based). Це одне з найважливіших питань по цій темі.*

* * *

### **3 ❓ Питання:**

Що таке Mockito і як його підключити до проекту з JUnit 5?

> #### **💬 Відповідь:**
>
> **Mockito** — це найпопулярніший open-source фреймворк у Java для створення моків, стабів та інших тестових дублерів. Він надає дуже простий та читабельний API для імітації залежностей у unit-тестах.
>
> **Основні можливості:**
>
> * Створення моків та стабів (`mock()`, `@Mock`).
> * Визначення поведінки (стаббінг): `when(...).thenReturn(...)`.
> * Перевірка взаємодій: `verify(...)`.
>
> **Як підключити до JUnit 5:**
>
> Найпростіший спосіб — використати **`MockitoExtension`**.
>
> 1. Додати залежності `mockito-core` та `mockito-junit-jupiter` у ваш `pom.xml` або `build.gradle`.
> 2. Анотувати ваш тестовий клас `@ExtendWith(MockitoExtension.class)`.
> 3. Позначити поля, які мають бути моками, анотацією `@Mock`.
>
> Це розширення автоматично ініціалізує всі поля з анотацією `@Mock` перед кожним тестом.

#### **📝 Приклад:**

```java
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class MyServiceTest {

    @Mock
    private UserRepository userRepository; // Mockito створить мок цього репозиторію

    @Test
    void testSomething() {
        // Тепер userRepository є моком, готовим до використання
        when(userRepository.findById(1L)).thenReturn(new User("Test"));
        // ...
    }
}
```

#### **📌 Що перевіряють:**

*Практичні знання основного інструментарію для мокування. Кандидат повинен знати, як налаштувати середовище для написання тестів з моками.*

* * *

### **4 ❓ Питання:**

Як за допомогою Mockito створити "стаб" (Stub), який буде повертати фіксоване значення?

> #### **💬 Відповідь:**
>
> Для створення стаб-поведінки в Mockito використовується ланцюжок методів `when(...).thenReturn(...)`.
>
> **Структура:**
> `when(mockObject.someMethod(arguments)).thenReturn(valueToReturn);`
>
> * **`when(...)`**: Вказує, для якого виклику методу на мок-об'єкті ми хочемо визначити поведінку.
> * **`thenReturn(...)`**: Вказує, яке саме значення потрібно повернути, коли цей метод буде викликаний.
>
> Цей підхід дозволяє "запрограмувати" мок на повернення будь-яких необхідних даних, роблячи тест передбачуваним.

#### **📝 Приклад:**

```java
@Mock
private UserDao userDao;

@Test
void findUserById_WhenUserExists_ShouldReturnUser() {
    // Arrange (готуємо стаб)
    User fakeUser = new User(1L, "John Doe");
    // "Навчаємо" мок: коли буде викликано userDao.findById(1L), повернути fakeUser
    when(userDao.findById(1L)).thenReturn(Optional.of(fakeUser));

    UserService userService = new UserService(userDao);

    // Act
    Optional<User> foundUser = userService.findUserById(1L);

    // Assert
    assertTrue(foundUser.isPresent());
    assertEquals("John Doe", foundUser.get().getName());
}
```

#### **📌 Що перевіряють:**

*Вміння використовувати базовий API Mockito для налаштування поведінки моків. Це фундаментальна навичка для написання unit-тестів.*

* * *

### **5 ❓ Питання:**

Як за допомогою Mockito перевірити, що певний метод був викликаний (тестування поведінки)?

> #### **💬 Відповідь:**
>
> Для перевірки взаємодії (тобто, для використання об'єкта як "моку") використовується метод **`verify()`**. Він дозволяє перевірити, чи був викликаний певний метод на мок-об'єкті, скільки разів, і з якими аргументами.
>
> **Синтаксис:**
> `verify(mockObject, mode).someMethod(arguments);`
>
> * **`verify(mockObject)`**: Починає перевірку взаємодії для даного мок-об'єкта.
> * **`mode` (опціонально):** Вказує, скільки разів очікувався виклик. Наприклад:
>   * `times(1)` (за замовчуванням)
>   * `times(2)`
>   * `never()`
>   * `atLeast(1)`
> * **`someMethod(arguments)`**: Метод та аргументи, виклик яких ми перевіряємо.

#### **📝 Приклад:**

Уявіть, що при реєстрації користувача має відправлятися емейл.

```java
@Mock
private EmailService emailService;
@Mock
private UserRepository userRepository;

@Test
void registerUser_ShouldSendWelcomeEmail() {
    UserService userService = new UserService(userRepository, emailService);
    User newUser = new User("test@test.com");
    
    // Act
    userService.register(newUser);
    
    // Assert (перевірка поведінки)
    // Перевіряємо, що метод sendWelcomeEmail був викликаний рівно 1 раз з email'ом нашого користувача
    verify(emailService, times(1)).sendWelcomeEmail("test@test.com");
}
```

#### **📌 Що перевіряють:**

*Вміння тестувати не тільки результат, а й побічні ефекти та взаємодію між компонентами, що є основною метою моків.*

* * *

### **6 ❓ Питання:**

Що таке "Spy" в Mockito і чим він відрізняється від "Mock"? ⭐️

> #### **💬 Відповідь:**
>
> Це важливе питання, яке перевіряє глибше розуміння інструментів.
>
> * **`@Mock`:**
>   * **Створює повну імітацію** (муляж) класу. Усі його методи є "порожніми" і за замовчуванням повертають `null`, `0` або порожні колекції. Виклик будь-якого методу не призведе до виконання реальної логіки.
>   * Використовується для повної ізоляції від зовнішніх залежностей.
>
> * **`@Spy`:**
>   * **Створює "шпигуна"**, який є **обгорткою навколо реального об'єкта**.
>   * За замовчуванням, при виклику будь-якого методу на `Spy`-об'єкті, буде викликано **реальний метод** цього об'єкта.
>   * `Spy` дозволяє "шпигувати" за реальним об'єктом (перевіряти виклики методів за допомогою `verify`) та, за потреби, **частково підміняти** його поведінку (за допомогою `doReturn(...).when(...)`).
>
> **Коли використовувати `Spy`:**
>
> * Коли ви хочете протестувати один метод класу, але при цьому хочете, щоб інші його методи працювали по-справжньому.
> * Коли ви працюєте з легасі-кодом, який важко повністю замінити на мок.
>
> **Небезпека `Spy`:** Оскільки він викликає реальні методи, це може призвести до небажаних побічних ефектів, якщо не бути обережним. Тому `@Mock` є більш безпечним і поширеним вибором.

#### **📌 Що перевіряють:**

*Знання просунутих можливостей Mockito. Кандидат повинен розуміти різницю між повною імітацією та частковою і знати, коли яку застосовувати.*

* * *

### **7 ❓ Питання:**

Що таке Argument Matchers (`any()`, `anyString()`, `eq()`) і для чого вони потрібні? ⭐️

> #### **💬 Відповідь:**
>
> **Argument Matchers (Зіставлячі аргументів)** — це спеціальні методи в Mockito (напр., з класу `org.mockito.ArgumentMatchers`), які використовуються всередині `when(...)` та `verify(...)` для гнучкої перевірки аргументів.
>
> **Проблема, яку вони вирішують:**
> Іноді ми не можемо або не хочемо перевіряти на точну рівність аргументу. Наприклад:
>
> * Ми не знаємо точне значення, яке буде передано (напр., об'єкт, створений всередині методу).
> * Нам неважливо, яке саме значення було передано, а важливо лише те, що воно певного типу.
>
> **Основні матчери:**
>
> * **`any()`:** Відповідає будь-якому об'єкту.
> * **`any(Class<T> type)`:** Відповідає будь-якому об'єкту вказаного типу (напр., `any(User.class)`).
> * **`anyString()`, `anyInt()`, `anyList()`:** Типізовані версії для поширених класів.
> * **`eq(value)`:** Використовується, коли потрібно змішати матчери з конкретними значеннями.
>
> **Важливе правило:** Якщо ви використовуєте матчер для одного аргументу, ви повинні використовувати матчери для **всіх** аргументів цього методу.

#### **📝 Приклад:**

```java
// Перевіряємо, що метод save був викликаний з будь-яким об'єктом User
verify(userRepository).save(any(User.class));

// Навчаємо мок повертати "OK" для будь-якого рядка
when(validator.validate(anyString())).thenReturn("OK");
```

#### **📌 Що перевіряють:**

*Практичний досвід роботи з Mockito. Розуміння матчерів є необхідним для написання гнучких тестів для реальних сценаріїв.*

* * *

### **8 ❓ Питання:**

Як протестувати `void`-метод, який взаємодіє із залежністю? ⭐️

> #### **💬 Відповідь:**
>
> Оскільки `void`-метод нічого не повертає, ми не можемо перевірити його результат (`state-based testing`). Замість цього ми повинні перевіряти його **побічні ефекти** або **поведінку** (`behavior-based testing`).
>
> Якщо `void`-метод викликає методи своїх залежностей, це ідеальний випадок для використання **моків** та перевірки взаємодії за допомогою **`verify()`**.
>
> **Алгоритм тестування:**
>
> 1. Замокати залежність.
> 2. Викликати наш `void`-метод.
> 3. Використовувати `verify()` на моку, щоб переконатися, що наш метод викликав очікуваний метод на залежності з правильними параметрами.

#### **📝 Приклад:**

```java
@Mock
private Logger mockLogger;

@Test
void processOrder_whenOrderIsInvalid_shouldLogError() {
    OrderService service = new OrderService(mockLogger);
    Order invalidOrder = new Order(-1);
    
    // Act (викликаємо void-метод)
    service.processOrder(invalidOrder);

    // Assert (перевіряємо поведінку - чи був викликаний логгер)
    verify(mockLogger).error("Order is invalid: " + invalidOrder.getId());
}
```

#### **📌 Що перевіряють:**

*Здатність застосовувати техніку тестування поведінки. Це ключова навичка, оскільки багато методів у реальних системах не повертають значень, а змінюють стан або взаємодіють з іншими компонентами.*

* * *

### **9 ❓ Питання:**

Як за допомогою Mockito симулювати ситуацію, коли метод залежності кидає виняток? ⭐️

> #### **💬 Відповідь:**
>
> Для цього використовується метод **`thenThrow(...)`** замість `thenReturn(...)` у ланцюжку `when()`.
>
> Це дозволяє перевірити, як наш код реагує на помилки, що виникають у його залежностях (наприклад, `SQLException` з бази даних або `IOException` з мережі).
>
> **Синтаксис:**
> `when(mockObject.someMethod()).thenThrow(new SomeException());`
> або
> `when(mockObject.someMethod()).thenThrow(SomeException.class);`

#### **📝 Приклад:**

```java
@Mock
private DatabaseConnection dbConnection;

@Test
void getData_whenDbIsUnavailable_shouldThrowCustomException() {
    DataService dataService = new DataService(dbConnection);
    
    // Arrange: Навчаємо мок кидати SQLException при спробі отримати дані
    when(dbConnection.fetchData()).thenThrow(new SQLException("Connection failed"));

    // Act & Assert: Перевіряємо, що наш сервіс правильно обробляє SQLException
    // і загортає його у власний кастомний виняток.
    assertThrows(DataAccessException.class, () -> {
        dataService.getData();
    });
}
```

#### **📌 Що перевіряють:**

*Вміння тестувати логіку обробки помилок. Надійний код повинен коректно реагувати на збої у своїх залежностях.*

* * *

### **10 ❓ Питання:**

Поясніть різницю між `@Mock` та `@InjectMocks`. Коли їх використовувати разом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дві специфічні анотації Mockito, які використовуються разом для автоматизації створення та ін'єкції моків.
>
> * **`@Mock`:**
>   * **Що робить:** Створює **мок-об'єкт** для поля, яке нею позначено. Цей об'єкт є повністю імітованим і не має реальної логіки.
>   * Призначення: для імітації **залежностей**.
>
> * **`@InjectMocks`:**
>   * **Що робить:** Створює **реальний екземпляр** класу для поля, яке нею позначено, і намагається **автоматично ін'єктувати** в нього всі поля, позначені `@Mock` або `@Spy` в цьому ж тестовому класі.
>   * Призначення: для **об'єкта, що тестується** (System Under Test, SUT).
>
> **Коли використовувати разом:**
> Їх використовують разом, щоб автоматизувати патерн Dependency Injection у тестах. Це позбавляє необхідності вручну створювати екземпляр тестованого об'єкта і передавати моки в його конструктор.
>
> ```java
> @ExtendWith(MockitoExtension.class)
> class OrderServiceTest {
> 
>     @Mock // 1. Створити мок для залежності
>     private OrderRepository mockOrderRepository;
> 
>     @InjectMocks // 2. Створити РЕАЛЬНИЙ OrderService і вставити в нього mockOrderRepository
>     private OrderService orderService; 
> 
>     @Test
>     void testPlaceOrder() {
>         // Тепер orderService готовий до використання з вбудованим моком
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Практичний досвід роботи з Mockito. Знання `@InjectMocks` вказує на те, що розробник вміє писати чисті та лаконічні тести для класів з багатьма залежностями.*

* * *

### **11 ❓ Питання:**

Як перевірити, що на мок-об'єкті **не було жодних** викликів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього в Mockito є спеціальний метод перевірки: **`verifyNoInteractions(...)`**.
>
> Він перевіряє, що для даного мок-об'єкта не було викликано жодного методу протягом виконання тесту.
>
> **Коли це корисно:**
>
> Це дуже корисно для перевірки умовної логіки. Наприклад, якщо у вас є умова, за якою не повинна відбуватися певна дія (напр., відправка емейлу для неактивного користувача), ви можете використати цей метод, щоб гарантувати, що дія справді не відбулася.

#### **📝 Приклад:**

```java
@Mock
private EmailService mockEmailService;

@Test
void processUser_whenUserIsInactive_shouldNotSendEmail() {
    UserService userService = new UserService(mockEmailService);
    User inactiveUser = new User(false);
    
    // Act
    userService.processUser(inactiveUser);
    
    // Assert: Перевіряємо, що з EmailService не було ЖОДНИХ взаємодій
    verifyNoInteractions(mockEmailService);
}
```

#### **Коментар:**

Також існує `verify(mock, never()).someMethod()` для перевірки, що конкретний метод не був викликаний. `verifyNoInteractions` є більш суворою перевіркою.

#### **📌 Що перевіряють:**

*Знання повного спектру можливостей Mockito для тестування поведінки, включаючи перевірку відсутності дій.*

* * *

### **12 ❓ Питання:**

У чому проблема з мокуванням статичних методів, `private` методів або конструкторів? Чи варто це робити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Проблема:**
> Код, який покладається на статичні методи, приватні методи або створює об'єкти через `new`, є **сильно зв'язаним (tightly coupled)** і, як наслідок, **погано тестованим**.
>
> * Стандартний Mockito **не може** мокати статичні методи, приватні методи або перехоплювати виклик конструктора, оскільки він працює шляхом створення проксі-класів, що не дозволяє змінювати таку поведінку.
>
> **Чи варто це робити:**
> **Ні, це вважається антипатерном.** Потреба мокати такі речі — це сильний сигнал, що ваш **код має проблеми з дизайном**.
>
> * **Статичні методи:** Часто приховують залежність. Замість `Utils.doSomething()`, краще передавати екземпляр `Utils` через конструктор.
> * **Приватні методи:** Їх не потрібно тестувати напряму. Тестуйте їх опосередковано через публічний API класу.
>
> **Що робити, якщо змінити код неможливо (легасі):**
> Для таких крайніх випадків існують більш потужні (але й небезпечні) бібліотеки, як-от **PowerMock**, яка розширює Mockito і дозволяє мокати статичні методи, конструктори тощо, маніпулюючи байт-кодом.
>
> Однак, PowerMock — це інструмент "останньої надії". Найкращим рішенням завжди буде **рефакторинг** коду до більш тестованого дизайну.

#### **📌 Що перевіряють:**

*Зрілість інженера. Кандидат повинен розуміти, що деякі проблеми тестування є наслідком поганого дизайну, і першим інстинктом має бути виправлення дизайну, а не використання "хаків".*

* * *

### **13 ❓ Питання:**

Що таке `ArgumentCaptor` і для якої задачі він призначений? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`ArgumentCaptor`** — це просунутий інструмент в Mockito, який дозволяє "захопити" об'єкт, переданий як аргумент в метод моку, для того, щоб виконати для нього детальніші перевірки (`assert`).
>
> **Коли він потрібен:**
>
> Коли вам недостатньо просто перевірити, що метод був викликаний (`verify`). Вам потрібно перевірити **вміст** об'єкта, переданого в цей метод, особливо якщо цей об'єкт був **створений всередині** методу, який ви тестуєте, і у вас немає прямого доступу до нього.
>
> **Процес:**
>
> 1. Створити `ArgumentCaptor` для потрібного типу.
> 2. У методі `verify` передати `captor.capture()` замість аргументу.
> 3. Після `verify`, отримати захоплене значення за допомогою `captor.getValue()`.
> 4. Виконати будь-які твердження на захопленому об'єкті.

#### **📝 Приклад:**

```java
@Mock private UserRepository userRepository;
private UserService userService;

@Test
void registerUser_shouldSaveUserWithCorrectData() {
    userService = new UserService(userRepository);

    // Створюємо captor для об'єкта User
    ArgumentCaptor<User> userCaptor = ArgumentCaptor.forClass(User.class);
    
    // Act
    userService.register("test@test.com", "password");
    
    // Assert: перевіряємо, що метод save був викликаний, і захоплюємо аргумент
    verify(userRepository).save(userCaptor.capture());
    
    // Отримуємо захоплений об'єкт
    User savedUser = userCaptor.getValue();
    
    // Робимо детальні перевірки на захопленому об'єкті
    assertEquals("test@test.com", savedUser.getEmail());
    assertTrue(savedUser.isActive());
    assertNotNull(savedUser.getCreationDate());
}
```

#### **📌 Що перевіряють:**

*Експертне володіння Mockito. `ArgumentCaptor` є потужним інструментом для тестування складних сценаріїв, і його знання вказує на глибокий досвід.*

* * *

### **14 ❓ Питання:**

Що краще використовувати для перевірки викликів: `verify(mock).method()` чи `Mockito.inOrder()`? Коли потрібен `InOrder`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`verify(mock).method()`** перевіряє, що виклик відбувся, але **не гарантує порядок** відносно інших викликів. Якщо у вас було два виклики `A` і `B`, звичайний `verify` пройде успішно незалежно від того, чи було викликано `A` до `B` чи навпаки.
>
> * **`Mockito.inOrder()`** використовується тоді, коли **порядок викликів є критично важливим** для бізнес-логіки. `InOrder` дозволяє перевірити, що кілька методів були викликані в суворо визначеній послідовності.
>
> **Коли потрібен `InOrder`:**
>
> * При тестуванні протоколів, де порядок операцій важливий (напр., `connect() -> authenticate() -> sendData() -> disconnect()`).
> * При тестуванні складних транзакційних операцій (напр., `begin() -> execute() -> commit()`).
>
> **Як використовувати:**
>
> 1. Створити екземпляр `InOrder`.
> 2. Викликати на ньому `verify()` для кожного моку в потрібному порядку.

#### **📝 Приклад:**

```java
@Test
void testTransactionFlow() {
    // ...
    service.executeTransaction();
    
    InOrder inOrder = Mockito.inOrder(connection, statement);

    // Перевіряємо, що спочатку відкрилося з'єднання, а вже потім виконався запит
    inOrder.verify(connection).open();
    inOrder.verify(statement).executeQuery();
}
```

**Висновок:** Використовуйте `InOrder` тільки тоді, коли порядок є частиною контракту методу. У більшості випадків звичайного `verify` достатньо.

#### **📌 Що перевіряють:**

*Розуміння нюансів тестування поведінки. Здатність визначити, коли важливий порядок взаємодії, свідчить про глибокий аналіз коду.*

* * *

### **15 ❓ Питання:**

Яким чином Mockito створює мок-об'єкти "під капотом"? Поясніть на високому рівні. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Mockito створює мок-об'єкти, використовуючи техніку **динамічного створення проксі-об'єктів (dynamic proxies)** під час виконання.
>
> **Основні підходи, які використовує Mockito:**
>
> 1. **Проксі на основі інтерфейсів (JDK Dynamic Proxies):**
>     * Це стандартний механізм Java. Якщо ви мокаєте **інтерфейс**, Mockito створює динамічний клас "на льоту", який реалізує цей інтерфейс. Всі виклики методів на цьому проксі-об'єкті перехоплюються обробником Mockito (`InvocationHandler`), який вирішує, що робити: повернути заздалегідь визначене значення (`when().thenReturn()`) або просто записати факт виклику для подальшої перевірки (`verify()`).
>
> 2. **Маніпуляція байт-кодом (з використанням бібліотеки Byte Buddy):**
>     * Якщо ви мокаєте **клас**, JDK-проксі не підходять (оскільки вони працюють лише з інтерфейсами). У цьому випадку Mockito використовує бібліотеку для маніпуляції байт-кодом, як-от **Byte Buddy**.
>     * Byte Buddy дозволяє створювати **підклас** вашого класу під час виконання. Цей підклас перевизначає всі методи оригінального класу. Коли ви викликаєте метод на такому моку, насправді виконується код з цього динамічно згенерованого підкласу, який знову ж таки передає управління логіці Mockito.
>     * Через це, Mockito **не може мокати `final`-класи та `final`-методи**, оскільки їх не можна успадкувати чи перевизначити.
>
> **Inline Mock Maker (сучасний підхід):**
> У нових версіях Mockito з'явився "inline mock maker", який використовує інструментацію JVM, щоб змінювати класи напряму, що дозволяє мокати навіть `final`-класи та методи. Проте це більш "магічний" і потенційно крихкий механізм.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як працюють фреймворки під капотом. Це питання демонструє дуже глибокі знання не тільки Mockito, а й Java в цілому (динамічні проксі, маніпуляція байт-кодом).*

* * *
