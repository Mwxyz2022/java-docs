---
layout: default
title: "Структура та іменування тестів"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 8
---

# Структура та іменування тестів

* короткий опис: Практика створення зрозумілих, послідовних назв тестових методів і класів ( `<methodName>_<state>_<expectedBehavior>`), а також організація тестового коду за принципом `Given-When-Then` ( `Arrange-Act-Assert`).

* поняття: Naming conventions, `methodName_StateUnderTest_ExpectedBehavior`, `Given-When-Then` (або `Arrange-Act-Assert`).

* приклади: `login_InvalidPassword_ShouldFail()`, `deposit_ValidAmount_ShouldIncreaseBalance()` (із розділенням `Given/When/Then`).

* ключові моменти: Підвищує читабельність, підтримуваність, самодокументованість тестів та полегшує діагностику помилок.

* що важливо знати на співбесіді: Рекомендовані правила іменування, структура тесту.

### **✅ Офіційне визначення:**

**Іменування тестів** — це практика створення **зрозумілих, послідовних назв тестових методів і класів**, яка дозволяє
легко читати, підтримувати та діагностувати тести.

### **🧠 Простими словами:**

Це як давати дітям нормальні імена замість "X" — коли тест провалиться, ти хочеш одразу знати, що він перевіряє і чому впав

---

## **Основні концепції / правила**

|        Елемент         |                                Опис                                |
|:----------------------:|:------------------------------------------------------------------:|
| **Формат імені тесту** |          **`methodName_StateUnderTest_ExpectedBehavior`**          |
|     **Описовість**     |      Назва має бути **людською**, зрозумілою навіть без коду       |
|  **Структура тесту**   |               Given–When–Then або Arrange–Act–Assert               |
| **Іменування класів**  | **`ClassNameTest`**, **`ClassNameTests`**, або **`TestClassName`** |
|  **Консистентність**   |            Уся команда має **тримати одну стилістику**             |

---

## **Пояснення на пальцях (💡)**

* Назва **`calculateTax_NegativeIncome_ShouldReturnZero()`** одразу говорить **що тестується, за яких умов і що має
  статись**.
* Гарна структура тесту допомагає уникати «магії всередині» — усе видно з назви й кроків.
* Клас **`OrderServiceTest`** — очевидно, тестує **`OrderService`**.
* Якщо кожен пише по-різному — буде як на Tower of Babel Build \#2025 🏗️

---

## **Корисні приклади (🧪)**

### **🔹 Структурована назва тесту**

```java
@Test
void withdraw_InsufficientFunds_ShouldThrowException() {
    BankAccount acc = new BankAccount(50);
    assertThrows(IllegalStateException.class, () -> acc.withdraw(100));
}
```

---

### **🔹 Формат Given–When–Then**

```java
@Test
void deposit_ValidAmount_ShouldIncreaseBalance() {
    // Given
    BankAccount acc = new BankAccount(100);

    // When
    acc.deposit(50);

    // Then
    assertEquals(150, acc.getBalance());
}
```

---

### **🔹 Клас `OrderServiceTest`**

```java
public class OrderServiceTest {
    // tests for OrderService class
}
```

---

## **Порівняння (⚖️)**

|                 Підхід                  |   Приклад назви   | Читабельність | Розбір при падінні | Консистентність |
|:---------------------------------------:|:-----------------:|:-------------:|:------------------:|:---------------:|
|              **`test1()`**              |       Немає       |       ❌       |         ❌          |        ❌        |
|          **`testWithdraw()`**           |       Легка       |      ⚠️       |         ⚠️         |       ⚠️        |
| **`withdraw_ZeroBalance_ShouldFail()`** | Найкращий варіант |       ✅       |         ✅          |        ✅        |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“MCECC” — Маємо Чіткий Еталон Конвенційних Кодів**

* **M**ethodName\_State\_Expected
* **C**onsistent naming
* **E**xpressive names
* **C**lear structure (AAA / Given–When–Then)
* **C**lassNameTest

Пам’ятай: якщо назва тесту довша, ніж обідній список, але зрозуміла — **це нормально** 😄

---

## **🟩 Підсумок:**

Грамотно і структуровано названі тести — це **суперсила** для читабельності, підтримки й командної роботи. Пам’ятай:

* Завжди використовуй описові назви.
* Дотримуйся принципу **`method_condition_expectedResult`**.
* Вибери структуру **`Given–When–Then`** або **`AAA`**.
* Тримай єдину стилістику в команді.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Чому правильне іменування та структура тестів є настільки важливими?

> #### **💬 Відповідь:**
>
> Правильне іменування та структура — це не просто "красивий код", а критично важлива інженерна практика, яка надає кілька переваг:
>
> 1. **Тести як документація:** Добре названий тест (`withdraw_InsufficientFunds_ShouldThrowException`) миттєво пояснює, як система повинна поводитись у певній ситуації, без необхідності читати виробничий код. Це жива, завжди актуальна документація.
> 2. **Швидка діагностика збоїв:** Коли тест падає, його назва одразу дає зрозуміти, яка саме функціональність зламалася і за яких умов. Це значно скорочує час на налагодження.
> 3. **Легкість підтримки та рефакторингу:** Структуровані та зрозумілі тести легко читати, розуміти та оновлювати, коли змінюється бізнес-логіка. Це знижує вартість підтримки коду.
> 4. **Покращення дизайну коду:** Намагаючись написати чистий та ізольований тест, розробники часто змушені писати більш тестований, а отже, більш якісний та слабо зв'язаний виробничий код.

#### **📝 Аналогія:**

Погано названий тест (`test1()`) — це як непідписаний запобіжник у щитку. Коли щось ламається, доводиться перевіряти всі, щоб знайти проблему. Добре названий тест — це як підписаний запобіжник "Кухня, розетки": ви одразу знаєте, куди дивитися.

#### **📌 Що перевіряють:**

*Розуміння того, що тести — це не просто перевірки, а важлива частина кодової бази, яка має бути такою ж читабельною та підтримуваною, як і виробничий код.*

* * *

### **2 ❓ Питання:**

Яка є найпоширеніша конвенція іменування тестових методів? Поясніть її структуру.

> #### **💬 Відповідь:**
>
> Найпоширенішою і однією з найефективніших конвенцій є **`methodName_StateUnderTest_ExpectedBehavior`** (метод_стан_очікуванаПоведінка).
>
> Ця структура розбиває назву тесту на три логічні частини:
>
> 1. **`methodName` (Що ми тестуємо?):** Назва методу або функціоналу, що піддається тестуванню.
>     * Приклад: `deposit`, `calculateDiscount`
>
> 2. **`StateUnderTest` (За яких умов?):** Опис стану системи або вхідних даних, за яких проводиться тест.
>     * Приклад: `NegativeAmount`, `WithValidUser`
>
> 3. **`ExpectedBehavior` (Що ми очікуємо?):** Опис очікуваного результату або поведінки.
>     * Приклад: `ShouldThrowException`, `ShouldReturnZero`

#### **📝 Приклад:**

* `isAdult_AgeLessThan18_ShouldReturnFalse`
* `login_InvalidPassword_ShouldFailAuthentication`
* `addUser_WithExistingEmail_ShouldThrowDuplicateUserException`
>
> Така назва повністю самодокументується і дає вичерпну інформацію про тест.

#### **📌 Що перевіряють:**

*Знання стандартних конвенцій іменування. Це одна з перших речей, яку очікують побачити в тестовому коді кандидата.*

* * *

### **3 ❓ Питання:**

Що таке структура тесту `Given-When-Then` або `Arrange-Act-Assert`?

> #### **💬 Відповідь:**
>
> Це два синонімічних підходи до структурування тіла тестового методу, які роблять його логіку зрозумілою та передбачуваною.
>
> **Arrange-Act-Assert (AAA):**
>
> * **Arrange (Підготовка):** У цьому блоці ви готуєте всі необхідні дані та об'єкти для тесту. Це можуть бути вхідні параметри, моки, стан системи.
> * **Act (Дія):** У цьому блоці ви виконуєте **одну** ключову дію — викликаєте метод, який тестуєте.
> * **Assert (Перевірка):** У цьому блоці ви перевіряєте результат дії за допомогою тверджень (`Assertions`), щоб переконатися, що він відповідає очікуванням.
>
> **Given-When-Then:**
> Це інша назва для того ж підходу, що походить з BDD (Behavior-Driven Development):
>
> * **Given (Дано):** Початковий стан системи (еквівалент `Arrange`).
> * **When (Коли):** Відбувається дія (еквівалент `Act`).
> * **Then (Тоді):** Очікується певний результат (еквівалент `Assert`).

#### **📝 Приклад:**

```java
@Test
void withdraw_ValidAmount_ShouldDecreaseBalance() {
    // Arrange (Given)
    BankAccount account = new BankAccount(100.0);
    double withdrawAmount = 30.0;
    
    // Act (When)
    account.withdraw(withdrawAmount);
    
    // Assert (Then)
    assertEquals(70.0, account.getBalance());
}
```

#### **📌 Що перевіряють:**

*Вміння писати чисті та добре структуровані тести. Дотримання AAA/GWT є ознакою хорошого стилю.*

* * *

### **4 ❓ Питання:**

Як правильно іменувати тестові класи?

> #### **💬 Відповідь:**
>
> Іменування тестових класів також має дотримуватися певних конвенцій для легкості навігації та автоматичного виявлення тестів системами збирання (Maven, Gradle).
>
> **Найпоширеніша конвенція:**
> До назви класу, що тестується, додається суфікс `Test`.
>
> * Клас: `OrderService` -> Тестовий клас: `OrderServiceTest`
> * Клас: `Calculator` -> Тестовий клас: `CalculatorTest`
>
> **Альтернативні конвенції:**
>
> * Додавання суфіксу `Tests` (у множині): `OrderServiceTests`
> * Додавання префіксу `Test`: `TestOrderService` (менш поширено в Java, частіше зустрічається в інших мовах).
>
> Більшість систем збирання, як-от Maven Surefire Plugin, за замовчуванням шукають файли, що відповідають патернам `*Test.java`, `*Tests.java` або `Test*.java`. Дотримання конвенції дозволяє автоматизувати запуск тестів без додаткової конфігурації.

#### **📌 Що перевіряють:**

*Знання стандартних практик організації проектів та налаштування інструментів збирання.*

* * *

### **5 ❓ Питання:**

Як анотація `@DisplayName` допомагає покращити іменування тестів?

> #### **💬 Відповідь:**
>
> Хоча конвенція `method_state_behavior` є дуже інформативною для розробників, вона має обмеження (не можна використовувати пробіли, розділові знаки). Анотація **`@DisplayName`** в JUnit 5 дозволяє вирішити цю проблему.
>
> Вона дозволяє надати тесту або тестовому класу **людсько-зрозумілу назву**, яка буде відображатися в IDE та звітах про тестування замість технічної назви методу.
>
> **Переваги:**
>
> * **Виняткова читабельність:** Можна писати повноцінні речення природною мовою.
> * **Дружність до нетехнічних фахівців:** Звіти стають зрозумілими для менеджерів, аналітиків та замовників.
> * **Гнучкість:** Можна використовувати будь-які символи, включаючи емодзі, для візуального акценту.

#### **📝 Приклад:**

```java
@Test
@DisplayName("❌ Переказ коштів має бути відхилено, якщо на рахунку недостатньо коштів")
void transfer_InsufficientFunds_ShouldBeRejected() {
    // ...
}
```

#### **Коментар:**

Найкраща практика — поєднувати обидва підходи: інформативну назву методу для розробника та `@DisplayName` для звітів.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей JUnit 5, спрямованих на покращення читабельності тестів та звітів.*

* * *

### **6 ❓ Питання:**

Порівняйте поганий та хороший приклад назви тесту. Поясніть недоліки поганого прикладу.

> #### **💬 Відповідь:**
>
> **Поганий приклад:**
>
> ```java
> @Test
> void test1() { ... }
> // або
> @Test
> void testUserLogin() { ... }
> ```
>
> **Недоліки:**
>
> * **Неінформативність:** `test1` не говорить абсолютно нічого. `testUserLogin` говорить, *що* тестується, але не говорить, *за яких умов* і *який очікується результат*.
> * **Складне налагодження:** Якщо `testUserLogin` впаде, вам доведеться читати весь код тесту, щоб зрозуміти, чи це був тест на успішний вхід, чи на вхід з неправильним паролем, чи на вхід із заблокованим акаунтом.
>
> **Хороший приклад:**
>
> ```java
> @Test
> void login_WithInvalidCredentials_ShouldReturnAuthenticationError() { ... }
>
> @Test
> @DisplayName("Логін з правильними даними повинен перенаправити на головну сторінку")
> void login_WithValidCredentials_ShouldRedirectToDashboard() { ... }
> ```
>
> **Переваги:**
>
> * **Самодокументованість:** Назва повністю описує сценарій.
> * **Швидка діагностика:** При збої ви одразу бачите, що пішло не так.

#### **📌 Що перевіряють:**

*Практичне розуміння, як назви тестів впливають на щоденну роботу розробника. Кандидат повинен вміти пояснити, чому погані назви створюють проблеми.*

* * *

### **7 ❓ Питання:**

Що таке принцип "одне твердження на тест" (Single Assertion per Test) і наскільки суворо його слід дотримуватися?

> #### **💬 Відповідь:**
>
> Принцип **"одне твердження на тест"** говорить, що кожен тестовий метод повинен містити лише одне твердження (`assert`).
>
> **Ідея:** Це робить тест максимально сфокусованим і гарантує, що він може провалитися лише з однієї причини, що спрощує налагодження.
>
> **Наскільки суворо дотримуватися:**
> У сучасному тестуванні цей принцип слід розуміти не буквально як "один виклик `assert`", а як **"одна логічна перевірка на тест"**.
>
> * **Добре (кілька `assert` для однієї логічної перевірки):** Коли ви перевіряєте стан одного об'єкта після якоїсь дії, доцільно мати кілька тверджень для різних його полів. Наприклад, після реєстрації користувача перевірити його ім'я, email і статус. У цьому випадку на допомогу приходить `assertAll()`.
>
> * **Погано (кілька логічних перевірок):** Якщо в одному тесті ви спочатку перевіряєте логін, потім зміну пароля, а потім видалення акаунта — це порушення принципу. Такий тест потрібно розбити на три окремих.
>
> **Висновок:** Дотримуйтеся принципу "одна концепція на тест". Не бійтеся використовувати кілька тверджень для повної перевірки цієї концепції, особливо з `assertAll()`.

#### **📌 Що перевіряють:**

*Прагматичний підхід до написання тестів. Кандидат не повинен сліпо слідувати догмам, а розуміти їхню мету і застосовувати їх розумно.*

* * *

### **8 ❓ Питання:**

Як використання коментарів (`// given`, `// when`, `// then`) допомагає структурувати тест? ⭐️

> #### **💬 Відповідь:**
>
> Використання коментарів, що позначають блоки AAA/GWT, є дуже хорошою практикою, яка візуально структурує тіло тесту і покращує його читабельність.
>
> ```java
> @Test
> void deposit_PositiveAmount_ShouldBeAddedToBalance() {
>     // Given (Arrange)
>     // ... підготовка ...
>
>     // When (Act)
>     // ... дія ...
>
>     // Then (Assert)
>     // ... перевірка ...
> }
> ```
>
> **Переваги:**
>
> 1. **Візуальне розділення:** Одразу видно, де закінчується підготовка і починається дія, а де — перевірка. Це допомагає швидко зрозуміти логіку тесту.
> 2. **Дотримання структури:** Самі коментарі спонукають розробника дотримуватися правильної структури і не змішувати підготовку з дією або перевіркою.
> 3. **Читабельність для інших:** Нова людина в команді, навіть не знаючи коду, може легко зрозуміти, що відбувається в тесті.
>
> Хоча для простих тестів з трьох рядків це може бути зайвим, для більш складних тестів з великою секцією підготовки (`Given`) такі коментарі є дуже корисними. Альтернативою є просто пусті рядки між блоками.

#### **📌 Що перевіряють:**

*Увагу до чистоти та читабельності коду. Кандидат повинен демонструвати навички, що полегшують роботу в команді.*

* * *

### **9 ❓ Питання:**

Які є альтернативні конвенції іменування тестів, крім `method_state_behavior`? ⭐️

> #### **💬 Відповідь:**
>
> Хоча `method_state_behavior` є дуже популярною, існують і інші підходи.
>
> 1. **Стиль BDD (Behavior-Driven Development):**
>     * Назви тестів пишуться як речення, що описують поведінку.
>     * Часто використовуються ключові слова `should`, `when`, `given`.
>     * Приклад: `itShouldReturnFalseWhenAgeIsLessThan18()`, `givenUserIsAdmin_whenDeletingProduct_thenProductIsDeleted()`.
>     * Для більшої читабельності назви пишуться в `camelCase`, але іноді з `_` для розділення логічних частин.
>
> 2. **Описові назви з `@DisplayName`:**
>     * Цей підхід повністю покладається на анотацію `@DisplayName`, а назви методів робляться простими, але унікальними.
>     * Приклад:
>
>         ```java
>         @Test
>         @DisplayName("Сума двох додатних чисел є коректною")
>         void testPositiveAddition() { ... }
>         ```
>
> 3. **"Backticks" стиль (в Kotlin/Groovy):**
>     * Мови, як-от Kotlin, дозволяють писати назви методів у зворотних лапках, використовуючи звичайні речення з пробілами.
>     * Приклад:
>
>         ```kotlin
>         @Test
>         fun `sum of two positive numbers should be correct`() { ... }
>         ```
>
> Найважливіше — це **консистентність**. Команда має обрати один стиль і дотримуватися його.

#### **📌 Що перевіряють:**

*Кругозір та гнучкість. Кандидат повинен знати, що існують різні підходи, і розуміти їхні переваги та недоліки.*

* * *

### **10 ❓ Питання:**

Що може означати, якщо назва тесту стає занадто довгою та складною? ⭐️

> #### **💬 Відповідь:**
>
> Занадто довга назва тесту (напр., `updateOrder_whenUserIsAdminAndPaymentIsConfirmedAndStockIsAvailable_shouldChangeStatusToShippedAndNotifyUser`) часто є **"кодовим запахом" (code smell)**.
>
> Це може вказувати на одну з кількох проблем:
>
> 1. **Тест порушує Принцип єдиної відповідальності (SRP):** Скоріш за все, тест перевіряє **занадто багато речей одночасно**. У прикладі вище тест перевіряє і зміну статусу, і відправку сповіщення. Це мають бути два окремі, сфокусовані тести.
> 2. **Занадто складна логіка підготовки (`Given`):** Якщо у вас багато умов (`...And...And...`), це може означати, що об'єкт, який ви тестуєте, має занадто багато залежностей або складний стан. Можливо, варто задуматися про рефакторинг самого виробничого коду.
> 3. **Неправильна назва методу, що тестується:** Можливо, сам метод `updateOrder` робить занадто багато, і його варто розбити на менші, більш керовані частини.
>
> **Як вирішити:**
> Розбити складний тест на кілька менших, більш сфокусованих тестів. Кожен тест повинен перевіряти лише один аспект поведінки.

#### **📌 Що перевіряють:**

*Здатність аналізувати код (включаючи тестовий) на наявність "кодових запахів" та розуміти, на які глибші проблеми в дизайні вони можуть вказувати. Це ознака досвідченого інженера.*

* * *

### **11 ❓ Питання:**

Як пов'язані структура тесту `Given-When-Then` і BDD (Behavior-Driven Development)? ⭐️

> #### **💬 Відповідь:**
>
> Структура `Given-When-Then` є **фундаментальною концепцією BDD**.
>
> **BDD (Розробка через поведінку)** — це методологія, яка фокусується на описі поведінки системи за допомогою сценаріїв, зрозумілих для всіх учасників проекту (розробників, QA, аналітиків, бізнесу).
>
> **Зв'язок:**
>
> * BDD використовує спеціальну мову **Gherkin** для написання сценаріїв у форматі `Given-When-Then`. Ці сценарії зберігаються в `.feature` файлах і описують бізнес-вимоги.
>
> * Структура `Given-When-Then` в **unit-тестах** є, по суті, **мікро-версією** цього підходу на рівні коду. Розробник, використовуючи коментарі `// Given, // When, // Then`, описує поведінку не всієї системи, а одного маленького компонента.
>
> Таким чином, `Given-When-Then` в тестах допомагає мислити в категоріях "поведінки" системи, а не просто "перевірки результату", що є головною ідеєю BDD. Це робить unit-тести ближчими до вимог і більш зрозумілими.

#### **📌 Що перевіряють:**

*Знання гнучких методологій розробки та розуміння того, як їхні принципи застосовуються на рівні написання коду.*

* * *

### **12 ❓ Питання:**

Як варто структурувати тестовий клас, що містить багато тестів? ⭐️

> #### **💬 Відповідь:**
>
> Якщо тестовий клас стає занадто великим, це може ускладнити навігацію та розуміння. Існує кілька підходів до його структурування:
>
> 1. **Групування за методом:** Зібрати всі тести, що стосуються одного методу, разом.
>
> 2. **Групування за сценарієм:** Згрупувати тести за сценарієм або станом. Наприклад, тести на "щасливий шлях" разом, тести на обробку помилок — окремо.
>
> 3. **Використання вкладених класів (`@Nested`):**
>     Це найпотужніший підхід у JUnit 5. Він дозволяє створювати внутрішні класи для кожної групи тестів. Це робить структуру дуже чіткою та ієрархічною.
>
> #### **Приклад з `@Nested`:**
>
> ```java
> @DisplayName("Тести для BankAccount")
> class BankAccountTest {
> 
>     @Nested
>     @DisplayName("При депозиті")
>     class DepositTests {
>         @Test void shouldIncreaseBalanceOnPositiveAmount() { ... }
>         @Test void shouldThrowExceptionOnNegativeAmount() { ... }
>     }
> 
>     @Nested
>     @DisplayName("При знятті коштів")
>     class WithdrawTests {
>         @Test void shouldDecreaseBalanceOnValidAmount() { ... }
>         @Test void shouldThrowExceptionOnInsufficientFunds() { ... }
>     }
> }
> ```
>
> Такий підхід робить тестовий клас схожим на зміст книги, що дуже полегшує його читання.

#### **📌 Що перевіряють:**

*Практичні навички організації коду. Розробник повинен вміти структурувати не тільки виробничий, а й тестовий код.*

* * *

### **13 ❓ Питання:**

Чи варто писати тести на приватні методи? Поясніть свою позицію. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Загальне правило — ні, не варто.** Unit-тести повинні перевіряти **публічний контракт** класу, тобто його публічні методи.
>
> **Аргументи проти тестування приватних методів:**
>
> 1. **Приватні методи — це деталь реалізації.** Вони можуть бути змінені або видалені під час рефакторингу. Якщо ваші тести прив'язані до приватних методів, вони стануть дуже крихкими і будуть ламатися при будь-якій внутрішній зміні, навіть якщо публічна поведінка класу залишилася правильною.
> 2. **Це ознака поганого дизайну.** Якщо у вас є складний приватний метод, який хочеться протестувати окремо, це часто означає, що цей метод має занадто багато відповідальності. Його варто винести в **окремий клас-помічник (helper class)** з публічними методами і протестувати вже цей новий клас.
> 3. **Порушення інкапсуляції.** Для тестування приватного методу потрібно використовувати рефлексію або робити його `protected`/`public`, що є поганою практикою.
>
> **Як правильно:**
> Тестуйте приватні методи **опосередковано**, через виклик публічних методів, які їх використовують. Якщо ваш публічний метод працює правильно, значить, і приватні методи, які він викликає, виконують свою роботу коректно.

#### **📌 Що перевіряють:**

*Глибоке розуміння принципів ООП (інкапсуляція) та хорошого дизайну. Це класичне питання, яке відділяє розробників, що фокусуються на "поведінці", від тих, хто фокусується на "реалізації".*

* * *

### **14 ❓ Питання:**

Яким чином консистентне іменування та структура тестів допомагають у процесі код-рев'ю? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Консистентний стиль значно спрощує та прискорює процес код-рев'ю.
>
> 1. **Зменшення когнітивного навантаження:** Коли вся команда дотримується єдиного стилю (напр., `метод_стан_результат` та `Given-When-Then`), рев'юеру не потрібно витрачати час на те, щоб розібратися в структурі та намірах кожного тесту. Він може одразу зосередитися на **суті** — на логіці тесту та виробничого коду.
>
> 2. **Швидке виявлення пропущених сценаріїв:** Коли тести впорядковані і добре названі, легко помітити, які сценарії не були покриті. Наприклад, якщо є тест `login_ValidCredentials_Success`, але немає `login_InvalidCredentials_Failure`, це одразу кидається в очі.
>
> 3. **Тести як критерії прийняття:** Під час код-рев'ю пул-реквеста рев'юер може просто переглянути назви тестів, щоб зрозуміти, який саме функціонал було реалізовано і чи відповідає він поставленій задачі.
>
> 4. **Спрощення автоматизованих перевірок:** Консистентність допомагає налаштовувати статичні аналізатори та лінтери, які можуть автоматично перевіряти дотримання стандартів іменування.
>
> Загалом, консистентність перетворює тестовий код з "шуму" на корисний інструмент для розуміння та перевірки змін.

#### **📌 Що перевіряють:**

*Розуміння важливості командної роботи та стандартів кодування. Кандидат повинен усвідомлювати, що він пише код не для себе, а для всієї команди.*

* * *

### **15 ❓ Питання:**

Уявіть, що ви бачите такий тест: `void testLogic() { ... }`. Це єдиний тест для складного сервісу. Як би ви підійшли до його рефакторингу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний приклад "тесту-монстра" або "тесту-смітника", який є серйозним технічним боргом. Рефакторинг такого тесту вимагає систематичного підходу.
>
> **Мої кроки:**
>
> 1. **Аналіз (ніяких змін поки що!):**
>     * Спочатку я б розібрався, що саме цей тест намагається перевірити. Я б додав коментарі, розділяючи його на логічні блоки `Given-When-Then`. Ймовірно, всередині нього буде кілька незалежних сценаріїв.
>     * Запустив би тест кілька разів, щоб переконатися, що він стабільний і "зелений".
>
> 2. **Декомпозиція (Extraction):**
>     * Я б почав **витягувати** кожен логічний сценарій з цього великого методу в **окремий, добре названий тестовий метод**.
>     * Кожен новий метод отримав би правильну назву за конвенцією `метод_стан_результат` та чітку структуру `AAA`.
>     * Після створення кожного нового тесту я б запускав і старий, і новий тест, щоб переконатися, що я не змінив логіку перевірки.
>
> 3. **Видалення дублювання:**
>     * Після того, як всі сценарії витягнуті в окремі методи, я б помітив, що в них багато дублювання коду підготовки (`Arrange`).
>     * Я б виніс спільну логіку ініціалізації в метод, позначений анотацією **`@BeforeEach`**.
>
> 4. **Фінальне очищення:**
>     * Після того, як вся логіка з `testLogic()` рознесена по нових, чистих та сфокусованих тестах, оригінальний метод `testLogic()` можна **безпечно видалити**.
>     * У результаті ми отримаємо набір невеликих, незалежних, читабельних та підтримуваних тестів замість одного великого і незрозумілого.
>
> Цей підхід ("Divide and Conquer") дозволяє безпечно рефакторити навіть дуже заплутаний тестовий код.

#### **📌 Що перевіряють:**

*Навички рефакторингу, систематичний підхід до вирішення проблем та розуміння, як працювати з технічним боргом. Це ознака зрілого та відповідального інженера.*

* * *
