---
layout: default
title: "Практичний приклад - тестування класу Abbreviation"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 9
---

# Практичний приклад - тестування класу Abbreviation

* короткий опис: Застосування `JUnit` для перевірки логіки, поведінки та обробки винятків окремого класу, включаючи позитивні сценарії, граничні випадки (`edge cases`) та очікувані винятки, використовуючи анотації `JUnit`.

* поняття: Позитивні сценарії, `edge cases`, тестування винятків, ініціалізація (`@BeforeAll`, `@BeforeEach`).

* приклади: Тестування методу `shorten("internationalization")` на `i18n`, обробка порожнього рядка, викидання `IllegalArgumentException` для `null`.

* ключові моменти: Покриття всіх сценаріїв, правильна організація тестового класу, використання `assertEquals()`, `assertThrows()`.

* що важливо знати на співбесіді: Як підходити до тестування класу, типи тестових сценаріїв.

### **✅ Офіційне визначення:**

**Тестування класу** — це процес перевірки логіки, поведінки та обробки винятків окремого класу за допомогою модульних (unit) тестів.

### **🧠 Простими словами:**

Це як "інтерв’ю" для твого коду: ми задаємо йому запитання у вигляді тестів і перевіряємо, чи він відповідає правильно — і під час нормальної роботи, і під час стресу.

---

## **📊 У межах тестування класу **`Abbreviation`** зазвичай виділяють:**

* **Позитивні сценарії** — метод працює як очікується для валідних вхідних даних.
* **Edge cases (граничні випадки)** — вхідні дані перебувають на межі допустимих значень.
* **Тестування винятків** — перевірка, що метод правильно кидає виключення при некоректних параметрах.
* **Ініціалізація** — налаштування стану перед виконанням тестів за допомогою **`@BeforeAll, @BeforeEach.`**

---

## **Пояснення на пальцях (💡)**

Уяви, ти створив клас **`Abbreviation`**, який скорочує рядки. Тобі потрібно переконатися, що:

* **`"internationalization"`** перетворюється на **`"i18n"`**,
* **`"cat"`** залишається **`"cat"`**,
* пустий рядок не викликає краш,
* null кидає виняток.

Це все і перевіряє тестовий клас: щоб твій код був "броньований" від несподіванок.

---

## **Корисні приклади (🧪)**

```java
class AbbreviationTest {
    static Abbreviation abbreviation;

    @BeforeAll
    static void setup() {
        abbreviation = new Abbreviation();
    }

    @Test
    void testValidAbbreviation() {
        assertEquals("i18n", abbreviation.shorten("internationalization"));
    }

    @Test
    void testShortWord() {
        assertEquals("cat", abbreviation.shorten("cat"));
    }

    @Test
    void testEmptyString() {
        assertEquals("", abbreviation.shorten(""));
    }

    @Test
    void testNullInput() {
        assertThrows(IllegalArgumentException.class, () -> abbreviation.shorten(null));
    }
}
```

---

## **Порівняння (⚖️)**

|        Тип тесту        |      Що перевіряє      |          Приклад          |
|:-----------------------:|:----------------------:|:-------------------------:|
| **Позитивний сценарій** |   Нормальні значення   | **`"dog"`** -> **`"dog"`** |
|  **Граничний випадок**  | Пусті рядки, 1 символ  |    **`""`** -> **`""`**    |
| **Тестування винятку**  | Некоректні дані (null) |  **`null`** -> Exception   |
|    **Ініціалізація**    |  Підготовка до тестів  |     **`@BeforeAll`**      |

---

## **🧠 Мнемоніка для запам’ятовування:**

🧩 **P-E-V-I**:

* **P**ositive — перевіряй звичну поведінку
* **E**dge — не забудь про межі
* **V**alidation — обробка помилок
* **I**nitialization — налаштуй середовище

---

## **🟩 Підсумок:**

* Клас **`Abbreviation`** слід тестувати на всі типи вхідних даних.
* Структура тестів повинна бути логічною, з описовими назвами.
* **`@BeforeAll`** допомагає підготувати об’єкт для багатьох тестів.
* **`assertEquals`**, **`assertThrows`** — головні інструменти для перевірок.
* Тестування — це не лише "перевірка", а гарантія, що код витримає бої в продакшені.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Як написати найпростіший позитивний тест ("happy path") для методу `shorten()` класу `Abbreviation`?

> #### **💬 Відповідь:**
>
> Позитивний тест перевіряє, що метод працює коректно за нормальних, очікуваних умов. Для класу `Abbreviation`, "happy path" — це скорочення довгого слова.
>
> Тест повинен слідувати структурі `Arrange-Act-Assert`:
>
> 1. **Arrange (Підготовка):** Створити екземпляр класу `Abbreviation` та визначити вхідний рядок і очікуваний результат.
> 2. **Act (Дія):** Викликати метод `shorten()`, передавши йому вхідний рядок.
> 3. **Assert (Перевірка):** За допомогою `assertEquals()` порівняти фактичний результат з очікуваним.

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;

class AbbreviationTest {

    @Test
    void shorten_LongWord_ShouldReturnAbbreviation() {
        // Arrange
        Abbreviation abbreviation = new Abbreviation();
        String input = "internationalization";
        String expected = "i18n";

        // Act
        String actual = abbreviation.shorten(input);

        // Assert
        assertEquals(expected, actual);
    }
}
```

#### **📝 Коментар:**

Хороша назва тесту (`shorten_LongWord_ShouldReturnAbbreviation`) одразу пояснює, що саме перевіряється, за яких умов, і який результат очікується.

#### **📌 Що перевіряють:**

*Базові навички написання unit-тестів: вміння створювати екземпляр класу, викликати метод та перевіряти результат за допомогою найпростішого твердження.*

* * *

### **2 ❓ Питання:**

Як уникнути повторного створення об'єкта `Abbreviation` у кожному тесті?

> #### **💬 Відповідь:**
>
> Щоб уникнути дублювання коду ініціалізації, JUnit надає анотації для підготовки тестового середовища. У цьому випадку, оскільки клас `Abbreviation` є stateless (не має стану), найкраще використати **`@BeforeAll`**.
>
> * **`@BeforeAll`**: Метод, позначений цією анотацією, виконується **один раз перед усіма тестами** в класі.
> * Він повинен бути `static`.
> * У цьому методі ми створюємо екземпляр `Abbreviation` і зберігаємо його у статичному полі.
>
> Якщо б наш об'єкт мав стан, який потрібно скидати перед кожним тестом, ми б використали `@BeforeEach`.

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

class AbbreviationTest {

    private static Abbreviation abbreviation;

    @BeforeAll
    static void setup() {
        // Цей код виконається один раз
        abbreviation = new Abbreviation();
    }

    @Test
    void testLongWord() {
        assertEquals("i18n", abbreviation.shorten("internationalization"));
    }
    
    @Test
    void testShortWord() {
        assertEquals("word", abbreviation.shorten("word"));
    }
}
```

#### **📌 Що перевіряють:**

*Знання анотацій життєвого циклу JUnit (`@BeforeAll`, `@BeforeEach`) та вміння використовувати їх для оптимізації та уникнення дублювання коду в тестах (принцип DRY).*

* * *

### **3 ❓ Питання:**

Як протестувати поведінку методу `shorten()` з коротким словом, яке не повинно скорочуватися?

> #### **💬 Відповідь:**
>
> Це також позитивний сценарій, який перевіряє іншу гілку логіки. Тест перевіряє, що якщо слово достатньо коротке (наприклад, менше 4 символів), метод повертає його без змін.
>
> Структура тесту залишається такою ж (`Arrange-Act-Assert`), але змінюються вхідні дані та очікуваний результат.

#### **📝 Приклад:**

```java
@Test
@DisplayName("Короткі слова не повинні скорочуватися")
void shorten_ShortWord_ShouldReturnSameWord() {
    // Arrange
    Abbreviation abbreviation = new Abbreviation();
    String input = "cat";
    String expected = "cat";
    
    // Act
    String actual = abbreviation.shorten(input);

    // Assert
    assertEquals(expected, actual);
}
```

#### **📝 Коментар:**

Важливо тестувати не лише випадки, коли відбувається дія (скорочення), а й коли вона не відбувається. Це гарантує, що умовна логіка в методі працює правильно.

#### **📌 Що перевіряють:**

*Повноту тестування. Кандидат повинен розуміти, що необхідно тестувати всі основні гілки виконання коду.*

* * *

### **4 ❓ Питання:**

Як протестувати, що метод `shorten()` правильно обробляє граничні випадки (edge cases), такі як порожній рядок?

> #### **💬 Відповідь:**
>
> Тестування граничних випадків (edge cases) є критично важливим, оскільки саме на них найчастіше виникають помилки. Порожній рядок — класичний граничний випадок для операцій з рядками.
>
> Ми очікуємо, що метод не впаде, а коректно поверне порожній рядок.

#### **📝 Приклад:**

```java
@Test
@DisplayName("Порожній рядок повинен повертатися як порожній рядок")
void shorten_EmptyString_ShouldReturnEmptyString() {
    // Arrange
    Abbreviation abbreviation = new Abbreviation();
    
    // Act & Assert
    assertEquals("", abbreviation.shorten(""));
}
```

**Інші граничні випадки, які варто було б перевірити:**

* Рядок з одного символу (`"a"`).
* Рядок з двох символів (`"hi"`).
* Рядок з пробілами (`" a word "`).
* Рядок, довжина якого точно на межі скорочення (напр., якщо скорочуємо слова > 3, то перевірити слово довжиною 3 і 4).

#### **📌 Що перевіряють:**

*Вміння думати як тестувальник. Розробник повинен не тільки тестувати "очевидні" випадки, а й шукати потенційно проблемні вхідні дані на межах умов.*

* * *

### **5 ❓ Питання:**

Як написати тест, що перевіряє, чи метод `shorten()` кидає виняток при передачі `null`?

> #### **💬 Відповідь:**
>
> Це тест на негативний сценарій, який перевіряє обробку некоректних вхідних даних. Для цього в JUnit 5 використовується твердження **`assertThrows()`**.
>
> Воно приймає два аргументи:
>
> 1. Клас очікуваного винятку (напр., `IllegalArgumentException.class`).
> 2. Виконуваний блок коду (у вигляді лямбда-виразу), який повинен кинути цей виняток.
>
> Тест пройде успішно, якщо виняток буде кинуто, і провалиться, якщо його не буде або буде кинуто виняток іншого типу.

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.Test;

class AbbreviationTest {
    
    @Test
    @DisplayName("Метод повинен кидати IllegalArgumentException для null вводу")
    void shorten_NullInput_ShouldThrowIllegalArgumentException() {
        Abbreviation abbreviation = new Abbreviation();

        // Перевіряємо, що виконання лямбди призводить до очікуваного винятку
        assertThrows(IllegalArgumentException.class, () -> {
            abbreviation.shorten(null);
        });
    }
}
```

#### **📌 Що перевіряють:**

*Знання API JUnit для тестування винятків та вміння тестувати обробку помилок, що є важливою частиною написання надійного коду.*

* * *

### **6 ❓ Питання:**

Як можна об'єднати всі тести на граничні та негативні випадки в одну групу?

> #### **💬 Відповідь:**
>
> У JUnit 5 для логічного групування тестів використовується анотація **`@Nested`**. Вона дозволяє створювати вкладені тестові класи, кожен з яких може мати свій контекст, назву та навіть власні методи `@BeforeEach`/`@AfterEach`.
>
> Це робить структуру тестового класу дуже читабельною та організованою, особливо коли тестів стає багато.

#### **📝 Приклад:**

```java
@DisplayName("Тестування класу Abbreviation")
class AbbreviationTest {
    private Abbreviation abbreviation;

    @BeforeEach
    void setup() {
        abbreviation = new Abbreviation();
    }

    @Nested
    @DisplayName("Позитивні сценарії")
    class PositiveTests {
        @Test void testLongWord() { /* ... */ }
        @Test void testShortWord() { /* ... */ }
    }

    @Nested
    @DisplayName("Граничні та негативні сценарії")
    class EdgeAndNegativeTests {
        @Test void testEmptyString() { /* ... */ }
        @Test void testNullInput() { /* ... */ }
    }
}
```

#### **📌 Що перевіряють:**

*Знання розширених можливостей JUnit 5 для структурування тестів. Використання `@Nested` демонструє прагнення писати чистий та організований тестовий код.*

* * *

### **7 ❓ Питання:**

Як би ви переписали тести для `Abbreviation` з використанням `@ParameterizedTest`?

> #### **💬 Відповідь:**
>
> **Параметризовані тести** — це ідеальний інструмент для цього класу, оскільки він має одну функцію, поведінка якої залежить від різних вхідних даних. Це дозволяє уникнути дублювання коду і перевірити багато сценаріїв за допомогою одного тестового методу.
>
> Ми можемо використати **`@CsvSource`** для надання пар "вхідний рядок - очікуваний результат".

#### **📝 Приклад:**

```java
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;

class AbbreviationParameterizedTest {
    private final Abbreviation abbreviation = new Abbreviation();
    
    @ParameterizedTest(name = "Скорочення '{0}' має бути '{1}'")
    @CsvSource({
        "internationalization, i18n",
        "accessibility, a11y",
        "cat, cat",
        "API, API",
        "'', ''" // Порожній рядок
    })
    void testShortenWithVariousInputs(String input, String expected) {
        assertEquals(expected, abbreviation.shorten(input));
    }
}
```

#### **Коментар:**

Такий підхід значно зменшує кількість коду і робить додавання нових тестових випадків дуже простим — достатньо додати новий рядок у `@CsvSource`. Тест на `null` все ще потрібно писати окремо.

#### **📌 Що перевіряють:**

*Вміння використовувати сучасні та ефективні підходи до тестування. Знання параметризованих тестів є важливою навичкою для розробника.*

* * *

### **8 ❓ Питання:**

При тестуванні класу `Abbreviation`, чи є сенс використовувати `assertAll`? Наведіть гіпотетичний приклад. ⭐️

> #### **💬 Відповідь:**
>
> Для простого методу `shorten()`, який повертає один рядок, `assertAll` не дуже потрібен. Однак, якби метод повертав складний об'єкт з кількома властивостями, `assertAll` був би дуже корисним.
>
> **Гіпотетичний приклад:**
> Уявімо, що наш метод повертає об'єкт `AbbreviationResult`, який містить саме скорочення, кількість видалених символів та прапорець, чи відбулося скорочення.
>
> ```java
> class AbbreviationResult {
>     String result;
>     int charsRemoved;
>     boolean wasShortened;
>     // getters...
> }
> ```
>
> В цьому випадку `assertAll` дозволив би перевірити всі поля об'єкта за один раз, і у випадку помилки ми б побачили, які саме поля не збігаються.

#### **📝 Приклад тесту:**

```java
@Test
void shorten_LongWord_ShouldReturnCorrectResultObject() {
    AbbreviationResult result = abbreviation.shortenToResult("internationalization");

    assertAll("result properties",
        () -> assertEquals("i18n", result.getResult()),
        () -> assertEquals(18, result.getCharsRemoved()),
        () -> assertTrue(result.wasShortened())
    );
}
```

#### **📌 Що перевіряють:**

*Розуміння, коли і для чого варто застосовувати просунуті твердження. Кандидат повинен розуміти, яку проблему вирішує `assertAll`.*

* * *

### **9 ❓ Питання:**

Що, якби вимоги до класу `Abbreviation` були такими, що він повинен ігнорувати слова з "чорного списку"? Як би ви протестували такий функціонал? ⭐️

> #### **💬 Відповідь:**
>
> Це значно ускладнює клас, оскільки тепер він має залежність (чорний список). Припустимо, цей список надається зовнішнім сервісом `BlacklistService`.
>
> Тестувати такий клас напряму, звертаючись до реального сервісу, в unit-тесті не можна. Потрібно **ізолювати** клас `Abbreviation` від його залежності за допомогою **мокування (mocking)**.
>
> **Стратегія тестування:**
>
> 1. Переконатися, що `Abbreviation` приймає `BlacklistService` як залежність (наприклад, через конструктор).
> 2. У тестах створити **мок** для `BlacklistService` за допомогою бібліотеки **Mockito**.
> 3. "Навчити" мок, як він має поводитись (наприклад, `when(mockBlacklist.isBlacklisted("internationalization")).thenReturn(true);`).
> 4. Написати тести для двох сценаріїв:
>     * Тест, де слово є в чорному списку (мок повертає `true`), і ми очікуємо, що скорочення не відбудеться.
>     * Тест, де слова немає в чорному списку (мок повертає `false`), і скорочення відбувається.

#### **📝 Приклад з Mockito:**

```java
@ExtendWith(MockitoExtension.class)
class AbbreviationWithBlacklistTest {
    @Mock private BlacklistService mockBlacklist;
    @InjectMocks private Abbreviation abbreviation;

    @Test
    void shorten_WordIsInBlacklist_ShouldNotShorten() {
        // Arrange
        String word = "internationalization";
        when(mockBlacklist.isBlacklisted(word)).thenReturn(true);

        // Act & Assert
        assertEquals(word, abbreviation.shorten(word));
        verify(mockBlacklist).isBlacklisted(word); // Перевірка взаємодії
    }
}
```

#### **📌 Що перевіряють:**

*Навички тестування коду із зовнішніми залежностями. Розуміння Dependency Injection та мокування є критично важливим для тестування реальних додатків.*

* * *

### **10 ❓ Питання:**

Якщо тест на `assertThrows` провалюється, що це може означати? ⭐️

> #### **💬 Відповідь:**
>
> Провал тесту з `assertThrows(ExpectedException.class, ...)` може означати одну з трьох речей:
>
> 1. **Не було кинуто жодного винятку:** Код, який ви тестували, успішно виконав свою роботу, хоча ви очікували, що він впаде з помилкою. Це означає, що ваша валідація або логіка обробки помилок не працює.
>
> 2. **Було кинуто виняток іншого типу:** Код кинув виняток, але не того типу, який ви очікували. Наприклад, ви чекали `IllegalArgumentException`, а отримали `NullPointerException`. Це може свідчити про іншу, більш глибоку проблему в коді.
>
> 3. **Виняток був кинутий не в тому місці:** Лямбда-вираз, переданий в `assertThrows`, повинен містити *саме той виклик*, який кидає виняток. Якщо помилка виникає на етапі підготовки, до виклику лямбди, тест також впаде, але вже з іншим винятком.
>
> У будь-якому випадку, провал `assertThrows` вказує на невідповідність між очікуваною та реальною поведінкою коду при обробці помилкових ситуацій.

#### **📌 Що перевіряють:**

*Здатність до діагностики та аналізу помилок у тестах. Кандидат повинен розуміти різні причини, чому тест на виняток може провалитися.*

* * *

### **11 ❓ Питання:**

Перед тим, як писати тести для класу `Abbreviation`, які уточнюючі питання ви б задали щодо вимог? ⭐️

> #### **💬 Відповідь:**
>
> Написання тестів починається з чіткого розуміння вимог. Перш ніж писати код, я б задав наступні питання:
>
> 1. **Який мінімальний поріг для скорочення?** Слово має бути довшим за 3 символи? За 10? Це визначає граничні випадки.
> 2. **Як поводитись з регістром?** Чи `Internationalization` і `internationalization` скорочуються однаково? Чи повинен результат зберігати початковий регістр першої та останньої літер? (напр., `I18n`).
> 3. **Як обробляти слова з дефісами або іншими символами?** Чи `"state-of-the-art"` — це одне слово? Чи потрібно його скорочувати?
> 4. **Що робити з уже скороченими словами?** Що має повернути `shorten("i18n")`?
> 5. **Яке очікується повідомлення про помилку** при передачі `null`? Чи має воно бути специфічним?
>
> Відповіді на ці питання допоможуть написати значно більш повний та коректний набір тестів і уникнути неправильних припущень.

#### **📌 Що перевіряють:**

*Аналітичні навички та проактивність. Хороший розробник не просто пише код, а спочатку намагається зрозуміти задачу, виявити неоднозначності та уточнити вимоги. Це частина "зсуву вліво" (shift-left) мислення.*

* * *

### **12 ❓ Питання:**

Чому важливо мати як unit-тест для `Abbreviation`, так і, можливо, інтеграційний тест, якщо цей клас використовується в UI? ⭐️

> #### **💬 Відповідь:**
>
> Unit-тест та інтеграційний тест перевіряють різні речі і доповнюють один одного, забезпечуючи надійність на різних рівнях піраміди тестування.
>
> * **Unit-тест для `Abbreviation`:**
>   * **Мета:** Перевірити **логіку самого класу** в повній ізоляції. Чи правильно він рахує символи, обробляє граничні випадки (`null`, порожні рядки) тощо.
>   * **Перевага:** Дуже швидкий, стабільний і точно вказує на помилку в класі `Abbreviation`.
>
> * **Інтеграційний/UI-тест (де використовується `Abbreviation`):**
>   * **Мета:** Перевірити, що клас `Abbreviation` **правильно інтегрований** в більшу систему. Наприклад, що значення з поля вводу UI коректно передається в метод `shorten()`, а результат його роботи правильно відображається в іншому елементі UI.
>   * **Що він може знайти:** Проблеми, не пов'язані з логікою `Abbreviation`, наприклад:
>     * Помилка в передачі даних з UI-компонента.
>     * `null`, що приходить з іншого сервісу, а не напряму в тест.
>     * Проблеми з оновленням UI після отримання результату.
>
> **Висновок:** Unit-тест гарантує, що "двигун" працює правильно. Інтеграційний тест гарантує, що "двигун" правильно підключено до "колес" та "керма". Потрібні обидва.

#### **📌 Що перевіряють:**

*Розуміння піраміди тестування та призначення тестів на різних рівнях. Кандидат не повинен змішувати цілі unit- та інтеграційного тестування.*

* * *

### **13 ❓ Питання:**

Припустимо, ваш тест `shorten_LongWord_ShouldReturnAbbreviation` раптово почав падати. Яким буде ваш процес діагностики проблеми? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Падаючий unit-тест — це сигнал, який вимагає систематичного підходу до аналізу.
>
> **Мої кроки:**
>
> 1. **Уважно прочитати повідомлення про помилку:**
>     * Це `AssertionFailedError`? Якщо так, яке було `expected` і яке `actual` значення? Це одразу покаже, що саме повертає метод.
>     * Чи це інший виняток (напр., `NullPointerException`)? Це вкаже, що проблема виникла всередині методу до повернення результату.
>
> 2. **Ізолювати проблему:**
>     * Перевірити, чи не було нещодавніх змін у **виробничому коді** класу `Abbreviation`. Продивитись історію комітів Git. Ймовірно, хтось вніс регресію.
>     * Перевірити, чи не було змін у **самому тесті**. Можливо, хтось змінив очікуване значення (`expected`) помилково.
>
> 3. **Налагодження (Debugging):**
>     * Поставити точку зупину (breakpoint) всередині методу `shorten()` і в тесті, який його викликає.
>     * Запустити тест у режимі налагодження і покроково пройти виконання, перевіряючи значення змінних на кожному кроці. Це дозволить точно побачити, де логіка відхиляється від очікуваної.
>
> 4. **Спрощення:**
>     * Якщо логіка складна, я можу спробувати написати ще простіший unit-тест, який перевіряє ще меншу частину функціоналу, щоб звузити коло пошуку.
>
> Головне — не панікувати і діяти методично. Падаючий unit-тест — це добре, бо він знайшов проблему до того, як її знайшли користувачі.

#### **📌 Що перевіряють:**

*Навички налагодження та систематичного підходу до вирішення проблем. Розробник повинен вміти ефективно використовувати свої інструменти (IDE, дебагер, Git) для діагностики помилок.*

* * *

### **14 ❓ Питання:**

Клас `Abbreviation` є частиною утилітарного модуля без стану. Який життєвий цикл екземпляра тесту (`@TestInstance`) доцільніше тут обрати: `PER_METHOD` чи `PER_CLASS`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для тестування stateless класу, як-от `Abbreviation`, обидва підходи є технічно припустимими, але вибір між ними є компромісом між ідеологічною чистотою та зручністю.
>
> * **`TestInstance.Lifecycle.PER_METHOD` (за замовчуванням):**
>   * **Перевага:** Це "найчистіший" підхід. Він гарантує повну ізоляцію кожного тесту, навіть якщо в майбутньому хтось помилково додасть стан до класу `Abbreviation`. Це найнадійніший варіант з точки зору дизайну тестів.
>   * **Недолік:** Вимагає, щоб методи `@BeforeAll`/`@AfterAll` були `static`, що може бути менш зручним.
>
> * **`TestInstance.Lifecycle.PER_CLASS`:**
>   * **Перевага:** Дозволяє робити методи `@BeforeAll`/`@AfterAll` нестатичними. Оскільки клас `Abbreviation` не має стану, ризик впливу тестів один на одного мінімальний. Це трохи спрощує код ініціалізації.
>   * **Недолік:** Створює потенційний ризик у майбутньому. Якщо до `Abbreviation` буде додано стан, тести можуть стати залежними один від одного.
>
> **Мій вибір:**
> Я б залишився з режимом **`PER_METHOD` за замовчуванням**. Хоча `PER_CLASS` тут безпечний, переваги від нього (нестатичні `@BeforeAll`) не настільки великі, щоб відмовлятися від гарантії повної ізоляції, яку надає стандартний життєвий цикл. Це більш стійка до майбутніх змін стратегія.

#### **📌 Що перевіряють:**

*Глибоке розуміння життєвого циклу JUnit 5 та здатність робити обґрунтований архітектурний вибір навіть у дрібницях. Кандидат повинен думати про майбутню підтримку коду.*

* * *

### **15 ❓ Питання:**

Що, якби `Abbreviation` працював асинхронно і повертав `CompletableFuture<String>`? Як би змінився ваш підхід до тестування? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тестування асинхронного коду значно складніше, оскільки тест повинен дочекатися завершення операції, перш ніж перевіряти результат.
>
> **Зміни в підході:**
>
> 1. **Отримання результату:** У тестовому методі потрібно було б викликати `.get()` або `.join()` на `CompletableFuture`, щоб заблокувати виконання тесту і дочекатися результату.
>
> 2. **Обробка винятків:** Асинхронні операції кидають винятки інакше. Виняток буде загорнутий у `CompletionException`. Для його перевірки потрібно аналізувати `getCause()`.
>
> 3. **Використання бібліотек для тестування асинхронного коду:**
>     Стандартний JUnit не дуже добре пристосований до цього. Краще використовувати спеціалізовані бібліотеки, як-от **Awaitility**, яка надає зручний DSL для очікування певних умов.
>
> #### **Приклад тесту (без додаткових бібліотек):**
>
> ```java
> @Test
> void shortenAsync_LongWord_ShouldCompleteWithValue() throws Exception {
>     AbbreviationAsync abbreviation = new AbbreviationAsync();
>     CompletableFuture<String> future = abbreviation.shorten("internationalization");
> 
>     // Блокуємо і чекаємо на результат
>     String result = future.get(1, TimeUnit.SECONDS); // Таймаут, щоб тест не завис
> 
>     assertEquals("i18n", result);
> }
> ```
>
> #### **Приклад тесту на виняток:**
>
> ```java
> @Test
> void shortenAsync_NullInput_ShouldCompleteExceptionally() {
>     CompletableFuture<String> future = abbreviation.shorten(null);
> 
>     // Перевіряємо, що CompletableFuture завершився з помилкою
>     CompletionException ex = assertThrows(CompletionException.class, () -> {
>         future.join();
>     });
> 
>     // Перевіряємо причину помилки
>     assertInstanceOf(IllegalArgumentException.class, ex.getCause());
> }
> ```
>
> Тестування асинхронності вимагає значно більше уваги до деталей, обробки таймаутів та правильної роботи з винятками.

#### **📌 Що перевіряють:**

*Досвід роботи з асинхронним програмуванням та його тестуванням. Це складна тема, і вміння тестувати такий код є ознакою сильного розробника.*

* * *
