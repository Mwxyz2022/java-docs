---
layout: default
title: "Best Practices для Unit тестів"
parent: "Java JUnit"
grand_parent: "Java Core"
nav_order: 10
---

# Best Practices для Unit тестів

* короткий опис: Набір перевірених рекомендацій для створення надійних, підтримуваних і ефективних модульних тестів. Включає тестування `edge cases`, уникнення `void`-тестів, перевірку винятків, дотримання консистентності стилю, ізоляцію тестів.

* поняття: Тестування `edge cases`, уникнення `void` методів у тестах, `assertThrows()`, консистентність стилю.

* приклади: `shouldReturnTrueForEmptyString()`, `shouldSumCorrectly()`, `shouldThrowOnNullInput()`.

* ключові моменти: Тести мають бути простими, зрозумілими, ізольованими, передбачуваними.

* що важливо знати на співбесіді: Ключові `best practices`, як писати якісні `Unit`-тести.

### **✅ Офіційне визначення:**

**`Best Practices`** для **`Unit`** тестування — це набір перевірених рекомендацій, правил та патернів, які допомагають створювати надійні, підтримувані й ефективні модульні тести.

### **🧠 Простими словами:**

Це поради, як писати тести так, щоб вони були зрозумілими, точними, перевіряли всі важливі сценарії і не дратували команду.

---

## **📊 Основні практики включають:**

* Тестування граничних випадків (edge cases)
* Уникнення void-тестів (тестів без перевірки результату)
* Кількість тестів ≈ кількість вхідних параметрів
* Обов’язкова перевірка винятків (**`assertThrows`**)
* Консистентність стилю тестів у команді
* Одна перевірка — один тест
* Уникай тестової залежності та порядку виконання
* Простий, зрозумілий та self-describing тест

---

## **Пояснення на пальцях (💡)**

* Тест без **`assert`** — як вірш без рими: гарно, але сенсу нуль.
* Якщо метод має кілька параметрів — кожен їх комбінаційний варіант має бути протестований окремо.
* Не тестуй усе в одному методі — дроби на маленькі кейси.
* Виключення — це теж результат. І його треба перевіряти.
* Тести мають бути як добрі жарти: короткі, зрозумілі й передбачувані.

---

## **Корисні приклади (🧪)**

```java
// Edge case -- порожній рядок
@Test
void shouldReturnTrueForEmptyString() {
    assertTrue(MyUtils.isEmpty(""));
}

// Avoid void test
@Test
void shouldSumCorrectly() {
    assertEquals(5, Calculator.add(2, 3));
}

// Test exception
@Test
void shouldThrowOnNullInput() {
    assertThrows(IllegalArgumentException.class, () -> myService.process(null));
}

// Parameter count match test count
@Test
void shouldHandleSingleParamCase() {
    assertEquals("A", myMapper.map("a"));
}

@Test
void shouldHandleMultipleParamCase() {
    assertEquals("AB", myMapper.map("a", "b"));
}
```

---

## **Порівняння (⚖️)**

|         Поганий підхід         |              Гарний підхід               |
|:------------------------------:|:----------------------------------------:|
|  Один тест на багато випадків  |          Один тест — один кейс           |
|        Без **`assert`**        |        Явні перевірки результату         |
|     Довгі тести з логікою      |          Короткі й прості тести          |
|    Не обробляються винятки     |        Винятки перевіряються явно        |
| Імена типу **`testMethod1()`** | Імена типу **`shouldReturnTrueWhenX()`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"PACTE"** — як "пакт" хороших тестів:

* **P** — Parameters --> окремі тести
* **A** — Assertions --> завжди є
* **C** — Clarity --> читається як книга
* **T** — Throwables --> перевіряй\!
* **E** — Edge cases --> не забувай

---

## **🟩 Підсумок:**

* Пиши тести так, щоб їх було легко зрозуміти й підтримувати.
* Покривай **`edge`** **`cases`**, особливо **`null`**, пусті рядки, **`мін / макс`** значення.
* Не забувай про перевірку винятків.
* Один тест — один **`assert`** (у більшості випадків).
* Дотримуйся конвенцій і стандартів команди.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Який найголовніший принцип при написанні unit-тестів?

> #### **💬 Відповідь:**
>
> Найголовніший принцип — тест має бути **простим, читабельним та передбачуваним**. Якщо тест складний для розуміння, його буде важко підтримувати, а його падіння буде важко діагностувати.
>
> Це правило часто описують акронімом **FIRST**:
>
> * **F (Fast)** — Швидкий. Unit-тести мають виконуватися за мілісекунди. Їх запускають дуже часто, і повільні тести гальмують розробку.
> * **I (Independent/Isolated)** — Незалежний/Ізольований. Тести не повинні залежати один від одного або від зовнішнього середовища (БД, мережі). Результат одного тесту не повинен впливати на інший.
> * **R (Repeatable)** — Повторюваний. Тест повинен давати однаковий результат при кожному запуску, незалежно від середовища.
> * **S (Self-Validating)** — Самовалідований. Тест сам повинен визначати, чи є результат правильним, за допомогою тверджень (`assert`). Він не повинен вимагати ручної перевірки логів чи виводу в консоль.
> * **T (Timely)** — Своєчасний. Unit-тести слід писати разом з виробничим кодом або навіть до нього (в стилі TDD).

#### **📝 Аналогія:**

Хороший unit-тест — це як коротке, зрозуміле речення. Поганий тест — це як заплутаний абзац з юридичного документа, який потрібно перечитувати кілька разів, щоб зрозуміти суть.

#### **📌 Що перевіряють:**

*Розуміння філософії unit-тестування. Кандидат повинен знати, що тест — це в першу чергу код, і до нього застосовуються ті ж вимоги чистоти та простоти, що і до виробничого коду.*

* * *

### **2 ❓ Питання:**

Чому тести повинні бути незалежними один від одного?

> #### **💬 Відповідь:**
>
> Незалежність тестів (ізоляція) є критично важливою практикою з кількох причин:
>
> 1. **Детермінізм:** Якщо тести залежать від порядку виконання, їх результат стає непередбачуваним. Тест може проходити, якщо його запустити в одному порядку, і падати — в іншому. Це робить тестовий набір ненадійним.
> 2. **Легкість налагодження:** Коли падає незалежний тест, ви знаєте, що проблема саме в тому функціоналі, який він перевіряє. Якщо тести залежать один від одного, падіння одного може бути спричинене помилкою в іншому тесті, що значно ускладнює пошук справжньої причини.
> 3. **Паралельне виконання:** Незалежні тести можна запускати паралельно, що значно прискорює загальний час виконання всього тестового набору на CI/CD.
>
> Спільний стан між тестами (наприклад, статичні змінні, дані в базі, які не очищуються) є поширеним антипатерном, якого слід уникати. Кожен тест має готувати власне середовище і прибирати за собою.

#### **📝 Аналогія:**

Залежні тести — це як доміно. Якщо впаде одна кістка, за нею посипляться інші, і буде важко зрозуміти, яка саме була першою "проблемною". Незалежні тести — це як лампочки на ялинці: якщо одна перегоріла, інші продовжують світити.

#### **📌 Що перевіряють:**

*Розуміння одного з ключових принципів якісного тестування. Кандидат повинен усвідомлювати небезпеку спільного стану в тестах.*

* * *

### **3 ❓ Питання:**

Що означає "одне твердження на тест" (Single Assertion Per Test)? Наскільки це правило є строгим?

> #### **💬 Відповідь:**
>
> Принцип "одне твердження на тест" стверджує, що ідеальний тестовий метод повинен містити лише один виклик `assert`.
>
> **Ідея цього принципу:** Кожен тест має перевіряти одну-єдину річ. Це гарантує, що якщо тест провалиться, ви одразу будете знати точну причину збою, без необхідності аналізувати, яке з десяти тверджень не спрацювало.
>
> **Наскільки строгим є це правило:**
> Це хороша **рекомендація**, а не **сувора догма**. Слід розуміти цей принцип як **"одна логічна концепція на тест"**.
>
> * **Коли це правило порушувати погано:** Якщо в одному тесті ви перевіряєте і логін користувача, і оновлення його профілю, і видалення — це три різні концепції, і їх слід рознести по трьох тестах.
> * **Коли це нормально порушувати:** Якщо ви перевіряєте стан одного об'єкта після операції. Наприклад, після створення об'єкта `User` ви можете перевірити одразу кілька його полів: ім'я, email, статус. Це все стосується однієї концепції — "коректне створення користувача". Для таких випадків ідеально підходить `assertAll()`.

#### **📌 Що перевіряють:**

*Прагматичний підхід до тестування. Кандидат не повинен сліпо дотримуватися правил, а розуміти їхню мету та використовувати розумно.*

* * *

### **4 ❓ Питання:**

Що таке тестування граничних випадків (edge cases) і чому воно обов'язкове?

> #### **💬 Відповідь:**
>
> **Граничні випадки (edge cases)** — це тести, які перевіряють поведінку системи на "межах" допустимих вхідних даних або в екстремальних, нетипових умовах.
>
> **Чому це обов'язково:**
> Більшість помилок у програмуванні (особливо помилки "на одиницю" — off-by-one errors) виникають саме на цих межах. Ігнорування edge cases означає залишати найуразливіші частини коду без перевірки.
>
> **Типові приклади граничних випадків:**
>
> * **Числа:** `0`, `-1`, `Integer.MAX_VALUE`, `Integer.MIN_VALUE`.
> * **Рядки:** `null`, порожній рядок (`""`), рядок з пробілів (`"   "`), рядок з одного символу.
> * **Колекції:** `null`, порожня колекція, колекція з одного елемента.
> * **Дати:** високосний рік, останній день місяця, перехід через опівніч.
>
> Тестування edge cases — це те, що відрізняє поверхове тестування від глибокого та надійного.

#### **📝 Приклад:**

Для функції, що валідує вік `(18-65)`:

* Happy path: `30`
* Edge cases: `17`, `18`, `65`, `66`

#### **📌 Що перевіряють:**

*Мислення як у тестувальника. Розробник повинен не просто перевіряти "щасливий шлях", а активно шукати потенційно проблемні сценарії.*

* * *

### **5 ❓ Питання:**

Чому тест без тверджень (`assert`) є поганою практикою?

> #### **💬 Відповідь:**
>
> Тест без тверджень, який іноді жартома називають "тестом на дим, що йде від процесора", є марним, оскільки він нічого не перевіряє.
>
> **Проблеми такого тесту:**
>
> 1. **Не виконує свою функцію:** Головна мета тесту — перевірити, що код поводиться **очікувано**. Без `assert`, тест лише виконує код. Він може "пройти" успішно, навіть якщо код повертає абсолютно неправильний результат.
>
> 2. **Створює хибне відчуття безпеки:** У звіті такий тест буде позначено як "зелений", створюючи ілюзію, що відповідний функціонал покритий тестами і працює коректно, хоча це не так.
>
> Єдиний виняток — це тест, який перевіряє, що певний код **не кидає виняток**, і для цього використовується `assertDoesNotThrow()`. Але це також є формою твердження.

#### **📝 Приклад (поганий тест):**

```java
@Test
void testSum() {
    Calculator calculator = new Calculator();
    calculator.add(2, 2); // Код виконався, але що він повернув? Невідомо.
    // Тест завжди буде "зеленим".
}
```

#### **📌 Що перевіряють:**

*Розуміння самої суті тестування. Це базове питання, неправильна відповідь на яке може бути серйозним червоним прапором.*

* * *

### **6 ❓ Питання:**

Чи слід використовувати логіку (if, for, try-catch) всередині тестів?

> #### **💬 Відповідь:**
>
> Загальне правило — **уникайте логіки в тестах**, наскільки це можливо. Тест має бути максимально простим і прямолінійним.
>
> **Чому це погано:**
>
> * **Тести теж можуть містити баги:** Чим більше логіки у вашому тесті, тим більша ймовірність, що ви припуститеся помилки в самому тесті. Хто буде тестувати тести?
> * **Зменшення читабельності:** Умовні оператори та цикли роблять тест складнішим для розуміння. Ідеальний тест повинен бути настільки простим, щоб його логіку можна було зрозуміти з одного погляду.
>
> **Коли це припустимо:**
>
> * **Цикли (`for`):** Якщо вам потрібно додати в колекцію багато однакових елементів або перевірити кілька простих умов, іноді цикл є виправданим. Але часто кращою альтернативою є **параметризовані тести**.
> * **Умовні оператори (`if`):** Слід уникати. Якщо вам здається, що потрібен `if`, скоріш за все, вам потрібно розбити тест на два окремих, для кожної гілки умови.
> * **`try-catch`:** Для перевірки винятків слід завжди використовувати `assertThrows()`, а не ручний `try-catch`.

#### **📌 Що перевіряють:**

*Прагнення до простоти та надійності тестів. Розробник повинен розуміти ризики ускладнення тестового коду.*

* * *

### **7 ❓ Питання:**

Що таке "тестованість коду" (testability) і як її покращити?

> #### **💬 Відповідь:**
>
> **Тестованість** — це властивість програмного коду, яка показує, наскільки легко для нього можна написати unit-тести.
>
> **Код з поганою тестованістю:**
>
> * Жорстко зв'язаний (tightly coupled).
> * Створення об'єктів-залежностей відбувається всередині методів (`new DatabaseClient()`).
> * Використовує багато статичних методів та глобального стану.
> * Методи є дуже довгими і виконують багато завдань.
>
> **Як покращити тестованість (писати testable code):**
>
> 1. **Дотримуватися принципів SOLID:** Особливо Принципу єдиної відповідальності (SRP) та Принципу інверсії залежностей (DIP).
> 2. **Використовувати ін'єкцію залежностей (Dependency Injection):** Замість створення залежностей всередині класу, передавати їх ззовні (через конструктор). Це дозволяє легко замінити реальні залежності на моки в тестах.
> 3. **Писати короткі, сфокусовані методи:** Невеликі методи, що виконують одну дію, набагато легше тестувати.
> 4. **Уникати статичних методів та глобального стану:** Статичні залежності важко замінити на моки, а глобальний стан створює залежність між тестами.
> 5. **Програмувати на основі інтерфейсів,** а не конкретних реалізацій.

#### **📌 Що перевіряють:**

*Розуміння того, як дизайн коду напряму впливає на можливість його тестування. Це ключова навичка для сучасного розробника.*

* * *

### **8 ❓ Питання:**

Чому тестування приватних методів напряму вважається антипатерном? ⭐️

> #### **💬 Відповідь:**
>
> Тестування приватних методів є антипатерном, оскільки воно порушує кілька ключових принципів хорошого дизайну та тестування.
>
> **Причини:**
>
> 1. **Тестується реалізація, а не поведінка:** Unit-тести мають перевіряти **публічний контракт** класу — що він робить, а не як він це робить. Приватні методи є **деталями реалізації**. Якщо ви прив'язуєте тести до них, будь-який рефакторинг внутрішньої структури класу (навіть якщо публічна поведінка не змінилася) зламає ваші тести.
> 2. **Порушення інкапсуляції:** Щоб протестувати приватний метод, вам доведеться "зламати" інкапсуляцію (за допомогою рефлексії або змінивши модифікатор доступу на `public`/`protected`), що є дуже поганою практикою.
> 3. **Сигнал поганого дизайну:** Якщо ви відчуваєте сильну потребу протестувати приватний метод, це часто означає, що цей метод занадто складний і виконує забагато відповідальності. Його варто винести в **окремий клас-помічник** з публічними методами і протестувати вже цей новий клас.
>
> **Правильний підхід:** Тестуйте приватні методи **опосередковано** — через публічні методи, які їх викликають.

#### **📌 Що перевіряють:**

*Глибоке розуміння ООП та його принципів. Це класичне питання для перевірки зрілості інженера.*

* * *

### **9 ❓ Питання:**

Що таке "нестабільні тести" (flaky tests) і як з ними боротися? ⭐️

> #### **💬 Відповідь:**
>
> **Нестабільний (або "плаваючий", "flaky") тест** — це тест, який може як проходити, так і падати без будь-яких змін у вихідному коді. Така непередбачуваність є дуже небезпечною, оскільки вона підриває довіру до всього тестового набору.
>
> **Поширені причини flaky-тестів:**
>
> 1. **Асинхронність:** Тест не чекає завершення асинхронної операції і намагається перевірити результат занадто рано. Іноді операція встигає завершитися (тест проходить), іноді ні (тест падає).
> 2. **Залежність від зовнішніх систем:** Залежність від реальної бази даних, мережевого сервісу, файлової системи, які можуть бути повільними або недоступними.
> 3. **Спільний стан (Shared State):** Залежність від результатів іншого тесту, який може виконуватися в довільному порядку.
> 4. **Випадкові дані:** Використання `new Random()` або поточного часу (`now()`), що робить результат непередбачуваним.
>
> **Як боротися:**
>
> * Використовувати спеціалізовані бібліотеки (як Awaitility) для тестування асинхронності.
> * Ізолювати тести від зовнішніх систем за допомогою моків та стабів.
> * Забезпечувати повну незалежність тестів (використовувати `@BeforeEach`/`@AfterEach` для очищення).
> * "Мокати" залежності від часу та випадковості.

#### **📌 Що перевіряють:**

*Досвід роботи з реальними, складними тестовими наборами. Вміння діагностувати та виправляти flaky-тести є важливою навичкою для підтримки здоров'я CI/CD пайплайну.*

* * *

### **10 ❓ Питання:**

У якому випадку краще писати інтеграційний, а не unit-тест? ⭐️

> #### **💬 Відповідь:**
>
> Unit-тест перевіряє компонент в ізоляції. **Інтеграційний тест** потрібен тоді, коли головна мета — перевірити **правильність взаємодії** цього компонента з іншими частинами системи або з зовнішнім світом.
>
> **Випадки, коли потрібен інтеграційний тест:**
>
> 1. **Взаємодія з базою даних:** Перевірити, що ваші JPQL/SQL запити написані правильно, що мапінг (`@Entity`) коректний і дані правильно зберігаються та зчитуються. Unit-тест з моком репозиторію цього не перевірить.
> 2. **Виклики зовнішніх REST API:** Перевірити, чи правильно ваш клієнт серіалізує запити та десеріалізує відповіді при взаємодії з реальним (або тестовим) екземпляром зовнішнього сервісу.
> 3. **Робота з системою обміну повідомленнями:** Перевірити, що ваш сервіс правильно відправляє та отримує повідомлення з Kafka або RabbitMQ.
> 4. **Повний HTTP-запит:** Перевірити весь ланцюжок від контролера до сервісу та бази даних, щоб переконатися, що всі шари правильно з'єднані.
>
> **Ключова ідея:** Інтеграційний тест потрібен там, де ризик помилки лежить не в логіці одного компонента, а в "клеї", що їх з'єднує.

#### **📌 Що перевіряють:**

*Розуміння меж та цілей unit-тестування. Кандидат повинен знати, що unit-тестів недостатньо для повної впевненості в коректності системи.*

* * *

### **11 ❓ Питання:**

Як підходити до тестування коду, що залежить від часу, наприклад, від `LocalDateTime.now()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Код, що напряму викликає `LocalDateTime.now()`, є **не тестованим**, оскільки його результат змінюється щомиті. Щоб зробити його тестованим, потрібно **винести залежність від часу назовні** і контролювати її в тестах.
>
> **Правильний підхід (з використанням `java.time.Clock`):**
>
> 1. **Інверсія залежностей (DI):** Ваш клас не повинен створювати час сам, а повинен отримувати екземпляр `java.time.Clock` через конструктор.
>
> 2. **Використання `Clock` в коді:** Замість `LocalDateTime.now()`, ви викликаєте `LocalDateTime.now(clock)`.
>
> 3. **Підстановка `Clock` в тестах:**
>     * **У виробничому коді:** ви передаєте `Clock.systemDefaultZone()`.
>     * **У тестовому коді:** ви передаєте `Clock.fixed(...)`, який завжди повертає один і той самий, заздалегідь визначений час.
>
> Це робить тест повністю детермінованим і дозволяє перевіряти логіку, що залежить від часу (наприклад, "чи минув термін дії?").

#### **📝 Приклад тесту:**

```java
@Test
void testExpiryLogic() {
    Instant fixedTime = Instant.parse("2023-01-01T10:00:00Z");
    Clock fixedClock = Clock.fixed(fixedTime, ZoneId.of("UTC"));
    
    MyService service = new MyService(fixedClock);
    // ...
}
```

#### **📌 Що перевіряють:**

*Вміння писати тестований код, застосовуючи патерни проектування (DI). Це одна з класичних складних задач в unit-тестуванні, і знання її вирішення демонструє високий рівень кандидата.*

* * *

### **12 ❓ Питання:**

Що таке Characterization Tests (характеризаційні тести) і коли вони використовуються? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Characterization Tests** — це тести, які пишуться для **існуючого легасі-коду, що не має тестів**.
>
> **Головна мета:** Їхня мета не знайти баги, а **задокументувати та зафіксувати поточну поведінку** системи, якою б вона не була (навіть якщо вона неправильна).
>
> **Процес:**
>
> 1. Ви берете частину легасі-коду.
> 2. Ви пишете тести, які не перевіряють, чи *повинен* код повертати певне значення, а просто **стверджують (`assert`) той результат, який він *фактично* повертає зараз**.
> 3. Ви отримуєте набір "зелених" тестів, який описує поточну поведінку системи.
>
> **Коли це корисно:**
>
> Це перший і найважливіший крок перед **рефакторингом легасі-коду**. Створивши набір характеризаційних тестів, ви отримуєте "сітку безпеки". Тепер ви можете змінювати код і після кожної зміни запускати ці тести. Якщо якийсь з них падає, це означає, що ви ненавмисно змінили існуючу поведінку системи.
>
> Це техніка, яка дозволяє безпечно вносити зміни у код, поведінку якого ви до кінця не розумієте.

#### **📌 Що перевіряють:**

*Знання просунутих технік для роботи з технічним боргом та легасі-системами. Це демонструє досвід роботи зі складними, реальними проектами.*

* * *

### **13 ❓ Питання:**

Що таке "тестовий код як документація"? Поясніть на прикладі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"Тестовий код як документація"** — це ідея про те, що добре написаний набір unit-тестів може слугувати живою, завжди актуальною документацією для виробничого коду.
>
> На відміну від звичайної документації в Confluence чи в коментарях, яка швидко застаріває, тестовий код не може застаріти. Якщо поведінка системи змінюється, а тести не оновлюються, вони почнуть падати, сигналізуючи про невідповідність.
>
> **Як це працює:**
>
> Новий розробник, який прийшов на проект, може не читати тонни документації, а просто відкрити тестовий клас для певного сервісу. Завдяки гарному іменуванню та структурі тестів він може одразу зрозуміти:
>
> * Який публічний API надає цей сервіс.
> * Яка очікувана поведінка для кожного методу.
> * Як обробляються помилки та граничні випадки.
> * Як правильно використовувати цей сервіс.

#### **📝 Приклад:**

Подивившись на тестовий клас `UserServiceTest`, розробник може побачити тести:

* `createUser_WithValidData_ShouldReturnNewUser()`
* `createUser_WithExistingEmail_ShouldThrowException()`
* `getUser_ByExistingId_ShouldReturnUser()`
* `getUser_ByNonExistingId_ShouldReturnEmptyOptional()`
>
> Цей набір тестів є набагато кращою документацією, ніж будь-який текстовий опис.

#### **📌 Що перевіряють:**

*Розуміння ширшої ролі тестів у процесі розробки. Кандидат повинен бачити в тестах не лише інструмент перевірки, а й інструмент комунікації та документації.*

* * *

### **14 ❓ Питання:**

Уявіть, що ваш тест проходить локально, але падає на CI-сервері. Які можуть бути причини? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже поширена і неприємна проблема. Причини зазвичай пов'язані з відмінностями у середовищі.
>
> **Найімовірніші причини:**
>
> 1. **Залежність від операційної системи:** Ваш тест може залежати від специфічних для вашої ОС речей, як-от формат шляхів до файлів (`\` у Windows проти `/` в Linux), кінець рядка (`CRLF` vs `LF`).
>
> 2. **Залежність від локалі або часового поясу:** Тест може неправильно працювати з форматуванням дат або рядків, оскільки на CI-сервері встановлена інша локаль (напр., `en_US`, а у вас `uk_UA`).
>
> 3. **Неявний порядок тестів:** Ваш набір тестів може проходити локально "випадково" через певний порядок виконання, який є іншим на CI. Це свідчить про наявність залежностей між тестами.
>
> 4. **Зовнішні залежності:** Тест покладається на файл, який існує на вашій машині, але відсутній на CI-агенті. Або він намагається з'єднатися із сервісом, доступ до якого є тільки з вашої локальної мережі.
>
> 5. **Різні версії залежностей:** На CI може використовуватися трохи інша версія Java, Maven/Gradle, або якоїсь бібліотеки, що призводить до іншої поведінки.
>
> **Як боротися:**
>
> * Писати повністю незалежні та ізольовані тести.
> * Ніколи не "хардкодити" шляхи, формати дат тощо.
> * Використовувати Docker для запуску тестів у такому ж середовищі, як і локально.

#### **📌 Що перевіряють:**

*Досвід роботи з CI/CD та навички діагностики складних, середовище-залежних проблем. Це ознака досвідченого інженера.*

* * *

### **15 ❓ Питання:**

Чи потрібно прагнути до 100% покриття коду unit-тестами? Поясніть компроміси. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не потрібно.** Погоня за 100% покриттям є класичним антипатерном і часто приносить більше шкоди, ніж користі.
>
> **Чому 100% — це погана мета:**
>
> 1. **Закон спадної віддачі (Diminishing Returns):** Досягти перших 80% покриття відносно легко і дуже корисно — ви тестуєте основну бізнес-логіку. Кожен наступний відсоток (80-100%) дається набагато важче і приносить менше користі. Останні відсотки — це зазвичай тестування тривіальних геттерів, сеттерів або складних, рідко виникаючих гілок, що вимагає величезних зусиль.
> 2. **Витрати перевищують користь:** Час, витрачений на досягнення 100% покриття, можна було б використати на написання більш значущих інтеграційних тестів, дослідницьке тестування або розробку нового функціоналу.
> 3. **100% покриття не гарантує відсутність багів:** Тест може "зачепити" рядок коду, але нічого не перевірити, або перевірити не те. Якість тестів набагато важливіша за їх кількість чи покриття.
>
> **Який рівень покриття є хорошим?**
>
> Зазвичай **70-90%** є хорошою, прагматичною метою для більшості проектів. Головне — переконатися, що **вся критично важлива бізнес-логіка** покрита на 100%. Для простих CRUD-операцій або тривіального коду покриття може бути нижчим.
>
> **Висновок:** Покриття коду — це корисний **індикатор**, а не **мета**. Його слід використовувати, щоб знаходити "сліпі плями" (непротестовані частини коду), а не як самоціль.

#### **📌 Що перевіряють:**

*Прагматичність, зрілість та бізнес-орієнтоване мислення. Кандидат повинен розуміти, що ресурси завжди обмежені, і вміти приймати рішення на основі компромісів та аналізу ризиків.*

* * *
