---
layout: default
title: "Static Factory Method"
parent: "Patterns and Recursion"
grand_parent: "Java Core"
nav_order: 3
---

# Static Factory Method (Статичний фабричний метод)

* короткий опис: Статичний метод, що повертає екземпляр класу (зазвичай з певною логікою створення) замість прямого використання конструктора.

* поняття: `Static Factory Method`, `of()`, `from()`, `valueOf()`, `instance()`, `getInstance()`, `newInstance()`.

* приклади: `User.of("Ivan")`, `Dog.of()` (повернення підтипу), `List.of("a", "b", "c")`, `Integer.valueOf("42")`.

* ключові моменти: Дозволяє іменовані методи (збільшує читабельність), може повертати вже існуючі (кешовані) об'єкти, дозволяє повертати підтипи. Обмежує наслідування, якщо всі конструктори приватні.

* що важливо знати на співбесіді: Визначення, переваги над конструктором (іменування, кешування, повернення підтипів, додаткова логіка), недоліки (обмеження наслідування, неочевидність використання), стандартні приклади в `Java API`.

### **✅ Офіційне визначення:**

**Static Factory Method** — це метод, який повертає екземпляр класу, зазвичай з певною логікою створення, замість прямого використання конструктора. Такий метод є статичним (**`static`**) і зазвичай має іменовану назву, як-от **`of(), from(), valueOf()`** тощо.

### **🧠 Простими словами:**

Це метод, який створює об'єкт, але не через **`new`**, а через щось типу **`User.of(...)`**. Він дає більше контролю — наприклад, можна повернути вже існуючий об’єкт, обрати підтип, кешувати, логувати тощо. Як фішка: ти наче “робиш вигляд”, що створюєш новий об’єкт, але насправді вирішуєш — як саме його створити.

---

## **📊 Поширені назви static factory methods:**

| Назва методу | Що означає |
| :---: | ----- |
| **`of()`** | Просте створення з параметрів |
| **`from()`** | Перетворення з іншого типу |
| **`valueOf()`** | Альтернатива **`of()`** з іншим змістом |
| **`instance()`** | Повернення вже існуючого екземпляра |
| **`getInstance()`** | Часто використовується в **`Singleton`** |
| **`newInstance()`** | Завжди створює новий екземпляр |

---

## **Пояснення на пальцях (💡)**

**Навіщо це потрібно?**  
Часто треба більше, ніж просто виклик **`new`**. Наприклад:

* хочеш повертати кешований обʼєкт (економія памʼяті),
* хочеш створити обʼєкт іншого класу (підтипу),
* або дати зрозумілу назву методу (**`ofString()`** звучить краще ніж **`new Object(...)`**).

**У чому фішка?**  
Іменовані методи дають читабельність, гнучкість і інкапсуляцію логіки створення.

**Типові граблі?**  
❗ Не можна наслідувати (якщо всі конструктори **`private`**, і ти хочеш робити спадкоємця — буде проблема).  
❗ Користувачу не завжди зрозуміло, що треба викликати метод, а не конструктор.

**Коли застосовувати?**  
Коли хочеш мати повний контроль над створенням об’єкта, особливо якщо потрібно додаткову логіку

---

## **Корисні приклади (🧪)**

### **Простий приклад:**

```java
public class User {
    private String name;

    private User(String name) {
        this.name = name;
    }

    public static User of(String name) {
        return new User(name);
    }
}
```

### **🔻 Використання:**

```java
User user = User.of("Ivan");
```

### **🔻 Повернення підтипу:**

```java
interface Animal {}

class Dog implements Animal {
    public static Animal of() {
        return new Dog(); // повертає підтип
    }
}
```

### **🔻 Стандартні приклади в Java:**

```java
List<String> list = List.of("a", "b", "c"); // Java 9+
Integer integer = Integer.valueOf("42");    // кешування чисел
```

---

## **Порівняння (⚖️)**

| Параметр | Static Factory Method | Конструктор (`new`) |
| :---: | ----- | ----- |
| **Іменування** | ✅ Може мати зрозумілу назву | ❌ Завжди назва класу |
| **Повернення підтипів** | ✅ Можна | ❌ Неможливо |
| **Повторне використання** | ✅ Може повертати кешовані обʼєкти | ❌ Завжди новий обʼєкт |
| **Наслідування** | ❌ Не можна розширити через subclass | ✅ Можна наслідувати |
| **Видимість** | ✅ Може приховати конструктор | ❌ Конструктор завжди відкритий |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“Factory of choice”** — **`of()`** \= вибір способу створення.

Замість **`new Something(...),`** можна "вибрати" кращий варіант через фабрику

---

## **🟩 Підсумок:**

**`Static Factory Method`** — це зручна та потужна альтернатива конструкторам. Вона дозволяє створювати об’єкти з додатковою логікою, контролювати повернення екземплярів, підвищити читабельність коду. В Java це активно використовується (наприклад, **`List.of(), Integer.valueOf()`**) і вважається кращою практикою у багатьох випадках.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке статичний фабричний метод (Static Factory Method)?

> #### **💬 Відповідь:**
>
> **Статичний фабричний метод** — це **статичний метод**, який повертає екземпляр класу. Це альтернатива прямому використанню публічного конструктора.
>
> Замість того, щоб клієнтський код викликав `new MyClass(...)`, він викликає `MyClass.of(...)`, `MyClass.createInstance()`, або інший метод зі зрозумілою назвою.
>
> Цей шаблон **не** є тим самим, що і шаблон `Factory Method` або `Abstract Factory` з книги "банди чотирьох" (GoF), хоча він і вирішує схожі задачі створення об'єктів. Це проста ідіома або конвенція в об'єктно-орієнтованих мовах, як-от Java.

#### **📝 Аналогія:**

Уявіть, що ви замовляєте каву.

* **Конструктор:** `new Coffee("Espresso", "small")` — ви самі вказуєте всі деталі, як бариста.
* **Статичний фабричний метод:** `Coffee.getStandardEspresso()` — ви просто просите "стандартний еспресо", а кафе (фабрика) саме знає, як його приготувати.

#### **📌 Що перевіряють:**

*Розуміння того, що це ідіома, а не повноцінний шаблон GoF. Кандидат не повинен плутати його з `Factory Method`.*

* * *

### **2 ❓ Питання:**

Назвіть основні переваги статичних фабричних методів над публічними конструкторами.

> #### **💬 Відповідь:**
>
> Джошуа Блох у книзі "Effective Java" виділяє кілька ключових переваг:
>
> 1. **Мають імена, що говорять самі за себе.** Назва методу (наприклад, `from()`, `of()`, `getInstance()`) може чітко пояснити, що саме він робить або які параметри приймає, на відміну від конструкторів, які мають лише назву класу.
>
> 2. **Не зобов'язані створювати новий об'єкт при кожному виклику.** Вони можуть повертати кешовані екземпляри, як у `Integer.valueOf()`, або забезпечувати реалізацію `Singleton`. Це дозволяє значно покращити продуктивність.
>
> 3. **Можуть повертати об'єкт будь-якого підтипу свого типу повернення.** Це дає величезну гнучкість. Наприклад, `List.of()` може повернути не `ArrayList`, а спеціалізований імутабельний внутрішній клас.
>
> 4. **Зменшують "балакучість" коду при створенні параметризованих типів.** Замість `Map<String, List<String>> map = new HashMap<String, List<String>>();` можна було написати `Map<String, List<String>> map = HashMap.newInstance();` (до появи оператора "діамант" `<>` в Java 7).
>
> 5. **Логіка створення об'єкта може бути більш складною.** Всередині фабричного методу можна розмістити будь-яку логіку перед викликом конструктора.

#### **📌 Що перевіряють:**

*Знання ключових переваг, що є основою для будь-якої розмови на цю тему. Згадка про книгу "Effective Java" буде великим плюсом.*

* * *

### **3 ❓ Питання:**

Поясніть на прикладі, як статичний фабричний метод надає "імена, що говорять самі за себе".

> #### **💬 Відповідь:**
>
> Конструктори обмежені тим, що вони повинні мати однакову назву з класом. Якщо у вас є кілька способів створення об'єкта, вам доведеться розрізняти їх тільки за сигнатурою (типами та кількістю параметрів), що не завжди зручно.
>
> **Приклад з конструкторами (незручно):**
> Уявімо клас для роботи з комплексними числами.
>
> ```java
> public class Complex {
>     // Конструктор з декартових координат
>     public Complex(double real, double imag) { /* ... */ }
>
>     // Як створити конструктор для полярних координат?
>     // new Complex(double rho, double theta) - НЕ СКОМПІЛЮЄТЬСЯ,
>     // бо сигнатура така ж (double, double)
> }
> ```
>
> **Приклад зі статичними фабричними методами (зручно):**
>
> ```java
> public class Complex {
>     private Complex(double real, double imag) { /* ... */ }
>
>     public static Complex fromCartesian(double real, double imag) {
>         return new Complex(real, imag);
>     }
>
>     public static Complex fromPolar(double rho, double theta) {
>         return new Complex(rho * Math.cos(theta), rho * Math.sin(theta));
>     }
> }
>
> // Використання стає дуже читабельним:
> Complex c1 = Complex.fromCartesian(3, 4);
> Complex c2 = Complex.fromPolar(5, 0.927);
> ```
>
> Імена `fromCartesian` та `fromPolar` чітко пояснюють, як саме створюється об'єкт.

#### **📌 Що перевіряють:**

*Здатність навести конкретний приклад, де перевага іменування стає очевидною.*

* * *

### **4 ❓ Питання:**

Наведіть приклади використання статичних фабричних методів у стандартній бібліотеці Java.

> #### **💬 Відповідь:**
>
> Статичні фабричні методи дуже широко використовуються в Java API.
>
> * **`Integer.valueOf(String s)` або `Integer.valueOf(int i)`:**
>   * Кешує об'єкти для значень від -128 до 127. Для цих значень він не створює новий об'єкт, а повертає екземпляр з кешу.
>
> * **`List.of("a", "b", "c")`, `Set.of()`, `Map.of()` (з Java 9):**
>   * Повертають не `ArrayList` або `HashSet`, а спеціалізовані **імутабельні (незмінні)** реалізації. Це є прикладом повернення підтипу.
>
> * **`Optional.of(value)`, `Optional.ofNullable(value)`, `Optional.empty()`:**
>   * Надають різні способи створення об'єкта `Optional` залежно від того, чи є значення `null`.
>
> * **`LocalDate.now()`, `LocalDate.of(2023, 1, 1)` (з `java.time`):**
>   * Вся бібліотека `java.time` спроектована з використанням статичних фабричних методів замість публічних конструкторів, що робить API дуже читабельним і гнучким.
>
> * **`Executors.newFixedThreadPool(int n)`:**
>   * Фабрика, що створює та конфігурує пул потоків, приховуючи від користувача складність створення `ThreadPoolExecutor`.

#### **📌 Що перевіряють:**

*Загальну ерудицію та знання стандартної бібліотеки. Здатність "впізнавати" патерни в існуючому коді.*

* * *

### **5 ❓ Питання:**

Які є недоліки у використання статичних фабричних методів? ⭐️

> #### **💬 Відповідь:**
>
> Незважаючи на численні переваги, є два основних недоліки:
>
> 1. **Класи, що містять тільки приватні конструктори та статичні фабричні методи, не можна успадковувати.** Оскільки підклас повинен мати можливість викликати конструктор суперкласу (`super()`), а всі конструктори є приватними, успадкування стає неможливим. Це може бути й перевагою, оскільки заохочує використовувати композицію замість успадкування.
>
> 2. **Їх не завжди легко знайти розробникам.** На відміну від конструкторів, які завжди чітко видно в документації (Javadoc) в окремому розділі, статичні фабричні методи виглядають як будь-які інші статичні методи. Розробник, який не знайомий з класом, може не одразу зрозуміти, як саме створювати його екземпляри.
>
> Щоб вирішити другу проблему, існують загальноприйняті конвенції іменування (`of`, `from`, `valueOf` і т.д.), які дають підказку про призначення методу.

#### **📌 Що перевіряють:**

*Здатність критично оцінювати патерни і бачити не тільки їхні переваги, але й недоліки та компроміси.*

* * *

### **6 ❓ Питання:**

У чому різниця між патерном `Static Factory Method` та `Factory Method` з класифікації GoF? ⭐️

> #### **💬 Відповідь:**
>
> Це два абсолютно різних поняття, які часто плутають через схожу назву.
>
> * **`Static Factory Method` (Статичний фабричний метод):**
>   * Це **один статичний метод** всередині класу, який просто повертає екземпляр цього ж класу (або його підтипу).
>   * Це **не** є окремим шаблоном GoF, це скоріше **ідіома програмування**.
>   * Не використовує успадкування для своєї реалізації.
>
> * **`Factory Method` (Фабричний метод) - шаблон GoF:**
>   * Це **повноцінний поведінковий шаблон**.
>   * Його мета — делегувати створення об'єктів **підкласам**.
>   * Він реалізується через **успадкування**: базовий клас визначає абстрактний "фабричний метод", а кожен підклас надає свою реалізацію, яка повертає свій тип об'єкта.
>   * Це **не статичний** метод.
>
> **Коротко:** `Static Factory Method` — це один метод. `Factory Method` — це патерн, заснований на успадкуванні для делегування створення об'єктів нащадкам.

#### **📌 Що перевіряють:**

*Глибоке знання класифікації шаблонів GoF. Здатність розрізняти схожі за назвою, але різні за суттю концепції.*

* * *

### **7 ❓ Питання:**

Наведіть поширені конвенції іменування для статичних фабричних методів.

> #### **💬 Відповідь:**
>
> Існують загальноприйняті назви, які допомагають зрозуміти призначення методу:
>
> * **`from(…)`**: Метод перетворення типів. Приймає один аргумент і створює екземпляр поточного класу на його основі.
>   * Приклад: `Date d = Date.from(instant);`
>
> * **`of(…)`**: Більш загальна версія `from`, агрегує кілька параметрів для створення екземпляра.
>   * Приклад: `Set<Rank> faceCards = EnumSet.of(JACK, QUEEN, KING);`
>
> * **`valueOf(…)`**: Більш "балакуча" альтернатива `of` та `from`. Часто використовується для перетворення з базових типів.
>   * Приклад: `Integer i = Integer.valueOf("42");`
>
> * **`instance()`** або **`getInstance()`**: Зазвичай використовується в патерні `Singleton` (або схожих), повертаючи єдиний або кешований екземпляр. Назва не гарантує, що параметри будуть тими ж.
>   * Приклад: `ConfigurationManager.getInstance();`
>
> * **`create…()`** або **`newInstance()`**: Гарантує, що при кожному виклику буде створено **новий** екземпляр.
>   * Приклад: `Object newArray = Array.newInstance(classObject, arrayLen);`
>
> Дотримання цих конвенцій робить API більш передбачуваним та легким для розуміння.

#### **📌 Що перевіряють:**

*Знання стандартних конвенцій іменування, що є ознакою досвідченого розробника.*

* * *

### **8 ❓ Питання:**

Як статичні фабричні методи допомагають реалізувати шаблон `Singleton`? ⭐️

> #### **💬 Відповідь:**
>
> Статичний фабричний метод є **ключовим елементом** реалізації патерну `Singleton`.
>
> **Механізм такий:**
>
> 1. Конструктор класу робиться `private`. Це унеможливлює створення екземплярів класу ззовні через `new`.
>
> 2. Всередині класу створюється єдиний екземпляр: `private static final Singleton INSTANCE = new Singleton();`.
>
> 3. Створюється **публічний статичний фабричний метод**, зазвичай з назвою `getInstance()`, який просто повертає цей єдиний екземпляр.
>
>     ```java
>     public class MySingleton {
>         private static final MySingleton INSTANCE = new MySingleton();
>
>         private MySingleton() { /* ... */ }
>
>         public static MySingleton getInstance() {
>             return INSTANCE;
>         }
>     }
>     ```
>
> Таким чином, статичний фабричний метод `getInstance()` стає єдиною "точкою входу" для отримання об'єкта, що дозволяє класу повністю контролювати свій життєвий цикл і гарантувати існування лише одного екземпляра.

#### **📌 Що перевіряють:**

*Розуміння того, як різні ідіоми та патерни можуть використовуватися разом для досягнення певної мети.*

* * *

### **9 ❓ Питання:**

Як перевага "можуть повертати об'єкт будь-якого підтипу" використовується в `java.util.Collections`? ⭐️

> #### **💬 Відповідь:**
>
> Ця перевага яскраво продемонстрована в утилітарному класі `Collections`.
>
> Наприклад, метод `Collections.unmodifiableList(list)`:
>
> * Приймає на вхід будь-який `List`.
> * Повертає об'єкт, який також є `List`.
> * Але насправді він повертає не екземпляр `ArrayList` чи `LinkedList`, а екземпляр **спеціалізованого внутрішнього (private) класу**, наприклад, `UnmodifiableList`, який "обгортає" оригінальний список і в якому всі методи модифікації (`add`, `remove`, `set`) кидають `UnsupportedOperationException`.
>
> Клієнтський код не знає і не повинен знати про існування класу `UnmodifiableList`. Він просто працює з інтерфейсом `List`.
>
> Це дозволяє API надавати спеціалізовані реалізації, приховуючи деталі і не "засмічуючи" публічний API десятками допоміжних класів. Те ж саме стосується `Collections.synchronizedList()`, `Collections.checkedList()` та інших.

#### **📌 Що перевіряють:**

*Глибоке розуміння переваг статичних фабрик та вміння навести просунуті приклади з JDK, які демонструють їхню гнучкість.*

* * *

### **10 ❓ Питання:**

Поясніть, як за допомогою статичного фабричного методу можна реалізувати кешування об'єктів. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Оскільки статичний фабричний метод не зобов'язаний створювати новий об'єкт, він може спочатку перевірити наявність потрібного об'єкта в кеші і, якщо він там є, повернути його.
>
> Класичним прикладом є `Integer.valueOf(int i)`.
>
> **Як це працює "під капотом":**
>
> ```java
> public final class Integer {
>     private static class IntegerCache {
>         static final Integer[] cache;
>         // ... в статичному блоці заповнюється масив для значень -128..127
>     }
>
>     public static Integer valueOf(int i) {
>         // Якщо значення в діапазоні кешу
>         if (i >= -128 && i <= 127) {
>             // Повертаємо об'єкт з кешу
>             return IntegerCache.cache[i + 128];
>         }
>         // Інакше створюємо новий об'єкт
>         return new Integer(i);
>     }
> }
> ```
>
> Таким чином, для найпоширеніших цілих чисел завжди перевикористовуються ті самі об'єкти, що зменшує навантаження на пам'ять та збирач сміття.
>
> Цей самий підхід можна застосувати і у власних класах для кешування об'єктів, що часто створюються з однаковими параметрами (патерн `Flyweight`).

#### **📌 Що перевіряють:**

*Здатність пояснити механізм кешування на прикладі стандартної бібліотеки. Це демонструє розуміння оптимізацій продуктивності.*

* * *

### **11 ❓ Питання:**

Який зв'язок між статичними фабричними методами та патерном `Builder`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вони часто використовуються разом для створення більш чистого та гнучкого API.
>
> Статичний фабричний метод може виступати як **точка входу для отримання екземпляра `Builder`**.
>
> **Приклад:**
>
> ```java
> public class User {
>     // ...
>
>     // Статичний фабричний метод, що повертає Builder
>     public static Builder builder() {
>         return new Builder();
>     }
>
>     public static class Builder {
>         // ...
>         public User build() { return new User(this); }
>     }
> }
>
> // Використання:
> User user = User.builder()
>                 .name("John")
>                 .age(30)
>                 .build();
> ```
>
> Це виглядає трохи чистіше і більш схоже на Fluent API, ніж `new User.Builder()`. Такий підхід робить API класу більш цілісним — для будь-якої дії (отримання білдера, створення з параметрів) клієнт звертається до самого класу `User`, а не до його внутрішніх частин.

#### **📌 Що перевіряють:**

*Розуміння того, як різні патерни та ідіоми можуть бути елегантно скомбіновані для покращення дизайну API.*

* * *

### **12 ❓ Питання:**

Якщо у класі є і публічні конструктори, і статичні фабричні методи, коли клієнт має використовувати одне, а коли інше? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання дизайну API. Зазвичай така ситуація виникає, коли:
>
> * **Конструктор** призначений для **базового, стандартного створення** об'єкта.
> * **Статичні фабричні методи** надають **спеціалізовані або більш зручні** способи створення.
>
> **Приклад: `String`**
>
> * **`new String("...")`**: Ви можете використати конструктор, якщо вам **гарантовано** потрібен новий об'єкт `String` в купі, який не буде в пулі. Це рідкісний випадок.
> * **Статична ініціалізація (`String s = "..."`) або `valueOf()`**: Використовуються в 99% випадків, оскільки вони дозволяють JVM використовувати пул рядків для оптимізації.
>
> **Загальне правило:**
>
> Розробник API повинен чітко документувати, в яких випадках який спосіб слід використовувати. Якщо такої документації немає, то загальна перевага надається **статичним фабричним методам** через їхні переваги (кешування, іменування тощо). Публічний конструктор залишається як основний, але, можливо, менш гнучкий механізм.

#### **📌 Що перевіряють:**

*Здатність аналізувати API і робити обґрунтований вибір на основі конвенцій та потенційних переваг.*

* * *

### **13 ❓ Питання:**

Чому клас, що не успадковується, який надає тільки статичні фабричні методи (з приватними конструкторами), легше змінити в майбутньому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що ви отримуєте **повний контроль над створенням екземплярів**.
>
> Оскільки клієнти не прив'язані до конкретних конструкторів (`new`), ви можете радикально змінити внутрішню реалізацію, не змінюючи публічне API.
>
> **Що можна змінити:**
>
> 1. **Додати кешування:** Ви можете в будь-який момент додати логіку кешування всередину фабричного методу, і клієнти про це навіть не дізнаються.
>
> 2. **Змінити клас, що повертається:** Сьогодні ваш метод `MyClass.create()` може повертати `MyClass`, а завтра, для оптимізації, ви можете зробити так, щоб він повертав `MyOptimizedSubClass` (підтип). Клієнти, які працюють з `MyClass`, цього не помітять.
>
> 3. **Перейти на `Singleton`:** Якщо з'ясується, що об'єкт має бути одинаком, ви можете просто змінити логіку фабричного методу, щоб він завжди повертав той самий екземпляр.
>
> Таким чином, статичні фабричні методи є **додатковим рівнем абстракції** над створенням об'єктів, що робить систему значно більш гнучкою до майбутніх змін та рефакторингу.

#### **📌 Що перевіряють:**

*Розуміння того, як інкапсуляція процесу створення об'єктів сприяє підтримуваності та розширюваності коду.*

* * *

### **14 ❓ Питання:**

Розглянемо `EnumSet.of(...)`. Як він використовує перевагу статичних фабрик "повертати об'єкт будь-якого підтипу"? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `EnumSet.of(...)` — це один з найяскравіших прикладів цієї переваги.
>
> **Як він працює:**
>
> Він аналізує, скільки елементів має `enum`, для якого створюється множина.
>
> * **Якщо елементів у `enum` 64 або менше**, він повертає екземпляр внутрішнього класу `RegularEnumSet`. Цей клас зберігає елементи множини дуже ефективно — у вигляді одного `long` (64 біти), де кожен біт відповідає за наявність або відсутність `enum`-константи.
>
> * **Якщо елементів у `enum` більше 64**, він повертає екземпляр іншого внутрішнього класу — `JumboEnumSet`, який використовує масив `long[]` для зберігання станів.
>
> **Що це дає:**
>
> * **Продуктивність:** Для найпоширенішого випадку (невеликі `enum`) використовується надзвичайно ефективна реалізація, що базується на бітових операціях.
> * **Гнучкість:** Для великих `enum` використовується інша реалізація, яка може їх обробити.
> * **Приховування деталей:** Клієнт нічого не знає про `RegularEnumSet` або `JumboEnumSet`. Він працює тільки з інтерфейсом `EnumSet`.
>
> Статичний фабричний метод дозволяє `EnumSet` "розумно" вибирати найкращу реалізацію в залежності від вхідних даних, що було б неможливо з публічним конструктором.

#### **📌 Що перевіряють:**

*Дуже глибокі знання JDK. Здатність аналізувати оптимізації, реалізовані в стандартній бібліотеці.*

* * *

### **15 ❓ Питання:**

Припустимо, у вас є вибір: зробити клас не-успадковуваним ( `final`), щоб безпечно використовувати статичні фабричні методи, або залишити його відкритим для успадкування. Як би ви приймали рішення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний архітектурний компроміс. Рішення має базуватися на принципі **"проектування для успадкування"**.
>
> Запитання, які варто собі поставити:
>
> 1. **Чи є реальна, передбачувана потреба в успадкуванні цього класу?** Чи дійсно інші розробники будуть створювати його підкласи, розширюючи його стан чи поведінку?
> 2. **Чи є клас повністю готовим до успадкування?** Це означає, що його внутрішня реалізація не покладається на те, що він є `final`, а методи не викликають інші методи, які можуть бути небезпечно перевизначені.
> 3. **Наскільки важливі переваги статичних фабрик (кешування, гнучкість повернення підтипів) для цього конкретного класу?**
>
> **Мій підхід був би таким:**
>
> Слідуючи пораді Джошуа Блоха, **"проектуйте та документуйте для успадкування, або забороняйте його"**.
>
> * **За замовчуванням** я б надавав перевагу тому, щоб зробити клас `final`. Це робить його простішим, безпечнішим і дозволяє повною мірою використовувати переваги статичних фабричних методів. Це слідує принципу "композиція краща за успадкування".
> * Я б залишив клас відкритим для успадкування тільки в тому випадку, якщо б це було **явним вимогам дизайну** (наприклад, я створюю базовий клас для фреймворку, який користувачі мають розширювати). У цьому випадку мені довелося б пожертвувати деякими перевагами статичних фабрик і більш ретельно проектувати API, щоб він був безпечним для розширення.

#### **📌 Що перевіряють:**

*Здатність приймати обґрунтовані архітектурні рішення, оцінюючи компроміси. Зрілість як розробника, який думає про майбутню підтримку та використання коду.*
