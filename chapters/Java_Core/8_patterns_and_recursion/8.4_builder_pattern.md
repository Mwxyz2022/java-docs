---
layout: default
title: "Builder Pattern"
parent: "Patterns and Recursion"
grand_parent: "Java Core"
nav_order: 4
---

# Builder Pattern (Патерн "Будівельник")

* короткий опис: Породжувальний шаблон проєктування, що дозволяє створювати складні об'єкти крок за кроком, відокремлюючи процес побудови від представлення.

* поняття: `Builder Pattern`, `Основний клас`, `Builder` (статичний вкладений клас), `withX()` методи, `build()` метод, `Телескопічний конструктор`.

* приклади: `new User.Builder().withName("Ivan").withAge(25).build()`, `StringBuilder`, `java.time.LocalDateTime.Builder`, `Stream.Builder`.

* ключові моменти: Вирішує проблему "телескопічного" конструктора з багатьма параметрами, покращує читабельність, підтримує створення `immutable` об'єктів, спрощує розширення.

* що важливо знати на співбесіді: Визначення, основні компоненти, переваги над конструкторами (читабельність, опціональні параметри, розширюваність), застосування для `immutable` об'єктів, принцип роботи методу `build()`

### **✅ Офіційне визначення:**

**Builder Pattern** — це породжувальний шаблон проєктування, який дозволяє створювати складні обʼєкти крок за кроком.
Будівельник дає можливість відокремити процес побудови об’єкта від його представлення, щоб той самий процес створення міг використовуватися для різних представлень.

### **🧠 Простими словами:**

Це підхід, при якому ти створюєш обʼєкт поетапно, викликаючи методи типу **`setName(), setAge()`** і в кінці — **`build()`** Особливо зручно, коли багато параметрів і не всі обов'язкові. Як конструктор із мільйоном опцій, але красиво.

---

## **📊 Структура класів:**

| Компонент         | Опис                                                                             |
|-------------------|----------------------------------------------------------------------------------|
| **Основний клас** | Клас, об’єкти якого ми створюємо (**`User`**)                                    |
| **Builder**       | Статичний вкладений клас (**`User.Builder`**) з полями та методами **`withX()`** |
| **Метод build()** | Завершує побудову і повертає готовий об’єкт                                      |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**
  
Щоб не мати великий "телескопічний" конструктор з 10+ параметрами — частина з них не обов’язкова. Замість цього —
поетапне, зручне і читабельне створення об’єкта.

### **У чому фішка?**  

🔹 Гарна читабельність  
🔹 Можна створювати **`immutable`** обʼєкти  
🔹 Легко додавати нові параметри, не порушуючи існуючий код

### **Типові граблі?**

* Забути викликати **`.build()`** — отримаєш помилку або **`null`**.
* Перенавантажити builder — робити з нього "смітник" з усіма можливими налаштуваннями.

### **Коли застосовувати?**

* Якщо конструктор має більше 3-4 параметрів
* Коли потрібні комбінації з опціональних полів
* Коли хочеш зробити обʼєкт незмінним (**`final`** поля)

---

## **Корисні приклади (🧪)**

### **🔻 Java-приклад класу з Builder:**

```java
public class User {
    private final String name;
    private final int age;
    private final String email;

    private User(Builder builder) {
        this.name = builder.name;
        this.age = builder.age;
        this.email = builder.email;
    }

    public static class Builder {
        private String name;
        private int age;
        private String email;

        public Builder withName(String name) {
            this.name = name;
            return this;
        }

        public Builder withAge(int age) {
            this.age = age;
            return this;
        }

        public Builder withEmail(String email) {
            this.email = email;
            return this;
        }

        public User build() {
            return new User(this);
        }
    }
}
```

### **🔻 Використання:**

```java
User user = new User.Builder()
        .withName("Ivan")
        .withAge(25)
        .withEmail("ivan@example.com")
        .build();
```

---

## **Порівняння (⚖️)**

| Підхід                | Builder Pattern                        | Телескопічні конструктори (`new A(x, y, z, ...)`) |
|-----------------------|----------------------------------------|---------------------------------------------------|
| Читабельність         | ✅ Висока, зрозумілі іменовані поля     | ❌ Важко зрозуміти, що за параметри                |
| Опціональні параметри | ✅ Просто реалізуються                  | ❌ Треба створювати багато перегрузок              |
| Immutable підтримка   | ✅ Легко зробити об’єкт незмінним       | ✅/❌ Залежить від реалізації                       |
| Розширюваність        | ✅ Легко додати нове поле без лома коду | ❌ Потрібно змінювати всі конструктори             |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Builder \= “Step-by-step”**

Як будівництво дому: спочатку фундамент, потім стіни, потім дах -> `.build()` -> готово\!

---

## **🟩 Підсумок:**

Builder — це шаблон для створення складних об'єктів із багатьма параметрами, особливо якщо частина з них опціональна.
Він покращує читабельність, спрощує підтримку, робить код чистим і розширюваним. У Java — це популярна практика, яку варто знати та використовувати.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке шаблон проєктування `Builder` і яку основну проблему він вирішує?

> #### **💬 Відповідь:**
>
> **`Builder` (Будівельник)** — це породжувальний шаблон проєктування, який дозволяє створювати **складні об'єкти крок за кроком**. Він відокремлює процес конструювання об'єкта від його фінального представлення, що дозволяє використовувати той самий процес побудови для створення різних варіацій об'єкта.
>
> **Основна проблема, яку він вирішує:**
>
> Він є вирішенням проблеми **"телескопічного конструктора" (telescoping constructor)**. Ця проблема виникає, коли для створення об'єкта потрібно передати багато параметрів, частина з яких є опціональними. Щоб це підтримати, доводиться створювати безліч перевантажених конструкторів з різними комбінаціями параметрів, що робить код громіздким, нечитабельним і складним для підтримки.
>
> `Builder` дозволяє задавати параметри об'єкта поетапно, через іменовані методи, і викликати фінальний метод `build()` для отримання готового об'єкта.

#### **📝 Аналогія:**</h4>

Уявіть, що ви замовляєте піцу.

* **Телескопічний конструктор:** `new Pizza("large", "thin", "tomato", "mozzarella", "salami", null, "olives")` — жахливо, легко помилитися.
* **Builder:** `new Pizza.Builder("large").withSauce("tomato").withCheese("mozzarella").withTopping("salami").withTopping("olives").build()` — зрозуміло, читабельно, гнучко.

#### **📌 Що перевіряють:**

*Розуміння основної проблеми, яку вирішує `Builder`, — боротьба з великою кількістю параметрів у конструкторі.*

* * *

### **2 ❓ Питання:**

Назвіть основні компоненти класичної реалізації шаблону `Builder`.

> #### **💬 Відповідь:**
>
> Класична реалізація `Builder` (як описано у "Effective Java") складається з двох основних класів:
>
> 1. **Основний клас (Product):**
>     * Це клас, об'єкти якого ми хочемо створювати (наприклад, `User`, `HttpRequest`).
>     * Його поля зазвичай є `final` (щоб зробити об'єкт імутабельним).
>     * Він має **приватний конструктор**, який приймає на вхід об'єкт `Builder`. Цей конструктор ініціалізує поля основного класу даними з білдера.
>
> 2. **Клас `Builder`:**
>     * Зазвичай реалізується як **статичний вкладений (static nested) клас** всередині основного.
>     * Він має поля, що дублюють поля основного класу, для поступового накопичення даних.
>     * Надає **"fluent" методи** (часто з префіксами `with` або без них, наприклад `name(...)`), які встановлюють значення полів і повертають `this`, що дозволяє створювати ланцюжки викликів.
>     * Має **публічний метод `build()`**, який викликає приватний конструктор основного класу (передаючи себе, `this`), і повертає готовий, сконструйований об'єкт.

#### **📌 Що перевіряють:**

*Знання стандартної структури шаблону та ролей його компонентів.*

* * *

### **3 ❓ Питання:**

Які основні переваги шаблону `Builder` над конструкторами?

> #### **💬 Відповідь:**
>
> `Builder` має кілька значних переваг:
>
> 1. **Покращення читабельності:** Код створення об'єкта стає дуже читабельним. Іменовані методи (`.withName("John")`) набагато зрозуміліші, ніж просто набір значень у конструкторі.
>
> 2. **Робота з опціональними параметрами:** Легко створювати об'єкти, задаючи лише обов'язкові параметри, а опціональні — за бажанням. Немає потреби створювати безліч конструкторів.
>
> 3. **Підтримка імутабельності (Immutability):** `Builder` є ідеальним для створення імутабельних об'єктів. Оскільки поля основного класу можна зробити `final` і ініціалізувати їх лише один раз у приватному конструкторі, ви отримуєте гарантію незмінності об'єкта після його створення.
>
> 4. **Спрощення валідації:** Логіку валідації можна розмістити в методі `build()`. Це дозволяє перевірити консистентність усіх параметрів разом перед створенням об'єкта.
>
> 5. **Підвищення розширюваності:** Додавання нового опціонального параметра вимагає лише додати нове поле та метод у `Builder`, не змінюючи сигнатури існуючих конструкторів і не ламаючи клієнтський код.

#### **📌 Що перевіряють:**

*Здатність чітко артикулювати ключові переваги патерну, особливо читабельність та підтримку імутабельності.*

* * *

### **4 ❓ Питання:**

Наведіть приклад реалізації шаблону `Builder` для простого імутабельного класу `User`.

> #### **💬 Відповідь:**
>
> ```java
> public final class User { // Клас імутабельний
>     private final String firstName; // final поля
>     private final String lastName;
>     private final int age;
>     private final String phone; // опціональне поле
>
>     // Приватний конструктор, приймає Builder
>     private User(Builder builder) {
>         this.firstName = builder.firstName;
>         this.lastName = builder.lastName;
>         this.age = builder.age;
>         this.phone = builder.phone;
>     }
>
>     // Геттери...
>
>     // Статичний вкладений клас Builder
>     public static class Builder {
>         // Обов'язкові поля
>         private final String firstName;
>         private final String lastName;
>
>         // Опціональні поля
>         private int age = 0;
>         private String phone = "";
>
>         // Конструктор білдера для обов'язкових полів
>         public Builder(String firstName, String lastName) {
>             this.firstName = firstName;
>             this.lastName = lastName;
>         }
>
>         public Builder age(int age) {
>             this.age = age;
>             return this;
>         }
>
>         public Builder phone(String phone) {
>             this.phone = phone;
>             return this;
>         }
>
>         // Метод, що створює фінальний об'єкт
>         public User build() {
>             return new User(this);
>         }
>     }
> }
> ```
>
> **Використання:**
> `User user = new User.Builder("John", "Doe").age(30).phone("123-456").build();`

#### **📌 Що перевіряють:**

*Практичні навички написання коду з використанням шаблону `Builder` згідно з найкращими практиками (вкладений статичний клас, `final`-поля тощо).*

* * *

### **5 ❓ Питання:**

Чим `Builder` відрізняється від статичних фабричних методів? Чи можуть вони використовуватися разом?

> #### **💬 Відповідь:**
>
> `Builder` та статичні фабричні методи — це різні породжувальні підходи.
>
> * **Статичний фабричний метод:** Зазвичай приймає всі необхідні параметри одразу і миттєво повертає готовий об'єкт. Він добре підходить для випадків з невеликою кількістю параметрів.
>
> * **`Builder`:** Призначений для **покрокового** конструювання об'єкта. Спочатку створюється об'єкт-будівельник, потім його стан налаштовується через ланцюжок викликів, і лише в кінці створюється фінальний об'єкт. Він ідеальний для об'єктів з багатьма опціональними параметрами.
>
> **Чи можуть вони використовуватися разом?**
>
> Так, можуть. Дуже поширена практика — надавати статичний фабричний метод для отримання екземпляра білдера, що робить API більш чистим:
>
> ```java
> public class User {
>     // ...
>     public static Builder builder() {
>         return new Builder();
>     }
>
>     public static class Builder { /* ... */ }
> }
>
> // Використання:
> User user = User.builder().name("John").build(); // замість new User.Builder()
> ```

#### **📌 Що перевіряють:**

*Розуміння різного призначення цих двох підходів та вміння бачити, як вони можуть доповнювати один одного.*

* * *

### **6 ❓ Питання:**

Які є недоліки у шаблону `Builder`? ⭐️

> #### **💬 Відповідь:**
>
> 1. **"Балакучість" коду:** Для реалізації шаблону потрібно створити додатковий клас `Builder`. Це збільшує кількість коду, особливо для простих класів.
> 2. **Більш громіздке створення об'єкта:** Хоча код і стає більш читабельним, сам процес створення об'єкта вимагає більше кроків: спочатку створити білдер, потім викликати кілька методів, і нарешті викликати `build()`.
> 3. **Вартість створення білдера:** При кожному створенні об'єкта потрібно спочатку створити екземпляр `Builder`. У дуже критичних до продуктивності сценаріях це може бути невеликим накладним витратами (хоча зазвичай цим можна знехтувати).
>
> Загалом, переваги `Builder` (читабельність, гнучкість, імутабельність) майже завжди переважають ці недоліки, коли йдеться про об'єкти з більш ніж 3-4 параметрами.

#### **📌 Що перевіряють:**

*Здатність критично оцінювати патерни і розуміти, що кожен з них має свою "ціну".*

* * *

### **7 ❓ Питання:**

Наведіть приклади використання `Builder` в стандартному Java API. ⭐️

> #### **💬 Відповідь:**
>
> Хоча `Builder` не так широко поширений в старому JDK, як інші патерни, він є ключовим у багатьох сучасних API:
>
> 1. **`StringBuilder` та `StringBuffer`:** Це класичні реалізації `Builder`. Вони дозволяють поетапно "будувати" рядок за допомогою методів `append()` і в кінці отримати фінальний `String` через `toString()`.
>
> 2. **`java.time.format.DateTimeFormatterBuilder`:** Дозволяє крок за кроком конструювати складний форматер для дат і часу.
>
> 3. **`java.util.stream.Stream.Builder`:** Надає білдер для поетапного створення потоку (`Stream`).
>
> 4. **`java.net.http.HttpRequest.Builder` (з Java 11):** Новий HTTP клієнт повністю побудований на `Builder`. Ви створюєте запит так: `HttpRequest.newBuilder().uri(...).header(...).POST(...).build();`.
>
> Ці приклади демонструють, що `Builder` є кращим вибором для створення складних або конфігурованих об'єктів.

#### **📌 Що перевіряють:**

*Загальну ерудицію в Java API. Знання того, де шукати "зразкові" реалізації патернів.*

* * *

### **8 ❓ Питання:**

Чому `Builder` зазвичай реалізують як статичний вкладений клас, а не як зовнішній клас? ⭐️

> #### **💬 Відповідь:**
>
> Реалізація `Builder` як **статичного вкладеного класу** (`static nested class`) є кращою практикою з кількох причин:
>
> 1. **Тісний зв'язок:** `Builder` логічно пов'язаний тільки з тим класом, який він будує. Розміщення його всередині основного класу підкреслює цей зв'язок і покращує інкапсуляцію. Це як покласти інструкцію зі збирання меблів всередину коробки з цими меблями.
>
> 2. **Доступ до приватного конструктора:** Як вкладений клас, `Builder` має доступ до `private`-членів свого зовнішнього класу, включаючи `private`-конструктор. Це дозволяє зробити конструктор основного класу недоступним для всіх, крім білдера, що є ключовим для забезпечення правильного створення об'єктів.
>
> 3. **Відсутність потреби в екземплярі зовнішнього класу:** Оскільки білдер є `static`, для його створення не потрібен екземпляр основного класу (`new User.Builder()` замість `new User().new Builder()`).
>
> Робити його зовнішнім класом було б менш зручно, оскільки йому довелося б або надати доступ до приватного конструктора основного класу (що погано), або основний клас мав би мати `package-private` конструктор.

#### **📌 Що перевіряють:**

*Знання про вкладені та внутрішні класи в Java та розуміння того, чому певна структура є кращою з точки зору дизайну та інкапсуляції.*

* * *

### **9 ❓ Питання:**

Як реалізувати валідацію параметрів у шаблоні `Builder`? ⭐️

> #### **💬 Відповідь:**
>
> `Builder` надає дуже зручне місце для валідації — **метод `build()`**.
>
> Перевага цього підходу в тому, що ви можете виконувати **перевірку консистентності** всіх параметрів разом, після того, як вони всі були встановлені.
>
> **Приклад:**
>
> ```java
> public static class Builder {
>     private Date startDate;
>     private Date endDate;
>
>     // ... сеттери ...
>
>     public Period build() {
>         // Валідація
>         if (startDate != null && endDate != null && startDate.after(endDate)) {
>             throw new IllegalStateException("Дата початку не може бути після дати закінчення");
>         }
>         return new Period(this);
>     }
> }
> ```
>
> У цьому прикладі ми перевіряємо, чи є часовий період валідним, тільки в самому кінці, коли маємо обидві дати. Робити таку перевірку в сеттерах було б складніше.
>
> Також можна додати простіші перевірки (наприклад, на `null` для обов'язкових полів) у конструктор самого білдера або в сеттери.

#### **📌 Що перевіряють:**

*Розуміння того, де найкраще розміщувати логіку валідації для забезпечення коректного стану об'єкта.*

* * *

### **10 ❓ Питання:**

Що таке "Fluent Interface" і як він пов'язаний з `Builder`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Fluent Interface (плинний/текучий інтерфейс)** — це стиль програмування API, при якому виклики методів можна об'єднувати в **ланцюжок (chaining)**. Це досягається тим, що кожен метод повертає сам об'єкт (`this`), на якому він був викликаний.
>
> **Як це пов'язано з `Builder`:**
> Шаблон `Builder` є **ідеальним прикладом** застосування Fluent Interface.
>
> ```java
> new User.Builder()
>     .name("John")   // <-- цей метод повертає Builder
>     .age(30)      // <-- і цей метод повертає Builder
>     .phone("123") // <-- і цей
>     .build();
> ```
>
> Методи `name()`, `age()` та `phone()` в білдері реалізовані так, що вони повертають `return this;`. Це дозволяє створювати довгі, читабельні ланцюжки викликів.
>
> Таким чином, `Builder` не просто вирішує проблему створення об'єктів, а й робить це в дуже елегантному та зручному для читання стилі завдяки використанню Fluent Interface. `StringBuilder` — ще один чудовий приклад.

#### **📌 Що перевіряють:**

*Знання сучасних стилів API та термінології. Здатність бачити, як `Builder` реалізує більш загальну ідею "плинного" інтерфейсу.*

* * *

### **11 ❓ Питання:**

Чим `Builder` відрізняється від встановлення полів через сеттери (JavaBeans-підхід)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва підходи дозволяють встановлювати поля по одному, вони мають фундаментальні відмінності в плані **імутабельності та консистентності стану**.
>
> **JavaBeans-підхід (з сеттерами):**
>
> 1. Об'єкт спочатку створюється за допомогою конструктора без аргументів.
> 2. Потім його стан змінюється через виклики сеттерів (`obj.setName(...)`, `obj.setAge(...)`).
> 3. **Проблеми:**
>     * **Немає імутабельності:** Об'єкт є мутабельним за своєю природою.
>     * **Неконсистентний стан:** Між викликом конструктора і останнім викликом сеттера об'єкт може перебувати в **неповному або невалідному стані**.
>     * **Непотокобезпечність:** Використання такого об'єкта в багатопотоковому середовищі вимагає додаткової синхронізації.
>
> **`Builder`-підхід:**
>
> 1. Стан накопичується в окремому, мутабельному об'єкті-білдері.
> 2. Фінальний об'єкт створюється **один раз** викликом `build()` і є **повністю сформованим та валідним**.
> 3. **Переваги:**
>     * **Дозволяє створювати імутабельні об'єкти.**
>     * **Гарантує консистентність:** Об'єкт не існує, доки не буде повністю сконфігурований і не пройде валідацію в методі `build()`.
>     * **Більш безпечний для багатопотоковості** (якщо об'єкт імутабельний).
>
> `Builder` — це значно більш надійний підхід для створення складних об'єктів.

#### **📌 Що перевіряють:**

*Глибоке розуміння проблем, пов'язаних з мутабельністю та неконсистентним станом, і того, як `Builder` їх вирішує.*

* * *

### **12 ❓ Питання:**

Чи можна використовувати один `Builder` для створення кількох різних об'єктів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна. Об'єкт `Builder` — це звичайний мутабельний об'єкт. Ви можете налаштувати його один раз, створити об'єкт за допомогою `build()`, потім змінити якісь поля білдера і створити другий, трохи інший об'єкт.
>
> **Приклад:**
>
> ```java
> User.Builder builder = new User.Builder("Shared", "Lastname");
>
> builder.age(25);
> User user1 = builder.build(); // User 1
>
> builder.age(30).phone("555-123");
> User user2 = builder.build(); // User 2, змінений
>
> System.out.println(user1);
> System.out.println(user2);
> ```
>
> Це може бути корисним, коли вам потрібно створити серію об'єктів з багатьма спільними полями і невеликими відмінностями.
>
> **Однак такий підхід є потенційно небезпечним,** оскільки стан білдера "перетікає" від одного створення до іншого, що може призвести до несподіваних результатів, якщо забути "скинути" або змінити якесь поле.
>
> Зазвичай більш безпечною та чистою практикою є створення **нового білдера** для кожного нового об'єкта, або використання методу `toBuilder()`, щоб почати з чистої копії стану.

#### **📌 Що перевіряють:**

*Здатність думати про "крайові випадки" використання патерну та оцінювати їхні ризики.*

* * *

### **13 ❓ Питання:**

Як `Builder` може бути реалізований для ієрархії класів (з успадкуванням)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Реалізація `Builder` для ієрархії є складнішою і вимагає використання **дженеріків та рекурсивного самотипування (recursive self-typing)**.
>
> **Проблема:** Якщо білдер `SubBuilder` успадковує від `SuperBuilder`, то методи, успадковані від `SuperBuilder` (наприклад, `withName()`), будуть повертати `SuperBuilder`, а не `SubBuilder`. Це "зламає" плинний інтерфейс.
>
> **Рішення:** Використовувати дженеріки, щоб кожен білдер "знав" свій власний тип і повертав його.
>
> **Приклад:**
>
> ```java
> abstract class Pizza { /* ... */ }
> class Calzone extends Pizza { /* ... */ }
>
> // Базовий білдер з рекурсивним дженериком
> abstract class PizzaBuilder<T extends PizzaBuilder<T>> {
>     Set<Topping> toppings = ...;
>
>     public T addTopping(Topping topping) {
>         toppings.add(topping);
>         return self(); // повертаємо правильний підтип
>     }
>
>     abstract Pizza build();
>     protected abstract T self(); // Метод для отримання "this" правильного типу
> }
>
> // Білдер-нащадок
> class CalzoneBuilder extends PizzaBuilder<CalzoneBuilder> {
>     private boolean sauceInside = false;
>     public CalzoneBuilder withSauceInside() { /* ... */ return this; }
>     @Override Calzone build() { /* ... */ }
>     @Override protected CalzoneBuilder self() { return this; }
> }
> ```
>
> Це дозволяє писати: `new CalzoneBuilder().addTopping(...).withSauceInside().build();` без втрати плинності.
> Це просунута техніка, яка демонструє глибоке розуміння дженеріків та ООП.

#### **📌 Що перевіряють:**

*Дуже глибокі знання Java. Здатність вирішувати складні проблеми дизайну, пов'язані з успадкуванням та дженеріками.*

* * *

### **14 ❓ Питання:**

Які інструменти кодогенерації (наприклад, Lombok) допомагають з реалізацією `Builder`? Які переваги та недоліки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Lombok** надає анотацію **`@Builder`**, яка автоматично генерує весь шаблонний код для `Builder` під час компіляції.
>
> **Як це працює:**
>
> ```java
> import lombok.Builder;
>
> @Builder
> public class User {
>     private String name;
>     private int age;
> }
> ```
>
> Цей простий код Lombok перетворить на повну реалізацію з класом `User.UserBuilder`, плинними методами `name()`, `age()` та методом `build()`.
>
> **Переваги:**
>
> * **Максимальна стислість:** Позбавляє необхідності писати десятки рядків шаблонного коду вручну.
> * **Простота підтримки:** Додавання нового поля в клас і в `Builder` стає тривіальним.
>
> **Недоліки:**
>
> * **"Магія":** Код генерується "під капотом". Якщо щось піде не так, або потрібна кастомна логіка (наприклад, валідація в `build()`), налагодження та розширення може бути менш очевидним.
> * **Приховування складності:** Розробник може перестати розуміти, як працює патерн насправді, сліпо покладаючись на анотацію.
> * **Залежність від інструменту:** Проект стає залежним від Lombok та відповідного плагіна для IDE.
>
> Загалом, для більшості випадків `@Builder` є чудовим інструментом, що прискорює розробку, але його слід використовувати усвідомлено.

#### **📌 Що перевіряють:**

*Знання сучасних інструментів, що підвищують продуктивність, та здатність оцінювати їхні переваги та недоліки.*

* * *

### **15 ❓ Питання:**

Розглянемо створення `java.util.regex.Pattern`. Це приклад якогось патерну? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, це цікавий приклад, який демонструє комбінацію кількох ідей. Створення `Pattern` відбувається через статичний метод `Pattern.compile(String regex)`.
>
> `Pattern pattern = Pattern.compile("\\d+");`
>
> Це можна розглядати як **статичний фабричний метод**.
>
> **Чому це не конструктор?**
>
> 1. **Складний процес створення:** Метод `compile` не просто створює об'єкт. Він **парсить** регулярний вираз, аналізує його синтаксис і **компілює** в оптимізоване внутрішнє представлення (кінцевий автомат), що є складною операцією. Ця складність інкапсульована всередині фабричного методу.
>
> 2. **Кешування:** Хоча поточна реалізація цього не робить, теоретично `Pattern.compile()` міг би кешувати найпопулярніші скомпільовані патерни для підвищення продуктивності. Використання фабричного методу залишає таку можливість.
>
> 3. **Імутабельність:** Повертає імутабельний, потокобезпечний об'єкт `Pattern`.
>
> Це чудовий приклад того, як статичний фабричний метод використовується для інкапсуляції складного процесу створення та ініціалізації об'єкта, надаючи клієнту простий та зрозумілий API.

#### **📌 Що перевіряють:**

*Здатність бачити патерни в неочевидних місцх та аналізувати дизайнерські рішення, що стоять за стандартними класами Java API.*
