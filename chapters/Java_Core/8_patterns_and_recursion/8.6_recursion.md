---
layout: default
title: "Рекурсія (Recursion)"
parent: "Patterns and Recursion"
grand_parent: "Java Core"
nav_order: 6
---

# Рекурсія (Recursion)

* короткий опис: Техніка програмування, де метод викликає сам себе для вирішення задачі, розбиваючи її на підзадачі до досягнення базового (граничного) випадку.

* поняття: `Рекурсія`, `Базовий випадок` (base case), `Рекурсивний виклик`, `Стек викликів` (Call Stack), `StackOverflowError`.

* приклади: `Факторіал`, `Числа Фібоначчі`, `Сума чисел від 1 до n`, `Обхід дерева` (`Depth-First Search` - DFS).

* ключові моменти: Задача природно розбивається на підзадачі; кожен виклик чекає результат від наступного; вимагає чіткої умови зупинки (базовий випадок); може призвести до `StackOverflowError`; часто споживає більше пам`яті, ніж ітерація.

* що важливо знати на співбесіді: Визначення, призначення базового випадку, як працює стек викликів, базові приклад реалізації, порівняння з ітерацією (переваги/недоліки щодо швидкодії та пам'яті), коли уникати рекурсії, відсутність оптимізації `хвостової рекурсії` в Java.

### **✅ Офіційне визначення:**

**Рекурсія** — це техніка програмування, коли метод викликає сам себе з новими параметрами для розв'язання задачі, розбиваючи її на підзадачі до досягнення базового (граничного) випадку.

### **🧠 Простими словами:**

Рекурсія — це коли метод “запитує сам у себе”, але з меншим завданням. Як матрьошка — поки не дійдеш до найменшої, нічого не відбувається. І тільки після цього всі “внутрішні” відповідають назад.

---

## **📊 Основні елементи рекурсії:**

| Елемент                         | Опис                                                        |
|---------------------------------|-------------------------------------------------------------|
| **Базовий випадок (base case)** | Умова зупинки — коли не треба далі викликати себе           |
| **Рекурсивний виклик**          | Метод викликає сам себе з новими параметрами                |
| **Стек викликів**               | Місце в пам’яті, де зберігається кожен виклик до завершення |

---

## **Пояснення на пальцях (💡)**

### **Як це працює?**  

  Метод викликає сам себе -> кожен виклик чекає результат від наступного -> коли досягнуто базового випадку, результат "
  розмотується" назад.

### **Чому це може бути небезпечно?**  

  Якщо забув вказати базовий випадок — отримаєш нескінченні виклики -> `StackOverflowError`.

### **Коли використовувати?**  

  Коли задача природно розбивається на підзадачі (дерева, обходи, рекурсивні формули, пошук шляхів).

### **Коли краще уникати?**  

  Коли багато рівнів рекурсії -> споживає багато пам’яті. Якщо можна замінити ітерацією — краще зробити це.

---

## **Корисні приклади (🧪)**

### **🔻 Факторіал (n\!)**

```java
public static int factorial(int n) {
    if (n == 0) return 1; // базовий випадок
    return n * factorial(n - 1); // рекурсивний виклик
}
```

### **🔻 Числа Фібоначчі**

```java
public static int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}
```

### **🔻 Сума чисел від 1 до n**

```java
public static int sum(int n) {
    if (n == 0) return 0;
    return n + sum(n - 1);
}
```

### **🔻 Обхід дерева (DFS)**

```java
void dfs(Node node) {
    if (node == null) return;
    System.out.println(node.value);
    dfs(node.left);
    dfs(node.right);
}
```

---

## **Порівняння (⚖️)**

| Параметр             | Рекурсія                        | Ітерація (цикли)                         |
|----------------------|---------------------------------|------------------------------------------|
| Читабельність        | ✅ Часто коротший і "чистіший"   | ✅ Зрозумілий для простих задач           |
| Швидкодія            | ❌ Може бути повільніше          | ✅ Менше викликів, швидше                 |
| Використання пам’яті | ❌ Більше (кожен виклик у стеку) | ✅ Мінімум пам’яті                        |
| Ризик StackOverflow  | ✅ Так                           | ❌ Немає                                  |
| Застосування         | Дерева, рекурсивні алгоритми    | Лінійні задачі (підрахунок, масиви тощо) |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“Base \+ Call \= Recursion”**

Запамʼятай: потрібні два елементи — `base case` і `recursive call`. Якщо щось одне пропустиш — буде біда

---

## **🟩 Підсумок:**

Рекурсія — потужний інструмент для вирішення задач, які природно розбиваються на підзадачі (дерева, графи, математичні
формули). Але потрібно бути обережним: завжди мати базовий випадок і слідкувати за кількістю викликів. У деяких
ситуаціях ітерація — більш безпечний і ефективний вибір.

---

### **🔗 Додаткові джерела:**

* [**Oracle Java Recursion Guide**](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/while.html#recursion)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке рекурсія в програмуванні?

> #### **💬 Відповідь:**
>
> **Рекурсія** — це техніка програмування, при якій метод (або функція) **викликає сам себе** для вирішення задачі.
>
> Основна ідея полягає в тому, щоб розбити велику, складну задачу на менші, простіші підзадачі, які є подібними до вихідної. Цей процес триває доти, доки не буде досягнуто **базового випадку** — простої задачі, яку можна вирішити напряму, без подальших рекурсивних викликів.
>
> Після досягнення базового випадку, результати починають повертатися вгору по ланцюжку викликів, "збираючи" фінальне рішення.

#### **📝 Аналогія:**

Уявіть, що ви стоїте першим у довгій черзі людей і хочете дізнатися, скільки всього людей у черзі. Ви не можете бачити кінець.

1. **Рекурсивний крок:** Ви запитуєте людину позаду вас: "Скільки людей за тобою?". Ця людина робить те саме — запитує наступну.
2. Цей процес триває, доки запитання не дійде до останньої людини.
3. **Базовий випадок:** Остання людина бачить, що за нею нікого немає, і відповідає: "За мною 0 людей".
4. **Повернення результату:** Попередня людина чує "0", додає до цього себе (`0 + 1 = 1`) і передає "1" далі. Так кожен додає себе і передає результат, доки відповідь не дійде до вас.

#### **📌 Що перевіряють:**

*Розуміння фундаментальної концепції рекурсії як методу розбиття задачі на подібні підзадачі.*

* * *

### **2 ❓ Питання:**

Які два ключові компоненти має мати будь-яка рекурсивна функція?

> #### **💬 Відповідь:**
>
> Будь-яка коректна рекурсивна функція обов'язково має складатися з двох компонентів:
>
> 1. **Базовий випадок (Base Case):**
>     * Це умова, за якої рекурсія **припиняється**. Вона визначає найпростіший випадок задачі, який можна вирішити без подальших рекурсивних викликів.
>     * **Наявність базового випадку є критично важливою**. Без нього функція буде викликати сама себе нескінченно, що призведе до переповнення стеку викликів та помилки `StackOverflowError`.
>
> 2. **Рекурсивний крок (Recursive Step):**
>     * Це частина функції, де вона викликає саму себе, але з **модифікованими аргументами**, які "наближають" задачу до базового випадку.
>     * Наприклад, при обчисленні факторіалу `factorial(n)`, рекурсивний крок викликає `factorial(n-1)`, зменшуючи `n`, доки воно не досягне базового випадку `n == 0`.

#### **📌 Що перевіряють:**

*Знання двох фундаментальних стовпів рекурсії. Це є основою для написання будь-якого рекурсивного коду.*

* * *

### **3 ❓ Питання:**

Що таке стек викликів (Call Stack) і як він пов'язаний з рекурсією?

> #### **💬 Відповідь:**
>
> **Стек викликів** — це структура даних (стек), яку Java (і більшість мов програмування) використовує для відстеження активних методів. Коли викликається метод, у стек додається новий "фрейм" (stack frame), який містить інформацію про цей виклик: його локальні змінні, параметри та адресу повернення.
>
> **Як це пов'язано з рекурсією:**
>
> * При кожному рекурсивному виклику у стек викликів **додається новий фрейм**.
> * Ці фрейми накопичуються в стеку. Перший виклик буде на самому дні стека, а останній — на вершині.
> * Коли функція досягає базового випадку і починає повертати результат, фрейми починають **"вийматися"** зі стека у зворотному порядку (LIFO — Last In, First Out).
> * Кожен виклик забирає результат попереднього, виконує свою частину роботи і повертає результат далі вгору по стеку.
>
> Якщо рекурсія занадто глибока (наприклад, через відсутність базового випадку), стек викликів переповнюється, і JVM кидає помилку `StackOverflowError`.

#### **📌 Що перевіряють:**

*Розуміння механізму роботи стека викликів, що є ключем до розуміння, чому рекурсія споживає пам'ять і чому виникає `StackOverflowError`.*

* * *

### **4 ❓ Питання:**

Наведіть приклад реалізації обчислення факторіалу за допомогою рекурсії.

> #### **💬 Відповідь:**
>
> Факторіал числа `n` (позначається `n!`) — це добуток усіх цілих чисел від 1 до `n`. `n! = n * (n-1) * (n-2) * ... * 1`. Також `0! = 1`.
>
> Цю задачу легко розбити рекурсивно: `n! = n * (n-1)!`
>
> ```java
> public class Factorial {
>     public static long calculate(int n) {
>         // Попередня перевірка на некоректні дані
>         if (n < 0) {
>             throw new IllegalArgumentException("Факторіал не визначений для від'ємних чисел");
>         }
>
>         // 1. Базовий випадок
>         if (n == 0) {
>             return 1;
>         }
>
>         // 2. Рекурсивний крок
>         return n * calculate(n - 1);
>     }
>
>     public static void main(String[] args) {
>         System.out.println("5! = " + calculate(5)); // Виведе: 5! = 120
>     }
> }
> ```
>
> * **Базовий випадок:** `n == 0`, функція повертає `1`.
> * **Рекурсивний крок:** `n * calculate(n - 1)`, задача зводиться до обчислення факторіалу для меншого числа.

#### **📌 Що перевіряють:**

*Практичні навички написання простого рекурсивного коду. Це класичне "hello, world" для рекурсії.*

* * *

### **5 ❓ Питання:**

Порівняйте рекурсивний та ітераційний підходи. Які їхні переваги та недоліки? ⭐️

> #### **💬 Відповідь:**
>
> | Характеристика | Рекурсія | Ітерація (цикли `for`, `while`) |
> |---|---|---|
> | **Читабельність коду** | ✅ **Висока** для задач, що є природно рекурсивними (робота з деревами, фрактали, деякі математичні задачі). Код може бути дуже лаконічним і елегантним. | ✅ **Висока** для лінійних задач (обхід масивів, підрахунок). Може бути складнішою для ієрархічних структур. |
> | **Продуктивність (швидкість)** | ❌ **Зазвичай повільніша** через накладні витрати на виклики методів (створення фреймів у стеку). | ✅ **Зазвичай швидша**, оскільки немає накладних витрат на виклики методів. |
> | **Використання пам'яті** | ❌ **Споживає більше пам'яті.** Кожен рекурсивний виклик додає фрейм у стек викликів. | ✅ **Споживає менше пам'яті.** Зазвичай використовує фіксовану кількість змінних. |
> | **Ризик `StackOverflowError`** | ✅ **Високий.** Може виникнути при великій глибині рекурсії. | ❌ **Відсутній.** Цикл обмежений тільки умовами і може виконуватися дуже довго. |
>
> **Висновок:** Рекурсія є потужним інструментом для певного класу задач, де вона значно спрощує код. Однак за цю простоту доводиться платити продуктивністю та споживанням пам'яті. Для більшості лінійних задач ітерація є більш ефективним та безпечним вибором.

#### **📌 Що перевіряють:**

*Здатність аналізувати компроміси між різними підходами до вирішення задач. Розуміння того, коли рекурсія є доцільною, а коли — ні.*

* * *

### **6 ❓ Питання:**

Що таке хвостова рекурсія (Tail Recursion) і чи підтримує її Java? ⭐️

> #### **💬 Відповідь:**
>
> **Хвостова рекурсія** — це особливий випадок рекурсії, коли рекурсивний виклик є **останньою операцією**, що виконується в методі. Результат рекурсивного виклику не використовується для подальших обчислень, а просто повертається нагору.
>
> **Приклад звичайної рекурсії (не хвостової):**
> `return n * factorial(n - 1);` // Потрібно дочекатися результату, щоб потім помножити на `n`.
>
> **Приклад хвостової рекурсії:**
>
> ```java
> long factorial(int n, long accumulator) {
>     if (n == 0) return accumulator;
>     return factorial(n - 1, n * accumulator); // Рекурсивний виклик - остання дія
> }
> ```
>
> **Оптимізація хвостової рекурсії (TCO - Tail Call Optimization):**
> Деякі мови (наприклад, Scala, Lisp) можуть оптимізувати хвостову рекурсію. Компілятор перетворює її на ітеративний код (цикл), що усуває створення нових фреймів у стеку і ризик `StackOverflowError`.
>
> **Чи підтримує її Java?**
> **Ні**, стандартний компілятор Java (`javac`) та JVM **не виконують** оптимізацію хвостової рекурсії. Тому в Java навіть хвостова рекурсія при великій глибині призведе до `StackOverflowError`.
>
> Це важливе обмеження, яке потрібно враховувати при написанні рекурсивного коду на Java.

#### **📌 Що перевіряють:**

*Знання просунутих концепцій, таких як TCO, та розуміння обмежень платформи Java в цьому аспекті.*

* * *

### **7 ❓ Питання:**

Наведіть приклад задачі, де рекурсивне рішення є значно простішим і читабельнішим за ітераційне. ⭐️

> #### **💬 Відповідь:**
>
> **Обхід дерева**, наприклад, для виводу всіх його вузлів (DFS - Depth-First Search, або обхід в глибину), є класичним прикладом, де рекурсія виглядає набагато природніше.
>
> **Рекурсивне рішення (дуже просте):**
>
> ```java
> class Node { Node left, right; int value; /*...*/ }
>
> public void printTree(Node node) {
>     // Базовий випадок
>     if (node == null) {
>         return;
>     }
>
>     // Рекурсивні кроки
>     System.out.println(node.value);
>     printTree(node.left);
>     printTree(node.right);
> }
> ```
>
> Код є лаконічним, інтуїтивно зрозумілим і точно відображає структуру самої задачі (обробити вузол, потім обробити його дітей).
>
> **Ітераційне рішення (значно складніше):**
> Для ітераційного обходу дерева доведеться використовувати додаткову структуру даних, наприклад, **стек (Stack)**, щоб імітувати рекурсивні виклики.
>
> ```java
> public void printTreeIterative(Node root) {
>     if (root == null) return;
>
>     Stack<Node> stack = new Stack<>();
>     stack.push(root);
>
>     while (!stack.isEmpty()) {
>         Node node = stack.pop();
>         System.out.println(node.value);
>
>         if (node.right != null) stack.push(node.right);
>         if (node.left != null) stack.push(node.left);
>     }
> }
> ```
>
> Ітераційний код є довшим, вимагає ручного управління стеком і є менш очевидним для розуміння.

#### **📌 Що перевіряють:**

*Розуміння сильних сторін рекурсії та здатність визначити задачі, для яких вона є найкращим інструментом.*

* * *

### **8 ❓ Питання:**

Чому рекурсивний розрахунок чисел Фібоначчі `fib(n-1) + fib(n-2)` є дуже неефективним? ⭐️

> #### **💬 Відповідь:**
>
> Класичне рекурсивне рішення `fib(n) = fib(n-1) + fib(n-2)` є вкрай неефективним через **величезну кількість повторних обчислень**.
>
> Коли ви викликаєте, наприклад, `fib(5)`, дерево викликів виглядає так:
>
> * `fib(5)` викликає `fib(4)` і `fib(3)`.
> * `fib(4)` викликає `fib(3)` і `fib(2)`.
>
> Вже на цьому етапі видно, що `fib(3)` обчислюється **двічі**. Чим більше `n`, тим більше таких дублюючих викликів. Наприклад, для `fib(6)` `fib(3)` буде обчислено тричі.
>
> Часова складність такого алгоритму є **експоненційною (O(2^n))**, що робить його абсолютно непридатним для великих `n`.
>
> **Кращі підходи:**
>
> 1. **Ітераційний:** Використання циклу для послідовного обчислення чисел Фібоначчі. Складність — **O(n)**.
> 2. **Рекурсія з мемоізацією (кешуванням):** Зберігання вже обчислених значень у мапі або масиві, щоб не обчислювати їх знову. Складність — **O(n)**.

#### **📌 Що перевіряють:**

*Здатність аналізувати часову складність алгоритмів. Розуміння того, що рекурсія може бути елегантною, але при цьому вкрай неефективною, якщо вона неоптимально розроблена.*

* * *

### **9 ❓ Питання:**

Як можна переписати неефективну рекурсію за допомогою **мемоізації**? Наведіть приклад для чисел Фібоначчі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Мемоізація** — це техніка оптимізації, яка полягає у **збереженні (кешуванні)** результатів виконання "дорогих" функцій і поверненні кешованого результату при повторних викликах з тими самими вхідними даними.
>
> **Як реалізувати для чисел Фібоначчі:**
>
> 1. Створюється масив або `Map` для зберігання вже обчислених значень.
> 2. Перед рекурсивним обчисленням перевіряється, чи є результат для даного `n` вже в кеші.
> 3. Якщо так, результат повертається з кешу.
> 4. Якщо ні, результат обчислюється, **зберігається в кеші**, а потім повертається.
>
> **Приклад з `Map`:**
>
> ```java
> public class MemoizedFibonacci {
>     private static Map<Integer, Long> cache = new HashMap<>();
>
>     public static long fib(int n) {
>         if (n <= 1) return n;
>
>         // 1. Перевіряємо, чи є результат в кеші
>         if (cache.containsKey(n)) {
>             return cache.get(n);
>         }
>
>         // 2. Якщо немає - обчислюємо
>         long result = fib(n - 1) + fib(n - 2);
>
>         // 3. Зберігаємо результат в кеші
>         cache.put(n, result);
>
>         return result;
>     }
> }
> ```> Цей підхід зводить часову складність з експоненційної O(2^n) до **лінійної O(n)**, оскільки кожне число Фібоначчі `fib(k)` обчислюється тільки один раз.

#### **📌 Що перевіряють:**

*Знання технік оптимізації рекурсивних алгоритмів. Вміння застосовувати мемоізацію на практиці.*

* * *

### **10 ❓ Питання:**

Що таке непряма (indirect) рекурсія? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Непряма (або взаємна) рекурсія** — це ситуація, коли два або більше методів викликають один одного по колу.
>
> Тобто метод `A` викликає метод `B`, а метод `B` у свою чергу викликає метод `A`.
>
> `A() -> B() -> A() -> ...`
>
> Як і у випадку прямої рекурсії, для непрямої також обов'язково потрібен **базовий випадок**, який розірве цей ланцюжок викликів, інакше виникне `StackOverflowError`.
>
> **Простий приклад:** перевірка числа на парність/непарність.
>
> ```java
> public class IndirectRecursion {
>
>     public static boolean isEven(int n) {
>         if (n == 0) return true;
>         // Число парне, якщо попереднє було непарним
>         return isOdd(n - 1);
>     }
>
>     public static boolean isOdd(int n) {
>         if (n == 0) return false;
>         // Число непарне, якщо попереднє було парним
>         return isEven(n - 1);
>     }
>
>     public static void main(String[] args) {
>         System.out.println("4 is even: " + isEven(4)); // true
>     }
> }
> ```
>
> Тут `isEven` викликає `isOdd`, а `isOdd` викликає `isEven`. Базовий випадок `n == 0` зупиняє рекурсію. Непряма рекурсія часто зустрічається в задачах синтаксичного аналізу (парсингу) або при обході складних структур даних.

#### **📌 Що перевіряють:**

*Знання про існування більш складних форм рекурсії та вміння їх розпізнавати.*

* * *

### **11 ❓ Питання:**

Як би ви перетворили простий рекурсивний метод (наприклад, для суми чисел) в ітераційний? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Розглянемо рекурсивний метод для суми чисел від 1 до `n`.
> **Рекурсивна версія:**
>
> ```java
> public int sumRecursive(int n) {
>     if (n <= 0) return 0;
>     return n + sumRecursive(n - 1);
> }
> ```
>
> Для перетворення його в ітераційний, нам потрібно:
>
> 1. Замінити рекурсивні виклики на **цикл** (наприклад, `for` або `while`).
> 2. Використовувати **змінну-акумулятор** для накопичення результату.
>
> **Ітераційна версія:**
>
> ```java
> public int sumIterative(int n) {
>     if (n <= 0) return 0;
>
>     int totalSum = 0; // Змінна-акумулятор
>
>     for (int i = 1; i <= n; i++) {
>         totalSum += i;
>     }
>
>     return totalSum;
> }
> ```
>
> Цей підхід є більш ефективним за пам'яттю та швидкістю і не має ризику `StackOverflowError`. Для простих лінійних рекурсій таке перетворення є досить тривіальним.

#### **📌 Що перевіряють:**

*Навички рефакторингу та розуміння того, як переходити від одного стилю програмування до іншого.*

* * *

### **12 ❓ Питання:**

Чи можна реалізувати рекурсію без явного виклику методу всередині себе, наприклад, використовуючи лямбди в Java? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна, хоча це і виглядає дещо незвично. Для цього потрібно обійти обмеження, яке полягає в тому, що лямбда-вираз не може посилатися на самого себе, оскільки він не має імені (і не може посилатися на змінну, якій його присвоюють, до завершення ініціалізації).
>
> **Рішення:**
>
> Один із способів — створити поле (або локальну змінну-масив), яке буде "тримати" посилання на функцію, і ініціалізувати його так, щоб тіло лямбди могло до нього звернутися.
>
> **Приклад з полем класу:**
>
> ```java
> import java.util.function.Function;
>
> public class LambdaRecursion {
>
>     // Поле для зберігання посилання на функцію
>     private Function<Integer, Long> factorial;
>
>     public LambdaRecursion() {
>         // Ініціалізація лямбди, яка може посилатися на саму себе через поле
>         factorial = n -> (n == 0) ? 1L : n * factorial.apply(n - 1);
>     }
>
>     public long calculateFactorial(int n) {
>         return factorial.apply(n);
>     }
> }
> ```> Цей підхід є скоріше академічним, ніж практичним, але він демонструє, що концепція рекурсії може бути реалізована і у функціональному стилі в Java. У мовах, де функції є громадянами першого класу, для цього існують більш елегантні конструкції (наприклад, `let rec` в F#).

#### **📌 Що перевіряють:**

*Дуже глибокі знання Java. Розуміння обмежень лямбда-виразів та креативність у пошуку шляхів їх обходу.*

* * *

### **13 ❓ Питання:**

Припустимо, ваш рекурсивний метод для обходу дерева працює занадто повільно. Які перші кроки ви зробите для діагностики та оптимізації? ⭐️⭐️

> #### **💬 Відповідь:**
>
> 1. **Аналіз складності алгоритму:** Перший крок — зрозуміти теоретичну складність алгоритму (O-нотацію). Можливо, проблема не в рекурсії як такій, а в тому, що сам алгоритм є неефективним (наприклад, експоненційний, як у наївному `fibonacci`). Якщо це так, потрібно шукати кращий алгоритм.
>
> 2. **Перевірка на дублюючі обчислення:** Проаналізувати, чи не виконує рекурсія одну й ту саму роботу кілька разів. Якщо так — застосувати **мемоізацію** для кешування результатів.
>
> 3. **Аналіз базового випадку:** Переконатися, що базовий випадок є оптимальним і досягається якомога швидше. Можливо, можна додати більше умов для раннього виходу з рекурсії.
>
> 4. **Профілювання коду:** Використовувати профайлер (наприклад, вбудований у VisualVM або IntelliJ IDEA) для аналізу часу виконання та кількості викликів методів. Це допоможе точно визначити, чи є сама рекурсія "вузьким місцем", чи проблема в інших операціях всередині методу.
>
> 5. **Розгляд ітераційного підходу:** Якщо глибина рекурсії є проблемою або накладні витрати на виклики занадто великі, розглянути можливість переписати алгоритм на **ітераційний** з використанням явного стеку або черги.
>
> 6. **Зменшення кількості параметрів:** Чим менше параметрів передається при рекурсивному виклику, тим меншим буде фрейм у стеку. Іноді параметри можна замінити полями класу, якщо це доцільно.

#### **📌 Що перевіряють:**

*Системний підхід до вирішення проблем продуктивності. Здатність застосовувати як теоретичний аналіз, так і практичні інструменти (профайлери).*

* * *

### **14 ❓ Питання:**

Уявіть, що ви обходите дуже глибоке дерево файлів за допомогою рекурсії. Ви отримали `StackOverflowError`. Які ваші дії, якщо переписати все на ітерацію занадто складно або довго? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо повний рефакторинг на ітераційний підхід є небажаним, є кілька стратегій:
>
> 1. **Збільшення розміру стека JVM:**
>     * Це "швидке і брудне" рішення. Можна запустити JVM з прапорцем `-Xss`, який збільшує розмір стека для кожного потоку. Наприклад, `-Xss4m`.
>     * **Недолік:** Це не вирішує фундаментальної проблеми і може просто відкласти її. Також це збільшує споживання пам'яті для кожного потоку в застосунку.
>
> 2. **Гібридний підхід (рекурсія + ітерація):**
>     * Обмежити глибину рекурсії. Коли рекурсія досягає певної глибини (наприклад, 1000 викликів), вона переривається і запускає **новий ітераційний обхід** з поточної точки. Або навпаки — ітеративно обходити верхні рівні, а для кожної піддиректорії запускати рекурсію до певної глибини.
>
> 3. **Використання `Fork/Join Framework`:**
>     * Для задач, які можна розпаралелити (як обхід файлового дерева), можна використати `Fork/Join Framework` (наприклад, `RecursiveTask` або `RecursiveAction`).
>     * Цей фреймворк розроблений для вирішення рекурсивних задач. Він розбиває задачу на підзадачі і виконує їх у пулі потоків. Хоча він використовує рекурсію логічно, він керує стеком більш ефективно, розподіляючи роботу і уникаючи глибоких ланцюжків викликів в одному потоці.
>
> Збільшення стека є найпростішим, але найменш надійним рішенням. `Fork/Join` є найбільш правильним для паралельних задач.

#### **📌 Що перевіряють:**

*Практичні знання JVM та її налаштувань. Здатність мислити про компромісні та гібридні рішення, коли ідеальне рішення є недосяжним.*

* * *

### **15 ❓ Питання:**

Чи можна використовувати рекурсію для реалізації циклу `for`? Поясніть на прикладі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, будь-який цикл можна емулювати за допомогою рекурсії. Цикл `for`, що виконується `n` разів, можна представити як рекурсивну функцію, яка викликає себе `n` разів, зменшуючи лічильник.
>
> **Приклад: ітераційний `for`**
>
> ```java
> for (int i = 0; i < 5; i++) {
>     System.out.println("Iteration " + i);
> }
> ```
>
> **Рекурсивна емуляція `for`:**
>
> ```java
> public void simulateForLoop(int i, int max) {
>     // Базовий випадок: еквівалент i < max
>     if (i >= max) {
>         return;
>     }
>
>     // Тіло "циклу"
>     System.out.println("Recursion " + i);
>
>     // Рекурсивний крок: еквівалент i++
>     simulateForLoop(i + 1, max);
> }
>
> // Початковий виклик
> // simulateForLoop(0, 5);
> ```
>
> Це демонструє, що ітерація та рекурсія є **теоретично еквівалентними** (можуть виражати одні й ті самі обчислення). Будь-яка ітерація може бути переписана як рекурсія, і будь-яка рекурсія — як ітерація (з використанням явного стеку).
>
> У функціональних мовах, які не мають вбудованих циклів (`for`/`while`), рекурсія є основним інструментом для організації повторюваних дій.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння теорії обчислень та еквівалентності ітерації та рекурсії. Це показує глибокі теоретичні знання.*
