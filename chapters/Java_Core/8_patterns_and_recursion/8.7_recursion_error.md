---
layout: default
title: "Типові помилки при рекурсії"
parent: "Patterns and Recursion"
grand_parent: "Java Core"
nav_order: 7
---

# Типові помилки при рекурсії

* короткий опис: Логічні або структурні помилки, які виникають при використанні рекурсивних методів, що призводять до помилок виконання (наприклад, `StackOverflowError`) або неправильних результатів.

* поняття: `Відсутній базовий випадок`, `Недосяжний базовий випадок`, `Некоректний return`, `Подвійні обчислення` (без мемоізації), `StackOverflowError`.

* приклади: Нескінченний цикл при відсутності `if (n == 0) return 1;` для факторіалу; `count(n + 1)` коли `base case` на `n < 0` і `n > 0`; `sum(n-1)` без `return`; неоптимізоване `fibonacci(n-1) + fibonacci(n-2)`.

* ключові моменти: Головні проблеми — відсутній або недосяжний базовий випадок (призводить до `StackOverflowError`) і неправильне повернення результату. Уникати надлишкових обчислень (`мемоізація`) та використання глобального стану.

* що важливо знати на співбесіді: Причини `StackOverflowError`, способи перевірки коректності рекурсії (тестування, логування), методи оптимізації (мемоізація), написання чистого рекурсивного коду (без глобальних змінних).

### **✅ Офіційне визначення:**

Типові помилки при рекурсії — це логічні або структурні хиби, які виникають під час використання рекурсивних методів і призводять до помилок виконання (наприклад, `StackOverflowError`) або неправильних результатів.

### **🧠 Простими словами:**

Це ті "граблі", на які легко наступити, коли пишеш метод, що викликає сам себе. Особливо, якщо забув, де він має зупинитись, або не розумієш, що повертається назад.

---

## **📊 Найпоширеніші помилки:**

| Помилка                         | Приклад / Суть                                                    |
|---------------------------------|-------------------------------------------------------------------|
| ❌ Відсутній базовий випадок     | Метод викликає сам себе без зупинки -> StackOverflow               |
| ❌ Базовий випадок не досяжний   | Є `if`, але умова ніколи не виконується                           |
| ❌ Некоректний `return`          | Повертає не те, або не повертає нічого                            |
| ⚠️ Зайва логіка в рекурсії      | Складні умови або дублювання обчислень (наприклад, в `fibonacci`) |
| ❌ Модифікація глобального стану | Викликає змінення змінних в кожному кроці без контролю            |

---

## **Пояснення на пальцях (💡)**

**1\. Відсутній базовий випадок**  
Якщо не сказати методу: "Зупинись, коли n \== 0", — він ніколи не зупиниться. Як бігти сходами в підвал без кінця.

**2\. Недосяжний base case**  
Наприклад: `if (n < 0) return ...`, але ти завжди передаєш `n > 0`. -> Ця умова ніколи не спрацює, і стек переповниться.

**3\. Поганий `return`**  
Якщо ти не повернув результат або не зібрав його правильно — отримаєш або `0`, або `null`, або неправильне значення.

**4\. Подвійні обчислення**  
У Фібоначчі: `fib(n - 1) + fib(n - 2)` — кожне викликається ще раз і ще... Результат правильний, але повільно
---

## **Корисні приклади (🧪)**

### **🔻 Відсутній базовий випадок:**

```java
// ❌ помилка: метод ніколи не зупиниться
public static void print(int n) {
    System.out.println(n);
    print(n - 1); // немає зупинки
}
```

### **🔻 Базовий випадок не досяжний:**

```java
public static int factorial(int n) {
    if (n < 0) return 1;  // ❌ ніколи не буде виконано при n > 0
    return n * factorial(n - 1);
}
```

### **🔻 Помилка в return:**

```java
public static int sum(int n) {
    if (n == 0) return 0;
    sum(n - 1); // ❌ нічого не повертається
}
```

✅ Правильно:

```java
public static int sum(int n) {
    if (n == 0) return 0;
    return n + sum(n - 1);
}
```

---

## **Порівняння (⚖️)**

| Помилка               | Симптом              | Як уникнути                      |
|-----------------------|----------------------|----------------------------------|
| Відсутній base case   | StackOverflowError   | Завжди перевіряй на зупинку      |
| Недосяжний base case  | Бескінечний цикл     | Перевір параметри перед викликом |
| Неправильний `return` | Помилкові результати | Ретельно тестуй приклади         |
| Повільна рекурсія     | Довгий час виконання | Мемоізація або ітерація          |

---

## **🧠 Мнемоніка для запам’ятовування:**

**“B-R-R” \= Base -> Reachable -> Return**

Тобто:

* є базовий випадок (Base),
* він реально досягається (Reachable),
* і результат коректно повертається (Return).

---

## **🟩 Підсумок:**

Рекурсія — потужний інструмент, але дуже чутливий до помилок. Найпоширеніші проблеми: відсутність або недосяжність
базового випадку, неправильне повернення результату, або дублювання обчислень. Кожну рекурсію потрібно проектувати
акуратно й перевіряти на прикладах. Використовуй принцип “B-R-R” для надійності.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка найпоширеніша помилка при написанні рекурсивного коду і до чого вона призводить?

> #### **💬 Відповідь:**
>
> Найпоширеніша і найкритичніша помилка — це **відсутній або некоректний базовий випадок (base case)**.
>
> **Базовий випадок** — це умова, яка припиняє ланцюжок рекурсивних викликів. Якщо його немає, або він написаний так, що ніколи не досягається, функція буде викликати сама себе нескінченно.
>
> **До чого це призводить:**
>
> Кожен виклик методу додає новий фрейм у стек викликів (Call Stack). При нескінченній рекурсії ці фрейми накопичуються, доки не вичерпається вся доступна для стека пам'ять. Це призводить до кидання помилки **`java.lang.StackOverflowError`**, що аварійно завершує потік виконання.
>
> **Приклад помилки (нескінченна рекурсія):**
>
> ```java
> public void countdown(int n) {
>     System.out.println(n);
>     // Немає умови зупинки. Буде викликатися для 0, -1, -2... нескінченно
>     countdown(n - 1);
> }
> ```
>
> Тут відсутній базовий випадок `if (n <= 0) return;`.

#### **📌 Що перевіряють:**

*Знання причини номер один для збою рекурсивних функцій. Розуміння того, як відсутність базового випадку призводить до `StackOverflowError`.*

* * *

### **2 ❓ Питання:**

Поясніть на прикладі, що означає "недосяжний базовий випадок".

> #### **💬 Відповідь:**
>
> **Недосяжний базовий випадок** — це ситуація, коли базовий випадок в коді є, але логіка рекурсивного кроку побудована так, що параметри ніколи не досягають значень, які задовольняють цю умову.
>
> **Приклад:**
>
> ```java
> // Мета: надрукувати числа від n до 1
> public void printDescending(int n) {
>     // Базовий випадок наче є...
>     if (n == 0) {
>         return;
>     }
>
>     System.out.println(n);
>     // Помилка: замість наближення до 0, ми віддаляємося від нього
>     printDescending(n + 1);
> }
>
> // Виклик printDescending(5) призведе до виведення 5, 6, 7...
> // і ніколи не досягне базового випадку n == 0.
> ```
>
> У цьому прикладі `n` збільшується, а не зменшується, тому умова `n == 0` для початкового `n > 0` ніколи не буде досягнута. Результат — такий самий, як і при відсутності базового випадку: `StackOverflowError`.

#### **📝 Коментар:**

Це логічна помилка, яку складніше помітити, ніж повну відсутність базового випадку. Вона вимагає аналізу того, як змінюються параметри при кожному рекурсивному виклику.

#### **📌 Що перевіряють:**

*Здатність аналізувати динаміку рекурсивного процесу та бачити логічні невідповідності між рекурсивним кроком та базовим випадком.*

* * *

### **3 ❓ Питання:**

Яка помилка часто виникає при роботі з поверненням значень (`return`) у рекурсії?

> #### **💬 Відповідь:**
>
> Поширена помилка — **ігнорування результату рекурсивного виклику**. Розробник викликає рекурсивний метод, але забуває використати його результат у `return` statement поточного виклику.
>
> **Приклад помилки (обчислення суми чисел):**
>
> ```java
> // НЕПРАВИЛЬНО
> public int sum(int n) {
>     if (n <= 0) {
>         return 0;
>     }
>     // Рекурсивний виклик виконується, але його результат ігнорується
>     sum(n - 1);
>     // Метод не повертає нічого (помилка компіляції) або повертає невірне значення
>     return n; // Наприклад, якщо написати так, результат для sum(3) буде 3, а не 6
> }
> ```
>
> **Правильна версія:**
>
> ```java
> // ПРАВИЛЬНО
> public int sum(int n) {
>     if (n <= 0) {
>         return 0;
>     }
>     // Результат рекурсивного виклику використовується
>     return n + sum(n - 1);
> }
> ```
>
> Потрібно пам'ятати, що результат має "підніматися" вгору по стеку викликів. Кожен рівень рекурсії повинен отримати результат від глибшого рівня, щось з ним зробити і повернути далі.

#### **📌 Що перевіряють:**

*Розуміння того, як результати "збираються" при зворотному ході рекурсії. Це одна з ключових концепцій для розуміння рекурсивних алгоритмів, що повертають значення.*

* * *

### **4 ❓ Питання:**

Що таке надлишкові або подвійні обчислення в рекурсії і до яких наслідків це призводить?

> #### **💬 Відповідь:**
>
> **Подвійні обчислення** — це ситуація, коли рекурсивна функція **багаторазово обчислює одні й ті самі підзадачі** з однаковими вхідними даними.
>
> Класичним прикладом є наївна реалізація чисел Фібоначчі:
> `fib(n) = fib(n-1) + fib(n-2)`
>
> При обчисленні `fib(5)`, дерево викликів показує, що:
>
> * `fib(3)` обчислюється 2 рази.
> * `fib(2)` обчислюється 3 рази.
>
> **Наслідки:**
>
> Це призводить до **експоненційної часової складності (O(2^n))**. Програма працює надзвичайно повільно навіть для відносно невеликих значень `n` (наприклад, `n = 45`). Хоча результат буде правильним, алгоритм є абсолютно непрактичним.
>
> **Рішення:**
>
> * **Мемоізація (кешування):** Зберігати вже обчислені результати в `Map` або масиві.
> * **Ітераційний підхід:** Використовувати цикл, який є набагато ефективнішим (O(n)).

#### **📌 Що перевіряють:**

*Здатність аналізувати часову складність алгоритмів. Розуміння того, що елегантний рекурсивний код може бути вкрай неефективним.*

* * *

### **5 ❓ Питання:**

Як використання глобальних змінних або полів класу може призвести до помилок у рекурсії? ⭐️

> #### **💬 Відповідь:**
>
> Використання глобальних змінних або полів класу для зберігання проміжного стану рекурсії є **антипатерном** і може призвести до серйозних помилок.
>
> **Проблема:**
>
> Кожен рекурсивний виклик має працювати з власним набором даних. Якщо ж всі виклики змінюють **спільний стан (shared state)**, вони починають заважати один одному.
>
> **Приклад помилки (неправильний підрахунок суми):**
>
> ```java
> public class BadRecursion {
>     private int totalSum = 0; // Спільне поле
>
>     public int sum(int n) {
>         if (n <= 0) {
>             return totalSum;
>         }
>         totalSum += n;
>         return sum(n - 1);
>     }
> }
> ```
>
> Цей код може "випадково" спрацювати для одного виклику. Але якщо ви викличете `sum(3)` на тому ж об'єкті ще раз, результат буде неправильним, оскільки `totalSum` не було скинуто до нуля. Що ще гірше, такий клас абсолютно **не є потокобезпечним**.
>
> **Правильне рішення:**
>
> Вся необхідна інформація для роботи рекурсивної функції має передаватися через **параметри методу**, а результат — повертатися через `return`.
>
> `public int sum(int n) { return n + sum(n-1); }`
>
> Це робить функцію **чистою (pure)**, без побічних ефектів, і гарантує, що кожен виклик є ізольованим.

#### **📌 Що перевіряють:**

*Розуміння важливості ізоляції стану та ризиків, пов'язаних з використанням спільних змінюваних даних. Це питання також стосується потокобезпечності.*

* * *

### **6 ❓ Питання:**

Чому `StackOverflowError` є `Error`, а не `Exception`? ⭐️

> #### **💬 Відповідь:**
>
> `StackOverflowError` є підкласом `java.lang.Error`, а не `java.lang.Exception`, тому що він сигналізує про **серйозну проблему в роботі JVM**, яку звичайна програма не повинна (і, як правило, не може) намагатися обробити.
>
> * **`Exception`**: Це помилки, які часто виникають у звичайній роботі програми і від яких можна відновитися (наприклад, `IOException`, `SQLException`). Програма повинна їх обробляти (`try-catch`).
>
> * **`Error`**: Це критичні, невідворотні помилки, пов'язані з проблемами в самій віртуальній машині або з вичерпанням ресурсів (наприклад, `OutOfMemoryError`). Продовжувати роботу програми після такої помилки зазвичай небезпечно і не має сенсу.
>
> `StackOverflowError` вказує на те, що стек викликів вичерпав виділену йому пам'ять. Це, як правило, є наслідком **логічної помилки в програмі** (нескінченна рекурсія), а не зовнішньою умовою, від якої можна відновитися. Намагатися зловити `StackOverflowError` у `catch` блоці — це поганий тон, тому що JVM вже може бути в нестабільному стані. Правильне рішення — **виправити код**, що спричинив помилку.

#### **📌 Що перевіряють:**

*Розуміння ієрархії винятків в Java (`Throwable`, `Error`, `Exception`) та семантичної різниці між ними.*

* * *

### **7 ❓ Питання:**

Як помилитися з базовим випадком при обході дерева або графу? ⭐️

> #### **💬 Відповідь:**
>
> При роботі з деревами або графами поширена помилка — некоректна обробка **листків дерева** або **вузлів без нащадків**.
>
> **Приклад помилки:**
>
> ```java
> // Мета: знайти максимальну глибину дерева
> public int maxDepth(Node node) {
>     // НЕПРАВИЛЬНИЙ базовий випадок. Що буде, якщо node.left == null?
>     // Виклик maxDepth(null) кине NullPointerException
>     if (node.left == null && node.right == null) {
>         return 1;
>     }
>     // ...
>     return 1 + Math.max(maxDepth(node.left), maxDepth(node.right));
> }
> ```
>
> **Правильний підхід:**
>
> Базовий випадок має перевіряти, чи є сам поточний вузол `null`. Це універсальна умова зупинки, яка коректно обробляє як порожні піддерева, так і ситуацію, коли у вузла немає одного з дітей.
>
> ```java
> public int maxDepth(Node node) {
>     // ПРАВИЛЬНИЙ базовий випадок
>     if (node == null) {
>         return 0;
>     }
>
>     // Рекурсивний крок
>     return 1 + Math.max(maxDepth(node.left), maxDepth(node.right));
> }
> ```
>
> Інша поширена помилка при обході графів — **відсутність перевірки на відвідані вузли**. Без цього, якщо в графі є цикли, рекурсія увійде в нескінченний цикл обходу, що призведе до `StackOverflowError`.

#### **📌 Що перевіряють:**

*Здатність аналізувати рекурсію на структурах даних та передбачати крайові випадки (null-вузли, цикли).*

* * *

### **8 ❓ Питання:**

Ви написали рекурсивну функцію, і вона завжди повертає `0` або `null`, хоча не має. У чому може бути причина? ⭐️

> #### **💬 Відповідь:**
>
> Це класичний симптом помилки, коли **результат рекурсивного виклику не використовується**. Найімовірніша причина в тому, що у рекурсивному кроці:
>
> 1. Ви викликаєте рекурсивний метод.
> 2. Але ви **не повертаєте** його результат (через `return`).
>
> **Приклад помилки для обчислення факторіалу:**
>
> ```java
> public long factorial(int n) {
>     // Базовий випадок коректний
>     if (n <= 1) {
>         return 1;
>     }
>     // Виклик є, але його результат нікуди не присвоюється і не повертається
>     factorial(n - 1);
>
>     // Якщо немає `return` (помилка компіляції) або тут стоїть якийсь інший
>     // return, наприклад, return 0;
>     return 0; // Для ілюстрації
> }
> ```
>
> У такому коді, коли рекурсія дійде до базового випадку `n=1`, вона поверне `1`. Але цей `1` "загубиться", бо виклик `factorial(1)` у фреймі `factorial(2)` не був використаний. Виконання у фреймі `factorial(2)` просто продовжиться, і врешті-решт, функція поверне те, що написано в кінці (в нашому прикладі - 0).
>
> **Виправлення:** Завжди пам'ятати, що результат глибокого виклику має бути "підхоплений" і переданий нагору: `return n * factorial(n - 1);`.

#### **📌 Що перевіряють:**

*Навички "дебагінгу в умі". Здатність за симптомами визначити найімовірнішу причину проблеми.*

* * *

### **9 ❓ Питання:**

Як тестування може допомогти уникнути помилок в рекурсії? Які випадки обов'язково потрібно покрити тестами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ретельне тестування є життєво важливим для написання коректного рекурсивного коду.
>
> **Обов'язкові тестові випадки (Test Cases):**
>
> 1. **Тест на базовий випадок (base case):**
>     * Перевірити, що функція працює коректно для найпростішого входу, який не вимагає рекурсії. Наприклад, для `factorial(0)` або `factorial(1)`. Це перевіряє умову зупинки.
>
> 2. **Тест на один рекурсивний крок:**
>     * Перевірити, що функція коректно працює для входу, який вимагає лише одного рекурсивного виклику. Наприклад, `factorial(2)`. Це перевіряє логіку рекурсивного кроку.
>
> 3. **Тест на загальний випадок (general case):**
>     * Перевірити для типового "середнього" входу, який вимагає кількох рівнів рекурсії (наприклад, `factorial(5)`). Це перевіряє, як результати "збираються" по ланцюжку викликів.
>
> 4. **Тест на граничні умови та некоректні дані:**
>     * Перевірити поведінку для нетипових, але можливих вхідних даних. Наприклад, що `factorial(-1)` коректно кидає `IllegalArgumentException`.
>
> 5. **Тест на потенційний `StackOverflowError` (необов'язково, але корисно):**
>     * Якщо є ризик великої глибини рекурсії, можна написати тест з великим вхідним значенням, щоб переконатися, що він або проходить, або падає з очікуваною помилкою.
>
> Покриття цих випадків значно підвищує впевненість у тому, що рекурсивна логіка не містить очевидних помилок.

#### **📌 Що перевіряють:**

*Системний підхід до тестування. Здатність визначати критичні тестові сценарії для рекурсивних алгоритмів.*

* * *

### **10 ❓ Питання:**

Як помилка з "off-by-one" (помилка на одиницю) може проявлятися в рекурсії? ⭐️⭐️

> #### **💬 Відповідь:**
>
> "Off-by-one" — це поширена помилка, коли цикл або рекурсія виконується на один раз більше або менше, ніж потрібно.
>
> **Як вона може проявитися в рекурсії:**
>
> 1. **Некоректний базовий випадок:**
>     * Припустимо, базовий випадок `if (n == 1) return 1;`. Якщо функцію викликати з `n=0`, базовий випадок буде пропущено, і рекурсія піде у від'ємні числа, що призведе до `StackOverflowError`. Правильно було б `if (n <= 1)`.
>
> 2. **Некоректний рекурсивний крок:**
>     * Наприклад, у задачі "знайти елемент у масиві" рекурсивний виклик передає діапазон `search(array, from, to)`. Якщо нові межі обчислені неправильно (`to-1` замість `mid-1`), можна або пропустити елемент, або вийти за межі масиву.
>
> **Приклад з друком масиву:**
>
> ```java
> // Мета: надрукувати всі елементи
> void printArray(int[] arr, int index) {
>     // Помилка "off-by-one": умова має бути `index < arr.length`
>     if (index > arr.length) { // Коли index == arr.length, умова хибна, цикл продовжується
>         return;
>     }
>     // При index == arr.length буде кинуто ArrayIndexOutOfBoundsException
>     System.out.println(arr[index]);
>     printArray(arr, index + 1);
> }
> ```
>
> Ця помилка виникає через неточне визначення граничних умов, що є дуже важливим у рекурсивних алгоритмах.

#### **📌 Що перевіряють:**

*Уважність до деталей та здатність бачити поширені логічні помилки, пов'язані з граничними умовами.*

* * *

### **11 ❓ Питання:**

Чи можна реалізувати рекурсію, яка ніколи не завершується, але і не кидає `StackOverflowError`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна. Це можливо в **асинхронній рекурсії**, де рекурсивний виклик не відбувається негайно, а планується на майбутнє, не блокуючи поточний стек викликів.
>
> **Приклад з використанням `CompletableFuture` (Java 8+):**
>
> ```java
> import java.util.concurrent.CompletableFuture;
>
> public class AsyncRecursion {
>
>     public static void endlessTask() {
>         System.out.println("Виконується крок...");
>
>         // Плануємо наступний "рекурсивний" виклик асинхронно
>         CompletableFuture.runAsync(() -> endlessTask());
>     }
>
>     public static void main(String[] args) throws InterruptedException {
>         endlessTask();
>         Thread.currentThread().join(); // Щоб головний потік не завершився
>     }
> }
> ```
>
> У цьому прикладі `endlessTask()` викликає сам себе, але асинхронно. Кожен виклик виконується, можливо, в іншому потоці з пулу. Поточний метод завершує свою роботу, його фрейм виймається зі стеку, і лише потім починає виконуватися наступний запланований виклик.
>
> Стек викликів **не накопичується**, тому `StackOverflowError` не виникає. Програма буде працювати нескінченно (доки її не зупинять), друкуючи "Виконується крок...". Це демонструє, що `StackOverflowError` пов'язаний саме з **синхронними, вкладеними** викликами.

#### **📌 Що перевіряють:**

*Глибокі знання про багатопотоковість та асинхронне програмування. Здатність мислити про рекурсію в більш широкому контексті, ніж просто синхронні виклики.*

* * *

### **12 ❓ Питання:**

Що може бути причиною помилки, якщо ваш рекурсивний код коректно працює для одних вхідних даних, але повертає неправильний результат для інших? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже поширена ситуація, і причини зазвичай криються в логіці, яка не є достатньо загальною:
>
> 1. **Некоректний базовий випадок:** Базовий випадок може обробляти лише одну з кількох можливих умов зупинки. Наприклад, `if (n == 0)` може працювати, але код не обробляє `n == 1` як інший базовий випадок (як у Фібоначчі).
>
> 2. **Помилка в рекурсивному кроці:** Логіка, яка "збирає" результат, може бути неправильною. Наприклад, `return 1 + sum(n-1)` замість `return n + sum(n-1)`. Для `n=1` результат буде правильним (`1+0=1`), але для `n=2` вже буде помилка (`1+(1+0)=2` замість `3`).
>
> 3. **Неправильна обробка "гілок":** У рекурсії з кількома гілками (як при обході дерева) може бути пропущена обробка однієї з гілок. Наприклад, викликається тільки `recursiveCall(node.left)`, а `recursiveCall(node.right)` — забутий.
>
> 4. **Використання цілочисельного ділення:** При задачах на ділення (`n / 2`) можна отримати неочікувані результати через відкидання дробової частини, якщо алгоритм на це не розрахований.
>
> 5. **Прихована залежність від стану:** Якщо рекурсія не є "чистою" і залежить від зовнішнього стану, різні послідовності викликів можуть призводити до різного кінцевого стану і, як наслідок, до різних результатів.
>
> Найкращий спосіб знайти таку помилку — це "прокрутити" алгоритм вручну на папері для тих даних, де він дає збій, відстежуючи значення змінних на кожному рівні рекурсії.

#### **📌 Що перевіряють:**

*Аналітичні навички та здатність систематично підходити до пошуку логічних помилок у складному коді.*

* * *

### **13 ❓ Питання:**

Чому ітераційний підхід з використанням явного стеку може бути кращим за рекурсію для обходу дуже глибоких дерев? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітераційний підхід з явним стеком (`java.util.Stack` або `ArrayDeque`) є кращим з двох головних причин:
>
> 1. **Запобігання `StackOverflowError`:**
>     * Явний стек, який ви створюєте, розміщується в **купі (heap)**, а не в стеку викликів. Пам'ять купи є набагато більшою, ніж пам'ять, виділена під стек одного потоку.
>     * Це означає, що ви можете обробляти дерева з дуже великою глибиною, не ризикуючи отримати `StackOverflowError`. Ви обмежені лише загальним обсягом доступної пам'яті в JVM.
>
> 2. **Більший контроль:**
>     * Ви отримуєте повний контроль над стеком. Ви можете перевіряти його розмір, заглядати на вершину, не виймаючи елемент (`peek()`), або навіть маніпулювати його вмістом, якщо це потрібно для складних алгоритмів.
>     * При рекурсії ви не маєте прямого доступу до стека викликів і не можете ним керувати.
>
> **Недолік:** Ітераційний код є значно менш читабельним і складнішим для написання та налагодження порівняно з елегантним рекурсивним рішенням. Тому це компроміс між простотою коду та надійністю для великих обсягів даних.

#### **📌 Що перевіряють:**

*Глибоке розуміння управління пам'яттю в Java (стек vs. купа). Здатність аналізувати компроміси між різними підходами до реалізації одного й того ж алгоритму.*

* * *

### **14 ❓ Питання:**

Припустимо, у вашому рекурсивному методі виникає перевіряємий виняток (наприклад, `IOException`). Як правильно його обробляти? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Обробка перевіряємих винятків у рекурсії вимагає уважності, щоб не "проковтнути" виняток і коректно передати його нагору.
>
> **Найкращий підхід** — це "прокидати" виняток далі.
>
> 1. Оголосити, що ваш рекурсивний метод `throws IOException`.
> 2. Рекурсивний виклик всередині методу обернути в `try-catch`, або просто дозволити винятку "пролетіти" вище. Зазвичай краще просто прокидати його.
>
> **Приклад з прокиданням:**
>
> ```java
> public void processDirectory(File dir) throws IOException {
>     if (dir == null) return;
>
>     File[] files = dir.listFiles();
>     if (files != null) {
>         for (File file : files) {
>             if (file.isDirectory()) {
>                 // Просто прокидаємо виняток, що може виникнути глибше
>                 processDirectory(file);
>             } else {
>                 // Тут може виникнути IOException
>                 // readFile(file);
>             }
>         }
>     }
> }
> ```
>
> Високорівневий, нерекурсивний метод, який ініціює весь процес, вже повинен мати `try-catch` блок для обробки `IOException`.
>
> **Чого не варто робити:**
>
> Не варто "ловити" `IOException` всередині рекурсивного методу і нічого з ним не робити (`catch (IOException e) { }`), або просто друкувати його (`e.printStackTrace()`). Це "проковтне" помилку, і код на верхньому рівні не дізнається, що сталася проблема, продовжуючи роботу, ніби все гаразд.

#### **📌 Що перевіряють:**

*Знання механізму обробки винятків та вміння правильно застосовувати його в рекурсивному контексті, щоб не втратити інформацію про помилку.*

* * *

### **15 ❓ Питання:**

Як помилка "неправильний тип даних" може призвести до нескінченної рекурсії? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це може статися, якщо логіка, що "зменшує" задачу, дає збій через особливості роботи з певними типами даних, наприклад, **цілочисельним діленням**.
>
> **Приклад:** Рекурсивний пошук в упорядкованому масиві, де допущено помилку.
>
> ```java
> // Мета: знайти елемент у діапазоні [low, high]
> // Припустимо, ми робимо помилку в обчисленні межі
> public boolean binarySearch(int[] arr, int target, int low, int high) {
>     if (low > high) {
>         return false; // Базовий випадок
>     }
>     int mid = low + (high - low) / 2;
>
>     if (arr[mid] == target) {
>         return true;
>     } else if (arr[mid] < target) {
>         // Правильно: mid + 1
>         return binarySearch(arr, target, mid + 1, high);
>     } else {
>         // Помилка тут: мало бути mid - 1
>         return binarySearch(arr, target, low, mid);
>     }
> }
> ```
>
> **Як виникає нескінченна рекурсія:**
>
> Розглянемо виклик `binarySearch(arr, target, 0, 1)`, де шуканий елемент не в цьому діапазоні і `target < arr[0]`.
>
> 1. `low = 0`, `high = 1`. `mid = 0`.
> 2. `arr[0]` не є `target`, і `target < arr[0]`.
> 3. Виконується рекурсивний виклик `binarySearch(arr, target, 0, 0)`.
>
> **Тепер розглянемо `binarySearch(arr, target, 0, 0)`:**
>
> 1. `low=0, high=0. mid=0`
> 2. `arr[0]`...
> 3. Виконується рекурсивний виклик `binarySearch(arr, target, 0, 0)`. **Застрягли**
>
> Помилка в тому, що коли `low` і `mid` однакові, `mid` не змінюється, і рекурсія зациклюється на тому самому діапазоні. Правильна реалізація мала б звужувати діапазон як `mid-1` і `mid+1`. Це показує, як неточна маніпуляція з цілими числами може зламати умову "наближення до базового випадку".

#### **📌 Що перевіряють:**

*Уважність до деталей, особливо до цілочисельної арифметики. Здатність проаналізувати, як помилки в обчисленнях можуть призвести до логічних помилок у рекурсії.*
