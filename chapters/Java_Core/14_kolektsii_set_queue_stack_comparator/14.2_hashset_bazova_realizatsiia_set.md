---
layout: default
title: "HashSet - базова реалізація Set"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 2
---

# HashSet - базова реалізація Set

* короткий опис: `HashSet` – це базова реалізація інтерфейсу `Set`, що зберігає елементи без дублікатів і без збереження порядку, базуючись на хеш-таблиці.

* поняття: `HashSet`, хеш-таблиця, `O(1)` (середня продуктивність), `hashCode()`, `equals()`.

* приклади: `set.add("Apple"); set.contains("Apple");`. Дозволяє один `null` елемент.

* ключові моменти: Використовує внутрішній `HashMap`. Забезпечує дуже швидкий доступ до елементів (O(1) в середньому) завдяки хешуванню.

* що важливо знати на співбесіді: Принцип роботи на основі хеш-таблиці, чому порядок не зберігається, продуктивність операцій (O(1)).

### **✅ Офіційне визначення:**

**`HashSet`** — це реалізація інтерфейсу **`Set`**, яка зберігає елементи **без дублікатів**, **без збереження порядку**, і базується на **хеш-таблиці**.

### **🧠 Простими словами:**

**`HashSet`** — як чарівна скриня, яка швидко перевіряє: “А ти тут вже був?” — якщо так, не впускає. Порядок не важливий, швидкість — максимальна.

---

## **📌 Ключові методи `HashSet`:*

| Метод | Опис |
| :---: | :---: |
| **`add(E e)`** | Додає елемент, якщо його ще немає |
| **`contains(Object o)`** | Перевіряє, чи є елемент у множині |
| **`remove(Object o)`** | Видаляє елемент |
| **`size()`** | Повертає кількість елементів |
| **`clear()`** | Видаляє всі елементи |
| **`isEmpty()`** | Перевіряє, чи множина порожня |

---

## **Пояснення на пальцях (💡)**

* **`HashSet`** використовує **внутрішній `HashMap`**, де елемент — це ключ, а значення — умовний **`Object`**\-заглушка.
* Швидкість доступу (**`add`**, **`remove`**, **`contains`**) забезпечується через **обчислення hashCode \+ equals**.
* Порядок додавання не гарантується.
* **Під капотом:** **`HashSet`** -> використовує **`HashMap<E, Object>`** з константною заглушкою **`PRESENT`**.

---

## **Корисні приклади (🧪)**

```java
Set<String> set = new HashSet<>();
set.add("Apple");
set.add("Banana");
set.add("Apple"); // дубль, не додасться
System.out.println(set); // [Banana, Apple] -- без гарантії порядку

System.out.println(set.contains("Apple")); // true
System.out.println(set.size()); // 2
set.remove("Banana");
set.clear(); // все видалено
```

---

## **Порівняння (⚖️)**

|       Параметр        |    HashSet    |     TreeSet     |    LinkedHashSet    |
|:---------------------:|:-------------:|:---------------:|:-------------------:|
| **Порядок елементів** |    ❌ Немає    | ✅ Відсортований | ✅ Порядок додавання |
|  **Продуктивність**   |    ✅ O(1)     |   ❌ O(log n)    |       ✅ O(1)        |
|   **Null елементи**   | ✅ Дозволено 1 | ❌ Не дозволено  |    ✅ Дозволено 1    |

---

## **🧠 Мнемоніка для запам’ятовування:**

**H.A.S.H.**

* **H** — Hash-based (на хеш-таблиці)
* **A** — Add швидко
* **S** — Set (унікальні значення)
* **H** — High performance (O(1))

---

## **🟩 Підсумок:**

* **`HashSet`** — реалізація **`Set`**, яка забезпечує **унікальність без порядку**
* Побудована на базі **`HashMap`**
* Доступ до елементів — **O(1)** у середньому випадку
* Працює з **`hashCode()`** \+ **`equals()`**, дозволяє один **`null`**
* Не зберігає порядок додавання
* Ідеально підходить для перевірки наявності / унікальності значень

---

### **🔗 Додаткові джерела:**

* [**HashSet — Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/HashSet.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `HashSet` в Java і які його основні характеристики?

> #### **💬 Відповідь:**
>
> **`HashSet`** — це одна з найпоширеніших реалізацій інтерфейсу `Set` у Java Collections Framework. Вона використовується для зберігання унікальних елементів у невпорядкованій колекції.
>
> Основні характеристики `HashSet`:
>
> * **Унікальність елементів:** `HashSet` не дозволяє зберігати дублікати. Спроба додати елемент, який вже існує (перевірка через `equals()`), буде проігнорована.
>
> * **Відсутність порядку:** `HashSet` не гарантує жодного порядку елементів. Порядок, в якому елементи повертаються під час ітерації, може змінюватися і не відповідати порядку їх додавання.
>
> * **Висока продуктивність:** Завдяки використанню хеш-таблиці, базові операції, такі як `add()`, `remove()`, `contains()` та `size()`, виконуються за константний середній час — **O(1)**.
>
> * **Дозволяє `null`:** `HashSet` може містити один `null` елемент. Спроба додати другий `null` буде проігнорована.

#### **📝 Коментар:**

Уявіть `HashSet` як мішок для іграшок. Ви можете швидко кинути туди іграшку (`add`), миттєво перевірити, чи є там певна іграшка (`contains`), і так само швидко її дістати (`remove`). Але коли ви висипаєте іграшки з мішка, вони випадають у випадковому порядку.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `HashSet` та його ключових властивостей: унікальність, відсутність порядку та швидкість.*

* * *

### **2 ❓ Питання:**

Як `HashSet` працює під капотом? На якій структурі даних він базується?

> #### **💬 Відповідь:**
>
> `HashSet` внутрішньо базується на структурі даних **`HashMap`**. Це ключовий аспект, який пояснює всі його властивості.
>
> * **Внутрішній `HashMap`:** Коли ви створюєте `HashSet`, насправді створюється екземпляр `HashMap<E, Object>`.
>
> * **Зберігання елементів:** Елемент, який ви додаєте в `HashSet` (наприклад, за допомогою `set.add("Apple")`), стає **ключем** у цьому внутрішньому `HashMap`.
>
> * **Значення-заглушка:** Значенням для кожного ключа в `HashMap` є константний об'єкт-заглушка, який називається `PRESENT`. Це приватний статичний об'єкт типу `Object`. Він однаковий для всіх елементів.
>
> Таким чином, виклик `set.add(element)` фактично перетворюється на `map.put(element, PRESENT)`. Оскільки ключі в `HashMap` унікальні, це автоматично забезпечує унікальність елементів у `HashSet`.

#### **📝 Мнемоніка:**

**H**ash**S**et = **H**ash**M**ap's **S**ecret. `HashSet` — це просто "фасад" або "обгортка" для `HashMap`, яка використовує тільки його функціональність для ключів, ігноруючи значення.

#### **📌 Що перевіряють:**

*Розуміння внутрішньої реалізації `HashSet`. Це питання показує, чи знає кандидат, що багато колекцій Java побудовані одна на одній, і як це впливає на їхню поведінку.*

* * *

### **3 ❓ Питання:**

Як `HashSet` гарантує унікальність елементів? Які методи для цього використовуються?

> #### **💬 Відповідь:**
>
> `HashSet` використовує комбінацію двох методів об'єкта — `hashCode()` та `equals()` — для забезпечення унікальності. Цей процес складається з двох кроків:
>
> 1. **Обчислення `hashCode()`:** Коли ви намагаєтесь додати новий елемент, `HashSet` спочатку викликає у нього метод `hashCode()`. Результат цього методу (хеш-код) використовується для визначення "комірки" (bucket) у внутрішній хеш-таблиці, де цей елемент має зберігатися. Це дозволяє швидко знайти потенційне місце для елемента.
>
> 2. **Перевірка через `equals()`:** Якщо комірка вже містить інші елементи (це називається колізією), `HashSet` починає перебирати їх і порівнювати новий елемент з кожним існуючим за допомогою методу `equals()`. Якщо `equals()` повертає `true` хоча б для одного з елементів у комірці, новий елемент вважається дублікатом і не додається.
>
> **Висновок:** Для того, щоб два об'єкти вважалися однаковими в `HashSet`, їх `hashCode()` можуть бути однаковими, але `equals()` обов'язково має повертати `true`.

#### **📝 Коментар:**

Це як сортування пошти. `hashCode()` — це поштовий індекс, який швидко відправляє лист у правильне місто. `equals()` — це вже листоноша, який у цьому місті знаходить точну вулицю і будинок, щоб перевірити, чи це той самий адресат.

#### **📌 Що перевіряють:**

*Розуміння фундаментального контракту між `hashCode()` та `equals()`, який є основою для всіх хеш-базованих колекцій в Java.*

* * *

### **4 ❓ Питання:**

Яка часова складність основних операцій (`add`, `remove`, `contains`) у `HashSet` і чому?

> #### **💬 Відповідь:**
>
> Часова складність основних операцій у `HashSet` є однією з його головних переваг.
>
> * **Середня складність: O(1) — константний час.**
>     Це означає, що час виконання операцій `add()`, `remove()` та `contains()` не залежить від кількості елементів у `HashSet`. Це досягається завдяки хешуванню:
>     1. Обчислення `hashCode()` займає фіксований час.
>     2. Пошук потрібної комірки (bucket) за хеш-кодом також відбувається миттєво.
>     За умови, що елементи рівномірно розподілені по комірках, ці операції виконуються дуже швидко.
>
> * **Найгірша складність: O(n) — лінійний час.**
>     Найгірший випадок трапляється, коли багато елементів мають однаковий `hashCode()`. Це призводить до колізій, і всі ці елементи потрапляють в одну комірку, утворюючи довгий список. Тоді для пошуку, додавання або видалення елемента доведеться перебрати всі `n` елементів у цій комірці, що зводить продуктивність до лінійної.

#### **📝 Коментар:**

Хоча теоретично існує найгірший випадок O(n), на практиці він зустрічається вкрай рідко, якщо використовується якісна реалізація `hashCode()`. Тому `HashSet` вважається надзвичайно ефективною структурою для перевірки наявності елементів.

#### **📌 Що перевіряють:**

*Знання часової складності (Big O notation) та здатність пояснити, чому вона саме така, згадуючи як середній, так і найгірший сценарії.*

* * *

### **5 ❓ Питання:**

Чи можна додати `null` в `HashSet`? Якщо так, то скільки разів?

> #### **💬 Відповідь:**
>
> Так, `HashSet` дозволяє зберігати `null`-значення.
>
> Однак, оскільки `HashSet` зберігає тільки унікальні елементи, ви можете додати **лише один `null`**.
>
> Під капотом `null` обробляється як особливий випадок. Для нього `hashCode()` не обчислюється; натомість він завжди розміщується у нульовій комірці (bucket 0) хеш-таблиці. Перша спроба додати `null` буде успішною. Всі наступні спроби додати `null` будуть проігноровані, оскільки `HashSet` вже містить такий "елемент".

#### **📝 Приклад:**

```java
Set<String> set = new HashSet<>();
System.out.println("Додаємо перший null: " + set.add(null)); // true
System.out.println("Додаємо другий null: " + set.add(null));  // false
set.add("Element");

System.out.println("Розмір сету: " + set.size()); // 2
System.out.println("Сет містить: " + set); // [null, Element]
```

#### **📌 Що перевіряють:**

*Уважність до деталей та знання того, як колекція обробляє крайні випадки, такі як `null`-значення. Це порівнює `HashSet` з іншими колекціями, наприклад, `TreeSet`, який не дозволяє `null`.*

* * *

### **6 ❓ Питання:**

Чому `HashSet` не зберігає порядок додавання елементів?

> #### **💬 Відповідь:**
>
> `HashSet` не зберігає порядок додавання, тому що його головний пріоритет — швидкість доступу до елементів, а не їх послідовність. Порядок визначається внутрішньою структурою — хеш-таблицею.
>
> 1. **Позиція залежить від хеш-коду:** Коли ви додаєте елемент, його позиція у внутрішньому масиві (хеш-таблиці) визначається його `hashCode()`, а не тим, коли його додали. Наприклад, об'єкт з `hashCode()` 12345 може опинитися в комірці №5, а наступний доданий об'єкт з `hashCode()` 987 — в комірці №3.
>
> 2. **Порядок ітерації:** Коли ви перебираєте `HashSet`, ітератор просто проходить по масиву комірок хеш-таблиці з початку до кінця і повертає елементи з кожної не порожньої комірки. Цей порядок обходу ніяк не пов'язаний з послідовністю викликів методу `add()`.
>
> Якщо потрібен порядок, слід використовувати інші реалізації `Set`: `LinkedHashSet` (зберігає порядок додавання) або `TreeSet` (зберігає елементи у відсортованому порядку).

#### **📝 Аналогія:**

Це як парковка з номерами місць. Ви не паркуєте машини по черзі (1, 2, 3...), а шукаєте вільне місце, яке вам "призначено" (на основі `hashCode`). Коли ви оглядаєте парковку, ви бачите машини в порядку номерів місць, а не в порядку їх прибуття.

#### **📌 Що перевіряють:**

*Розуміння причинно-наслідкового зв'язку між внутрішньою структурою (`HashMap`) і зовнішньою поведінкою (`HashSet`). Кандидат повинен пояснити, *чому* порядок відсутній.*

* * *

### **7 ❓ Питання:**

У чому різниця між `HashSet`, `LinkedHashSet` та `TreeSet`?

> #### **💬 Відповідь:**
>
> Всі три класи є реалізаціями інтерфейсу `Set` і зберігають унікальні елементи, але вони відрізняються за трьома ключовими параметрами: порядок, продуктивність та підтримка `null`.
>
> | **Параметр** | **`HashSet`** | **`LinkedHashSet`** | **`TreeSet`** |
> | :--- | :--- | :--- | :--- |
> | **Порядок елементів** | **Немає** (невпорядкований) | **Зберігає порядок додавання** | **Є** (відсортований природним порядком або за допомогою `Comparator`) |
> | **Внутрішня структура** | `HashMap` | `LinkedHashMap` | `TreeMap` (червоно-чорне дерево) |
> | **Продуктивність** | **O(1)** (найвища) | **O(1)** (трохи повільніше `HashSet` через підтримку зв'язного списку) | **O(log n)** (повільніше через деревоподібну структуру) |
> | **Підтримка `null`** | **Дозволяє один `null`** | **Дозволяє один `null`** | **Не дозволяє `null`** (викликає `NullPointerException`) |

#### **📝 Коментар:**

* Використовуйте **`HashSet`**, коли потрібна максимальна швидкість і порядок не важливий. (Наприклад, перевірка унікальності ID).
* Використовуйте **`LinkedHashSet`**, коли важливий порядок, в якому елементи були додані. (Наприклад, кеш, де важливий порядок доступу).
* Використовуйте **`TreeSet`**, коли елементи мають бути завжди відсортовані. (Наприклад, відображення списку користувачів за алфавітом).

#### **📌 Що перевіряють:**

*Здатність порівнювати схожі структури даних та обирати правильний інструмент для конкретного завдання. Це демонструє широту знань Java Collections Framework.*

* * *

### **8 ❓ Питання:**

Що таке `load factor` (коефіцієнт завантаження) та `initial capacity` (початкова ємність) у `HashSet`? ⭐️

> #### **💬 Відповідь:**
>
> `HashSet`, оскільки він базується на `HashMap`, має два важливі параметри, що впливають на його продуктивність та використання пам'яті:
>
> 1. **`Initial Capacity` (Початкова ємність):** Це початкова кількість "комірок" (buckets) у хеш-таблиці. За замовчуванням вона дорівнює **16**. Це початковий розмір внутрішнього масиву.
>
> 2. **`Load Factor` (Коефіцієнт завантаження):** Це поріг, який визначає, наскільки заповненою може бути хеш-таблиця, перш ніж її ємність буде автоматично збільшена. За замовчуванням він дорівнює **0.75** (або 75%).
>
> **Як це працює:**
> Коли кількість елементів у `HashSet` перевищує `(capacity * load factor)`, відбувається процес, що називається **re-hashing** (перехешування). Створюється нова хеш-таблиця (зазвичай удвічі більша), і всі існуючі елементи перерозподіляються по нових комірках відповідно до їхніх хеш-кодів. Це дорога операція, яка може тимчасово "підвісити" програму.

#### **📝 Приклад:**

За замовчуванням (ємність 16, коефіцієнт 0.75), перехешування відбудеться, коли ви спробуєте додати 13-й елемент (бо `16 * 0.75 = 12`). Ємність буде збільшена до 32.

#### **📝 Коментар:**

Якщо ви заздалегідь знаєте, що будете зберігати в `HashSet` велику кількість елементів (наприклад, 1 мільйон), краще створити його з відповідною початковою ємністю: `new HashSet<>(1_000_000)`. Це дозволить уникнути багаторазових і дорогих операцій перехешування.

#### **📌 Що перевіряють:**

*Глибше розуміння внутрішніх механізмів оптимізації `HashSet`. Кандидат повинен знати, як можна налаштувати колекцію для кращої продуктивності в конкретних сценаріях.*

* * *

### **9 ❓ Питання:**

Що станеться, якщо змінити об'єкт після того, як його додали в `HashSet`? ⭐️

> #### **💬 Відповідь:**
>
> Це класичне питання з підступом. Якщо змінити поле об'єкта, яке бере участь в обчисленні `hashCode()`, це призведе до **порушення контракту `HashSet`** і непередбачуваної поведінки.
>
> Ось що відбувається:
>
> 1. Коли ви додаєте об'єкт, `HashSet` обчислює його `hashCode()` і зберігає в відповідній комірці.
> 2. Потім ви змінюєте поле об'єкта (наприклад, ім'я користувача).
> 3. Тепер, якщо у цього об'єкта викликати `hashCode()`, він поверне нове значення, відмінне від початкового.
>
> **Наслідки:**
>
> * **Об'єкт буде "втрачено":** Коли ви спробуєте перевірити наявність цього об'єкта (`set.contains(myObject)`), `HashSet` обчислить новий хеш-код і буде шукати в **неправильній комірці**. Він не знайде об'єкт, хоча той фізично все ще лежить у старій комірці. `contains()` поверне `false`.
> * **Неможливість видалення:** `set.remove(myObject)` також не спрацює з тієї ж причини.
> * **Можливість додати дублікат:** Ви зможете додати інший об'єкт, який є `equals()` до зміненого, тому що `HashSet` не зможе знайти оригінал для порівняння.
>
> **Правило:** Об'єкти, що додаються в `HashSet` (або як ключі в `HashMap`), мають бути **незмінними (immutable)**, принаймні в частині полів, що використовуються в `hashCode()` та `equals()`.

#### **📝 Приклад:**

```java
class User {
    String name;
    // ... hashCode() і equals() залежать від name
}

Set<User> users = new HashSet<>();
User user1 = new User("Alice");
users.add(user1); // hashCode() обчислено для "Alice"

System.out.println(users.contains(user1)); // true

user1.name = "Bob"; // Змінюємо поле, що впливає на hashCode()

System.out.println(users.contains(user1)); // Поверне false! Об'єкт "втрачено".
```

#### **📌 Що перевіряють:**

*Глибоке розуміння наслідків порушення контракту `hashCode`/`equals`. Це питання перевіряє, чи усвідомлює кандидат небезпеку роботи зі змінними об'єктами в хеш-колекціях.*

* * *

### **10 ❓ Питання:**

Який контракт повинен виконуватися між методами `hashCode()` та `equals()`? ⭐️

> #### **💬 Відповідь:**
>
> Контракт між `hashCode()` та `equals()` є фундаментальним для коректної роботи всіх хеш-базованих колекцій Java, включаючи `HashSet`. Він складається з трьох правил:
>
> 1. **Якщо два об'єкти рівні за `equals()`, їх `hashCode()` мають бути однаковими.**
>     `if (a.equals(b)) then a.hashCode() == b.hashCode()`.
>     Це найважливіше правило. Якщо його порушити, `HashSet` не зможе знайти об'єкт у правильній комірці і може зберегти дублікати.
>
> 2. **Якщо два об'єкти не рівні за `equals()`, їх `hashCode()` можуть бути як різними, так і однаковими.**
>     `if (!a.equals(b))` про їх `hashCode()` нічого сказати не можна. Ситуація, коли хеш-коди однакові, називається колізією. Це нормально і обробляється хеш-таблицею.
>
> 3. **`hashCode()` має повертати однакове значення для одного й того ж об'єкта протягом одного виконання програми**, за умови, що поля, які використовуються для обчислення, не змінювалися.
>
> Коротко: **рівні об'єкти — рівні хеш-коди, нерівні об'єкти — не обов'язково різні хеш-коди.**

#### **📝 Коментар:**

Найпростіший спосіб запам'ятати: уявляйте людей-близнюків. Якщо вони справді ідентичні (`equals() == true`), то вони мають жити в одному будинку (`hashCode()` однаковий). Але в одному великому будинку (`hashCode()` однаковий) можуть жити і просто сусіди, які не є близнюками (`equals() == false`). Не можна селити близнюків у різні будинки.

#### **📌 Що перевіряють:**

*Знання одного з найважливіших контрактів у Java. Нерозуміння цього контракту є червоним прапором на співбесіді, оскільки воно свідчить про відсутність глибокого розуміння основ мови.*

* * *

### **11 ❓ Питання:**

Коли варто віддати перевагу `HashSet` перед `ArrayList` для перевірки наявності елемента? ⭐️

> #### **💬 Відповідь:**
>
> Вибір між `HashSet` та `ArrayList` для перевірки наявності елемента (операція `contains()`) залежить від розміру колекції та частоти операцій.
>
> **`HashSet`:**
>
> * **Продуктивність `contains()`:** **O(1)** в середньому. Час пошуку не залежить від кількості елементів.
> * **Використання пам'яті:** Більше, ніж `ArrayList`, через накладні витрати на хеш-таблицю.
> * **Ідеальний сценарій:** Коли у вас велика колекція (тисячі, мільйони елементів) і вам потрібно часто і швидко перевіряти, чи міститься в ній певний елемент.
>
> **`ArrayList`:**
>
> * **Продуктивність `contains()`:** **O(n)**. Для пошуку елемента `ArrayList` змушений перебирати всі елементи поспіль, від початку до кінця, доки не знайде його.
> * **Використання пам'яті:** Менше, ніж `HashSet`, бо зберігає лише елементи в масиві.
> * **Ідеальний сценарій:** Коли колекція маленька, або коли перевірка наявності відбувається дуже рідко, а основні операції — це доступ за індексом (`get(i)`).
>
> **Висновок:** Для завдань, де домінує пошук елементів (наприклад, "чи бачили ми вже цього користувача?"), `HashSet` є беззаперечним переможцем, особливо на великих обсягах даних. Перетворення `ArrayList` в `HashSet` для однієї операції пошуку може бути виправданим, якщо список дуже великий: `new HashSet<>(myList).contains(element)`.

#### **📝 Коментар:**

Це як шукати книгу в бібліотеці. `ArrayList.contains()` — це переглядати кожну книгу на кожній полиці по черзі. `HashSet.contains()` — це підійти до комп'ютерного каталогу (хеш-функція), ввести назву і миттєво отримати номер полиці.

#### **📌 Що перевіряють:**

*Практичне розуміння компромісів (trade-offs) між різними структурами даних. Кандидат повинен вміти аналізувати задачу і обирати оптимальне рішення на основі вимог до продуктивності.*

* * *

### **12 ❓ Питання:**

Як можна проітеруватися по `HashSet`? Чи є якісь особливості? ⭐️

> #### **💬 Відповідь:**
>
> Існує кілька способів ітерації по `HashSet`, але всі вони мають одну спільну рису: **порядок ітерації не гарантований**.
>
> 1. **Enhanced `for` loop (for-each):** Найпростіший і найпоширеніший спосіб.
>
>     ```java
>     Set<String> set = new HashSet<>();
>     // ... додаємо елементи
>     for (String element : set) {
>         System.out.println(element);
>     }
>     ```
>
> 2. **Використання `Iterator`:** Класичний спосіб, який дозволяє безпечно видаляти елементи під час ітерації.
>
>     ```java
>     Iterator<String> iterator = set.iterator();
>     while (iterator.hasNext()) {
>         String element = iterator.next();
>         if (element.equals("Apple")) {
>             iterator.remove(); // Безпечне видалення
>         }
>     }
>     ```
>
> 3. **Java 8 `forEach` з лямбда-виразом:** Сучасний та компактний спосіб.
>
>     ```java
>     set.forEach(element -> System.out.println(element));
>     // Або з посиланням на метод
>     set.forEach(System.out::println);
>     ```
>
> **Важлива особливість:** Намагатися видалити елемент з `HashSet` за допомогою звичайного `for` або `for-each` циклу (через `set.remove()`) призведе до `ConcurrentModificationException`. Для видалення під час ітерації слід використовувати виключно `Iterator.remove()`.

#### **📌 Що перевіряють:**

*Знання стандартних способів ітерації по колекціях та розуміння правил безпечної модифікації колекції під час перебору, зокрема небезпеки `ConcurrentModificationException`.*

* * *

### **13 ❓ Питання:**

Що таке колізії в `HashSet` і як вони впливають на продуктивність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Колізія** — це ситуація, коли два або більше різних (за `equals()`) об'єктів мають однаковий `hashCode()`.
>
> Оскільки позиція об'єкта в хеш-таблиці визначається його хеш-кодом, усі об'єкти з однаковим хеш-кодом потрапляють в одну й ту саму "комірку" (bucket).
>
> **Як `HashSet` (через `HashMap`) обробляє колізії:**
>
> 1. **До Java 8:** Всі елементи, що потрапили в одну комірку, зберігалися у вигляді **зв'язного списку (linked list)**. Коли потрібно було знайти елемент, Java перебирала цей список і порівнювала кожен елемент за допомогою `equals()`.
>
> 2. **Починаючи з Java 8:** Для оптимізації, якщо кількість елементів в одній комірці перевищує певний поріг (**`TREEIFY_THRESHOLD`**, за замовчуванням 8), зв'язний список перетворюється на **збалансоване бінарне дерево (червоно-чорне дерево)**.
>
> **Вплив на продуктивність:**
>
> * **Без колізій:** Пошук елемента — це O(1).
> * **З колізіями (зв'язний список):** Пошук перетворюється на O(k), де k — кількість елементів у комірці. У найгіршому випадку (всі елементи в одній комірці) це O(n).
> * **З колізіями (дерево, Java 8+):** Пошук у комірці оптимізується до O(log k). Це значно покращує продуктивність у найгірших випадках.
>
> Хороша функція `hashCode()`, яка рівномірно розподіляє елементи, є ключем до уникнення колізій і підтримки продуктивності O(1).

#### **📝 Коментар:**

Уявіть, що хеш-код — це номер поверху в готелі. В ідеалі, кожен гість отримує окрему кімнату (поверх). Колізія — це коли кільком гостям дають один і той самий номер поверху. Тоді їх селять у різні кімнати на цьому поверсі (зв'язний список). Якщо на поверсі стає занадто багато людей, адміністратор перебудовує його в ефективний лабіринт (дерево), щоб можна було швидше знайти потрібну кімнату.

#### **📌 Що перевіряють:**

*Експертне розуміння внутрішньої архітектури `HashSet` та її еволюції. Знання про оптимізацію зі зв'язного списку до дерева в Java 8 є ознакою глибоких знань кандидата.*

* * *

### **14 ❓ Питання:**

Чому складність операцій `HashSet` вказують як O(1) в середньому, але O(n) у найгіршому випадку? Продемонструйте приклад класу, який призведе до найгіршого сценарію. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Середній випадок (O(1)):** Передбачає, що хеш-функція добре розподіляє елементи по різних комірках (buckets) хеш-таблиці. Кількість колізій мінімальна, і пошук, додавання чи видалення зводяться до одного-двох кроків.
>
> * **Найгірший випадок (O(n)):** Виникає, коли всі або більшість елементів мають однаковий `hashCode()`. У цьому випадку всі вони потрапляють в одну комірку, і структура даних вироджується. `HashSet` змушений перебирати всі `n` елементів у цій комірці (чи то у вигляді зв'язного списку, чи дерева), щоб виконати операцію. Це нівелює всі переваги хешування.
>
> **Приклад класу з поганим `hashCode()`:**
> Щоб спровокувати найгірший випадок, достатньо створити клас, у якого метод `hashCode()` завжди повертає константу.

#### **📝 Приклад:**

```java
class BadHashObject {
    private final int value;

    public BadHashObject(int value) {
        this.value = value;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        BadHashObject that = (BadHashObject) o;
        return value == that.value;
    }

    @Override
    public int hashCode() {
        return 42; // Усі об'єкти матимуть однаковий хеш-код!
    }
}

// Демонстрація:
Set<BadHashObject> set = new HashSet<>();
// Додавання 10000 елементів
for (int i = 0; i < 10000; i++) {
    set.add(new BadHashObject(i));
}
// Тепер операція contains буде дуже повільною, бо всі 10000 елементів
// знаходяться в одній комірці.
// Пошук буде мати складність O(n).
long startTime = System.nanoTime();
set.contains(new BadHashObject(9999));
long endTime = System.nanoTime();
System.out.println("Час пошуку: " + (endTime - startTime) + " нс"); // Буде значно довшим, ніж у звичайному HashSet
```

#### **📌 Що перевіряють:**

*Глибоке розуміння теорії складності алгоритмів (Big O) та її практичного застосування. Кандидат повинен не просто знати визначення, а й уміти змоделювати ситуацію, що призводить до деградації продуктивності.*

* * *

### **15 ❓ Питання:**

Вам потрібно видалити дублікати зі списку `ArrayList`, зберігши при цьому початковий порядок елементів. Як ви це зробите найефективніше? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання-пастка. Перша інтуїтивна відповідь — використати `HashSet`, оскільки він автоматично видаляє дублікати. Однак `HashSet` не зберігає порядок елементів, що порушує другу частину умови.
>
> **Неправильне рішення:**
> `new ArrayList<>(new HashSet<>(listWithDuplicates));`
> Цей код видалить дублікати, але порядок елементів буде втрачено.
>
> **Правильне та найефективніше рішення:**
> Використати **`LinkedHashSet`**. Ця структура даних поєднує в собі найкраще з двох світів:
>
> 1. **Швидкість та унікальність `HashSet`:** Вона використовує хеш-таблицю для швидкого додавання та перевірки на унікальність (O(1)).
> 2. **Порядок `LinkedList`:** Вона підтримує додатковий двонаправлений зв'язний список, який фіксує порядок додавання елементів.
>
> Таким чином, для вирішення задачі достатньо виконати один рядок коду:

#### **📝 Приклад:**

```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedHashSet;
import java.util.List;

public class PreserveOrderExample {
    public static void main(String[] args) {
        List<String> listWithDuplicates = new ArrayList<>(
            Arrays.asList("A", "B", "C", "A", "D", "B")
        );
        System.out.println("Початковий список: " + listWithDuplicates);

        // Створюємо LinkedHashSet зі списку, щоб видалити дублікати і зберегти порядок
        LinkedHashSet<String> uniqueSet = new LinkedHashSet<>(listWithDuplicates);

        // Перетворюємо назад у список
        List<String> listWithoutDuplicates = new ArrayList<>(uniqueSet);

        System.out.println("Список без дублікатів зі збереженим порядком: " + listWithoutDuplicates);
        // Очікуваний результат: [A, B, C, D]
    }
}
```

#### **📝 Коментар:**

Це завдання перевіряє не стільки знання `HashSet`, скільки розуміння всього сімейства `Set` і вміння обрати правильний інструмент для конкретних вимог. Кандидат, який одразу пропонує `LinkedHashSet`, демонструє глибоке та практичне знання Java Collections Framework.

#### **📌 Що перевіряють:**

*Практичні навички вирішення проблем. Здатність кандидата аналізувати всі вимоги задачі (не тільки "видалити дублікати", а й "зберегти порядок") і обирати найбільш відповідну структуру даних.*

* * *
