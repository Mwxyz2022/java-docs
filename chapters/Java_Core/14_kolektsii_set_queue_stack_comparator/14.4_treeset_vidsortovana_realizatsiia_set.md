---
layout: default
title: "TreeSet - відсортована реалізація Set"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 4
---

# TreeSet - відсортована реалізація Set

* короткий опис: `TreeSet` – це реалізація інтерфейсу `Set`, що зберігає елементи у відсортованому порядку (природному або за допомогою `Comparator`). Базується на червоно-чорному дереві.

* поняття: `TreeSet`, Red-Black Tree, `Comparable`, `Comparator`, `O(log n)` (продуктивність).

* приклади: `numbers.add(10); numbers.add(5); System.out.println(numbers);` (виведе `[5, 10]`).

* ключові моменти: Автоматично сортує елементи. Не дозволяє `null` елементів. Потребує, щоб елементи були `Comparable` або щоб було надано `Comparator`.

* що важливо знати на співбесіді: Принцип роботи, продуктивність операцій (O(log n)), вимоги до елементів.

### **✅ Офіційне визначення:**

**`TreeSet`** — це реалізація інтерфейсу **`Set`**, яка зберігає елементи **відсортованими у зростаючому порядку** відповідно до природного порядку (через **`Comparable`**) або на основі наданого **`Comparator`**. Побудована на **Red-Black Tree**.

### **🧠 Простими словами:**

**`TreeSet`** — це як строгий охоронець на вечірці, який не тільки впускає унікальних гостей, а ще й ставить усіх **по зросту**\! Без **`null`**, без дублікатів, зате красиво впорядковано ✨

---

## **Основне**

📊 Основні характеристики **`TreeSet`**:

* **Відсортована множина** (ascending by default)
* Побудована на **червоний-чорному дереві**
* Не дозволяє **`null`**\-елементів
* Елементи повинні бути **`Comparable`** **або** надається **`Comparator`**
* Продуктивність: **`O(log n)`** на **`add`**, **`remove`**, **`contains`**

**📌 Основні методи:**

* **`add(E e)`** — додає елемент
* **`remove(Object o)`** — видаляє
* **`contains(Object o)`** — перевіряє наявність
* **`first(), last()`** — мінімальний/максимальний елемент
* **`ceiling(E e), floor(E e)`** — найближчі елементи
* **`headSet(), tailSet(), subSet()`** — підмножини

---

## **Пояснення на пальцях (💡)**

Уяви полиці в бібліотеці, де всі книги стоять **по алфавіту**. Ти не можеш покласти два однакові томи, і порожні обкладинки (**`null`**) не приймаються. Це і є **`TreeSet`**: унікально, відсортовано, без бардаку. А головне — бібліотекар (Comparator або Comparable) слідкує, щоб усе було "по поличках"

---

## **Корисні приклади (🧪)**

```java
import java.util.TreeSet;

public class Main {
    public static void main(String[] args) {
        TreeSet<Integer> numbers = new TreeSet<>();
        numbers.add(10);
        numbers.add(5);
        numbers.add(20);
        numbers.add(10); // дублікат

        System.out.println(numbers); // [5, 10, 20]
        System.out.println("Найменший: " + numbers.first()); // 5
        System.out.println("Найбільший: " + numbers.last());  // 20
    }
}
```

**🧪 Усі елементи автоматично відсортовані по зростанню.**

---

## **Порівняння (⚖️)**

| Параметр | HashSet | LinkedHashSet | TreeSet |
| :---: | :---: | :---: | :---: |
| **Унікальність** | ✅ | ✅ | ✅ |
| **Порядок** | ❌ | ✅ (вставки) | ✅ (сортування) |
| **Дозволяє null** | ✅ (1 елемент) | ✅ (1 елемент) | ❌ |
| **Структура** | HashMap | LinkedHashMap | TreeMap (RB Tree) |
| **Потрібен Comparable** | ❌ | ❌ | ✅ (або Comparator) |
| **Швидкодія** | **`O(1)`** | **`O(1)`** | **`O(log n)`** |
| **Пам’ять** | 🟢 Мала | 🟡 Середня | 🔴 Більше |

---

## **🧠 Мнемоніка для запам’ятовування:**

**T.R.E.E.**

* **T** — TreeMap всередині
* **R** — Requires Comparable / Comparator
* **E** — Elements sorted
* **E** — Excludes null

---

## **🟩 Підсумок:**

* **`TreeSet`** — це **відсортована множина** без дублікатів
* В основі — **Red-Black Tree**, сортує елементи автоматично
* **Не допускає null**
* Потрібен **`Comparable`** або **`Comparator`**
* Повільніше за **`HashSet`**, але забезпечує **сортування**
* Підходить для задач, де важливий **порядок** і **операції над діапазонами**

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — TreeSet**](https://docs.oracle.com/javase/8/docs/api/java/util/TreeSet.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `TreeSet` в Java і які його основні характеристики?

> #### **💬 Відповідь:**
>
> **`TreeSet`** — це реалізація інтерфейсу `NavigableSet` (який розширює `SortedSet`), що зберігає унікальні елементи у відсортованому порядку.
>
> Основні характеристики `TreeSet`:
>
> * **Унікальність елементів:** Як і будь-яка реалізація `Set`, `TreeSet` не дозволяє зберігати дублікати.
>
> * **Відсортований порядок:** Це його ключова особливість. Елементи автоматично сортуються під час додавання, або за їх **природним порядком** (якщо клас реалізує `Comparable`), або за допомогою **спеціального `Comparator`**, наданого при створенні `TreeSet`.
>
> * **Заснований на дереві:** Внутрішньо `TreeSet` побудований на збалансованому бінарному дереві пошуку, а саме на **червоно-чорному дереві (Red-Black Tree)**, що реалізовано через `TreeMap`.
>
> * **Не дозволяє `null`:** Спроба додати `null` елемент призведе до `NullPointerException`, оскільки `null` неможливо порівняти з іншими об'єктами.
>
> * **Продуктивність O(log n):** Основні операції (`add`, `remove`, `contains`) виконуються за логарифмічний час, що повільніше, ніж O(1) у `HashSet`, але є платою за постійну відсортованість.

#### **📝 Аналогія:**

Уявіть, що `TreeSet` — це книжкова полиця з енциклопедіями. Кожен том унікальний. Коли ви ставите новий том, ви не ставите його в кінець, а одразу знаходите йому правильне місце за алфавітом, щоб полиця завжди залишалася відсортованою.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `TreeSet`, його головних відмінностей від інших сетів (сортування) та вимог (немає `null`, елементи порівнювані).*

* * *

### **2 ❓ Питання:**

Як `TreeSet` працює під капотом? На якій структурі даних він базується?

> #### **💬 Відповідь:**
>
> `TreeSet` під капотом є "обгорткою" над **`TreeMap`**. Це фундаментальний аспект його роботи.
>
> * **Внутрішній `TreeMap`:** Коли ви створюєте `TreeSet`, насправді створюється екземпляр `TreeMap<E, Object>`.
>
> * **Елементи як ключі:** Елемент, який ви додаєте в `TreeSet` (наприклад, `set.add("Apple")`), стає **ключем** у цьому внутрішньому `TreeMap`.
>
> * **Значення-заглушка:** Значенням для кожного ключа виступає статична константа `PRESENT` типу `Object`. Воно однакове для всіх елементів і не несе жодного смислового навантаження.
>
> Оскільки `TreeMap` автоматично сортує свої ключі, `TreeSet` успадковує цю поведінку. `TreeMap` для цього використовує структуру **червоно-чорного дерева**, що забезпечує збалансованість і логарифмічну складність операцій.

#### **📝 Коментар:**

Запам'ятайте просту відповідність:

* `HashSet` -> `HashMap`
* `LinkedHashSet` -> `LinkedHashMap`
* `TreeSet` -> `TreeMap`
`TreeSet` просто використовує функціональність `TreeMap` для роботи з ключами, ігноруючи значення.

#### **📌 Що перевіряють:**

*Розуміння, що колекції Java часто побудовані на основі інших колекцій. Це демонструє глибші знання Java Collections Framework.*

* * *

### **3 ❓ Питання:**

Яким чином `TreeSet` сортує елементи? Які є два способи визначення порядку?

> #### **💬 Відповідь:**
>
> `TreeSet` вимагає, щоб для його елементів був визначений порядок порівняння. Це можна зробити двома способами:
>
> 1. **Природний порядок (`Comparable`):** Клас елементів, які ви додаєте, повинен реалізовувати інтерфейс `java.lang.Comparable<T>`. Цей інтерфейс має один метод `compareTo()`, який визначає природний порядок сортування для об'єктів цього класу (наприклад, числа сортуються за зростанням, рядки — за алфавітом). Це найпоширеніший спосіб.
>
> 2. **Спеціальний порядок (`Comparator`):** Якщо клас не реалізує `Comparable`, або вам потрібен інший порядок сортування (наприклад, за спаданням або за іншим полем об'єкта), ви можете передати реалізацію інтерфейсу `java.util.Comparator<T>` в конструктор `TreeSet`. Метод `compare()` цього компаратора буде використовуватися для порівняння елементів.
>
> Якщо не виконано жодної з цих умов (немає `Comparable` і не надано `Comparator`), спроба додати перший елемент призведе до `ClassCastException`.

#### **📝 Приклад:**

```java
// Спосіб 1: Comparable (String реалізує його за замовчуванням)
Set<String> set1 = new TreeSet<>();
set1.add("C");
set1.add("A"); // Сортується автоматично

// Спосіб 2: Comparator (сортування рядків за довжиною)
Comparator<String> lengthComparator = (s1, s2) -> Integer.compare(s1.length(), s2.length());
Set<String> set2 = new TreeSet<>(lengthComparator);
set2.add("Java");   // довжина 4
set2.add("Python"); // довжина 6
set2.add("Go");     // довжина 2
System.out.println(set2); // [Go, Java, Python]
```

#### **📌 Що перевіряють:**

*Чітке розуміння двох механізмів сортування в Java та їх застосування в контексті `TreeSet`. Це фундаментальна концепція для роботи з сортованими колекціями.*

* * *

### **4 ❓ Питання:**

Чому `TreeSet` не дозволяє додавати `null` елементи?

> #### **💬 Відповідь:**
>
> `TreeSet` не дозволяє `null`, тому що його основна функція — сортування елементів. Для того, щоб розмістити елемент у дереві, `TreeSet` повинен порівняти його з іншими елементами.
>
> Спроба викликати метод порівняння на `null` об'єкті призведе до **`NullPointerException`**.
>
> Наприклад, при додаванні першого ненульового елемента `X` все гаразд. Але при спробі додати `null` `TreeSet` спробує виконати щось на зразок `null.compareTo(X)` (якщо використовується `Comparable`) або `comparator.compare(null, X)` (якщо `Comparator`). Перший варіант одразу ж кине `NullPointerException`. Другий варіант також зазвичай не призначений для обробки `null`, якщо компаратор не написаний спеціально для цього (що є рідкісним і нетиповим використанням).
>
> Щоб уникнути такої неоднозначності та потенційних помилок, розробники Java вирішили повністю заборонити `null` в `TreeSet` (починаючи з JDK 7).

#### **📝 Коментар:**

Це ключова відмінність від `HashSet` та `LinkedHashSet`, які дозволяють один `null` елемент, оскільки їм не потрібно ні з чим його порівнювати для визначення порядку.

#### **📌 Що перевіряють:**

*Розуміння причинно-наслідкового зв'язку: `TreeSet` сортує -> сортування вимагає порівняння -> `null` неможливо порівняти -> `null` заборонено.*

* * *

### **5 ❓ Питання:**

Яка часова складність (`Big O`) основних операцій (`add`, `remove`, `contains`) в `TreeSet` і чому вона така?

> #### **💬 Відповідь:**
>
> Часова складність для основних операцій у `TreeSet` гарантовано становить **O(log n)**.
>
> * `add(E element)`: O(log n)
> * `remove(Object o)`: O(log n)
> * `contains(Object o)`: O(log n)
>
> **Чому саме O(log n)?**
> Це пов'язано з тим, що `TreeSet` базується на **червоно-чорному дереві**. Це самозбалансоване бінарне дерево пошуку.
>
> 1. **Пошук:** Щоб знайти місце для нового елемента, перевірити його наявність чи видалити, потрібно спуститися по дереву від кореня до потрібного вузла.
> 2. **Збалансованість:** Оскільки дерево завжди збалансоване, його висота є логарифмічною відносно кількості вузлів `n` (висота ≈ log₂(n)).
> 3. **Кількість кроків:** Максимальна кількість кроків для будь-якої з цих операцій пропорційна висоті дерева. Тому складність є логарифмічною.
>
> Це трохи повільніше, ніж O(1) у `HashSet`, але значно швидше, ніж O(n) у несортованому масиві або списку.

#### **📝 Коментар:**

Це класичний компроміс (trade-off) в інформатиці: ви платите невелику ціну в продуктивності (O(log n) замість O(1)) за отримання потужної додаткової функціональності (автоматичне сортування).

#### **📌 Що перевіряють:**

*Знання часової складності та здатність пояснити її, посилаючись на базову структуру даних (збалансоване бінарне дерево).*

* * *

### **6 ❓ Питання:**

Чим відрізняються методи `first()`, `last()`, `floor()`, `ceiling()`?

> #### **💬 Відповідь:**
>
> Ці методи є частиною інтерфейсу `NavigableSet`, який реалізує `TreeSet`, і дозволяють ефективно працювати з відсортованою структурою.
>
> * **`first()`**: Повертає **найменший (перший)** елемент у сеті. Кидає `NoSuchElementException`, якщо сет порожній.
>
> * **`last()`**: Повертає **найбільший (останній)** елемент у сеті. Кидає `NoSuchElementException`, якщо сет порожній.
>
> * **`floor(E e)`**: Повертає **найбільший елемент, який є меншим або рівним (`<=`)** заданому елементу `e`. Повертає `null`, якщо такого елемента немає.
>
> * **`ceiling(E e)`**: Повертає **найменший елемент, який є більшим або рівним (`>=`)** заданому елементу `e`. Повертає `null`, якщо такого елемента немає.

#### **📝 Приклад:**

```java
TreeSet<Integer> numbers = new TreeSet<>(Arrays.asList(10, 20, 30, 40, 50));

System.out.println("first(): " + numbers.first());     // 10
System.out.println("last(): " + numbers.last());       // 50
System.out.println("floor(35): " + numbers.floor(35));   // 30 (найбільший елемент, що <= 35)
System.out.println("ceiling(35): " + numbers.ceiling(35)); // 40 (найменший елемент, що >= 35)
System.out.println("floor(30): " + numbers.floor(30));   // 30 (елемент рівний 30)
System.out.println("ceiling(60): " + numbers.ceiling(60)); // null
```

#### **📌 Що перевіряють:**

*Знання розширеного API `TreeSet` (`NavigableSet`). Ці методи дуже корисні в задачах, що вимагають пошуку діапазонів або найближчих значень, і їх незнання може вказувати на поверхневі знання колекцій.*

* * *

### **7 ❓ Питання:**

Що станеться, якщо спробувати додати в `TreeSet` об'єкт класу, який не реалізує `Comparable`, і не надати `Comparator`?

> #### **💬 Відповідь:**
>
> Програма скомпілюється успішно, оскільки компілятор не може знати, якого типу об'єкти будуть додаватися в `TreeSet` під час виконання (через використання узагальнень).
>
> Однак, під час виконання, при спробі додати **перший же елемент** у такий `TreeSet`, буде викинуто виняток **`ClassCastException`**.
>
> Причина в тому, що `TreeSet` при додаванні першого елемента намагається з'ясувати, як його порівнювати. Якщо `Comparator` не було надано, він намагається привести (cast) доданий об'єкт до типу `Comparable`, щоб викликати його метод `compareTo()`. Якщо об'єкт не реалізує цей інтерфейс, приведення типів не вдається, і JVM кидає `ClassCastException`.

#### **📝 Приклад:**

```java
class UncomparableObject {}

public class Test {
    public static void main(String[] args) {
        Set<UncomparableObject> set = new TreeSet<>();
        // Наступний рядок викине ClassCastException під час виконання
        set.add(new UncomparableObject());
    }
}
```

#### **📌 Що перевіряють:**

*Розуміння вимог `TreeSet` до елементів і того, як ці вимоги перевіряються під час виконання програми. Це також перевіряє знання поширених винятків у Java.*

* * *

### **8 ❓ Питання:**

Поясніть різницю між `Comparable` та `Comparator` і коли варто використовувати кожен з них. ⭐️

> #### **💬 Відповідь:**
>
> І `Comparable`, і `Comparator` — це інтерфейси для визначення логіки порівняння об'єктів, але вони використовуються в різних контекстах.
>
> **`Comparable`**:
>
> * **Що це:** Інтерфейс, який реалізується **самим класом**, об'єкти якого потрібно порівнювати.
> * **Метод:** `int compareTo(T o)`.
> * **Призначення:** Визначає **єдиний, природний порядок** сортування для класу. Наприклад, для класу `Person` природним може бути сортування за прізвищем.
> * **Коли використовувати:** Коли існує очевидний, основний спосіб сортування, який буде використовуватися в більшості випадків.
>
> **`Comparator`**:
>
> * **Що це:** Інтерфейс, реалізація якого створюється **окремо** від класу, об'єкти якого потрібно порівнювати.
> * **Метод:** `int compare(T o1, T o2)`.
> * **Призначення:** Визначає **спеціальний або альтернативний порядок** сортування. Ви можете мати кілька різних компараторів для одного класу (наприклад, сортування `Person` за віком, за зростом, за датою народження).
> * **Коли використовувати:**
>   * Коли потрібно кілька способів сортування.
>   * Коли ви не можете змінити вихідний код класу, щоб додати `Comparable` (наприклад, це клас із зовнішньої бібліотеки).
>   * Коли ви хочете визначити порядок для класів, які ви не контролюєте.

#### **📝 Коментар:**

* `Comparable` — це "я знаю, як порівнювати себе з іншими".
* `Comparator` — це "я знаю, як порівнювати двох інших".

#### **📌 Що перевіряють:**

*Розуміння різниці між внутрішньою та зовнішньою логікою порівняння. Це питання перевіряє гнучкість мислення кандидата та його здатність проектувати гнучкі системи.*

* * *

### **9 ❓ Питання:**

Що таке червоно-чорне дерево і чому саме його використовують у `TreeSet` (та `TreeMap`)? ⭐️

> #### **💬 Відповідь:**
>
> **Червоно-чорне дерево (Red-Black Tree)** — це тип самозбалансованого бінарного дерева пошуку. Кожен вузол у такому дереві має додатковий біт для кольору (червоний або чорний) і дотримується певних правил, які гарантують, що будь-який шлях від кореня до листка не буде більш ніж удвічі довшим за будь-який інший.
>
> **Чому використовується саме воно:**
>
> 1. **Гарантована продуктивність O(log n):** Найголовніша причина. Звичайне бінарне дерево пошуку може виродитися в зв'язний список (якщо додавати елементи послідовно), що дасть продуктивність O(n). Червоно-чорне дерево через операції **перефарбування** та **обертання** автоматично підтримує баланс, гарантуючи, що його висота завжди буде логарифмічною.
>
> 2. **Швидкі операції вставки та видалення:** Хоча існують і інші збалансовані дерева (наприклад, АВЛ-дерева), червоно-чорні дерева вимагають менше обертань під час вставки/видалення в середньому. Вони є хорошим компромісом між строгістю балансування та швидкістю модифікації.
>
> **Простими словами**, це розумна структура, яка не дозволяє дереву ставати "перекошеним" в один бік, забезпечуючи швидкий пошук, додавання та видалення елементів за будь-яких умов.

#### **📌 Що перевіряють:**

*Глибокі знання алгоритмів та структур даних, що лежать в основі стандартних бібліотек Java. Кандидат повинен розуміти, чому були обрані саме ці реалізації, а не інші.*

* * *

### **10 ❓ Питання:**

Як створити `TreeSet`, який буде сортувати елементи у зворотному (descending) порядку? ⭐️

> #### **💬 Відповідь:**
>
> Для цього потрібно надати `TreeSet` спеціальний `Comparator`, який буде інвертувати природний порядок сортування. Існує два простих способи це зробити:
>
> 1. **Використовуючи `Comparator.reverseOrder()`:** Це найпростіший і найбільш ідіоматичний спосіб, доступний з Java 8. Цей статичний метод повертає компаратор, що реалізує зворотний до природного порядок.
>
> 2. **Написавши власний `Comparator` з лямбда-виразом:** Можна явно визначити логіку порівняння, яка буде інвертувати результат `compareTo`.
>
#### **📝 Приклад:**

```java
import java.util.*;

public class ReverseOrderExample {
    public static void main(String[] args) {
        // Спосіб 1: Comparator.reverseOrder()
        Set<Integer> reverseSet1 = new TreeSet<>(Comparator.reverseOrder());
        reverseSet1.add(10);
        reverseSet1.add(5);
        reverseSet1.add(20);
        System.out.println("За допомогою reverseOrder(): " + reverseSet1); // [20, 10, 5]

        // Спосіб 2: Лямбда-вираз
        Set<String> reverseSet2 = new TreeSet<>((s1, s2) -> s2.compareTo(s1));
        reverseSet2.add("Apple");
        reverseSet2.add("Zebra");
        reverseSet2.add("Monkey");
        System.out.println("За допомогою лямбди: " + reverseSet2); // [Zebra, Monkey, Apple]
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички роботи з `Comparator` та знання сучасного API Java (зокрема, статичних методів в інтерфейсах, як `Comparator.reverseOrder()`).*

* * *

### **11 ❓ Питання:**

Що станеться, якщо логіка `compareTo()` або `compare()` несумісна з `equals()`? Наприклад, `a.compareTo(b) == 0`, але `a.equals(b)` повертає `false`. ⭐️

> #### **💬 Відповідь:**
>
> Це дуже важливе питання, що розкриває тонкощі роботи `TreeSet`. Якщо `compareTo()` (або `compare()`) несумісний з `equals()`, `TreeSet` буде дотримуватися логіки **`compareTo()`**, що може порушити загальний контракт інтерфейсу `Set`.
>
> **Контракт `Set` говорить:** множина не може містити двох елементів `e1` та `e2` таких, що `e1.equals(e2)` є `true`.
>
> **Поведінка `TreeSet`:** для перевірки на унікальність `TreeSet` використовує `compareTo()` або `compare()`. Якщо `a.compareTo(b) == 0`, `TreeSet` вважає ці елементи **однаковими** і не дозволить додати другий.
>
> **Наслідки:**
>
> * Ви можете додати до `TreeSet` два об'єкти, які **не є рівними** за `equals()`, але `compareTo()` для них повертає 0. У такому випадку `TreeSet` збереже тільки перший доданий об'єкт, "втративши" другий.
> * І навпаки (що більш небезпечно), якщо для двох об'єктів, рівних за `equals()`, `compareTo()` поверне ненульове значення, `TreeSet` дозволить зберегти **обидва об'єкти**, що прямо **порушує контракт `Set`**.
>
> **Рекомендація:** Завжди реалізуйте `compareTo()` так, щоб він був узгоджений з `equals()`: `(x.compareTo(y) == 0) == (x.equals(y))`.

#### **📌 Що перевіряють:**

*Глибоке розуміння контрактів Java Collections Framework. Кандидат повинен усвідомлювати, що поведінка деяких реалізацій може відхилятися від загального контракту інтерфейсу, якщо не дотримуватися неявних правил.*

* * *

### **12 ❓ Питання:**

Чи можна змінити об'єкт після його додавання в `TreeSet`? Які можуть бути наслідки? ⭐️

> #### **💬 Відповідь:**
>
> Змінювати об'єкт, який вже знаходиться в `TreeSet`, є **вкрай небезпечною практикою**, якщо зміна торкається полів, що беруть участь у порівнянні (`compareTo` або `compare`).
>
> Це може призвести до **руйнування внутрішньої структури** червоно-чорного дерева.
>
> **Наслідки:**
>
> 1. **Порушення порядку сортування:** Після зміни поле, за яким відбувалося сортування, об'єкт може опинитися не на своєму місці. `TreeSet` не буде автоматично пересортовувати його.
> 2. **Втрата об'єкта:** Операції `contains()`, `remove()` можуть перестати працювати для цього об'єкта. `TreeSet` буде шукати його, починаючи з кореня дерева, і слідувати шляхом, який тепер не відповідає новому значенню об'єкта. Він просто не знайде його у старому місці.
> 3. **Можливість додавання дублікатів:** Може з'явитися можливість додати інший об'єкт, який тепер вважається унікальним, хоча раніше був би дублікатом зміненого.
>
> **Правило:** Елементи, що додаються до `TreeSet`, повинні бути **ефективно незмінними (effectively immutable)**, принаймні щодо полів, які використовуються для сортування.

#### **📌 Що перевіряють:**

*Усвідомлення ризиків, пов'язаних із мутацією об'єктів усередині колекцій, особливо в сортованих або хешованих структурах. Це питання показує зрілість розробника.*

* * *

### **13 ❓ Питання:**

Уявіть, що ви працюєте з великим `TreeSet`. Які переваги надають методи `headSet()`, `tailSet()` та `subSet()` порівняно з ручним перебором і фільтрацією? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Методи `headSet()`, `tailSet()` та `subSet()` надають величезні переваги в продуктивності та зручності, оскільки вони не створюють нових копій даних.
>
> **Вони повертають "вид" (view) оригінального `TreeSet`**. Це означає, що вони повертають об'єкт, який внутрішньо посилається на той самий `TreeSet`, але обмежує видиму область.
>
> **Переваги:**
>
> 1. **Продуктивність O(log n) для створення:** Створення такого "виду" є дуже швидкою операцією. `TreeSet` просто створює новий об'єкт `NavigableSet` з посиланнями на межі (від і до). Немає потреби ітерувати по всіх `n` елементах і копіювати їх. Ручна фільтрація мала б складність O(n).
> 2. **Ефективне використання пам'яті:** Оскільки дані не копіюються, пам'ять не витрачається на створення нової колекції. Це критично для великих сетів.
> 3. **"Живий" вид:** Будь-які зміни в оригінальному `TreeSet` будуть відображені в "виді". І навпаки, зміни, зроблені через "вид" (наприклад, видалення елемента), будуть відображені в оригінальному `TreeSet`.
>
> **Приклад:**
>
> * `headSet(E toElement)`: "Вид" всіх елементів, що `< toElement`.
> * `tailSet(E fromElement)`: "Вид" всіх елементів, що `>= fromElement`.
> * `subSet(E fromElement, E toElement)`: "Вид" всіх елементів в діапазоні `[fromElement, toElement)`.

#### **📌 Що перевіряють:**

*Знання концепції "видів" (views) у Java Collections Framework. Це демонструє розуміння ефективних способів роботи з даними та відрізняє кандидата, який просто знає методи, від того, хто розуміє, як вони реалізовані.*

* * *

### **14 ❓ Питання:**

`TreeSet` для своєї роботи використовує `compareTo` (або `compare`), а `HashSet` — `hashCode` та `equals`. Чи використовує `TreeSet` методи `hashCode` або `equals` взагалі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом. Для своїх **ключових операцій** — сортування, додавання, видалення, пошук — `TreeSet` використовує **тільки `compareTo()` (або `compare()`)**. Йому не потрібні `hashCode()` та `equals()` для визначення порядку та унікальності елементів.
>
> Однак, `TreeSet` успадковує деякі методи від своїх батьківських класів, таких як `AbstractSet` та `AbstractCollection`, які *можуть* опосередковано використовувати `equals()` або `hashCode()`.
>
> **Приклад:**
> Метод `removeAll(Collection<?> c)`. Типова реалізація цього методу в `AbstractSet` буде ітерувати по переданій колекції `c` і для кожного її елемента викликати `this.remove(element)`, що для `TreeSet` використає `compareTo`. Але, якщо реалізація буде оптимізована (наприклад, перевірятиме `c.contains(thisElement)`), то вона може викликати `hashCode/equals` на елементах *іншої* колекції.
>
> **Ключовий висновок:** Для своєї внутрішньої логіки та гарантій `TreeSet` покладається виключно на механізми порівняння. Якщо ви використовуєте `TreeSet` за його прямим призначенням, `hashCode()` та `equals()` не відіграють ролі.

#### **📝 Коментар:**

Найкраща відповідь буде такою: "У 99% випадків — ні. `TreeSet` керується виключно порівнянням. Але варто пам'ятати, що він є частиною великого фреймворку, і деякі успадковані, менш поширені методи можуть мати реалізації, що спираються на `equals()`."

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як різні частини Java Collections Framework взаємодіють. Кандидат повинен чітко розмежовувати власну логіку класу та успадковану.*

* * *

### **15 ❓ Питання:**

Як реалізувати `TreeSet` для класу, який має кілька полів для сортування (наприклад, сортувати користувачів спочатку за прізвищем, а якщо прізвища однакові — то за іменем)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для реалізації такої "багаторівневої" логіки сортування потрібно створити відповідний `Comparable` або `Comparator`, який буде послідовно порівнювати поля.
>
> **Алгоритм порівняння:**
>
> 1. Порівняти об'єкти за першим, головним полем (наприклад, прізвищем).
> 2. Якщо результат порівняння **не дорівнює 0** (тобто прізвища різні), то повернути цей результат.
> 3. Якщо результат **дорівнює 0** (прізвища однакові), то перейти до порівняння за другим полем (наприклад, іменем) і повернути його результат.
> 4. Цей процес можна продовжувати для будь-якої кількості полів.
>
> З Java 8 це можна зробити дуже елегантно за допомогою статичних методів `Comparator.comparing()` та `thenComparing()`.

#### **📝 Приклад:**

```java
import java.util.*;

class User {
    private String lastName;
    private String firstName;
    // конструктор, геттери...
    
    @Override
    public String toString() { return lastName + " " + firstName; }
}

public class MultiSortExample {
    public static void main(String[] args) {
        // Сучасний підхід з Java 8
        Comparator<User> userComparator = Comparator.comparing(u -> u.lastName)
                                                     .thenComparing(u -> u.firstName);
        
        Set<User> users = new TreeSet<>(userComparator);
        users.add(new User("Smith", "John"));
        users.add(new User("Doe", "Jane"));
        users.add(new User("Smith", "Alice"));
        
        // Виведе: [Doe Jane, Smith Alice, Smith John]
        System.out.println(users); 
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички вирішення реальних завдань з сортування. Кандидат повинен не тільки знати про існування `Comparator`, а й уміти реалізувати складну логіку порівняння, в ідеалі — з використанням сучасних можливостей мови.*

* * *
