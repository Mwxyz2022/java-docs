---
layout: default
title: "Інтерфейс Set та його особливості"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 1
---

# Інтерфейс Set та його особливості

* короткий опис: `Set` – це колекція в Java, яка не дозволяє зберігати дублікати елементів. Він належить до Java Collections Framework і базується на логіці математичних множин.

* поняття: `Set`, унікальність, відсутність індексів.

* приклади: `HashSet` (без порядку), `LinkedHashSet` (з порядком вставки), `TreeSet` (відсортований). `set.add("Alice"); set.add("Alice");` (додасться лише один раз).

* ключові моменти: Гарантує унікальність елементів (за допомогою `equals()` та `hashCode()`), не підтримує доступ за індексом.

* що важливо знати на співбесіді: Про унікальність елементів та відсутність індексів, відмінності між `Set` та `List`.

### **✅ Офіційне визначення:**

**`Set`** — це колекція в Java, яка не дозволяє зберігати дублікати елементів. Інтерфейс **`Set`** належить до пакету **`java.util`** і є частиною **Java Collections Framework**.

### **🧠 Простими словами:**

**`Set`** — це як список гостей на приватну вечірку: якщо вже є Петро, другий Петро не пройде. Порядок гостей — неважливий, головне — **жодного дубля**.

---

## **📊 Основні реалізації інтерфейсу **`Set`**:**

|         Тип         |             Клас              |                Особливості                 |
|:-------------------:|:-----------------------------:|:------------------------------------------:|
|    **`HashSet`**    |    **`java.util.HashSet`**    |    Найшвидший, але не зберігає порядок     |
| **`LinkedHashSet`** | **`java.util.LinkedHashSet`** |         Зберігає порядок додавання         |
|    **`TreeSet`**    |    **`java.util.TreeSet`**    | Зберігає елементи у відсортованому порядку |

---

## **Пояснення на пальцях (💡)**

Уяви, ти колекціонуєш унікальні бейджики на конференції. Якщо дублікат — ти його викидаєш. Ось так працює **`Set`**. Тобі **не важливо**, хто перший, хто останній — головне, щоб **не повторювались**.

---

## **Корисні приклади (🧪)**

```java
Set<String> names = new HashSet<>();
names.add("Alice");
names.add("Bob");
names.add("Alice"); // не додасться вдруге

System.out.println(names); // [Alice, Bob]
```

```java
Set<Integer> numbers = new TreeSet<>();
numbers.add(5);
numbers.add(1);
numbers.add(3);

System.out.println(numbers); // [1, 3, 5] -- автоматично відсортовано
```

---

## **Порівняння (⚖️)**

|       Параметр        |            Set             |        List         |
|:---------------------:|:--------------------------:|:-------------------:|
|     **Дублікати**     |       ❌ Не дозволяє        |     ✅ Дозволяє      |
|    **Індексація**     |          ❌ Немає           |  ✅ Є (get(index))   |
| **Порядок елементів** | 🔄 Залежить від реалізації |   ✅ Зберігається    |
|  **Продуктивність**   |      ✅ Швидкий пошук       | ❌ Повільніший пошук |
| **Основні сценарії**  |     Унікальні значення     | Порядкові колекції  |

---

## **🧠 Мнемоніка для запам’ятовування:**

**S** — Single (без дублікатів)

**E** — Elements (унікальні обʼєкти)

**T** — Trash duplicates (дублікатам — ні\!)

---

## **🟩 Підсумок:**

* **`Set`** — це колекція, яка гарантує **унікальність елементів**
* Не має індексів, не підтримує дублікати
* Реалізації: **`HashSet`** (швидкий), **`LinkedHashSet`** (з порядком), **`TreeSet`** (з сортуванням)
* Краще використовувати, коли **не важливий порядок**, а важлива **унікальність**
* Підходить для операцій множин: об’єднання, перетин, різниця

---

### **🔗 Додаткові джерела:**

* [**Java Set Interface – Oracle Docs**](https://docs.oracle.com/javase/8/docs/api/java/util/Set.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс `Set` і яка його головна відмінність від `List`?

> #### **💬 Відповідь:**
>
> **`java.util.Set`** — це один з основних інтерфейсів у Java Collections Framework, який розширює `Collection` і представляє **множину унікальних елементів**.
>
> **Головна відмінність від `List` полягає у двох аспектах:**
>
> 1. **Унікальність:** `Set` **не дозволяє** зберігати дублікати. Спроба додати елемент, який вже існує в множині (згідно з `equals()`), буде проігнорована, і метод `add()` поверне `false`.
>
> 2. **Доступ за індексом:** `Set` **не надає** доступу до елементів за індексом. У нього немає методу `get(int index)`, оскільки в більшості його реалізацій елементи не мають чітко визначеної позиції.
>
> **Коротко:** `List` — це впорядкована колекція, що може містити дублікати, а `Set` — це (зазвичай) невпорядкована колекція унікальних елементів.

#### **📝 Аналогія:**

* `List` — це як список покупок. Ви можете записати "молоко" кілька разів, і кожен запис має свій порядок.
* `Set` — це як список гостей на вечірку. Ви не** *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс `Set` і яка можете запросити одну й ту саму людину двічі.

#### **📌 Що перевіряють:**

*Чітке розуміння двох ключових відмінностей: унікальність елементів та відсутність індексації.*

* * *

### **2 ❓ Питання:**

Які три основні реалізації інтерфейсу `Set` ви знаєте і чим вони відрізняються?

> #### **💬 Відповідь:**
>
> Трьома основними реалізаціями є `HashSet`, `LinkedHashSet` та `TreeSet`. Вони відрізняються **порядком зберігання елементів** та **продуктивністю**.
>
> 1. **`HashSet`:**
>     * **Структура:** На основі **хеш-таблиці (`HashMap`)**.
>     * **По його головна відмінність від `List`?

> #### **💬 Відповідь:**
>
> **`java.util.Set`** — це один з основних інтерфейсів у Java Collections Framework, який розширює `Collection` і представляє **множину унікальних елементів**.
>
> **Головна відмінність від `List`:**
>
> * **Унікальність:** `Set` **забороняє** дублювання елементів. Спроба додати елемент, який вже існує в `Set` (згідно з `equals()`), буде проігнорована. `List`, навпаки, дозволяє будь-яку кількість дублікатів.
>
> * **Доступ:** `Set` **не надає** доступу до елементів за індексом. У нього немає методу `get(int index)`. Доступ до елементів можливий тільки через ірядок:** **Не гарантує** жодного порядку елементів. Порядок може змінюватися з часом.
>   * **Продуктивність:** **Найшвидший**. Операції `add`, `remove`, `contains` виконуються за **O(1)** в середньому.
>   * **Дозволяє `null`**: Так, один `null` елемент.
>
> 2. **`LinkedHashSet`:**
>     * **Структура:** Комбінація **хеш-таблиці та двозв'язного списку**.
>     * **Порядок:** Зберігає елементи **в порядку їх додавання**.
>     * **Продуктивність:** Майже така ж швидка, як `HashSet` (O(1)), але з трохи більшими накладними витратами на підтримку списку.
>
> 3. **`TreeSet`:**
>     * **Структура:** На основі **червоно-чорного дерева**.
>     * **Порядок:** Зберігає елементи **відсортованими** (природне сортування або за допомогою `Comparator`).
>     * **Продуктивність:** Повільніша. Операції `add`, `remove`, терацію. `List` надає повний контроль над позицією елементів.
>
> * **Порядок:** Загальний контракт `Set` **не вимагає** збереження порядку елементів (хоча деякі реалізації, як `LinkedHashSet`, це роблять). `List` завжди гарантує впорядкованість.
>
> **Коротко:** `List` — впорядкована колекція з дублікатами. `Set` — (зазвичай) невпорядкована колекція без дублікатів.

#### **📝 Аналогія:**</h4>

`List` — це список покупок, де може бути два "молока". `Set` — це колекція унікальних гостей на вечірці, "Петро" не може бути в списку двічі.

#### **📌 Що перевіряють:**

*Чітке розуміння трьох основних відмінностей: унікальність, індексація, порядок.*

* * *

### **2 ❓ Питання:**

Які основні реалізації інтерфейсу `Set` ви знаєте і чим вони відрізняються?

> #### **💬 Відповідь:**
>
> Існує три основні реалізації `Set`:
>
> 1. **`HashSet`:**
>     * **Структура:** Базується на **хеш-таблиці** (`HashMap`).
>     * **Порядок:** **Не гарантує** жодного порядку елементів. Порядок може змінюватися з часом.
>     * **Продуктивність:** **Найшвидший** з трьох. Операції `add`, `remove`, `contains` виконуються за **O(1)** (константний час) в середньому.
>     * **Дозволяє `null`:** Можна додати один елемент `null`.
>
> 2. **`LinkedHashSet`:**
>     * **Структура:** Наслідує `HashSet` і додає **двозв'язний список** для відстеження порядку.
>     *`contains` виконуються за **O(log n)**.
>     * **Не дозволяє `null`**.

#### **📌 Що перевіряють:**

*Знання трьох основних реалізацій та вміння вибрати потрібну залежно від вимог до порядку та продуктивності.*

* * *

### **3 ❓ Питання:**

Як `HashSet` забезпечує унікальність елементів?

> #### **💬 Відповідь:**
>
> `HashSet` використовує для цього комбінацію методів **`hashCode()`** та **`equals()`**.
>
> **"Під капотом" `HashSet` — це просто обгортка над `HashMap`**. Елементи, які ви додаєте в `Set`, стають **ключами** у внутрішній `HashMap`, а як значення використовується константа-заглушка (`private static final Object PRESENT`).
>
> **Процес `add(element)`:**
>
> 1. Викликається `put(element, PRESENT)` для внутрішньої мапи.
> 2. `HashMap` обчислює `hashCode()` для `element`, щоб знайти потрібний "кошик".
> 3. Далі він перевіряє всі елементи в цьому кошику, викликаючи `equals()`, щоб знайти, чи існує вже такий самий елемент.
> 4. Якщо такий елемент **вже існує**, новий не додається, і метод `add()` повертає `false`.
> 5. Якщо **не існує**, новий елемент додається, і метод `add()` повертає `true`.
>
> Таким чином, для коректної роботи `HashSet` з вашими власними об'єктами **обов'язково** потрібно правильно перевизначити і `hashCode()`, і `equals()`.

#### **📌 Що перевіряють:**

*Розуміння того, що `Set` покладається на контракт `hashCode/equals`, та знання про внутрішню реалізацію `HashSet` через `HashMap`.*

* * *

### **4 ❓ Питання:**

   **Порядок:** Гарантує збереження **порядку вставки** елементів.
>     *   **Продуктивність:** Майже така ж швидка, як `HashSet` (O(1)), але з невеликими накладними витратами на підтримку списку.
>
> 3.  **`TreeSet`:**
> ***Структура:** Базується на **червоно-чорному дереві** (`TreeMap`).
>
> * **Порядок:** Зберігає елементи **відсортованими** (природне сортування або за допомогою `Comparator`).
> ***Продуктивність:** Повільніший. Операції `add`, `remove`, `contains` виконуються за **O(log n)**.
> * **Не дозволяє `null`:** Кине `NullPointerException` (бо `null` не можна порівнювати).

#### **📌 Що перевіряють:**

*Знання трьох основних реалізацій та вміння вибрати потрібну залежно від вимог до порядку та продуктивності.*

* * *

### **3 ❓ Питання:**

Як `HashSet` забезпечує унікальність елементів?

> #### **💬 Відповідь:**
>
> `HashSet` використовує **хеш-таблицю** і покладається на **контракт методів `hashCode()` та `equals()`** для забезпечення унікальності.
>
> **Процес додавання нового елемента (`add(element)`):**
>
> 1. `HashSet` обчислює `hashCode()` для нового елемента, щоб визначити "кошик" (bucket), де цей елемент має зберігатися.
> 2. Потім він перевіряє всі елементи, що вже є в цьому кошику.
> 3. Для кожного існуючого елемента в кошику, `HashSet` порівнює хеш-коди, і якщо вони однакові, викликає метод `equals()` для точного порівняння.
> 4. Якщо **знаходиться елемент, для якого `equals()` повертає `true`**, то новий елемент вважається **дублікатом** і не додається. Метод `add()` повертає `false`.
> 5. Якщо після перевірки всього кошика жодного рівного елемента не знайдено, новий елемент додається в цей кошик. Метод `add()` повертає `true`.
>
> Таким чином, унікальність повністю залежить від коректної реалізації `hashCode()` та `equals()` у класів об'єктів, що зберігаються.

#### **📌 Що перевіряють:**

*Розуміння того, що `HashSet` "під капотом" використовує `HashMap`, та нерозривного зв'язку між `Set` та контрактом `hashCode`/`equals`.*

* * *

### **4 ❓ Питання:**

Що буде, якщо додати в `HashSet` об'єкт, у якого `equals()` та `hashCode()` не перевизначені?

> #### **💬 Відповідь:**
>
> Якщо ці методи не перевизначені, буде використовуватися **реалізація з класу `Object`**.
>
> * `equals()` буде працювати як **Що поверне `set.add(element)` і як це можна використати?

> #### **💬 Відповідь:**
>
> Метод `boolean add(E element)` з інтерфейсу `Set` (і `Collection`) повертає:
>
> * **`true`** — якщо елемент **був успішно доданий** (тобто його раніше не було в `Set`).
> * **`false`** — якщо елемент **не був доданий**, оскільки такий самий елемент (згідно з `equals()`) вже присутній у `Set`.
>
> **Як це можна використати:**
>
> Це дозволяє легко перевірити, чи є елемент унікальним, і виконати певну дію тільки в тому випадку, якщо він додається вперше.
>
> ```java
> Set<String> uniqueUsers = new HashSet<>();
> String newUser = "john_doe";
>
> if (uniqueUsers.add(newUser)) {
>     System.out.println("Користувача " + newUser + " успішно зареєстровано.");
> } else {
>     System.out.println("Помилка: користувач з таким іменем вже існує.");
> }
> ```

#### **📌 Що перевіряють:**

*Знання сигнатури та значення, що повертається, для методу `add`, та вміння застосувати це на практиці.*

* * *

### **5 ❓ Питання:**

Чому `TreeSet` не дозволяє додавати `null` елементи? ⭐️

> #### **💬 Відповідь:**
>
> `TreeSet` **не дозволяє `null`** (починаючи з деяких версій Java, до цього було можливо, але викликало проблеми), тому що він **повинен мати можливість порівнювати елементи** між собою для їх сортування.
>
> 1. `TreeSet` базується на червоно-чорному дереві. Щоб розмістити новий елемент у дереві, його потрібно порівняти з існуючими вузлами.
>
> 2. Порівняння відбувається або через метод `compareTo()` (з інтерфейсу `Comparable`), або через `compare()` (з `Comparator`).
>
> 3. Спроба викликати **`null.compareTo(...)`** або **`compare(null, ...)`** призвела б до **`NullPointerException`**.
>
> Щоб уникнути цього і гарантувати консистентність своєї структури, `TreeSet` просто забороняє додавання `null` елементів, кидаючи `NullPointerException` при спробі це зробити.

#### **📌 Що перевіряють:**

*Розуміння того, як внутрішня структура `TreeSet` (сортування) накладає обмеження на типи даних, які він може зберігати.*

* * *

### **6 ❓ Питання:**

Як перетворити `List` з дублікатами на `Set` з унікальними елементами`==`**, тобто порівнювати посилання.
>
> * `hashCode()` буде генеруватися на основі **внутрішньої адреси об'єкта в пам'яті**.
>
> **Наслідки:**
>
> `HashSet` буде вважати **різними** два логічно однакових об'єкти, оскільки вони є різними екземплярами в пам'яті.
>
> ```java
> class Person {
>     String name;
>     Person(String name) { this.name = name; }
>     // equals() і hashCode() не перевизначені
> }
>
> Set<Person> people = new HashSet<>();
> people.add(new Person("Alice"));
> people.add(new Person("Alice")); // Цей об'єкт буде успішно доданий!
>
> System.out.println(people.size()); // 2 - хоча логічно вони однакові
> ```
>
> `HashSet` буде працювати не як множина унікальних сутностей, а як множина унікальних посилань.

#### **📌 Що перевіряють:**

*Практичне розуміння наслідків порушення контракту `equals/hashCode` при роботі з `Set`.*

* * *

### **5 ❓ Питання:**

Чи можна зберігати `null` у `HashSet`, `LinkedHashSet` та `TreeSet`? ⭐️

> #### **💬 Відповідь:**
>
> * **`HashSet`:** **Так**, дозволяє зберігати **один** `null` елемент.
> * **`LinkedHashSet`:** **Так**, дозволяє зберігати **один** `null` елемент.
> * **`TreeSet`:** **Ні**, не дозволяє. Спроба додати `null` призведе до **`NullPointerException`**.
>
> **Чому `TreeSet` не дозволяє `null`?**
>
> `TreeSet` — це відсортована множина. Для сортування їй потрібно **порівнювати** елементи між собою (за допомогою `compareTo()` або `Comparator`). Операція порівняння `null` з будь-яким іншим об'єктом є невизначеною і зазвичай призводить до `NPE`. Щоб уникнути цього, `TreeSet` просто забороняє `null`.

#### **📌 Що перевіряють:**

*Знання деталей API для різних реалізацій `Set` та вміння пояснити причини певних обмежень.*

* * *

### **6 ❓ Питання:**

Як видалити всі дублікати з `ArrayList`? Наведіть ефективний спосіб. ⭐️

> #### **💬 Відповідь:**
>
> Найпростішим і найефективнішим способом є використання `Set`.
>
> 1. Створюємо `Set` (наприклад, `LinkedHashSet`, щоб зберегти порядок) з елементів `ArrayList`.
> 2. `Set` автоматично видалить усі дублікати під час свого створення.
> 3. Очищуємо оригінальний `ArrayList`.
> 4. Додаємо всі унікальні елементи з `Set` назад у `ArrayList`.
>
> **Приклад коду:**
>
> ```java
> List<String> listWithDuplicates = new ArrayList<>(List.of("a", "b", "a", "c", "b"));
>
> // 1, 2. Створюємо LinkedHashSet, щоб зберегти порядок, дублікати видаляються
> Set<String> setWithoutDuplicates = new LinkedHashSet<>(listWithDuplicates);
>
> // 3, 4. Оновлюємо оригінальний список
> listWithDuplicates.clear();
> listWithDuplicates.addAll(setWithoutDuplicates);
>
> System.out.println(listWithDuplicates); // [a, b, c]
> ```
>
> **Більш лаконічний варіант:**
> `List<String> listWithoutDuplicates = new ArrayList<>(new LinkedHashSet<>(listWithDuplicates));`
> Цей код в один рядок робить те ж саме, створюючи новий список без дублікатів.

#### **📌 Що перевіряють:**

*Практичні навички роботи з колекціями. Розуміння того, як можна комбінувати різні колекції для вирішення поширених завдань.*

* * *

### **7 ❓ Питання:**

Які математичні операції над множинами можна виконати за допомогою методів `Set`? ⭐️

> #### **💬 Відповідь:**
>
> Інтерфейс `Collection` надає методи, які дозволяють легко виконувати стандартні операції над множинами:
>
> 1. **Об'єднання (Union):** `A ∪ B`
>     * **Метод:** `addAll(Collection c)`
>     * **Приклад:** `setA.addAll(setB);` ( `setA` тепер містить усі елементи з `setA` і `setB`).
>
> 2. **Перетин (Intersection):** `A ∩ B`
>     * **Метод:** `retainAll(Collection c)`
>     * **Приклад:** `setA.retainAll(setB);` (в `setA` залишаться тільки ті елементи, які є і в `setB`).
>
> 3. **Різниця (Difference):** `A \ B`
>     * **Метод:** `removeAll(Collection c)`
>     * **Приклад:** `setA.removeAll(setB);` (з `setA` будуть видалені всі елементи, які також є в `setB`).
>
> Оскільки `Set` розширює `Collection`, всі ці методи доступні і для нього.

#### **📌 Що перевіряють:**

*Знання методів для групових операцій та їх зв'язку з теорією множин.*

* * *

### **8 ❓ Питання:**

Яким буде порядок елементів при ітерації по `HashSet`? Чи може він змінюватися? ⭐️

> #### **💬 Відповідь:**
>
> `HashSet` **не гарантує жодного порядку** елементів.
>
> * Порядок, у якому ви обходите `HashSet?

> #### **💬 Відповідь:**
>
> Це дуже поширена задача, і вирішується вона дуже просто за допомогою **конструктора колекцій**.
>
> Класи `HashSet` та інші реалізації `Set` мають конструктор, який приймає на вхід будь-яку `Collection`.
>
> ```java
> List<String> listWithDuplicates = List.of("A", "B", "A", "C", "B");
>
> // Просто передаємо список у конструктор HashSet
> Set<String> uniqueElements = new HashSet<>(listWithDuplicates);
>
> System.out.println(uniqueElements); // Виведе [A, B, C] (порядок не гарантується)
> ```
>
> Під час створення `HashSet` він буде ітерувати по вхідному списку і намагатися додати кожен елемент. Дублікати будуть автоматично проігноровані.
>
> Якщо потрібно **зберегти порядок** першого входження елементів, слід використовувати `LinkedHashSet`:
>
> `Set<String> orderedUnique = new LinkedHashSet<>(listWithDuplicates); // [A, B, C]`

#### **📌 Що перевіряють:**

*Практичні навички роботи з колекціями. Знання про конструктори, що приймають інші колекції.*

* * *

### **7 ❓ Питання:**

У чому різниця між ітераторами для `HashSet` та `LinkedHashSet`? ⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає у **порядку**, в якому ітератори повертають елементи.
>
> * **Ітератор для `HashSet`:**
>   * **Не гарантує жодного порядку.** Елементи будуть повернуті в порядку, що залежить від їхніх хеш-кодів та внутрішньої структури хеш-таблиці. Цей порядок може змінюватися навіть після `resize`.
>
> * **Ітератор для `LinkedHashSet`:**
>   * **Гарантує** повернення елементів **в порядку їх додавання**.
>   * Це можливо, оскільки `LinkedHashSet`, крім хеш-таблиці, підтримує двозв'язний список, який фіксує послідовність вставки. Ітератор проходить саме по цьому списку.
>
> Це робить `LinkedHashSet` передбачуваним, тоді як ітерація по `HashSet` є хаотичною.

#### **📌 Що перевіряють:**

*Розуміння того, як внутрішня структура реалізації впливає на поведінку її ітератора.*

* * *

### **8 ❓ Питання:**

Які математичні операції над множинами можна виконати за допомогою методів `Set`?

> #### **💬 Відповідь:**
>
> Інтерфейс `Collection` (а отже, і `Set`) надає методи для виконання основних операцій з множинами:
>
> 1. **Об'єднання (Union): `addAll(Collection c)`**
>     * Додає всі елементи з `c` у поточну множину. Дублікати будуть проігноровані.
>
> 2. **Перетин (Intersection): `retainAll(Collection c)`**
>     * **Видаляє** з поточної множини всі елементи, яких **немає** в `c`. Залишаються тільки спільні елементи.
>
> 3. **Різниця (Difference): `removeAll(Collection c)`**
>     * **Видаляє** з поточної множини всі елементи, які **є** в `c`.
>
> **Приклад:**
>
> ```java
> Set<Integer> s1 = new HashSet<>(List.of(1, 2, 3));
> Set<Integer> s2 = new HashSet<>(List.of(3, 4, 5));
>
> s1.retainAll(s2); // Залишає в s1 тільки спільні елементи
> System.out.println(s1); // [3]
> ```
>
> Важливо пам'ятати, що ці методи **модифікують вихідну множину**.

#### **📌 Що перевіряють:**

*Знання API колекцій для виконання теоретико-множинних операцій.*

* * *

### **9 ❓ Питання:**

Чому `TreeSet` вимагає, щоб його елементи були `Comparable` або щоб було надано `Comparator`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що `TreeSet` — це **відсортована** множина. Її внутрішня структура базується на **червоно-чорному дереві** (збалансованому бінарному дереві пошуку).
>
> Щоб розмістити новий елемент у дереві, `TreeSet` повинен **мати можливість порівнювати** його з існуючими елементами, щоб визначити, куди його поставити (в ліве чи праве піддерево).
>
> Для цього є два шляхи:
>
> 1. **Природний порядок:** Клас елементів реалізує інтерфейс `Comparable<T>`, надаючи метод `compareTo()`.
> 2. **Зовнішній порядок:** В конструктор `TreeSet` передається об'єкт `Comparator<T>`, який містить логіку порівняння.
>
> Якщо не виконати жодну з цих умов, `TreeSet` не знатиме, як порівнювати елементи, і кине **`ClassCastException`** під час спроби додати другий елемент (коли відбудеться перше порівняння).
>
> Також унікальність у `TreeSet` визначається не через `equals`, а через `compareTo`/`compare`: якщо порівняння повертає 0, елементи вважаються однаковими.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що сортування вимагає механізму порівняння. Зна` (наприклад, через `for-each`), залежить від`hashCode()` елементів та внутрішньої структури `HashMap` (ємність, колізії).
>
> * Цей порядок може здаватися **хаотичним** і не відповідає порядку додавання.
> * Що ще важливіше, цей порядок **не є стабільним** і може **змінитися** після того, як `HashSet` буде змінено (наприклад, через додавання нових елементів, що спричинило `resize`).
>
> **Висновок:** **Ніколи не покладайтеся** на порядок елементів у `HashSet`. Якщо порядок важливий, використовуйте `LinkedHashSet` (порядок вставки) або `TreeSet` (порядок сортування).

#### **📌 Що перевіряють:**

*Розуміння того, що відсутність порядку є ключовою характеристикою `HashSet`.*

* * *

### **9 ❓ Питання:**

Яким вимогам мають відповідати об'єкти, щоб їх можна було додавати у `TreeSet`? ⭐️

> #### **💬 Відповідь:**
>
> `TreeSet` є відсортованою множиною. Для того, щоб він міг сортувати елементи, йому потрібен механізм їх порівняння.
>
> Тому елементи, що додаються у `TreeSet`, мають відповідати **одній з двох** вимог:
>
> 1. Клас цих елементів повинен **реалізовувати інтерфейс `java.lang.Comparable<T>`**. Це визначає їхній "природний порядок".
>
> 2. Або в конструктор `TreeSet` має бути передано **екземпляр `java.util.Comparator<T>`**. Цей компаратор буде використовуватися для порівняння елементів.
>
> Якщо не виконано жодної з цих умов, спроба додати перший же елемент (другий, якщо бути точним) у `TreeSet` призведе до кидання `ClassCastException`, оскільки `TreeSet` не буде знати, як його порівняти з іншими.

#### **📌 Що перевіряють:**

*Розуміння вимог `TreeSet` до елементів. Знання про інтерфейси `Comparable` та `Comparator`.*

* * *

### **10 ❓ Питання:**

`HashSet` "під капотом" використовує `HashMap`. Поясніть, як саме. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `HashSet` — це, по суті, просто **обгортка над `HashMap`**.
>
> **Як це реалізовано:**
>
> 1. Всередині `HashSet` має поле типу `HashMap<E, Object>`.
>
> 2. Коли ви викликаєте `set.add(element)`, `HashSet` насправді викликає `map.put(element, PRESENT)`.
>     * Елемент, який ви додаєте, стає **ключем** у `HashMap`.
>     * Як значення використовується **одна й та сама константа-заглушка** (`private static final Object PRESENT = new Object();`). Вона не несе жодного сенсу і потрібна лише для відповідності API `Map`.
>
> 3. Метод `set.add()` повертає `true`, якщо `map.put()` повернув `null` (тобто такого ключа раніше не було).
>
> 4. Методи, як-от `contains()`, `remove()`, просто делегують виклики відповідним методам `HashMap` (`containsKey`, `remove`).
>
> Таким чином, `HashSet` використовує весь потужний механізм хеш-таблиці `HashMap` для забезпечення швидких операцій та унікальності ключів.

#### **📌 Що перевіряють:**

*Глибокі знання про внутрішню реалізацію колекцій. Це питання дозволяє перевірити, чи кандидат розуміє, як побудована стандартна бібліотека.*

* * *

### **11 ❓ Питання:**

Що більш ефективно за пам'яттю: `HashSet<K>` чи `HashMap<K, Boolean>` для зберігання множини унікальних елементів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`HashSet<K>` є більш ефективним за пам'яттю.**
>
> Хоча здається, що `HashMap<K, Boolean>` мав би займати приблизно стільки ж пам'яті ( `boolean` майже нічого не важить), різниця полягає в **накладних витратах на об'єкт `Map.Entry`**.
>
> * `HashMap<K, Boolean>` для кожного запису створює об'єкт `Map.Entry`, який зберігає: хеш, посилання на ключ, посилання на **об'єкт `Boolean`** і посилання на наступний елемент.
>
> * `HashSet<K>` використовує `HashMap<K, Object>`, де **всі значення** посилаються на **один і той самий статичний об'єкт-заглушку `PRESENT`**.
>
> Таким чином, `HashSet` не створює нових об'єктів для значень при кожному додаванні. `HashMap<K, Boolean>` буде створювати нові об'єкти `Boolean.TRUE` або `Boolean.FALSE` (хоча вони і кешуються, все одно є додаткове посилання).
>
> Отже, `HashSet` є не тільки семантично більш правильним, але й трохи більш оптимізованим за пам'яттю.

#### **📌 Що перевіряють:**

*Уважність до деталей реалізації та здатність аналізувати споживання пам'яті.*

* * *

### **12 ❓ Питання:**

Чому `Set` не розширює `List` або навпаки?

> #### **💬 Відповідь:**
>
> Тому що вони представляють **абсолютно різні контракти**.
>
> * **Основний контракт `List`:** Це **впорядкована** послідовність, яка гарантує доступ **за індексом**.
>
> * **Основний контракт `Set`:** Це колекція **унікальних** елементів, яка **не гарантує** порядку і **не надає** доступу за індексом.
>
> Якби `Set` успадковував від `List`, він був би змушений реалізовувати методи, як-от `get(int index)` або `indexOf(Object o)`, що не має сенсу для невпорядкованої множини.
>
> Якби `List` успадковував від `Set`, він не зміг би дозволяти дублікати, що порушило б його основний контракт.
>
> Вони обидва розширюють `Collection`, оскільки надають спільну базову функціональність ( `add`, `remove`, `size`), але далі їхні шляхи розходяться, бо вони вирішують різні завдання.

#### **📌 Що перевіряють:**

*Розуміння принципів проектування інтерфейсів та того, чому різні контракти вимагають окремих ієрархій.*

* * *

### **13 ❓ Питання:**

Наведіть приклад, де `LinkedHashSet` буде кращим вибором, ніж `HashSet` або `TreeSet`. ⭐️

> #### **💬 Відповідь:**
>
> `LinkedHashSet` є кращим вибором, коли вам потрібні **всі три** властивості одночасно:
>
> 1. **Унікальність** елементів (як у `Set`).
> 2. **Швидкі операції** додавання, видалення та пошуку (O(1), як у `HashSet`).
> 3. **Збереження порядку вставки** елементів (на відміну від `HashSet` та `TreeSet`).
>
> **Приклад сценарію:**
>
> У вас є потік подій або логів, і вам потрібно зберегти унікальні типи подій **у тому порядку, в якому вони вперше з'явилися**.
>
> ```java
> String[] eventStream = {"LOGIN", "VIEW_PAGE", "LOGOUT", "LOGIN", "VIEW_PAGE"};
> Set<String> uniqueEventsInOrder = new LinkedHashSet<>();
>
> for (String event : eventStream) {
>     uniqueEventsInOrder.add(event);
> }
>
> // Виведе: [LOGIN, VIEW_PAGE, LOGOUT] - унікальні, в порядку першого появлення
> System.out.println(uniqueEventsInOrder);
> ```
>
> * `HashSet` дав би ті самі елементи, але в непередбачуваному порядку.
> * `TreeSet` відсортував би їх за алфавітом (`[LOGIN, LOGOUT, VIEW_PAGE]`).
>
> `LinkedHashSet` ідеально підходить для створення кешів, що зберігають порядок, або для видалення дублікатів зі списку, зберігаючи вихідний порядок.

#### **📌 Що перевіряють:**

*Практичне вміння вибрати найбільш відповідну структуру даних на основі тонких вимог до порядку та продуктивності.*

* * *

### **14 ❓ Питання:**

Чи можна безпечно ітерувати по `Set` і видаляти елементи? ⭐️

> #### **💬 Відповідь:**
>
> Так, можна, але тільки **одним способом** — за допомогою методу **`remove()`** об'єкта **`Iterator`**.
>
> Будь-яка спроба змінити `Set` напряму всередині циклу `for-each` призведе до **`ConcurrentModificationException`**, оскільки ітератори для `HashSet`, `LinkedHashSet` та `TreeSet` є **`fail-fast`**.
>
> **Правильний спосіб:**
>
> ```java
> Set<Integer> numbers = new HashSet<>(Setння про `Comparable` та `Comparator`.*

* * *

### **10 ❓ Питання:**

Що таке `EnumSet` і в чому його перевага? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`EnumSet`** — це спеціалізована реалізація `Set`, розроблена виключно для роботи з **`enum` типами**.
>
> **Переваги та особливості:**
>
> 1. **Надзвичайна продуктивність та ефективність по пам'яті:**
>     * Внутрішньо `EnumSet` реалізований не через хеш-таблицю, а через **бітову маску** (зазвичай, `long` або `long[]`).
>     * Кожен біт у масці відповідає за одну `enum`-константу. Якщо біт встановлений в `1`, елемент є в множині, якщо в `0` — немає.
>     * Всі операції ( `add`, `contains`, `remove`) зводяться до дуже швидких **бітових операцій** (`|`, `&`, `~`), що робить `EnumSet` значно швидшим за `HashSet`.
>
> 2. **Компактність:** Займає дуже мало пам'яті. Для `enum`, що має до 64 констант, достатньо одного `long`.
>
> **Коли використовувати:** Завжди, коли вам потрібна множина `enum` констант.
>
> **Як створювати:**
> `EnumSet` не має публічних конструкторів. Його екземпляри створюються через статичні фабричні методи, наприклад:
> `Set<MyEnum> set = EnumSet.of(MyEnum.A, MyEnum.C);`
> `Set<MyEnum> all = EnumSet.allOf(MyEnum.class);`

#### **📌 Що перевіряють:**

*Знання про існування високоспеціалізованих та оптимізованих колекцій, таких як `EnumSet`.*

* * *

### **11 ❓ Питання:**

Чи може `Set` містити `null`? Які реалізації це дозволяють?

> #### **💬 Відповідь:**
>
> * **`HashSet`**: **Так, дозволяє**. Може містити **один** `null` елемент. (Бо "під капотом" `HashMap` дозволяє один `null` ключ).
> * **`LinkedHashSet`**: **Так, дозволяє**. Поводиться так само, як `HashSet`.
> * **`TreeSet`**: **Ні, не дозволяє** (зазвичай). При спробі додати `null` кине `NullPointerException`, оскільки `null` не можна порівнювати. (Виняток: якщо ви надасте `Comparator`, який вміє обробляти `null`, але це не є стандартною практикою).

#### **📌 Що перевіряють:**

*Уважність до деталей API для різних реалізацій.*

* * *

### **12 ❓ Питання:**

Розглянемо `NavigableSet`. Які додаткові можливості він надає порівняно з `SortedSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `NavigableSet` розширює `SortedSet` і додає значно більше можливостей для **навігації** по відсортованій множині.
>
> **Додаткові можливості:**
>
> 1. **Пошук "найближчих" відповідників:**
>     * `lower(e)`: знайти найбільший елемент, що є строго меншим за `e`.
>     * `floor(e)`: знайти найбільший елемент, що є меншим або рівним `e`.
>     * `ceiling(e)`: знайти найменший елемент, що є більшим або рівним `e`.
>     * `higher(e)`: знайти найменший елемент, що є строго більшим за `e`.
>
> 2. **Отримання та видалення крайніх елементів:**
>     * `pollFirst()`: отримати і видалити перший (найменший) елемент.
>     * `pollLast()`: отримати і видалити останній (найбільший) елемент.
>
> 3. **Зворотний обхід:**
>     * `descendingSet()`: повертає `NavigableSet`, який є видом оригінальної множини, але в зворотному порядку.
>     * `descendingIterator()`: повертає ітератор для обходу в зворотному порядку.
>
> Основною реалізацією `NavigableSet` є `TreeSet`. Ці методи роблять `TreeSet` дуже потужним інструментом для роботи з діапазонами та відсортованими даними.

#### **📌 Що перевіряють:**

*Знання просунутих інтерфейсів колекцій та їх можливостей для навігації.*

* * *

### **13 ❓ Питання:**

Чому ітерація по `HashSet` не є детермінованою?

> #### **💬 Відповідь:**
>
> Ітерація не є детермінованою, оскільки порядок елементів залежить від:
>
> 1. **`hashCode()` елементів:** Розташування у внутрішній таблиці напряму залежить від хеш-коду.
> 2. **Поточної ємності (`capacity`)** `HashSet`: Після операції **`resize`** всі елементи перехешовуються і перерозподіляються по новій, більшій таблиці, що повністю **змінює порядок** їх обходу.
>
> Навіть якщо для фіксованого набору даних на одній і тій самій версії JVM порядок може здаватися стабільним, **немає жодних гарантій**, що він не зміниться при додаванні нового елемента або при запуску на іншій JVM.
>
> Тому на порядок ітерації по `HashSet` **ніколи не можна покладатися**. Якщо потрібен стабільний порядок, слід використовувати `LinkedHashSet` (порядок вставки) або `TreeSet` (сортування).

#### **📌 Що перевіряють:**

*Розуміння того, що внутрішні механізми (хешування, ресайзинг) роблять порядок `HashSet` непередбачуваним.*

* * *

### **14 ❓ Питання:**

Що таке `CopyOnWriteArraySet`? Чим він відрізняється від `HashSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `CopyOnWriteArraySet` — це **потокобезпечна** реалізація `Set` з пакету `java.util.concurrent`.
>
> **Відмінності від `HashSet`:**
>
> 1. **Потокобезпечність:**
>     * `CopyOnWriteArraySet` є **потокобезпечним**.
>     * `HashSet` — **ні**.
>
> 2. **Внутрішня структура:**
>     * `CopyOnWriteArraySet` "під капотом" використовує **`CopyOnWriteArrayList`**. Це масив.
>     * `HashSet` використовує `HashMap`.
>
> 3. **Принцип роботи:**
>     * `CopyOnWriteArraySet` реалізує стратегію **"копіювання при записі"**. Будь-яка операція модифікації (`add`, `remove`) створює **повну нову копію** внутрішнього масиву. Це робить операції запису дуже "дорогими" (O(n)).
>
> 4. **Продуктивність:**
>     * **Запис:** `CopyOnWriteArraySet` дуже повільний (O(n)). `HashSet` швидкий (O(1)).
>     * **Читання/Ітерація:** `CopyOnWriteArraySet` дуже швидкий, оскільки не вимагає блокувань.
>
> 5. **Ітератор:**
>     * Ітератор для `CopyOnWriteArraySet` є **`fail-safe`**.
>     * Ітератор для `HashSet` є `fail-fast`.
>
> **Коли використовувати:** `CopyOnWriteArraySet` підходить для сценаріїв, де **кількість читань та ітерацій значно перевищує кількість записів** у багатопотоковому середовищі.

#### **📌 Що перевіряють:**

*Знання про конкурентні колекції. Розуміння компромісів патерну "copy-on-write".*

* * *

### **15 ❓ Питання:**

Як реалізовано `HashSet` через `HashMap`? Що виступає в ролі значення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, `HashSet` є просто **обгорткою над `HashMap`**.
>
> **Як це реалізовано:**
>
> 1. Всередині класу `HashSet` є приватне поле: `private transient HashMap<E,Object> map;`.
>
> 2. Елементи, які ви додаєте в `HashSet`, стають **ключами** у цій внутрішній `HashMap`.
>
> 3. **Що виступає в ролі значення?**
>     В якості значення для всіх ключів використовується одна і та ж сама **константа-заглушка (dummy value)**. Це приватний статичний `final`-об'єкт: `private static final Object PRESENT = new Object();`.
>
> **Реалізація `add` (спрощено):**
>
> ```java
> public boolean add(E e) {
>     // Метод put поверне null, якщо елемент новий, і попереднє значення (PRESENT),
>     // якщо елемент вже існує.
>     return map.put(e, PRESENT) == null;
> }
> ```
>
> Таким чином, `HashSet` перевикористовує всю складну та оптимізовану логіку `HashMap` (хешування, обробку колізій, ресайзинг), надаючи простий API, що відповідає контракту `Set`.

#### **📌 Що перевіряють:**

*Дуже глибоке знання про те, як реалізовані стандартні колекції. Це питання часто використовують для перевірки кандидатів рівня Middle+.*
