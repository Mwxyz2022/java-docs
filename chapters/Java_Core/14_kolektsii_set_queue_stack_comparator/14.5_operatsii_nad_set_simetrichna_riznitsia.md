---
layout: default
title: "Операції над Set - симетрична різниця"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 5
---

# Операції над Set - симетрична різниця

* короткий опис: Симетрична різниця множин – це множина, яка містить елементи, що належать лише одній з двох множин, але не обом одночасно (`A △ B = (A ∪ B) − (A ∩ B)`).

* поняття: Симетрична різниця, `addAll()` (об'єднання), `retainAll()` (перетин), `removeAll()` (різниця).

* приклади: Реалізація симетричної різниці через комбінацію `addAll()`, `retainAll()`, `removeAll()`.

* ключові моменти: Немає прямого методу для симетричної різниці, реалізується через комбінацію стандартних Set-операцій.

* що важливо знати на співбесіді: Як реалізувати, відмінність від простої різниці.

### **✅ Офіційне визначення:**

**Симетрична різниця множин** — це множина, яка містить елементи, що належать **тільки одній з двох множин**, але **не обом одночасно**.

Формально:

**`A △ B = (A ∪ B) − (A ∩ B)`**

### **🧠 Простими словами:**

**Симетрична різниця** — це все унікальне, що не спільне. Уяви двох друзів, що обмінюються картками — усе, що в них **не співпало**, і є симетрична різниця 🔀

---

## **Основне**

**📊 Операції над множинами** (**`Set`**) у Java:

* **Об'єднання** (**`union`**): **`addAll()`**
* **Перетин** (**`intersection`**): **`retainAll()`**
* **Різниця** (**`difference`**): **`removeAll()`**
* **Симетрична різниця** (**`symmetric difference`**): поєднання **`addAll(), retainAll(), removeAll()`**

**📌 Методи Java для реалізації симетричної різниці:**

* Немає прямого **`symmetricDifference()`**, але можна реалізувати так:

```java
Set<T> result = new HashSet<>(set1);
result.addAll(set2);            // union
Set<T> temp = new HashSet<>(set1);
temp.retainAll(set2);           // intersection
result.removeAll(temp);         // result = union - intersection
```

---

## **Пояснення на пальцях (💡)**

Уяви два плейлисти: **Рок** і **Поп**. Симетрична різниця — це всі треки, які **виключно рок** або **виключно поп**, але не ті, що зустрічаються в обох. Мікс без повторів — чистий кайф 🎧

---

## **Корисні приклади (🧪)**

```java
Set<String> setA = new HashSet<>(List.of("Java", "Python", "Go"));
Set<String> setB = new HashSet<>(List.of("Python", "Rust", "JavaScript"));

Set<String> result = new HashSet<>(setA);  // [Java, Python, Go]
result.addAll(setB);                       // union: [Java, Python, Go, Rust, JavaScript]

Set<String> intersection = new HashSet<>(setA);
intersection.retainAll(setB);              // intersection: [Python]

result.removeAll(intersection);            // symmetric diff: [Java, Go, Rust, JavaScript]

System.out.println(result); // [Java, Go, Rust, JavaScript]
```

---

## **Порівняння (⚖️)**

| Операція | Метод | Опис |
| :---: | :---: | :---: |
| **Об'єднання** | **`addAll()`** | Усі елементи з обох множин |
| **Перетин** | **`retainAll()`** | Тільки спільні |
| **Різниця** | **`removeAll()`** | Те, що є в A, але нема в B |
| **Симетрична різниця** | **комбіновано** | Унікальні в кожній, але не спільні |

---

## **🧠 Мнемоніка для запам’ятовування:**

**S.D. \= Solo Difference**

**"Solo in A, Solo in B"** — усе, що виступає **соло**, а не в дуеті 🎸

---

## **🟩 Підсумок:**

* Симетрична різниця — це **унікальні елементи**, які є тільки в одній множині.
* У Java реалізується через **`addAll`**, **`retainAll`**, **`removeAll`**.
* Добре підходить для **завдань з унікальністю**, **фільтрації**, **аналізу змін** між двома наборами.
* Використання **`Set`** дозволяє досягти **O(n)** чи краще у продуктивності.
* Важлива концепція на технічних інтерв’ю (особливо з **`Collections`**, **`HashSet`**, **`TreeSet`**, **`Map`**).

---

**🔗 Доповнюючі джерела:**

* [**Java TreeSet Docs (Oracle)**](https://docs.oracle.com/javase/8/docs/api/java/util/Set.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке симетрична різниця множин?

> #### **💬 Відповідь:**
>
> **Симетрична різниця** двох множин (позначається як `A △ B`) — це нова множина, яка містить усі елементи, що належать **або** множині A, **або** множині B, але **не обом одночасно**.
>
> Це можна уявити двома еквівалентними способами:
>
> 1. **Об'єднання мінус перетин:** Взяти всі елементи з обох множин (`A ∪ B`), а потім видалити ті, що є спільними для обох (`A ∩ B`).
>     * Формула: `A △ B = (A ∪ B) \ (A ∩ B)`
>
> 2. **Об'єднання двох різниць:** Взяти елементи, що є тільки в A (`A \ B`), взяти елементи, що є тільки в B (`B \ A`), і об'єднати їх.
>     * Формула: `A △ B = (A \ B) ∪ (B \ A)`

#### **📝 Аналогія:**

Уявіть, що є два кола друзів: ваші друзі та друзі вашого партнера. Симетрична різниця — це всі люди, які є друзями тільки для вас або тільки для вашого партнера, але не ваші спільні друзі.

#### **📌 Що перевіряють:**

*Базове розуміння теоретико-множинних операцій. Кандидат повинен чітко сформулювати визначення симетричної різниці.*

* * *

### **2 ❓ Питання:**

Як реалізувати симетричну різницю для двох `Set` в Java, використовуючи стандартні методи?

> #### **💬 Відповідь:**
>
> У Java немає прямого методу `symmetricDifference()`, тому її реалізують через комбінацію трьох стандартних методів `Set`:
>
> 1. **`addAll()`** — для об'єднання множин.
> 2. **`retainAll()`** — для знаходження перетину (спільних елементів).
> 3. **`removeAll()`** — для знаходження різниці.
>
> **Алгоритм:**
>
> 1. Створити множину-результат, яка є об'єднанням двох початкових множин.
> 2. Створити тимчасову множину, яка є перетином двох початкових множин.
> 3. Видалити з множини-результату всі елементи, що містяться у множині-перетині.

#### **📝 Приклад:**

```java
public <T> Set<T> symmetricDifference(Set<T> set1, Set<T> set2) {
    // 1. Створюємо об'єднання (union)
    Set<T> union = new HashSet<>(set1);
    union.addAll(set2);

    // 2. Створюємо перетин (intersection)
    Set<T> intersection = new HashSet<>(set1);
    intersection.retainAll(set2);

    // 3. З об'єднання видаляємо перетин
    union.removeAll(intersection);

    return union;
}
```

#### **📌 Що перевіряють:**

*Практичні навички роботи з `java.util.Set` та його основними методами. Здатність перекласти математичну формулу на мову програмного коду.*

* * *

### **3 ❓ Питання:**

У чому різниця між простою різницею (`removeAll`) та симетричною різницею?

> #### **💬 Відповідь:**
>
> Головна відмінність полягає в тому, що **проста різниця є односторонньою**, а **симетрична різниця — двосторонньою**.
>
> * **Проста різниця (A \ B):** Операція `setA.removeAll(setB)` видаляє з `setA` всі елементи, що також є в `setB`. В результаті в `setA` залишаться тільки ті елементи, які були **унікальними для `setA`**. Елементи, унікальні для `setB`, ігноруються.
>
> * **Симетрична різниця (A △ B):** Результат містить елементи, унікальні як для `setA`, так і для `setB`. Вона включає елементи, які належать лише одній з множин.

#### **📝 Приклад:**

Нехай:
`setA = {1, 2, 3}`
`setB = {3, 4, 5}`

* Проста різниця `A \ B` (результат `setA.removeAll(setB)`): `{1, 2}`.
* Симетрична різниця `A △ B`: `{1, 2, 4, 5}`.

#### **📌 Що перевіряють:**

*Уважність до термінології та точне розуміння поведінки кожної операції. Це перевіряє, чи не плутає кандидат схожі, але різні за своєю суттю поняття.*

* * *

### **4 ❓ Питання:**

Коротко поясніть призначення методів `addAll()`, `retainAll()` та `removeAll()` в інтерфейсі `Set`.

> #### **💬 Відповідь:**
>
> Ці три методи є основними інструментами для виконання теоретико-множинних операцій у Java. Всі вони **модифікують** колекцію, на якій викликаються.
>
> * **`boolean addAll(Collection<? extends E> c)` (Об'єднання):**
>     Додає до поточного `Set` всі елементи із зазначеної колекції `c`. Якщо в результаті операції `Set` змінився (були додані нові елементи), метод повертає `true`.
>
> * **`boolean retainAll(Collection<?> c)` (Перетин):**
>     Видаляє з поточного `Set` всі елементи, які **не** містяться у зазначеній колекції `c`. Таким чином, в `Set` залишаються тільки спільні елементи. Якщо `Set` змінився, повертає `true`.
>
> * **`boolean removeAll(Collection<?> c)` (Різниця):**
>     Видаляє з поточного `Set` всі елементи, які містяться у зазначеній колекції `c`. Якщо `Set` змінився, повертає `true`.

#### **📝 Коментар:**

Мнемоніка:

* `addAll`: "Додати **все**."
* `retainAll`: "**Зберегти** все (спільне)."
* `removeAll`: "**Видалити** все (спільне)."

#### **📌 Що перевіряють:**

*Знання базового API Java Collections Framework. Це фундаментальні методи, незнання яких є серйозною прогалиною.*

* * *

### **5 ❓ Питання:**

Яка реалізація `Set` (`HashSet`, `LinkedHashSet` чи `TreeSet`) найкраще підходить для виконання операцій над множинами, як-от симетрична різниця? Чому?

> #### **💬 Відповідь:**
>
> **`HashSet` є найкращим вибором** для виконання цих операцій з точки зору продуктивності.
>
> **Причина:** Усі три ключові методи (`addAll`, `retainAll`, `removeAll`) у своїй основі використовують базові операції `add()`, `contains()` та `remove()`.
>
> * У **`HashSet`** ці операції мають середню часову складність **O(1)**. Тому розрахунок симетричної різниці для двох `HashSet` розмірами `m` і `n` буде мати складність приблизно **O(m + n)**.
>
> * У **`TreeSet`** базові операції мають складність **O(log k)**. Відповідно, вся операція буде значно повільнішою, приблизно **O((m+n) * log(m+n))**. `TreeSet` варто використовувати тільки якщо вам потрібно, щоб кінцевий результат був відсортований.
>
> * **`LinkedHashSet`** має таку ж продуктивність, як `HashSet` (O(1)), але використовує трохи більше пам'яті. Його варто обрати, якщо потрібно зберегти порядок вставки елементів у результуючій множині.
>
> **Висновок:** Для чистої швидкості обчислень — `HashSet`.

#### **📌 Що перевіряють:**

*Розуміння того, як вибір конкретної реалізації впливає на продуктивність алгоритму. Кандидат повинен вміти обґрунтовувати свій вибір, спираючись на знання часової складності.*

* * *

### **6 ❓ Питання:**

Наведіть повний приклад коду для знаходження симетричної різниці двох множин рядків.

> #### **💬 Відповідь:**
>
> Ось повний, готовий до виконання приклад, що демонструє знаходження симетричної різниці між двома множинами рядків за допомогою стандартного підходу.

#### **📝 Приклад:**

```java
import java.util.Arrays;
import java.util.HashSet;
import java.util.Set;

public class SymmetricDifferenceExample {

    public static void main(String[] args) {
        // Початкові множини
        Set<String> set1 = new HashSet<>(Arrays.asList("Java", "Python", "Go"));
        Set<String> set2 = new HashSet<>(Arrays.asList("Python", "Rust", "JavaScript"));

        System.out.println("Множина 1: " + set1);
        System.out.println("Множина 2: " + set2);

        // Крок 1: Об'єднання
        Set<String> union = new HashSet<>(set1);
        union.addAll(set2);
        System.out.println("Об'єднання (union): " + union);

        // Крок 2: Перетин
        Set<String> intersection = new HashSet<>(set1);
        intersection.retainAll(set2);
        System.out.println("Перетин (intersection): " + intersection);

        // Крок 3: Симетрична різниця (Об'єднання - Перетин)
        union.removeAll(intersection);
        Set<String> symmetricDifference = union;

        System.out.println("Симетрична різниця: " + symmetricDifference);
    }
}
```

#### **📌 Що перевіряють:**

*Здатність написати простий, але коректний і читабельний код, що вирішує поставлену задачу.*

* * *

### **7 ❓ Питання:**

Чи модифікуються початкові множини під час виконання операцій `addAll`, `retainAll`, `removeAll`?

> #### **💬 Відповідь:**
>
> **Так, модифікуються.** Усі ці методи змінюють стан об'єкта `Set`, на якому вони викликаються. Це операції, що мають побічні ефекти (side-effects).
>
> Якщо вам потрібно зберегти початкові множини незмінними, ви повинні виконувати всі операції на **копіях**.
>
> **Правильний підхід для збереження оригіналів:**
>
> 1. Створити нову множину-результат, ініціалізувавши її як копію першої множини (`new HashSet<>(set1)`).
> 2. Виконати всі подальші операції (`addAll`, `removeAll` і т.д.) на цій новій множині, використовуючи друге оригінальне `Set` як аргумент.

#### **📝 Приклад (безпечна реалізація):**

```java
public <T> Set<T> getSymmetricDifference(Set<T> set1, Set<T> set2) {
    // Працюємо з копіями, щоб не змінювати оригінали
    Set<T> result = new HashSet<>(set1); // Копія set1
    Set<T> temp = new HashSet<>(set2);   // Копія set2

    result.removeAll(set2); // в result тепер унікальні елементи з set1
    temp.removeAll(set1);   // в temp тепер унікальні елементи з set2

    result.addAll(temp); // об'єднуємо унікальні
    return result;
}
```

#### **📌 Що перевіряють:**

*Розуміння концепції мутабельності та важливості захисного копіювання (defensive copying) при написанні API. Це ознака досвідченого та обачного розробника.*

* * *

### **8 ❓ Питання:**

Чи можна реалізувати симетричну різницю ефективніше, без створення додаткової множини для перетину? ⭐️

> #### **💬 Відповідь:**
>
> Так, можна реалізувати симетричну різницю за допомогою одного циклу по кожній множині, що дозволяє уникнути створення та заповнення проміжної множини `intersection`. Цей підхід може бути трохи ефективнішим з точки зору використання пам'яті.
>
> **Алгоритм:**
>
> 1. Створити порожню множину-результат.
> 2. Пройтися по першій множині. Для кожного елемента перевірити, чи він НЕ міститься у другій множині. Якщо ні — додати його до результату.
> 3. Пройтися по другій множині. Для кожного елемента перевірити, чи він НЕ міститься у першій множині. Якщо ні — додати його до результату.

#### **📝 Приклад:**

```java
public <T> Set<T> symmetricDifferenceOnePass(Set<T> set1, Set<T> set2) {
    Set<T> result = new HashSet<>();

    // Додаємо елементи, унікальні для set1
    for (T element : set1) {
        if (!set2.contains(element)) {
            result.add(element);
        }
    }

    // Додаємо елементи, унікальні для set2
    for (T element : set2) {
        if (!set1.contains(element)) {
            result.add(element);
        }
    }
    return result;
}
```

#### **📌 Що перевіряють:**

*Здатність мислити алгоритмічно та оптимізувати код. Кандидат повинен вміти запропонувати альтернативну реалізацію, проаналізувавши її переваги (менше пам'яті) та недоліки (потенційно більше викликів `contains`).*

* * *

### **9 ❓ Питання:**

Яка часова складність (Big O) розрахунку симетричної різниці для двох `HashSet`'ів? ⭐️

> #### **💬 Відповідь:**
>
> Часова складність для розрахунку симетричної різниці двох `HashSet` розмірами **m** (для `set1`) і **n** (для `set2`) є **O(m + n)**.
>
> Давайте проаналізуємо класичний трикроковий алгоритм:
>
> 1. `union = new HashSet<>(set1)`: **O(m)**, оскільки кожен елемент з `set1` додається в нову множину.
> 2. `union.addAll(set2)`: **O(n)**, оскільки кожен елемент з `set2` намагаються додати.
> 3. `intersection = new HashSet<>(set1)`: **O(m)**.
> 4. `intersection.retainAll(set2)`: **O(m)** в середньому. Реалізація `retainAll` ітерує по `set1` (розмір `m`) і для кожного елемента перевіряє `contains` в `set2` (операція O(1)).
> 5. `union.removeAll(intersection)`: **O(k)**, де k - розмір `intersection`.
>
> Кожен крок виконується за лінійний час відносно розміру одного чи обох сетів. Оскільки операції виконуються послідовно, загальна складність буде домінуватись найбільшим з лінійних кроків, що в сумі дає **O(m + n)**.

#### **📌 Що перевіряють:**

*Здатність аналізувати часову складність алгоритмів, що використовують стандартні колекції. Це важлива навичка для написання ефективного коду.*

* * *

### **10 ❓ Питання:**

Як реалізувати симетричну різницю за допомогою Java 8 Streams API? ⭐️

> #### **💬 Відповідь:**
>
> Симетричну різницю можна реалізувати за допомогою стрімів, хоча такий підхід може бути менш продуктивним для `HashSet`'ів порівняно з прямими методами, оскільки `contains()` буде викликатися багато разів у лямбдах. Проте, цей спосіб демонструє знання сучасного API.
>
> **Найбільш зрозумілий спосіб:**
>
> 1. Створити стрім з елементів, унікальних для першої множини (`set1 \ set2`).
> 2. Створити стрім з елементів, унікальних для другої множини (`set2 \ set1`).
> 3. Об'єднати ці два стріми за допомогою `Stream.concat()` і зібрати результат у новий `Set`.

#### **📝 Приклад:**

```java
import java.util.Set;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public <T> Set<T> symmetricDifferenceStream(Set<T> set1, Set<T> set2) {
    // Стрім з унікальних для set1 елементів
    Stream<T> diff1 = set1.stream()
                           .filter(e -> !set2.contains(e));

    // Стрім з унікальних для set2 елементів
    Stream<T> diff2 = set2.stream()
                           .filter(e -> !set1.contains(e));

    // Об'єднуємо два стріми і збираємо в новий Set
    return Stream.concat(diff1, diff2)
                 .collect(Collectors.toSet());
}
```

#### **📌 Що перевіряють:**

*Знання Java 8 Streams API та вміння застосовувати функціональний підхід до вирішення алгоритмічних задач. Також перевіряється розуміння можливих наслідків для продуктивності.*

* * *

### **11 ❓ Питання:**

Як зміниться продуктивність розрахунку симетричної різниці, якщо замість `HashSet` використати `TreeSet`? ⭐️

> #### **💬 Відповідь:**
>
> Продуктивність значно **погіршиться**.
>
> Якщо для `HashSet` складність операції становить `O(m + n)`, то для `TreeSet` вона буде приблизно **`O((m + n) * log(m + n))`**.
>
> **Чому так відбувається:**
> `TreeSet` базується на червоно-чорному дереві. Будь-яка операція додавання (`add`), видалення (`remove`) або перевірки наявності (`contains`) вимагає проходу по дереву, що займає `O(log k)` часу, де `k` — кількість елементів у сеті.
>
> * `union.addAll(set2)`: Кожен з `n` елементів `set2` додається в `union`, який росте. Складність кожної вставки — `O(log k)`. Сумарно це буде приблизно `O(n * log(m+n))`.
> * Так само для інших операцій (`retainAll`, `removeAll`).
>
> **Коли це може бути виправдано?**
> Використання `TreeSet` має сенс тільки в тому випадку, якщо вам критично важливо отримати результуючу множину **відсортованою**. Це компроміс між швидкістю та впорядкованістю.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як внутрішня структура даних (хеш-таблиця vs збалансоване дерево) кардинально впливає на продуктивність алгоритмів, що на ній базуються.*

* * *

### **12 ❓ Питання:**

Напишіть метод `symmetricDifference(Set<T> set1, Set<T> set2)`, який гарантує, що вхідні множини `set1` і `set2` залишаться незмінними. ⭐️

> #### **💬 Відповідь:**
>
> Щоб гарантувати незмінність вхідних даних, метод не повинен викликати мутуючі операції (`addAll`, `removeAll` і т.д.) на самих об'єктах `set1` і `set2`. Усі операції повинні виконуватися на їх копіях.

#### **📝 Приклад:**

Ось один з канонічних варіантів реалізації такого "чистого" методу, що не має побічних ефектів.

```java
import java.util.HashSet;
import java.util.Set;

public class ImmutableSymmetricDifference {

    /**
     * Повертає нову множину, що є симетричною різницею двох інших.
     * Не змінює вхідні множини.
     * @param set1 перша множина
     * @param set2 друга множина
     * @param <T> тип елементів
     * @return нова множина з симетричною різницею
     */
    public static <T> Set<T> calculate(Set<T> set1, Set<T> set2) {
        // Створюємо копію першої множини для роботи
        Set<T> result = new HashSet<>(set1);

        // Обчислюємо (set1 U set2) \ (set1 ∩ set2)
        // Альтернативна імплементація (set1 \ set2) U (set2 \ set1)
        // є навіть трохи кращою з точки зору пам'яті

        Set<T> tempSet2 = new HashSet<>(set2);
        
        result.removeAll(tempSet2); // Тепер в result (set1 \ set2)
        tempSet2.removeAll(set1);   // Тепер в tempSet2 (set2 \ set1)
        
        result.addAll(tempSet2);    // Тепер в result (set1 \ set2) U (set2 \ set1)

        return result;
    }
}
```

#### **📌 Що перевіряють:**

*Навички проектування API, зокрема вміння писати чисті функції без побічних ефектів. Це є основою функціонального програмування та хорошою практикою в об'єктно-орієнтованому програмуванні.*

* * *

### **13 ❓ Питання:**

Яким чином якість реалізації методів `hashCode()` та `equals()` елементів множини впливає на продуктивність розрахунку симетричної різниці? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якість `hashCode()` та `equals()` має **критичний вплив** на продуктивність, оскільки від неї залежить ефективність `HashSet`.
>
> 1. **Продуктивність `HashSet`:** Робота `HashSet` базується на припущенні, що `hashCode()` добре розподіляє елементи по комірках (buckets), а операції `add()`, `remove()` та `contains()` виконуються за середній час O(1).
>
> 2. **Погана реалізація `hashCode()`:** Якщо `hashCode()` реалізовано погано (наприклад, завжди повертає константу), всі елементи потрапляють в одну комірку. `HashSet` вироджується до зв'язного списку (або дерева в Java 8+). Час виконання операцій `contains()` та інших деградує до O(n).
>
> 3. **Вплив на симетричну різницю:** Оскільки розрахунок симетричної різниці — це серія операцій `add()`, `contains()` (неявно в `retainAll/removeAll`), деградація `HashSet` призводить до катастрофічної деградації всього алгоритму. Його складність перетворюється з **O(m + n)** на **O(m * n)**.
>
> **Неправильний `equals()`:** Якщо `equals()` реалізовано неправильно (порушено контракт з `hashCode`), `HashSet` почне поводитися некоректно: може зберігати дублікати або "втрачати" елементи. Це призведе не до погіршення продуктивності, а до **логічно неправильного результату**.

#### **📌 Що перевіряють:**

*Глибоке, багатошарове розуміння Java Collections Framework. Кандидат повинен бачити зв'язок між контрактом об'єкта (`hashCode`/`equals`), реалізацією структури даних (`HashSet`) та продуктивністю алгоритму, що її використовує.*

* * *

### **14 ❓ Питання:**

Уявіть, що вам потрібно знайти симетричну різницю для двох дуже великих сетів, які не вміщуються в оперативну пам'ять одночасно. Як би ви підійшли до вирішення цієї задачі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема обробки великих даних, і її не можна вирішити стандартними in-memory колекціями. Існує кілька підходів:
>
> **1. Підхід "розділяй і володарюй" (External Hashing/Sorting):**
>
> * **Розділення:** Прочитати обидва сети по черзі. Для кожного елемента обчислити хеш (наприклад, `element.hashCode() % K`, де `K` — кількість файлів-комірок). Записати сам елемент у відповідний файл-комірку (`bucket_A_i`, `bucket_B_i`). Таким чином, ми розбиваємо два величезних файли на `2*K` менших файлів, де елементи з однаковим хешем гарантовано потраплять в однойменні комірки.
> * **Обробка:** Тепер можна послідовно обробляти кожну пару файлів (`bucket_A_i`, `bucket_B_i`), які вже, ймовірно, помістяться в пам'ять. Для кожної пари обчислити симетричну різницю стандартним способом і записати результат.
>
> **2. Використання баз даних:**
>
> * Завантажити обидві множини у дві таблиці в реляційну або NoSQL базу даних.
> * Виконати операцію за допомогою SQL-запиту, що реалізує симетричну різницю (наприклад, через `FULL OUTER JOIN ... WHERE A.id IS NULL OR B.id IS NULL`). База даних візьме на себе оптимізацію виконання на диску.
>
> **3. Використання фреймворків для Big Data:**
>
> * Використати Apache Spark, Apache Flink або аналогічний фреймворк. Вони надають високорівневі API для роботи з розподіленими наборами даних (RDD, Dataset), які включають оптимізовані операції `subtract` та `union`, здатні працювати на кластері машин.

#### **📌 Що перевіряють:**

*Системне мислення та здатність виходити за межі стандартних завдань. Кандидат повинен продемонструвати розуміння проблем масштабованості та знання інструментів для їх вирішення.*

* * *

### **15 ❓ Питання:**

Стандартна реалізація симетричної різниці через об'єднання та перетин вимагає створення трьох нових сетів у гіршому випадку. Напишіть реалізацію, яка використовує лише одну додаткову множину. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна обійтися лише однією додатковою множиною для результату. Це робить код компактнішим і трохи ефективнішим з точки зору використання пам'яті. Логіка полягає в тому, щоб по черзі додавати та видаляти елементи з однієї множини-результату.
>
> **Алгоритм:**
>
> 1. Створити результуючу множину як копію `set1`.
> 2. Пройтися по `set2`. Для кожного елемента `set2`:
>    * Спробувати видалити його з результату (`result.remove(element)`).
>    * Якщо видалення не вдалося (метод повернув `false`), це означає, що такого елемента в `set1` не було. Отже, це унікальний для `set2` елемент, і його треба додати до результату (`result.add(element)`).
>
> Таким чином, після циклу в результуючій множині залишаться елементи, унікальні для `set1`, і до них додадуться елементи, унікальні для `set2`.

#### **📝 Приклад:**

```java
public <T> Set<T> memoryEfficientSymmetricDifference(Set<T> set1, Set<T> set2) {
    Set<T> result = new HashSet<>(set1);

    for (T element : set2) {
        // Якщо елемент є в `result` (тобто був у `set1`), remove() поверне true,
        // і елемент буде видалено.
        // Якщо елемента немає, remove() поверне false.
        if (!result.remove(element)) {
            // Це унікальний для `set2` елемент, додаємо його.
            result.add(element);
        }
    }
    return result;
}
```

#### **📌 Що перевіряють:**

*Винахідливість та глибоке розуміння API (`remove()` повертає `boolean`). Кандидат демонструє здатність написати не просто робочий, а елегантний та оптимізований алгоритм, використовуючи всі можливості стандартної бібліотеки.*

* * *
