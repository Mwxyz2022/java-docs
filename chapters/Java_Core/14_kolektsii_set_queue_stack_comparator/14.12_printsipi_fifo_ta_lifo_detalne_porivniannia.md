---
layout: default
title: "Принципи FIFO та LIFO - детальне порівняння"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 12
---

# Принципи FIFO та LIFO - детальне порівняння

* короткий опис: `FIFO` (First-In-First-Out) – елементи обробляються в порядку додавання (як черга). `LIFO` (Last-In-First-Out) – елементи обробляються в зворотньому порядку додавання (як стопка).

* поняття: `FIFO`, `LIFO`, `Queue`, `Stack`, `Deque`.

* приклади: Черга в магазині (`FIFO`), стопка тарілок (`LIFO`).

* ключові моменти: `Queue` реалізує `FIFO`, `Stack` – `LIFO`. `Deque` може імітувати обидва.

* що важливо знати на співбесіді: Чіткі відмінності, їх застосування.

### **✅ Офіційне визначення:**

* **FIFO (First-In, First-Out)** — це принцип обробки даних, за яким першим обробляється той елемент, який був доданий раніше.
* **LIFO (Last-In, First-Out)** — це принцип обробки, за яким першим обробляється останній доданий елемент.

### **🧠 Простими словами:**

* **FIFO** — як черга в магазині: хто перший став, той перший і вийде.
* **LIFO** — як стопка тарілок: хто зверху, той перший піде в обіг.

---

## 📊 **Список структур даних, що реалізують ці принципи:**

* **FIFO:**
  * **`Queue`** (інтерфейс)
  * **`LinkedList`** (як реалізація черги)
  * **`ArrayDeque`** (для черги)
* **LIFO:**
  * **`Stack`**
  * **`ArrayDeque`** (для стеку)
  * **`LinkedList`** (може виступати і як стек)

---

## **Пояснення на пальцях (💡)**

* **FIFO** — уяви собі чергу до банкомата. Перший клієнт — перший, кого обслуговують.
* **LIFO** — уяви собі стопку млинців. Новий млинець зверху, і саме його ти береш першим.
* **Deque** — унікальна структура, яка дозволяє діяти і як FIFO, і як LIFO — залежно від використаних методів.

---

## **Корисні приклади (🧪)**

```java
// FIFO -- Queue
Queue<String> queue = new LinkedList<>();
queue.add("A");
queue.add("B");
System.out.println(queue.poll()); // A

// LIFO -- Stack
Stack<String> stack = new Stack<>();
stack.push("X");
stack.push("Y");
System.out.println(stack.pop()); // Y
```

---

## **Порівняння (⚖️)**

| Критерій | FIFO | LIFO |
| :---: | :---: | :---: |
| **Порядок** | Перший прийшов — перший вийшов | Останній прийшов — перший вийшов |
| **Реалізації** | **`Queue`**, **`LinkedList`**, **`ArrayDeque`** | **`Stack`**, **`ArrayDeque`**, **`LinkedList`** |
| **Методи** | **`add(), poll()`** | **`push(), pop(), peek()`** |
| **Приклади з життя** | Черга в магазині, друк файлів | Стос книг, тарілки |
| **Застосування** | Обробка подій, черги завдань | Відкат операцій, рекурсія |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **FIFO** \= *First In — First Out* -> як черга в супермаркеті: перший прийшов, перший вийшов.
* **LIFO** \= *Last In — First Out* -> як стопка паперів: останній покладений — перший береться.

---

## **🟩 Підсумок:**

* **FIFO і LIFO** — це базові принципи роботи з колекціями, які визначають порядок обробки елементів.
* **FIFO** використовується у **`Queue`** — для обробки у порядку надходження.
* **LIFO** використовується у **`Stack`** — для зворотного порядку.
* **Deque** — універсальна структура, яка може поводитись як Queue або Stack.
* Знання цих принципів критичне для написання алгоритмів, парсингу, роботи з рекурсією, менеджменту завдань.

---

### **🔗 Додаткові джерела:**

* [**Java Queue (Oracle Docs)**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Queue.html)
* [**Java Stack (Oracle Docs)**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Stack.html)
* [**Deque Interface (Oracle Docs)**](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Deque.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке принципи FIFO та LIFO, і в чому їхня головна відмінність?

> #### **💬 Відповідь:**
>
> **FIFO** та **LIFO** — це два фундаментальні принципи обробки даних у комп'ютерних науках, які визначають порядок, у якому елементи витягуються з колекції.
>
> * **FIFO (First-In, First-Out) — "Перший прийшов, перший пішов"**
>   * **Принцип:** Елемент, який був доданий у колекцію першим, буде оброблений і видалений також першим.
>   * **Структура даних:** Цей принцип реалізується в **`Queue` (черга)**.
>
> * **LIFO (Last-In, First-Out) — "Останній прийшов, перший пішов"**
>   * **Принцип:** Елемент, який був доданий у колекцію останнім, буде оброблений і видалений першим.
>   * **Структура даних:** Цей принцип реалізується в **`Stack` (стек)**.
>
> **Головна відмінність** полягає в точці доступу: FIFO має дві різні точки для додавання (хвіст) і видалення (голова), тоді як LIFO використовує одну точку (вершину) для обох операцій.

#### **📝 Аналогія:**

* **FIFO:** Черга до каси в магазині. Хто прийшов раніше, того обслужать раніше.
* **LIFO:** Стопка тарілок. Ви кладете нову тарілку наверх і берете тарілку теж зверху.

#### **📌 Що перевіряють:**

*Базове розуміння визначень та ключової різниці між FIFO та LIFO. Це основа для розуміння роботи черг та стеків.*

* * *

### **2 ❓ Питання:**

Які структури даних у Java реалізують принципи FIFO та LIFO?

> #### **💬 Відповідь:**
>
> У Java Collections Framework існують спеціальні інтерфейси та класи для реалізації цих принципів:
>
> * **Для FIFO (Черга):**
>   * **Інтерфейс:** `java.util.Queue`.
>   * **Популярні реалізації:**
>     * **`LinkedList`**: Класична реалізація черги на основі зв'язного списку.
>     * **`ArrayDeque`**: Більш ефективна реалізація на основі масиву. Рекомендується для більшості випадків.
>     * **`PriorityQueue`**: Особливий випадок, де порядок визначається пріоритетом, а не часом додавання.
>
> * **Для LIFO (Стек):**
>   * **Класичний клас:** `java.util.Stack`. Він вважається застарілим, але все ще використовується.
>   * **Сучасна альтернатива (інтерфейс + клас):** Інтерфейс **`Deque`** та його реалізація **`ArrayDeque`**. API `Deque` містить методи `push`/`pop`/`peek` для повноцінної роботи зі стеком.

#### **📝 Коментар:**

`ArrayDeque` є універсальною структурою, яка може ефективно працювати і як черга, і як стек, тому це кращий вибір у сучасному коді.

#### **📌 Що перевіряють:**

*Знання конкретних класів та інтерфейсів Java Collections Framework, які відповідають за реалізацію FIFO та LIFO.*

* * *

### **3 ❓ Питання:**

Які ключові методи використовуються для реалізації FIFO та LIFO?

> #### **💬 Відповідь:**
>
> Кожен принцип має свій набір характерних операцій.
>
> * **FIFO (в інтерфейсі `Queue`):**
>   * **Додавання в кінець:** `add(e)` / `offer(e)`
>   * **Видалення з початку:** `remove()` / `poll()`
>   * **Перегляд початку:** `element()` / `peek()`
>
> * **LIFO (в класі `Stack` та інтерфейсі `Deque`):**
>   * **Додавання на вершину:** `push(e)`
>   * **Видалення з вершини:** `pop()`
>   * **Перегляд вершини:** `peek()`

#### **📝 Коментар:**

Зауважте, що метод `peek()` є в обох API. У випадку з `Queue`/`Deque`, `peek()` дивиться на "голову" черги. У випадку зі стеком, `peek()` дивиться на "вершину", що, по суті, є тим самим місцем (`head` для `Deque`), звідки відбуваються `pop` і `push`.

#### **📌 Що перевіряють:**

*Знання основного API для черг та стеків. Кандидат повинен чітко розрізняти `add/poll` і `push/pop`.*

* * *

### **4 ❓ Питання:**

Наведіть приклади реальних завдань, де використовується FIFO, а де — LIFO.

> #### **💬 Відповідь:**
>
> **Сценарії для FIFO (Черга):**
>
> * **Планування завдань:** Обробка завдань у порядку їх надходження (наприклад, черга друку, обробка запитів на сервері).
> * **Алгоритм пошуку в ширину (BFS):** Для обходу графів або дерев рівень за рівнем.
> * **Буферизація даних:** Потокова передача даних (відео, аудіо), де дані обробляються в тому порядку, в якому вони були отримані.
> * **Системи обміну повідомленнями:** Message brokers, як-от RabbitMQ або Kafka, використовують черги для гарантованої доставки повідомлень.
>
> **Сценарії для LIFO (Стек):**
>
> * **Механізм Undo/Redo:** Скасування останньої дії (наприклад, у текстовому редакторі).
> * **Стек викликів у програмуванні:** Відстеження викликів функцій та їхніх локальних змінних. Рекурсія напряму залежить від цього.
> * **Парсинг та обчислення виразів:** Перевірка збалансованості дужок, перетворення інфіксної нотації в постфіксну.
> * **Алгоритм пошуку в глибину (DFS):** Для обходу графів або дерев, коли потрібно йти "вглиб" до упору.

#### **📌 Що перевіряють:**

*Здатність пов'язати абстрактні структури даних з конкретними практичними задачами. Це демонструє глибину розуміння та досвід.*

* * *

### **5 ❓ Питання:**

Поясніть, як `Deque` може реалізувати обидва принципи — FIFO та LIFO.

> #### **💬 Відповідь:**
>
> `Deque` (двостороння черга) за своєю природою є гнучкою структурою, яка дозволяє додавати і видаляти елементи з обох кінців: з початку (голови) і з кінця (хвоста). Це дозволяє емулювати як чергу, так і стек, просто вибравши правильну комбінацію методів.
>
> * **Емуляція FIFO (Черга):**
>   * **Додавання:** в один кінець (напр., хвіст) -> `addLast()` / `offerLast()`
>   * **Видалення:** з протилежного кінця (голова) -> `removeFirst()` / `pollFirst()`
>
> * **Емуляція LIFO (Стек):**
>   * **Додавання:** в один кінець (напр., голова) -> `addFirst()` / `push()`
>   * **Видалення:** з того ж самого кінця (голова) -> `removeFirst()` / `pop()`

#### **📝 Коментар:**

`Deque` спеціально містить методи `push`, `pop`, `peek`, щоб його можна було використовувати як пряму заміну `Stack`, не змінюючи звичні назви операцій.

#### **📌 Що перевіряють:**

*Розуміння універсальності `Deque` та знання його API. Це важливе знання для написання сучасного Java-коду.*

* * *

### **6 ❓ Питання:**

Продемонструйте на коді, як обробка однакової послідовності елементів призведе до різного результату при використанні FIFO та LIFO.

> #### **💬 Відповідь:**
>
> У цьому прикладі ми додамо елементи 1, 2, 3 у чергу та стек, а потім подивимося, в якому порядку вони будуть витягнуті.

#### **📝 Приклад:**

```java
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.LinkedList;
import java.util.Queue;

public class FifoLifoDemo {
    public static void main(String[] args) {
        // --- FIFO (Черга) ---
        Queue<Integer> fifoQueue = new LinkedList<>();
        fifoQueue.add(1);
        fifoQueue.add(2);
        fifoQueue.add(3);
        System.out.print("FIFO вивід: ");
        while (!fifoQueue.isEmpty()) {
            System.out.print(fifoQueue.poll() + " "); // 1 2 3
        }
        System.out.println();

        // --- LIFO (Стек) ---
        Deque<Integer> lifoStack = new ArrayDeque<>();
        lifoStack.push(1);
        lifoStack.push(2);
        lifoStack.push(3);
        System.out.print("LIFO вивід: ");
        while (!lifoStack.isEmpty()) {
            System.out.print(lifoStack.pop() + " "); // 3 2 1
        }
        System.out.println();
    }
}
```

#### **📌 Що перевіряють:**

*Практичне розуміння принципів FIFO/LIFO та вміння передбачити результат роботи коду, що їх використовує.*

* * *

### **7 ❓ Питання:**

Що краще використовувати для FIFO-черги: `LinkedList` чи `ArrayDeque`?

> #### **💬 Відповідь:**
>
> У більшості випадків для FIFO-черги **`ArrayDeque` є кращим вибором** з точки зору продуктивності та використання пам'яті.
>
> * **`ArrayDeque`:**
>   * **Переваги:** Використовує внутрішній масив, що забезпечує кращу локальність кешу та відсутність накладних витрат на створення об'єктів-вузлів для кожного елемента. Операції додавання/видалення з обох кінців мають амортизований час O(1).
>   * **Недолік:** Не дозволяє `null` елементи.
>
> * **`LinkedList`:**
>   * **Переваги:** Дозволяє `null` елементи. Забезпечує стабільний час O(1) для операцій додавання/видалення з кінців.
>   * **Недоліки:** Витрачає більше пам'яті та може бути повільнішим через необхідність переходу за посиланнями та створення вузлів.
>
> **Висновок:** Використовуйте `ArrayDeque` за замовчуванням, якщо вам не потрібна підтримка `null` значень.

#### **📌 Що перевіряють:**

*Здатність аналізувати компроміси (trade-offs) між різними реалізаціями та обирати оптимальну на основі вимог до продуктивності та функціональності.*

* * *

### **8 ❓ Питання:**

В яких випадках поведінка LIFO може бути корисною для управління пам'яттю? ⭐️

> #### **💬 Відповідь:**
>
> Принцип LIFO лежить в основі роботи **стеку викликів (call stack)**, який є ключовим механізмом для управління пам'яттю, виділеною для локальних змінних та параметрів функцій.
>
> **Як це працює:**
>
> 1. Коли викликається функція, в стеку створюється новий фрейм для її даних.
> 2. Якщо ця функція викликає іншу, новий фрейм кладеться поверх старого.
> 3. Коли функція завершує роботу, її фрейм видаляється з вершини стеку, автоматично звільняючи всю пам'ять, що була виділена для її локальних змінних.
>
> Ця LIFO-поведінка є надзвичайно ефективною, оскільки виділення та звільнення пам'яті зводиться до простої зміни вказівника на вершину стеку. Це набагато швидше, ніж динамічне виділення пам'яті в купі (heap). Рекурсія була б неможливою без такого стекового механізму.

#### **📌 Що перевіряють:**

*Глибокі знання основ інформатики та розуміння того, як абстрактні принципи (LIFO) застосовуються на низькому рівні в середовищах виконання, таких як JVM.*

* * *

### **9 ❓ Питання:**

Система повинна обробляти вхідні запити користувачів. Який принцип, FIFO чи LIFO, краще забезпечить "справедливість" обробки? ⭐️

> #### **💬 Відповідь:**
>
> **FIFO однозначно краще забезпечує справедливість.**
>
> **Пояснення:**
>
> * **FIFO (Черга):** гарантує, що запити будуть оброблені в тому порядку, в якому вони надійшли. Користувач, який відправив запит раніше, буде обслужений раніше. Це інтуїтивно зрозумілий і "чесний" підхід, який запобігає ситуації, коли старі запити "голодують" (starvation) і ніколи не обробляються.
>
> * **LIFO (Стек):** призвів би до протилежного. Нові запити завжди оброблялися б першими. Якщо потік запитів є постійним, то старі запити, що знаходяться внизу стеку, можуть ніколи не отримати своєї черги на обробку. Це створює вкрай "несправедливу" систему.
>
> Тому для будь-яких систем, де важливий порядок надходження (веб-сервери, планувальники задач, системи підтримки клієнтів), FIFO є стандартом де-факто.

#### **📌 Що перевіряють:**

*Здатність застосувати правильний принцип до проектування системи, виходячи з бізнес-вимог ("справедливість").*

* * *

### **10 ❓ Питання:**

Які обмеження має клас `Stack` у Java порівняно з `Deque` при реалізації LIFO? ⭐️

> #### **💬 Відповідь:**
>
> Хоча `Stack` і реалізує LIFO, `Deque` пропонує набагато більш гнучкий і сучасний підхід.
>
> **Обмеження `Stack`:**
>
> 1. **Погана обробка помилок:** `Stack` кидає `EmptyStackException` при спробі `pop()` або `peek()` з порожнього стеку. У вас немає альтернативи, окрім як писати `if (!stack.empty())` або обробляти виняток.
>
> 2. **Відсутність гнучкості:** `Stack` — це клас, а не інтерфейс. Ви не можете легко змінити його реалізацію.
>
> 3. **Непотрібні методи:** Успадкування від `Vector` "забруднює" API методами, що порушують семантику стеку.
>
> **Переваги `Deque`:**
>
> * **Гнучка обробка помилок:** `Deque` надає дві пари методів: `pop/peek` (кидають винятки) та `pollFirst/peekFirst` (повертають `null`). Це дає розробнику вибір.
> * **Робота через інтерфейс:** Можна писати код для інтерфейсу `Deque`, а реалізацію обирати за потребою (`ArrayDeque`, `LinkedList`).
> * **Чисте API:** Надає тільки ті методи, які відповідають концепції.

#### **📌 Що перевіряють:**

*Розуміння переваг сучасного дизайну API (гнучкість, чистота, програмування на основі інтерфейсів) над застарілими підходами.*

* * *

### **11 ❓ Питання:**

Що станеться, якщо я спробую реалізувати LIFO-стек, використовуючи `addLast()` та `removeFirst()` на `Deque`? ⭐️

> #### **💬 Відповідь:**
>
> Якщо ви будете використовувати `addLast()` для додавання і `removeFirst()` для видалення, ви отримаєте не LIFO, а **FIFO**-поведінку.
>
> **Пояснення:**
>
> * `addLast()`: додає елемент у хвіст.
> * `removeFirst()`: видаляє елемент з голови.
>
> Це класична схема роботи **черги**. Елемент, що був доданий першим (`A`), залишається на початку (в голові) і буде видалений першим.
>
> | Крок | Операція | Стан `Deque` | Видалено |
> | :--- | :--- | :--- | :--- |
> | 1 | `addLast("A")` | `[A]` | - |
> | 2 | `addLast("B")` | `[A, B]` | - |
> | 3 | `removeFirst()` | `[B]` | "A" |
>
> Щоб отримати LIFO (стек), потрібно працювати з **одним кінцем**, наприклад: `addFirst()` (push) та `removeFirst()` (pop).

#### **📌 Що перевіряють:**

*Уважність та вміння точно простежити логіку роботи методів. Це питання-пастка, щоб перевірити, чи не плутає кандидат комбінації методів для емуляції FIFO та LIFO.*

* * *

### **12 ❓ Питання:**

Уявіть структуру даних "черга з пріоритетом" (`PriorityQueue`). Який принцип вона реалізує: FIFO, LIFO чи щось інше? ⭐️

> #### **💬 Відповідь:**
>
> `PriorityQueue` реалізує **ні FIFO, ні LIFO**.
>
> Хоча `PriorityQueue` є реалізацією інтерфейсу `Queue`, порядок видалення елементів у ній визначається не часом їх додавання, а їхнім **пріоритетом**. Пріоритет визначається або через реалізацію `Comparable` елементами, або через переданий `Comparator`.
>
> При виклику `poll()` з `PriorityQueue` завжди повертається елемент з **найвищим пріоритетом** (за замовчуванням — найменший елемент).
>
> **Приклад:** Якщо в `PriorityQueue` додати числа `[5, 1, 3]`, то перший виклик `poll()` поверне `1`, другий — `3`, третій — `5`, незалежно від порядку їх додавання.
>
> Таким чином, це зовсім інший принцип, який можна назвати "Highest-Priority-First-Out".

#### **📌 Що перевіряють:**

*Знання спеціалізованих колекцій та розуміння того, що реалізація інтерфейсу не завжди означає дотримання його "найпоширенішої" семантики.*

* * *

### **13 ❓ Питання:**

Як би ви реалізували LIFO-стек, який додатково підтримує операцію `findMiddle()` (повертає середній елемент) за O(1)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Стандартні реалізації не підтримують таку операцію за O(1). Для цього потрібно розробити власну структуру даних. Один із способів — використати **двонаправлений зв'язний список (doubly-linked list)** разом з додатковим вказівником на середній елемент.
>
> **Структура:**
>
> * Використовуємо `DoublyLinkedList` для зберігання елементів стеку.
> * Зберігаємо `head` — вказівник на вершину стеку.
> * Зберігаємо `middle` — вказівник на середній вузол.
> * Зберігаємо лічильник елементів `count`.
>
> **Логіка операцій:**
>
> * **`push(e)` (O(1))**:
>     1. Додаємо новий елемент на початок списку.
>     2. Інкрементуємо `count`.
>     3. Якщо `count` став непарним, пересуваємо `middle` на один вузол назад (`middle = middle.prev`). Це тому, що тепер "середина" змістилась.
>
> * **`pop(e)` (O(1))**:
>     1. Видаляємо елемент з початку списку.
>     2. Декрементуємо `count`.
>     3. Якщо `count` став парним, пересуваємо `middle` на один вузол вперед (`middle = middle.next`).
>
> * **`findMiddle()` (O(1))**:
>     1. Просто повертаємо значення, що зберігається у вузлі, на який вказує `middle`.
>
> Цей підхід забезпечує виконання всіх операцій за константний час, хоча і є значно складнішим в реалізації, ніж стандартний стек.

#### **📌 Що перевіряють:**

*Здатність проектувати власні структури даних для вирішення нетривіальних задач. Це перевіряє глибокі знання алгоритмів та структур даних.*

* * *

### **14 ❓ Питання:**

Розробіть калькулятор для обчислення виразів у зворотній польській нотації (Reverse Polish Notation, RPN), наприклад, "3 4 + 5 *". Який принцип, FIFO чи LIFO, ви б використали? Поясніть чому. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для обчислення виразів у RPN ідеально підходить принцип **LIFO**, реалізований за допомогою **стеку**.
>
> **Чому LIFO?**
> RPN — це постфіксна нотація, де оператор йде після своїх операндів. Щоб обчислити такий вираз, нам потрібно тимчасово зберігати операнди, доки ми не зустрінемо оператор, який до них застосовується. Останні два операнди, які ми побачили, є тими, що потрібні для операції. Ця "останній-побачив-перший-використав" логіка — це суть LIFO.
>
> **Алгоритм:**
>
> 1. Створити порожній стек для чисел.
> 2. Розділити вхідний рядок на токени (числа та оператори).
> 3. Для кожного токена:
>    а. Якщо токен — це **число**, `push` його в стек.
>    б. Якщо токен — це **оператор** (`+`, `*` і т.д.):
>       i. `pop` два останні операнди зі стеку (наприклад, `b = stack.pop()`, `a = stack.pop()`).
>       ii. Виконати операцію: `result = a оператор b`.
>       iii. `push` результат назад у стек.
> 4. Після обробки всіх токенів у стеку залишиться єдине число — результат обчислення.

#### **📌 Що перевіряють:**

*Класичне застосування LIFO для задач парсингу та обчислень. Кандидат повинен знати цей алгоритм і вміти пояснити, чому стек є ключовою структурою для його реалізації.*

* * *

### **15 ❓ Питання:**

Як реалізувати FIFO-чергу, використовуючи два LIFO-стеки? Поясніть часову складність операцій. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне алгоритмічне завдання, що ілюструє, як можна імітувати одну структуру іншою.
>
> **Структура:**
>
> * `inStack`: стек для операцій додавання (enqueue).
> * `outStack`: стек для операцій видалення (dequeue).
>
> **Логіка:**
>
> * **`enqueue(E item)`:**
>   * Просто додаємо елемент в `inStack`. **Складність: O(1)**.
>
> * **`dequeue()`:**
>     1. Якщо `outStack` **не порожній**, просто витягуємо елемент з його вершини. Елементи там вже в потрібному FIFO-порядку.
>     2. Якщо `outStack` **порожній**, ми повинні перемістити всі елементи з `inStack` в `outStack`. Це фактично реверсує порядок. Після цього витягуємо елемент з вершини `outStack`.
>     3. Якщо обидва стеки порожні, черга порожня.
>
> **Аналіз часової складності `dequeue()`:**
>
> * **Амортизована складність: O(1)**.
> * Хоча в деяких випадках (коли `outStack` порожній) операція вимагає `O(n)` часу для переміщення всіх елементів, це трапляється рідко. Кожен елемент переміщується з `inStack` в `outStack` лише один раз за весь час життя черги. Тому якщо "розмазати" вартість дорогої операції на всі дешеві, середня вартість на операцію буде константною, тобто O(1).

#### **📌 Що перевіряють:**

*Алгоритмічне мислення, розуміння концепції амортизованої складності та вміння розв'язувати нетривіальні задачі на структури даних.*

* * *
