---
layout: default
title: "Інтерфейс Deque - двостороння черга"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 10
---

# Інтерфейс Deque - двостороння черга

* короткий опис: `Deque` (Double-Ended Queue) – це інтерфейс `Java Collections Framework`, який дозволяє додавати й видаляти елементи як з початку, так і з кінця черги, що робить її універсальною для реалізації як `Queue` (FIFO), так і `Stack` (LIFO).

* поняття: `Deque`, `double-ended queue`, `addFirst()`, `addLast()`, `removeFirst()`, `removeLast()`, `peekFirst()`, `peekLast()`, `ArrayDeque` (ефективна реалізація).

* приклади: Використання `Deque` як `Queue` (`addLast()`, `removeFirst()`) та як `Stack` (`addFirst()`, `removeFirst()`/`pop()`).

* ключові моменти: Гнучкість використання, висока продуктивність `ArrayDeque`. Рекомендована заміна для `java.util.Stack`.

* що важливо знати на співбесіді: Призначення `Deque`, як використовувати як `Stack`/`Queue`.

### **✅ Офіційне визначення:**

**`Deque`** (Double-Ended Queue) — це інтерфейс **Java Collections Framework**, який дозволяє **додавати й видаляти елементи як з початку, так і з кінця** черги.

### **🧠 Простими словами:**

Це черга з двома входами/виходами: можна додати або витягнути елемент **з обох боків**, що робить її універсальною

---

## **Типи реалізацій Deque**

| Реалізація | Особливості |
| :---: | :---: |
| **`ArrayDeque`** | Найпопулярніша, швидка, без обмежень на розмір |
| **`LinkedList`** | Також реалізує **`Deque`**, працює з null |
| **`ConcurrentLinkedDeque`** | Безпечна для багатопотокових середовищ |

---

## **Концепція double-ended queue (FIFO \+ LIFO) (💡)**

**Deque дозволяє працювати:**

* як **черга (Queue)**: **`addLast()`** \+ **`removeFirst()`** -> FIFO
* як **стек (Stack)**: **`addFirst()`** \+ **`removeFirst()`** -> LIFO

**🔁 Тобто можна:**

* Додати елемент на початок або кінець
* Видалити елемент з початку або з кінця

---

## **Методи роботи з обома кінцями (🛠️)**

| Метод | Опис |
| :---: | :---: |
| **`addFirst(E e)`** | Додає елемент на початок |
| **`addLast(E e)`** | Додає елемент у кінець |
| **`removeFirst()`** | Видаляє та повертає з початку |
| **`removeLast()`** | Видаляє та повертає з кінця |
| **`peekFirst()`** | Переглядає перший елемент |
| **`peekLast()`** | Переглядає останній елемент |
| **`offerFirst(E e)`** | Безпечне додавання на початок |
| **`offerLast(E e)`** | Безпечне додавання в кінець |

---

## **Приклади використання (🧪)**

#### **✅ Як Queue (FIFO):**

```java
Deque<String> deque = new ArrayDeque<>();
deque.addLast("Face");
deque.addLast("GPT");
System.out.println(deque.removeFirst()); // Face
```

#### **✅ Як Stack (LIFO):**

```java
Deque<Integer> stack = new ArrayDeque<>();
stack.addFirst(10);
stack.addFirst(20);
System.out.println(stack.removeFirst()); // 20
```

---

## **Практичні сценарії застосування**

✅ **Stack і Queue в одному флаконі**  
✅ **Пошук у ширину (BFS)** — черга  
✅ **Пошук у глибину (DFS)** — стек  
✅ **Кешування з двостороннім видаленням (LRU Cache)**  
✅ **Обробка undo/redo команд**

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
Deque = Double Ended QUEue -> двостороння черга

"DEQUE -- де *КЮ* вхід з обох сторін, як у супермаркет з обома дверима"
```

---

## **🟩 Підсумок:**

* **`Deque`** — структура даних з можливістю **додавати/видаляти з обох кінців**.
* Працює як **Queue (FIFO)** або **Stack (LIFO)**.
* Найефективніша реалізація — **`ArrayDeque`**.
* Методи: **`addFirst, addLast, removeFirst, removeLast, peekFirst, peekLast.`**
* Важлива для задач зі змішаною логікою черг/стеків.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs – Deque Interface**](https://docs.oracle.com/javase/8/docs/api/java/util/Deque.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс `Deque` і яка його головна відмінність від `Queue`?

> #### **💬 Відповідь:**
>
> **`Deque` (вимовляється "дек")** — це скорочення від **Double-Ended Queue**, що в перекладі означає "двостороння черга". Це інтерфейс в `java.util`, який розширює інтерфейс `Queue`.
>
> **Головна відмінність:**
>
> * Звичайна `Queue` є **односторонньою**: елементи додаються в кінець (хвіст), а видаляються з початку (голова), працюючи за принципом FIFO.
> * `Deque` є **двосторонньою**: вона дозволяє ефективно додавати та видаляти елементи **як з початку, так і з кінця** колекції.
>
> Ця гнучкість робить `Deque` універсальною структурою даних, яку можна використовувати і як чергу (FIFO), і як стек (LIFO).

#### **📝 Аналогія:**

Якщо `Queue` — це ескалатор, що рухається тільки вгору, то `Deque` — це тунель з двома входами/виходами. Ви можете зайти або вийти з будь-якого кінця.

#### **📌 Що перевіряють:**

*Базове визначення `Deque` та розуміння його ключової характеристики — можливості двостороннього доступу, що відрізняє його від стандартної черги.*

* * *

### **2 ❓ Питання:**

Як можна використовувати `Deque` для емуляції `Queue` (черги FIFO)?

> #### **💬 Відповідь:**
>
> Щоб `Deque` працював як звичайна `Queue` за принципом FIFO (Перший прийшов — перший пішов), потрібно узгодити операції додавання та видалення:
>
> * **Додавання:** Використовуйте методи для додавання елементів **у кінець (хвіст)** черги.
>   * `addLast(e)` або `offerLast(e)` (більш безпечний варіант)
>
> * **Видалення:** Використовуйте методи для видалення елементів **з початку (голови)** черги.
>   * `removeFirst()` або `pollFirst()` (більш безпечний варіант)
>
> Інтерфейс `Deque` також містить стандартні методи `Queue` (`add`, `offer`, `remove`, `poll`), які за замовчуванням працюють саме так: `add(e)` еквівалентний `addLast(e)`, а `poll()` — `pollFirst()`.

#### **📝 Приклад:**

```java
Deque<String> queue = new ArrayDeque<>();
// Додаємо в кінець
queue.offerLast("User1");
queue.offerLast("User2");

// Видаляємо з початку
System.out.println(queue.pollFirst()); // Виведе "User1"
```

#### **📌 Що перевіряють:**

*Розуміння того, як зіставити методи `Deque` з поведінкою FIFO, і знання, як поводяться стандартні методи `Queue` в контексті `Deque`.*

* * *

### **3 ❓ Питання:**

Як можна використовувати `Deque` для емуляції `Stack` (стека LIFO)?

> #### **💬 Відповідь:**
>
> `Deque` є рекомендованою сучасною заміною для застарілого класу `Stack`. Щоб емулювати стек за принципом LIFO (Останній прийшов — перший пішов), потрібно виконувати всі операції **з одного кінця**, наприклад, з початку.
>
> Для цього в `Deque` є спеціальні методи, що повністю відповідають операціям стеку:
>
> * **Додавання на вершину стеку (`push`):**
>   * `push(e)` (цей метод є в інтерфейсі `Deque` і еквівалентний `addFirst(e)`)
>
> * **Видалення з вершини стеку (`pop`):**
>   * `pop()` (цей метод є в інтерфейсі `Deque` і еквівалентний `removeFirst()`)
>
> * **Перегляд вершини стеку (`peek`):**
>   * `peek()` (цей метод є в інтерфейсі `Deque` і еквівалентний `peekFirst()`)

#### **📝 Приклад:**

```java
Deque<String> stack = new ArrayDeque<>();
// Додаємо на "вершину"
stack.push("Page 1");
stack.push("Page 2");
stack.push("Page 3");

// Видаляємо з "вершини"
System.out.println(stack.pop()); // Виведе "Page 3"
System.out.println(stack.peek()); // Виведе "Page 2"
```

#### **📌 Що перевіряють:**

*Знання того, що `Deque` є сучасною альтернативою `Stack`, і вміння зіставити його методи з класичними операціями стеку.*

* * *

### **4 ❓ Питання:**

Які існують основні реалізації інтерфейсу `Deque`?

> #### **💬 Відповідь:**
>
> Дві найпоширеніші реалізації `Deque` у стандартній бібліотеці Java:
>
> 1. **`ArrayDeque`**:
>     * Реалізація на основі **кільцевого масиву**, що динамічно змінює розмір.
>     * **Дуже швидка** та ефективна з точки зору пам'яті. Не має накладних витрат на вузли, як у `LinkedList`.
>     * **Не дозволяє `null`** елементи.
>     * Є **кращим вибором** для більшості випадків використання як стеку або черги.
>
> 2. **`LinkedList`**:
>     * Реалізація на основі **двонаправленого зв'язного списку**.
>     * **Дозволяє `null`** елементи.
>     * Трохи повільніша за `ArrayDeque` для операцій з кінцями через необхідність роботи з посиланнями та об'єктами-вузлами.
>     * `LinkedList` також реалізує інтерфейс `List`, що робить його універсальним, але менш спеціалізованим.

#### **📌 Що перевіряють:**

*Знання основних класів Java Collections Framework та розуміння ключових відмінностей між ними (`ArrayDeque` vs `LinkedList`).*

* * *

### **5 ❓ Питання:**

Чому `ArrayDeque` є кращою реалізацією стеку, ніж застарілий клас `java.util.Stack`?

> #### **💬 Відповідь:**
>
> `ArrayDeque` вважається кращою заміною для `Stack` з двох основних причин, пов'язаних з дизайном:
>
> 1. **Продуктивність:** Клас `Stack` успадковує `Vector`, а всі методи `Vector` є **синхронізованими (`synchronized`)**. Це означає, що при кожному виклику `push` або `pop` відбувається блокування, що створює значні накладні витрати на продуктивність, особливо в однопотоковому середовищі. `ArrayDeque` не є синхронізованим і, відповідно, набагато швидший.
>
> 2. **Дотримання принципів дизайну:** Наслідування `Stack` від `Vector` є прикладом поганого дизайну ("композиція важливіша за наслідування"). `Stack` успадковує всі публічні методи `Vector`, включаючи можливість додавати або видаляти елементи за індексом (`add(index, e)`), що порушує строгий LIFO-принцип стеку. `Deque` API є значно чистішим і надає лише ті операції, що відповідають його призначенню.

#### **📝 Коментар:**

Завжди використовуйте `Deque<T> stack = new ArrayDeque<>();` для реалізації стеку в сучасному Java-коді.

#### **📌 Що перевіряють:**

*Розуміння недоліків застарілих класів (legacy classes) та вміння пояснити, чому сучасні альтернативи є кращими з точки зору продуктивності та дизайну API.*

* * *

### **6 ❓ Питання:**

Поясніть різницю між двома парами методів: `addFirst`/`addLast` та `offerFirst`/`offerLast`.

> #### **💬 Відповідь:**
>
> Як і в `Queue`, інтерфейс `Deque` надає дві групи методів для додавання елементів, які відрізняються своєю поведінкою в разі помилки. Це особливо актуально для обмежених за розміром реалізацій (`bounded deque`).
>
> **Пара 1: Методи, що кидають винятки**
>
> * `addFirst(E e)`: Додає елемент на початок.
> * `addLast(E e)`: Додає елемент в кінець.
> * **Поведінка:** Якщо операція не вдається (напр., дек повний), обидва методи кидають виняток `IllegalStateException`.
>
> **Пара 2: Методи, що повертають спеціальні значення**
>
> * `offerFirst(E e)`: Додає елемент на початок.
> * `offerLast(E e)`: Додає елемент в кінець.
> * **Поведінка:** Якщо операція не вдається, обидва методи повертають `false` і не кидають виняток.
>
> Пара `offer...` є більш бажаною, оскільки обробка `false` зазвичай простіша за обробку винятків.

#### **📌 Що перевіряють:**

*Уважність до API та розуміння механізмів обробки помилок у колекціях. Це повторює логіку `Queue`, але застосовану до обох кінців.*

* * *

### **7 ❓ Питання:**

Поясніть призначення методів `peekFirst()` та `peekLast()`.

> #### **💬 Відповідь:**
>
> Ці методи призначені для "підглядання" за елементами на кінцях деку **без їх видалення**.
>
> * **`peekFirst()`**:
>   * Повертає елемент **з початку (з голови)** деку.
>   * Дек при цьому **не змінюється**.
>   * Якщо дек порожній, повертає `null`.
>   * Цей метод повністю еквівалентний методу `peek()` з інтерфейсу `Queue`.
>
> * **`peekLast()`**:
>   * Повертає елемент **з кінця (з хвоста)** деку.
>   * Дек також **не змінюється**.
>   * Якщо дек порожній, повертає `null`.
>
> Ці методи корисні, коли потрібно перевірити, який елемент наступний для обробки, не змінюючи при цьому стан самої колекції.

#### **📌 Що перевіряють:**

*Розуміння неруйнівних (non-destructive) операцій та вміння відрізнити їх від руйнівних (як `poll`/`remove`).*

* * *

### **8 ❓ Питання:**

Чи можна додавати `null` елементи в `Deque`? Від чого це залежить? ⭐️

> #### **💬 Відповідь:**
>
> Можливість додавання `null` залежить виключно від **конкретної реалізації `Deque`**.
>
> * **`ArrayDeque`**: **ЗАБОРОНЯЄ `null`**. Спроба додати `null` будь-яким методом (`addFirst`, `offerLast` і т.д.) призведе до `NullPointerException`. Причина полягає в тому, що `null` використовується методами `poll` та `peek` як індикатор порожнього деку. Дозвіл на зберігання `null` створив би неоднозначність.
>
> * **`LinkedList`**: **ДОЗВОЛЯЄ `null`**. Оскільки `LinkedList` є більш загальною структурою даних (також реалізує `List`), для нього `null` є припустимим значенням. При використанні `LinkedList` як `Deque` ви можете зберігати `null`.
>
> **Висновок:** Якщо вам не потрібна підтримка `null`, завжди віддавайте перевагу `ArrayDeque` через його вищу продуктивність.

#### **📌 Що перевіряють:**

*Знання специфічних контрактів різних реалізацій колекцій. Це питання часто ставлять, щоб перевірити уважність до деталей API.*

* * *

### **9 ❓ Питання:**

Які є реальні сценарії, де `Deque` є кращим вибором, ніж звичайні `Queue` або `Stack`? ⭐️

> #### **💬 Відповідь:**
>
> `Deque` є ідеальним вибором у задачах, де потрібен доступ або модифікація колекції з обох кінців.
>
> **Практичні приклади:**
>
> 1. **Алгоритм "ковзного вікна" (Sliding Window):** Наприклад, для знаходження максимуму в кожному підмасиві фіксованого розміру `k`. Нові елементи додаються в один кінець деку, а старі, що виходять з вікна, видаляються з іншого. Також елементи видаляються з хвоста, якщо вони менші за новий елемент, що додається. Це вимагає операцій з обох кінців.
>
> 2. **Обробник завдань "Work Stealing":** У багатопотокових системах, кожен потік може мати свій власний `Deque` завдань. Свій потік бере завдання з голови (як стек), а інший, вільний потік, може "вкрасти" завдання з хвоста (як черга), щоб зменшити конфлікти.
>
> 3. **Реалізація функції Undo/Redo:** Коли користувач виконує дію, вона додається в `undoDeque`. Коли він натискає "Undo", дія переміщується з `undoDeque` в `redoDeque`. Коли він натискає "Redo", дія переміщується назад. Обидві операції реалізуються як стеки.

#### **📌 Що перевіряють:**

*Здатність застосовувати теоретичні знання про структури даних до вирішення реальних, нетривіальних проблем. Це показує практичний досвід та алгоритмічне мислення.*

* * *

### **10 ❓ Питання:**

Чому `ArrayDeque` зазвичай швидший, ніж `LinkedList` для операцій `Deque`? ⭐️

> #### **💬 Відповідь:**
>
> `ArrayDeque` виграє у продуктивності з кількох причин, пов'язаних з його внутрішньою реалізацією на масиві:
>
> 1. **Локальність кешу (Cache Locality):** Елементи `ArrayDeque` зберігаються в суміжних комірках пам'яті в масиві. Коли процесор завантажує один елемент, він також завантажує в свій кеш сусідні елементи. Це робить послідовний доступ до них надзвичайно швидким. Вузли `LinkedList`, навпаки, розкидані по всій пам'яті (heap), що призводить до "промахів кешу" (cache misses) і сповільнює доступ.
>
> 2. **Накладні витрати на пам'ять та об'єкти:** Кожен елемент в `LinkedList` вимагає створення окремого об'єкта `Node`, який зберігає сам елемент і два посилання (`next`, `prev`). Це не тільки споживає більше пам'яті, а й створює додаткове навантаження на збирача сміття (Garbage Collector). `ArrayDeque` зберігає лише самі об'єкти в масиві.
>
> 3. **Менше операцій з посиланнями:** Операції в `ArrayDeque` — це в основному арифметика з індексами, тоді як в `LinkedList` — це маніпуляції з посиланнями, що може бути трохи повільніше.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як внутрішня структура даних впливає на продуктивність на низькому рівні (робота з пам'яттю, кешування). Це ознака досвідченого розробника.*

* * *

### **11 ❓ Питання:**

Як `ArrayDeque` реалізує двосторонній доступ за допомогою звичайного масиву? ⭐️

> #### **💬 Відповідь:**
>
> `ArrayDeque` використовує для цього розумну техніку, що називається **кільцевий буфер (circular buffer)** або **кільцевий масив**.
>
> **Як це працює:**
>
> 1. `ArrayDeque` має внутрішній масив і два вказівники (індекси): `head` (початок деку) і `tail` (кінець деку).
> 2. Елементи деку знаходяться в масиві між цими вказівниками.
> 3. **"Зациклення":** Коли `head` або `tail` досягають кінця фізичного масиву, вони "перестрибують" на його початок. Це досягається за допомогою операції по модулю.
>     * Додавання в кінець: `tail` інкрементується. Якщо `tail` дорівнює довжині масиву, він стає `0`.
>     * Додавання на початок: `head` декрементується. Якщо `head` стає `-1`, він стає `array.length - 1`.
> 4. **Зміна розміру:** Коли `head` і `tail` "зустрічаються" (дек стає повним), створюється новий, більший масив. Всі елементи з старого масиву копіюються в новий у правильному лінійному порядку (від `head` до `tail`), а вказівники `head` і `tail` оновлюються.

#### **📌 Що перевіряють:**

*Знання реалізації ключових структур даних. Розуміння концепції кільцевого буфера демонструє глибокі знання в області інформатики.*

* * *

### **12 ❓ Питання:**

Чи безпечно ітерувати по `ArrayDeque` за допомогою `for-each` циклу? В якому порядку будуть повернуті елементи? ⭐️

> #### **💬 Відповідь:**
>
> Так, це **повністю безпечно**. Ітератор, який повертає `ArrayDeque`, розроблений так, щоб коректно обробляти внутрішню структуру кільцевого масиву.
>
> **Порядок елементів:**
> Ітератор завжди буде повертати елементи в логічній послідовності — **від голови (`head`) до хвоста (`tail`)**.
>
> Навіть якщо внутрішньо елементи "зациклені" в масиві (наприклад, голова знаходиться на індексі 5, а хвіст — на індексі 2), ітератор спочатку пройде від індексу 5 до кінця масиву, а потім — від початку масиву до індексу 2. Для користувача це буде прозоро, він отримає логічно впорядковану послідовність.

#### **📝 Коментар:**

Однак, якщо ви змінюєте дек під час ітерації не через `iterator.remove()`, ви отримаєте `ConcurrentModificationException`, як і в більшості колекцій Java.

#### **📌 Що перевіряють:**

*Розуміння того, як контракти ітераторів виконуються в нестандартних внутрішніх структурах, та знання можливих ризиків (як `ConcurrentModificationException`).*

* * *

### **13 ❓ Питання:**

Уявіть, що ви використовуєте `Deque` як стек для відстеження викликів рекурсивної функції. Що краще використовувати для перевірки, чи порожній стек: `stack.size() == 0` чи `stack.isEmpty()`? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Завжди краще використовувати **`stack.isEmpty()`**.
>
> Хоча для `ArrayDeque` обидва методи мають однакову продуктивність **O(1)** і дадуть той самий результат, `isEmpty()` має дві концептуальні та практичні переваги:
>
> 1. **Читабельність та виразність:** `isEmpty()` напряму виражає ваше намірення — "я хочу знати, чи колекція порожня". Код `size() == 0` вимагає додаткового розумового зусилля для інтерпретації. Це кращий стиль кодування.
>
> 2. **Продуктивність для інших реалізацій:** `ArrayDeque` може обчислити розмір за O(1), оскільки зберігає лічильник. Однак існують інші реалізації колекцій (зокрема, деякі конкурентні, як `ConcurrentLinkedQueue`), де метод `size()` може бути **дорогою операцією O(n)**, оскільки вимагає повного обходу структури. Метод `isEmpty()` для таких колекцій залишається швидким (O(1)).
>
> Використання `isEmpty()` є хорошою звичкою, яка робить ваш код не тільки чистішим, але й більш стійким до зміни реалізації колекції в майбутньому.

#### **📌 Що перевіряють:**

*Здатність писати "чистий" код та розуміння, що однакові за результатом дії можуть мати різну семантику та продуктивність залежно від контексту. Це ознака досвіду та глибокого розуміння API.*

* * *

### **14 ❓ Питання:**

`Deque` успадковує від `Collection` метод `remove(Object o)`. Чому використання цього методу на `ArrayDeque` може бути дуже неефективним, і яка його часова складність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча `ArrayDeque` і надає цей метод, його використання є антипатерном для деку, і воно дуже неефективне.
>
> **Часова складність: O(n)**, де `n` — кількість елементів у деку.
>
> **Чому так повільно?**
>
> 1. **Лінійний пошук:** `Deque` не індексований для швидкого пошуку елементів за значенням. Щоб знайти об'єкт, який потрібно видалити, `ArrayDeque` змушений **лінійно перебрати всі елементи** від `head` до `tail`, порівнюючи кожен за допомогою `equals()`. Це вже займає O(n).
>
> 2. **Зсув елементів:** Після того, як елемент знайдено, його потрібно видалити з внутрішнього масиву. Це створює "дірку". Щоб її закрити і зберегти цілісність деку, потрібно **зсунути** всі елементи, що знаходяться між видаленим елементом та найближчим кінцем (`head` або `tail`). Цей зсув може зайняти до `O(n)` часу в гіршому випадку (якщо елемент знаходиться далеко від кінців).
>
> **Висновок:** Якщо вам потрібно видаляти елементи з середини колекції за значенням, `Deque` — це абсолютно неправильний вибір структури даних. Для цього краще підійде `HashSet` або `HashMap`.

#### **📌 Що перевіряють:**

*Розуміння того, як алгоритми та внутрішні структури даних впливають на часову складність операцій. Кандидат повинен усвідомлювати, що наявність методу в API ще не означає, що його використання є доцільним.*

* * *

### **15 ❓ Питання:**

Розробіть структуру даних, яка підтримує операції `push`, `pop`, `peek`, та `getMin` (повертає мінімальний елемент) за час O(1). Як `Deque` може допомогти в цьому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне завдання "Min Stack", і його можна елегантно вирішити за допомогою **двох деків (або стеків)**, реалізованих через `ArrayDeque`.
>
> 1. **`mainDeque`**: Звичайний дек (використовується як стек), де зберігаються всі елементи.
> 2. **`minDeque`**: Допоміжний дек (також як стек), який на своїй вершині завжди зберігає поточний мінімальний елемент в `mainDeque`.
>
> **Логіка операцій:**
>
> * **`push(int x)` (O(1))**:
>     1. `x` завжди додається в `mainDeque`.
>     2. Якщо `minDeque` порожній або `x` менший або рівний елементу на вершині `minDeque` (`minDeque.peek()`), то `x` також додається в `minDeque`.
>
> * **`pop()` (O(1))**:
>     1. Видаляємо елемент `x` з `mainDeque`.
>     2. Якщо видалений `x` дорівнює елементу на вершині `minDeque` (`minDeque.peek()`), то видаляємо елемент і з `minDeque`. Це гарантує, що `minDeque` завжди актуальний.
>
> * **`peek()` (O(1))**:
>   * Просто повертає `mainDeque.peek()`.
>
> * **`getMin()` (O(1))**:
>   * Просто повертає `minDeque.peek()`.
>
> Цей підхід дозволяє отримати мінімум за константний час, використовуючи додаткову пам'ять для другого деку.

#### **📌 Що перевіряють:**

*Здатність проектувати складніші структури даних на основі простих. Це питання перевіряє алгоритмічне мислення, креативність та глибоке розуміння властивостей `Stack`/`Deque`.*

* * *
