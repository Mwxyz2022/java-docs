---
layout: default
title: "Клас Stack та принцип LIFO"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 11
---

# Клас Stack та принцип LIFO

* короткий опис: `Stack` – це клас (`java.util`) зі структури даних, що реалізує поведінку `LIFO` (Last-In-First-Out: останній прийшов – перший пішов). Є підкласом `Vector`.

* поняття: `Stack`, `LIFO`, `push()` (додати), `pop()` (видалити і повернути), `peek()` (повернути без видалення), `empty()`, `EmptyStackException`.

* приклади: `browserHistory.push("google.com"); browserHistory.pop();`.

* ключові моменти: `LIFO` порядок. `pop()`/`peek()` на порожньому стеку викидає `EmptyStackException`. Вважається `legacy` класом через успадкування `Vector` (синхронізований, менш ефективний).

* що важливо знати на співбесіді: Принцип `LIFO`, основні методи, чому `Stack` вважається `legacy`.

### **✅ Офіційне визначення:**

**`Stack`** — це **клас зі структури даних**, що реалізує поведінку "останній прийшов — перший вийшов" (LIFO — Last In, First Out). У Java **`Stack`** є підкласом **`Vector`**.

### **🧠 Простими словами:**

Усі нові елементи кладуться **поверх** попередніх — і витягуються **зверху** першими. Як стопка тарілок або млинців. 😋

---

## **Принцип роботи: LIFO**

| Операція | Приклад дій |
| :---: | :---: |
| **`push`** | Покласти елемент на верхівку |
| **`pop`** | Витягти останній доданий |
| **`peek`** | Подивитись на верхівку |

📌 **Останній елемент, який зайшов, — виходить першим.**

---

## **Основні методи класу Stack (🛠️)**

| Метод | Опис |
| :---: | :---: |
| **`push(E item)`** | Додає елемент на верх стеку |
| **`pop()`** | Видаляє та повертає елемент з верхівки |
| **`peek()`** | Повертає верхній елемент, не видаляючи |
| **`empty()`** | Повертає **`true`**, якщо стек порожній |
| **`isEmpty()`** | Синонім до **`empty()`** (загальний з **`Collection`**) |
| **`search(Object o)`** | Повертає позицію елемента (1 — верх), або \-1, якщо не знайдено |

---

## **Обробка `EmptyStackException`**

🔺 Якщо викликати **`pop()`** або **`peek()`** на порожньому стеку -> буде кинуто **`EmptyStackException`**.

```java
Stack<Integer> stack = new Stack<>();
stack.pop(); // ⚠️ java.util.EmptyStackException
```

✅ Рекомендовано перевіряти **`stack.empty()`** перед **`pop()`** або **`peek()`**.

---

## **Приклад використання Stack (🧪)**

```java
Stack<String> browserHistory = new Stack<>();
browserHistory.push("google.com");
browserHistory.push("youtube.com");

System.out.println(browserHistory.peek()); // youtube.com
System.out.println(browserHistory.pop());  // youtube.com
System.out.println(browserHistory.pop());  // google.com
System.out.println(browserHistory.empty()); // true
```

---

## **Практичні сценарії використання Stack**

* 🔁 **Зворотній обхід** (наприклад, реверс рядка)

* 🧭 **Backtracking** (лабіринти, пошук у глибину)

* 🧮 **Обчислення виразів** (postfix notation, калькулятори)

* 🛠️ **Undo/Redo функціональність**

* 🧬 **Парсинг дужок у виразах**

---

## **Чому Stack вважається legacy-класом?**

| Причина | Альтернатива |
| :---: | :---: |
| **Наслідує `Vector` (синхронний)** | Використовує непотрібну синхронізацію |
| **Менш гнучкий** | **`Deque`** (через **`ArrayDeque`**) |
| **Застарілий стиль** | Нові API (Deque) з кращою продуктивністю |

📌 **Рекомендується замінювати `Stack` на `Deque` (`ArrayDeque`)**, коли це можливо.

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
Stack -- СТІЛЕЦЬ зі стопкою книжок:
📚📚📚
➕ push -> покласти нову зверху  
➖ pop -> зняти верхню  
👀 peek -> зазирнути, що там зверху
```

---

## **🟩 Підсумок:**

* **`Stack`** реалізує LIFO.
* Основні методи: **`push`**, **`pop`**, **`peek`**, **`empty`**.
* Сценарії: обхід графів, парсинг виразів, undo/redo.
* Може викидати **`EmptyStackException`**.
* Вважається **legacy**, краще використовувати **`Deque`**.

---

### **🔗 Додаткові джерела:**

* [**Java Stack (Oracle)**](https://docs.oracle.com/javase/8/docs/api/java/util/Stack.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке клас `Stack` і який основний принцип його роботи?

> #### **💬 Відповідь:**
>
> **`Stack`** (Стек) — це класичний клас у `java.util`, який реалізує лінійну структуру даних, що працює за принципом **LIFO (Last-In, First-Out)**, тобто "Останній прийшов — перший пішов".
>
> **Принцип роботи LIFO:**
> Елементи додаються на "вершину" стеку, і з неї ж вони видаляються. Це означає, що елемент, який був доданий останнім, буде оброблений першим.
>
> У Java клас `Stack` є застарілим (legacy), оскільки він успадковує клас `Vector`, що призводить до певних недоліків у дизайні та продуктивності.

#### **📝 Аналогія:**

Уявіть собі стопку тарілок. Ви кладете нову тарілку завжди зверху, і коли вам потрібна тарілка, ви також берете її зверху. Ви не можете взяти тарілку з середини або з самого низу, не знявши верхні. Це і є LIFO.

#### **📌 Що перевіряють:**

*Базове розуміння поняття "стек" та його ключового принципу — LIFO.*

* * *

### **2 ❓ Питання:**

Які основні методи є в класі `Stack` і що вони роблять?

> #### **💬 Відповідь:**
>
> Клас `Stack` надає п'ять основних методів, які повністю описують його функціональність:
>
> * **`push(E item)`**: Додає (кладе) елемент `item` **на вершину** стеку. Повертає сам доданий елемент.
>
> * **`pop()`**: **Видаляє та повертає** елемент з вершини стеку. Якщо стек порожній, кидає `EmptyStackException`.
>
> * **`peek()`**: **Повертає** елемент з вершини стеку, але **не видаляє** його. Якщо стек порожній, також кидає `EmptyStackException`.
>
> * **`empty()`**: Перевіряє, чи є стек порожнім. Повертає `true`, якщо в стеку немає елементів, і `false` в іншому випадку.
>
> * **`search(Object o)`**: Шукає об'єкт у стеку і повертає його позицію (відстань від вершини). Позиція рахується з **1** (вершина — це позиція 1). Якщо об'єкт не знайдено, повертає `-1`.

#### **📌 Що перевіряють:**

*Знання базового API класу `Stack`. Це фундаментальні методи, без яких неможливо працювати з цією структурою даних.*

* * *

### **3 ❓ Питання:**

Наведіть простий приклад використання `Stack` для реалізації історії переглядів у браузері.

> #### **💬 Відповідь:**
>
> Стек ідеально підходить для моделювання функції "Назад" у браузері. Кожна нова відвідана сторінка кладеться на вершину стеку. Коли користувач натискає "Назад", остання сторінка витягується зі стеку.

#### **📝 Приклад:**

```java
import java.util.Stack;

public class BrowserHistoryDemo {
    public static void main(String[] args) {
        Stack<String> history = new Stack<>();

        // Користувач відвідує сторінки
        history.push("google.com");
        history.push("wikipedia.org");
        history.push("github.com");
        System.out.println("Поточна сторінка (вершина стеку): " + history.peek());

        // Користувач натискає "Назад"
        String previousPage = history.pop();
        System.out.println("Повертаємося на: " + previousPage); // github.com
        System.out.println("Тепер поточна сторінка: " + history.peek()); // wikipedia.org

        // Ще раз натискає "Назад"
        history.pop();
        
        // Перевіряємо, чи порожня історія
        if (!history.empty()) {
            System.out.println("Залишилась сторінка: " + history.peek());
        }
    }
}
```

#### **📌 Що перевіряють:**

*Здатність застосувати концепцію стеку для вирішення простої практичної задачі.*

* * *

### **4 ❓ Питання:**

Що станеться, якщо викликати методи `pop()` або `peek()` на порожньому стеку?

> #### **💬 Відповідь:**
>
> Якщо спробувати викликати `pop()` або `peek()` на стеку, який не містить жодного елемента, буде викинуто виняток **`java.util.EmptyStackException`**.
>
> Це неперевірюваний виняток (unchecked exception), тому компілятор не змушує вас обробляти його в блоці `try-catch`. Однак, це призведе до аварійного завершення роботи програми, якщо його не перехопити.
>
> Щоб уникнути цього винятку, **завжди** перевіряйте, чи не порожній стек, перш ніж викликати `pop()` або `peek()`, за допомогою методу `empty()` (або `isEmpty()`).

#### **📝 Приклад (правильний підхід):**

```java
Stack<Integer> stack = new Stack<>();
// ... якась логіка

if (!stack.empty()) {
    Integer topElement = stack.pop();
    System.out.println("Вилучено елемент: " + topElement);
} else {
    System.out.println("Стек порожній, нічого вилучати.");
}
```

#### **📌 Що перевіряють:**

*Знання механізму обробки помилок в класі `Stack` та вміння писати безпечний код, що уникає поширених винятків.*

* * *

### **5 ❓ Питання:**

Чому клас `Stack` вважається застарілим (legacy) і не рекомендується до використання в новому коді?

> #### **💬 Відповідь:**
>
> `Stack` вважається застарілим через серйозні недоліки в його дизайні, які випливають з того, що він **успадковує клас `Vector`**:
>
> 1. **Непотрібна синхронізація:** `Vector` є потокобезпечним класом, усі його методи (включаючи `push`, `pop`) позначені як `synchronized`. Це створює значні накладні витрати на продуктивність, навіть якщо стек використовується в одному потоці. У більшості випадків така синхронізація не потрібна.
>
> 2. **Порушення принципу "композиція важливіша за наслідування":** Через наслідування `Stack` отримує доступ до всіх публічних методів `Vector`, таких як `add(index, element)`, `remove(index)` тощо. Це дозволяє маніпулювати елементами в середині стеку, що повністю **руйнує принцип LIFO**. Хороший дизайн мав би використовувати композицію, приховуючи непотрібні методи.

#### **📝 Коментар:**

Замість `Stack` завжди рекомендується використовувати інтерфейс `Deque` та його реалізацію `ArrayDeque`, яка є швидшою і має кращий дизайн.

#### **📌 Що перевіряють:**

*Розуміння недоліків застарілих класів, знання принципів об'єктно-орієнтованого дизайну та сучасних альтернатив у Java Collections Framework.*

* * *

### **6 ❓ Питання:**

Що краще використовувати замість `Stack` в сучасному Java-коді?

> #### **💬 Відповідь:**
>
> Найкращою сучасною альтернативою для `Stack` є інтерфейс **`Deque`** (двостороння черга) та його реалізація **`ArrayDeque`**.
>
> **Чому `Deque` / `ArrayDeque` краще:**
>
> * **Має повний API для стеку:** `Deque` надає методи, що повністю емулюють поведінку стеку: `push()`, `pop()`, `peek()`.
> * **Вища продуктивність:** `ArrayDeque` не є синхронізованим, тому він значно швидший для однопотокових застосувань.
> * **Кращий дизайн:** API `Deque` є чистішим і не "протікає" непотрібними методами.
> * **Більша гнучкість:** `Deque` можна використовувати як стек (LIFO) і як чергу (FIFO).

#### **📝 Приклад (заміна `Stack` на `Deque`):**

```java
// Старий стиль
Stack<Integer> oldStack = new Stack<>();
oldStack.push(10);

// Сучасний, рекомендований стиль
Deque<Integer> newStack = new ArrayDeque<>();
newStack.push(10); // push() є частиною Deque API
```

#### **📌 Що перевіряють:**

*Практичні знання сучасних API та здатність писати код, що відповідає сучасним стандартам Java.*

* * *

### **7 ❓ Питання:**

Чим відрізняються методи `empty()` та `isEmpty()` в класі `Stack`?

> #### **💬 Відповідь:**
>
> Функціонально, ці методи роблять **абсолютно те ж саме** — перевіряють, чи порожній стек.
>
> Різниця полягає в їхньому походженні:
>
> * **`empty()`**: Це "рідний" метод, оголошений **безпосередньо в класі `Stack`**. Він існує з перших версій Java.
>
> * **`isEmpty()`**: Цей метод `Stack` **успадковує від свого батьківського класу `Vector`**, який, у свою чергу, реалізує його з інтерфейсу `Collection`.
>
> Хоча `isEmpty()` є стандартним для всього Collections Framework, багато розробників за звичкою використовують `empty()`, оскільки він є частиною оригінального API стеку. Обидва варіанти є правильними.

#### **📌 Що перевіряють:**

*Уважність до деталей API. Це питання-дрібниця, але відповідь на нього показує, наскільки глибоко кандидат вивчав клас `Stack`.*

* * *

### **8 ❓ Питання:**

Чи є `Stack` потокобезпечним (thread-safe)? Які наслідки це має? ⭐️

> #### **💬 Відповідь:**
>
> **Так, клас `Stack` є потокобезпечним.**
>
> Це тому, що він успадковує клас `Vector`, а всі методи `Vector`, що модифікують стан (включаючи `push`, `pop`, `peek`), позначені ключовим словом **`synchronized`**. Це означає, що тільки один потік може виконувати ці методи на одному екземплярі стеку в один і той же час.
>
> **Наслідки:**
>
> 1. **Негативний вплив на продуктивність:** Механізм синхронізації (блокування) є відносно дорогою операцією. Якщо стек використовується в **однопотоковому** середовищі, ці витрати на блокування є **абсолютно непотрібними** і лише сповільнюють програму.
>
> 2. **Зайва безпека:** У більшості випадків, коли потрібен стек, потокобезпечність не є вимогою.
>
> Саме цей аспект є однією з головних причин, чому `Stack` вважається застарілим і рекомендується використовувати несинхронізований `ArrayDeque`. Якщо ж потрібна потокобезпечна реалізація, краще використовувати спеціалізовані конкурентні колекції, такі як `ConcurrentLinkedDeque`.

#### **📌 Що перевіряють:**

*Знання багатопотоковості, розуміння вартості синхронізації та її доцільності. Кандидат повинен розуміти компроміси між безпекою та продуктивністю.*

* * *

### **9 ❓ Питання:**

Як використовувати стек для перевірки збалансованості дужок у рядку (наприклад, `"{[()]}"`)? ⭐️

> #### **💬 Відповідь:**
>
> Стек є ідеальною структурою даних для цієї задачі, оскільки дужки мають вкладену, LIFO-подібну структуру: дужка, що відкрилася останньою, має закритися першою.
>
> **Алгоритм:**
>
> 1. Створити порожній стек.
> 2. Проходити по кожному символу в рядку.
> 3. Якщо символ — це **відкриваюча дужка** (`(`, `[`, `{`), додати її на вершину стеку (`push`).
> 4. Якщо символ — це **закриваюча дужка** (`)`, `]`, `}`):
>     а. Перевірити, чи не порожній стек. Якщо так — вираз незбалансований (закриваюча дужка без відкриваючої).
>     б. Витягти зі стеку останню відкриваючу дужку (`pop`).
>     в. Перевірити, чи відповідає витягнута дужка поточній закриваючій (напр., `(` для `)`). Якщо ні — вираз незбалансований.
> 5. Після проходження всього рядка, **стек має бути порожнім**. Якщо в ньому залишилися відкриваючі дужки — вираз незбалансований.

#### **📝 Приклад коду:**

```java
public boolean isBalanced(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    for (char c : s.toCharArray()) {
        if (c == '(' || c == '[' || c == '{') {
            stack.push(c);
        } else if (c == ')' && !stack.isEmpty() && stack.peek() == '(') {
            stack.pop();
        } else if (c == ']' && !stack.isEmpty() && stack.peek() == '[') {
            stack.pop();
        } else if (c == '}' && !stack.isEmpty() && stack.peek() == '{') {
            stack.pop();
        } else {
            return false; // Незакрита або невірна дужка
        }
    }
    return stack.isEmpty();
}
```

#### **📌 Що перевіряють:**

*Здатність застосовувати структури даних для вирішення класичних алгоритмічних задач. Це популярне питання на технічних співбесідах.*

* * *

### **10 ❓ Питання:**

Назвіть переваги використання `Deque` замість `Stack` з точки зору дизайну API. ⭐️

> #### **💬 Відповідь:**
>
> Переваги `Deque` в дизайні API є значними:
>
> 1. **Програмування на основі інтерфейсу:** `Deque` — це **інтерфейс**, а `Stack` — **клас**. Програмування з використанням інтерфейсів (`Deque<Integer> stack = new ArrayDeque<>();`) є набагато гнучкішим. Це дозволяє легко змінити реалізацію (напр., з `ArrayDeque` на `LinkedList`) без зміни решти коду.
>
> 2. **Чистота API:** `Deque` надає чітко визначений набір операцій для двосторонньої черги. На відміну від `Stack`, він не успадковує безліч методів від `Vector` (`elementAt`, `indexOf`, etc.), які не стосуються семантики стеку. Це робить API `Deque` чистішим і менш схильним до неправильного використання.
>
> 3. **Краща обробка помилок:** API `Deque` пропонує подвійний набір методів: один кидає винятки (`addFirst`, `removeFirst`), а інший повертає спеціальні значення (`offerFirst`, `pollFirst`). Це дає розробнику більше гнучкості в обробці крайніх випадків, ніж `Stack`, який завжди кидає `EmptyStackException`.

#### **📌 Що перевіряють:**

*Розуміння принципів хорошого дизайну API, таких як "програмування на основі інтерфейсів", гнучкість та чистота. Це ознака досвідченого розробника.*

* * *

### **11 ❓ Питання:**

Як реалізовано метод `search(Object o)` і яка його часова складність? ⭐️

> #### **💬 Відповідь:**
>
> Метод `search(Object o)` шукає перше входження об'єкта в стек, починаючи з вершини, і повертає його "відстань" від вершини.
>
> **Як реалізовано:**
> `Stack`, будучи нащадком `Vector`, використовує його внутрішню реалізацію. По суті, `search` викликає `lastIndexOf(o)` на внутрішньому масиві `Vector`. `lastIndexOf` виконує **лінійний пошук** по масиву, рухаючись від кінця до початку.
>
> **Часова складність: O(n)**, де `n` — кількість елементів у стеку.
>
> У гіршому випадку (елемент знаходиться внизу стеку або відсутній), метод мусить перебрати всі `n` елементів. Це робить `search` дуже неефективною операцією, яка, до того ж, не є типовою для семантики стеку. У більшості випадків доступ має бути лише до верхнього елемента (O(1)).

#### **📌 Що перевіряють:**

*Увагу до деталей API та розуміння часової складності операцій. Не всі методи в класі, що здається швидким, є такими.*

* * *

### **12 ❓ Питання:**

Як стек пов'язаний з рекурсією та викликами методів у JVM? ⭐️

> #### **💬 Відповідь:**
>
> Стек є фундаментальною концепцією, на якій побудована робота з викликами методів у JVM.
>
> **Call Stack (Стек викликів):**
> Кожен потік виконання в JVM має свій власний стек, який називається стеком викликів.
>
> 1. Коли викликається метод, для нього створюється **фрейм стеку (stack frame)**, який містить локальні змінні, параметри та адресу повернення. Цей фрейм кладеться **на вершину** стеку викликів.
> 2. Якщо цей метод викликає інший, новий фрейм для другого методу кладеться поверх першого.
> 3. Коли метод завершує свою роботу, його фрейм **видаляється з вершини** стеку (`pop`), і управління повертається до методу, що знаходиться нижче.
>
> **Рекурсія:**
> Рекурсія — це коли метод викликає сам себе. Це просто призводить до того, що на стек викликів кладуться однакові фрейми один на одного. Якщо не буде базового випадку, що зупинить рекурсію, стек врешті-решт переповниться, що призведе до **`StackOverflowError`**.
>
> Будь-яку рекурсивну функцію можна переписати в ітеративну за допомогою явного використання `Stack` для зберігання стану.

#### **📌 Що перевіряють:**

*Глибокі знання з інформатики, розуміння того, як працює JVM, і зв'язок між абстрактними структурами даних та їх реальною реалізацією в середовищі виконання.*

* * *

### **13 ❓ Питання:**

Клас `Stack` успадковує `Vector`. Який ключовий принцип SOLID це порушує? Поясніть. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Успадкування `Stack` від `Vector` є класичним прикладом порушення **Принципу заміщення Лісков (Liskov Substitution Principle - LSP)**.
>
> **Суть LSP:** Об'єкти підкласу повинні бути взаємозамінними з об'єктами батьківського класу без зміни коректності програми. Тобто, якщо у вас є код, що працює з `Vector`, ви маєте могти підставити туди `Stack`, і все повинно працювати як очікувалось.
>
> **Як `Stack` порушує LSP:**
> `Vector` (як і будь-який `List`) має певні інваріанти, наприклад: "Якщо я додам елемент на позицію 0, то при виклику `get(0)` я отримаю цей же елемент".
> `Stack` змінює цю поведінку. `push()` додає елемент в кінець (як `Vector.add()`), а от `pop()` видаляє елемент також з кінця.
>
> Найгірше те, що `Stack` дозволяє викликати методи `Vector`, які суперечать ідеології стеку: `stack.add(0, "someElement")`. Це додає елемент на початок, руйнуючи LIFO. Таким чином, `Stack` не може поводитись як очікуваний `Vector` у всіх контекстах, порушуючи LSP.
>
> Це також порушує **Принцип єдиної відповідальності (SRP)** і **Принцип відкритості/закритості (OCP)**, але LSP є найбільш очевидним порушенням.

#### **📌 Що перевіряють:**

*Знання принципів SOLID та здатність застосовувати їх для аналізу дизайну існуючих класів. Це ознака глибокого розуміння ООП.*

* * *

### **14 ❓ Питання:**

Як реалізувати `Queue` (чергу FIFO) за допомогою двох `Stack`'ів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне алгоритмічне завдання, що демонструє, як можна імітувати одну структуру даних за допомогою іншої.
>
> **Ідея:** Один стек (`inStack`) використовується для операцій додавання, а інший (`outStack`) — для операцій видалення. `outStack` зберігає елементи у зворотному порядку, що дозволяє досягти FIFO.
>
> **Алгоритм:**
>
> 1. **`enqueue` (додавання в чергу):**
>     * Просто `push` елемент у `inStack`.
>
> 2. **`dequeue` (видалення з черги):**
>     * Якщо `outStack` **не порожній**, просто `pop` з нього. Елементи там вже в правильному, реверсованому порядку.
>     * Якщо `outStack` **порожній**, перемістити всі елементи з `inStack` в `outStack` один за одним. Це фактично реверсує порядок елементів. Після цього `pop` з `outStack`.
>     * Якщо обидва стеки порожні, то й черга порожня.

#### **📝 Псевдокод:**

```java
class QueueWithTwoStacks<E> {
    private Stack<E> inStack = new Stack<>();
    private Stack<E> outStack = new Stack<>();

    public void enqueue(E item) {
        inStack.push(item);
    }

    public E dequeue() {
        if (outStack.isEmpty()) {
            while (!inStack.isEmpty()) {
                outStack.push(inStack.pop());
            }
        }
        if (outStack.isEmpty()) {
            throw new NoSuchElementException();
        }
        return outStack.pop();
    }
}
```

#### **📌 Що перевіряють:**

*Алгоритмічне мислення, креативність у вирішенні проблем та глибоке розуміння властивостей LIFO та FIFO.*

* * *

### **15 ❓ Питання:**

Якби ви проектували `Stack` сьогодні з нуля, як би він виглядав? Опишіть його API (інтерфейс та клас). ⭐️⭐️

> #### **💬 Відповідь:**
>
> Сучасний дизайн `Stack` був би зосереджений на чистоті API, гнучкості та продуктивності, уникаючи помилок минулого. Він би був реалізований на основі інтерфейсу та композиції.
>
> **1. Інтерфейс `Stack<E>`:**
>
> ```java
> public interface Stack<E> {
>     void push(E element);      // Додати елемент
>     E pop();                   // Видалити та повернути (кидає виняток)
>     E peek();                  // Переглянути (кидає виняток)
>     boolean isEmpty();         // Перевірити на порожнечу
>     int size();                // Повернути розмір
>
>     // Можливо, додати версії, що не кидають винятки, для гнучкості
>     Optional<E> poll(); // Альтернатива pop()
>     Optional<E> peekOptional(); // Альтернатива peek()
> }
> ```
>
> Це дозволило б програмувати на основі інтерфейсу.
>
> **2. Клас `ArrayStack<E>`:**
>
> ```java
> public class ArrayStack<E> implements Stack<E> {
>     // Використання композиції, а не наслідування
>     private Deque<E> delegate = new ArrayDeque<>();
>
>     @Override
>     public void push(E element) { delegate.push(element); }
>
>     @Override
>     public E pop() {
>         if (isEmpty()) throw new EmptyStackException();
>         return delegate.pop();
>     }
>
>     //... і так далі для інших методів ...
> }
> ```
>
> **Ключові риси дизайну:**
>
> * **Програмування через інтерфейс:** Використовуємо `Stack<E> stack = new ArrayStack<>();`.
> * **Композиція:** Клас `ArrayStack` **використовує** `ArrayDeque` (делегує йому роботу), але не **є** `ArrayDeque`. Це приховує непотрібні методи.
> * **Відсутність синхронізації:** Немає зайвих накладних витрат. Для потокобезпечності можна було б створити окрему обгортку `SynchronizedStack` або `ConcurrentArrayStack`.
> * **Чисте API:** Назовні виставлені тільки ті методи, які відповідають семантиці стеку.

#### **📌 Що перевіряють:**

*Здатність до проектування API, розуміння сучасних практик розробки ПЗ (інтерфейси, композиція, чисте API) та вміння робити висновки з помилок у дизайні існуючих бібліотек.*

* * *
