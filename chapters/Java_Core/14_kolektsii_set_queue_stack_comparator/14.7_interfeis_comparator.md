---
layout: default
title: "Інтерфейс Comparator"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 7
---

# Інтерфейс Comparator

* короткий опис: Інтерфейс `Comparator<T>` (`java.util`) дозволяє визначити зовнішній (кастомний) порядок сортування для об'єктів, не змінюючи їхній клас. Містить метод `compare(T o1, T o2)`.

* поняття: `Comparator<T>`, `compare(T o1, T o2)`, `thenComparing()` (комбінування), `comparing()` (статичний метод для створення компаратора), `reversed()`.

* приклади: `Comparator.comparing(Person::getAge)`, використання з `TreeSet` або `Collections.sort()`.

* ключові моменти: Зовнішній порядок, можна створювати багато `Comparator` для одного класу. Не змінює оригінальний клас.

* що важливо знати на співбесіді: Призначення, відмінність від `Comparable`, як комбінувати.

### **✅ Офіційне визначення:**

Інтерфейс **`Comparator<T>`** дозволяє визначити **зовнішній (кастомний) порядок сортування** для об'єктів. Він містить метод **`compare(T o1, T o2)`**.

### **🧠 Простими словами:**

**`Comparator`** — це **сторонній суддя**, який оцінює два об'єкти та каже: **"Цей кращий"**, навіть якщо сам об'єкт нічого про себе не знає

---

## **Основні методи**

| Метод | Призначення |
| :---: | :---: |
| **`compare(T o1, T o2)`** | Порівнює два об'єкти |
| **`reversed()`** | Створює зворотний компаратор |
| **`thenComparing()`** | Комбінує кілька компараторів |
| **`comparing(Function)`** | Статичний метод Java 8 для швидкого створення компаратора |

---

## **Пояснення на пальцях (💡)**

Якщо **`Comparable`** — це **вбудований навігатор**, то **`Comparator`** — **GPS-навігатор сторонньої фірми**, який дозволяє сортувати **по-різному, коли захочеш**. Наприклад, сортувати студентів по балу, потім по імені, потім по даті народження — і все це без зміни класу **`Student`**

---

## **Корисні приклади (🧪)**

#### **✅ Клас без `Comparable`:**

```java
class Student {
    String name;
    int grade;

    Student(String name, int grade) {
        this.name = name;
        this.grade = grade;
    }

    public String toString() {
        return name + " (" + grade + ")";
    }
}
```

#### **✅ КомPARATOR за оцінкою:**

```java
Comparator<Student> byGrade = (s1, s2) -> Integer.compare(s1.grade, s2.grade);
```

#### **✅ КомPARATOR за іменем:**

```java
Comparator<Student> byName = Comparator.comparing(s -> s.name);
```

#### **✅ TreeSet з Comparator:**

```java
TreeSet<Student> students = new TreeSet<>(byGrade);
students.add(new Student("Alice", 85));
students.add(new Student("Bob", 92));
students.add(new Student("Charlie", 78));
System.out.println(students);
```

---

## **Порівняння Comparator vs Comparable (⚖️)**

| Параметр | `Comparable` | `Comparator` |
| :---: | :---: | :---: |
| **Порівняння** | Всередині класу | Зовні класу |
| **Метод** | **`compareTo(T o)`** | **`compare(T o1, T o2)`** |
| **Кількість порядків** | Один (природний) | Багато (можна створювати скільки завгодно) |
| **Потрібна модифікація класу** | Так | Ні |
| **Підходить для** | **`Collections.sort()`, `TreeSet`** | **`Collections.sort()`, `TreeSet(Comparator)`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"COMPARATOR \= CO-MPARING TWO OBJECTS"**

* **`compare(o1, o2)`** — порівнює **два об’єкти**
* Клас **не обов’язково** має реалізовувати інтерфейс

---

## **🟩 Підсумок:**

* **`Comparator`** дає **зовнішній** спосіб сортувати обʼєкти.
* Він зручний, коли:
  * клас не твій
  * треба кілька способів сортування
* Гнучко використовується з **`TreeSet`**, **`Collections.sort()`**, **`Stream.sorted()`**.
* Можна комбінувати компаратори, інвертувати порядок.

---

### **🔗 Додаткові джерела:**

* [**Java Docs: Comparator**](https://docs.oracle.com/javase/8/docs/api/java/util/Comparator.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс `Comparator` і для чого він використовується?

> #### **💬 Відповідь:**
>
> **`Comparator<T>`** — це стандартний інтерфейс в `java.util`, призначений для визначення **зовнішньої, користувацької логіки сортування** для об'єктів. На відміну від `Comparable`, `Comparator` є окремим об'єктом, що не є частиною класу, об'єкти якого потрібно сортувати.
>
> Він містить ключовий метод: **`int compare(T o1, T o2)`**.
>
> `Comparator` використовується, коли:
>
> * Клас об'єктів не реалізує `Comparable`.
> * Ви не можете змінити вихідний код класу (наприклад, це клас із зовнішньої бібліотеки).
> * Вам потрібно **кілька різних способів сортування** для одного й того ж класу (наприклад, сортувати користувачів за іменем, за віком, за датою реєстрації).
> * Вам потрібна тимчасова, специфічна логіка сортування.

#### **📝 Аналогія:**

Якщо `Comparable` — це вміння кожної людини самостійно сказати, хто з двох вищий, то `Comparator` — це зовнішній суддя на змаганні. Сьогодні цей суддя оцінює спортсменів за швидкістю, завтра — за силою, а післязавтра — за технікою. Спортсмени ті ж самі, але критерії оцінки (сортування) різні, і їх визначає суддя.

#### **📌 Що перевіряють:**

*Розуміння ролі `Comparator` як інструменту для гнучкого, зовнішнього сортування та його основних сценаріїв використання.*

* * *

### **2 ❓ Питання:**

Поясніть контракт методу `compare()`. Чим він відрізняється від `compareTo()`?

> #### **💬 Відповідь:**
>
> Метод `int compare(T o1, T o2)` порівнює **два зовнішні об'єкти** (`o1` та `o2`), на відміну від `compareTo()`, який порівнює поточний об'єкт (`this`) з іншим.
>
> Контракт `compare` такий самий, як у `compareTo`:
>
> * **Від'ємне число:** `o1` є **меншим** за `o2`.
> * **Нуль:** `o1` та `o2` є **рівними** з точки зору порядку сортування.
> * **Додатне число:** `o1` є **більшим** за `o2`.
>
> **Ключові відмінності від `compareTo()`:**
>
> 1. **Кількість аргументів:** `compare(o1, o2)` приймає два аргументи, `compareTo(other)` — один.
> 2. **Місце реалізації:** `compare` реалізується в окремому класі-компараторі, `compareTo` — всередині самого класу, що сортується.
> 3. **Виклик:** `compare` не викликається на самому об'єкті, а передає два об'єкти як параметри: `comparator.compare(o1, o2)`.

#### **📌 Що перевіряють:**

*Чітке розмежування сигнатур та призначення методів `compare()` та `compareTo()`. Це фундаментальне питання для теми сортування.*

* * *

### **3 ❓ Питання:**

Наведіть приклад створення `Comparator` за допомогою лямбда-виразу.

> #### **💬 Відповідь:**
>
> Починаючи з Java 8, `Comparator` є функціональним інтерфейсом, тому його найзручніше створювати за допомогою лямбда-виразів. Це робить код значно компактнішим та читабельнішим.
>
> Розглянемо клас `Book`, який ми хочемо сортувати за кількістю сторінок.

#### **📝 Приклад:**

```java
import java.util.*;

class Book {
    String title;
    int pages;

    public Book(String title, int pages) {
        this.title = title;
        this.pages = pages;
    }
    public String toString() { return title + " (" + pages + " pages)"; }
}

public class ComparatorLambdaExample {
    public static void main(String[] args) {
        List<Book> books = new ArrayList<>();
        books.add(new Book("The Hobbit", 310));
        books.add(new Book("1984", 328));
        books.add(new Book("A Brief History of Time", 256));

        // Створення Comparator за допомогою лямбда-виразу
        Comparator<Book> byPagesComparator = (book1, book2) -> Integer.compare(book1.pages, book2.pages);

        // Використання з Collections.sort()
        books.sort(byPagesComparator);

        System.out.println(books);
        // Виведе: [A Brief History of Time (256 pages), The Hobbit (310 pages), 1984 (328 pages)]
    }
}
```

#### **📌 Що перевіряють:**

*Володіння сучасним синтаксисом Java 8+ та вміння застосовувати функціональний підхід для розповсюджених завдань.*

* * *

### **4 ❓ Питання:**

Що таке статичні методи `Comparator.comparing()` та `thenComparing()`, і як вони спрощують написання компараторів?

> #### **💬 Відповідь:**
>
> `comparing()` та `thenComparing()` — це статичні та `default` методи-помічники (helper methods) в інтерфейсі `Comparator`, введені в Java 8, які дозволяють створювати та комбінувати компаратори в декларативному стилі.
>
> 1. **`Comparator.comparing(Function<T, U> keyExtractor)`:**
>     * **Що робить:** Створює `Comparator`, який сортує об'єкти за певним "ключем". Ви передаєте йому функцію, яка витягує з об'єкта ключ для сортування (наприклад, `Person::getAge`).
>     * **Спрощення:** Позбавляє потреби вручну писати лямбда-вираз `(o1, o2) -> ...` для простих порівнянь.
>
> 2. **`thenComparing(Comparator<? super T> other)`:**
>     * **Що робить:** Дозволяє створити ланцюжок порівнянь. Якщо попередній компаратор повернув `0` (об'єкти рівні), то для визначення порядку буде використано компаратор, переданий в `thenComparing()`.
>     * **Спрощення:** Замінює громіздкі `if-else` конструкції в методі `compare` для багаторівневого сортування.

#### **📝 Приклад:**

```java
// Сортування співробітників за зарплатою, а при рівній зарплаті — за іменем
Comparator<Employee> complexComparator = Comparator.comparing(Employee::getSalary)
                                                      .thenComparing(Employee::getName);
```

Цей код є значно читабельнішим за ручну реалізацію з `if-else`.

#### **📌 Що перевіряють:**

*Знання сучасного, ідіоматичного API для роботи з `Comparator`. Кандидат повинен демонструвати вміння писати чистий, стислий та декларативний код.*

* * *

### **5 ❓ Питання:**

Як відсортувати колекцію у зворотному порядку за допомогою `Comparator`?

> #### **💬 Відповідь:**
>
> Існує кілька способів, але найпростіший і найкращий — використати `default` метод **`reversed()`**, який доступний на будь-якому екземплярі `Comparator`.
>
> Метод `reversed()` повертає новий `Comparator`, який інвертує логіку оригінального.
>
> **Альтернатива:**
> Також можна використати статичний метод `Comparator.reverseOrder()`, який повертає компаратор, що реалізує зворотний до природного порядок (тобто, працює для об'єктів, що реалізують `Comparable`).

#### **📝 Приклад:**

```java
List<String> names = new ArrayList<>(Arrays.asList("Alice", "Charlie", "Bob"));

// Створюємо компаратор для природного порядку
Comparator<String> naturalOrder = Comparator.naturalOrder();

// Інвертуємо його
Comparator<String> reverseOrder = naturalOrder.reversed();

names.sort(reverseOrder);
System.out.println(names); // [Charlie, Bob, Alice]

// Більш стислий варіант
names.sort(Comparator.reverseOrder());
```

#### **📌 Що перевіряють:**

*Знання корисних `default` методів в інтерфейсах та вміння виконувати поширені завдання (як-от зворотне сортування) найбільш ефективним способом.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати `Comparator` для сортування об'єктів класу, який вже реалізує `Comparable`?

> #### **💬 Відповідь:**
>
> Так, можна, і це дуже поширена практика. Якщо ви передасте `Comparator` в конструктор `TreeSet` або в метод `sort()`, то він буде мати **вищий пріоритет** і буде використаний замість "природного" порядку, визначеного в `Comparable`.
>
> `Comparator` дозволяє **перевизначити** природний порядок для конкретної операції сортування.
>
> **Приклад:** Клас `String` реалізує `Comparable` і сортується лексикографічно. Але ми можемо легко відсортувати список рядків за довжиною, передавши відповідний `Comparator`.

#### **📝 Приклад:**

```java
List<String> names = new ArrayList<>(Arrays.asList("Eve", "Alice", "David"));

// Сортування за природним порядком (Comparable)
Collections.sort(names);
System.out.println("За Comparable: " + names); // [Alice, David, Eve]

// Сортування за довжиною (Comparator)
Comparator<String> byLength = Comparator.comparing(String::length);
names.sort(byLength);
System.out.println("За Comparator: " + names); // [Eve, Alice, David]
```

#### **📌 Що перевіряють:**

*Розуміння пріоритетів механізмів сортування: зовнішній `Comparator` завжди "перемагає" внутрішній `Comparable`.*

* * *

### **7 ❓ Питання:**

Як створити `Comparator`, що обробляє `null` значення? ⭐️

> #### **💬 Відповідь:**
>
> Стандартні компаратори, створені через `Comparator.comparing()`, викинуть `NullPointerException`, якщо функція-екстрактор ключа поверне `null`.
>
> Для коректної обробки `null` значень інтерфейс `Comparator` надає два статичні методи-помічники:
>
> 1. **`Comparator.nullsFirst(Comparator<? super T> comparator)`**: Створює "обгортку" над існуючим компаратором. Ця обгортка вважає `null` **меншим** за будь-який не-`null` об'єкт, розміщуючи `null`-и на початку відсортованої колекції.
>
> 2. **`Comparator.nullsLast(Comparator<? super T> comparator)`**: Робить те саме, але вважає `null` **більшим** за будь-який не-`null` об'єкт, розміщуючи `null`-и в кінці.

#### **📝 Приклад:**

```java
List<String> list = new ArrayList<>(Arrays.asList("b", null, "a", "c"));

// Сортування, де null йде першим
list.sort(Comparator.nullsFirst(Comparator.naturalOrder()));
System.out.println(list); // [null, a, b, c]

// Сортування, де null йде останнім
list.sort(Comparator.nullsLast(Comparator.naturalOrder()));
System.out.println(list); // [a, b, c, null]
```

#### **📌 Що перевіряють:**

*Знання розширеного API `Comparator` та вміння писати надійний код, що коректно обробляє крайні випадки (edge cases), такі як `null` значення.*

* * *

### **8 ❓ Питання:**

Напишіть `Comparator` для сортування `User`'ів спочатку за статусом (активні спочатку), а потім за спаданням віку. ⭐️

> #### **💬 Відповідь:**
>
> Це завдання на багаторівневе сортування, яке ідеально вирішується за допомогою ланцюжка `comparing()` та `thenComparing()`.

#### **📝 Приклад:**

```java
class User {
    boolean isActive;
    int age;
    // ... конструктор, геттери
}

// Активні користувачі (isActive=true) мають йти першими.
// В булевому сортуванні `false` < `true`, тому нам потрібен зворотний порядок.
Comparator<User> userComparator = Comparator.comparing(User::isActive).reversed()
                                           .thenComparing(Comparator.comparing(User::getAge).reversed());

// Розберемо детальніше:
// 1. Comparator.comparing(User::isActive) -> сортує за статусом, false йдуть першими.
// 2. .reversed() -> інвертуємо, тепер true йдуть першими (активні попереду).
// 3. .thenComparing(...) -> якщо статуси однакові...
// 4. Comparator.comparing(User::getAge) -> ...сортуємо за віком за зростанням...
// 5. .reversed() -> ...інвертуємо, щоб сортувати за спаданням.
```

#### **📌 Що перевіряють:**

*Вміння комбінувати різні компаратори, включаючи зворотне сортування, для створення складної бізнес-логіки. Це демонструє впевнене володіння інструментом.*

* * *

### **9 ❓ Питання:**

Чи можна реалізувати `Comparator` за допомогою анонімного внутрішнього класу? Коли це може бути доречно? ⭐️

> #### **💬 Відповідь:**
>
> Так, до появи лямбда-виразів в Java 8 це був стандартний спосіб створення екземпляра `Comparator`.
>
> **Анонімний клас** — це клас без імені, який оголошується та ініціалізується в одному виразі.
>
> **Коли це доречно сьогодні?**
> Використання анонімних класів замість лямбд має сенс у дуже рідкісних випадках:
>
> 1. Коли вам потрібно реалізувати компаратор, який має **стан** (поля), що неможливо в лямбда-виразі.
> 2. Коли вам потрібно реалізувати, крім `compare()`, ще й інші методи (хоча це зазвичай ознака поганого дизайну).
> 3. Коли ви працюєте на дуже старій версії Java (до 8-ої).
>
> В 99% сучасних випадків **лямбда-вираз є кращим вибором**, оскільки він значно компактніший та виразніший.

#### **📝 Приклад:**

```java
List<String> names = ...;

// Реалізація за допомогою анонімного класу (старий стиль)
names.sort(new Comparator<String>() {
    @Override
    public int compare(String o1, String o2) {
        return Integer.compare(o1.length(), o2.length());
    }
});

// Еквівалентна реалізація за допомогою лямбди (сучасний стиль)
names.sort((o1, o2) -> Integer.compare(o1.length(), o2.length()));
```

#### **📌 Що перевіряють:**

*Розуміння еволюції мови Java. Кандидат повинен знати про старіші підходи та вміти пояснити, чому сучасні є кращими.*

* * *

### **10 ❓ Питання:**

Яка часова складність сортування колекції `ArrayList` за допомогою `Collections.sort(list, comparator)`? ⭐️

> #### **💬 Відповідь:**
>
> Часова складність сортування `ArrayList` за допомогою `Collections.sort()` становить **O(n log n)**.
>
> **Пояснення:**
> Під капотом `Collections.sort()` (а точніше, `List.sort()`, який він викликає з Java 8) використовує високоефективний алгоритм сортування під назвою **Timsort**.
>
> **Timsort** — це гібридний стабільний алгоритм, що поєднує в собі:
>
> * **Сортування вставками (Insertion Sort):** Дуже ефективне для невеликих масивів.
> * **Сортування злиттям (Merge Sort):** Ефективне для великих масивів, гарантує складність O(n log n) у найгіршому випадку.
>
> `Comparator` сам по собі не впливає на асимптотичну складність *алгоритму сортування*, але впливає на загальний час виконання, оскільки метод `compare` викликається `O(n log n)` разів. Якщо `compare` буде повільним, то і все сортування буде повільним.

#### **📌 Що перевіряють:**

*Знання алгоритмів, що використовуються в стандартній бібліотеці Java, та їхньої часової складності.*

* * *

### **11 ❓ Питання:**

Поясніть різницю між `Comparator.naturalOrder()` та `Comparator.reverseOrder()`. ⭐️

> #### **💬 Відповідь:**
>
> Це два статичні методи-помічники, що створюють компаратори для роботи з об'єктами, які реалізують `Comparable`.
>
> 1. **`Comparator.naturalOrder()`**:
>     * **Що робить:** Повертає `Comparator`, який сортує об'єкти відповідно до їхнього **природного порядку**, тобто так, як визначено в їхньому методі `compareTo()`.
>     * **Коли використовується:** Рідко використовується напряму, оскільки `Collections.sort(list)` вже робить це за замовчуванням. Може бути корисним для ясності коду або як основа для подальших операцій, наприклад `Comparator.naturalOrder().reversed()`.
>
> 2. **`Comparator.reverseOrder()`**:
>     * **Що робить:** Повертає `Comparator`, який сортує об'єкти в **зворотному до природного** порядку.
>     * **Коли використовується:** Це найпростіший спосіб відсортувати `Comparable` об'єкти за спаданням.
>
> **Коротко:** `reverseOrder()` — це, по суті, еквівалент `naturalOrder().reversed()`.

#### **📌 Що перевіряють:**

*Детальне знання API `Comparator` та вміння обирати найпростіший та найчитабельніший спосіб для вирішення задачі.*

* * *

### **12 ❓ Питання:**

Назвіть переваги використання Method Reference (напр. `User::getAge`) з `Comparator.comparing()` порівняно з лямбда-виразом (`u -> u.getAge()`). ⭐️

> #### **💬 Відповідь:**
>
> Використання посилань на методи (Method References) є кращою практикою, коли це можливо, з кількох причин:
>
> 1. **Читабельність та виразність:** Посилання на метод `User::getAge` є більш декларативним. Воно говорить, *що* потрібно зробити (взяти вік), а не *як* (взяти `u` і викликати `u.getAge()`). Це робить код чистішим і ближчим до природної мови.
>
> 2. **Стислість:** Посилання на метод зазвичай коротше, ніж еквівалентний лямбда-вираз.
>
> 3. **Краща інформація про помилки:** Якщо метод, на який ви посилаєтесь, кидає перевірюваний виняток, компілятор видасть більш чітку помилку. З лямбдами це може бути менш очевидно.
>
> 4. **Потенційна оптимізація:** Хоча на практиці різниця зазвичай мізерна, JVM потенційно може краще оптимізувати посилання на методи, оскільки вони надають більше інформації про те, який саме код буде виконано.
>
> Лямбда-вираз слід використовувати тоді, коли логіка витягування ключа є складнішою, ніж простий виклик одного методу, наприклад `u -> u.getFirstName() + " " + u.getLastName()`.

#### **📌 Що перевіряють:**

*Розуміння стилістичних та практичних переваг сучасних функцій Java. Кандидат повинен прагнути писати не просто робочий, а чистий та ідіоматичний код.*

* * *

### **13 ❓ Питання:**

Як написати `Comparator`, який буде стабільним, якщо деякі поля об'єктів рівні? Чому це важливо? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Стабільне сортування** — це сортування, яке не змінює відносний порядок рівних елементів. `Collections.sort` та `Arrays.sort` (Timsort) є стабільними.
>
> **Чому це важливо:** Уявіть, що ви сортуєте список транзакцій спочатку за датою, а потім за сумою. Якщо сортування за сумою не буде стабільним, воно може перемішати транзакції з однаковою сумою, зруйнувавши початковий порядок за датою.
>
> **Як написати стабільний `Comparator`:**
> Щоб компаратор був стабільним, він ніколи не повинен повертати `0` для об'єктів, які не є повністю ідентичними. Для цього, після порівняння за основними критеріями, потрібно додати **"останній" критерій порівняння**, який гарантовано буде унікальним. Це може бути ID об'єкта, індекс у масиві або навіть `System.identityHashCode()`.
>
> **Найкращий спосіб:** Використовувати ланцюжок `thenComparing` доти, доки ви не дійдете до унікального ідентифікатора.

#### **📝 Приклад:**

```java
// Сортування співробітників за зарплатою. Щоб зберегти початковий порядок 
// співробітників з однаковою зарплатою, додаємо сортування за унікальним ID.
Comparator<Employee> stableComparator = Comparator.comparing(Employee::getSalary)
                                                 .thenComparing(Employee::getId); // ID унікальний
```

Цей компаратор гарантує, що якщо `e1.getSalary() == e2.getSalary()`, то їх відносний порядок буде визначений за `Id`, що робить сортування повністю детермінованим і стабільним.

#### **📌 Що перевіряють:**

*Глибоке розуміння характеристик алгоритмів сортування. Це питання виходить за рамки простого використання API і торкається теоретичних аспектів та їх практичного застосування для написання передбачуваного коду.*

* * *

### **14 ❓ Питання:**

Яким чином `default` методи змінили інтерфейс `Comparator` в Java 8? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Default` методи, введені в Java 8, кардинально змінили `Comparator`, перетворивши його з простого функціонального інтерфейсу на потужний інструмент для **композиції та маніпуляції логікою сортування**.
>
> До Java 8 `Comparator` мав лише один абстрактний метод `compare`. Уся логіка (ланцюжки, зворотний порядок) повинна була реалізовуватися вручну, часто за допомогою громіздких анонімних класів.
>
> **З Java 8 з'явилися `default` методи:**
>
> 1. **`reversed()`**: Дозволив легко інвертувати будь-який компаратор.
> 2. **`thenComparing(Comparator)`**: Уможливив створення декларативних ланцюжків порівнянь.
> 3. **`thenComparing(Function)`**, **`thenComparingInt`**, etc.: Спростили створення ланцюжків без необхідності писати додаткові лямбди.
>
> Разом зі статичними методами (`comparing`, `nullsFirst` і т.д.), `default` методи перетворили `Comparator` на невеликий **fluent API** або **DSL (Domain-Specific Language)** для побудови логіки сортування. Це дозволило писати код, який є значно більш читабельним, стислим і менш схильним до помилок.

#### **📌 Що перевіряють:**

*Розуміння впливу ключових нововведень мови (`default` методів) на дизайн стандартних бібліотек. Кандидат повинен не просто знати про фічу, а розуміти, як вона змінила підходи до програмування.*

* * *

### **15 ❓ Питання:**

Припустимо, `Comparator.comparing(User::getCity)` використовується для сортування користувачів. Клас `City` не реалізує `Comparable`. Що станеться? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання з підступом, що перевіряє уважність і глибоке розуміння, як працює `Comparator.comparing()`.
>
> **Відповідь:** Код **скомпілюється**, але впаде з `ClassCastException` під час виконання, як тільки сортування спробує порівняти два об'єкти `City`.
>
> **Пояснення:**
> Метод `Comparator.comparing(Function<T, U> keyExtractor)` створює компаратор для типу `T` (в нашому випадку `User`). Ключ для сортування `U` (в нашому випадку `City`) витягується за допомогою функції `User::getCity`.
>
> **Але** для того, щоб порівняти ці ключі (`City`), `Comparator.comparing` покладається на їхній **природний порядок**. Тобто він намагається привести об'єкт `City` до `Comparable` і викликати `compareTo()`.
>
> Оскільки `City` не реалізує `Comparable`, це призведе до `ClassCastException`.
>
> **Як це виправити?**
> Потрібно надати `Comparator.comparing` другий аргумент — компаратор для ключів (для `City`).
>
> ```java
> Comparator<City> cityComparator = Comparator.comparing(City::getName);
> Comparator<User> userComparator = Comparator.comparing(User::getCity, cityComparator);
> ```
>
> Таким чином, ми явно вказуємо, як саме потрібно порівнювати об'єкти `City`.

#### **📌 Що перевіряють:**

*Найглибше розуміння роботи методів-помічників `Comparator` та їхніх вимог. Це питання вимагає знання не тільки основної, а й перевантаженої версії методу `comparing` та її призначення.*

* * *
