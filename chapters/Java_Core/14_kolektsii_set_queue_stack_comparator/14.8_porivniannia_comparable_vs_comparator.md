---
layout: default
title: "Порівняння Comparable vs Comparator"
parent: "Колекції: Set, Queue, Stack, Comparator"
grand_parent: "Java Core"
nav_order: 8
---

# Порівняння Comparable vs Comparator

* короткий опис: `Comparable` – для визначення внутрішнього, природного порядку об'єкта. `Comparator` – для зовнішнього, кастомного порядку.

* поняття: `Comparable`, `Comparator`, `java.lang` vs `java.util`, `compareTo()` vs `compare()`.

* приклади: Таблиця відмінностей за місцем реалізації, методом, кількістю порядків, необхідністю модифікації класу.

* ключові моменти: `Comparable` для одного, фіксованого порядку. `Comparator` для гнучкості та множинних стратегій сортування.

* що важливо знати на співбесіді: Детальні відмінності, їх застосування.

### **✅ Офіційне визначення:**

* **Comparable** — інтерфейс, який дозволяє об’єктам **порівнювати себе з іншими** для визначення "природного порядку".
* **Comparator** — інтерфейс, що дозволяє створити **зовнішнє порівняння** між двома об’єктами без зміни класу.

### **🧠 Простими словами:**

* **`Comparable`** — це **вбудоване сортування**, яке об’єкт **знає сам про себе**.
* **`Comparator`** — це **зовнішній сортувальник**, який можна використовувати для **різних стратегій** без зміни класу.

---

## **Основне**

| Ознака | `Comparable` | `Comparator` |
| :---: | :---: | :---: |
| **Пакет** | **`java.lang`** | **`java.util`** |
| **Метод** | **`int compareTo(T o)`** | **`int compare(T o1, T o2)`** |
| **Кількість порядків** | Один (природний) | Можна створити багато |
| **Потрібна зміна класу** | Так (реалізується в класі) | Ні (реалізується окремо) |
| **Використовується з** | **`Collections.sort(), TreeSet, TreeMap`** | **`Collections.sort(), TreeSet, Stream.sorted()`** |
| **Гнучкість** | Менш гнучкий | Більш гнучкий |
| **Java 8 API підтримка** | Так | Потужні можливості (**`thenComparing`**, etc.) |
| **Часто використовується для** | Простих моделей | Складних моделей з кількома критеріями |

---

## **Пояснення на пальцях (💡)**

Уяви, що об’єкт — це **студент**:

* Якщо студент каже: “Я порівнюю себе з іншими за GPA” — це **`Comparable`**.
* Якщо **зовнішній викладач** сортує студентів по імені чи віку — це **`Comparator`**.

**`Comparable`** зручний для одного стабільного порядку.  
**`Comparator`** — для сценаріїв з **альтернативними або складеними критеріями сортування**.

---

## **Корисні приклади (🧪)**

#### **✅ Comparable:**

```java
class Book implements Comparable<Book> {
    int pages;
    public int compareTo(Book other) {
        return Integer.compare(this.pages, other.pages);
    }
}
Collections.sort(listOfBooks); // сортування за сторінками
```

#### **✅ Comparator:**

```java
Comparator<Book> byTitle = (b1, b2) -> b1.title.compareTo(b2.title);
Collections.sort(listOfBooks, byTitle); // сортування за назвою
```

---

## **Порівняння (⚖️)**

#### **✅ Comparable -> Один варіант сортування:**

```java
class User implements Comparable<User> {
    int age;
    public int compareTo(User other) {
        return Integer.compare(this.age, other.age);
    }
}
```

#### **✅ Comparator -> Кілька варіантів:**

```java
Comparator<User> byName = Comparator.comparing(u -> u.name);
Comparator<User> byAge = Comparator.comparingInt(u -> u.age);
```

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
"COMPARABLE = CLASS порівнює СЕБЕ"
"COMPARATOR = СТОРОННІЙ порівнює ДВОХ"
```

**Або простіше:**

**`Comparable = Compare Me`**

**`Comparator = Compare Them`**

---

## **🟩 Підсумок:**

* **`Comparable`** — для **одного вбудованого порядку**, реалізується **всередині класу**.
* **`Comparator`** — для **гнучкого, зовнішнього сортування**, не змінює клас.
* Обидва підходи активно використовуються в **`Collections.sort()`**, **`TreeSet`**, **`TreeMap`**.
* Для простих класів — **`Comparable`**, для складних сценаріїв або сторонніх класів — **`Comparator`**.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs – Comparable**](https://docs.oracle.com/javase/8/docs/api/java/lang/Comparable.html)
* [**Oracle Docs – Comparator**](https://docs.oracle.com/javase/8/docs/api/java/util/Comparator.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому полягає основна концептуальна різниця між `Comparable` та `Comparator`?

> #### **💬 Відповідь:**
>
> Основна різниця полягає в тому, **де визначається логіка сортування** і **хто її надає**:
>
> * **`Comparable`** — це про **внутрішнє, природне сортування**. Клас сам "знає", як порівнювати свої екземпляри з іншими. Це частина самого об'єкта.
>   * **Аналогія:** Людина знає свій зріст і може сказати, хто вищий, а хто нижчий.
>
> * **`Comparator`** — це про **зовнішнє, кастомне сортування**. Логіка порівняння визначається окремим об'єктом-компаратором, який не є частиною класу, що сортується.
>   * **Аналогія:** Зовнішній суддя, який оцінює людей за різними критеріями: сьогодні за силою, завтра — за швидкістю. Люди ті ж, критерії — різні.
>
> | **Параметр** | `Comparable` | `Comparator` |
> |:---:|:---:|:---:|
> | **Хто порівнює** | Сам об'єкт | Зовнішній "суддя" |
> | **Тип логіки** | Внутрішня, природна | Зовнішня, кастомна |

#### **📝 Мнемоніка:**

* **Compara**ble = **Compare me** (Порівняй мене)
* **Compara**tor = **Compare them** (Порівняй їх)

#### **📌 Що перевіряють:**

*Базове, високорівневе розуміння призначення двох інтерфейсів. Це найперше і найважливіше питання для перевірки знань цієї теми.*

* * *

### **2 ❓ Питання:**

До яких пакетів належать `Comparable` та `Comparator` і що це означає?

> #### **💬 Відповідь:**
>
> Розташування інтерфейсів у пакетах вказує на їх фундаментальне призначення в мові Java:
>
> * **`java.lang.Comparable`**: Належить до пакету `java.lang`, який імпортується автоматично в усі Java-класи. Це підкреслює, що здатність об'єкта до порівняння є його **фундаментальною, внутрішньою властивістю**, такою ж як `toString()` чи `equals()`.
>
> * **`java.util.Comparator`**: Належить до пакету `java.util`, де зібрані різні утиліти та структури даних, включаючи Collections Framework. Це вказує на те, що `Comparator` є **допоміжною утилітою** для виконання специфічних операцій над колекціями, а не властивістю самого об'єкта.

#### **📌 Що перевіряють:**

*Знання структури стандартної бібліотеки Java та розуміння, як архітектурні рішення (розташування в пакетах) відображають дизайн та філософію мови.*

* * *

### **3 ❓ Питання:**

Порівняйте сигнатури основних методів `Comparable` та `Comparator`.

> #### **💬 Відповідь:**
>
> Сигнатури методів чітко відображають різницю в підходах до порівняння:
>
> * **`Comparable`**:
>   * Метод: **`int compareTo(T other)`**
>   * Приймає **один** аргумент `other`.
>   * Порівнює **поточний об'єкт (`this`)** з переданим об'єктом.
>   * Приклад виклику: `thisObject.compareTo(otherObject)`
>
> * **`Comparator`**:
>   * Метод: **`int compare(T o1, T o2)`**
>   * Приймає **два** аргументи `o1` та `o2`.
>   * Порівнює **два зовнішні об'єкти** між собою.
>   * Приклад виклику: `myComparator.compare(object1, object2)`

#### **📝 Коментар:**

Різна кількість аргументів — це найпростіший спосіб запам'ятати, який метод до якого інтерфейсу належить.

#### **📌 Що перевіряють:**

*Технічна точність. Кандидат повинен знати точні назви методів та кількість їх параметрів.*

* * *

### **4 ❓ Питання:**

Коли обов'язково потрібно використовувати `Comparator` замість `Comparable`?

> #### **💬 Відповідь:**
>
> Існують ситуації, коли використання `Comparator` є єдиним можливим або єдино правильним рішенням:
>
> 1. **Ви не можете змінити клас:** Якщо вам потрібно сортувати об'єкти класу з **зовнішньої бібліотеки** або **фреймворку**, ви не можете додати `implements Comparable` до його вихідного коду.
>
> 2. **Потрібно кілька стратегій сортування:** Найпоширеніший випадок. Клас може мати один "природний" порядок (наприклад, сортування `Employee` за ID), але в різних частинах програми вам може знадобитися сортувати його за прізвищем, зарплатою, датою прийняття на роботу тощо. Для кожної такої стратегії створюється свій `Comparator`.
>
> 3. **Логіка сортування залежить від контексту:** Порядок сортування може залежати від зовнішніх умов, які не є властивостями самого об'єкта. Наприклад, сортування товарів за "популярністю", де популярність обчислюється в реальному часі.

#### **📌 Що перевіряють:**

*Практичний досвід та вміння аналізувати вимоги задачі для вибору правильного інструменту. Це показує, що кандидат стикався з реальними проблемами дизайну.*

* * *

### **5 ❓ Питання:**

Наведіть код, де клас реалізує `Comparable`, але сортування відбувається за іншим критерієм за допомогою `Comparator`.

> #### **💬 Відповідь:**
>
> Цей приклад показує, як `Comparator` може "перевизначити" природний порядок `Comparable`. Клас `Student` сортується за замовчуванням за оцінкою, але ми можемо відсортувати його за іменем.

#### **📝 Приклад:**

```java
import java.util.*;

// Клас із природним порядком (за оцінкою)
class Student implements Comparable<Student> {
    String name;
    int grade;
    public Student(String name, int grade) { this.name = name; this.grade = grade; }
    public String toString() { return name + " (" + grade + ")"; }
    @Override public int compareTo(Student other) { return Integer.compare(this.grade, other.grade); }
}

public class Main {
    public static void main(String[] args) {
        List<Student> students = new ArrayList<>();
        students.add(new Student("Bob", 92));
        students.add(new Student("Alice", 85));
        students.add(new Student("Charlie", 85));

        // 1. Сортування за природним порядком (Comparable)
        Collections.sort(students);
        System.out.println("За Comparable (оцінка): " + students);
        
        // 2. Сортування за допомогою зовнішнього Comparator (за іменем)
        Comparator<Student> byNameComparator = Comparator.comparing(s -> s.name);
        students.sort(byNameComparator);
        System.out.println("За Comparator (ім'я):   " + students);
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички написання коду та розуміння пріоритету: `Comparator` має вищий пріоритет над `Comparable` при сортуванні.*

* * *

### **6 ❓ Питання:**

Що відбудеться, якщо сортувати список об'єктів (`Collections.sort(list)`), клас яких не реалізує `Comparable`?

> #### **💬 Відповідь:**
>
> Такий код **не скомпілюється**. На відміну від `TreeSet`, який перевіряє `Comparable` під час виконання, метод `Collections.sort(List<T> list)` має таку сигнатуру, де на тип `T` накладається обмеження: `T` має бути підтипом `Comparable<? super T>`.
>
> Це означає, що компілятор перевірить, чи реалізує клас елементів списку інтерфейс `Comparable`. Якщо ні, ви отримаєте помилку компіляції, яка повідомить, що тип не відповідає необхідним обмеженням.
>
> Це чудовий приклад того, як узагальнення (generics) забезпечують безпеку типів на етапі компіляції.

#### **📌 Що перевіряють:**

*Уважність до деталей API, зокрема, до сигнатур методів та обмежень типів (bounded wildcards). Це відрізняє `TreeSet` (runtime check) від `Collections.sort` (compile-time check).*

* * *

### **7 ❓ Питання:**

Якщо потрібно відсортувати об'єкти класу `Person` спочатку за прізвищем, а потім за іменем, який інструмент (`Comparable` чи `Comparator`) краще для цього підходить?

> #### **💬 Відповідь:**
>
> Обидва інструменти можуть вирішити це завдання, але вибір залежить від контексту:
>
> * Якщо сортування "прізвище -> ім'я" є **єдиним і основним** способом впорядкування `Person` у вашій системі, то логічно зробити його "природним" і реалізувати в `Comparable`.
>
> * Однак, **`Comparator` є значно гнучкішим і кращим рішенням** у більшості випадків, навіть для багаторівневого сортування. З Java 8 це можна зробити дуже елегантно за допомогою `Comparator.comparing().thenComparing()`. Це робить код більш декларативним, читабельним і дозволяє легко додавати або змінювати критерії сортування в майбутньому, не чіпаючи клас `Person`.
>
> **Висновок:** Для багаторівневого сортування майже завжди варто віддавати перевагу `Comparator`.

#### **📌 Що перевіряють:**

*Здатність аналізувати не лише поточні, а й майбутні вимоги до коду, а також прагнення до чистоти та гнучкості дизайну.*

* * *

### **8 ❓ Питання:**

Як Java 8 змінила підходи до роботи з `Comparator` і `Comparable`? ⭐️

> #### **💬 Відповідь:**
>
> Java 8 здійснила революцію в роботі з `Comparator`, зробивши його набагато потужнішим і зручнішим, тоді як `Comparable` залишився практично без змін.
>
> **Ключові зміни для `Comparator`:**
>
> 1. **Функціональний інтерфейс:** Дозволило створювати компаратори за допомогою лаконічних **лямбда-виразів**.
> 2. **Статичні методи-фабрики:** З'явились методи `Comparator.comparing()`, `comparingInt()`, `nullsFirst()`, `naturalOrder()`, `reverseOrder()`, які дозволяють створювати компаратори декларативно.
> 3. **Default методи для композиції:** Методи `thenComparing()` та `reversed()` дозволили легко будувати ланцюжки сортувань та інвертувати порядок.
>
> **Наслідок:**
> До Java 8 створення складних компараторів вимагало написання громіздких анонімних класів. Після Java 8 `Comparator` перетворився на гнучкий DSL (Domain-Specific Language) для опису логіки сортування. Це сильно схилило шальки терезів на користь використання `Comparator` замість `Comparable` у більшості сценаріїв.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей Java. Кандидат повинен продемонструвати, що він не застряг у застарілих підходах і вміє використовувати сучасний API для написання кращого коду.*

* * *

### **9 ❓ Питання:**

Узагальніть, коли слід віддавати перевагу `Comparable`, а коли — `Comparator`. ⭐️

> #### **💬 Відповідь:**
>
> **Обирайте `Comparable`, коли:**
>
> * Логіка сортування є **єдиною та природною** для об'єкта. Наприклад, клас `Money` завжди сортується за сумою, `Date` — за хронологією.
> * Ви контролюєте вихідний код класу і можете його змінювати.
> * Порядок сортування не залежить від зовнішніх факторів.
> * Простота є пріоритетом, і не очікується появи альтернативних способів сортування.
>
> **Обирайте `Comparator`, коли:**
>
> * Вам потрібно **кілька способів сортування** для одного й того ж класу.
> * Ви сортуєте об'єкти **чужого класу**, який не можете змінити.
> * Потрібно реалізувати **складну, багаторівневу** логіку сортування (`thenComparing`).
> * Логіка сортування є специфічною для конкретного випадку і не повинна бути "вшита" в сам клас.
> * Вам потрібна максимальна гнучкість та читабельність, яку надає сучасний `Comparator` API.

#### **📝 Висновок:**

`Comparable` — для простих, очевидних випадків. `Comparator` — для всього іншого.

#### **📌 Що перевіряють:**

*Стратегічне мислення. Кандидат повинен вміти приймати обґрунтовані архітектурні рішення на основі аналізу переваг та недоліків кожного підходу.*

* * *

### **10 ❓ Питання:**

Поясніть, що означає "узгодженість з `equals`" для `Comparable` і `Comparator` та чому це важливо. ⭐️

> #### **💬 Відповідь:**
>
> Узгодженість з `equals` означає, що `compare(o1, o2) == 0` (або `o1.compareTo(o2) == 0`) має повертати `true` тоді і тільки тоді, коли `o1.equals(o2)` повертає `true`.
>
> **Чому це важливо?**
>
> * **Для `TreeSet` та `TreeMap`:** Ці колекції використовують **тільки `compare` / `compareTo`** для визначення унікальності елементів. Якщо узгодженості немає, це призведе до порушення контракту `Set` чи `Map`. Наприклад, `TreeSet` може або відмовитися додати об'єкт, який не є `equals` іншому, або, навпаки, зберегти два об'єкти, які є `equals`.
>
> * **Для інших алгоритмів сортування:** Хоча `Collections.sort` не використовує це для унікальності, неузгоджена поведінка може здивувати розробників та призвести до логічних помилок. Програма стає непередбачуваною.
>
> **Чи це обов'язково?**
> Формально, документація каже, що це **настійно рекомендується**, але не є суворою вимогою. Однак, відхилятися від цієї рекомендації варто лише тоді, коли ви точно розумієте наслідки для сортованих колекцій.

#### **📌 Що перевіряють:**

*Розуміння тонких аспектів контрактів Java. Це показує, що кандидат думає про взаємодію різних частин програми та наслідки свого дизайну.*

* * *

### **11 ❓ Питання:**

Яким чином `Comparable` та `Comparator` пов'язані з принципами SOLID? ⭐️

> #### **💬 Відповідь:**
>
> `Comparable` та `Comparator` є чудовою ілюстрацією деяких принципів SOLID:
>
> 1. **Принцип єдиної відповідальності (Single Responsibility Principle - SRP):**
>     * Клас, що реалізує `Comparable`, може порушувати SRP, оскільки він бере на себе дві відповідальності: зберігання даних і логіку їх порівняння.
>     * Використання `Comparator` дотримується SRP. Клас моделі відповідає лише за дані, а окремий клас `Comparator` — лише за логіку сортування.
>
> 2. **Принцип відкритості/закритості (Open/Closed Principle - OCP):**
>     * `Comparable` порушує OCP. Якщо вам потрібен новий спосіб сортування, вам потрібно змінити існуючий клас.
>     * `Comparator` ідеально відповідає OCP. Клас моделі закритий для модифікацій, але відкритий для розширення функціональності шляхом створення нових `Comparator`'ів.
>
> **Висновок:** З точки зору SOLID, `Comparator` є значно кращим дизайнерським рішенням, оскільки він веде до більш гнучкого, роз'єднаного (decoupled) та підтримуваного коду.

#### **📌 Що перевіряють:**

*Здатність застосовувати теоретичні принципи дизайну ПЗ до практичних прикладів зі стандартної бібліотеки. Це демонструє високий рівень розуміння архітектури.*

* * *

### **12 ❓ Питання:**

Розглянемо `Collections.max(collection)` і `Collections.max(collection, comparator)`. Поясніть роль `Comparable` та `Comparator` в цих методах. ⭐️

> #### **💬 Відповідь:**
>
> Обидва методи шукають максимальний елемент у колекції, але використовують різні стратегії для визначення "максимальності".
>
> * **`Collections.max(Collection<? extends T> coll)`**:
>   * Ця версія методу працює з колекцією, елементи `T` якої повинні реалізовувати **`Comparable`**.
>   * Метод ітерує по колекції, використовуючи `compareTo()` для порівняння поточного "максимуму" з наступним елементом. "Максимальним" вважається елемент, який є "найбільшим" відповідно до його **природного порядку**.
>
> * **`Collections.max(Collection<? extends T> coll, Comparator<? super T> comp)`**:
>   * Ця версія приймає **`Comparator`** як другий аргумент.
>   * Вона також ітерує по колекції, але для порівняння використовує наданий `Comparator`. "Максимальним" вважається елемент, який є "найбільшим" відповідно до логіки цього **зовнішнього компаратора**.
>
> Роль `Comparable` та `Comparator` тут така ж, як і в сортуванні: перший визначає внутрішній порядок, другий — зовнішній.

#### **📌 Що перевіряють:**

*Розуміння, що `Comparable` та `Comparator` — це не тільки про сортування, а про будь-які операції, що вимагають порівняння елементів, як-от пошук екстремумів.*

* * *

### **13 ❓ Питання:**

Яким чином `Stream API` підтримує `Comparable` та `Comparator` в методі `sorted()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `sorted()` в Stream API має дві перевантажені версії, які напряму відповідають парадигмі `Comparable` та `Comparator`.
>
> 1. **`Stream<T> sorted()`**:
>     * Ця версія не приймає аргументів. Вона сортує елементи стріму відповідно до їхнього **природного порядку**.
>     * Вимога: Тип `T` елементів стріму повинен реалізовувати `Comparable`. Якщо ні — під час виконання термінальної операції буде викинуто `ClassCastException`.
>
> 2. **`Stream<T> sorted(Comparator<? super T> comparator)`**:
>     * Ця версія приймає `Comparator` як аргумент.
>     * Вона сортує елементи стріму, використовуючи логіку, визначену в цьому компараторі. Типу `T` не потрібно реалізовувати `Comparable`.
>
> `Stream API` повністю інтегрувало обидві моделі сортування, роблячи їх використання в конвеєрах обробки даних дуже простим і природним. Поєднання `stream().sorted(Comparator.comparing(...))` є сучасною ідіомою для сортування колекцій.

#### **📝 Приклад:**

```java
List<String> list = Arrays.asList("bbb", "a", "cc");
// sorted() використовує природний порядок String
List<String> sortedList1 = list.stream().sorted().collect(Collectors.toList()); // [a, bbb, cc]

// sorted(comparator) використовує зовнішній компаратор
List<String> sortedList2 = list.stream().sorted(Comparator.comparing(String::length)).collect(Collectors.toList()); // [a, cc, bbb]
```

#### **📌 Що перевіряють:**

*Знання інтеграції класичних концепцій Java (як сортування) в сучасні API (як Stream API). Це демонструє вміння працювати з функціональним стилем.*

* * *

### **14 ❓ Питання:**

Чому `Comparator` краще підходить для реалізації "нестабільного" порядку сортування, наприклад, сортування точок за відстанню до заданого центру? ⭐️⭐️

> #### **💬 Відповідь:**
>
> "Нестабільний" порядок — це порядок, який залежить від зовнішнього стану або контексту, а не тільки від властивостей самого об'єкта. Сортування точок за відстанню до заданого центру — ідеальний приклад.
>
> **Чому `Comparable` тут не підходить:**
>
> * Природний порядок, який визначає `Comparable`, має бути внутрішнім та постійним. Якби клас `Point` реалізовував `Comparable`, він би мав порівнювати себе з іншою точкою, не знаючи про "центр". Який тоді буде "природний" порядок? За `x`, потім за `y`? Будь-який вибір буде довільним.
> * `compareTo` не може приймати додаткові параметри, як-от "центр", тому реалізувати таку логіку всередині нього неможливо.
>
> **Чому `Comparator` ідеально підходить:**
>
> * `Comparator` є зовнішнім об'єктом і **може мати стан**. Ми можемо створити клас компаратора, який зберігає координати "центру" у своїх полях.
> * Це дозволяє створювати різні компаратори для сортування відносно різних центрів, не змінюючи клас `Point`.

#### **📝 Приклад:**

```java
class DistanceComparator implements Comparator<Point> {
    private final Point center;
    public DistanceComparator(Point center) { this.center = center; }

    @Override
    public int compare(Point p1, Point p2) {
        double dist1 = p1.distance(center);
        double dist2 = p2.distance(center);
        return Double.compare(dist1, dist2);
    }
}
// Використання:
// List<Point> points = ...;
// Point myCenter = new Point(10, 10);
// points.sort(new DistanceComparator(myCenter));
```

#### **📌 Що перевіряють:**

*Здатність аналізувати складніші сценарії та розуміти, коли стан є необхідною частиною логіки порівняння, що робить `Comparator` єдиним правильним вибором.*

* * *

### **15 ❓ Питання:**

Поясніть, що відбувається "під капотом" виклику `Comparator.comparing(User::getAge).thenComparing(User::getName)`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз створює композитний `Comparator` шляхом ланцюгового виклику методів. Ось що відбувається крок за кроком:
>
> 1. **`Comparator.comparing(User::getAge)`**:
>     * Цей статичний метод створює та повертає новий екземпляр `Comparator`.
>     * Всередині його методу `compare(u1, u2)` буде викликана функція-екстрактор `User::getAge` для обох користувачів, а потім їхні вікові категорії (`Integer`) будуть порівняні за допомогою їхнього природного порядку (`Integer.compareTo()`).
>
> 2. **`.thenComparing(User::getName)`**:
>     * Це `default` метод, що викликається на першому компараторі (`byAgeComparator`).
>     * Він повертає **новий, третій `Comparator`** (композитний).
>     * Його метод `compare(u1, u2)` реалізований приблизно так:
>
>         ```java
>         public int compare(User u1, User u2) {
>             // 1. Спочатку спробуй порівняти за допомогою першого компаратора
>             int result = byAgeComparator.compare(u1, u2);
>             // 2. Якщо вони не рівні, поверни результат
>             if (result != 0) {
>                 return result;
>             }
>             // 3. Якщо рівні, використай другий компаратор
>             return byNameComparator.compare(u1, u2);
>         }
>         ```
>
>         Де `byNameComparator` також створюється через `Comparator.comparing(User::getName)`.
>
> Таким чином, створюється елегантний ланцюжок відповідальності, де кожен наступний компаратор вступає в дію тільки якщо попередній визнав об'єкти рівними.

#### **📌 Що перевіряють:**

*Найглибше розуміння того, як працюють `default` та статичні методи в інтерфейсах для створення функціональних композицій. Це демонструє, що кандидат не просто використовує API, а й розуміє, як воно сконструйовано.*

* * *
