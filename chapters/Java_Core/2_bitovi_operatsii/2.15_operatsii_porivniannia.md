---
layout: default
title: "Операції порівняння: ==, !=, <, >"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 15
---

# Операції порівняння: ==, !=, <, >

* короткий опис: Оператори порівняння (`==`, `!=`, `<`, `>`, `<=`, `>=`) порівнюють два значення та повертають `boolean` (`true` або `false`).

* поняття: `==` (рівність), `!=` (нерівність), `<` (менше), `>` (більше), `<=` (менше або рівне), `>=` (більше або рівне), `equals()` (для об'єктів).

* приклади: `a == b` (для примітивів та посилань), `x > 10`, `str1.equals(str2)` (для вмісту об'єктів).

* ключові моменти: `==` для примітивів порівнює значення, для об'єктів – посилання. Для порівняння вмісту об'єктів завжди використовуйте `equals()`.

* що важливо знати на співбесіді: Різниця `==` та `equals()`, як порівнювати `String` та `Wrapper` класи.

### **✅ Офіційне визначення:**

Операції порівняння — це бінарні оператори, що порівнюють два значення і повертають **`boolean`** (тобто **`true`** або **`false`**). Застосовуються до чисел, символів, булевих значень і частково — до обʼєктів.

### **🧠 Простими словами:**

Це як питання в стилі:

* "Чи більше?"
* "Чи дорівнює?"
* "Чи не дорівнює?"  
  І Java відповідає: **`true`** або **`false`**.

---

## **Повний список операторів порівняння (📊)**

| Оператор | Назва | Пояснення | Приклад |
| :---: | :---: | :---: | :---: |
| **`==`** | рівність | чи однакові значення | **`a == b`** |
| **`!=`** | нерівність | чи різні значення | **`a != b`** |
| **`>`** | більше | **`a`** більше **`b`** | **`a > b`** |
| **`<`** | менше | **`a`** менше **`b`** | **`a < b`** |
| **`>=`** | більше або рівне | **`a ≥ b`** | **`a >= b`** |
| **`<=`** | менше або рівне | **`a ≤ b`** | **`a <= b`** |

---

## **Пояснення на пальцях (💡)**

* Працюють з примітивами: **`int, float, char, double`**
* Повертають **`true`** або **`false`**
* **`==`** і **`!=`** також можна застосовувати до **`boolean`** і **`char`**
* ⚠️ **З об'єктами `==` порівнює посилання**, не вміст\! (тому **`s1 == s2`** не завжди працює як ти думаєш)

---

## **Корисні приклади (🧪)**

```java
int a = 10;
int b = 20;

System.out.println(a < b);   // true
System.out.println(a == b);  // false
System.out.println(a != b);  // true
System.out.println(b >= 20); // true

char c = 'A';
System.out.println(c < 'Z'); // true (бо 'A' = 65, 'Z' = 90)

boolean flag = true;
System.out.println(flag == true); // true
System.out.println(!flag);        // false
```

**🔸 Обʼєкти:**

```java
String s1 = new String("hi");
String s2 = new String("hi");

System.out.println(s1 == s2);       // false (різні обʼєкти)
System.out.println(s1.equals(s2));  // true (однаковий текст)
```

---

## **Порівняння між типами (⚖️)**

| Тип 1 | Тип 2 | Працює? | Що відбувається |
| :---: | :---: | :---: | :---: |
| **`int`** | **`int`** | ✅ | пряме порівняння |
| **`int`** | **`double`** | ✅ | **`int`** конвертується у **`double`** |
| **`char`** | **`int`** | ✅ | порівнюється числовий код символу |
| **`boolean`** | **`int`** | ❌ | не можна — типи несумісні |

---

## **🧠 Мнемоніка для запам’ятовування:**

| Оператор | Пояснення |
| :---: | :---: |
| **`==`** | Чи рівні? |
| **`!=`** | Чи різні? |
| **`<`** | Менше |
| **`>`** | Більше |
| **`<=`** | Менше або рівно |
| **`>=`** | Більше або рівно |

  💡 *"***`==`*****друзі,* **`!=`*** **вороги,* **`<`*** **менше,***`>`*****більше, а решта — на компроміс"*

---

## **🟩 Підсумок:**

* Оператори порівняння — базовий інструмент логіки в Java.

* З примітивами — працюють чітко. З обʼєктами — будь обережний і використовуй **`equals()`**.

* Це те, на чому стоїть більшість умовних конструкцій, циклів і прийняття рішень у коді.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які основні оператори порівняння існують в Java і що вони повертають?

> #### **💬 Відповідь:**
>
> Оператори порівняння в Java використовуються для порівняння двох значень і завжди повертають результат типу **`boolean`** (`true` або `false`).
>
> Існує шість основних операторів:
>
> 1. **`==` (Дорівнює):**
>     * Повертає `true`, якщо операнди рівні. Для примітивів порівнює значення, для об'єктів — посилання.
>
> 2. **`!=` (Не дорівнює):**
>     * Повертає `true`, якщо операнди нерівні.
>
> 3. **`>` (Більше ніж):**
>     * Повертає `true`, якщо лівий операнд більший за правий.
>
> 4. **`<` (Менше ніж):**
>     * Повертає `true`, якщо лівий операнд менший за правий.
>
> 5. **`>=` (Більше або дорівнює):**
>     * Повертає `true`, якщо лівий операнд більший або дорівнює правому.
>
> 6. **`<=` (Менше або дорівнює):**
>     * Повертає `true`, якщо лівий операнд менший або дорівнює правому.
>
> Оператори `>` `<` `>=` `<=` застосовуються лише до числових примітивів (і `char`, який неявно перетворюється в `int`). Оператори `==` та `!=` можна застосовувати до всіх примітивних типів та об'єктних посилань.

#### **📌 Що перевіряють:**

*Базові знання синтаксису мови. Це фундаментальне питання, яке перевіряє основи.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між оператором `==` та методом `.equals()`?

> #### **💬 Відповідь:**
>
> Це одна з найважливіших концепцій у Java. Різниця полягає в тому, **що саме** вони порівнюють.
>
> * **Оператор `==`:**
>   * **Для примітивних типів (`int`, `double`, `char` тощо):** Він порівнює **фактичні значення**.
>       `int a = 5; int b = 5; // (a == b) поверне true`
>   * **Для об'єктних (посилальних) типів (`String`, `Integer`, будь-який клас):** Він порівнює **посилання на об'єкти (адреси в пам'яті)**. Він поверне `true` тільки якщо обидві змінні посилаються на **один і той самий об'єкт** у купі (Heap).
>
> * **Метод `.equals(Object obj)`:**
>   * Цей метод призначений для порівняння об'єктів за їхнім **вмістом або логічним значенням**.
>   * Його поведінка залежить від реалізації в конкретному класі. Наприклад, у класі `String` він порівнює послідовності символів. У класі `Integer` — числові значення.
>   * Якщо клас не перевизначає `.equals()`, він успадковує реалізацію з `Object`, яка працює так само, як `==` (порівнює посилання).
>
> **Висновок:** Для примітивів використовуйте `==`. Для порівняння **вмісту** об'єктів **завжди** використовуйте `.equals()`.

#### **📝 Мнемоніка:**

* `==` → порівнює "ярлики" (адреси).
* `.equals()` → порівнює "вміст коробок" (значення).

#### **📌 Що перевіряють:**

*Найважливіше питання теми. Розуміння цієї різниці є критичним для уникнення логічних помилок при роботі з об'єктами.*

* * *

### **3 ❓ Питання:**

Поясніть, що виведе цей код, і чому результати різні.

```java
String s1 = "hello";
String s2 = "hello";
String s3 = new String("hello");

System.out.println(s1 == s2);
System.out.println(s1 == s3);
System.out.println(s1.equals(s3));
```

> #### **💬 Відповідь:**
>
> **Вивід:**
>
> ```
> true
> false
> true
> ```
>
> **Пояснення:**
>
> 1. **`s1 == s2` → `true`:**
>     * Обидві змінні `s1` та `s2` створюються як **рядкові літерали**.
>     * JVM оптимізує такі рядки, поміщаючи їх у **String Pool**.
>     * Оскільки вміст "hello" однаковий, обидві змінні отримують посилання на **один і той самий об'єкт** у пулі. Тому порівняння посилань `==` повертає `true`.
>
> 2. **`s1 == s3` → `false`:**
>     * `s3` створюється за допомогою оператора **`new`**.
>     * `new` **гарантовано створює новий об'єкт** у купі (Heap), поза String Pool.
>     * Таким чином, `s1` та `s3` посилаються на два **різні об'єкти** в пам'яті, хоча їх вміст однаковий. Порівняння їхніх посилань повертає `false`.
>
> 3. **`s1.equals(s3)` → `true`:**
>     * Метод `.equals()` для класу `String` перевизначено так, що він **порівнює вміст** рядків (послідовності символів).
>     * Оскільки вміст обох рядків — "hello", метод повертає `true`.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про `==` vs `equals` та String Pool до практичного прикладу. Це класичне питання для співбесіди.*

* * *

### **4 ❓ Питання:**

Що виведе цей код? Поясніть свою відповідь.

```java
Integer a = 127;
Integer b = 127;
System.out.println(a == b);

Integer c = 128;
Integer d = 128;
System.out.println(c == d);
```

> #### **💬 Відповідь:**
>
> **Вивід:**
>
> ```
> true
> false
> ```
>
> **Пояснення:**
>
> Ця поведінка пояснюється наявністю **кешу цілих чисел (Integer Cache)** у Java.
>
> 1. **`a == b` → `true`:**
>     * Створення `Integer a = 127;` є **автоупаковкою (Autoboxing)**, яка неявно викликає `Integer.valueOf(127)`.
>     * `Integer.valueOf()` **кешує** об'єкти для значень у діапазоні від **-128 до 127**.
>     * Оскільки `127` входить у цей діапазон, `a` та `b` отримують посилання на **один і той самий об'єкт** з кешу.
>     * Тому порівняння посилань `==` дає `true`.
>
> 2. **`c == d` → `false`:**
>     * Значення `128` **не входить** до стандартного діапазону кешування.
>     * Для `c` та `d` JVM змушена створити **два нові, окремі об'єкти `Integer`**.
>     * Оскільки `c` та `d` посилаються на різні об'єкти, порівняння посилань `==` повертає `false`.
>
> **Мораль:** як і з рядками, **ніколи не порівнюйте об'єкти-обгортки за допомогою `==`**. Завжди використовуйте `.equals()`.

#### **📌 Що перевіряють:**

*Знання про існування кешу об'єктів-обгорток та його вплив на результати порівняння. Це питання-пастка.*

* * *

### **5 ❓ Питання:**

Чи можна порівнювати між собою змінні різних числових примітивних типів? Наприклад, `int` та `double`.

> #### **💬 Відповідь:**
>
> **Так, можна.** При порівнянні змінних різних числових типів Java застосовує правила **розширення числових типів (Numeric Promotion)**.
>
> Менший тип автоматично "розширюється" до більшого перед виконанням порівняння.
>
> **Правила розширення:** `byte` → `short` → `int` → `long` → `float` → `double`.
>
> **Приклад:**
>
> ```java
> int i = 10;
> double d = 10.0;
> long l = 10L;
> float f = 10.0f;
>
> // i розширюється до double, потім порівняння
> System.out.println(i == d); // true (10.0 == 10.0)
>
> // i розширюється до long
> System.out.println(i == l); // true
>
> // l розширюється до float
> System.out.println(l == f); // true
>
> // НЕ МОЖНА ПОРІВНЮВАТИ ЧИСЛОВІ ТИПИ З BOOLEAN
> // if (i == true) { ... } // Помилка компіляції
> ```

#### **📌 Що перевіряють:**

*Знання правил приведення типів при виконанні порівнянь, що є важливою частиною розуміння роботи мови.*

* * *

### **6 ❓ Питання:**

Поясніть контракт між методами `.equals()` та `.hashCode()`. Чому він важливий?

> #### **💬 Відповідь:**
>
> **Контракт `.equals()` і `.hashCode()`** — це фундаментальне правило в Java, яке є критично важливим для коректної роботи хеш-колекцій (`HashMap`, `HashSet`, `Hashtable`).
>
> **Контракт складається з двох основних правил:**
>
> 1. **Якщо два об'єкти рівні за `.equals()`, то їхні `.hashCode()` також повинні бути рівними.**
>     `if (a.equals(b)) { assert a.hashCode() == b.hashCode(); }`
>
> 2. **Якщо `.hashCode()` двох об'єктів різні, то ці об'єкти гарантовано не є рівними за `.equals()`**
>     `if (a.hashCode() != b.hashCode()) { assert !a.equals(b); }`
>
> **Зворотнє невірне:** якщо `.hashCode()` рівні, це *не* означає, що об'єкти рівні за `.equals()`. Це називається **колізією**.
>
> **Чому це важливо:**
>
> * `HashMap` та `HashSet` використовують `hashCode()` для швидкого визначення "кошика" (bucket), в якому слід зберігати або шукати об'єкт. Якщо порушити контракт (наприклад, для рівних об'єктів `hashCode` буде різним), то при пошуку об'єкта `map.get(key)` хеш-мапа буде шукати його в неправильному кошику і **ніколи не знайде**, навіть якщо такий ключ був доданий. Це призводить до логічних помилок та витоків пам'яті.

#### **📌 Що перевіряють:**

*Глибокі знання ООП, контрактів стандартних методів та принципів роботи хеш-колекцій.*

* * *

### **7 ❓ Питання:**

Чи можна використовувати оператори `>` та `<` для порівняння рядків (`String`)? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> Оператори порівняння порядку (`>`, `<`, `>=`, `<=`) в Java визначені **лише для числових примітивних типів** (та `char`).
>
> Спроба написати `String s1 = "a"; String s2 = "b"; if (s1 < s2)` призведе до **помилки компіляції**: `bad operand types for binary operator '<'`.
>
> **Як правильно порівнювати рядки на порядок:**
>
> Для лексикографічного (словникового) порівняння рядків слід використовувати метод **`compareTo(String anotherString)`** з інтерфейсу `Comparable`.
>
> * `s1.compareTo(s2)` повертає:
>   * **Від'ємне число**, якщо `s1` лексикографічно менше `s2` (`"a" < "b"`).
>   * **Нуль**, якщо рядки рівні.
>   * **Додатне число**, якщо `s1` лексикографічно більше `s2` (`"c" > "b"`).
>
> **Приклад:**
>
> ```java
> String s1 = "apple";
> String s2 = "banana";
> if (s1.compareTo(s2) < 0) {
>     System.out.println(s1 + " йде перед " + s2);
> }
> ```

#### **📌 Що перевіряють:**

*Знання String API та правильних способів роботи з об'єктами. Розуміння, що оператори працюють переважно з примітивами.*

* * *

### **8 ❓ Питання:**

Що виведе цей код і чому?
`System.out.println('a' < 'b');`
`System.out.println('a' == 97.0);` ⭐️

> #### **💬 Відповідь:**
>
> **Вивід:**
>
> ```
> true
> true
> ```
>
> **Пояснення:**
>
> Це демонструє, як тип `char` бере участь в арифметичних та порівняльних операціях.
>
> **1. `System.out.println('a' < 'b');` → `true`**
>
> * При порівнянні двох `char`'ів Java **неявно розширює їх до `int`**, використовуючи їхні Unicode (або ASCII) коди.
> * Код символу `'a'` — `97`.
> * Код символу `'b'` — `98`.
> * Виконується порівняння `97 < 98`, яке повертає `true`.
>
> **2. `System.out.println('a' == 97.0);` → `true`**
>
> * Тут відбувається подвійне розширення типів.
> * `char 'a'` розширюється до `int` (`97`).
> * Потім `int 97` розширюється до `double` (`97.0`) для порівняння з `97.0`.
> * Виконується порівняння `97.0 == 97.0`, яке повертає `true`.
>
> Таким чином, `char` у порівняннях поводиться як ціле число.

#### **📌 Що перевіряють:**

*Знання правил numeric promotion, особливо для типу `char`.*

* * *

### **9 ❓ Питання:**

Чи можна використовувати `==` для порівняння значень `enum`? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна і навіть рекомендовано.**
>
> `enum` в Java є особливим типом, для якого JVM гарантує, що для кожної константи **існує лише один екземпляр об'єкта** в межах програми. По суті, кожен `enum`-тип є **синглтоном (singleton)**.
>
> **Приклад:**
>
> ```java
> public enum Status {
>     ACTIVE, INACTIVE
> }
>
> Status s1 = Status.ACTIVE;
> Status s2 = Status.ACTIVE;
> ```
>
> У цьому коді і `s1`, і `s2` будуть посилатися на **один і той самий об'єкт** `Status.ACTIVE`.
>
> Через цю гарантію:
>
> * **Порівняння через `==`** є повністю безпечним та надійним для `enum`-ів. Воно буде працювати так само, як `.equals()`.
> * Використання `==` є **навіть кращим** за `.equals()` з точки зору продуктивності (порівняння посилань швидше за виклик методу) і безпеки (немає ризику `NullPointerException`, якщо одна зі змінних `null`).
>
> `enum` — це один з небагатьох посилальних типів, де використання `==` є ідіоматично правильним.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як `enum` реалізовано в Java, та знання винятків із загального правила "не використовуй `==` для об'єктів".*

* * *

### **10 ❓ Питання:**

Що виведе цей код і чому?

```java
Long a = 127L;
Long b = 127L;
System.out.println(a == b);

Long c = 128L;
Long d = 128L;
System.out.println(c == d);
```

⭐️

> #### **💬 Відповідь:**
>
> Вивід може бути неочікуваним:
>
> ```
> true
> false
> ```
>
> **Пояснення:**
>
> Це та сама логіка, що і для `Integer`, але для типу **`Long`**. Java також кешує об'єкти `Long` для невеликих значень, щоб підвищити продуктивність.
>
> Стандартний діапазон кешу для `Long` такий самий, як і для `Integer`: **від -128 до 127**.
>
> 1. **`a == b` → `true`:** Значення `127` входить до кешованого діапазону. `a` та `b` посилаються на той самий об'єкт з пулу.
> 2. **`c == d` → `false`:** Значення `128` **не** входить до стандартного кешу. Тому створюються два нові, окремі об'єкти `Long`, і їхні посилання не є рівними.
>
> Це вкотре підкреслює, що покладатися на `==` для об'єктів-обгорток — погана ідея.

#### **📌 Що перевіряють:**

*Знання того, що кешування застосовується не тільки до `Integer`, але й до інших обгорток, і розуміння його наслідків.*

* * *

### **11 ❓ Питання:**

Чи можна порівнювати `null` за допомогою `==` та `.equals()`? ⭐️

> #### **💬 Відповідь:**
>
> * **З `==` — так, можна і потрібно.**
>     Оператор `==` є єдиним правильним способом перевірити, чи посилання є `null`.
>     `if (myObject == null) { ... }`
>
> * **З `.equals()` — небезпечно.**
>     Виклик будь-якого методу, включаючи `.equals()`, на `null`-посиланні призведе до **`NullPointerException`**.
>
>     ```java
>     String s = null;
>     if (s.equals("hello")) { // Кине NullPointerException!
>         // ...
>     }
>     ```
>
> **Як безпечно використовувати `.equals()` з можливим `null`:**
>
> Завжди викликайте `.equals()` на об'єкті, який **гарантовано не є `null`**, і передавайте потенційно `null` об'єкт як аргумент.
>
> ```java
> String s = null;
>
> // БЕЗПЕЧНО: літерал "hello" ніколи не буде null
> if ("hello".equals(s)) {
>     // Код не впаде, просто поверне false
> }
> ```
>
> Ця ідіома відома як **"Yoda Conditions"** і є гарною практикою для уникнення NPE.

#### **📌 Що перевіряють:**

*Навички написання безпечного коду та розуміння, як працювати з `null`.*

* * *

### **12 ❓ Питання:**

Чому цей код не скомпілюється? `boolean result = 1 > 2 > 3;` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код не скомпілюється через **невідповідність типів**.
>
> Компілятор обробляє цей вираз зліва направо, керуючись пріоритетом операторів.
>
> 1. **Перший крок:** обчислюється вираз `1 > 2`.
>     * Результат цього порівняння — **`false`**, і він має тип **`boolean`**.
>
> 2. **Другий крок:** вираз стає `false > 3`.
>
> 3. **Помилка:** Тепер компілятор намагається застосувати оператор `>` до операндів **`boolean`** та **`int`**.
>
> Оператор `>` (більше ніж) **не визначений** для типу `boolean`. Його можна застосовувати лише до числових типів. Через цю невідповідність типів компілятор видасть помилку.
>
> Такий ланцюжковий запис порівнянь, хоч і може використовуватися в математиці, є синтаксично некоректним у Java. Кожне порівняння має бути окремим і об'єднаним логічними операторами.

#### **📌 Що перевіряють:**

*Знання строгої системи типів Java та розуміння того, які оператори можна застосовувати до яких типів даних.*

* * *

### **13 ❓ Питання:**

Що виведе цей код і чому?

```java
int i = 1;
long l = 1L;
double d = 1.0;
Integer wrapperI = 1;

System.out.println(i == l);
System.out.println(i == d);
System.out.println(i == wrapperI);
```

⭐️⭐️

> #### **💬 Відповідь:**
>
> Усі три порівняння виведуть **`true`**.
>
> **Пояснення:**
>
> 1. **`System.out.println(i == l);` → `true`**
>     * Порівнюються `int` та `long`.
>     * Завдяки **numeric promotion**, `int i` розширюється до `long`.
>     * Виконується порівняння `(long)1 == 1L`, що є `true`.
>
> 2. **`System.out.println(i == d);` → `true`**
>     * Порівнюються `int` та `double`.
>     * `int i` розширюється до `double`.
>     * Виконується порівняння `(double)1 == 1.0`, що є `true`.
>
> 3. **`System.out.println(i == wrapperI);` → `true`**
>     * Це найцікавіший випадок. Порівнюються `int` та `Integer`.
>     * Якщо один з операндів оператора `==` є примітивом, а інший — обгорткою, то обгортка буде **автоматично розпакована (Unboxing)** до примітивного типу.
>     * `wrapperI` розпаковується в `int` зі значенням `1`.
>     * Виконується порівняння `1 == 1` для двох примітивів, що є `true`.
>
> Таким чином, у всіх трьох випадках порівняння зводиться до порівняння примітивних значень.

#### **📌 Що перевіряють:**

*Глибоке знання правил numeric promotion та auto-unboxing, а також їх взаємодії під час порівняння.*

* * *

### **14 ❓ Питання:**

Ви маєте два об'єкти вашого власного класу `Person`. Чому `p1 == p2` повертає `false`, навіть якщо їхні поля однакові? Як це виправити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `p1 == p2` повертає `false`, тому що `p1` та `p2` — це **два різних об'єкти**, які займають **різні місця в пам'яті (Heap)**, навіть якщо значення їхніх полів (наприклад, `name`, `age`) повністю збігаються.
>
> Оператор `==` порівнює посилання (адреси в пам'яті), які в цьому випадку є різними.
>
> **Як це виправити:**
>
> Щоб порівнювати об'єкти за їхнім **логічним змістом**, потрібно **перевизначити (`@Override`)** метод **`equals()`** (і, за контрактом, метод `hashCode()`) у класі `Person`.
>
> **Приклад правильної реалізації:**
>
> ```java
> public class Person {
>     private String name;
>     private int age;
>
>     // ... конструктор, геттери ...
>
>     @Override
>     public boolean equals(Object o) {
>         // 1. Перевірка на той самий об'єкт
>         if (this == o) return true;
>         // 2. Перевірка на null та на відповідність класу
>         if (o == null || getClass() != o.getClass()) return false;
>         // 3. Приведення типу та порівняння полів
>         Person person = (Person) o;
>         return age == person.age && java.util.Objects.equals(name, person.name);
>     }
>
>     @Override
>     public int hashCode() {
>         // 4. Реалізація hashCode на основі тих же полів
>         return java.util.Objects.hash(name, age);
>     }
> }
> ```
>
> Тепер, виклик `p1.equals(p2)` коректно порівняє поля `name` та `age` і поверне `true`, якщо вони однакові.

#### **📌 Що перевіряють:**

*Знання ООП-концепцій, зокрема, правильної реалізації методів `equals` та `hashCode`, що є критично важливим для створення власних типів даних.*

* * *

### **15 ❓ Питання:**

У яких випадках вираз `a != a` може повернути `true`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це може статися лише в одному випадку в Java — коли змінна `a` має значення **`NaN` (Not a Number)**.
>
> `NaN` — це спеціальне значення для типів з плаваючою комою (`float` та `double`), яке представляє невизначений результат арифметичної операції (наприклад, `0.0 / 0.0` або `Math.sqrt(-1)`).
>
> **Головна властивість `NaN`:**
> `NaN` є **невпорядкованим**. Згідно зі специфікацією IEEE 754, будь-яке порівняння за допомогою операторів `==`, `<`, `>` з `NaN`, включаючи порівняння `NaN` з самим собою, **завжди повертає `false`**.
>
> **Приклад:**
>
> ```java
> double a = Double.NaN;
>
> System.out.println(a == a);    // false
> System.out.println(a != a);    // true
> System.out.println(a < a);     // false
> System.out.println(a > a);     // false
> ```
>
> **Як правильно перевіряти на `NaN`:**
>
> Оскільки `a == Double.NaN` завжди `false`, для перевірки, чи є змінна `NaN`, потрібно використовувати статичний метод `Double.isNaN()` або `Float.isNaN()`.
>
> ```java
> if (Double.isNaN(a)) {
>     System.out.println("a is NaN");
> }
> ```
>
> Це єдиний випадок у Java, де порівняння змінної з самою собою на нерівність дає `true`.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про типи даних з плаваючою комою та їхні спеціальні значення. Це питання на знання "темних кутків" мови.*
