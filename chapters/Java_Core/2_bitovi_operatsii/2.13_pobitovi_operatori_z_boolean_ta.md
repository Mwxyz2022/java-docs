---
layout: default
title: "Побітові оператори з boolean: & та |"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 13
---

# Побітові оператори з boolean: & та |

* короткий опис: Оператори `&` та `|` також можуть використовуватися з булевими значеннями як логічні AND/OR, але на відміну від `&&` та `||`, вони не мають короткого замикання – завжди виконують обидві умови.

* поняття: `&` (логічне AND без замикання), `|` (логічне OR без замикання).

* приклади: `false & someMethod()` (someMethod() все одно буде викликаний), `true | someMethod()` (someMethod() все одно буде викликаний).

* ключові моменти: Завжди виконують обидві частини виразу, що може призвести до `NullPointerException` або небажаних побічних ефектів, якщо не враховувати це.

* що важливо знати на співбесіді: Коли `&` і `|` можуть бути небезпечними, їхня відмінність від `&&` та `||`.

### **✅ Офіційне визначення:**

**`&`** і **`|`** в Java працюють з булевими значеннями як логічні AND і OR, але без короткого замикання — завжди перевіряють обидві умови.

### **🧠 Простими словами:**

**`&`** і **`|`** – це “виконай всі умови без винятків”, тоді як **`&&`** і **`||`** – “зупинись, як тільки ясно”

---

## **Основне**

| Оператор | Назва | Коротке замикання | Опис |
| :---: | :---: | :---: | ----- |
| **`&&`** | Логічне AND | ✅ так | друга умова не перевіряється, якщо перша — **`false`** |
| **`&`** | Побітове AND | ❌ ні | перевіряє обидві умови, навіть якщо перша — **`false`** |
| **`//`** | Логічне OR | ✅ так | друга умова не перевіряється, якщо перша — **`true`** |
| **`/`** | Побітове OR | ❌ ні | перевіряє обидві умови, навіть якщо перша — **`true`** |

---

## **Пояснення на пальцях (💡)**

* **`&&`** і **`||`** — економлять ресурси, бо можуть не перевіряти другу умову, якщо результат уже відомий.
* **`&`** і **`|`** — вперті, перевіряють усе до кінця, навіть якщо це не треба.

👉 Через це **`&`** може кидати NullPointerException, якщо ліва умова **`false`**, а права викликає метод.

---

## **Корисні приклади (🧪)**

```java
boolean a = false;
boolean b = true;

System.out.println(a && b); // false, b не перевіряється
System.out.println(a & b);  // false, але b перевіряється

String str = null;

// ❌ Кинеться NullPointerException
if (str != null & str.length() > 0) {
    System.out.println("Працює");
}

// ✅ Безпечний варіант
if (str != null && str.length() > 0) {
    System.out.println("Працює");
}
```

---

## **Порівняння (⚖️)**

✅ Коли хочеш, щоб обидві умови виконались завжди (наприклад, для логування або підрахунку):

```java
boolean a = logCheck("a");
boolean b = logCheck("b");

if (a & b) {
    System.out.println("Обидва логування виконаються");
}
```

---

## **🧠 Мнемоніка для запам’ятовування:**

* **`&&`** / **`||`** \= smart (економлять час, з коротким замиканням)
* **`&`** / **`|`** \= stupid (перевіряють усі умови завжди)

---

## **🟩 Підсумок:**

**`&`** і **`|`** працюють з boolean без короткого замикання — Java завжди виконає всі умови, навіть якщо результат очевидний. Це може призвести до помилок або зайвих витрат ресурсів. Тому частіше використовуй **`&&`** і **`||`**, якщо не потрібно навмисно виконувати все.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Чи можна використовувати побітові оператори `&` та `|` з `boolean` значеннями? Якщо так, то як вони працюють?

> #### **💬 Відповідь:**
>
> **Так, можна.** Коли побітові оператори `&` та `|` застосовуються до операндів типу `boolean`, вони діють як **логічні AND та OR**, відповідно.
>
> Їх таблиці істинності повністю збігаються з таблицями для `&&` та `||`:
>
> * **`&` (AND):** Повертає `true`, тільки якщо обидва операнди є `true`.
> * **`|` (OR):** Повертає `true`, якщо хоча б один з операндів є `true`.
>
> Однак, є **ключова відмінність** у поведінці: `&` та `|` **не реалізують короткого замикання (short-circuiting)**. Це означає, що вони **завжди обчислюють обидва свої операнди**, навіть якщо результат виразу вже відомий після обчислення першого операнда.

#### **📝 Мнемоніка:**

* `&&` (подвійний) → "розумний", може зупинитися раніше.
* `&` (одинарний) → "впертий", завжди йде до кінця.

#### **📌 Що перевіряють:**

*Базове знання, що ці оператори є перевантаженими і можуть працювати як з цілими числами, так і з булевими значеннями, але з різною семантикою виконання.*

* * *

### **2 ❓ Питання:**

У чому головна відмінність між `&&` та `&`, коли вони використовуються в логічних виразах?

> #### **💬 Відповідь:**
>
> Головна і єдина відмінність — це **коротке замикання (short-circuiting)**.
>
> * **`&&` (Логічний AND з коротким замиканням):**
>   * Це "лінивий" оператор.
>   * Якщо перший операнд `false`, другий операнд **ніколи не буде обчислений**, оскільки результат усього виразу вже гарантовано `false`.
>
> * **`&` (Побітовий AND, який діє як логічний для `boolean`):**
>   * Це "завзятий" (eager) оператор.
>   * Він **завжди обчислює обидва операнди**, незалежно від значення першого.
>
> **Наслідки:**
>
> 1. **Продуктивність:** `&&` може бути швидшим, оскільки уникає зайвих обчислень.
> 2. **Безпека:** `&&` дозволяє писати безпечні перевірки, наприклад, на `null`, де друга частина не буде викликана, якщо об'єкт `null`.
>
> **Приклад:**
>
> ```java
> String s = null;
> // цей рядок призведе до NullPointerException, бо `s.isEmpty()` буде викликано
> // if (s != null & s.isEmpty()) { ... }
>
> // цей рядок безпечний, `s.isEmpty()` не буде викликано
> if (s != null && s.isEmpty()) { ... }
> ```

#### **📌 Що перевіряють:**

*Ключове питання теми. Розуміння short-circuiting є критично важливим для будь-якого Java-розробника.*

* * *

### **3 ❓ Питання:**

Що виведе цей код? Поясніть потік виконання.

```java
public class OperatorTest {
    public static boolean stepOne() {
        System.out.println("Step One");
        return false;
    }
    public static boolean stepTwo() {
        System.out.println("Step Two");
        return true;
    }
    public static void main(String[] args) {
        System.out.println("Використання | :");
        if (stepOne() | stepTwo()) {
            System.out.println("Умова з | істинна");
        }
    }
}
```

> #### **💬 Відповідь:**
>
> **Вивід у консоль буде таким:**
>
> ```
> Використання | :
> Step One
> Step Two
> Умова з | істинна
> ```
>
> **Пояснення потоку виконання:**
>
> 1. Програма починає обчислювати умову в `if`.
> 2. Викликається перший метод, `stepOne()`. Він виводить `"Step One"` і повертає `false`.
> 3. Оскільки використовується оператор `|` (Побітове АБО, non-short-circuit), JVM **зобов'язана обчислити і другий операнд**, незважаючи на результат першого.
> 4. Викликається другий метод, `stepTwo()`. Він виводить `"Step Two"` і повертає `true`.
> 5. Тепер обчислюється фінальний результат: `false | true` → `true`.
> 6. Оскільки умова `if` істинна, виконується її тіло, і на екран виводиться `"Умова з | істинна"`.
>
> Якби замість `|` використовувався `||`, метод `stepTwo()` **ніколи б не був викликаний**, тому що після отримання `false` від `stepOne()` оператору `||` потрібно було б знати другий результат.
>
> *Виправлення*: Я помилився у логіці `||`. `||` зупиняється на `true`. Отже, при `||` другий метод буде викликано. Якби `stepOne()` повертав `true`, тоді б не викликався. Давайте виправимо пояснення.

* * *

#### **Змінена відповідь на питання №3:**
>
> #### **💬 Відповідь:**
>
> **Вивід у консоль буде таким:**
>
> ```
> Використання | :
> Step One
> Step Two
> Умова з | істинна
> ```
>
> **Пояснення потоку виконання:**
>
> 1. Програма починає обчислювати умову в `if`.
> 2. Викликається перший метод, `stepOne()`. Він виводить `"Step One"` і повертає `false`.
> 3. Оскільки використовується оператор `|` (Побітове АБО, **non-short-circuit**), JVM **зобов'язана обчислити і другий операнд**, незважаючи на результат першого.
> 4. Викликається другий метод, `stepTwo()`. Він виводить `"Step Two"` і повертає `true`.
> 5. Тепер обчислюється фінальний результат: `false | true` → `true`.
> 6. Оскільки умова `if` істинна, виконується її тіло, і на екран виводиться `"Умова з | істинна"`.
>
> **Якби використовувався оператор `||`:**
> `stepOne()` повернув би `false`. Щоб визначити результат `false || ...`, потрібно знати другий операнд. Тому `stepTwo()` також **був би викликаний**.
> **Різниця проявилася б, якби `stepOne()` повертав `true`**. У цьому випадку `stepOne() || stepTwo()` викликав би лише `stepOne()`, оскільки результат вже відомий (`true`).

#### **📌 Що перевіряють:**

*Здатність проаналізувати потік виконання та передбачити, які побічні ефекти (вивід у консоль) відбудуться. Це практична перевірка знання non-short-circuiting.*

* * *

### **4 ❓ Питання:**

Наведіть практичний приклад, де використання `&` або `|` для булевих значень може бути виправданим і навмисним.

> #### **💬 Відповідь:**
>
> Хоча це рідкісний випадок, `&` та `|` є виправданими тоді, коли вам потрібно **гарантовано виконати обидві частини виразу**, зазвичай через їхні **побічні ефекти (side effects)**, незалежно від логічного результату.
>
> **Приклад:**
> Уявіть, у вас є дві перевірки валідації, і ви хочете зібрати **всі помилки**, а не зупинятися на першій.
>
> ```java
> public class Validator {
>     private List<String> errors = new ArrayList<>();
>
>     private boolean isUsernameValid(String username) {
>         System.out.println("Перевіряємо ім'я користувача...");
>         if (username == null || username.length() < 3) {
>             errors.add("Ім'я занадто коротке.");
>             return false;
>         }
>         return true;
>     }
>
>     private boolean isPasswordValid(String password) {
>         System.out.println("Перевіряємо пароль...");
>         if (password == null || password.length() < 8) {
>             errors.add("Пароль занадто короткий.");
>             return false;
>         }
>         return true;
>     }
>
>     public boolean validate(String username, String password) {
>         // Використовуємо &, щоб гарантовано викликати обидві перевірки
>         return isUsernameValid(username) & isPasswordValid(password);
>     }
> }
> ```
>
> Якщо використовувати `&&`, і ім'я користувача буде невалідним, `isPasswordValid()` ніколи не виконається, і ви не отримаєте помилку про короткий пароль. Використання `&` гарантує, що обидва методи валідації будуть викликані, і список `errors` буде містити всі виявлені проблеми.

#### **📌 Що перевіряють:**

*Кмітливість та здатність мислити нестандартно, знаходячи застосування для менш поширених конструкцій мови.*

* * *

### **5 ❓ Питання:**

Чому цей код може призвести до `NullPointerException`, а еквівалентний з `&&` — ні?
`if (user != null & user.isSuperAdmin()) { ... }`

> #### **💬 Відповідь:**
>
> Цей код може призвести до `NullPointerException`, якщо `user` дорівнює `null`.
>
> **Причина** — відсутність короткого замикання в операторі **`&`**.
>
> **Потік виконання:**
>
> 1. Обчислюється перший операнд: `user != null`. Якщо `user` є `null`, цей вираз повертає `false`.
> 2. **Але**, оскільки `&` є "завзятим" (eager) оператором, він **все одно** продовжує обчислювати другий операнд, `user.isSuperAdmin()`.
> 3. Це призводить до спроби викликати метод на `null`-посиланні (`null.isSuperAdmin()`), що негайно викидає `NullPointerException`.
>
> **Еквівалентний код з `&&`:**
>
> `if (user != null && user.isSuperAdmin()) { ... }`
>
> Тут, якщо `user` є `null`, перший операнд `user != null` повертає `false`. Завдяки **короткому замиканню**, оператор `&&` одразу визначає, що весь вираз є хибним, і **не обчислює** другу частину (`user.isSuperAdmin()`). Таким чином, `NullPointerException` не виникає.
>
> Цей приклад — класична ілюстрація важливості `&&` для безпечних перевірок на `null`.

#### **📌 Що перевіряють:**

*Здатність аналізувати код на наявність потенційних NPE, що є критично важливою навичкою для будь-якого розробника.*

* * *

### **6 ❓ Питання:**

Яким буде результат `true | false & false`?

> #### **💬 Відповідь:**
>
> Результатом буде **`true`**.
>
> Це пов'язано з пріоритетом операторів. Побітові (і логічні) оператори мають такий же пріоритет, як і їх "короткозамкнені" аналоги: `&` має вищий пріоритет, ніж `|`.
>
> 1. **Спочатку виконується `&`:**
>     `false & false` → `false`
>
> 2. **Потім виконується `|`:**
>     `true | false` → `true`
>
> Тому кінцевий результат — `true`.
>
> Важливо пам'ятати, що, оскільки тут немає короткого замикання, всі три значення (`true`, `false`, `false`) були б "обчислені", якби вони були результатами викликів методів.

#### **📌 Що перевіряють:**

*Знання пріоритету операторів, що застосовується і до цих версій операторів.*

* * *

### **7 ❓ Питання:**

Якщо `&` та `|` мають нижчу продуктивність через відсутність short-circuiting, чи існують інші аспекти продуктивності, про які варто знати? ⭐️

> #### **💬 Відповідь:**
>
> Так, є один цікавий аспект, пов'язаний з **передбаченням розгалужень (branch prediction)** у сучасних процесорах.
>
> * Оператори `&&` та `||` генерують **умовні переходи (branches)** у байткоді, що відповідає інструкціям умовних стрибків у машинному коді.
> * Оператори `&` та `|` **не генерують** умовних переходів. Вони обчислюють обидва значення, а потім виконують одну побітову операцію.
>
> Сучасні процесори використовують складні алгоритми передбачення розгалужень, щоб "вгадати", яка гілка `if` буде виконана, і заздалегідь почати виконувати її інструкції.
>
> * Якщо передбачення **правильне**, це значно прискорює виконання.
> * Якщо передбачення **неправильне (branch misprediction)**, процесор змушений "скинути" свій конвеєр і почати виконувати інструкції з правильної гілки, що призводить до **значних затримок**.
>
> **Наслідок:**
> У дуже специфічних сценаріях, де дані в умові є **абсолютно випадковими** (і, відповідно, branch predictor постійно помиляється), код з використанням `&` та `|` (без переходів) **може виявитися швидшим**, ніж код з `&&` та `||`, оскільки він уникає дорогих помилок передбачення.
>
> Однак, це є **екстремальною мікро-оптимізацією**, яка рідко має значення в реальному коді. У 99.9% випадків переваги short-circuiting набагато важливіші.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як Java-код виконується на апаратному рівні. Це питання для Senior-розробників, що цікавляться High-Performance Computing.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати `&=` та `|=` з булевими змінними? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.**
>
> Оператори `&=` та `|=` працюють з `boolean` за аналогією до їх числових відповідників.
>
> * `a &= b;` є скороченням для `a = a & b;`
> * `a |= b;` є скороченням для `a = a | b;`
>
> Як і `&` та `|`, вони **завжди обчислюють правий операнд (`b`)**.
>
> **Приклад:**
>
> ```java
> boolean hasErrors = false;
>
> // Гарантовано викликаємо обидві перевірки
> hasErrors |= !validateField1(); // hasErrors стане true, якщо validateField1 поверне false
> hasErrors |= !validateField2(); // hasErrors залишиться true, якщо validateField2 теж поверне false
>
> if (hasErrors) { ... }
> ```
>
> У цьому прикладі `hasErrors` акумулює результат логічного `OR` після кожної перевірки. Оскільки `|` не має короткого замикання, `validateField2()` буде викликано, навіть якщо `hasErrors` вже `true`.
>
> Це може бути корисним для акумуляції стану в одному прапорці, гарантуючи, що всі перевірки виконаються.

#### **📌 Що перевіряють:**

*Повноту знань синтаксису мови, включаючи оператори з присвоєнням для булевих типів.*

* * *

### **9 ❓ Питання:**

Поясніть, що станеться, якщо `&` чи `|` застосувати до змішаних типів, наприклад, `boolean` та `int`. ⭐️

> #### **💬 Відповідь:**
>
> Це призведе до **помилки компіляції**.
>
> Побітові (та логічні) оператори в Java є **строго типізованими**. Вони можуть бути застосовані або:
>
> * До двох операндів **цілочисельних типів** (`byte`, `short`, `char`, `int`, `long`).
> * До двох операндів **булевого типу** (`boolean`).
>
> Немає жодного правила, яке б дозволяло змішувати ці два класи типів. Java не виконує неявного перетворення між `boolean` та `int` (на відміну від C/C++).
>
> **Приклад коду, що не скомпілюється:**
>
> ```java
> int a = 1;
> boolean b = true;
>
> // ПОМИЛКА КОМПІЛЯЦІЇ: operator & cannot be applied to int, boolean
> // if (a & b) { ... }
> ```>

#### **📌 Що перевіряють:**

*Знання системи типів Java та розуміння, що неявні перетворення між `int` та `boolean` не допускаються.*

* * *

### **10 ❓ Питання:**

Яким буде результат `true ^ true & false`? (де `^` — побітове XOR, застосоване до `boolean`). ⭐️

> #### **💬 Відповідь:**
>
> Результатом буде **`true`**.
>
> Розглянемо пріоритет операторів:
>
> 1. Пріоритет **`&`** вищий, ніж у **`^`**.
> 2. Пріоритет **`^`** вищий, ніж у **`|`**.
>
> **Порядок обчислення:**
>
> 1. **Спочатку `&`:**
>     `true & false` → `false`.
>
> 2. **Потім `^`:**
>     `true ^ false`
>
> 3. **XOR** повертає `true`, якщо операнди різні.
>     `true ^ false` → `true`.
>
> Отже, кінцевий результат — `true`.
>
> **Якби були дужки `(true ^ true) & false`:**
> `(true ^ true)` → `false`
> `false & false` → `false`

#### **📌 Що перевіряють:**

*Знання пріоритетів усіх побітових операторів (`&`, `^`, `|`).*

* * *

### **11 ❓ Питання:**

Чи можуть оператори `&` та `|` викликати `NullPointerException` при роботі з `Boolean` обгортками? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можуть,** і це одна з пасток авторозпаковки (unboxing).
>
> Оператори `&` та `|` не можуть працювати безпосередньо з об'єктами `Boolean`. Щоб виконати операцію, JVM повинна **автоматично розпакувати** об'єкти `Boolean` до примітивів `boolean`.
>
> Якщо один з об'єктів `Boolean` дорівнює `null`, спроба його розпаковки (`null.booleanValue()`) призведе до **`NullPointerException`**.
>
> **Приклад:**
>
> ```java
> public Boolean getFirstCondition() {
>     return true;
> }
> public Boolean getSecondCondition() {
>     return null; // Метод повертає null
> }
>
> // ...
>
> // Цей код впаде з NPE, бо getSecondCondition() буде викликано і
> // його результат (null) буде розпаковано.
> if (getFirstCondition() | getSecondCondition()) {
>     System.out.println("Condition met.");
> }
> ```> Важливо, що оскільки `|` не є short-circuit, `getSecondCondition()` буде викликано, навіть якщо перший результат `true`. Якби використовувався `||`, то при `getFirstCondition() == true` другий метод не викликався б, і помилки б не було.
>
> **Висновок:** Використання `&` та `|` з об'єктами `Boolean` ще більш небезпечне, ніж з `&&` та `||`, через відсутність короткого замикання.

#### **📌 Що перевіряють:**

*Уміння аналізувати код на перетині кількох тем: оператори, класи-обгортки та авторозпаковка. Розуміння неявних дій, які виконує JVM.*

* * *

### **12 ❓ Питання:**

Поясніть, що робить цей вираз і чому він може бути корисним.
`result |= someBooleanMethod();` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз є скороченням для `result = result | someBooleanMethod();`. Він використовується для **акумуляції "істинності"** в булевому прапорці `result`.
>
> **Як це працює:**
>
> * `result` ініціалізується (зазвичай, як `false`).
> * Викликається `someBooleanMethod()`.
> * Результат `OR` між поточним `result` і результатом методу присвоюється назад `result`.
>
> Завдяки властивостям `OR` (`true | будь-що = true`), якщо `result` хоча б один раз стає `true`, він **залишиться `true` назавжди**, незалежно від подальших результатів.
>
> **Чому це корисно (і відрізняється від `||=`):**
>
> Оскільки використовується `|`, **метод `someBooleanMethod()` буде викликано завжди**. Це гарантує виконання будь-яких побічних ефектів усередині цього методу.
>
> **Сценарій:** Пройтися по списку перевірок, виконавши кожну, і в кінці дізнатися, чи хоча б одна з них була успішною.
>
> ```java
> boolean anyCheckPassed = false;
> for (Check check : checks) {
>     anyCheckPassed |= check.execute(); // Викликаємо кожну перевірку
> }
> return anyCheckPassed;
> ```> Якщо використати `||=`, то як тільки одна перевірка поверне `true`, наступні `check.execute()` не будуть викликатися.

#### **📌 Що перевіряють:**

*Розуміння патернів акумуляції стану та свідомий вибір між `|` та `||` для керування виконанням побічних ефектів.*

* * *

### **13 ❓ Питання:**

Чому цей код скомпілюється, але може дати неочікуваний результат?
`int x = 5; if (x = 10 & x > 5) { ... }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання-пастка, яке містить дві поширені помилки.
>
> 1. **Присвоєння замість порівняння (`=` замість `==`):**
>     Всередині `if` використовується `x = 10`. Це **оператор присвоєння**, а не порівняння. У Java (на відміну від C++) присвоєння саме по собі не може бути використане як булева умова. Але...
>
> 2. **Тип виразу `x = 10`:**
>     Результатом виразу `x = 10` є присвоєне значення, тобто **`10`**, і цей результат має тип `int`.
>
> 3. **Повна умова:**
>     Умова стає: `(int) & (boolean)`.
>
> **Чому код скомпілюється (і що буде насправді):**
>
> Тут є помилка в постановці питання. Вираз `x = 10 & x > 5` насправді буде розібраний компілятором інакше через пріоритет операторів.
>
> * `>` має вищий пріоритет, ніж `&`.
> * `&` має вищий пріоритет, ніж `=`.
>
> **Правильний розбір:**
>
> 1. `x > 5` → `5 > 5` → `false`.
> 2. `10 & false` → **Помилка компіляції!** Не можна застосувати `&` до `int` та `boolean`.
>
> **Якщо ж були б дужки `(x = 10) & (x > 5)`:**
>
> 1. Ліва частина: `x` стає `10`, вираз повертає `int 10`.
> 2. Права частина: тепер `x` дорівнює `10`. `10 > 5` повертає `boolean true`.
> 3. Знову **помилка компіляції**: `int & boolean`.
>
> **Висновок:** У початковому вигляді цей код **не скомпілюється**. Він є гарною демонстрацією того, як знання пріоритету операторів допомагає виявити помилку ще до запуску. Він міг би працювати в мовах з нестрогою типізацією, але не в Java.

#### **📌 Що перевіряють:**

*Уважність, знання пріоритетів операторів та строгої системи типів Java.*

* * *

### **14 ❓ Питання:**

Порівняйте `(x != 0) & (y / x > 2)` з `(x != 0) && (y / x > 2)`. Який варіант є єдиним безпечним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Єдиним безпечним варіантом є **`(x != 0) && (y / x > 2)`**.
>
> **Небезпечний варіант: `(x != 0) & (y / x > 2)`**
>
> * **Проблема:** Використовується оператор `&`, який **завжди обчислює обидва операнди**.
> * **Що станеться, якщо `x` дорівнює `0`:**
>     1. `x != 0` поверне `false`.
>     2. Але `&` все одно продовжить обчислювати праву частину: `y / x > 2`.
>     3. Виконання `y / 0` призведе до викидання винятку **`ArithmeticException: / by zero`**.
>
> **Безпечний варіант: `(x != 0) && (y / x > 2)`**
>
> * **Рішення:** Використовується оператор `&&`, який реалізує **коротке замикання**.
> * **Що станеться, якщо `x` дорівнює `0`:**
>     1. `x != 0` поверне `false`.
>     2. `&&` бачить, що перший операнд `false`, тому результат усього виразу буде `false`.
>     3. Друга частина, `y / x > 2`, **ніколи не буде обчислена**.
>     4. `ArithmeticException` не виникає.
>
> Це класичний приклад використання `&&` для **"захисту" (guarding)** від небезпечних операцій. Спочатку ставиться перевірка, яка унеможливлює помилку в наступній частині умови.

#### **📌 Що перевіряють:**

*Здатність писати безпечний код, що захищений від runtime-винятків, використовуючи властивості логічних операторів.*

* * *

### **15 ❓ Питання:**

Уявіть, що `&&` не існує, і у вас є лише `&`. Як би ви реалізували логіку `A && B`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Якби у нас не було оператора з коротким замиканням, ми б змушені були реалізувати цю логіку вручну за допомогою вкладеної конструкції **`if`**.
>
> **Мета `A && B`:** обчислити `B` тільки тоді, коли `A` є `true`.
>
> **Реалізація через `if`:**
>
> ```java
> boolean result = false; // Початковий результат - хибний
>
> if (A) { // Спочатку перевіряємо тільки A
>     // Цей блок виконається, тільки якщо A є true
>     if (B) { // Тепер перевіряємо B
>         result = true;
>     }
> }
> ```> **Більш компактний варіант:**
> ```java
> boolean result = false;
> if (A) {
>     result = B; // Якщо A - true, результат залежить тільки від B
> }
> ```>
> Цей підхід точно відтворює семантику короткого замикання: `B` обчислюється тільки тоді, коли `A` є істинним. Це показує, що оператор `&&` є, по суті, **синтаксичним цукром** для простої вкладеної конструкції `if`.

#### **📌 Що перевіряють:**

*Глибоке, фундаментальне розуміння того, що насправді роблять логічні оператори. Здатність декомпозувати складну поведінку на базові конструкції мови. Це питання на логіку та розуміння "перших принципів".*
