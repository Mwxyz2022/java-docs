---
layout: default
title: "Перетворення Binary -> Decimal"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 4
---

# Перетворення Binary -> Decimal

* короткий опис: Обчислення десяткового значення двійкового числа як суми добутків кожного біта на 2 у відповідному степені позиції, починаючи з 0 справа наліво.

* поняття: Степінь 2, позиція, сума.

* приклади: Перетворення `10111` (двійкове) у `23` (десяткове).

* ключові моменти: Кожна позиція відповідає степені 2 (2^0, 2^1, ...), значення біта множиться на цей степінь.

* що важливо знати на співбесіді: Як вручну переводити числа.

### **✅ Офіційне визначення:**

Перетворення з двійкової у десяткову систему — це обчислення значення двійкового числа як суми добутків кожного біта на 2 у відповідному степені позиції, починаючи з 0 справа наліво.

### **🧠 Простими словами:**

Уяви, що кожна цифра 0 або 1 — це маленький пазлик у великій мозаїці. Кожен пазлик — це степінь 2\.

Складаєш всі пазлики, де стоїть 1, і отримуєш число у звичній десятковій системі

---

## **Основне**

| Позиція (справа) | Цифра | Степінь 2 | Добуток |
| :---: | :---: | :---: | :---: |
| 0 | 1 | 2⁰ \= 1 | 1 × 1 \= 1 |
| 1 | 1 | 2¹ \= 2 | 1 × 2 \= 2 |
| 2 | 1 | 2² \= 4 | 1 × 4 \= 4 |
| 3 | 0 | 2³ \= 8 | 0 × 8 \= 0 |
| 4 | 1 | 2⁴ \= 16 | 1 × 16 \= 16 |

Складаємо: 16 \+ 0 \+ 4 \+ 2 \+ 1 \= **23**

---

## **Пояснення на пальцях (💡)**

* Починаємо справа — це 2⁰, найменший розряд.
* Якщо цифра 1 — додаємо відповідне значення.
* Якщо 0 — пропускаємо (бо 0 × щось \= 0).
* Це як гра в «склади пазл», де рахуються лише одиниці.

---

## **Корисні приклади (🧪)**

| Двійкове число | Обчислення | Десяткове |
| :---: | :---: | :---: |
| 10111 | 1×16 \+ 0×8 \+ 1×4 \+ 1×2 \+ 1×1 | 23 |
| 101 | 1×4 \+ 0×2 \+ 1×1 | 5 |
| 1100 | 1×8 \+ 1×4 \+ 0×2 \+ 0×1 | 12 |
| 1111 | 1×8 \+ 1×4 \+ 1×2 \+ 1×1 | 15 |
| 100000 | 1×32 \+ 0 \+ ... | 32 |
| 10010 | 1×16 \+ 0 \+ 0 \+ 1×2 \+ 0 | 18 |

---

## **Порівняння (⚖️)**

| Binary | Десяткове пояснення | Decimal |
| :---: | :---: | :---: |
| 110 | 1×4 \+ 1×2 \+ 0×1 | 6 |
| 1001 | 1×8 \+ 0 \+ 0 \+ 1×1 | 9 |
| 111 | 1×4 \+ 1×2 \+ 1×1 | 7 |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Binary -> Multiply by powers of 2 -> Add**

(Двійкове -> Помножити на степені 2 -> Скласти)

---

## **🟩 Підсумок:**

Щоб перевести двійкове число в десяткове — помнож кожну цифру на відповідний степінь 2, починаючи справа, і склади ці значення.

Наприклад, **`10111 -> 23.`**

Звісно. Ця тема є логічним продовженням попередньої, тому я складу питання, фокусуючись на **алгоритмах, реалізації, граничних випадках та зв'язку з Java API**.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Опишіть класичний алгоритм переведення двійкового числа у десяткове.

> #### **💬 Відповідь:**
>
> Класичний алгоритм базується на визначенні **позиційної системи числення**. Кожен біт у двійковому числі має свою "вагу", яка є степенем двійки.
>
> **Покроковий процес:**
>
> 1. Пронумеруйте позиції бітів у двійковому числі **справа наліво**, починаючи з 0.
> 2. Для кожного біта, що дорівнює **1**, обчисліть його вагу: `2` у степені `позиція_біта`.
> 3. **Підсумуйте** ваги всіх бітів, що дорівнюють 1. Біти, що дорівнюють 0, можна ігнорувати, оскільки їхній внесок у суму завжди буде нулем.
>
> **Приклад для двійкового числа `1101`:**
>
> * Крайній правий біт `1` стоїть на позиції `0`. Його вага: `1 * 2⁰ = 1`.
> * Наступний біт `0` стоїть на позиції `1`. Його вага: `0 * 2¹ = 0`.
> * Наступний біт `1` стоїть на позиції `2`. Його вага: `1 * 2² = 4`.
> * Крайній лівий біт `1` стоїть на позиції `3`. Його вага: `1 * 2³ = 8`.
>
> Тепер підсумовуємо всі ваги: `1 + 0 + 4 + 8 = 13`.
> Отже, двійкове `1101` дорівнює десятковому `13`.

#### **📌 Що перевіряють:**

*Знання базової теорії систем числення та здатності застосувати формулу позиційної системи для двійкового коду.*

* * *

### **2 ❓ Питання:**

Напишіть Java-метод, який перетворює рядок з двійковим числом на десятковий `int` без використання `Integer.parseInt()`.

> #### **💬 Відповідь:**
>
> Звісно. Для цього можна пройтися по рядку і для кожного символу '1' додати до результату відповідний степінь двійки.
>
> ```java
> public class BinaryToDecimalConverter {
>
>     public static int convert(String binaryString) {
>         if (binaryString == null || binaryString.isEmpty()) {
>             throw new IllegalArgumentException("Вхідний рядок не може бути порожнім.");
>         }
>
>         int decimalValue = 0;
>         int power = 0;
>
>         // Ітеруємо по рядку справа наліво
>         for (int i = binaryString.length() - 1; i >= 0; i--) {
>             char bit = binaryString.charAt(i);
>
>             if (bit == '1') {
>                 decimalValue += Math.pow(2, power);
>             } else if (bit != '0') {
>                 throw new NumberFormatException("Рядок містить невалідні символи: " + bit);
>             }
>             
>             power++;
>         }
>
>         return decimalValue;
>     }
>
>     public static void main(String[] args) {
>         System.out.println(convert("1101")); // Виведе: 13
>         System.out.println(convert("101010")); // Виведе: 42
>     }
> }
> ```
>
> Цей метод також ілюструє, як важливо валідувати вхідні дані (перевірка на не-'0' та не-'1' символи).

#### **📝 Коментар:**

Це також дуже поширена задача на кодування, що перевіряє вміння працювати з рядками та циклами.

#### **📌 Що перевіряють:**

*Навички написання коду: ітерація по рядку, обробка символів, використання математичних функцій та валідація вхідних даних.*

* * *

### **3 ❓ Питання:**

Який спосіб перетворення двійкового рядка в десяткове число є найефективнішим у Java і чому?

> #### **💬 Відповідь:**
>
> Найефективнішим способом є використання вбудованого статичного методу **`Integer.parseInt(String s, int radix)`**.
>
> **Приклад:**
>
> ```java
> int decimal = Integer.parseInt("1101", 2); // decimal буде 13
> ```
>
> **Чому він найефективніший:**
>
> 1. **Нативна реалізація:** "Під капотом" цей метод зазвичай реалізований значно ефективніше, ніж можна написати на чистій Java. Частина його логіки може бути реалізована на C/C++ або оптимізована JIT-компілятором до нативних машинних інструкцій.
>
> 2. **Оптимізації:** Він містить численні оптимізації для швидкої обробки рядка, перевірки на переповнення та роботи з різними основами системи числення. Наприклад, він може використовувати побітові зсуви замість повільних викликів `Math.pow()`.
>
> 3. **Надійність:** Метод коректно обробляє різні граничні випадки, як-от переповнення `Integer`, та кидає стандартизований виняток `NumberFormatException` при некоректних даних.
>
> Написання власної реалізації є гарною вправою, але в реальному коді для продуктивності та надійності завжди слід віддавати перевагу вбудованим методам JDK.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки та розуміння того, що вбудовані рішення зазвичай є кращими за власні "велосипеди" з точки зору продуктивності та надійності.*

* * *

### **4 ❓ Питання:**

Що таке **схема Горнера**, і як її можна використати для більш ефективного перетворення двійкового рядка в десяткове число? ⭐️

> #### **💬 Відповідь:**
>
> **Схема Горнера** — це ефективний алгоритм для обчислення значення полінома. Перетворення числа з будь-якої системи числення з основою `b` можна розглядати як обчислення полінома.
>
> Для двійкового числа `d_n d_{n-1} ... d_1 d_0` (де `d` - це біт), його десяткове значення — це:
> `d_n*2^n + d_{n-1}*2^{n-1} + ... + d_1*2^1 + d_0*2^0`
>
> Схема Горнера дозволяє переписати цей вираз так, щоб уникнути дорогих операцій піднесення до степеня:
> `((...((d_n * 2 + d_{n-1}) * 2 + d_{n-2}) * 2 + ...) * 2 + d_0)`
>
> **Алгоритм:**
>
> 1. Починаємо з 0.
> 2. Ітеруємо по двійковому рядку **зліва направо**.
> 3. На кожному кроці: **множимо** поточний результат на 2 і **додаємо** значення наступного біта.
>
> **Приклад реалізації:**
>
> ```java
> public static int convertWithHorner(String binaryString) {
>     int decimalValue = 0;
>     for (int i = 0; i < binaryString.length(); i++) {
>         int bitValue = binaryString.charAt(i) - '0'; // Перетворюємо '0'/'1' в 0/1
>         decimalValue = decimalValue * 2 + bitValue;
>     }
>     return decimalValue;
> }
> ```
>
> `convertWithHorner("1101")`:
>
> * i=0, bit=1: `decimal = 0*2 + 1 = 1`
> * i=1, bit=1: `decimal = 1*2 + 1 = 3`
> * i=2, bit=0: `decimal = 3*2 + 0 = 6`
> * i=3, bit=1: `decimal = 6*2 + 1 = 13`
>
> **Переваги:** Цей метод набагато ефективніший, оскільки використовує лише прості операції множення та додавання. Замість множення на 2 можна використовувати ще швидший **побітовий зсув вліво `<< 1`**.
> `decimalValue = (decimalValue << 1) | bitValue;`

#### **📌 Що перевіряють:**

*Алгоритмічні знання, здатність запропонувати більш оптимізоване рішення та знання класичних обчислювальних схем.*

* * *

### **5 ❓ Питання:**

Як перетворити двійкове число, представлене в **доповняльному коді (two's complement)**, у його десятковий еквівалент? Наприклад, 8-бітне `11111011`. ⭐️

> #### **💬 Відповідь:**
>
> Якщо двійкове число починається з **1**, це означає, що воно від'ємне. Існує два способи його перетворення.
>
> **1. Математичний метод (з від'ємною вагою старшого біта):**
>
> * Для числа в доповняльному коді, старший (крайній лівий) біт має **від'ємну вагу**.
> * Для 8-бітного числа вага старшого біта буде **-2⁷** (-128). Вага інших бітів залишається додатною.
>
> Розглянемо `11111011`:
>
> * `(1 * -2⁷) + (1 * 2⁶) + (1 * 2⁵) + (1 * 2⁴) + (1 * 2³) + (0 * 2²) + (1 * 2¹) + (1 * 2⁰)`
> * `= -128 + 64 + 32 + 16 + 8 + 0 + 2 + 1`
> * `= -128 + 123 = -5`
>
> **2. Алгоритмічний метод ("зворотне перетворення"):**
>
> * Оскільки ми бачимо, що число від'ємне, виконуємо зворотні кроки для отримання його абсолютного значення.
>     1. Візьмемо `11111011`.
>     2. **Віднімемо 1**: `11111011 - 1 = 11111010`.
>     3. **Інвертуємо всі біти**: `00000101`.
>     4. Переводимо `00000101` в десяткову систему: `4 + 1 = 5`.
>     5. Оскільки початкове число було від'ємним, додаємо знак мінус. Результат: **-5**.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як представляються від'ємні числа, та володіння різними методами роботи з доповняльним кодом.*

* * *

### **6 ❓ Питання:**

Як перевести двійковий дріб (наприклад, `101.101`) у десятковий?

> #### **💬 Відповідь:**
>
> Перетворення двійкового дробу є прямим продовженням алгоритму для цілих чисел. Розряди праворуч від двійкової коми мають **від'ємні степені двійки**.
>
> **Алгоритм:**
>
> 1. Окремо перетворити цілу частину (ліворуч від коми).
> 2. Окремо перетворити дробову частину (праворуч від коми).
> 3. Додати результати.
>
> **Приклад для `101.101`:**
>
> * **Ціла частина (`101`):**
>   * `1 * 2² + 0 * 2¹ + 1 * 2⁰`
>   * `= 4 + 0 + 1 = 5`
>
> * **Дробова частина (`.101`):**
>   * Розряди рахуються вправо, починаючи з -1.
>   * `1 * 2⁻¹ + 0 * 2⁻² + 1 * 2⁻³`
>   * `= 1 * 0.5 + 0 * 0.25 + 1 * 0.125`
>   * `= 0.5 + 0 + 0.125 = 0.625`
>
> **Загальний результат:** `5 + 0.625 = 5.625`.
>
> Отже, двійкове `101.101` дорівнює десятковому `5.625`.

#### **📌 Що перевіряють:**

*Повноту знань про позиційні системи числення, включаючи дробові частини.*

* * *

### **7 ❓ Питання:**

Які винятки може кинути метод `Integer.parseInt(s, 2)`? Наведіть приклади.

> #### **💬 Відповідь:**
>
> Цей метод може кинути виняток **`NumberFormatException`** у кількох випадках.
>
> **`NumberFormatException` буде викинуто, якщо:**
>
> 1. **Вхідний рядок є `null` або порожнім.**
>
>     ```java
>     Integer.parseInt(null, 2);      // NullPointerException, а не NFE, це важливо!
>     Integer.parseInt("", 2);        // NumberFormatException
>     ```
>
>     *Виправлення*: `parseInt` кидає `NFE` для `null`, а не `NPE`. Дозвольте мені уточнити.

* * *

#### **Змінена відповідь на питання №7:**

> #### **💬 Відповідь:**
>
> Цей метод може кинути виняток **`NumberFormatException`** у кількох випадках.
>
> **`NumberFormatException` буде викинуто, якщо:**
>
> 1. **Вхідний рядок є `null` або порожнім.**
>
>     ```java
>     Integer.parseInt(null, 2); // Кидає NumberFormatException
>     Integer.parseInt("", 2);   // Кидає NumberFormatException
>     ```
>
> 2. **Рядок містить символи, які не є валідними для двійкової системи (тобто, не '0' або '1').**
>
>     ```java
>     Integer.parseInt("102", 2); // Кидає NFE, бо містить '2'
>     Integer.parseInt("abc", 2); // Кидає NFE
>     ```
>
> 3. **Число, представлене в рядку, виходить за межі діапазону `int` (`Integer.MIN_VALUE` ... `Integer.MAX_VALUE`).**
>
>     ```java
>     // Рядок представляє число більше, ніж Integer.MAX_VALUE
>     String tooLarge = "10000000000000000000000000000000"; // 32 біти '1'
>     Integer.parseInt(tooLarge, 2); // Кидає NFE
>     ```
>
> 4. **Рядок містить знаки `+` або `-`, окрім як на першій позиції.**
>
> Код, що використовує цей метод, завжди повинен бути готовий обробити цей виняток за допомогою `try-catch`.

#### **📌 Що перевіряють:**

*Знання API та його контрактів, включаючи можливі виняткові ситуації. Навички написання надійного коду.*

* * *

### **8 ❓ Питання:**

Уявіть, що двійкове число передається у вигляді масиву цілих чисел `int[] bits = {1, 1, 0, 1};`. Напишіть метод для його перетворення в десяткове. ⭐️

> #### **💬 Відповідь:**
>
> Це завдання дуже схоже на роботу з рядком, але ми будемо ітеруватися по масиву. Ми можемо використати схему Горнера для ефективності.
>
> ```java
> public class ArrayToDecimalConverter {
>
>     public static int convert(int[] bits) {
>         if (bits == null || bits.length == 0) {
>             return 0;
>         }
>
>         int decimalValue = 0;
>         // Ітеруємо по масиву зліва направо
>         for (int bit : bits) {
>             if (bit != 0 && bit != 1) {
>                 throw new IllegalArgumentException("Масив може містити лише 0 та 1.");
>             }
>             // Застосовуємо схему Горнера
>             decimalValue = (decimalValue << 1) | bit;
>         }
>         return decimalValue;
>     }
>
>     public static void main(String[] args) {
>         int[] bits = {1, 1, 0, 1};
>         System.out.println(convert(bits)); // Виведе: 13
>     }
> }
> ```
>
> У цьому коді `(decimalValue << 1)` еквівалентно `decimalValue * 2`, а побітове `OR` (`|`) еквівалентно додаванню `bit`, оскільки `bit` може бути лише 0 або 1. Це найпродуктивніший спосіб реалізації.

#### **📌 Що перевіряють:**

*Здатність адаптувати алгоритм до іншого формату вхідних даних. Навички роботи з масивами та побітовими операціями.*

* * *

### **9 ❓ Питання:**

Чому `Integer.parseInt("11111111111111111111111111111111", 2)` повертає `-1`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, як `Integer.parseInt()` обробляє представлення чисел у **доповняльному коді (two's complement)**.
>
> 1. Тип `int` в Java — це 32-бітне знакове ціле число.
> 2. Рядок `"11111111111111111111111111111111"` складається з 32 одиниць.
> 3. Коли `parseInt` парсить двійковий рядок, він інтерпретує його відповідно до правил доповняльного коду.
> 4. У доповняльному коді представлення, що складається з усіх одиниць, завжди відповідає числу **-1**.
>
> Давайте перевіримо:
>
> * Беремо `-1`. Його двійкове представлення:
> * `+1` → `00...01`
> * Інвертуємо → `11...10`
> * Додаємо 1 → `11...11` (32 одиниці).
>
> Метод `parseInt` коректно розпізнає цей патерн як `-1`. Так само, `"1000...000"` (одиниця і 31 нуль) він розпарсить як `Integer.MIN_VALUE`. Він повністю відтворює бітове представлення знакових цілих чисел.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як стандартна бібліотека працює з доповняльним кодом, і як це впливає на парсинг бітових рядків.*

* * *

### **10 ❓ Питання:**

Яка максимальна довжина двійкового рядка, який може бути переданий в `Integer.parseInt(s, 2)` без викидання `NumberFormatException` через переповнення? ⭐️

> #### **💬 Відповідь:**
>
> Максимальна довжина залежить від знака числа.
>
> 1. **Для додатних чисел:**
>     * Найбільше додатне число в `int` — це `Integer.MAX_VALUE`, яке дорівнює `2^31 - 1`.
>     * Його двійкове представлення — це `0` і за ним 31 одиниця: `01111111111111111111111111111111`.
>     * Довжина цього рядка — **31 символ** (ведучий `0` можна опустити). Якщо передати рядок з 31 одиниці, все буде добре.
>     * Якщо передати рядок довжиною 32 символи, що починається з 0, це також буде валідне додатне число.
>
> 2. **Для від'ємних чисел:**
>     * `int` має 32 біти.
>     * Рядок довжиною **до 32 символів** може бути розпарсений, якщо він представляє число в межах діапазону `int`.
>     * Наприклад, `"11111111111111111111111111111111"` (32 символи) буде успішно розпарсено в `-1`.
>
> **Якщо ж передати рядок, довший за 32 символи**, наприклад, `"1010..."` довжиною 33, то `parseInt` одразу викине `NumberFormatException`, оскільки таке число не може бути представлене типом `int`.
>
> Таким чином, максимальна довжина — **32 символи**, за умови, що представлене число входить до діапазону `int`.

#### **📌 Що перевіряють:**

*Увагу до деталей API, знання діапазонів примітивних типів та зв'язок між ними і їх двійковим представленням.*

* * *

### **11 ❓ Питання:**

Як зміниться алгоритм перетворення Binary -> Decimal, якщо двійкове число містить **двійковий роздільник** (аналог десяткової коми)? ⭐️

> #### **💬 Відповідь:**
>
> Алгоритм повинен бути розширений для обробки двох частин числа: цілої та дробової.
>
> 1. Спочатку рядок потрібно розділити на дві частини по двійковому роздільнику.
> 2. **Ціла частина** (ліворуч від роздільника) перетворюється у десяткове число за стандартним алгоритмом (сума добутків на додатні степені двійки).
> 3. **Дробова частина** (праворуч від роздільника) перетворюється окремо. Позиції рахуються вправо від роздільника, починаючи з `-1`. Вага кожної `1` дорівнює `2` у відповідному від'ємному степені (`2⁻¹=0.5`, `2⁻²=0.25` і т.д.).
> 4. **Результати підсумовуються**, щоб отримати фінальне десяткове значення типу `double` або `float`.
>
> **Приклад `101.11`:**
>
> * Ціла частина `101` → `1*2² + 0*2¹ + 1*2⁰ = 4 + 0 + 1 = 5`.
> * Дробова частина `.11` → `1*2⁻¹ + 1*2⁻² = 0.5 + 0.25 = 0.75`.
> * Результат: `5 + 0.75 = 5.75`.

#### **📌 Що перевіряють:**

*Повноту розуміння теми та здатність адаптувати базовий алгоритм до більш складного випадку.*

* * *

### **12 ❓ Питання:**

Чому пряме порівняння `(0.1 + 0.2) == 0.3` повертає `false`? Поясніть це, використовуючи знання про перетворення десяткових чисел у двійкову систему. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема, яка виникає через те, що **комп'ютер не може точно представити деякі десяткові дроби у своїй двійковій системі**.
>
> **Покрокове пояснення:**
>
> 1. **Перетворення в двійкову форму:**
>     * Десяткове `0.1` перетворюється в нескінченний двійковий періодичний дріб: `0.0001100110011...`
>     * Десяткове `0.2` перетворюється в інший нескінченний двійковий періодичний дріб: `0.001100110011...`
>
> 2. **Обрізання та втрата точності:**
>     * Оскільки тип `double` має обмежену кількість біт для зберігання мантиси, ці нескінченні дроби **обрізаються**.
>     * Таким чином, у пам'яті зберігаються лише їхні **наближені** значення.
>     * `0.1` зберігається приблизно як `0.10000000000000001`.
>     * `0.2` зберігається приблизно як `0.20000000000000001`.
>
> 3. **Додавання наближень:**
>     * Коли ми додаємо ці два наближених значення, результат буде `0.30000000000000004`.
>
> 4. **Порівняння:**
>     * Тим часом, десятковий літерал `0.3` також перетворюється у своє (теж неточне) двійкове наближення.
>     * Виявляється, що `0.30000000000000004` (результат суми) і наближення для `0.3` є **різними бітовими послідовностями**.
>     * Тому порівняння `==` повертає `false`.
>
> **Висновок:** ніколи не слід порівнювати числа з плаваючою комою на точну рівність. Замість цього слід перевіряти, чи є різниця між ними меншою за деяку малу величину (епсилон).

#### **📌 Що перевіряють:**

*Фундаментальне розуміння обмежень представлення чисел з плаваючою комою. Це питання на перетині кількох тем: системи числення, типи даних та кращі практики програмування.*

* * *

### **13 ❓ Питання:**

Як би ви перетворили дуже велике двійкове число (представлене у `String`), яке не вміщується в `long`, у десяткове? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для роботи з числами, що не вміщуються навіть у 64-бітний `long`, потрібно використовувати клас **`java.math.BigInteger`**.
>
> Він надає конструктор, який може приймати рядок та основу системи числення.
>
> **Приклад:**
>
> ```java
> public class LargeBinaryConverter {
>     public static BigInteger convert(String largeBinaryString) {
>         // Конструктор приймає рядок і radix (основу)
>         return new BigInteger(largeBinaryString, 2);
>     }
>
>     public static void main(String[] args) {
>         // Цей двійковий рядок представляє число, значно більше за Long.MAX_VALUE
>         String binary = "10101010101010101010101010101010101010101010101010101010101010101";
>
>         BigInteger decimal = convert(binary);
>
>         // toString() за замовчуванням повертає десяткове представлення
>         System.out.println(decimal.toString());
>     }
> }
> ```
>
> Клас `BigInteger` внутрішньо використовує масив для зберігання числа будь-якої довжини і реалізує всю необхідну арифметику та логіку перетворення програмно. Його реалізація `new BigInteger(s, 2)` є надійною та ефективною для цієї задачі.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки Java та вміння обирати правильний інструмент для роботи з великими числами.*

* * *

### **14 ❓ Питання:**

Поясніть, як ви можете отримати десяткове значення, знаючи бітове представлення IP-адреси v4 (наприклад, "192.168.1.1"). ⭐️⭐️

> #### **💬 Відповідь:**
>
> IP-адреса v4 — це 32-бітне число, яке для зручності записується як чотири десяткових числа (октети), розділених крапками. Кожен октет представляє 8 біт (1 байт).
>
> Щоб отримати єдине 32-бітне десяткове значення, потрібно:
>
> 1. Перетворити кожен з чотирьох октетів у його 8-бітне двійкове представлення.
> 2. З'єднати ці чотири двійкові послідовності в один 32-бітний рядок.
> 3. Перетворити цей 32-бітний рядок у десяткове число.
>
> **Або простіше, за допомогою побітових зсувів:**
>
> **Алгоритм:**
> `(перший_октет << 24) | (другий_октет << 16) | (третій_октет << 8) | (четвертий_октет)`
>
> **Приклад для "192.168.1.1":**
>
> 1. Октети: `192`, `168`, `1`, `1`.
> 2. Виконуємо зсуви і `OR`:
>     * `192 << 24` → `11000000 00000000 00000000 00000000`
>     * `168 << 16` → `00000000 10101000 00000000 00000000`
>     * `1 << 8` →   `00000000 00000000 00000001 00000000`
>     * `1` →          `00000000 00000000 00000000 00000001`
>
> 3. Побітове `OR` об'єднає ці значення в одне 32-бітне число:
>     `11000000 10101000 00000001 00000001` (двійкове).
>
> 4. Це двійкове число відповідає десятковому **3232235777**.
>
> **Реалізація в Java:**
>
> ```java
> String[] octets = "192.168.1.1".split("\\.");
> long result = (Long.parseLong(octets[0]) << 24) |
>               (Long.parseLong(octets[1]) << 16) |
>               (Long.parseLong(octets[2]) << 8)  |
>               (Long.parseLong(octets[3]));
> ```
>
> Ми використовуємо `long` для уникнення проблем зі знаком, оскільки `int` може бути від'ємним.

#### **📌 Що перевіряють:**

*Практичне застосування знань про двійкову систему та побітові операції для вирішення реальної задачі з мережевого програмування.*

* * *

### **15 ❓ Питання:**

Чому ітерація по двійковому рядку зліва направо при перетворенні в десяткове (схема Горнера) є обчислювально ефективнішою, ніж ітерація справа наліво (з `Math.pow()`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Схема Горнера** (ітерація зліва направо) є значно ефективнішою через тип операцій, які вона використовує.
>
> **1. Метод з ітерацією справа наліво (з `Math.pow()`):**
>
> ```java
> // decimal += bit * Math.pow(2, power);
> ```
>
> * **Операції:** На кожній ітерації виконується множення та виклик **`Math.pow()`**.
> * **Проблема:** `Math.pow()` — це відносно "дорога" операція. Вона призначена для роботи з `double` і може включати складні обчислення, перевірки та навіть виклик нативних методів, що повільніше за прості арифметичні дії. Вона не оптимізована для цілочисельного піднесення до степеня 2.
>
> **2. Метод ітерації зліва направо (схема Горнера):**
>
> ```java
> // decimal = decimal * 2 + bit;
> // або ще краще:
> // decimal = (decimal << 1) | bit;
> ```
>
> * **Операції:** На кожній ітерації виконуються лише **множення/зсув** та **додавання/OR**.
> * **Перевага:** Ці операції є одними з найшвидших, які може виконувати процесор. Побітовий зсув (`<< 1`) — це одна машинна інструкція, яка значно швидша за виклик загального методу піднесення до степеня.
>
> **Підсумок:**
>
> Схема Горнера замінює багатократне обчислення степенів на послідовність простих, швидких операцій, що робить її ідеальним вибором для цього завдання. Кількість операцій є лінійною і не залежить від "дорогих" викликів. Це класичний приклад алгоритмічної оптимізації.

#### **📌 Що перевіряють:**

*Розуміння не тільки Big O складності, але й відносної вартості різних операцій. Здатність порівняти два алгоритми з точки зору реальної продуктивності.*
