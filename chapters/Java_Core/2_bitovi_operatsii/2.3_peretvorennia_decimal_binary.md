---
layout: default
title: "Перетворення Decimal -> Binary"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 3
---

# Перетворення Decimal -> Binary

* короткий опис: Алгоритм переведення десяткового числа у двійкове шляхом послідовного ділення на 2 та запису остач, які потім читаються у зворотному порядку.

* поняття: Ділення на 2, остача, зворотний порядок.

* приклади: Перетворення `23` (десяткове) у `10111` (двійкове).

* ключові моменти: Остачі формують біти двійкового числа, читаються знизу вгору.

* що важливо знати на співбесіді: Як вручну переводити числа.

### **✅ Офіційне визначення:**

Перетворення з десяткової системи у двійкову — це процес, коли число з основою 10 записують у вигляді послідовності 0 і 1, де кожна цифра — це степінь числа 2\.

### **🧠 Простими словами:**

Хочеш «розговорити» комп’ютер? Він розуміє лише 0 і 1

Тому беремо число, ділимо його на 2, записуємо остачі — і ось у нас двійковий код

---

## **Покроковий алгоритм (📊)**

* Поділи десяткове число на 2\.
* Запиши остачу (0 або 1).
* Поділи отриману частку знову на 2\.
* Повторюй, поки частка не стане 0\.
* Остачі читаються в зворотному порядку — знизу вгору.

---

## **Пояснення на пальцях (💡)**

* Кожне ділення на 2 дає нам **частку і остачу** (0 або 1).
* Остача — це біт двійкового числа.
* Починаємо з молодших бітів — вони з’являються першими.
* В результаті отримуємо двійковий запис.

---

## **Корисні приклади (🧪)**

##### **Приклад: 23 у двійковому**

| Крок | Ділення | Остача |
| :---: | :---: | :---: |
| **1** | 23 ÷ 2 \= 11 | 1 |
| **2** | 11 ÷ 2 \= 5 | 1 |
| **3** | 5 ÷ 2 \= 2 | 1 |
| **4** | 2 ÷ 2 \= 1 | 0 |
| **5** | 1 ÷ 2 \= 0 | 1 |

📥 Остачі знизу вгору -> **`10111`**

##### **Ще приклади:**

| Decimal | Binary |
| :---: | :---: |
| 5 | 101 |
| 8 | 1000 |
| 12 | 1100 |
| 15 | 1111 |
| 32 | 100000 |

---

## **Порівняння з десятковою системою (⚖️)**

| Decimal | Кроки ділення -> Остачі | Binary |
| :---: | :---: | :---: |
| 10 | **`10->5+0->2->1+0->0->1`** | 1010 |
| 7 | **`7->3+1->1+1->0->1`** | 111 |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Divide -> Record -> Repeat -> Reverse**

(Поділи -> Запиши остачу -> Повтори -> Переверни)

---

## **🟩 Підсумок:**

Щоб перевести десяткове число у двійкове — діли його на 2, записуй остачі, поки частка не стане 0, потім прочитай остачі знизу вгору.

Наприклад, **`23 -> 10111`**.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Опишіть класичний алгоритм переведення додатного десяткового цілого числа в двійкову систему.

> #### **💬 Відповідь:**
>
> Класичний алгоритм для перетворення десяткового числа `N` в двійкову систему базується на **послідовному діленні на 2 з остачею**.
>
> **Покроковий процес:**
>
> 1. Візьміть початкове десяткове число `N`.
> 2. Поки `N` більше 0, повторюйте наступні кроки:
>     а. Обчисліть **остачу** від ділення `N` на 2 (`N % 2`). Ця остача (0 або 1) буде наступним бітом двійкового числа.
>     б. Запишіть цю остачу.
>     в. Оновіть `N`, виконавши **цілочисельне ділення** на 2 (`N = N / 2`).
> 3. Коли `N` стане 0, процес ділення завершено.
> 4. **Переверніть** послідовність отриманих остач. Перша отримана остача відповідає найменш значущому біту (крайньому правому), а остання — найбільш значущому.
>
> **Приклад для числа 23:**
>
> * 23 / 2 = 11, остача **1**
> * 11 / 2 = 5, остача **1**
> * 5 / 2 = 2, остача **1**
> * 2 / 2 = 1, остача **0**
> * 1 / 2 = 0, остача **1**
>
> Послідовність остач: `1, 1, 1, 0, 1`.
> Перевертаємо її і отримуємо: `10111`.

#### **📌 Що перевіряють:**

*Знання базового алгоритму. Це фундаментальне питання, яке показує, чи кандидат розуміє математичну основу систем числення.*

* * *

### **2 ❓ Питання:**

Напишіть Java-метод, який реалізує цей алгоритм для перетворення `int` в двійковий `String` без використання вбудованих функцій типу `Integer.toBinaryString()`.

> #### **💬 Відповідь:**
>
> Звісно. Найкраще для цього підходить `StringBuilder` через його ефективність при конкатенації.
>
> ```java
> public class DecimalToBinaryConverter {
>
>     public static String convert(int n) {
>         // Обробка граничного випадку з нулем
>         if (n == 0) {
>             return "0";
>         }
>         // Цей простий алгоритм працює для додатніх чисел
>         if (n < 0) {
>             // Можна було б реалізувати доповняльний код, але для простоти...
>             throw new IllegalArgumentException("Цей метод підтримує тільки додатні числа");
>         }
>
>         StringBuilder binaryString = new StringBuilder();
>
>         int numberToProcess = n;
>         while (numberToProcess > 0) {
>             int remainder = numberToProcess % 2;
>             binaryString.append(remainder);
>             numberToProcess /= 2;
>         }
>
>         // Перевертаємо рядок, оскільки ми збирали біти з кінця
>         return binaryString.reverse().toString();
>     }
>
>     public static void main(String[] args) {
>         System.out.println(convert(23)); // Виведе: 10111
>         System.out.println(convert(42)); // Виведе: 101010
>         System.out.println(convert(0));  // Виведе: 0
>     }
> }
> ```

#### **📝 Коментар:**

Це одна з найпоширеніших "легких" задач на кодування на співбесідах, щоб перевірити базові навички програмування.

#### **📌 Що перевіряють:**

*Практичні навички програмування: робота з циклами, арифметичними операціями та побудова рядків (`StringBuilder`).*

* * *

### **3 ❓ Питання:**

Чому в алгоритмі перетворення потрібно читати остачі у зворотному порядку?

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, як працює **позиційна система числення**.
>
> Алгоритм ділення на 2 на кожному кроці "відщеплює" **найменш значущий біт (Least Significant Bit - LSB)**, тобто той, що відповідає розряду 2⁰.
>
> Розглянемо число `23` (`10111` в двійковій).
>
> * Коли ми ділимо `23` на 2, остача `1` — це саме той крайній правий біт (`...1`).
> * Нове число `11` (`1011` в двійковій) — це, по суті, початкове число, зсунуте на один біт вправо.
> * Коли ми ділимо `11` на 2, остача `1` — це наступний біт справа наліво (`...11`).
>
> Таким чином, процес ділення генерує нам біти **справа наліво**, від молодших розрядів до старших. А для запису числа нам потрібно розташувати їх у звичайному порядку — **зліва направо**, від старших до молодших. Саме тому в кінці потрібно виконати операцію реверсу.

#### **📝 Аналогія:**

Це як збирати розсипані намистини: ви підбираєте їх по одній, а потім нанизуєте на нитку в правильному порядку, починаючи з першої підібраної.

#### **📌 Що перевіряють:**

*Глибоке розуміння логіки алгоритму, а не просто механічне запам'ятовування кроків.*

* * *

### **4 ❓ Питання:**

Які граничні випадки (edge cases) слід враховувати при реалізації цього алгоритму?

> #### **💬 Відповідь:**
>
> При реалізації алгоритму перетворення важливо врахувати кілька граничних випадків, щоб зробити його надійним:
>
> 1. **Число `0`:** Основний цикл `while (n > 0)` ніколи не виконається для `n = 0`. Цей випадок потрібно обробити окремо, просто повернувши `"0"`.
>
> 2. **Від'ємні числа:** Класичний алгоритм ділення з остачею розроблений для додатних чисел. Якщо передати від'ємне число, результат буде некоректним або метод зациклиться. Потрібно або:
>     * Кидати виняток (`IllegalArgumentException`).
>     * Реалізувати логіку для доповняльного коду (two's complement), що є значно складнішим завданням. Для `int`, наприклад, це означало б повернути 32-бітний рядок.
>
> 3. **Граничні значення типів:** Наприклад, `Integer.MAX_VALUE`. Алгоритм повинен коректно працювати з найбільшими можливими числами для даного типу без переповнень. (У цьому конкретному алгоритмі переповнень не буде, але про це варто пам'ятати).

#### **📝 Коментар:**

Обробка `0` є найпростішим і найчастішим граничним випадком, про який питають. Обробка від'ємних чисел — це вже завдання складніше.

#### **📌 Що перевіряють:**

*Уважність до деталей та здатність думати про нетипові вхідні дані. Написання коду, що працює тільки для "ідеальних" умов — ознака недосвідченого розробника.*

* * *

### **5 ❓ Питання:**

Чи можна реалізувати цей алгоритм за допомогою рекурсії? Напишіть приклад.

> #### **💬 Відповідь:**
>
> **Так, можна**, і рекурсивне рішення виходить дуже елегантним, оскільки воно природно обробляє "зворотний порядок".
>
> **Ідея рекурсивного алгоритму:**
>
> 1. Базовий випадок: якщо число 0, нічого не робити (або повернути порожній рядок).
> 2. Рекурсивний крок:
>     * Викликати той самий метод для `n / 2`.
>     * **Після повернення** з рекурсивного виклику, надрукувати або додати до рядка остачу `n % 2`.
>
> Завдяки тому, що ми друкуємо остачу **після** рекурсивного виклику, цифри виводяться в правильному порядку, і реверс не потрібен.
>
> **Приклад реалізації:**
>
> ```java
> public static void convertRecursive(int n) {
>     if (n == 0) {
>         return; // Базовий випадок
>     }
>     // Спочатку йдемо вглиб рекурсії
>     convertRecursive(n / 2);
>     // Друкуємо остачу на виході з рекурсії
>     System.out.print(n % 2);
> }
>
> public static void main(String[] args) {
>     convertRecursive(23); // Виведе: 10111
> }
> ```

#### **📌 Що перевіряють:**

*Розуміння рекурсії та здатність застосовувати її для вирішення завдань. Уміння побачити, як стек викликів може спростити логіку.*

* * *

### **6 ❓ Питання:**

Як можна перевести десятковий дріб (наприклад, 0.75) у двійкову систему? ⭐️

> #### **💬 Відповідь:**
>
> Алгоритм для дробової частини відрізняється від алгоритму для цілої. Він базується на **послідовному множенні на 2**.
>
> **Алгоритм:**
>
> 1. Візьміть дробову частину десяткового числа.
> 2. Поки дробова частина не стане 0 (або поки не досягнете потрібної точності):
>     а. **Помножте** поточну дробову частину на 2.
>     б. **Ціла частина** результату (0 або 1) буде наступним бітом двійкового дробу.
>     в. **Дробова частина** результату стає новою поточною дробовою частиною для наступної ітерації.
> 3. Біти записуються в **прямому порядку**, зліва направо, після двійкової коми.
>
> **Приклад для 0.75:**
>
> 1. 0.75 * 2 = **1**.5 → Ціла частина: **1**. Новий дріб: 0.5
> 2. 0.5 * 2 = **1**.0 → Ціла частина: **1**. Новий дріб: 0.0
>
> Дробова частина стала 0, зупиняємось. Записуємо отримані цілі частини: **11**.
>
> Отже, десятковий 0.75 = двійковий **0.11**.
> (`1 * 2⁻¹ + 1 * 2⁻² = 0.5 + 0.25 = 0.75`).

#### **📝 Коментар:**

Важливо пам'ятати, що багато простих десяткових дробів (як `0.1`) в двійковій системі стають нескінченними, і цей процес може ніколи не завершитися.

#### **📌 Що перевіряють:**

*Повноту знань про системи числення, включаючи роботу з дробами.*

* * *

### **7 ❓ Питання:**

Чому `Integer.parseInt("101", 2)` є зворотним до перетворення десяткового числа в двійковий рядок?

> #### **💬 Відповідь:**
>
> Цей метод виконує **зворотну операцію** — парсить рядок, який представляє число в певній системі числення, і перетворює його на десятковий `int`.
>
> Метод `Integer.parseInt(String s, int radix)`:
>
> * `s`: рядок для парсингу ("101").
> * `radix`: основа системи числення (2 — для двійкової).
>
> Він реалізує алгоритм перетворення з будь-якої позиційної системи в десяткову.
>
> **Як він працює для `"101"` (двійкове):**
>
> 1. Він розглядає рядок як послідовність цифр.
> 2. Для кожної цифри він обчислює її внесок у загальну суму за формулою `цифра * основа^позиція`.
>
> `101` (двійкове) →
> `(1 * 2²) + (0 * 2¹) + (1 * 2⁰)` →
> `(1 * 4) + (0 * 2) + (1 * 1)` →
> `4 + 0 + 1 = 5` (десяткове).
>
> Таким чином, `Integer.parseInt("101", 2)` поверне `5`.
> Це точно обернена дія до перетворення десяткового 5 у двійковий рядок "101".

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки та розуміння симетричності операцій перетворення.*

* * *

### **8 ❓ Питання:**

Чи можна використовувати побітові операції для реалізації перетворення в двійкову систему? Які їх переваги? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна і це є більш ефективним підходом**, оскільки він ближчий до того, як працює процесор.
>
> Для реалізації можна використовувати **бітову маску** та **зсув вправо**.
>
> **Алгоритм:**
>
> 1. Ітеруємо по бітах числа від старшого до молодшого (наприклад, від 31 до 0 для `int`).
> 2. На кожній ітерації ми "виділяємо" один біт, щоб перевірити, чи він 1, чи 0.
> 3. Для цього ми зсуваємо число вправо, щоб потрібний нам біт опинився на крайній правій позиції, а потім застосовуємо маску `& 1`.
>
> **Приклад реалізації:**
>
> ```java
> public static String toBinaryWithBitwiseOps(int n) {
>     if (n == 0) return "0";
>
>     StringBuilder sb = new StringBuilder();
>     // Починаємо з самого старшого біта (для int це 31)
>     boolean leadingZeros = true; // Для ігнорування ведучих нулів
>     for (int i = 31; i >= 0; i--) {
>         // Зсуваємо число так, щоб i-тий біт став крайнім правим
>         // і перевіряємо його
>         int bit = (n >> i) & 1;
>
>         if (bit == 1) {
>             leadingZeros = false;
>         }
>
>         if (!leadingZeros) {
>             sb.append(bit);
>         }
>     }
>     return sb.toString();
> }
> ```
>
> **Переваги:** Цей підхід може бути значно швидшим, ніж арифметичні операції `%` та `/`, особливо на низькому рівні, і дає краще розуміння бітового представлення числа.

#### **📌 Що перевіряють:**

*Вміння використовувати побітові операції для вирішення алгоритмічних задач. Розуміння альтернативних, більш продуктивних підходів.*

* * *

### **9 ❓ Питання:**

Як би ви змінили свій метод `convert(int n)` (з питання №2), щоб він коректно обробляв від'ємні числа, повертаючи їх 32-бітне двійкове представлення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Щоб коректно обробляти від'ємні числа, нам потрібно відтворити **доповняльний код (two's complement)**. Простий алгоритм ділення з остачею тут не працює. Набагато простіше використовувати **побітові операції**.
>
> Метод, який використовує зсув та маску, майже ідеально підходить для цього. Йому потрібне лише невелике доопрацювання — він не повинен ігнорувати ведучі одиниці для від'ємних чисел.
>
> **Модифікована реалізація:**
>
> ```java
> public static String convertWithNegative(int n) {
>     if (n == 0) return "0";
>
>     StringBuilder binaryString = new StringBuilder();
>     // Для 32-бітного int, ми проходимо по всіх 32 бітах
>     for (int i = 31; i >= 0; i--) {
>         int bit = (n >> i) & 1;
>         binaryString.append(bit);
>
>         // Для читабельності, можна додати пробіл кожні 8 біт
>         if (i % 8 == 0 && i != 0) {
>             binaryString.append(' ');
>         }
>     }
>     return binaryString.toString();
> }
> ```
>
> **Як це буде працювати для `-1`:**
>
> 1. Внутрішнє представлення `-1` — це 32 одиниці.
> 2. Цикл пройде 32 рази.
> 3. На кожній ітерації, зсув `(-1 >> i)` і `& 1` буде давати `1`.
> 4. В результаті метод поверне рядок `11111111 11111111 11111111 11111111`.
>
> Цей метод тепер універсальний і коректно відображає бітове представлення будь-якого `int`.

#### **📌 Що перевіряють:**

*Глибокі знання про представлення від'ємних чисел та здатність написати код, що працює з бітовими патернами безпосередньо.*

* * *

### **10 ❓ Питання:**

Яка часова та просторова складність (Big O) алгоритму перетворення десяткового числа `N` в двійкову систему? ⭐️

> #### **💬 Відповідь:**
>
> Давайте проаналізуємо складність класичного алгоритму ділення на 2.
>
> **Часова складність: O(log N)**
>
> * Кількість ітерацій у циклі `while (N > 0)` залежить від того, скільки разів ми можемо поділити `N` на 2, перш ніж воно стане 0.
> * Це, по суті, **логарифм від N за основою 2** (log₂N). Кількість бітів у двійковому представленні числа N приблизно дорівнює log₂N.
> * Оскільки операції `%` та `/` вважаються константними за часом (O(1)), загальна часова складність є **O(log N)**.
>
> **Просторова складність: O(log N)**
>
> * Нам потрібно десь зберігати результат — двійковий рядок.
> * Довжина цього рядка, як ми вже з'ясували, пропорційна кількості біт у числі, тобто log₂N.
> * Тому просторова складність (кількість пам'яті, що використовується) також є **O(log N)**.
>
> **Приклад:** Для числа 1,000,000 нам знадобиться близько 20 (log₂10⁶ ≈ 19.9) ітерацій та приблизно стільки ж символів для зберігання результату.

#### **📌 Що перевіряють:**

*Розуміння аналізу складності алгоритмів (Big O Notation). Це стандартне питання для оцінки алгоритмічної підготовки.*

* * *

### **11 ❓ Питання:**

Як можна використовувати стек для реалізації перетворення десяткового числа в двійкове? ⭐️

> #### **💬 Відповідь:**
>
> Використання стека (LIFO - Last-In, First-Out) — це дуже елегантний спосіб уникнути необхідності робити реверс рядка в кінці.
>
> **Алгоритм:**
>
> 1. Створіть порожній стек (наприклад, `Stack<Integer>`).
> 2. Поки початкове десяткове число більше 0:
>     а. Обчисліть остачу від ділення числа на 2 (`n % 2`).
>     б. **Покладіть (push)** цю остачу на вершину стека.
>     в. Оновіть число, поділивши його на 2 націло (`n /= 2`).
> 3. Тепер діставайте елементи зі стека і формуйте з них рядок. Оскільки ви будете діставати їх у зворотному порядку до того, як ви їх клали (LIFO), вони автоматично будуть у правильному порядку (від старшого біта до молодшого).
>
> **Приклад реалізації:**
>
> ```java
> public static String convertWithStack(int n) {
>     if (n == 0) return "0";
>
>     Stack<Integer> stack = new Stack<>();
>     while (n > 0) {
>         stack.push(n % 2);
>         n /= 2;
>     }
>
>     StringBuilder sb = new StringBuilder();
>     while (!stack.isEmpty()) {
>         sb.append(stack.pop());
>     }
>
>     return sb.toString();
> }
> ```
>
> Це рішення є логічно чистим і демонструє розуміння структур даних.

#### **📌 Що перевіряють:**

*Знання базових структур даних (`Stack`) та здатність застосувати їх для вирішення алгоритмічних задач.*

* * *

### **12 ❓ Питання:**

Чим відрізняється процес перетворення для цілих чисел і для чисел з плаваючою комою, наприклад, `double`? ⭐️

> #### **💬 Відповідь:**
>
> Процеси відрізняються кардинально, оскільки для `double` потрібно перетворити і цілу, і дробову частини, а потім представити їх у стандарті IEEE 754.
>
> **Для цілого числа (`int`, `long`):**
>
> * Перетворюється лише ціла частина за алгоритмом ділення на 2. Результат — проста послідовність біт.
>
> **Для числа з плаваючою комою (`double`):**
>
> * Дотримується стандарту **IEEE 754**. Число розкладається на три компоненти:
>     1. **Знак (Sign bit):** 1 біт (0 — додатне, 1 — від'ємне).
>     2. **Порядок (Exponent):** 11 біт для `double`. Зберігає степінь двійки, на який потрібно помножити мантису.
>     3. **Мантиса (Mantissa/Fraction):** 52 біти для `double`. Зберігає значущі цифри числа в нормалізованому вигляді.
>
> **Процес перетворення для `double`:**
>
> 1. Визначити знак.
> 2. Перевести цілу частину в двійкову.
> 3. Перевести дробову частину в двійкову (множенням на 2).
> 4. Скомбінувати їх і **нормалізувати** (привести до вигляду `1.xxxxx * 2^E`).
> 5. Закодувати порядок (`E`) та мантису (`xxxxx`) у відповідні бітові поля згідно зі стандартом IEEE 754.
>
> Цей процес значно складніший і є причиною, чому `double` та `float` мають проблеми з точністю представлення десяткових дробів.

#### **📌 Що перевіряють:**

*Розуміння різниці між представленням цілих та дійсних чисел у пам'яті. Знання про стандарт IEEE 754.*

* * *

### **13 ❓ Питання:**

Що станеться, якщо спробувати перевести в двійкову систему число, яке є більшим, ніж `Integer.MAX_VALUE`? Як Java обробляє таку ситуацію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Напряму передати в метод `convert(int n)` число, більше за `Integer.MAX_VALUE`, неможливо — це призведе до помилки компіляції, якщо це літерал, або до переповнення, якщо це результат обчислень.
>
> Щоб працювати з такими числами, потрібно використовувати тип **`long`** або клас **`java.math.BigInteger`**.
>
> **1. Використання `long`:**
> Якщо число поміщається в `long`, ми можемо просто написати аналогічний метод для цього типу. Алгоритм не зміниться.
>
> ```java
> long largeNumber = 3_000_000_000L; // більше, ніж Integer.MAX_VALUE
> String binary = Long.toBinaryString(largeNumber);
> ```
>
> **2. Використання `BigInteger`:**
> Для чисел, що не вміщуються навіть у `long` (наприклад, у криптографії), призначений клас `BigInteger`. Він не має обмежень за розміром.
>
> * Він має метод `toString(int radix)`, який може перевести число в рядок у будь-якій системі числення.
>
> ```java
> BigInteger veryLargeNumber = new BigInteger("123456789012345678901234567890");
> String binary = veryLargeNumber.toString(2);
> ```
>
> "Під капотом" `BigInteger` використовує масив для зберігання числа і реалізує арифметичні операції та перетворення програмно. Його алгоритм перетворення в двійкову систему також базується на операціях ділення та остачі, але для великих чисел.
>
> **Отже, Java обробляє цю ситуацію, надаючи відповідні типи даних для роботи з великими числами.**

#### **📌 Що перевіряють:**

*Знання обмежень примітивних типів та стандартних класів Java для роботи з великими числами.*

* * *

### **14 ❓ Питання:**

Який зв'язок між перетворенням у двійкову систему та реалізацією `EnumSet`? Чому `EnumSet` є таким ефективним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `EnumSet` — це високопродуктивна реалізація інтерфейсу `Set` для роботи з елементами одного `enum`. Його ефективність базується на тому, що він внутрішньо представляє набір `enum`-констант як **бітову маску (bitmask)**, зазвичай усередині одного `long`.
>
> **Зв'язок із двійковою системою:**
>
> 1. Коли JVM завантажує `enum`, вона присвоює кожній константі унікальний порядковий номер (`ordinal`), починаючи з 0.
> 2. `EnumSet` використовує цей `ordinal` як **індекс біта**. Присутність `enum`-константи в наборі означає, що біт з відповідним індексом дорівнює **1**. Відсутність — **0**.
> 3. Оскільки `long` має 64 біти, `EnumSet` може зберігати набір до 64 `enum`-констант всього в одній `long` змінній (для більшої кількості використовується масив `long`).
>
> **Приклад:**
>
> ```java
> enum Days { MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY }
> ```
>
> * `MONDAY` має ordinal `0`, `TUESDAY` — `1`, і т.д.
> * Набір `EnumSet.of(Days.MONDAY, Days.WEDNESDAY)` буде представлений як двійкове число, де 0-й та 2-й біти встановлені: `...00000101`. (1 + 4 = 5).
>
> **Чому це ефективно:**
>
> * **Пам'ять:** Дуже компактне зберігання.
> * **Продуктивність:** Усі операції (`add`, `contains`, `remove`) зводяться до надзвичайно швидких **побітових операцій** (`|`, `&`, `~`) замість обчислення хеш-кодів та роботи з хеш-таблицями, як у `HashSet`.
>
> Це чудовий приклад того, як розуміння двійкового представлення даних дозволяє створювати високопродуктивні структури даних.

#### **📌 Що перевіряють:**

*Глибокі знання стандартної бібліотеки Java, її внутрішньої реалізації та здатність пов'язати низькорівневі концепції (двійкова система) з високорівневими структурами даних (`EnumSet`).*

* * *

### **15 ❓ Питання:**

Напишіть код для перетворення десяткового числа 5.625 у його двійкове представлення. Поясніть кожен крок. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для перетворення такого числа потрібно окремо обробити цілу та дробову частини.
>
> **Число: 5.625**
>
> **Крок 1: Перетворення цілої частини (5)**
>
> Використовуємо метод ділення на 2:
>
> * 5 / 2 = 2, остача **1**
> * 2 / 2 = 1, остача **0**
> * 1 / 2 = 0, остача **1**
>
> Читаємо остачі у зворотному порядку: **101**.
> Отже, ціла частина `5` (десяткова) = `101` (двійкова).
>
> **Крок 2: Перетворення дробової частини (0.625)**
>
> Використовуємо метод множення на 2:
>
> 1. 0.625 * 2 = **1**.25
>     * Ціла частина: **1**. Нова дробова частина: 0.25.
> 2. 0.25 * 2 = **0**.5
>     * Ціла частина: **0**. Нова дробова частина: 0.5.
> 3. 0.5 * 2 = **1**.0
>     * Ціла частина: **1**. Нова дробова частина: 0.0.
>
> Дробова частина стала 0, зупиняємось. Записуємо отримані цілі частини у прямому порядку: **101**.
> Отже, дробова частина `0.625` (десяткова) = `0.101` (двійкова).
>
> **Крок 3: Об'єднання результатів**
>
> Складаємо цілу та дробову частини:
> `101` (ціла) + `.` + `101` (дроб.) = **101.101**
>
> **Перевірка:**
> `101.101` (двійкове) = (1*2²) + (0*2¹) + (1*2⁰) + (1*2⁻¹) + (0*2⁻²) + (1*2⁻³)
> = 4 + 0 + 1 + 0.5 + 0 + 0.125 = **5.625** (десяткове).
>
> Все правильно.

#### **📌 Що перевіряють:**

*Здатність застосувати два різних алгоритми для перетворення змішаного числа. Повноту розуміння теми, включаючи роботу з дробами. Уважність до деталей.*
