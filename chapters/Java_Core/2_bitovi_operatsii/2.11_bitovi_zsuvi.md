---
layout: default
title: "Бітові зсуви: <<, >>, >>>"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 11
---

# Бітові зсуви: <<, >>, >>>

* короткий опис: Побітові операції, що переміщують біти числа вліво або вправо на задану кількість позицій. `<<` (лівий зсув) – множення на 2^n; `>>` (правий зсув зі знаком) – ділення на 2^n, зберігає знак; `>>>` (беззнаковий правий зсув) – ділення на 2^n, додає нулі зліва.

* поняття: `<<`, `>>`, `>>>`.

* приклади: `4 << 1 = 8`, `-8 >> 1 = -4`, `-8 >>> 1 = 2147483644`.

* ключові моменти: Використовуються для швидкого множення/ділення на степені 2, а також для бітових маніпуляцій, що ігнорують знак.

* що важливо знати на співбесіді: Різниця між `>>` і `>>>`, використання для множення/ділення.

### **✅ Офіційне визначення:**

Зсув бітів — побітова операція, що переміщує всі біти числа вліво або вправо на задану кількість позицій.

В Java:

* **`<<`** — лівий зсув (зсуває вліво, додає нулі справа)
* **`>>`** — правий зсув зі знаком (зсуває вправо, зберігає знак)
* **`>>>`** — беззнаковий правий зсув (зсуває вправо, додає нулі зліва)

### **🧠 Простими словами:**

Як пересування цифр у числі, але для бітів:

* **`<<`** -> більше (помножити)
* **`>>`** -> менше, з урахуванням знака
* **`>>>`** -> менше, без знака (чистий зсув)

---

## **Основне**

| Оператор | Назва | Що робить | Приклад |
| :---: | :---: | :---: | :---: |
| **`<<`** | Лівий зсув | Множить число на 2^n | **`4 << 1 = 8`** |
| **`>>`** | Правий зсув зі знаком | Ділить на 2^n, зберігає знак | **`-8 >> 1 = -4`** |
| **`>>>`** | Беззнаковий правий зсув | Ділить на 2^n, додає 0 зліва | **`-8 >>> 1 = велике +`** |

---

## **Пояснення на пальцях (💡)**

* **`<<`** — додати нуль справа (помножити)
* **`>>`** — зберегти знак, якщо число від’ємне — зліва буде 1 (продовжує знак)
* **`>>>`** — завжди додає 0 зліва, ігнорує знак, тому навіть від’ємне число стане великим позитивним

---

## **Корисні приклади (🧪)**

```java
int a = 4;   // 0000_0100
System.out.println(a << 1); // 8   (0000_1000)
System.out.println(a << 2); // 16  (0001_0000)

int b = -8;  // 1111_1000 (int: 32 біти)
System.out.println(b >> 1);  // -4  (1111_1100)
System.out.println(b >>> 1); // велике додатне число
```

Як побачити бітову картину:

```java
System.out.println(Integer.toBinaryString(-8));      // 11111111111111111111111111111000
System.out.println(Integer.toBinaryString(-8 >> 1)); // 11111111111111111111111111111100
System.out.println(Integer.toBinaryString(-8 >>> 1));// 01111111111111111111111111111100
```

---

## **Порівняння (⚖️)**

| Оператор | Математичний еквівалент | Знак враховується? | Пояснення |
| :---: | :---: | :---: | :---: |
| **`<<`** | × 2ⁿ | Ні | Зсуває біти вліво, множить |
| **`>>`** | ÷ 2ⁿ | Так | Зсуває вправо, зберігає знак |
| **`>>>`** | ÷ 2ⁿ | Ні | Зсуває вправо, без знака, додає 0 |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **`<<`** \= Ліво \= Множення на 2ⁿ
* **`>>`** \= Право \= Ділення зі знаком
* **`>>>`** \= Право \= Ділення без знака (заповнює нулями)

---

## **🟩 Підсумок:**

Зсуви бітів — це твій швидкий і ефективний спосіб множити, ділити і маніпулювати двійковим кодом.

* **`<<`** — множить на 2ⁿ
* **`>>`** — ділить з урахуванням знака (продовжує знак)
* **`>>>`** — ділить без знака, завжди додає 0 зліва

Ідеально для низькорівневого коду, алгоритмів та роботи з прапорами.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке побітові зсуви в Java? Назвіть три основні оператори зсуву.

> #### **💬 Відповідь:**
>
> **Побітові зсуви (Bit shift operations)** — це операції, які переміщують всі біти цілочисельного значення вліво або вправо на задану кількість позицій. Вони є дуже швидким способом виконання операцій, еквівалентних множенню або діленню на степені двійки.
>
> У Java існує три основні оператори зсуву:
>
> 1. **`<<` (Лівий зсув / Left Shift):**
>     * Зсуває всі біти **вліво**.
>     * Позиції, що звільняються справа, завжди заповнюються **нулями**.
>
> 2. **`>>` (Арифметичний зсув вправо / Signed Right Shift):**
>     * Зсуває всі біти **вправо**.
>     * Зберігає знак числа: позиції, що звільняються зліва, заповнюються **копією знакового (старшого) біта**.
>
> 3. **`>>>` (Логічний зсув вправо / Unsigned Right Shift):**
>     * Зсуває всі біти **вправо**.
>     * Ігнорує знак числа: позиції, що звільняються зліва, **завжди заповнюються нулями**.

#### **📝 Коментар:**

Розуміння різниці між `>>` та `>>>` є ключовим, оскільки це найчастіше питання на цю тему.

#### **📌 Що перевіряють:**

*Знання базових операторів зсуву та їхнього основного призначення.*

* * *

### **2 ❓ Питання:**

Поясніть, як працює лівий зсув (`<<`) і як він пов'язаний з множенням.

> #### **💬 Відповідь:**
>
> Оператор лівого зсуву (`<<`) зсуває всі біти числа на вказану кількість позицій вліво. Позиції, що звільняються праворуч, заповнюються нулями.
>
> **Як це пов'язано з множенням:**
>
> Кожен зсув вліво на одну позицію еквівалентний **множенню числа на 2**. Відповідно, зсув на `N` позицій еквівалентний **множенню на 2^N**.
>
> **Приклад для `5 << 3` (множення на 2³=8):**
>
> 1. Десяткове `5` у двійковому вигляді: `...00000101`
>
> 2. Зсуваємо всі біти на 3 позиції вліво:
>     `...00101000`
>
> 3. Праворуч дописуються три нулі.
>
> 4. Перетворюємо результат `...00101000` назад у десяткове число:
>     `32 + 8 = 40`.
>
> 5. Перевірка: `5 * 2³ = 5 * 8 = 40`.
>
> На апаратному рівні зсув є значно швидшою операцією, ніж повне множення, тому його часто використовують в оптимізованому коді.

#### **📝 Аналогія:**

Зсув вліво — це як дописати нуль в кінці десяткового числа. 12 → 120 (помножили на 10). У двійковій системі дописування нуля — це множення на 2.

#### **📌 Що перевіряють:**

*Розуміння того, як лівий зсув впливає на бітове представлення числа та його математичний еквівалент.*

* * *

### **3 ❓ Питання:**

У чому фундаментальна різниця між зсувом вправо зі знаком (`>>`) та беззнаковим зсувом вправо (`>>>`)?

> #### **💬 Відповідь:**
>
> Фундаментальна різниця полягає в тому, **як вони обробляють знаковий біт** (крайній лівий біт), що критично для від'ємних чисел.
>
> * **`>>` (Арифметичний, або знаковий зсув):**
>   * **Зберігає знак.** При зсуві бітів вправо, вільні позиції зліва заповнюються **копією знакового біта**.
>   * Якщо число додатне (знаковий біт `0`), зліва додаються нулі.
>   * Якщо число від'ємне (знаковий біт `1`), зліва додаються одиниці.
>   * Ця операція є математично еквівалентною **цілочисельному діленню на 2^N**.
>
> * **`>>>` (Логічний, або беззнаковий зсув):**
>   * **Ігнорує знак.** При зсуві бітів вправо, вільні позиції зліва **завжди заповнюються нулями**, незалежно від того, яким було початкове число.
>   * Для додатних чисел `>>>` працює ідентично до `>>`.
>   * Для від'ємних чисел цей зсув "ламає" їхній знак: від'ємне число перетворюється на велике додатне, оскільки знаковий біт стає нулем.
>
> **Приклад:**
> `int a = -8;` (`...11111000`)
> `a >> 1` → `...11111100` (`-4`) — знак зберігся.
> `a >>> 1` → `0...11111100` (дуже велике додатне число) — знак змінився.

#### **📌 Що перевіряють:**

*Ключове питання теми. Кандидат повинен чітко пояснити поведінку обох операторів для додатних і, що важливіше, для від'ємних чисел.*

* * *

### **4 ❓ Питання:**

Що виведе цей код і чому?

```java
int negative = -16;
System.out.println(negative >> 2);
System.out.println(negative >>> 2);
```

> #### **💬 Відповідь:**
>
> **Вивід:**
>
> ```
> -4
> 1073741820
> ```
>
> **Пояснення:**
>
> `int -16` у 32-бітному двійковому представленні: `11111111 11111111 11111111 11110000`.
>
> **1. `negative >> 2` (арифметичний зсув):**
>
> * Біти зсуваються на 2 позиції вправо.
> * Два старших біти (знакові), що звільняються, **заповнюються одиницями**, щоб зберегти знак.
> * Результат: `11111111 11111111 11111111 11111100`.
> * Це бітове представлення числа **-4** у доповняльному коді. (`-16 / 2² = -4`).
>
> **2. `negative >>> 2` (логічний зсув):**
>
> * Біти зсуваються на 2 позиції вправо.
> * Два старших біти, що звільняються, **заповнюються нулями**.
> * Результат: `00111111 11111111 11111111 11111100`.
> * Оскільки знаковий біт тепер `0`, це велике додатне число, яке в десятковій системі дорівнює **1,073,741,820**.
> (2³⁰ + 2²⁹ + ... - 16)
>

#### **📌 Що перевіряють:**

*Здатність застосувати теоретичні знання про різницю `>>` та `>>>` для розв'язання практичного завдання.*

* * *

### **5 ❓ Питання:**

Що відбувається з бітами, які "виходять" за межі числа при зсуві?

> #### **💬 Відповідь:**
>
> Біти, які зсуваються за межі типу даних, **просто відкидаються і зникають назавжди**.
>
> * При **лівому зсуві (`<<`)**, старші біти, які зсуваються за ліву межу, втрачаються.
> * При **правому зсуві (`>>` або `>>>`)**, молодші біти, які зсуваються за праву межу, втрачаються.
>
> **Приклад:**
>
> ```java
> byte b = (byte) 0b10110101; // це -75
>
> // Зсуваємо вліво на 1. Старший біт '1' зникає.
> byte result = (byte) (b << 1); // стає 01101010 (106)
> ```
>
> Це важливо, бо якщо знаковий біт "виштовхується" при лівому зсуві, знак числа може несподівано змінитися.
>
> ```java
> int positive = 1_500_000_000;
> System.out.println(Integer.toBinaryString(positive)); // 01011001...
> int changedSign = positive << 1; // 1011001... (тепер від'ємне)
> ```
>
> Через цю втрату бітів лівий зсув не завжди є точним еквівалентом множення. Він працює як множення тільки доти, доки значущі біти не "виходять" за межі типу.

#### **📌 Що перевіряють:**

*Повноту розуміння механізму зсувів. Кандидат повинен знати не тільки про те, як заповнюються нові позиції, але й про те, що відбувається з бітами, що зникають.*

* * *

### **6 ❓ Питання:**

Як кількість зсувів (`n`) обробляється для типів `int` та `long`? Що буде, якщо зсунути `int` на 32 або більше позицій? ⭐️

> #### **💬 Відповідь:**
>
> Кількість позицій для зсуву обробляється не так, як можна було б очікувати. Використовується лише **остача від ділення** кількості зсувів на розмір типу в бітах.
>
> **Для `int` (32 біти):**
>
> * Фактична кількість зсувів обчислюється як `n % 32`.
> * Використовуються лише **5 молодших біт** значення `n`.
> * **Приклад:**
>   * `x << 32` еквівалентно `x << (32 % 32)`, тобто `x << 0`. Зсуву не буде.
>   * `x << 35` еквівалентно `x << (35 % 32)`, тобто `x << 3`.
>
> **Для `long` (64 біти):**
>
> * Фактична кількість зсувів обчислюється як `n % 64`.
> * Використовуються лише **6 молодших біт** значення `n`.
> * **Приклад:**
>   * `y << 64` еквівалентно `y << 0`.
>   * `y << 70` еквівалентно `y << 6`.
>
> Таким чином, спроба зсунути `int` на 32 або `long` на 64 позиції просто поверне початкове число без змін.

#### **📌 Що перевіряють:**

*Знання тонких нюансів специфікації мови Java. Це одна з пасток, в яку можна потрапити, очікуючи, що зсув на 32 біти "обнулить" число.*

* * *

### **7 ❓ Питання:**

Які типи даних можна використовувати як правий операнд операторів зсуву (тобто, кількість позицій для зсуву)?

> #### **💬 Відповідь:**
>
> Правий операнд може бути будь-якого **цілочисельного типу** (`byte`, `short`, `char`, `int`).
>
> Однак, незалежно від його початкового типу, він завжди **підлягає розширенню типу (numeric promotion) до `int`** перед тим, як буде використано для визначення кількості зсувів.
>
> **Приклад:**
>
> ```java
> byte shiftAmount = 2;
> int number = 10;
> int result = number << shiftAmount;
> ```
>
> Тут `shiftAmount` буде розширено до `int` перед виконанням операції зсуву.
>
> Важливо пам'ятати, що після розширення до `int`, використовуються лише 5 молодших бітів цього значення для визначення фактичної кількості зсувів (якщо лівий операнд — `int`) або 6 молодших бітів (якщо лівий операнд — `long`).

#### **📌 Що перевіряють:**

*Детальне знання правил numeric promotion у контексті операторів зсуву.*

* * *

### **8 ❓ Питання:**

Як за допомогою операторів зсуву та `AND` можна виділити окремі байти з `int` числа? ⭐️

> #### **💬 Відповідь:**
>
> `int` займає 4 байти. Щоб виділити кожен байт, можна послідовно зсувати число вправо і застосовувати маску `0xFF`, яка "відріже" все, крім молодшого байта.
>
> **Алгоритм:**
>
> 1. Щоб отримати **наймолодший байт (0-й)**, просто застосовуємо маску: `(n & 0xFF)`.
> 2. Щоб отримати **наступний байт (1-й)**, зсуваємо число на 8 біт вправо, а потім застосовуємо маску: `(n >> 8) & 0xFF`.
> 3. Для **2-го байта**: `(n >> 16) & 0xFF`.
> 4. Для **3-го (найстаршого) байта**: `(n >> 24) & 0xFF`.
>
> **Приклад коду:**
>
> ```java
> int number = 0x12345678; // 4 байти: 12, 34, 56, 78
>
> int byte0 = (number)       & 0xFF; // 0x78
> int byte1 = (number >> 8)  & 0xFF; // 0x56
> int byte2 = (number >> 16) & 0xFF; // 0x34
> int byte3 = (number >> 24) & 0xFF; // 0x12
>
> System.out.printf("Байт 0: 0x%X\n", byte0); // 0x78
> System.out.printf("Байт 1: 0x%X\n", byte1); // 0x56
> System.out.printf("Байт 2: 0x%X\n", byte2); // 0x34
> System.out.printf("Байт 3: 0x%X\n", byte3); // 0x12
> ```
>
> Використання `>>` замість `>>>` тут некритичне для позитивних чисел, але є кращою практикою, оскільки зберігає знак при роботі з від'ємними.

#### **📌 Що перевіряють:**

*Практичні навички виділення частин даних з цілих чисел за допомогою побітових операцій. Це часто потрібно при роботі з форматами файлів чи мережевими протоколами.*

* * *

### **9 ❓ Питання:**

Наведіть приклад, де логічний зсув `>>>` є корисним і необхідним. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Логічний зсув `>>>` є необхідним, коли нам потрібно працювати з бітами числа, **ігноруючи його знак**, або коли ми хочемо обробити `int` як **контейнер 32 беззнакових біт**.
>
> **Класичний приклад — обробка кольорів у форматі ARGB.**
>
> Колір часто представляють як 32-бітне `int`, де кожен з 4 байтів відповідає за один канал: **A**lpha (прозорість), **R**ed, **G**reen, **B**lue.
>
> `int color = 0xFF123456;`
>
> * `FF` — Alpha
> * `12` — Red
> * `34` — Green
> * `56` — Blue
>
> Якщо це число інтерпретувати як знаковий `int`, воно буде від'ємним, оскільки старший біт `1`.
>
> Щоб виділити червоний компонент (`12`), нам потрібно зсунути число на 16 біт вправо.
>
> * **Якщо використати `>>`:**
>     `color >> 16` дасть від'ємне число, бо знаковий біт (з каналу Alpha) буде скопійований. Результат буде некоректним.
> * **Якщо використати `>>>`:**
>     `color >>> 16` зсуне біти, заповнюючи вільні місця нулями. Це "скине" знаковий біт і залишить нас з `0x0000FF12`. Потім за допомогою маски `& 0xFF` ми можемо отримати чисте значення `0x12`.
>
> ```java
> int color = 0xFF123456;
> int red = (color >>> 16) & 0xFF; // red = 0x12
> ```
>
> Саме тут `>>>` є єдиним правильним вибором.

#### **📌 Що перевіряють:**

*Здатність застосовувати правильний інструмент (`>>>`) для вирішення реальної проблеми, де семантика знака є небажаною.*

* * *

### **10 ❓ Питання:**

Як зміниться результат, якщо в операції `x << y` число `x` має тип `long`, а `y` — `int`? ⭐️

> #### **💬 Відповідь:**
>
> Якщо лівий операнд має тип `long`, то операція зсуву виконується з **64-бітною логікою**.
>
> **Основні зміни:**
>
> 1. **Результат операції** завжди буде типу `long`.
> 2. **Кількість зсувів** обчислюється як `y % 64`. Тобто, використовуються 6 молодших біт `y`.
>
> **Приклад:**
>
> ```java
> long x = 1L;
> int y = 65;
>
> // x << y:
> // Фактичний зсув буде 65 % 64 = 1.
> // 1L << 1 = 2L
> long result = x << y; // result буде 2
> ```
>
> Якби `x` був `int`, то `y` обчислювалося б як `65 % 32 = 1`, і результат був би таким самим, але `result` мав би тип `int`.
>
> **Важливий момент з переповненням:**
>
> ```java
> int i = 1_500_000_000;
> // (i << 1) -- відбудеться переповнення int, результат від'ємний
> System.out.println(i << 1);
>
> long l = 1_500_000_000L;
> // (l << 1) -- виконується з long, переповнення немає
> System.out.println(l << 1);
> ```
>
> Це демонструє, що тип лівого операнда визначає "простір" для виконання зсуву і, відповідно, його результат.

#### **📌 Що перевіряють:**

*Знання правил numeric promotion для операторів зсуву, які відрізняються від інших арифметичних операцій.*

* * *

### **11 ❓ Питання:**

Яким буде результат `(-1) << 1` і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Результатом буде **-2**.
>
> **Пояснення на рівні бітів:**
>
> 1. Значення `-1` типу `int` у доповняльному коді — це патерн з 32 одиниць:
>     `11111111 11111111 11111111 11111111`
>
> 2. Виконуємо зсув вліво на 1 позицію: `<< 1`.
>     * Усі біти зсуваються на одну позицію вліво.
>     * Крайній лівий біт (знаковий) **відкидається**.
>     * Позиція, що звільнилася справа, **заповнюється нулем**.
>
> 3. Результат:
>     `11111111 11111111 11111111 11111110`
>
> 4. Тепер інтерпретуємо цей бітовий патерн. Це представлення числа **-2** у доповняльному коді.
>     * `+2` → `...0010`
>     * Інвертуємо → `...1101`
>     * Додаємо 1 → `...1110`
>
> Це логічно, оскільки `<< 1` еквівалентне множенню на 2. `-1 * 2 = -2`. Переповнення не відбувається.

#### **📌 Що перевіряють:**

*Розуміння того, як зсуви працюють з від'ємними числами в доповняльному коді.*

* * *

### **12 ❓ Питання:**

Чи можна отримати `0` за допомогою лівого зсуву `<<` з ненульового числа? Якщо так, то як? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.**
>
> Це станеться, коли в результаті зсуву **всі значущі біти** (одиниці) будуть "виштовхнуті" за межі типу даних.
>
> **Приклад для `byte` (8 біт):**
>
> `byte b = (byte) 0b10000000;` // це -128
> `byte result = (byte) (b << 1);`
>
> 1. `10000000 << 1` → `00000000` (старша `1` "вийшла" за межі байта).
> 2. Результат `result` буде **0**.
>
> **Приклад для `int`:**
>
> ```java
> int number = Integer.MIN_VALUE; // ...10000000 00000000 00000000 00000000
> System.out.println(Integer.toBinaryString(number));
>
> // Зсунемо всі біти, включаючи старшу 1, за межі int
> number = number << 1;
>
> // Результатом буде 0
> System.out.println(number); // Виведе 0
> System.out.println(Integer.toBinaryString(number)); // 0
> ```
>
> Будь-яке `int` число `x` стане 0 після `x << 32`, але, як ми знаємо, `<< 32` еквівалентно `<< 0`, тому це не спрацює. Правильний спосіб — зсунути на достатню кількість позицій, щоб виштовхнути всі одиниці. Наприклад, `1 << 31` ще не буде нулем, але `1 << 32` (що є `1 << 0`) — теж не нуль. А от `1 << 31` перетвориться на `Integer.MIN_VALUE`.

* * *

#### **Змінена відповідь на питання №12:**

> #### **💬 Відповідь:**
>
> **Так, можна.** Це станеться, коли в результаті лівого зсуву (`<<`) **всі значущі біти** (одиниці) будуть "виштовхнуті" за ліву межу типу даних, і в межах типу залишаться лише нулі.
>
> **Приклад:**
>
> Розглянемо число `8` (`...00001000`). Якщо зсунути його на 29 позицій вліво:
> `8 << 29` → `int result = 8 << 29;`
>
> 1. Початкове число: `000...0001000` (1 на 3-й позиції)
> 2. Після зсуву на 29, одиниця опиниться на `3 + 29 = 32`-й позиції.
> 3. Оскільки тип `int` має 32 біти (нумерація 0-31), 32-га позиція вже знаходиться **за межами** типу.
> 4. Всі 32 біти числа `result` будуть заповнені нулями.
>
> **Як це перевірити:**
> `Integer.MAX_VALUE` → `0111...`. `Integer.MAX_VALUE << 1` → `111...1110` (від'ємне).
> `Integer.MAX_VALUE << 2` → `111...1100` (від'ємне).
>
> Візьмемо `int x = -1` (`111...111`).
> `x << 1` → `111...110` (-2).
> `x << 31` → `100...000` (`Integer.MIN_VALUE`).
> `x << 32` (тобто `x << 0`) → `x` (-1).
>
> Щоб гарантовано отримати 0, потрібно щоб число було таким, щоб після зсуву всі його біти опинилися поза межами типу. Наприклад, для `int`, `1 << 31` дасть `Integer.MIN_VALUE`, а не 0. `Integer.MIN_VALUE << 1` дасть `0`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що операції відбуваються в межах фіксованого розміру типу, і як біти, що "виходять" за межі, впливають на результат.*

* * *

### **13 ❓ Питання:**

Як реалізувати множення цілого числа на 10, використовуючи лише побітові зсуви та додавання? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне завдання на оптимізацію, де множення на довільне число можна розкласти на суму множень на степені двійки, які реалізуються зсувами.
>
> **Ідея:**
> `N * 10 = N * (8 + 2)`
> `N * 10 = (N * 8) + (N * 2)`
>
> * Множення на 8 — це зсув вліво на 3 (`<< 3`).
> * Множення на 2 — це зсув вліво на 1 (`<< 1`).
>
> **Реалізація:**
>
> ```java
> public int multiplyBy10(int n) {
>     return (n << 3) + (n << 1);
> }
> ```
>
> **Приклад для `n = 7`:**
>
> * `n << 3` → `7 << 3` → `56` (`7 * 8`)
> * `n << 1` → `7 << 1` → `14` (`7 * 2`)
> * Результат: `56 + 14 = 70`.
>
> Це значно швидше, ніж стандартна операція множення, оскільки вона замінена на дві дуже швидкі операції зсуву та одне додавання. JIT-компілятор часто виконує таку оптимізацію (strength reduction) автоматично для констант.

#### **📌 Що перевіряють:**

*Здатність декомпозувати математичну задачу та застосувати побітові операції для її ефективного вирішення.*

* * *

### **14 ❓ Питання:**

Чи можна отримати однаковий результат, використовуючи `>> 1` та `>>> 1`? Якщо так, то коли? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.**
>
> Результати `>> 1` (арифметичний зсув) та `>>> 1` (логічний зсув) будуть **абсолютно ідентичними** для будь-якого **додатного числа** або **нуля**.
>
> **Пояснення:**
>
> * **Різниця** між ними полягає лише в тому, яким бітом заповнюється звільнена позиція зліва.
> * `>>` заповнює її **знаковим бітом**.
> * `>>>` заповнює її **нулем**.
>
> Для **додатних чисел та нуля** знаковий біт завжди дорівнює **`0`**.
> Отже, в обох випадках звільнена позиція буде заповнена **нулем**.
>
> **Приклад:**
>
> ```java
> int positiveNum = 100; // ...01100100
>
> System.out.println(positiveNum >> 1);  // 50
> System.out.println(positiveNum >>> 1); // 50
> // Результат однаковий
>
> int negativeNum = -100;
> System.out.println(negativeNum >> 1);  // -50
> System.out.println(negativeNum >>> 1); // 2147483598
> // Результат різний
> ```>
> Таким чином, `>>>` відрізняється від `>>` своєю поведінкою **лише для від'ємних чисел**.

#### **📌 Що перевіряють:**

*Дуже точне розуміння механіки роботи операторів зсуву, включаючи їх поведінку для різних діапазонів значень (додатних та від'ємних).*

* * *

### **15 ❓ Питання:**

Яким буде результат `(-1) >>> 0`? Поясніть свою відповідь. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Результатом буде **`-1`**.
>
> Це питання-пастка, яке перевіряє точне розуміння того, як працюють зсуви, і змушує думати, чи є якісь особливі випадки.
>
> **Пояснення:**
>
> Оператор зсуву `x >>> y` зсуває біти числа `x` на `y` позицій.
>
> Якщо `y` дорівнює **`0`**, це означає, що зсув виконується на **нуль позицій**.
>
> Ніякого переміщення бітів не відбувається. Початкове число залишається без змін.
>
> 1. Беремо `int` зі значенням `-1`. Його двійкове представлення — це 32 одиниці (`111...111`).
> 2. Виконуємо зсув на 0 позицій.
> 3. Нічого не змінюється.
> 4. Результатом залишається початкове значення, тобто **`-1`**.
>
> Це правило є вірним для всіх трьох операторів зсуву: `<< 0`, `>> 0`, `>>> 0` завжди повертають початкове число.
>
> Плутанина могла б виникнути, якби кандидат почав міркувати про логічний зсув та заповнення нулями, забувши, що кількість зсувів дорівнює нулю.

#### **📌 Що перевіряють:**

*Максимальну уважність до деталей та здатність не "перемудрити" відповідь, а слідувати базовій логіці оператора.*
