---
layout: default
title: "Доповнення до двох (Two’s complement)"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 8
---

# Доповнення до двох (Two’s complement)

* короткий опис: Two's Complement – це стандартний метод представлення від'ємних цілих чисел у двійковій формі, який використовується в Java та більшості комп'ютерів. Отримується шляхом інвертування всіх бітів числа та додавання 1.

* поняття: Two's complement, знаковий біт, інверсія бітів.

* приклади: Представлення `-5` як `11111011` (8-бітний), формула `~n = -(n+1)`.

* ключові моменти: Використовується для спрощення арифметичних операцій над знаковими числами, має лише одне представлення нуля.

* що важливо знати на співбесіді: Як представляються від'ємні числа в Java.

### **✅ Офіційне визначення:**

Two’s complement — стандартне представлення від’ємних чисел у двійковій формі в комп’ютерах.

В Java всі знакові цілі типи (**`byte, short, int, long`**) саме так і кодуються.

### **🧠 Простими словами:**

Java не має окремого "мінусового" біта. Вона просто перевертає всі біти числа (0 -> 1, 1 -> 0\) і додає 1\.

Це схоже на магію, що перетворює додатне в від’ємне — і дає можливість робити додавання та віднімання, ніби це одне й те саме

---

## **Пояснення алгоритму (📊)**

Щоб отримати двійкове від’ємне число:

1. Візьми додатне число (наприклад, 12\)
2. Переведи його в двійковий код
3. Інвертуй всі біти (0 ↔ 1\)
4. Додай 1 до результату

---

## **Пояснення на пальцях (💡)**

**Приклад:** –12

* 12 у 8-біт -> **`00001100`**
* Інверсія -> **`11110011`**
* \+1 -> **`11110100`**  
  Отже, –12 \= **`11110100`**

---

## **Корисні приклади (🧪)**

```java
byte x = -12;
System.out.println(Integer.toBinaryString(x & 0xFF)); // 11110100
```

---

## **Порівняння (⚖️)**

| Число | Binary | Інверсія | \+1 | Two’s complement |
| :---: | :---: | :---: | :---: | :---: |
| 12 | 00001100 | – | – | 00001100 |
| –12 | – | 11110011 | \+1 | 11110100 |
| –1 | 00000001 | 11111110 | \+1 | 11111111 |
| –128 | 10000000 | – | – | Особливий випадок |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
+N -> двійкове -> інвертуй всі біти -> додай 1 -> отримуєш -N
```

---

## **🟩 Підсумок:**

* **`Two’s complement`** — це спосіб, як Java і більшість комп’ютерів кодують від’ємні числа. Просто інвертуй біти і додай 1 — і отримаєш “бінарний мінус”. Це дозволяє робити арифметику без зайвих головоломок і фейлів.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке двійковий доповняльний код (Two's complement) і чому він є стандартом для представлення знакових цілих чисел у комп'ютерах?

> #### **💬 Відповідь:**
>
> **Доповняльний код** — це математична операція та стандартний метод для представлення від'ємних цілих чисел у двійковій системі. Усі знакові цілочисельні типи в Java (`byte`, `short`, `int`, `long`) використовують саме цей підхід.
>
> **Чому він є стандартом:**
>
> Його популярність зумовлена тим, що він значно **спрощує апаратну реалізацію арифметичних операцій**, зокрема:
>
> 1. **Єдине представлення нуля:** На відміну від інших методів (наприклад, прямого коду зі знаковим розрядом, де є `+0` і `-0`), у доповняльному коді існує лише одне унікальне представлення нуля (`00...00`).
>
> 2. **Спрощення віднімання:** Найважливіша перевага полягає в тому, що операція віднімання (`A - B`) зводиться до операції додавання (`A + (-B)`). Це дозволяє процесору використовувати один і той же апаратний блок (суматор) як для додавання, так і для віднімання, що робить його архітектуру простішою та дешевшою.
>
> 3. **Послідовна арифметика:** Арифметичні операції працюють однаково як для додатних, так і для від'ємних чисел, включаючи механізм переповнення (wrap-around).

#### **📌 Що перевіряють:**

*Розуміння того, чому саме цей метод був обраний як індустріальний стандарт. Кандидат повинен пояснити інженерні переваги, а не тільки описати алгоритм.*

* * *

### **2 ❓ Питання:**

Опишіть покроковий алгоритм для отримання доповняльного коду від'ємного числа. Наведіть приклад для -13 у 8-бітному форматі.

> #### **💬 Відповідь:**
>
> Щоб отримати двійкове представлення від'ємного числа `-N` у доповняльному коді, потрібно виконати наступні три кроки:
>
> 1. **Взяти додатне число N** і записати його у двійковому форматі, доповнивши нулями зліва до потрібної кількості біт.
> 2. **Інвертувати всі біти** (замінити всі `0` на `1` і `1` на `0`). Цей результат називається оберненим кодом (one's complement).
> 3. **Додати одиницю** до отриманого результату.
>
> **Приклад для -13 у 8-бітному форматі:**
>
> 1. **Представлення `+13`:**
>     `13` у двійковій системі — це `1101`. У 8-бітному форматі: `00001101`.
>
> 2. **Інвертування бітів:**
>     `00001101` → `11110010`.
>
> 3. **Додавання одиниці:**
>     `11110010 + 1 = 11110011`.
>
> Отже, десяткове `-13` у 8-бітному доповняльному коді — це двійкове **`11110011`**.

#### **📌 Що перевіряють:**

*Володіння основним алгоритмом та здатність застосувати його на практиці для конкретного прикладу.*

* * *

### **3 ❓ Питання:**

Яка роль знакового біта (sign bit) у доповняльному коді?

> #### **💬 Відповідь:**
>
> У доповняльному коді **старший (крайній лівий) біт** відіграє роль **знакового біта**.
>
> * Якщо знаковий біт дорівнює **0**, число є **додатним або нулем**.
> * Якщо знаковий біт дорівнює **1**, число є **від'ємним**.
>
> На відміну від прямого коду, знаковий біт тут є не просто "прапорцем", а **невід'ємною частиною математичного представлення числа**.
>
> У позиційній системі доповняльного коду старший біт має **від'ємну вагу**. Для `N`-бітного числа його вага дорівнює **-2^(N-1)**.
>
> **Приклад для 8-бітного `11111011` (-5):**
>
> `(1 * -128) + (1*64) + (1*32) + (1*16) + (1*8) + (0*4) + (1*2) + (1*1) = -128 + 123 = -5`.
>
> Саме ця від'ємна вага старшого розряду і робить число від'ємним.

#### **📝 Коментар:**

Розуміння від'ємної ваги старшого біта — це альтернативний і більш математично строгий спосіб перетворення від'ємних двійкових чисел у десяткові.

#### **📌 Що перевіряють:**

*Глибоке розуміння структури числа в доповняльному коді, зокрема ролі знакового біта як повноцінного розряду з від'ємною вагою.*

* * *

### **4 ❓ Питання:**

Поясніть, чому діапазон для `byte` від -128 до 127, а не, наприклад, від -127 до 127. Чому він асиметричний?

> #### **💬 Відповідь:**
>
> Асиметрія діапазону є прямою властивістю доповняльного коду.
>
> 1. `byte` має 8 біт, що дає 2⁸ = **256** унікальних комбінацій.
> 2. **Нуль (`0`)** має єдине представлення: `00000000`. Це займає одну з 256 комбінацій.
> 3. **Додатні числа:** Старший біт повинен бути `0`. Комбінації від `00000001` до `01111111` представляють числа від **1 до 127**. Це 127 значень.
> 4. **Від'ємні числа:** Старший біт повинен бути `1`. Залишилося `256 - 1 (для нуля) - 127 (для додатних) = 128` комбінацій. Ці комбінації (від `10000000` до `11111111`) представляють числа від **-128 до -1**. Це 128 значень.
>
> Найменше значення, `10000000`, представляє **-128**. У нього немає "додатного" відповідника. Спроба взяти `abs(-128)` призводить до переповнення, бо `+128` не вміщується у 7 біт для значень.
>
> Таким чином, через єдиний нуль і представлення від'ємних чисел у нас є на одне від'ємне число більше, ніж додатних. Ця асиметрія характерна для всіх знакових цілочисельних типів у Java.

#### **📌 Що перевіряють:**

*Здатність пояснити асиметрію діапазону через особливості бітового представлення. Розуміння того, як розподіляються комбінації бітів.*

* * *

### **5 ❓ Питання:**

Що поверне `~10` в Java і чому?

> #### **💬 Відповідь:**
>
> Оператор побітового **`NOT` (`~`)** інвертує кожен біт у двійковому представленні числа. Результат виклику `~10` буде **-11**.
>
> Існує проста формула для побітового NOT: `~n = -(n + 1)`.
>
> **Пояснення на рівні бітів (для 32-бітного `int`):**
>
> 1. Десяткове **`10`** у двійковому `int`:
>     `00000000 00000000 00000000 00001010`
>
> 2. Інвертуємо всі біти:
>     `11111111 11111111 11111111 11110101`
>
> 3. Тепер потрібно перетворити цей результат назад у десяткове число. Оскільки старший біт `1`, це від'ємне число в доповняльному коді. Застосуємо зворотний алгоритм:
>     * Віднімемо 1: `...11110101 - 1 = ...11110100`
>     * Інвертуємо біти: `...00001011`
>     * Це двійкове представлення числа `11` (8 + 2 + 1).
>     * Оскільки початкове число було від'ємним, результат — **-11**.

#### **📌 Що перевіряють:**

*Знання роботи оператора побітового NOT та його зв'язку з доповняльним кодом. Уміння застосувати як формулу, так і бітові маніпуляції.*

* * *

### **6 ❓ Питання:**

Як виконується операція віднімання `10 - 5` за допомогою доповняльного коду?

> #### **💬 Відповідь:**
>
> Операція віднімання `A - B` зводиться до додавання `A + (-B)`, де `-B` представлено у доповняльному коді.
>
> **Крок 1: Отримати доповняльний код для `-5` (в 8-бітному форматі):**
>
> * `+5` → `00000101`
> * Інвертуємо → `11111010`
> * Додаємо 1 → `11111011` (це є `-5`)
>
> **Крок 2: Додати `10` та `-5`:**
>
> * `10` → `00001010`
> * `-5` → `11111011`
>
>     ```
>       00001010  (10)
>     + 11111011  (-5)
>     ----------------
>      100000101  (результат)
>     ```
>
> **Крок 3: Інтерпретувати результат:**
>
> * Результат `100000101` має 9 біт. Оскільки ми працюємо у 8-бітному форматі, **дев'ятий біт (переповнення) ігнорується**.
> * Залишається `00000101`, що в десятковій системі дорівнює **`5`**.
>
> Таким чином, ми виконали віднімання, використовуючи лише операцію додавання, що і є головною перевагою доповняльного коду.

#### **📌 Що перевіряють:**

*Практичне розуміння, як доповняльний код спрощує апаратну реалізацію арифметики.*

* * *

### **7 ❓ Питання:**

Як результат `Math.abs(Integer.MIN_VALUE)` пов'язаний з доповняльним кодом і переповненням? ⭐️

> #### **💬 Відповідь:**
>
> Виклик `Math.abs(Integer.MIN_VALUE)` повертає **від'ємне число**, тобто саме `Integer.MIN_VALUE`. Це є прямим наслідком асиметрії діапазону в доповняльному коді.
>
> **Пояснення на рівні бітів:**
>
> 1. `Integer.MIN_VALUE` у 32-бітному доповняльному коді — це **`1000...000`** (1 і 31 нуль).
> 2. Операція унарного мінуса (яку виконує `Math.abs` для від'ємного числа) в доповняльному коді еквівалентна інвертуванню всіх бітів і додаванню одиниці.
> 3. Давайте виконаємо це для `1000...000`:
>     * Інвертуємо біти: `0111...111` (0 і 31 одиниця).
>     * Додаємо 1: `0111...111 + 1 = 1000...000`.
> 4. Ми **повернулися до початкового бітового патерну** `1000...000`, що відповідає `Integer.MIN_VALUE`.
>
> Абсолютне значення `Integer.MIN_VALUE` (`2^31`) на одиницю більше за `Integer.MAX_VALUE` (`2^31 - 1`), тому воно не може бути представлене як додатне `int`. Відбувається **переповнення**, і результат "обертається" назад до `Integer.MIN_VALUE`.

#### **📌 Що перевіряють:**

*Глибоке розуміння граничних випадків, пов'язаних з діапазонами типів та їх представленням у доповняльному коді.*

* * *

### **8 ❓ Питання:**

Чим відрізняються побітові оператори зсуву `>>` (арифметичний) та `>>>` (логічний) з точки зору доповняльного коду? ⭐️

> #### **💬 Відповідь:**
>
> Різниця полягає в тому, як вони заповнюють біти, що звільняються зліва, що має критичне значення для від'ємних чисел у доповняльному коді.
>
> * **`>>` (Арифметичний зсув вправо, Sign-propagating right shift):**
>   * **Зберігає знак числа.**
>   * Коли біти зсуваються вправо, позиція, що звільняється зліва (знаковий біт), заповнюється **копією поточного знакового біта**.
>   * Для додатних чисел (знаковий біт 0) зліва додаються нулі.
>   * Для від'ємних чисел (знаковий біт 1) зліва додаються одиниці.
>   * Тому `>>` еквівалентний **цілочисельному діленню на степінь двійки**. `-10 >> 1` дасть `-5`.
>
> * **`>>>` (Логічний зсув вправо, Zero-fill right shift):**
>   * **Не зберігає знак.**
>   * Позиція, що звільняється зліва, **завжди заповнюється нулем**, незалежно від знака числа.
>   * Для від'ємного числа це призведе до того, що знаковий біт стане 0, і число буде інтерпретуватися як дуже велике додатне.
>   * Наприклад, `-10 >>> 1` дасть велике додатне число.
>
> `>>>` використовують тоді, коли потрібно працювати з `int` як з набором 32 біт, ігноруючи його знак.

#### **📌 Що перевіряють:**

*Знання нюансів побітових операцій, особливо їх впливу на від'ємні числа, що є важливою частиною розуміння доповняльного коду.*

* * *

### **9 ❓ Питання:**

Які альтернативи доповняльному коду для представлення від'ємних чисел існують, і чому вони використовуються рідше? ⭐️

> #### **💬 Відповідь:**
>
> Існує кілька інших способів, але вони мають суттєві недоліки порівняно з доповняльним кодом.
>
> **1. Прямий код (Sign and magnitude):**
>
> * **Ідея:** Старший біт використовується як знак (0 для '+', 1 для '-'), а решта бітів зберігають абсолютне значення (модуль) числа.
> * **Приклад:** `+5` → `0...0101`, `-5` → `1...0101`.
> * **Недоліки:**
>   * **Два представлення нуля:** `+0` (`00...00`) і `-0` (`10...00`), що ускладнює перевірку на нуль.
>   * **Складна арифметика:** Алгоритми додавання та віднімання повинні враховувати знаки операндів, що вимагає складнішої апаратної логіки.
>
> **2. Обернений код (One's complement):**
>
> * **Ідея:** Від'ємне число отримується простою інверсією всіх бітів додатного.
> * **Приклад:** `+5` → `0...0101`, `-5` → `1...1010`.
> * **Недоліки:**
>   * **Також має два представлення нуля:** `00...00` (`+0`) і `11...11` (`-0`).
>   * Хоча арифметика трохи простіша, ніж у прямому коді, вона все одно складніша, ніж у доповняльному (вимагає так званого "циклічного переносу").
>
> **Висновок:** Доповняльний код переміг, оскільки він забезпечує найпростішу та найефективнішу апаратну реалізацію арифметичних операцій.

#### **📌 Що перевіряють:**

*Повноту розуміння теми та знання альтернатив. Кандидат може пояснити, чому стандартне рішення є найкращим.*

* * *

### **10 ❓ Питання:**

Чому цей код скомпілюється, але результат буде "неправильним"? `byte b = (byte)200;` ⭐️

> #### **💬 Відповідь:**
>
> Цей код скомпілюється, бо ми використовуємо **явне приведення типів (`cast`)**. Цим ми кажемо компілятору: "Я знаю, що тут може бути втрата даних, і я беру відповідальність на себе".
>
> **Чому результат "неправильний":**
>
> Відбувається **звужуюче примітивне перетворення (narrowing primitive conversion)**.
>
> 1. Число `200` у 32-бітному `int` представленні:
>     `00000000 00000000 00000000 11001000`
> 2. При приведенні до `byte` Java просто **відкидає старші 24 біти** і залишає лише молодші 8.
>     `11001000`
> 3. Тепер цей 8-бітний патерн інтерпретується як `byte` у **доповняльному коді**.
> 4. Оскільки старший біт `1`, це від'ємне число.
> 5. Перетворюємо `11001000` в десяткове:
>     * Інвертуємо: `00110111`
>     * Додаємо 1: `00111000`
>     * `00111000` (двійкове) = `32 + 16 + 8 = 56`.
>     * Оскільки початкове було від'ємним, результат **-56**.
>
> Таким чином, `byte b = (byte)200;` присвоїть `b` значення `-56`.

#### **📌 Що перевіряють:**

*Розуміння того, як працює явне приведення типів на бітовому рівні та як це пов'язано з доповняльним кодом.*

* * *

### **11 ❓ Питання:**

Як представлення `null` в Java пов'язано з доповняльним кодом? ⭐️

> #### **💬 Відповідь:**
>
> **Ніяк.** Це питання з підступом.
>
> * **Доповняльний код** використовується виключно для представлення **цілочисельних примітивних типів** (`byte`, `short`, `int`, `long`).
>
> * **`null`** — це не число. Це спеціальний **літерал для посилальних типів** (`reference types`), який означає, що посилання **не вказує на жоден об'єкт** у пам'яті.
>
> У них абсолютно різна природа і сфера застосування. Не можна застосувати доповняльний код до `null` або навпаки. Їх плутають лише через те, що обидва мають відношення до представлення "відсутності" або "негативності", але на абсолютно різних рівнях.
>
> Внутрішнє представлення `null` в JVM зазвичай є нульовим вказівником (`0x0`), але це деталь реалізації, і вона ніяк не пов'язана з доповняльним кодом, який є частиною арифметики цілих чисел.

#### **📌 Що перевіряють:**

*Здатність розрізняти концепції, що стосуються примітивних типів, від концепцій, що стосуються посилальних типів. Уміння розпізнати питання-пастку.*

* * *

### **12 ❓ Питання:**

Який зв'язок між типом `char` та доповняльним кодом? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Прямого зв'язку немає.** `char` в Java є **єдиним беззнаковим (unsigned)** цілочисельним типом.
>
> * Він займає 16 біт і його діапазон від **0 до 65,535**.
> * Він не використовує доповняльний код, оскільки не призначений для представлення від'ємних чисел.
>
> Однак, **непрямий зв'язок** виникає, коли `char` бере участь в арифметичних операціях. При цьому він розширюється до `int`.
>
> **Приклад:**
>
> ```java
> char c = 'A'; // Unicode 65
> int i = -1;
>
> // char 'c' розширюється до int 65
> // `int` i вже в доповняльному коді
> int result = c + i; // 65 + (-1) = 64
>
> System.out.println(result); // 64
> System.out.println((char)result); // '@' (Unicode 64)
> ```
>
> Таким чином, `char` (беззнаковий) може взаємодіяти з `int` (знаковим, у доповняльному коді) через спільний тип `int`, до якого вони обидва приводяться перед операцією. Але сам `char` доповняльний код не використовує.

#### **📌 Що перевіряють:**

*Глибокі знання системи типів Java, включаючи унікальний статус `char` як беззнакового типу.*

* * *

### **13 ❓ Питання:**

Чи можна реалізувати арифметику в доповняльному коді, використовуючи `BigInteger`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, `BigInteger` повністю підтримує семантику доповняльного коду.** Це є його стандартним способом представлення від'ємних чисел.
>
> Оскільки `BigInteger` може представляти числа довільного розміру, його "бітова довжина" є динамічною. Однак він поводиться так, ніби він завжди використовує доповняльний код для своїх операцій.
>
> **Приклади, що це демонструють:**
>
> 1. **Побітові операції:** Всі побітові оператори (`and`, `or`, `not`, `xor`) працюють так, ніби `BigInteger` має "нескінченне" розширення знакового біта.
>
>     ```java
>     BigInteger ten = new BigInteger("10");
>     BigInteger notTen = ten.not(); // ~10
>     // notTen буде -11
>     System.out.println(notTen);
>     ```
>
>     Це відповідає формулі `~n = -(n+1)`.
>
> 2. **Робота з від'ємними числами:** Всі операції, такі як `add`, `subtract`, коректно працюють з від'ємними числами відповідно до правил доповняльного коду.
>
> `BigInteger` абстрагує розробника від ручної реалізації доповняльного коду для чисел, що виходять за межі `long`, надаючи надійний і перевірений інструментарій.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки для великих чисел та розуміння того, що `BigInteger` наслідує стандартні принципи представлення чисел, прийняті в Java.*

* * *

### **14 ❓ Питання:**

Чому цей код призводить до нескінченного циклу? `for(int i = 2147483647 - 1; i <= 2147483647; i++)` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це хитра варіація проблеми переповнення, що демонструє властивості доповняльного коду.
>
> `2147483647` — це `Integer.MAX_VALUE`.
>
> **Покрокове виконання циклу:**
>
> 1. **Початкова ітерація:** `i = Integer.MAX_VALUE - 1`. Умова `i <= Integer.MAX_VALUE` істинна. Тіло циклу виконується. `i` інкрементується до `Integer.MAX_VALUE`.
>
> 2. **Наступна ітерація:** `i = Integer.MAX_VALUE`. Умова `i <= Integer.MAX_VALUE` істинна. Тіло циклу виконується.
>
> 3. **Крок інкременту:** Виконується `i++` для `i = Integer.MAX_VALUE`.
>
> 4. **Переповнення:** Тут відбувається переповнення. `Integer.MAX_VALUE + 1` "обертається" і стає `Integer.MIN_VALUE` (`-2147483648`).
>
> 5. **Перевірка умови:** Тепер `i` дорівнює `Integer.MIN_VALUE`. Умова `i <= Integer.MAX_VALUE` перевіряється: `-2147483648 <= 2147483647`. Ця умова **завжди буде істинною**, оскільки `i` тепер є від'ємним числом.
>
> 6. **Нескінченний цикл:** Значення `i` ніколи не стане більшим за `Integer.MAX_VALUE`. Воно буде циклічно змінюватися у діапазоні `int`, і умова виходу ніколи не буде виконана.
>
> **Висновок:** Переповнення лічильника в циклі може "зламати" умову виходу, якщо вона неправильно спроектована.

#### **📌 Що перевіряють:**

*Здатність аналізувати граничні випадки в циклах та передбачати наслідки переповнення, яке базується на доповняльному коді.*

* * *

### **15 ❓ Питання:**

Як можна отримати абсолютне значення для будь-якого `int`, включаючи `Integer.MIN_VALUE`, без переповнення та без використання `long`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це складне завдання, яке вимагає використання побітових "трюків", щоб обійти стандартну проблему переповнення `Math.abs(Integer.MIN_VALUE)`.
>
> **Ідея:**
> Для будь-якого числа `n` в доповняльному коді, `-n` еквівалентно `~n + 1`. Ми можемо використати цю властивість.
>
> **Алгоритм:**
>
> 1. Визначаємо знак числа. Найпростіше це зробити, зсунувши знаковий біт на крайню праву позицію. Для `int` це `n >> 31`. Для додатних `n` це дасть `0`, для від'ємних — `-1` (всі біти одиниці).
> 2. Далі ми можемо використати операцію `XOR`. `n ^ mask` інвертує біти `n` там, де в масці стоять одиниці. Якщо `mask` дорівнює `-1` (всі біти `1`), `n ^ mask` еквівалентно `~n`.
> 3. Отже, `(n + mask) ^ mask` може дати абсолютне значення.
>
> **Реалізація:**
>
> ```java
> public static int absNoOverflow(int n) {
>     // Створюємо маску: 0 для додатних n, -1 (всі 1) для від'ємних n
>     int mask = n >> 31; 
>
>     // Якщо n додатне, mask=0. (n + 0) ^ 0 = n.
>     // Якщо n від'ємне, mask=-1.
>     // (n + (-1)) ^ (-1)  =  (n-1) ^ (-1)
>     // це (~n + 1 -1) ^ -1 = (~n) ^ (-1) = n
>     // Тут щось не те, це працює для -n, а не abs(n)
>
>     // Правильний трюк:
>     // n + mask -- якщо n від'ємне, це додавання (-1)
>     // ^ mask -- інвертує біти там де маска 1
>     return (n + mask) ^ mask;
> }
> ```
>
> *Перевіряємо для `-5 (1...1011)`*: `mask = -1 (1...1111)`. `(-5 + -1) ^ -1` = `-6 ^ -1` = `(1...1010) ^ (1...1111) = 0...0101 = 5`. Працює!
>
> *Перевіряємо для `Integer.MIN_VALUE (10...000)`*: `mask = -1 (1...1111)`.
> `(MIN_VALUE - 1)` — це `MAX_VALUE`. `MAX_VALUE ^ -1` = `(01...111) ^ (11...111) = 10...000`, що є `MIN_VALUE`. Не спрацювало.

* * *
Виправлю відповідь №15:

> #### **💬 Відповідь:**
>
> **Прямого способу отримати `2147483648` у змінній типу `int` немає.** Але якщо завдання полягає в тому, щоб написати метод `abs`, який не "ламається" для `Integer.MIN_VALUE`, а повертає коректний, хоч і несподіваний, результат, то є трюк.
>
> Класична проблема `Math.abs` у тому, що `-n` для `n = Integer.MIN_VALUE` призводить до переповнення.
>
> **Побітовий трюк для обчислення `abs(n)`:**
>
> 1. Спочатку ми отримуємо маску, яка складається з усіх одиниць, якщо число від'ємне, і з усіх нулів, якщо число додатне. Це робиться за допомогою арифметичного зсуву: `int mask = n >> 31;`.
>
> 2. Далі використовуємо властивість `XOR`: `n XOR -1` інвертує всі біти `n`.
>
> 3. **Фінальний алгоритм:** `(n + mask) ^ mask`
>
> ```java
> public static int absWithoutBranching(int n) {
>     // Отримуємо маску: 0 для n >= 0,  -1 (всі 1) для n < 0
>     final int mask = n >> 31;
>
>     // Якщо n >= 0: mask=0 -> (n+0)^0 = n.
>     // Якщо n < 0: mask=-1 -> (n-1)^-1
>     // Для n = -5 -> (-5-1)^-1 = -6^-1 = 5
>     // Для n = MIN_VALUE -> (MIN_VALUE-1)^-1 = MAX_VALUE ^ -1
>     // 0111...111 ^ 1111...111 = 1000...000, що є MIN_VALUE.
>     // Так, цей трюк дає той самий результат для MIN_VALUE
>
>     // Давайте використаємо інший підхід.
>     // abs(n) = (n ^ (n >> 31)) - (n >> 31)
>     return (n ^ mask) - mask;
> }
> ```
>
> *Перевіряємо `abs(n) = (n ^ mask) - mask`:*
>
> * Для `n = 5`: `mask = 0`. `(5 ^ 0) - 0 = 5`.
> * Для `n = -5`: `mask = -1`. `(-5 ^ -1) - (-1)`. `(-5 ^ -1)` це `4`. `4 - (-1) = 5`.
> * Для `n = Integer.MIN_VALUE`: `mask = -1`. `(MIN_VALUE ^ -1) - (-1)`.
> `(10...0 ^ 11...1)` це `01...1` (`MAX_VALUE`).
> `MAX_VALUE - (-1)` це `MAX_VALUE + 1`, що **переповнюється** і дає `MIN_VALUE`.
>
> **Висновок:** На жаль, **неможливо** отримати коректне додатне значення `2147483648`, залишаючись у межах типу `int`. Всі побітові трюки також будуть "обертатися" через переповнення для `Integer.MIN_VALUE`. Єдиний спосіб отримати правильне математичне абсолютне значення — це **використовувати тип з ширшим діапазоном**, наприклад `long`.
>
> ```java
> public static long absSafe(int n) {
>     return n == Integer.MIN_VALUE ? 2147483648L : Math.abs(n);
> }
> ```

#### **📌 Що перевіряють:**

*Експертне розуміння обмежень цілочисельної арифметики. Кандидат повинен усвідомити, що деякі математичні операції просто неможливі в межах фіксованого типу, і знати, коли потрібно переходити на типи з ширшим діапазоном.*
