---
layout: default
title: "Побітові маски та флаги"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 16
---

# Побітові маски та флаги

* короткий опис: Використання бітів у цілому числі для представлення та управління набором `boolean` станів (флагів). Кожен біт виступає як окремий "вимикач".

* поняття: Бітова маска, флаг, `1 << n` (для створення маски біта), `|=` (увімкнути), `&= ~` (вимкнути), `^=` (перемкнути), `&` (перевірити).

* приклади: `flags |= (1 << 2)` (увімкнути 2-й біт), `(flags & (1 << 0)) != 0` (перевірити 0-й біт).

* ключові моменти: Компактне зберігання станів, ефективні операції для керування набором прапорів.

* що важливо знати на співбесіді: Як встановлювати, скидати, перевіряти, інвертувати біти.

### **✅ Офіційне визначення:**

Побітова маска (**`bitmask`**) — це число, в якому окремі біти (позиції) мають значення **`1`**, що означає активні флаги. Застосовується для **встановлення, зчитування, скидання або перевірки станів** у вигляді одного цілого значення (**`int`**, **`byte`**, **`long`**).

### **🧠 Простими словами:**

Це спосіб запакувати багато **`true / false`** значень у **одне число**, де кожен біт — це окремий вимикач.

Наприклад: **`0b00000101`** — це значить, що флаг №0 і №2 активні (рахуємо справа наліво з нуля).

---

## **Основні операції з масками (📊)**

| Операція | Код | Пояснення |
| :---: | :---: | :---: |
| **Увімкнути** | \`flags | \= 1 \<\< n;\` |
| **Вимкнути** | **`flags &= ~(1 << n);`** | Обнуляє біт **`n`** |
| **Перемкнути** | **`flags ^= 1 << n;`** | 0 -> 1 або 1 -> 0 |
| **Перевірити** | **`(flags & (1 << n)) != 0`** | Чи біт `n` встановлений? |

---

## **Пояснення на пальцях (💡)**

```java
int flags = 0b00000000; // Всі флаги вимкнені
```

✅ Хочеш увімкнути флаг №2?

```java
flags |= 1 << 2; // -> 0b00000100
```

❌ Хочеш його вимкнути?

```java
flags &= ~(1 << 2); // -> 0b00000000 (знову всі вимкнені)
```

❓ Перевірити, чи він увімкнений?

```java
if ((flags & (1 << 2)) != 0) {
    System.out.println("Флаг №2 увімкнено");
}
```

---

## **Корисні приклади (🧪)**

```java
int flags = 0;

// Встановити флаг №1
flags |= 1 << 1; // 0b00000010

// Встановити флаг №3
flags |= 1 << 3; // 0b00001010

// Перевірити флаг №1
if ((flags & (1 << 1)) != 0) {
    System.out.println("Флаг 1 активний");
}

// Зняти флаг №1
flags &= ~(1 << 1); // 0b00001000
```

---

## **Порівняння (⚖️)**

🔸 **Права доступу:**

```java
final int READ = 1 << 0;     // 0b00000001
final int WRITE = 1 << 1;    // 0b00000010
final int EXECUTE = 1 << 2;  // 0b00000100
```

🔸 **Режими роботи:** DEBUG, VERBOSE, TESTING  
🔸 **Оптимізація памʼяті:** Наприклад, для IoT, мобільних ігрових систем  
🔸 **Фреймворки/Ігри:** Зберігання станів обʼєкта (наприклад, “живий”, “невидимий”, “неуразливий”)

---

## **🧠 Мнемоніка для запам’ятовування:**

**Бітмейстерська формула:**  
"Зсув \+ побітова магія \= компактна істина в числі\!"

| Символ | Запам’ятовування |
| :---: | :---: |
| **`1 << n`** | Обираємо біт **`n`** |
| **`/=`** | Вмикаємо (OR -> **`/`**) |
| **`&= ~`** | Вимикаємо |
| **`&`** | Перевіряємо |
| **`^=`** | Перемикаємо (toggle) |

---

## **🟩 Підсумок:**

* Побітові маски — це спосіб **ефективно керувати великою кількістю логічних станів** в одному числі.

* Через операції зсуву (**`1 << n`**) можна точно керувати кожним бітом: увімкнути, вимкнути, перевірити, перемкнути.

* Незамінні в системному програмуванні, грі на ресурсах, роботі з режимами й конфігураціями.

Звісно. Я ознайомився з конспектом. На співбесідах питання про побітові маски та флаги є чудовим способом перевірити, чи кандидат вміє мислити "на рівні бітів" та застосовувати ці знання для вирішення практичних задач з оптимізації та керування станом.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке бітові флаги (bit flags) та бітова маска (bitmask)?

> #### **💬 Відповідь:**
>
> Це техніка програмування, яка дозволяє зберігати та маніпулювати набором булевих станів (прапорців або флагів) у межах однієї цілочисельної змінної.
>
> * **Бітовий флаг (Bit Flag):** Це окремий біт, який представляє один конкретний стан `true`/`false`. `1` означає, що флаг встановлено (активний), `0` — що його скинуто (неактивний).
>
> * **Бітова маска (Bitmask):** Це значення (зазвичай `int` або `long`), в якому один або кілька бітів встановлено в `1`. Маска використовується як "трафарет" для застосування побітових операцій (`AND`, `OR`, `XOR`) до змінної з флагами, щоб маніпулювати ними.
>
> **Приклад:**
> В одній 8-бітній змінній `permissions` можна зберігати 8 різних прапорів дозволів. Кожен біт — окремий дозвіл.
>
> ```java
> // 0-й біт -- READ, 1-й -- WRITE, 2-й -- EXECUTE
> int permissions = 0b00000101; // Дозволено READ та EXECUTE
>
> int MASK_WRITE = 0b00000010; // Маска для роботи з правом WRITE
> ```
>
> Цей підхід надзвичайно ефективний з точки зору пам'яті та швидкості.

#### **📝 Аналогія:**

Це як панель з 8 вимикачами світла. Кожен вимикач (`біт`) — це окремий флаг. Число `permissions` — це опис поточного стану всієї панелі. Маска — це ваш палець, яким ви натискаєте на конкретний вимикач.

#### **📌 Що перевіряють:**

*Базове розуміння концепції, яка є основою для всіх наступних питань про маніпуляції з бітами.*

* * *

### **2 ❓ Питання:**

Як створити маску для роботи з `N`-м бітом?

> #### **💬 Відповідь:**
>
> Маска для `N`-го біта — це число, у якого встановлено в `1` тільки `N`-й біт, а всі інші — `0`. (Нумерація бітів ведеться справа наліво, починаючи з 0).
>
> Найпростіший та найпоширеніший спосіб зробити це — використати **побітовий зсув вліво (`<<`)** для числа `1`.
>
> **Формула:** `1 << N`
>
> **Пояснення:**
>
> * Число `1` у двійковому вигляді має встановленим лише 0-й біт: `...00000001`.
> * Оператор `1 << N` зсуває цю єдину одиницю на `N` позицій вліво, ефективно встановлюючи `N`-й біт.
>
> **Приклади:**
>
> * Маска для 0-го біта: `1 << 0` → `1` (`0b0001`)
> * Маска для 1-го біта: `1 << 1` → `2` (`0b0010`)
> * Маска для 4-го біта: `1 << 4` → `16` (`0b10000`)
>
> Ця ідіома є стандартною для створення масок.

#### **📝 Коментар:**

Запис `1 << N` є набагато читабельнішим та менш схильним до помилок, ніж написання готових значень (наприклад, `16`), бо він явно показує, з яким бітом ми працюємо.

#### **📌 Що перевіряють:**

*Знання стандартної ідіоми для створення масок. Це базовий "будівельний блок" для всіх операцій з флагами.*

* * *

### **3 ❓ Питання:**

Як за допомогою маски **встановити (set)** конкретний біт у змінній?

> #### **💬 Відповідь:**
>
> Щоб встановити `N`-й біт (зробити його рівним `1`), не змінюючи інші, використовується **побітове АБО (`|`)** разом з маскою.
>
> **Операція:** `flags |= (1 << N);` (це скорочення для `flags = flags | (1 << N);`)
>
> **Пояснення:**
>
> * Оператор `|` повертає `1`, якщо хоча б один з бітів дорівнює `1`.
> * Маска `1 << N` має `1` тільки на `N`-й позиції.
> * При застосуванні `OR` з цією маскою:
>   * `N`-й біт у `flags` стане `1` (`0|1=1` або `1|1=1`).
>   * Усі інші біти у `flags` застосують `OR` з нулями з маски, тому вони **не змінять свого значення** (`0|0=0`, `1|0=1`).
>
> **Приклад: встановити 2-й біт у числі 9 (`1001`)**
>
> ```java
> int flags = 9;              // ...1001
> int mask = 1 << 2;          // ...0100
>
> flags |= mask;
>
> /*
>   ...1001 (9)
> | ...0100 (4)
> ---------
>   ...1101 (13)
> */
> // flags тепер дорівнює 13
> ```

#### **📌 Що перевіряють:**

*Практичні навички встановлення бітів. Це одна з трьох ключових операцій (встановити, скинути, перевірити).*

* * *

### **4 ❓ Питання:**

Як за допомогою маски **скинути (clear/unset)** конкретний біт у змінній?

> #### **💬 Відповідь:**
>
> Щоб скинути `N`-й біт (зробити його рівним `0`), не змінюючи інші, використовується **побітове І (`&`)** разом з **інвертованою маскою (`~`)**.
>
> **Операція:** `flags &= ~(1 << N);`
>
> **Пояснення:**
>
> 1. Спочатку створюється маска для `N`-го біта: `1 << N`. Вона має `1` на `N`-й позиції і `0` скрізь.
> 2. Потім маска **інвертується** за допомогою `~`. Тепер вона має `0` на `N`-й позиції і `1` на всіх інших.
> 3. Застосовується `AND` з цією інвертованою маскою:
>     * `N`-й біт у `flags` застосує `AND` з `0` і **гарантовано стане `0`**.
>     * Усі інші біти у `flags` застосують `AND` з `1` і **не змінять свого значення**.
>
> **Приклад: скинути 3-й біт у числі 13 (`1101`)**
>
> ```java
> int flags = 13;             // ...1101
> int mask = ~(1 << 3);       // ~...1000 -> ...0111
>
> flags &= mask;
>
> /*
>   ...1101 (13)
> & ...0111 (~8)
> ---------
>   ...0101 (5)
> */
> // flags тепер дорівнює 5
> ```

#### **📌 Що перевіряють:**

*Практичні навички скидання бітів. Кандидат повинен знати про необхідність інвертувати маску.*

* * *

### **5 ❓ Питання:**

Як за допомогою маски **перевірити (check)**, чи встановлено конкретний біт?

> #### **💬 Відповідь:**
>
> Щоб перевірити, чи встановлено `N`-й біт, використовується **побітове І (`&`)** разом з маскою, а результат порівнюється з нулем.
>
> **Операція:** `(flags & (1 << N)) != 0`
>
> **Пояснення:**
>
> * Маска `1 << N` має `1` лише на `N`-й позиції.
> * Операція `&` з цією маскою "обнулить" усі біти у `flags`, крім `N`-го.
> * **Якщо `N`-й біт у `flags` був `1`**, то результат `&` буде ненульовим (а саме, дорівнюватиме `1 << N`).
> * **Якщо `N`-й біт у `flags` був `0`**, то результат `&` буде `0`.
>
> **Приклад: перевірити 2-й біт у числі 12 (`1100`)**
>
> ```java
> int flags = 12;            // ...1100
> int mask = 1 << 2;         // ...0100
>
> boolean isSet = (flags & mask) != 0;
>
> /*
>   ...1100 (12)
> & ...0100 (4)
> ---------
>   ...0100 (4) -> не нуль -> true
> */
> // isSet буде true
> ```
>
> **Важливо:** результат слід порівнювати саме з `!= 0`, а не, наприклад, `== 1`, оскільки `(flags & mask)` поверне `1 << N`, а не просто `1`.

#### **📌 Що перевіряють:**

*Практичні навички перевірки бітів. Це остання з трьох ключових операцій.*

* * *

### **6 ❓ Питання:**

Як за допомогою маски **перемкнути (toggle/flip)** конкретний біт?

> #### **💬 Відповідь:**
>
> Щоб інвертувати `N`-й біт (змінити `0` на `1` і `1` на `0`), не чіпаючи інші, використовується **виключне АБО (`XOR` або `^`)**.
>
> **Операція:** `flags ^= (1 << N);`
>
> **Пояснення:**
>
> `XOR` має такі властивості:
>
> * `X ^ 0 = X` (біт не змінюється)
> * `X ^ 1 = ~X` (біт інвертується)
>
> * Маска `1 << N` має `1` на `N`-й позиції і `0` на всіх інших.
> * При застосуванні `XOR` з цією маскою:
>   * `N`-й біт у `flags` застосує `XOR` з `1` і **інвертується**.
>   * Усі інші біти застосують `XOR` з `0` і **не змінять свого значення**.
>
> **Приклад: перемкнути 2-й біт у числі 9 (`1001`)**
>
> ```java
> int flags = 9;            // ...1001
> int mask = 1 << 2;        // ...0100
>
> flags ^= mask; // -> ...1101 (13)
> flags ^= mask; // -> ...1001 (9)
>
> /*
>   ...1001 (9)
> ^ ...0100 (4)
> ---------
>   ...1101 (13)
> */
> ```

#### **📌 Що перевіряють:**

*Знання властивостей `XOR` та його застосування для реалізації перемикання стану.*

* * *

### **7 ❓ Питання:**

Наведіть реальний приклад з JDK, де використовуються бітові маски та флаги. ⭐️

> #### **💬 Відповідь:**
>
> Чудовим прикладом є клас **`java.lang.reflect.Modifier`**.
>
> Цей клас надає методи для аналізу модифікаторів доступу (`public`, `private`, `protected`), а також інших модифікаторів (`static`, `final`, `abstract`), які є у полів, методів та класів.
>
> "Під капотом" JVM представляє всі модифікатори одного члена класу (наприклад, методу) як **одне ціле число (`int`)**, де кожен модифікатор — це окремий бітовий флаг.
>
> У класі `Modifier` визначені публічні константи для цих флагів:
>
> * `Modifier.PUBLIC` = `0x00000001` (`1 << 0`)
> * `Modifier.PRIVATE` = `0x00000002` (`1 << 1`)
> * `Modifier.PROTECTED` = `0x00000004` (`1 << 2`)
> * `Modifier.STATIC` = `0x00000008` (`1 << 3`)
> * `Modifier.FINAL` = `0x00000010` (`1 << 4`)
> і так далі.
>
> Метод `getModifiers()` у класів `Method`, `Field`, `Class` повертає саме це `int`-значення. А статичні методи, як-от `Modifier.isPublic(int mod)`, `Modifier.isStatic(int mod)`, працюють так:
>
> ```java
> public static boolean isStatic(int mod) {
>     return (mod & STATIC) != 0;
> }
> ```
>
> Тобто вони просто використовують **бітову маску для перевірки**, чи встановлено відповідний флаг.
>
> Це класичний приклад ефективного зберігання набору булевих властивостей в одному цілому числі.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки та здатність пов'язати теоретичну концепцію з її реальною реалізацією в JDK.*

* * *

### **8 ❓ Питання:**

Як перевірити, чи встановлено **кілька бітів одночасно**? Наприклад, що користувач має права і на читання (`READ`), і на запис (`WRITE`). ⭐️

> #### **💬 Відповідь:**
>
> Для цього потрібно створити маску, що містить усі потрібні біти, і перевірити, чи результат операції `AND` дорівнює самій масці.
>
> **Алгоритм:**
>
> 1. Створіть комбіновану маску, об'єднавши маски для окремих прапорів за допомогою `OR` (`|`).
> 2. Застосуйте операцію `AND` між змінною з флагами та комбінованою маскою.
> 3. Порівняйте результат із самою комбінованою маскою.
>
> **Приклад:**
>
> ```java
> final int READ = 1;  // 001
> final int WRITE = 2; // 010
> final int EXECUTE = 4; // 100
>
> int permissions = READ | EXECUTE; // 101
>
> // Створюємо маску для READ і WRITE
> int readWriteMask = READ | WRITE; // 011
>
> // Перевіряємо
> boolean hasBothPermissions = (permissions & readWriteMask) == readWriteMask;
>
> /*
>   ...101 (permissions)
> & ...011 (mask)
> --------
>   ...001 (result) -> НЕ дорівнює масці 011 -> false
> */
>
> System.out.println(hasBothPermissions); // Виведе: false
> ```
>
> **Чому `!= 0` не працює?**
>
> Перевірка `(permissions & readWriteMask) != 0` лише скаже, чи встановлено *хоча б один* з бітів маски. Для перевірки *всіх* бітів маски потрібно порівнювати результат з самою маскою.

#### **📌 Що перевіряють:**

*Увагу до деталей та розуміння тонкощів роботи з комбінованими масками.*

* * *

### **9 ❓ Питання:**

Що робить цей вираз і для чого він може бути корисний? `x & (x - 1)` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз є відомим побітовим трюком, який **скидає (робить нульовим) крайній правий встановлений біт (рівний 1)** у числі `x`.
>
> **Як це працює:**
>
> * Коли ми віднімаємо 1 від будь-якого додатного числа, у його двійковому представленні **всі крайні праві нулі стають одиницями, а крайній правий біт '1' стає '0'**. Всі біти лівіше залишаються без змін.
>
> * **Приклад для `x = 12 (1100)`:**
>   * `x - 1` → `11 (1011)`
>   * Застосовуємо `AND`:
>
>         ```
>           1100 (x)
>         & 1011 (x-1)
>         --------
>           1000 (результат)
>         ```
>     Як бачимо, крайній правий '1' у числі `1100` був скинутий.
>
> **Для чого це корисно:**
>
> 1. **Підрахунок встановлених бітів (алгоритм Кернігана):** Повторюючи цю операцію в циклі, доки число не стане нулем, можна ефективно порахувати кількість '1' у числі. Кількість ітерацій буде дорівнювати кількості встановлених бітів.
>
> 2. **Перевірка, чи є число степенем двійки:** Якщо число є степенем двійки, воно має лише один встановлений біт. Отже, після однієї операції `x & (x - 1)` воно стане нулем. Перевірка: `(x > 0) && ((x & (x - 1)) == 0)`.

#### **📌 Що перевіряють:**

*Знання поширених побітових "хаків" та їхнього застосування для вирішення алгоритмічних задач.*

* * *

### **10 ❓ Питання:**

Поясніть, що таке `EnumSet` і як його ефективність пов'язана з бітовими масками. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `EnumSet` — це спеціалізована, високопродуктивна реалізація інтерфейсу `Set` для роботи з елементами одного перелічувального типу (`enum`).
>
> **Його ефективність напряму пов'язана з тим, що "під капотом" він використовує бітові флаги.**
>
> **Як це реалізовано:**
>
> 1. JVM присвоює кожній константі `enum` унікальний порядковий номер (ordinal), починаючи з 0.
> 2. `EnumSet` використовує цей `ordinal` як **індекс біта** у внутрішній змінній типу `long` (або масиві `long`, якщо констант більше 64).
> 3. **Присутність** `enum`-константи в наборі означає, що відповідний біт встановлено в **1**.
>
> **Наслідки:**
>
> * **Висока продуктивність:** Усі основні операції `Set` (`add`, `contains`, `remove`) зводяться до надзвичайно швидких побітових операцій (`|`, `&`, `^`) замість повільнішого обчислення хеш-кодів та роботи з хеш-таблицями, як у `HashSet`.
> * **Компактність:** `EnumSet` займає дуже мало пам'яті. Наприклад, набір з 10 `enum`-констант буде зберігатися всього в одній `long` змінній (8 байт).
>
> `EnumSet` є ідеальною ілюстрацією того, як концепція бітових масок може бути інкапсульована у високорівневий, безпечний та зручний API.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють структури даних стандартної бібліотеки, та здатність пов'язати високорівневе API з низькорівневими оптимізаціями.*

* * *

### **11 ❓ Питання:**

Як отримати значення `N`-го біта числа, не використовуючи зсуви (`<<`, `>>`, `>>>`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча зсуви є стандартним способом, це можна зробити і за допомогою арифметичних операцій, зокрема **ділення** та **взяття остачі**.
>
> **Алгоритм:**
>
> 1. Щоб "дістатися" до `N`-го біта, нам потрібно спочатку відкинути всі молодші біти. Це можна зробити, поділивши число на `2^N`.
> 2. Після ділення, `N`-й біт початкового числа стане наймолодшим бітом (0-м) у результаті.
> 3. Тепер потрібно просто перевірити, чи є цей результат парним. Якщо він непарний, то його 0-й біт дорівнює `1`, що означає, що `N`-й біт початкового числа був `1`.
>
> **Код:**
>
> ```java
> public int getNthBit(int number, int n) {
>     // (int)Math.pow(2, n) обчислює 2^N
>     return (number / (int)Math.pow(2, n)) % 2;
> }
> ```
>
> **Приклад для `getNthBit(13, 2)` (2-й біт числа `1101`):**
>
> * `n = 13`, `N = 2`. `2^2 = 4`.
> * `13 / 4` → `3` (цілочисельне ділення). `3` в двійковій це `11`. 2-й біт з `1101` став 0-м.
> * `3 % 2` → `1`.
> * Результат `1`, що правильно.
>
> Цей метод є **значно повільнішим**, ніж `(number >> n) & 1`, через використання "дорогих" операцій ділення та піднесення до степеня, але він демонструє альтернативний математичний підхід.

#### **📌 Що перевіряють:**

*Кмітливість та здатність мислити алгоритмічно, знаходячи альтернативні (хоч і менш ефективні) шляхи для вирішення задачі.*

* * *

### **12 ❓ Питання:**

Ви маєте два `int` числа, `a` і `b`. Напишіть вираз, що поверне `true`, якщо вони мають протилежні знаки, використовуючи лише побітові операції. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання можна вирішити, аналізуючи **знакові біти** чисел.
>
> * У додатних чисел знаковий біт (крайній лівий) дорівнює **0**.
> * У від'ємних — **1**.
>
> Числа мають протилежні знаки, якщо один знаковий біт `0`, а інший `1`.
>
> **Ідея:**
> Можна застосувати операцію **`XOR` (`^`)** до двох чисел. `XOR` дає `1` у біті результату, тільки якщо відповідні біти операндів різні.
>
> Якщо `a` і `b` мають протилежні знаки, їхні знакові біти будуть різними. Отже, знаковий біт результату `a ^ b` буде `1`, і результат `a ^ b` буде **від'ємним числом**.
>
> **Рішення:**
>
> ```java
> public boolean haveOppositeSigns(int a, int b) {
>     return (a ^ b) < 0;
> }
> ```
>
> **Розглянемо випадки:**
>
> * `a = 10` (`0...`), `b = -5` (`1...`). `a^b` → знаковий біт `0^1=1`. Результат від'ємний.
> * `a = 10` (`0...`), `b = 5` (`0...`). `a^b` → знаковий біт `0^0=0`. Результат додатний.
> * `a = -10` (`1...`), `b = -5` (`1...`). `a^b` → знаковий біт `1^1=0`. Результат додатний.
>
> Це дуже елегантне та ефективне рішення, яке уникає будь-яких умовних операторів. (За умови, що 0 не розглядається).

#### **📌 Що перевіряють:**

*Вміння використовувати властивості доповняльного коду та `XOR` для написання хитрих та ефективних перевірок.*

* * *

### **13 ❓ Питання:**

Як за допомогою маски можна виділити, наприклад, 4 біти, починаючи з 8-ї позиції? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Щоб виділити групу біт, потрібно два кроки:
>
> 1. **Обнулити** всі біти, що не входять до потрібного діапазону.
> 2. **Зсунути** результат вправо, щоб виділена група біт стала молодшими бітами.
>
> **Алгоритм:**
>
> **Крок 1: Створення маски.**
> Нам потрібна маска з чотирма одиницями (`1111`) на позиціях 8, 9, 10, 11.
>
> * Маска з 4 одиниць — це `0xF` (або `15`).
> * Зсуваємо її на 8 позицій вліво: `int mask = 0xF << 8;`.
>
> **Крок 2: Застосування маски.**
> `int isolatedBits = number & mask;`
> Ця операція залишить лише біти на позиціях 8-11, а всі інші зробить нулями.
>
> **Крок 3: Нормалізація результату.**
> `int result = isolatedBits >>> 8;`
> Зсуваємо результат на 8 позицій вправо. Важливо використовувати логічний зсув `>>>`, щоб уникнути проблем зі знаком, якщо старший біт виділеної групи був `1`.
>
> **Все в одному рядку:**
>
> ```java
> public int getBitRange(int number, int start, int length) {
>     // 1. Створити маску з 'length' одиниць. Трюк: (1 << length) - 1
>     int mask = (1 << length) - 1;
>     // 2. Зсунути маску на стартову позицію
>     mask <<= start;
>     // 3. Виділити і зсунути назад
>     return (number & mask) >>> start;
> }
> // Виклик: getBitRange(number, 8, 4)
> ```

#### **📌 Що перевіряють:**

*Вміння працювати зі складними масками для виділення бітових полів, що є типовою задачею в низькорівневому програмуванні.*

* * *

### **14 ❓ Питання:**

Що означає, коли кажуть, що `XOR` є "оборотним" оператором, і як це використовується для обміну значень двох змінних без третьої? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> "Оборотність" `XOR` полягає в його унікальній властивості:
> **`if C = A ^ B, then A = C ^ B and B = C ^ A`**
>
> Простіше кажучи:
> **`(A ^ B) ^ B = A`**
>
> Якщо ви двічі застосовуєте `XOR` до числа з тим самим ключем, ви отримуєте початкове число. Це робить `XOR` основою для простого шифрування та трюку з обміном значень.
>
> **Обмін значень `a` та `b`:**
>
> ```java
> int a = 10;
> int b = 25;
>
> // 1. a стає "змішаною" версією a і b
> a = a ^ b;
>
> // 2. b стає початковим значенням a
> // b = (a_старе ^ b_старе) ^ b_старе = a_старе
> b = a ^ b;
>
> // 3. a стає початковим значенням b
> // a = (a_старе ^ b_старе) ^ a_старе = b_старе
> a = a ^ b;
> ```
>
> Після цих трьох операцій значення `a` та `b` поміняються місцями.
>
> Цей трюк був популярним у старих мовах програмування для економії пам'яті (не потрібна тимчасова змінна) та іноді для швидкості (уникає звернень до пам'яті).
>
> В сучасній Java цей підхід майже не використовується, оскільки:
>
> * Код менш читабельний, ніж простий обмін через третю змінну.
> * Сучасні JIT-компілятори настільки добре оптимізують код, що виграш у продуктивності, якщо він і є, буде мінімальним.
>
> Але це чудова демонстрація властивостей `XOR`.

#### **📌 Що перевіряють:**

*Глибоке знання властивостей побітових операторів та відомих алгоритмічних трюків. Це питання на кмітливість.*

* * *

### **15 ❓ Питання:**

Як реалізувати прапори з більш ніж двома станами (наприклад, стан: `NONE`, `READ`, `WRITE`, `BOTH`) в одній цілочисельній змінній? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це можливо, якщо виділити для одного стану **групу бітів**, а не один біт.
>
> Припустимо, ми хочемо зберігати один з чотирьох станів. Для кодування 4 значень нам потрібно **2 біти** (`2² = 4`).
>
> **1. Визначення станів та масок:**
>
> Ми виділяємо, наприклад, біти 0 та 1 для нашого стану.
>
> ```java
> public static final int STATE_NONE = 0b00; // 0
> public static final int STATE_READ = 0b01; // 1
> public static final int STATE_WRITE = 0b10; // 2
> public static final int STATE_BOTH = 0b11; // 3
>
> // Маска, щоб "виділити" ці два біти
> public static final int STATE_MASK = 0b11; // 3
> ```
>
> **2. Встановлення стану:**
>
> Щоб встановити новий стан, потрібно спочатку **скинути старі біти стану**, а потім **встановити нові**.
>
> ```java
> int flags = 0b11110101; // Початкові прапори
>
> // Хочемо встановити стан WRITE
> // 1. Скидаємо поточні біти стану
> flags &= ~STATE_MASK; // ...0101 & ...1100 -> ...0100
> // 2. Встановлюємо нові біти стану
> flags |= STATE_WRITE; // ...0100 | ...0010 -> ...0110
> ```
>
> **3. Отримання поточного стану:**
>
> ```java
> int currentState = flags & STATE_MASK;
> switch (currentState) {
>     case STATE_READ: // ...
>     case STATE_WRITE: // ...
> }
> ```
>
> Таким чином, групуючи біти, можна кодувати в одній цілочисельній змінній не тільки набір незалежних `boolean` прапорів, але й кілька полів зі скінченною кількістю станів. Це дуже поширена практика в системному програмуванні та форматах даних.

#### **📌 Що перевіряють:**

*Експертний рівень володіння бітовими масками. Здатність проектувати складні системи станів на низькому рівні. Це питання, що вимагає комбінаторного мислення.*
