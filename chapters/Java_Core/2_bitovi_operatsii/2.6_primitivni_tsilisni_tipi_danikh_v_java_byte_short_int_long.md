---
layout: default
title: "Примітивні цілісні типи даних в Java (byte, short, int, long)"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 6
---

# Примітивні цілісні типи даних в Java (byte, short, int, long)

* короткий опис: Примітивні типи для зберігання цілих чисел: `byte` (8 біт), `short` (16 біт), `int` (32 біти), `long` (64 біти). Всі вони є знаковими.

* поняття: Розмір (біти), діапазон значень, знаковий тип.

* приклади: `byte b = 127;`, `int x = 1_000_000;`, `long bigNum = 123L;`.

* ключові моменти: `int` є типом за замовчуванням, `L` суфікс обов'язковий для `long` літералів, діапазони значень.

* що важливо знати на співбесіді: Розміри та діапазони, типовий вибір.

### **✅ Офіційне визначення:**

У Java цілісні типи — це примітиви для зберігання цілих чисел без дробової частини. Найпоширеніші: **`byte, short, int, long`**. Відрізняються розміром, діапазоном і споживанням пам’яті.

### **🧠 Простими словами:**

* `byte` — маленький хлопчик, який економить пам’ять.
* `int` — найлегендарніший, працьовитий середній.
* `long` — богатир для великих чисел.
* `short` — ніби між ними, але фактично майже непотрібен — як той друг, який завжди на паузі.

---

## **Таблиця типів (📊)**

| Тип | Розмір | Діапазон | За замовчуванням |
| :---: | :---: | :---: | :---: |
| **byte** | 8 біт | –128 до 127 | ❌ |
| **short** | 16 біт | –32\_768 до 32\_767 | ❌ |
| **int** | 32 біт | –2\_147\_483\_648 до 2\_147\_483\_647 | ✅ |
| **long** | 64 біт | –9\_223\_372\_036\_854\_775\_808 до 9\_223\_372\_036\_854\_775\_807 | ❌ |

---

## **Пояснення на пальцях (💡)**

* **`byte`** — як маленький гном: добре зберігає пам’ять, працює з байтами в масивах.
* **`short`** — рідко потрібен, майже забутий.
* **`int`** — герой робочих буднів: лічильники, індекси, все що завгодно.
* **`long`** — для чисел, що хочуть вийти за межі int.

Усі знакові — половина діапазону відведена під від’ємні числа.

---

## **Корисні приклади (🧪)**

```java
byte a = 127;          // Максимум для byte
short b = -32000;
int count = 1_000_000; // 1 мільйон, читається краще з підкресленням
long bigNumber = 9_000_000_000L; // суфікс 'L' обов'язковий!
```

🟥 Помилка:

```java
byte x = 128; // error: incompatible types -- перевищення діапазону
```

---

## **Порівняння (⚖️)**

| Тип | Байтів | Пам’ять | Використання |
| :---: | :---: | :---: | :---: |
| **byte** | 1 | економний | масиви байтів, IO |
| **short** | 2 | економний | рідко, матриці, де треба мало пам’яті |
| **int** | 4 | стандарт | індекси, лічильники, майже все |
| **long** | 8 | великий | таймстампи, великі числа |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
byte < short < int < long
  8б   16б   32б   64б
```

Кожен наступний тип вдвічі більший за попередній — класична геометрія пам’яті

---

## **🟩 Підсумок:**

Цілісні типи в Java — це твої найкращі друзі для роботи з цілими числами різного масштабу. **`int`** — найуніверсальніший, **`long`** — для дійсно великих чисел, **`byte`** — економить пам’ять, **`short`** — рідкісний гість.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Які цілочисельні примітивні типи існують у Java? Назвіть їх та вкажіть розмір у бітах.

> #### **💬 Відповідь:**
>
> У Java існує чотири примітивні типи для зберігання цілих чисел. Вони відрізняються розміром пам'яті, що виділяється, і, як наслідок, діапазоном значень, які можуть зберігати.
>
> Ось вони в порядку зростання розміру:
>
> * **`byte`:**
>   * Розмір: **8 біт**
>   * Призначення: для економії пам'яті у великих масивах, роботи з потоками даних (I/O).
>
> * **`short`:**
>   * Розмір: **16 біт**
>   * Призначення: також для економії пам'яті, але на практиці використовується рідко.
>
> * **`int`:**
>   * Розмір: **32 біти**
>   * Призначення: тип за замовчуванням для цілочисельних значень. Найбільш використовуваний тип для лічильників, індексів, більшості арифметичних операцій.
>
> * **`long`:**
>   * Розмір: **64 біти**
>   * Призначення: для роботи з дуже великими цілими числами, які виходять за межі діапазону `int` (наприклад, мітки часу (timestamps), великі ідентифікатори).

#### **📝 Мнемоніка:**

Розмір кожного наступного типу подвоюється: 8 → 16 → 32 → 64.

#### **📌 Що перевіряють:**

*Знання базової системи типів Java. Це фундаментальне питання, на яке повинен знати відповідь кожен розробник.*

* * *

### **2 ❓ Питання:**

Чи є цілочисельні типи в Java знаковими чи беззнаковими? Який діапазон значень має тип `byte`?

> #### **💬 Відповідь:**
>
> **Усі** цілочисельні примітивні типи в Java є **знаковими (signed)**.
>
> Це означає, що вони можуть представляти як додатні, так і від'ємні числа, а також нуль. У їхньому двійковому представленні старший (крайній лівий) біт використовується як **знаковий біт** (0 для додатних чисел, 1 для від'ємних).
>
> Через це половина діапазону "віддається" під від'ємні числа.
>
> **Діапазон значень типу `byte`:**
>
> * `byte` має 8 біт.
> * Загальна кількість значень: 2⁸ = 256.
> * Діапазон: від **-128** до **+127**.
>
> Діапазон для `N`-бітного знакового типу розраховується як `від -2^(N-1)` до `2^(N-1) - 1`. Для `byte` (N=8) це від -2⁷ до 2⁷-1.

#### **📝 Коментар:**

На відміну від деяких інших мов (наприклад, C++), в Java немає ключових слів для створення беззнакових примітивів (наприклад, `unsigned int`).

#### **📌 Що перевіряють:**

*Розуміння того, як організовано зберігання цілих чисел в Java, включаючи знаковість та розрахунок діапазонів.*

* * *

### **3 ❓ Питання:**

Що станеться, якщо спробувати присвоїти змінній типу `byte` значення, що виходить за її межі?
`byte b = 128;`

> #### **💬 Відповідь:**
>
> Цей код **не скомпілюється**. Компілятор Java видасть помилку типу `incompatible types: possible lossy conversion from int to byte`.
>
> **Пояснення:**
>
> 1. Числовий літерал `128` за замовчуванням має тип **`int`**.
> 2. Компілятор Java є достатньо "розумним". Коли ви намагаєтеся присвоїти `int`-літерал змінній типу `byte` (або `short`), він перевіряє, чи вміщується це значення в діапазон цільового типу.
> 3. Максимальне значення для `byte` — це `127`.
> 4. Оскільки `128` більше за `127`, компілятор розуміє, що таке присвоєння призведе до втрати даних, і забороняє його на етапі компіляції.
>
> **Щоб виправити це (якщо ми впевнені, що нам потрібне саме таке перетворення):**
>
> Потрібно виконати **явне приведення типів (explicit cast)**:
>
> ```java
> byte b = (byte) 128;
> ```
>
> У цьому випадку компілятор "знімає з себе відповідальність". Відбудеться **переповнення**, і в `b` буде записано значення **-128**. Це відбувається через особливості роботи доповняльного коду.

#### **📌 Що перевіряють:**

*Знання правил приведення типів та того, які перевірки виконує компілятор автоматично.*

* * *

### **4 ❓ Питання:**

Чому для літералів типу `long` потрібно додавати суфікс `L`? Що буде, якщо його не вказати?

> #### **💬 Відповідь:**
>
> Суфікс `L` (або `l`) є обов'язковим для числових літералів, які виходять за межі діапазону `int`.
>
> **Причина:**
>
> За замовчуванням, будь-який цілочисельний літерал в Java (наприклад, `123`, `90000`) розглядається компілятором як значення типу **`int`**.
>
> **Що буде, якщо не вказати `L`:**
>
> * **Якщо число вміщується в `int`:**
>
>     ```java
>     long number = 100000; // Все добре, помилки не буде
>     ```
>
>     Тут компілятор бачить `int` літерал `100000` і неявно розширює його до типу `long` при присвоєнні.
>
> * **Якщо число НЕ вміщується в `int`:**
>
>     ```java
>     // Помилка компіляції: integer number too large
>     // long bigNumber = 3000000000;
>     ```
>
>     Максимальне значення для `int` — приблизно 2.1 мільярда. Число `3000000000` не може бути представлене як `int`-літерал. Комп'лятор бачить його як помилковий `int` ще до того, як дійде до присвоєння `long` змінній.
>
> **Правильний запис:**
>
> ```java
> long bigNumber = 3000000000L;
> ```
>
> Суфікс `L` явно вказує компілятору, що цей літерал слід одразу розглядати як `long`, оминаючи обмеження `int`.

#### **📌 Що перевіряють:**

*Розуміння того, як компілятор працює з числовими літералами та їхніми типами за замовчуванням.*

* * *

### **5 ❓ Питання:**

Що таке переповнення цілого числа (integer overflow)? Наведіть приклад.

> #### **💬 Відповідь:**
>
> **Переповнення цілого числа** — це ситуація, що виникає, коли результат арифметичної операції виходить за межі діапазону, який може представити даний числовий тип.
>
> В Java цілочисельні типи поводяться як **циклічні (wrap around)**. Це означає, що при досягненні максимального значення і додаванні до нього 1, значення "перескакує" на мінімальне, і навпаки. Java **не кидає виняток** при переповненні примітивних типів.
>
> **Приклад:**
>
> Тип `int` має максимальне значення `Integer.MAX_VALUE` (2,147,483,647).
>
> ```java
> int maxValue = Integer.MAX_VALUE;
> System.out.println("Max value: " + maxValue); // 2147483647
>
> int overflowedValue = maxValue + 1;
> // Результатом буде Integer.MIN_VALUE
> System.out.println("Max value + 1: " + overflowedValue); // -2147483648
> ```
>
> Це відбувається через особливості роботи двійкового доповняльного коду. `011...111` + `1` = `100...000`, що є представленням `Integer.MIN_VALUE`.
>
> Це дуже небезпечна поведінка, яка може призвести до неочевидних багів, особливо в фінансових чи наукових розрахунках, якщо не використовувати типи з більшим діапазоном (наприклад, `long` або `BigInteger`).

#### **📌 Що перевіряють:**

*Розуміння обмежень примітивних типів та небезпек, пов'язаних з переповненням. Це критично важливо для написання надійного коду.*

* * *

### **6 ❓ Питання:**

Що станеться при виконанні такої операції? Яким буде тип результату?
`short a = 10; short b = 20; short c = a + b;`

> #### **💬 Відповідь:**
>
> Рядок `short c = a + b;` **не скомпілюється**. Компілятор видасть помилку `incompatible types: possible lossy conversion from int to short`.
>
> **Пояснення:**
>
> Це пов'язано з правилом **розширення типів при арифметичних операціях (Numeric Promotion)** у Java.
>
> Коли ви виконуєте арифметичну операцію (`+`, `-`, `*`, `/`) зі змінними типів `byte`, `short` або `char`, Java **автоматично розширює (promotes)** їх до типу **`int`** перед виконанням операції.
>
> 1. Змінні `a` (типу `short`) та `b` (типу `short`) розширюються до `int`.
> 2. Виконується додавання `int` + `int`.
> 3. Результат операції `a + b` має тип **`int`**.
> 4. Потім програма намагається присвоїти цей `int`-результат змінній `c` типу `short`, що є звужуючим перетворенням, яке може призвести до втрати даних. Тому компілятор забороняє його без явного приведення.
>
> **Правильний запис:**
>
> ```java
> short c = (short) (a + b);
> ```

#### **📌 Що перевіряють:**

*Знання правил Numeric Promotion, що є неочевидною, але важливою частиною специфікації мови.*

* * *

### **7 ❓ Питання:**

Коли на практиці варто використовувати `byte` або `short` замість `int`?

> #### **💬 Відповідь:**
>
> Хоча `int` є типом за замовчуванням, існують специфічні сценарії, де `byte` або `short` є кращим вибором. Головна причина — це **економія пам'яті**.
>
> **Використання `byte`:**
>
> * **При роботі з великими масивами:** Якщо у вас є масив з мільйонів або мільярдів елементів, і ви впевнені, що значення кожного елемента не перевищить діапазон `byte` (-128..127), використання `byte[]` замість `int[]` може зекономити пам'ять у 4 рази. Це критично для додатків з великим об'ємом даних.
> * **При роботі з потоками вводу/виводу (I/O) та мережевими протоколами:** Дані часто читаються або записуються у вигляді послідовності байтів. Робота з `byte[]` в цьому випадку є природною.
> * **При обробці зображень, аудіо, відео:** Файли цих форматів часто представляють собою набори байтів.
>
> **Використання `short`:**
>
> * На практиці `short` **використовується вкрай рідко**. Економія пам'яті в 2 рази порівняно з `int` рідко є настільки суттєвою, щоб виправдати його використання. Проблеми з numeric promotion до `int` також роблять роботу з ним менш зручною. Його можна зустріти в успадкованому коді або при взаємодії з C/C++ бібліотеками, де він може відповідати типу `short` з цих мов.
>
> **Загальна рекомендація:** Використовуйте `int`, поки не зіткнетеся з явною проблемою споживання пам'яті у великих масивах. Тоді переходьте на `byte`.

#### **📌 Що перевіряють:**

*Практичне розуміння компромісів (trade-offs) між зручністю використання та оптимізацією пам'яті.*

* * *

### **8 ❓ Питання:**

Що таке числовий літерал з підкресленнями, введений у Java 7? Наведіть приклад. ⭐️

> #### **💬 Відповідь:**
>
> Починаючи з Java 7, для покращення **читабельності** великих числових літералів було дозволено використовувати **символ підкреслення (`_`)** як роздільник між цифрами.
>
> **Правила:**
>
> * Підкреслення можна ставити **лише між цифрами**.
> * Їх не можна ставити на початку або в кінці літералу, а також поруч з десятковою крапкою або суфіксами (`L`, `F`, `D`).
>
> **Приклад:**
>
> Замість важкого для читання:
>
> ```java
> int oneMillion = 1000000;
> long creditCardNumber = 1234567812345678L;
> ```
>
> Можна написати набагато чистіше:
>
> ```java
> int oneMillion = 1_000_000;
> long creditCardNumber = 1234_5678_1234_5678L;
> double pi = 3.141_592_653;
> int binary = 0b1101_0101_0001_0100;
> ```> Компілятор **повністю ігнорує** ці символи підкреслення. Вони існують виключно для зручності розробника і ніяк не впливають на значення числа чи згенерований байткод.

#### **📌 Що перевіряють:**

*Знання сучасних (хоч і не дуже нових) синтаксичних можливостей Java, спрямованих на покращення якості коду.*

* * *

### **9 ❓ Питання:**

Чи можна використовувати `long` для зберігання беззнакових 32-бітних чисел? ⭐️

> #### **💬 Відповідь:**
>
> **Так, це поширена практика.** Хоча в Java немає беззнакових типів, `long` (64 біти) є достатньо великим, щоб вмістити повний діапазон беззнакового 32-бітного числа (від 0 до 2³²-1, що приблизно 4.2 мільярда).
>
> Це часто потрібно при взаємодії з іншими системами або мовами програмування (наприклад, C++ `unsigned int`), які використовують беззнакові типи.
>
> **Як це працює:**
>
> 1. Коли ви отримуєте 32-бітне значення (наприклад, з потоку даних), яке семантично є беззнаковим, ви можете прочитати його як `int`.
> 2. Якщо старший біт цього `int` дорівнює 1, Java буде інтерпретувати його як від'ємне число.
> 3. Щоб отримати правильне беззнакове значення у вигляді `long`, потрібно "обнулити" старші 32 біти `long` і залишити лише молодші 32 біти. Це робиться за допомогою **побітової маски**.
>
> **Приклад:**
>
> ```java
> int signedInt = -1; // Бітове представлення: 0xFFFFFFFF
> // Щоб отримати беззнакове значення
> long unsignedValue = signedInt & 0xFFFFFFFFL;
> System.out.println(unsignedValue); // Виведе 4294967295 (2^32 - 1)
> ```
>
> Маска `0xFFFFFFFFL` гарантує, що при розширенні `int` до `long` знаковий біт не буде скопійований у старші розряди `long`, і ми отримаємо коректне додатне значення.

#### **📌 Що перевіряють:**

*Практичні навички роботи з бітовими масками та перетвореннями типів, що корисно при інтеграції з іншими системами.*

* * *

### **10 ❓ Питання:**

Як Java гарантує, що розміри примітивних типів (наприклад, `int` — 32 біти) є однаковими на всіх платформах? ⭐️

> #### **💬 Відповідь:**
>
> Це одна з ключових особливостей Java, що забезпечує її принцип **"Write Once, Run Anywhere"**.
>
> На відміну від мов програмування типу C++, де розмір `int` може залежати від архітектури процесора (наприклад, 16, 32 або 64 біти), **специфікація мови Java (Java Language Specification - JLS)** жорстко визначає розмір кожного примітивного типу.
>
> * `byte` — завжди 8 біт
> * `short` — завжди 16 біт
> * `int` — завжди 32 біти
> * `long` — завжди 64 біти
>
> **JVM (Java Virtual Machine)**, яка є платформо-залежною, бере на себе відповідальність за реалізацію цих вимог на будь-якій конкретній операційній системі та апаратному забезпеченні.
>
> Це означає, що програма, яка використовує `int`, буде поводитися однаково як на 32-бітній Windows-машині, так і на 64-бітному сервері Linux чи ARM-процесорі в мобільному телефоні. Розробнику не потрібно турбуватися про платформенні відмінності у розмірах типів.

#### **📌 Що перевіряють:**

*Розуміння однієї з фундаментальних переваг платформи Java — кросплатформенності — та того, як вона досягається на рівні специфікації типів.*

* * *

### **11 ❓ Питання:**

Уявіть, що вам потрібно реалізувати лічильник, який буде інкрементуватися в багатопотоковому середовищі. Чи буде `volatile long` достатнім для цієї задачі? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, `volatile long` буде недостатньо** для реалізації потокобезпечного лічильника.
>
> **Пояснення:**
>
> * Ключове слово **`volatile`** гарантує **видимість (visibility)** змін. Тобто, якщо один потік запише нове значення у `volatile` змінну, інші потоки гарантовано побачать це оновлення. Воно запобігає кешуванню значення змінної в регістрах процесора.
>
> * **Проблема:** `volatile` **не гарантує атомарності** для складних операцій.
>
> Операція інкременту (`count++`) не є атомарною. Вона складається щонайменше з трьох кроків:
>
> 1. **Читання** поточного значення `count`.
> 2. **Збільшення** прочитаного значення на 1.
> 3. **Запис** нового значення назад у `count`.
>
> У багатопотоковому середовищі може виникнути **стан гонитви (race condition)**. Два потоки можуть одночасно прочитати одне й те саме значення (наприклад, 10), обидва інкрементувати його до 11, і потім по черзі записати 11. У результаті лічильник буде 11, хоча мав би стати 12.
>
> **Правильне рішення:**
>
> Для реалізації потокобезпечного лічильника слід використовувати класи з пакета `java.util.concurrent.atomic`, наприклад, **`AtomicLong`**. Його метод `incrementAndGet()` є **атомарним** і гарантує коректну роботу в багатопотоковому середовищі завдяки використанню низькорівневих апаратних інструкцій (Compare-And-Swap).

#### **📌 Що перевіряють:**

*Глибокі знання багатопотоковості. Кандидат повинен чітко розрізняти поняття видимості та атомарності та знати відповідні інструменти для їх забезпечення.*

* * *

### **12 ❓ Питання:**

Що таке двійковий доповняльний код (two's complement) і чому він використовується для представлення знакових цілих чисел у Java? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Двійковий доповняльний код** — це стандартний спосіб представлення знакових цілих чисел у комп'ютерах. Усі цілочисельні типи в Java (`byte`, `short`, `int`, `long`) використовують саме його.
>
> **Основні принципи:**
>
> * Старший біт є знаковим (0 - додатні, 1 - від'ємні).
> * Додатні числа зберігаються у звичайному двійковому вигляді.
> * Від'ємне число `-N` отримується шляхом інвертування всіх бітів додатного `N` і додавання одиниці.
>
> **Чому він використовується:**
>
> Головна перевага доповняльного коду полягає в тому, що він значно **спрощує апаратну реалізацію арифметичних операцій**.
>
> 1. **Єдиний нуль:** На відміну від інших підходів (як-от знако-розрядний), у доповняльному коді є лише одне представлення для нуля (`00...00`). Це усуває плутанину.
>
> 2. **Віднімання через додавання:** Віднімання числа `B` від `A` (`A - B`) може бути виконане як додавання доповняльного коду `B` до `A` (`A + (-B)`). Це дозволяє використовувати один і той же апаратний суматор і для додавання, і для віднімання, що робить процесори простішими та дешевшими.
>
> 3. **Проста арифметика:** Арифметика в доповняльному коді є послідовною та передбачуваною, включаючи циклічну поведінку при переповненні.

#### **📌 Що перевіряють:**

*Фундаментальні знання комп'ютерної архітектури та того, чому певні інженерні рішення були обрані при проектуванні процесорів та мов програмування.*

* * *

### **13 ❓ Питання:**

Як `char` в Java може брати участь в цілочисельних операціях? Чи є він знаковим чи беззнаковим? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тип `char` в Java, хоч і призначений для зберігання символів, технічно є **цілочисельним типом**.
>
> * **Розмір:** 16 біт.
> * **Знаковість:** `char` є **єдиним беззнаковим (unsigned)** примітивним типом у Java. Його діапазон від **0** до **65,535** (2¹⁶ - 1). Він представляє коди символів Unicode (UTF-16).
>
> **Участь в цілочисельних операціях:**
>
> Оскільки `char` є числовим типом, він може брати участь в арифметичних операціях. При цьому він, як і `byte` та `short`, **автоматично розширюється до `int`**.
>
> **Приклад:**
>
> ```java
> char a = 'A'; // Unicode-код для 'A' - 65
> char b = 'B'; // Unicode-код для 'B' - 66
>
> // char 'a' розширюється до int 65
> int result = a + 1; // 65 + 1 = 66
>
> // Обидва char розширюються до int
> System.out.println(a + b); // Виведе 131 (65 + 66)
>
> // Щоб отримати символ, потрібно явне приведення
> System.out.println((char)result); // Виведе 'B'
> ```
>
> Це дозволяє робити трюки, як-от `for (char c = 'A'; c <= 'Z'; c++)`, для ітерації по символах. Але слід пам'ятати про неявне розширення до `int`, яке може призвести до неочікуваних результатів при додаванні `char`.

#### **📌 Що перевіряють:**

*Знання тонких нюансів системи типів Java, зокрема, подвійної природи `char` як символу та числа.*

* * *

### **14 ❓ Питання:**

Чому цей код не видасть помилку переповнення, але дасть неочікуваний результат?
`long result = 24 * 60 * 60 * 1000 * 1000;` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код дасть неочікуваний, **неправильний результат** через те, що **обчислення виконується з використанням `int` арифметики**, і лише **після цього** результат присвоюється `long` змінній.
>
> **Покроковий розбір:**
>
> 1. Всі числові літерали в правій частині виразу (`24`, `60`, `1000`) є **`int` літералами**.
> 2. Компілятор Java виконує всі множення зліва направо, і результат кожної проміжної операції також має тип `int`.
> 3. `24 * 60 * 60 * 1000` = `86,400,000`. Це число ще вміщується в діапазон `int`.
> 4. **Проблемний крок:** `86_400_000 * 1000` = `86_400_000_000`. Це число **значно перевищує** `Integer.MAX_VALUE` (приблизно 2.1 млрд).
> 5. Відбувається **переповнення `int`**, і результат "обрізається" та стає непередбачуваним (в даному випадку `1410065408`).
> 6. І **тільки після** цього неправильний `int` результат присвоюється змінній `result` типу `long` через розширення типу.
>
> **Як виправити:**
>
> Потрібно змусити компілятор виконувати обчислення з використанням `long` арифметики. Для цього достатньо зробити **перший операнд** у виразі `long` літералом.
>
> ```java
> long result = 24L * 60 * 60 * 1000 * 1000;
> ```
>
> Тепер, завдяки правилам Numeric Promotion, всі подальші операції будуть виконуватися з типом `long`, і переповнення не відбудеться.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як Java визначає тип виразу, і як це може призвести до "тихих" логічних помилок. Це класичне питання-пастка.*

* * *

### **15 ❓ Питання:**

Чи гарантує Java, що операції з `long` є атомарними на 32-бітних архітектурах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не гарантує.** Це одна з важливих особливостей Java Memory Model.
>
> Специфікація Java дозволяє 64-бітним операціям читання та запису для типів `long` та `double` **не бути атомарними** на 32-бітних архітектурах.
>
> **Що це означає:**
>
> На 32-бітній JVM, щоб записати або прочитати 64-бітне значення, може знадобитися **дві окремі 32-бітні операції**.
>
> **Ризик у багатопотоковому середовищі:**
>
> 1. Припустимо, Потік А записує нове значення `long` у змінну. Він встигає записати перші 32 біти.
> 2. У цей момент планувальник потоків перемикається на Потік Б.
> 3. Потік Б читає цю `long` змінну. Він читає **нові** перші 32 біти і **старі** другі 32 біти.
> 4. В результаті Потік Б отримує "розірване" (torn read), абсолютно некоректне значення, яке не дорівнює ні старому, ні новому значенню.
>
> **Як це вирішити:**
>
> Щоб гарантувати атомарність операцій читання та запису для `long` (і `double`) на будь-якій платформі, змінну слід оголосити з ключовим словом **`volatile`**.
>
> `volatile long myValue;`
>
> `volatile` гарантує, що операції читання та запису для цього поля завжди будуть атомарними, навіть на 32-бітних системах.
>
> (Або, для інкрементів/декрементів, використовувати `AtomicLong`).

#### **📌 Що перевіряють:**

*Експертні знання Java Memory Model та проблем багатопотоковості. Розуміння того, де Java надає гарантії, а де потрібна додаткова обачність з боку розробника.*
