---
layout: default
title: "Двійкова система числення (Binary)"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 2
---

# Двійкова система числення (Binary)

* короткий опис: Позиційна система числення з основою 2, яка використовує лише цифри 0 і 1. Основа комп'ютерної логіки.

* поняття: Основа 2, біт (binary digit).

* приклади: `1011` (двійкове) = `1*2^3 + 0*2^2 + 1*2^1 + 1*2^0` = `11` (десяткове).

* ключові моменти: Кожна позиція відповідає степені 2, використовується комп'ютерами для представлення даних.

* що важливо знати на співбесіді: Чому комп'ютери використовують двійкову систему.

### **✅ Офіційне визначення:**

Двійкова система числення — це **позиційна система з основою 2**, у якій використовуються **лише дві цифри: 0 і 1**.

Кожна цифра множиться на **2 у степені позиції справа наліво**, наприклад:

**`1011 = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 8 + 0 + 2 + 1 = 11`**

### **🧠 Простими словами:**

Комп’ютер — це **машина, що бачить лише 0 і 1**.

Усі числа, які ми знаємо (**`10, 125, 999`**), комп’ютер представляє як **послідовності бітів (binary digits)**.

У Java всі дані (наприклад, **`int`**) зберігаються саме в такій формі

---

## 📊 **Характеристики двійкової системи:**

| Властивість | Опис |
| :---: | :---: |
| **Основа** | 2 |
| **Цифри** | 0, 1 |
| **Тип** | Позиційна |
| **Розряди** | Кожна позиція — ступінь 2 |
| **Приклад** | 1011 \= 8 \+ 0 \+ 2 \+ 1 \= **11** |

---

## **Пояснення на пальцях (💡)**

Тут усе просто: **лише 0 і 1**.  
Кожен розряд — **степінь 2**:

```java
позиції справа наліво -> 2⁰, 2¹, 2², 2³, 2⁴...
тобто: 1, 2, 4, 8, 16, 32, 64, 128...
```

Наприклад, **`23`** -> це **`10111`** у двійковій системі.

---

## **Корисні приклади (🧪)**

🔹 **Десяткове -> Двійкове:**

```java
23 = 1×2⁴ + 0×2³ + 1×2² + 1×2¹ + 1×2⁰
   = 16 + 0 + 4 + 2 + 1
   = 23
   => 10111
```

### **Ще приклади:**

| Десяткове | Двійкове |
| :---: | :---: |
| 0 | 0 |
| 1 | 1 |
| 2 | 10 |
| 4 | 100 |
| 7 | 111 |
| 8 | 1000 |
| 15 | 1111 |
| 32 | 100000 |

---

## **Порівняння (⚖️)**

| Позиція (справа) | Значення в двійковій | Степінь 2 | Внесок у число (10111) |
| :---: | :---: | :---: | :---: |
| 0 | 1 | 2⁰ \= 1 | ✅ 1 |
| 1 | 1 | 2¹ \= 2 | ✅ 2 |
| 2 | 1 | 2² \= 4 | ✅ 4 |
| 3 | 0 | 2³ \= 8 | ❌ 0 |
| 4 | 1 | 2⁴ \= 16 | ✅ 16 |

**Підсумок:** **`10111 = 16 + 4 + 2 + 1 = 23`**

---

## **🧠 Мнемоніка для запам’ятовування:**

* **Binary -> 2 цифри**: **`0`** і **`1`**
* **Кожна позиція -> степінь 2**
* **`1011 = 8 + 0 + 2 + 1 = 11`**

💡 Пам’ятай: "біти — як вимикачі: увімкнено чи вимкнено"

---

## **🟩 Підсумок:**

Двійкова система — це **позиційна система числення з основою 2**, яка використовує **лише 0 і 1**.

Кожна цифра множиться на **2 у степені її позиції**.

Це **основа для всіх цифрових технологій**, від Java до мікропроцесора.

Наприклад, **`1011`** у двійковій — це **`11`** у десятковій.

---

### **🔗 Додаткові джерела:**

* [**Binary number — Wikipedia**](https://en.wikipedia.org/wiki/Binary_number)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке двійкова система числення і чому вона є фундаментальною для комп'ютерів?

> #### **💬 Відповідь:**
>
> **Двійкова система числення (Binary system)** — це позиційна система числення з **основою 2**, яка використовує лише дві цифри для представлення чисел: **0** та **1**. Кожна така цифра називається **біт (binary digit)**.
>
> **Чому вона фундаментальна для комп'ютерів:**
>
> Комп'ютерна техніка побудована на електронних компонентах, зокрема транзисторах, які фізично можуть перебувати лише у двох станах:
>
> * **Увімкнено (On):** Є електричний струм або висока напруга. Це інтерпретується як **1**.
> * **Вимкнено (Off):** Немає струму або низька напруга. Це інтерпретується як **0**.
>
> Двійкова система ідеально відповідає цій двостанній логіці. Вона дозволяє надійно представляти, зберігати та обробляти будь-яку інформацію (числа, текст, зображення) у вигляді послідовностей нулів та одиниць. Будь-яка складніша система (наприклад, десяткова) вимагала б наявності апаратних компонентів, що стабільно розрізняють 10 рівнів напруги, що значно складніше, дорожче і менш надійно.

#### **📝 Аналогія:**

Комп'ютер — це як величезна кімната з мільярдами вимикачів світла. Кожен вимикач може бути або "Увімкнено" (1), або "Вимкнено" (0). Будь-яка інформація кодується у вигляді унікальної комбінації станів цих вимикачів.

#### **📌 Що перевіряють:**

*Розуміння фізичних причин, чому комп'ютери "мислять" у двійковій системі, а не просто знання визначення.*

* * *

### **2 ❓ Питання:**

Як перевести десяткове число 13 у двійкову систему числення? Поясніть процес.

> #### **💬 Відповідь:**
>
> Існує два основних способи: метод ділення з остачею та метод віднімання степенів двійки.
>
> **1. Метод ділення з остачею (найпоширеніший):**
> Ми послідовно ділимо число на 2, записуючи остачі. Двійкове число формується з остач, записаних у зворотному порядку.
>
> * 13 / 2 = 6, остача **1**
> * 6 / 2 = 3, остача **0**
> * 3 / 2 = 1, остача **1**
> * 1 / 2 = 0, остача **1**
>
> Тепер читаємо остачі знизу вгору: **1101**.
> Отже, десяткове 13 = двійкове 1101.
>
> **2. Метод віднімання степенів двійки:**
> Знаходимо найбільший степінь двійки, що є меншим або рівним 13. Це `8` (2³).
>
> * 13 - 8 (2³) = 5. Записуємо 1 для розряду 2³.
> * Найбільший степінь двійки <= 5? Це `4` (2²).
> * 5 - 4 (2²) = 1. Записуємо 1 для розряду 2².
> * Розряду 2¹ (`2`) в залишку немає. Записуємо 0.
> * Найбільший степінь двійки <= 1? Це `1` (2⁰).
> * 1 - 1 (2⁰) = 0. Записуємо 1 для розряду 2⁰.
>
> Отримуємо біти для розрядів 2³, 2², 2¹, 2⁰: **1101**.

#### **📌 Що перевіряють:**

*Практичні навички переведення між системами числення. Це базове завдання для розуміння представлення даних.*

* * *

### **3 ❓ Питання:**

Що таке біт, байт, кілобайт? Скільки значень можна закодувати одним байтом?

> #### **💬 Відповідь:**
>
> Це основні одиниці вимірювання цифрової інформації, які базуються на двійковій системі.
>
> * **Біт (Bit):**
>   * Це найменша, базова одиниця інформації.
>   * Може приймати лише два значення: **0** або **1**.
>
> * **Байт (Byte):**
>   * Це група з **8 біт**. Наприклад, `10110101`.
>   * Історично склалося, що байт — це мінімальна одиниця пам'яті, яку можна адресувати.
>
> * **Кілобайт (KB), Мегабайт (MB), Гігабайт (GB):**
>   * У комп'ютерній науці, на відміну від десяткової системи, ці префікси традиційно означали степені 2, а не 10.
>   * 1 Кілобайт (KB) = 2¹⁰ = **1024 байти**.
>   * 1 Мегабайт (MB) = 1024 KB.
>   * 1 Гігабайт (GB) = 1024 MB.
>   * **Примітка:** Для уникнення плутанини були введені двійкові префікси (Кібібайт `KiB`, Мебібайт `MiB`), але в побуті та маркетингу часто використовують десяткові значення (1 KB = 1000 байт).
>
> **Скільки значень можна закодувати одним байтом?**
>
> Один байт складається з 8 біт. Кожен біт може мати 2 стани. Отже, загальна кількість комбінацій (і значень) — це **2⁸ = 256**.
>
> Це можуть бути:
>
> * Цілі числа без знака від **0** до **255**.
> * Цілі числа зі знаком від **-128** до **127** (з використанням доповняльного коду).
> * 256 різних символів (наприклад, у кодуванні ASCII).

#### **📌 Що перевіряють:**

*Знання базових одиниць вимірювання інформації та їх співвідношення. Це основа для будь-яких розмов про пам'ять та продуктивність.*

* * *

### **4 ❓ Питання:**

Як представлені від'ємні числа в двійковій системі? Поясніть на прикладі доповняльного коду (two's complement).

> #### **💬 Відповідь:**
>
> Від'ємні числа в сучасних комп'ютерах представляються за допомогою **доповняльного коду (two's complement)**. Цей підхід дозволяє виконувати операції додавання та віднімання за допомогою одного й того самого апаратного обладнання.
>
> **Правила доповняльного коду:**
>
> * Старший (крайній лівий) біт використовується як **знаковий біт**. `0` — для додатних чисел, `1` — для від'ємних.
> * **Додатне число** записується у звичайному двійковому вигляді. Наприклад, для 4-бітного представлення, `5` — це `0101`.
> * **Від'ємне число** отримується за таким алгоритмом:
>     1. Взяти двійкове представлення відповідного додатного числа.
>     2. **Інвертувати всі біти** (замінити 0 на 1 і 1 на 0). Це називається оберненим кодом (one's complement).
>     3. **Додати одиницю** до результату.
>
> **Приклад: представити -5 у 4-бітному форматі:**
>
> 1. Двійкове представлення `+5` → `0101`.
> 2. Інвертуємо всі біти → `1010`.
> 3. Додаємо одиницю → `1010 + 1` = `1011`.
>
> Отже, **-5** у 4-бітному доповняльному коді — це **`1011`**.
>
> Ця система елегантно вирішує проблему з поданням нуля (він один — `0000`) і спрощує арифметику.

#### **📌 Що перевіряють:**

*Розуміння того, як комп'ютери працюють з від'ємними числами. Це важлива концепція, що лежить в основі цілочисельної арифметики.*

* * *

### **5 ❓ Питання:**

Що таке побітові операції (bitwise operations) і як вони пов'язані з двійковою системою? Наведіть приклад з `AND` та `OR`.

> #### **💬 Відповідь:**
>
> **Побітові операції** — це операції, які застосовуються безпосередньо до **двійкового представлення** чисел, маніпулюючи окремими бітами.
>
> **Приклади основних операцій:**
>
> * **`AND` (`&`)**: Повертає 1 в біті результату, тільки якщо обидва відповідні біти в операндах дорівнюють 1.
> * **`OR` (`|`)**: Повертає 1, якщо хоча б один з відповідних бітів дорівнює 1.
> * **`XOR` (`^`)**: Повертає 1, якщо відповідні біти різні.
> * **`NOT` (`~`)**: Інвертує всі біти числа.
>
> **Приклад з `AND` та `OR` для десяткових чисел 10 та 12:**
>
> Десяткове `10` → Двійкове `1010`
> Десяткове `12` → Двійкове `1100`
>
> **Операція `AND`:**
>
> ```
>   1010  (10)
> & 1100  (12)
> ------
>   1000  (8)
> ```
>
> `10 & 12` дасть результат `8`.
>
> **Операція `OR`:**
>
> ```
>   1010  (10)
> | 1100  (12)
> ------
>   1110  (14)
> ```
>
> `10 | 12` дасть результат `14`.
>
> Побітові операції використовуються для швидких математичних обчислень, роботи з прапорами, масками, шифруванням та низькорівневим програмуванням.

#### **📌 Що перевіряють:**

*Практичні знання про побітові операції та здатність виконувати їх вручну. Це часто використовується в алгоритмічних задачах.*

* * *

### **6 ❓ Питання:**

Що таке побітові зсуви (bit shifts) `<<`, `>>` та `>>>`? Поясніть на прикладі.

> #### **💬 Відповідь:**
>
> **Побітові зсуви** — це операції, які зсувають усі біти числа вліво або вправо на вказану кількість позицій.
>
> * **`<<` (Зсув вліво):**
>   * Зсуває біти вліво. Позиції справа, що звільнилися, заповнюються **нулями**.
>   * Зсув вліво на N біт еквівалентний **множенню на 2^N**.
>   * **Приклад:** `10 << 2` → `1010` << 2 → `101000` (двійкове) = `40` (десяткове). `10 * 2² = 40`.
>
> * **`>>` (Арифметичний зсув вправо):**
>   * Зсуває біти вправо. Позиції зліва, що звільнилися, заповнюються **знаковим бітом** (0 для додатних, 1 для від'ємних), щоб зберегти знак числа.
>   * Зсув вправо на N біт еквівалентний **цілочисельному діленню на 2^N**.
>   * **Приклад:** `10 >> 1` → `0...1010` >> 1 → `0...0101` = `5`. `-10 >> 1` → `...10110` >> 1 → `...11011` = `-5`.
>
> * **`>>>` (Логічний або беззнаковий зсув вправо):**
>   * Зсуває біти вправо. Позиції зліва, що звільнилися, **завжди заповнюються нулями**, незалежно від знака числа.
>   * Для додатних чисел працює так само, як `>>`.
>   * Для від'ємних чисел **не зберігає знак** і може перетворити від'ємне число на велике додатне.
>   * **Приклад:** `int a = -1; System.out.println(a >>> 1);` виведе дуже велике додатне число, бо всі біти `1` зсунуться, а зліва з'явиться `0`.

#### **📌 Що перевіряють:**

*Знання просунутих побітових операцій та різниці між арифметичним і логічним зсувом. Це важливо для низькорівневих оптимізацій.*

* * *

### **7 ❓ Питання:**

Як за допомогою двійкових операцій можна швидко перевірити, чи є число парним? Або помножити/поділити на 2?

> #### **💬 Відповідь:**
>
> Двійкові операції дозволяють виконувати деякі арифметичні трюки значно швидше, ніж стандартні операції.
>
> **1. Перевірка на парність/непарність:**
>
> * Число є **непарним** тоді і тільки тоді, коли його **найменш значущий біт (крайній правий) дорівнює 1**.
> * Можна перевірити це за допомогою операції `AND` з одиницею.
>
> ```java
> if ((number & 1) == 0) {
>     // Число парне (останній біт 0)
> } else {
>     // Число непарне (останній біт 1)
> }
> ```
>
> `number & 1` обнуляє всі біти, крім останнього.
>
> **2. Множення на 2:**
>
> * Множення на 2 еквівалентне **побітовому зсуву вліво на 1 позицію (`<< 1`)**.
>     `int result = number << 1;`
>
> **3. Цілочисельне ділення на 2:**
>
> * Цілочисельне ділення на 2 еквівалентне **арифметичному зсуву вправо на 1 позицію (`>> 1`)**.
>     `int result = number >> 1;`
>
> Ці операції на рівні процесора виконуються набагато швидше, ніж повне множення чи ділення, і їх часто використовують у коді, критичному до продуктивності, та в системному програмуванні.

#### **📌 Що перевіряють:**

*Практичне застосування побітових операцій для мікро-оптимізацій. Здатність мислити "на рівні бітів".*

* * *

### **8 ❓ Питання:**

Чому вираз `-1` в `int` у двійковій системі складається з усіх одиниць? ⭐️

> #### **💬 Відповідь:**
>
> Це випливає з правил представлення чисел у **доповняльному коді (two's complement)**.
>
> Давайте представимо `-1` у 32-бітному `int`:
>
> 1. Візьмемо відповідне додатне число: `+1`.
> 2. Його двійкове представлення в 32-бітному форматі:
>     `00000000 00000000 00000000 00000001`
>
> 3. **Інвертуємо всі біти** (отримуємо обернений код):
>     `11111111 11111111 11111111 11111110`
>
> 4. **Додаємо одиницю** до результату:
>     `...11111110 + 1 = ...11111111`
>
> **Результат:** `11111111 11111111 11111111 11111111`
>
> Отже, двійкове представлення `-1` для типу `int` — це 32 одиниці. Ця властивість робить деякі побітові операції дуже зручними. Наприклад, логічний зсув вправо `>>>` на `1` для `-1` перетворить його на `Integer.MAX_VALUE / 2` (приблизно).

#### **📌 Що перевіряють:**

*Глибоке розуміння доповняльного коду та його властивостей для граничних значень, як-от `-1`.*

* * *

### **9 ❓ Питання:**

Що таке бітова маска? Наведіть приклад, як за допомогою маски встановити або перевірити певний біт. ⭐️

> #### **💬 Відповідь:**
>
> **Бітова маска** — це змінна (зазвичай цілочисельна), яка використовується для маніпуляцій з бітами іншої змінної за допомогою побітових операцій (`AND`, `OR`, `XOR`). Маска дозволяє "виділити", встановити, скинути або перевірити конкретні біти.
>
> **Приклад: У нас є 8 прапорів, кожен представлений одним бітом у байті.**
> `byte flags = 0b00101000;` // Деякі прапори встановлені
>
> **1. Перевірити, чи встановлено 3-й біт (нумерація справа, з 0):**
>
> * Створюємо маску, де встановлено лише 3-й біт. `int mask = 1 << 3;` (тобто `0b00001000`)
> * Використовуємо `AND`: `(flags & mask) != 0`
>
>     ```
>       00101000
>     & 00001000
>     ----------
>       00001000  (не нуль, отже біт був встановлений)
>     ```
>
> **2. Встановити 5-й біт (зробити його рівним 1):**
>
> * Створюємо маску для 5-го біта: `int mask = 1 << 5;` (`0b00100000`)
> * Використовуємо `OR`: `flags = (byte) (flags | mask);`
>
>     ```
>       00101000
>     | 00100000
>     ----------
>       00101000 // тепер стало 00101000
>       // був 00001000, 5й біт - 0... зараз буде...
>       flags = 0b00001000
>       mask =  0b00100000
>       ----------
>       flags | mask = 0b00101000
>     ```
>
> Виправлюсь: `byte flags = 0b00001000;`
> `flags | mask` -> `0b00101000` (встановили 5-й біт).
>
> **3. Скинути 3-й біт (зробити його рівним 0):**
>
> * Створюємо маску і інвертуємо її: `int mask = ~(1 << 3);` (`~0b00001000` → `...11110111`)
> * Використовуємо `AND`: `flags = (byte) (flags & mask);`
>
> Це дуже поширений підхід у мережевому програмуванні, драйверах, системах дозволів.

* * *
Виправимо відповідь №9:

> #### **💬 Відповідь:**
>
> **Бітова маска** — це змінна (зазвичай цілочисельна), яка використовується для маніпуляцій з бітами іншої змінної за допомогою побітових операцій (`AND`, `OR`, `XOR`). Маска дозволяє "виділити", встановити, скинути або перевірити конкретні біти.
>
> **Приклад: У нас є 8 прапорів, кожен представлений одним бітом у байті.**
> `byte flags = 0b00001000;` // Встановлено лише 3-й біт.
>
> **1. Перевірити, чи встановлено 3-й біт (нумерація справа, з 0):**
>
> * Створюємо маску: `int mask = 1 << 3;` (це `0b00001000`)
> * Використовуємо `AND`: `(flags & mask) != 0`
>
>     ```
>       00001000 (flags)
>     & 00001000 (mask)
>     ----------
>       00001000 (результат не нуль, отже, біт був встановлений) -> true
>     ```
>
> **2. Встановити 5-й біт (зробити його рівним 1):**
>
> * Створюємо маску для 5-го біта: `int mask = 1 << 5;` (це `0b00100000`)
> * Використовуємо `OR`: `flags |= mask;`
>
>     ```
>       00001000 (flags)
>     | 00100000 (mask)
>     ----------
>       00101000 (новий flags)
>     ```
>
> **3. Скинути 3-й біт (зробити його рівним 0):**
>
> * Створюємо маску і інвертуємо її: `int mask = ~(1 << 3);` (`~0b00001000` → `...11110111`)
> * Використовуємо `AND`: `flags &= mask;`
>
>     ```
>       00101000 (попередній flags)
>     & 11110111 (інвертована маска)
>     ----------
>       00100000 (новий flags, 3-й біт скинуто)
>     ```
>
>
> Це дуже поширений підхід у мережевому програмуванні, драйверах, системах дозволів.

#### **📌 Що перевіряють:**

*Здатність застосовувати побітові операції для вирішення реальних задач низькорівневого програмування.*

* * *

### **10 ❓ Питання:**

Що виведе цей код: `System.out.println(-5 >> 1);` та `System.out.println(-5 >>> 1);`? Поясніть різницю. ⭐️

> #### **💬 Відповідь:**
>
> **Вивід:**
>
> ```
> -3
> 2147483645
> ```
>
> **Пояснення:**
>
> Число `-5` в `int` у двійковому доповняльному коді виглядає так:
> `11111111 11111111 11111111 11111011`
>
> 1. **`-5 >> 1` (арифметичний зсув вправо):**
>     * Біти зсуваються на одну позицію вправо.
>     * Позиція, що звільнилася зліва, **заповнюється знаковим бітом**. Оскільки знаковий біт `1`, зліва додається `1`.
>     * Результат: `11111111 ... 11111101`.
>     * Це двійкове представлення числа **-3**. Результат цілочисельного ділення `-5 / 2`.
>
> 2. **`-5 >>> 1` (логічний зсув вправо):**
>     * Біти зсуваються на одну позицію вправо.
>     * Позиція, що звільнилася зліва, **завжди заповнюється нулем**.
>     * Результат: `01111111 ... 11111101`.
>     * Тепер знаковий біт став `0`, тобто число стало **додатним**. Це дуже велике додатне число (2³¹ - 3), що дорівнює **2147483645**.
>
> **Висновок:** `>>` зберігає знак, `>>>` — ні. `>>>` майже ніколи не використовується для роботи зі знаковими числами.

#### **📌 Що перевіряють:**

*Чітке розуміння різниці між двома типами зсуву вправо та їх впливу на від'ємні числа.*

* * *

### **11 ❓ Питання:**

Чи можна використовувати побітові операції з типами `float` та `double`? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, напряму — не можна.** Побітові оператори в Java (`&`, `|`, `^`, `~`, `<<`, `>>`, `>>>`) визначені **тільки для цілочисельних типів** (`byte`, `short`, `int`, `long`, а також `char`).
>
> Спроба застосувати їх до `float` або `double` призведе до **помилки компіляції**.
>
> ```java
> double d = 10.5;
> // int result = d << 1; // ПОМИЛКА: operator << cannot be applied to double, int
> ```
>
> **Чому так?**
>
> * Структура чисел з плаваючою комою (знак, експонента, мантиса) значно складніша, ніж у цілих чисел.
> * Застосування побітових операцій до них не має простого арифметичного сенсу. Наприклад, що означало б "зсунути біти вліво" для числа з плаваючою комою? Це не було б еквівалентно множенню.
>
> **Обхідний шлях (для специфічних завдань):**
>
> Якщо потрібно маніпулювати бітовим представленням `float` чи `double`, можна скористатися методами з класів-обгорток, щоб отримати їхнє цілочисельне бітове представлення:
>
> * `Float.floatToIntBits(float f)`
> * `Double.doubleToLongBits(double d)`
>
> Ці методи повертають `int` та `long` відповідно, біти яких ідентичні бітам `float` та `double` згідно зі стандартом IEEE 754. Вже з цим цілим числом можна працювати за допомогою побітових операцій.

#### **📌 Що перевіряють:**

*Знання про обмеження застосування побітових операцій та інструментів стандартної бібліотеки для низькорівневої роботи з різними типами даних.*

* * *

### **12 ❓ Питання:**

Як можна поміняти місцями значення двох цілочисельних змінних без використання третьої (тимчасової) змінної, використовуючи побітові операції? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична алгоритмічна задача, яку можна вирішити за допомогою оператора **`XOR` (`^`)**.
>
> Властивість `XOR`: `(A ^ B) ^ B = A`. Тобто, якщо двічі застосувати `XOR` з тим самим числом, ми повернемося до початкового значення.
>
> **Алгоритм (на прикладі змінних `a` та `b`):**
>
> ```java
> int a = 10; // Двійкове 1010
> int b = 7;  // Двійкове 0111
>
> a = a ^ b; // a тепер (1010 ^ 0111) = 1101 (13)
> b = a ^ b; // b тепер (1101 ^ 0111) = 1010 (10) -- значення a
> a = a ^ b; // a тепер (1101 ^ 1010) = 0111 (7)  -- значення b
>
> System.out.println("a = " + a); // виведе 7
> System.out.println("b = " + b); // виведе 10
> ```
>
> **Крок за кроком:**
>
> 1. `a = a ^ b;` — у `a` тепер зберігається "змішана" інформація про обидва числа.
> 2. `b = a ^ b;` — (`a` старе `^ b`) `^ b` = `a` старе. У `b` тепер старе значення `a`.
> 3. `a = a ^ b;` — (`a` старе `^ b`) `^` (`a` старе) = `b`. У `a` тепер старе значення `b`.
>
> Цей трюк є цікавою демонстрацією властивостей `XOR`, але в сучасному коді він рідко використовується, оскільки може бути менш читабельним, ніж простий обмін через тимчасову змінну.

#### **📌 Що перевіряють:**

*Кмітливість, знання алгоритмічних трюків та властивостей побітових операцій.*

* * *

### **13 ❓ Питання:**

Якби ви реалізовували систему дозволів (permissions), де кожен користувач може мати комбінацію прав (READ, WRITE, EXECUTE, DELETE), як би ви ефективно зберігали ці дозволи для одного користувача, використовуючи двійкову систему? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це ідеальний сценарій для використання **бітових прапорів (bit flags)**, де кожен дозвіл представлений одним бітом у цілочисельній змінній.
>
> **1. Визначення прапорів:**
> Ми визначаємо кожен дозвіл як степінь двійки, щоб їхні двійкові представлення мали одиницю в унікальній позиції.
>
> ```java
> public static final int READ = 1;     // 0001
> public static final int WRITE = 2;    // 0010
> public static final int EXECUTE = 4;  // 0100
> public static final int DELETE = 8;   // 1000
> ```
>
> **2. Зберігання дозволів користувача:**
> Дозволи користувача зберігаються в одній `int` змінній, яка є результатом операції `OR` над потрібними прапорами.
>
> ```java
> // Користувач має права на читання і запис
> int userPermissions = READ | WRITE; // 0001 | 0010 = 0011
> ```
>
> **3. Робота з дозволами:**
>
> * **Додати право (наприклад, EXECUTE):**
>     `userPermissions |= EXECUTE; // 0011 | 0100 = 0111`
>
> * **Перевірити право (наприклад, WRITE):**
>     `boolean canWrite = (userPermissions & WRITE) != 0; // (0111 & 0010) = 0010 (true)`
>     `boolean canDelete = (userPermissions & DELETE) != 0; // (0111 & 1000) = 0000 (false)`
>
> * **Забрати право (наприклад, WRITE):**
>     `userPermissions &= ~WRITE; // 0111 & (~0010) = 0111 & 1101 = 0101`
>
> Цей підхід є надзвичайно ефективним з точки зору пам'яті (один `int` для зберігання до 32 прапорів) та швидкості (всі операції — це швидкі побітові маніпуляції).

#### **📌 Що перевіряють:**

*Здатність проектувати ефективні рішення, використовуючи низькорівневі концепції. Це класичний приклад використання бітових прапорів.*

* * *

### **14 ❓ Питання:**

Що таке порядок байтів (Endianness), і чи потрібно програмісту на Java про це турбуватися? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Порядок байтів (Endianness)** визначає, в якому порядку байти багатобайтового числа (наприклад, 4-байтового `int`) розташовуються в пам'яті комп'ютера.
>
> * **Little-endian (від молодшого до старшого):** Молодший байт зберігається за меншою адресою. Це домінуючий порядок на більшості сучасних процесорів (x86, ARM).
> * **Big-endian (від старшого до молодшого):** Старший байт зберігається за меншою адресою. Це стандарт для мережевих протоколів (TCP/IP).
>
> **Приклад для числа `0x12345678` (4 байти):**
>
> * **Little-endian:** байти в пам'яті: `78 56 34 12`
> * **Big-endian:** байти в пам'яті: `12 34 56 78`
>
> **Чи потрібно турбуватися про це програмісту на Java?**
>
> **Зазвичай ні.** **Java Virtual Machine (JVM) повністю абстрагує** розробника від апаратного порядку байтів. Всередині JVM використовується **Big-endian** як єдиний стандарт, незалежно від платформи, на якій вона запущена.
>
> Вам **потрібно** про це думати, тільки якщо ви:
>
> * Працюєте з **нативним кодом через JNI**.
> * Взаємодієте з апаратним забезпеченням напряму.
> * Працюєте з "сирими" двійковими даними з файлів або мережі, які були створені на системі з іншим порядком байтів. У таких випадках клас `java.nio.ByteBuffer` дозволяє явно вказати порядок байтів: `buffer.order(ByteOrder.LITTLE_ENDIAN);`.
>
> В решті випадків Java забезпечує повну кросплатформеність на цьому рівні.

#### **📌 Що перевіряють:**

*Глибокі знання про взаємодію Java з апаратним забезпеченням. Розуміння того, які проблеми вирішує JVM "за лаштунками".*

* * *

### **15 ❓ Питання:**

Напишіть код для підрахунку кількості встановлених (рівних 1) бітів у цілому числі без використання `Integer.bitCount()`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Існує кілька алгоритмів для цього. Ось два класичних:
>
> **1. Простий ітеративний метод:**
>
> Ми перевіряємо кожен біт числа, використовуючи зсув та маску.
>
> ```java
> public static int countSetBits(int n) {
>     int count = 0;
>     while (n != 0) {
>         // Додаємо останній біт до лічильника
>         count += (n & 1);
>         // Використовуємо беззнаковий зсув, щоб коректно обробляти від'ємні числа
>         n >>>= 1;
>     }
>     return count;
> }
> ```
>
> Цей алгоритм зробить 32 ітерації для 32-бітного `int`.
>
> **2. Більш ефективний алгоритм (алгоритм Кернігана):**
>
> Цей підхід є швидшим, оскільки кількість ітерацій дорівнює кількості встановлених бітів.
>
> **Ідея:** операція `n & (n - 1)` скидає **крайній правий встановлений біт** (рівний 1) числа `n`. Ми повторюємо цю операцію, доки число не стане нулем.
>
> ```java
> public static int countSetBitsOptimized(int n) {
>     int count = 0;
>     while (n != 0) {
>         // Скидаємо крайній правий '1'
>         n = n & (n - 1);
>         // Інкрементуємо лічильник
>         count++;
>     }
>     return count;
> }
> ```
>
> **Приклад для `n = 13 (1101)`:**
>
> 1. `count=1`, `n = 1101 & 1100 = 1100` (12)
> 2. `count=2`, `n = 1100 & 1011 = 1000` (8)
> 3. `count=3`, `n = 1000 & 0111 = 0000` (0)
> 4. Цикл завершується. Повертаємо `3`.
>
> Цей метод є значно ефективнішим для розріджених чисел (з малою кількістю одиниць).

#### **📌 Що перевіряють:**

*Алгоритмічне мислення, знання побітових "трюків" та здатність писати оптимізований код для низькорівневих завдань.*
