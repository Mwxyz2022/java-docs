---
layout: default
title: "Переповнення типу (Byte overflow)"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 7
---

# Переповнення типу (Byte overflow)

* короткий опис: Переповнення (overflow) виникає, коли результат операції перевищує максимальне або мінімальне значення типу, і значення "обертається" (wrap-around) на протилежний кінець діапазону.

* поняття: Overflow, wrap-around, діапазон.

* приклади: `byte b = 127; b++;` (результат `b` стане `-128`), `byte b = -128; b--;` (результат `b` стане `127`).

* ключові моменти: Java не кидає виняток при переповненні цілочисельних типів, результат є циклічним.

* що важливо знати на співбесіді: Що таке переповнення, як його виявити та уникнути.

### **✅ Офіційне визначення:**

Переповнення — це коли результат арифметичної операції вилітає за межі типу, і значення "обертається" (wrap-around) на протилежний край діапазону.

### **🧠 Простими словами:**

Уяви **`byte`** як вечірку в кімнаті на 256 осіб, але двері лише між –128 і 127\.

Якщо ти додаси 1 до 127 — ох, двері зачинились і ти раптом опинився на –128\!

Той самий ефект, коли з –128 віднімаєш 1 — і бум\! — 127\

---

## **Пояснення типів переповнення (📊)**

| Тип переповнення | Що відбувається |
| :---: | :---: |
| **Позитивне** | Значення \> MAX -> стрибок на MIN |
| **Негативне** | Значення \< MIN -> стрибок на MAX |

## **🔸 Для byte (8 біт) діапазон:**

**`-128 до 127`**

---

## **Пояснення на пальцях (💡)**

* **`byte`** — 8 бітів -> 2⁸ \= 256 можливих значень
* Половина для від’ємних, половина для додатніх
* Верхня межа: 127, нижня: –128
* Потім "цикл по колу":  
  127 \+ 1 \= –128  
  –128 – 1 \= 127

---

## **Корисні приклади (🧪)**

```java
byte x = 127;
x++;
System.out.println(x); // -128
```

```java
byte y = -128;
y--;
System.out.println(y); // 127
```

## **🔹 Розшифровка бінарки:**

127 у двійковому: **`01111111`**  
\+1 -> **`10000000`** — і це вже –128 у two's complement.

Так ніби біт старшого розряду сказав: "Та ні, хлопці, це ж знак\!"

---

## **Порівняння (⚖️)**

| Тип | Діапазон | Приклад переповнення |
| :---: | :---: | :---: |
| **byte** | –128 … 127 | 127 \+ 1 \= –128 |
| **short** | –32\_768 … 32\_767 | 32\_767 \+ 1 \= –32\_768 |
| **int** | –2\_147\_483\_648 … 2\_147\_483\_647 | 2\_147\_483\_647 \+ 1 \= –2\_147\_483\_648 |
| **long** | Дуже великий | Аналогічно, wrap-around |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
byte = 8 біт = 256 значень
min = -128, max = 127
127 + 1 = -128 (wrap-around)
```

---

## **🟩 Підсумок:**

* Переповнення — це як на вечірці, де лічильник не може більше і повертається в початок, не питаючи твоєї згоди.

* Для **`byte`**: 127 \+ 1 \= –128, –128 – 1 \= 127\. Java не робить "вибуху" з помилкою, тож будь пильним\!

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке переповнення типу (type overflow) в Java?

> #### **💬 Відповідь:**
>
> **Переповнення типу (overflow)** — це ситуація, що виникає, коли результат арифметичної операції (наприклад, додавання, множення) виходить за межі максимального або мінімального значення, яке може зберігати певний числовий примітивний тип.
>
> Ключовою особливістю Java є те, що для цілочисельних типів (`byte`, `short`, `int`, `long`) переповнення **не викликає винятку**. Замість цього відбувається **циклічне "обертання" (wrap-around)**.
>
> * При досягненні максимального значення і додаванні одиниці, значення "перескакує" на мінімальне.
> * При досягненні мінімального значення і відніманні одиниці, значення "перескакує" на максимальне.
>
> Це пов'язано з тим, як числа представлені у двійковому доповняльному коді.

#### **📝 Аналогія:**

Це як 12-годинний годинник. Якщо зараз 11 годин і ви додасте 2 години, ви не отримаєте "13 годину", а отримаєте "1 годину". Стрілка просто перейде через верхню межу (12) і почне знову.

#### **📌 Що перевіряють:**

*Базове розуміння концепції переповнення та її "тихої" природи в Java, що є потенційним джерелом помилок.*

* * *

### **2 ❓ Питання:**

Покажіть на прикладі, що станеться, якщо до максимального значення `byte` додати одиницю.

> #### **💬 Відповідь:**
>
> Діапазон значень для `byte` — від **-128** до **127**.
>
> Якщо ми візьмемо максимальне значення (`127`) і додамо до нього одиницю, відбудеться переповнення, і результат стане мінімальним значенням діапазону (`-128`).
>
> **Приклад коду:**
>
> ```java
> byte b = 127; // Максимальне значення для byte
> System.out.println("Початкове значення b: " + b);
>
> // Інкрементуємо значення
> b++; // Еквівалентно b = (byte)(b + 1)
>
> System.out.println("Значення b після інкременту: " + b);
> ```
>
> **Вивід:**
>
> ```
> Початкове значення b: 127
> Значення b після інкременту: -128
> ```
>
> **Пояснення на рівні бітів:**
>
> * Десяткове `127` у 8-бітному двійковому вигляді — це `01111111`.
> * Додаємо `1`: `01111111 + 1 = 10000000`.
> * У доповняльному коді бітовий патерн `10000000` представляє десяткове число **-128**.

#### **📌 Що перевіряють:**

*Практичне розуміння механізму wrap-around для конкретного типу. Кандидат повинен знати діапазони основних типів.*

* * *

### **3 ❓ Питання:**

Чи кидає Java виняток при цілочисельному переповненні? Якщо ні, то чому?

> #### **💬 Відповідь:**
>
> **Ні, за замовчуванням Java не кидає жодного винятку** при переповненні примітивних цілочисельних типів. Вона просто "обертає" значення.
>
> **Чому так зроблено:**
>
> Головна причина — **продуктивність**.
>
> * Арифметичні операції з цілими числами (`+`, `*`) напряму транслюються в дуже швидкі інструкції процесора.
> * Якби JVM повинна була на кожній такій операції перевіряти, чи не відбулося переповнення, це б додало значні накладні витрати і **суттєво сповільнило** всі арифметичні обчислення в Java.
> * Творці Java вирішили, що для низькорівневих, орієнтованих на продуктивність операцій швидкість є важливішою за безпеку переповнення. Вони переклали відповідальність за контроль діапазонів на розробника.
>
> Це відрізняється від мов, як-от Ada або C# (де можна увімкнути перевірку), які надають перевагу безпеці над швидкістю.
>
> Якщо вам потрібна безпечна арифметика з перевіркою переповнення, слід використовувати методи класу `Math` (з Java 8) або клас `BigInteger`.

#### **📌 Що перевіряють:**

*Розуміння дизайнерських рішень, закладених у мову, зокрема, компромісу між продуктивністю та безпекою.*

* * *

### **4 ❓ Питання:**

Як можна виявити або запобігти переповненню `int` при виконанні додавання?

> #### **💬 Відповідь:**
>
> Оскільки Java не робить це за нас, розробник повинен реалізувати перевірку вручну. Існує кілька підходів.
>
> **1. Ручна перевірка перед операцією:**
>
> Можна перевірити, чи не призведе додавання до виходу за межі `Integer.MAX_VALUE`.
>
> ```java
> public boolean canAddWithoutOverflow(int a, int b) {
>     if (a > 0 && b > 0 && a > Integer.MAX_VALUE - b) {
>         // Переповнення в додатному напрямку
>         return false;
>     }
>     if (a < 0 && b < 0 && a < Integer.MIN_VALUE - b) {
>         // Переповнення у від'ємному напрямку
>         return false;
>     }
>     return true;
> }
> ```
>
> **2. Використання `long` для проміжних обчислень:**
>
> Можна привести операнди до `long`, виконати операцію і перевірити, чи результат вміщується назад в `int`.
>
> ```java
> long sum = (long) a + b;
> if (sum > Integer.MAX_VALUE || sum < Integer.MIN_VALUE) {
>     // Відбулося переповнення
> }
> ```
>
> **3. Використання методів з `Math` (з Java 8) — найкращий спосіб:**
>
> В Java 8 були додані спеціальні методи, які виконують арифметичні операції і **кидають `ArithmeticException`** у випадку переповнення.
>
> ```java
> try {
>     int sum = Math.addExact(a, b);
>     System.out.println("Сума: " + sum);
> } catch (ArithmeticException e) {
>     System.err.println("Помилка: відбулося переповнення!");
> }
> ```
>
> Існують також `subtractExact`, `multiplyExact`, `incrementExact` і т.д. Це найбільш надійний та читабельний спосіб.

#### **📌 Що перевіряють:**

*Практичні навички написання надійного коду. Знання стандартної бібліотеки (`Math.addExact`) та вміння реалізувати логіку перевірки вручну.*

* * *

### **5 ❓ Питання:**

Чому цей код дає неправильний результат, і як його виправити?
`long microseconds = 24 * 60 * 60 * 1000 * 1000;`

> #### **💬 Відповідь:**
>
> Цей код дає неправильний результат через **переповнення типу `int`**, яке відбувається **до** присвоєння результату змінній типу `long`.
>
> **Покрокове пояснення:**
>
> 1. Всі числові літерали у виразі (`24`, `60`, `1000`) за замовчуванням є типу `int`.
> 2. Тому вся арифметика в правій частині виразу виконується з використанням **32-бітної `int`-арифметики**.
> 3. Результат множення `24 * 60 * 60 * 1000` — `86,400,000`, що ще вміщується в `int`.
> 4. Наступна операція `86_400_000 * 1000` дає результат `86_400_000_000`, який значно **перевищує** `Integer.MAX_VALUE` (≈ 2.1 млрд).
> 5. Відбувається **переповнення `int`**, і результат "обертається", стаючи некоректним числом (в даному випадку, `1410065408`).
> 6. І лише після цього цей **неправильний `int`-результат** присвоюється `long` змінній. Втрата даних вже відбулася.
>
> **Як виправити:**
>
> Потрібно змусити компілятор виконувати обчислення з використанням `long`-арифметики з самого початку. Для цього достатньо зробити хоча б один з операндів (краще перший) `long`-літералом, додавши суфікс `L`.
>
> ```java
> long microseconds = 24L * 60 * 60 * 1000 * 1000;
> ```
>
> Тепер всі операції будуть виконуватися з типом `long`, і переповнення не відбудеться.

#### **📌 Що перевіряють:**

*Дуже поширене питання-пастка, яке перевіряє глибоке розуміння того, як Java визначає тип виразу, та правил numeric promotion.*

* * *

### **6 ❓ Питання:**

Що таке underflow (неповне заповнення) і як воно пов'язано з overflow? Наведіть приклад.

> #### **💬 Відповідь:**
>
> **Underflow** — це, по суті, дзеркальне до overflow поняття. Воно означає вихід за **нижню (мінімальну)** межу діапазону типу.
>
> У контексті цілих чисел в Java, `underflow` також призводить до циклічного "обертання" (wrap-around), але в протилежному напрямку.
>
> **Приклад з `byte`:**
>
> Діапазон `byte`: від -128 до 127.
>
> ```java
> byte b = -128; // Мінімальне значення для byte
> System.out.println("Початкове значення b: " + b);
>
> // Декрементуємо значення
> b--; // Еквівалентно b = (byte)(b - 1);
>
> System.out.println("Значення b після декременту: " + b);
> ```
>
> **Вивід:**
>
> ```
> Початкове значення b: -128
> Значення b після декременту: 127
> ```
>
> Значення "перескочило" з мінімального на максимальне.
>
> **Примітка:** Термін `underflow` також часто використовується для чисел з плаваючою комою, де він означає ситуацію, коли результат обчислення настільки близький до нуля, що не може бути представлений, і округлюється до нуля. Але для цілих чисел це просто зворотне переповнення.

#### **📌 Що перевіряють:**

*Повноту розуміння механізму wrap-around для всього діапазону типу, а не тільки для верхньої межі.*

* * *

### **7 ❓ Питання:**

Чи однаково відбувається переповнення для цілих чисел (`int`) та чисел з плаваючою комою (`double`)?

> #### **💬 Відповідь:**
>
> **Ні, абсолютно по-різному.**
>
> * **Цілі числа (`int`, `long`):**
>   * При переповненні відбувається **циклічне обертання (wrap-around)**.
>   * `Integer.MAX_VALUE + 1` стає `Integer.MIN_VALUE`.
>   * Немає спеціальних значень для позначення "нескінченності".
>
> * **Числа з плаваючою комою (`double`, `float`):**
>   * Поводяться згідно зі стандартом IEEE 754.
>   * **Циклічного обертання не відбувається.**
>   * При виході за межі діапазону результат стає спеціальним значенням:
>     * **`Infinity`** (нескінченність) — якщо результат занадто великий.
>     * **`-Infinity`** (мінус нескінченність) — якщо результат занадто малий (дуже великий за модулем від'ємний).
>   * Також є значення **`NaN`** (Not a Number) для невизначених результатів (наприклад, `0.0 / 0.0`).
>
> **Приклад:**
>
> ```java
> System.out.println(Integer.MAX_VALUE + 1); // -2147483648
> System.out.println(Double.MAX_VALUE * 2); // Infinity
> ```

#### **📌 Що перевіряють:**

*Знання різниці в поведінці примітивних типів та стандартів, що їх регулюють (Two's Complement vs IEEE 754).*

* * *

### **8 ❓ Питання:**

Як впливає на переповнення розширення типів при операціях? Поясніть на прикладі `byte + byte`. ⭐️

> #### **💬 Відповідь:**
>
> Розширення типів може допомогти **уникнути переповнення** під час проміжних обчислень, але не при фінальному присвоєнні.
>
> Коли ви виконуєте арифметичні операції з типами `byte` або `short`, вони автоматично **розширюються до `int`**.
>
> **Приклад:**
>
> ```java
> byte b1 = 100;
> byte b2 = 100;
>
> // byte result = b1 + b2; // ПОМИЛКА КОМПІЛЯЦІЇ
>
> // Як це відбувається насправді:
> int sum = b1 + b2; // b1 і b2 розширюються до int
> System.out.println(sum); // Виведе 200
> ```
>
> Тут відбувається наступне:
>
> 1. `b1` (100) та `b2` (100) розширюються до `int`.
> 2. Додаються як `int`'и: `100 + 100 = 200`.
> 3. Результат `200` має тип `int`.
>
> Оскільки `200` вміщується в `int`, **переповнення `int` не відбувається**. Проте, цей результат (`200`) вже виходить за межі діапазону `byte` (який до 127). Тому спроба `byte result = b1 + b2;` призведе до помилки компіляції, бо потрібне звужуюче перетворення.
>
> Якби ми зробили `byte result = (byte) (b1 + b2);`, то переповнення `int` б не було, але відбулося б **переповнення `byte`** при приведенні `200` до `byte`.
> `(byte)200` → `-56`.

#### **📌 Що перевіряють:**

*Глибоке розуміння правил Numeric Promotion та їхньої взаємодії з переповненням і приведенням типів.*

* * *

### **9 ❓ Питання:**

Які ризики безпеки можуть бути пов'язані з переповненням цілих чисел? ⭐️

> #### **💬 Відповідь:**
>
> Переповнення цілих чисел є серйозною вразливістю, яка може призвести до різних атак.
>
> **Типові ризики:**
>
> 1. **Переповнення буфера (Buffer Overflow):** Це класичний приклад. Якщо розмір буфера для даних розраховується на основі вхідних даних від користувача, зловмисник може підібрати такі числа, щоб їх сума або добуток переповнилися і стали меншими. Програма виділить менше пам'яті, ніж потрібно, а потім при копіюванні даних відбудеться запис за межами виділеного буфера, що може призвести до виконання довільного коду.
>
>     ```java
>     int size1 = Integer.MAX_VALUE;
>     int size2 = 100;
>     int totalSize = size1 + size2; // totalSize стане від'ємним!
>     byte[] buffer = new byte[totalSize]; // Exception!
>     ```
>
> 2. **Обхід фінансових перевірок:** В фінансових системах, якщо не використовувати `BigDecimal`, переповнення може призвести до того, що великий борг перетвориться на великий кредит.
>
> 3. **Порушення логіки циклів та умов:** Якщо лічильник циклу або умова `if` базується на змінній, яка може переповнитися, поведінка програми стає непередбачуваною. Цикл може стати нескінченним або, навпаки, не виконатися жодного разу.
>     `for (int i = 0; i <= Integer.MAX_VALUE; i++)` — при `i = Integer.MAX_VALUE`, наступний `i++` зробить `i` від'ємним, і умова `i <= Integer.MAX_VALUE` залишиться істинною, що призведе до нескінченного циклу.

#### **📌 Що перевіряють:**

*Розуміння практичних наслідків переповнення для безпеки та надійності програм. Це показує, що кандидат думає про потенційні вразливості.*

* * *

### **10 ❓ Питання:**

Чи можна використовувати `BigInteger` для запобігання переповненню? Як він це робить? ⭐️

> #### **💬 Відповідь:**
>
> **Так, `BigInteger` є ідеальним інструментом** для запобігання переповненню при роботі з цілими числами.
>
> Клас `java.math.BigInteger` представляє цілі числа **довільної точності**. Він не має теоретичного обмеження на максимальне або мінімальне значення (обмежений лише обсягом доступної пам'яті).
>
> **Як він це робить:**
>
> * `BigInteger` внутрішньо зберігає число у вигляді **масиву байтів або `int`'ів**.
> * Коли число стає занадто великим для одного елемента масиву, `BigInteger` автоматично **розширює цей масив**, щоб вмістити більше значущих цифр.
> * Усі арифметичні операції (`add`, `multiply` і т.д.) реалізовані програмно. Вони працюють з цими масивами, імітуючи "шкільну" арифметику, але в двійковій системі.
>
> Оскільки масив може динамічно зростати, **переповнення ніколи не відбувається**.
>
> ```java
> BigInteger bigNum = new BigInteger(String.valueOf(Integer.MAX_VALUE));
> System.out.println(bigNum);
>
> bigNum = bigNum.add(BigInteger.ONE); // Додаємо 1
>
> System.out.println(bigNum); // Коректно виведе 2147483648
> ```>
> Недоліком `BigInteger` є те, що операції з ним значно повільніші, ніж з примітивними типами, оскільки вони є викликами методів, а не простими інструкціями процесора.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки для вирішення проблем обмежень примітивів.*

* * *

### **11 ❓ Питання:**

Як результат `Math.abs(Integer.MIN_VALUE)` пов'язаний з переповненням? Що поверне цей виклик? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей виклик поверне **саме `Integer.MIN_VALUE`**, тобто **-2147483648**. Результат буде від'ємним.
>
> **Пояснення:**
>
> 1. Діапазон `int` є **асиметричним**: від -2,147,483,648 до +2,147,483,647.
> 2. Абсолютне значення `Integer.MIN_VALUE` — це `2,147,483,648`.
> 3. Це число **на одиницю більше**, ніж `Integer.MAX_VALUE`.
> 4. Внутрішньо метод `Math.abs(int a)` реалізований як `(a < 0) ? -a : a;`.
> 5. Коли він намагається обчислити `- (Integer.MIN_VALUE)`, відбувається **переповнення цілого числа**.
> 6. Операція `-` на `Integer.MIN_VALUE` (бітовий патерн `100...000`) дає те саме значення (`100...000`).
> 7. Тому `Math.abs(Integer.MIN_VALUE)` повертає саме `Integer.MIN_VALUE`.
>
> Це документована поведінка. Для отримання правильного абсолютного значення в цьому єдиному випадку, потрібно використовувати `long`:
> `long absValue = Math.abs((long)Integer.MIN_VALUE);`
>
> Та ж сама проблема існує і для `Math.abs(Long.MIN_VALUE)`.

#### **📌 Що перевіряють:**

*Знання граничних випадків та незвичайних аспектів роботи стандартних бібліотечних методів. Це питання на глибоке розуміння та уважність.*

* * *

### **12 ❓ Питання:**

Чому цей код може призвести до нескінченного циклу?
`for (byte b = 0; b <= 127; b++) { ... }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це хитрий код, який, на перший погляд, повинен працювати. Проблема полягає в тому, як він поводиться на останній ітерації та **як оновлюється лічильник**.
>
> 1. Цикл працює коректно, поки `b` не досягне `127`.
> 2. На ітерації, коли `b` дорівнює `127`, умова `b <= 127` все ще істинна, і тіло циклу виконується.
> 3. Після цього виконується крок інкременту: `b++`.
> 4. Тут відбувається **переповнення**: `(byte)127 + 1` стає `-128`.
> 5. Наступна перевірка умови циклу: `b <= 127`.
> 6. Оскільки `b` тепер `-128`, умова `-128 <= 127` **є істинною**!
> 7. Цикл продовжується, `b` знову інкрементується від -128 до 127, знову переповнюється і так далі.
>
> В результаті, умова виходу з циклу **ніколи не буде досягнута**, і він стане нескінченним.
>
> **Як виправити:**
> Використовувати тип, який не переповниться в межах циклу, наприклад, `int`:
> `for (int i = 0; i <= 127; i++) { ... }`

#### **📌 Що перевіряють:**

*Уміння аналізувати логіку циклів у поєднанні зі знанням про переповнення. Це гарний приклад тихої логічної помилки.*

* * *

### **13 ❓ Питання:**

Припустимо, `a` та `b` — два великих додатних `int`. Як можна безпечно обчислити їх середнє значення `(a + b) / 2`, уникнувши переповнення? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Прямий підхід `(a + b) / 2` є небезпечним, оскільки сума `a + b` може призвести до **переповнення**, навіть якщо середнє значення вміщується в `int`.
>
> Існує кілька безпечних способів:
>
> **1. Використання `long` для проміжних обчислень (простий і надійний):**
>
> ```java
> int avg = (int)(((long)a + b) / 2);
> ```
>
> Ми розширюємо `a` до `long`, тому додавання виконується з `long`-арифметикою, що запобігає переповненню.
>
> **2. Арифметичний трюк (без розширення типу):**
>
> Цей спосіб уникає великої суми.
>
> ```java
> int avg = a / 2 + b / 2 + (a % 2 + b % 2) / 2;
> ```
>
> Ми окремо додаємо половинки чисел і окремо — їх остачі, поділені на 2.
>
> **3. Побітові операції (найбільш ефективний та елегантний):**
>
> ```java
> int avg = (a & b) + ((a ^ b) >> 1);
> ```
>
> * `a & b`: знаходить біти, які є спільними для обох чисел. Це біти, які точно будуть у середньому значенні, поділеному на 2.
> * `a ^ b`: знаходить біти, які відрізняються. `(a ^ b) >> 1` — це половина різниці, яку потрібно додати.
>
> Цей метод є найшвидшим, оскільки використовує лише побітові операції.
>
> **Інший варіант:**
> `int avg = a + (b - a) / 2;` (якщо ми знаємо, що `b >= a`). Це також уникає великої суми.

#### **📌 Що перевіряють:**

*Алгоритмічне мислення, кмітливість та знання різних способів вирішення проблеми, включаючи математичні та побітові трюки.*

* * *

### **14 ❓ Питання:**

Чому цей код скомпілюється без помилок? Поясніть роль `final`.
`final byte b1 = 50; final byte b2 = 50; byte sum = b1 + b2;` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код скомпілюється, тому що **компілятор може обчислити результат на етапі компіляції** завдяки тому, що операнди є `final` і ініціалізовані константами.
>
> **Покрокове пояснення:**
>
> 1. Як ми знаємо, `b1 + b2` зазвичай призводить до розширення типів до `int`, і присвоєння `int` до `byte` вимагає явного приведення.
> 2. **Але** оскільки `b1` і `b2` оголошені як `final` і ініціалізовані **константами часу компіляції** (compile-time constants), компілятор виконує оптимізацію.
> 3. Він обчислює `50 + 50 = 100` прямо на етапі компіляції.
> 4. Тепер компілятор бачить вираз як `byte sum = 100;`.
> 5. Він перевіряє, чи вміщується константа `100` у діапазон типу `byte`. Оскільки `100 <= 127`, перевірка проходить успішно.
> 6. Тому явне приведення типів не потрібне, і код компілюється.
>
> **Якби результат перевищував 127**, наприклад:
> `final byte b1 = 100; final byte b2 = 100; byte sum = b1 + b2;`
>
> Тоді компілятор обчислив би `200` і видав би помилку `possible lossy conversion from int to byte`, оскільки константа `200` не вміщується в `byte`.

#### **📌 Що перевіряють:**

*Експертне знання про те, як працює компілятор Java, включаючи оптимізації, пов'язані з `final` змінними (constant folding).*

* * *

### **15 ❓ Питання:**

Що буде результатом виклику `Byte.toUnsignedInt((byte)-1)` і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Результатом буде **`255`**.
>
> Метод `Byte.toUnsignedInt(byte b)` (доданий у Java 8) призначений для того, щоб отримати **беззнакове цілочисельне представлення** `byte`.
>
> **Пояснення:**
>
> 1. Значення `-1` типу `byte` у 8-бітному доповняльному коді представляється як **`11111111`**.
>
> 2. Метод `toUnsignedInt` бере ці 8 біт і інтерпретує їх як **беззнакове число**, а потім розширює до `int` (32 біти), заповнюючи старші біти нулями.
>
> 3. Двійкове число `11111111` як беззнакове дорівнює:
>     `1*2⁷ + 1*2⁶ + 1*2⁵ + 1*2⁴ + 1*2³ + 1*2² + 1*2¹ + 1*2⁰`
>     `= 128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 = 255`
>
> Цей метод дуже корисний, коли ви працюєте з даними з інших систем, де `byte` може бути беззнаковим (наприклад, у деяких мережевих протоколах або форматах файлів), і вам потрібно отримати його правильне числове значення в діапазоні від 0 до 255, а не від -128 до 127.

#### **📌 Що перевіряють:**

*Знання сучасного API Java (з Java 8) для роботи з беззнаковою арифметикою та глибоке розуміння того, як один і той самий бітовий патерн може інтерпретуватися по-різному (як знакове та беззнакове число).*
