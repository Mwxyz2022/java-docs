---
layout: default
title: "Логічні операції: && , ||, !"
parent: "Бітові операції"
grand_parent: "Java Core"
nav_order: 12
---

# Логічні операції: && , ||

* короткий опис: Логічні оператори, що працюють з булевими значеннями: `&&` (логічне AND), `||` (логічне OR), `!` (логічне NOT). Підтримують коротке замикання.

* поняття: `&&`, `||`, `!`, коротке замикання, пріоритет операторів, дужки.

* приклади: `true && false = false`, `true || false = true`, `!true = false`. `if (a != null && a.isEmpty())` (безпечна перевірка завдяки короткому замиканню).

* ключові моменти: `&&` та `||` можуть не обчислювати другу умову, якщо результат вже відомий. Дужки `()` змінюють стандартний пріоритет операторів.

* що важливо знати на співбесіді: Різниця між `&` та `&&`, пріоритет операторів, як уникнути `NullPointerException`.

### **✅ Офіційне визначення:**

Логічні оператори в Java (**`&&`**, **`||`**, **`!`**) працюють із булевими значеннями **`true`** або **`false`**, комбінуючи чи змінюючи умови в логічних виразах.

### **🧠 Простими словами:**

Це як в реальному житті:

* **`&&`** — і те, і те має бути правдою (AND)
* **`||`** — достатньо, щоб хоча б одна умова була правдою (OR)
* **`!`** — "не", заперечення

---

## **Як працюють (📊)**

| Оператор | Назва | Опис | Приклад |
| :---: | :---: | :---: | :---: |
| **`&&`** | Логічне AND | true тільки якщо **обидва** true | **`true && true = true`** |
| **`//`** | Логічне OR | **`true`**, якщо хоча б одне **`true`** | **`true // false = true`** |
| **`!`** | NOT (заперечення) | міняє **`true`** **`↔`** **`false`** | **`!true = false`** |

* `//` --> `||`

---

## **Пояснення на пальцях (💡)**

* **`&&`** \= Все має бути **true**
* **`||`** \= Достатньо одного **true**
* **`!`** \= Все навпаки, **заперечення**

**Пріоритет:**

1. **`!`** (NOT) — найвищий
2. **`&&`** (AND)
3. **`||`** (OR)

Щоб змінити порядок — використовуй дужки **`()`**:

```java
boolean result = true || false && false;   // true (&& виконується раніше)
boolean result2 = (true || false) && false; // false (дужки змінюють порядок)
```

## **Коротке замикання (Short-Circuiting) 🧠**

✅ Java не перевіряє другу умову, якщо результат можна визначити з першої.

* **`false && щось`** -> завжди **`false`**, друга умова не виконується

* **`true || щось`** -> завжди **`true`**, друга умова не виконується

```java
boolean a = false;
boolean b = true;

if (a && someHeavyMethod()) { ... } // someHeavyMethod() не викликається
if (b || someHeavyMethod()) { ... } // someHeavyMethod() не викликається
```

📌 Це економить час і запобігає помилкам (наприклад, **`NullPointerException`**).

---

## **Корисні приклади (🧪)**

```java
boolean a = true;
boolean b = false;

System.out.println(a && b);      // false
System.out.println(a || b);      // true
System.out.println(!a);          // false
System.out.println(!(a || b));   // false

int x = 5;
int y = 10;

if (x < 10 && y > 5) {
    System.out.println("Умова виконалась");  // Виведе це
}
```

---

## **🧠 Мнемоніка для запам’ятовування:**

Логічні оператори **`&&, ||, !`** — це основа побудови умов у Java.

* **`&&`** — всі умови мають бути правдою
* **`||`** — достатньо однієї правди
* **`!`** — заперечення, зміна стану

Short-circuiting допомагає писати ефективні та безпечні умови.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке логічні оператори `&&` (AND), `||` (OR) та `!` (NOT) в Java?

> #### **💬 Відповідь:**
>
> Це основні оператори, що використовуються для роботи з булевими значеннями (`boolean`) та для побудови складних умов.
>
> * **`&&` (Логічне І / AND):**
>   * Повертає `true`, тільки якщо **обидва** операнди є `true`.
>   * Якщо хоча б один операнд `false`, результат буде `false`.
>
> * **`||` (Логічне АБО / OR):**
>   * Повертає `true`, якщо **хоча б один** з операндів є `true`.
>   * Результат буде `false`, тільки якщо обидва операнди є `false`.
>
> * **`!` (Логічне НЕ / NOT):**
>   * Це унарний оператор, який **інвертує** булеве значення.
>   * `!true` повертає `false`.
>   * `!false` повертає `true`.
>
> Ці оператори є фундаментом для керуючих конструкцій, таких як `if`, `while`, та `for`.

#### **📝 Таблиця істинності:**

| A | B | A && B | A \|\| B | !A |
| :---: | :---: | :---: | :---: |:---:|
| `true` | `true` | `true` | `true` |`false`|
| `true` |`false` | `false`| `true` |`false`|
| `false`| `true` | `false`| `true` |`true` |
| `false`|`false` | `false`| `false`|`true` |

#### **📌 Що перевіряють:**

*Базові знання логічних операцій, які є основою програмування.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між `&` та `&&`, а також між `|` та `||`?

> #### **💬 Відповідь:**
>
> Ключова різниця полягає в механізмі **короткого замикання (short-circuiting)**.
>
> * **`&&` та `||` (Логічні, Short-circuit):**
>   * Вони "ліниві". Другий операнд **не буде обчислюватися**, якщо результат вже відомий після обчислення першого.
>     * Для `expr1 && expr2`: якщо `expr1` є `false`, результат усього виразу одразу `false`, і `expr2` **не виконується**.
>     * Для `expr1 || expr2`: якщо `expr1` є `true`, результат одразу `true`, і `expr2` **не виконується**.
>
> * **`&` та `|` (Побітові, Non-short-circuit):**
>   * Коли вони застосовуються до `boolean` значень, вони працюють як логічні, але "завзяті". Вони **завжди обчислюють обидва операнди**, незалежно від значення першого.
>   * Вони є побітовими, якщо застосовуються до цілочисельних типів.
>
> **Висновок:** Для логічних умов **майже завжди** слід використовувати `&&` та `||`. Це не тільки ефективніше (бо уникає зайвих обчислень), але й безпечніше.

#### **📝 Приклад:**

```java
String s = null;

// БЕЗПЕЧНО: друга частина не виконається, NPE не буде
if (s != null && s.length() > 0) { ... }

// НЕБЕЗПЕЧНО: друга частина виконається, і буде NPE на s.length()
// if (s != null & s.length() > 0) { ... }
```

#### **📌 Що перевіряють:**

*Розуміння концепції short-circuiting. Це одне з найважливіших і найчастіших питань на цю тему.*

* * *

### **3 ❓ Питання:**

Що таке коротке замикання (short-circuiting) і чому це важливо?

> #### **💬 Відповідь:**
>
> **Коротке замикання** — це поведінка логічних операторів `&&` та `||`, за якої обчислення виразу припиняється, як тільки його фінальний результат стає відомим.
>
> **Чому це важливо:**
>
> 1. **Підвищення продуктивності:**
>     * Це дозволяє уникнути виконання "дорогих" операцій (наприклад, виклику методів, що звертаються до бази даних або мережі), якщо в цьому немає потреби.
>
>     ```java
>     if (isCached() || loadFromDatabase()) { /* ... */ }
>     ```
>
>     Якщо `isCached()` поверне `true`, "важкий" метод `loadFromDatabase()` ніколи не буде викликаний.
>
> 2. **Запобігання помилок часу виконання (runtime errors):**
>     * Найпоширеніший випадок — **захист від `NullPointerException`**. Ми можемо безпечно викликати метод на об'єкті, тільки переконавшись, що сам об'єкт не `null`.
>
>     ```java
>     if (user != null && user.isActive()) {
>         // user.isActive() буде викликано, тільки якщо user не null
>     }
>     ```
>     Без короткого замикання такий код був би небезпечним.
>
> Таким чином, short-circuiting — це не просто мікро-оптимізація, а фундаментальний інструмент для написання ефективного та надійного коду.

#### **📌 Що перевіряють:**

*Здатність пояснити концепцію та, що важливіше, її практичні переваги з точки зору продуктивності та безпеки.*

* * *

### **4 ❓ Питання:**

Що виведе цей код і чому?

```java
public class LogicTest {
    public static boolean getTrue() {
        System.out.println("getTrue() called");
        return true;
    }
    public static boolean getFalse() {
        System.out.println("getFalse() called");
        return false;
    }

    public static void main(String[] args) {
        if (getFalse() && getTrue()) {
            // ...
        }
    }
}
```

> #### **💬 Відповідь:**
>
> **Вивід у консоль буде таким:**
>
> ```
> getFalse() called
> ```
>
> **Пояснення:**
>
> 1. Використовується оператор **`&&` (логічне І з коротким замиканням)**.
> 2. JVM починає обчислювати вираз зліва направо і викликає перший метод `getFalse()`.
> 3. Він виводить у консоль "getFalse() called" і повертає `false`.
> 4. Оператор `&&` бачить, що перший операнд є `false`. Для `&&`, якщо хоча б один операнд `false`, весь вираз гарантовано буде `false`, незалежно від значення другого операнда.
> 5. Завдяки **короткому замиканню**, JVM **не обчислює** другий операнд і **не викликає** метод `getTrue()`.
> 6. Тому "getTrue() called" ніколи не буде надруковано.
>
> Якби замість `&&` використовувався `&`, то були б викликані обидва методи.

#### **📌 Що перевіряють:**

*Практичне розуміння short-circuiting. Кандидат повинен прослідкувати потік виконання та передбачити, які методи будуть викликані.*

* * *

### **5 ❓ Питання:**

Який пріоритет у логічних операторів `!`, `&&`, `||`? Що виведе цей код?
`System.out.println(true || true && false);`

> #### **💬 Відповідь:**
>
> Пріоритет операторів (від найвищого до найнижчого) такий:
>
> 1. **`!`** (Логічне НЕ) — має найвищий пріоритет.
> 2. **`&&`** (Логічне І)
> 3. **`||`** (Логічне АБО) — має найнижчий пріоритет.
>
> **Розбір виразу `true || true && false`:**
>
> 1. Спочатку, через вищий пріоритет, виконується операція `&&`:
>     `true && false` → `false`.
>
> 2. Тепер вираз стає:
>     `true || false`
>
> 3. Операція `||` повертає `true`, якщо хоча б один операнд `true`.
>     `true || false` → `true`.
>
> Отже, код виведе **`true`**.
>
> **Для зміни порядку виконання слід використовувати дужки:**
> `(true || true) && false`
>
> 1. `(true || true)` → `true`
> 2. `true && false` → `false`
> Результатом буде `false`.

#### **📌 Що перевіряють:**

*Знання пріоритету операторів, що є важливим для правильного читання та написання складних логічних виразів.*

* * *

### **6 ❓ Питання:**

Чи можна сказати, що `if (a == true)` є еквівалентом `if (a)`? Який запис кращий?

> #### **💬 Відповідь:**
>
> **Так, функціонально вони є еквівалентними**, за умови, що `a` є змінною типу `boolean`. Обидва вирази перевіряють, чи є значення `a` істинним.
>
> **Однак, запис `if (a)` є значно кращим і є стандартом у Java-розробці.**
>
> **Чому `if (a == true)` є поганою практикою:**
>
> * **Надлишковість (Redundancy):** Сам `if` вже перевіряє "істинність" виразу в дужках. Порівнювати булеве значення з `true` — це все одно, що питати: "Чи правда, що правда є правдою?". Це тавтологія.
> * **Погана читабельність:** Для досвідченого розробника такий код виглядає "шумним" і може свідчити про недосвідченість автора.
>
> **Особливо погано виглядає такий запис:**
> `if (a == false)`
> Замість цього завжди слід використовувати оператор `NOT`:
> `if (!a)`
>
> **Висновок:** Завжди використовуйте `if (a)` та `if (!a)` для перевірки `boolean` змінних. Це більш чисто, коротко та ідіоматично.

#### **📌 Що перевіряють:**

*Стиль кодування та знання "хороших манер" у програмуванні. Здатність писати код, який є не тільки робочим, але й чистим та професійним.*

* * *

### **7 ❓ Питання:**

Як реалізувати логіку "Виключне АБО" (XOR), використовуючи лише оператори `&&`, `||`, `!`?

> #### **💬 Відповідь:**
>
> **"Виключне АБО" (XOR)** повертає `true` тоді і тільки тоді, коли операнди мають **різні значення** (один `true`, інший `false`).
>
> Існує кілька способів реалізувати це:
>
> **1. Через пряме порівняння:**
> Це найпростіший і найчитабельніший спосіб.
>
> ```java
> boolean result = (a != b);
> ```
>
> Якщо `a` і `b` різні, результат буде `true`, що точно відповідає логіці XOR.
>
> **2. Через комбінацію AND, OR, NOT:**
> Це більш формальний спосіб, що відображає логічну формулу.
> Логіка така: (A і не B) АБО (не A і B).
>
> ```java
> boolean result = (a && !b) || (!a && b);
> ```
>
> **Приклад для `a=true`, `b=false`:**
> *   `a && !b` → `true && true` → `true`
> *   `!a && b` → `false && false` → `false`
> *   `true || false` → `true`
>
> Хоча другий варіант працює, перший (`a != b`) є значно простішим, ефективнішим та кращим з точки зору читабельності для реалізації XOR. Також у Java є побітовий оператор `^`, який можна застосувати до `boolean`, і `a ^ b` також реалізує логіку XOR.

#### **📌 Що перевіряють:**

*Логічне мислення, здатність конструювати складні логічні вирази та знання альтернативних способів реалізації.*

* * *

### **8 ❓ Питання:**

Що виведе цей код і чому?
`boolean result = false || true && !false;` ⭐️

> #### **💬 Відповідь:**
>
> Код виведе **`true`**.
>
> Розберемо обчислення згідно з пріоритетом операторів (`!`, потім `&&`, потім `||`).
>
> 1. **Спочатку `!` (NOT):**
>     * `!false` обчислюється як `true`.
>     * Вираз стає: `false || true && true`.
>
> 2. **Потім `&&` (AND):**
>     * `true && true` обчислюється як `true`.
>     * Вираз стає: `false || true`.
>
> 3. **В кінці `||` (OR):**
>     * `false || true` обчислюється як `true`.
>
> Фінальний результат, який буде присвоєно змінній `result`, — це `true`.

#### **📌 Що перевіряють:**

*Здатність коректно застосовувати правила пріоритету операторів до складного виразу.*

* * *

### **9 ❓ Питання:**

Який результат повертає метод `Boolean.logicalAnd(boolean a, boolean b)` і чим він відрізняється від `&&`? ⭐️

> #### **💬 Відповідь:**
>
> Метод `Boolean.logicalAnd(a, b)` повертає результат логічного **І** так само, як і `a && b`. Він поверне `true`, тільки якщо і `a`, і `b` є `true`.
>
> **Однак, є ключова відмінність:**
>
> `Boolean.logicalAnd()` (як і `logicalOr`, `logicalXor`) — це **звичайний метод**. Він **не реалізує скороченого обчислення (short-circuiting)**.
>
> * При виклику методу, згідно з правилами Java, **всі його аргументи повинні бути обчислені** перед тим, як буде виконано тіло методу.
> * Отже, виклик `Boolean.logicalAnd(expr1, expr2)` завжди призведе до обчислення **і `expr1`, і `expr2`**, навіть якщо `expr1` вже `false`.
>
> Це робить його схожим на побітовий оператор `&`, застосований до `boolean` значень.
>
> **Коли його використовувати:**
>
> Практично ніколи. Він був доданий для відповідності операторам `&`, `|`, `^`, і може бути корисним у дуже рідкісних випадках, коли вам потрібно передати саму операцію як функціональний інтерфейс (`BiFunction<Boolean, Boolean, Boolean> logicalAnd = Boolean::logicalAnd;`).
>
> В звичайному коді **завжди** слід віддавати перевагу операторам `&&` та `||`.

#### **📌 Що перевіряють:**

*Знання стандартної бібліотеки, зокрема менш відомих методів класу `Boolean`, та розуміння різниці між оператором мови і викликом методу.*

* * *

### **10 ❓ Питання:**

Чи можна застосовувати `&&` та `||` до цілочисельних типів? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, не можна.**
>
> Логічні оператори `&&` та `||` визначені **виключно для операндів типу `boolean`**.
>
> Спроба застосувати їх до цілочисельних типів (наприклад, `int a = 5; int b = 10; if (a && b)`) призведе до **помилки компіляції**. Компілятор повідомить, що `operator && cannot be applied to 'int', 'int'`.
>
> **Чому так?**
>
> Це відмінність Java від мов, як-от C/C++, де цілочисельні значення можуть бути неявно інтерпретовані як булеві (`0` як `false`, а будь-яке інше число як `true`).
>
> У Java існує **строга типізація**. Немає неявного перетворення з `int` у `boolean`. Якщо ви хочете перевірити число, ви повинні використовувати явне порівняння, яке поверне `boolean`:
>
> ```java
> if (a != 0 && b != 0) {
>     // ...
> }
> ```
> Для побітових маніпуляцій з цілими числами слід використовувати побітові оператори `&`, `|`.

#### **📌 Що перевіряють:**

*Розуміння строгої типізації в Java та відмінностей від інших мов програмування.*

* * *

### **11 ❓ Питання:**

Перепишіть цей код, використовуючи один `if` і логічні оператори:

```java
if (user.isLoggedIn()) {
    if (user.hasAdminRights()) {
        System.out.println("Welcome, Admin!");
    }
}
```

⭐️

> #### **💬 Відповідь:**
>
> Цей код є прикладом вкладених умов. Його можна легко переписати, об'єднавши дві перевірки за допомогою логічного оператора **`&&` (AND)**.
>
> **Переписаний код:**
>
> ```java
> if (user.isLoggedIn() && user.hasAdminRights()) {
>     System.out.println("Welcome, Admin!");
> }
> ```
>
> **Чому це працює і є безпечним:**
>
> 1.  Логіка така: тіло `if` має виконатися, тільки якщо і перша, і друга умови є істинними. Це точно відповідає семантиці оператора `&&`.
>
> 2.  **Важливо:** завдяки **короткому замиканню**, якщо користувач не залогінений (`user.isLoggedIn()` повертає `false`), друга перевірка `user.hasAdminRights()` навіть **не буде викликана**, що робить цей код ефективним. Порядок перевірок важливий — спочатку слід ставити більш "легку" або більш загальну перевірку.

#### **📌 Що перевіряють:**

*Здатність до рефакторингу коду та спрощення умовних конструкцій.*

* * *

### **12 ❓ Питання:**

Як закон де Моргана може допомогти спростити цей вираз? `! (a > 10 || b < 5)` ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Закони де Моргана** — це правила в булевій алгебрі, які дозволяють трансформувати логічні вирази.
>
> Один із законів стверджує:
> **`! (A || B)` еквівалентно `!A && !B`**
>
> (Заперечення диз'юнкції є кон'юнкцією заперечень).
>
> Давайте застосуємо це до нашого виразу `! (a > 10 || b < 5)`.
>
> 1. `A` — це `a > 10`.
> 2. `B` — це `b < 5`.
>
> Застосовуємо правило:
>
> * `!A` → `! (a > 10)` → `a <= 10`
> * `!B` → `! (b < 5)` → `b >= 5`
>
> Отже, спрощений вираз буде:
> **`(a <= 10) && (b >= 5)`**
>
> Ця трансформація може зробити код більш читабельним, оскільки позбавляє від зовнішнього заперечення і робить умови "позитивними" та зрозумілішими. Це корисний інструмент для рефакторингу складних умов.

#### **📌 Що перевіряють:**

*Знання базових законів логіки та здатність застосовувати їх для спрощення та рефакторингу коду. Це показує більш глибокий рівень мислення.*

* * *

### **13 ❓ Питання:**

Чи можна сказати, що `A || B` завжди еквівалентно `!(!A && !B)`? Чому це може бути важливо? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, з точки зору логічного результату вони абсолютно еквівалентні.**
>
> Це також є одним із **законів де Моргана**:
> `A || B` еквівалентно `!(!A && !B)`.
>
> Давайте доведемо:
> `!(!A && !B)` → (застосовуємо закон де Моргана до внутрішньої частини) → `!(!A) || !(!B)` → `A || B`.
>
> **Чому це може бути важливо:**
>
> Хоча логічно вони однакові, їхнє застосування може мати сенс у специфічних контекстах:
>
> 1. **Формальні доведення:** У теоретичній інформатиці або при доведенні коректності алгоритмів ці перетворення використовуються для спрощення виразів.
>
> 2. **Схемотехніка (NAND/NOR логіка):** Всі логічні операції можна звести до використання лише одного типу вентилів, наприклад, NAND (`!(A && B)`) або NOR (`!(A || B)`). Розуміння цих еквівалентностей дозволяє будувати будь-які логічні схеми. `A || B` можна представити, використовуючи лише вентилі NAND.
>
> 3. **Оптимізація коду (рідко):** У дуже рідкісних випадках компілятор може генерувати більш ефективний код для однієї форми, ніж для іншої, хоча сучасні компілятори зазвичай самі зводять їх до оптимальної форми.
>
> Для більшості розробників на Java, розуміння цієї еквівалентності важливе більше для загального розвитку та вміння читати складні логічні вирази.

#### **📌 Що перевіряють:**

*Здатність до логічних міркувань та знання фундаментальних законів булевої алгебри.*

* * *

### **14 ❓ Питання:**

Яка різниця в згенерованому байткоді між `if(a && b)` та `if(a & b)` (де `a` та `b` є `boolean`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Різниця в байткоді буде суттєвою і буде відображати їхню різну семантику (short-circuiting vs eager evaluation).
>
> **1. Байткод для `if (a && b)`:**
>
> Згенерований байткод буде використовувати **умовні переходи** для реалізації короткого замикання. Спрощено це виглядає так:
>
> ```
> Завантажити 'a'
> IFEQ L1     // Якщо 'a' дорівнює 0 (false), перейти на мітку L1 (кінець if)
> Завантажити 'b'
> IFEQ L1     // Якщо 'b' дорівнює 0 (false), перейти на мітку L1
> // Тіло if
> ...
> L1:
> // Код після if
> ```
>
> Друга частина обчислюється, тільки якщо перша не призвела до переходу.
>
> **2. Байткод для `if (a & b)`:**
>
> Згенерований байткод **не буде містити проміжних умовних переходів**. Він буде послідовно обчислювати обидва операнди.
>
> ```
>
> Завантажити 'a'
> Завантажити 'b'
> IAND        // Виконати побітове AND (яке для boolean працює як логічне)
> IFEQ L1     // Перевірити кінцевий результат. Якщо він 0 (false), перейти на L1
> // Тіло if
> ...
> L1:
> // Код після if
>
> ```
>
> Тут чітко видно, що `a` і `b` завантажуються на стек послідовно, і лише потім виконується операція `AND`. Це підтверджує, що обидва операнди завжди обчислюються.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, як високорівневі конструкції мови транслюються у низькорівневий байткод. Це питання для Senior-розробників.*

* * *

### **15 ❓ Питання:**

Напишіть складний логічний вираз, використовуючи `&&` та `||`, який демонструє важливість використання дужок для правильного порядку виконання. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Уявімо собі систему перевірки доступу до звіту. Користувач може бачити звіт, якщо:
>
> * Він є **адміністратором**.
> * АБО він є **менеджером** у **своєму відділі**.
>
> При цьому, незалежно від ролі, доступ дозволено лише **в робочий час**.
>
> Давайте представимо це як булеві змінні:
>
> ```java
> boolean isAdmin = false;
> boolean isManager = true;
> boolean isOwnDepartment = true;
> boolean isWorkingHours = false;
> ```
>
> **Неправильний вираз (без дужок):**
>
> ```java
> boolean hasAccess = isWorkingHours && isAdmin || isManager && isOwnDepartment;
> ```
>
> Через те, що `&&` має вищий пріоритет, цей вираз буде обчислено як:
> `(isWorkingHours && isAdmin) || (isManager && isOwnDepartment)`
> `(false && false) || (true && true)`
> `false || true` → **`true`**
>
> Результат **`true`**! Користувач отримав доступ **не в робочий час**, тому що він менеджер свого відділу. Це **помилка в логіці безпеки**.
>
> **Правильний вираз (з дужками):**
>
> Перевірка робочого часу має стосуватися всього виразу.
>
> ```java
> boolean hasAccessCorrect = isWorkingHours && (isAdmin || (isManager && isOwnDepartment));
> ```
>
> Обчислюємо:
> `isWorkingHours && (false || (true && true))`
> `isWorkingHours && (false || true)`
> `isWorkingHours && true`
> `false && true` → **`false`**
>
> Результат **`false`**. Тепер доступ коректно заборонено, оскільки перевірка `isWorkingHours` застосовується до всього результату перевірки ролей. Цей приклад чітко показує, як відсутність однієї пари дужок може призвести до серйозної логічної помилки.

#### **📌 Що перевіряють:**

*Здатність конструювати складні логічні вирази та розуміння критичної важливості дужок для забезпечення правильної логіки виконання. Це перевірка практичного досвіду та уважності.*
