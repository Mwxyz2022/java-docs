---
layout: default
title: "Обробка помилок: IOException, FileNotFoundException"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 14
---

# Обробка помилок: IOException, FileNotFoundException

* короткий опис: `IOException` – це `Checked Exception` (перевіряємий виняток) у Java, який сигналізує про проблеми з введенням/виведенням при роботі з файлами. `FileNotFoundException` – це його підтип, що виникає, коли файл не існує або недоступний.

* поняття: `IOException`, `FileNotFoundException`, `checked exception`, `try-catch`, `throws`.

* приклади: Обробка `FileNotFoundException` при спробі відкрити неіснуючий файл за допомогою `try-catch`.

* ключові моменти: `IOException` – це загальний тип для I/O проблем. Ці винятки є `checked`, тому компілятор вимагає їх обробляти або оголошувати.

* що важливо знати на співбесіді: Роль `IOException`, як обробляти I/O-винятки.

### **✅ Офіційне визначення:**  

**`IOException`** — це **перевіряємий виняток** (**`checked`** `exception`) із пакета **`java`**`.io`, який сигналізує про **проблеми з введенням/виведенням** під час читання/запису файлів.

### **🧠 Простими словами:**  

Це тип помилки, який виникає, якщо Java **не може працювати з файлом** — наприклад, файл не знайдено, немає прав доступу, або сталася помилка при читанні/записі.

---

## **Основні види (📊)**

| Виняток | Коли виникає |
| :---: | :---: |
| **`FileNotFoundException`** | Файл не існує, або шлях некоректний |
| **`IOException`** | Будь-яка інша помилка вводу/виводу |

---

## **Пояснення на пальцях (💡)**

* Якщо ти намагаєшся відкрити файл, а його **немає** — буде **`FileNotFoundException`**.

* Якщо ти читаєш файл, і щось пішло не так (наприклад, диск зник, файл пошкоджено) — буде **`IOException`**.

* Усі ці помилки потрібно **обробити** або через **`try`**`-catch`, або оголосити у **`throws`**.

---

## **Приклади коду (🧪)**

#### **✅ Обробка через `try-catch`:**

```java
try {
    FileReader reader = new FileReader("data.txt");
    BufferedReader br = new BufferedReader(reader);
    String line = br.readLine();
    System.out.println(line);
    br.close();
} catch (FileNotFoundException e) {
    System.out.println("Файл не знайдено: " + e.getMessage());
} catch (IOException e) {
    System.out.println("Помилка читання файлу: " + e.getMessage());
}
```

#### **✅ Оголошення `throws`:**

```java
public void readFile() throws IOException {
    BufferedReader br = new BufferedReader(new FileReader("data.txt"));
    String line = br.readLine();
    System.out.println(line);
    br.close();
}
```

---

## **Порівняння способів (⚖️)**

| Спосіб | Плюси | Мінуси |
| ----- | ----- | ----- |
| **`try-catch`** | Можна обробити помилку одразу | Код стає довший |
| **`throws`** | Простий код, передає помилку далі | Не обробляє, покладається на викликаючий код |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
FileNotFound -> немає файлу
IOError     -> помилка зчитування
try-catch   -> ловимо тут
throws      -> передаємо вище
```

---

## **🟩 Підсумок:**

При роботі з файлами **всі дії можуть кинути IOException** — зчитування, запис, відкриття.  
Цей виняток потрібно **обов'язково обробити** або через **`try`**`-catch`, або оголосити через **`throws`**.  
Класична помилка — це **`FileNotFoundException`**, коли немає файлу або вказано неправильний шлях.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `IOException` і чому він є `checked` винятком?

> #### **💬 Відповідь:**
>
> **`java.io.IOException`** — це базовий **перевірюваний (`checked`)** виняток для всіх помилок, пов'язаних з операціями **вводу/виводу (I/O)**. Він сигналізує про те, що сталася якась проблема при взаємодії із зовнішнім ресурсом (файлом, мережевим сокетом тощо).
>
> **Чому він є `checked`:**
>
> Він є перевірюваним, тому що помилки вводу/виводу є **очікуваними, зовнішніми ризиками**, які не залежать від логіки програми. Розробник не може гарантувати, що файл завжди буде існувати або що мережеве з'єднання не розірветься.
>
> Зробивши `IOException` перевірюваним, Java **змушує** розробника **обробляти ці потенційні проблеми**, написавши логіку відновлення або коректного завершення. Це робить програми, що працюють з I/O, більш **надійними**.
>
> Код, що викликає метод, який може кинути `IOException`, зобов'язаний або обробити його в `try-catch`, або оголосити `throws IOException` у своїй сигнатурі.

#### **📝 Аналогія:**

Це як відправка посилки поштою. Ви не можете бути на 100% впевнені, що вона дійде. Вона може загубитися, її може пошкодити (`IOException`). Тому ви отримуєте номер для відстеження і маєте план дій на випадок проблеми (`try-catch`).

#### **📌 Що перевіряють:**

*Розуміння філософії `checked` винятків та того, чому саме I/O операції вимагають такої уваги.*

* * *

### **2 ❓ Питання:**

Яка ієрархія між `IOException` та `FileNotFoundException`? У якому порядку їх слід обробляти в `catch`-блоках?

> #### **💬 Відповідь:**
>
> **`FileNotFoundException` є підкласом (нащадком) `IOException`.**
> `java.lang.Object` → `java.lang.Throwable` → `java.lang.Exception` → `java.io.IOException` → `java.io.FileNotFoundException`
>
> **Порядок обробки:**
> Згідно з правилами обробки винятків, `catch`-блоки повинні йти **від більш специфічного (дочірнього) до більш загального (батьківського)**.
>
> **Тому `FileNotFoundException` завжди повинен перехоплюватися перед `IOException`**.
>
> **Правильний порядок:**
>
> ```java
> try {
>     new FileReader("file.txt");
> } catch (FileNotFoundException e) {
>     // Специфічна обробка: Файл не знайдено
>     System.err.println("Помилка: файл не існує.");
> } catch (IOException e) {
>     // Обробка всіх інших помилок вводу/виводу
>     // (наприклад, немає прав доступу)
>     System.err.println("Помилка вводу/виводу: " + e.getMessage());
> }
> ```
>
> Якщо поміняти їх місцями, виникне **помилка компіляції**, оскільки блок `catch (FileNotFoundException e)` стане недосяжним кодом.

#### **📌 Що перевіряють:**

*Знання ієрархії стандартних винятків та правил порядку `catch`-блоків.*

* * *

### **3 ❓ Питання:**

Які дії можуть призвести до виникнення `IOException` або його підтипів?

> #### **💬 Відповідь:**
>
> Практично будь-яка операція, що пов'язана з читанням або записом даних із зовнішніх джерел, може кинути `IOException`.
>
> **Типові дії:**
>
> * **Робота з файлами:**
>   * `new FileReader("non_existent_file.txt")` → `FileNotFoundException`.
>   * `reader.read()` — помилка під час читання (наприклад, пошкоджено диск).
>   * `writer.write()` — закінчилося місце на диску, або немає прав на запис.
>   * `file.createNewFile()` — немає прав або директорії.
>
> * **Робота з мережею:**
>   * `new Socket("invalid-host", 80)` → `UnknownHostException`.
>   * `socket.getInputStream().read()` — з'єднання розірвано (`SocketException`).
>   * Тайм-аут при очікуванні відповіді → `SocketTimeoutException`.
>
> * **Робота з потоками загалом:**
>   * Спроба прочитати з закритого потоку.
>   * Виклик `stream.close()` сам по собі може кинути `IOException`.

#### **📌 Що перевіряють:**

*Практичний досвід та розуміння того, в яких саме операціях криються ризики I/O помилок.*

* * *

### **4 ❓ Питання:**

Як `try-with-resources` спрощує обробку `IOException`?

> #### **💬 Відповідь:**
>
> `try-with-resources` (з Java 7) значно спрощує та робить надійнішим код, що працює з ресурсами, які кидають `IOException`.
>
> **Проблема, яку він вирішує:**
> До його появи, для гарантованого закриття ресурсу потрібно було використовувати `finally` блок. Однак, сам метод `close()` також може кинути `IOException`, що вимагало вкладеного `try-catch` у `finally`, роблячи код дуже громіздким.
>
> **Як `try-with-resources` це спрощує:**
>
> 1. **Автоматичне закриття:** Ресурси, оголошені в дужках `try`, автоматично закриваються. Немає потреби у `finally` для виклику `close()`.
> 2. **Спрощена обробка помилок:** Вам потрібно лише обробляти винятки, що виникають під час **роботи** з ресурсом. Винятки, що виникають при **закритті**, обробляються автоматично.
>
> **Приклад:**
>
> ```java
> // Коротко, чисто і безпечно
> try (FileInputStream fis = new FileInputStream("file.txt")) {
>     // ... робота з потоком ...
> } catch (IOException e) {
>     // Цей блок зловить і FileNotFoundException, і помилки при read()
>     e.printStackTrace();
> }
> ```
>
> Крім того, `try-with-resources` коректно обробляє **придушені винятки**, якщо помилка виникає і в `try` блоці, і при закритті.

#### **📌 Що перевіряють:**

*Знання сучасних найкращих практик для роботи з ресурсами.*

* * *

### **5 ❓ Питання:**

Наведіть приклад ситуації, коли варто ловити `FileNotFoundException` окремо від `IOException`. ⭐️

> #### **💬 Відповідь:**
>
> Окреме перехоплення `FileNotFoundException` доцільно, коли ви можете **запропонувати користувачеві або системі альтернативну, специфічну логіку** саме для цього випадку.
>
> **Сценарій: завантаження файлу конфігурації.**
>
> * Якщо файл не знайдено, можливо, це перший запуск програми, і ми можемо створити його зі значеннями за замовчуванням.
> * Якщо ж файл існує, але сталася інша помилка читання (`IOException`), це може свідчити про пошкодження файлу або проблеми з правами доступу, і програму слід завершити.
>
> **Приклад коду:**
>
> ```java
> Properties config = new Properties();
> try (FileInputStream fis = new FileInputStream("config.properties")) {
>     config.load(fis);
> } catch (FileNotFoundException e) {
>     System.out.println("Конфігураційний файл не знайдено. Створюємо новий зі значеннями за замовчуванням.");
>     // Створюємо конфігурацію за замовчуванням
>     config.setProperty("theme", "dark");
>     config.setProperty("language", "en");
> } catch (IOException e) {
>     // Фатальна помилка при читанні існуючого файлу
>     System.err.println("Не вдалося прочитати конфігураційний файл. Роботу програми зупинено.");
>     throw new RuntimeException("Некоректний конфігураційний файл", e);
> }
> ```

#### **📌 Що перевіряють:**

*Здатність проектувати логіку відновлення після різних типів помилок.*

* * *

### **6 ❓ Питання:**

Чому цей код може призвести до `NullPointerException`, хоча він обробляє `IOException`?

```java
BufferedReader reader = null;
try {
    reader = new BufferedReader(new FileReader("file.txt"));
    String line = reader.readLine();
} catch (IOException e) {
    e.printStackTrace();
}
System.out.println("Прочитано рядок: " + reader.readLine());
```

> #### **💬 Відповідь:**
>
> Цей код призведе до **`NullPointerException`**, тому що посилання `reader` залишиться `null` після того, як у `try` блоці виникне виняток.
>
> **Послідовність подій:**
>
> 1. Припустимо, файл `"file.txt"` не існує.
> 2. Виклик `new FileReader("file.txt")` всередині `try` кидає `FileNotFoundException` (який є `IOException`).
> 3. Виконання `try` блоку **негайно переривається**. Змінна `reader` так і не буде ініціалізована (залишиться `null`).
> 4. Керування переходить до `catch` блоку, який просто друкує стек-трейс.
> 5. Програма продовжує виконуватися після `try-catch`.
> 6. Настає черга рядка `System.out.println("..." + reader.readLine());`.
> 7. Оскільки `reader` все ще `null`, спроба викликати `reader.readLine()` призведе до `NullPointerException`.
>
> Це класичний приклад того, як **некоректна обробка помилок може призвести до нових помилок**. Код, що знаходиться після `try-catch`, не повинен покладатися на те, що змінні всередині `try` були успішно ініціалізовані.

#### **📌 Що перевіряють:**

*Здатність аналізувати потік виконання при виникненні помилок та виявляти потенційні `NPE`.*

* * *

### **7 ❓ Питання:**

Яка відмінність у киданні `IOException` в методах `AutoCloseable.close()` та `Closeable.close()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Відмінність полягає в **типі винятку**, який оголошено в їхніх сигнатурах.
>
> * **`AutoCloseable.close()`:**
>   * `void close() throws Exception;`
>   * Оголошує, що може кинути загальний `Exception`. Це робить інтерфейс більш гнучким, оскільки його можуть реалізовувати ресурси, чиє закриття може призвести до будь-яких помилок, а не лише до I/O помилок.
>
> * **`java.io.Closeable.close()` (який успадковує від `AutoCloseable`):**
>   * `void close() throws IOException;`
>   * **Звужує** тип винятку до `IOException`. Це більш специфічний контракт, призначений саме для ресурсів вводу/виводу.
>
> **Практичний наслідок:**
>
> * Якщо ви створюєте власний ресурс, не пов'язаний з I/O, вам краще реалізувати `AutoCloseable`.
> * Якщо ваш ресурс тісно пов'язаний з потоками, краще реалізувати `Closeable`, оскільки це надає клієнтам більш точну інформацію про тип помилки.
> * `try-with-resources` працює з обома.

#### **📌 Що перевіряють:**

*Знання тонких відмінностей у контрактах стандартних інтерфейсів.*

* * *

### **8 ❓ Питання:**

Чи можна "загорнути" `IOException` в `RuntimeException`? Коли це доцільно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, це дуже поширена і часто правильна практика.** Це патерн **"Exception Wrapping"**.
>
> **Коли це доцільно:**
>
> 1. **На межах архітектурних шарів:** Щоб **абстрагуватися** від деталей реалізації і не "забруднювати" сигнатури методів високого рівня `throws IOException`. Наприклад, сервісний шар не повинен знати, що репозиторій працює з файлами.
>
> 2. **Коли помилка є невідворотною:** Якщо в вашому контексті `IOException` (наприклад, неможливість прочитати основний конфігураційний файл) є **фатальною помилкою**, від якої програма не може оговтатися, то перетворення її в `unchecked` виняток є виправданим. Це дозволить програмі "впасти швидко" (fail-fast), а не змушувати кожен метод у стеку викликів прокидати `IOException`.
>
> **Як це зробити правильно:**
> Завжди передавайте оригінальний `IOException` як **`cause`** в конструктор нового `RuntimeException`.
>
> ```java
> try {
>     // ...
> } catch (IOException e) {
>     throw new UnrecoverableSystemException("Не вдалося завантажити критичний ресурс", e);
> }
> ```

#### **📌 Що перевіряють:**

*Архітектурне мислення та розуміння, коли і як правильно обходити механізм `checked` винятків.*

* * *

### **9 ❓ Питання:**

Що краще використовувати для роботи з текстовими файлами з явно вказаним кодуванням: `FileReader` чи `Files.newBufferedReader()`? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Files.newBufferedReader()` є однозначно кращим вибором.**
>
> **Проблема з `FileReader`/`FileWriter`:**
> Вони є класами для зручності, але мають серйозний недолік: вони завжди використовують **кодування за замовчуванням для поточної платформи**. Це робить код непереносимим. Файл, створений в `UTF-8` на Linux, може некоректно прочитатися в `windows-1251` на Windows.
>
> **Переваги `Files.newBufferedReader(Path path, Charset cs)`:**
>
> 1. **Явне керування кодуванням:** Ви можете (і повинні) явно вказати `Charset`, наприклад, `StandardCharsets.UTF_8`. Це гарантує, що ваш код буде працювати однаково на будь-якій платформі.
> 2. **Використання сучасного NIO.2:** Працює з `Path`, яке є більш потужним і гнучким, ніж старий `File`.
> 3. **Ефективність:** Він одразу повертає `BufferedReader`, забезпечуючи ефективне буферизоване читання.
>
> `FileWriter` та `FileReader` можна вважати застарілими для будь-якого серйозного коду, де важлива коректна робота з кодуваннями.

#### **📌 Що перевіряють:**

*Знання сучасного I/O API (NIO.2) та розуміння його переваг над старим.*

* * *

### **10 ❓ Питання:**

Поясніть, що таке `UncheckedIOException` (з Java 8) і в яких випадках він корисний. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`java.io.UncheckedIOException`** — це `unchecked` виняток (`RuntimeException`), який був доданий в Java 8 спеціально для "загортання" `IOException`.
>
> **Навіщо він був введений:**
>
> * Його поява тісно пов'язана зі **Stream API** та **лямбда-виразами**.
> * Функціональні інтерфейси, що використовуються в стрімах (`Function`, `Predicate`), не оголошують `throws IOException`.
> * Тому, якщо всередині лямбди потрібно виконати операцію, що кидає `IOException`, виникає проблема.
>
> `UncheckedIOException` надає **стандартизований** спосіб обійти це обмеження. Ви ловите `IOException` і загортаєте його в `UncheckedIOException`.
>
> **Приклад:**
>
> ```java
> List<String> lines = Files.lines(path) // Files.lines() кидає IOException
>         .collect(Collectors.toList()); // Не скомпілюється
>
> // Виправлення:
> try {
>     Files.lines(path)...
> } catch (IOException e) {
>     throw new UncheckedIOException(e); // Стандартний спосіб "прокинути" IO помилку
> }
> ```
>
> Таким чином, `UncheckedIOException` є офіційним інструментом для роботи з I/O помилками у функціональному стилі.

#### **📌 Що перевіряють:**

*Знання нових класів, доданих в Java 8, та розуміння, які проблеми вони вирішують.*

* * *

### **11 ❓ Питання:**

Чим відрізняється обробка `EOFException` від звичайного `IOException`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`EOFException` (End Of File Exception)** — це специфічний підклас `IOException`. Він сигналізує про **несподіване досягнення кінця файлу або потоку** під час читання.
>
> **Різниця:**
>
> * **Кінець потоку — це не завжди помилка.** Багато методів читання (наприклад, `read()`) просто повертають `-1`, коли дані закінчилися.
>
> * **`EOFException`** кидається тоді, коли **кінець потоку є помилковою ситуацією**. Це трапляється з методами, які очікують прочитати певну кількість байт, але не можуть цього зробити.
>
> **Приклади:**
>
> * `DataInputStream.readFully()`: цей метод обіцяє прочитати повний масив байт. Якщо файл закінчиться раніше, він кине `EOFException`.
> * `DataInputStream.readInt()`: очікує прочитати 4 байти. Якщо доступно лише 2, буде `EOFException`.
>
> Отже, обробка `EOFException` зазвичай означає, що **файл пошкоджено або має неочікуваний формат**. Це не те ж саме, що звичайна помилка читання з диска, тому її можна ловити окремо для специфічної діагностики.

#### **📌 Що перевіряють:**

*Знання специфічних підтипів `IOException` та їхнього семантичного значення.*

* * *

### **12 ❓ Питання:**

Чи можна використовувати `final` для параметра винятку в `catch` блоці? `catch (final IOException e)` ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна, але це має сенс лише до Java 7.**
>
> Модифікатор `final` для параметра `catch` блоку робить посилання `e` **незмінним** всередині цього блоку. Тобто, ви не можете йому переприсвоїти нове значення.
>
> `catch (final IOException e) { e = new IOException(); // ПОМИЛКА КОМПІЛЯЦІЇ }`
>
> **Навіщо це було потрібно:**
>
> До Java 7, для того, щоб "загорнути" і повторно кинути виняток, іноді використовували такий підхід, де параметр `e` міг бути переприсвоєний.
>
> **З Java 7 з'явилися `multi-catch` та "rethrow з аналізом типів"**:
>
> * У `multi-catch` блоці (`catch (A | B e)`), змінна `e` **неявно є `final`**.
> * Тому для уніфікації, `final` тепер можна вказувати, але це вважається **надлишковим**.
>
> В сучасному коді ви практично ніколи не побачите `final` у `catch` блоці, оскільки переприсвоєння параметра винятку саме по собі є поганою практикою.

#### **📌 Що перевіряють:**

*Історичні знання про мову та розуміння того, чому певні конструкції втратили свою актуальність.*

* * *

### **13 ❓ Питання:**

У `try-with-resources` блок може неявно кинути виняток з методу `close()`. Чи потрібно оголошувати цей виняток у `throws` клаузі, якщо сам `try` блок "чистий"? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, потрібно**, якщо виняток, який може кинути `close()`, є **`checked`**.
>
> Компілятор аналізує не тільки тіло `try`, але й **сигнатури методів `close()`** усіх ресурсів, оголошених у дужках.
>
> **Приклад:**
>
> ```java
> class MyResource implements AutoCloseable {
>     // close() оголошує, що кидає checked виняток
>     @Override
>     public void close() throws Exception {
>         throw new Exception("Помилка закриття");
>     }
> }
>
> // Цей метод НЕ скомпілюється без throws Exception
> public void processResource() throws Exception {
>     try (MyResource r = new MyResource()) {
>         System.out.println("Робота в try..."); // try блок не кидає виняток
>     }
>     // Але close() може його кинути, тому компілятор вимагає обробки
> }
> ```
>
> `try-with-resources` є просто синтаксичним цукром над конструкцією, де `close()` викликається. Тому всі правила для `checked` винятків застосовуються і до цього неявного виклику.

#### **📌 Що перевіряють:**

*Дуже точне розуміння того, як `try-with-resources` розкривається компілятором і як це впливає на перевірку винятків.*

* * *

### **14 ❓ Питання:**

Чим відрізняється `IOException`, кинутий `FileOutputStream`, від `IOException`, кинутого `BufferedWriter`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання на розуміння **ланцюжка обгорток**.
>
> * **`FileOutputStream`:** Це **байтовий потік**, що працює напряму з файловою системою. `IOException`, кинутий ним, зазвичай пов'язаний з **реальними проблемами на рівні ОС або диска**:
>   * Недостатньо прав.
>   * Диск заповнено.
>   * Файл заблоковано.
>
> * **`BufferedWriter`:** Це **символьний буферизований потік**. Він сам по собі рідко є джерелом `IOException`. Виняток, який він кидає, це зазвичай **"прокинутий" далі** виняток, що виник у **вкладеному потоці**, який він обгортає.
>
> **Сценарій:**
> `new BufferedWriter(new FileWriter(...))`
>
> 1. Ви викликаєте `writer.write(...)` на `BufferedWriter`.
> 2. Дані потрапляють у буфер. Помилки немає.
> 3. Буфер заповнюється, і `BufferedWriter` намагається викликати `write()` на внутрішньому `FileWriter`.
> 4. `FileWriter` намагається записати байти на диск, і **ось тут** виникає справжній `IOException` (наприклад, "Disk Full").
> 5. `FileWriter` кидає цей `IOException`.
> 6. `BufferedWriter` ловить його і **прокидає далі**, не змінюючи.
>
> Таким чином, `IOException` від `BufferedWriter` — це, по суті, той самий виняток, що виник на більш низькому рівні.

#### **📌 Що перевіряють:**

*Розуміння патерну "Декоратор" в `java.io` та того, як винятки "подорожують" по ланцюжку обгорток.*

* * *

### **15 ❓ Питання:**

Що таке **причина (`cause`)** винятку і як вона пов'язана з `IOException` та його ієрархією? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Причина (`cause`)** — це механізм, який дозволяє винятку зберігати посилання на інший виняток, який його спричинив. Це основа **ланцюжкових винятків (exception chaining)**.
>
> **Зв'язок з `IOException`:**
>
> Це дуже важливо при **загортанні винятків**. Розглянемо приклад з `java.nio`.
>
> Нове NIO.2 API переважно використовує **ієрархію підтипів `IOException`** для сигналізації про помилки (`FileSystemException`, `NoSuchFileException` тощо).
>
> Але іноді низькорівнева операція може кинути інший, несподіваний `checked` або `unchecked` виняток. Щоб не "втратити" цю інформацію, методи NIO.2 часто **загортають** її в `IOException`, використовуючи `cause`.
>
> **Приклад:**
> Уявіть, що під час роботи з файловою системою, яка реалізована через якийсь мережевий протокол, виникає `InterruptedException`.
>
> Метод з NIO.2 може зловити його і кинути `IOException`, зберігши `InterruptedException` як причину:
> `throw new IOException("Операція була перервана", interruptedException);`
>
> Це дозволяє дотримуватися контракту (кидати лише `IOException`), але при цьому **не втрачати** інформацію про реальну першопричину проблеми. Аналізуючи `cause`, розробник може зрозуміти, що проблема була не в самому файлі, а в тому, що потік був перерваний.

#### **📌 Що перевіряють:**

*Експертне розуміння дизайну API стандартної бібліотеки та патернів обробки помилок, які в ній використовуються.*
