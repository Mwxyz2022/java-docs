---
layout: default
title: "Серіалізація: ObjectOutputStream / ObjectInputStream"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 13
---

# Серіалізація: ObjectOutputStream / ObjectInputStream

* короткий опис: Серіалізація – це процес перетворення об'єкта в потік байтів для збереження (наприклад, у файл) або передачі (по мережі). Зворотний процес – десеріалізація. Використовує інтерфейс `Serializable` та класи `ObjectOutputStream`/`ObjectInputStream`.

* поняття: Серіалізація, `Serializable` (маркерний інтерфейс), `ObjectOutputStream`, `ObjectInputStream`, `transient` (модифікатор для виключення полів), `serialVersionUID` (для контролю версій).

* приклади: Запис об'єкта `User` у файл (`oos.writeObject(user)`), зчитування об'єкта назад (`ois.readObject()`).

* ключові моменти: `Serializable` – це дозвіл на серіалізацію. `transient` виключає поле. `serialVersionUID` забезпечує сумісність версій класу.

* що важливо знати на співбесіді: Що це, навіщо, `serialVersionUID`, `transient`.

### **✅ Офіційне визначення:**  

Серіалізація в Java — це процес **перетворення об'єкта в потік байтів**, щоб його можна було **зберегти у файл**, передати по мережі або іншим чином передати.

### **🧠 Простими словами:**  

Це як "упакувати" об'єкт у файл, а потім його можна "розпакувати" назад у програмі.

---

## **Основні компоненти**

| Елемент | Призначення |
| :---: | ----- |
| **`Serializable`** | Позначає, що клас можна серіалізувати |
| **`ObjectOutputStream`** | Запис об'єкта у файл |
| **`ObjectInputStream`** | Зчитування об'єкта з файлу |
| **`transient`** | Поле, яке **не потрібно** серіалізувати |
| **`serialVersionUID`** | Версія класу, яка перевіряється при десеріалізації |

---

## **Пояснення на пальцях (💡)**

🔹 **Serializable** — маркерний інтерфейс (не має методів).  
Позначає: "Цей клас можна серіалізувати".

🔹 **ObjectOutputStream / ObjectInputStream**  
Використовуються для **запису об'єкта у файл** і **читання назад**:

```java
ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("data.obj"));
oos.writeObject(myObject);
oos.close();

ObjectInputStream ois = new ObjectInputStream(new FileInputStream("data.obj"));
MyClass obj = (MyClass) ois.readObject();
ois.close();
```

🔹 **transient**  
Поле з цим модифікатором **не буде записане** у файл. Наприклад, пароль, кеш тощо.

🔹 **serialVersionUID**  
Унікальний ідентифікатор класу. Дозволяє перевірити, чи об'єкт з файлу сумісний із поточною версією класу.

---

## **Корисні приклади (🧪)**

```java
import java.io.*;

public class User implements Serializable {
    private static final long serialVersionUID = 1L;

    private String username;
    private transient String password; // не буде збережено

    public User(String username, String password) {
        this.username = username;
        this.password = password;
    }

    public String toString() {
        return username + " / " + password;
    }
}
```

#### **Серіалізація:**

```java
User user = new User("admin", "secret");

ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("user.ser"));
oos.writeObject(user);
oos.close();
```

#### **Десеріалізація:**

```java
ObjectInputStream ois = new ObjectInputStream(new FileInputStream("user.ser"));
User loadedUser = (User) ois.readObject();
System.out.println(loadedUser); // password буде null
ois.close();
```

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
Serializable     -> Можна серіалізувати
transient        -> Не серіалізується
UID              -> Унікальний ID класу
```

---

## **🟩 Підсумок:**

Серіалізація — це **збереження об'єктів у файл**.  
Клас має реалізувати **`Serializable`**, і всі його поля мають бути серіалізованими.  
Використовуй **`transient`** для **приватних** або **чутливих даних**, які **не потрібно зберігати**.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке серіалізація та десеріалізація в Java?

> #### **💬 Відповідь:**
>
> * **Серіалізація (Serialization)** — це процес перетворення стану об'єкта Java в потік байтів. Цей потік байтів можна потім зберегти у файл, передати по мережі або зберегти в базу даних.
>
> * **Десеріалізація (Deserialization)** — це зворотний процес, коли потік байтів перетворюється назад в об'єкт Java з тим самим станом, який він мав на момент серіалізації.
>
> Основна мета серіалізації — забезпечити збереження стану об'єкта поза межами життєвого циклу програми та його передачу між різними JVM.

#### **📝 Аналогія:**

Серіалізація — це як розібрати меблі на деталі (байти), запакувати їх у коробку (файл або мережевий пакет) для транспортування. Десеріалізація — це зібрати меблі назад з цих деталей у пункті призначення.

#### **📌 Що перевіряють:**

*Розуміння базової концепції та призначення механізму серіалізації.*

* * *

### **2 ❓ Питання:**

Яка роль інтерфейсу `Serializable` і чому він не має методів?

> #### **💬 Відповідь:**
>
> `java.io.Serializable` — це **маркерний інтерфейс (marker interface)**. Він не має жодних методів для реалізації. Його єдина мета — повідомити віртуальній машині Java (JVM), що об'єкти цього класу **можна** серіалізувати.
>
> Якщо ви спробуєте серіалізувати об'єкт класу, який не реалізує `Serializable`, буде кинуто виняток `java.io.NotSerializableException`.
>
> Таким чином, `implements Serializable` — це свідомий дозвіл від розробника на використання механізму серіалізації для цього класу. Це також зобов'язує розробника подумати про наслідки цього, наприклад, про безпеку та версійність.

#### **📝 Коментар:**

Це приклад патерну проектування "Маркер", де інтерфейс використовується не для визначення контракту, а для надання метаінформації про клас.

#### **📌 Що перевіряють:**

*Розуміння концепції маркерних інтерфейсів та ролі `Serializable` як дозволу на серіалізацію.*

* * *

### **3 ❓ Питання:**

Для чого використовується ключове слово `transient`?

> #### **💬 Відповідь:**
>
> Ключове слово `transient` використовується для позначення полів класу, які **не повинні бути серіалізовані**.
>
> Коли об'єкт серіалізується, всі його поля (крім `static` та `transient`) перетворюються в потік байтів. Поля, позначені як `transient`, ігноруються і не включаються в цей потік.
>
> Після десеріалізації такого об'єкта `transient`-поля отримають **значення за замовчуванням** для свого типу (наприклад, `null` для об'єктів, `0` для числових типів, `false` для `boolean`).
>
> **Коли це корисно:**
>
> * Для полів, що містять чутливу інформацію (наприклад, паролі, ключі шифрування).
> * Для полів, які легко обчислити або відновити після десеріалізації.
> * Для полів, які містять посилання на несеріалізовані об'єкти (наприклад, з'єднання з базою даних, потоки).

#### **📝 Приклад:**

```java
public class User implements Serializable {
    private String login;
    private transient String password; // Пароль не буде збережено
    private transient int accessLevelCache; // Цей кеш можна обчислити знову

    // ... конструктори та методи
}
```

#### **📌 Що перевіряють:**

*Знання способу виключення полів із процесу серіалізації та розуміння, в яких випадках це доцільно робити.*

* * *

### **4 ❓ Питання:**

Що таке `serialVersionUID` і що станеться, якщо його не вказати?

> #### **💬 Відповідь:**
>
> `private static final long serialVersionUID` — це унікальний ідентифікатор версії серіалізованого класу.
>
> **Його призначення** — перевіряти, що клас, який використовувався для серіалізації об'єкта, сумісний з класом, який використовується для його десеріалізації.
>
> * Під час серіалізації `serialVersionUID` класу записується в потік байтів разом з даними об'єкта.
> * Під час десеріалізації JVM порівнює `serialVersionUID` з потоку з `serialVersionUID` локального класу. Якщо вони не збігаються, кидається `java.io.InvalidClassException`.
>
> **Якщо не вказати `serialVersionUID` явно:**
> JVM **згенеє його автоматично** під час компіляції на основі структури класу (назв полів, методів, їхніх сигнатур тощо).
> **Проблема:** Будь-яка, навіть незначна, зміна в класі (додавання поля, зміна методу) призведе до генерації нового, іншого `serialVersionUID`. В результаті, ви не зможете десеріалізувати об'єкти, збережені старою версією класу, новою версією.
>
> **Висновок:** Явно вказувати `serialVersionUID` — це **обов'язкова хороша практика** для будь-якого `Serializable` класу, щоб забезпечити контроль над версійністю.

#### **📌 Що перевіряють:**

*Розуміння механізму версіонування серіалізованих класів та наслідків відсутності явного `serialVersionUID`.*

* * *

### **5 ❓ Питання:**

Як серіалізувати та десеріалізувати об'єкт?

> #### **💬 Відповідь:**
>
> Для цього використовуються класи-обгортки `ObjectOutputStream` та `ObjectInputStream`, які працюють поверх інших потоків (наприклад, `FileOutputStream` та `FileInputStream`).
>
> **Серіалізація (запис об'єкта):**
>
> 1. Створюється `FileOutputStream`, який вказує на файл для запису.
> 2. Він "обгортається" в `ObjectOutputStream`.
> 3. Викликається метод `oos.writeObject(yourObject)`.
>
> **Десеріалізація (читання об'єкта):**
>
> 1. Створюється `FileInputStream`, який вказує на файл з даними.
> 2. Він "обгортається" в `ObjectInputStream`.
> 3. Викликається метод `ois.readObject()`, який повертає `Object`.
> 4. Результат приводиться до потрібного типу (`(YourClass) obj`).

#### **📝 Приклад:**

```java
import java.io.*;

// Припускаємо, що клас User реалізує Serializable
public class SerializationExample {
    public static void main(String[] args) throws IOException, ClassNotFoundException {
        User user = new User("testuser", "password123");
        File file = new File("user.ser");

        // 1. Серіалізація
        try (ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(file))) {
            oos.writeObject(user);
        }
        System.out.println("Об'єкт серіалізовано.");

        // 2. Десеріалізація
        try (ObjectInputStream ois = new ObjectInputStream(new FileInputStream(file))) {
            User loadedUser = (User) ois.readObject();
            System.out.println("Об'єкт десеріалізовано: " + loadedUser);
        }

        file.delete();
    }
}
```

#### **📌 Що перевіряють:**

*Практичні навички використання основних класів для серіалізації/десеріалізації.*

* * *

### **6 ❓ Питання:**

Чи серіалізуються статичні (`static`) поля?

> #### **💬 Відповідь:**
>
> Ні, статичні поля **не серіалізуються**.
>
> Механізм серіалізації призначений для збереження **стану об'єкта (instance state)**, а не стану класу. Статичні поля належать класу в цілому, а не конкретному екземпляру, тому вони не є частиною стану об'єкта.
>
> При десеріалізації об'єкта його статичні поля матимуть те значення, яке на даний момент встановлено в класі в поточній JVM, а не те, яке було на момент серіалізації.

#### **📝 Коментар:**

Це логічно, оскільки якби статичні поля серіалізувалися, десеріалізація одного об'єкта могла б несподівано змінити стан для всіх інших об'єктів цього класу в поточній JVM.

#### **📌 Що перевіряють:**

*Розуміння різниці між станом об'єкта та станом класу, та як це впливає на процес серіалізації.*

* * *

### **7 ❓ Питання:**

Що станеться, якщо поле класу, який ми серіалізуємо, є посиланням на об'єкт іншого класу, що не реалізує `Serializable`?

> #### **💬 Відповідь:**
>
> Буде кинуто виняток **`java.io.NotSerializableException`** під час спроби серіалізації.
>
> Механізм серіалізації в Java є **транзитивним**. Це означає, що коли ви серіалізуєте об'єкт, JVM намагається серіалізувати і всі об'єкти, на які він посилається (і так далі, рекурсивно).
>
> Якщо в цьому ланцюжку посилань зустрінеться об'єкт класу, що не реалізує `Serializable`, вся операція серіалізації зазнає невдачі.
>
> **Як це вирішити:**
>
> * Зробити вкладений клас серіалізованим (`implements Serializable`).
> * Або позначити поле, що посилається на несеріалізований об'єкт, ключовим словом **`transient`**.

#### **📌 Що перевіряють:**

*Розуміння рекурсивної природи серіалізації та вимоги, що всі частини графу об'єктів мають бути серіалізованими.*

* * *

### **8 ❓ Питання:**

Як впливає успадкування на серіалізацію? Що буде, якщо дочірній клас реалізує `Serializable`, а батьківський — ні? ⭐️

> #### **💬 Відповідь:**
>
> Це складний випадок з важливою особливістю:
>
> Якщо дочірній клас реалізує `Serializable`, а батьківський — ні, то:
>
> 1. Поля дочірнього класу будуть серіалізовані як зазвичай.
> 2. Поля батьківського класу **не будуть серіалізовані**.
>
> Під час десеріалізації для ініціалізації стану батьківського класу буде викликано його **публічний або protected конструктор без аргументів (`no-arg constructor`)**. Якщо такого конструктора у батьківському класі немає, під час десеріалізації буде кинуто `java.io.InvalidClassException`.
>
> **Висновок:** Якщо ви робите дочірній клас серіалізованим, а батьківський ні, переконайтеся, що батьківський клас має доступний конструктор без аргументів. Стан батьківського класу буде ініціалізовано цим конструктором, а не відновлено з потоку.

#### **📌 Що перевіряють:**

*Глибоке розуміння взаємодії серіалізації та успадкування, знання вимоги до конструктора несеріалізованого батька.*

* * *

### **9 ❓ Питання:**

Що таке методи `writeObject` та `readObject` і коли їх варто перевизначати? ⭐️

> #### **💬 Відповідь:**
>
> `private void writeObject(ObjectOutputStream out)` та `private void readObject(ObjectInputStream in)` — це "магічні" методи, які, якщо їх визначити в серіалізованому класі, дозволяють **взяти під контроль процес серіалізації та десеріалізації**.
>
> Коли JVM серіалізує об'єкт, вона перевіряє, чи має клас ці методи. Якщо так, то замість механізму за замовчуванням вона викликає їх.
>
> **Коли їх варто використовувати:**
>
> 1. **Для шифрування/дешифрування полів:** У `writeObject` можна зашифрувати поле перед записом, а в `readObject` — дешифрувати після читання.
> 2. **Для полів `transient`:** Якщо поле оголошено як `transient`, але його стан все ж потрібно зберегти (можливо, в іншому форматі), це можна зробити вручну в `writeObject` і відновити в `readObject`.
> 3. **Для забезпечення валідації:** В `readObject` можна додати логіку перевірки даних після десеріалізації, щоб упевнитися, що стан об'єкта є коректним.

#### **📝 Приклад:**

```java
private void writeObject(java.io.ObjectOutputStream out) throws IOException {
    out.defaultWriteObject(); // Викликаємо стандартну серіалізацію
    // Додаткова логіка: наприклад, шифрування
    // String encryptedData = encrypt(this.someSensitiveField);
    // out.writeObject(encryptedData);
}

private void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException {
    in.defaultReadObject(); // Викликаємо стандартну десеріалізацію
    // Додаткова логіка
    // String encryptedData = (String) in.readObject();
    // this.someSensitiveField = decrypt(encryptedData);
}
```

#### **📌 Що перевіряють:**

*Знання просунутих технік кастомізації серіалізації та розуміння, як можна керувати цим процесом вручну.*

* * *

### **10 ❓ Питання:**

Які є ризики безпеки, пов'язані з десеріалізацією, і як їх мінімізувати? ⭐️

> #### **💬 Відповідь:**
>
> Десеріалізація даних з неперевірених джерел є однією з **найсерйозніших вразливостей в Java** (Deserialization of Untrusted Data).
>
> **Основний ризик:** Зловмисник може створити спеціально підготовлений потік байтів (payload), який під час десеріалізації змусить програму виконати шкідливий код. Це можливо, якщо в `classpath` програми є класи з "гаджетами" — методами, які виконують небезпечні операції (наприклад, виконання команд ОС, робота з файлами) і які можна викликати через ланцюжок викликів під час `readObject`.
>
> **Способи мінімізації ризиків:**
>
> 1. **Не десеріалізувати дані з ненадійних джерел.** Це найголовніше правило.
> 2. **Використовувати альтернативні формати:** Замість нативної серіалізації використовувати безпечні текстові формати, як-от JSON або XML, з надійними бібліотеками (Jackson, GSON). Вони не виконують довільний код.
> 3. **Валідація даних перед десеріалізацією.**
> 4. **Використання `ObjectInputFilter` (з Java 9):** Дозволяє створити "білий" або "чорний" список класів, які можна (або не можна) десеріалізувати, обмежуючи можливі вектори атаки.
> 5. **Мінімізація привілеїв:** Запускати код, що виконує десеріалізацію, з мінімально необхідними правами доступу.

#### **📌 Що перевіряють:**

*Усвідомлення серйозних ризиків безпеки, пов'язаних з нативною серіалізацією, та знання сучасних практик для захисту від атак.*

* * *

### **11 ❓ Питання:**

У чому різниця між `Serializable` та `Externalizable`? ⭐️

> #### **💬 Відповідь:**
>
> Обидва інтерфейси використовуються для серіалізації, але надають різний рівень контролю.
>
> * **`Serializable` (автоматична серіалізація):**
>   * Це маркерний інтерфейс.
>   * JVM автоматично дбає про запис та відновлення стану об'єкта.
>   * Розробник може частково втрутитися через `transient` та методи `writeObject`/`readObject`.
>   * **Простіший у використанні.**
>
> * **`Externalizable` (ручна серіалізація):**
>   * Цей інтерфейс успадковує `Serializable` і має два методи для реалізації: `writeExternal(ObjectOutput out)` та `readExternal(ObjectInput in)`.
>   * Розробник **повністю відповідає** за те, які дані, в якому форматі і в якому порядку записувати та читати. JVM не робить нічого автоматично.
>   * При десеріалізації завжди викликається **публічний конструктор без аргументів**, на відміну від `Serializable`.
>   * **Дає повний контроль** і може бути більш продуктивним, оскільки дозволяє зберігати дані у компактному бінарному форматі.

**Коли використовувати `Externalizable`?** Коли вам потрібен максимальний контроль над форматом серіалізованих даних, важлива продуктивність, або ви інтегруєтесь з системами, що вимагають специфічного бінарного протоколу.

#### **📌 Що перевіряють:**

*Знання про альтернативний механізм серіалізації та розуміння компромісів між автоматизацією (`Serializable`) та повним контролем (`Externalizable`).*

* * *

### **12 ❓ Питання:**

Чи можна серіалізувати лямбда-вираз? Якщо так, то за яких умов? ⭐️

> #### **💬 Відповідь:**
>
> Так, лямбда-вираз можна серіалізувати, але за однієї важливої умови: цільовий функціональний інтерфейс, до якого приводиться лямбда, повинен **розширювати інтерфейс `java.io.Serializable`**.
>
> Багато стандартних функціональних інтерфейсів (`Runnable`, `Callable`) вже є серіалізованими.
>
> Однак `Function`, `Predicate`, `Consumer` та інші з пакету `java.util.function` **не є** серіалізованими за замовчуванням.
>
> При серіалізації лямбди зберігається не її байткод, а спеціальне представлення, яке вказує на метод, що її реалізує. Стан лямбди (захоплені змінні) також серіалізується.

#### **📝 Приклад:**

```java
import java.io.Serializable;
import java.util.function.Predicate;

public class LambdaSerialization {
    // Цей предикат можна серіалізувати
    Predicate<String> p1 = (Predicate<String> & Serializable) s -> s.length() > 5;

    // Цей - ні, бо Predicate не розширює Serializable
    // Predicate<String> p2 = s -> s.length() > 5;
}

@FunctionalInterface
interface MySerializablePredicate<T> extends Predicate<T>, Serializable {
}

// Або так, що є більш правильним
MySerializablePredicate<String> p3 = s -> s.length() > 5;
```

#### **📌 Що перевіряють:**

*Знання нюансів роботи з лямбда-виразами, зокрема їх взаємодії з механізмом серіалізації.*

* * *

### **13 ❓ Питання:**

Припустимо, ми змінили клас, додавши нове поле. Як забезпечити зворотну сумісність, щоб нова версія програми могла читати об'єкти, серіалізовані старою версією класу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне завдання управління еволюцією класу. Якщо `serialVersionUID` залишився тим самим, то сумісність можлива.
>
> **Додавання нового поля:**
>
> * **Процес:** Коли нова версія класу десеріалізує старий об'єкт, у потоці байтів не буде даних для нового поля. Тому нове поле буде ініціалізоване **значенням за замовчуванням** (`null`, `0`, `false`).
> * **Як керувати:** Якщо вам потрібне інше значення за замовчуванням для цього поля, ви можете реалізувати метод `readObject` і вручну встановити потрібне значення після виклику `in.defaultReadObject()`.
>
> **Видалення поля:**
>
> * **Процес:** Коли нова версія (без поля) десеріалізує старий об'єкт (з полем), дані для видаленого поля в потоці будуть **просто проігноровані**. Це не викличе помилки.
>
> **Ключ до сумісності:**
>
> 1. **Не змінювати `serialVersionUID`**.
> 2. Зміни мають бути сумісними (додавання/видалення полів, додавання методів). Зміна типу поля або ієрархії класів є несумісною зміною.
> 3. Бути готовим до того, що нові поля отримають значення за замовчуванням при читанні старих об'єктів.

#### **📌 Що перевіряють:**

*Глибоке розуміння правил еволюції серіалізованих класів та вміння планувати зміни так, щоб не порушити зворотну сумісність.*

* * *

### **14 ❓ Питання:**

Що таке "десеріалізаційна бомба" і як вона пов'язана з `ObjectInputStream`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Десеріалізаційна бомба (Deserialization Bomb)** — це тип DoS-атаки (Denial of Service), коли зловмисник створює відносно невеликий потік байтів, який при десеріалізації змушує програму виділяти величезну кількість пам'яті, що призводить до `OutOfMemoryError` і відмови в обслуговуванні.
>
> Класичний приклад реалізується через колекції, які можуть містити посилання на себе. Наприклад, можна створити `HashSet`, який містить самого себе як елемент. Або ще гірше — `HashSet`, що містить інший `HashSet`, і так далі, утворюючи глибоко вкладену структуру.
>
> При десеріалізації, `ObjectInputStream` буде намагатися рекурсивно відтворити цю структуру. Наприклад, якщо потік байтів описує `HashSet`, що містить 100 однакових `HashSet`-ів, кожен з яких містить ще 100, і так 10 разів, то `readObject` намагатиметься створити 100^10 об'єктів.
>
> Ця атака використовує той факт, що в серіалізованому форматі можна посилатися на вже визначені об'єкти, тому сам потік байтів може бути дуже компактним, а об'єкт, що розгортається в пам'яті, — гігантським.
>
> **Захист:**
>
> * Використання `ObjectInputFilter` (Java 9+) для обмеження глибини вкладеності об'єктів (`filter.maxdepth`) та загальної кількості об'єктів, що створюються.
> * Не десеріалізувати дані з неперевірених джерел.

#### **📌 Що перевіряють:**

*Розуміння не тільки класичних вразливостей (виконання коду), а й інших векторів атак, таких як DoS, через механізм десеріалізації.*

* * *

### **15 ❓ Питання:**

Що таке метод `readResolve()` і для чого він може використовуватися, наприклад, при реалізації патерну Singleton? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `private Object readResolve()` — це ще один "магічний" метод серіалізації. JVM викликає його **після** того, як об'єкт було повністю десеріалізовано (після `readObject`), але **перед** тим, як повернути його з `ois.readObject()`.
>
> **Його призначення** — дозволити об'єкту, що десеріалізується, **замінити себе** на інший об'єкт. Той об'єкт, який поверне `readResolve()`, і буде фінальним результатом операції десеріалізації.
>
> **Використання для Singleton:**
> Патерн Singleton передбачає існування лише одного екземпляра класу. Однак стандартна десеріалізація **порушує** цей патерн, оскільки кожен виклик `ois.readObject()` створює **новий** екземпляр об'єкта.
>
> Щоб виправити це, у класі Singleton потрібно реалізувати `readResolve()` так, щоб він повертав **існуючий єдиний екземпляр** (`INSTANCE`).

#### **📝 Приклад (захист Singleton від десеріалізації):**

```java
import java.io.Serializable;

public class Singleton implements Serializable {
    private static final Singleton INSTANCE = new Singleton();

    private Singleton() {
    }

    public static Singleton getInstance() {
        return INSTANCE;
    }

    // Цей метод захищає Singleton від створення нових екземплярів при десеріалізації
    private Object readResolve() {
        return INSTANCE; // Завжди повертаємо єдиний існуючий екземпляр
    }
}
```

Таким чином, скільки б разів ви не десеріалізували збережений Singleton, ви завжди будете отримувати посилання на той самий `INSTANCE`, а нові об'єкти будуть відкидатися збирачем сміття.

#### **📌 Що перевіряють:**

*Знання просунутих механізмів життєвого циклу серіалізації та їх застосування для вирішення практичних проблем, таких як підтримка інваріантів патерну Singleton.*
