---
layout: default
title: "Files.write() + Path (NIO)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 5
---

# Files.write() + Path (NIO)

* короткий опис: Метод `Files.write()` з пакету `java.nio.file` – це сучасний, зручний спосіб запису вмісту (списку рядків або масиву байтів) у файл. Працює з об'єктом `Path` та автоматично закриває ресурс.

* поняття: `Files.write()`, `Path` (сучасний шлях до файлу), `Paths.get()`, `StandardOpenOption.APPEND` (для дозапису).

* приклади: `Files.write(Path.of("output.txt"), List.of("line1"));` (запис списку рядків), `Files.write(path, byteArray);` (запис байтів).

* ключові моменти: Однорядковий запис, перезаписує за замовчуванням, автоматично закриває потік, не потребує `close()`.

* що важливо знати на співбесіді: Переваги NIO, як записати `List<String>` або `byte[]`.

### **✅ Офіційне визначення:**  

Метод **`Files.write()`** з пакету **`java.nio.file`** — це сучасний, зручний спосіб **запису вмісту у файл**. Він працює з об'єктом `Path` і дозволяє швидко записати:

* рядки (`List<String>`)

* масиви байтів (`byte[]`)

### **🧠 Простими словами:**  

Це як **швидкий спосіб з Java 7**, щоби без зайвих класів (FileWriter, BufferedWriter) **однією командою записати все у файл**. Пишемо просто: **`Files.write(path, дані)`** — і готово\!

---

## **Основні методи та варіанти**

📊 Таблиця:

| Метод | Що робить |
| ----- | ----- |
| **`Files.write(Path path, byte[])`** | Записує байти у файл |
| **`Files.write(Path path, List<String>)`** | Записує список рядків у файл |
| **`Files.write(path, ..., StandardOpenOption)`** | Модифікує поведінку (наприклад, append) |

---

## **Пояснення на пальцях (💡)**

#### **Коли зручно:**

* Хочеш **одним рядком записати у файл**

* Працюєш із **невеликим обсягом даних**

* Хочеш використовувати **новий API** (`Path` замість старого `File`)

#### **Важливо:**

* Цей метод **записує одразу все**

* **Перезаписує файл за замовчуванням**

* Можна вказати **`StandardOpenOption.APPEND`**, щоб **дописувати**

---

## **Корисні приклади (🧪)**

### **🔹 Запис байтів у файл:**

```java
Path path = Paths.get("output.txt");
byte[] data = "Привіт, світ!".getBytes(StandardCharsets.UTF_8);
Files.write(path, data);
```

### **🔹 Запис списку рядків:**

```java
List<String> lines = List.of("Рядок 1", "Рядок 2", "Ще щось");
Files.write(Paths.get("output.txt"), lines);
```

### **🔹 Запис з опцією дозапису:**

```java
Files.write(
    Paths.get("log.txt"),
    List.of("Новий рядок"),
    StandardOpenOption.CREATE,      // створити, якщо немає
    StandardOpenOption.APPEND       // додати в кінець
);
```

---

## **Порівняння (⚖️)**

| Підхід | Буфер | Гнучкість | Форматування | Ідеально для |
| ----- | ----- | ----- | ----- | ----- |
| **`FileWriter`** | ❌ | 🟡 | ❌ | Простий посимвольний запис |
| **`BufferedWriter`** | ✅ | ✅ | ❌ | Великі обсяги даних |
| **`PrintWriter`** | ✅ | ✅ | ✅ | Форматовані звіти |
| **`Files.write()`** | ❌ | 🟡 | ❌ | Швидкий одноразовий запис |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
Files.write(path, data) -> один рядок, повний запис
List.of("A", "B") -> рядки
.getBytes()       -> байти
APPEND            -> дописування
```

---

## **🟩 Підсумок:**

**`Files.write()`** — це **новий, зручний і мінімалістичний API**, щоб записувати дані у файл без зайвих обгортань. Підходить для швидких рішень, скриптів, логів. Але для великих обсягів даних краще — **`BufferedWriter`**.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `java.nio.file.Files.write()` і яка його основна перевага?

> #### **💬 Відповідь:**
>
> `Files.write()` — це статичний метод з утилітарного класу `java.nio.file.Files`, введеного в **Java 7** як частина нового I/O API (NIO.2). Він надає **спрощений, високорівневий спосіб для запису даних у файл**.
>
> **Основна перевага:**
> **Лаконічність та зручність**. Він дозволяє записати масив байтів або колекцію рядків у файл **одним рядком коду**, приховуючи всю складну роботу з відкриттям, записом та закриттям потоків.
>
> **Приклад:**
>
> ```java
> // Старий підхід
> try (BufferedWriter writer = new BufferedWriter(new FileWriter("file.txt"))) {
>     writer.write("line 1");
>     writer.newLine();
>     writer.write("line 2");
> }
>
> // Сучасний підхід з Files.write()
> List<String> lines = List.of("line 1", "line 2");
> Files.write(Paths.get("file.txt"), lines);
> ```
>
> `Files.write()` автоматично керує ресурсами, тому немає потреби в `try-with-resources`.

#### **📌 Що перевіряють:**

*Знання сучасного NIO.2 API та його ключових переваг над старим `java.io`.*

* * *

### **2 ❓ Питання:**

Які типи даних можна записати за допомогою `Files.write()`? Наведіть приклади.

> #### **💬 Відповідь:**
>
> `Files.write()` має кілька перевантажених версій для запису різних типів даних. Два основні:
>
> 1. **Запис масиву байтів (`byte[]`):**
>     * Цей метод приймає `Path` та `byte[]` і записує "сирі" байти у файл.
>     * **Синтаксис:** `Path write(Path path, byte[] bytes, OpenOption... options)`
>     * **Приклад:**
>
>         ```java
>         String content = "Hello, NIO.2!";
>         byte[] bytes = content.getBytes(StandardCharsets.UTF_8);
>         Files.write(Paths.get("binary.dat"), bytes);
>         ```
>
> 2. **Запис колекції рядків (`Iterable<? extends CharSequence>`):**
>
>     * Цей метод приймає `Path` та колекцію (наприклад, `List<String>`). Кожен рядок з колекції записується у файл як окремий рядок з додаванням системного роздільника.
>     * **Синтаксис:** `Path write(Path path, Iterable<? extends CharSequence> lines, OpenOption... options)`
>     * **Приклад:**
>
>         ```java
>         List<String> lines = List.of("First line", "Second line");
>         Files.write(Paths.get("text.txt"), lines);
>         ```
>
>     Також існують версії, що приймають `Charset` для явної вказівки кодування.

#### **📌 Що перевіряють:**

*Знання основних перевантажених версій методу та вміння їх застосовувати.*

* * *

### **3 ❓ Питання:**

Чим `java.nio.file.Path` кращий за `java.io.File`?

> #### **💬 Відповідь:**
>
> `Path` — це сучасна заміна `File`, яка має низку суттєвих переваг:
>
> 1. **Більш потужне API:** `Path` інтегрований з класом `Files`, який надає величезну кількість утилітарних методів для роботи з файлами та директоріями (копіювання, переміщення, читання/запис атрибутів, обхід дерева файлів). `File` має значно бідніший функціонал.
>
> 2. **Краща обробка помилок:** Методи `Files`, що працюють з `Path`, кидають **інформативні `checked` винятки** (наприклад, `NoSuchFileException`, `DirectoryNotEmptyException`), що полегшує діагностику. Методи `File` (`delete()`, `renameTo()`) просто повертають `boolean`, не пояснюючи причину невдачі.
>
> 3. **Іммутабельність:** Об'єкти `Path` є **незмінними (immutable)**, що робить їх більш безпечними для використання у багатопотоковому середовищі. `File` є змінним.
>
> 4. **Краща підтримка файлових систем:** `Path` є частиною API, яке краще абстрагує від конкретної файлової системи та підтримує роботу з віртуальними файловими системами (наприклад, всередині ZIP-архіву).
>
> **Як отримати `Path`:**
> Найпростіший спосіб — через статичний метод `Paths.get()`:
> `Path path = Paths.get("data", "myfile.txt");`

#### **📌 Що перевіряють:**

*Розуміння того, чому NIO.2 є кращим і сучасним підходом для роботи з файловою системою.*

* * *

### **4 ❓ Питання:**

Як за допомогою `Files.write()` дозаписати дані в кінець файлу?

> #### **💬 Відповідь:**
>
> За замовчуванням, `Files.write()` **перезаписує** файл (або створює новий, якщо його немає).
>
> Щоб увімкнути режим **дозапису (append)**, потрібно передати відповідну опцію відкриття файлу — **`StandardOpenOption.APPEND`**.
>
> Опції передаються як додаткові varargs-аргументи.
>
> **Приклад дозапису:**
>
> ```java
> Path logFile = Paths.get("application.log");
> List<String> newLogEntry = List.of("User logged in at " + new Date());
>
> // Передаємо опції APPEND та CREATE (створити файл, якщо він не існує)
> try {
>     Files.write(logFile, newLogEntry, StandardCharsets.UTF_8,
>                 StandardOpenOption.APPEND,
>                 StandardOpenOption.CREATE);
> } catch (IOException e) {
>     e.printStackTrace();
> }
> ```
>
> Використання `StandardOpenOption` робить код більш читабельним та явним, ніж булевий прапорець `true` у `FileWriter`.

#### **📌 Що перевіряють:**

*Знання `StandardOpenOption` та вміння використовувати їх для керування режимом запису.*

* * *

### **5 ❓ Питання:**

Які ще корисні `StandardOpenOption` існують, крім `APPEND`?

> #### **💬 Відповідь:**
>
> `StandardOpenOption` — це `enum`, що надає набір стандартних опцій для роботи з файлами.
>
> **Найкорисніші з них:**
>
> * **`CREATE`**: Створити новий файл, якщо він не існує. Якщо він існує, нічого не робити (зазвичай використовується разом з `APPEND` або `WRITE`).
> * **`CREATE_NEW`**: Створити новий файл, але **кинути `FileAlreadyExistsException`**, якщо файл вже існує. Це корисно для атомарної перевірки та створення.
> * **`WRITE`**: Відкрити файл для запису. За замовчуванням ця опція вже включена для `Files.write()`.
> * **`APPEND`**: Дописувати дані в кінець файлу.
> * **`TRUNCATE_EXISTING`**: Якщо файл існує, його вміст **повністю очищується** перед записом. Це є поведінкою за замовчуванням для `Files.write()`, якщо `APPEND` не вказано.
> * **`DELETE_ON_CLOSE`**: Видалити файл автоматично після того, як потік буде закрито. Корисно для тимчасових файлів.
>
> Ці опції можна комбінувати, передаючи їх через кому.

#### **📌 Що перевіряють:**

*Глибокі знання NIO.2 API та інструментів для гнучкого управління файловими операціями.*

* * *

### **6 ❓ Питання:**

У яких випадках `Files.write()` може бути неефективним? Коли краще використати `BufferedWriter`? ⭐️

> #### **💬 Відповідь:**
>
> `Files.write()` є зручним, але може бути неефективним у певних сценаріях.
>
> **Коли `Files.write()` є НЕефективним:**
>
> * **При записі дуже великого об'єкта:** Метод `Files.write(path, List<String> lines)` спочатку завантажує **всю колекцію рядків у пам'ять**, а потім записує її за раз. Якщо колекція дуже велика (мільйони рядків), це може призвести до **`OutOfMemoryError`**.
>
> * **При поступовому записі даних (у циклі):** Якщо ви генеруєте дані поступово в циклі і викликаєте `Files.write(..., StandardOpenOption.APPEND)` на кожній ітерації, це буде **вкрай неефективно**. Кожен виклик буде заново відкривати, записувати і закривати файл.
>
> **Коли краще `BufferedWriter`:**
>
> * Коли ви працюєте з **потоком даних (stream)** або генеруєте дані "на льоту". `BufferedWriter` дозволяє писати дані **порційно**, не зберігаючи їх усі в пам'яті.
> * Коли ви виконуєте **багато дрібних записів** у циклі. `BufferedWriter` накопичить їх у буфер і запише на диск одним великим блоком.
>
> **Коротко:**
>
> * `Files.write()` — для **запису вже готового, невеликого об'єкта**.
> * `BufferedWriter` — для **потокового запису великих обсягів даних**.

#### **📌 Що перевіряють:**

*Розуміння компромісів між зручністю API та продуктивністю. Кандидат повинен знати, коли "простий" спосіб стає "вузьким місцем".*

* * *

### **7 ❓ Питання:**

Чому цей код не потребує явного `try-catch` або `throws` клаузи?
`Files.write(Paths.get("file.txt"), List.of("text"));` ⭐️

> #### **💬 Відповідь:**
>
> Цей код **потребує** обробки. Це питання з підступом.
>
> Метод `Files.write()` **оголошує `throws IOException`**, який є **`checked` винятком**.
>
> Отже, такий код, як написано, **не скомпілюється**. Компілятор Java вимагатиме обробити цей виняток.
>
> **Правильний код повинен виглядати так:**
>
> **Варіант 1: `try-catch`**
>
> ```java
> try {
>     Files.write(Paths.get("file.txt"), List.of("text"));
> } catch (IOException e) {
>     e.printStackTrace();
> }
> ```
>
> **Варіант 2: `throws`**
>
> ```java
> public void myMethod() throws IOException {
>     Files.write(Paths.get("file.txt"), List.of("text"));
> }
> ```
>
> Той факт, що `Files.write` сам керує ресурсами, не звільняє розробника від **обробки потенційних помилок**, що можуть виникнути під час цього процесу (файл не знайдено, немає прав тощо).

#### **📌 Що перевіряють:**

*Уважність та знання того, що зручність API не скасовує необхідність обробки `checked` винятків.*

* * *

### **8 ❓ Питання:**

Як забезпечити, що запис за допомогою `Files.write()` є атомарним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це можна зробити, використовуючи специфічні `OpenOption`.
>
> **Атомарна операція** означає, що вона або виконується повністю, або не виконується взагалі. Інші процеси не побачать "частково записаного" файлу.
>
> Стандартний `Files.write()` не гарантує атомарності. Щоб досягти цього, часто використовують такий підхід:
>
> 1. Записати дані у **тимчасовий файл** у тій же директорії.
> 2. Використати метод **`Files.move()`** з опцією **`StandardCopyOption.ATOMIC_MOVE`**, щоб атомарно перейменувати/перемістити тимчасовий файл на місце цільового.
>
> **Приклад:**
>
> ```java
> Path target = Paths.get("mydata.dat");
> Path temp = Paths.get("mydata.dat.tmp");
>
> try {
>     byte[] data = ...;
>     Files.write(temp, data); // Записуємо у тимчасовий файл
>     // Атомарно переміщуємо, перезаписуючи існуючий, якщо він є
>     Files.move(temp, target, StandardCopyOption.REPLACE_EXISTING,
>                           StandardCopyOption.ATOMIC_MOVE);
> } catch (IOException e) {
>     // ... обробка, видалення тимчасового файлу ...
>     Files.deleteIfExists(temp);
> }
> ```
>
> Цей підхід гарантує, що інші процеси побачать або стару версію файлу, або вже повністю записану нову, але ніколи — проміжний стан.

#### **📌 Що перевіряють:**

*Знання просунутих можливостей NIO.2 та патернів для надійного запису даних.*

* * *

### **9 ❓ Питання:**

Поясніть, що "під капотом" робить `Files.write(path, lines)`. Чи використовує він буферизацію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, він використовує буферизацію.**
>
> Реалізація `Files.write(path, lines)` у JDK, по суті, є **синтаксичним цукром** над створенням ланцюжка потоків, який ми б писали вручну.
>
> **Спрощено, він робить наступне:**
>
> 1. Відкриває `OutputStream` для вказаного `Path` з переданими опціями.
> 2. "Обгортає" його в `OutputStreamWriter` з вказаним кодуванням.
> 3. "Обгортає" `OutputStreamWriter` у `BufferedWriter`.
> 4. Ітерується по колекції `lines`.
> 5. Для кожного рядка викликає `writer.write(line)` та `writer.newLine()`.
> 6. Вся ця конструкція неявно знаходиться всередині `try-with-resources`, тому після завершення всі потоки коректно закриваються.
>
> Таким чином, `Files.write()` вже оптимізований для ефективного запису завдяки вбудованій буферизації.

#### **📌 Що перевіряють:**

*Розуміння того, як високорівневе API реалізовано через більш низькорівневі компоненти.*

* * *

### **10 ❓ Питання:**

Чому для запису у файл рекомендується вказувати кодування, наприклад, `StandardCharsets.UTF_8`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Явне вказування кодування є **критично важливим** для забезпечення **переносимості (portability)** та **коректності** текстових даних.
>
> **Проблема, якщо не вказувати кодування:**
>
> * `Files.write()` (і старий `FileWriter`) будуть використовувати **кодування платформи за замовчуванням**.
> * На **Windows** це часто `windows-1251`.
> * На **Linux** та **macOS** — `UTF-8`.
>
> Якщо ви створите файл з кириличними символами на Windows, а потім спробуєте прочитати його на Linux, ви отримаєте "кракозябри", тому що байти, записані в `windows-1251`, будуть некоректно інтерпретовані як `UTF-8`.
>
> **Рішення:**
>
> Використовуючи `StandardCharsets.UTF_8`, ви **гарантуєте**, що файл буде записано у **стандартизованому, універсальному кодуванні**, яке буде однаково зрозуміле на будь-якій системі. `UTF-8` є де-факто стандартом для текстових даних сьогодні.
>
> ```java
> Files.write(path, lines, StandardCharsets.UTF_8, ...);
> ```
>
> Це робить ваш додаток незалежним від налаштувань операційної системи.

#### **📌 Що перевіряють:**

*Розуміння проблем інтернаціоналізації та важливості явного керування кодуваннями.*

* * *

### **11 ❓ Питання:**

Порівняйте `Files.write()` та `Files.writeString()` (з Java 11). У чому різниця? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Files.writeString()` — це ще один зручний метод, доданий у **Java 11**, який спрощує запис **одного цілого рядка**.
>
> **`Files.write(Path, Iterable<String>, ...)`:**
>
> * **Призначення:** Записує **колекцію рядків**.
> * **Поведінка:** Кожен рядок з колекції записується у файл, і між ними **автоматично додається системний роздільник рядків**.
>
> **`Files.writeString(Path, CharSequence, ...)`:**
>
> * **Призначення:** Записує **один рядок (`CharSequence`)**.
> * **Поведінка:** Записує рядок **"як є"**, **без додавання** будь-яких роздільників рядків. Якщо вам потрібен новий рядок, ви повинні додати його самі (`"my text\n"`).
>
> **Коли який використовувати:**
>
> * Якщо у вас є `List<String>` або інша колекція — використовуйте `Files.write()`.
> * Якщо у вас є один великий `String` (наприклад, готовий JSON або XML), і ви хочете записати його без змін — використовуйте `Files.writeString()`.

#### **📌 Що перевіряють:**

*Знання сучасних (з Java 11) методів API та їхніх відмінностей.*

* * *

### **12 ❓ Питання:**

Як `Files.write()` працює з винятками у порівнянні зі старим `File.createNewFile()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> `Files.write()` надає набагато **більш інформативну та точну** обробку помилок.
>
> **Старий `File.createNewFile()`:**
>
> * Кидає лише загальний **`IOException`**. Якщо, наприклад, ви не можете створити файл через відсутність прав доступу, ви отримаєте `IOException`, але без деталей.
> * Повертає `boolean`, що теж не дуже інформативно.
>
> **Новий `Files.write()` (та інші методи `Files`):**
>
> * Використовує **ієрархію нащадків `IOException`** для сигналізації про конкретні проблеми.
> * **Приклади:**
>   * Якщо ви намагаєтеся писати в директорію, якої не існує, ви отримаєте `NoSuchFileException`.
>   * Якщо у вас немає прав на запис, ви отримаєте `AccessDeniedException`.
>   * Якщо ви використовуєте `StandardOpenOption.CREATE_NEW` і файл вже існує, ви отримаєте `FileAlreadyExistsException`.
>
> Це дозволяє писати більш надійні `catch` блоки, які можуть по-різному реагувати на різні типи помилок, що значно покращує діагностику та логіку відновлення.

#### **📌 Що перевіряють:**

*Розуміння того, як NIO.2 покращило механізм обробки помилок у порівнянні з `java.io`.*

* * *

### **13 ❓ Питання:**

Поясніть, що робить кожна опція в цьому виклику:
`Files.write(path, data, StandardOpenOption.CREATE, StandardOpenOption.TRUNCATE_EXISTING);` ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ця комбінація опцій є поведінкою `Files.write()` за замовчуванням (якщо `APPEND` не вказано).
>
> * **`StandardOpenOption.CREATE`:**
>   * **Інструкція:** Якщо файл за вказаним шляхом **не існує, створити його**. Якщо він існує, нічого не робити.
>   * **Мета:** Гарантує, що операція не "впаде", якщо файл ще не створено.
>
> * **`StandardOpenOption.TRUNCATE_EXISTING`:**
>   * **Інструкція:** Якщо файл вже існує, його вміст буде **повністю видалено (довжина файлу стане 0)** перед тим, як будуть записані нові дані.
>   * **Мета:** Гарантує, що файл буде **перезаписано**.
>
> **Комбінація:**
>
> * Якщо файл не існує, він буде створений, і в нього запишуться нові дані.
> * Якщо файл існує, його вміст буде стерто, а потім в нього запишуться нові дані.
>
> Отже, це є стандартним режимом **"записати/перезаписати"**. `Files.write()` використовує `WRITE`, `CREATE` та `TRUNCATE_EXISTING` за замовчуванням.

#### **📌 Що перевіряють:**

*Дуже детальне знання `StandardOpenOption` та того, як їхні комбінації визначають поведінку файлових операцій.*

* * *

### **14 ❓ Питання:**

Чому `Path` є кращим для роботи в багатопотоковому середовищі, ніж `File`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Path` є кращим** через свою **незмінність (immutability)**.
>
> * **Об'єкт `File` є змінним (mutable):** Його внутрішній стан може бути змінено після створення, хоча API для цього обмежене (основний метод — `renameTo`, який є проблематичним). Спільне використання змінного об'єкта `File` між потоками без синхронізації може призвести до непередбачуваної поведінки, хоча ризик не такий високий, як з колекціями.
>
> * **Об'єкт `Path` є незмінним (immutable):** Після того, як об'єкт `Path` створено (`Paths.get(...)`), його внутрішній шлях не можна змінити. Будь-які операції, що "змінюють" шлях (наприклад, `path.resolve(otherPath)`), повертають **новий об'єкт `Path`**.
>
> **Переваги незмінності:**
>
> Незмінні об'єкти є **потокобезпечними за своєю природою**. Ви можете безпечно передавати один і той самий екземпляр `Path` у кілька потоків, і ви можете бути впевнені, що жоден з них не зможе змінити його стан, що усуває цілий клас потенційних помилок.

#### **📌 Що перевіряють:**

*Розуміння того, як характеристики об'єктів (змінність/незмінність) впливають на їхню безпечність у багатопотокових додатках.*

* * *

### **15 ❓ Питання:**

Уявіть, що вам потрібно атомарно записати дані у файл, який можуть читати інші процеси. Який патерн з використанням NIO.2 API ви б застосували? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класична проблема, яка вирішується патерном **"запис у тимчасовий файл з атомарним переміщенням"**. `Files` API надає для цього ідеальні інструменти.
>
> **Алгоритм:**
>
> 1. Визначити шлях до **цільового** файлу та **тимчасового** файлу (бажано в тій самій директорії).
> 2. Записати всі дані у **тимчасовий** файл за допомогою `Files.write()` або `BufferedWriter`. Це безпечно, оскільки інші процеси не знають про існування цього файлу.
> 3. Після успішного запису, виконати **атомарне переміщення (перейменування)** тимчасового файлу на місце цільового за допомогою `Files.move()`.
>
> **Код:**
>
> ```java
> Path targetFile = Paths.get("data/config.json");
> Path tempFile = Paths.get("data/config.json.tmp");
> byte[] content = ...;
>
> try {
>     // 1. Пишемо у тимчасовий файл
>     Files.write(tempFile, content);
>
>     // 2. Атомарно замінюємо старий файл новим
>     Files.move(tempFile, targetFile,
>                StandardCopyOption.ATOMIC_MOVE,
>                StandardCopyOption.REPLACE_EXISTING);
>
> } catch (IOException e) {
>     // У разі помилки, прибираємо за собою
>     Files.deleteIfExists(tempFile);
>     // Обробляємо помилку...
> }
> ```
>
> **Чому це працює:**
>
> * `StandardCopyOption.ATOMIC_MOVE` дає інструкцію файловій системі виконати перейменування як **одну, неподільну операцію**.
> * Інші процеси, що читають `config.json`, побачать **або стару версію файлу, або вже повністю записану нову**, але ніколи — проміжний, неповний стан. Це гарантує консистентність даних.

#### **📌 Що перевіряють:**

*Знання просунутих патернів для роботи з файловою системою. Кандидат повинен розуміти, як забезпечити атомарність та консистентність даних.*
