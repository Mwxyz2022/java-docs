---
layout: default
title: "FileReader (java.io.FileReader)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 6
---

# FileReader (java.io.FileReader)

* короткий опис: `FileReader` – це клас для посимвольного зчитування текстових файлів. Читає файл посимвольно, використовуючи системне кодування за замовчуванням.

* поняття: `FileReader`, `read()`, кодування.

* приклади: Читання файлу символ за символом у циклі (`int ch = fr.read()`).

* ключові моменти: Працює з символами, повільніший за `BufferedReader` (не має буфера), `read()` повертає `-1` наприкінці файлу.

* що важливо знати на співбесіді: Призначення `FileReader`, відмінність від `BufferedReader`, як змінити кодування (через `InputStreamReader`).

### **✅ Офіційне визначення:**  

  **`FileReader`** — це клас з пакета `java.io`, який забезпечує **читання символів** з файлів. Він читає файл **посимвольно**, автоматично використовуючи **системну кодування** (зазвичай UTF-8).

### **🧠 Простими словами:**  

Це як **ліхтарик**, який читає **текстовий файл символ за символом**. Зручно, якщо хочеш працювати з літерами, словами, обробляти файл покроково.

---

## **📊 Основні методи**

| Метод | Що робить |
| ----- | ----- |
| **`read()`** | Читає **один символ**, повертає int |
| **`read(char[] buffer)`** | Читає масив символів |
| **`close()`** | Закриває потік |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

* Щоб **читати звичайні текстові файли**

* Працювати з **символами** замість байтів

### **У чому особливість?**

* **Не читає весь файл одразу**, а посимвольно

* Підходить для **обробки символів, перевірки конкретних букв**

* Працює повільніше, ніж `BufferedReader`, але гнучкіше

#### **Граблі:**

* Треба **закривати потік вручну**, інакше може зависнути ресурс

* **Не читає построчно**, лише символами

* Для UTF-8 з BOM може бути некоректне читання -> краще `InputStreamReader`

---

## **Корисні приклади (🧪)**

### **🔹 Зчитування символів по одному:**

```java
FileReader fr = new FileReader("data.txt");
int ch;
while ((ch = fr.read()) != -1) {
    System.out.print((char) ch);
}
fr.close();
```

### **🔹 Зчитування у масив:**

```java
FileReader fr = new FileReader("data.txt");
char[] buffer = new char[100];
int len = fr.read(buffer);
System.out.println(new String(buffer, 0, len));
fr.close();
```

### **🔹 try-with-resources:**

```java
try (FileReader fr = new FileReader("data.txt")) {
    int c;
    while ((c = fr.read()) != -1) {
        System.out.print((char) c);
    }
}
```

---

## **Порівняння (⚖️)**

| Клас | Тип вводу | Рівень | Підходить для |
| ----- | ----- | ----- | ----- |
| **`FileInputStream`** | байтовий | низький | зображення, бінарні |
| **`FileReader`** | символи | базовий | простий текст |
| **`BufferedReader`** | символи | буферизований | великі текстові файли |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
FileReader -> читає символи
read() -> читає один символ
read(char[]) -> багато символів
close() -> не забудь закрити!
```

---

## **🟩 Підсумок:**

**`FileReader`** — це базовий клас для **зчитування тексту з файлів символами**. Простий у використанні, але обмежений. Рекомендується для невеликих задач або у поєднанні з **`BufferedReader`**, якщо потрібно читати **рядки**.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `java.io.FileReader` і для якої основної задачі він призначений?

> #### **💬 Відповідь:**
>
> **`FileReader`** — це клас із пакета `java.io`, призначений для **читання символів (тексту)** з файлу. Він успадковує `InputStreamReader`, що робить його символьним потоком, а не байтовим.
>
> **Основна задача:**
> Надати простий спосіб для **посимвольного читання** текстових файлів. Він автоматично перетворює байти з файлу в символи (`char`), використовуючи **кодування за замовчуванням** для поточної операційної системи.
>
> **Приклад:**
>
> ```java
> try (FileReader reader = new FileReader("file.txt")) {
>     int character;
>     // read() повертає -1 в кінці файлу
>     while ((character = reader.read()) != -1) {
>         System.out.print((char) character);
>     }
> } catch (IOException e) {
>     e.printStackTrace();
> }
> ```
>
> По суті, `new FileReader(fileName)` є скороченням для `new InputStreamReader(new FileInputStream(fileName))`.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `FileReader` як інструменту для читання саме текстових даних, а не "сирих" байтів.*

* * *

### **2 ❓ Питання:**

Які основні недоліки має `FileReader`?

> #### **💬 Відповідь:**
>
> `FileReader` — це клас для зручності, але він має два суттєвих недоліки, через які його використання в серйозних додатках не рекомендується:
>
> 1. **Неможливість вказати кодування:**
>     * `FileReader` **завжди** використовує **кодування за замовчуванням** для поточної платформи (`System.getProperty("file.encoding")`).
>     * Це робить код **непереносимим**. Файл, створений в кодуванні `UTF-8` на Linux, може бути некоректно прочитаний `FileReader`'ом на Windows, де кодуванням за замовчуванням може бути `windows-1251`. Це призведе до пошкодження символів.
>
> 2. **Відсутність буферизації:**
>     * Кожен виклик методу `read()` потенційно може призвести до **системного виклику** для читання даних з диску.
>     * Операції вводу/виводу є дуже повільними. Часті звернення до диска значно **знижують продуктивність**, особливо при читанні великих файлів.
>
> Через ці проблеми, для читання текстових файлів майже завжди слід використовувати більш гнучкі та ефективні альтернативи.

#### **📌 Що перевіряють:**

*Розуміння ключових слабких місць `FileReader`, що мотивує використання більш сучасних і надійних підходів.*

* * *

### **3 ❓ Питання:**

Яка є краща альтернатива `FileReader` для читання текстових файлів, і чому?

> #### **💬 Відповідь:**
>
> Найкращою альтернативою є комбінація **`Files.newBufferedReader(path, charset)`** (з NIO.2, Java 8+) або ручне створення ланцюжка **`new BufferedReader(new InputStreamReader(new FileInputStream(...), charset))`**.
>
> **Чому це краще:**
>
> 1. **Явне керування кодуванням:** Ви можете (і повинні) явно вказати кодування, наприклад, `StandardCharsets.UTF_8`. Це гарантує, що ваш код буде працювати однаково на будь-якій платформі, і ви зможете коректно читати файли з інтернаціональними символами.
>
> 2. **Ефективна буферизація:** `BufferedReader` "обгортає" потік і читає дані з диска **великими порціями** у внутрішній буфер в пам'яті. Подальші виклики `read()` беруть дані з цього швидкого буфера, що значно **зменшує кількість дорогих I/O операцій** і підвищує продуктивність.
>
> 3. **Зручне API:** `BufferedReader` надає дуже корисний метод `readLine()`, який дозволяє читати файл рядок за рядком, що є найпоширенішим сценарієм.
>
> **Приклад сучасного підходу:**
>
> ```java
> Path path = Paths.get("file.txt");
> try (BufferedReader reader = Files.newBufferedReader(path, StandardCharsets.UTF_8)) {
>     String line;
>     while ((line = reader.readLine()) != null) {
>         System.out.println(line);
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Знання сучасного NIO.2 API та вміння обґрунтувати, чому воно краще за застарілі класи `java.io`.*

* * *

### **4 ❓ Питання:**

Що повертає метод `read()` з `FileReader`, і чому він повертає `int`, а не `char`?

> #### **💬 Відповідь:**
>
> Метод `read()` без аргументів має сигнатуру **`public int read() throws IOException`**.
>
> **Що він повертає:**
>
> * Якщо вдалося прочитати символ, він повертає **код цього символу (`char`)** у вигляді `int` (в діапазоні від 0 до 65535).
> * Якщо досягнуто **кінця потоку** (файлу), він повертає **-1**.
>
> **Чому `int`, а не `char`?**
>
> Це зроблено для того, щоб мати можливість **надійно сигналізувати про кінець потоку**.
>
> * Тип `char` є **беззнаковим** і його діапазон від `0` до `65535`.
> * Якби `read()` повертав `char`, не було б жодного значення, яке можна було б використати для позначення кінця потоку, не плутаючи його з реальним символом.
> * Тип `int` є значно ширшим. Використання **-1** як спеціального значення "кінець потоку" гарантує, що воно ніколи не буде сплутане з кодом будь-якого валідного `char`.
>
> Тому при роботі з цим методом завжди потрібно перевіряти на `-1` і явно приводити результат до `(char)` для отримання символу.

#### **📌 Що перевіряють:**

*Розуміння дизайну API `java.io` та причин, чому для читання символів використовується тип `int`.*

* * *

### **5 ❓ Питання:**

Напишіть код, що читає вміст файлу за допомогою `FileReader` та `BufferedReader`.

> #### **💬 Відповідь:**
>
> Звісно. Найкраща практика — використовувати `try-with-resources` для автоматичного закриття потоків.
>
> ```java
> import java.io.BufferedReader;
> import java.io.FileReader;
> import java.io.IOException;
>
> public class ReadWithBufferedReader {
>     public static void main(String[] args) {
>         String filePath = "my_file.txt";
>
>         // try-with-resources автоматично закриє і reader, і bufferedReader
>         try (BufferedReader bufferedReader = new BufferedReader(new FileReader(filePath))) {
>             String line;
>             System.out.println("Вміст файлу:");
>
>             // читаємо файл рядок за рядком, поки не дійдемо до кінця
>             while ((line = bufferedReader.readLine()) != null) {
>                 System.out.println(line);
>             }
>         } catch (IOException e) {
>             System.err.println("Сталася помилка при читанні файлу: " + e.getMessage());
>         }
>     }
> }
> ```
>
> **Пояснення коду:**
>
> 1. `new FileReader(filePath)` створює потік для читання символів з файлу.
> 2. `new BufferedReader(...)` "обгортає" `FileReader`, додаючи до нього ефективну буферизацію та зручний метод `readLine()`.
> 3. Цикл `while` читає кожен рядок, доки `readLine()` не поверне `null`, що сигналізує про кінець файлу.
> 4. Після завершення `try` блоку обидва потоки автоматично закриваються.

#### **📌 Що перевіряють:**

*Практичні навички написання коду для читання файлів з використанням патерну "Декоратор" та `try-with-resources`.*

* * *

### **6 ❓ Питання:**

Чому цей код може кинути `NullPointerException`?
`new FileReader(file);`
де `File file = null;` ⭐️

> #### **💬 Відповідь:**
>
> Якщо передати `null` у конструктор `FileReader` (а також `FileInputStream` чи `FileWriter`), буде кинуто **`NullPointerException`**.
>
> **Пояснення:**
>
> * Конструктори цих класів внутрішньо перевіряють свої вхідні параметри.
> * `new FileReader(File file)` очікує отримати валідний об'єкт `File`, що представляє шлях у файловій системі.
> * Якщо замість цього передати `null`, це є **програмерською помилкою** — порушенням контракту конструктора.
> * В таких випадках, згідно з найкращими практиками Java, слід кидати `NullPointerException` (який є `RuntimeException`), щоб негайно сигналізувати про баг.
>
> Цей виняток є `unchecked`, тому що його слід **попереджати**, а не обробляти. Розробник повинен був перевірити, що об'єкт `File` не є `null`, перед тим, як передавати його в конструктор.

#### **📌 Що перевіряють:**

*Знання контрактів стандартних класів та розуміння того, які помилки призводять до `RuntimeException`.*

* * *

### **7 ❓ Питання:**

Яким чином `FileReader` пов'язаний з `FileInputStream` та `InputStreamReader`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `FileReader` — це, по суті, **клас для зручності**, який є комбінацією `FileInputStream` та `InputStreamReader`.
>
> * **`FileInputStream`:** Це **байтовий** потік. Він читає "сирі" байти з файлу.
>
> * **`InputStreamReader`:** Це **клас-"міст" (`bridge`)**, який "обгортає" байтовий потік (як `FileInputStream`) і **перетворює** байти на **символи (`char`)**, використовуючи певне кодування.
>
> * **`FileReader`:** Клас `FileReader` **успадковується від `InputStreamReader`**. У своєму конструкторі він просто створює `new FileInputStream(fileName)` і передає його в конструктор `super()`.
>
> Таким чином, виклик:
>
> `new FileReader("file.txt")`
>
> є повністю еквівалентним (за винятком того, що ви не можете вказати кодування):
>
> `new InputStreamReader(new FileInputStream("file.txt"), Charset.defaultCharset())`
>
> Розуміння цього ланцюжка є ключовим для розуміння того, чому `FileReader` має проблеми з кодуваннями.

#### **📌 Що перевіряють:**

*Глибокі знання ієрархії потоків `java.io` та розуміння, як байтові потоки перетворюються на символьні.*

* * *

### **8 ❓ Питання:**

Які є альтернативи `FileReader` в сучасному Java (Java 7+)? Назвіть щонайменше дві. ⭐️

> #### **💬 Відповідь:**
>
> Сучасний `NIO.2 API`, введений у Java 7, надає значно кращі та гнучкіші альтернативи.
>
> **1. `Files.newBufferedReader(Path, Charset)`:**
>
> * Це **найкраща альтернатива** для читання текстових файлів рядок за рядком.
> * **Переваги:** працює з `Path`, дозволяє явно вказати кодування, одразу повертає буферизований потік.
>
> **2. `Files.readString(Path, Charset)` (з Java 11):**
>
> * Найпростіший спосіб, якщо вам потрібно прочитати **весь вміст файлу одразу в один рядок (`String`)**.
> * Дуже зручно для невеликих файлів.
> * **Недолік:** Не підходить для великих файлів, бо завантажує весь файл у пам'ять.
>
> **3. `Files.readAllLines(Path, Charset)`:**
>
> * Читає **весь файл одразу в `List<String>`**.
> * Кожен рядок списку відповідає рядку у файлі.
> * Також не підходить для дуже великих файлів.
>
> **4. `Files.lines(Path, Charset)` (з Java 8):**
>
> * Це **найбільш гнучкий та ефективний** спосіб для обробки великих файлів.
> * Повертає **`Stream<String>`**, який читає файл **"ліниво" (lazily)**, рядок за рядком. Це дозволяє обробляти файли будь-якого розміру, не завантажуючи їх повністю в пам'ять.

#### **📌 Що перевіряють:**

*Знання сучасного NIO.2 та Stream API для роботи з файлами. Кандидат повинен знати кілька способів вирішення задачі та їхні компроміси.*

* * *

### **9 ❓ Питання:**

Чи можна використовувати `FileReader` для читання бінарних файлів, таких як зображення? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, категорично не можна.** `FileReader` призначений **виключно для текстових даних**.
>
> **Чому:**
>
> * `FileReader` (через `InputStreamReader`) намагається **інтерпретувати** послідовність байтів з файлу як **символи** згідно з кодуванням платформи.
> * Бінарний файл (як `.jpg`, `.png`, `.zip`) — це не текст. Його байти не відповідають валідним символам у жодному кодуванні.
> * Коли `FileReader` спробує перетворити ці "сирі" байти в символи, відбудеться **пошкодження даних**. Ви отримаєте некоректні символи, і якщо ви спробуєте записати їх назад, ви отримаєте пошкоджений файл.
>
> **Правильний інструмент для бінарних файлів:**
>
> Для читання бінарних даних слід використовувати **байтові потоки**, наприклад, **`FileInputStream`** у поєднанні з `BufferedInputStream` для ефективності.
>
> `try (InputStream is = new BufferedInputStream(new FileInputStream("image.jpg"))) { ... }`

#### **📌 Що перевіряють:**

*Чітке розмежування між символьними та байтовими потоками та їхнім призначенням.*

* * *

### **10 ❓ Питання:**

Як `FileReader` реагує на BOM (Byte Order Mark) в UTF-8 файлах? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Стандартний `FileReader` та `InputStreamReader` (без спеціальних бібліотек) **не обробляють BOM коректно**.
>
> * **BOM** — це спеціальний невидимий символ на початку файлу (`U+FEFF`), який вказує на порядок байтів та кодування (наприклад, UTF-8, UTF-16).
> * `FileReader` **не розпізнає** BOM як метадані. Він прочитає його як **звичайний символ** ('псевдо-пробіл').
>
> **Наслідки:**
>
> Якщо ви читаєте перші символи файлу, першим символом у вас буде цей зайвий BOM-символ (`\uFEFF`), що може призвести до помилок при парсингу (наприклад, XML або JSON) або просто до зайвого символу на початку рядка.
>
> **Як вирішити:**
>
> * Вручну перевіряти перший символ і пропускати його.
> * Використовувати сторонні бібліотеки, як-от **Apache Commons IO**, які мають класи (наприклад, `BOMInputStream`), що коректно обробляють BOM.
> * За можливості, створювати файли без BOM.
>
> Це один з тонких моментів, де "зручний" `FileReader` може підвести.

#### **📌 Що перевіряють:**

*Знання тонкощів роботи з кодуваннями та поширених проблем, як-от BOM.*

* * *

### **11 ❓ Питання:**

Поясніть, що відбувається "під капотом" тут: `new FileReader("my/file")`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей один рядок насправді запускає ланцюжок дій:
>
> 1. Викликається конструктор `FileReader(String fileName)`.
> 2. Всередині цього конструктора створюється об'єкт `File` на основі переданого рядка: `new File("my/file")`.
> 3. Потім створюється **байтовий потік** `FileInputStream` для цього об'єкта `File`: `new FileInputStream(file)`. Саме на цьому етапі відбувається реальна взаємодія з файловою системою. Якщо файл не знайдено, тут буде кинуто `FileNotFoundException`.
> 4. Потім конструктор `FileReader` викликає **конструктор свого батька** (`InputStreamReader`), передаючи йому щойно створений `FileInputStream`.
> 5. `InputStreamReader` ініціалізується, використовуючи **кодування платформи за замовчуванням**, і тепер він готовий читати байти з `FileInputStream` і перетворювати їх на символи.
>
> Отже, це просто "синтаксичний цукор" над створенням ланцюжка `InputStreamReader(new FileInputStream(...))`.

#### **📌 Що перевіряють:**

*Розуміння того, як високорівневі класи `java.io` побудовані на основі більш низькорівневих.*

* * *

### **12 ❓ Питання:**

У `try-with-resources`, що буде закрито: `BufferedReader` чи внутрішній `FileReader`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> ```java
> try (BufferedReader reader = new BufferedReader(new FileReader(...))) {
>     ...
> }
> ```
>
> **Буде закрито обидва.** Але важливо розуміти, як це відбувається.
>
> 1. `try-with-resources` викликає `close()` лише на **зовнішньому** ресурсі, оголошеному в дужках, тобто на `BufferedReader`.
>
> 2. **Однак**, реалізація методу `BufferedReader.close()` спроектована так, що вона **делегує** виклик `close()` **вкладеному (wrapped) потоку**, який був переданий у конструктор.
>
> 3. Тому виклик `reader.close()` спочатку закриває сам `BufferedReader` (наприклад, очищає його внутрішній буфер), а потім викликає `close()` на `FileReader`.
>
> **Правило "Декоратора" в `java.io`:**
> **Завжди закривайте лише крайню зовнішню обгортку.** Вона сама подбає про закриття всіх внутрішніх потоків. Спроба закрити внутрішній потік окремо може призвести до помилок.

#### **📌 Що перевіряють:**

*Знання патерну "Декоратор" та правил роботи з ланцюжками потоків в `java.io`.*

* * *

### **13 ❓ Питання:**

Чому `FileNotFoundException` є `checked`, а `NoSuchFileException` (з NIO.2) також `checked`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидва є `checked`, тому що вони сигналізують про **очікувану помилку при взаємодії із зовнішньою системою (файловою системою)**, від якої клієнтський код може оговтатися.
>
> * `FileNotFoundException` є підкласом `IOException`.
> * `NoSuchFileException` (з NIO.2) є підкласом `FileSystemException`, який, у свою чергу, є підкласом `IOException`.
>
> **Філософія:**
>
> * Те, що файл може бути відсутнім за вказаним шляхом, — це **не баг у вашій програмі**, а нормальна ситуація при роботі з файлами. Користувач міг видалити його, перемістити, або ввести неправильний шлях.
> * Тому Java **змушує** розробника передбачити цей сценарій і написати логіку обробки: повідомити користувача, запропонувати створити новий файл, спробувати інший шлях тощо.
>
> Хоча API змінилося (`File` → `Path`), філософський підхід до обробки таких помилок залишився тим самим. `NoSuchFileException` просто є більш специфічною та інформативною версією `FileNotFoundException`.

#### **📌 Що перевіряють:**

*Розуміння того, чому певні помилки є `checked`, і як цей принцип зберігається у нових API.*

* * *

### **14 ❓ Питання:**

Чи можна використовувати один екземпляр `FileReader` у кількох потоках одночасно? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, це не є потокобезпечним і призведе до непередбачуваної поведінки.**
>
> Класи з пакета `java.io`, включаючи `FileReader` та `BufferedReader`, **не призначені** для одночасного використання у кількох потоках.
>
> **Проблеми, які виникнуть:**
>
> 1. **Пошкодження внутрішнього стану:** `FileReader` (та `FileInputStream`) має внутрішній "вказівник" на поточну позицію у файлі. Якщо два потоки одночасно викликають `read()`, вони будуть хаотично "смикати" цей вказівник, і кожен потік прочитає лише випадкові фрагменти даних. Дані будуть пошкоджені або перемішані.
>
> 2. **Стан гонитви (Race Condition):** Послідовність читання стане непередбачуваною.
>
> **Як правильно працювати з файлами у багатопотоковому середовищі:**
>
> * **Кожен потік повинен створювати свій власний, незалежний екземпляр `FileReader`** для того самого файлу. Операційна система коректно впорається з кількома читачами одного файлу.
> * Або потрібно використовувати **зовнішню синхронізацію** (`synchronized` блок або `Lock`) для доступу до одного спільного екземпляра `FileReader`, але це значно знизить продуктивність і зазвичай є гіршим рішенням.

#### **📌 Що перевіряють:**

*Знання аспектів багатопотоковості для стандартних класів. Кандидат повинен знати, що більшість класів `java.io` не є потокобезпечними.*

* * *

### **15 ❓ Питання:**

Як, не читаючи файл, визначити, чи `File` об'єкт представляє директорію, чи файл? Яка можлива проблема з цим? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Клас `File` надає для цього два методи:
>
> * `boolean isFile()`
> * `boolean isDirectory()`
>
> Вони повертають `true`, якщо шлях вказує на файл або директорію відповідно.
>
> **Приклад:**
>
> ```java
> File f = new File("some/path");
> if (f.isFile()) {
>     System.out.println("Це файл");
> } else if (f.isDirectory()) {
>     System.out.println("Це директорія");
> }
> ```
>
> **Можлива проблема (стан гонитви — Race Condition):**
>
> Проблема виникає у **багатозадачних середовищах**. Між моментом, коли ви **перевіряєте** тип (`isFile()`) і моментом, коли ви **використовуєте** результат (`new FileReader()`), може втрутитися інший процес.
>
> **Сценарій:**
>
> 1. Ваш код перевіряє `if (file.isFile())` і отримує `true`.
> 2. **Перед тим**, як ваш код перейде до наступного рядка, інший процес або користувач **видаляє** цей файл і створює на його місці **директорію з такою ж назвою**.
> 3. Ваш код намагається виконати `new FileReader(file)`, але тепер цей шлях вказує на директорію.
> 4. Ви отримуєте `FileNotFoundException` (або схожу помилку), хоча ваша перевірка щойно пройшла успішно.
>
> Ця проблема "Перевірка-потім-дія" (Check-then-act) є класичним прикладом стану гонитви. NIO.2 API частково вирішує деякі з цих проблем за допомогою більш атомарних операцій, але повністю уникнути цього у файловій системі важко.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що стан файлової системи може змінюватися, та вміння аналізувати код на наявність race condition.*
