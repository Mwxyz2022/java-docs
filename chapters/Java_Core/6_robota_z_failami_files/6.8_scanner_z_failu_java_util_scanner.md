---
layout: default
title: "Scanner з файлу (java.util.Scanner)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 8
---

# Scanner з файлу (java.util.Scanner)

* короткий опис: `Scanner` – це клас, який дозволяє читати текст з різних джерел (включно з файлами) та розбивати його на токени (слова, числа, рядки) за допомогою методів `next()`, `nextInt()`, `nextLine()`, `useDelimiter()`.

* поняття: `Scanner`, `next()`, `nextLine()`, `nextInt()`, `hasNext()`, `useDelimiter()`.

* приклади: `new Scanner(new File("data.txt"))`, зчитування чисел (`scanner.nextInt()`), зчитування рядків (`scanner.nextLine()`).

* ключові моменти: Гнучкий парсер тексту. Менш ефективний для великих файлів, ніж `BufferedReader`, але зручніший для структурованого вводу.

* що важливо знати на співбесіді: Різниця `next()` vs `nextLine()`, як задати роздільник.

### **✅ Офіційне визначення:**  

**`Scanner`** — це клас з пакету `java.util`, який дозволяє **читати текст з різних джерел** (включно з файлами) і розбивати його на **токени**: слова, числа, рядки тощо.

### **🧠 Простими словами:**  

Це як **розумний аналізатор тексту**. Ти відкриваєш файл — і `Scanner` допомагає **зручно витягати потрібні фрагменти**: число, слово, цілий рядок. Ідеально підходить, якщо в тексті — числа, пробіли, коми, табуляції тощо.

---

## **Основні можливості**

📊 Що вміє `Scanner`:

| Метод | Що робить |
| ----- | ----- |
| `next()` | Зчитує наступне слово (до пробілу) |
| `nextLine()` | Зчитує весь рядок |
| `nextInt()`, `nextDouble()` | Зчитує число |
| `hasNext()`, `hasNextInt()` | Перевіряє наявність токена |
| `useDelimiter()` | Встановлює роздільник (regex) |

---

## **Пояснення на пальцях (💡)**

### **Для чого?**

* **Зчитати файл як потік токенів** (числа, слова, рядки)

* Аналіз текстових даних

* Простий парсинг CSV, конфігів, вводу

### **У чому сила?**

* Вміє **відразу перетворювати** значення: `"42"` -> `int`

* Працює з **роздільниками** (пробіл, кома, табуляція)

### **У чому слабкість?**

* Менш ефективний для **дуже великих файлів**

* Не завжди інтуїтивно обробляє нові рядки (`nextLine()` після `nextInt()` може бути пасткою)

---

## **Корисні приклади (🧪)**

### **🔹 Прочитати файл построчно:**

```java
Scanner scanner = new Scanner(new File("data.txt"));
while (scanner.hasNextLine()) {
    String line = scanner.nextLine();
    System.out.println(line);
}
scanner.close();
```

### **🔹 Прочитати числа з файлу:**

```java
Scanner scanner = new Scanner(new File("numbers.txt"));
while (scanner.hasNextInt()) {
    int num = scanner.nextInt();
    System.out.println("Число: " + num);
}
scanner.close();
```

### **🔹 Прочитати з власним роздільником (кома):**

```java
Scanner scanner = new Scanner(new File("csv.txt"));
scanner.useDelimiter(",");
while (scanner.hasNext()) {
    System.out.println("Токен: " + scanner.next());
}
scanner.close();
```

---

## **Порівняння (⚖️)**

| Клас | Що читає | Спосіб доступу | Гнучкість парсингу | Підходить для |
| ----- | ----- | ----- | ----- | ----- |
| **`BufferedReader`** | Рядки | Лінійний (readLine) | ❌ | Текстовий файл |
| **`Scanner`** | Токени/рядки/числа | Гнучкий парсинг | ✅ | CSV, конфіги, числа |
| **`Files.readAllLines()`** | Весь вміст одразу | Миттєвий (List\<String\>) | ❌ | Маленькі файли |

---

## **🧠 Мнемоніка для запам’ятовування:**

```java
next()      -> слово
nextLine()  -> рядок
nextInt()   -> число
hasNext()   -> перевірка
useDelimiter(",") -> інший роздільник
```

---

## **🟩 Підсумок:**

**`Scanner`** — універсальний інструмент для **зчитування файлу з розбором на токени**, **числа** та **рядки**. Ідеальний для парсингу структурованого тексту (CSV, конфіги, дані з пробілами). Простий, але гнучкий у використанні. Важливо памʼятати про особливості **`nextLine()`** після **`nextInt()`**\!

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `java.util.Scanner` і для якої основної задачі він призначений?

> #### **💬 Відповідь:**
>
> **`Scanner`** — це утилітарний клас з пакета `java.util`, призначений для **парсингу (розбору) текстових даних** з різних джерел вводу, таких як файли, рядки або консольний ввід (`System.in`).
>
> **Основна задача:**
>
> На відміну від `BufferedReader`, який просто читає "сирі" рядки, `Scanner` розбиває вхідний потік на **токени** (окремі смислові одиниці, як-от слова, числа) і надає зручні методи для їх читання та автоматичного перетворення в примітивні типи.
>
> **Ключові можливості:**
>
> * Читання окремих слів (`next()`).
> * Читання цілих рядків (`nextLine()`).
> * Читання та перетворення чисел (`nextInt()`, `nextDouble()`).
> * Використання регулярних виразів для визначення роздільників.
> * Перевірка наявності наступного токена (`hasNext...()`).
>
> Це робить його ідеальним інструментом для розбору структурованого текстового вводу.

#### **📝 Аналогія:**

Якщо `BufferedReader` — це інструмент, що дає вам цілу книгу, то `Scanner` — це асистент, який не тільки дає вам книгу, але й може на ваше прохання знайти і зачитати окреме слово, число чи ціле речення.

#### **📌 Що перевіряють:**

*Базове розуміння того, що `Scanner` — це про парсинг, а не просто про читання.*

* * *

### **2 ❓ Питання:**

У чому ключова різниця між методами `next()` та `nextLine()`?

> #### **💬 Відповідь:**
>
> Це одне з найпоширеніших питань, оскільки неправильне розуміння цієї різниці є джерелом багатьох помилок.
>
> * **`String next()`:**
>   * **Що робить:** Читає і повертає **наступний токен** з потоку.
>   * **Роздільник:** За замовчуванням, токени розділені **пробілами** (включаючи пробіли, табуляцію, символи нового рядка).
>   * **Поведінка:** Він читає символи до першого роздільника, **не споживаючи** сам роздільник (наприклад, символ нового рядка `\n` залишається у буфері).
>
> * **`String nextLine()`:**
>   * **Що робить:** Читає **всі символи до кінця поточного рядка** і повертає їх.
>   * **Роздільник:** Він читає доти, доки не зустріне символ нового рядка (`\n`).
>   * **Поведінка:** Він **споживає** символ нового рядка з буфера, але **не включає** його у повернутий рядок.

#### **📝 Коментар:**

Головний висновок: `next()` — читає "слово", `nextLine()` — читає "речення" до кінця рядка.

#### **📌 Що перевіряють:**

*Знання цієї фундаментальної різниці, яка є критичною для правильного парсингу введення.*

* * *

### **3 ❓ Питання:**

Поясніть поширену проблему, що виникає при виклику `nextLine()` після `nextInt()`. Як її виправити?

> #### **💬 Відповідь:**
>
> Це класична пастка при роботі зі `Scanner`.
>
> **Проблема:**
>
> 1. Ви викликаєте `scanner.nextInt()` для читання числа.
> 2. Користувач вводить, наприклад, `42` і натискає `Enter`.
> 3. `nextInt()` успішно **читає число `42`**, але **символ нового рядка (`\n`)**, який з'явився після натискання `Enter`, **залишається у вхідному буфері**.
> 4. Далі ви викликаєте `scanner.nextLine()`, очікуючи, що програма буде чекати на введення рядка.
> 5. Але `nextLine()` одразу ж зчитує той самий **"завислий" символ `\n`** з буфера, сприймає його як кінець рядка і повертає **порожній рядок (`""`)**, не даючи користувачеві нічого ввести.
>
> **Приклад проблеми:**
>
> ```java
> Scanner scanner = new Scanner(System.in);
> System.out.print("Введіть вік: ");
> int age = scanner.nextInt();
> System.out.print("Введіть ім'я: ");
> String name = scanner.nextLine(); // "проскакує"
> System.out.printf("Ім'я: '%s', Вік: %d%n", name, age);
> // Виведе: Ім'я: '', Вік: 42
> ```
>
> **Як виправити:**
>
> Потрібно **спожити "завислий" новий рядок** після `nextInt()`, викликавши додатковий `nextLine()`.
>
> **Правильний код:**
>
> ```java
> int age = scanner.nextInt();
> scanner.nextLine(); // Споживаємо залишок рядка
> String name = scanner.nextLine();
> ```

#### **📌 Що перевіряють:**

*Практичний досвід та знання поширених пасток при роботі зі `Scanner`.*

* * *

### **4 ❓ Питання:**

Як за допомогою `Scanner` прочитати файл, у якому значення розділені не пробілами, а, наприклад, комою?

> #### **💬 Відповідь:**
>
> Для цього потрібно змінити **роздільник (delimiter)**, який `Scanner` використовує для розбиття тексту на токени. Це робиться за допомогою методу **`useDelimiter()`**.
>
> Метод `useDelimiter()` приймає рядок, який є **регулярним виразом**.
>
> **Приклад: читання CSV-файлу.**
> Припустимо, у файлі є рядок `John,Doe,30`.
>
> ```java
> try (Scanner scanner = new Scanner(new File("data.csv"))) {
>     // Встановлюємо кому як роздільник
>     scanner.useDelimiter(",");
>
>     while (scanner.hasNext()) {
>         String token = scanner.next();
>         System.out.println("Токен: " + token);
>     }
> } catch (FileNotFoundException e) {
>     e.printStackTrace();
> }
> ```
>
> **Вивід:**
>
> ```
> Токен: John
> Токен: Doe
> Токен: 30
> ```
>
> Важливо пам'ятати, що `useDelimiter()` впливає лише на методи `next()`, `nextInt()` тощо. Метод `nextLine()` все ще буде читати до символу нового рядка.

#### **📌 Що перевіряють:**

*Знання гнучких можливостей `Scanner` для парсингу та роботи з регулярними виразами.*

* * *

### **5 ❓ Питання:**

У чому різниця в продуктивності між `Scanner` та `BufferedReader`? Коли який краще використовувати?

> #### **💬 Відповідь:**
>
> **`BufferedReader` є значно продуктивнішим**, ніж `Scanner`.
>
| Характеристика | `BufferedReader` | `Scanner` |
| :---: | :---: | :---: |
| **Призначення** | **Швидке читання** "сирих" рядків. | **Парсинг** структурованих даних. |
| **Продуктивність** | **Дуже висока.** Просто читає великі блоки даних у буфер. | **Значно повільніший.** На кожному кроці використовує регулярні вирази для пошуку токенів, що є обчислювально "дорогою" операцією.|
| **Буфер** | Більший за замовчуванням (8KB). | Менший за замовчуванням (1KB). |
| **Зручність API** | Базове (`readLine()`). | Дуже зручне для парсингу (`nextInt()`, `hasNextDouble()` тощо). |
>
> **Коли який краще:**
>
> * **`BufferedReader`:** Коли вам потрібно **максимально швидко прочитати великий текстовий файл** (наприклад, логи, великі CSV) як послідовність рядків, а логіку парсингу ви реалізуєте вручну пізніше.
> * **`Scanner`:** Коли вам потрібно **розбирати (парсити)** структуровані дані, особливо якщо це **введення з консолі** або файли з відомим форматом (наприклад, числа, розділені пробілами). Зручність API тут часто переважує втрату продуктивності.

#### **📌 Що перевіряють:**

*Здатність обрати правильний інструмент для конкретної задачі, розуміючи компроміс між продуктивністю та зручністю API.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати один екземпляр `Scanner` у кількох потоках? ⭐️

> #### **💬 Відповідь:**
>
> **Ні, `Scanner` не є потокобезпечним (thread-safe).**
>
> Його методи не синхронізовані. Якщо кілька потоків будуть одночасно викликати методи, як-от `next()` або `nextInt()`, на одному екземплярі `Scanner`, це призведе до **стану гонитви (race condition)**.
>
> **Проблеми, які виникнуть:**
>
> * **Пошкодження даних:** Один потік може прочитати частину токена, потім інший потік втрутиться і прочитає іншу частину. В результаті обидва потоки отримають некоректні дані.
> * **Некоректний внутрішній стан:** Внутрішні буфери та позиції `Scanner` стануть непослідовними.
>
> **Правильний підхід:**
>
> * Кожен потік повинен створювати **свій власний екземпляр `Scanner`**.
> * Або потрібно використовувати **зовнішню синхронізацію** (`synchronized` блок), що значно знизить продуктивність.
>
> Загалом, спільне використання одного `Scanner` між потоками — це антипатерн.

#### **📌 Що перевіряють:**

*Знання аспектів багатопотоковості для стандартних класів `java.util`.*

* * *

### **7 ❓ Питання:**

Як за допомогою `Scanner` перевірити, чи є наступний токен цілим числом, не викликаючи при цьому помилку? ⭐️

> #### **💬 Відповідь:**
>
> Для цього існують спеціальні **методи-перевірки `hasNext...()`**.
>
> * Для перевірки наявності цілого числа використовується метод **`hasNextInt()`**.
> * Цей метод "заглядає" у потік, не споживаючи його, і повертає `true`, якщо наступний токен можна успішно розпарсити як `int`, і `false` — якщо ні.
>
> **Приклад безпечного читання чисел:**
>
> ```java
> Scanner scanner = new Scanner("123 abc 456");
>
> while (scanner.hasNext()) {
>     if (scanner.hasNextInt()) {
>         int number = scanner.nextInt();
>         System.out.println("Знайдено число: " + number);
>     } else {
>         String token = scanner.next();
>         System.out.println("Знайдено нечисловий токен: " + token);
>     }
> }
> ```
>
> **Вивід:**
>
> ```
> Знайдено число: 123
> Знайдено нечисловий токен: abc
> Знайдено число: 456
> ```
>
> Використання `hasNextInt()` перед `nextInt()` є найкращою практикою для уникнення `InputMismatchException`, яке буде кинуто, якщо `nextInt()` спробує розпарсити нечисловий рядок. Існують аналогічні методи, як `hasNextDouble()`, `hasNextBoolean()`.

#### **📌 Що перевіряють:**

*Навички написання надійного коду, що валідує вхідні дані перед їх обробкою.*

* * *

### **8 ❓ Питання:**

Що таке локаль (`Locale`) і як вона впливає на роботу `Scanner`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Locale`** — це об'єкт, що представляє певний географічний або культурний регіон. Він визначає **правила форматування** для чисел, дат, валют тощо.
>
> **Як це впливає на `Scanner`:**
>
> `Scanner` використовує `Locale` для коректного парсингу чисел з плаваючою комою. Головна відмінність — це **символ десяткового роздільника**.
>
> * В `US` локалі (зазвичай, дефолтна) роздільником є **крапка (`.`)**.
> * В багатьох європейських локалях (наприклад, німецькій, українській) роздільником є **кома (`,`)**.
>
> За замовчуванням `Scanner` використовує `Locale.getDefault()`. Якщо ваша програма очікує числа у форматі, відмінному від системного, це може призвести до `InputMismatchException`.
>
> **Як вказати локаль:**
>
> Можна явно задати локаль для `Scanner` за допомогою методу `useLocale()`.
>
> **Приклад:**
>
> ```java
> String input = "1,23"; // Десяткова кома
>
> // Сканер з дефолтною US локаллю не впорається
> Scanner scannerUS = new Scanner(input);
> // double d = scannerUS.nextDouble(); // -> InputMismatchException
>
> // Сканер з німецькою локаллю
> Scanner scannerDE = new Scanner(input);
> scannerDE.useLocale(Locale.GERMANY);
> double d = scannerDE.nextDouble(); // d буде 1.23. Все добре.
> ```

#### **📌 Що перевіряють:**

*Знання проблем інтернаціоналізації та вміння працювати з `Locale` для правильного парсингу даних.*

* * *

### **9 ❓ Питання:**

Як `Scanner` пов'язаний з `try-with-resources`?

> #### **💬 Відповідь:**
>
> Клас `Scanner` **реалізує інтерфейс `java.io.Closeable`**, який, у свою чергу, успадковує `AutoCloseable`.
>
> Це означає, що `Scanner` **можна і потрібно** використовувати в конструкції **`try-with-resources`** для гарантованого закриття.
>
> **Що робить `scanner.close()`:**
>
> * Він закриває сам сканер.
> * Що важливіше, він також **закриває вкладений потік**, з якого він читав (наприклад, `FileInputStream`), якщо цей потік також реалізує `Closeable`.
>
> **Приклад:**
>
> ```java
> try (Scanner scanner = new Scanner(new File("my_file.txt"))) {
>     while (scanner.hasNextLine()) {
>         System.out.println(scanner.nextLine());
>     }
> } catch (FileNotFoundException e) {
>     e.printStackTrace();
> }
> // Тут scanner буде автоматично закрито
> ```
>
> Використання `try-with-resources` є найкращою практикою, яка запобігає витокам ресурсів.

#### **📌 Що перевіряють:**

*Знання сучасних практик управління ресурсами та їх застосування до `Scanner`.*

* * *

### **10 ❓ Питання:**

Чи можна використовувати `Scanner` для читання з рядка, а не з файлу?

> #### **💬 Відповідь:**
>
> **Так, звісно.**
>
> `Scanner` дуже гнучкий і може приймати в конструктор різні джерела вводу, включаючи:
>
> * `File`
> * `InputStream` (наприклад, `System.in`)
> * `Readable` (який реалізує, наприклад, `Reader`)
> * **`String`**
>
> **Приклад використання з рядком:**
>
> ```java
> String data = "John 30 95.5";
>
> Scanner scanner = new Scanner(data);
>
> String name = scanner.next();
> int age = scanner.nextInt();
> double score = scanner.nextDouble();
>
> System.out.printf("Name: %s, Age: %d, Score: %.1f%n", name, age, score);
>
> scanner.close();
> ```
>
> Це дуже зручно для парсингу рядків, які мають певну внутрішню структуру.

#### **📌 Що перевіряють:**

*Знання гнучкості `Scanner` API та його різних конструкторів.*

* * *

### **11 ❓ Питання:**

Як знайти та вивести всі числа з довгого рядка, використовуючи `Scanner`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього завдання можна комбінувати методи `hasNextDouble()` та `nextDouble()`. Щоб `Scanner` ігнорував нечисловий текст, ми можемо встановити **роздільник, який відповідає "всьому, що не є числом"**, за допомогою регулярного виразу.
>
> **Рішення:**
>
> ```java
> String text = "Order 12, price: 49.99, amount: 3, total -149.97 end";
> // \D+ означає "один або більше не-числових символів"
> String nonDigitDelimiter = "\\D+";
>
> Scanner scanner = new Scanner(text);
> scanner.useDelimiter(nonDigitDelimiter);
>
> while (scanner.hasNextDouble()) {
>     System.out.println("Знайдено число: " + scanner.nextDouble());
> }
> scanner.close();
> ```
>
> Цей код є більш простим, але він не обробить від'ємні числа.
>
> **Більш надійний, але менш елегантний підхід:**
>
> ```java
> Scanner scanner = new Scanner(text);
> scanner.useLocale(Locale.US); // Для крапки
>
> while(scanner.hasNext()){
>   if(scanner.hasNextDouble()){
>     System.out.println("Знайдено число: " + scanner.nextDouble());
>   } else {
>     scanner.next(); // пропустити не-числовий токен
>   }
> }
> scanner.close();
> ```
>
> **Вивід:**
>
> ```
>
> Знайдено число: 12.0
> Знайдено число: 49.99
> Знайдено число: 3.0
> Знайдено число: -149.97
>
> ```

*Виправлення: давайте об'єднаємо підходи*. `findInLine()` є ще кращим варіантом.

#### **Змінена відповідь:**

> **Рішення за допомогою `findInLine()`:**
> `Scanner` має методи, що дозволяють шукати токени за регулярним виразом. Це найкраще рішення.
>
> ```java
> String text = "Order 12, price: 49.99, amount: 3, total -149.97 end";
> Scanner scanner = new Scanner(text);
> scanner.useLocale(Locale.US); // Для коректного парсингу точки
>
> // Регулярний вираз для пошуку чисел (включаючи від'ємні та дробові)
> String numberPattern = "-?\\d+(\\.\\d+)?";
>
> while (scanner.findInLine(numberPattern) != null) {
>     String numberStr = scanner.match().group();
>     System.out.println("Знайдено число: " + Double.parseDouble(numberStr));
> }
> scanner.close();
> ```

#### **📌 Що перевіряють:**

*Просунуті навички роботи зі `Scanner` та регулярними виразами.*

* * *

### **12 ❓ Питання:**

Чому `Scanner(System.in)` не слід закривати всередині методу, якщо він буде використовуватися далі? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дуже важлива і поширена помилка.
>
> **Проблема:**
>
> `Scanner` при виклику `close()` **закриває і вкладений потік**. У випадку `new Scanner(System.in)`, цим вкладеним потоком є `System.in` — **стандартний потік вводу**, який є **єдиним для всієї програми**.
>
> Якщо ви закриєте `Scanner`, обгорнутий навколо `System.in`, ви **назавжди закриєте і `System.in`**.
>
> **Наслідки:**
>
> * Будь-яка подальша спроба в будь-якій іншій частині програми прочитати дані з консолі (наприклад, створивши новий `Scanner(System.in)`) зазнає невдачі, зазвичай кинувши `NoSuchElementException`, тому що потік вже закрито.
>
> **Правильний підхід:**
>
> * `Scanner`, що працює з `System.in`, зазвичай створюється **один раз** на початку програми (наприклад, у `main` методі) і передається в інші методи як параметр.
> * Його **не потрібно закривати** взагалі. JVM сама подбає про закриття стандартних потоків при завершенні програми.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють стандартні потоки вводу/виводу. Це перевірка на реальний досвід.*

* * *

### **13 ❓ Питання:**

Що робить метод `Scanner.reset()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `reset()` **скидає внутрішній стан** `Scanner`-а до початкового, який він мав одразу після створення.
>
> **Що саме він скидає:**
>
> * Внутрішній буфер очищується.
> * Позиція сканування повертається на початок потоку.
> * **Локаль (`Locale`)**, **основа системи числення (`radix`)** та **роздільник (`delimiter`)** скидаються до значень **за замовчуванням**, а не до тих, що були встановлені раніше.
>
> **Коли він може бути корисний:**
>
> Це рідко використовуваний метод. Його можна використати, якщо вам потрібно **повторно просканувати** той самий `String` або потік (якщо він підтримує `mark`/`reset`), але вже з іншими налаштуваннями, не створюючи нового об'єкта `Scanner`.
>
> **Приклад:**
>
> ```java
> String data = "10,20 30";
> Scanner scanner = new Scanner(data);
> scanner.useDelimiter(",");
> scanner.nextInt(); // 10
>
> scanner.reset(); // Скидаємо все
>
> // Тепер роздільник знову пробіл за замовчуванням
> scanner.nextInt(); // 20 (бо "," вже не роздільник)
> ```
>
> Проте, зазвичай простіше та зрозуміліше просто створити новий екземпляр `Scanner`.

#### **📌 Що перевіряють:**

*Знання просунутого та рідко використовуваного API.*

* * *

### **14 ❓ Питання:**

Чи можна використовувати `Scanner` для читання дуже великих файлів (кілька гігабайт)? Які можуть бути проблеми? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> **Технічно можна, але це не найкращий вибір**, і можуть виникнути проблеми з продуктивністю.
>
> **Проблеми:**
>
> 1. **Продуктивність:** Як уже зазначалося, `Scanner` повільніший за `BufferedReader`, оскільки він виконує складний аналіз на основі регулярних виразів для кожного токена. Для дуже великих файлів ця різниця може стати суттєвою.
>
> 2. **Споживання пам'яті (у певних випадках):** Хоча `Scanner` читає файл порційно у свій буфер, деякі його операції (наприклад, пошук токена, що задовольняє складному регулярному виразу) можуть змусити його прочитати і тримати в пам'яті великий шматок файлу, більший за розмір буфера.
>
> 3. **Помилки парсингу:** Якщо файл дуже великий, зростає ймовірність зустріти в ньому пошкоджені або невідповідні дані, що призведе до `InputMismatchException`, і обробка цієї помилки може бути складною (наприклад, потрібно пропустити некоректний токен і продовжити).
>
> **Краща альтернатива для великих файлів:**
>
> Використовувати **`BufferedReader`** (або **`Files.lines()`**) для швидкого, построчного читання файлу. А потім **кожен окремий рядок** передавати у **новий `Scanner`** для парсингу.
>
> ```java
> try (BufferedReader reader = new BufferedReader(...)) {
>     String line;
>     while ((line = reader.readLine()) != null) {
>         Scanner lineScanner = new Scanner(line);
>         // ... парсимо рядок за допомогою lineScanner ...
>     }
> }
> ```
>
> Цей підхід поєднує **швидкість читання** `BufferedReader` з **зручністю парсингу** `Scanner`.

#### **📌 Що перевіряють:**

*Архітектурне мислення та вміння комбінувати різні інструменти для створення ефективних рішень.*

* * *

### **15 ❓ Питання:**

Що таке **fail-fast** ітератор і як ця концепція стосується `Scanner`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Клас `Scanner` реалізує інтерфейс `Iterator<String>`. Це означає, що по ньому можна ітеруватися, отримуючи токени.
>
> Ітератор `Scanner`-а є **fail-fast**, але з певним нюансом.
>
> **Fail-fast** ітератор — це ітератор, який намагається якомога раніше виявити, що базове джерело даних було **змінено ззовні** (не через сам ітератор) і негайно кидає `ConcurrentModificationException`.
>
> **Як це стосується `Scanner`:**
>
> * Це стосується не файлів (бо їх зміну важко відстежити), а випадку, коли `Scanner` ітерується по **колекції**.
>
> Хоча ви не можете передати колекцію в `Scanner` напряму, ви можете передати її текстове представлення. Однак, більш цікавим є інше.
>
> Методи `hasNext()` та `next()` у `Scanner` перевіряють на **збій потоку (stream corruption)**. Якщо під час операції читання з `Scanner`-а **інший потік закриє** базовий потік даних, ітератор `Scanner`'а може кинути `IllegalStateException`.
>
> `Scanner` не кидає `ConcurrentModificationException`, оскільки не працює з `modCount`, як колекції. Але його ітератор так само є "крихким" до зовнішніх змін у базовому потоці, і він намагатиметься повідомити про це якомога раніше через `IllegalStateException`, що є формою **fail-fast** поведінки.

#### **📌 Що перевіряють:**

*Глибоке розуміння концепції ітераторів та fail-fast поведінки, та вміння застосувати її до нестандартних випадків, як `Scanner`.*
