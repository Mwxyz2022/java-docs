---
layout: default
title: "Клас File (java.io.File)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 1
---

# Клас File (java.io.File)

*   короткий опис: Клас `java.io.File` представляє шлях до файлу або директорії в файловій системі. Дозволяє маніпулювати файлами/папками (створювати, видаляти, перейменовувати, перевіряти існування, отримувати атрибути), але не працює з їхнім вмістом.
*   поняття: `java.io.File`, шлях, директорія, `createNewFile()`, `mkdir()`, `mkdirs()`, `exists()`, `delete()`, `getAbsolutePath()`, `isFile()`, `isDirectory()`.
*   приклади: Створення файлу (`file.createNewFile()`), створення папки (`folder.mkdir()`), перевірка існування (`file.exists()`).
*   ключові моменти: `File` – це лише метаінформація про файл/папку, а не самі дані.
*   що важливо знати на співбесіді: Призначення `File` класу, основні методи.

##### ✅ **Офіційне визначення:**

**`java.io.File`** — це клас, який представляє шлях до файлу або директорії в файловій системі. Він дозволяє створювати, видаляти, перейменовувати файли та папки, перевіряти їх наявність, атрибути, шлях тощо.

### **🧠 Простими словами:**

Це як "указка", що вказує на якийсь файл або папку. З ним можна дізнатись: чи існує файл, яке його ім’я, створити його або видалити. Але **не для читання чи запису даних** — тільки для **маніпуляції з файлами/папками**.

---

## ** Основні методи та категорії**

##### 📊 Таблиця ключових методів:

| Категорія | Метод | Для чого використовується |
| :---: | :---: | ----- |
| 🔧 Створення /видалення | **`createNewFile()`** | Створити новий файл (повертає **`true`**/**`false`**) |
|  | **`mkdir(), mkdirs()`** | Створити папку (одну / з батьківськими) |
|  | **`delete()`** | Видалити файл або пусту директорію |
| 📍 Шлях/назва | **`getName(), getPath()`** | Отримати назву або шлях |
|  | **`getAbsolutePath()`** | Повний шлях до файлу |
| 🔍 Перевірки | **`exists()`** | Чи існує файл/директорія |
|  | **`isFile(), isDirectory()`** | Чи це файл або директорія |
|  | **`canRead(), canWrite()`** | Перевірка прав доступу |
| 📦 Інше | **`length()`** | Розмір у байтах |
|  | **`renameTo(File dest)`** | Перейменувати або перемістити файл |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**


* Щоб **створити файл або папку**, якщо їх ще немає
* Щоб **перевірити, чи існує файл**
* Щоб **отримати повний шлях**
* Щоб **видалити тимчасові файли або лог-файли**
* Щоб **передати файл у метод** (наприклад, у **`FileReader`**, **`Scanner`**)

#### **У чому особливість?**

* **`File`** не містить самих даних\! **Лише** "**схему**": шлях \+ інформація про об’єкт
* Щоб працювати з файлами **вмісту** — використовуй **`FileReader`**, **`BufferedReader`** тощо

#### **Які граблі?**

* **`createNewFile()`** не створює батьківські папки — треба **`mkdirs()`**
* **`delete()`** не видалить непорожню директорію
* Шляхи працюють по-різному в Windows / Linux -> краще **`File.separator`** або **`Paths.get(...)`**

#### **Коли застосовувати?**

* Перед створенням нового файлу перевірити: **`if (!file.exists())`**
* При роботі з кодуванням — краще передавати **`File`**, а не просто рядок

---

## **Корисні приклади (🧪)**

### **🔹 Створити новий файл:**

```java
File file = new File("myFile.txt");
if (file.createNewFile()) {
    System.out.println("Файл створено!");
} else {
    System.out.println("Файл вже існує.");
}
```
### **🔹 Створити директорію:**

```java
File folder = new File("data");
folder.mkdir(); // або folder.mkdirs() -- створить з батьківськими
```
### **🔹 Перевірка існування та отримання шляху:**
```java
if (file.exists()) {
    System.out.println("Шлях: " + file.getAbsolutePath());
    System.out.println("Чи файл: " + file.isFile());
    System.out.println("Чи можна читати: " + file.canRead());
}
```
### **🔹 Видалення:**

```java
if (file.delete()) {
    System.out.println("Файл видалено");
}
```
### **🔹 Перейменування або переміщення:**

```java
File oldFile = new File("old.txt");
File newFile = new File("archive/new.txt");
oldFile.renameTo(newFile);
```
---

## **Порівняння (⚖️)**

| Операція | Метод Java | Альтернатива (NIO) |
| :---: | :---: | :---: |
| Створити файл | **`createNewFile()`** | **`Files.createFile(path)`** |
| Створити папку | **`mkdir()`** | **`Files.createDirectory(path)`** |
| Перевірити існування | **`exists()`** | **`Files.exists(path)`** |
| Отримати шлях | **`getAbsolutePath()`** | **`Path.toAbsolutePath()`** |

---

## **FAQ — Часті запитання (❓)**

### **❓ Питання:**

Чи створює **`new File("file.txt")`** сам файл?

### **💬 Відповідь:**

Ні, це просто створює об’єкт **`File`**, файл ще не існує. Його потрібно створити через **`createNewFile()`.

### **❓ Питання:**

Чим відрізняється **`mkdir()`** від **`mkdirs()`?

### **💬 Відповідь:**

**`mkdir()`** створює лише одну папку. **`mkdirs()`** — усю ієрархію, якщо потрібно.

### **❓ Питання:**

Як дізнатися розмір файлу?

### **💬 Відповідь:**

`file.length()` — повертає розмір у байтах.

### **❓ Питання:**

Як отримати абсолютний шлях до файлу?

### **💬 Відповідь:**

`file.getAbsolutePath()`

---

## **🧠 Мнемоніка для запам’ятовування**

```java
File = "вказівник на файл"
Тільки "дивиться", не читає / не пише вміст!

file.exists() -> чи є файл?
file.createNewFile() -> створити
file.delete() -> видалити
file.getAbsolutePath() -> повний шлях
file.mkdir() / mkdirs() -> створити папку.
```
---

## **🟩 Підсумок:**

Клас `File` у Java — це базовий інструмент для **роботи з файлами та папками на рівні файлової системи**. Він дозволяє створювати, перевіряти, видаляти, отримувати атрибути, але **не читає і не записує вміст** — для цього існують інші класи (`FileReader`, `Scanner`, `BufferedWriter`). Обов’язково вміти його використовувати для задач перевірки наявності та управління ресурсами.

---

## **9\. Приклад Коду:**

```java
public static void main(String[] args) {
       try {
//            // 1.ALL Created files Obj File + .createNewFile()
//            File file = new File("text.txt");
//            file.createNewFile();
           
           // 1. Create Folder: files Obj File
           File folder  = new File("myFolder");
           folder.mkdir();
           
           // 2. Create path at file
           String filePath = "myFolder" + File.separator + "myFile.txt";
           
           // 3. Create Obj file
           File file = new File(filePath);
           file.createNewFile();
           
           /** File.separator:
            *
            * Windows - '\\'
            * OS, Linux - '/'
            *
            * **/
           
       } catch (IOException e) {
           throw new RuntimeException("Can`t create file!",e);
       }
   }

```