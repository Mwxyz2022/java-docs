---
layout: default
title: "Клас File (java.io.File)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 1
---

# Клас File (java.io.File)

* короткий опис: Клас `java.io.File` представляє шлях до файлу або директорії в файловій системі. Дозволяє маніпулювати файлами/папками (створювати, видаляти, перейменовувати, перевіряти існування, отримувати атрибути), але не працює з їхнім вмістом.

* поняття: `java.io.File`, шлях, директорія, `createNewFile()`, `mkdir()`, `mkdirs()`, `exists()`, `delete()`, `getAbsolutePath()`, `isFile()`, `isDirectory()`.

* приклади: Створення файлу (`file.createNewFile()`), створення папки (`folder.mkdir()`), перевірка існування (`file.exists()`).

* ключові моменти: `File` – це лише метаінформація про файл/папку, а не самі дані.

* що важливо знати на співбесіді: Призначення `File` класу, основні методи.

### **✅ Офіційне визначення:**

**`java.io.File`** — це клас, який представляє шлях до файлу або директорії в файловій системі. Він дозволяє створювати, видаляти, перейменовувати файли та папки, перевіряти їх наявність, атрибути, шлях тощо.

### **🧠 Простими словами:**

Це як "указка", що вказує на якийсь файл або папку. З ним можна дізнатись: чи існує файл, яке його ім’я, створити його або видалити. Але **не для читання чи запису даних** — тільки для **маніпуляції з файлами/папками**.

---

## **Основні методи та категорії**

##### 📊 Таблиця ключових методів

| Категорія | Метод | Для чого використовується |
| :---: | :---: | ----- |
| 🔧 Створення /видалення | **`createNewFile()`** | Створити новий файл (повертає **`true`**/**`false`**) |
|  | **`mkdir(), mkdirs()`** | Створити папку (одну / з батьківськими) |
|  | **`delete()`** | Видалити файл або пусту директорію |
| 📍 Шлях/назва | **`getName(), getPath()`** | Отримати назву або шлях |
|  | **`getAbsolutePath()`** | Повний шлях до файлу |
| 🔍 Перевірки | **`exists()`** | Чи існує файл/директорія |
|  | **`isFile(), isDirectory()`** | Чи це файл або директорія |
|  | **`canRead(), canWrite()`** | Перевірка прав доступу |
| 📦 Інше | **`length()`** | Розмір у байтах |
|  | **`renameTo(File dest)`** | Перейменувати або перемістити файл |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

* Щоб **створити файл або папку**, якщо їх ще немає
* Щоб **перевірити, чи існує файл**
* Щоб **отримати повний шлях**
* Щоб **видалити тимчасові файли або лог-файли**
* Щоб **передати файл у метод** (наприклад, у **`FileReader`**, **`Scanner`**)

#### **У чому особливість?**

* **`File`** не містить самих даних\! **Лише** "**схему**": шлях \+ інформація про об’єкт
* Щоб працювати з файлами **вмісту** — використовуй **`FileReader`**, **`BufferedReader`** тощо

#### **Які граблі?**

* **`createNewFile()`** не створює батьківські папки — треба **`mkdirs()`**
* **`delete()`** не видалить непорожню директорію
* Шляхи працюють по-різному в Windows / Linux -> краще **`File.separator`** або **`Paths.get(...)`**

#### **Коли застосовувати?**

* Перед створенням нового файлу перевірити: **`if (!file.exists())`**
* При роботі з кодуванням — краще передавати **`File`**, а не просто рядок

---

## **Корисні приклади (🧪)**

### **🔹 Створити новий файл:**

```java
File file = new File("myFile.txt");
if (file.createNewFile()) {
    System.out.println("Файл створено!");
} else {
    System.out.println("Файл вже існує.");
}
```

### **🔹 Створити директорію:**

```java
File folder = new File("data");
folder.mkdir(); // або folder.mkdirs() -- створить з батьківськими
```

### **🔹 Перевірка існування та отримання шляху:**

```java
if (file.exists()) {
    System.out.println("Шлях: " + file.getAbsolutePath());
    System.out.println("Чи файл: " + file.isFile());
    System.out.println("Чи можна читати: " + file.canRead());
}
```

### **🔹 Видалення:**

```java
if (file.delete()) {
    System.out.println("Файл видалено");
}
```

### **🔹 Перейменування або переміщення:**

```java
File oldFile = new File("old.txt");
File newFile = new File("archive/new.txt");
oldFile.renameTo(newFile);
```

---

## **Порівняння (⚖️)**

| Операція | Метод Java | Альтернатива (NIO) |
| :---: | :---: | :---: |
| Створити файл | **`createNewFile()`** | **`Files.createFile(path)`** |
| Створити папку | **`mkdir()`** | **`Files.createDirectory(path)`** |
| Перевірити існування | **`exists()`** | **`Files.exists(path)`** |
| Отримати шлях | **`getAbsolutePath()`** | **`Path.toAbsolutePath()`** |

---

## **🧠 Мнемоніка для запам’ятовування**

```java
File = "вказівник на файл"
Тільки "дивиться", не читає / не пише вміст!

file.exists() -> чи є файл?
file.createNewFile() -> створити
file.delete() -> видалити
file.getAbsolutePath() -> повний шлях
file.mkdir() / mkdirs() -> створити папку.
```

---

## **🟩 Підсумок:**

Клас `File` у Java — це базовий інструмент для **роботи з файлами та папками на рівні файлової системи**. Він дозволяє створювати, перевіряти, видаляти, отримувати атрибути, але **не читає і не записує вміст** — для цього існують інші класи (`FileReader`, `Scanner`, `BufferedWriter`). Обов’язково вміти його використовувати для задач перевірки наявності та управління ресурсами.

---

## **9\. Приклад Коду:**

```java
public static void main(String[] args) {
       try {
//            // 1.ALL Created files Obj File + .createNewFile()
//            File file = new File("text.txt");
//            file.createNewFile();
           
           // 1. Create Folder: files Obj File
           File folder  = new File("myFolder");
           folder.mkdir();
           
           // 2. Create path at file
           String filePath = "myFolder" + File.separator + "myFile.txt";
           
           // 3. Create Obj file
           File file = new File(filePath);
           file.createNewFile();
           
           /** File.separator:
            *
            * Windows - '\\'
            * OS, Linux - '/'
            *
            * **/
           
       } catch (IOException e) {
           throw new RuntimeException("Can`t create file!",e);
       }
   }

```

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке клас `java.io.File` і яка його основна мета? Чим він не є?

> #### **💬 Відповідь:**
>
> Клас **`java.io.File`** — це абстрактне представлення **шляху** до файлу або директорії у файловій системі.
>
> **Основна мета:**
> Надавати методи для роботи з **метаінформацією та структурою** файлової системи. Це включає:
>
> * Створення, видалення та перейменування файлів і директорій.
> * Перевірку існування, прав доступу (`read`, `write`, `execute`).
> * Отримання атрибутів (розмір, дата останньої зміни).
> * Роботу з шляхами (отримання абсолютного шляху, імені батьківської директорії тощо).
>
> **Чим `File` НЕ є:**
> Він **НЕ є вмістом файлу**. Об'єкт `File` не містить у собі даних, що зберігаються у файлі, і не надає методів для **читання або запису** цих даних. Для цього використовуються потоки (`FileInputStream`, `FileReader`) або канали (`FileChannel`), які часто приймають об'єкт `File` у свій конструктор.

#### **📝 Аналогія:**

Об'єкт `File` — це як **ярлик на робочому столі**. Ви можете його перейменувати, видалити, подивитися його властивості, але щоб відкрити сам документ і працювати з текстом, вам потрібно запустити відповідну програму (аналог `FileInputStream`).

#### **📌 Що перевіряють:**

*Фундаментальне розуміння того, що `File` — це лише абстракція шляху, а не сам файл з даними.*

* * *

### **2 ❓ Питання:**

Як створити новий файл та директорію за допомогою класу `File`?

> #### **💬 Відповідь:**
>
> Для цього існують спеціалізовані методи. Важливо пам'ятати, що створення об'єкта `new File("path")` **не створює** сам файл або директорію у файловій системі — воно лише створює Java-об'єкт, що представляє цей шлях.
>
> **Створення файлу:**
>
> * Використовується метод `boolean createNewFile() throws IOException`.
> * Він створює новий, порожній файл.
> * Повертає `true`, якщо файл було створено, і `false`, якщо файл вже існував.
>
>     ```java
>     File file = new File("myfile.txt");
>     if (file.createNewFile()) { /* ... створено */ }
>     ```
>
>
> **Створення директорії:**
>
> * **`boolean mkdir()`**: Створює директорію, зазначену цим шляхом. Поверне `false`, якщо директорія вже існує або якщо **не існує батьківських директорій**.
>
> * **`boolean mkdirs()`**: **Створює цю директорію разом з усіма необхідними, але неіснуючими батьківськими директоріями.** Це більш надійний та поширений метод.
>
>     ```java
>     File dirs = new File("data/input/logs");
>     dirs.mkdirs(); // Створить всю структуру папок
>     ```

#### **📌 Що перевіряють:**

*Знання основних методів для маніпуляцій у файловій системі та різниці між `mkdir()` і `mkdirs()`.*

* * *

### **3 ❓ Питання:**

Чим відрізняються методи `getPath()`, `getAbsolutePath()` та `getCanonicalPath()`?

> #### **💬 Відповідь:**
>
> Ці три методи повертають шлях до файлу, але в різному форматі.
>
> * **`String getPath()`:**
>   * Повертає **той самий рядок**, який було передано в конструктор `File`. Це може бути як відносний, так і абсолютний шлях.
>
> * **`String getAbsolutePath()`:**
>   * Повертає **повний шлях** до файлу від кореня файлової системи.
>   * Якщо початковий шлях був відносним, він "вирішується" відносно поточної робочої директорії програми.
>   * Цей шлях може містити надлишкові елементи, як `.` (поточна директорія) або `..` (батьківська).
>
> * **`String getCanonicalPath() throws IOException`:**
>   * Повертає **канонічний (унікальний, нормалізований) шлях**.
>   * Він є **абсолютним** і **унікальним**. Це означає, що всі `.` та `..` вирішуються, а також обробляються символічні посилання (у *nix системах).
>   * Це найточніший спосіб отримати унікальний шлях до файлу, але він "дорожчий", оскільки може вимагати взаємодії з файловою системою і кидає `IOException`.
>
> **Приклад:**
> `File f = new File("./../data/file.txt");`
>
> * `getPath()` → `"./../data/file.txt"`
> * `getAbsolutePath()` → `"/home/user/project/./../data/file.txt"`
> * `getCanonicalPath()` → `"/home/user/data/file.txt"`

#### **📌 Що перевіряють:**

*Детальне знання `File API`, особливо різниці між різними представленнями шляхів.*

* * *

### **4 ❓ Питання:**

Чому клас `java.io.File` вважається застарілим і яка є сучасна альтернатива?

> #### **💬 Відповідь:**
>
> Клас `File` має низку суттєвих недоліків, які були виправлені в **New I/O API (NIO.2)**, введеному в **Java 7**. Сучасною альтернативою є використання інтерфейсу **`java.nio.file.Path`** та утилітарного класу **`java.nio.file.Files`**.
>
> **Недоліки `java.io.File`:**
>
> 1. **Погана обробка помилок:** Багато методів (`delete()`, `renameTo()`) повертають лише `boolean`, **не даючи жодної інформації про причину невдачі** (наприклад, "файл зайнятий" чи "немає прав"). Методи NIO.2 кидають **інформативні винятки**.
>
> 2. **Відсутність підтримки символічних посилань:** `File` погано працює з символічними посиланнями, що є важливою частиною *nix систем.
>
> 3. **Обмежена кількість операцій:** Відсутні вбудовані методи для копіювання, переміщення файлів, читання всіх байтів тощо. Це доводилося реалізовувати вручну.
>
> 4. **Проблеми з кросплатформенністю:** Залежність від `File.separator` не є ідеальною. `Path` краще абстрагує від специфіки ОС.
>
> **Переваги `Path` та `Files`:**
>
> * Значно **більш потужне та гнучке API** (копіювання, переміщення, робота з атрибутами, обхід дерева файлів).
> * **Краща обробка помилок** через кидання специфічних `IOException`.
> * Повна підтримка **символічних посилань** та атрибутів файлової системи (POSIX permissions).
>
> У новому коді завжди слід надавати перевагу **`java.nio.file.Path`**.

#### **📌 Що перевіряють:**

*Знання сучасної екосистеми Java та розуміння того, чому старі API були замінені на нові.*

* * *

### **5 ❓ Питання:**

Як за допомогою класу `File` отримати список усіх файлів та директорій у певній папці? ⭐️

> #### **💬 Відповідь:**
>
> Для цього існують два основних методи:
>
> 1. **`String[] list()`:**
>     * Повертає масив **рядків (`String[]`)** з іменами файлів та директорій, що знаходяться у вказаній директорії.
>     * Повертає `null`, якщо `File` не є директорією.
>
> 2. **`File[] listFiles()`:**
>     * Це **більш корисний та поширений** метод.
>     * Повертає масив **об'єктів `File[]`**, що представляють файли та директорії.
>     * З цими об'єктами можна одразу продовжувати працювати (перевіряти `isFile()`, `isDirectory()`, отримувати розмір тощо).
>
> **Приклад:**
>
> ```java
> File folder = new File("/path/to/directory");
>
> if (folder.exists() && folder.isDirectory()) {
>     File[] files = folder.listFiles();
>     if (files != null) {
>         for (File file : files) {
>             System.out.println(
>                 (file.isDirectory() ? "DIR : " : "FILE: ") + file.getName()
>             );
>         }
>     }
> }
> ```
>
> Існують також перевантажені версії `list(FilenameFilter)` та `listFiles(FileFilter)`, які дозволяють відфільтрувати вміст директорії за певними критеріями.

#### **📌 Що перевіряють:**

*Практичні навички роботи з `File API` для ітерації по файловій системі.*

* * *

### **6 ❓ Питання:**

Чому `file.delete()` може повернути `false`? Назвіть кілька можливих причин.

> #### **💬 Відповідь:**
>
> Метод `delete()` повертає `false`, якщо видалення не вдалося. Це одна з головних проблем `File API` — метод **не пояснює, чому саме** операція провалилася.
>
> **Можливі причини:**
>
> 1. **Файл або директорія не існують:** `delete()` на неіснуючому шляху поверне `false`.
> 2. **Недостатньо прав доступу:** Користувач, від імені якого запущена програма, не має прав на видалення файлу або прав на запис у батьківську директорію.
> 3. **Директорія не є порожньою:** Метод `delete()` може видалити лише **порожню** директорію. Щоб видалити директорію з вмістом, потрібно спочатку рекурсивно видалити всі її файли та піддиректорії.
> 4. **Файл використовується іншим процесом:** Операційна система (особливо Windows) може блокувати файл, якщо він відкритий в іншій програмі, і не дозволить його видалити.
>
> Сучасне `Files.delete(path)` (з NIO.2) у цих випадках кинуло б інформативний `IOException` (наприклад, `NoSuchFileException`, `DirectoryNotEmptyException`), що набагато краще для діагностики.

#### **📌 Що перевіряють:**

*Розуміння обмежень `File API` та можливих причин невдач при роботі з файловою системою.*

* * *

### **7 ❓ Питання:**

У чому різниця між `mkdir()` та `mkdirs()`?

> #### **💬 Відповідь:**
>
> Обидва методи призначені для створення директорій, але вони відрізняються своєю поведінкою щодо батьківських директорій.
>
> * **`boolean mkdir()`:**
>   * Намагається створити **лише одну, кінцеву директорію**, зазначену в об'єкті `File`.
>   * Вона поверне `true`, лише якщо **всі батьківські директорії на шляху вже існують**.
>   * Якщо хоча б однієї проміжної директорії немає, метод поверне `false`.
>
> * **`boolean mkdirs()` (з "s" на кінці — plural):**
>   * Намагається створити не тільки кінцеву директорію, але й **усі необхідні, але неіснуючі батьківські директорії** на шляху.
>   * Це більш надійний та "потужний" метод.
>
> **Приклад:**
> Припустимо, у нас є лише диск `C:\`.
>
> `new File("C:\\data\\myapp").mkdir();` → поверне `false` (бо немає папки `data`).
> `new File("C:\\data\\myapp").mkdirs();` → поверне `true` (створить і `data`, і `myapp`).
>
> У 99% випадків на практиці слід використовувати `mkdirs()`.

#### **📌 Що перевіряють:**

*Точне знання `File API`. Це одне з поширених питань для перевірки уважності.*

* * *

### **8 ❓ Питання:**

Чому `new File("a/b.txt")` не кидає виняток, навіть якщо файлу чи директорії не існує? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, що об'єкт `File` є лише **абстрактним представленням шляху**, а **не самого файлу** на диску.
>
> Коли ви викликаєте `new File(...)`, ви **не взаємодієте з файловою системою**. Ви просто створюєте Java-об'єкт, який інкапсулює в собі рядок `"a/b.txt"`.
>
> Взаємодія з реальною файловою системою відбувається лише тоді, коли ви викликаєте такі методи, як:
>
> * `exists()`
> * `createNewFile()`
> * `delete()`
> * `length()`
>
> Саме ці методи можуть повернути `false` або кинути `IOException`, якщо шлях є невалідним або дія не може бути виконана.
>
> Отже, конструктор `File` не може знати, чи існує цей шлях, тому він і не кидає винятків.

#### **📌 Що перевіряють:**

*Глибоке розуміння концепції `File` як абстракції над шляхом.*

* * *

### **9 ❓ Питання:**

Як забезпечити кросплатформенність при роботі зі шляхами? Яка є проблема з `new File("data\\config.txt")`? ⭐️

> #### **💬 Відповідь:**
>
> **Проблема** цього коду в тому, що він використовує **жорстко закодований роздільник шляхів `\`**, який є специфічним для **Windows**. Цей код не буде працювати на Linux або macOS, де роздільником є `/`.
>
> **Як забезпечити кросплатформенність:**
>
> 1. **Використання `File.separator`:**
>     * Це статична константа `public static final String separator`, яка містить правильний роздільник для поточної операційної системи.
>
>     ```java
>     String path = "data" + File.separator + "config.txt";
>     File file = new File(path);
>     ```
>
> 2. **Використання конструктора `File(File parent, String child)`:**
>     * Цей конструктор є ще кращим, оскільки він сам коректно об'єднує батьківську директорію та ім'я файлу.
>
>     ```java
>     File parentDir = new File("data");
>     File file = new File(parentDir, "config.txt");
>     ```
>
> 3. **Використання сучасного NIO.2 API (`Path`) — найкращий підхід:**
>     * Інтерфейс `Path` та клас `Paths` надають значно кращі та більш надійні інструменти для роботи зі шляхами.
>
>     ```java
>     Path path = Paths.get("data", "config.txt");
>     ```
>
> Метод `Paths.get()` автоматично використовує правильний роздільник файлової системи.

#### **📌 Що перевіряють:**

*Розуміння проблем кросплатформенності та знання інструментів для написання переносимого коду.*

* * *

### **10 ❓ Питання:**

Поясніть, що таке відносний та абсолютний шлях. Який з них є кращим для використання в додатках? ⭐️

> #### **💬 Відповідь:**
>
> * **Абсолютний шлях:**
>   * Це повний шлях до файлу або директорії, що починається від **кореня файлової системи**.
>   * Він не залежить від того, звідки запущена програма.
>   * **Приклади:** `C:\Users\John\Documents\file.txt` (Windows), `/home/user/app/file.txt` (Linux).
>
> * **Відносний шлях:**
>   * Це шлях, який визначається **відносно поточної робочої директорії** програми.
>   * **Приклади:** `data/file.txt`, `../logs/app.log`.
>
> **Який краще використовувати:**
>
> **У більшості випадків слід надавати перевагу відносним шляхам**, але конфігурувати їх через зовнішні налаштування.
>
> **Проблеми з абсолютними шляхами:**
>
> * Вони **жорстко прив'язують** додаток до конкретної структури директорій. Якщо ви перенесете програму на інший комп'ютер, де шлях `C:\MyApp` не існує, вона перестане працювати. Це дуже крихкий підхід.
>
> **Переваги відносних шляхів:**
>
> * Вони роблять додаток **переносимим**. Ви можете роздавати ваш додаток як архів, і він буде працювати з файлами у своїй власній директорії, незалежно від того, де його розпакують.
>
> **Найкраща практика:**
> Не кодувати жорстко ні той, ні інший. Основна директорія додатка (наприклад, для логів або даних) повинна **задаватися у зовнішньому конфігураційному файлі**. Це дає максимальну гнучкість.

#### **📌 Що перевіряють:**

*Розуміння проблем, пов'язаних з конфігурацією файлових шляхів, та способів написання переносимих додатків.*

* * *

### **11 ❓ Питання:**

Як видалити не порожню директорію за допомогою класу `File`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `file.delete()` може видалити лише **порожню** директорію. Щоб видалити не порожню, потрібно **написати рекурсивний метод**, який спочатку видалить весь її вміст.
>
> **Алгоритм рекурсивного видалення:**
>
> 1. Перевірити, чи існує вказаний `File` і чи є він директорією.
> 2. Отримати список всього вмісту директорії (`listFiles()`).
> 3. Пройтися по цьому списку в циклі.
> 4. Для кожного елемента:
>     * Якщо це **файл**, просто видалити його (`file.delete()`).
>     * Якщо це **директорія**, **рекурсивно викликати** той самий метод видалення для неї.
> 5. Після того, як весь вміст видалено, видалити саму (тепер вже порожню) директорію.
>
> **Приклад реалізації:**
>
> ```java
> public static void deleteDirectoryRecursively(File dir) {
>     if (dir.isDirectory()) {
>         File[] children = dir.listFiles();
>         if (children != null) {
>             for (File child : children) {
>                 deleteDirectoryRecursively(child);
>             }
>         }
>     }
>     dir.delete(); // Видаляємо файл або тепер вже порожню директорію
> }
> ```
>
> Сучасне NIO.2 API (`java.nio.file.Files`) значно спрощує цю задачу, але для `java.io.File` потрібна ручна рекурсія.

#### **📌 Що перевіряють:**

*Алгоритмічне мислення та вміння працювати з рекурсією для обходу файлової системи.*

* * *

### **12 ❓ Питання:**

Чому `file.renameTo()` може не спрацювати і вважається ненадійним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `file.renameTo(destFile)` — це метод, що намагається перейменувати або перемістити файл. Він є **дуже ненадійним**, бо:
>
> 1. **Повертає лише `boolean`:** Як і `delete()`, він не дає жодної інформації про причину невдачі. Він може повернути `false` з безлічі причин.
>
> 2. **Залежить від платформи:** Його поведінка **не є гарантованою** на всіх операційних системах.
>     * Зокрема, він зазвичай **не може переміщувати файли між різними файловими системами** (наприклад, з диска C: на D:).
>     * Він може не впоратися, якщо цільовий файл вже існує, або якщо у вас немає прав доступу.
>
> 3. **Неатомарність:** Операція може не бути атомарною.
>
> **Сучасна альтернатива:**
>
> Метод **`Files.move(sourcePath, targetPath, options...)`** з NIO.2 є набагато кращим вибором.
>
> * Він **кидає інформативний `IOException`** у разі невдачі.
> * Він є **атомарним** на більшості файлових систем.
> * За допомогою опцій (`StandardCopyOption.REPLACE_EXISTING`) можна контролювати поведінку (наприклад, чи перезаписувати існуючий файл).

#### **📌 Що перевіряють:**

*Розуміння обмежень старого `File` API та знання про сучасні, більш надійні альтернативи.*

* * *

### **13 ❓ Питання:**

Чому цей код може бути небезпечним для пам'яті?
`File[] files = new File("/large_directory").listFiles();` ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код є небезпечним, якщо `/large_directory` містить **дуже велику кількість файлів** (сотні тисяч або мільйони).
>
> **Проблема:**
> Метод `listFiles()` намагається **завантажити всю інформацію** про всі файли та піддиректорії **в пам'ять одразу**, створюючи для кожного елемента об'єкт `File` і поміщаючи їх у масив.
>
> **Наслідки:**
>
> * Це може призвести до споживання величезної кількості пам'яті в купі (Heap).
> * Якщо файлів занадто багато, це може легко спричинити **`OutOfMemoryError`**.
>
> **Як правильно обробляти великі директорії:**
>
> Потрібно використовувати підходи, які обробляють вміст **потоково**, не завантажуючи все в пам'ять.
>
> * **З Java 8 та NIO.2:**
>     Найкращим рішенням є **`Files.list(Path dir)`** або **`Files.walk(Path start)`**.
>
>     ```java
>     // Files.list() повертає ледачий Stream<Path>
>     try (Stream<Path> stream = Files.list(Paths.get("/large_directory"))) {
>         stream.forEach(System.out::println);
>     }
>     ```
>
>     Цей підхід є **"лінивим" (lazy)**. Він обробляє файли по одному, не зберігаючи весь список у пам'яті, що є значно ефективнішим для великих директорій.

#### **📌 Що перевіряють:**

*Розуміння проблем управління пам'яттю та знання сучасного Stream API для роботи з файловою системою.*

* * *

### **14 ❓ Питання:**

Як `File` та `Path` взаємодіють між собою? Чи можна їх конвертувати один в одного? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, для забезпечення зворотної сумісності Java надає прості способи конвертації між старим API (`File`) та новим (`Path`).
>
> **`File` → `Path`:**
>
> * Кожен об'єкт `File` має метод `toPath()`, який повертає відповідний `Path`.
>
>     ```java
>     File file = new File("file.txt");
>     Path path = file.toPath();
>     ```
>
>
> **`Path` → `File`:**
>
> * Кожен об'єкт `Path` має метод `toFile()`, який повертає відповідний `File`.
>
>     ```java
>     Path path = Paths.get("file.txt");
>     File file = path.toFile();
>     ```
>
> Ця взаємодія є дуже важливою при роботі з **успадкованим кодом (legacy code)**. Якщо у вас є метод, який приймає `File`, а ви працюєте з `Path`, ви можете легко його конвертувати.
>
> **Рекомендація:**
>
> У новому коді завжди **починайте з `Path`**. Використовуйте `toFile()` лише тоді, коли вам потрібно викликати старий API, який вимагає об'єкт `File` (наприклад, деякі старі бібліотеки). Це найкраща практика.

#### **📌 Що перевіряють:**

*Знання механізмів сумісності між старим та новим I/O API.*

* * *

### **15 ❓ Питання:**

Створіть власний файловий фільтр для `listFiles()`, який повертає лише файли з розширенням `.log`. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього потрібно створити клас, який реалізує інтерфейс **`java.io.FileFilter`** (або `FilenameFilter`).
>
> `FileFilter` має один метод: `boolean accept(File pathname)`. Цей метод повертає `true`, якщо файл слід включити до результату.
>
> **Реалізація за допомогою анонімного класу:**
>
> ```java
> File dir = new File(".");
> File[] logFiles = dir.listFiles(new FileFilter() {
>     @Override
>     public boolean accept(File file) {
>         // Повертаємо true, якщо це файл і його назва закінчується на .log
>         return file.isFile() && file.getName().toLowerCase().endsWith(".log");
>     }
> });
> ```
>
> **Реалізація за допомогою лямбда-виразу (більш сучасний підхід):**
>
> Оскільки `FileFilter` є функціональним інтерфейсом, його можна легко реалізувати за допомогою лямбди.
>
> ```java
> File dir = new File(".");
> File[] logFiles = dir.listFiles(file ->
>     file.isFile() && file.getName().toLowerCase().endsWith(".log")
> );
>
> // Виведемо результат
> if (logFiles != null) {
>     for (File f : logFiles) {
>         System.out.println(f.getName());
>     }
> }
> ```
>
> Цей підхід є більш гнучким і дозволяє створювати складні правила для фільтрації вмісту директорій безпосередньо "на місці".

#### **📌 Що перевіряють:**

*Навички роботи з інтерфейсами та функціональним стилем (лямбдами) для реалізації кастомної логіки.*
