---
layout: default
title: "PrintWriter (java.io.PrintWriter)"
parent: "Робота з файлами (Files)"
grand_parent: "Java Core"
nav_order: 4
---

# PrintWriter (java.io.PrintWriter)

* короткий опис: `PrintWriter` – це клас, що надає зручні методи для форматованого запису текстових даних у файл або інший потік (наприклад, консоль), включаючи `print()`, `println()`, `printf()`.

* поняття: `PrintWriter`, форматований вивід, `print()`, `println()`, `printf()`.

* приклади: `pw.println("Hello");`, `pw.printf("Sum: %.2f\n", total);`.

* ключові моменти: Спрощує форматований вивід, має власний буфер. За замовчуванням не кидає `IOException` (потрібно перевіряти `checkError()`).

* що важливо знати на співбесіді: Коли використовувати `PrintWriter` замість `BufferedWriter`, форматований вивід.

### **✅ Офіційне визначення:**  

**`PrintWriter`** — клас із пакету `java.io`, який надає **зручні методи** для запису текстових даних у файл або в інший потік (наприклад, консоль). Він підтримує **форматований вивід**, методи **`print()`, `println()`, `printf()`.**

### **🧠 Простими словами:**

Це як **універсальна ручка** ✍️, яка вміє записувати все — числа, рядки, булеві значення, навіть **форматувати текст**, як у **`System.out.println()`** — але **у файл**.

---

## **Основні методи і приклади**

📊 Таблиця:

| Метод | Що робить | Аналог |
| ----- | ----- | ----- |
| `print(x)` | Записує без переносу рядка | `System.out.print()` |
| `println(x)` | Записує \+ новий рядок | `System.out.println()` |
| `printf(...)` | Форматований запис (як у C) | `System.out.printf()` |
| `flush()` | Записує буфер у файл |  |
| `close()` | Закриває ресурс |  |

---

## **Пояснення на пальцях (💡)**

### **Для чого це?**

* Коли треба **зручно і красиво** писати у файл

* Коли потрібно **форматувати дані** (наприклад, числа з 2 знаками після коми)

* Для **генерації звітів**, CSV, логів

### **У чому особливість?**

* Дуже **зручний синтаксис**

* Має власний **внутрішній буфер**

* **Підтримує форматований вивід** (`printf`)

### **Типові граблі:**

* Забути `close()` -> нічого не збережеться

* **Не кидає винятків по замовчуванню**, якщо сталася помилка -> треба перевіряти `checkError()`

---

## **Корисні приклади (🧪)**

### **🔹 Запис рядків:**

```java
PrintWriter pw = new PrintWriter("output.txt");
pw.println("Привіт, світ!");
pw.println("Ще один рядок");
pw.close();
```

### **🔹 Форматований запис:**

```java
PrintWriter pw = new PrintWriter("report.txt");
pw.printf("Сума: %.2f грн\n", 123.456); // виведе: Сума: 123.46 грн
pw.close();
```

### **🔹 try-with-resources:**

```java
try (PrintWriter pw = new PrintWriter("log.txt")) {
    pw.println("Рядок 1");
    pw.printf("Дата: %tF\n", new java.util.Date());
}
```

---

## **Порівняння (⚖️)**

| Клас | Особливості | Форматування | Буфер |
| ----- | ----- | ----- | ----- |
| `FileWriter` | Прямий запис символів | ❌ | малий |
| `BufferedWriter` | Буферизація \+ швидкість | ❌ | ✅ |
| `PrintWriter` | Зручні `print`, `println`, `printf` | ✅ | ✅ |

---

## **🧠 Мнемоніка для запам’ятовування**

```java
PrintWriter -> зручно, як System.out
println() -> новий рядок
printf() -> формат
checkError() -> перевір помилки
```

---

## **🟩 Підсумок:**

`PrintWriter` — ідеальний для зручного запису у файл, коли потрібно багато `print/println` або форматований вивід. Його легко зрозуміти, він має знайомі методи, і підходить як для простих задач, так і для генерації файлів звітів або логів.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `java.io.PrintWriter` і яка його головна відмінність від інших `Writer`-ів?

> #### **💬 Відповідь:**
>
> **`PrintWriter`** — це клас з пакету `java.io`, який "обгортає" інший `Writer` або `OutputStream` і надає **потужні та зручні можливості для форматованого запису текстових даних**.
>
> **Головні відмінності та переваги:**
>
> 1. **Наявність методів `print()`, `println()` та `printf()`:** Це його ключова перевага. Він надає API, ідентичний до `System.out`, що дозволяє легко записувати примітивні типи, об'єкти (викликаючи їх `toString()`) та форматовані рядки без необхідності ручного перетворення їх у `String`.
>
> 2. **Обробка помилок (не кидає `IOException`):** На відміну від інших `Writer`-ів, методи `print`/`println` у `PrintWriter` **ніколи не кидають `IOException`**. Замість цього, якщо виникає помилка, вони встановлюють внутрішній прапор, стан якого можна перевірити за допомогою методу `checkError()`.
>
> 3. **Авто-флаш (Auto-flushing):** `PrintWriter` має конструктор, що дозволяє увімкнути режим автоматичного "скидання" буфера щоразу, коли викликається `println()`, `printf()` або `format()`.
>
> Це робить `PrintWriter` ідеальним інструментом для генерації людськочитабельних текстових файлів, логів та звітів.

#### **📝 Аналогія:**

* `BufferedWriter` — це як швидка ручка для безперервного запису тексту.
* `PrintWriter` — це як просунутий текстовий процесор, який вміє не тільки писати, а й форматувати текст, додавати нові рядки та обробляти різні типи даних автоматично.

#### **📌 Що перевіряють:**

*Базове розуміння призначення `PrintWriter` як інструменту для зручного, форматованого виводу.*

* * *

### **2 ❓ Питання:**

Поясніть, як `PrintWriter` обробляє помилки. Чому його методи не кидають `IOException`?

> #### **💬 Відповідь:**
>
> `PrintWriter` має унікальну механіку обробки помилок, яка відрізняє його від інших потоків.
>
> **Методи `print()`, `println()` та `printf()` ніколи не кидають `IOException`.**
>
> **Як це працює:**
>
> 1. Якщо під час запису даних виникає `IOException` у вкладеному потоці, `PrintWriter` "ковтає" цей виняток.
> 2. Замість цього, він встановлює свій **внутрішній прапор помилки**.
> 3. Щоб перевірити, чи сталася помилка під час попередніх операцій, потрібно викликати метод **`public boolean checkError()`**.
>
>     * Цей метод повертає `true`, якщо помилка була, і `false` — якщо ні.
>     * Важливо: `checkError()` також викликає `flush()` перед перевіркою.
>
> **Чому так зроблено:**
>
> Ця поведінка була успадкована від дизайну `PrintStream` (яким є `System.out`). Головна ідея — **спростити** написання коду для сценаріїв, де помилка виводу не є критичною і не вимагає негайної обробки (наприклад, при виводі в консоль). Це дозволяє писати `System.out.println()` без необхідності обгортати його в `try-catch`.
>
> **Однак:** для надійного запису у файл така поведінка може бути **небезпечною**, оскільки помилка запису може залишитися непоміченою. Тому при роботі з файлами важливо перевіряти `checkError()` після завершення операцій.

#### **📌 Що перевіряють:**

*Знання цієї унікальної та неочевидної особливості `PrintWriter`. Кандидат повинен знати про метод `checkError()`.*

* * *

### **3 ❓ Питання:**

Наведіть приклад форматованого запису у файл за допомогою `PrintWriter` та методу `printf()`.

> #### **💬 Відповідь:**
>
> Метод `printf(String format, Object... args)` дозволяє записувати форматовані рядки, використовуючи той самий синтаксис, що й `String.format()` або `System.out.printf()`.
>
> **Приклад: генерація простого звіту у файл.**
>
> ```java
> import java.io.FileWriter;
> import java.io.IOException;
> import java.io.PrintWriter;
>
> public class ReportGenerator {
>     public static void main(String[] args) {
>         String[] products = {"Ноутбук", "Миша"};
>         double[] prices = {1250.758, 25.5};
>
>         try (PrintWriter writer = new PrintWriter(new FileWriter("report.txt"))) {
>             writer.println("--- Звіт про продажі ---");
>
>             for (int i = 0; i < products.length; i++) {
>                 // %-10s - рядок, вирівняний по лівому краю, 10 символів
>                 // %.2f - число з плаваючою комою, 2 знаки після коми
>                 // %n - платформо-незалежний новий рядок
>                 writer.printf("Товар: %-10s | Ціна: %.2f грн%n", products[i], prices[i]);
>             }
>
>             writer.println("--- Кінець звіту ---");
>
>             // Перевіряємо, чи були помилки
>             if (writer.checkError()) {
>                 System.err.println("Сталася помилка при записі звіту.");
>             }
>
>         } catch (IOException e) {
>             e.printStackTrace();
>         }
>     }
> }
> ```
>
> **Вміст `report.txt`:**
>
> ```
>
> --- Звіт про продажі ---
> Товар: Ноутбук    | Ціна: 1250.76 грн
> Товар: Миша       | Ціна: 25.50 грн
> --- Кінець звіту ---
>
> ```

#### **📌 Що перевіряють:**

*Практичні навички форматування виводу та знання синтаксису `printf`.*

* * *

### **4 ❓ Питання:**

Чим `PrintWriter` відрізняється від `BufferedWriter`? Коли який краще використовувати?

> #### **💬 Відповідь:**
>
> Обидва класи є буферизованими `Writer`-ами, але вони призначені для різних цілей.
>
| Характеристика | `PrintWriter` | `BufferedWriter` |
| :---: | :---: | :---: |
| **Основне призначення**| **Форматований текстовий вивід.** | **Ефективний, швидкий запис** великих обсягів тексту.|
| **Методи** | Надає зручні `print()`, `println()`, `printf()` для будь-яких типів. | Надає лише базові `write()` та `newLine()`. |
| **Обробка помилок** | **Не кидає `IOException`** на методах `print`/`println`. Потрібно перевіряти через `checkError()`.| **Кидає `IOException`**. Змушує обробляти помилки. |
| **Продуктивність** | Трохи повільніший через додаткову логіку форматування та перевірки помилок. | Максимально швидкий для "сирого" запису символів.|
>
> **Коли який краще:**
>
> * **`PrintWriter`:** Коли вам потрібен **зручний, форматований вивід**, схожий на `System.out`. Ідеально для генерації **людськочитабельних** звітів, логів, текстових файлів.
> * **`BufferedWriter`:** Коли вам потрібна **максимальна продуктивність** для запису великих обсягів неформатованих даних. Наприклад, при генерації великих CSV, JSON або SQL-скриптів.

#### **📌 Що перевіряють:**

*Здатність обрати правильний інструмент для конкретної задачі, розуміючи компроміс між зручністю API та продуктивністю.*

* * *

### **5 ❓ Питання:**

Як `PrintWriter` працює з кодуванням символів?

> #### **💬 Відповідь:**
>
> `PrintWriter` сам по собі не працює з кодуваннями. Він працює з **символами (`char`)**. Відповідальність за перетворення символів у байти (з використанням певного кодування) лежить на **вкладеному `Writer`**, який він обгортає.
>
> * **Якщо ви створюєте `PrintWriter` так:**
>     `new PrintWriter(new FileWriter("file.txt"))`
>     ...то буде використовуватися **кодування за замовчуванням для платформи**, оскільки `FileWriter` працює саме так. Це **не рекомендований** підхід для переносимого коду.
>
> * **Якщо вам потрібно вказати кодування (наприклад, UTF-8), слід робити так:**
>
>     ```java
>     try (PrintWriter writer = new PrintWriter(
>             new OutputStreamWriter(
>                 new FileOutputStream("file.txt"), StandardCharsets.UTF_8
>             )
>         )) {
>         writer.println("Привіт!");
>     }
>     ```
>
>     Тут `OutputStreamWriter` відповідає за коректне кодування.
>
> * **Простіший спосіб (з Java 11+):**
>     Конструктор `PrintWriter` був розширений.
>     `new PrintWriter(String fileName, Charset charset)`
>
>     ```java
>     try (PrintWriter writer = new PrintWriter("file.txt", StandardCharsets.UTF_8)) {
>         writer.println("Привіт!");
>     }
>     ```

#### **📌 Що перевіряють:**

*Розуміння того, що `PrintWriter` є високорівневою обгорткою, а за кодування відповідають нижчі шари потоків.*

* * *

### **6 ❓ Питання:**

Що таке auto-flushing у `PrintWriter` і як його увімкнути? ⭐️

> #### **💬 Відповідь:**
>
> **Auto-flushing (автоматичне скидання буфера)** — це режим роботи `PrintWriter`, при якому він автоматично викликає `flush()` після виконання певних методів.
>
> **Як його увімкнути:**
> Це робиться за допомогою спеціального конструктора, який приймає булевий параметр `autoFlush`.
>
> `public PrintWriter(OutputStream out, boolean autoFlush)`
>
> **Як це працює:**
>
> Якщо `autoFlush` встановлено в `true`, то `flush()` буде автоматично викликано щоразу після виконання одного з цих трьох методів:
>
> * `println()`
> * `printf()`
> * `format()`
>
> Зверніть увагу, що звичайний `print()` **не викликає** auto-flush.
>
> **Коли це корисно:**
>
> * Це особливо зручно при роботі з **мережевими сокетами** або **консоллю (`System.out`)**. Коли ви відправляєте повідомлення по мережі або виводите рядок у лог, ви зазвичай хочете, щоб він був відправлений негайно.
>
> **Приклад:**
>
> ```java
> // `true` вмикає auto-flush
> PrintWriter networkWriter = new PrintWriter(socket.getOutputStream(), true);
> networkWriter.println("GET / HTTP/1.1"); // Дані будуть негайно відправлені
> ```

#### **📌 Що перевіряють:**

*Знання просунутих можливостей API та їх застосування, особливо в контексті мережевого програмування.*

* * *

### **7 ❓ Питання:**

Чому `System.out` є екземпляром `PrintStream`, а не `PrintWriter`? Яка між ними різниця? ⭐️

> #### **💬 Відповідь:**
>
> Хоча їхні API дуже схожі, ключова різниця між `PrintStream` та `PrintWriter` полягає в тому, **з якими даними вони працюють на найнижчому рівні**.
>
> * **`PrintStream`:**
>   * Це **байтовий** потік. Він обгортає `OutputStream`.
>   * Коли ви передаєте в нього рядок, він **самостійно перетворює символи в байти**, використовуючи кодування платформи.
>   * Історично `PrintStream` був основним класом для текстового виводу.
>
> * **`PrintWriter`:**
>   * Це **символьний** потік. Він обгортає `Writer`.
>   * Він **не займається** перетворенням символів у байти. Це робить вкладений `Writer` (наприклад, `OutputStreamWriter`).
>
> **Чому `System.out` є `PrintStream`:**
>
> * **Історичні причини:** `PrintStream` з'явився в Java 1.0, задовго до появи повноцінної ієрархії символьних потоків `Writer` в Java 1.1. `System.out` було створено саме як `PrintStream`.
> * **Зворотна сумісність:** Щоб не "ламати" мільйони існуючих програм, його тип ніколи не змінювали.
>
> **Сучасна рекомендація:**
>
> Для будь-якої нової роботи з **текстовими** даними, перевагу слід надавати ієрархії **`Writer`** та, відповідно, **`PrintWriter`**, оскільки вона є більш гнучкою і правильною з точки зору архітектури (чітке розділення символьної та байтової роботи).

#### **📌 Що перевіряють:**

*Розуміння відмінностей між байтовими та символьними потоками та історичного контексту дизайну `java.io`.*

* * *

### **8 ❓ Питання:**

Якщо `PrintWriter` не кидає `IOException`, як надійно перевірити, чи всі операції запису були успішними? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього існує двоступенева перевірка.
>
> 1. **Викликати `flush()` або `close()`:** Перш за все, потрібно гарантувати, що всі дані з буфера були передані у вкладений потік.
>
> 2. **Викликати `checkError()`:** Після цього слід викликати метод `checkError()`.
>
> **Метод `checkError()`:**
>
> * Він викликає `flush()` на внутрішньому потоці.
> * Потім він перевіряє внутрішній прапор помилки.
> * Повертає `true`, якщо під час **будь-якої попередньої операції `write`, `print`, `println`, `printf`, `format`, `flush` або `close`** виникла помилка. В іншому випадку — `false`.
> * **Важливо:** цей метод **не скидає** прапор помилки. Якщо помилка сталася один раз, `checkError()` буде повертати `true` і надалі.
>
> **Приклад надійного завершення роботи:**
>
> ```java
> PrintWriter writer = new PrintWriter(...);
> try {
>     writer.println("Some data");
>     writer.println("More data");
> } finally {
>     boolean hasError = writer.checkError(); // checkError() сам викликає flush()
>     writer.close();
>
>     if (hasError) {
>         System.err.println("Під час запису сталася помилка!");
>     }
> }
> ```
>
> Використання `try-with-resources` тут менш зручне, бо `checkError()` потрібно викликати до автоматичного закриття.

#### **📌 Що перевіряють:**

*Практичні знання про те, як надійно працювати з "некидаючим" API `PrintWriter`.*

* * *

### **9 ❓ Питання:**

У чому може бути проблема з таким кодом? `new PrintWriter(System.out);` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Головна проблема полягає в **непередбачуваній поведінці з кодуваннями**, особливо при виведенні не-ASCII символів (наприклад, кирилиці).
>
> **Що відбувається:**
>
> 1. `PrintWriter` створює "під капотом" `OutputStreamWriter` для обгортання `System.out` (який є `OutputStream`).
> 2. Цей `OutputStreamWriter` буде використовувати **кодування платформи за замовчуванням**.
> 3. З іншого боку, **консоль**, куди виводиться `System.out`, також має **своє власне кодування**.
>
> **Проблема:**
>
> Якщо кодування, яке обрав `PrintWriter`, і кодування вашої консолі **не збігаються**, то при виведенні, наприклад, кириличних символів, ви побачите "кракозябри" (`?????`).
>
> **Як правильно:**
> Якщо потрібен контроль над кодуванням при виводі в консоль, слід створювати `PrintWriter` явно, вказуючи `Charset`:
>
> `PrintWriter pw = new PrintWriter(new OutputStreamWriter(System.out, StandardCharsets.UTF_8), true);`
>
> Цей код є більш надійним і переносимим.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як працюють кодування в `java.io`, та здатність бачити потенційні проблеми.*

* * *

### **10 ❓ Питання:**

Напишіть код, що демонструє, як `PrintWriter` може "проковтнути" `IOException`. ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Ми можемо створити кастомний `Writer`, який завжди кидає `IOException` при спробі запису, і передати його в `PrintWriter`.
>
> ```java
> import java.io.IOException;
> import java.io.PrintWriter;
> import java.io.Writer;
>
> public class SwallowingExample {
>
>     // Створюємо Writer, який завжди "падає"
>     private static class FaultyWriter extends Writer {
>         @Override
>         public void write(char[] cbuf, int off, int len) throws IOException {
>             throw new IOException("Диск заповнено!");
>         }
>         @Override public void flush() throws IOException {}
>         @Override public void close() throws IOException {}
>     }
>
>     public static void main(String[] args) {
>         PrintWriter writer = new PrintWriter(new FaultyWriter());
>
>         // Цей виклик не кине виняток
>         writer.println("Спроба запису");
>
>         System.out.println("Код продовжує працювати...");
>
>         // Але якщо ми перевіримо, то побачимо помилку
>         if (writer.checkError()) {
>             System.out.println("Помилку було виявлено через checkError()!");
>         }
>     }
> }
> ```
>
> **Вивід:**
>
> ```
>
> Код продовжує працювати...
> Помилку було виявлено через checkError()!
>
> ```
>
> Цей приклад чітко показує, що `println()` перехопив `IOException` з `FaultyWriter`, встановив внутрішній прапор помилки, але не прокинув виняток далі. Програма продовжила виконуватися, ніби нічого не сталося.

#### **📌 Що перевіряють:**

*Практичну демонстрацію та глибоке розуміння унікальної механіки обробки помилок у `PrintWriter`.*

* * *

### **11 ❓ Питання:**

Чому `BufferedWriter` є кращим вибором для `FileWriter`, але `PrintWriter` не обов'язково потребує обгортання в `BufferedWriter`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що `PrintWriter` **вже є буферизованим**.
>
> Хоча в його назві немає слова "Buffered", клас `PrintWriter` зазвичай "під капотом" **використовує `BufferedWriter`** або реалізує власну схожу логіку буферизації.
>
> Якщо ви подивитеся на вихідний код `PrintWriter`, ви побачите, що він обгортає переданий `Writer` у `BufferedWriter`, якщо той ще не є буферизованим.
>
> **Тому:**
>
> * **`new BufferedWriter(new FileWriter(...))`** — це **обов'язкова** конструкція для ефективності, бо `FileWriter` не має ефективної буферизації.
> * **`new BufferedWriter(new PrintWriter(...))`** — це **надлишково та безглуздо**. Ви будете обгортати буферизований потік в інший буферизований потік, що не дасть жодної переваги, а лише створить зайвий об'єкт.
>
> Таким чином, `PrintWriter` і `BufferedWriter` — це дві **альтернативні** обгортки для `Writer`, а не послідовні. Ви обираєте або одну, або іншу, залежно від того, що вам потрібно: **продуктивність (`BufferedWriter`)** чи **зручне форматування (`PrintWriter`)**.

#### **📌 Що перевіряють:**

*Знання внутрішньої реалізації класів `java.io` та розуміння, коли застосування патерну "Декоратор" є надлишковим.*

* * *

### **12 ❓ Питання:**

У якому випадку `close()` у `PrintWriter` може кинути `IOException`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча методи `print()` та `println()` "ковтають" винятки, метод `close()` **поводиться інакше**.
>
> Згідно з контрактом, `close()` спочатку викликає `flush()`, а потім закриває вкладений потік.
>
> * Якщо **вкладений потік** при виклику `close()` (або `flush()` зсередини `close()`) кидає `IOException`, то `PrintWriter.close()` **не буде "ковтати"** цей виняток, а **прокине його далі**.
>
> **Сценарій:**
>
> Уявіть, що ви успішно записали дані в буфер `PrintWriter`, але в момент виклику `close()` фізичний пристрій (наприклад, флешка) був від'єднаний. Спроба `flush()` даних на цей пристрій призведе до `IOException`. `PrintWriter` не буде приховувати таку серйозну помилку.
>
> **Висновок:**
> `PrintWriter` приховує помилки лише під час операцій **форматування та запису в буфер**. Фінальна операція закриття все ще може кинути `checked` виняток, якщо щось пішло не так з базовим потоком. Саме тому `try-with-resources` або `try-catch` є необхідними при роботі з `PrintWriter` так само, як і з іншими потоками.

#### **📌 Що перевіряють:**

*Дуже глибоке знання API `PrintWriter` та того, що його "безпечна" поведінка не поширюється на `close()`.*

* * *

### **13 ❓ Питання:**

Як реалізувати власний `Writer`, який логує кожен запис у консоль перед тим, як передати його у `PrintWriter`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Це завдання вирішується за допомогою патерну **Декоратор**. Ми створимо свій `Writer`, який буде обгортати інший `Writer` і додавати логіку логування.
>
> ```java
> public class LoggingWriter extends Writer {
>     private final Writer delegate;
>
>     public LoggingWriter(Writer delegate) {
>         this.delegate = delegate;
>     }
>
>     // Перевизначаємо ключовий метод запису
>     @Override
>     public void write(char[] cbuf, int off, int len) throws IOException {
>         // 1. Додаємо свою логіку (логування)
>         System.out.println("LOG: Записуємо рядок: '" + new String(cbuf, off, len) + "'");
>
>         // 2. Делегуємо запис оригінальному Writer'у
>         delegate.write(cbuf, off, len);
>     }
>
>     // Обов'язково делегуємо flush() та close()
>     @Override
>     public void flush() throws IOException {
>         delegate.flush();
>     }
>
>     @Override
>     public void close() throws IOException {
>         delegate.close();
>     }
> }
> ```
>
> **Як використовувати:**
>
> ```java
> try (PrintWriter pw = new PrintWriter(
>                          new LoggingWriter(
>                              new FileWriter("file.txt")))) {
>
>     pw.println("Test message");
> } catch (IOException e) {
>     e.printStackTrace();
> }
> ```
>
> **Вивід у консоль:**
>
> `LOG: Записуємо рядок: 'Test message'`
> `LOG: Записуємо рядок: '\r\n'` (або `\n`)
>
> Цей підхід дозволяє додавати нову поведінку до існуючих потоків, не змінюючи їхній код.

#### **📌 Що перевіряють:**

*Знання патерну "Декоратор" та його практичне застосування для розширення функціональності `java.io`.*

* * *

### **14 ❓ Питання:**

Якщо `PrintWriter` створюється на основі `ByteArrayOutputStream`, куди буде спрямовано вивід, і як його отримати? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> У цьому випадку вивід буде спрямовано **не у файл, а в пам'ять**.
>
> `ByteArrayOutputStream` — це реалізація `OutputStream`, яка записує дані у **внутрішній масив байтів (`byte[]`)**, що динамічно розширюється.
>
> **Як це працює:**
>
> 1. `new PrintWriter(new ByteArrayOutputStream())` створює `PrintWriter`, який буде писати в пам'ять.
> 2. Викликаючи `writer.print("Hello")`, ви записуєте символи. `PrintWriter` через внутрішній `OutputStreamWriter` перетворює їх на байти і пише у `ByteArrayOutputStream`.
> 3. Дані накопичуються у внутрішньому байтовому масиві.
>
> **Як отримати результат:**
>
> Після завершення запису ви можете отримати ці дані з `ByteArrayOutputStream` одним з двох способів:
>
> * `byte[] getBytes = baos.toByteArray();` — повертає копію внутрішнього масиву.
> * `String content = baos.toString(StandardCharsets.UTF_8);` — повертає вміст як рядок з вказаним кодуванням.
>
> **Приклад:**
>
> ```java
> ByteArrayOutputStream baos = new ByteArrayOutputStream();
> try (PrintWriter writer = new PrintWriter(baos)) {
>     writer.println("Це запис у пам'ять");
> }
> String result = baos.toString();
> System.out.println(result);
> ```
>
> Це дуже корисний підхід для **тестування** (коли не потрібен реальний файл) або для **генерації контенту в пам'яті** перед тим, як відправити його, наприклад, в HTTP-відповідь.

#### **📌 Що перевіряють:**

*Знання різних реалізацій потоків `java.io`, включаючи ті, що працюють з пам'яттю, а не з файлами.*

* * *

### **15 ❓ Питання:**

Який зв'язок між `PrintWriter`, `Locale` та методом `printf()`? ⭐️⭐️⭐️

> #### **💬 Відповідь:**
>
> Зв'язок проявляється у **форматуванні чисел та дат**, яке залежить від локалі.
>
> **`Locale`** — це об'єкт, що представляє певний географічний, політичний або культурний регіон. Він визначає правила форматування, такі як:
>
> * **Роздільник дробової частини** (крапка `.` чи кома `,`).
> * **Роздільник груп розрядів** (пробіл, кома).
> * **Символ валюти**.
> * **Формат дати та часу**.
>
> **Як `printf()` це використовує:**
>
> Метод `printf(Locale l, String format, Object... args)` дозволяє **явно вказати локаль**, яку слід використовувати для форматування.
>
> Якщо локаль не вказано, використовується **локаль за замовчуванням** для JVM.
>
> **Приклад:**
>
> ```java
> double number = 1234.56;
>
> // Використання локалі за замовчуванням (наприклад, US)
> writer.printf("Default: %.2f%n", number); // -> 1234.56
>
> // Явно вказуємо німецьку локаль
> writer.printf(Locale.GERMANY, "German: %.2f%n", number); // -> 1234,56
> ```
>
> `PrintWriter`, на відміну від `System.out.printf`, має конструктор `PrintWriter(File file, String csn)` і перевантажений метод `printf(Locale, String, Object...)` . Це робить його дуже потужним інструментом для створення **інтернаціоналізованих** текстових звітів.

Виправлю свою відповідь, `PrintWriter` **не має** такого конструктора або перевантаженого `printf`, це функція `String.format()` і `Formatter`.

**Виправлена відповідь:**
>
> #### **💬 Відповідь:**
>
> Зв'язок опосередкований. **`PrintWriter.printf()`** сам по собі **не приймає `Locale`** як аргумент. Однак, він використовує всередині себе клас `java.util.Formatter`, який **може** бути створений з явно вказаною локаллю.
>
> **Як це працює "під капотом":**
>
> * Виклик `writer.printf(format, args)` еквівалентний виклику `formatter.format(format, args)`, де `formatter` асоційований з потоком `writer`.
>
> **Для явного керування локаллю:**
>
> * Вам потрібно **створити `Formatter` з потрібною локаллю** і передати йому `PrintWriter` (точніше, його `Appendable`-представлення).
>
>     ```java
>     try (PrintWriter writer = new PrintWriter("report.txt")) {
>         Formatter formatter = new Formatter(writer, Locale.GERMANY);
>         formatter.format("Число: %.2f", 1234.56); // Запише "Число: 1234,56"
>     }
>     ```
>
> Або, що простіше, **сформувати рядок заздалегідь** за допомогою `String.format(Locale, ...)` і потім записати його у `PrintWriter`.
>
> ```java
> String formatted = String.format(Locale.GERMANY, "%.2f", 1234.56); // "1234,56"
> writer.print(formatted);
> ```
>
> Отже, хоча `PrintWriter` напряму не керує локаллю, він є частиною екосистеми, яка дозволяє виконувати форматування з урахуванням регіональних налаштувань.

#### **📌 Що перевіряють:**

*Експертне розуміння того, як працює форматування та інтернаціоналізація (i18n) в Java, та знання зв'язків між `PrintWriter`, `Formatter` та `Locale`.*
