---
layout: default
title: "LinkedList"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 5
---

# LinkedList

* короткий опис: `LinkedList` – це реалізація інтерфейсів `List` та `Deque`, що базується на подвійно зв'язаному списку. Кожен елемент (`Node`) зберігає посилання на наступний (`next`) та попередній (`prev`) вузли.

* поняття: `LinkedList`, подвійно зв'язаний список, `Node<E>`, `first`, `last`, `next`, `prev`.

* приклади: Швидке додавання/видалення елементів на початку/кінці списку (`addFirst()`, `removeLast()`).

* ключові моменти: Швидка вставка/видалення у будь-якому місці (O(1), якщо є посилання на елемент), але повільний доступ за індексом (O(N)) через необхідність обходу вузлів.

* що важливо знати на співбесіді: Внутрішня робота `LinkedList`, продуктивність операцій.

### **✅ Офіційне визначення:**

**`LinkedList`** — це реалізація інтерфейсу **`List`** та **`Deque`** у Java, яка базується на **`подвійно`** **`зв’язаному списку`**.

### **🧠 Простими словами:**

**`LinkedList`** — це список, де кожен елемент знає про сусідів зліва і справа, що дозволяє швидко додавати та видаляти елементи в будь-якому місці, але повільно шукати елемент за індексом

---

## **Структура LinkedList:**

* **Node\<E\>** — внутрішній приватний клас, який зберігає:
  * **`E item`** — сам елемент
  * **`Node<E> next`** — посилання на наступний вузол
  * **`Node<E> prev`** — посилання на попередній вузол
* Поля **`first`** і **`last`** — посилання на перший і останній елементи списку
* Додавання та видалення змінює зв’язки між сусідніми вузлами, оновлюючи поля **`next`** і **`prev`**

---

## **Пояснення на пальцях (💡)**

Уяви **`LinkedList`** як ланцюжок людей, де кожен тримає за руку сусідів зліва і справа. Якщо ти хочеш вставити нового в середину — просто відпускаєш руки старого і даєш новому взяти їх, ніякого зміщення не треба. Але щоб знайти конкретну людину, доводиться йти по ланцюжку від початку або кінця

---

## **Корисні приклади (🧪)**

```java
LinkedList<String> list = new LinkedList<>();
list.add("A");               // Додати в кінець
list.addFirst("Start");      // Додати на початок
list.addLast("End");         // Додати в кінець (альтернатива add)
list.remove("A");            // Видалити перше входження "A"
String first = list.getFirst();  // Отримати перший елемент
String last = list.getLast();    // Отримати останній елемент
String elem = list.get(1);        // Отримати елемент за індексом (повільно!)
```

---

## **Порівняння (⚖️)**

**`LinkedList`** vs **`ArrayList`**:

* Швидке додавання/видалення на початку, в кінці та середині (без зсуву)
* Повільний доступ за індексом (O(n)), бо треба пройти по вузлах від початку або кінця
* Більше витрат пам’яті через зберігання двох посилань (**`next`** і **`prev`**)

---

## **🧠 Мнемоніка для запам’ятовування:**

**`LinkedList`** — це ланцюжок з двома руками (**`prev`**, **`next`**), який швидко дає додати або прибрати будь-якого сусіда, але не любить, коли треба знайти гостя по імені (індексу)

---

## **🟩 Підсумок:**

**`LinkedList`** — оптимальний вибір, коли потрібно часто додавати або видаляти елементи у будь-якому місці списку, але рідко звертатися до елемента за індексом. Його структура на основі подвійно зв’язаного списку дозволяє легко змінювати зв’язки між елементами.

**🔗 Доповнююча інформація:**

* [**Oracle Docs LinkedList**](https://docs.oracle.com/en/java/javase/20/docs/api/java.base/java/util/LinkedList.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `LinkedList` і яка його внутрішня структура даних?

> #### **💬 Відповідь:**
>
> **`java.util.LinkedList`** — це клас в Java Collections Framework, який є реалізацією інтерфейсів **`List`** та **`Deque`** (двостороння черга).
>
> **Внутрішня структура:**
>
> `LinkedList` базується на **двозв'язному списку (doubly-linked list)**. Він складається з послідовності вузлів (`Node`). Кожен вузол містить три частини:
>
> 1. **Сам елемент (дані)**.
> 2. **Посилання (`next`)** на наступний вузол у списку.
> 3. **Посилання (`prev`)** на попередній вузол у списку.
>
> Сам клас `LinkedList` зберігає посилання тільки на перший (`first`) та останній (`last`) вузли. Завдяки цій структурі додавання або видалення елемента не вимагає зсуву інших елементів — потрібно лише змінити посилання у сусідніх вузлах.

#### **📝 Аналогія:**</h4>

Це як вагончики поїзда. Кожен вагончик (`Node`) з'єднаний з попереднім і наступним. Щоб додати новий вагончик у середину, не потрібно пересувати весь поїзд — достатньо роз'єднати два сусідні та вставити новий між ними.

#### **📌 Що перевіряють:**

*Розуміння базової структури двозв'язного списку та його ключових компонентів (вузол, посилання `next`/`prev`).*

* * *

### **2 ❓ Питання:**

Які сильні та слабкі сторони `LinkedList`? Коли його варто використовувати?

> #### **💬 Відповідь:**
>
> **Сильні сторони:**
>
> 1. **Швидке додавання та видалення елементів:** Операції додавання/видалення на початку, в кінці або в середині списку виконуються за **константний час (O(1))**, **за умови**, що у вас вже є посилання (ітератор) на місце вставки/видалення. Не потрібно зсувати елементи, як в `ArrayList`.
>
> 2. **Ефективна реалізація `Deque` та `Queue`:** Завдяки швидкому доступу до "голови" та "хвоста", `LinkedList` є хорошою реалізацією для черг та стеків.
>
> **Слабкі сторони:**
>
> 1. **Повільний доступ до елементів за індексом:** Операція `get(int index)` виконується за **лінійний час (O(n))**. Щоб знайти `i`-й елемент, потрібно пройти по ланцюжку `n/2` вузлів від початку або кінця.
> 2. **Більше використання пам'яті:** Кожен вузол `LinkedList` зберігає не тільки сам елемент, але й два додаткових посилання (`next` і `prev`), що збільшує накладні витрати на пам'ять порівняно з `ArrayList`.
>
> **Коли варто використовувати:**
>
> * Коли основні операції — це **часте додавання та видалення** елементів, особливо на початку або в кінці списку.
> * Коли ви реалізуєте **стек (LIFO) або чергу (FIFO)**, хоча для цього `ArrayDeque` зазвичай є більш продуктивним.
> * Коли доступ до елементів за індексом є рідкісною операцією.

#### **📌 Що перевіряють:**

*Здатність порівняти `LinkedList` з `ArrayList` за продуктивністю ключових операцій та вибрати правильну структуру для конкретної задачі.*

* * *

### **3 ❓ Питання:**

Які інтерфейси реалізує `LinkedList` і що це йому дає?

> #### **💬 Відповідь:**
>
> `LinkedList` реалізує три основні інтерфейси:
>
> 1. **`List<E>`**: Це означає, що він поводиться як **впорядкований список**. Він підтримує доступ за індексом (хоча й повільний), дозволяє дублікати та `null`.
>
> 2. **`Deque<E>` (Double-ended Queue):** Це двостороння черга. Реалізація цього інтерфейсу надає `LinkedList` потужні методи для роботи з обома кінцями списку:
>     * **`addFirst()` / `addLast()`**
>     * **`removeFirst()` / `removeLast()`**
>     * **`getFirst()` / `getLast()`**
>     * `push()` / `pop()` / `peek()` (для використання як стек)
>
> 3. **`Queue<E>`**: Оскільки `Deque` розширює `Queue`, `LinkedList` також може використовуватися як звичайна FIFO-черга (методи `offer()`, `poll()`, `peek()`).
>
> Завдяки цій універсальності, `LinkedList` можна використовувати як список, стек або чергу, залежно від того, через який інтерфейс ви з ним працюєте.

#### **📌 Що перевіряють:**

*Знання про універсальність `LinkedList` та його здатність реалізовувати декілька контрактів з Collections Framework.*

* * *

### **4 ❓ Питання:**

Порівняйте `ArrayList` та `LinkedList` за використанням пам'яті. ⭐️

> #### **💬 Відповідь:**
>
> **`LinkedList` зазвичай використовує значно більше пам'яті**, ніж `ArrayList` для зберігання тієї ж кількості елементів.
>
> **Чому так:**
>
> * **`ArrayList`:**
>   * Зберігає лише посилання на самі об'єкти у внутрішньому масиві `Object[]`.
>   * Накладні витрати: невеликі, в основному це "зайві" комірки в масиві, якщо `capacity > size`.
>
> * **`LinkedList`:**
>   * Для кожного елемента він створює **додатковий об'єкт-вузол (`Node`)**.
>   * Кожен об'єкт `Node` зберігає:
>         1. Посилання на сам елемент.
>         2. Посилання на наступний вузол (`next`).
>         3. Посилання на попередній вузол (`prev`).
>
> Отже, для кожного елемента в `LinkedList` є накладні витрати у вигляді об'єкта `Node` та двох додаткових посилань. На 64-бітній JVM це може бути приблизно 24 байти додаткової пам'яті на кожен елемент, не враховуючи сам елемент.
>
> Тому, якщо пам'ять є критичним ресурсом, `ArrayList` є значно кращим вибором.

#### **📌 Що перевіряють:**

*Розуміння того, як внутрішня структура даних впливає на споживання пам'яті. Здатність оцінювати не тільки часову, а й просторову складність.*

* * *

### **5 ❓ Питання:**

Чому доступ за індексом (`get(int index)`) у `LinkedList` має лінійну складність O(n)?

> #### **💬 Відповідь:**
>
> Це пов'язано з тим, що `LinkedList` не має прямого доступу до елементів, як масив. Щоб знайти елемент за індексом `i`, `LinkedList` повинен **пройти по ланцюжку вузлів** від початку або кінця списку.
>
> **Як працює `get(index)`:**
>
> 1. `LinkedList` перевіряє, чи знаходиться індекс у першій чи другій половині списку ( `index < (size >> 1)`).
> 2. **Якщо індекс у першій половині:** Він починає ітерацію з **першого елемента (`first`)** і рухається вперед за посиланнями `next` `index` разів.
> 3. **Якщо індекс у другій половині:** Він починає ітерацію з **останнього елемента (`last`)** і рухається назад за посиланнями `prev` `(size - 1 - index)` разів.
>
> У найгіршому випадку (доступ до елемента в середині списку) йому доведеться пройти приблизно `n/2` вузлів, де `n` — кількість елементів. Тому асимптотична складність цієї операції є **лінійною (O(n))**.

#### **📌 Що перевіряють:**

*Детальне розуміння внутрішнього алгоритму `get()` в `LinkedList` та причин його низької продуктивності.*

* * *

### **6 ❓ Питання:**

Чи можна сортувати `LinkedList` за допомогою `Collections.sort()`? Чи є це ефективним? ⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Метод `Collections.sort()` приймає будь-яку реалізацію `List`, тому `LinkedList` також можна передати.
>
> **Чи це ефективно?**
> **Ні, це вкрай неефективно.**
>
> **Чому?**
> Алгоритми сортування, що використовуються в `Collections.sort` (Timsort/Mergesort), інтенсивно використовують **доступ до елементів за індексом** (`get(i)`) та їх **заміну** (`set(i, e)`).
> Як ми знаємо, для `LinkedList` ці операції мають складність O(n). Це перетворює алгоритм сортування з хорошої продуктивності O(n log n) на дуже погану (близько **O(n^2)**).
>
> **Що робить `Collections.sort()` "під капотом"?**
> Він є достатньо розумним, щоб не сортувати `LinkedList` напряму. Він спочатку **копіює всі елементи з `LinkedList` у тимчасовий масив**, сортує цей масив, а потім **копіює відсортовані елементи назад** у `LinkedList`.
>
> Це все одно є неефективним через копіювання, тому найкраща практика — якщо вам потрібно часто сортувати список, **використовуйте `ArrayList`**.

#### **📌 Що перевіряють:**

*Здатність аналізувати продуктивність стандартних алгоритмів у поєднанні з різними структурами даних. Розуміння того, як JDK намагається оптимізувати такі неефективні сценарії.*

* * *

### **7 ❓ Питання:**

У яких випадках `LinkedList` буде швидшим за `ArrayList` для видалення елемента? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це залежить від того, **як саме** виконується видалення.
>
> * **`list.remove(int index)` (видалення за індексом):**
>   * Для `ArrayList`: O(n) через зсув.
>   * Для `LinkedList`: O(n) через пошук.
>   * **`ArrayList` тут, ймовірно, буде швидшим** через швидке копіювання пам'яті (`System.arraycopy`) та кращу локальність кешу, порівняно з проходом по посиланнях у `LinkedList`.
>
> * **`list.remove(Object o)` (видалення за об'єктом):**
>   * Обидві реалізації спочатку повинні **знайти** елемент, що займає O(n).
>   * Після знаходження `LinkedList` видалить його за O(1), а `ArrayList` — за O(n).
>   * Загальна складність в обох випадках — O(n), і знову `ArrayList` може бути швидшим через кращу локальність даних.
>
> * **Видалення за допомогою `Iterator.remove()` (ключовий випадок):**
>   * Ось тут `LinkedList` **перемагає**. Коли ви ітеруєте по списку і знаходите потрібний елемент, у вас вже є посилання на відповідний вузол.
>   * `iterator.remove()` для `LinkedList` виконується за **O(1)**.
>   * `iterator.remove()` для `ArrayList` все ще виконується за **O(n)**, оскільки йому потрібно зсунути всі наступні елементи.
>
> **Висновок:** `LinkedList` значно швидший тільки тоді, коли ви **видаляєте елемент, на який у вас вже є ітератор**, наприклад, під час ітерації.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, що складність операцій `add`/`remove` для `LinkedList` є O(1) **тільки тоді**, коли не потрібен попередній пошук елемента.*

* * *

### **8 ❓ Питання:**

Чому `ArrayDeque` є кращою реалізацією стеку, ніж `LinkedList`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча `LinkedList` також реалізує `Deque` і може використовуватися як стек, `ArrayDeque` є **рекомендованим і більш продуктивним** вибором.
>
> **Причини:**
>
> 1. **Ефективність по пам'яті:**
>     * `ArrayDeque` використовує **масив**, який є значно більш компактним.
>     * `LinkedList` для кожного елемента створює додатковий об'єкт `Node` з двома посиланнями, що призводить до більших накладних витрат на пам'ять.
>
> 2. **Продуктивність (швидкість):**
>     * Операції `push` ( `addFirst`) та `pop` (`removeFirst`) в `ArrayDeque` є дуже швидкими, оскільки вони зводяться до маніпуляцій з індексами у внутрішньому масиві.
>     * `ArrayDeque` має кращу **локальність кешу (cache locality)**. Елементи масиву знаходяться в пам'яті послідовно, що дозволяє процесору швидше зчитувати їх у кеш. У `LinkedList` вузли можуть бути розкидані по всій пам'яті, що призводить до більшої кількості промахів кешу.
>
> `LinkedList` може бути корисним тільки в рідкісних випадках, коли вам потрібно видаляти елементи з середини черги (що `ArrayDeque` не дозволяє ефективно робити), але для класичного стека/черги `ArrayDeque` є беззаперечним переможцем.

#### **📌 Що перевіряють:**

*Знання сучасних "best practices". Розуміння таких концепцій, як локальність кешу, та їхнього впливу на реальну продуктивність.*

* * *

### **9 ❓ Питання:**

Як реалізовано метод `size()` в `LinkedList`? Чи має він складність O(n)?

> #### **💬 Відповідь:**
>
> Це поширений міф, який базується на старих реалізаціях.
>
> У сучасних версіях JDK (починаючи з ранніх версій), `LinkedList` має **додаткове поле `size` типу `int`**.
>
> * Це поле **інкрементується** при кожному додаванні елемента (`add`, `addFirst` тощо).
> * Це поле **декрементується** при кожному видаленні елемента (`remove`, `poll` тощо).
>
> Тому метод `size()` просто **повертає значення цього поля**. Його складність є **константною (O(1))**.
>
> Немає ніякого проходу по всьому списку для підрахунку елементів. `LinkedList` був би абсолютно непридатним для використання, якби `size()` працював за O(n).

#### **📌 Що перевіряють:**

*Спростування поширених міфів. Знання актуальної реалізації, а не застарілих уявлень.*

* * *

### **10 ❓ Питання:**

Чи є ітератор для `LinkedList` `fail-fast`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, ітератор, що повертається методами `iterator()` та `listIterator()`, є **`fail-fast`**, так само, як і для `ArrayList`.
>
> Він використовує той самий механізм:
>
> 1. `LinkedList` має поле-лічильник `modCount`, яке збільшується при кожній структурній модифікації (додавання/видалення вузлів).
> 2. Ітератор при створенні запам'ятовує `modCount`.
> 3. При кожному виклику `next()` або `previous()` він перевіряє, чи не змінився `modCount` в оригінальному списку.
> 4. Якщо змінився, кидається **`ConcurrentModificationException`**.
>
> Тому модифікувати `LinkedList` під час ітерації за допомогою циклу `for-each` так само небезпечно, як і для `ArrayList`. Для безпечного видалення слід використовувати `iterator.remove()`.

#### **📌 Що перевіряють:**

*Знання того, що механізм `fail-fast` є загальним для більшості колекцій з `java.util`.*

* * *

### **11 ❓ Питання:**

Припустимо, ви ітеруєте по `LinkedList` і хочете додати новий елемент одразу після поточного. Як це зробити найкращим чином? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цього найкраще використовувати **`ListIterator`**, оскільки він надає метод `add(E e)`.
>
> **Як працює `listIterator.add(E e)`:**
>
> Він вставляє новий елемент **перед** тим елементом, який буде повернутий наступним викликом `next()`, і **після** того, який був повернутий останнім викликом `previous()`.
>
> **Приклад:**
>
> ```java
> LinkedList<String> list = new LinkedList<>(List.of("A", "C"));
> ListIterator<String> iterator = list.listIterator();
>
> while (iterator.hasNext()) {
>     String current = iterator.next();
>     if (current.equals("A")) {
>         // Додаємо "B" одразу після "A"
>         iterator.add("B");
>     }
> }
>
> System.out.println(list); // Виведе [A, B, C]
> ```
>
> Ця операція є дуже ефективною для `LinkedList`, оскільки ітератор вже "знає", де знаходиться, і вставка вимагає лише зміни кількох посилань.

#### **📌 Що перевіряють:**

*Знання просунутих можливостей `ListIterator` та вміння застосовувати його для ефективної модифікації `List`.*

* * *

### **12 ❓ Питання:**

У `LinkedList` є методи `add()`, `offer()`, `push()`. Всі вони додають елементи. У чому їхня відмінність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Всі вони додають елементи, але вони належать до **різних інтерфейсів**, які реалізує `LinkedList`, і мають різну семантику.
>
> 1. **`add(E e)`:**
>     * **Звідки:** З інтерфейсу `List` та `Collection`.
>     * **Що робить:** Додає елемент **в кінець** списку.
>     * **Поведінка при помилці:** Зазвичай повертає `true`, але контракт `Collection` дозволяє кидати виняток, якщо додавання не вдалося (хоча для `LinkedList` це не так).
>
> 2. **`offer(E e)` (або `offerLast(E e)`):**
>     * **Звідки:** З інтерфейсу `Queue` та `Deque`.
>     * **Що робить:** Додає елемент **в кінець** черги (списку).
>     * **Поведінка при помилці:** Повертає `false`, якщо елемент не можна додати (наприклад, у черзі з обмеженим розміром). Більш "м'який", ніж `add`.
>
> 3. **`push(E e)`:**
>     * **Звідки:** З інтерфейсу `Deque`, призначений для використання як **стек**.
>     * **Що робить:** Додає елемент **на початок** списку. Еквівалентний `addFirst(e)`.
>
> Вибір методу залежить від того, яку саме структуру даних ви емулюєте за допомогою `LinkedList`: звичайний список (`add`), чергу (`offer`) чи стек (`push`).

#### **📌 Що перевіряють:**

*Детальне знання API та розуміння того, що семантика методів визначається інтерфейсами, які вони реалізують.*

* * *

### **13 ❓ Питання:**

Яку колекцію з JDK 1.1 `LinkedList` замінив або є кращою альтернативою? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча прямої заміни не було, `LinkedList` разом з `ArrayDeque` стали сучасною і кращою альтернативою для застарілого класу **`java.util.Stack`**.
>
> **Проблеми `Stack`:**
>
> * Він успадковує від `Vector`, що є поганим дизайном. Стек — це не вектор.
> * Він синхронізований, що знижує продуктивність.
>
> **Як `LinkedList`/`ArrayDeque` вирішують це:**
>
> * Вони реалізують інтерфейс `Deque`.
> * Цей інтерфейс надає повний набір операцій для роботи зі стеком (LIFO):
>   * `push(e)` — додати елемент на вершину.
>   * `pop()` — взяти і видалити елемент з вершини.
>   * `peek()` — подивитися на елемент на вершині, не видаляючи.
>
> **Сучасна практика:** Для реалізації стеку завжди слід використовувати `Deque`, а його конкретною реалізацією зазвичай обирають `ArrayDeque` через кращу продуктивність.
>
> `Deque<String> stack = new ArrayDeque<>();`

#### **📌 Що перевіряють:**

*Знання історії JDK та "best practices". Розуміння, чому старі класи є поганими і які є їхні сучасні аналоги.*

* * *

### **14 ❓ Питання:**

Чому `LinkedList` не реалізує маркерний інтерфейс `RandomAccess`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`RandomAccess`** — це маркерний інтерфейс, який сигналізує, що реалізація `List` надає **швидкий (зазвичай, O(1)) довільний доступ** до елементів за індексом.
>
> **`LinkedList` не реалізує цей інтерфейс**, тому що він **не** надає швидкого доступу за індексом. Його операція `get(int index)` має **лінійну складність O(n)**.
>
> **Навіщо цей інтерфейс потрібен?**
>
> Він служить підказкою для алгоритмів, які можуть оптимізувати свою роботу залежно від структури списку.
>
> **Приклад:**
> Алгоритм може перевірити `list instanceof RandomAccess`.
>
> * **Якщо `true` (це `ArrayList`):** Він може безпечно використовувати ітерацію за індексом (`for (int i=0;...)`).
> * **Якщо `false` (це `LinkedList`):** Він має використовувати ітератор (`Iterator`), щоб уникнути жахливої продуктивності `get()` в циклі.
>
> Саме так працюють деякі алгоритми в `Collections`, вибираючи оптимальну стратегію обходу залежно від типу `List`.

#### **📌 Що перевіряють:**

*Знання про існування `RandomAccess` та його роль як механізму для оптимізації алгоритмів, що працюють з `List`.*

* * *

### **15 ❓ Питання:**

Якщо вам потрібно зберегти колекцію унікальних елементів, зберігши порядок їх додавання, і при цьому часто додавати/видаляти елементи з середини. Яку комбінацію колекцій ви б використали? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це складне завдання, оскільки жодна стандартна колекція не задовольняє всім цим вимогам одночасно.
>
> * Унікальні елементи -> `Set`.
> * Збереження порядку додавання -> `LinkedHashSet`.
> * Часте додавання/видалення з середини -> `LinkedList` (але він дозволяє дублікати).
>
> **Рішення:** Потрібно скомбінувати дві структури.
>
> **Найкращий підхід** — це **`LinkedHashMap<K, V>`**, яку можна використовувати для реалізації **`LinkedHashSet`** (саме так `LinkedHashSet` і реалізований "під капотом").
>
> Якщо потрібно зробити це вручну або для більш складного сценарію:
>
> **Комбінація `LinkedList` та `HashSet`:**
>
> 1. Основною структурою для зберігання буде `LinkedList`, оскільки він забезпечує ефективну вставку/видалення та зберігає порядок.
> 2. Додатковою структурою буде `HashSet`, який буде використовуватися **тільки для швидкої перевірки на унікальність** (O(1)).
>
> **Реалізація власного класу `OrderedUniqueList`:**
>
> ```java
> public class OrderedUniqueList<E> {
>     private LinkedList<E> list = new LinkedList<>();
>     private HashSet<E> set = new HashSet<>();
>
>     public boolean add(E element) {
>         // Швидка перевірка на унікальність
>         if (set.add(element)) {
>             // Якщо елемент унікальний, додаємо в обидві колекції
>             list.add(element);
>             return true;
>         }
>         return false;
>     }
>
>     public boolean remove(E element) {
>         if(set.remove(element)){
>             list.remove(element);
>             return true;
>         }
>         return false;
>     }
>     //... інші методи
> }
> ```
>
> Цей підхід комбінує сильні сторони обох структур даних, хоча і вимагає трохи більше пам'яті та написання власного класу-обгортки. Але для поставлених вимог — це оптимальне рішення.

#### **📌 Що перевіряють:**

*Здатність аналізувати складні вимоги та проектувати власні структури даних, комбінуючи існуючі. Це демонструє високий рівень розуміння Collections Framework.*
