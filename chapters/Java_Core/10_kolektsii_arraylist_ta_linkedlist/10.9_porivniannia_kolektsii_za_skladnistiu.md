---
layout: default
title: "Порівняння колекцій за складністю"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 9
---

# Порівняння колекцій за складністю

* короткий опис: Оцінка ефективності основних операцій (`add`, `get`, `remove`, `contains`) для різних колекцій Java (`ArrayList`, `LinkedList`, `HashMap`, `TreeMap`, `HashSet`, `TreeSet`) за асимптотичною складністю (Big-O).

* поняття: `ArrayList`, `LinkedList`, `HashMap`, `TreeMap`, `HashSet`, `TreeSet`, `O(1)`, `O(N)`, `O(log N)`.

* приклади: Таблиця складності для різних операцій. `HashMap`/`HashSet` – O(1) у середньому; `TreeMap`/`TreeSet` – O(log N).

* ключові моменти: `HashMap`/`HashSet` є найшвидшими для пошуку без порядку, `TreeMap`/`TreeSet` для сортованих даних. `LinkedList` ефективний для вставок/видалень на краях, `ArrayList` – для доступу за індексом.

* що важливо знати на співбесіді: Продуктивність основних операцій для ключових колекцій.

### **✅ Офіційне визначення:**

Оцінка ефективності операцій (додавання, пошук, видалення) для різних структур даних Java Collections Framework за асимптотичною складністю.

### **🧠 Простими словами:**

Як швидко і ефективно працюють різні колекції Java у типових операціях, щоб знати, коли що використовувати.

---

## **Основне**

| Колекція | Додавання | Пошук (contains/get) | Видалення | Особливість / коментар |
| :---: | :---: | :---: | :---: | :---: |
| **ArrayList** | O(1)\* (амортиз.) | O(n) | O(n) | Додаток в кінець швидкий, пошук лінійний |
| **LinkedList** | O(1) | O(n) | O(1) (якщо є посилання на елемент) / O(n) (за значенням) | Добре для вставок/видалень на початку/кінці |
| **HashMap** | O(1)\* (амортиз.) | O(1)\* | O(1)\* | Відмінна швидкість для ключів, але немає порядку |
| **TreeMap** | O(log n) | O(log n) | O(log n) | Зберігає порядок за ключем, повільніший за HashMap |

**`* Амортизована складність`** — середня по багатьох операціях, іноді можуть бути повільніші.

**Особливості "найгіршого випадку"**

* **`HashMap`** у найгіршому випадку (коли багато колізій) може деградувати до **`O(n)`** для операцій, але в Java 8+ використовується дерево замість списку для колізій, що покращує до **`O(log n)`**.
* **`ArrayList`** при збільшенні масиву виконує resize з копіюванням, що разово займає O(n), але амортизовано **`O(1)`** на операцію.
* **`LinkedList`** повільний для випадкового доступу — **`O(n)`** за індексом.

---

## **Пояснення на пальцях (💡)**

* **`ArrayList`** — як зошит, куди швидко додаєш нову сторінку в кінець, але щоб знайти слово, потрібно листати весь зошит.
* **`LinkedList`** — це як ланцюжок зі сторінок, де можна швидко вставити чи видалити сторінку, але знайти потрібну треба з початку.
* **`HashMap`** — наче телефонна книга, де за іменем (ключем) ти одразу бачиш номер (значення).
* **`TreeMap`** — це вже телефонна книга, але з алфавітним порядком, тож пошук трохи повільніший, але впорядкований.

---

## **Корисні приклади (🧪)**

```java
// ArrayList - додавання в кінець (амортизовано O(1))
ArrayList<String> list = new ArrayList<>();
list.add("apple");

// LinkedList - додавання на початок (O(1))
LinkedList<String> linkedList = new LinkedList<>();
linkedList.addFirst("banana");

// HashMap - пошук за ключем (O(1))
HashMap<Integer, String> map = new HashMap<>();
map.put(1, "John");
String name = map.get(1);

// TreeMap - зберігає ключі впорядкованими (O(log n))
TreeMap<Integer, String> treeMap = new TreeMap<>();
treeMap.put(3, "C");
treeMap.put(1, "A");
treeMap.put(2, "B");
```

---

## **Вибір правильної структури даних залежно від задачі (⚖️)**

* Якщо потрібен швидкий випадковий доступ і додавання в кінець — **`ArrayList`**.
* Якщо часто вставляєш/видаляєш елементи на початку або в середині — **`LinkedList`**.
* Якщо важливий швидкий пошук за ключем без впорядкування — **`HashMap`**.
* Якщо потрібен відсортований словник за ключем — **`TreeMap`**.

---

## **🧠 Мнемоніка для запам’ятовування:**

**`ArrayList`** — швидкий доступ,

**`LinkedList`** — гнучка вставка,

**`HashMap`** — блискавичний пошук,

**`TreeMap`** — впорядкований друг

---

## **🟩 Підсумок:**

Розуміння складності операцій у колекціях Java допомагає обирати найбільш підходящу структуру для задачі, покращуючи продуктивність і ефективність коду. Знання "найгіршого випадку" допомагає передбачити потенційні проблеми.

### **🔗 Додаткові джерела:**

* [**Big-O Cheatsheet \- Collections**](https://www.bigocheatsheet.com/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яку складність мають основні операції для `ArrayList`?

> #### **💬 Відповідь:**
>
> | Операція | Складність | Пояснення |
> |---|---|---|
> | **`get(index)`** | **O(1)** | Дуже швидкий доступ за індексом, оскільки `ArrayList` базується на масиві. |
> | **`add(E element)` (в кінець)** | **O(1) амортизовано** | Зазвичай швидкий, але іноді може вимагати розширення масиву (O(n)), тому середній час — O(1). |
> | **`add(index, E element)` (в середину)** | **O(n)** | Вимагає зсуву всіх наступних елементів. Чим ближче до початку, тим повільніше. |
> | **`remove(index)`** | **O(n)** | Аналогічно до додавання, вимагає зсуву елементів. |
> | **`contains(Object o)`** | **O(n)** | Вимагає лінійного пошуку по всьому списку. |
>
> **Коли використовувати:** `ArrayList` є найкращим вибором для сценаріїв, де **переважає читання та доступ за індексом**, а модифікації відбуваються переважно в кінці списку.

#### **📌 Що перевіряють:**

*Знання продуктивності найпоширенішої реалізації `List`. Особливо важливе розуміння амортизованої складності `add`.*

* * *

### **2 ❓ Питання:**

Яку складність мають основні операції для `LinkedList`?

> #### **💬 Відповідь:**
>
> | Операція | Складність | Пояснення |
> |---|---|---|
> | **`get(index)`** | **O(n)** | Дуже повільний. Потрібно пройти по ланцюжку вузлів від початку або кінця. |
> | **`addFirst(E e)`, `addLast(E e)`** | **O(1)** | Дуже швидкі операції, оскільки `LinkedList` зберігає посилання на "голову" і "хвіст". |
> | **`add(index, E e)`** | **O(n)** | Складність в основному визначається пошуком місця для вставки (O(n)), сама вставка є швидкою (O(1)). |
> | **`removeFirst()`, `removeLast()`** | **O(1)** | Дуже швидкі операції. |
> | **`remove(index)`** | **O(n)** | Аналогічно до додавання, спочатку пошук, потім видалення. |
> | **`contains(Object o)`** | **O(n)** | Вимагає лінійного пошуку. |
>
> **Коли використовувати:** `LinkedList` ефективний, коли вам потрібно часто **додавати/видаляти елементи з початку або кінця списку**, або коли ви ітеруєте по ньому і модифікуєте через ітератор.

#### **📌 Що перевіряють:**

*Знання сильних та слабких сторін `LinkedList`, зокрема різниці у складності між доступом за індексом та модифікацією на краях.*

* * *

### **3 ❓ Питання:**

Яку складність мають операції `add`, `remove`, `contains` для `HashSet`? Від чого вона залежить?

> #### **💬 Відповідь:**
>
> * **Середній випадок: O(1) (константний час).**
>     Це те, на що ми розраховуємо при використанні `HashSet`. Швидкість досягається завдяки тому, що хеш-код елемента дозволяє миттєво визначити "кошик" (bucket), де він має знаходитися.
>
> * **Найгірший випадок: O(n) до Java 8, O(log n) з Java 8+.**
>     Цей випадок виникає при **великій кількості хеш-колізій**, коли багато елементів потрапляють в один кошик.
>
> **Від чого залежить продуктивність:**
>
> Продуктивність `HashSet` критично залежить від **якості реалізації методу `hashCode()`** для об'єктів, що зберігаються. Хороший `hashCode` забезпечує рівномірний розподіл, мінімізує колізії і гарантує продуктивність, близьку до O(1).
>
> Якщо `hashCode` реалізовано погано (наприклад, завжди повертає те саме число), `HashSet` вироджується в іншу структуру даних (зв'язаний список або дерево), і всі переваги хешування втрачаються.

#### **📌 Що перевіряють:**

*Розуміння того, що продуктивність хеш-структур напряму залежить від якості хеш-функції.*

* * *

### **4 ❓ Питання:**

Порівняйте `HashSet` та `TreeSet` за складністю основних операцій.

> #### **💬 Відповідь:**
>
> | Операція | `HashSet` (на основі хеш-таблиці) | `TreeSet` (на основі червоно-чорного дерева) |
> |---|---|---|
> | **`add(e)`** | **O(1)** в середньому | **O(log n)** |
> | **`remove(o)`** | **O(1)** в середньому | **O(log n)** |
> | **`contains(o)`** | **O(1)** в середньому | **O(log n)** |
>
> **Ключові відмінності у виборі:**
>
> * **`HashSet`** є значно **швидшим** для базових операцій додавання, видалення та пошуку. Його слід використовувати, коли вам не важливий порядок елементів.
> * **`TreeSet`** є **повільнішим**, але він гарантує, що елементи завжди будуть зберігатися у **відсортованому порядку**. Його слід використовувати, коли вам потрібна сортована множина.
> * `TreeSet` також вимагає, щоб елементи були порівнюваними (`implements Comparable`) або щоб було надано `Comparator`.

#### **📌 Що перевіряють:**

*Здатність порівняти дві основні реалізації `Set` і вибрати потрібну залежно від вимог (швидкість vs порядок).*

* * *

### **5 ❓ Питання:**

Яку складність мають основні операції для `HashMap`? ⭐️

> #### **💬 Відповідь:**
>
> `HashMap` має таку ж складність, як і `HashSet`, оскільки `HashSet` "під капотом" є просто обгорткою над `HashMap`.
>
> | Операція | Складність |
> |---|---|
> | **`put(K, V)`** | **O(1)** в середньому, O(log n) у найгіршому випадку (з Java 8) |
> | **`get(K)`** | **O(1)** в середньому, O(log n) у найгіршому випадку |
> | **`remove(K)`** | **O(1)** в середньому, O(log n) у найгіршому випадку |
> | **`containsKey(K)`**| **O(1)** в середньому, O(log n) у найгіршому випадку |
>
> Продуктивність `HashMap` також критично залежить від реалізації `hashCode()` та `equals()` для **ключів**.

#### **📌 Що перевіряють:**

*Знання асимптотичної складності найважливішої структури даних у Java.*

* * *

### **6 ❓ Питання:**

Яку складність мають основні операції для `TreeMap`? ⭐️

> #### **💬 Відповідь:**
>
> `TreeMap` базується на **червоно-чорному дереві**, що є збалансованим бінарним деревом пошуку. Це визначає його продуктивність.
>
> | Операція | Складність | Пояснення |
> |---|---|---|
> | **`put(K, V)`** | **O(log n)** | Потрібно знайти місце в дереві для нового вузла та, можливо, виконати балансування. |
> | **`get(K)`** | **O(log n)** | Пошук у збалансованому дереві займає логарифмічний час. |
> | **`remove(K)`** | **O(log n)** | Аналогічно до `put`, вимагає пошуку та можливого балансування. |
> | **`containsKey(K)`**| **O(log n)** | Також базується на пошуку в дереві. |
>
> `TreeMap` є повільнішим за `HashMap`, але його основна перевага — **ключі завжди зберігаються у відсортованому порядку**. Це дозволяє ефективно виконувати операції, що залежать від порядку, наприклад, знаходити "найближчий" ключ.

#### **📌 Що перевіряють:**

*Знання складності для деревних структур даних та їх компромісів (швидкість vs порядок).*

* * *

### **7 ❓ Питання:**

Чому доступ до елемента в середині `LinkedList` має складність O(n), а видалення — O(1)? Поясніть цей нюанс. ⭐️

> #### **💬 Відповідь:**
>
> Це важливий нюанс, який часто неправильно розуміють.
>
> 1. **Доступ за індексом (`get(index)`):**
>     Щоб отримати елемент з індексом `i`, `LinkedList` повинен пройти **n/2** вузлів від початку або кінця списку. Ця операція **пошуку** має складність **O(n)**.
>
> 2. **Видалення:**
>     * Якщо ви викликаєте **`remove(index)`** або **`remove(Object)`**, то спочатку виконується **пошук** елемента, що займає **O(n)**. А вже після цього саме видалення (зміна посилань у сусідніх вузлах) займає O(1). Загальна складність операції все одно **O(n)**.
>     * Складність **O(1)** для видалення досягається тільки тоді, коли вам **не потрібно шукати елемент**, тобто коли у вас **вже є посилання (ітератор) на цей вузол**. Наприклад, під час ітерації за допомогою `Iterator` та виклику `iterator.remove()`. Ітератор "пам'ятає" поточну позицію, тому пошук не потрібен.
>
> Таким чином, низька складність `LinkedList` для видалення/вставки проявляється лише тоді, коли пошук позиції є тривіальним (початок/кінець) або вже виконаний.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що складає повну вартість операції (пошук + дія), і коли `LinkedList` дійсно має перевагу.*

* * *

### **8 ❓ Питання:**

Порівняйте часову складність `ArrayList` та `LinkedList` для додавання великої кількості елементів в кінець списку. ⭐️

> #### **💬 Відповідь:**
>
> Для додавання елементів **в кінець списку** обидві структури мають **амортизовану складність O(1)**. Однак на практиці їхня продуктивність може відрізнятися.
>
> * **`LinkedList`:**
>     Кожне додавання в кінець — це стабільна і швидка операція **O(1)**: створення нового вузла та оновлення посилання `last`.
>
> * **`ArrayList`:**
>   * Більшість додавань виконуються за **O(1)**.
>   * Але **іноді** відбувається **розширення масиву** — дорога операція **O(n)**, яка копіює всі існуючі елементи в новий масив.
>
> **Результат:**
>
> Якщо ви додаєте велику кількість елементів, `ArrayList` буде періодично "завмирати" для виконання операцій розширення. `LinkedList` буде додавати елементи більш рівномірно за часом.
>
> Проте, через кращу локальність кешу та відсутність створення об'єктів-вузлів, загальний час для **`ArrayList` може виявитися меншим**, особливо якщо ви заздалегідь встановите потрібну ємність через `ensureCapacity()` або конструктор, щоб уникнути розширень.
>
> **Коротко:** Асимптотично вони однакові (O(1) амортизовано), але на практиці `ArrayList` часто виявляється швидшим, якщо уникнути частих розширень.

#### **📌 Що перевіряють:**

*Здатність аналізувати не тільки асимптотику, а й реальну продуктивність з урахуванням апаратних особливостей (кеш) та внутрішніх механізмів (розширення).*

* * *

### **9 ❓ Питання:**

Яка складність у `PriorityQueue` для операцій додавання (`offer`) та отримання (`poll`) елемента? Чому вона така? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для `PriorityQueue` складність операцій `offer` та `poll` становить **O(log n)**.
>
> **Чому така складність?**
>
> `PriorityQueue` внутрішньо реалізована на основі **бінарної купи (binary heap)**.
>
> * **Додавання (`offer`):**
>     1. Новий елемент додається в кінець купи (на останній рівень). Це O(1).
>     2. Потім він "спливає" вгору по дереву, міняючись місцями з батьківськими елементами, доки не відновиться властивість купи ( `sift-up` операція). Висота дерева дорівнює `log n`, тому цей процес займає **O(log n)**.
>
> * **Отримання/видалення (`poll`):**
>     1. Повертається кореневий елемент (найменший/найбільший). Це O(1).
>     2. На його місце ставиться останній елемент купи.
>     3. Потім цей елемент "тоне", опускаючись вниз по дереву і міняючись місцями з дочірніми елементами, доки не відновиться властивість купи (`sift-down` операція). Це також займає **O(log n)**.
>
> Саме використання купи дозволяє ефективно підтримувати частково впорядковану структуру і швидко отримувати доступ до елемента з найвищим пріоритетом.

#### **📌 Що перевіряють:**

*Знання внутрішньої реалізації `PriorityQueue` (купа) та вміння пояснити логарифмічну складність її операцій.*

* * *

### **10 ❓ Питання:**

Виберіть оптимальну колекцію для наступних сценаріїв і поясніть її часову складність для ключової операції.

1. Зберігання унікальних ID користувачів для швидкої перевірки існування.
2. Реалізація черги завдань, де завдання з вищим пріоритетом обробляються першими.
3. Зберігання списку історичних транзакцій, які не змінюються і часто переглядаються по порядку.

> #### **💬 Відповідь:**
>
> 1. **Унікальні ID для швидкої перевірки:**
>     * **Колекція:** `HashSet`.
>     * **Ключова операція:** `contains(id)`.
>     * **Складність:** **O(1)** в середньому. `HashSet` забезпечує найшвидший можливий пошук.
>
> 2. **Черга завдань з пріоритетами:**
>     * **Колекція:** `PriorityQueue`.
>     * **Ключова операція:** `poll()` (отримання завдання з найвищим пріоритетом).
>     * **Складність:** **O(log n)**. Вона завжди ефективно повертає пріоритетний елемент.
>
> 3. **Список незмінних транзакцій для перегляду:**
>     * **Колекція:** `ArrayList` (або просто `List`, можливо, імутабельний, створений через `List.of()`).
>     * **Ключова операція:** `get(index)` для доступу та ітерація.
>     * **Складність:** `get(index)` — **O(1)**, ітерація — O(n). Оскільки дані не змінюються, повільні операції `add/remove` в середині не мають значення, а швидкий доступ за індексом є перевагою.

#### **📌 Що перевіряють:**

*Практичне вміння вибирати правильну структуру даних на основі аналізу вимог задачі.*

* * *

### **11 ❓ Питання:**

Яку просторову складність (Space Complexity) мають `ArrayList` та `LinkedList`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> В обох випадках просторова складність для зберігання `n` елементів є **O(n)**, але фактичне використання пам'яті різне.
>
> * **`ArrayList`:**
>   * Складність **O(n)**.
>   * Пам'ять потрібна для зберігання `n` посилань на об'єкти у внутрішньому масиві.
>   * Має **менші накладні витрати**, ніж `LinkedList`, але може виділяти зайву пам'ять, якщо `capacity > size`.
>
> * **`LinkedList`:**
>   * Складність **O(n)**.
>   * Пам'ять потрібна для зберігання `n` елементів, але **з великими накладними витратами**.
>   * Для кожного елемента створюється об'єкт `Node`, який містить посилання на сам елемент, на наступний вузол та на попередній. Таким чином, для зберігання одного елемента потрібно сам елемент + об'єкт `Node` + 2 посилання.
>
> **Висновок:** Хоча асимптотична просторова складність однакова (лінійна), **`LinkedList` споживає значно більше пам'яті на кожен елемент**, ніж `ArrayList`.

#### **📌 Що перевіряють:**

*Знання не тільки про часову, а й про просторову складність. Вміння аналізувати використання пам'яті різними структурами даних.*

* * *

### **12 ❓ Питання:**

Що таке амортизована складність `O(1)` для `ArrayList.add()`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Амортизована складність** — це середній час виконання операції, розрахований на довгій послідовності операцій.
>
> Для `ArrayList.add(E element)`:
>
> * Більшість операцій додавання є дуже швидкими, **O(1)**. Це відбувається, коли у внутрішньому масиві є вільне місце.
> * Іноді, коли масив заповнюється, відбувається **розширення**. Ця одна операція є дуже повільною — **O(n)**, оскільки вимагає створення нового масиву і копіювання `n` елементів.
>
> **Чому амортизовано виходить O(1)?**
>
> Тому що "вартість" рідкісної повільної операції O(n) "розмазується" по всіх попередніх швидких операціях O(1).
>
> Уявімо, що масив збільшується вдвічі. Щоб заповнити масив розміром `n`, а потім розширити його до `2n`, нам знадобилося `n` швидких додавань і одна "дорога" операція копіювання `n` елементів.
>
> Загальна вартість ~ `n` (за додавання) + `n` (за копіювання) = `2n`.
> Якщо розділити загальну вартість `2n` на кількість операцій `n`, отримаємо середню вартість `2`. А `2` — це константа, отже, **амортизована складність дорівнює O(1)**.

#### **📌 Що перевіряють:**

*Глибоке розуміння концепції амортизованого аналізу на класичному прикладі з `ArrayList`.*

* * *

### **13 ❓ Питання:**

Уявіть, що ви перебираєте `List` і для кожного елемента перевіряєте його наявність у `Set`. Якою буде загальна складність? `for (e : list) { set.contains(e); }` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Загальна складність буде залежати від конкретних реалізацій `List` та `Set`.
>
> * Цикл `for-each` по будь-якому `List` (і `ArrayList`, і `LinkedList`) виконується за **O(n)**, де `n` — кількість елементів у `list`.
>
> * Складність `set.contains(e)` залежить від реалізації `Set`.
>
> **Сценарії:**
>
> 1. **`List` + `HashSet`:**
>     * Ітерація по `List`: O(n)
>     * `HashSet.contains(e)`: O(1) в середньому.
>     * **Загальна складність:** n * O(1) = **O(n)**. Це дуже ефективний підхід.
>
> 2. **`List` + `TreeSet`:**
>     * Ітерація по `List`: O(n)
>     * `TreeSet.contains(e)`: O(log k), де `k` — кількість елементів у `set`.
>     * **Загальна складність:** n * O(log k) = **O(n log k)**.
>
> 3. **`ArrayList` + `ArrayList` (найгірший випадок):**
>     * Якщо замість `Set` був би `ArrayList`: `list2.contains(e)` мало б складність O(k).
>     * Загальна складність була б **O(n * k)**, тобто квадратичною.
>
> Це демонструє, як вибір правильної колекції (`HashSet`) для операцій пошуку може кардинально вплинути на загальну продуктивність алгоритму.

#### **📌 Що перевіряють:**

*Здатність аналізувати складність комбінованих алгоритмів. Розуміння того, як вибрати правильні структури даних для вкладених операцій.*

* * *

### **14 ❓ Питання:**

У чому відмінність складності для `Collections.sort(List)` для `ArrayList` та `LinkedList`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Для `ArrayList`:**
>   * Складність буде **O(n log n)**.
>   * `ArrayList` реалізує `RandomAccess`, тому `Collections.sort` (точніше, `List.sort`) буде використовувати ефективний алгоритм, як-от Timsort, який активно використовує швидкий доступ за індексом.
>
> * **Для `LinkedList`:**
>   * Теоретична складність **також O(n log n)**, але на практиці він буде **значно повільнішим**.
>   * Оскільки `LinkedList` не надає швидкого доступу за індексом, наївне сортування було б O(n²).
>   * Щоб уникнути цього, реалізація `Collections.sort()` (і `List.sort()` за замовчуванням) **оптимізована**: вона спочатку **копіює всі елементи з `LinkedList` у тимчасовий масив**, сортує цей масив (за O(n log n)), а потім **копіює елементи назад** у `LinkedList`.
>   * Ці операції копіювання вносять значні накладні витрати, тому реальна продуктивність значно гірша, ніж для `ArrayList`.
>
> Таким чином, хоча асимптотична складність в обох випадках однакова, реальний час виконання для `LinkedList` буде набагато більшим.

#### **📌 Що перевіряють:**

*Знання про оптимізації всередині `Collections.sort` та розуміння того, що асимптотична складність не завжди відображає реальну продуктивність, якщо ігнорувати вартість базових операцій.*

* * *

### **15 ❓ Питання:**

Яка складність видалення всіх елементів з `ArrayList` (`clear()`) та `LinkedList` (`clear()`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`ArrayList.clear()`:**
>   * Має складність **O(n)**, де `n` — кількість елементів у списку.
>   * Він **проходить по внутрішньому масиву** і присвоює **`null`** кожній комірці, що містить елемент. Це необхідно, щоб збирач сміття міг звільнити пам'ять, зайняту цими об'єктами. Сам масив не видаляється і його `capacity` не змінюється.
>
> * **`LinkedList.clear()`:**
>   * Має складність **O(n)**.
>   * Він також повинен пройти по **всім вузлам (`Node`) списку** і "обнулити" їхні посилання (`item`, `next`, `prev`), щоб і самі вузли, і елементи, які вони містять, стали доступними для збирача сміття.
>   * Крім того, він встановлює `first = null`, `last = null` і `size = 0`.
>
> Хоча можна було б уявити, що для `LinkedList` достатньо обнулити `first` та `last` (що було б O(1)), цього недостатньо, бо тоді всі проміжні вузли залишилися б в пам'яті, утворюючи витік. Тому `clear()` для обох колекцій має лінійну складність.

#### **📌 Що перевіряють:**

*Дуже детальне розуміння внутрішньої реалізації та управління пам'яттю. Здатність мислити про те, як працює збирач сміття у зв'язку з колекціями.*
