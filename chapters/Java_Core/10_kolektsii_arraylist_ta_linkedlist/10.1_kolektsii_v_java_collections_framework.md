---
layout: default
title: "Колекції в Java (Collections Framework)"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 1
---

# Колекції в Java (Collections Framework)

* короткий опис: Collections Framework – це набір інтерфейсів та класів у Java, що представляє групу об'єктів та дозволяє зручно зберігати, обробляти й маніпулювати ними.

* поняття: Collections Framework, `Collection` (кореневий інтерфейс), `List` (впорядкований), `Set` (унікальний), `Queue` (FIFO).

* приклади: `ArrayList` (реалізація `List`), `HashSet` (реалізація `Set`), `LinkedList` (реалізація `Queue`).

* ключові моменти: Уніфікований підхід до роботи з даними, допомагає обирати правильну структуру даних для конкретної задачі.

* що важливо знати на співбесіді: Що таке колекції, основні інтерфейси, їх відмінності.

### **✅ Офіційне визначення:**

**`Колекції`** — це структура даних у Java, що представляє групу об’єктів (елементів), яка дозволяє зручно зберігати, обробляти та маніпулювати цими об’єктами за допомогою спеціальних інтерфейсів та класів.

### **🧠 Простими словами:**

**`Колекції`** — це контейнери для об'єктів, які дозволяють додавати, видаляти, шукати та впорядковувати елементи, як-от список покупок або черга людей.

---

## **📊 Основні інтерфейси Collections Framework:**

* **`Collection`** — кореневий інтерфейс, що містить базові операції (додавання, видалення, перевірка, ітерація).
* **`List`** — впорядкований список елементів, допускає дублікати та доступ за індексом.
* **`Set`** — унікальний набір елементів, без дублікатів, без індексації.
* **`Queue`** — черга елементів за принципом FIFO (перший прийшов — перший пішов).

---

## **Пояснення на пальцях (💡)**

* **`Collection`** — це як шафа, в яку ти складаєш речі.
* **`List`** — це як полиця з книгами, де кожна книга має своє місце.
* **`Set`** — це як колекція унікальних марок, де одна марка може бути лише одна.
* **`Queue`** — це черга в кафе, хто прийшов раніше — того і обслуговують першим.

---

## **Корисні приклади (🧪)**

**List — Список улюблених фруктів (дублікати допускаються)**

```java
import java.util.ArrayList;
import java.util.List;

public class ListExample {
    public static void main(String[] args) {
        List<String> favoriteFruits = new ArrayList<>();
        favoriteFruits.add("Apple");
        favoriteFruits.add("Banana");
        favoriteFruits.add("Apple");  // Дублікати дозволені

        System.out.println(favoriteFruits);  
        // Виведе: [Apple, Banana, Apple]
    }
}
```

**Set — Колекція унікальних користувачів за ID (дублікати заборонені)**

```java
import java.util.HashSet;
import java.util.Set;

public class SetExample {
    public static void main(String[] args) {
        Set<Integer> userIds = new HashSet<>();
        userIds.add(101);
        userIds.add(102);
        userIds.add(101);  // Цей дублікати не буде додано

        System.out.println(userIds);  
        // Виведе: [101, 102]
    }
}
```

---

**Queue — Черга з завдань на обробку в системі (FIFO)**

```java
import java.util.LinkedList;
import java.util.Queue;

public class QueueExample {
    public static void main(String[] args) {
        Queue<String> taskQueue = new LinkedList<>();
        taskQueue.add("Завдання 1");
        taskQueue.add("Завдання 2");
        taskQueue.add("Завдання 3");

        while (!taskQueue.isEmpty()) {
            String task = taskQueue.poll();  // Витягує і видаляє перший елемент
            System.out.println("Обробляємо: " + task);
        }
        // Виведе:
        // Обробляємо: Завдання 1
        // Обробляємо: Завдання 2
        // Обробляємо: Завдання 3
    }
}
```

---

## **Порівняння (⚖️)**

| Інтерфейс | Допускає дублікати | Підтримує індексацію | Порядок збереження | Приклад реалізації |
| ----- | :---: | :---: | ----- | ----- |
| **`List`** | **Так** | **Так** | Впорядкований | **`ArrayList`**, **`LinkedList`** |
| **`Set`** | **Ні** | **Ні** | Невпорядкований або впорядкований (залежить від реалізації) | **`HashSet`**, **`TreeSet`** |
| **`Queue`** | **Зазвичай так** | **Ні** | FIFO | **`PriorityQueue`**, **`ArrayDeque`** |

---

## **🧠 Мнемоніка для запам’ятовування:**

**Колекції — це C.L.Q.S:**

* **`C`** — **`Collection`** (корінь)
* **`L`** — **`List`** (впорядкований список)
* **`Q`** — **`Queue`** (черга)
* **`S`** — **`Set`** (унікальний набір)

---

## **🟩 Підсумок:**

**`Колекції`** в Java — це потужний інструмент для роботи з групами об’єктів. Розуміння різних типів колекцій і їхніх особливостей допомагає обирати правильну структуру даних залежно від задачі. Вміти пояснювати їхні відмінності — важлива навичка для співбесід.

**🔗 Ресурси для глибшого вивчення:**

* [**Oracle Docs: Collections Framework**](https://docs.oracle.com/javase/8/docs/technotes/guides/collections/overview.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке Java Collections Framework?

> #### **💬 Відповідь:**
>
> **Java Collections Framework** — це уніфікована архітектура для представлення та маніпулювання групами об'єктів (колекціями). Це набір **інтерфейсів**, **класів-реалізацій** та **алгоритмів**, який надає стандартні способи для зберігання та обробки даних.
>
> **Ключові елементи фреймворку:**
>
> * **Інтерфейси:** Визначають абстрактні типи даних, такі як список (`List`), множина (`Set`), черга (`Queue`), мапа (`Map`). Вони визначають *що* можна робити з колекцією.
> * **Класи-реалізації:** Це конкретні структури даних, що реалізують ці інтерфейси, наприклад, `ArrayList`, `HashSet`, `LinkedList`. Вони визначають *як* це робиться.
> * **Алгоритми:** Це поліморфні методи (зазвичай статичні, в класі `Collections`), що виконують операції над колекціями, такі як сортування, пошук, перемішування.
>
> Мета фреймворку — надати стандартний, ефективний та багаторазово використовуваний набір інструментів для роботи з групами об'єктів.

#### **📝 Аналогія:**

Це як набір стандартних контейнерів для зберігання речей. `List` — це стелаж з пронумерованими полицями. `Set` — це скриня для унікальних предметів. `Map` — це картотека з картками, де на кожній є ключ (номер) і значення (інформація).

#### **📌 Що перевіряють:**

*Розуміння того, що Collections Framework — це цілісна архітектура, а не просто набір окремих класів. Розуміння різниці між інтерфейсами та реалізаціями.*

* * *

### **2 ❓ Питання:**

Яка ієрархія основних інтерфейсів у Collections Framework?

> #### **💬 Відповідь:**
>
> Ієрархія починається з інтерфейсу `Iterable`, але ключовим є `Collection`.
>
> ```
> java.lang.Iterable<T>
> │
> └── java.util.Collection<E>
>     ├── java.util.List<E>
>     │   └── (Реалізації: ArrayList, LinkedList, Vector, Stack)
>     │
>     ├── java.util.Set<E>
>     │   ├── java.util.SortedSet<E>
>     │   │   └── java.util.NavigableSet<E>
>     │   │       └── (Реалізація: TreeSet)
>     │   └── (Реалізації: HashSet, LinkedHashSet)
>     │
>     └── java.util.Queue<E>
>         ├── java.util.Deque<E>
>         │   └── (Реалізації: ArrayDeque, LinkedList)
>         └── (Реалізація: PriorityQueue)
> ```
>
> Важливо зазначити, що інтерфейс **`Map<K, V>` не успадковує** від `Collection`, оскільки він представляє структуру "ключ-значення", а не просто групу елементів. Однак `Map` все одно вважається частиною Collections Framework.

#### **📌 Що перевіряють:**

*Знання базової ієрархії. Особливо важливо розуміти, що `Map` стоїть окремо від `Collection`.*

* * *

### **3 ❓ Питання:**

У чому основна різниця між `List`, `Set` та `Queue`?

> #### **💬 Відповідь:**
>
> Це три основні "гілки" інтерфейсу `Collection`, які відрізняються своїми ключовими властивостями:
>
> * **`List` (Список):**
>   * **Порядок:** Впорядкований. Зберігає елементи в порядку їх додавання.
>   * **Дублікати:** Дозволяє дублювання елементів.
>   * **Доступ:** Дозволяє доступ до елементів за **індексом** (`get(int index)`).
>   * **Аналогія:** Список покупок або список треків у плейлисті.
>
> * **`Set` (Множина):**
>   * **Порядок:** Зазвичай невпорядкований ( `HashSet`), хоча є і впорядковані реалізації ( `TreeSet`, `LinkedHashSet`).
>   * **Дублікати:** **Забороняє** дублювання елементів. Кожен елемент є унікальним.
>   * **Доступ:** Немає доступу за індексом.
>   * **Аналогія:** Колекція унікальних email-адрес.
>
> * **`Queue` (Черга):**
>   * **Порядок:** Зазвичай працює за принципом **FIFO** (First-In, First-Out) — перший прийшов, перший вийшов. Хоча є винятки (`PriorityQueue`).
>   * **Дублікати:** Зазвичай дозволяє.
>   * **Доступ:** Доступ тільки до "голови" черги (отримати/видалити перший елемент).
>   * **Аналогія:** Черга людей до каси.

#### **📌 Що перевіряють:**

*Чітке розуміння основних властивостей (порядок, унікальність, доступ) трьох головних типів колекцій.*

* * *

### **4 ❓ Питання:**

Яка різниця між `ArrayList` та `LinkedList`? Коли який краще використовувати? ⭐️

> #### **💬 Відповідь:**
>
> `ArrayList` та `LinkedList` — це дві різні реалізації інтерфейсу `List`, що відрізняються своєю внутрішньою структурою даних.
>
> * **`ArrayList`:**
>   * **Структура:** Внутрішньо реалізований на основі **масиву**.
>   * **Переваги:**
>     * **Швидкий доступ за індексом (`get(i)`): O(1)**. Оскільки це масив, ми можемо миттєво звернутися до будь-якого елемента.
>   * **Недоліки:**
>     * **Повільне додавання/видалення елементів в середину/на початок списку: O(n)**. Потрібно зсувати всі наступні елементи.
>
> * **`LinkedList`:**
>   * **Структура:** Внутрішньо реалізований як **двозв'язний список**, де кожен елемент зберігає посилання на попередній та наступний.
>   * **Переваги:**
>     * **Швидке додавання/видалення елементів на початок/в кінець/в середину списку (якщо є ітератор): O(1)**. Потрібно лише змінити кілька посилань.
>   * **Недоліки:**
>     * **Повільний доступ за індексом (`get(i)`): O(n)**. Щоб дістатися до `i`-го елемента, потрібно пройти по ланцюжку від початку або кінця.
>
> **Коли що використовувати:**
>
> * **`ArrayList`** — найкращий вибір у більшості випадків, особливо коли вам потрібен **частий доступ до елементів за індексом** і ви рідко додаєте/видаляєте елементи з середини.
> * **`LinkedList`** — хороший вибір, коли вам потрібно часто виконувати операції **вставки/видалення** на початку/в середині списку. Також він реалізує `Deque`, тому є ідеальним для реалізації стеків та черг.

#### **📌 Що перевіряють:**

*Розуміння внутрішньої будови `ArrayList` та `LinkedList` та здатність аналізувати компроміси між ними залежно від сценарію використання. Це одне з найпоширеніших питань на тему колекцій.*

* * *

### **5 ❓ Питання:**

Поясніть, у чому відмінність між `HashSet`, `LinkedHashSet` та `TreeSet`. ⭐️

> #### **💬 Відповідь:**
>
> Всі три є реалізаціями інтерфейсу `Set`, але відрізняються **порядком зберігання елементів** та **продуктивністю**.
>
> * **`HashSet`:**
>   * **Структура:** На основі **хеш-таблиці (`HashMap`)**.
>   * **Порядок:** **Не гарантує** жодного порядку елементів. Порядок може змінюватися з часом.
>   * **Продуктивність:** **Найшвидший** з трьох. Операції `add`, `remove`, `contains` виконуються в середньому за **O(1)**.
>   * **Вимоги:** Вимагає коректної реалізації `hashCode()` та `equals()` для об'єктів, що зберігаються.
>
> * **`LinkedHashSet`:**
>   * **Структура:** Наслідує `HashSet` і додає **двозв'язний список** для відстеження порядку.
>   * **Порядок:** Зберігає елементи **в порядку їх додавання**.
>   * **Продуктивність:** Трохи повільніший за `HashSet` через накладні витрати на підтримку списку, але все ще має **O(1)** для основних операцій.
>
> * **`TreeSet`:**
>   * **Структура:** На основі **червоно-чорного дерева**.
>   * **Порядок:** Зберігає елементи **відсортованими** (або за природним порядком, або за допомогою `Comparator`).
>   * **Продуктивність:** Повільніший за `HashSet`. Операції `add`, `remove`, `contains` виконуються за **O(log n)**.
>   * **Вимоги:** Елементи повинні реалізовувати `Comparable`, або `Comparator` має бути переданий у конструктор.
>
> **Коротко:** `HashSet` — швидкість, `LinkedHashSet` — порядок додавання, `TreeSet` — сортування.

#### **📌 Що перевіряють:**

*Знання різних реалізацій `Set` та вміння вибрати потрібну залежно від вимог до порядку та продуктивності.*

* * *

### **6 ❓ Питання:**

Чому `Map` не є нащадком `Collection`?

> #### **💬 Відповідь:**
>
> `Map` не успадковує від `Collection`, тому що вони представляють **різні за своєю природою структури даних**.
>
> * **`Collection`** представляє **групу окремих елементів**. Її основні методи — це `add(E element)`, `remove(Object o)`, `contains(Object o)`.
>
> * **`Map`** представляє набір пар **"ключ-значення" (`<K, V>`)**. Її основні методи — `put(K key, V value)`, `get(Object key)`, `containsKey(Object key)`.
>
> Інтерфейс `Collection` не має методів для роботи з ключами, тому "вписати" `Map` в ієрархію `Collection` було б нелогічно і порушило б контракт. Наприклад, як реалізувати метод `add(E element)` для мапи? Що є цим `element` — ключ, значення, чи пара?
>
> Хоча `Map` і не успадковує від `Collection`, вона надає методи для отримання її даних у вигляді колекцій:
>
> * `keySet()` повертає `Set<K>` (множину ключів).
> * `values()` повертає `Collection<V>` (колекцію значень).
> * `entrySet()` повертає `Set<Map.Entry<K, V>>` (множину записів "ключ-значення").
>
> Ці методи служать "мостом" між світом `Map` та світом `Collection`.

#### **📌 Що перевіряють:**

*Розуміння фундаментальної різниці в семантиці `Map` та `Collection` та дизайнерських рішень, що стоять за архітектурою фреймворку.*

* * *

### **7 ❓ Питання:**

Назвіть застарілі (legacy) класи колекцій і чим вони відрізняються від сучасних. ⭐️

> #### **💬 Відповідь:**
>
> До застарілих класів, що існували до появи Collections Framework, належать:
>
> * **`Vector`** (аналог `ArrayList`)
> * **`Stack`** (наслідує від `Vector`)
> * **`Hashtable`** (аналог `HashMap`)
> * `Properties` (наслідує від `Hashtable`)
>
> **Чим вони відрізняються від сучасних:**
>
> 1. **Потокобезпечність:** Усі ці класи є **синхронізованими (synchronized)**. Кожен їхній метод ( `get()`, `put()`, `add()`) є синхронізованим, що робить їх потокобезпечними "з коробки", але водночас значно **знижує продуктивність** у однопотоковому середовищі або коли синхронізація не потрібна.
>
> 2. **Невідповідність фреймворку:** Хоча їх пізніше "адаптували" для реалізації інтерфейсів `List` та `Map`, вони мають деякі методи, що не входять до стандартних інтерфейсів (наприклад, `elements()` у `Vector`). `Stack` наслідує від `Vector`, що є поганим дизайном (стек не є вектором).
>
> 3. **Дозвіл на `null`:** `Hashtable` **не дозволяє** `null` ані як ключ, ані як значення. `HashMap` дозволяє.
>
> **Сучасна практика:** Використовувати `ArrayList`, `HashMap` та `ArrayDeque`. Якщо потрібна потокобезпечність, використовувати або **конкурентні колекції** ( `ConcurrentHashMap`) або **синхронізовані обгортки** (`Collections.synchronizedList()`, `Collections.synchronizedMap()`).

#### **📌 Що перевіряють:**

*Знання історії Java та "legacy" класів. Розуміння переваг сучасних несуγχронізованих колекцій.*

* * *

### **8 ❓ Питання:**

Для чого потрібен клас `Collections` (з `s` в кінці)?

> #### **💬 Відповідь:**
>
> `java.util.Collections` — це **утилітарний (utility)** клас, який містить виключно **статичні методи** для виконання операцій над колекціями. Він надає поліморфні алгоритми, які можуть працювати з будь-якою реалізацією відповідного інтерфейсу.
>
> **Основні групи методів:**
>
> * **Алгоритми:**
>   * `sort(List<T> list)` — сортування списку.
>   * `binarySearch(List<? extends Comparable> list, T key)` — бінарний пошук.
>   * `reverse(List<?> list)` — реверс списку.
>   * `shuffle(List<?> list)` — перемішування.
>   * `max(Collection...)`, `min(Collection...)` — пошук максимального/мінімального елемента.
>
> * **Обгортки (Wrappers):**
>   * `synchronizedList(List<T> list)`, `synchronizedSet()`, `synchronizedMap()` — створюють потокобезпечні "обгортки".
>   * `unmodifiableList(List<T> list)`, `unmodifiableSet()`, `unmodifiableMap()` — створюють незмінні (тільки для читання) "обгортки".
>
> * **Фабричні методи для порожніх колекцій:**
>   * `emptyList()`, `emptySet()`, `emptyMap()` — повертають імутабельні порожні колекції, що є ефективним, бо не створює нових об'єктів.

#### **📌 Що перевіряють:**

*Знання про існування та призначення утилітарного класу `Collections` та його основних можливостей.*

* * *

### **9 ❓ Питання:**

Як забезпечити унікальність елементів у `HashSet` для власних об'єктів? ⭐️

> #### **💬 Відповідь:**
>
> `HashSet` використовує `hashCode()` та `equals()` для забезпечення унікальності. Щоб `HashSet` коректно працював з вашими власними об'єктами (наприклад, `User`), ви **зобов'язані** коректно **перевизначити** обидва ці методи.
>
> **Контракт, на який покладається `HashSet`:**
>
> 1. Коли ви додаєте новий елемент, `HashSet` спочатку обчислює його `hashCode()` для визначення "кошика".
> 2. Потім він перевіряє всі елементи в цьому кошику. Якщо `hashCode()` нового елемента збігається з хеш-кодом існуючого, і при цьому `equals()` також повертає `true`, то `HashSet` вважає, що такий елемент **вже існує**, і не додає новий.
>
> Якщо ви не перевизначите ці методи, будуть використовуватися реалізації з класу `Object`. `equals()` буде порівнювати посилання, а `hashCode()` генеруватися на основі адреси. В результаті два логічно однакових об'єкти `new User("admin")` та `new User("admin")` будуть вважатися різними, і ви зможете додати обидва в `HashSet`, що порушить логіку множини.

#### **📌 Що перевіряють:**

*Розуміння нерозривного зв'язку між `HashSet`, `hashCode` та `equals`. Це питання перетинається з темою контракту `hashCode`/`equals`.*

* * *

### **10 ❓ Питання:**

Яка різниця між `Iterator` та `ListIterator`? ⭐️

> #### **💬 Відповідь:**
>
> `Iterator` та `ListIterator` — це інтерфейси для ітерації (обходу) колекцій, але `ListIterator` є більш потужним і спеціалізованим.
>
> * **`Iterator`:**
>   * **Напрямок:** Дозволяє рухатися **тільки вперед** (`hasNext()`, `next()`).
>   * **Операції:** Дозволяє видаляти елементи під час ітерації (`remove()`).
>   * **Застосовність:** Працює для **всіх** типів колекцій (`Collection`: `List`, `Set`, `Queue`).
>
> * **`ListIterator`:**
>   * **Напрямок:** Дозволяє рухатися **в обох напрямках** — вперед (`hasNext()`, `next()`) і **назад** (`hasPrevious()`, `previous()`).
>   * **Операції:** Додатково до `remove()`, дозволяє **модифікувати** (`set(E e)`) та **додавати** (`add(E e)`) елементи під час ітерації.
>   * **Додатково:** Надає доступ до індексу наступного та попереднього елементів (`nextIndex()`, `previousIndex()`).
>   * **Застосовність:** Працює **тільки** для `List`, оскільки вимагає впорядкованості та індексації.
>
> **Коротко:** `ListIterator` є розширеною версією `Iterator`, призначеною спеціально для `List`.

#### **📌 Що перевіряють:**

*Знання просунутих інструментів для роботи з колекціями та їхніх відмінностей.*

* * *

### **11 ❓ Питання:**

Що таке `fail-fast` та `fail-safe` ітератори? Наведіть приклади. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це дві різні стратегії поведінки ітераторів при зміні колекції під час ітерації.
>
> * **`fail-fast` (швидко-падаючий):**
>   * **Поведінка:** Якщо структура колекції (кількість елементів) змінюється під час ітерації будь-яким іншим способом, крім методу `remove()` самого ітератора, ітератор негайно кидає виняток **`ConcurrentModificationException`**.
>   * **Як працює:** Використовує внутрішній лічильник модифікацій (`modCount`). При створенні ітератора він запам'ятовує поточне значення `modCount`. При кожному виклику `next()` він перевіряє, чи не змінилося це значення.
>   * **Приклади:** Ітератори для `ArrayList`, `HashSet`, `HashMap`.
>   * **Мета:** Не гарантувати потокобезпечність, а повідомити про можливу помилку (наприклад, зміну колекції, по якій йде ітерація, в тому ж потоці).
>
> * **`fail-safe` (безпечний до збоїв):**
>   * **Поведінка:** Ітератор **не кидає** `ConcurrentModificationException`. Він працює з **копією** або **"знімком" (snapshot)** колекції, який був створений на момент створення ітератора.
>   * **Наслідки:** Зміни, внесені в оригінальну колекцію після створення ітератора, не будуть відображені в ітераторі. Він працює зі старими даними.
>   * **Приклади:** Ітератори для **конкурентних колекцій**, таких як `CopyOnWriteArrayList`, `ConcurrentHashMap`.
>
> **Висновок:** `fail-fast` — для виявлення програмістських помилок, `fail-safe` — для надійної роботи в багатопотоковому середовищі.

#### **📌 Що перевіряють:**

*Глибоке розуміння поведінки ітераторів та проблем, пов'язаних з конкурентною модифікацією. Знання про конкурентні колекції.*

* * *

### **12 ❓ Питання:**

Які є реалізації інтерфейсу `Queue`? Поясніть різницю між `LinkedList`, `ArrayDeque` та `PriorityQueue`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`LinkedList`:**
>   * Це реалізація на основі **двозв'язного списку**. Він реалізує як `List`, так і `Deque` (а отже, і `Queue`).
>   * Підходить для реалізації простих FIFO-черг, але має певні накладні витрати на зберігання посилань.
>
> * **`ArrayDeque`:**
>   * Реалізація на основі **циклічного масиву**. `Deque` означає "double-ended queue" (двостороння черга).
>   * Дозволяє ефективно додавати та видаляти елементи **як з початку, так і з кінця**.
>   * **Продуктивність:** Зазвичай **швидший** за `LinkedList` для реалізації черг та стеків, оскільки не має накладних витрат на створення об'єктів-вузлів.
>   * **Рекомендований вибір** для реалізації стека (`push`/`pop`) або FIFO-черги (`add`/`poll`).
>
> * **`PriorityQueue` (Пріоритетна черга):**
>   * **Особливість:** Це **не** FIFO-черга. Елементи в ній впорядковані згідно з їхнім **пріоритетом**.
>   * При виклику `poll()` або `peek()`, черга повертає елемент з **найвищим пріоритетом** (зазвичай, найменший елемент).
>   * **Структура:** Внутрішньо реалізована на основі **купи (heap)**.
>   * **Вимоги:** Елементи мають реалізовувати `Comparable`, або `Comparator` має бути переданий у конструктор.
>   * **Продуктивність:** Додавання та видалення елемента — **O(log n)**.
>
> **Коротко:** `ArrayDeque` — для швидких FIFO-черг/стеків, `PriorityQueue` — для черг, де важливий порядок, визначений пріоритетом.

#### **📌 Що перевіряють:**

*Знання різних реалізацій черг та їхнього специфічного призначення.*

* * *

### **13 ❓ Питання:**

Чи може `HashMap` мати ключ `null`? А значення `null`? А `Hashtable`? ⭐️

> #### **💬 Відповідь:**
>
> * **`HashMap`:**
>   * **Ключ `null`:** **Так, може**. Дозволяється **один** ключ `null`. Він зберігається в кошику з індексом 0.
>   * **Значення `null`:** **Так, може**. Дозволяється **скільки завгодно** значень `null`.
>
> * **`Hashtable` (застарілий клас):**
>   * **Ключ `null`:** **Ні, не може.** Спроба додати запис з ключем `null` призведе до `NullPointerException`.
>   * **Значення `null`:** **Ні, не може.** Спроба додати запис зі значенням `null` також призведе до `NullPointerException`.
>
> Ця відмінність є однією з ключових при порівнянні цих двох класів. Також `Hashtable` є синхронізованим, а `HashMap` — ні.

#### **📌 Що перевіряють:**

*Уважність до деталей специфікації популярних колекцій. Знання відмінностей між сучасними та legacy класами.*

* * *

### **14 ❓ Питання:**

Для чого потрібен клас `Arrays`? Чи є він частиною Collections Framework? ⭐️

> #### **💬 Відповідь:**
>
> `java.util.Arrays` — це **утилітарний (utility) клас**, який надає набір **статичних методів** для роботи з **масивами** (`arrays`), а не з колекціями.
>
> Він **не є** частиною ієрархії `Collection`, але тісно пов'язаний з фреймворком, оскільки надає "мости" між масивами та колекціями.
>
> **Основні можливості:**
>
> * **Сортування:** `Arrays.sort()` (використовує Dual-Pivot Quicksort або Timsort).
> * **Пошук:** `Arrays.binarySearch()`.
> * **Порівняння:** `Arrays.equals()` та `Arrays.deepEquals()`.
> * **Заповнення:** `Arrays.fill()`.
> * **Копіювання:** `Arrays.copyOf()`.
> * **Створення потоку:** `Arrays.stream()` (з Java 8).
> * **Перетворення на список:** `Arrays.asList()` — **важливий метод**, який повертає **не `ArrayList`**, а незмінного розміру обгортку над масивом.

#### **📌 Що перевіряють:**

*Знання про існування та призначення утилітарного класу `Arrays`. Розуміння, що він працює саме з масивами, а не з колекціями.*

* * *

### **15 ❓ Питання:**

Ви викликаєте `myList.toArray()`. Який тип масиву поверне цей метод і чому? Як отримати масив правильного типу, наприклад, `String[]`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * Метод `toArray()` без аргументів **завжди** повертає масив типу **`Object[]`**.
>
> **Чому?**
> Через **стирання типів (type erasure)**. Під час виконання колекція (наприклад, `ArrayList<String>`) не знає інформації про свій тип-параметр `<String>`. Найбезпечніше, що вона може зробити — це створити та повернути масив найбільш загального типу, `Object[]`, який може вмістити будь-які об'єкти.
>
> **Як отримати масив правильного типу `String[]`?**
>
> Потрібно використовувати перевантажену версію методу — **`toArray(T[] a)`**. Ви передаєте в нього масив-шаблон потрібного типу, і метод використовує його для створення нового масиву правильного типу.
>
> **Ідіоматичний спосіб:**
>
> ```java
> List<String> list = List.of("a", "b", "c");
>
> // Передача масиву нульової довжини є найпоширенішою практикою
> String[] stringArray = list.toArray(new String[0]);
> ```
>
> **Як це працює:** Метод `toArray` отримує `new String[0]`, бачить, що тип компонентів — це `String`, розуміє, що розмір `0` замалий, і за допомогою рефлексії створює новий масив типу `String` правильного розміру (`list.size()`) і копіює туди елементи.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як колекції взаємодіють з масивами, та знання стандартного способу типобезпечного перетворення колекції в масив.*
