---
layout: default
title: "Інтерфейс List"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 3
---

# Інтерфейс List

* короткий опис: `List` – це впорядкована колекція, що може містити дублікати, і забезпечує доступ до елементів за індексом, починаючи з 0.

* поняття: `List`, індексація, дублікати.

* приклади: `add(E element)`, `get(int index)`, `set(int index, E element)`, `remove(int index)`.

* ключові моменти: Зберігає порядок вставки елементів. Доступ за індексом може бути швидким (`ArrayList`) або повільним (`LinkedList`) залежно від реалізації.

* що важливо знати на співбесіді: Основні методи `List`, його властивості.

### **✅ Офіційне визначення:**

**Інтерфейс** **`List`** у Java є частиною **`Java Collections Framework`** і описує впорядковану колекцію, яка може містити дублікати. Кожен елемент у списку має позицію (**`індекс`**), починаючи з **`0`**.

### **🧠 Простими словами:**

**`List`** — це як упорядкований список речей, куди ти можеш додавати елементи, брати їх по порядку, змінювати, видаляти — і все це за індексом, як у масиві, але з більшою гнучкістю

---

## **Основні методи List:**

* **`add(E element)`** — додати елемент у кінець списку
* **`get(int index)`** — отримати елемент за індексом
* **`set(int index, E element)`** — замінити елемент за індексом
* **`remove(int index)`** — видалити елемент за індексом
* **`size()`** — розмір списку
* **`clear()`** — очистити список
* **`indexOf(Object o)`** — знайти індекс першого входження елемента

---

## **Пояснення на пальцях (💡)**

## **Особливості доступу за індексом:**

**`List`** дозволяє легко отримати елемент по позиції, як у масиві, тому, якщо знаєш індекс — доступ дуже швидкий. Але у різних реалізаціях цей доступ може мати різну швидкість (наприклад, у **`ArrayList`** швидкий, у **`LinkedList`** — повільніший).

---

## **Корисні приклади (🧪)**

```java
List<String> fruits = new ArrayList<>();
fruits.add("Apple");         // Додати "Apple"
fruits.add("Banana");        // Додати "Banana"
String firstFruit = fruits.get(0);  // Отримати "Apple"
fruits.set(1, "Orange");     // Заміна "Banana" на "Orange"
fruits.remove(0);            // Видалити "Apple"
int size = fruits.size();    // Розмір списку -- 1
fruits.clear();              // Очистити список
```

---

## **Порівняння (⚖️)**

* **`ArrayList`** — швидкий доступ за індексом, але повільне вставлення/видалення в середині
* **`LinkedList`** — повільний доступ за індексом, але швидке вставлення/видалення в будь-якому місці

---

## **🧠 Мнемоніка для запам’ятовування:**

"**`List`** — це Лінійка Індексів Структурованих Товарів" — уяви, що список — це лінійка, де кожен товар (елемент) стоїть на своєму місці за індексом

---

## **🟩 Підсумок:**

Інтерфейс **`List`** — це впорядкована колекція з доступом по індексу, яка підтримує дублікати. Найчастіше використовується через реалізації **`ArrayList`** і **`LinkedList`**. Ідеально підходить, коли потрібен список із швидким або гнучким доступом до елементів.

**🔗 Доповнююча інформація:**

* [**Oracle Docs List Interface**](https://docs.oracle.com/en/java/javase/20/docs/api/java.base/java/util/List.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке інтерфейс `List` і які його ключові характеристики?

> #### **💬 Відповідь:**
>
> **`java.util.List`** — це один з основних інтерфейсів у Java Collections Framework, який розширює інтерфейс `Collection`.
>
> **Ключові характеристики `List`:**
>
> 1. **Впорядкованість (Ordered):** Елементи зберігаються і повертаються у певній послідовності, зазвичай у тому порядку, в якому їх було додано.
> 2. **Доступ за індексом:** `List` надає методи для доступу до елементів за їхньою цілочисельною позицією (індексом), яка починається з 0.
> 3. **Дозвіл на дублікати:** На відміну від `Set`, `List` може містити однакові елементи. Якщо додати об'єкт `o` в список, де він уже є, то список буде містити два входження цього об'єкта.
> 4. **Дозвіл на `null`:** Більшість реалізацій `List` дозволяють зберігати `null` елементи.

#### **📝 Аналогія:**</h4>

`List` — це як список треків у плейлисті. У них є чіткий порядок (перший трек, другий...), один і той самий трек може зустрічатися кілька разів, і ви можете вибрати трек за його номером.

#### **📌 Що перевіряють:**

*Знання трьох основних властивостей `List`: впорядкованість, індексація, дозвіл на дублікати.*

* * *

### **2 ❓ Питання:**

Які основні реалізації інтерфейсу `List` ви знаєте?

> #### **💬 Відповідь:**
>
> Існує кілька основних реалізацій `List`, кожна зі своїми перевагами та недоліками:
>
> 1. **`ArrayList`:**
>     * **Найпоширеніша** реалізація.
>     * Внутрішня структура: **динамічний масив (`Object[]`)**.
>     * **Сильні сторони:** Швидкий доступ до елементів за індексом (O(1)).
>     * **Слабкі сторони:** Повільне додавання/видалення елементів з середини або початку списку (O(n)), оскільки вимагає зсуву елементів.
>
> 2. **`LinkedList`:**
>     * Внутрішня структура: **двозв'язний список**.
>     * **Сильні сторони:** Швидке додавання/видалення елементів на початку/в кінці (O(1)) та в середині (якщо є ітератор).
>     * **Слабкі сторони:** Повільний доступ за індексом (O(n)), оскільки вимагає проходу по ланцюжку вузлів.
>
> 3. **`Vector`:**
>     * **Застаріла** реалізація, аналогічна `ArrayList`.
>     * **Ключова відмінність:** Усі її методи є **синхронізованими (`synchronized`)**, що робить її потокобезпечною, але повільною.
>
> 4. **`Stack`:**
>     * **Застаріла** реалізація структури даних LIFO (Last-In-First-Out).
>     * Успадковує від `Vector`, що є поганим дизайнерським рішенням.
>
> У сучасному коді для списків зазвичай використовують `ArrayList` або `LinkedList`.

#### **📌 Що перевіряють:**

*Знання основних класів-реалізацій та їхніх фундаментальних відмінностей.*

* * *

### **3 ❓ Питання:**

Назвіть основні методи, які `List` додає до інтерфейсу `Collection`.

> #### **💬 Відповідь:**
>
> `List` додає методи, що базуються на **роботі з індексами**, яких немає в `Collection`.
>
> **Основні додані методи:**
>
> * **`get(int index)`**: Повертає елемент за вказаним індексом.
> * **`set(int index, E element)`**: Замінює елемент за вказаним індексом на новий.
> * **`add(int index, E element)`**: Вставляє елемент за вказаним індексом, зсуваючи всі наступні елементи.
> * **`remove(int index)`**: Видаляє елемент за вказаним індексом.
> * **`indexOf(Object o)`**: Повертає індекс першого входження елемента.
> * **`lastIndexOf(Object o)`**: Повертає індекс останнього входження елемента.
> * **`listIterator()` / `listIterator(int index)`**: Повертає `ListIterator`, який дозволяє обхід у двох напрямках та модифікацію списку.
> * **`subList(int fromIndex, int toIndex)`**: Повертає "вид" (view) частини списку.

#### **📌 Що перевіряють:**

*Розуміння того, що ключовою особливістю `List`, яка відрізняє його від інших колекцій, є індексація та операції, пов'язані з нею.*

* * *

### **4 ❓ Питання:**

У чому різниця між `list.remove(Object o)` та `list.remove(int index)`? Яка помилка може виникнути при роботі з `List<Integer>`? ⭐️

> #### **💬 Відповідь:**
>
> * **`remove(Object o)`:** Цей метод успадковано від `Collection`. Він шукає **перше входження** об'єкта, рівного `o` (за методом `equals()`), і видаляє його. Повертає `true`, якщо елемент був знайдений і видалений.
>
> * **`remove(int index)`:** Цей метод додано в `List`. Він видаляє елемент за **конкретним індексом**. Повертає елемент, який був видалений.
>
> **Помилка з `List<Integer>`:**
>
> Через **автобоксинг** виклик `remove` може стати неоднозначним і призвести до помилки.
>
> ```java
> List<Integer> numbers = new ArrayList<>(List.of(10, 20, 30));
>
> // Намір: видалити елемент зі значенням 20.
> // Але компілятор бачить `int` і викликає `remove(int index)`
> numbers.remove(20); // Кине IndexOutOfBoundsException!
> ```
>
> **Як правильно видалити за значенням:**
>
> Потрібно явно передати об'єкт `Integer`, щоб уникнути вибору перевантаженого методу `remove(int index)`.
>
> `numbers.remove(Integer.valueOf(20));` // OK
> `numbers.remove((Integer) 20);` // OK, але менш читабельно

#### **📌 Що перевіряють:**

*Розуміння механізму перевантаження методів та його взаємодії з автобоксингом. Це класичне питання-"пастка".*

* * *

### **5 ❓ Питання:**

Яку асимптотичну складність (Big O) мають операції `get`, `add`, `remove` для `ArrayList` та `LinkedList`? ⭐️

> #### **💬 Відповідь:**
>
> | Операція | `ArrayList` (на основі масиву) | `LinkedList` (на основі списку) |
> |---|---|---|
> | **Доступ (`get(index)`)** | **O(1)** — дуже швидко. | **O(n)** — повільно. Потрібно пройти n/2 елементів в середньому. |
> | **Додавання в кінець (`add(E e)`)** | **O(1)** в середньому (амортизовано), але іноді **O(n)**, якщо потрібно розширювати масив. | **O(1)** — дуже швидко. |
> | **Додавання в середину (`add(index, E e)`)** | **O(n)** — дуже повільно. Потрібно зсунути всі наступні елементи. | **O(1)**, якщо у вас вже є ітератор, що вказує на місце. O(n), якщо потрібно спочатку знайти місце. |
> | **Видалення з середини (`remove(index)`)** | **O(n)** — дуже повільно. Потрібно зсунути всі елементи. | **O(1)**, якщо є ітератор. O(n), якщо потрібно спочатку знайти. |
>
> **Короткий висновок:**
>
> * `ArrayList`: Ідеальний для читання та доступу за індексом.
> * `LinkedList`: Ідеальний для частих операцій вставки/видалення.

#### **📌 Що перевіряють:**

*Фундаментальне розуміння асимптотичної складності та вміння застосовувати його для аналізу продуктивності основних структур даних.*

* * *

### **6 ❓ Питання:**

Що повертає метод `list.subList(from, to)`? Чи є зміни у "підсписку" незалежними від оригінального? ⭐️

> #### **💬 Відповідь:**
>
> Метод `subList` повертає не нову копію, а **"вид" (view) або "представлення"** частини оригінального списку.
>
> Це означає, що "підсписок" та оригінальний список **посилаються на одні й ті самі дані**.
>
> **Наслідки:**
>
> * **Зміни у "підсписку" відображаються в оригінальному списку:**
>
>     ```java
>     List<String> list = new ArrayList<>(List.of("a", "b", "c", "d"));
>     List<String> sub = list.subList(1, 3); // ["b", "c"]
>
>     sub.set(0, "X"); // Змінюємо "b" на "X" у підсписку
>
>     System.out.println(list); // Виведе [a, X, c, d] - оригінал теж змінився!
>     ```
>
> * **Зміни в оригінальному списку відображаються у "підсписку".**
>
> * **Структурна модифікація оригінального списку** (через `add` або `remove` для `list`) призведе до того, що будь-яка подальша спроба працювати з `subList` кине **`ConcurrentModificationException`**.
>
> **Висновок:** `subList` є потужним, але небезпечним інструментом. Його слід використовувати обережно, пам'ятаючи про спільний стан.

#### **📌 Що перевіряють:**

*Розуміння того, що `subList` повертає "вид", а не копію, та пов'язаних з цим ризиків.*

* * *

### **7 ❓ Питання:**

Як безпечно видалити елементи зі списку під час ітерації? Наведіть приклади. ⭐️

> #### **💬 Відповідь:**
>
> **Неправильний спосіб:** Використовувати цикл `for-each` і викликати `list.remove()` напряму. Це призведе до `ConcurrentModificationException`.
>
> **Правильні способи:**
>
> 1. **Використовувати явний `Iterator` та його метод `remove()`:**
>     Це класичний і надійний спосіб.
>
>     ```java
>     Iterator<String> iterator = list.iterator();
>     while (iterator.hasNext()) {
>         String element = iterator.next();
>         if (element.equals("to_delete")) {
>             iterator.remove(); // Видалення через ітератор
>         }
>     }
>     ```
>
> 2. **Використовувати метод `removeIf()` (з Java 8):**
>     Це найбільш сучасний, лаконічний і рекомендований спосіб.
>
>     ```java
>     list.removeIf(element -> element.equals("to_delete"));
>     ```
>
> 3. **Ітерація за індексом у зворотному порядку:**
>     Це менш елегантний, але робочий "трюк". Ітерація у зворотному порядку гарантує, що видалення поточного елемента не вплине на індекси тих елементів, які ми ще не перевірили.
>
>     ```java
>     for (int i = list.size() - 1; i >= 0; i--) {
>         if (list.get(i).equals("to_delete")) {
>             list.remove(i);
>         }
>     }
>     ```

#### **📌 Що перевіряють:**

*Знання `fail-fast` поведінки ітераторів. Вміння запропонувати кілька способів вирішення поширеної проблеми, включаючи сучасний підхід з `removeIf`.*

* * *

### **8 ❓ Питання:**

Що таке `ListIterator` і які додаткові можливості він надає порівняно з `Iterator`?

> #### **💬 Відповідь:**
>
> `ListIterator` — це розширений ітератор, призначений **спеціально для `List`**. Він надає значно більше можливостей, ніж стандартний `Iterator`:
>
> * **Двонаправлений обхід:** Дозволяє рухатися не тільки вперед (`hasNext()`, `next()`), але й **назад** (`hasPrevious()`, `previous()`).
> * **Модифікація списку:** Дозволяє не тільки видаляти (`remove()`), але й **додавати (`add(E e)`)** та **замінювати (`set(E e)`)** елементи під час ітерації.
> * **Доступ до індексів:** Надає методи `nextIndex()` та `previousIndex()` для отримання індексів сусідніх елементів.
>
> Отримати його можна за допомогою методу `list.listIterator()`. `ListIterator` не доступний для `Set` або `Queue`, оскільки вони не гарантують індексації.

#### **📌 Що перевіряють:**

*Знання про існування `ListIterator` та його розширених можливостей для роботи з `List`.*

* * *

### **9 ❓ Питання:**

Чим відрізняється поведінка конструкторів `new ArrayList()` та `new ArrayList(Collection<? extends E> c)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`new ArrayList()` (конструктор без аргументів):**
>   * Створює **порожній** `ArrayList`.
>   * Внутрішній масив за замовчуванням створюється з певною початковою ємністю (зазвичай 10, хоча це деталь реалізації). Ця ємність буде збільшуватися по мірі додавання елементів.
>
> * **`new ArrayList(Collection<? extends E> c)` (конструктор копіювання):**
>   * Створює **новий** `ArrayList`, який містить **всі елементи** з переданої колекції `c`.
>   * Елементи копіюються в тому порядку, в якому їх повертає ітератор колекції `c`.
>   * Це створює **поверхневу копію (shallow copy)**. Новий список буде містити **посилання** на ті самі об'єкти, що і в оригінальній колекції.
>
> **Приклад:**
> `List<String> original = List.of("a", "b");`
> `List<String> copy = new ArrayList<>(original); //`copy`буде містити ["a", "b"]`
>
> Це зручний спосіб створити змінювану копію (наприклад, `ArrayList`) з незмінної колекції (наприклад, `List.of()`).

#### **📌 Що перевіряють:**

*Знання API колекцій, зокрема, конструкторів, та розуміння різниці між створенням порожньої колекції та копіюванням існуючої.*

* * *

### **10 ❓ Питання:**

Чи можна сортувати `LinkedList` за допомогою `Collections.sort()`? Чи буде це ефективно? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, можна.** Метод `Collections.sort(List<T> list)` приймає будь-яку реалізацію `List`, включаючи `LinkedList`.
>
> **Чи буде це ефективно?**
> **Ні, це буде дуже неефективно.**
>
> **Чому?**
> Алгоритми сортування, які використовуються в `Collections.sort` (як-от Timsort), активно покладаються на **швидкий доступ до елементів за індексом** та на операцію **заміни (`set(index, element)`)**.
>
> Для `LinkedList` обидві ці операції (`get(index)` і `set(index)`) мають складність **O(n)**. Це перетворює алгоритм сортування, який мав би бути O(n log n), на щось значно гірше (близько O(n^2)).
>
> **Найкращий підхід, якщо потрібно відсортувати `LinkedList`:**
>
> 1. Створити новий `ArrayList` з елементів `LinkedList`.
> 2. Відсортувати `ArrayList`.
> 3. За бажанням, створити новий `LinkedList` з відсортованого `ArrayList`.
>
> ```java
> List<String> sortedList = new ArrayList<>(myLinkedList);
> Collections.sort(sortedList);
> myLinkedList = new LinkedList<>(sortedList);
> ```
>
> Цей підхід, хоча і вимагає додаткової пам'яті, буде значно швидшим.

#### **📌 Що перевіряють:**

*Здатність аналізувати продуктивність алгоритмів у поєднанні з різними структурами даних. Розуміння, що операція може бути доступною, але абсолютно неефективною.*

* * *

### **11 ❓ Питання:**

Що поверне `Arrays.asList("a", "b", "c")`? Чи можна додавати елементи в такий список? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `Arrays.asList()` повертає екземпляр **приватного статичного вкладеного класу `java.util.Arrays.ArrayList`**, який є обгорткою над вхідним масивом.
>
> **Чи можна додавати елементи?**
> **Ні, не можна.** Ця реалізація `List` має **фіксований розмір**.
>
> Будь-яка спроба викликати методи, що змінюють розмір списку, такі як `add()` або `remove()`, призведе до кидання **`UnsupportedOperationException`** під час виконання.
>
> **Важливий нюанс:**
> Ви **можете** змінити існуючі елементи за допомогою методу `set()`, і ця зміна відобразиться у вихідному масиві, якщо він існує.
>
> ```java
> String[] array = {"a", "b", "c"};
> List<String> list = Arrays.asList(array);
>
> list.set(0, "X"); // Змінюємо елемент
>
> System.out.println(array[0]); // Виведе "X" - вихідний масив теж змінився
> ```
>
> Це одна з найпоширеніших "пасток" при роботі з колекціями. Для створення повноцінного змінюваного `ArrayList` з масиву слід використовувати `new ArrayList<>(Arrays.asList(...))`.

#### **📌 Що перевіряють:**

*Знання специфічної та неінтуїтивної поведінки `Arrays.asList()`. Це питання дозволяє відсіяти кандидатів, які знають API лише поверхнево.*

* * *

### **12 ❓ Питання:**

Який клас `List` є синхронізованим, і чому його використання не рекомендується в сучасному коді? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Синхронізованим** є застарілий клас **`java.util.Vector`**. Всі його методи ( `add`, `get`, `remove` тощо) позначені ключовим словом `synchronized`.
>
> **Чому його не рекомендується використовувати:**
>
> 1. **Погана продуктивність:** Блокування відбувається на **кожній окремій операції**. Якщо вам потрібно виконати кілька операцій послідовно (наприклад, перевірити наявність, а потім додати), це не є атомарною операцією. Інший потік може втрутитися між цими викликами.
>
> 2. **Низька пропускна здатність:** Використовується одне блокування на весь об'єкт. Це означає, що одночасно тільки один потік може виконувати будь-яку операцію, що є вкрай неефективним у високонавантажених системах.
>
> 3. **Небезпечна ітерація:** Хоча окремі методи синхронізовані, **ітерація** по `Vector` **не є потокобезпечною**. Якщо один потік ітерує, а інший змінює `Vector`, все одно може виникнути `ConcurrentModificationException`. Потрібна додаткова зовнішня синхронізація.
>
> **Сучасні альтернативи:**
>
> * Для простих випадків — **`Collections.synchronizedList(new ArrayList<>())`**. Створює обгортку з такою ж логікою блокування, як у `Vector`.
> * Для високої продуктивності в багатопотоковому середовищі — **конкурентні колекції**, зокрема **`CopyOnWriteArrayList`**. Він є потокобезпечним і оптимізованим для сценаріїв, де читання відбувається набагато частіше, ніж запис.

#### **📌 Що перевіряють:**

*Розуміння проблем, пов'язаних з наївною синхронізацією, та знання про сучасні конкурентні колекції.*

* * *

### **13 ❓ Питання:**

Як `ArrayList` керує своєю ємністю (capacity)? Що відбувається, коли внутрішній масив заповнюється? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `ArrayList` внутрішньо використовує масив для зберігання елементів. Цей масив має певну **ємність (capacity)**, яка може відрізнятися від **розміру (size)** списку (кількості фактичних елементів).
>
> **Що відбувається, коли масив заповнюється:**
>
> 1. Коли ви намагаєтеся додати елемент, і поточна ємність масиву є недостатньою, `ArrayList` виконує операцію **розширення (resizing)**.
>
> 2. **Алгоритм розширення** (зазвичай):
>     * Створюється **новий масив**, розмір якого є більшим за старий. У більшості реалізацій JDK нова ємність розраховується як `стара_ємність + (стара_ємність >> 1)`, що приблизно дорівнює **збільшенню в 1.5 рази**.
>     * Всі елементи зі старого масиву **копіюються** в новий за допомогою `System.arraycopy()` (що є дуже швидкою `native` операцією).
>     * Посилання `elementData` переключається на новий масив, а старий масив стає доступним для збирача сміття.
>
> Ця операція є "дорогою" (O(n)), але завдяки тому, що ємність збільшується не на один елемент, а в кілька разів, **середня (амортизована) вартість** додавання елемента залишається **O(1)**.
>
> Якщо ви знаєте приблизну кількість елементів заздалегідь, краще створювати `ArrayList` з початковою ємністю (`new ArrayList<>(capacity)`), щоб уникнути зайвих операцій розширення.

#### **📌 Що перевіряють:**

*Глибокі знання про внутрішню роботу `ArrayList`. Розуміння понять "ємність", "розмір" та "амортизована складність".*

* * *

### **14 ❓ Питання:**

Чому `List.of()` (з Java 9) повертає імутабельний список? Які переваги це дає? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Статичні фабричні методи `List.of()`, `Set.of()`, `Map.of()` повертають **імутабельні (незмінні)** колекції.
>
> **Переваги цього підходу:**
>
> 1. **Безпека:** Імутабельні об'єкти є **потокобезпечними "з коробки"**. Оскільки їхній стан не можна змінити, ними можна безпечно ділитися між потоками без будь-якої синхронізації.
> 2. **Надійність:** Ви можете передати таку колекцію в інший метод і бути впевненим, що він не зможе її змінити. Це запобігає несподіваним побічним ефектам.
> 3. **Продуктивність та ефективність по пам'яті:**
>     * Немає потреби зберігати додаткову інформацію для відстеження модифікацій (як `modCount`).
>     * JVM може більш агресивно оптимізувати роботу з такими об'єктами.
>     * Часто повертаються екземпляри спеціалізованих, дуже компактних внутрішніх класів. Наприклад, `List.of("a")` поверне не `ArrayList`, а спеціальний об'єкт для одного елемента.
>
> **Важливо пам'ятати:**
> Будь-яка спроба змінити такий список (`add`, `set`, `remove`) призведе до `UnsupportedOperationException`.

#### **📌 Що перевіряють:**

*Знання сучасного Java API (Java 9+). Розуміння переваг імутабельності для створення надійних та ефективних систем.*

* * *

### **15 ❓ Питання:**

Припустимо, у вас є `ArrayList` та `LinkedList` з мільйоном елементів. Яка з операцій буде виконуватися значно швидше для `LinkedList`: `list.add(0, "new")` чи `list.add(500000, "new")`? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Операція **`list.add(0, "new")` буде значно швидшою.**
>
> **Пояснення:**
>
> Хоча `LinkedList` відомий тим, що операція вставки в нього має складність O(1), це справедливо тільки тоді, коли у вас **вже є посилання (ітератор)** на місце вставки.
>
> Метод `add(int index, E element)` спочатку повинен **знайти** це місце.
>
> * **`list.add(0, "new")`:** `LinkedList` оптимізований для доступу до початку та кінця. Щоб додати елемент за індексом `0`, йому не потрібно нікуди йти. Він просто оновлює посилання `first`. Ця операція має складність **O(1)**.
>
> * **`list.add(500000, "new")`:** Щоб додати елемент у середину (за індексом 500,000), `LinkedList` повинен **пройти по ланцюжку** від початку (або кінця, залежно від того, що ближче) до потрібного вузла. Ця операція пошуку має складність **O(n)**. У нашому випадку йому доведеться пройти 500,000 вузлів.
>
> **А для `ArrayList`?**
>
> Для `ArrayList` все навпаки. `add(0, "new")` буде найповільнішою (потрібно зсунути мільйон елементів), а `add(500000, "new")` буде трохи швидшою (потрібно зсунути "лише" півмільйона).
>
> Це питання демонструє важливість розуміння не тільки асимптотичної складності, але й того, **як саме** вона досягається в конкретній реалізації.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння внутрішньої роботи `LinkedList` та його оптимізацій. Здатність аналізувати продуктивність конкретних операцій, а не тільки загальну складність.*
