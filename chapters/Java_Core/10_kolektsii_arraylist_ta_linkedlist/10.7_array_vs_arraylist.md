---
layout: default
title: "Array vs ArrayList"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 7
---

# Array vs ArrayList

* короткий опис: `Array` – структура фіксованого розміру, що підтримує примітиви та об'єкти. `ArrayList` – динамічна реалізація `List`, що автоматично змінює розмір, але працює лише з об'єктами (використовує `autoboxing` для примітивів).

* поняття: `Array`, `ArrayList`, `autoboxing`, `unboxing`.

* приклади: Порівняння за розміром (фіксований vs динамічний), типами елементів, швидкістю доступу, підтримкою generics.

* ключові моменти: `ArrayList` гнучкіший, `Array` – продуктивніший. `Autoboxing` в `ArrayList` може впливати на продуктивність.

* що важливо знати на співбесіді: Детальні відмінності, вплив `autoboxing`.

### **✅ Офіційне визначення:**

* **`Array`** — структура даних фіксованого розміру, що зберігає елементи одного типу в безперервній пам’яті.
* **`ArrayList`** — динамічна **`реалізація`** інтерфейсу **`List`** на базі масиву, що автоматично змінює свій розмір і надає багатий API для роботи з елементами.

### **🧠 Простими словами:**

**`Array`** — це статична коробка з фіксованою кількістю комірок, а **`ArrayList`** — це гнучкий ящик, який може сам розширюватись і стискатись, щоб вміщати змінну кількість речей

---

## **Таблиця порівняння Array vs ArrayList**

| Характеристика | Array | ArrayList |
| :---: | :---: | :---: |
| **Розмір** | Фіксований | Динамічний (змінюється автоматично) |
| **Типи елементів** | Примітивні \+ об'єкти | Тільки об’єкти (обгортки для примітивів) |
| **Підтримка дженериків** | Немає | Так |
| **Ітерація** | Через індекси або foreach | Через ітератор або foreach |
| **Швидкість доступу** | **`O(1)`**, дуже швидкий | **`O(1)`**, але трохи повільніший через накладні витрати |
| **Збільшення розміру** | Немає (потрібно створювати новий масив) | Автоматичне (зазвичай x1.5 від поточного розміру) |
| **Примітивні типи** | Підтримуються напряму | Потрібен автобоксинг (обгортки) |
| **Додаткові методи** | Мінімальні (length, доступ за індексом) | Багато: **`add`**, **`remove`**, **`contains`**, **`clear`**, **`sort`** і т.д. |
| **Пам'ять** | Менше накладних витрат | Більше накладних витрат |

---

## **Пояснення на пальцях (💡)**

* **`Array`** — це як жорстко зафіксована полиця в шафі, на якій є лише певна кількість місць. Якщо хочеш більше місця — треба купити нову полицю.
* **`ArrayList`** — це полиця, що сама розширюється, коли ставиш на неї більше речей, але для цього треба трохи більше часу і ресурсів.
* **`Автобоксинг/анбоксинг`** — це автоматичне перетворення між примітивними типами (**`int`**, **`double`**) і їх обгортками (**`Integer`**, **`Double`**), яке відбувається, коли примітиви використовуються там, де потрібні об’єкти (наприклад, в **`ArrayList`**).

---

## **Корисні приклади (🧪)**

```java
// Масив int (примітиви)
int[] arr = {1, 2, 3};
System.out.println(arr.length);  // 3
System.out.println(arr[0]);      // 1

// ArrayList<Integer> (обгортки для int)
ArrayList<Integer> list = new ArrayList<>();
list.add(1);  // автобоксинг int -> Integer
list.add(2);
list.add(3);
System.out.println(list.size());  // 3
System.out.println(list.get(0));  // 1 (анбоксинг Integer -> int)
```

---

## **Порівняння (⚖️)**

* Якщо потрібна висока продуктивність і фіксований розмір, використовуй масиви.
* Якщо потрібна гнучкість, багатофункціональність, підтримка **`generics`** і легкість у роботі — **`ArrayList`** ідеальний вибір.

---

## **🧠 Мнемоніка для запам’ятовування:**

**`Array`** — це фіксований ящик,

**`ArrayList`** — розумний ящик, що росте як підліток.

---

## **🟩 Підсумок:**

**`Array`** — швидкий, простий і ефективний, але жорстко фіксований у розмірі.

**`ArrayList`** — більш гнучкий, має потужний API і підтримує generics, але потребує трохи більше ресурсів через динамічне розширення і автобоксинг.

**🔗 Для глибшого вивчення:**

* [**Oracle Docs \- Array**](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/arrays.html)
* [**Oracle Docs \- ArrayList**](https://docs.oracle.com/javase/8/docs/api/java/util/ArrayList.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Яка основна різниця між `Array` (масивом) та `ArrayList`?

> #### **💬 Відповідь:**
>
> Основна різниця полягає у наступних аспектах:
>
> 1. **Розмір:**
>     * **`Array`:** Має **фіксований розмір**, який визначається при створенні і не може бути змінений.
>     * **`ArrayList`:** Має **динамічний розмір**, який може автоматично збільшуватися або зменшуватися під час виконання.
>
> 2. **Типи даних:**
>     * **`Array`:** Може зберігати як **примітивні типи** (`int[]`, `double[]`), так і **об'єкти** (`String[]`).
>     * **`ArrayList`:** Працює **тільки з об'єктами**. Для примітивів використовуються їхні класи-обгортки (`ArrayList<Integer>`, `ArrayList<Double>`).
>
> 3. **Функціональність (API):**
>     * **`Array`:** Надає лише базову властивість `length` та доступ за індексом `[]`.
>     * **`ArrayList`:** Є повноцінним класом з Java Collections Framework з багатим набором методів для маніпуляції даними (`add`, `remove`, `sort`, `contains` тощо).
>
> 4. **Підтримка дженеріків:**
>     * **`Array`:** Не є дженериком. Типобезпечність частково забезпечується під час виконання (`ArrayStoreException`).
>     * **`ArrayList`:** Повністю підтримує дженеріки, забезпечуючи типобезпечність на етапі компіляції.

#### **📝 Аналогія:**

`Array` — це парковка з чітко розміченою кількістю місць. Якщо всі місця зайняті, новій машині ніде стати. `ArrayList` — це поле біля стадіону: спочатку воно невелике, але коли приїжджає багато машин, його розширюють.

#### **📌 Що перевіряють:**

*Чітке розуміння чотирьох ключових відмінностей: розмір, типи даних, API та дженеріки.*

* * *

### **2 ❓ Питання:**

Чи можна змінити розмір масиву після його створення?

> #### **💬 Відповідь:**
>
> Ні, **розмір масиву є фіксованим і не може бути змінений** після його створення.
>
> Якщо вам потрібно "змінити розмір" масиву, ви насправді повинні виконати наступні кроки:
>
> 1. Створити **новий масив** потрібного розміру.
> 2. **Скопіювати** всі елементи зі старого масиву в новий (зазвичай за допомогою `System.arraycopy()` або `Arrays.copyOf()`).
> 3. Присвоїти посилання на новий масив вашій змінній.
>
> Цю роботу "під капотом" автоматично виконує `ArrayList` при своєму розширенні. Саме динамічна зміна розміру є однією з ключових переваг `ArrayList` над звичайними масивами.

#### **📌 Що перевіряють:**

*Розуміння того, що масиви є статичними за своєю природою.*

* * *

### **3 ❓ Питання:**

Що ефективніше з точки зору продуктивності: `Array` чи `ArrayList`?

> #### **💬 Відповідь:**
>
> **Масиви (`Array`) є більш продуктивними**.
>
> **Причини:**
>
> 1. **Прямий доступ до пам'яті:** Доступ до елемента масиву (`array[i]`) — це дуже швидка, низькорівнева операція, яка напряму транслюється в машинні інструкції. `ArrayList.get(i)` — це виклик методу, який має невеликі накладні витрати (хоча JIT-компілятор може його оптимізувати).
>
> 2. **Відсутність автобоксингу/анбоксингу:** Масиви можуть зберігати примітиви напряму (`int[]`). `ArrayList<Integer>` вимагає перетворення `int` в `Integer` (автобоксинг) і навпаки (анбоксинг), що створює додаткові об'єкти і споживає час.
>
> 3. **Відсутність накладних витрат на керування:** `ArrayList` має додаткові поля (`size`, `modCount`) і логіку для розширення, що також вносить невелику затримку.
>
> **Коли ця різниця важлива?**
>
> Для більшості бізнес-застосунків різниця в продуктивності є несуттєвою і нею можна знехтувати заради гнучкості `ArrayList`. Однак у високопродуктивних обчисленнях, обробці графіки, наукових розрахунках, де виконуються мільйони операцій у циклах, перевага масивів стає значною.

#### **📌 Що перевіряють:**

*Розуміння того, чому масиви є "ближчими до заліза" і, як наслідок, швидшими.*

* * *

### **4 ❓ Питання:**

Як відбувається ітерація по `Array` та `ArrayList`?

> #### **💬 Відповідь:**
>
> Обидві структури підтримують два основні способи ітерації:
>
> 1. **Цикл `for-each` (Enhanced for-loop):**
>     * Це найбільш поширений і читабельний спосіб. Синтаксис однаковий для обох.
>
>     **Для `Array`:**
>     `for (int item : intArray) { ... }`
>     **Для `ArrayList`:**
>     `for (String item : stringList) { ... }`
>
> 2. **Класичний `for`-цикл з індексом:**
>     * Цей підхід є дуже ефективним для обох, оскільки і масив, і `ArrayList` забезпечують швидкий доступ за індексом.
>
>     **Для `Array`:**
>     `for (int i = 0; i < intArray.length; i++) { ... }`
>     **Для `ArrayList`:**
>     `for (int i = 0; i < stringList.size(); i++) { ... }`
>
> **Додатково для `ArrayList`:**
> `ArrayList` також надає `Iterator` та `ListIterator`, які дозволяють виконувати безпечне видалення елементів під час ітерації. Масиви не мають такого механізму.

#### **📌 Що перевіряють:**

*Практичні навички роботи з обома структурами та знання різних способів їх обходу.*

* * *

### **5 ❓ Питання:**

Поясніть, що таке автобоксинг в контексті `ArrayList` і як він впливає на продуктивність. ⭐️

> #### **💬 Відповідь:**
>
> Оскільки `ArrayList` може зберігати тільки об'єкти, при спробі додати в `ArrayList<Integer>` примітив `int` Java виконує **автобоксинг (autoboxing)** — автоматичне перетворення примітиву `int` на об'єкт `Integer`.
>
> ```java
> List<Integer> list = new ArrayList<>();
> int primitive = 10;
> list.add(primitive); // "Під капотом" відбувається list.add(Integer.valueOf(primitive));
> ```
>
> **Вплив на продуктивність:**
>
> * Кожна операція автобоксингу створює **новий об'єкт-обгортку** (за винятком кешованих значень, наприклад, від -128 до 127 для `Integer`).
> * Якщо це відбувається у великому циклі, створюється велика кількість короткоживучих об'єктів.
> * Це призводить до **додаткового навантаження на збирач сміття (Garbage Collector)** і може викликати короткочасні "паузи" в роботі програми.
>
> **Анбоксинг** (зворотне перетворення) при читанні даних (`int value = list.get(0);`) також має невеликі накладні витрати на виклик методу (`intValue()`).
>
> Тому для інтенсивних числових обчислень `int[]` є значно кращим вибором, ніж `ArrayList<Integer>`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що зручність автобоксингу має свою ціну, та здатність аналізувати її вплив на продуктивність.*

* * *

### **6 ❓ Питання:**

Як можна ініціалізувати `Array` та `ArrayList` при створенні? ⭐️

> #### **💬 Відповідь:**
>
> **Ініціалізація масиву (`Array`):**
>
> * Можна використовувати синтаксис з фігурними дужками напряму.
>
> ```java
> String[] names = {"Alice", "Bob", "Charlie"};
> int[] numbers = new int[]{1, 2, 3};
> ```
>
> **Ініціалізація `ArrayList`:**
>
> 1. **Через `Arrays.asList()` (поширений спосіб):**
>
>     ```java
>     List<String> names = new ArrayList<>(Arrays.asList("Alice", "Bob"));
>     ```
>
> 2. **Через `List.of()` (з Java 9, для імутабельних списків):**
>
>     Якщо вам потрібен змінюваний список, ви передаєте результат `List.of()` у конструктор `ArrayList`.
>
>     ```java
>     List<String> names = new ArrayList<>(List.of("Alice", "Bob"));
>     ```
>
> 3. **Через `Stream API` (з Java 8):**
>
>     ```java
>     List<Integer> numbers = Stream.of(1, 2, 3).collect(Collectors.toList());
>     ```
>
> 4. **"Подвійні фігурні дужки" (Double Brace Initialization) — НЕ РЕКОМЕНДУЄТЬСЯ!**
>
>     ```java
>     List<String> names = new ArrayList<>() {{
>         add("Alice");
>         add("Bob");
>     }};
>     ```
>
> Цей підхід створює анонімний підклас `ArrayList`, що може призвести до витоків пам'яті і є антипатерном.

#### **📌 Що перевіряють:**

*Знання різних ідіоматичних способів ініціалізації. Важливо знати про недоліки "double brace initialization".*

* * *

### **7 ❓ Питання:**

Чи можна зберігати елементи різних типів у `Array` та `ArrayList`?

> #### **💬 Відповідь:**
>
> * **`Array`:**
>   * Масив є **строго типізованим**. `int[]` може зберігати тільки `int`, `String[]` — тільки `String`.
>   * **Виняток:** Ви можете створити `Object[]`, який зможе зберігати об'єкти будь-якого типу, оскільки всі класи є нащадками `Object`.
>
>     ```java
>     Object[] mixedArray = {"Hello", 123, new User()};
>     ```
>
> * **`ArrayList`:**
>
>   * Завдяки дженерікам `ArrayList` є **типобезпечним**. `ArrayList<String>` може зберігати тільки `String`.
>   * Щоб зберігати елементи різних типів, ви можете використовувати **сирий тип `ArrayList`** (не рекомендується) або параметризувати його `Object` — **`ArrayList<Object>`**.
>
>     ```java
>     List<Object> mixedList = new ArrayList<>();
>     mixedList.add("Hello");
>     mixedList.add(123);
>     ```
>
> В обох випадках при отриманні елементів з такої гетерогенної структури доведеться використовувати `instanceof` та приведення типів, щоб безпечно працювати з ними.

#### **📌 Що перевіряють:**

*Розуміння того, як працювати з гетерогенними даними в обох структурах.*

* * *

### **8 ❓ Питання:**

Як отримати розмір `Array` та `ArrayList`?

> #### **💬 Відповідь:**
>
> Це важлива синтаксична відмінність:
>
> * **`Array`:**
>   * Розмір отримується через **публічне поле `length`**.
>   * Це не виклик методу, тому дужки не потрібні.
>
>     ```java
>     int[] array = {1, 2, 3};
>     int size = array.length; // 3
>     ```
>
> * **`ArrayList`:**
>
>   * Розмір отримується через виклик **методу `size()`**.
>
>     ```java
>     List<Integer> list = List.of(1, 2, 3);
>     int size = list.size(); // 3
>     ```
>
> Плутанина між `length` та `size()` є поширеною помилкою для новачків.

#### **📌 Що перевіряють:**

*Уважність до синтаксичних деталей мови.*

* * *

### **9 ❓ Питання:**

Поясніть, що відбувається в пам'яті при створенні `int[]` та `ArrayList<Integer>`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`int[] array = new int[10];`:**
>   * Створюється **один об'єкт** в купі (heap) — об'єкт масиву.
>   * Цей масив займає **неперервну ділянку пам'яті**, достатню для зберігання 10 значень типу `int`.
>   * Самі **значення `int`** зберігаються безпосередньо всередині цієї ділянки пам'яті. Немає жодних додаткових об'єктів. Це дуже ефективно.
>
> * **`ArrayList<Integer> list = new ArrayList<>(10);`:**
>   * Створюється щонайменше **два об'єкти**:
>         1. Об'єкт `ArrayList`.
>         2. Внутрішній масив `Object[10]`.
>   * Коли ви додаєте елементи, наприклад, `list.add(5);`, відбувається **автобоксинг**.
>   * Це означає, що створюється **ще один об'єкт** — екземпляр класу `Integer` (якщо його немає в кеші).
>   * У внутрішньому масиві `Object[10]` зберігається **посилання** на цей об'єкт `Integer`.
>   * Якщо ви додаєте 10 унікальних чисел (поза кешем), то в пам'яті буде 1 об'єкт `ArrayList`, 1 масив `Object[]` та 10 об'єктів `Integer`. Всього **12 об'єктів**.
>
> **Висновок:** `ArrayList<Integer>` створює значно більше об'єктів і має набагато більші накладні витрати на пам'ять, ніж простий `int[]`.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як структури даних розташовуються в пам'яті, та впливу автобоксингу на це.*

* * *

### **10 ❓ Питання:**

Як дженеріки дають перевагу `ArrayList` над масивами з точки зору типобезпечності? ⭐️

> #### **💬 Відповідь:**
>
> Дженеріки переносять перевірку типів з **етапу виконання (runtime) на етап компіляції (compile-time)**.
>
> * **З `Array`:**
>
>     ```java
>     Object[] objectArray = new String[1];
>     // Помилка виявляється тільки під час виконання, коли JVM перевіряє типи
>     objectArray[0] = 123; // ArrayStoreException в runtime
>     ```
>
>     Проблема може проявитися значно пізніше і в іншій частині коду.
>
> * **З `ArrayList`:**
>
>     ```java
>     List<String> stringList = new ArrayList<>();
>     // Помилка виявляється на етапі компіляції!
>     // stringList.add(123); // Помилка компіляції
>     ```
>
>     Компілятор просто не дозволить вам написати код, який може порушити типобезпечність `ArrayList<String>`. Ви дізнаєтесь про помилку одразу, а не коли програма вже запущена у клієнта.
>
> Таким чином, `ArrayList` завдяки дженерікам надає набагато **вищий рівень гарантій типобезпечності** ще до запуску програми.

#### **📌 Що перевіряють:**

*Чітке розуміння переваги перевірок на етапі компіляції над перевірками на етапі виконання.*

* * *

### **11 ❓ Питання:**

Що поверне `new int[0].length` та `new ArrayList<>().size()`?

> #### **💬 Відповідь:**
>
> Обидва вирази повернуть **0**.
>
> * `new int[0].length` поверне `0`, тому що створюється масив нульової довжини.
> * `new ArrayList<>().size()` поверне `0`, тому що створюється порожній список, в якому немає елементів (його `size` дорівнює 0).

#### **📌 Що перевіряють:**

*Уважність до базових операцій та вміння працювати з "порожніми" структурами.*

* * *

### **12 ❓ Питання:**

Чи можна додати `null` у `String[]` та `ArrayList<String>`?

> #### **💬 Відповідь:**
>
> Так, можна додати `null` **в обидві структури**.
>
> * **`Array`:**
>   * Масив об'єктів може зберігати `null` як одне зі своїх значень.
>
>     ```java
>     String[] names = new String[2];
>     names[0] = "Alice";
>     names[1] = null; // Валідно
>     ```
>
> * **`ArrayList`:**
>   * Стандартна реалізація `ArrayList` також дозволяє додавати та зберігати `null` значення.
>
>     ```java
>     List<String> names = new ArrayList<>();
>     names.add("Alice");
>     names.add(null); // Валідно
>     ```
>
> Важливо пам'ятати про це, оскільки при обробці таких структур даних може знадобитися додаткова перевірка на `null` для уникнення `NullPointerException`.

#### **📌 Що перевіряють:**

*Знання того, як масиви та колекції обробляють `null` значення.*

* * *

### **13 ❓ Питання:**

Якщо потрібно створити багатовимірну структуру (матрицю), що краще використовувати: `int[][]` чи `ArrayList<ArrayList<Integer>>`? Поясніть компроміси. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вибір залежить від вимог задачі.
>
> **`int[][]` (багатовимірний масив):**
>
> * **Переваги:**
>   * **Продуктивність:** Значно вища. Доступ до елемента `matrix[i][j]` є дуже швидким.
>   * **Ефективність по пам'яті:** Значно менші накладні витрати. Немає об'єктів-обгорток, об'єктів `ArrayList` для кожного рядка тощо.
>   * **Зручність синтаксису:** `matrix[i][j]` є більш лаконічним і звичним для математичних операцій.
> * **Недоліки:**
>   * **Фіксований розмір:** Кількість рядків і стовпців є фіксованою.
>
> **`ArrayList<ArrayList<Integer>>`:**
>
> * **Переваги:**
>   * **Гнучкість:** Кількість рядків і довжина кожного рядка можуть бути **динамічними**. Можна легко додати або видалити цілий рядок або елемент у рядку.
> * **Недоліки:**
>   * **Продуктивність:** Значно нижча через багаторівневі виклики методів (`list.get(i).get(j)`), накладні витрати та анбоксинг.
>   * **Пам'ять:** Дуже великі накладні витрати на зберігання об'єктів `ArrayList` для кожного рядка та об'єктів `Integer` для кожного числа.
>
> **Висновок:**
>
> * Для **математичних обчислень, обробки зображень, ігрової логіки** та будь-яких задач, де розмір матриці фіксований і продуктивність критична, **завжди використовуйте `int[][]`**.
> * `ArrayList<ArrayList<Integer>>` може бути виправданий тільки якщо вам потрібна **динамічна "зубчаста" матриця**, де рядки мають різну довжину і часто змінюються.

#### **📌 Що перевіряють:**

*Здатність аналізувати компроміси для більш складних структур даних. Розуміння того, коли простота та продуктивність масивів переважають гнучкість колекцій.*

* * *

### **14 ❓ Питання:**

Чи можна передати масив у метод, що приймає `varargs` (`...`)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна. `Varargs` є синтаксичним цукром над **масивами**. Метод, оголошений з `String...`, насправді приймає `String[]`.
>
> ```java
> public void printNames(String... names) { // насправді це printNames(String[] names)
>     for (String name : names) {
>         System.out.println(name);
>     }
> }
> ```
>
> **Ви можете викликати цей метод двома способами:**
>
> 1. **Перераховуючи елементи:**
>     `printNames("Alice", "Bob"); // Java сама створить масив new String[]{"Alice", "Bob"}`
>
> 2. **Передаючи існуючий масив:**
>     `String[] nameArray = {"Charlie", "David"};`
>     `printNames(nameArray); // Масив буде передано напряму`
>
> Це робить `varargs`-методи дуже гнучкими. Це також підкреслює тісний зв'язок між `varargs` і масивами, і пояснює, чому `varargs` з дженериками (`T...`) можуть бути небезпечними.

#### **📌 Що перевіряють:**

*Розуміння того, що `varargs` — це синтаксичний цукор над масивами.*

* * *

### **15 ❓ Питання:**

Що поверне такий код і чому?
`List<String> list = Arrays.asList("a", "b");`
`String[] array = new String[]{"a", "b"};`
`list.equals(array)` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код поверне **`false`**.
>
> **Причина:**
>
> Метод `equals()` для `List` (реалізований в `AbstractList`) визначений так, що він може бути рівним тільки **іншому об'єкту `List`**.
>
> Його реалізація виглядає приблизно так:
>
> 1. Перевірка `if (obj == this) return true;`.
> 2. Перевірка `if (!(obj instanceof List)) return false;`.
> 3. Далі йде по елементне порівняння.
>
> Оскільки масив (`array`) **не є екземпляром `List`** (`!(array instanceof List)` поверне `true`), метод `equals` для списку негайно поверне `false`, навіть не починаючи порівнювати елементи.
>
> **Взаємне порівняння:**
> `array.equals(list)` також поверне `false`, бо `equals` для масивів порівнює посилання.
>
> Незважаючи на те, що масиви та списки можуть представляти однакові дані, вони є різними типами, і їхні методи `equals` не є взаємосумісними.

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння реалізації методу `equals` для стандартних колекцій та їхньої строгої перевірки на тип.*
