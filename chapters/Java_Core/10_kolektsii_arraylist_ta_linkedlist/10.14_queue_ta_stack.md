---
layout: default
title: "Queue та Stack"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 14
---

# Queue та Stack

* короткий опис: `Queue` – це інтерфейс черги, що працює за принципом `FIFO` (First-In-First-Out). `Stack` – це структура даних, що працює за принципом `LIFO` (Last-In-First-Out). `Deque` – це двостороння черга, що може працювати як `Queue` та `Stack`.

* поняття: `Queue`, `Stack`, `FIFO`, `LIFO`, `Deque`, `ArrayDeque`, `PriorityQueue`.

* приклади: `queue.add()`, `queue.poll()` (для `Queue`), `stack.push()`, `stack.pop()` (для `Stack`). `ArrayDeque` як рекомендована заміна `Stack`.

* ключові моменти: `Queue` для черг, `Stack` для стеку (застарілий). `Deque` є універсальною та ефективною заміною для обох.

* що важливо знати на співбесіді: Принципи `FIFO`/`LIFO`, основні реалізації, `Stack` вважається `legacy`.

### **✅ Офіційне визначення:**

* **`Queue`** — структура даних, яка працює за принципом **FIFO** (*First-In-First-Out*).
* **`Stack`** — структура даних, яка працює за принципом **LIFO** (*Last-In-First-Out*).

### **🧠 Простими словами:**

* **`Черга`**: перший прийшов — перший пішов (як черга в магазині 🧍🧍‍♂️🧍‍♀️).
* **`Стек`**: останній прийшов — перший пішов (як стопка тарілок 🍽️).

---

## **📊 Основні інтерфейси та класи:**

| Тип | Інтерфейс/Клас | Особливості |
| :---: | :---: | ----- |
| **Queue** | **`Queue<E>`** | Основний інтерфейс для черг |
| **Deque** | **`Deque<E>`** | Двобічна черга (можна як Queue, так і Stack) |
| **Stack** | **`Stack<E>`** | Застарілий клас, краще не використовувати |
| **ArrayDeque** | **`ArrayDeque<E>`** | Швидка реалізація черги та стека |
| **PriorityQueue** | **`PriorityQueue<E>`** | Черга з пріоритетом |
| **LinkedList** | **`LinkedList<E>`** | Реалізує і Queue, і Deque |

---

## **Пояснення на пальцях (💡)**

* **`Queue`** — каса в супермаркеті 🛒
* **`PriorityQueue`** — VIP-клієнти проходять без черги 😎
* **`Stack`** — стопка млинців: останній зверху, перший з'їдається 🥞
* **`Deque`** — універсал: і черга, і стек у одному 🚗

---

## **Корисні приклади (🧪)**

### **Queue:**

```java
Stack<String> stack = new Stack<>();
stack.push("first");
stack.push("second");
System.out.println(stack.pop()); // second
```

### **Stack (застарілий спосіб):**

```java
Stack<String> stack = new Stack<>();
stack.push("first");
stack.push("second");
System.out.println(stack.pop()); // second
```

### **Рекомендований спосіб — Deque як стек:**

```java
Deque<String> stack = new ArrayDeque<>();
stack.push("first");
stack.push("second");
System.out.println(stack.pop()); // second
```

### **PriorityQueue:**

```java
Queue<Integer> pq = new PriorityQueue<>();
pq.add(10);
pq.add(2);
pq.add(30);
System.out.println(pq.poll()); // 2 -- найменший елемент
```

---

## **Порівняння (⚖️)**

| Характеристика | Queue | Stack | Deque | PriorityQueue |
| :---: | :---: | :---: | :---: | :---: |
| **Принцип** | FIFO | LIFO | FIFO/LIFO | Залежно від пріоритету |
| **Синхронізований** | Ні | Так (але застарілий) | Ні | Ні |
| **Швидкість** | Висока (ArrayDeque) | Середня | Висока | Залежить від об'єму |
| **Підтримка null** | Не рекомендується | Так | Ні | Не допускає null |

---

## **🧠 Мнемоніка для запам’ятовування:**

* **"FIFO — Queue, бо ти перший у фойє"**
* **"LIFO — Stack, бо останній закинув рюкзак — перший витяг"**
* **"Deque — як ніж: з обох боків ріже"**

---

## **🟩 Підсумок:**

* **`Queue`** і **`Stack`** — базові структури даних, корисні для задач типу "обробка в порядку".
* **`ArrayDeque`** — універсальний і продуктивний варіант для більшості потреб.
* **`PriorityQueue`** — ідеально для задач із пріоритетами.
* Уникай класу **`Stack`**, віддай перевагу **`Deque`**.

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Queue**](https://docs.oracle.com/javase/8/docs/api/java/util/Queue.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке черга (`Queue`) та стек (`Stack`)? У чому їхня основна відмінність?

> #### **💬 Відповідь:**
>
> `Queue` та `Stack` — це дві лінійні структури даних, які відрізняються **порядком доступу до елементів**.
>
> * **`Queue` (Черга):**
>   * Працює за принципом **FIFO (First-In, First-Out)** — "перший прийшов, перший пішов".
>   * Елементи додаються в кінець черги (`add`/`offer`) і видаляються з її початку (`remove`/`poll`).
>   * **Аналогія:** черга людей в магазині.
>
> * **`Stack` (Стек):**
>   * Працює за принципом **LIFO (Last-In, First-Out)** — "останній прийшов, перший пішов".
>   * Елементи додаються і видаляються з одного й того ж кінця, який називається "вершиною" стеку (`push`/`pop`).
>   * **Аналогія:** стопка тарілок. Ви кладете нову тарілку наверх і берете теж з верху.
>
> **Основна відмінність:** `Queue` обробляє елементи в тому порядку, в якому вони надійшли, а `Stack` — у зворотному порядку.

#### **📌 Що перевіряють:**

*Знання базових принципів FIFO та LIFO та здатність їх пояснити на простих аналогіях.*

* * *

### **2 ❓ Питання:**

Які основні реалізації інтерфейсу `Queue` існують в Java?

> #### **💬 Відповідь:**
>
> До основних реалізацій `Queue` належать:
>
> 1. **`LinkedList`:**
>     * Реалізація на основі двозв'язного списку. Вона реалізує інтерфейс `Deque`, тому може використовуватися як FIFO-черга. Є гнучкою, але може бути менш продуктивною, ніж `ArrayDeque`.
>
> 2. **`ArrayDeque`:**
>     * Реалізація на основі динамічного циклічного масиву. Це дуже ефективна реалізація як черги, так і стеку. Зазвичай є **кращим вибором** для FIFO-черг, якщо не потрібна потокобезпечність.
>
> 3. **`PriorityQueue`:**
>     * **Особлива реалізація**, яка є **не FIFO-чергою**. Елементи в ній впорядковані згідно з їхнім **пріоритетом** (або за природним порядком, або за допомогою `Comparator`).
>     * Метод `poll()` завжди повертає елемент з найвищим пріоритетом. Внутрішньо реалізована на основі **бінарної купи (heap)**.

#### **📌 Що перевіряють:**

*Знання основних класів для роботи з чергами та їхніх особливостей (`LinkedList` vs `ArrayDeque` vs `PriorityQueue`).*

* * *

### **3 ❓ Питання:**

Чому клас `java.util.Stack` вважається застарілим і що рекомендується використовувати замість нього? ⭐️

> #### **💬 Відповідь:**
>
> Клас `Stack` вважається застарілим (`legacy`) через **погане дизайнерське рішення** в його реалізації.
>
> **Проблема:** **`Stack extends Vector`**.
>
> * `Vector` — це синхронізований аналог `ArrayList`.
> * Стек за своєю логікою **не є** вектором. Успадкування призводить до того, що `Stack` отримує купу **непотрібних і шкідливих методів**, як-от `get(index)` або `add(index, element)`, які дозволяють маніпулювати елементами в середині стеку, що порушує сам принцип LIFO.
> * Крім того, оскільки `Vector` синхронізований, `Stack` також є `synchronized`, що створює непотрібні накладні витрати на блокування в однопотоковому середовищі.
>
> **Що використовувати замість нього:**
>
> Рекомендується використовувати будь-яку реалізацію інтерфейсу **`java.util.Deque`**, а саме — **`ArrayDeque`**.
>
> ```java
> // Старий підхід:
> // Stack<String> stack = new Stack<>();
>
> // Сучасний рекомендований підхід:
> Deque<String> stack = new ArrayDeque<>();
> ```
>
> Інтерфейс `Deque` надає повний набір методів для роботи зі стеком (`push`, `pop`, `peek`) і є значно більш продуктивним та правильно спроектованим.

#### **📌 Що перевіряють:**

*Розуміння, чому успадкування `Stack` від `Vector` є поганим дизайном. Знання сучасних "best practices" для реалізації стеку.*

* * *

### **4 ❓ Питання:**

Яка різниця між методами `add()`/`remove()` та `offer()`/`poll()` для `Queue`?

> #### **💬 Відповідь:**
>
> Ці пари методів виконують однакові дії, але **по-різному поводяться в "помилкових" ситуаціях**, зокрема при роботі з чергами обмеженого розміру.
>
> **Методи, що кидають винятки:**
>
> * **`add(E e)`:** Додає елемент. Якщо черга повна (для `BlockingQueue`), кидає **`IllegalStateException`**.
> * **`remove()`:** Видаляє елемент. Якщо черга порожня, кидає **`NoSuchElementException`**.
>
> **Методи, що повертають спеціальні значення:**
>
> * **`offer(E e)`:** Додає елемент. Якщо черга повна, **просто повертає `false`**.
> * **`poll()`:** Видаляє елемент. Якщо черга порожня, **просто повертає `null`**.
>
> **Коли що використовувати:**
> Методи `offer()` та `poll()` є **кращими** для програмної логіки, оскільки вони дозволяють уникнути блоків `try-catch` і обробити ситуацію за допомогою звичайної `if`-перевірки. Методи `add()`/`remove()` використовуються, коли неможливість виконати операцію є винятковою ситуацією, яка має перервати нормальний потік виконання.

#### **📌 Що перевіряють:**

*Уважність до деталей API. Розуміння різниці між методами, що сигналізують про помилку через виняток, та методами, що повертають спеціальне значення.*

* * *

### **5 ❓ Питання:**

Що таке `Deque` і як його можна використовувати для реалізації і стеку, і черги?

> #### **💬 Відповідь:**
>
> **`Deque`** розшифровується як **"double-ended queue" (двостороння черга)**. Це інтерфейс, що розширює `Queue` і дозволяє додавати та видаляти елементи **як з початку, так і з кінця**.
>
> Завдяки цій універсальності, `Deque` може імітувати обидві структури:
>
> * **Як чергу (FIFO):**
>   * Додавання в кінець: `addLast()` або `offerLast()`.
>   * Видалення з початку: `removeFirst()` або `pollFirst()`.
>
> * **Як стек (LIFO):**
>   * Додавання наверх стеку (в початок `Deque`): `addFirst()` або **`push()`**.
>   * Видалення з верху стеку (з початку `Deque`): `removeFirst()` або **`pop()`**.
>
> `Deque` надає методи `push()` та `pop()` спеціально для зручності використання як стеку.

#### **📌 Що перевіряють:**

*Знання інтерфейсу `Deque` та його універсальності.*

* * *

### **6 ❓ Питання:**

Що таке `PriorityQueue` і чим вона відрізняється від звичайної черги? ⭐️

> #### **💬 Відповідь:**
>
> **`PriorityQueue`** — це особливий тип черги, який **не дотримується принципу FIFO**.
>
> **Ключова відмінність:**
> `PriorityQueue` впорядковує елементи згідно з їхнім **пріоритетом**. При виклику `poll()` або `peek()` вона завжди повертає елемент з **найвищим пріоритетом**. За замовчуванням, це **найменший** елемент (згідно з природним порядком).
>
> **Як визначається пріоритет:**
>
> 1. Або елементи, що додаються, повинні реалізовувати інтерфейс **`Comparable`**.
> 2. Або в конструктор `PriorityQueue` потрібно передати об'єкт **`Comparator`**, який буде визначати порядок.
>
> **Внутрішня структура:** Вона реалізована на основі **бінарної купи (binary heap)**, що дозволяє ефективно (за **O(log n)**) додавати елементи та отримувати елемент з найвищим пріоритетом.
>
> **Використання:** Ідеально підходить для задач, де потрібно обробляти елементи не в порядку їх надходження, а в порядку їхньої важливості (наприклад, планувальник завдань).

#### **📌 Що перевіряють:**

*Розуміння концепції пріоритетних черг, їхньої реалізації на основі купи та основних сценаріїв використання.*

* * *

### **7 ❓ Питання:**

Яку колекцію ви б використали для реалізації алгоритму "Пошук в ширину" (BFS) на графі, а яку для "Пошуку в глибину" (DFS)? ⭐️

> #### **💬 Відповідь:**
>
> * **Для Пошуку в ширину (Breadth-First Search, BFS):**
>   * Використовується **`Queue` (Черга)**.
>   * **Чому:** BFS досліджує граф "шар за шаром". Черга ідеально підходить для цього: ми додаємо сусідів поточного вузла в кінець черги, а для обробки беремо вузол з початку. Це гарантує, що ми спочатку обробимо всі вузли на поточному рівні, перш ніж перейти на наступний.
>   * **Конкретна реалізація:** `ArrayDeque` або `LinkedList` (як `Queue`).
>
> * **Для Пошуку в глибину (Depth-First Search, DFS):**
>   * Використовується **`Stack` (Стек)**, або реалізується рекурсивно (що неявно використовує стек викликів).
>   * **Чому:** DFS досліджує одну гілку графу настільки глибоко, наскільки це можливо, перш ніж повернутися назад. Стек (LIFO) ідеально для цього підходить: ми кладемо на вершину сусідів і одразу беремо звідти останній доданий для подальшого заглиблення.
>   * **Конкретна реалізація:** `ArrayDeque` (як `Stack`).

#### **📌 Що перевіряють:**

*Знання класичних алгоритмів та вміння вибрати для них правильні структури даних. Це питання перетинається з алгоритмічною підготовкою.*

* * *

### **8 ❓ Питання:**

Чи дозволяє `PriorityQueue` зберігати `null`? А `ArrayDeque`?

> #### **💬 Відповідь:**
>
> * **`PriorityQueue`**: **Ні, не дозволяє.**
>   * **Причина:** `PriorityQueue` повинна мати можливість порівнювати елементи між собою, щоб визначати їхній пріоритет. Операція порівняння `null` з будь-яким іншим об'єктом є невизначеною і зазвичай призводить до `NullPointerException`. Щоб уникнути цього, `PriorityQueue` просто забороняє додавання `null`.
>
> * **`ArrayDeque`**: **Ні, не дозволяє.**
>   * **Причина:** Ця заборона пов'язана з методами, як-от `poll()`, які повертають `null`, щоб сигналізувати, що черга/стек порожні. Якби `null` можна було зберігати як елемент, то отримання `null` з `poll()` стало б неоднозначним: це може означати, що черга порожня, або що ми дістали елемент `null`. Щоб уникнути цієї неоднозначності, `null` як елемент заборонений.
>
> **А `LinkedList`?** `LinkedList` **дозволяє** `null`, оскільки його можна використовувати як звичайний `List`, де `null` може бути валідним значенням.

#### **📌 Що перевіряють:**

*Уважність до деталей специфікації API. Розуміння причин, що стоять за певними обмеженнями.*

* * *

### **9 ❓ Питання:**

Поясніть, що роблять методи `peek()` та `element()` для `Queue`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Обидва методи призначені для того, щоб **"подивитися"** на елемент у голові черги, **не видаляючи** його.
>
> Їхня різниця, як і у пари `poll/remove`, полягає в поведінці для порожньої черги.
>
> * **`element()`**:
>   * Повертає елемент з голови черги.
>   * Якщо черга **порожня**, кидає виняток **`NoSuchElementException`**.
>
> * **`peek()`**:
>   * Повертає елемент з голови черги.
>   * Якщо черга **порожня**, повертає **`null`**.
>
> Так само, `peek()` є кращим для програмної обробки, оскільки дозволяє перевірити наявність елемента без використання `try-catch`.

#### **📌 Що перевіряють:**

*Детальне знання API черг та поведінки методів у крайових випадках.*

* * *

### **10 ❓ Питання:**

Що таке `BlockingQueue` і де вона використовується? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`BlockingQueue`** — це розширення `Queue` з пакету `java.util.concurrent`, яке додає **блокуючу поведінку** і призначене для багатопотокового середовища.
>
> **Ключова поведінка:**
>
> * **Блокування при отриманні:** Якщо потік намагається отримати елемент з **порожньої** черги (через метод `take()`), він **блокується** і чекає, доки інший потік не додасть елемент.
> * **Блокування при додаванні:** Якщо черга має **обмежений розмір** і є повною, то потік, що намагається додати елемент (через метод `put()`), **блокується** і чекає, доки не з'явиться вільне місце.
>
> **Де використовується:**
>
> `BlockingQueue` є ідеальним інструментом для реалізації патерну **"Продюсер-Споживач" (Producer-Consumer)**.
>
> * **Продюсери** додають завдання в чергу.
> * **Споживачі** забирають завдання з черги.
> * Черга сама дбає про всю складну синхронізацію між ними.
>
> Цей патерн є основою для багатьох систем, включаючи пули потоків ( `ThreadPoolExecutor` використовує `BlockingQueue` для зберігання завдань).

#### **📌 Що перевіряють:**

*Знання конкурентних колекцій та їхньої ролі в організації взаємодії між потоками.*

* * *

### **11 ❓ Питання:**

Якщо вам потрібен стек з фіксованим, обмеженим розміром, яку колекцію найкраще використати? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Найкращим вибором для цього завдання буде **`ArrayBlockingQueue`**. Хоча її назва говорить про "чергу", її можна використовувати для реалізації обмеженого стеку, якщо це потрібно. Однак для стека з обмеженням розміру довелося б писати власну обгортку, що незручно.
>
> Але якщо нам потрібна саме поведінка LIFO(Stack) і обмежений розмір то для такої специфічної задачі може знадобитися **створення власної реалізації**, яка "під капотом" використовує масив і відстежує його заповнення.
>
> **Приклад простої реалізації:**
>
> ```java
> public class BoundedStack<E> {
>     private final E[] elements;
>     private int size = 0;
>
>     @SuppressWarnings("unchecked")
>     public BoundedStack(int capacity) {
>         this.elements = (E[]) new Object[capacity];
>     }
>
>     public synchronized void push(E item) {
>         if (size == elements.length) {
>             // або кидати виняток, або ігнорувати
>             throw new StackOverflowError();
>         }
>         elements[size++] = item;
>     }
>
>     public synchronized E pop() {
>         if (size == 0) {
>             throw new EmptyStackException();
>         }
>         E item = elements[--size];
>         elements[size] = null; // для збирача сміття
>         return item;
>     }
> }
> ```
>
> Використання `ArrayDeque` напряму не підходить, бо він **не має обмеженої ємності** (він динамічно розширюється).

#### **📌 Що перевіряють:**

*Здатність аналізувати нетипові вимоги та розуміння, що іноді стандартні колекції не підходять і потрібно писати власну реалізацію.*

* * *

### **12 ❓ Питання:**

Чи можна ітерувати по `PriorityQueue` за допомогою циклу `for-each`? Якщо так, то в якому порядку будуть повернуті елементи? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, `PriorityQueue` реалізує `Iterable`, тому **можна** ітерувати по ній за допомогою `for-each`.
>
> **Однак** порядок, у якому будуть повернуті елементи під час такої ітерації, **не гарантується**.
>
> **Чому?**
>
> * `PriorityQueue` внутрішньо використовує **бінарну купу (heap)**. Купа є лише **частково впорядкованою** структурою. Вона гарантує, що батьківський вузол завжди має вищий пріоритет, ніж його діти.
> * Але вона **не гарантує** жодного порядку між "братами" (sibling nodes) або між вузлами на різних рівнях.
> * Ітератор `PriorityQueue` зазвичай виконує простий обхід масиву, що представляє купу, і **не повертає** елементи у відсортованому порядку.
>
> **Єдиний спосіб** отримати елементи з `PriorityQueue` у порядку пріоритету — це **послідовно викликати `poll()`**, доки черга не стане порожньою.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що внутрішня структура (купа) не є повністю відсортованою, і це впливає на поведінку ітератора.*

* * *

### **13 ❓ Питання:**

Назвіть часову складність основних операцій для `ArrayDeque`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> `ArrayDeque` є дуже ефективним завдяки своїй реалізації на основі циклічного масиву.
>
> * `addFirst(e)` / `push(e)` (додавання на початок): **O(1) амортизовано**.
> * `addLast(e)` / `offer(e)` (додавання в кінець): **O(1) амортизовано**.
> * `removeFirst()` / `pop()` (видалення з початку): **O(1)**.
> * `removeLast()` (видалення з кінця): **O(1)**.
> * `peekFirst()` / `peek()` (подивитися на початок): **O(1)**.
> * `peekLast()` (подивитися на кінець): **O(1)**.
> * `contains(o)`: **O(n)**. Як і для `ArrayList`, потрібен лінійний пошук.
>
> **Амортизована** складність для додавання пов'язана з тим, що іноді `ArrayDeque` може потребувати **розширення** свого внутрішнього масиву, що є операцією O(n). Але оскільки це трапляється рідко, середня вартість залишається O(1).
>
> Саме ці показники роблять `ArrayDeque` кращим вибором для реалізації стека та черги, ніж `LinkedList`.

#### **📌 Що перевіряють:**

*Знання асимптотичної складності для `ArrayDeque`, яка є рекомендованою реалізацією `Deque`.*

* * *

### **14 ❓ Питання:**

Якби вам потрібно було реалізувати задачу "розбір виразу зі дужками" (наприклад, перевірка `({[]})`), яку структуру даних ви б використали і чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для цієї класичної задачі ідеальним вибором є **стек (Stack)**, реалізований через `Deque` (наприклад, `ArrayDeque`).
>
> **Чому стек?**
>
> Принцип LIFO (Last-In, First-Out) ідеально відповідає логіці вкладених дужок. Дужка, що відкрилася останньою, має закритися першою.
>
> **Алгоритм:**
>
> 1. Створюємо порожній стек: `Deque<Character> stack = new ArrayDeque<>();`.
> 2. Ітеруємо по рядку символів.
> 3. Якщо зустрічаємо **відкриваючу дужку** (`(`, `{`, `[`), ми "кладемо" її на вершину стеку (`stack.push(char)`).
> 4. Якщо зустрічаємо **закриваючу дужку** (`)`, `}`, `]`):
>     * Перевіряємо, чи стек порожній. Якщо так — помилка.
>     * Виймаємо останню відкриваючу дужку з вершини стеку (`stack.pop()`).
>     * Перевіряємо, чи відповідає вона поточній закриваючій дужці. Якщо ні — помилка.
> 5. Після завершення ітерації по рядку, **стек має бути порожнім**. Якщо ні — помилка (є незакриті дужки).
>
> Стек дозволяє легко і елегантно відстежувати "рівні вкладеності" та очікуваний порядок закриття дужок.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про структури даних для вирішення класичних алгоритмічних задач.*

* * *

### **15 ❓ Питання:**

Як реалізувати чергу за допомогою двох стеків? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичне питання на співбесідах, яке перевіряє креативність та розуміння роботи стека.
>
> **Ідея:**
>
> 1. Використовуються два стеки: `inbox` (для додавання) та `outbox` (для видалення).
> 2. **Операція `enqueue` (додавання в чергу):**
>     * Новий елемент завжди додається (`push`) у стек `inbox`.
> 3. **Операція `dequeue` (видалення з черги):**
>     * **Спочатку перевіряємо `outbox`.** Якщо він не порожній, просто викликаємо `outbox.pop()`, оскільки там елементи вже лежать у правильному FIFO-порядку.
>     * **Якщо `outbox` порожній,** ми "перевертаємо" `inbox`. Ми послідовно виймаємо всі елементи з `inbox` (`inbox.pop()`) і додаємо їх у `outbox` (`outbox.push()`). Після цього елементи в `outbox` будуть у зворотному порядку, що відповідає FIFO.
>     * Після цього викликаємо `outbox.pop()`.
>
> **Амортизована складність:**
>
> Хоча операція перенесення всіх елементів з `inbox` в `outbox` має складність O(n), вона відбувається рідко. Кожен елемент переноситься лише один раз за весь час. Тому **амортизована складність** операції `dequeue` залишається **O(1)**.
>
> Ця задача демонструє, як за допомогою більш простих структур (стек) можна реалізувати більш складну (чергу).

#### **📌 Що перевіряють:**

*Алгоритмічне мислення. Здатність вирішувати нетривіальні задачі та аналізувати амортизовану складність.*
