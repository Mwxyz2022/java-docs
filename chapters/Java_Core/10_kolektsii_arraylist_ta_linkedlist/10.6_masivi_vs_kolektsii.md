---
layout: default
title: "Масиви vs Колекції"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 6
---

# Масиви vs Колекції

* короткий опис: Масиви (Array) – це структури з фіксованим розміром, що зберігають елементи одного типу (примітиви або об'єкти) у безперервній пам'яті. Колекції (Collections Framework) – динамічні структури для груп об'єктів, що надають багатий API.

* поняття: Масив, Колекції, фіксований/динамічний розмір, примітиви/об'єкти, API.

* приклади: `int[]` (масив), `ArrayList<Integer>` (колекція).

* ключові моменти: Масиви швидші та ефективніші для фіксованих обсягів, колекції – гнучкіші та багатофункціональні. Колекції працюють лише з об'єктами.

* що важливо знати на співбесіді: Коли що використовувати, переваги/недоліки.

### **✅ Офіційне визначення:**

* **`Масив (Array)`** — структура даних з фіксованим розміром, яка зберігає елементи одного типу у безперервній ділянці пам’яті.
* **`Колекції (Collections Framework)`** — динамічні структури даних у Java, які надають набір інтерфейсів та реалізацій для роботи з групами об’єктів різної складності.

### **🧠 Простими словами:**

**`Масив`** — це як лінійка з фіксованою кількістю комірок, а колекції — як змінний список речей, який може розростатися або звужуватися в міру потреби

---

## **Порівняльна таблиця:**

| Критерій | Масив | Колекції |
| :---: | ----- | ----- |
| **Розмір** | Статичний (фіксований) | Динамічний (змінний) |
| **Типи даних** | Примітивні і об’єкти | Тільки об’єкти |
| **API** | Обмежений (length, індексація) | Багатий (додавання, видалення, пошук, сортування, потокові операції) |
| **Гнучкість** | Низька | Висока |
| **Продуктивність** | Вища (пряма робота з пам’яттю) | Трохи нижча через накладні витрати |
| **Доступ за індексом** | Швидкий (O(1)) | Залежить від реалізації (ArrayList — швидкий, LinkedList — повільний) |
| **Пам’ять** | Ефективніше | Менш ефективно через додаткові структури |

---

## **Пояснення на пальцях (💡)**

* **`Масив`** — це коробка з фіксованою кількістю відділень, де кожне відділення зайняте для конкретного типу речей. Якщо треба більше — треба купувати нову коробку.
* **`Колекції`** — це як рюкзак, який сам підлаштовується під кількість речей, додаючи або прибираючи відділення.

---

## **Корисні приклади (🧪)**

```java
// Масив з 3-ма цілими числами (фіксований розмір)
int[] arr = new int[3];
arr[0] = 10;
arr[1] = 20;
arr[2] = 30;

// Колекція ArrayList з динамічним розміром
ArrayList<Integer> list = new ArrayList<>();
list.add(10);
list.add(20);
list.add(30);
list.add(40);  // можна додати більше елементів без обмежень
```

---

## **Порівняння (⚖️)**

* **`Масиви`** мають перевагу в продуктивності та меншому споживанні пам’яті, особливо для примітивних типів.
* **`Колекції`** мають більш зручний та багатий API, підтримують динамічну зміну розміру, потужні операції з фільтрацією, сортуванням, потоками (Streams).

---

## **🧠 Мнемоніка для запам’ятовування:**

**`Масив`** — це як коробка з фіксованою кількістю осередків, а **`колекції`** — як рюкзак, що підлаштовується під твої потреби

---

## **🟩 Підсумок:**

**`Масиви`** — прості, швидкі та ефективні для фіксованих наборів даних, особливо примітивних.

**`Колекції`** — гнучкі, з багатим API та динамічним розміром, ідеальні для складних задач роботи з об’єктами. Вибір залежить від вимог продуктивності та зручності.

---

**🔗 Для поглиблення:**

* [**Oracle Docs \- Arrays**](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/arrays.html)
* [**Oracle Docs \- Collections Framework**](https://docs.oracle.com/javase/8/docs/technotes/guides/collections/overview.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому полягає фундаментальна різниця між масивами (Arrays) та колекціями (Collections) в Java?

> #### **💬 Відповідь:**
>
> Фундаментальна різниця полягає у наступних ключових аспектах:
>
> 1. **Розмір:**
>     * **Масиви:** Мають **фіксований розмір**, який визначається під час їх створення і не може бути змінений.
>     * **Колекції:** Є **динамічними**. Їхній розмір може збільшуватися або зменшуватися під час виконання програми.
>
> 2. **Типи даних:**
>     * **Масиви:** Можуть зберігати як **примітивні типи** (`int[]`, `double[]`), так і **об'єкти** (`String[]`).
>     * **Колекції:** Працюють **тільки з об'єктами**. Для зберігання примітивів використовуються їхні класи-обгортки (`List<Integer>`, `Set<Double>`).
>
> 3. **Функціональність (API):**
>     * **Масиви:** Надають лише базові можливості: доступ за індексом та властивість `length`.
>     * **Колекції:** Є частиною потужного **Collections Framework**, який надає багатий набір методів для сортування, пошуку, фільтрації, ітерації, маніпуляцій з даними.
>
> 4. **Реалізація:**
>     * **Масиви:** Є вбудованою конструкцією мови.
>     * **Колекції:** Є набором класів та інтерфейсів у стандартній бібліотеці Java.

#### **📝 Аналогія:**

* **Масив** — це як лоток для яєць. У нього є фіксована кількість комірок (10), і ви не можете додати 11-те яйце, не взявши новий лоток.
* **Колекція** — це як сумка для покупок. Ви можете класти в неї продукти, і вона буде розтягуватися по мірі необхідності.

#### **📌 Що перевіряють:**

*Чітке розуміння основних відмінностей: фіксований vs динамічний розмір, примітиви vs об'єкти, обмежений API vs багатий.*

* * *

### **2 ❓ Питання:**

Коли варто обирати масиви, а коли — колекції?

> #### **💬 Відповідь:**
>
> Вибір залежить від конкретної задачі та вимог.
>
> **Коли варто обирати масиви:**
>
> * **Коли розмір даних фіксований і відомий заздалегідь.** Наприклад, для зберігання днів тижня, місяців, або коли ви працюєте з низькорівневими даними, як-от масив байтів для I/O.
> * **Для максимальної продуктивності та ефективності по пам'яті.** Масиви мають менше накладних витрат і забезпечують найшвидший доступ до даних, особливо для примітивних типів. Вони ідеальні для "гарячих" циклів та інтенсивних обчислень.
> * При роботі з **багатовимірними структурами** (матрицями). Синтаксис `int[][]` є більш зручним, ніж `List<List<Integer>>`.
>
> **Коли варто обирати колекції:**
>
> * **Коли кількість елементів невідома або динамічно змінюється.** Це найпоширеніший випадок у бізнес-логіці.
> * Коли вам потрібен **багатий API** для роботи з даними: сортування, фільтрація, групування, перетворення (через `Stream API`).
> * Коли вам потрібна **специфічна структура даних**, наприклад, множина унікальних елементів (`Set`) або черга (`Queue`).
> * Коли ви пишете API, який має бути гнучким для клієнтського коду (повертати `List` або `Set` краще, ніж `String[]`).
>
> **Загальне правило:** у більшості сучасних застосунків перевага надається **колекціям** через їхню гнучкість, тоді як **масиви** використовуються для оптимізації або для роботи з низькорівневим API.

#### **📌 Що перевіряють:**

*Здатність приймати обґрунтоване рішення щодо вибору структури даних на основі вимог до продуктивності, пам'яті та гнучкості.*

* * *

### **3 ❓ Питання:**

Які переваги мають колекції над масивами?

> #### **💬 Відповідь:**
>
> 1. **Динамічний розмір:** Колекції можуть автоматично збільшуватися та зменшуватися, що позбавляє необхідності вручну керувати розміром, як у масивах.
> 2. **Багатий API:** Collections Framework надає велику кількість готових методів для поширених операцій: сортування, пошук, перемішування, додавання, видалення та інше. З масивами більшість цих операцій довелося б реалізовувати вручну.
> 3. **Набір готових структур даних:** Ви можете легко вибрати потрібну структуру ( `List`, `Set`, `Queue`, `Map`) залежно від задачі, не реалізовуючи її з нуля.
> 4. **Повна інтеграція з дженеріками:** Колекції є типобезпечними на етапі компіляції, що запобігає `ClassCastException`.
> 5. **Повна інтеграція з `Stream API` (з Java 8):** Колекції є основним джерелом для потоків, що дозволяє писати потужний та виразний функціональний код.

#### **📌 Що перевіряють:**

*Знання основних переваг Collections Framework.*

* * *

### **4 ❓ Питання:**

Які переваги мають масиви над колекціями?

> #### **💬 Відповідь:**
>
> 1. **Продуктивність:** Масиви зазвичай є **швидшими**, ніж їхні аналоги з колекцій. Доступ до елемента масиву — це одна проста операція доступу до пам'яті. У колекцій є накладні витрати на виклики методів, перевірки тощо.
> 2. **Ефективність по пам'яті:** Масиви споживають **менше пам'яті**. `ArrayList`, наприклад, має накладні витрати на зберігання `size`, `modCount` та потенційно порожніх комірок у внутрішньому масиві. `LinkedList` має ще більші витрати на об'єкти `Node`.
> 3. **Підтримка примітивних типів:** Масиви можуть зберігати примітиви (`int[]`, `boolean[]`) напряму, без накладних витрат на класи-обгортки (`Integer`, `Boolean`) та автобоксинг/анбоксинг. Це дуже важливо для наукових та математичних обчислень.
> 4. **Коваріантність:** Масиви є коваріантними ( `String[]` є `Object[]`), що іноді може бути зручно (хоча й небезпечно).

#### **📌 Що перевіряють:**

*Розуміння того, що масиви, незважаючи на свою простоту, все ще мають свої ніші, де вони є кращим вибором через продуктивність та ефективність.*

* * *

### **5 ❓ Питання:**

Як перетворити масив на `List` і `List` на масив? Наведіть приклади. ⭐️

> #### **💬 Відповідь:**
>
> **1. Перетворення масиву на `List`:**
>
> * **Спосіб 1: `Arrays.asList()`**
>   * **Особливість:** Повертає **незмінного розміру** `List`, який є обгорткою над вихідним масивом. Ви не можете додавати або видаляти елементи.
>
>     ```java
>     String[] array = {"a", "b", "c"};
>     List<String> list = Arrays.asList(array);
>     ```
>
> * **Спосіб 2: `new ArrayList<>(Arrays.asList(...))`**
>   * Створює **повноцінний, змінюваний** `ArrayList`. Це найкращий спосіб, якщо вам потрібен звичайний список.
>
>     ```java
>     List<String> mutableList = new ArrayList<>(Arrays.asList(array));
>     ```
>
> * **Спосіб 3: `Stream API` (з Java 8+)**
>   * Дуже гнучкий спосіб.
>
>     ```java
>     List<String> streamList = Arrays.stream(array).collect(Collectors.toList());
>     ```
>
> **2. Перетворення `List` на масив:**
>
> * **Спосіб 1: `list.toArray(new T[0])`**
>   * Це **рекомендований, типобезпечний** спосіб. Ви передаєте масив-шаблон нульової довжини, щоб метод знав, масив якого типу створювати.
>
>     ```java
>     List<String> list = List.of("x", "y", "z");
>     String[] array = list.toArray(new String[0]);
>     ```
>
> * **Спосіб 2: `list.toArray()`**
>
>   * Повертає `Object[]`, що вимагає ручного і небезпечного приведення типів. **Не рекомендується**.
>
> * **Спосіб 3: `Stream API` (з Java 8+)**
>
>     ```java
>     String[] streamArray = list.stream().toArray(String[]::new);
>     ```

#### **📌 Що перевіряють:**

*Практичні навички роботи з "мостами" між масивами та колекціями. Знання особливостей `Arrays.asList()`.*

* * *

### **6 ❓ Питання:**

Чи можна використовувати `for-each` цикл для обходу масиву? А для колекції?

> #### **💬 Відповідь:**
>
> Так, `for-each` цикл (Enhanced for-loop) можна використовувати **як для масивів, так і для будь-якої колекції**, що реалізує інтерфейс `java.lang.Iterable`.
>
> **Приклад з масивом:**
>
> ```java
> int[] numbers = {1, 2, 3};
> for (int num : numbers) {
>     System.out.println(num);
> }
> ```
>
> **Приклад з колекцією:**
>
> ```java
> List<String> names = List.of("Alice", "Bob");
> for (String name : names) {
>     System.out.println(name);
> }
> ```
>
> Синтаксис `for-each` є синтаксичним цукром. Для масивів він компілюється у звичайний `for` з індексом, а для колекцій — у код, що використовує `Iterator`.

#### **📌 Що перевіряють:**

*Знання універсальності циклу `for-each`.*

* * *

### **7 ❓ Питання:**

У чому відмінність зберігання елементів у пам'яті для `ArrayList` та масиву? ⭐️

> #### **💬 Відповідь:**
>
> * **Масив:** Зберігає елементи у **неперервній ділянці пам'яті**.
>   * Для **примітивів** (`int[]`) значення зберігаються безпосередньо в цій ділянці.
>   * Для **об'єктів** (`String[]`) у ділянці зберігаються **посилання** на об'єкти, які самі знаходяться в купі.
>   * Неперервність забезпечує **високу локальність кешу**, що прискорює послідовний доступ.
>
> * **`ArrayList`:**
>   * "Під капотом" використовує **масив `Object[]`**. Тому для нього справедливі ті ж принципи зберігання, що й для масиву об'єктів.
>   * Однак `ArrayList` має **додаткові накладні витрати**:
>         1. Він є **об'єктом**, що вимагає пам'яті.
>         2. Має поля, як-от `size`, `modCount`.
>         3. Його внутрішній масив може мати **невикористані ("порожні") комірки**, якщо `capacity > size`, що також є витратою пам'яті.
>
> Якщо порівнювати `int[]` та `ArrayList<Integer>`, то масив примітивів є набагато ефективнішим, оскільки не має витрат на об'єкти-обгортки `Integer`.

#### **📌 Що перевіряють:**

*Розуміння того, як дані розташовуються в пам'яті, та пов'язаних з цим аспектів продуктивності (локальність кешу).*

* * *

### **8 ❓ Питання:**

Чи можуть масиви використовувати дженеріки? Наприклад, `Array<T>`? ⭐️

> #### **💬 Відповідь:**
>
> Ні, масиви в Java **не є дженериками**. Вони мають спеціальний синтаксис `T[]`, але вони не є класом, параметризованим типом `T`.
>
> Це означає, що вони не можуть використовувати всі переваги дженеріків. Хоча ви можете створити `String[]`, це не є еквівалентом `Array<String>`.
>
> Основна проблема — **неможливість створення масиву дженерик-типу**, наприклад, `new T[]`. Це пов'язано з конфліктом між стиранням типів у дженеріках та перевіркою типів під час виконання у масивах.
>
> Колекції були введені, в тому числі, щоб надати повноцінну, типобезпечну альтернативу масивам, яка повністю інтегрована з системою дженеріків.

#### **📌 Що перевіряють:**

*Чітке розмежування між масивами та дженерик-класами, незважаючи на схожий синтаксис `T[]`.*

* * *

### **9 ❓ Питання:**

Поясніть, що таке коваріантність масивів і чому це може бути небезпечно. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Коваріантність масивів** означає, що якщо клас `A` є підтипом класу `B`, то тип масиву `A[]` також вважається підтипом `B[]`.
>
> **Приклад:** `String` є підтипом `Object`, тому `String[]` є підтипом `Object[]`.
>
> `String[] strings = {"a"};`
> `Object[] objects = strings; // Валідне присвоєння!`
>
> **Чому це небезпечно?**
> Це створює "діру" в системі типів, яку потрібно контролювати під час виконання.
>
> ```java
> Object[] objects = new String[1]; // Створюємо масив String[], але посилаємося через Object[]
> // Компілятор дозволяє це, оскільки тип змінної `objects` - Object[]
> objects[0] = 123; // Спроба покласти Integer у масив рядків
> ```
>
> Цей код **скомпілюється**, але під час виконання JVM знає, що реальний тип масиву — це `String[]`, і не дозволить покласти туди `Integer`. В результаті буде кинуто **`java.lang.ArrayStoreException`**.
>
> Ця проблема є однією з причин, чому дженеріки були зроблені **інваріантними** (`List<String>` не є `List<Object>`), щоб виявляти такі помилки ще на етапі компіляції.

#### **📌 Що перевіряють:**

*Розуміння концепції коваріантності та пов'язаної з нею помилки `ArrayStoreException`.*

* * *

### **10 ❓ Питання:**

Як дженеріки вирішують проблему `ArrayStoreException`, яка існує у масивів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Дженеріки вирішують цю проблему шляхом **заборони коваріантності**, роблячи параметризовані типи **інваріантними**.
>
> * У масивах: `String[]` є підтипом `Object[]`, що дозволяє `Object[] arr = new String[1];`. Це створює ризик `ArrayStoreException`.
>
> * У дженеріках: `List<String>` **не є** підтипом `List<Object>`.
>
> ```java
> List<String> stringList = new ArrayList<>();
> // List<Object> objectList = stringList; // ПОМИЛКА КОМПІЛЯЦІЇ
> ```
>
> Компілятор **не дозволяє** це присвоєння. Таким чином, він унеможливлює ситуацію, коли ви через посилання `List<Object>` могли б додати `Integer` у список `List<String>`.
>
> Помилка виявляється **на етапі компіляції**, а не під час виконання, що є значно безпечнішим. Дженеріки переносять перевірку типів з рантайму в компіляцію.

#### **📌 Що перевіряють:**

*Здатність порівняти механізми типобезпечності масивів та колекцій і пояснити, чому дженеріки є більш надійним підходом.*

* * *

### **11 ❓ Питання:**

Якщо вам потрібна незмінна (immutable) структура, що краще використати: масив чи колекцію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Колекцію.** Створити по-справжньому незмінну структуру за допомогою колекцій значно простіше і надійніше.
>
> * **З колекціями:**
>   * Java надає готові інструменти для створення імутабельних колекцій:
>         1. **`Collections.unmodifiableList(list)`** (і аналоги для `Set`, `Map`): Створює незмінну "обгортку" навколо існуючої колекції.
>         2. **`List.of(...)`, `Set.of(...)` (з Java 9):** Створює повністю імутабельні колекції одразу.
>
> * **З масивами:**
>   * Масиви в Java **завжди є мутабельними**. Ви не можете заборонити зміну елемента масиву: `myArray[0] = newValue;`.
>   * Якщо ви повертаєте масив з методу, клієнтський код завжди може змінити його вміст.
>   * Щоб забезпечити імутабельність, вам доведеться при кожному виклику геттера повертати **копію масиву** (`array.clone()`), що є менш ефективним і не завжди очевидним.
>
> **Висновок:** Якщо імутабельність є вимогою, використання `List.of()` або `Collections.unmodifiableList()` є значно кращою і більш ідіоматичною практикою, ніж робота з масивами.

#### **📌 Що перевіряють:**

*Розуміння концепції імутабельності та інструментів для її досягнення в Collections Framework. Знання того, що масиви за своєю природою є змінними.*

* * *

### **12 ❓ Питання:**

Як би ви перетворили `List<Integer>` на `int[]` (масив примітивів)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Напряму це зробити одним методом не можна. `list.toArray()` поверне `Object[]`, а `list.toArray(new Integer[0])` поверне `Integer[]` (масив обгорток).
>
> Найкращий спосіб зробити це в сучасній Java — через `Stream API`.
>
> **Рішення з Java 8+:**
>
> ```java
> List<Integer> wrapperList = List.of(1, 2, 3);
>
> // 1. Створюємо потік з колекції
> // 2. Перетворюємо Stream<Integer> на IntStream (потік примітивів), виконуючи анбоксинг
> // 3. Збираємо IntStream у масив int[]
> int[] primitiveArray = wrapperList.stream()
>                                     .mapToInt(Integer::intValue) // або i -> i
>                                     .toArray();
> ```
>
> **Ручний спосіб (до Java 8):**
>
> Потрібно було ітерувати по списку і вручну копіювати елементи в новий масив примітивів.
>
> ```java
> int[] primitiveArray = new int[wrapperList.size()];
> for (int i = 0; i < wrapperList.size(); i++) {
>     primitiveArray[i] = wrapperList.get(i); // Анбоксинг відбувається тут
> }
> ```
>
> Підхід зі стрімами є більш функціональним, декларативним і лаконічним.

#### **📌 Що перевіряють:**

*Знання `Stream API` та його спеціалізованих потоків для примітивів. Вміння виконувати перетворення між об'єктами-обгортками та примітивами.*

* * *

### **13 ❓ Питання:**

Що більш продуктивно для доступу до даних, які гарантовано є в неперервній пам'яті: `int[]` чи `ByteBuffer`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це питання про низькорівневу продуктивність.
>
> У загальному випадку, **прямий доступ до `int[]` буде швидшим**.
>
> * **`int[]`:**
>   * Це конструкція мови високого рівня. Доступ `array[i]` напряму транслюється JVM у дуже ефективні машинні інструкції. JVM та JIT-компілятор можуть виконувати безліч оптимізацій (наприклад, `bounds-check elimination` — усунення перевірки виходу за межі масиву в циклах).
>
> * **`ByteBuffer`:**
>   * Це клас-обгортка над буфером байтів. Доступ до `int` вимагає виклику методу, наприклад `buffer.getInt(index)`.
>   * Хоча `DirectByteBuffer` може працювати з пам'яттю поза купою, що корисно для I/O, сам виклик методу має невеликі накладні витрати.
>   * Крім того, при роботі з `ByteBuffer` потрібно враховувати порядок байтів (byte order), що додає складності.
>
> **Коли `ByteBuffer` кращий:**
>
> `ByteBuffer` є незамінним при **роботі з низькорівневим вводом-виводом (NIO)**, мережевими протоколами або при взаємодії з нативним кодом, оскільки він надає повний контроль над бінарним представленням даних.
>
> **Висновок:** Для **обчислень у пам'яті** `int[]` є кращим вибором. Для **I/O операцій** — `ByteBuffer`.

#### **📌 Що перевіряють:**

*Розуміння різниці між високорівневими конструкціями мови та низькорівневими буферами. Знання про NIO.*

* * *

### **14 ❓ Питання:**

Уявіть, що у вас є API, яке приймає великий набір даних. Коли варто обрати `T[]`, а коли `Collection<T>` як тип параметра? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Вибір типу параметра — це важливе дизайнерське рішення для API.
>
> **Використовуйте `Collection<T>` (або `List<T>`, `Set<T>`) коли:**
>
> * **Потрібна гнучкість для клієнта.** Клієнт може передати `ArrayList`, `LinkedList`, `HashSet` або будь-яку іншу реалізацію. Це є найбільш гнучким підходом.
> * **Немає жорстких вимог до продуктивності.**
> * **Кількість даних може бути невідомою.**
>
> **Використовуйте `T[]` (масив) коли:**
>
> * **API є низькорівневим і критичним до продуктивності.** Масиви мають менше накладних витрат.
> * **Ви працюєте з varargs.** Метод, що приймає `T...`, насправді працює з масивом `T[]`.
> * **API має інтегруватися з C/C++ через JNI.** Передавати масиви в нативний код значно простіше, ніж колекції.
> * **API повертає або приймає дані від іншого низькорівневого API, який вже працює з масивами.**
>
> **Загальна рекомендація для публічного API:**
> Зазвичай **краще використовувати найбільш загальний інтерфейс**, який задовольняє ваші потреби. Наприклад, `Collection<T>` або `Iterable<T>`, якщо вам потрібен лише обхід. Це надає клієнту максимальну гнучкість. Використання масиву в публічному API є більш обмежуючим.

#### **📌 Що перевіряють:**

*Здатність проектувати API. Розуміння того, як вибір типів параметрів впливає на гнучкість та зручність використання API.*

* * *

### **15 ❓ Питання:**

Який зв'язок між сирими типами (raw types) та масивами? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Зв'язок у тому, що сирі типи поводяться в деяких аспектах подібно до `Object[]`.
>
> 1. **Втрата інформації про тип:**
>     * Сирий `List` поводиться як список `Object`-ів.
>     * Будь-який масив об'єктів (наприклад, `String[]`) може бути присвоєний змінній `Object[]`.
>     * В обох випадках ви можете (з різними наслідками) додати в структуру об'єкт невідповідного типу, і помилка виникне під час виконання.
>
> 2. **Джерело помилок у рантаймі:**
>     * Використання сирих типів веде до ризику `ClassCastException`.
>     * Використання коваріантності масивів веде до ризику `ArrayStoreException`.
>
> **Основна відмінність:**
>
> **Дженеріки були введені, щоб виправити "зламану" систему типів масивів.** Вони замінили коваріантність масивів (небезпечну в рантаймі) на інваріантність (безпечну в компіляції), перенісши перевірку типів з етапу виконання на етап компіляції.
>
> Сирі типи — це "спадщина", яка змушує дженеріки іноді поводитися так само небезпечно, як і масиви, що було ціною за зворотну сумісність.

#### **📌 Що перевіряють:**

*Глибоке, філософське розуміння історії та еволюції системи типів в Java. Здатність бачити паралелі та відмінності між старими та новими підходами.*
