---
layout: default
title: "Складність алгоритмів (Time Complexity)"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 8
---

# Складність алгоритмів (Time Complexity)

* короткий опис: Асимптотична складність (Big-O нотація) описує, як час виконання або використання пам'яті алгоритмом залежить від розміру вхідних даних, показуючи поведінку алгоритму при дуже великих обсягах даних.

* поняття: Big-O (`O`), Big-Omega (`Ω`), Big-Theta (`Θ`), `O(1)` (константна), `O(N)` (лінійна), `O(N²)` (квадратична), `O(log N)` (логарифмічна).

* приклади: Доступ до елемента масиву (O(1)), лінійний пошук (O(N)), сортування бульбашкою (O(N²)), бінарний пошук (O(log N)).

* ключові моменти: Допомагає обирати ефективні алгоритми та структури даних.

* що важливо знати на співбесіді: Що таке Big-O, типові складності, їх значення.

### **✅ Офіційне визначення:**

Асимптотична складність описує, як змінюється час виконання або використання пам’яті алгоритмом у залежності від розміру вхідних даних. Вона показує поведінку алгоритму при дуже великих обсягах даних.

### **🧠 Простими словами:**

Це як виміряти, наскільки повільно або швидко працює твій код, якщо даних стає дуже багато.

---

## **Основне**

* **`Big-O (O)`** — верхня оцінка, гірший випадок (максимальний час).
* **`Big-Omega (Ω)`** — нижня оцінка, кращий випадок (мінімальний час).
* **`Big-Theta (Θ)`** — точна оцінка, коли верхня і нижня межі збігаються.

---

## **Пояснення на пальцях (💡)**

| Позначення | Опис | Приклад |
| ----- | ----- | ----- |
| **`O(1)`** | Константна | Доступ до елемента масиву за індексом |
| **`O(n)`** | Лінійна | Перебір усіх елементів списку |
| **`O(n²)`** | Квадратична | Подвійний вкладений цикл (сортування бульбашкою) |
| **`O(log n)`** | Логарифмічна | Пошук у відсортованому масиві (бінарний пошук) |

* **`O(1)`** — це як натиснути кнопку, не залежить від кількості даних.
* **`O(n)`** — це як переглянути всі листівки в альбомі одну за одною.
* **`O(n²)`** — це як порівнювати кожну листівку з кожною іншою — довго і нудно.
* **`O(log n)`** — це як ділити альбом навпіл щоразу, щоб знайти потрібну листівку швидко.

---

## **Корисні приклади (🧪)**

```java
// O(1) - доступ за індексом
int[] arr = {1,2,3,4};
int x = arr[2]; // швидко і незалежно від розміру arr

// O(n) - лінійний пошук
boolean found = false;
for(int i = 0; i < arr.length; i++) {
    if(arr[i] == 3) {
        found = true;
        break;
    }
}

// O(n²) - сортування бульбашкою (приклад)
for(int i=0; i < arr.length; i++) {
    for(int j=0; j < arr.length - i - 1; j++) {
        if(arr[j] > arr[j+1]) {
            int temp = arr[j];
            arr[j] = arr[j+1];
            arr[j+1] = temp;
        }
    }
}

// O(log n) - бінарний пошук (потрібен відсортований масив)
int binarySearch(int[] arr, int key) {
    int low = 0, high = arr.length - 1;
    while(low <= high) {
        int mid = (low + high) / 2;
        if(arr[mid] == key) return mid;
        else if(arr[mid] < key) low = mid + 1;
        else high = mid - 1;
    }
    return -1;
}
```

---

## **Порівняння (⚖️)**

* Щоб обрати найбільш ефективний алгоритм або структуру даних під конкретне завдання, враховуючи обмеження по часу та ресурсах.
* Допомагає уникати повільних операцій на великих обсягах даних.
* Полегшує розуміння компромісів між швидкістю, пам’яттю та складністю коду.

---

## **🧠 Мнемоніка для запам’ятовування:**

1 кнопка — **`O(1),`**

усі листівки — **`O(n),`**

всі на всі — **`O(n²),`**

діли і володарюй — **`O(log n).`**

---

## **🟩 Підсумок:**

Розуміння асимптотичної складності допомагає обирати правильні алгоритми і структури даних, роблячи програми швидшими і більш ефективними, особливо при великих обсягах інформації.

### **🔗 Додаткові джерела:**

* [**Big-O notation — Wikipedia**](https://en.wikipedia.org/wiki/Big_O_notation)
* [**Big-O Cheat Sheet**](https://www.bigocheatsheet.com/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке складність алгоритму і що описує нотація Big-O?

> #### **💬 Відповідь:**
>
> **Складність алгоритму** — це оцінка ресурсів (зазвичай часу або пам'яті), які потрібні алгоритму для виконання, в залежності від розміру вхідних даних.
>
> **Нотація Big-O (O-велике)** — це математичний спосіб описати **асимптотичну поведінку** складності алгоритму. Вона показує, як зростає час виконання (або використання пам'яті) при **зростанні розміру вхідних даних (n) до нескінченності**.
>
> Big-O описує **верхню межу (upper bound)** або **найгірший сценарій (worst-case)**. Вона відповідає на питання: "Наскільки повільно може працювати мій алгоритм у найгіршому випадку?".
>
> Наприклад, `O(n)` означає, що в найгіршому випадку час виконання зростає лінійно зі збільшенням кількості елементів `n`.

#### **📝 Аналогія:**

Уявіть, що ви шукаєте книгу в бібліотеці.

* **O(1):** Ви точно знаєте номер полиці та місце (доступ за індексом).
* **O(n):** Ви не знаєте, де книга, і послідовно переглядаєте всі `n` книг на полиці.
* **O(log n):** Полиці відсортовані за алфавітом, і ви використовуєте бінарний пошук (щоразу ділите ділянку пошуку навпіл).

#### **📌 Що перевіряють:**

*Розуміння того, що Big-O — це опис асимптотичної поведінки в найгіршому випадку, а не точний час виконання.*

* * *

### **2 ❓ Питання:**

Назвіть та поясніть найпоширеніші класи складності Big-O.

> #### **💬 Відповідь:**
>
> Ось основні класи складності, від найшвидшого до найповільнішого:
>
> 1. **`O(1)` — Константна складність:**
>     * Час виконання не залежить від розміру вхідних даних.
>     * **Приклад:** Доступ до елемента масиву за індексом (`array[i]`), додавання/видалення елемента в `HashMap` (в середньому).
>
> 2. **`O(log n)` — Логарифмічна складність:**
>     * Час виконання зростає дуже повільно. З подвоєнням `n`, час збільшується на константу. Зазвичай пов'язана з алгоритмами, що ділять проблему навпіл на кожному кроці.
>     * **Приклад:** Бінарний пошук у відсортованому масиві.
>
> 3. **`O(n)` — Лінійна складність:**
>     * Час виконання зростає прямо пропорційно розміру вхідних даних.
>     * **Приклад:** Лінійний пошук (перебір усіх елементів масиву), обчислення суми елементів.
>
> 4. **`O(n log n)` — Лінійно-логарифмічна (лінеаритмічна) складність:**
>     * "Золотий стандарт" для ефективних алгоритмів сортування.
>     * **Приклад:** Сортування злиттям (Merge Sort), швидке сортування (Quick Sort, в середньому), `Arrays.sort()`.
>
> 5. **`O(n²)` — Квадратична складність:**
>     * Час виконання зростає пропорційно квадрату розміру даних. Зазвичай це вкладені цикли по тій самій колекції.
>     * **Приклад:** Прості алгоритми сортування (бульбашкове, вибором, вставками), порівняння кожного елемента з кожним іншим.
>
> 6. **`O(2^n)` — Експоненційна складність:**
>     * Дуже повільна, стає непрактичною навіть для невеликих `n`. Часто пов'язана з повним перебором всіх можливих підмножин.
>     * **Приклад:** Наївне рекурсивне обчислення чисел Фібоначчі.

#### **📌 Що перевіряють:**

*Знання основної ієрархії складностей та здатність навести класичні приклади для кожної з них.*

* * *

### **3 ❓ Питання:**

Чим відрізняються Big-O, Big-Omega (Ω) та Big-Theta (Θ)?

> #### **💬 Відповідь:**
>
> Ці три нотації використовуються для опису різних аспектів асимптотичної поведінки алгоритму:
>
> * **Big-O (O): Верхня межа (гірший випадок — worst-case).**
>   * Відповідає на питання: "Чи може алгоритм працювати **не повільніше**, ніж ця функція?".
>   * Наприклад, лінійний пошук має `O(n)`, бо в найгіршому випадку доведеться перевірити всі `n` елементів. Але в найкращому — `Ω(1)`.
>
> * **Big-Omega (Ω): Нижня межа (кращий випадок — best-case).**
>   * Відповідає на питання: "Чи може алгоритм працювати **не швидше**, ніж ця функція?".
>   * Наприклад, лінійний пошук має `Ω(1)`, бо в найкращому випадку ми знайдемо елемент з першої спроби.
>
> * **Big-Theta (Θ): Точна межа (середній випадок або коли кращий і гірший випадки збігаються).**
>   * Відповідає на питання: "Чи зростає час виконання **так само швидко**, як ця функція?".
>   * Алгоритм має складність `Θ(f(n))`, якщо його складність одночасно є `O(f(n))` і `Ω(f(n))`.
>   * Наприклад, доступ до елемента масиву за індексом завжди займає константний час, тому його складність — `Θ(1)`.
>
> У практиці розробки найчастіше використовують **Big-O**, оскільки важливо знати, як алгоритм буде поводитися в найгіршому випадку, щоб гарантувати продуктивність.

#### **📌 Що перевіряють:**

*Розуміння того, що Big-O — це лише один з аспектів аналізу. Знання про `Ω` та `Θ` свідчить про більш глибокі теоретичні знання.*

* * *

### **4 ❓ Питання:**

Яку складність має операція доступу до елемента `ArrayList` за індексом? А для `LinkedList`? Поясніть чому. ⭐️

> #### **💬 Відповідь:**
>
> * **`ArrayList.get(index)` має складність O(1) (константна).**
>   * **Чому:** `ArrayList` базується на масиві. Масиви зберігають дані в неперервній ділянці пам'яті. Щоб отримати доступ до `i`-го елемента, потрібно лише обчислити його адресу в пам'яті (`адреса_початку + i * розмір_елемента`). Ця операція займає однаковий час незалежно від розміру масиву.
>
> * **`LinkedList.get(index)` має складність O(n) (лінійна).**
>   * **Чому:** `LinkedList` базується на двозв'язному списку, де елементи не зберігаються послідовно. Щоб дістатися до `i`-го елемента, потрібно пройти по ланцюжку посилань від початку (або кінця) списку, відвідавши в середньому `n/2` вузлів. Час доступу прямо залежить від позиції елемента.

#### **📌 Що перевіряють:**

*Вміння аналізувати складність операцій для основних структур даних Java. Це дуже поширене питання.*

* * *

### **5 ❓ Питання:**

Яку складність мають операції `put`, `get`, `remove` для `HashMap`? Від чого вона залежить? ⭐️

> #### **💬 Відповідь:**
>
> Для `HashMap` складність цих операцій залежить від якості хеш-функції.
>
> * **Середній (і найкращий) випадок: O(1) (константна).**
>   * Це досягається, коли хеш-функція забезпечує хороший розподіл, і колізії трапляються рідко. Операція зводиться до обчислення хешу, індексу кошика та доступу до нього.
>
> * **Найгірший випадок:**
>   * **До Java 8: O(n) (лінійна).**
>   * **З Java 8: O(log n) (логарифмічна).**
>
> **Чому виникає найгірший випадок?**
> Він виникає, коли всі (або більшість) ключів мають **однаковий хеш-код** і потрапляють в один і той самий кошик.
>
> * До Java 8 такий кошик був **зв'язаним списком**. Пошук у ньому вимагав O(n).
> * У Java 8 при досягненні певного порогу цей список **перетворюється на червоно-чорне дерево**, що покращує пошук до O(log n).
>
> Таким чином, якість методу `hashCode()` є критично важливою для продуктивності `HashMap`.

#### **📌 Що перевіряють:**

*Знання середньої та найгіршої складності для `HashMap`, а також ролі хеш-колізій та оптимізації з Java 8.*

* * *

### **6 ❓ Питання:**

Поясніть на прикладі коду, що таке квадратична складність `O(n²)`.

> #### **💬 Відповідь:**
>
> Квадратична складність `O(n²)` зазвичай виникає, коли алгоритм для кожного елемента вхідних даних повинен пройтися по всім іншим елементам. Класичним прикладом є **вкладені цикли** по одній і тій самій колекції.
>
> **Приклад: Пошук дублікатів у масиві (наївний підхід)**
>
> ```java
> public boolean containsDuplicates(int[] array) {
>     int n = array.length;
>     // Зовнішній цикл виконується n разів
>     for (int i = 0; i < n; i++) {
>         // Внутрішній цикл виконується приблизно n разів для кожного `i`
>         for (int j = i + 1; j < n; j++) {
>             // Порівнюємо кожен елемент з кожним іншим
>             if (array[i] == array[j]) {
>                 return true; // Знайдено дублікат
>             }
>         }
>     }
>     return false;
> }
> ```
>
> Загальна кількість операцій порівняння тут буде приблизно `n * (n-1) / 2`, що асимптотично дорівнює `n²`. Якщо масив збільшиться в 10 разів, час виконання зросте приблизно в 100 разів. Це робить такі алгоритми неефективними для великих `n`.
>
> **Примітка:** цю задачу можна вирішити значно ефективніше (за O(n)) за допомогою `HashSet`.

#### **📌 Що перевіряють:**

*Здатність "бачити" складність `O(n²)` у коді та розуміти, що її спричиняють вкладені ітерації.*

* * *

### **7 ❓ Питання:**

Яку складність має бінарний пошук і яка головна вимога до даних для його застосування? ⭐️

> #### **💬 Відповідь:**
>
> * **Складність:** Бінарний пошук має **логарифмічну складність `O(log n)`**.
>
> * **Головна вимога:** Дані (масив або список) **повинні бути відсортовані**.
>
> **Як він працює:**
>
> 1. Береться елемент у середині відсортованої колекції.
> 2. Якщо він дорівнює шуканому, пошук завершено.
> 3. Якщо шуканий елемент більший, то **відкидається вся перша половина** колекції.
> 4. Якщо менший — **відкидається вся друга половина**.
> 5. Процес повторюється для половини, що залишилася.
>
> Оскільки на кожному кроці ми **зменшуємо область пошуку вдвічі**, кількість операцій зростає дуже повільно. Наприклад, для 1 мільйона елементів потрібно лише близько 20 порівнянь.

#### **📌 Що перевіряють:**

*Знання одного з найважливіших алгоритмів пошуку, його складності та ключової передумови (відсортовані дані).*

* * *

### **8 ❓ Питання:**

Що означає "амортизована складність"? Наведіть приклад з `ArrayList`. ⭐️

> #### **💬 Відповідь:**
>
> **Амортизована складність** — це середній час виконання операції, якщо розглядати її в контексті **послідовності операцій**. Вона використовується для аналізу алгоритмів, де окремі операції можуть бути дуже "дорогими", але такі дорогі операції трапляються рідко.
>
> **Приклад з `ArrayList.add(E e)`:**
>
> * **Більшість викликів** `add()` виконуються за **O(1)**. Ми просто додаємо елемент у вільну комірку внутрішнього масиву.
> * **Іноді** (коли масив заповнений) відбувається **розширення (`resize`)**. Ця операція є дуже "дорогою" і має складність **O(n)**, оскільки вимагає створення нового масиву і копіювання всіх `n` елементів.
>
> Якби ми розглядали тільки найгірший випадок, то складність `add()` була б O(n). Але **амортизований аналіз** показує, що оскільки дорога операція O(n) відбувається дуже рідко, її "вартість" можна "розмазати" по всім попереднім дешевим операціям O(1).
>
> В результаті **середня, або амортизована, вартість** операції `add()` для `ArrayList` становить **O(1)**.

#### **📌 Що перевіряють:**

*Розуміння більш просунутої концепції амортизованого аналізу та вміння застосувати її до `ArrayList`.*

* * *

### **9 ❓ Питання:**

Як впливають на складність константи та члени нижчого порядку в Big-O нотації? Наприклад, `O(2n + 1)` або `O(n² + n)`. ⭐️⭐️

> #### **💬 Відповідь:**
>
> В Big-O нотації константи та члени нижчого порядку **ігноруються**.
>
> **Чому?**
>
> Big-O описує **асимптотичну** поведінку, тобто те, як час виконання зростає при **дуже великих `n`**. При великих `n` вплив членів нижчого порядку та констант стає несуттєвим порівняно з членом найвищого порядку.
>
> * **Константи ігноруються:**
>   * `O(2n)` або `O(100n)` спрощується до **`O(n)`**. Хоча `100n` у 100 разів довше, ніж `n`, швидкість зростання в обох випадках є **лінійною**.
>
> * **Члени нижчого порядку ігноруються:**
>   * `O(n² + n + 100)` спрощується до **`O(n²)`**. Коли `n` стає мільйоном, `n²` (трильйон) настільки більше за `n` (мільйон), що внеском `n` можна знехтувати.
>
> **Висновок:**
> `O(2n + 1)` -> `O(n)`
> `O(n² + n)` -> `O(n²)`
>
> Big-O нотація фокусується на **порядку зростання**, а не на точному часі виконання.

#### **📌 Що перевіряють:**

*Знання правил спрощення виразів у Big-O нотації. Розуміння того, що Big-O — це про швидкість зростання, а не про абсолютні значення.*

* * *

### **10 ❓ Питання:**

Яку часову складність мають операції з `HashSet`? А з `TreeSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`HashSet` (на основі хеш-таблиці):**
>   * `add(e)`: **O(1)** в середньому, O(n) або O(log n) у найгіршому випадку.
>   * `remove(o)`: **O(1)** в середньому, O(n) або O(log n) у найгіршому.
>   * `contains(o)`: **O(1)** в середньому, O(n) або O(log n) у найгіршому.
>
> * **`TreeSet` (на основі червоно-чорного дерева):**
>   * `add(e)`: **O(log n)**. Потрібно знайти правильне місце в дереві.
>   * `remove(o)`: **O(log n)**.
>   * `contains(o)`: **O(log n)**.
>
> **Порівняння:**
> `HashSet` є значно швидшим для базових операцій, але не гарантує жодного порядку елементів. `TreeSet` є повільнішим, але гарантує, що елементи завжди зберігаються у відсортованому порядку. Вибір залежить від того, чи потрібне вам сортування.

#### **📌 Що перевіряють:**

*Знання складності операцій для `Set`, що демонструє розуміння базових структур даних (хеш-таблиця vs дерево).*

* * *

### **11 ❓ Питання:**

Як складність алгоритму впливає на вибір між `ArrayList` та `LinkedList` для задачі, де потрібно часто видаляти елементи з середини списку? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це класичний компроміс.
>
> **Задача:** знайти елемент у списку і видалити його.
>
> * **`ArrayList`:**
>   * Пошук елемента (`indexOf()`): **O(n)**.
>   * Видалення за індексом (`remove(index)`): **O(n)**.
>   * **Загальна складність: O(n) + O(n) = O(n)**.
>
> * **`LinkedList`:**
>   * Пошук елемента (`indexOf()`): **O(n)**.
>   * Видалення (після того, як вузол знайдено): **O(1)**.
>   * **Загальна складність: O(n) + O(1) = O(n)**.
>
> Асимптотично обидва мають складність O(n). **Однак** на практиці `ArrayList` може бути швидшим.
> **Чому?**
>
> * **Пошук в `ArrayList`** відбувається в неперервному блоці пам'яті, що добре використовує кеш процесора.
> * **Пошук в `LinkedList`** вимагає переходу по посиланнях, що може призводити до промахів кешу.
> * **Видалення в `ArrayList`** використовує дуже швидкий `System.arraycopy`.
>
> **`LinkedList` отримує перевагу** тільки тоді, коли вам **не потрібно шукати** елемент. Наприклад, якщо ви видаляєте його під час ітерації за допомогою `Iterator.remove()`. У цьому випадку пошук не потрібен, і операція для `LinkedList` дійсно буде O(1), тоді як для `ArrayList` залишиться O(n).

#### **📌 Що перевіряють:**

*Глибокий аналіз, який виходить за рамки простого знання Big-O. Розуміння того, як апаратні особливості (кеш) та конкретні сценарії (видалення через ітератор) впливають на реальну продуктивність.*

* * *

### **12 ❓ Питання:**

Що таке просторова складність (Space Complexity) і чим вона відрізняється від часової? Наведіть приклад. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Часова складність (Time Complexity):** Оцінює, як час виконання алгоритму залежить від розміру вхідних даних.
>
> * **Просторова складність (Space Complexity):** Оцінює, скільки **додаткової пам'яті** (крім пам'яті для вхідних даних) потрібно алгоритму для виконання, в залежності від розміру вхідних даних.
>
> **Приклад:**
>
> **Задача:** знайти суму елементів масиву.
>
> ```java
> int sum(int[] array) {
>     int total = 0; // Додаткова пам'ять
>     for (int i = 0; i < array.length; i++) {
>         total += array[i];
>     }
>     return total;
> }
> ```
>
> * **Часова складність:** **O(n)**, оскільки ми проходимо по всьому масиву один раз.
> * **Просторова складність:** **O(1)**, оскільки нам потрібна лише фіксована кількість додаткової пам'яті (змінні `total` та `i`) незалежно від розміру масиву `n`.
>
> **Інший приклад:** Створення копії масиву.
>
> ```java
> int[] copy(int[] array) {
>     int[] newArray = new int[array.length]; // Додаткова пам'ять
>     // ...
>     return newArray;
> }
> ```
>
> * **Часова складність:** **O(n)**.
> * **Просторова складність:** **O(n)**, оскільки ми створюємо новий масив розміром `n`.
>
> Іноді доводиться йти на компроміс: використовувати більше пам'яті для прискорення алгоритму (наприклад, мемоізація) або економити пам'ять ціною довшого виконання.

#### **📌 Що перевіряють:**

*Знання того, що складність буває не тільки часовою, а й просторовою. Вміння аналізувати використання пам'яті алгоритмами.*

* * *

### **13 ❓ Питання:**

Яка просторова складність у рекурсивного обчислення факторіалу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Просторова складність рекурсивного обчислення факторіалу становить **O(n)**.
>
> `long factorial(int n) { if (n == 0) return 1; return n * factorial(n - 1); }`
>
> **Чому O(n)?**
>
> Просторова складність у цьому випадку визначається **глибиною стеку викликів (Call Stack)**.
>
> * При виклику `factorial(n)`, він викликає `factorial(n-1)`, той — `factorial(n-2)`, і так далі, до `factorial(0)`.
> * Кожен виклик створює новий **фрейм** у стеку викликів для зберігання своїх параметрів та локальних змінних.
> * Для обчислення `factorial(n)` стек викликів досягне глибини `n+1` (від `n` до `0`).
> * Отже, пам'ять, що використовується стеком, зростає **лінійно** пропорційно `n`.
>
> На відміну від цього, **ітераційна версія** факторіалу має просторову складність **O(1)**, оскільки вона використовує фіксовану кількість змінних незалежно від `n`.

#### **📌 Що перевіряють:**

*Здатність аналізувати просторову складність рекурсивних алгоритмів, враховуючи стек викликів.*

* * *

### **14 ❓ Питання:**

Чи завжди `O(n)` швидше за `O(n²)`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Асимптотично, для великих `n` — так, завжди.** `O(n)` зростає набагато повільніше, ніж `O(n²)`.
>
> **Однак на практиці для малих `n` це може бути не так.**
>
> Big-O нотація **ігнорує константи та члени нижчого порядку**. Розглянемо два гіпотетичні алгоритми:
>
> * Алгоритм A (лінійний): час виконання `1000 * n + 5000`
> * Алгоритм B (квадратичний): час виконання `n²`
>
> **Порівняння:**
>
> * Для `n = 10`:
>   * A: `1000 * 10 + 5000 = 15000`
>   * B: `10 * 10 = 100` ( **B швидше!** )
> * Для `n = 100`:
>   * A: `1000 * 100 + 5000 = 105000`
>   * B: `100 * 100 = 10000` ( **B все ще швидше!** )
> * Для `n = 2000`:
>   * A: `1000 * 2000 + 5000 = 2 005 000`
>   * B: `2000 * 2000 = 4 000 000` ( **A стає швидшим** )
>
> Отже, якщо константний фактор у лінійного алгоритму дуже великий, то для **малих або середніх вхідних даних** квадратичний алгоритм з малим фактором може виявитися швидшим.
>
> Наприклад, деякі алгоритми сортування (як `Insertion Sort` - O(n²)) можуть бути швидшими за `Quick Sort` (O(n log n)) на дуже малих масивах, тому `Quick Sort` часто переключається на `Insertion Sort` для малих підмасивів.

#### **📌 Що перевіряють:**

*Розуміння того, що Big-O — це про **асимптотику**, і на реальну продуктивність також впливають константні фактори, що може бути важливо для малих наборів даних.*

* * *

### **15 ❓ Питання:**

Дано два алгоритми. Один має складність `O(n)`, інший `O(1)`. Чи означає це, що другий завжди буде працювати швидше? Поясніть. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не обов'язково**.
>
> **Асимптотично**, `O(1)` є кращим за `O(n)`. Це означає, що при зростанні `n` до нескінченності, алгоритм `O(n)` неминуче стане повільнішим.
>
> Але **в реальному світі** ми працюємо з **конкретними часами виконання**, які Big-O нотація спрощує, ігноруючи константи.
>
> **Гіпотетичний приклад:**
>
> * **Алгоритм A (O(n)):**
>   * Виконує одну просту інструкцію для кожного елемента. Час: `T_A(n) = n` наносекунд.
> * **Алгоритм B (O(1)):**
>   * Виконує дуже складну, але фіксовану послідовність дій (наприклад, встановлення мережевого з'єднання, запит до бази даних). Час: `T_B(n) = 1,000,000` наносекунд (1 мілісекунда).
>
> **Порівняння:**
>
> * Для `n = 100`, алгоритм A (`100 нс`) буде значно швидшим за B (`1,000,000 нс`).
> * Тільки коли `n` перевищить `1,000,000`, алгоритм A стане повільнішим.
>
> **Висновок:**
> Big-O є надзвичайно корисним інструментом для **порівняння масштабованості** алгоритмів. Але при виборі між двома алгоритмами для конкретної задачі з певним діапазоном `n` важливо також враховувати **реальну вартість** операцій, яку можна визначити тільки через **вимірювання (профілювання)**.

#### **📌 Що перевіряють:**

*Глибоке, прагматичне розуміння Big-O. Здатність бачити різницю між теоретичною асимптотикою та реальною продуктивністю в конкретних умовах.*
