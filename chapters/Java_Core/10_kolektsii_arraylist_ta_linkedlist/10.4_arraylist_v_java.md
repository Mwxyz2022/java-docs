---
layout: default
title: "ArrayList в Java"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 4
---

# ArrayList в Java

* короткий опис: `ArrayList` – це реалізація інтерфейсу `List` на основі динамічного масиву. Він зберігає елементи в порядку вставлення, підтримує дублікати та `null`-значення, автоматично збільшуючи розмір масиву при заповненні.

* поняття: `ArrayList`, динамічний масив, `Object[] elementData`, `resize` (збільшення на 1.5 раза), `System.arraycopy`.

* приклади: Додавання елементів, видалення за індексом (зсув елементів), автоматичне збільшення місткості.

* ключові моменти: Швидкий доступ за індексом (O(1)), але повільна вставка/видалення в середині (O(N)) через зсув елементів.

* що важливо знати на співбесіді: Внутрішня робота `ArrayList`, продуктивність операцій.

### **✅ Офіційне визначення:**

**`ArrayList`** — це клас у Java, який реалізує інтерфейс **`List`** та базується на динамічному масиві. Він дозволяє зберігати елементи в порядку вставлення, підтримує дублікати й **`null`**\-значення.

### **🧠 Простими словами:**

**`ArrayList`** — це список на основі масиву, який може "розтягуватись", якщо в нього додають більше елементів, ніж поточна місткість

---

## **Внутрішні механізми ArrayList:**

* Внутрішній масив **`Object[]`** `elementData` тримає всі елементи
* При заповненні масиву відбувається збільшення розміру (resize) на 1.5 раза
* При вставці або видаленні елементів відбувається зміщення (зсув) елементів для збереження порядку
* Копіювання елементів під час resize робиться через **`System.arraycopy`** для ефективності
* Підтримує зберігання **`null`** і дублікати

---

## **Пояснення на пальцях (💡)**

Уяви **`ArrayList`** як коробку з комірками (масив), де ти складаєш книги. Якщо коробка заповнена — береш більшу (в 1.5 рази) і перекладаєш книги туди. Якщо треба вставити книгу не в кінець, а в середину, пересуваєш всі книги праворуч, щоб звільнити місце

---

## **Корисні приклади (🧪)**

```java
ArrayList<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
list.add(null);              // Підтримка null
list.add("Java");           // Дублікати допускаються

System.out.println(list.get(1)); // Виведе "Python"

list.remove(0);             // Видалення "Java", зсув елементів

list.add(1, "C++");         // Вставка "C++" на позицію 1 (зсув "Python" праворуч)
```

---

## **Порівняння (⚖️)**

* **`ArrayList`** vs **`LinkedList`**:
  * **`ArrayList`** — швидкий доступ по індексу, але повільна вставка/видалення в середині через зсув
  * **`LinkedList`** — повільний доступ по індексу, але швидша вставка/видалення в середині

---

## **🧠 Мнемоніка для запам’ятовування:**

"**`ArrayList`** — це динамічна коробка (масив), що росте на півтора, і де книги можна вставляти тільки зі зсувом."

---

## **🟩 Підсумок:**

**`ArrayList`** — це найпопулярніша реалізація списку в Java, ідеальна для швидкого доступу за індексом і коли рідко вставляєш або видаляєш елементи в середині. Збільшує розмір автоматично, підтримує **`null`** і дублікати.

**🔗 Доповнююча інформація:**

* [**Oracle Docs ArrayList**](https://docs.oracle.com/en/java/javase/20/docs/api/java.base/java/util/ArrayList.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке `ArrayList` і на якій структурі даних він базується?

> #### **💬 Відповідь:**
>
> **`java.util.ArrayList`** — це клас в Java Collections Framework, який є однією з найпоширеніших реалізацій інтерфейсу `List`.
>
> Він базується на **динамічному масиві (dynamic array)**. Це означає, що "під капотом" `ArrayList` використовує звичайний масив для зберігання елементів (в JDK це поле називається `elementData` і має тип `Object[]`).
>
> **Ключові характеристики `ArrayList`:**
>
> * Дозволяє зберігати елементи в **порядку їх додавання**.
> * Дозволяє **дублювання** елементів.
> * Дозволяє зберігати **`null`** значення.
> * Автоматично **збільшує свою ємність**, коли внутрішній масив заповнюється.

#### **📝 Аналогія:**

Це як гумова стрічка з комірками. Спочатку вона коротенька, але коли ви додаєте нові предмети і місце закінчується, стрічка "розтягується", створюючи нові комірки.

#### **📌 Що перевіряють:**

*Розуміння того, що `ArrayList` — це обгортка над звичайним масивом, яка надає додаткову функціональність.*

* * *

### **2 ❓ Питання:**

Які сильні та слабкі сторони `ArrayList`? Коли його варто використовувати?

> #### **💬 Відповідь:**
>
> **Сильні сторони:**
>
> 1. **Швидкий доступ до елементів за індексом:** Операція `get(int index)` виконується за **константний час (O(1))**. Оскільки це масив, ми можемо миттєво звернутися до будь-якого елемента за його позицією.
> 2. **Ефективність по пам'яті:** `ArrayList` є досить ефективним, оскільки зберігає лише самі елементи без додаткових накладних витрат на посилання (на відміну від `LinkedList`).
>
> **Слабкі сторони:**
>
> 1. **Повільне додавання/видалення елементів не в кінець списку:** Операції `add(int index, E element)` та `remove(int index)` виконуються за **лінійний час (O(n))**. Це пов'язано з тим, що для збереження послідовності потрібно **зсунути** всі наступні елементи в масиві, що є "дорогою" операцією.
>
> **Коли варто використовувати:**
>
> `ArrayList` є **найкращим вибором за замовчуванням** для реалізації списку. Він ідеально підходить, коли:
>
> * Вам потрібен **частий і швидкий доступ до елементів за індексом**.
> * Основні операції — це читання та додавання елементів **в кінець списку**.
> * Кількість операцій вставки/видалення в середині списку є невеликою.

#### **📌 Що перевіряють:**

*Здатність аналізувати компроміси структури даних та робити обґрунтований вибір на основі типових сценаріїв використання.*

* * *

### **3 ❓ Питання:**

Що відбувається "під капотом", коли `ArrayList` заповнюється і ви намагаєтеся додати новий елемент?

> #### **💬 Відповідь:**
>
> Коли внутрішній масив `ArrayList` заповнюється, і ви викликаєте `add()`, відбувається процес **розширення (resizing)**.
>
> **Кроки цього процесу:**
>
> 1. `ArrayList` виявляє, що поточна ємність (`capacity`) масиву недостатня для додавання нового елемента.
> 2. Створюється **новий, більший масив**.
> 3. Нова ємність зазвичай розраховується як `стара_ємність + (стара_ємність >> 1)`, що еквівалентно **збільшенню приблизно в 1.5 рази**.
> 4. Всі елементи зі старого масиву **копіюються** в новий. Для цього використовується дуже швидкий `native` метод `System.arraycopy()`.
> 5. Внутрішнє поле `elementData` `ArrayList` тепер посилається на цей новий масив. Старий масив стає доступним для збирача сміття.
> 6. Після цього новий елемент додається в новий масив.
>
> Цей процес є "дорогою" операцією (O(n)), але завдяки тому, що ємність збільшується не на один елемент, а в кілька разів, **середня (амортизована) вартість** додавання залишається O(1).

#### **📌 Що перевіряють:**

*Знання внутрішнього механізму розширення `ArrayList`, включаючи коефіцієнт збільшення та використання `System.arraycopy`.*

* * *

### **4 ❓ Питання:**

Чим відрізняються поняття `size` та `capacity` в `ArrayList`? ⭐️

> #### **💬 Відповідь:**
>
> * **`size` (розмір):**
>   * Це **кількість елементів, які фактично знаходяться** в `ArrayList` на даний момент.
>   * Це значення повертає метод `list.size()`.
>
> * **`capacity` (ємність/місткість):**
>   * Це **розмір внутрішнього масиву**, який використовується для зберігання елементів. Ємність — це те, скільки елементів може вмістити `ArrayList` **до того, як йому доведеться розширюватися**.
>   * Ємність завжди **більша або дорівнює** розміру (`capacity >= size`).
>   * У `ArrayList` **немає публічного методу `getCapacity()`**, оскільки це деталь реалізації, яка не є частиною контракту `List`.
>
> **Приклад:**
>
> `ArrayList<String> list = new ArrayList<>(10); // capacity = 10, size = 0`
> `list.add("a"); // capacity = 10, size = 1`

#### **📝 Коментар:**</h4>

Розуміння цієї різниці допомагає оптимізувати продуктивність. Якщо ви знаєте приблизну кількість елементів заздалегідь, краще створювати `ArrayList` з потрібною початковою ємністю, щоб уникнути зайвих операцій розширення.

#### **📌 Що перевіряють:**

*Чітке розмежування між фактичною кількістю елементів та внутрішньою місткістю структури даних.*

* * *

### **5 ❓ Питання:**

Яку асимптотичну складність (Big O) мають основні операції в `ArrayList`?

> #### **💬 Відповідь:**
>
> * **`get(int index)`:**
>   * **O(1)** — константний час. Доступ до будь-якого елемента масиву за індексом є миттєвим.
>
> * **`add(E element)` (додавання в кінець):**
>   * **O(1)** — амортизований константний час. У більшості випадків це просто запис в наступну вільну комірку, але іноді вимагає розширення (O(n)).
>
> * **`add(int index, E element)` (додавання в середину/початок):**
>   * **O(n)** — лінійний час. Потрібно зсунути в середньому `n/2` елементів.
>
> * **`remove(int index)`:**
>   * **O(n)** — лінійний час. Аналогічно до додавання в середину, вимагає зсуву елементів.
>
> * **`remove(Object o)`:**
>   * **O(n)** — лінійний час. У найгіршому випадку потрібно пройти по всьому списку, щоб знайти елемент.
>
> * **`contains(Object o)`:**
>   * **O(n)** — лінійний час. Вимагає лінійного пошуку.

#### **📌 Що перевіряють:**

*Знання асимптотичної складності основних операцій. Це базове питання для аналізу продуктивності коду, що використовує `ArrayList`.*

* * *

### **6 ❓ Питання:**

Чому ітерація по `ArrayList` за допомогою класичного `for`-циклу з індексом є ефективною? ⭐️

> #### **💬 Відповідь:**
>
> Ітерація за допомогою `for`-циклу з індексом є дуже ефективною для `ArrayList` через його внутрішню структуру на основі масиву.
>
> **Приклад циклу:**
>
> ```java
> for (int i = 0; i < list.size(); i++) {
>     String s = list.get(i);
>     // ...
> }
> ```
>
> **Чому це ефективно:**
>
> 1. Виклик `list.size()` є дуже швидким (O(1)), він просто повертає значення поля.
> 2. Основна операція всередині циклу — `list.get(i)`. Для `ArrayList` ця операція має складність **O(1)**.
>
> В результаті, весь цикл виконується за **лінійний час O(n)**, що є оптимальним для обходу всіх елементів.
>
> Навпаки, такий самий цикл для **`LinkedList`** був би вкрай **неефективним (O(n^2))**, оскільки кожен виклик `list.get(i)` всередині циклу виконувався б за O(n). Для `LinkedList` слід завжди використовувати ітератор (`for-each` або явний `Iterator`).

#### **📌 Що перевіряють:**

*Здатність аналізувати продуктивність коду залежно від використовуваної реалізації `List`.*

* * *

### **7 ❓ Питання:**

Як `ArrayList` працює з пам'яттю? Коли пам'ять, зайнята видаленими елементами, звільняється? ⭐️

> #### **💬 Відповідь:**
>
> При видаленні елемента з `ArrayList` відбувається наступне:
>
> 1. Якщо це видалення не останнього елемента, всі наступні елементи зсуваються вліво (`System.arraycopy`).
> 2. Посилання на останній елемент у старому масиві **зануляється** (`elementData[--size] = null;`).
>
> **Коли пам'ять звільняється:**
>
> * Сам `ArrayList` **не зменшує ємність (capacity)** свого внутрішнього масиву автоматично після видалення елементів. Масив залишається того ж розміру. Це робиться для оптимізації, щоб уникнути частих операцій перестворення масиву, якщо після видалення будуть нові додавання.
>
> * Однак, оскільки посилання на видалений об'єкт видалено зі списку (і занулено, якщо це був останній елемент), цей об'єкт стає **доступним для збирача сміття (Garbage Collector)**. Пам'ять буде звільнена під час наступного циклу збирання сміття, за умови, що на цей об'єкт немає інших посилань у програмі.
>
> Якщо ви видалили велику кількість елементів і хочете звільнити зайву пам'ять, яку займає `ArrayList`, ви можете викликати метод **`trimToSize()`**. Він створює новий масив точно такого розміру, як кількість елементів, і копіює їх туди.

#### **📌 Що перевіряють:**

*Розуміння того, як `ArrayList` керує пам'яттю, та знання про метод `trimToSize`.*

* * *

### **8 ❓ Питання:**

Яка початкова ємність `ArrayList` при виклику конструктора без аргументів? ⭐️

> #### **💬 Відповідь:**
>
> Це залежить від версії Java.
>
> * **До Java 6:** Початкова ємність була **10**. Тобто при виклику `new ArrayList()` одразу створювався масив на 10 елементів.
>
> * **Починаючи з Java 7:** Реалізація була оптимізована. При виклику `new ArrayList()` створюється **порожній масив** (`private static final Object[] EMPTY_ELEMENTDATA = {};`).
>
>     І тільки **при додаванні першого елемента** `ArrayList` виділяє пам'ять і створює масив з ємністю **10**.
>
> Ця оптимізація називається **"лінивою ініціалізацією"** і дозволяє уникнути виділення пам'яті для списків, які створюються, але ніколи не наповнюються.
>
> Незважаючи на цю зміну в реалізації, "логічна" початкова ємність залишається 10.

#### **📌 Що перевіряють:**

*Знання деталей реалізації та її еволюції. Це питання може відрізнити кандидата, який читав вихідний код, від того, хто знає тільки API.*

* * *

### **9 ❓ Питання:**

Чи є `ArrayList` потокобезпечним? Якщо ні, то як отримати його потокобезпечну версію? ⭐️

> #### **💬 Відповідь:**
>
> Ні, `ArrayList` **не є потокобезпечним**.
>
> Якщо кілька потоків одночасно намагаються змінити `ArrayList` (наприклад, додавати або видаляти елементи), це може призвести до **неконсистентного стану**, `ConcurrentModificationException` або втрати даних.
>
> **Як отримати потокобезпечну версію:**
>
> 1. **`Collections.synchronizedList(new ArrayList<>(...))`:**
>     * Це найпростіший спосіб. Метод `Collections.synchronizedList()` повертає **обгортку (wrapper)** над вашим `ArrayList`, де кожен метод (`add`, `get`, `remove` тощо) є `synchronized`.
>     * **Недолік:** Це забезпечує лише потокобезпечність **окремих операцій**. Ітерація по такій колекції все одно вимагає зовнішньої синхронізації.
>
> 2. **`CopyOnWriteArrayList`:**
>     * Це спеціалізована **конкурентна колекція** з пакету `java.util.concurrent`.
>     * Вона є потокобезпечною.
>     * **Як працює:** При кожній **операції запису** (`add`, `remove`) створюється **повна нова копія** внутрішнього масиву. Це робить операції запису "дорогими".
>     * **Перевага:** Операції **читання** є дуже швидкими і не вимагають блокувань. Ітератор для неї є **`fail-safe`** і ніколи не кине `ConcurrentModificationException`.
>     * **Коли використовувати:** Ідеально підходить для сценаріїв, де **кількість читань значно перевищує кількість записів** (наприклад, для зберігання слухачів - listeners).

#### **📌 Що перевіряють:**

*Розуміння проблем багатопотоковості для колекцій. Знання стандартних способів забезпечення потокобезпечності, включаючи `synchronizedList` та `CopyOnWriteArrayList`.*

* * *

### **10 ❓ Питання:**

Як `remove(Object o)` знаходить елемент для видалення і що станеться, якщо у списку є `null`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `remove(Object o)` виконує **лінійний пошук**, ітеруючи по елементах списку.
>
> Він шукає перший елемент `e`, для якого виконується умова: `(o == null ? e == null : o.equals(e))`.
>
> **Поведінка з `null`:**
>
> * **Якщо ви намагаєтеся видалити `null` (`list.remove(null)`):**
>   * `ArrayList` буде ітерувати по списку і шукати перший елемент, який дорівнює `null` (`e == null`).
>   * Якщо `null` буде знайдено, він буде видалений, і метод поверне `true`.
>   * Якщо `null` не знайдено, метод поверне `false`.
>
> `ArrayList` коректно обробляє `null` як для пошуку, так і для зберігання.
>
> **Поведінка з дублікатами:**
> Важливо пам'ятати, що `remove(Object o)` видаляє **тільки перше** знайдене входження елемента. Якщо у вас є кілька дублікатів, для видалення всіх їх доведеться викликати цей метод у циклі або використовувати `removeAll()` чи `removeIf()`.

#### **📌 Що перевіряють:**

*Уважність до деталей роботи API, зокрема, коректна обробка `null` та дублікатів.*

* * *

### **11 ❓ Питання:**

Поясніть поведінку ітератора для `ArrayList`. Чи є він `fail-fast`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, ітератор, який повертається методом `ArrayList.iterator()`, є **`fail-fast`**.
>
> **Як це працює:**
>
> 1. `ArrayList` має внутрішнє поле-лічильник `modCount`, яке інкрементується при кожній структурній модифікації списку (`add`, `remove`, `clear`).
>
> 2. Коли створюється ітератор, він запам'ятовує поточне значення `modCount` у власному полі `expectedModCount`.
>
> 3. При кожному виклику методів ітератора (`next()`, `hasNext()`) він спочатку перевіряє, чи `modCount` все ще дорівнює `expectedModCount`.
>
> 4. Якщо значення не збігаються, це означає, що список був змінений іншим способом (не через `iterator.remove()`) під час ітерації. Ітератор негайно кидає **`ConcurrentModificationException`**.
>
> **Приклад, що призводить до помилки:**
>
> ```java
> for (String s : list) {
>     if (s.equals("b")) {
>         list.remove(s); // Помилка! Модифікація не через ітератор.
>     }
> }
> ```
>
> Цей механізм призначений для виявлення програмістських помилок, а не для забезпечення потокобезпечності.

#### **📌 Що перевіряють:**

*Глибоке розуміння механізму `fail-fast`, ролі `modCount` та правил безпечної модифікації колекції під час ітерації.*

* * *

### **12 ❓ Питання:**

Що поверне `Arrays.asList("a", "b").getClass() == new ArrayList<String>().getClass()`? Поясніть. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз поверне **`false`**.
>
> **Причина:**
>
> * `Arrays.asList("a", "b")` повертає **не `java.util.ArrayList`**, а екземпляр **приватного статичного вкладеного класу `java.util.Arrays.ArrayList`**. Це абсолютно інший клас, хоча він і реалізує інтерфейс `List`.
>
> * `new ArrayList<String>()` створює екземпляр стандартного, публічного класу **`java.util.ArrayList`**.
>
> Оскільки це об'єкти **різних класів**, виклик `getClass()` для них поверне різні об'єкти `Class`, і порівняння через `==` дасть `false`.
>
> Це питання є класичною "пасткою", яка перевіряє, чи знає кандидат про особливість реалізації `Arrays.asList()`.

#### **📌 Що перевіряють:**

*Дуже глибокі знання JDK. Розуміння того, що один і той же інтерфейс може мати багато різних, в тому числі і приватних, реалізацій.*

* * *

### **13 ❓ Питання:**

Припустимо, у вас є `ArrayList`, і вам потрібно значно прискорити операцію `contains()`. Що можна зробити? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Операція `contains()` для `ArrayList` має лінійну складність **O(n)**, оскільки вона виконує послідовний перебір елементів. Якщо ця операція стає "вузьким місцем", і вам потрібен швидкий пошук, `ArrayList` є поганим вибором.
>
> **Що можна зробити:**
>
> Найкращим рішенням є **перетворити `ArrayList` на `HashSet`**.
>
> ```java
> List<String> myList = ...;
> // Створюємо HashSet з елементів списку. Це займе O(n) часу.
> Set<String> mySet = new HashSet<>(myList);
>
> // Тепер операція contains буде виконуватися значно швидше
> boolean exists = mySet.contains("some_element"); // O(1) в середньому
> ```
>
> **Компроміси:**
>
> * **Втрата порядку:** `HashSet` не гарантує порядок елементів. Якщо порядок важливий, можна використовувати `LinkedHashSet`.
> * **Втрата дублікатів:** `Set` автоматично видалить всі дублікати з вихідного списку.
> * **Додаткова пам'ять:** Потрібно виділити пам'ять під нову структуру даних (`HashSet`).
>
> Цей підхід є ефективним, якщо ви збираєтеся виконувати **багато** операцій пошуку (`contains`). Вартість перетворення (O(n)) "окупиться" за рахунок дуже швидкого подальшого пошуку (O(1)).

#### **📌 Що перевіряють:**

*Здатність вибирати правильну структуру даних для задачі. Розуміння сильних сторін `Set` для операцій пошуку.*

* * *

### **14 ❓ Питання:**

Чи завжди `ArrayList` швидший за `LinkedList` для доступу за індексом? Чи є винятки? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, **практично завжди**. Доступ за індексом (`get(i)`) є головною перевагою `ArrayList` (O(1)) і слабкістю `LinkedList` (O(n)).
>
> **Чи є винятки?**
>
> Можна придумати **теоретичні крайні випадки**, коли `LinkedList` може виграти, але на практиці вони майже не зустрічаються.
>
> 1. **Доступ до першого/останнього елемента:** Для індексів `0` та `size()-1`, `LinkedList` має таку ж складність O(1), як і `ArrayList`.
>
> 2. **Дуже специфічний патерн доступу:** Якби ви послідовно ітерували по `LinkedList` за допомогою ітератора, а потім робили один `get` для елемента поруч, це могло б бути швидше, ніж випадковий доступ в `ArrayList` (хоча це дуже надумано).
>
> Однак, навіть з урахуванням констант, кешування та оптимізацій процесора (spatial locality для масивів), `ArrayList` для **випадкового доступу** за індексом (`random access`) завжди буде на порядки швидшим.
>
> Тому інтерфейс `List` має маркерний підінтерфейс `RandomAccess`. `ArrayList` його реалізує, а `LinkedList` — ні. Алгоритми, як-от `Collections.sort`, можуть перевіряти наявність цього інтерфейсу і вибирати більш оптимальну стратегію залежно від реалізації `List`.

#### **📌 Що перевіряють:**

*Глибоке розуміння деталей продуктивності та знання про маркерний інтерфейс `RandomAccess`.*

* * *

### **15 ❓ Питання:**

Як метод `ensureCapacity(int minCapacity)` може бути використаний для оптимізації `ArrayList`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Метод `ensureCapacity(int minCapacity)` дозволяє **програмно збільшити ємність (capacity)** внутрішнього масиву `ArrayList`, щоб він міг вмістити щонайменше `minCapacity` елементів без подальших розширень.
>
> **Як це використовується для оптимізації:**
>
> Якщо ви заздалегідь знаєте, що збираєтеся додати в `ArrayList` велику кількість елементів (наприклад, 1 000 000), ви можете викликати `ensureCapacity` **один раз** перед циклом додавання.
>
> **Без `ensureCapacity`:**
>
> ```java
> ArrayList<Integer> list = new ArrayList<>();
> for (int i = 0; i < 1_000_000; i++) {
>     list.add(i); // Відбудеться багато дорогих операцій розширення
> }
> ```
>
> **З `ensureCapacity`:**
>
> ```java
> ArrayList<Integer> list = new ArrayList<>();
> list.ensureCapacity(1_000_000); // Виділяємо всю потрібну пам'ять один раз
>
> for (int i = 0; i < 1_000_000; i++) {
>     list.add(i); // Тепер розширень не буде
> }
> ```
>
> **Перевага:**
> Це дозволяє уникнути багаторазових операцій створення нових масивів та копіювання даних (`System.arraycopy`), які відбувалися б при кожному розширенні. Ви виконуєте одну, хоча і "велику", операцію виділення пам'яті, що в сумі значно підвищує продуктивність для масового додавання елементів.
>
> Використання конструктора `new ArrayList<>(capacity)` має той самий ефект.

#### **📌 Що перевіряють:**

*Знання про менш відомі, але корисні методи для оптимізації роботи з `ArrayList`. Вміння думати про продуктивність заздалегідь.*
