---
layout: default
title: "Iterable та Iterator — інтерфейси для проходу по елементам колекцій"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 11
---

# Iterable та Iterator — інтерфейси для проходу по елементам колекцій

* короткий опис: `Iterable` – це інтерфейс, який дозволяє об'єкту бути ітерованим (наприклад, у циклі `for-each`). `Iterator` – інтерфейс, що надає механізм для послідовного доступу до елементів колекції (`hasNext()`, `next()`, `remove()`).

* поняття: `Iterable`, `Iterator`, `hasNext()`, `next()`, `remove()`.

* приклади: Цикл `for-each` (використовує `Iterable` та `Iterator` під капотом), явне використання `Iterator` для безпечного видалення елементів під час ітерації (`iterator.remove()`).

* ключові моменти: `for-each` працює через `Iterator`. `Iterator.remove()` – єдиний безпечний спосіб видалення елементів під час ітерації, щоб уникнути `ConcurrentModificationException`.

* що важливо знати на співбесіді: Призначення, як працює `for-each`, як видаляти елементи під час ітерації.

### **✅ Офіційне визначення:**

* **`Iterable`** — функціональний інтерфейс, який представляє об’єкт, по якому можна ітерувати (наприклад, у циклі `for-each`), і має один метод `iterator()`.
* **`Iterator`** — інтерфейс, що надає механізм для послідовного доступу до елементів колекції без розкриття її внутрішньої реалізації.

### **🧠 Простими словами:**

* **`Iterable`** — це "контейнер", який каже: «Мене можна перебирати».
* **`Iterator`** — це "кур'єр", який знає, як пройти по всіх елементах, один за одним.

---

## **📊 Основні методи:**

* **`Iterable.iterator()`** — повертає об’єкт **`Iterator`**
* **`Iterator.hasNext()`** — перевіряє, чи є ще елементи
* **`Iterator.next()`** — повертає наступний елемент
* **`Iterator.remove()`** — видаляє поточний елемент (опційний метод)

---

## **Пояснення на пальцях (💡)**

Уяви, що **`Iterable`** — це книжка, а **`Iterator`** — це закладка, яка поступово показує сторінку за сторінкою.

Коли ти пишеш:

```java
for (String fruit : fruits) {
    System.out.println(fruit);
}
```

Під капотом це працює як:

```java
Iterator<String> iterator = fruits.iterator();
while (iterator.hasNext()) {
    String fruit = iterator.next();
    System.out.println(fruit);
}
```

---

## **Корисні приклади (🧪)**

```java
List<String> list = new ArrayList<>();
list.add("apple");
list.add("banana");
list.add("cherry");

// Явна ітерація
Iterator<String> it = list.iterator();
while (it.hasNext()) {
    String fruit = it.next();
    System.out.println(fruit);
}

// Видалення під час ітерації (тільки через iterator)
it = list.iterator();
while (it.hasNext()) {
    if (it.next().equals("banana")) {
        it.remove(); // safe
    }
}
System.out.println(list); // [apple, cherry]
```

---

## **Порівняння (⚖️)**

| Властивість | Iterable | Iterator |
| :---: | :---: | :---: |
| **Призначення** | Дає доступ до ітератора | Ітерація по елементах |
| **Метод** | **`iterator()`** | **`hasNext(), next(), remove()`** |
| **Підтримує for-each** | ✅ | ❌ (тільки вручну) |
| **Впроваджують** | **`Collection`**, **`List`**, **`Set`** | **`ArrayList.iterator(),`** тощо |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"`Iterable` — це те, що перебирають, `Iterator` — той, хто перебирає."**

Іншими словами: Iterable дає дозвіл, Iterator — виконує

---

## **🟩 Підсумок:**

**`Iterable`** — інтерфейс для об'єктів, що дозволяють ітерування (наприклад, у **`for-each`**). **`Iterator`** — це механізм, який дозволяє поетапно отримувати елементи. Для видалення елементів під час ітерації слід використовувати **`Iterator.remove()`**.

### **🔗 Додаткові джерела:**

* [**Oracle Docs — java.lang.Iterable**](https://docs.oracle.com/javase/8/docs/api/java/lang/Iterable.html)
* [**Oracle Docs — java.util.Iterator**](https://docs.oracle.com/javase/8/docs/api/java/util/Iterator.html)  

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому полягає різниця між інтерфейсами `Iterable` та `Iterator`?

> #### **💬 Відповідь:**
>
> Це два різних, але тісно пов'язаних інтерфейси, які вирішують різні завдання:
>
> * **`Iterable<T>` (той, який можна ітерувати):**
>   * **Призначення:** Це **контракт**, який об'єкт (наприклад, колекція) реалізує, щоб повідомити: "по мені можна проходитися".
>   * **Що надає:** Він має лише **один метод — `iterator()`**, який повертає об'єкт `Iterator`.
>   * **Хто реалізує:** Усі класи з Collections Framework (`ArrayList`, `HashSet` тощо). Реалізація цього інтерфейсу дозволяє використовувати об'єкт у циклі `for-each`.
>
> * **`Iterator<T>` (той, хто ітерує):**
>   * **Призначення:** Це **об'єкт**, який безпосередньо виконує обхід колекції. Він знає, як отримати наступний елемент і перевірити, чи є ще елементи.
>   * **Що надає:** Має три основні методи:
>     * `hasNext()`: перевіряє, чи є наступний елемент.
>     * `next()`: повертає наступний елемент і зсуває "курсор".
>     * `remove()`: видаляє останній повернутий елемент.
>   * `Iterator` має стан (він "пам'ятає" поточну позицію в колекції).
>
> **Коротко:** `Iterable` — це **об'єкт**, `Iterator` — це **механізм** для обходу цього об'єкта.

#### **📝 Аналогія:**

`Iterable` — це книжка. `Iterator` — це ваша закладка, яку ви пересуваєте зі сторінки на сторінку, щоб знати, де ви зупинилися.

#### **📌 Що перевіряють:**

*Чітке розмежування між "об'єктом, що ітерується" та "об'єктом, що виконує ітерацію".*

* * *

### **2 ❓ Питання:**

Як цикл `for-each` пов'язаний з `Iterable` та `Iterator`?

> #### **💬 Відповідь:**
>
> Цикл `for-each` є **синтаксичним цукром** над використанням `Iterator`.
>
> **Код, який ви пишете:**
>
> ```java
> List<String> names = List.of("Alice", "Bob");
> for (String name : names) {
>     System.out.println(name);
> }
> ```
>
> **Код, на який його перетворює компілятор:**
>
> ```java
> List<String> names = List.of("Alice", "Bob");
> Iterator<String> iterator = names.iterator(); // 1. Отримуємо ітератор
> while (iterator.hasNext()) { // 2. Перевіряємо, чи є наступний елемент
>     String name = iterator.next(); // 3. Отримуємо елемент
>     System.out.println(name);
> }
> ```
>
> Це означає, що цикл `for-each` може використовуватися з **будь-яким** об'єктом, клас якого реалізує інтерфейс `Iterable`, оскільки це гарантує наявність методу `iterator()`.

#### **📌 Що перевіряють:**

*Розуміння того, що `for-each` — це зручна обгортка, і "під капотом" він працює через `Iterator`.*

* * *

### **3 ❓ Питання:**

Чому `Iterator.remove()` є єдиним безпечним способом видалити елемент з колекції під час ітерації?

> #### **💬 Відповідь:**
>
> Тому що це єдиний спосіб уникнути винятку **`ConcurrentModificationException`**, який кидають **`fail-fast` ітератори**.
>
> **Проблема:**
> Більшість стандартних колекцій (як `ArrayList`, `HashSet`) використовують `fail-fast` ітератори. Це означає, що якщо структура колекції (наприклад, її розмір) змінюється під час ітерації будь-яким іншим способом, крім методів самого ітератора, ітератор негайно "падає" з помилкою.
>
> **Чому це небезпечно:**
>
> ```java
> for (String name : names) {
>     if (name.equals("delete_me")) {
>         names.remove(name); // Небезпечна модифікація, яка призведе до ConcurrentModificationException
>     }
> }
> ```
>
> **Як працює `Iterator.remove()`:**
>
> Метод `iterator.remove()` розроблений так, що він **сповіщає** колекцію (або ітератор сам знає), що відбувається "легальне" видалення. Він не тільки видаляє елемент з базової колекції, але й **оновлює внутрішній стан ітератора**, щоб той міг коректно продовжити роботу. Це запобігає виникненню `ConcurrentModificationException`.
>
> **Правильний підхід:**
>
> ```java
> Iterator<String> it = names.iterator();
> while (it.hasNext()) {
>     if (it.next().equals("delete_me")) {
>         it.remove(); // Безпечне видалення
>     }
> }
> ```

#### **📌 Що перевіряють:**

*Знання про `ConcurrentModificationException` та правильний ідіоматичний спосіб модифікації колекції під час її обходу.*

* * *

### **4 ❓ Питання:**

Назвіть три основні методи інтерфейсу `Iterator` і поясніть їх призначення.

> #### **💬 Відповідь:**
>
> 1. **`boolean hasNext()`**
>     * **Призначення:** Перевіряє, чи є в колекції ще елементи для обходу.
>     * **Повертає:** `true`, якщо ітерація ще не завершена, і `false`, якщо всі елементи вже пройдено. Цей метод зазвичай використовується як умова в циклі `while`.
>
> 2. **`E next()`**
>     * **Призначення:** Повертає **наступний** елемент у колекції і зсуває внутрішній "курсор" ітератора на одну позицію вперед.
>     * **Повертає:** Наступний елемент. Якщо викликати `next()`, коли елементів більше немає ( `hasNext()` повернуло `false`), буде кинуто `NoSuchElementException`.
>
> 3. **`void remove()`**
>     * **Призначення:** Видаляє з базової колекції **останній елемент, повернутий методом `next()`**.
>     * **Важливо:** Цей метод можна викликати лише **один раз** після виклику `next()`. Повторний виклик `remove()` без проміжного виклику `next()` призведе до `IllegalStateException`. Це опціональний метод, і деякі ітератори можуть не підтримувати його (кидаючи `UnsupportedOperationException`).

#### **📌 Що перевіряють:**

*Знання базового API `Iterator` та поведінки його методів, включаючи можливі винятки.*

* * *

### **5 ❓ Питання:**

Чи можна реалізувати власний клас, який буде підтримувати цикл `for-each`? Якщо так, то як? ⭐️

> #### **💬 Відповідь:**
>
> Так, можна. Для цього ваш клас повинен **реалізувати інтерфейс `java.lang.Iterable<T>`**.
>
> Це вимагає від вас реалізації всього одного методу — **`iterator()`**, який повинен повертати екземпляр `Iterator<T>`.
>
> **Приклад: власний клас для зберігання списку завдань.**
>
> ```java
> import java.util.Iterator;
> import java.util.LinkedList;
> import java.util.List;
>
> // 1. Клас реалізує Iterable
> public class TaskList implements Iterable<String> {
>     private List<String> tasks = new LinkedList<>();
>
>     public void addTask(String task) {
>         tasks.add(task);
>     }
>
>     // 2. Реалізація методу iterator()
>     @Override
>     public Iterator<String> iterator() {
>         // Просто повертаємо ітератор внутрішньої колекції
>         return tasks.iterator();
>     }
>
>     public static void main(String[] args) {
>         TaskList myTasks = new TaskList();
>         myTasks.addTask("Task 1");
>         myTasks.addTask("Task 2");
>
>         // 3. Тепер можна використовувати for-each
>         for (String task : myTasks) {
>             System.out.println(task);
>         }
>     }
> }
> ```
>
> Таким чином, будь-який об'єкт, який ви хочете зробити "ітерованим", має реалізувати `Iterable`.

#### **📌 Що перевіряють:**

*Практичні навички реалізації стандартних інтерфейсів та розуміння, як зробити власні класи сумісними з конструкціями мови.*

* * *

### **6 ❓ Питання:**

Що таке `ListIterator` і які додаткові можливості він надає?

> #### **💬 Відповідь:**
>
> `ListIterator` — це розширений підінтерфейс `Iterator`, призначений спеціально для роботи з колекціями типу **`List`**.
>
> **Додаткові можливості:**
>
> 1. **Двонаправлений обхід:** Дозволяє рухатися не тільки вперед, але й **назад** за допомогою методів:
>     * `hasPrevious()`: перевіряє, чи є попередній елемент.
>     * `previous()`: повертає попередній елемент.
>
> 2. **Модифікація списку:** Дозволяє не тільки видаляти, але й **додавати (`add(E e)`)** та **замінювати (`set(E e)`)** елементи під час ітерації.
>
> 3. **Доступ до індексів:** Надає методи `nextIndex()` та `previousIndex()` для отримання індексів сусідніх елементів.
>
> Отримати його можна за допомогою методу `list.listIterator()`.

#### **📌 Що перевіряють:**

*Знання про існування `ListIterator` та його переваг для роботи з `List`.*

* * *

### **7 ❓ Питання:**

У чому різниця між `fail-fast` та `fail-safe` ітераторами? Наведіть приклади. ⭐️

> #### **💬 Відповідь:**
>
> Це дві різні стратегії поведінки ітераторів при зміні колекції під час обходу.
>
> * **`fail-fast` (швидко-падаючий):**
>   * **Поведінка:** Якщо колекція структурно змінюється іншим способом, крім як через методи самого ітератора, ітератор негайно кидає **`ConcurrentModificationException`**.
>   * **Приклади:** Ітератори для **більшості стандартних колекцій** (`ArrayList`, `HashSet`, `HashMap`).
>   * **Мета:** Не забезпечити потокобезпечність, а якомога раніше виявити **програмістську помилку** (модифікація колекції, по якій іде ітерація).
>
> * **`fail-safe` (безпечний до збоїв):**
>   * **Поведінка:** Ітератор **не кидає** `ConcurrentModificationException`. Він працює з **копією** або **"знімком" (snapshot)** колекції, створеним на момент початку ітерації.
>   * **Наслідки:** Зміни, зроблені в оригінальній колекції, не будуть видимі для ітератора. Він працює зі старими, "замороженими" даними.
>   * **Приклади:** Ітератори для **конкурентних колекцій** з `java.util.concurrent`, таких як `CopyOnWriteArrayList` та `ConcurrentHashMap`.

#### **📌 Що перевіряють:**

*Глибоке розуміння поведінки ітераторів. Знання про конкурентні колекції та їхню відмінність від стандартних.*

* * *

### **8 ❓ Питання:**

Чому `remove()` в `Iterator` є опціональним методом?

> #### **💬 Відповідь:**
>
> Метод `remove()` оголошений в інтерфейсі `Iterator` як **метод за замовчуванням (default method)** (з Java 8), який кидає `UnsupportedOperationException`. Раніше він був просто абстрактним, але реалізаціям дозволялося кидати цей виняток.
>
> **Причина опціональності:**
>
> Не всі колекції є **змінюваними (modifiable)**. Наприклад, якщо у вас є колекція "тільки для читання", було б логічно, щоб її ітератор не дозволяв видаляти елементи.
>
> **Приклади:**
>
> * Колекції, створені через `List.of()` або `Set.of()`, є імутабельними. Їхні ітератори кинуть `UnsupportedOperationException` при виклику `remove()`.
> * Обгортки `Collections.unmodifiableList()` також повертають ітератор, що не підтримує видалення.
>
> Таким чином, опціональність `remove()` дозволяє інтерфейсу `Iterator` бути достатньо гнучким для роботи як зі змінюваними, так і з незмінними колекціями.

#### **📌 Що перевіряють:**

*Розуміння того, що деякі методи в інтерфейсах є опціональними, та вміння пояснити, чому це необхідно.*

* * *

### **9 ❓ Питання:**

Як реалізовано ітератор для `LinkedList`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітератор для `LinkedList` — це об'єкт, який внутрішньо зберігає посилання на **поточний вузол (`Node`)** у списку, а не просто числовий індекс.
>
> **Як працюють його методи:**
>
> * **`hasNext()`:** Перевіряє, чи посилання на наступний вузол не є `null`.
>
> * **`next()`:**
>     1. Повертає значення з поточного вузла.
>     2. Пересуває свій внутрішній "курсор" на наступний вузол, слідуючи посиланню `next`.
>
> * **`remove()`:**
>   * Це дуже **ефективна операція (O(1))**. Оскільки ітератор вже має посилання на поточний вузол, йому не потрібно шукати його.
>   * Він просто змінює посилання `next` та `prev` у сусідніх вузлів, щоб "виключити" поточний вузол з ланцюжка.
>
> Оскільки `LinkedList` є двозв'язним, його `ListIterator` може ефективно рухатися в обох напрямках, просто слідуючи відповідним посиланням (`next` або `prev`).

#### **📌 Що перевіряють:**

*Знання внутрішньої реалізації ітератора для однієї з ключових структур даних. Розуміння того, чому `iterator.remove()` для `LinkedList` є швидким.*

* * *

### **10 ❓ Питання:**

Які альтернативи використанню `Iterator` існують з Java 8? ⭐️

> #### **💬 Відповідь:**
>
> З появою Java 8 та `Stream API` з'явилися більш функціональні та декларативні альтернативи ручному обходу через `Iterator`.
>
> 1. **`Stream API` та метод `forEach(Consumer action)`:**
>     Це найбільш поширена альтернатива для простого обходу.
>
>     ```java
>     names.stream().forEach(name -> System.out.println(name));
>     // або ще коротше, через посилання на метод
>     names.forEach(System.out::println);
>     ```
>
> 2. **`removeIf(Predicate filter)`:**
>     Це найкраща альтернатива для безпечного видалення елементів під час ітерації.
>
>     ```java
>     // Замість ручного циклу з iterator.remove()
>     names.removeIf(name -> name.startsWith("A"));
>     ```
>
> 3. **Інші операції `Stream API` (`map`, `filter`, `reduce`):**
>     Замість того, щоб ітерувати по колекції для її перетворення або агрегації, `Stream API` дозволяє описати ці операції декларативно.
>
> Хоча `Iterator` все ще є фундаментальним механізмом "під капотом", сучасний Java-код часто використовує більш високорівневі абстракції `Stream API` для роботи з колекціями.

#### **📌 Що перевіряють:**

*Знання `Stream API` та розуміння того, як функціональний підхід може замінити імперативні цикли з ітераторами.*

* * *

### **11 ❓ Питання:**

Чи можна отримати `Iterator` для `Map`? Якщо так, то як? ⭐️

> #### **💬 Відповідь:**
>
> Оскільки `Map` **не реалізує** `Iterable`, ви **не можете** викликати `map.iterator()` напряму.
>
> Однак `Map` надає три методи, які повертають представлення її даних у вигляді **колекцій (`Set` або `Collection`)**, і вже для цих колекцій можна отримати ітератор.
>
> 1. **`map.keySet().iterator()`:**
>     * Повертає `Iterator<K>` для обходу **ключів**.
>
> 2. **`map.values().iterator()`:**
>     * Повертає `Iterator<V>` для обходу **значень**.
>
> 3. **`map.entrySet().iterator()`:**
>     * **Найбільш поширений і корисний** спосіб.
>     * Повертає `Iterator<Map.Entry<K, V>>` для обходу **пар "ключ-значення"**. `Map.Entry` — це об'єкт, що містить і ключ, і значення.
>
> **Приклад ітерації по `entrySet`:**
>
> ```java
> Iterator<Map.Entry<String, Integer>> iterator = map.entrySet().iterator();
> while (iterator.hasNext()) {
>     Map.Entry<String, Integer> entry = iterator.next();
>     String key = entry.getKey();
>     Integer value = entry.getValue();
>     // ...
> }
> ```
>
> У циклі `for-each` це виглядає ще простіше: `for (Map.Entry<String, Integer> entry : map.entrySet())`.

#### **📌 Що перевіряють:**

*Розуміння того, як працювати з `Map`, та знання про її "представлення" (`view collections`).*

* * *

### **12 ❓ Питання:**

Що таке `Spliterator` і чим він відрізняється від `Iterator`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`Spliterator` (splitable iterator)** — це новий тип ітератора, введений у Java 8, який є основою для `Stream API`, особливо для **паралельних потоків**.
>
> **Відмінності від `Iterator`:**
>
> 1. **Можливість розпаралелювання:** `Spliterator` має метод `trySplit()`, який може "розділити" послідовність на дві частини. Це дозволяє `Stream API` розбивати велику колекцію на менші шматки і обробляти їх у кількох потоках одночасно. `Iterator` є суто послідовним.
>
> 2. **Ітерація та обхід:** Замість `hasNext()`/`next()`, він використовує метод `tryAdvance(Consumer action)`. Цей метод виконує передану дію для наступного елемента і повертає `true`, якщо елемент був.
>
> 3. **Не підтримує `remove()`:** `Spliterator` призначений для обходу, а не для модифікації.
>
> **Коротко:** `Iterator` — для простого, послідовного обходу. `Spliterator` — для високоефективного, розпаралелюваного обходу, що лежить в основі `Stream API`. Звичайним розробникам рідко доводиться реалізовувати `Spliterator` вручну.

#### **📌 Що перевіряють:**

*Знання про існування `Spliterator` та його ключової ролі у паралельних стрімах. Це просунуте питання.*

* * *

### **13 ❓ Питання:**

Як реалізовано `hasNext()` у ітератора для `HashSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітератор для `HashSet` (який "під капотом" використовує `HashMap`) є більш складним, ніж для списку.
>
> 1. `HashSet` складається з масиву "кошиків" (buckets). Кожен кошик може бути або порожнім, або містити список/дерево елементів (через колізії).
>
> 2. Ітератор `HashSet` зберігає два основні покажчики:
>     * `int index` — індекс поточного кошика у внутрішньому масиві.
>     * `Node<E> current` — посилання на поточний вузол всередині зв'язаного списку/дерева в цьому кошику.
>
> 3. Метод **`hasNext()`** працює приблизно так:
>     * Спочатку він перевіряє, чи є наступний елемент (`current.next`) у **поточному кошику**. Якщо є — повертає `true`.
>     * Якщо елементів у поточному кошику більше немає, він починає **шукати наступний не порожній кошик**, рухаючись по внутрішньому масиві (`for` цикл від `index`).
>     * Якщо він знаходить не порожній кошик, він встановлює `current` на перший елемент у ньому і повертає `true`.
>     * Якщо він доходить до кінця масиву і не знаходить більше не порожніх кошиків, він повертає `false`.
>
> Ця логіка є досить складною, бо ітератор має "стрибати" по кошиках і проходити по внутрішніх структурах кожного з них.

#### **📌 Що перевіряють:**

*Дуже глибокі знання про внутрішню реалізацію `HashSet` та її ітератора.*

* * *

### **14 ❓ Питання:**

Якщо у вашому власному `Iterable` класі метод `iterator()` буде щоразу повертати один і той самий екземпляр `Iterator`, до яких проблем це може призвести? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це призведе до серйозних проблем і зробить клас практично непридатним для використання.
>
> `Iterator` має **внутрішній стан** (поточну позицію). Якщо `iterator()` щоразу повертає той самий об'єкт-ітератор, то цей стан буде **спільним (shared)** для всіх спроб ітерації.
>
> **Проблеми:**
>
> 1. **Неможливість повторної ітерації:**
>     * Після того, як ви пройдете по колекції один раз, ітератор дійде до кінця, і його `hasNext()` завжди буде повертати `false`. Другий цикл `for-each` по тій самій колекції просто не виконається.
>
> 2. **Проблеми з вкладеними циклами:**
>
>     ```java
>     for (Item item1 : myIterable) {
>         for (Item item2 : myIterable) { // Цей цикл використовує той самий ітератор!
>             // ...
>         }
>     }
>     ```
>
>     Внутрішній цикл "доведе" спільний ітератор до кінця, і зовнішній цикл після першої ж ітерації завершиться.
>
> **Правильна реалізація:** Метод `iterator()` **завжди** повинен створювати і повертати **новий екземпляр** `Iterator`, який починає обхід з самого початку і має свій власний, незалежний стан.
>
> `public Iterator<T> iterator() { return new MyIterator(); }`

#### **📌 Що перевіряють:**

*Розуміння того, що `Iterator` є `stateful`, і наслідків створення спільного стану.*

* * *

### **15 ❓ Питання:**

Як ітератор працює з видаленням у `ConcurrentHashMap`? Чи кидає він `ConcurrentModificationException`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, ітератори для `ConcurrentHashMap` (та інших конкурентних колекцій з `java.util.concurrent`) є **`fail-safe`**, а не `fail-fast`. Вони **не кидають `ConcurrentModificationException`**.
>
> **Поведінка ітератора `ConcurrentHashMap`:**
>
> * Ітератор відображає стан мапи на момент свого створення або на певний момент у часі.
> * Він **може, але не зобов'язаний**, відображати зміни, які відбулися в мапі після його створення.
> * Гарантується, що ітератор пройде по кожному елементу, який існував на момент початку ітерації, **рівно один раз**, і не кине виняток, навіть якщо інші потоки в цей час додають або видаляють елементи.
>
> **`iterator.remove()`:**
>
> * Метод `remove()` для ітераторів `ConcurrentHashMap` є **потокобезпечним**. Він коректно видаляє поточний елемент з мапи.
>
> **Підсумок:** `ConcurrentHashMap` надає надійний, потокобезпечний ітератор, який розроблений для роботи у високонавантаженому багатопотоковому середовищі і не "падає" при одночасних модифікаціях.

#### **📌 Що перевіряють:**

*Знання про конкурентні колекції та їхні ключові відмінності від стандартних, зокрема, поведінку їхніх ітераторів (`fail-safe`).*
