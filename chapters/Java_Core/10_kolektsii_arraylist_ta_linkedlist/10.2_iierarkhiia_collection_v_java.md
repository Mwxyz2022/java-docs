---
layout: default
title: "Ієрархія Collection в Java"
parent: "Колекції: ArrayList та LinkedList"
grand_parent: "Java Core"
nav_order: 2
---

# Ієрархія Collection в Java

* короткий опис: Інтерфейс `Collection` є базовим для `List`, `Set` та `Queue`. Вся ієрархія базується на `java.lang.Iterable`, що дозволяє ітерувати по елементах. `Map` не є частиною цієї ієрархії.

* поняття: `Iterable`, `Collection`, `List`, `Set`, `Queue`, реалізації (`ArrayList`, `HashSet`, `PriorityQueue`, `ArrayDeque`).

* приклади: Діаграма ієрархії, що показує зв'язки між інтерфейсами та їхніми класами-реалізаціями.

* ключові моменти: `Iterable` – дозволяє `for-each` цикл. `Collection` – основа для групування. `Map` – окремий інтерфейс.

* що важливо знати на співбесіді: Основна ієрархія, ключові інтерфейси та їх реалізації.

### **✅ Офіційне визначення:**

Інтерфейс **`Collection`** — базовий інтерфейс у **`Java Collections Framework`**, що описує групу об’єктів (елементів).
Його основні підінтерфейси — **`List`**, **`Set`** та **`Queue`**, які деталізують поведінку колекцій залежно від особливостей зберігання та доступу до елементів.

### **🧠 Простими словами:**

**`Collection`** — це як загальна коробка для зберігання речей, а **`List`**, **`Set`**, **`Queue`** — це різні способи організувати ці речі: як список, як унікальний набір або як черга на обробку

---

## **Основне**

| Інтерфейс        | Особливості                                                | Приклади класів реалізації                              |
|------------------|------------------------------------------------------------|---------------------------------------------------------|
| **`Collection`** | Загальний інтерфейс для групи об’єктів                     | —                                                       |
| **`List`**       | Упорядкований список, допускає дублікати                   | **`ArrayList`**, **`LinkedList`**, **`Vector`**         |
| **`Set`**        | Колекція унікальних елементів, без дублікатив              | **`HashSet`**, **`LinkedHashSet`**, **`TreeSet`**       |
| **`Queue`**      | Колекція з принципом **`FIFO`** або спеціальними правилами | **`LinkedList`**, **`PriorityQueue`**, **`ArrayDeque`** |

### **Ієрархія Collection**

![image](/assets/images/Снимок экрана от 2025-06-10 18-35-56.png)

```java
java.lang.Iterable
   └──java.util.Collection
          ├──List
          │      ├──ArrayList
          │      ├──LinkedList
          │      └──Vector
          │
                  ├──Set
          │      ├──HashSet
          │      ├──LinkedHashSet
          │      └──TreeSet
          │
                  └──Queue
                 ├──PriorityQueue
                 ├──

LinkedList(реалізує також Queue)
                 └──ArrayDeque
```

* **`Iterable`** — корінь, який дає можливість "проходити" по елементах.
* **`Collection`** — базовий інтерфейс, що описує групу об’єктів.
* **`List`** — упорядкований список (дозволяє дублікати).
* **`Set`** — унікальний набір (без дублікатів).
* **`Queue`** — структура черги (FIFO або з пріоритетом).
* Класи під кожним інтерфейсом — це конкретні реалізації.

---

## **Пояснення на пальцях (💡)**

* **`List`** — як записник, де можна зберігати записи в порядку, в якому додали, і дублікати — це нормально.
* **`Set`** — як унікальний список гостей на вечірку, де кожен може бути тільки один раз.
* **`Queue`** — як черга в магазині: перший прийшов — перший обслугований.

---

## **Корисні приклади (🧪)**

**List — Список улюблених фруктів (дублікати допускаються)**

```java
import java.util.ArrayList;
import java.util.List;

public class ListExample {
    public static void main(String[] args) {
        List<String> favoriteFruits = new ArrayList<>();
        favoriteFruits.add("Apple");
        favoriteFruits.add("Banana");
        favoriteFruits.add("Apple");  // Дублікати дозволені

        System.out.println(favoriteFruits);
        // Виведе: [Apple, Banana, Apple]
    }
}
```

**Set — Колекція унікальних користувачів за ID (дублікати заборонені)**

```java
import java.util.HashSet;
import java.util.Set;

public class SetExample {
    public static void main(String[] args) {
        Set<Integer> userIds = new HashSet<>();
        userIds.add(101);
        userIds.add(102);
        userIds.add(101);  // Цей дублікати не буде додано

        System.out.println(userIds);
        // Виведе: [101, 102]
    }
}
```

---

**Queue — Черга з завдань на обробку в системі (FIFO)**

```java
import java.util.LinkedList;
import java.util.Queue;

public class QueueExample {
    public static void main(String[] args) {
        Queue<String> taskQueue = new LinkedList<>();
        taskQueue.add("Завдання 1");
        taskQueue.add("Завдання 2");
        taskQueue.add("Завдання 3");

        while (!taskQueue.isEmpty()) {
            String task = taskQueue.poll();  // Витягує і видаляє перший елемент
            System.out.println("Обробляємо: " + task);
        }
        // Виведе:
        // Обробляємо: Завдання 1
        // Обробляємо: Завдання 2
        // Обробляємо: Завдання 3
    }
}
```

---

## **Порівняння (⚖️)**

|      Критерій      |    `List`     |                           `Set`                           |            `Queue`            |
|:------------------:|:-------------:|:---------------------------------------------------------:|:-----------------------------:|
|     Дублікати      |   Дозволені   |                        Заборонені                         |           Дозволені           |
|      Порядок       | Підтримується | Не гарантований (крім **`LinkedHashSet`**, **`TreeSet`**) | Підтримується (FIFO або інше) |
| Доступ до елемента |  За індексом  |                     Немає індексації                      |    Лише через методи черги    |

---

## **🧠 Мнемоніка для запам’ятовування:**

Уяви вечірку (**`Set`**) з гостями (елементами) — жоден гість не може бути двічі. Відкритий записник (**`List`**) з нотатками, де можна повторювати записи. І черга біля каси (**`Queue`**), де обслуговують по порядку

---

## **🟩 Підсумок:**

**`Collection`** — це основа для всіх колекцій в Java, а **`List`**, **`Set`**, **`Queue`** — найпопулярніші її
підінтерфейси з різними правилами роботи з елементами. Знання ієрархії та особливостей реалізацій допомагає вибрати
правильний тип колекції для задачі.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Який інтерфейс знаходиться на вершині ієрархії колекцій, і що він надає?

> #### **💬 Відповідь:**
>
> На самій вершині ієрархії знаходиться інтерфейс **`java.lang.Iterable<T>`**.
>
> **Що він надає:**
> Він надає лише один абстрактний метод: `iterator()`, який повертає об'єкт `Iterator<T>`.
>
> `public interface Iterable<T> { Iterator<T> iterator(); }`
>
> **Навіщо він потрібен:**
> Реалізація цього інтерфейсу дозволяє об'єкту бути "ітерованим", тобто його елементи можна обходити за допомогою **циклу `for-each`**.
>
> ```java
> for (Element e : iterableCollection) {
>     // ...
> }
> ```
>
> Інтерфейс `java.util.Collection<E>` розширює `Iterable<E>`, тому всі стандартні колекції в Java ( `List`, `Set`, `Queue`) підтримують цикл `for-each`.

#### **📌 Що перевіряють:**

*Знання того, що ієрархія починається з `Iterable`, та розуміння його ролі у підтримці синтаксису `for-each`.*

* * *

### **2 ❓ Питання:**

Яку роль відіграє інтерфейс `Collection<E>` і які основні методи він визначає?

> #### **💬 Відповідь:**
>
> **`Collection<E>`** — це кореневий інтерфейс для більшості структур даних у фреймворку. Він визначає **базовий набір операцій** для будь-якої групи об'єктів, яка розглядається як єдине ціле.
>
> Він успадковує `Iterable<E>`.
>
> **Основні методи `Collection<E>`:**
>
> * `boolean add(E e)`: Додає елемент до колекції.
> * `boolean remove(Object o)`: Видаляє одне входження елемента.
> * `int size()`: Повертає кількість елементів.
> * `boolean isEmpty()`: Перевіряє, чи порожня колекція.
> * `boolean contains(Object o)`: Перевіряє наявність елемента.
> * `void clear()`: Видаляє всі елементи.
> * `Iterator<E> iterator()`: Повертає ітератор для обходу.
> * `Object[] toArray()`: Повертає масив з елементів колекції.
> * **Методи для групових операцій:** `addAll()`, `removeAll()`, `retainAll()`, `containsAll()`.
>
> Він **не** включає методи для доступу за індексом, оскільки не всі колекції (наприклад, `Set`) є впорядкованими.

#### **📌 Що перевіряють:**

*Розуміння того, що `Collection` є основою, що визначає найзагальніші операції для всіх типів колекцій.*

* * *

### **3 ❓ Питання:**

Назвіть три основні підінтерфейси `Collection` і їхні ключові відмінності.

> #### **💬 Відповідь:**
>
> Трьома основними підінтерфейсами `Collection` є `List`, `Set` та `Queue`.
>
> * **`List<E>` (Список):**
>   * **Головна властивість:** Впорядкована колекція (послідовність).
>   * **Дублікати:** Дозволяє.
>   * **Доступ:** Підтримує доступ до елементів за **індексом** ( `get(int index)`).
>   * **Реалізації:** `ArrayList`, `LinkedList`, `Vector`.
>
> * **`Set<E>` (Множина):**
>   * **Головна властивість:** Колекція **унікальних** елементів.
>   * **Дублікати:** **Заборонені.** Спроба додати існуючий елемент буде проігнорована.
>   * **Доступ:** Немає доступу за індексом.
>   * **Реалізації:** `HashSet`, `LinkedHashSet`, `TreeSet`.
>
> * **`Queue<E>` (Черга):**
>   * **Головна властивість:** Колекція, призначена для зберігання елементів перед їх обробкою. Зазвичай працює за принципом **FIFO** (First-In, First-Out).
>   * **Дублікати:** Зазвичай дозволяє.
>   * **Доступ:** Доступ тільки до "голови" черги (методи `peek()`, `poll()`, `remove()`).
>   * **Реалізації:** `LinkedList`, `ArrayDeque`, `PriorityQueue`.

#### **📌 Що перевіряють:**

*Чітке знання і розуміння трьох основних видів колекцій та їхніх визначальних характеристик.*

* * *

### **4 ❓ Питання:**

Чому `Map` не успадковує від `Collection`?

> #### **💬 Відповідь:**
>
> `Map` не є нащадком `Collection`, тому що їхні фундаментальні **контракти та структури** кардинально відрізняються.
>
> * `Collection` представляє **групу окремих елементів**. Його основний контракт будується навколо операцій з окремими елементами (`add(E element)`, `contains(Object element)`).
>
> * `Map` представляє набір пар **"ключ-значення" (`<K, V>`)**. Його контракт будується навколо операцій з ключами (`get(Object key)`, `containsKey(Object key)`).
>
> **Ключова несумісність:**
>
> Основний метод `Collection.add(E element)` не має сенсу для `Map`. Що є цим `element`? Ключ? Значення? Чи пара `Map.Entry`? Прийняття будь-якого з цих варіантів порушило б контракт або зробило б його дуже незручним.
>
> Тому розробники Java вирішили, що `Map` є окремою ієрархією. Хоча вона і не є нащадком `Collection`, `Map` все одно вважається частиною **Java Collections Framework** і надає методи (`keySet`, `values`, `entrySet`) для отримання своїх даних у вигляді колекцій.

#### **📌 Що перевіряють:**

*Глибоке розуміння семантичної різниці між цими двома структурами. Це дуже поширене питання для перевірки фундаментальних знань.*

* * *

### **5 ❓ Питання:**

Назвіть по одній основній реалізації для `List`, `Set` та `Map`.

> #### **💬 Відповідь:**
>
> * **Для `List` — `ArrayList`:**
>   * Найбільш поширена реалізація. Внутрішньо використовує масив. Надає швидкий доступ за індексом. Ідеально підходить для більшості випадків, коли основною операцією є читання.
>
> * **Для `Set` — `HashSet`:**
>   * Найбільш поширена реалізація. Внутрішньо використовує хеш-таблицю. Надає найшвидші операції додавання, видалення та перевірки наявності (O(1)). Не гарантує порядку елементів.
>
> * **Для `Map` — `HashMap`:**
>   * Найбільш поширена реалізація. Внутрішньо використовує хеш-таблицю. Надає дуже швидкі операції `put`, `get`, `containsKey` (O(1)). Не гарантує порядку.

#### **📌 Що перевіряють:**

*Знання найпопулярніших "робочих конячок" серед колекцій, які використовуються у 90% випадків.*

* * *

### **6 ❓ Питання:**

Що таке `Deque` і які класи його реалізують? ⭐️

> #### **💬 Відповідь:**
>
> **`Deque`** (вимовляється "дек") розшифровується як **"double-ended queue"** (двостороння черга).
>
> Це підінтерфейс `Queue`, який дозволяє додавати та видаляти елементи **як з початку, так і з кінця** черги.
>
> Завдяки цій властивості `Deque` є ідеальним для реалізації двох структур даних:
>
> 1. **Черги (Queue, FIFO):** Використовуючи `addLast()` (або `offerLast()`) для додавання і `removeFirst()` (або `pollFirst()`) для видалення.
> 2. **Стеку (Stack, LIFO):** Використовуючи `addFirst()` (або `push()`) для додавання і `removeFirst()` (або `pop()`) для видалення.
>
> **Основні реалізації:**
>
> * **`ArrayDeque`:** Реалізація на основі **циклічного масиву**. Є дуже ефективною і **рекомендується** для використання як стек або черга, якщо не потрібна потокобезпечність.
> * **`LinkedList`:** Реалізація на основі **двозв'язного списку**. Також реалізує `Deque`, але зазвичай трохи повільніша за `ArrayDeque` через накладні витрати на об'єкти-вузли.

#### **📝 Коментар:**

Офіційна документація Java рекомендує використовувати реалізації `Deque` (зокрема `ArrayDeque`) замість застарілого класу `Stack`.

#### **📌 Що перевіряють:**

*Знання про існування `Deque` та розуміння його переваг для реалізації стека та черги.*

* * *

### **7 ❓ Питання:**

Поясніть, де в ієрархії знаходяться відсортовані колекції. ⭐️

> #### **💬 Відповідь:**
>
> Відсортовані колекції представляють окремі "гілки" в ієрархії для `Set` та `Map`.
>
> * **Для `Set`:**
>     1. `Set<E>`
>     2. `SortedSet<E>` (розширює `Set`)
>         * Додає контракт, що елементи **впорядковані**. Надає методи для отримання першого/останнього елемента (`first()`, `last()`) та для отримання підмножин (`headSet()`, `tailSet()`, `subSet()`).
>     3. `NavigableSet<E>` (розширює `SortedSet`)
>         * Додає ще більше можливостей для навігації: отримання "найближчого" більшого/меншого елемента (`higher()`, `lower()`, `ceiling()`, `floor()`).
>   * **Основна реалізація:** `TreeSet`.
>
> * **Для `Map`:**
>     1. `Map<K, V>`
>     2. `SortedMap<K, V>` (розширює `Map`)
>         * Гарантує, що ключі **відсортовані**.
>     3. `NavigableMap<K, V>` (розширює `SortedMap`)
>         * Надає методи для навігації за ключами.
>   * **Основна реалізація:** `TreeMap`.

#### **📌 Що перевіряють:**

*Знання про існування спеціалізованих інтерфейсів для відсортованих колекцій та їх основних реалізацій.*

* * *

### **8 ❓ Питання:**

Чим відрізняється поведінка `add()` і `offer()` для `Queue`? А `remove()` і `poll()`? ⭐️

> #### **💬 Відповідь:**
>
> Ці пари методів виконують однакову функцію, але по-різному поводяться в **"помилкових" ситуаціях**, зокрема, при роботі з обмеженими за розміром чергами.
>
> **Додавання елемента:**
>
> * **`add(E e)`**: Якщо операція успішна, повертає `true`. Якщо черга обмежена за розміром і в ній немає місця, цей метод **кидає виняток `IllegalStateException`**.
> * **`offer(E e)`**: Якщо операція успішна, повертає `true`. Якщо в черзі немає місця, він **просто повертає `false`** і не кидає виняток.
>
> **Отримання та видалення елемента:**
>
> * **`remove()`**: Повертає і видаляє елемент з голови черги. Якщо черга **порожня**, він **кидає виняток `NoSuchElementException`**.
> * **`poll()`**: Повертає і видаляє елемент. Якщо черга **порожня**, він **просто повертає `null`** і не кидає виняток.
>
> **Висновок:** Методи `offer`, `poll`, `peek` (останній не кидає виняток, якщо черга порожня) є кращими для програмної обробки, оскільки вони дозволяють уникнути блоків `try-catch`, тоді як `add`, `remove`, `element` є більш "суворими".

#### **📌 Що перевіряють:**

*Уважність до деталей API. Розуміння різниці між методами, що кидають винятки, і методами, що повертають спеціальні значення.*

* * *

### **9 ❓ Питання:**

Назвіть застарілий клас для реалізації стеку і рекомендовану сучасну альтернативу. ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Застарілий клас:** `java.util.Stack`.
>
> * **Рекомендована сучасна альтернатива:** Будь-яка реалізація інтерфейсу **`java.util.Deque`**, наприклад, **`ArrayDeque`**.
>
> **Чому `Stack` є поганим вибором:**
>
> 1. **Поганий дизайн успадкування:** `Stack extends Vector`. `Vector` — це синхронізований, змінюваного розміру масив (схожий на `ArrayList`). Стек (LIFO) семантично **не є** вектором. Це порушує принцип "is-a". Через це успадкування `Stack` отримує купу непотрібних методів, як-от `get(int index)`, що дозволяє доступатися до елементів у середині стека, що порушує його основну ідею.
>
> 2. **Продуктивність:** Оскільки `Stack` наслідує від `Vector`, усі його методи є `synchronized`, що створює непотрібні накладні витрати на блокування в однопотоковому середовищі.
>
> **Чому `ArrayDeque` є кращим:**
>
> * Це **чиста реалізація** інтерфейсу `Deque`.
> * Він **несинхронізований** і дуже **швидкий** (на основі масиву).
> * Надає стандартні методи для стека: `push(e)` (додати наверх) і `pop()` (взяти з верху).
>
> **Приклад:**
> `Deque<Integer> stack = new ArrayDeque<>();`

#### **📌 Що перевіряють:**

*Розуміння поганих дизайнерських рішень у старому JDK. Знання сучасних "best practices" для реалізації поширених структур даних.*

* * *

### **10 ❓ Питання:**

Який зв'язок між `equals/hashCode` та `Set`? Або `Comparable/Comparator` та `TreeSet`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Зв'язок є прямим і визначає, як ці колекції працюють.
>
> * **`equals/hashCode` та `HashSet`/`LinkedHashSet`:**
>   * Ці реалізації `Set` використовують **хеш-таблицю** для зберігання елементів. Щоб забезпечити **унікальність**, вони покладаються на контракт `equals/hashCode`:
>     1. `hashCode()` використовується для визначення "кошика", де має зберігатися елемент.
>     2. `equals()` використовується для порівняння нового елемента з тими, що вже є в цьому кошику, щоб перевірити, чи це не дублікат.
>   * Без коректної реалізації цих методів унікальність не буде гарантована.
>
> * **`Comparable/Comparator` та `TreeSet`:**
>   * `TreeSet` — це **відсортована** множина. Для сортування їй потрібен спосіб **порівнювати** елементи.
>   * Вона покладається на:
>         1. Або на те, що елементи реалізують інтерфейс **`Comparable`** (природний порядок).
>         2. Або на те, що в конструктор `TreeSet` передано об'єкт **`Comparator`** (зовнішня логіка порівняння).
>   * **Унікальність** у `TreeSet` також визначається через порівняння. Якщо `compareTo()` або `compare()` повертає `0`, елементи вважаються рівними. Це означає, що для `TreeSet` **контракт `equals` має бути узгоджений з `compareTo`/`compare`**.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, на які контракти та інтерфейси покладаються різні реалізації колекцій для своєї роботи.*

* * *

### **11 ❓ Питання:**

Що таке "скелетні реалізації" (skeletal implementations) в Collections Framework? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Скелетні реалізації** — це **абстрактні класи**, які надають **часткову (скелетну) реалізацію** основного інтерфейсу колекції.
>
> **Мета:** Зменшити зусилля, необхідні для створення нової, власної реалізації інтерфейсу.
>
> **Приклади:**
>
> * `AbstractCollection`
> * `AbstractList`
> * `AbstractSet`
> * `AbstractMap`
>
> **Як вони працюють (патерн `Template Method`):**
>
> Вони реалізують більшість "складних" або "неосновних" методів інтерфейсу на основі кількох простих, "примітивних" методів, які вони залишають **абстрактними**.
>
> Наприклад, щоб створити свою власну незмінну реалізацію `List`, достатньо успадкувати від `AbstractList` і реалізувати лише `size()` та `get(int index)`. `AbstractList` сам надасть реалізацію для `iterator`, `indexOf`, `hashCode` та інших, використовуючи ваші `size()` та `get()`.
>
> Це значно спрощує процес створення кастомних колекцій.

#### **📌 Що перевіряють:**

*Знання про існування абстрактних класів-помічників та розуміння того, як вони використовують патерн `Template Method`.*

* * *

### **12 ❓ Питання:**

Чому ітератори `ArrayList` вважаються `fail-fast`? Як цей механізм реалізовано? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ітератори `ArrayList` (і більшості колекцій з `java.util`) є **`fail-fast`**, що означає, що вони намагаються виявити конкурентну модифікацію колекції під час ітерації і негайно кидають **`ConcurrentModificationException`**.
>
> **Як це реалізовано:**
>
> 1. В `ArrayList` (і в `AbstractList`) є поле-лічильник: `protected transient int modCount = 0;`.
>     * Цей лічильник **інкрементується** при будь-якій **структурній модифікації** колекції ( `add()`, `remove()`, `clear()` тощо).
>
> 2. Коли ви створюєте ітератор, він запам'ятовує поточне значення `modCount` у власному полі, наприклад, `expectedModCount`.
>     `int expectedModCount = modCount;`
>
> 3. При кожному виклику методів ітератора (`next()`, `hasNext()`, `remove()`) він спочатку перевіряє:
>     `if (modCount != expectedModCount)`
>
> 4. Якщо значення не збігаються (це означає, що колекція була змінена ззовні), ітератор негайно кидає `ConcurrentModificationException`.
>
> **Важливо:** Цей механізм не гарантує виявлення всіх модифікацій, особливо в багатопотоковому середовищі. Він призначений для виявлення програмістських помилок, а не для забезпечення потокобезпечності.

#### **📌 Що перевіряють:**

*Детальне розуміння механізму `fail-fast`, зокрема, ролі `modCount`.*

* * *

### **13 ❓ Питання:**

Що поверне `Arrays.asList("a", "b", "c").add("d")`? Чому? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей код кине **`UnsupportedOperationException`** під час виконання.
>
> **Причина:**
>
> Метод `java.util.Arrays.asList(...)` повертає **не `java.util.ArrayList`**, а екземпляр **приватного статичного вкладеного класу `java.util.Arrays.ArrayList`**.
>
> Цей клас-обгортка реалізує інтерфейс `List`, але **"під капотом" він продовжує посилатися на вихідний масив**.
>
> **Основні властивості цієї обгортки:**
>
> 1. **Розмір фіксований.** Оскільки він базується на масиві, ви не можете змінювати його розмір.
> 2. Методи, що змінюють розмір списку ( `add()`, `remove()`, `clear()`), в цій реалізації **не підтримуються** і просто кидають `UnsupportedOperationException`.
> 3. Зміна елемента через `set()` змінить і сам вихідний масив, якщо він не був створений анонімно.
>
> **Висновок:** `Arrays.asList()` створює **незмінного розміру** представлення масиву у вигляді `List`.

#### **📌 Що перевіряють:**

*Знання про поширену "пастку", пов'язану з `Arrays.asList()`. Уважність до деталей реалізації стандартного API.*

* * *

### **14 ❓ Питання:**

Якби вам потрібно було реалізувати кеш з обмеженням розміру та видаленням найстаріших елементів (LRU Cache), яку існуючу колекцію ви б використали за основу? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Для реалізації LRU (Least Recently Used) кешу ідеально підходить клас **`LinkedHashMap`**.
>
> `LinkedHashMap` надає дві ключові можливості:
>
> 1. **Зберігає порядок вставки** (або, що важливіше, **порядок доступу**). Він підтримує і хеш-таблицю для швидкого доступу (O(1)), і двозв'язний список для збереження порядку.
>
> 2. **Спеціальний конструктор та метод:**
>     * `public LinkedHashMap(int initialCapacity, float loadFactor, boolean accessOrder)`: Якщо `accessOrder` встановлено в `true`, `LinkedHashMap` буде переміщувати елемент, до якого відбувся доступ, у кінець списку. Таким чином, на початку списку завжди будуть "найменш недавно використані" елементи.
>     * `protected boolean removeEldestEntry(Map.Entry<K,V> eldest)`: Цей метод викликається після кожної операції `put()`. Ви можете перевизначити його, щоб він повертав `true`, коли розмір мапи перевищує заданий ліміт, що автоматично призведе до видалення найстарішого елемента.
>
> **Приклад реалізації LRU-кешу:**
>
> ```java
> public class LruCache<K, V> extends LinkedHashMap<K, V> {
>     private final int capacity;
>
>     public LruCache(int capacity) {
>         // `true` вмикає порядок доступу
>         super(capacity, 0.75f, true);
>         this.capacity = capacity;
>     }
>
>     @Override
>     protected boolean removeEldestEntry(Map.Entry<K, V> eldest) {
>         // Видаляти найстаріший елемент, якщо розмір перевищує ємність
>         return size() > capacity;
>     }
> }
> ```
>
> Цей код в кілька рядків реалізує повноцінний LRU-кеш.

#### **📌 Що перевіряють:**

*Знання менш поширених, але дуже потужних можливостей стандартних колекцій. Здатність застосувати їх для вирішення класичних алгоритмічних задач.*

* * *

### **15 ❓ Питання:**

Чи можна безпечно ітерувати по `ArrayList` і видаляти елементи в циклі `for-each`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не можна**. Це призведе до **`ConcurrentModificationException`**.
>
> **Приклад помилки:**
>
> ```java
> List<String> list = new ArrayList<>(List.of("a", "b", "c"));
>
> for (String s : list) { // Цикл for-each неявно створює ітератор
>     if ("b".equals(s)) {
>         list.remove(s); // Модифікація колекції не через ітератор
>     }
> }
> ```
>
> **Чому це не працює:**
>
> 1. Цикл `for-each` "під капотом" створює `Iterator`.
> 2. Коли ви викликаєте `list.remove(s)`, ви змінюєте колекцію напряму, а не через цей ітератор.
> 3. Лічильник `modCount` в `ArrayList` збільшується.
> 4. На наступному кроці циклу ітератор перевіряє `modCount`, бачить, що він змінився, і кидає `ConcurrentModificationException`.
>
> **Правильний спосіб видалення під час ітерації:**
>
> 1. **Використовувати явний `Iterator` та його метод `remove()`:**
>
>     ```java
>     Iterator<String> iterator = list.iterator();
>     while (iterator.hasNext()) {
>         String s = iterator.next();
>         if ("b".equals(s)) {
>             iterator.remove(); // Правильно!
>         }
>     }
>     ```
>
> 2. **Використовувати `removeIf()` (з Java 8):**
>
>     ```java
>     list.removeIf(s -> "b".equals(s)); // Найбільш сучасний і лаконічний спосіб
>     ```

#### **📌 Що перевіряють:**

*Розуміння того, як працює цикл `for-each`, та правил `fail-fast` ітераторів. Знання ідіоматичних способів безпечної модифікації колекцій.*
