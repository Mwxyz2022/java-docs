---
layout: default
title: "Deep Copy vs Shallow Copy"
parent: "Immutable об'єкти"
grand_parent: "Java Core"
nav_order: 6
---

# Deep Copy vs Shallow Copy

* короткий опис: `Shallow copy` (поверхнева копія) – копіює примітиви та посилання на вкладені об'єкти (вкладені об'єкти спільні). `Deep copy` (глибока копія) – рекурсивно копіює всі вкладені об'єкти, створюючи повну незалежну копію.

* поняття: Shallow copy, Deep copy, `clone()`, `defensive copy`.

* приклади: Зміна вкладеного `Address` у `Person` в `shallow copy` впливає на оригінал, тоді як у `deep copy` – ні.

* ключові моменти:  `Deep copy` обов'язкова для `mutable` полів в `immutable` класах, щоб гарантувати повну незмінність.

* що важливо знати на співбесіді: Різниця, коли використовувати, як реалізувати deep copy.

### **✅ Офіційне визначення:**

* **Shallow copy (поверхнева копія)** — це копія об’єкта, яка копіює **тільки сам об’єкт**, але **не копіює вкладені об'єкти** (тільки посилання).
* **Deep copy (глибока копія)** — це копія об’єкта разом з **усіма вкладеними об'єктами**, тобто рекурсивно копіюються всі поля.

### **🧠 Простими словами:**

* **Shallow copy**: копія коробки 📦, але всередині ті самі речі.
* **Deep copy**: копія коробки 📦, і всередині теж усе нове.

---

## **📊 Приклади:**

| Тип копії | Що копіюється | Поведінка | Приклад |
| :---: | :---: | :---: | :---: |
| **`Shallow copy`** | Примітиви, посилання | Об’єкти спільні | **`clone()`** без глибокого копіювання |
| **`Deep copy`** | Примітиви \+ рекурсивні об’єкти | Все нове | Кастомний конструктор, серіалізація |

---

## **Пояснення на пальцях (💡)**

Уяви, що ти робиш копію рецепта 🧾.

* **Shallow copy**: скопіював інструкцію, але **всі інгредієнти — спільні**.
* **Deep copy**: не лише рецепт, а й **нові інгредієнти** — окремі, незалежні.

---

## **Корисні приклади (🧪)**

```java
class Address {
    String city;
    Address(String city) { this.city = city; }
}

class Person implements Cloneable {
    String name;
    Address address;

    Person(String name, Address address) {
        this.name = name;
        this.address = address;
    }

    // Shallow copy
    protected Person clone() throws CloneNotSupportedException {
        return (Person) super.clone(); // address посилання залишається
    }

    // Deep copy
    Person deepCopy() {
        return new Person(name, new Address(address.city));
    }
}
```

---

## **Порівняння (⚖️)**

| Властивість | Shallow Copy | Deep Copy |
| :---: | :---: | :---: |
| **Глибина копії** | Лише зовнішній об’єкт | Усі вкладені об’єкти |
| **Незалежність копії** | Часткова | Повна |
| **Ризик побічних ефектів** | Високий | Мінімальний |
| **Використання в immutable** | Недостатній | Обов’язковий у складних об’єктах |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"SSD" \= Shallow — Shared Data, Deep — Separate Data** 💽

* **S** — Shallow
* **S** — Shared
* **D** — Deep (copy \= different\!)

---

## **🟩 Підсумок:**

* **`Shallow copy`** — це копія об’єкта з тими самими вкладеними посиланнями.
* **`Deep copy`** — це повна, незалежна копія всього стану.
* У контексті **immutable класів**, deep copy **в конструкторах і геттерах** — must-have, щоб уникнути змін через посилання на вкладені об'єкти.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому ключова різниця між поверхневим (`shallow`) та глибоким (`deep`) копіюванням?

> #### **💬 Відповідь:**
>
> Ключова різниця полягає в тому, **як копіюються поля, що є посиланнями на інші об'єкти**.
>
> * **Поверхневе копіювання (Shallow Copy):**
>   * Створюється новий об'єкт, але копіюються лише значення його полів.
>   * Якщо поле є примітивним типом (`int`), копіюється його значення.
>   * Якщо поле є посиланням на інший об'єкт, копіюється **тільки посилання (адреса)**, а не сам об'єкт.
>   * **Результат:** Оригінал і копія **розділяють (share) спільні вкладені об'єкти**.
>
> * **Глибоке копіювання (Deep Copy):**
>   * Створюється повністю незалежна копія.
>   * **Рекурсивно копіюються не тільки посилання, а й самі об'єкти**, на які вони вказують.
>   * **Результат:** Оригінал та копія є повністю ізольованими. Зміна стану вкладеного об'єкта в копії не впливає на оригінал.

#### **📝 Аналогія:**</h4>

У вас є папка з документами.

* **Shallow Copy** — це як створити нову папку і покласти в неї **ярлики**, що вказують на оригінальні документи. У вас дві папки, але обидві працюють з тими самими файлами.
* **Deep Copy** — це як взяти кожен документ, зробити його **ксерокопію** і покласти ці копії в нову папку. У вас дві папки з двома незалежними наборами документів.

#### **📌 Що перевіряють:**

*Розуміння фундаментальної різниці: "копіюється посилання" vs "копіюється об'єкт".*

* * *

### **2 ❓ Питання:**

Наведіть приклад коду, що демонструє побічні ефекти (side effects) поверхневого копіювання.

> #### **💬 Відповідь:**
>
> Розглянемо клас `Person`, який має мутабельне поле `Address`.
>
> ```java
> class Address { public String city; /*...*/ }
>
> class Person implements Cloneable {
>     public String name;
>     public Address address; // мутабельне поле
>
>     public Person clone() throws CloneNotSupportedException {
>         // super.clone() виконує поверхневе копіювання
>         return (Person) super.clone();
>     }
> }
>
> public class ShallowCopyProblem {
>     public static void main(String[] args) throws CloneNotSupportedException {
>         Address address = new Address("Kyiv");
>         Person person1 = new Person("John", address);
>
>         // 1. Створюємо поверхневу копію
>         Person person2 = person1.clone();
>
>         // 2. Змінюємо місто у копії
>         person2.address.city = "Lviv";
>
>         // 3. Перевіряємо місто в оригіналі
>         System.out.println(person1.address.city); // Виведе "Lviv"!
>         // Неочікувано змінився стан оригінального об'єкта!
>     }
> }
> ```
> Проблема виникла, тому що `person1.address` і `person2.address` **посилаються на один і той самий об'єкт `Address`** у пам'яті.

#### **📌 Що перевіряють:**

*Практичне розуміння того, як спільний стан при поверхневому копіюванні призводить до неочікуваних змін.*

* * *

### **3 ❓ Питання:**

Як `Object.clone()` пов'язаний з поверхневим та глибоким копіюванням?

> #### **💬 Відповідь:**
>
> Метод `Object.clone()`, який є основою для механізму клонування в Java, **за замовчуванням завжди виконує поверхневе копіювання (shallow copy)**.
>
> Він створює новий об'єкт і побітово копіює значення всіх полів з оригіналу.
>
> **Щоб реалізувати глибоке копіювання** за допомогою `clone()`, ви **зобов'язані** перевизначити цей метод і після виклику `super.clone()` вручну, рекурсивно клонувати всі мутабельні поля.
>
> **Приклад глибокого копіювання в `clone()`:**
>
> ```java
> @Override
> public Person clone() throws CloneNotSupportedException {
>     // 1. Починаємо з поверхневої копії
>     Person cloned = (Person) super.clone();
>     // 2. Вручну клонуємо мутабельне поле
>     cloned.address = this.address.clone();
>     return cloned;
> }
> ```
>
> Без цього додаткового кроку ваш `clone()` залишиться поверхневим.

#### **📌 Що перевіряють:**

*Знання стандартної поведінки `Object.clone()` та розуміння того, що глибоке копіювання вимагає додаткового коду.*

* * *

### **4 ❓ Питання:**

Коли можна безпечно обійтися поверхневим копіюванням?

> #### **💬 Відповідь:**
>
> Поверхневе копіювання є абсолютно безпечним і достатнім тоді, коли ваш клас складається **виключно з полів примітивних типів та/або імутабельних об'єктів**.
>
> 1. **Примітивні типи** (`int`, `double`, `boolean`...): Їхні значення копіюються, і проблеми спільного стану не виникає.
> 2. **Імутабельні об'єкти** (`String`, `Integer`, `LocalDate`...): Оскільки їхній стан не можна змінити, немає жодного ризику, що зміна одного посилання вплине на інше. Спільне використання посилань на імутабельні об'єкти є безпечним та ефективним.
>
> **Приклад безпечного поверхневого копіювання:**
>
> ```java
> public final class ImmutableData implements Cloneable {
>     private final int id;
>     private final String code; // String є імутабельним
>     // ... super.clone() тут буде достатньо
> }
> ```
> Якщо ж клас містить хоча б одне посилання на **мутабельний** об'єкт ( `ArrayList`, `Date`, власний мутабельний клас), поверхневе копіювання є небезпечним.

#### **📌 Що перевіряють:**

*Розуміння зв'язку між імутабельністю та безпекою поверхневого копіювання.*

* * *

### **5 ❓ Питання:**

Назвіть кілька способів реалізувати глибоке копіювання об'єкта. ⭐️

> #### **💬 Відповідь:**
>
> Існує кілька підходів, кожен зі своїми перевагами та недоліками:
>
> 1. **Перевизначення `clone()` з рекурсивним клонуванням:**
>     * Класичний, але складний підхід. Вимагає, щоб всі класи в ієрархії реалізовували `Cloneable` і коректно викликали `super.clone()`.
>
> 2. **Конструктор копіювання (Copy Constructor):**
>     * **Рекомендований підхід.** Ви створюєте конструктор, який приймає об'єкт того ж класу і вручну копіює його поля, рекурсивно викликаючи конструктори копіювання для вкладених об'єктів. Це більш прозоро і безпечно, ніж `clone()`.
>
>     ```java
>     public Person(Person other) {
>         this.name = other.name;
>         this.address = new Address(other.address); // рекурсивне копіювання
>     }
>     ```
>
> 3. **Використання серіалізації:**
>     * Цей "трюк" полягає в тому, щоб серіалізувати об'єкт у потік байтів, а потім одразу ж десеріалізувати його. Результатом буде повна глибока копія.
>     * **Плюс:** Просто в реалізації.
>     * **Мінус:** Дуже повільно, і всі класи мають реалізовувати `Serializable`.
>
> 4. **Сторонні бібліотеки:**
>     * Бібліотеки, як-от `Apache Commons Lang`, `ModelMapper` або `Jackson` (через перетворення в JSON), надають готові утиліти для глибокого копіювання.

#### **📌 Що перевіряють:**

*Знання різних технік глибокого копіювання та їхніх компромісів (складність vs продуктивність vs простота).*

* * *

### **6 ❓ Питання:**

Що таке захисне копіювання (defensive copy) і як воно пов'язане з імутабельністю? ⭐️

> #### **💬 Відповідь:**
>
> **Захисне копіювання** — це застосування **глибокого копіювання** для захисту **інкапсуляції** та **імутабельності** класу.
>
> Ця техніка використовується, щоб запобігти зміні внутрішнього стану класу через зовнішні посилання на його мутабельні поля.
>
> **Застосовується у двох місцях:**
>
> 1. **У конструкторі:** Приймаючи мутабельний об'єкт як аргумент, потрібно створювати і зберігати його копію, а не саме посилання. Це захищає від подальших змін зовнішнього об'єкта.
>
> 2. **У геттері:** Повертаючи внутрішній мутабельний об'єкт, потрібно повертати його копію (або незмінне представлення), щоб клієнтський код не міг змінити внутрішній стан.
>
> Без захисного копіювання клас, який містить мутабельні поля, **не може бути по-справжньому імутабельним**, навіть якщо всі його поля оголошені як `final`.

#### **📌 Що перевіряють:**

*Розуміння того, що захисне копіювання є обов'язковим елементом для створення надійних імутабельних класів.*

* * *

### **7 ❓ Питання:**

Якщо у вас є `ArrayList<User>`, де `User` — мутабельний клас, що станеться при виклику `new ArrayList<>(originalList)`? ⭐️

> #### **💬 Відповідь:**
>
> Конструктор `new ArrayList<>(originalList)` виконає **поверхневе копіювання**.
>
> **Що це означає:**
>
> 1. Буде створено **новий, незалежний об'єкт `ArrayList`**. Зміни в цьому новому списку (додавання/видалення елементів) не вплинуть на оригінальний список.
>
> 2. **Але** в цей новий список будуть скопійовані **посилання на ті самі об'єкти `User`**, що були в оригінальному списку.
>
> **Наслідки:**
>
> ```java
> User user1 = new User("John");
> List<User> originalList = new ArrayList<>(List.of(user1));
> List<User> copiedList = new ArrayList<>(originalList);
>
> // Отримуємо посилання на User з копії і змінюємо його
> copiedList.get(0).setName("Peter");
>
> // Перевіряємо оригінальний список
> System.out.println(originalList.get(0).getName()); // Виведе "Peter"!
> ```
>
> Оскільки обидва списки містять посилання на один і той самий об'єкт `User`, зміна цього об'єкта через один список буде видна і через інший. Для створення повної копії потрібно було б ітерувати по списку і глибоко копіювати кожен об'єкт `User`.

#### **📌 Що перевіряють:**

*Здатність аналізувати копіювання для вкладених структур (колекція мутабельних об'єктів).*

* * *

### **8 ❓ Питання:**

Як реалізувати глибоке копіювання для масиву об'єктів? `array.clone()` — це глибока чи поверхнева копія? ⭐️

> #### **💬 Відповідь:**
>
> Метод `array.clone()` для масиву об'єктів виконує **поверхневе копіювання**. Він створює новий масив, але копіює в нього посилання на ті самі об'єкти.
>
> **Щоб реалізувати глибоке копіювання масиву**, потрібно вручну створити новий масив і в циклі скопіювати кожен елемент, створюючи для нього також глибоку копію.
>
> **Приклад:**
>
> ```java
> public static User[] deepCopyArray(User[] original) {
>     if (original == null) return null;
>
>     User[] copy = new User[original.length];
>     for (int i = 0; i < original.length; i++) {
>         if (original[i] != null) {
>             // Припускаємо, що User має конструктор копіювання
>             copy[i] = new User(original[i]);
>         }
>     }
>     return copy;
> }
> ```
> Цей підхід гарантує, що новий масив та об'єкти в ньому є повністю незалежними від оригінальних.

#### **📌 Що перевіряють:**

*Розуміння того, як працює `clone()` для масивів, та практичні навички реалізації глибокого копіювання.*

* * *

### **9 ❓ Питання:**

Чому глибоке копіювання може бути проблемою для об'єктів з циклічними посиланнями? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо наївний алгоритм глибокого копіювання зустрічає **циклічні посилання**, він увійде в **нескінченну рекурсію** і впаде з помилкою `StackOverflowError`.
>
> **Приклад циклічного посилання:**
>
> ```java
> class Employee {
>     String name;
>     Employee manager;
> }
>
> Employee alice = new Employee("Alice");
> Employee bob = new Employee("Bob");
>
> alice.manager = bob;
> bob.manager = alice; // Цикл! Alice -> Bob -> Alice
> ```
> **Проблема при копіюванні:**
> 1. Починаємо копіювати `alice`.
> 2. Створюємо копію `alice_copy`.
> 3. Намагаємося скопіювати поле `manager` — це `bob`.
> 4. Рекурсивно викликаємо копіювання для `bob`.
> 5. Створюємо `bob_copy`.
> 6. Намагаємося скопіювати поле `manager` у `bob` — це `alice`.
> 7. Рекурсивно викликаємо копіювання для `alice` (знову!).... і так до `StackOverflowError`.
>
> **Рішення:**
> Потрібен більш складний алгоритм, який **відстежує вже скопійовані об'єкти**. Зазвичай для цього використовується `Map<Original, Copy>`. Перед копіюванням кожного об'єкта алгоритм перевіряє, чи він вже є в мапі. Якщо так — він повертає посилання на існуючу копію. Якщо ні — копіює, додає в мапу і продовжує рекурсію.

#### **📌 Що перевіряють:**

*Здатність аналізувати складні алгоритмічні проблеми (обхід графу з циклами) та пропонувати робочі рішення.*

* * *

### **10 ❓ Питання:**

Що більш продуктивне: глибоке чи поверхневе копіювання? ⭐️

> #### **💬 Відповідь:**
>
> **Поверхневе копіювання є значно більш продуктивним.**
>
> * **Поверхневе копіювання:**
>   * Виконується дуже швидко. Це одна операція створення нового об'єкта та копіювання значень полів (посилань та примітивів). Зазвичай це дуже швидка, низькорівнева операція з пам'яттю.
>
> * **Глибоке копіювання:**
>   * Є **значно повільнішим**. Воно вимагає створення **нового екземпляра для кожного вкладеного об'єкта** по всьому графу.
>   * Це вимагає не тільки часу на виділення пам'яті та виклик конструкторів, але й може призвести до значного навантаження на збирач сміття.
>
> **Висновок:**
> Глибоке копіювання слід використовувати тільки тоді, коли воно **справді необхідне** для забезпечення незалежності та ізоляції стану. У всіх інших випадках, особливо якщо вкладені об'єкти є імутабельними, слід надавати перевагу поверхневому копіюванню заради продуктивності.

#### **📌 Що перевіряють:**

*Розуміння того, що глибоке копіювання має свою "ціну" з точки зору продуктивності та використання пам'яті.*

* * *

### **11 ❓ Питання:**

Припустимо, у вас є об'єкт з полем `transient`. Як це поле поводитиметься при глибокому копіюванні через серіалізацію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Поле, позначене як `transient`, не буде скопійовано.**
>
> Механізм глибокого копіювання через серіалізацію працює наступним чином:
>
> 1. Об'єкт серіалізується (перетворюється на потік байтів).
> 2. **Поля, позначені `transient`**, **ігноруються** і не включаються в цей потік.
> 3. З цього потоку байтів десеріалізується новий об'єкт.
>
> **Результат:**
>
> У новому, скопійованому об'єкті `transient`-поле отримає **значення за замовчуванням** для свого типу ( `null` для об'єктів, `0` для `int`, `false` для `boolean` і т.д.), оскільки інформація про його оригінальне значення була втрачена під час серіалізації.
>
> Це важливий побічний ефект, який потрібно враховувати, обираючи такий спосіб глибокого копіювання.

#### **📌 Що перевіряють:**

*Знання механізму серіалізації та його особливостей, зокрема, поведінки `transient`-полів.*

* * *

### **12 ❓ Питання:**

Як можна використовувати `Stream API` для глибокого копіювання `List` об'єктів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `Stream API` надає елегантний та функціональний спосіб для реалізації глибокого копіювання списку.
>
> Припустимо, у вас є `List<User>`, і клас `User` має **конструктор копіювання** `new User(User other)`.
>
> **Реалізація:**
>
> ```java
> public static List<User> deepCopyList(List<User> originalList) {
>     if (originalList == null) return null;
>
>     return originalList.stream() // 1. Створюємо потік з оригінального списку
>                        .map(User::new)  // 2. Для кожного елемента викликаємо конструктор копіювання
>                        .collect(Collectors.toList()); // 3. Збираємо нові об'єкти в новий список
> }
> ```
>
> **Як це працює:**
>
> 1. `stream()` створює потік з посиланнями на оригінальні об'єкти `User`.
> 2. `map(User::new)` — це ключовий крок. Це посилання на метод еквівалентно `user -> new User(user)`. Для **кожного** елемента `user` у потоці викликається конструктор копіювання, що створює **новий, незалежний** об'єкт `User`.
> 3. `collect(Collectors.toList())` збирає ці нові об'єкти у новий `ArrayList`.
>
> Цей підхід є декларативним, читабельним і повністю реалізує глибоке копіювання списку.

#### **📌 Що перевіряють:**

*Знання `Stream API` та його застосування для розв'язання практичних задач, таких як глибоке копіювання.*

* * *

### **13 ❓ Питання:**

У `java.lang.Object` метод `clone()` є `protected`. Чому?

> #### **💬 Відповідь:**
>
> `clone()` є `protected`, щоб **заборонити довільне клонування** будь-якого об'єкта і змусити розробника **свідомо** увімкнути цю можливість.
>
> Це дизайнерське рішення служить захисним механізмом:
>
> * Якщо б `clone()` був `public` в `Object`, будь-який код міг би спробувати викликати `obj.clone()` для будь-якого об'єкта.
> * Але більшість класів не розраховані на клонування (особливо, якщо вони керують унікальними ресурсами). Такий виклик міг би створити некоректну копію і призвести до помилок.
>
> `protected`-доступ означає, що викликати `super.clone()` можна тільки **всередині самого класу або його нащадків**.
>
> Щоб зробити клас клонованим для зовнішнього світу, розробник зобов'язаний виконати два явних кроки:
>
> 1. Реалізувати інтерфейс `Cloneable`.
> 2. Перевизначити `clone()` і зробити його **`public`**.
>
> Це робить рішення про клонування явним та свідомим.

#### **📌 Що перевіряють:**

*Розуміння ролі модифікаторів доступу у дизайні API.*

* * *

### **14 ❓ Питання:**

Чи завжди `x.clone().getClass() == x.getClass()` буде `true`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, не завжди**, хоча контракт `clone()` це **настійно рекомендує**.
>
> Загальноприйнята конвенція полягає в тому, що `clone()` має повертати об'єкт того ж самого класу. Реалізація `Object.clone()` дотримується цього правила.
>
> Однак, оскільки розробник може **перевизначити `clone()`**, він теоретично може повернути об'єкт **іншого класу** (наприклад, підкласу або просто зовсім іншого типу).
>
> **Приклад (поганий дизайн):**
>
> ```java
> class A implements Cloneable {
>     @Override public Object clone() {
>         // Повертаємо об'єкт іншого класу!
>         return new B();
>     }
> }
> class B {}
>
> A a = new A();
> // a.clone().getClass() == B.class
> // a.getClass() == A.class
> // a.clone().getClass() == a.getClass() -> false!
> ```
> Це вважається **порушенням неявного контракту** і поганою практикою, оскільки клієнтський код, що викликає `clone()`, зазвичай очікує отримати об'єкт того ж класу. Але технічно це можливо.

#### **📌 Що перевіряють:**

*Розуміння того, що `clone()` є звичайним методом, і його поведінка повністю залежить від реалізації, яка може відхилятися від загальноприйнятих конвенцій.*

* * *

### **15 ❓ Питання:**

Чому імутабельність усуває потребу в глибокому копіюванні?

> #### **💬 Відповідь:**
>
> Імутабельність усуває потребу в глибокому копіюванні, тому що вона **усуває саму проблему**, яку вирішує глибоке копіювання — **проблему спільного змінюваного стану**.
>
> * **Глибоке копіювання** потрібне для того, щоб створити повну, ізольовану копію об'єкта, щоб зміни в одному не впливали на інший.
>
> * **Імутабельний об'єкт**, за визначенням, **не може бути змінений**.
>
> Отже, якщо всі вкладені об'єкти є імутабельними, немає жодного ризику, що хтось змінить їхній стан. **Спільне використання посилань** на імутабельні об'єкти є абсолютно **безпечним**.
>
> **Розглянемо наш `Person` з `Address`:**
>
> * Якщо `Address` є **мутабельним**, нам потрібне глибоке копіювання `Address`.
> * Якщо `Address` є **імутабельним**, нам достатньо **поверхневого копіювання**. Ми просто копіюємо посилання на той самий, незмінний об'єкт `Address`, і це безпечно та ефективно.
>
> Це одна з найпотужніших переваг імутабельності — вона значно спрощує міркування про стан та копіювання об'єктів.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як імутабельність і глибоке копіювання є двома різними підходами до вирішення однієї і тієї ж проблеми — ізоляції стану.*
