---
layout: default
title: "Чому immutable клас повинен бути final"
parent: "Immutable об'єкти"
grand_parent: "Java Core"
nav_order: 3
---

# Чому immutable клас повинен бути final

* короткий опис: `Immutable` клас повинен бути `final`, щоб заборонити його успадкування. Це запобігає можливості підкласам порушити його незмінність, додавши `mutable` логіку або перевизначивши методи.

* поняття: `final class`, наслідування, поліморфізм, інваріанти.

* приклади: Підклас може перевизначити геттер, додавши `mutable` поведінку, що "зламає" ілюзію незмінності.

* ключові моменти:  `final` – єдина гарантія незмінності поведінки класу. Поліморфізм може бути "діркою" у безпеці, якщо клас не `final`.

* що важливо знати на співбесіді: Як підклас може "зламати" незмінність, важливість `final` для безпеки.

### **✅ Офіційне визначення:**

**`final`**\-клас — це клас, який **не може бути успадкований**. У контексті immutable-класів це гарантує, що поведінка та інваріанти об'єкта **не будуть змінені через спадкування**.

### **🧠 Простими словами:**

**`final`** — це як бетонна кришка на сейфі: ніхто не зможе підкинути додаткові методи або змінити старі, щоб «зламати» незмінність. Без **`final`** навіть найлогічніший immutable-клас може стати mutable через *"хитре успадкування"*

---

## **Основні причини важливості final при створенні immutable класу**

**📊 Ключові поняття:**

* **Успадкування** — підкласи можуть додавати або змінювати поведінку.
* **Перевизначення** — **`override`** методів може вплинути на поведінку класу.
* **Поліморфізм** — при виклику методів може бути використана версія з підкласу, що порушує інваріанти.

**📌 Чому це небезпечно:**

| Явище | Проблема при відсутності `final` |
| :---: | :---: |
| **Успадкування** | Дозволяє змінити логіку базового класу |
| **Перевизначення методів** | Можна вставити mutable-логіку |
| **Поліморфізм** | Працює під виглядом immutable, але ззовні — mutable |

---

## **Пояснення на пальцях (💡)**

Уяви immutable клас як сейф без ключа — все зашито. Але якщо хтось успадкує цей клас, вставить дверцята збоку (через override) — і вуаля, доступ до внутрішніх даних відкрито. Сам об'єкт виглядає як сейф, але хтось уже підключив чорний хід

---

## **Корисні приклади (🧪)**

```java
public class Immutable {
    private final String data;

    public Immutable(String data) {
        this.data = data;
    }

    public String getData() {
        return data;
    }
}

// ❗ Підклас змінює логіку "immutable" класу
public class Mutable extends Immutable {
    private String hackedData;

    public Mutable(String data) {
        super(data);
        this.hackedData = data;
    }

    @Override
    public String getData() {
        // ❗ Поведінка змінена
        return hackedData + " 😈";
    }

    public void setData(String newData) {
        this.hackedData = newData;
    }
}

// Використання
Immutable obj = new Mutable("Safe?");
System.out.println(obj.getData()); // --> Safe? 😈
```

📌 Незважаючи на те, що **`obj`** має тип **`Immutable`**, метод **`getData`** викликається з підкласу, який додає змінність

---

## **Порівняння (⚖️)**

|  | Клас `final` | Клас без `final` |
| :---: | :---: | :---: |
| **Успадкування** | Заборонено | Дозволено |
| **Зламати інваріанти** | Неможливо | Можна через підкласи |
| **Перевизначення** | Немає | Доступно через **`@Override`** |
| **Гарантія незмінності** | Повна | Вразлива |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"FIRM"** — immutable має бути твердим, як бетон 🧱

* **F**inal class
* **I**nvariant behavior
* **R**estricted inheritance
* **M**utable access — заблокований

---

## **🟩 Підсумок:**

* Клас без **`final`** може бути успадкований, і через підкласи його незмінність зламується.
* **`final`** — це гарантія, що ніхто не змінить логіку, не додасть mutable-методів.
* Використання **`final`** — обов’язковий етап при проектуванні справжнього immutable класу.
* Поліморфізм — потужний інструмент, але в контексті immutable класів він — потенційна дірка.

---

### **🔗 Додаткові джерела:**

* [**Why should immutable classes be final? — StackOverflow**](https://stackoverflow.com/questions/4031048/why-should-immutable-objects-be-final)
* [**Java Concurrency in Practice — Immutable Objects**](https://jcip.net/)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Чому імутабельний (immutable) клас рекомендується оголошувати як `final`?

> #### **💬 Відповідь:**
>
> Оголошення класу як `final` **забороняє успадкування**, і це є критично важливим для гарантування імутабельності з наступних причин:
>
> 1. **Запобігання перевизначенню методів:** Підклас може перевизначити методи базового класу таким чином, що вони почнуть змінювати стан або повертати мутабельні представлення, руйнуючи всі гарантії імутабельності.
>
> 2. **Захист від додавання мутабельного стану:** Підклас може додати власні мутабельні поля і методи для їх зміни. Якщо об'єкт такого підкласу буде використовуватися там, де очікується імутабельний базовий клас, це призведе до непередбачуваної поведінки.
>
> 3. **Збереження інваріантів:** `final` гарантує, що контракт і інваріанти, закладені в імутабельному класі, не будуть порушені його нащадками.
>
> Таким чином, `final` — це захисний бар'єр, який гарантує, що поведінка вашого імутабельного класу не може бути змінена ззовні через механізми успадкування.

#### **📝 Аналогія:**</h4>

Це як запаяти сейф назавжди. Якщо ви не запаяєте його (`final`), хтось може приварити до нього власні дверцята (`підклас`) і отримати доступ до вмісту.

#### **📌 Що перевіряють:**

*Розуміння того, що успадкування може бути вектором атаки на імутабельність, і як `final` цьому запобігає.*

* * *

### **2 ❓ Питання:**

Наведіть приклад, як саме підклас може "зламати" імутабельність, якщо базовий клас не є `final`. ⭐️

> #### **💬 Відповідь:**
>
> Розглянемо простий "імутабельний" клас `BaseValue`, який не є `final`.
>
> ```java
> public class BaseValue {
>     private final int value;
>
>     public BaseValue(int value) {
>         this.value = value;
>     }
>
>     public int getValue() {
>         return value;
>     }
> }
> ```
>
> Тепер створимо "зловмисний" мутабельний підклас:
>
> ```java
> public class MutableSubclass extends BaseValue {
>     private int currentValue;
>
>     public MutableSubclass(int value) {
>         super(value);
>         this.currentValue = value;
>     }
>
>     // Перевизначаємо геттер, щоб він повертав змінне поле
>     @Override
>     public int getValue() {
>         return currentValue;
>     }
>
>     // Додаємо сеттер, який руйнує імутабельність
>     public void setValue(int newValue) {
>         this.currentValue = newValue;
>     }
> }
> ```
>
> **Демонстрація проблеми:**
>
> ```java
> public void processValue(BaseValue val) {
>     System.out.println("Початкове значення: " + val.getValue());
>     // Тут ми очікуємо, що val незмінний
>     // Але якщо val насправді є MutableSubclass...
>     if (val instanceof MutableSubclass) {
>         ((MutableSubclass) val).setValue(999);
>     }
>     System.out.println("Кінцеве значення: " + val.getValue()); // Значення несподівано змінилося!
> }
>
> BaseValue value = new MutableSubclass(10);
> processValue(value); // Виведе: Початкове значення: 10, Кінцеве значення: 999
> ```
>
> Поліморфізм дозволяє передати мутабельний об'єкт туди, де очікується імутабельний, що є серйозною проблемою.

#### **📌 Що перевіряють:**

*Здатність на практичному прикладі продемонструвати, як перевизначення методів у підкласі може порушити контракт суперкласу.*

* * *

### **3 ❓ Питання:**

Якщо всі поля в класі є `private` і `final`, чи достатньо цього для імутабельності, навіть якщо клас не `final`?

> #### **💬 Відповідь:**
>
> Ні, **недостатньо**.
>
> Навіть якщо всі поля базового класу є `private final`, підклас все одно може **додати свої власні мутабельні поля** і змінити поведінку, перевизначивши методи.
>
> У прикладі з попереднього питання, поля `value` у `BaseValue` було `private final`, і ми все одно змогли обійти це обмеження, перевизначивши `getValue()` і додавши нове поле `currentValue` в `MutableSubclass`.
>
> **Головна проблема:** без `final` на класі ви не можете контролювати, що роблять його нащадки. Навіть якщо базовий клас спроектований ідеально, будь-хто може створити підклас, який не дотримується цих правил. Тому **єдиною справжньою гарантією** є заборона успадкування.

#### **📌 Що перевіряють:**

*Розуміння того, що імутабельність стосується поведінки об'єкта в цілому, а не тільки стану його базових полів.*

* * *

### **4 ❓ Питання:**

Чи є якісь альтернативи тому, щоб робити клас `final`, для забезпечення імутабельності? ⭐️

> #### **💬 Відповідь:**
>
> Так, є альтернативний підхід, хоча він і більш складний. Замість того, щоб робити клас `final`, можна зробити **всі його методи `final`**, а **конструктор зробити `private` або `package-private`**, і створювати екземпляри тільки через **статичні фабричні методи**.
>
> **Приклад:**
>
> ```java
> public class AlternativeImmutable {
>     private final int value;
>
>     // Приватний конструктор
>     private AlternativeImmutable(int value) {
>         this.value = value;
>     }
>
>     // Всі публічні методи є final
>     public final int getValue() {
>         return value;
>     }
>
>     // Об'єкти створюються тільки через фабрику
>     public static AlternativeImmutable of(int value) {
>         return new AlternativeImmutable(value);
>     }
> }
> ```
>
> **Як це захищає:**
>
> * Оскільки конструктор приватний, зовнішній код (в іншому пакеті) не може створити підклас.
> * `final`-методи не можна перевизначити.
>
> **Недоліки цього підходу:**
>
> * Він складніший.
> * Якщо клас має підкласи в тому ж пакеті, вони все ще можуть успадкувати його, якщо конструктор є `package-private`.
>
> Тому зробити сам клас `final` є значно простішим і надійнішим способом досягти тієї ж мети.

#### **📌 Що перевіряють:**

*Розуміння альтернативних, менш поширених підходів до дизайну. Здатність аналізувати компроміси різних рішень.*

* * *

### **5 ❓ Питання:**

Як імутабельність і `final` на класі впливають на принцип підстановки Лісков (LSP)? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Імутабельність та `final` **захищають** принцип підстановки Лісков.
>
> **LSP** стверджує, що об'єкт підкласу має поводитися так само, як об'єкт базового класу, не змінюючи його інваріантів та коректності програми.
>
> **Як порушується LSP без `final`:**
> У нашому прикладі з `BaseValue` та `MutableSubclass`:
>
> * `BaseValue` має неявний контракт: "моє значення незмінне".
> * `MutableSubclass` **порушує** цей контракт, додаючи можливість змінювати значення.
> * Тому `MutableSubclass` не є коректною "заміною" для `BaseValue` в будь-якому контексті, що покладається на імутабельність.
>
> **Як `final` допомагає:**
> Коли ви оголошуєте клас як `final`, ви **унеможливлюєте створення будь-яких підкласів**. Це означає, що ніхто не може створити "неправильну" заміну для вашого класу. Таким чином, ви гарантуєте, що LSP для вашого класу ніколи не буде порушено через успадкування.

#### **📌 Що перевіряють:**

*Здатність пов'язувати практичні рекомендації (робити клас `final`) з фундаментальними принципами ООП-дизайну (SOLID).*

* * *

### **6 ❓ Питання:**

Якщо імутабельний клас реалізує інтерфейс, чи може `final` на класі якось зашкодити?

> #### **💬 Відповідь:**
>
> Ні, `final` на класі ніяк не зашкодить. Навпаки, це є хорошою практикою.
>
> Якщо у вас є інтерфейс, ви можете мати кілька його реалізацій, і деякі з них можуть бути імутабельними, а деякі — мутабельними.
>
> **Приклад:**
>
> ```java
> interface Point {
>     int getX();
>     int getY();
> }
>
> // Імутабельна реалізація
> public final class ImmutablePoint implements Point { /* ... */ }
>
> // Мутабельна реалізація
> public class MutablePoint implements Point { /* ... */ }
> ```
>
> `final` тут є частиною **контракту реалізації `ImmutablePoint`** і ніяк не обмежує сам інтерфейс чи інші його реалізації.
>
> Це дозволяє клієнтському коду, що працює з інтерфейсом `Point`, отримувати різні реалізації. Якщо клієнту потрібна гарантія незмінності, він може перевірити `point instanceof ImmutablePoint`.
>
> Тому використання `final` для імутабельної реалізації інтерфейсу є абсолютно коректним і рекомендованим.

#### **📌 Що перевіряють:**

*Розуміння того, як взаємодіють `final`, імутабельність та програмування через інтерфейси.*

* * *

### **7 ❓ Питання:**

Чому `String` в Java оголошений як `final`?

> #### **💬 Відповідь:**
>
> Клас `String` є `final` саме для того, щоб гарантувати його **імутабельність** та **безпеку**.
>
> Якби `String` не був `final`, хтось міг би успадкувати від нього і перевизначити його методи, що призвело б до серйозних проблем:
>
> 1. **Порушення `String Pool`:** Підклас міг би змінити стан рядка, що призвело б до хаосу, оскільки пул посилається на один і той самий об'єкт для однакових літералів.
>
> 2. **Порушення безпеки:**
>     * Уявіть, що метод перевіряє SQL-запит на ін'єкції. Якщо `String` був би мутабельним, зловмисний підклас міг би "пройти" перевірку, а потім змінити свій вміст на шкідливий.
>
> 3. **Порушення контракту `equals()` та `hashCode()`:** Підклас міг би змінити логіку порівняння або хешування, що зруйнувало б роботу `HashMap`.
>
> Роблячи `String` `final`, розробники Java **захистили** ці фундаментальні механізми від будь-яких можливих модифікацій через успадкування.

#### **📌 Що перевіряють:**

*Застосування загальних знань про імутабельність до найважливішого класу в Java — `String`.*

* * *

### **8 ❓ Питання:**

Якщо ви не можете зробити клас `final` (наприклад, через вимоги фреймворку), які є альтернативні способи захисту імутабельності? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Якщо успадкування заборонити не можна, потрібно ускладнювати захист. Ось кілька підходів:
>
> 1. **Приватний конструктор + статична фабрика:**
>     * Зробити всі конструктори `private` або `package-private`. Це обмежить можливість створення підкласів (підкласи з інших пакетів не зможуть викликати `super()`).
>     * Надавати екземпляри через `public static` фабричні методи.
>     * Це ефективний спосіб, але він обмежує гнучкість.
>
> 2. **Додавання перевірок у `equals`:**
>     * У методі `equals` використовувати строгу перевірку **`getClass() == obj.getClass()`** замість `instanceof`. Це не дозволить об'єкту базового класу бути рівним об'єкту підкласу, що запобігає порушенню симетричності.
>
> 3. **Зробити методи, що повертають внутрішній стан, `final`:**
>     * Це захистить геттери від перевизначення. Наприклад, `public final List<String> getItems() { return Collections.unmodifiableList(items); }`.
>
> Жоден з цих способів не дає такої **залізної гарантії**, як `final` на класі, оскільки підклас все ще може додати свій мутабельний стан. Але вони значно ускладнюють "атаку" на імутабельність.

#### **📌 Що перевіряють:**

*Здатність думати про альтернативні механізми захисту, коли ідеальне рішення недоступне. Розуміння компромісів.*

* * *

### **9 ❓ Питання:**

Чи робить `private`-конструктор клас імутабельним?

> #### **💬 Відповідь:**
>
> Ні, **сам по собі — не робить**.
>
> Приватний конструктор є інструментом для контролю над створенням екземплярів. Він є **необхідною частиною** патерну `Singleton`, але не гарантує імутабельності.
>
> **Що він робить:**
>
> * Забороняє створення екземплярів класу ззовні через `new`.
> * Ускладнює успадкування.
>
> **Чого він не робить:**
>
> * Він не забороняє наявність **сеттерів** або інших методів, які змінюють стан.
> * Він не робить поля `final`.
>
> ```java
> public class MutableWithPrivateConstructor {
>     private String name;
>
>     private MutableWithPrivateConstructor() {}
>
>     // Цей клас не імутабельний
>     public void setName(String name) { this.name = name; }
>
>     public static MutableWithPrivateConstructor create() { return new MutableWithPrivateConstructor(); }
> }
> ```
>
> Таким чином, приватний конструктор — це лише один з елементів, який може використовуватися для створення імутабельного класу (зазвичай у поєднанні зі статичними фабриками), але він не є гарантією.

#### **📌 Що перевіряють:**

*Розмежування між контролем над створенням об'єктів та контролем над їхнім станом.*

* * *

### **10 ❓ Питання:**

Розглянемо `record` з Java 16. Чи є вони завжди імутабельними і чи потрібно їх оголошувати `final`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **Чи є вони `final`?**
>   * Так, `record` **неявно є `final`**. Ви не можете успадкувати від `record`. Це правило вбудовано в специфікацію мови, тому його `final`-природа гарантована.
>
> * **Чи є вони завжди імутабельними?**
>   * **Не завжди.** `record` створений для того, щоб бути "прозорим носієм для незмінних даних", і його поля є неявно `final`. Це забезпечує **поверхневу імутабельність**.
>   * **Однак**, якщо `record` містить поле, що є посиланням на **мутабельний об'єкт**, то `record` **не є по-справжньому імутабельним**, якщо не вжити заходів.
>
>     **Приклад:**
>     `public record Order(int id, List<String> items) {}`
>     Тут ви можете отримати `items` і змінити список: `order.items().add("new item");`.
>
>     **Рішення:** Потрібно **вручну реалізувати захисне копіювання** в канонічному конструкторі та перевизначити метод доступу, щоб він повертав копію.
>
>     ```java
>     public record Order(int id, List<String> items) {
>         public Order {
>             items = List.copyOf(items); // захисна копія
>         }
>         @Override public List<String> items() {
>             return Collections.unmodifiableList(this.items);
>         }
>     }
>     ```
>
> Таким чином, `record` дає вам імутабельність "з коробки" тільки для полів примітивних або імутабельних типів.

#### **📌 Що перевіряють:**

*Розуміння того, що `record` вирішує проблему шаблонного коду, але не проблему глибокої імутабельності.*

* * *

### **11 ❓ Питання:**

У чому може бути проблема з імутабельністю, якщо клас містить кеш для обчислюваного значення? `private int cachedHashCode;` ⭐️⭐️

> #### **💬 Відповідь:**
>
> Наявність кешу в імутабельному класі **не порушує імутабельність**, за умови, що це зроблено **правильно**.
>
> **Правильна реалізація:**
> Кешоване значення має обчислюватися **ліниво** і тільки **один раз**. Воно не повинно змінювати "логічний" стан об'єкта, видимий для клієнта.
>
> ```java
> public final class MyImmutable {
>     private final String data;
>     private volatile int hashCodeCache; // volatile для потокобезпечності
>
>     @Override
>     public int hashCode() {
>         int result = hashCodeCache;
>         if (result == 0) {
>             result = data.hashCode();
>             hashCodeCache = result;
>         }
>         return result;
>     }
> }
> ```
>
> Тут, хоча поле `hashCodeCache` не є `final`, **логічний стан об'єкта** (`data`) залишається незмінним. `hashCode()` для клієнта завжди буде повертати однакове значення. Це вважається валідною реалізацією. Клас `String` використовує подібний підхід.
>
> **Проблема може виникнути**, якщо логіка кешування реалізована неправильно і може бути змінена ззовні, або якщо кешоване значення не є детермінованим. Але сама ідея кешу в імутабельному об'єкті є допустимою.

#### **📌 Що перевіряють:**

*Здатність розрізняти "логічний" стан об'єкта (видимий клієнту) та його "внутрішній" стан (деталі реалізації).*

* * *

### **12 ❓ Питання:**

Як `final` на полі пов'язаний з Java Memory Model та потокобезпечністю імутабельних об'єктів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ключове слово `final` відіграє критично важливу роль у **гарантіях видимості (visibility)** в Java Memory Model.
>
> JMM надає спеціальні гарантії для `final`-полів:
>
> **Якщо об'єкт створено коректно (тобто посилання `this` не "витікає" з конструктора), то після того, як конструктор завершить свою роботу, всі потоки гарантовано побачать правильно ініціалізовані значення всіх його `final`-полів.**
>
> **Що це означає:**
>
> 1. Немає ризику побачити значення `final`-поля за замовчуванням (`0` чи `null`).
> 2. Для публікації імутабельного об'єкта між потоками не потрібна додаткова синхронізація (`volatile` або `synchronized`), щоб інші потоки побачили його стан.
>
> Якщо ж поля не були б `final`, то не було б жодних гарантій. Один потік міг би створити об'єкт, а інший — побачити його поля з неініціалізованими або застарілими значеннями.
>
> Таким чином, `final` є фундаментальною основою, яка робить імутабельні об'єкти потокобезпечними "з коробки".

#### **📌 Що перевіряють:**

*Дуже глибокі знання про Java Memory Model. Розуміння зв'язку між `final`, конструкторами та гарантіями видимості.*

* * *

### **13 ❓ Питання:**

Якщо ваш клас містить поле типу `String[]`, чи достатньо зробити це поле `private final` і повернути `array.clone()` у геттері для імутабельності? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **недостатньо**, якщо самі елементи масиву є **мутабельними**.
>
> Метод `array.clone()` для масиву об'єктів виконує **поверхневе копіювання (`shallow copy`)**. Він створює новий масив, але **копіює в нього посилання** на ті самі об'єкти.
>
> **Приклад з `StringBuilder[]`:**
>
> ```java
> public final class NotImmutable {
>     private final StringBuilder[] data;
>
>     public NotImmutable(StringBuilder[] data) {
>         this.data = data.clone(); // Копія масиву, але не об'єктів
>     }
>
>     public StringBuilder[] getData() {
>         return data.clone();
>     }
> }
>
> // ...
> StringBuilder[] originalData = {new StringBuilder("a")};
> NotImmutable obj = new NotImmutable(originalData);
>
> obj.getData()[0].append("b"); // Ми змінюємо стан об'єкта всередині!
>
> System.out.println(originalData[0]); // все ще "a"
> System.out.println(obj.getData()[0]); // "ab"! Внутрішній стан змінився!
> ```
>
> Для **`String[]`** цей підхід **спрацює**, тому що `String` є імутабельним. Змінити стан рядка неможливо.
>
> Але для **масиву мутабельних об'єктів** для повної імутабельності потрібно було б виконати **глибоке копіювання** — створити новий масив і вручну скопіювати кожен елемент, викликаючи для нього `clone()` (якщо він клонований).

#### **📌 Що перевіряють:**

*Дуже глибоке розуміння того, що таке поверхневе та глибоке копіювання, особливо у контексті масивів.*

* * *

### **14 ❓ Питання:**

Що, на вашу думку, важливіше для гарантії імутабельності: `final` на класі чи `final` на полях? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Хоча обидва є важливими частинами "ідеальної" імутабельності, якщо потрібно вибрати щось одне, то **`final` на полях (у поєднанні з приватним доступом та відсутністю сеттерів) є більш фундаментальним**.
>
> **Чому?**
>
> * `final` на полях напряму забезпечує **незмінність стану** об'єкта на рівні самого класу. Він забороняє переприсвоєння посилань, що є основою.
>
> * `final` на класі є **захистом від зовнішніх загроз** — він захищає від порушення інваріантів через успадкування. Це важливий захисний шар, але він захищає **поведінку**, а не сам **стан**.
>
> Можна уявити ситуацію (хоча це поганий дизайн), де клас не є `final`, але його поля `private final`, і він все ще є "умовно імутабельним", поки ніхто не створює "зловмисних" нащадків.
>
> Але неможливо уявити імутабельний клас, поля якого не є `final` і можуть бути змінені.
>
> **Висновок:** `final` на полях — це про внутрішню цілісність. `final` на класі — це про захист від зовнішнього світу. Обидва потрібні для повної гарантії.

#### **📌 Що перевіряють:**

*Здатність аналізувати та пріоритезувати різні аспекти дизайну. Розуміння того, що саме є ядром імутабельності.*

* * *

### **15 ❓ Питання:**

Чому `record` в Java є неявним `final`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `record` є неявним `final` з кількох пов'язаних причин, що випливають з його основного призначення.
>
> **Основна мета `record`:** Бути "прозорим, незмінним носієм для даних".
>
> 1. **Гарантія семантичного контракту:** Компілятор автоматично генерує `equals()`, `hashCode()` та `toString()` на основі **всіх** компонентів запису. Якби від `record` можна було успадковувати і додавати нові поля, цей автоматично згенерований контракт був би порушений. Підклас мав би новий стан, який не враховується в `equals`/`hashCode` батька, що призвело б до порушення їхнього контракту.
>
> 2. **Забезпечення імутабельності:** Успадкування є одним із способів "зламати" імутабельність. Роблячи `record` `final`, специфікація мови закриває цю можливість "за замовчуванням".
>
> 3. **Простота та передбачуваність:** `record` було створено, щоб бути простим `data-carrier`. Заборона на успадкування робить їхню поведінку абсолютно передбачуваною і усуває цілий клас потенційних проблем, пов'язаних з ієрархіями. Ви завжди можете бути впевнені, що `record Point` — це просто `Point`, а не якийсь його хитрий нащадок.
>
> Таким чином, `final` для `record` — це не обмеження, а **ключова дизайнерська риса**, що забезпечує їхню надійність та простоту.

#### **📌 Що перевіряють:**

*Розуміння філософії дизайну, що стоїть за новими можливостями мови, як-от `record`.*
