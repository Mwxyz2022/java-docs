---
layout: default
title: "Mutable vs Immutable: порівняння"
parent: "Immutable об'єкти"
grand_parent: "Java Core"
nav_order: 5
---

# Mutable vs Immutable: порівняння

* короткий опис: `Mutable` об'єкт – його стан можна змінювати після створення (`StringBuilder`, `ArrayList`). `Immutable` об'єкт – його стан не можна змінити після створення, усі зміни створюють новий об'єкт (`String`, `Integer`).

* поняття: `mutable`, `immutable`.

* приклади: `StringBuilder` (mutable) для ефективної конкатенації, `String` (immutable) для безпеки та кешування.

* ключові моменти:  `Immutable` – `thread-safe`, ідеальні для ключів та передачі між потоками. `Mutable` – ефективніші для частих in-place змін.

* що важливо знати на співбесіді: Переваги/недоліки обох, коли що обирати.

### **✅ Офіційне визначення:**

* **Mutable об’єкт** — це об’єкт, **стан якого можна змінювати** після створення.
* **Immutable об’єкт** — це об’єкт, **стан якого не можна змінити** після створення. Усі зміни призводять до створення нового об’єкта.

### **🧠 Простими словами:**

* **Mutable** \= "пластилін" — можна ліпити скільки завгодно.
* **Immutable** \= "камінь" — раз виліпив, і все. Хочеш зміни — бери новий камінь.

---

## **Основне**

| Категорія | Mutable | Immutable |
| :---: | :---: | :---: |
| **Типи в Java** | **`StringBuilder, ArrayList`** | **`String, Integer, LocalDate`** |
| **Поведінка** | змінює внутрішній стан | не змінює, повертає новий |
| **Потікобезпека** | не є безпечними за замовчуванням | є безпечними (за дизайном) |
| **Ефективність** | швидше при частих змінах | краще при читанні, кешуванні |

---

## **Пояснення на пальцях (💡)**

Уяви **`String`** як сторінку книги 📖: якщо ти хочеш змінити слово — ти **переписуєш нову сторінку**.

А **`StringBuilder`** — це чернетка 📝: ти просто стираєш і переписуєш слово

---

## **Корисні приклади (🧪)**

**Immutable: `String`**

```java
String s1 = "Hello";
String s2 = s1.concat(" World");

System.out.println(s1); // Hello
System.out.println(s2); // Hello World
```

**Mutable: `StringBuilder`**

```java
StringBuilder sb = new StringBuilder("Hello");
sb.append(" World");

System.out.println(sb); // Hello World
```

🧠 **`String`** не змінюється — створюється новий об'єкт.  
🛠️ **`StringBuilder`** змінюється in-place.

---

## **Порівняння (⚖️)**

| Властивість | Mutable | Immutable |
| :---: | :---: | :---: |
| **Стан** | Може змінюватись | Незмінний |
| **Потікобезпека (thread-safe)** | ❌ (потрібна синхронізація) | ✅ (safe by design) |
| **Продуктивність** | Краще при багатьох змінах | Краще при читанні та кешуванні |
| **Використання в колекціях** | Небезпечно як ключ | Ідеальні як ключі (**`Map`**, **`Set`**) |
| **Приклад** | **`ArrayList, StringBuilder`** | **`String, Integer, BigDecimal`** |
| **Підходить для** | Частих змін, буферів | Безпеки, обміну між потоками |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"IMMU" — Immutable \= My Memory Under control** 💾

* **I** — Ізольованість
* **M** — Мінімізація побічних ефектів
* **M** — Можливість безпечного доступу з багатьох потоків
* **U** — Unchangeable (ідеально для ключів\!)

---

## **🟩 Підсумок:**

* Mutable об'єкти дозволяють змінювати стан, але вимагають обережності в багатопотокових середовищах.
* Immutable об'єкти — стабільні, потікобезпечні, ідеальні для передачі, кешування та використання у колекціях.
* Обирай **mutable**, якщо часто змінюєш стан, і **immutable**, коли важлива стабільність та передбачуваність.

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

У чому фундаментальна різниця між мутабельними (mutable) та імутабельними (immutable) об'єктами?

> #### **💬 Відповідь:**
>
> Фундаментальна різниця полягає в тому, чи можна **змінити внутрішній стан** об'єкта після його створення.
>
> * **Мутабельний (Mutable) об'єкт:**
>   * Його стан (значення полів) **можна змінювати** після створення.
>   * Методи такого об'єкта змінюють стан **самого об'єкта (`this`)**.
>   * **Приклад:** `StringBuilder`, `ArrayList`, `HashMap`, `java.util.Date`.
>
> * **Імутабельний (Immutable) об'єкт:**
>   * Його стан **не можна змінити** після створення.
>   * Будь-яка операція, що виглядає як зміна, насправді створює і повертає **новий об'єкт** з новим станом, залишаючи оригінал недоторканим.
>   * **Приклад:** `String`, `Integer`, `Double`, `LocalDate`.

#### **📝 Аналогія:**

* **Мутабельний об'єкт** — це як дошка для малювання маркером. Ви можете стирати і малювати на ній щось нове скільки завгодно.
* **Імутабельний об'єкт** — це як фотографія. Якщо ви хочете щось змінити, вам доведеться зробити нову фотографію. Стара залишиться такою, як була.

#### **📌 Що перевіряють:**

*Розуміння основного критерію розмежування — можливість зміни стану після створення.*

* * *

### **2 ❓ Питання:**

Наведіть приклад `String` та `StringBuilder`, що демонструє різницю між мутабельністю та імутабельністю.

> #### **💬 Відповідь:**
>
> **`String` — імутабельний:**
>
> ```java
> String s1 = "Hello";
> System.out.println("s1 до зміни: " + s1.hashCode());
>
> String s2 = s1.concat(" World"); // Створюється новий об'єкт
>
> System.out.println(s1); // "Hello" (оригінал не змінився)
> System.out.println(s2); // "Hello World" (це новий рядок)
>
> System.out.println("s1 після зміни: " + s1.hashCode()); // Хеш-код s1 не змінився
> System.out.println("Хеш-код s2: " + s2.hashCode());
> ```
>
> **`StringBuilder` — мутабельний:**
>
> ```java
> StringBuilder sb = new StringBuilder("Hello");
> System.out.println("sb до зміни: " + sb.hashCode());
>
> sb.append(" World"); // Змінюється внутрішній стан того самого об'єкта
>
> System.out.println(sb); // "Hello World"
> System.out.println("sb після зміни: " + sb.hashCode()); // Хеш-код sb не змінився, бо це той самий об'єкт
> ```
>
> Основна відмінність: `concat()` для `String` повертає новий об'єкт, а `append()` для `StringBuilder` змінює існуючий.

#### **📌 Що перевіряють:**

*Практичне розуміння поведінки `String` та `StringBuilder` як канонічних прикладів імутабельності та мутабельності.*

* * *

### **3 ❓ Питання:**

Які переваги мають імутабельні об'єкти?

> #### **💬 Відповідь:**
>
> 1. **Потокобезпечність (Thread Safety):**
>     * Імутабельні об'єкти є потокобезпечними за своєю природою. Ними можна безпечно ділитися між потоками без синхронізації, оскільки їхній стан не може бути змінений.
>
> 2. **Простота та передбачуваність:**
>     * Легше міркувати про стан програми, коли ви знаєте, що об'єкти не змінюються.
>
> 3. **Безпечне використання в колекціях:**
>     * Ідеальні для ключів у `HashMap` та елементів у `HashSet`, оскільки їхній `hashCode` стабільний.
>
> 4. **Можливість кешування:**
>     * Оскільки об'єкти незмінні, їх можна безпечно кешувати і перевикористовувати, як це робиться для `Integer` або `String`.
>
> 5. **Безпека:**
>     * Захищають від несанкціонованих змін важливих даних.

#### **📌 Що перевіряють:**

*Знання ключових переваг імутабельності, особливо її зв'язку з багатопотоковістю та колекціями.*

* * *

### **4 ❓ Питання:**

Які переваги мають мутабельні об'єкти? Коли їх варто використовувати?

> #### **💬 Відповідь:**
>
> Основна перевага мутабельних об'єктів — це **продуктивність та ефективність по пам'яті** у сценаріях, що вимагають **частих змін**.
>
> **Коли їх варто використовувати:**
>
> * **При великій кількості модифікацій:**
>   * Якщо вам потрібно виконати багато операцій зміни стану, створення нового імутабельного об'єкта на кожному кроці буде дуже "дорогим".
>   * **Приклад:** Конкатенація рядків у циклі. Використання мутабельного `StringBuilder.append()` значно ефективніше, ніж `String +=`, оскільки не створює тисячі проміжних об'єктів.
>
> * **Для представлення об'єктів, стан яких за своєю природою змінюється:**
>   * Такі об'єкти, як банківський рахунок (`Account`), кошик для покупок (`ShoppingCart`) або графічний компонент на екрані, за своєю суттю є мутабельними. Робити їх імутабельними було б неприродно і незручно.
>
> * **Коли потрібно передавати об'єкт для зміни "за місцем":**
>   * Іноді зручно передати мутабельний об'єкт у метод, щоб той змінив його стан, замість того, щоб повертати новий об'єкт.

#### **📌 Що перевіряють:**

*Розуміння того, що мутабельність також має свої сильні сторони, особливо у питаннях продуктивності при частих змінах.*

* * *

### **5 ❓ Питання:**

Поясніть, як імутабельність та мутабельність впливають на потокобезпечність. ⭐️

> #### **💬 Відповідь:**
>
> * **Імутабельні об'єкти:**
>   * **Потокобезпечні "за дизайном" (inherently thread-safe).**
>   * Оскільки їхній стан не можна змінити, немає потреби захищати їх від одночасного доступу. Кілька потоків можуть читати імутабельний об'єкт паралельно без будь-яких ризиків (стану гонитви) чи необхідності в синхронізації.
>
> * **Мутабельні об'єкти:**
>   * **Не є потокобезпечними за замовчуванням.**
>   * Якщо кілька потоків отримують доступ до спільного мутабельного об'єкта, і принаймні один з них його змінює, це може призвести до **стану гонитви** та неконсистентності даних.
>   * Для безпечної роботи з ними в багатопотоковому середовищі потрібна **явна синхронізація** (`synchronized` блоки, `Lock`-и) або використання спеціалізованих **потокобезпечних мутабельних класів** (наприклад, `ConcurrentHashMap`, `AtomicInteger`).

#### **📌 Що перевіряють:**

*Чітке розуміння того, що імутабельність = безпека в багатопотоковості, а мутабельність = ризик і потреба в синхронізації.*

* * *

### **6 ❓ Питання:**

Чому `String` в Java імутабельний, а `StringBuilder` — мутабельний? Які задачі вирішує кожен з них? ⭐️

> #### **💬 Відповідь:**
>
> Це дизайнерське рішення для вирішення різних завдань.
>
> * **`String` (імутабельний):**
>   * **Задача:** Представляти **незмінні** послідовності символів.
>   * **Чому імутабельний:** Це забезпечує **безпеку**, надійність, можливість кешування (`String Pool`) та безпечне використання як ключів `HashMap`. Рядки часто представляють константні, фундаментальні дані, які не повинні змінюватися.
>
> * **`StringBuilder` (мутабельний):**
>   * **Задача:** **Ефективно будувати або змінювати** послідовності символів.
>   * **Чому мутабельний:** Він був створений як **оптимізація** для випадків, коли потрібно виконати багато операцій конкатенації. Замість того, щоб створювати новий об'єкт `String` на кожному кроці, `StringBuilder` змінює свій внутрішній масив символів. Це значно швидше і менш ресурсоємно.
>
> **Коротко:** `String` — для **зберігання** та **передачі** даних, `StringBuilder` — для їх **динамічного створення**.

#### **📌 Що перевіряють:**

*Розуміння того, що `String` та `StringBuilder` не є конкурентами, а інструментами для різних завдань.*

* * *

### **7 ❓ Питання:**

Що таке "витік посилання" (leaking a reference) і як це пов'язано з мутабельністю? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **"Витік посилання"** — це ситуація, коли клас, який мав би інкапсулювати свій внутрішній стан, **повертає пряме посилання** на свій внутрішній **мутабельний** об'єкт.
>
> Це повністю руйнує інкапсуляцію і може зламати імутабельність.
>
> **Приклад:**
>
> ```java
> public final class UserProfile {
>     private final List<String> roles; // мутабельне поле
>
>     public UserProfile(List<String> roles) {
>         this.roles = new ArrayList<>(roles);
>     }
>
>     // Помилка: цей геттер "зливає" посилання на внутрішній стан
>     public List<String> getRoles() {
>         return this.roles;
>     }
> }
>
> //...
> UserProfile profile = new UserProfile(List.of("USER"));
>
> // Клієнтський код отримує посилання і змінює внутрішній стан об'єкта!
> List<String> leakedRoles = profile.getRoles();
> leakedRoles.add("ADMIN"); // Імутабельність порушено!
> ```
>
> **Рішення:** Замість повернення прямого посилання, потрібно повертати **копію** (`new ArrayList<>(this.roles)`) або **незмінний вид** (`Collections.unmodifiableList(this.roles)`).

#### **📌 Що перевіряють:**

*Розуміння того, як неправильна реалізація геттерів може зруйнувати інкапсуляцію та імутабельність.*

* * *

### **8 ❓ Питання:**

Порівняйте `Date` та `LocalDate`. Який з них є імутабельним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> * **`java.util.Date` (застарілий клас):**
>   * Є **мутабельним**.
>   * Має методи, як-от `setTime(long time)`, які дозволяють змінити його внутрішній стан (кількість мілісекунд з початку епохи).
>   * Через свою мутабельність він є джерелом багатьох помилок у коді, особливо в багатопотоковому. Вимагає захисного копіювання.
>
> * **`java.time.LocalDate` (з Java 8):**
>   * Є **імутабельним**.
>   * Будь-який метод, що "змінює" дату (наприклад, `plusDays(1)`), не змінює оригінальний об'єкт, а повертає **новий екземпляр `LocalDate`**.
>
> **Висновок:** Увесь `java.time` API ( `LocalDate`, `LocalTime`, `LocalDateTime`, `ZonedDateTime`...) був спроектований з нуля на основі імутабельності, що робить його набагато надійнішим, простішим у використанні та потокобезпечним. `Date` слід уникати в новому коді.

#### **📌 Що перевіряють:**

*Знання про недоліки старого Date/Time API та переваги імутабельного дизайну нового.*

* * *

### **9 ❓ Питання:**

Як мутабельність ключів впливає на `HashMap`?

> #### **💬 Відповідь:**
>
> Використання мутабельного ключа може призвести до того, що ви **"втратите"** запис у `HashMap`.
>
> **Механізм проблеми:**
>
> 1. Ви додаєте мутабельний ключ в `HashMap`. Мапа обчислює його `hashCode` і розміщує запис у відповідний "кошик".
> 2. Ви змінюєте стан ключа.
> 3. Через зміну стану його `hashCode()` тепер повертає інше значення.
> 4. При спробі доступу `map.get(key)`, `HashMap` обчислює новий хеш і шукає ключ у **новому, неправильному кошику**.
>
> **Результат:** `map.get()` поверне `null`, хоча об'єкт все ще знаходиться в мапі.
>
> Саме тому ключі `HashMap` **повинні бути імутабельними**.

#### **📌 Що перевіряють:**

*Повторення і закріплення знань про критичну вимогу імутабельності для ключів `HashMap`.*

* * *

### **10 ❓ Питання:**

Що краще для багатопотоковості: імутабельний об'єкт чи мутабельний синхронізований об'єкт? ⭐️⭐️

> #### **💬 Відповідь:**
>
> У більшості випадків **імутабельний об'єкт є значно кращим**.
>
> * **Імутабельний об'єкт:**
>   * **Переваги:** Абсолютно потокобезпечний **без будь-яких блокувань**. Надає максимальну продуктивність при читанні, оскільки потокам не потрібно конкурувати за доступ. Значно спрощує код.
>   * **Недолік:** Не підходить, якщо стан об'єкта справді має часто змінюватися.
>
> * **Мутабельний синхронізований об'єкт:**
>   * **Переваги:** Дозволяє змінювати стан.
>   * **Недоліки:**
>     * **Потребує блокувань (`synchronized` або `Lock`),** що може призвести до проблем з продуктивністю (конкуренція потоків) та до дедлоків.
>     * **Код стає значно складнішим** для написання та аналізу.
>
> **Загальне правило (Concurrency-Aware Design):**
>
> **Мінімізуйте змінюваний стан.** Намагайтеся зробити якомога більше об'єктів у вашій системі імутабельними. Змінюваність має бути тільки там, де вона абсолютно необхідна, і цей змінюваний стан має бути ретельно захищений.
>
> Тому відповідь — **імутабельність майже завжди є кращим вибором для простоти, надійності та часто для продуктивності**.

#### **📌 Що перевіряють:**

*Розуміння того, що уникнення блокувань (завдяки імутабельності) є кращою стратегією для багатопотоковості, ніж керування блокуваннями.*

* * *

### **11 ❓ Питання:**

Як `final` поля допомагають, але не гарантують імутабельність? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `final` на полі **гарантує незмінність посилання**, але **не незмінність об'єкта**, на який воно вказує.
>
> **Як допомагає:**
>
> `private final String name;` — після ініціалізації в конструкторі, ви не зможете присвоїти полю `name` інший рядок.
>
> **Чому не гарантує:**
>
> `private final List<String> list;` — посилання `list` є `final`, але сам об'єкт `List` є мутабельним. Ви все ще можете викликати `list.add("new element")` і змінити його вміст.
>
> Крім того, `final` для поля **гарантує його видимість** у багатопотоковому середовищі після завершення конструктора.
>
> Отже, `final` є необхідним, але **недостатнім** правилом. Для повної імутабельності потрібно також забезпечити захисне копіювання для мутабельних полів.

#### **📌 Що перевіряють:**

*Чітке розуміння того, що `final` діє на посилання, а не на об'єкт.*

* * *

### **12 ❓ Питання:**

Як патерн `Builder` допомагає в роботі і з мутабельними, і з імутабельними об'єктами?

> #### **💬 Відповідь:**
>
> `Builder` служить мостом між двома світами:
>
> 1. **Створення імутабельних об'єктів:**
>     * `Builder` є **мутабельним** на етапі конструювання, дозволяючи зручно накопичувати стан.
>     * Але його метод `build()` створює і повертає **фінальний, імутабельний** об'єкт. Це його найпоширеніше застосування.
>
> 2. **Робота з мутабельними об'єктами:**
>     * Хоча менш поширено, `Builder` можна використовувати для **початкової валідної конфігурації** мутабельного об'єкта. Він гарантує, що об'єкт буде створений одразу в консистентному, правильному стані, навіть якщо потім його можна буде змінювати.

#### **📌 Що перевіряють:**

*Здатність бачити `Builder` як гнучкий інструмент, що може застосовуватися в різних контекстах.*

* * *

### **13 ❓ Питання:**

Розглянемо `record` з Java 16. Чи є він завжди імутабельним? Поясніть. ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, **не завжди**. `record` забезпечує **поверхневу імутабельність**, але не **глибоку**.
>
> * За замовчуванням, всі поля `record` є `private final`, і сам клас є `final`. Це забезпечує імутабельність, **якщо всі його поля також є імутабельними** (`String`, `int`, `LocalDate`).
>
> * **Проблема:** Якщо поле `record` є посиланням на **мутабельний об'єкт** (`List`, `Date`), то `record` **не є по-справжньому імутабельним**.
>
> **Приклад:**
> `public record Order(int id, List<String> items) {}`
> Клієнтський код може отримати `items` і змінити список: `order.items().add(...)`.
>
> Для досягнення повної імутабельності потрібно **вручну** додати захисне копіювання в конструктор та геттери `record`.

#### **📌 Що перевіряють:**

*Розуміння того, що `record` — це зручно, але не "срібна куля". Імутабельність вкладених об'єктів залишається відповідальністю розробника.*

* * *

### **14 ❓ Питання:**

Коли варто вибрати `String`, а коли `StringBuilder` для конкатенації рядків? ⭐️

> #### **💬 Відповідь:**
>
> * **Використовуйте `String` та оператор `+`, коли:**
>   * У вас є невелика, **фіксована** кількість конкатенацій.
>   * `String result = "Hello, " + name + "!";`
>   * У таких випадках компілятор Java **сам оптимізує** цей код і перетворить його на `StringBuilder`, тому немає втрати продуктивності.
>
> * **Використовуйте `StringBuilder`, коли:**
>   * Ви виконуєте конкатенацію **у циклі** або програмно "будуєте" рядок з **великої або невідомої кількості частин**.
>   * Це значно ефективніше, оскільки не створює безлічі проміжних об'єктів `String`.
>
> **Приклад, де `StringBuilder` є обов'язковим:**
>
> ```java
> StringBuilder sb = new StringBuilder();
> for (String word : words) {
>     sb.append(word).append(" ");
> }
> String result = sb.toString();
> ```

#### **📌 Що перевіряють:**

*Практичне розуміння того, коли оптимізації компілятора достатньо, а коли потрібне явне використання `StringBuilder`.*

* * *

### **15 ❓ Питання:**

Чому `List.of()` з Java 9 повертає імутабельну колекцію? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Це свідоме дизайнерське рішення, яке відображає сучасний тренд у програмуванні на користь імутабельності.
>
> **Причини:**
>
> 1. **Безпека та надійність:**
>     * Імутабельні колекції набагато безпечніше передавати між методами або потоками. Ви можете бути впевнені, що колекція не буде несподівано змінена.
>
> 2. **Ефективність по пам'яті та продуктивності:**
>     * Оскільки ці колекції незмінні, їхня реалізація може бути **значно оптимізована**. Наприклад, `List.of("a", "b")` повертає не `ArrayList`, а спеціалізований, дуже компактний внутрішній клас, який не має логіки для розширення, `modCount` і т.д.
>
> 3. **Явний намір:**
>     * `List.of()` призначений для створення **константних** списків. Зробивши їх імутабельними, API чітко виражає цей намір. Якщо вам потрібен змінюваний список, ви можете легко створити його, передавши імутабельний список у конструктор `ArrayList`.
>     * `List<String> mutableList = new ArrayList<>(List.of("a", "b"));`
>
> Це робить стандартну бібліотеку більш надійною та передбачуваною.

#### **📌 Що перевіряють:**

*Розуміння переваг імутабельності та сучасних трендів у дизайні API Java.*
