---
layout: default
title: "Immutable class в Java"
parent: "Immutable об'єкти"
grand_parent: "Java Core"
nav_order: 1
---

# Immutable class в Java

* короткий опис: `Immutable class` – це клас, екземпляри якого неможливо змінити після створення. Усі поля мають фіксовані значення, встановлені під час ініціалізації.

* поняття: `immutable`, `state` (стан), `final`.

* приклади: `String` та його метод `replace()` (повертає новий рядок, не змінюючи оригінал), `Integer`, `LocalDate`.

* ключові моменти:  Стан об'єкта не змінюється після створення. Забезпечує прогнозовану поведінку, потокобезпеку та безпечне використання як ключі у `HashMap`.

* що важливо знати на співбесіді: Чому `String` immutable, переваги `immutable` об'єктів.

### **✅ Офіційне визначення:**

**Immutable class** — це клас, екземпляри якого неможливо змінити після створення. Усі поля такого класу мають фіксовані значення, встановлені під час ініціалізації.

### **🧠 Простими словами:**

Це клас, об’єкти якого «заморожені»: їх не можна змінити після створення. Усе, що ти передав у конструктор — назавжди. Як банкомат: вставив картку — не зміниш пін-код прямо в процесі зняття.

---

## **📊 Ознаки класу, який можна вважати immutable:**

* Клас оголошено як **`final`**
* Всі поля — **`private`** `final`
* Відсутні сеттери (setter-и)
* Усі змінні об'єктного типу всередині також **`immutable`** або мають захист (наприклад, через глибоку копію)
* Повернення копій полів-об'єктів, а не самих об'єктів (захист від зовнішніх змін)

---

## **Пояснення на пальцях (💡)**

Уяви, що ти купив квиток на концерт. Ти не можеш змінити дату, місце або виконавця на квитку — усе фіксовано. Клас **`String`** — як такий квиток. Навіть якщо зробиш **`replace()`**, це не змінить оригінал — просто створиться нова версія з новим текстом

---

## **Корисні приклади (🧪)**

```java
public final class Person {
    private final String name;
    private final int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    public String getName() {
        return name;
    }

    public int getAge() {
        return age;
    }
}
```

```java
Person p = new Person("Anna", 25);
// Не існує способу змінити p.name або p.age -- це immutable об'єкт.
```

**Приклад з String:**

```java
String s = "Hello";
String modified = s.replace("H", "J");
System.out.println(s); // Hello
System.out.println(modified); // Jello
```

---

## **Порівняння (⚖️)**

|  | Immutable | Mutable |
| :---: | :---: | :---: |
| **Зміна після створення** | ❌ Неможлива | ✅ Можлива |
| **Потокобезпека** | ✅ Так, без додаткових засобів | ❌ Потрібна синхронізація |
| **Приклад** | **String**, **Integer**, **LocalDate** | **StringBuilder**, **ArrayList**, **HashMap** |
| **Поведінка в HashMap** | 🔒 Стабільні ключі | ⚠️ Можуть викликати баги |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"F-PINS"**:

* **F**inal class
* **P**rivate final fields
* **I**nitialize in constructor
* **N**o setters
* **S**afe copies of references

---

## **🟩 Підсумок:**

**Immutable**\-класи — ключ до безпечного коду, особливо у багатопотоковому середовищі. Вони прості у використанні, важливі в колекціях типу **`HashMap`** і лежать в основі таких класів як **`String`**, **`Integer`**, **`LocalDate`**. Завжди переконуйся, що:

* Поля не можна змінити після створення
* Немає способів обійти обмеження (через сеттери, наслідування тощо)
* Посилальні типи надійно захищені

### **🔗 Додаткові джерела:**

* [**Oracle — Java Immutable Objects**](https://docs.oracle.com/javase/tutorial/essential/concurrency/immutable.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке імутабельний (immutable) клас в Java?

> #### **💬 Відповідь:**
>
> **Імутабельний клас** — це клас, екземпляри (об'єкти) якого **неможливо змінити** після їх створення. Весь стан об'єкта (значення його полів) встановлюється один раз під час ініціалізації (зазвичай у конструкторі) і залишається незмінним протягом усього життя об'єкта.
>
> Будь-яка операція, яка виглядає як зміна імутабельного об'єкта, насправді створює і повертає **новий екземпляр** з новими даними, залишаючи оригінал недоторканим.
>
> Класичними прикладами імутабельних класів у JDK є `String`, `Integer`, `Double`, а також усі класи з пакету `java.time` (наприклад, `LocalDate`, `Instant`).

#### **📝 Аналогія:**</h4>

Імутабельний об'єкт — це як викарбувана монета. Після того, як її викарбували, ви не можете змінити її номінал або рік випуску. Якщо вам потрібна інша монета, ви берете новий шматок металу і карбуєте нову.

#### **📌 Що перевіряють:**

*Розуміння основного принципу імутабельності: стан об'єкта незмінний після створення.*

* * *

### **2 ❓ Питання:**

Які правила потрібно виконати, щоб зробити клас імутабельним?

> #### **💬 Відповідь:**
>
> Щоб гарантувати імутабельність класу, слід дотримуватися кількох правил:
>
> 1. **Зробити клас `final`**: Це забороняє успадкування. Таким чином, ніхто не зможе створити підклас, який змінить поведінку і зробить його мутабельним.
>
> 2. **Зробити всі поля `private` та `final`**:
>     * `private` — для інкапсуляції.
>     * `final` — для гарантії, що посилання на поля будуть присвоєні лише один раз (у конструкторі).
>
> 3. **Не надавати сеттерів (setter-методів)**: Методи не повинні дозволяти змінювати значення полів.
>
> 4. **Ініціалізувати всі поля в конструкторі**: Всі `final`-поля мають бути встановлені під час створення об'єкта.
>
> 5. **Обережно поводитися з мутабельними полями (найважливіший крок!):**
>     * Якщо клас містить посилання на **мутабельні** об'єкти (наприклад, `List`, `Date`), ви повинні:
>         * **У конструкторі** створювати їх **захисну копію (defensive copy)**, а не просто присвоювати посилання.
>         * **У геттерах** також повертати **копію**, а не посилання на внутрішній об'єкт. Це захищає від змін ззовні.

#### **📝 Мнемоніка (F-PINS):**</h4>

* **F**inal class
* **P**rivate **I**mmutable/`final` fields
* **N**o setters
* **S**afe copies in/out

#### **📌 Що перевіряють:**

*Знання всіх правил створення імутабельних класів, особливо увага до роботи з мутабельними полями (захисне копіювання).*

* ** * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке імутабельний (immutable) клас у Java?

> #### **💬 *

### **3 ❓ Питання:**

Наведіть приклад імутабельного класу.

> #### **💬 Відповід Відповідь:**
>
> **Імутабельний клас** — це клас, екземпляри якого **неможливо змінити** після їхнього створення.
>
> Це означає, що весь внутрішній стан об'єкта (знаь:**
>
> Ось приклад імутабельного класу `Person`, який також містить мутабельне поле `hobbies`.
>
> ```java
> import java.util.ArrayList;
> import java.util.List;
>
> public final class ImmutablePerson {
>     private final String name;
>     private final int age;
чення його полів) встановлюється один раз під час створення (зазвичай у конструкторі) і більше ніколи не змінюється протягом усього життя об'єкта.
>
> Будь-яка операція, яка виглядає як "зміна" імутабельного об'єкта, насправді створює і повертає **новий екземпляр** з оновленим станом, залишаючи оригінальний об'єкт недоторканим. Кла>     private final List<String> hobbies;
>
>     public ImmutablePerson(String name, int age, List<String> hobbies) {
>         this.name = name;
>         this.age = age;
>         // 1. Створюємо захисну копію вхідного списку
>         this.hobbies = new ArrayList<>(hobbies);
>     }
>
>     // Геттери
>     public String getName() { return name; }
>     public int getAge() { return age; }
>
>     // 2. Повертаємо копію внутрішнього списку, а не посилання на нього
>     public List<String> getHobbies() {
>         return new ArrayList<>(this.hobbies);
>         // або Collections.unmodifiableList(this.hobbies)
>     }
> }
>
> ```
> Завдяки захисним копіям, навіть якщо клієнтський код спробує змінити список, який він передав у конструктор, або список, який він отримав з геттера, внутрішній стан об'єкта `ImmutablePerson` залишиться незмінним.

#### **📌 Що перевіряють:**

*Практичні навички написання імутабельного класу з урахуванням захисного копіювання.*

* * *

### **4 ❓ Питання:**

Які основні переваги використання імутабельних об'єктів? ⭐️

> #### **💬 Відповідсичним прикладом є `java.lang.String`

#### **📝 Аналогія:**</h4>

Імутабельний об'єкт — це як вигравіюваний напис на камені. Ви не можете змінити його. Якщо вам потрібен інший напис, ви берете новий камінь і робите нове гравіювання. Старий камінь залишається таким, як був.

#### **📌 Що перевіряють:**

*Розуміння основного принципу імутабельності — незмінність стану після створення.*

* * *

### **2 ❓ Питання:**

Назвіть основні правила для створення імутабельного класу.

> #### **💬 Відповідь:**
>
> Щоб зробити клас по-справжньому імутаь:**
>
> 1. **Потокобезпечність (Thread Safety):**
>     * Імутабельні об'єкти є **потокобезпечними "з коробки"**. Оскільки їхній стан не можна змінити, ними можна безпечно ділитися між багатьма потоками без будь-якої синхронізації (`synchronized`, `lock`). Це значно спрощує написання багатопотокового коду.
>
> 2. **Простота та передбачуваність:**
>     * Легше міркувати про стан програми, коли ви знаєте, що об'єкти не змінюються несподівано. Стан об'єкта є консистентним протягом усього його життя.
>
> 3. **Безпечне використання як ключів у `HashMap` та елементів у `HashSet`:**
>     * Оскільки `hashCode()` імутабельного об'єкта не змінюється, його можна безпечно використовувати як ключ. Якщо б ключ був мутабельним, його хеш-код міг би змінитисябельним, потрібно дотримуватися кількох правил:
>
> 1. **Зробіть клас `final`:** Це заборонить успадкування, щоб підкласи не могли змінити його поведінку (наприклад, перевизначити методи і зробити об'єкт мутабельним).
>
> 2. **Зробіть усі поля `private` та `final`:**
>     * `private` — для заборони прямого доступу ззовні.
>     * `final` — для гарантії, що посилання на поле буде присвоєно лише один раз (у конструкторі).
>
> 3. **Не надавайте сеттерів (setter methods):** Не повинно бути жодних методів, які змінюють стан полів.
>
> 4. **Ініціалізуйте всі поля в конструкторі:** Усі `final`-поля мають отримати свої значення під час створення об'єкта.
>
> 5. **Забезпечте імутабельність для мутабельних полів:**
>     * Якщо ваш клас містить посилання на **мутабельні об'єкти** (наприклад, `List`, `Date`, або ваші власні мутабельні класи), ви повинні:
>         * Створювати їхні **глибокі копії ("захисні копії")** в конструкторі, щоб не зберігати посилання на зовнішні об'єкти.
>         * Повертати їхні **копії** в геттерах, щоб не "випускати" посилання на внутрішній стан назовні.

#### **📌 Що перевіряють:**

*Чітке знання всіх після додавання, і ви б "втратили" об'єкт у мапі.
>
> 4. **Можливість кешування:**
>     * Оскільки об'єкти незмінні, їхні результати (наприклад, `hashCode()`) можна кешувати. Клас `String` кешує свій хеш-код. Також можна кешувати самі імутабельні об'єкти.
>
> 5. **Захист від помилок:** Запобігають несподіваним змінам стану через "витік посилань" (leaky references).

#### **📌 Що перевіряють:**

*Розуміння того, чому імутабельність є потужною концепцією, що веде до написання більш надійного та простого коду.*

* * *

### **5 ❓ Питання:**

Чому клас `String` в Java є імутабельним? ⭐️

> #### **💬 Відповідь:**
>
> `String` зроблено імутабельним з кількох критично важливих причин:
>
> 1. **String Pool:** Java підтримує "пул рядків" для економії пам'яті. Коли ви створюєте рядковий літерал (`String s = "hello"`), він потра кроків, необхідних для створення гарантовано імутабельного класу, особливо важливого п'ятого пункту про мутабельні поля.*

* * *

### **3 ❓ Питання:**

Чому клас `String` є імутабельним і які переваги це дає? ⭐️

> #### **💬 Відповідь:**
>
> Клас `String` в Java є імутабельним. Це означає, що будь-який метод, як-от `toUpperCase()`, `substring()` чи `replace()`, не змінює вихідний рядок, а створює і повертає **новий об'єкт `String`**.
>
> **Переваги імутабельності `String`:**
>
> 1. **Безпека та надійність:** Рядки широко використовуються для передачі параметрів (URL, шляхи до файлів, SQL-запити). Імутабельність гарантує, що значення рядка не буде несподівано змінено після перевірки безпеки.
>
> 2. **Потокобезпечність (Thread Safety):** Імутабельні об'єкти є потокобезпечними "з коробки". Їх можна безпечно передавати між потоками без будь-якої синхронізації, оскільки їхній стан не може бути змінений.
>
> 3. **Кешування `hashCode()`:** Оскільки рядок незмінний, його хеш-код можна обчислити один раз і закешувати. Це значно прискорює роботу `HashMap`, де рядки часто використовуються як ключі.
>
> 4. **String Pool:** Імутабельність є основою для роботи **пулу рядків (String Pool)**. JVM може зберігати один екземпляр рядкового літералу і перевикористовувати його,пляє в пул. Наступне створення такого ж літералу поверне посилання на той самий об'єкт з пулу. Якби рядки були змінюваними, то зміна одного посилання вплинула б на всі інші змінні, що посилаються на той самий об'єкт у пулі. Це було б катастрофою.
>
> 2. **Безпека:** Рядки широко використовуються для передачі критичних даних, таких як імена файлів, мережеві адреси, паролі, SQL-запити. Якби вони були мутабельними, зловмисник міг би змінити рядок після того, як він пройшов перевірку безпеки, але до того, як він був використаний.
>
> 3. **Безпечне використання в колекціях:** Імутабельність робить `String` ідеальним ключем для `HashMap`. Його `hashCode` обчислюється один раз і кешується, що підвищує продуктивність.
>
> 4. **Потокобезпечність:** Оскільки рядки незмінні, їх можна безпечно передавати між потоками без синхронізації.
>
> Всі ці причини роблять імутабельність `String` однією з фундаментальних основ надійності платформи Java.

#### **📌 Що перевіряють:**

*Розуміння ключових причин, чому `String` є незмінним. Це одне з найпоширеніших питань на співбесідах.*

* * *

### **6 ❓ Питання:**

Якщо `String` є імутабельним, то що відбувається, коли ми робимо `String s = "a"; s = s + "b";`?

> #### **💬 Відповідь:**
>
> Коли ви виконуєте `s = s + "b";`, ви **не змінюєте** оригінальний рядок `"a"`.
>
> **Натомість відбувається наступне:**
>
> 1. Створюється **новий** об'єкт `String` — результат конкатенації `" що економить пам'ять. Це було б неможливо, якби рядок можна було змінити.

#### **📌 Що перевіряють:**

*Розуміння причин, чому імутабельність є однією з ключових дизайнерських особливостей `String`, та пов'язаних з цим переваг.*

* * *

### **4 ❓ Питання:**

Наведіть приклад створення власного простого імутабельного класу.

> #### **💬 Відповідь:**
>
> ```java
> // 1. Клас оголошено як final
> public final class ImmutablePoint {
>
>     // 2. Всі поля private та final
>     private final int x;
>     private final int y;
>
>     // 3. Ініціалізація в конструкторі
>     public ImmutablePoint(int x, int y) {
>         this.x = x;
>         this.y = y;
>     }
>
>     // 4. Тільки геттери, без сеттерів
>     public int getX() {
>         return x;
>     }
>
>     public int getY() {
>         return y;
>     }
>
>     // 5. Метод "зміни" повертає новий об'єкт
>     public ImmutablePoint move(int dx, int dy) {
>         return new ImmutablePoint(this.x + dx, this.y + dy);
>     }
> }
> ```
>
> У цьому класі немає жодного способу змінити `x` або `y` після створення об'єкта `ImmutablePoint`. Будь-яка "модифікація", як-от `move()`, створює новий екземпляр.

#### **📌 Що перевіряють:**

*Практичні навички написання імутабельного класу з дотриманням основних правил.*

* * *

### **5 ❓ Питання:**

Як забезпечити імутабельність, якщо клас містить поле, що є посиланням на мутабельний об'єкт (наприклад, `List`)? ⭐️

> #### **💬 Відповідь:**
>
> Це найскладніший аспект створення імутабельних класів. Потрібно використовувати **захисне копіювання (defensive copying)**.
>
> 1. **У конструкторі:** Створювати **глибоку копію** вхідної колекції, щоб не зберігати посилання на зовнішній, потенційно змінюваний об'єкт.
> 2. **У геттері:** Повертати **копію** внутрішньої колекції, щоб не "випускати" посилання на свій внутрішній стан. Часто повертають незмінну обгортку.
>
> **Приклад:**
>
> ```java
> import java.util.ArrayList;
> import java.util.Collections;
> import java.util.List;
>
> public final class ImmutableUser {
>     private final String name;
>     private final List<String> roles; // List є мутабельним
>
>     public ImmutableUser(String name, List<String> roles) {
>         this.name = name;
>         // 1. Захисне копіювання в конструкторі
>         this.roles = new ArrayList<>(roles);
>     }
>
>     public String getName() { return name; }
>
>     public List<String> getRoles() {
>         // 2. Повертаємо незмінну копію, а не посилання
>         return Collections.unmodifiableList(this.roles);
>     }
> }
> ```
>
> Без цих кроків, клієнтський код міг би змінити список ролей після створення `ImmutableUser` або отримати доступ до внутрішнього списку і змінити його, порушивши імутабельність.

#### **📌 Що перевіряють:**

*Розуміння проблеми "витоку посилань" (leaking references) та вміння застосовувати захисне копіa"` та `"b"`. Цей новий рядок буде мати значення`"ab"`.
> 2.  Посилання `s` **перенаправляється** на цей новий об'єкт `"ab"`.
> 3.  Оригінальний об'єкт `"a"` залишається незмінним у пам'яті (у пулі рядків) і може бути використаний іншими частинами програми. Якщо на нього більше немає посилань, він стане доступним для збирача сміття.
>
> "Під капотом" компілятор зазвичай оптимізує таку конкатенацію, використовуючи `StringBuilder` для ефективності.
> `String s = new StringBuilder().append(s).append("b").toString();`
>
> Таким чином, імутабельність зберігається: ми не змінюємо існуючі об'єкти, а створюємо нові.

#### **📌 Що перевіряють:**

*Розуміння того, що операції, які виглядають як зміна `String`, насправді створюють нові об'єкти.*

* * *

### **7 ❓ Питання:**

Що таке захисне копіювання (defensive copy) і коли його слід використовувати?

> #### **💬 Відповідь:**
>
> **Захисне копіювання** — це техніка створення **копій мутабельних об'єктів**, які передаються в ваш клас або повертаються з нього, щоб захистити його внутрішній стан (інваріанти).
>
> **Коли його слід використовувати:**
>
> 1. **У конструкторах (при отриманні мутабельних об'єктів):**
>     * Якщо ви приймаєте в конструктор посилання на мутабельний об'єкт (наприклад, `Date` або `ArrayList`) і просто присвоюєте його полю, то клієнтський код, який передав цей об'єкт, зможе **змінити його стан ззовні** навіть після створення вашого об'єкта. Щоб запобігти цьому, потрібно створити і зберегти копію.
>
> 2. **У геттерах (при поверненні мутабельних об'єктів):**
>     * Якщо ви повертаєте пряме посилання на внутрішній мутабельний об'єкт, клієнтський код зможе змінити його,ювання для її вирішення.*

* * *

### **6 ❓ Питання:**

Які основні переваги використання імутабельних об'єктів?

> #### **💬 Відповідь:**
>
> 1. **Простота та передбачуваність:** Ви можете бути впевнені, що стан об'єкта не зміниться несподівано. Це значно спрощує аналіз коду та відстеження помилок.
>
> 2. **Потокобезпечність (Thread Safety):** Імутабельні об'єкти є за своєю природою потокобезпечними. Оскільки їхній стан не можна змінити, немає потреби в синхронізації для захисту від стану гонитви. Ними можна безпечно ділитися між потоками.
>
> 3. **Безпечне використання як ключів у `Map` або елементів у `Set`:**
>     * `hashCode()` для імутабельного об'єкта завжди однаковий, тому його можна безпечно кешувати.
>     * Немає ризику, що об'єкт, покладений у `HashSet` або як ключ у `HashMap`, "загубиться" через зміну свого стану.
>
> 4. **Можливість кешування:** Оскільки об'єкт незмінний, його можна безпечно кешувати і перевикористовувати. Приклад — `Integer` кеш для значень від -128 до 127.
>
> 5. **Безпека:** Імутабельність запобігає несанкціонованим змінам важливих даних (як у випадку зі `String`).

#### **📌 Що перевіряють:**

*Знання ключових переваг імутабельності, особливо її зв'язку з потокобезпечністю та використанням у колекціях.*

* * *

### **7 ❓ Питання:**

Наведіть приклади імутабельних класів у стандартній бібліотеці Java.

> #### **💬 Відповідь:**
>
> * **`java.lang.String`:** Найвідоміший приклад.
> * **Класи-обгортки примітивів:** `Integer`, `Long`, `Double`, `Boolean` тощо.
> * **`java.math.BigInteger` та `java.math.BigDecimal`:** Хоча їхні методи (`add`, `multiply`) повертають нові об'єкти.
> * **Класи з `java.time` API (з Java 8):** `LocalDate`, `LocalTime`, `LocalDateTime`, `Instant`, `Duration`. Вся бібліотека `java.time` спроектована на основі імутабельності, що є однією з її ключових переваг над старим `java.util.Date`.
> * **`java.io.File`:** Хоча він представляє файл на диску, який може змінюватися, сам об'єкт `File`, що містить шлях, є імутабельним.
> * Колекції, що повертаються методами **`List.of()`**, **`Set.of()`**, **`Map.of()`** (з Java 9).
> * Колекції, обгорнуті в **`Collections.unmodifiable...()`**.

#### **📌 Що перевіряють:**

*Загальну ерудицію та знання JDK.*

* * *

### **8 ❓ Питання:**

Чим відрізняються `String` та `StringBuilder` з точки зору імутабельності? ⭐️

> #### **💬 Відповідь:**
>
> Це класичний приклад протиставлення імутабельного та мутабельного підходів.
>
> * **`String` — імутабельний:**
>   * Кожна операція, яка "змінює" рядок (`+`, `concat`, `replace`), створює **новий об'єкт `String`** у пам'яті.
>   * Це може бути неефективним, якщо потрібно виконати багато операцій конкатенації в циклі, оскільки буде створено багато проміжних об'єктів.
>
> * **`StringBuilder` (і `StringBuffer`) — мутабельний:**
>   * Це клас, спеціально розроблений для **ефективної модифікації рядків**.
>   * Й порушуючи інкапсуляцію. Щоб запобігти цьому, потрібно повертати копію.
>
> Ця техніка є **ключовою** для створення по-справжньому імутабельних класів, які містять мутабельні поля.

#### **📌 Що перевіряють:**

*Знання техніки захисного копіювання та розуміння її ролі у забезпеченні інкапсуляції та імутабельності.*

* * *

### **8 ❓ Питання:**

Наведіть приклад мутабельного класу та імутабельного класу.

> #### **💬 Відповідь:**
>
> **Мутабельний (змінний) клас:** `StringBuilder`
>
> ```java
> StringBuilder sb = new StringBuilder("Hello");
> System.out.println(sb); // Hello
>
> // Змінюємо внутрішній стан того ж самого об'єкта
> sb.append(", World!");
> System.out.println(sb); // Hello, World!
> ```
>
> Після виклику `append()` сам об'єкт `sb` змінився.
>
> **Імутабельний (незмінний) клас:** `String`
>
> ```java
> String s = "Hello";
> System.out.println(s); // Hello
>
> // `toUpperCase()` не змінює `s`, а створює новий об'єкт
> String upper_s = s.toUpperCase();
>
> System.out.println(s);       // Hello (оригінал залишився незмінним)
> System.out.println(upper_s); // HELLO (це новий рядок)
> ```
>
> **Інші приклади:**
>
> * **Мутабельні:** `ArrayList`, `HashMap`, `Date`, `Calendar`.
> * **Імутабельні:** `Integer`, `Double`, `LocalDate`, `LocalTime`, `List.of(...)`.

#### **📌 Що перевіряють:**

*Здатність розрізняти мутабельні та імутабельні класи зі стандартної бібліотеки.*

* * *

### **9 ❓ Питання:**

Як імутабельність спрощує багатопотокове програмування? ⭐️

> #### **💬 Відповідь:**
>
> Імутабельність **кардинально** спрощує багатопотокове програмування, оскільки **усуває цілий клас проблем**, пов'язаних з конкурентним доступом до спільного стану.
>
> 1. **Відсутність станів гонитви (Race Conditions):** Оскільки стан імутабельного об'єкта не можна змінити, кілька потоків не можуть "змагатися" за його зміну. Проблема "перевір-і-зміни" просто не виникає.
>
> 2. **Не потрібна синхронізація:** Вам не потрібні `synchronized` блоки або `Lock`-и для доступу до імутабельних об'єктів. Це робить код значно простішим, чистішим і швидшим.
>
> 3. **Гарантована видимість (Visibility):** Оскільки стан `final`-полів встановлюється в конструкторі, модель пам'яті Java гарантує, що після того, як об'єкт був безпечно опублікований (тобто посилання на нього стало доступним іншим потокам), всі потоки будуть бачити **однаковий, коректно ініціалізований стан**.
>
> Завдяки цим властивостям, імутабельними об'єктами можна **безпечно ділитися** між потоками без будь-яких додаткових зусиль.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, чому імутабельність є однією з фундаментальних технік для написання безпечного конкурентного коду.*

* * *

### **10 ❓ Питання:**

Чому імутабельні об'єкти є ідеальними кандидатами для ключів у `HashMap`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Тому що вони гарантують, що **`hashCode()` ключа ніколи не зміниться** після того, як об'єкт було додано в мапу.
>
> **Проблема з мутабельними ключами:**
>
> * `HashMap` визначає "кошик" дляого методи, як-от `append()`, `insert()`, `delete()`, **змінюють внутрішній стан** того самого об'єкта, не створюючи нових на кожному кроці.
>   * Внутрішньо він використовує масив символів, який може розширюватися.
>
> **Коли що використовувати:**
>
> * Використовуйте **`String`** для рядків, які не будуть змінюватися.
> * Використовуйте **`StringBuilder`** для випадків, коли вам потрібно програмно та ефективно "будувати" або змінювати рядок, особливо у циклах. (`StringBuffer` — для багатопотокового середовища).

#### **📌 Що перевіряють:**

*Розуміння різниці між `String` та `StringBuilder` та правильних сценаріїв їх використання.*

* * *

### **9 ❓ Питання:**

Якщо `String` імутабельний, то як працює операція `s = s + "!";`? ⭐️

> #### **💬 Відповідь:**
>
> Хоча виглядає так, ніби ми змінюємо рядок `s`, насправді це не так.
>
> **Що відбувається "під капотом":**
>
> 1. Оператор `+` для рядків перетворюється компілятором у код з використанням **`StringBuilder`**.
>
> 2. Вираз `s + "!"` виконується приблизно так:
>     * Створюється новий `StringBuilder`, ініціалізований значенням `s`.
>     * Викликається `append("!")` для цього `StringBuilder`.
>     * Викликається `toString()`, який створює **абсолютно новий об'єкт `String`** з результуючим рядком.
>
> 3. **`s = ...`**: Посилання `s` тепер вказує на цей **новий** об'єкт. Старий об'єкт, на який `s` посилався раніше, залишається незмінним і стане доступним для збирача сміття, якщо на нього більше немає посилань.
>
> Таким чином, **імутабельність `String` не порушується**. Ми не змінюємо старий об'єкт, а створюємо новий і переприсвоюємо посилання.

#### **📌 Що перевіряють:**

*Розуміння того, як працює конкатенація рядків у Java і що вона насправді створює нові об'єкти.*

* * *

### **10 ❓ Питання:**

Чому імутабельні об'єкти є хорошими ключами для `HashMap`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Імутабельні об'єкти є **ідеальними ключами** для `HashMap` (та елементами для `HashSet`), тому що вони гарантують, що їхній **`hashCode()` буде незмінним**.
>
> **Проблема з мутабельними ключами:**
>
> * `HashMap` визначає "кошик" для зберігання об'єкта на основі його `hashCode()`, обчисленого в момент виклику `put()`.
> * Якщо після додавання в мапу ви **зміните стан** об'єкта-ключа, його `hashCode()` також може змінитися.
> * В результаті, при наступній спробі знайти цей об'єкт (`get()`), `HashMap` буде шукати його в **новому**, неправильному кошику і **не знайде** його. Об'єкт буде "втрачено".
>
> **Перевага імутабельних ключів:**
>
> Оскільки їхній стан, а отже і `hashCode()`, не може змінитися, "адреса" об'єкта в `HashMap` залишається постійною. Це гарантує, що ви завжди зможете знайти запис за цим ключем.

#### **📌 Що перевіряють:**

*Розуміння того, як імутабельність ключів забезпечує стабільність та коректність роботи `HashMap`.*

* * *

### **11 ❓ Питання:**

Як `final`-поля допомагають в реалізації імутабельності, і чи достатньо їх одних? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `final`-поля є **важливою частиною** створення імутабельного класу, але їх **одних недостатньо**.
>
> **Як `final` допомагає:**
>
> * `final` гарантує, що посилання на поле буде **присвоєно лише один раз** (у конструкторі або при ініціалізації). Це запобігає переприсвоєнню посилання на інший об'єкт.
>
> **Чому `final` недостатньо:**
>
> * Якщо `final`-поле посилається на **мутабельний** об'єкт (наприклад, `private final List<String> list;`), то саме посилання на список змінити не можна, але **внутрішній стан самого списку можна**!
>
>     ```java
>     public class NotQuiteImmutable {
>         private final List<String> items;
>         public NotQuiteImmutable(List<String> items) { this.items = items; }
>         public List<String> getItems() { return this.items; }
>     }
>
>     List<String> originalList = new ArrayList<>();
>     NotQuiteImmutable obj = new NotQuiteImmutable(originalList);
>     originalList.add("new data"); // Змінюємо зовнішній список - змінюється і стан об'єкта!
>
>     obj.getItems().add("more data"); // Змінюємо внутрішній список через геттер!
>     ```
>
> **Що потрібно зберігання запису на основі `hashCode()` ключа.
>
> * Якщо ви використовуєте мутабельний об'єкт як ключ і змінюєте його стан після додавання в мапу, його `hashCode()` може змінитися.
>
> * Це призведе до того, що `HashMap` буде шукати цей ключ у **новому, неправильному кошику**, і **не зможе знайти** запис.
>
> **Перевага імутабельних ключів:**
>
> * Оскільки їхній стан незмінний, їхній `hashCode()` також є **стабільним**.
> * Це гарантує, що об'єкт завжди буде знаходитися в правильному кошику, і операції `get()`, `remove()` завжди будуть працювати коректно.
> * Саме тому `String`, `Integer` та інші імутабельні класи є чудовими ключами.

#### **📌 Що перевіряють:**

*Розуміння критичної вимоги до стабільності `hashCode` для ключів у хеш-структурах.*

* * *

### **11 ❓ Питання:**

Що краще для конкатенації рядків у циклі: `String` чи `StringBuilder`? Поясніть з точки зору імутабельності. ⭐️

> #### **💬 Відповідь:**
>
> **`StringBuilder` є значно кращим і продуктивнішим** вибором.
>
> **Чому `String` є поганим вибором у циклі:**
>
> * Оскільки `String` є **імутабельним**, кожна операція конкатенації `+` всередині циклу **створює новий об'єкт `String`**.
> * `for (int i = 0; i < 10000; i++) { s += "a"; }`
> * Цей код створить **10 000 проміжних об'єктів `String`**, які одразу ж стають "сміттям". Це створює величезне навантаження на збирач сміття і є дуже неефективним.
>
> **Чому `StringBuilder` є кращим вибором:**
>
> * `StringBuilder` є **мутабельним**. Він внутрішньо використовує масив символів, який може розширюватися.
> * Його метод `append()` **не створює нового об'єкта**, а просто додає символи до існуючого внутрішнього буфера.
> * Новий об'єкт `String` створюється лише один раз у самому кінці, при виклику `toString()`.
>
> **Висновок:** Для побудови рядків з кількох частин, особливо в циклах, завжди слід використовувати мутабельний `StringBuilder` (або `StringBuffer` для багатопотоковості), щоб уникнути непотрібного створення об'єктів.

#### **📌 Що перевіряють:**

*Практичне розуміння наслідків імутабельності `String` для продуктивності та знання про `StringBuilder` як про правильний інструмент для конкатенації.*

* * *

### **12 ❓ Питання:**

Чи робить ключове слово `final` клас або поле імутабельним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, `final` **само по собі не гарантує** імутабельності. Це лише частина механізму.
>
> * **`final`-клас:**
>   * `public final class MyClass { ... }`
>   * Забороняє **успадкування**. Це захищає від створення мутабельних підкласів, але не робить сам клас імутабельним. Його поля все ще можуть бути змінними.
>
> * **`final`-поле:**
>   * `private final MyField field;`
>   * Гарантує, що **посилання** на об'єкт буде присвоєно **лише один раз** (у конструкторі).
>   * **Але!** Якщо `MyField` є **мутабельним** класом (наприклад, `ArrayList`), ви **не можете змінити посилання** `field` (тобто присвоїти йому новий `ArrayList`), але ви все ще можете **змінити внутрішній стан** об'єкта, на який воно посилається (наприклад, `field.add(...)`).
>
> **Щоб досягти повної імутабельності**, потрібно поєднати кілька правил: клас `final`, поля `private final`, відсутність сеттерів, і, найголовніше, **захисне копіювання** для будь-яких мутабельних полів.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між незмінністю посилання (`final`) та незмінністю об'єкта (імутабельність).*

* * *

### **13 ❓ Питання:**

Як `record` (з Java 16) спрощує створення імутабельних класів? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **`record`** — це спеціальний тип класу, який є "синтаксичним цукром" і призначений для створення простих, імутабельних класів-носіїв даних.
>
> **Що компілятор генерує автоматично:**
>
> 1. **`private final` поля** для кожного компонента, оголошеного в заголовку.
> 2. **Канонічний конструктор**, що ініціалізує всі ці поля.
> 3. **Публічні методи доступу (геттери)** без префікса `get` (наприклад, `name()` замість `getName()`).
> 4. **Правильну реалізацію** `equals()`, `hashCode()` та `toString()` на основі всіх компонентів.
>
> **Приклад:**
>
> Замість цього:
>
> ```java
> public final class Point {
>     private final int x;
>     private final int y;
>     // ... конструктор, equals, hashCode, toString ...
> }
> ``` додатково:**
>     Необхідне **захисне копіювання** в конструкторі та повернення копій (або незмінних обгорток) у геттерах, щоб повністю захистити внутрішній стан.

#### **📌 Що перевіряють:**

*Глибоке розуміння різниці між незмінністю посилання (`final`) та незмінністю стану об'єкта, на який воно вказує.*

* * *

### **12 ❓ Питання:**

Порівняйте імутабельність в `record` (з Java 16+) та в класичному класі. ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Записи (`record`)** значно спрощують створення імутабельних класів.
>
> * **Імутабельність у `record`:**
>   * **За замовчуванням:** Компілятор автоматично генерує `private final` поля для всіх компонентів, оголошених у заголовку.
>   * Він також генерує публічні геттери (з назвою поля, наприклад `name()`) та канонічний конструктор, який ініціалізує всі ці `final`-поля.
>   * `record` неявно є `final`, тому успадкування від нього заборонено.
>   * Він автоматично надає реалізації `equals`, `hashCode` та `toString` на основі всіх полів.
>   * **Але!** `record` **не** забезпечує автоматичного захисного копіювання для мутабельних полів. Це все ще **відповідальність розробника**.
>
> * **Імутабельність у класичному класі:**
>   * Потребує **ручного** написання великої кількості шаблонного коду: оголошення полів `private final`, написання конструктора, геттерів, перевизначення `equals`, `hashCode`, `toString` і оголошення класу як `final`.
>
> **Висновок:** `record` — це **синтаксичний цукор** для створення простих імутабельних класів-носіїв даних. Він значно зменшує кількість коду і робить намір розробника більш явним. Однак він не вирішує проблему із захисним копіюванням, її все ще потрібно реалізовувати вручну, зазвичай в компактному конструкторі.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови (`record`) та розуміння, які проблеми вони вирішують, а які — ні.*

* * *

### **13 ❓ Питання:**

Чому `java.util.Date` є поганим прикладом для використання в імутабельному класі і яка є краща альтернатива? ⭐️⭐️

> #### **💬 Відповідь:**
>
> `java.util.Date` є **мутабельним** класом. Він має методи, як-от `setTime()`, які дозволяють змінити його внутрішній стан (кількість мілісекунд).
>
> Це робить його дуже **небезпечним** полем для імутабельного класу. Якщо ви не виконаєте **захисне копіювання**, ви "пропустите" посилання на мутабельний об'єкт назовні, що зруйнує імутабельність.
>
> **Приклад проблеми:**
> `public Date getCreationDate() { return this.creationDate; }` — цей геттер повертає посилання на внутрішній об'єкт `Date`. Клієнт може викликати `myObject.getCreationDate().setTime(...)` і змінити стан вашого "імутабельного" об'єкта.
>
> **Краща альтернатива (з Java 8):**
>
> Використовувати класи з нового **`java.time` API**, такі як:
>
> * `LocalDate`
> * `LocalTime`
> * `LocalDateTime`
> * `ZonedDateTime`
> * `Instant`
>
> Усі ці класи є **імутабельними**. Це означає, що їх можна безпечно зберігати і повертати без необхідності захисного копіювання. Вони є набагато більш надійними та зручними для використання.

#### **📌 Що перевіряють:**

*Розуміння проблем старого `Date/Time` API та знання переваг сучасного `java.time`.*

* * *

### **14 ❓ Питання:**

Якщо у вас є вибір, чи варто робити клас імутабельним? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Так, варто, якщо це можливо і доцільно.** Це одна з ключових рекомендацій з "Effective Java".
>
> **Золоте правило:** **"Робіть класи імутабельними, якщо у вас немає вагомої причини робити їх мутабельними."**
>
> **Коли варто робити клас імутабельним:**
>
> * Для **класів-значень (value objects)**, які представляють певну сутність або дані (координата, грошова сума, часовий проміжок, DTO).
> * Для **ключів `Map`** та **елементів `Set`**.
> * Коли об'єкти будуть використовуватися в **багатопотоковому середовищі**.
>
> **Коли мутабельність є виправданою:**
>
> * Коли об'єкт має дуже багато полів, і створення нового екземпляра при кожній зміні є надто "дорогим" за продуктивністю або пам'яттю (хоча `Builder` може допомогти).
> * Коли клас представляє сутність, стан якої за своєю природою має змінюватися (наприклад, `StringBuilder`, `ArrayList` або об'єкт `Account` з балансом).
>
> Навіть для мутабельних класів слід прагнути **мінімізувати їхню змінюваність**, роблячи якомога більше полів `final`.

#### **📌 Що перевіряють:**

*Здатність приймати обґрунтовані дизайнерські рішення. Розуміння імутабельності як "опції за замовчуванням" для написання надійного коду.*

* * *

### **15 ❓ Питання:**

Як імутабельність може спростити реалізацію `equals` та `hashCode`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Імутабельність значно спрощує реалізацію і, що важливіше, подальше використання цих методів.
>
> 1. **Простота `equals`:** Вам не потрібно турбуватися про порівняння станів, які можуть змінюватися. Логіка порівняння є прямолінійною.
>
> 2. **Можливість кешування `hashCode`:**
>     * Оскільки стан імутабельного об'єкта **ніколи не змінюється**, його `hashCode` також **завжди буде однаковий**.
>     * Це дозволяє безпечно **обчислити хеш-код один раз** (зазвичай при першому виклику) і **закешувати** його в приватному полі.
>     * Усі наступні виклики `hashCode()` будуть просто повертати це збережене значення, що є дуже швидкою операцією.
>     * Це значно підвищує продуктивність, коли об'єкти використовуються як ключі в `HashMap`. Клас `String` використовує саме такий підхід.
>
> 3. **Консистентність:** Виклик `equals` та `hashCode` для одного і того ж імутабельного об'єкта завжди буде повертати однаковий результат, що ідеально відповідає вимогам їхніх контрактів.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, як різні концепції (імутабельність, `hashCode`) взаємодіють і підсилюють одна> **Пишемо так:**
> `public record Point(int x, int y) {}`
>
> `record` автоматично створює імутабельний клас з усіма необхідними методами, значно зменшуючи кількість шаблонного коду. Однак, якщо `record` містить мутабельні поля, ви все ще зобов'язані вручну реалізувати захисне копіювання у його конструкторі.

#### **📌 Що перевіряють:**

*Знання сучасних можливостей мови (`record`) та того, як вони вирішують поширені проблеми, як-от написання імутабельних DTO.*

* * *

### **14 ❓ Питання:**

Які є недоліки у імутабельності? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Незважаючи на численні переваги, імутабельність має і свої недоліки:
>
> 1. **Продуктивність та використання пам'яті:**
>     * Для кожної, навіть найменшої зміни, потрібно створювати **новий об'єкт**.
>     * Це може призвести до **великої кількості короткоживучих об'єктів**, що створює додаткове навантаження на **збирач сміття**. Наприклад, конкатенація `String` у циклі.
>
> 2. **Більш громіздкий код для змін:**
>     * Якщо вам потрібно змінити кілька полів імутабельного об'єкта, вам доведеться створювати новий об'єкт, копіюючи всі старі поля і змінюючи потрібні. Це може бути менш зручно, ніж просто викликати сеттери. (Патерн `Builder` з методом `toBuilder()` допомагає вирішити цю проблему).
>
> 3. **Не підходить для всіх завдань:**
>     * Імутабельність не підходить для об'єктів, які за своєю природою мають **представляти змінний стан** (наприклад, `StringBuilder`, об'єкти в GUI, що відображають стан, або `Entity`, що мапиться на рядок у базі даних).
>
> **Висновок:** Імутабельність — це потужний інструмент, але його потрібно застосовувати там, де це доцільно (для `value objects`, `DTO`, конфігурацій), і уникати там, де потрібен змінний стан.

#### **📌 Що перевіряють:**

*Здатність критично аналізувати підходи. Розуміння того, що кожен патерн є компромісом.*

* * *

### **15 ❓ Питання:**

Припустимо, ви створюєте імутабельний клас. Який зі способів повернення колекції з геттера є найкращим і чому?

1. `return this.myList;`
2. `return new ArrayList<>(this.myList);`
3. `return Collections.unmodifiableList(this.myList);`

> #### **💬 Відповідь:**
>
> **Найкращим** способом є **варіант 3: `return Collections.unmodifiableList(this.myList);`**.
>
> **Аналіз варіантів:**
>
> 1. **`return this.myList;` (Дуже погано):**
>     * Це **"витік посилання"**. Клієнтський код отримує пряме посилання на ваш внутрішній, мутабельний список і може його змінити, повністю руйнуючи імутабельність.
>
> 2. **`return new ArrayList<>(this.myList);` (Добре, але не ідеально):**
>     * Це **захисне копіювання**. Клієнт отримує копію, і зміна цієї копії не вплине на внутрішній стан об'єкта.
>     * Це гарантує імутабельність.
>     * **Недолік:** Це створює враження, що повернутий список можна змінювати, хоча ці зміни ні на що не вплинуть. Це може заплутати клієнта API. Також це вимагає копіювання даних, що може бути "дорогим".
>
> 3. **`return Collections.unmodifiableList(this.myList);` (Найкращий варіант):**
>     * Цей метод повертає **незмінну "обгортку"** над вашим списком.
>     * **Переваги:**
>         * **Безпечно:** Захищає внутрішній стан так само, як і захисне копіювання.
>         * **Ясно виражає намір:** Чітко говорить клієнту: "ось тобі список, але ти не можеш його змінити". Будь-яка спроба викличе `UnsupportedOperationException`.
>         * **Ефективно:** Не створює повної копії даних, а лише легковагу обгортку.
>
> З Java 9 ще кращим варіантом може бути `List.copyOf(this.myList)`, який також повертає повністю імутабельну колекцію.

#### **📌 Що перевіряють:**

*Розуміння різних способів захисту внутрішнього стану та здатність вибрати найбільш ідіоматичний та ефективний з них.*
