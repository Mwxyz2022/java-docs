---
layout: default
title: "Wrapper-класи як приклади immutable"
parent: "Immutable об'єкти"
grand_parent: "Java Core"
nav_order: 8
---

# Wrapper-класи як приклади immutable

* короткий опис: Усі `wrapper`-класи в Java (`Integer`, `Double`, `Boolean`, `Character`, `Long` тощо) є `immutable`. Їхній стан не може бути змінений після створення, що забезпечує безпеку та стабільність.

* поняття: `Wrapper`, `boxed types`, `value objects`, кешування.

* приклади: `Integer x = 100; Integer y = 100; System.out.println(x == y);` (true, завдяки кешу в діапазоні -128 до 127).

* ключові моменти:  Гарантують безпеку та стабільність при використанні в колекціях (`HashMap`, `HashSet`) та під час автобоксінгу.

* що важливо знати на співбесіді: Чому `wrapper`-и `immutable`, вплив кешування.

### **✅ Офіційне визначення:**

**Wrapper-класи** — це обгортки навколо примітивних типів Java (**`int`**, **`double`**, **`char`** тощо), які дозволяють використовувати їх як об’єкти.

### **🧠 Простими словами:**

Wrapper-и — це як Java каже: «Хочеш, щоб **`int`** поводився як повноцінний об'єкт із методами? — Отримай **`Integer`**\!»

І найкраще: ці об’єкти **незмінні**, отже поводяться безпечно.

---

## **Основне**

| Примітив | Wrapper-клас | Immutable? |
| :---: | :---: | :---: |
| **`int`** | `Integer` | ✅ |
| **`double`** | `Double` | ✅ |
| **`char`** | `Character` | ✅ |
| **`boolean`** | `Boolean` | ✅ |
| **`byte`** | `Byte` | ✅ |
| **`short`** | `Short` | ✅ |
| **`long`** | `Long` | ✅ |
| **`float`** | `Float` | ✅ |

---

## **Пояснення на пальцях (💡)**

Уяви: **`int`** — це суровий пацан з району. Він швидкий, але не вміє поводитися з об’єктами.

А от **`Integer`** — це його ввічливий двійник у костюмі: поводиться культурно (має методи), **не змінюється після створення**, і з ним можна безпечно працювати в колекціях або багатопоточному коді

---

## **Корисні приклади (🧪)**

```java
Integer x = 42;
Integer y = x + 1; // створюється новий Integer

System.out.println(x); // 42
System.out.println(y); // 43
x залишився незмінним -- нове значення було створене в y.
```

Внутрішній стан **`Integer`** після ініціалізації ніколи не змінюється
---

## **Порівняння (⚖️)**

| Характеристика | Примітив `int` | Обгортка `Integer` |
| :---: | :---: | :---: |
| **Незмінність** | ❌ | ✅ |
| **Можна використовувати в колекціях** | ❌ | ✅ |
| **Має методи (`compareTo`, `equals`)** | ❌ | ✅ |
| **Потокобезпечність** | Залежить | ✅ (immutable) |

---

## **🧠 Мнемоніка для запам’ятовування:**

**"Wrapper \= Value in a Vault"**

* Обгортка (**`wrapper`**) зберігає значення, як у сейфі 🔐 — без змін і з гарантією безпеки.

---

## **🟩 Підсумок:**

* Всі wrapper-класи в Java (**`Integer`**, **`Boolean`**, **`Character`** тощо) — **immutable**.
* Це робить їх **безпечними для використання в колекціях, потоках і кешах**.
* Immutable nature \+ внутрішнє кешування \= **висока продуктивність** і **безпечна поведінка**.

---

### **🔗 Додаткові джерела:**

* [**Oracle Docs — Wrapper Classes**](https://docs.oracle.com/javase/tutorial/java/data/numberclasses.html)

* * *

# **FAQ — Часті запитання (❓)**

### **1 ❓ Питання:**

Що таке wrapper-класи (класи-обгортки) в Java?

> #### **💬 Відповідь:**
>
> **Wrapper-класи** — це набір класів у Java, які надають **об'єктне представлення** для примітивних типів даних. Для кожного з восьми примітивних типів існує відповідний клас-обгортка.
>
> * `int` -> `Integer`
> * `double` -> `Double`
> * `boolean` -> `Boolean`
> * `char` -> `Character`
> * і т.д.
>
> **Основні призначення wrapper-класів:**
>
> 1. **Дозволити примітивам працювати з дженеріками:** Колекції, як-от `List<T>`, можуть зберігати тільки об'єкти, тому замість `List<int>` використовується `List<Integer>`.
> 2. **Надати утилітарні методи:** Класи-обгортки містять корисні статичні методи (наприклад, `Integer.parseInt("123")`) та константи (`Integer.MAX_VALUE`).
> 3. **Дозволити `null` значення:** Примітивний тип, як `int`, не може бути `null`, тоді як об'єкт `Integer` — може.

#### **📌 Що перевіряють:**

*Розуміння базового призначення wrapper-класів як моста між світом примітивів та світом об'єктів.*

* * *

### **2 ❓ Питання:**

Чи є класи-обгортки, як-от `Integer` та `Double`, імутабельними?

> #### **💬 Відповідь:**
>
> Так, **усі вісім стандартних класів-обгорток** (`Integer`, `Long`, `Short`, `Byte`, `Double`, `Float`, `Character`, `Boolean`) є **імутабельними (immutable)**.
>
> Це означає, що після створення об'єкта-обгортки його внутрішнє значення **неможливо змінити**.
>
> **Приклад:**
>
> ```java
> Integer x = 10;
> Integer y = x; // y посилається на той самий об'єкт, що і x
>
> x = x + 1; // Тут відбувається не зміна, а створення нового об'єкта
> // 1. Анбоксинг: x перетворюється на int 10.
> // 2. Додавання: 10 + 1 = 11.
> // 3. Автобоксинг: створюється новий об'єкт Integer зі значенням 11.
> // 4. Посилання x тепер вказує на цей новий об'єкт.
>
> System.out.println(y); // 10 (об'єкт, на який посилався y, не змінився)
> System.out.println(x); // 11
> ```
>
> Ця імутабельність робить їх потокобезпечними і надійними для використання в колекціях.

#### **📌 Що перевіряють:**

*Знання ключової властивості wrapper-класів — їхньої імутабельності.*

* * *

### **3 ❓ Питання:**

Які переваги дає імутабельність wrapper-класів?

> #### **💬 Відповідь:**
>
> Імутабельність надає wrapper-класам ті самі переваги, що й будь-яким іншим імутабельним об'єктам:
>
> 1. **Потокобезпечність (Thread Safety):** Ними можна безпечно ділитися між потоками без синхронізації.
>
> 2. **Надійність:** Ви можете передати `Integer` у метод і бути впевненим, що його значення не буде змінено.
>
> 3. **Безпечне використання як ключів у `HashMap`:** Оскільки `hashCode` та `equals` для `Integer` базуються на його значенні і є незмінними, `Integer` є чудовим ключем для `HashMap` та елементом `HashSet`.
>
> 4. **Можливість кешування:** Імутабельність дозволяє JVM кешувати часто використовувані екземпляри, що підвищує продуктивність.

#### **📌 Що перевіряють:**

*Здатність застосувати загальні переваги імутабельності до конкретного випадку — wrapper-класів.*

* * *

### **4 ❓ Питання:**

Що таке кешування для `Integer` (Integer Cache)? ⭐️

> #### **💬 Відповідь:**
>
> **Integer Cache** — це механізм оптимізації в Java, при якому JVM заздалегідь створює та кешує об'єкти `Integer` для невеликого діапазону значень.
>
> * **Діапазон:** За замовчуванням це числа **від -128 до 127**.
> * **Механізм:** Коли відбувається автобоксинг або виклик `Integer.valueOf(int)` для числа з цього діапазону, JVM **не створює новий об'єкт**, а повертає **посилання на вже існуючий екземпляр** з кешу.
>
> **Наслідки:**
>
> Це призводить до цікавої поведінки при порівнянні через `==`:
>
> ```java
> Integer a = 100; // значення 100 в діапазоні [-128, 127]
> Integer b = 100;
> System.out.println(a == b); // true, бо a та b посилаються на той самий об'єкт з кешу
>
> Integer x = 200; // значення 200 поза діапазоном
> Integer y = 200;
> System.out.println(x == y); // false, бо були створені два нових, різних об'єкти
> ```
>
> **Висновок:** Через кешування, **ніколи не слід** порівнювати `Integer` (та інші обгортки) за допомогою `==`. Завжди використовуйте **`equals()`**.

#### **📌 Що перевіряють:**

*Знання про існування та роботу Integer Cache — це одне з найпоширеніших питань "з підступом" на співбесідах.*

* * *

### **5 ❓ Питання:**

Як кешування `Integer` пов'язане з його імутабельністю?

> #### **💬 Відповідь:**
>
> Кешування можливе **тільки тому**, що `Integer` є **імутабельним**.
>
> Якби `Integer` був мутабельним, цей механізм був би абсолютно небезпечним.
>
> **Сценарій проблеми (якби `Integer` був мутабельним):**
>
> 1. `Integer a = 10;` (отримуємо об'єкт зі значенням 10 з кешу)
> 2. `Integer b = 10;` (отримуємо **той самий** об'єкт з кешу, `a == b`)
> 3. Припустимо, ми можемо змінити значення `a`: `a.setValue(20);`
> 4. Оскільки `a` та `b` посилаються на один і той самий об'єкт у кеші, значення `b` також **несподівано стане 20**.
>
> Це зруйнувало б логіку програми.
>
> Імутабельність гарантує, що об'єкти в кеші ніколи не змінять свого стану, тому їх можна безпечно перевикористовувати.

#### **📌 Що перевіряють:**

*Глибоке розуміння того, що імутабельність є необхідною передумовою для безпечного кешування та перевикористання об'єктів.*

* * *

### **6 ❓ Питання:**

Для яких ще wrapper-класів існує подібне кешування?

> #### **💬 Відповідь:**
>
> Кешування за замовчуванням реалізовано для наступних wrapper-класів, щоб оптимізувати роботу з найчастіше використовуваними значеннями:
>
> * **`Byte`**, **`Short`**, **`Integer`**, **`Long`**: Для діапазону **-128 до 127**.
> * **`Character`**: Для діапазону **від `\u0000` до `\u007f`** (перші 128 символів ASCII).
> * **`Boolean`**: Існують лише два екземпляри — **`Boolean.TRUE`** та **`Boolean.FALSE`**. `Boolean.valueOf(true)` завжди повертає той самий об'єкт.
>
> Класи `Float` та `Double` **не кешують** значення (за винятком специфічних NaN та нескінченностей), оскільки кількість можливих значень між двома цілими числами є занадто великою.

#### **📌 Що перевіряють:**

*Загальні знання про реалізацію wrapper-класів.*

* * *

### **7 ❓ Питання:**

Якщо у вас є `int a = 5;` та `Integer b = 5;`. Що поверне `a == b`? Поясніть чому. ⭐️

> #### **💬 Відповідь:**
>
> Цей вираз поверне **`true`**.
>
> **Чому:**
>
> Коли оператор `==` використовується для порівняння **примітиву** та **об'єкта-обгортки**, Java виконує **анбоксинг (unboxing)** об'єкта-обгортки.
>
> 1. Компілятор бачить, що з одного боку `int`, а з іншого — `Integer`.
> 2. Він автоматично перетворює об'єкт `b` на примітив: `b.intValue()`.
> 3. Фактично, порівняння стає **`5 == 5`**.
> 4. Результат, звісно, `true`.
>
> Це стосується всіх пар примітив-обгортка.

#### **📌 Що перевіряють:**

*Знання правил анбоксингу при операціях порівняння.*

* * *

### **8 ❓ Питання:**

Розглянемо `Integer x = new Integer(10); Integer y = new Integer(10);`. Що поверне `x == y`? ⭐️

> #### **💬 Відповідь:**
>
> Цей вираз поверне **`false`**.
>
> **Причина:**
>
> Використання ключового слова **`new`** **примусово створює новий об'єкт** у купі (heap). Навіть якщо значення `10` входить у діапазон кешування, оператор `new` **ігнорує кеш** і завжди створює новий екземпляр.
>
> Таким чином, `x` та `y` будуть посилатися на **два різні об'єкти** в пам'яті, які просто містять однакове значення. Оператор `==` порівнює посилання, які є різними, тому результат `false`.
>
> **Примітка:** Конструктор `new Integer(int)` є **застарілим (deprecated)** з Java 9, і рекомендується використовувати статичний фабричний метод `Integer.valueOf(int)`.

#### **📌 Що перевіряють:**

*Розуміння того, як `new` обходить механізм кешування, та його різниці від `valueOf()` або автобоксингу.*

* * *

### **9 ❓ Питання:**

Чому `double d1 = 0.1; double d2 = 0.2; System.out.println(d1 + d2 == 0.3);` може повернути `false`? Як це пов'язано з імутабельністю `Double`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Цей вираз може повернути `false` через **проблеми з точністю представлення чисел з плаваючою комою** у бінарному форматі (стандарт IEEE 754).
>
> Числа, як-от `0.1`, не мають точного скінченного представлення у двійковій системі, так само, як `1/3` не має скінченного представлення у десятковій (`0.333...`). Це призводить до невеликих помилок округлення.
>
> Сума `0.1 + 0.2` насправді може дорівнювати чомусь на кшталт `0.30000000000000004`, що не дорівнює `0.3`.
>
> **Як це пов'язано з імутабельністю `Double`?**
>
> **Опосередковано.** Сама імутабельність `Double` тут не є причиною проблеми, але вона підкреслює, що об'єкт `Double` просто "обгортає" це неточне `double` значення.
>
> Через цю проблему, порівнювати `double` або `float` за допомогою `==` є дуже поганою практикою. Замість цього слід **порівнювати їх з певною похибкою (epsilon)**.
>
> `if (Math.abs((d1 + d2) - 0.3) < 0.000001) { ... }`
>
> Для точних фінансових або математичних розрахунків слід використовувати **`BigDecimal`**, який є імутабельним і дозволяє уникнути проблем з точністю.

#### **📌 Що перевіряють:**

*Знання про проблеми з точністю `float`/`double`. Розуміння, коли не можна покладатися на просте порівняння.*

* * *

### **10 ❓ Питання:**

Які є два основні способи створити об'єкт `Integer` зі значенням `10`? Чим вони відрізняються?

> #### **💬 Відповідь:**
>
> 1. **Статичний фабричний метод `Integer.valueOf(10)`** (або автобоксинг `Integer i = 10;`, який "під капотом" викликає `valueOf`).
>
> 2. **Конструктор `new Integer(10)`** (який є застарілим).
>
> **Основна відмінність** — **використання кешу**.
>
> * `Integer.valueOf(int)` **використовує кеш**. Якщо число знаходиться в діапазоні від -128 до 127, він поверне існуючий об'єкт з кешу. Якщо ні — створить новий.
>
> * `new Integer(int)` **завжди створює новий об'єкт** у купі, ігноруючи кеш.
>
> **Висновок:** Завжди слід надавати перевагу `Integer.valueOf()` або автобоксингу. Це більш ефективно з точки зору пам'яті та продуктивності.

#### **📌 Що перевіряють:**

*Вміння порівняти два способи створення об'єктів та пояснити, який з них є кращим і чому.*

* * *

### **11 ❓ Питання:**

Припустимо, у вас є метод `void process(Long number)`. Якщо ви передасте в нього `int`, чи спрацює автобоксинг? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Ні, такий код **не скомпілюється**.
>
> `int myInt = 10; process(myInt); // ПОМИЛКА КОМПІЛЯЦІЇ`
>
> **Чому?**
>
> Java виконує **лише один крок** неявного перетворення.
> Вона **не** робить два кроки: `int` -> `long` (розширення), а потім `long` -> `Long` (автобоксинг).
>
> * Компілятор бачить `int` і метод, що очікує `Long`.
> * Він міг би зробити `int` -> `Integer`, але `Integer` не є `Long`.
> * Він міг би зробити `int` -> `long`, але `long` не є `Long`.
>
> Оскільки немає прямого шляху перетворення за один крок, компілятор видає помилку.
>
> **Як виправити:**
>
> Потрібно зробити перетворення явним:
> `process((long) myInt); // int -> long, потім автобоксинг long -> Long`
> `process(Long.valueOf(myInt)); // Явно створюємо Long`

#### **📌 Що перевіряють:**

*Глибоке розуміння правил автоматичного перетворення типів та їхніх обмежень.*

* * *

### **12 ❓ Питання:**

Чи можна змінити значення внутрішнього кешу для `Integer`? ⭐️⭐️

> #### **💬 Відповідь:**
>
> Так, можна, хоча це і не є поширеною практикою.
>
> Верхню межу кешу `Integer` можна налаштувати за допомогою **аргументу JVM**.
>
> `-Djava.lang.Integer.IntegerCache.high=<розмір>`
> або
> `-XX:AutoBoxCacheMax=<розмір>`
>
> Нижня межа (-128) є фіксованою, а верхню (127) можна збільшити. Це може бути корисно у специфічних випадках, якщо програма інтенсивно працює з `Integer` у певному, більш широкому діапазоні, щоб зменшити створення об'єктів.
>
> Однак це є низькорівневою оптимізацією, і до неї слід вдаватися тільки після профілювання, яке підтвердить, що автобоксинг є проблемою. Для більшості застосунків стандартних значень цілком достатньо.

#### **📌 Що перевіряють:**

*Знання про можливості тонкого налаштування JVM. Це питання для просунутого рівня.*

* * *

### **13 ❓ Питання:**

Розглянемо `if (myBoolean) { ... }`, де `myBoolean` має тип `Boolean`. Чи може цей рядок кинути `NullPointerException`?

> #### **💬 Відповідь:**
>
> Так, **може**.
>
> **Чому:**
>
> * Умова в операторі `if` вимагає значення типу **примітив `boolean`**.
> * Якщо `myBoolean` має тип `Boolean` (обгортка), то для виконання умови компілятор додає **неявний анбоксинг** (викликає `myBoolean.booleanValue()`).
> * Якщо `myBoolean` на цей момент дорівнює `null`, то виклик `null.booleanValue()` призведе до **`NullPointerException`**.
>
> **Безпечний спосіб перевірки:**
>
> ```java
> Boolean myBoolean = null;
>
> // Правильна і безпечна перевірка
> if (Boolean.TRUE.equals(myBoolean)) {
>     // ...
> }
> ```
>
> Цей підхід ніколи не кине `NPE`, оскільки `equals` коректно обробляє `null` як аргумент.

#### **📌 Що перевіряють:**

*Уважність до неявних операцій (анбоксинг) та їхніх ризиків, особливо з `Boolean`.*

* * *

### **14 ❓ Питання:**

Чому `Boolean.valueOf("true")` є кращим, ніж `new Boolean("true")`? ⭐️

> #### **💬 Відповідь:**
>
> **`Boolean.valueOf("true")`** є кращим з двох причин:
>
> 1. **Використання кешу:**
>     * `Boolean` має тільки два можливі екземпляри: `Boolean.TRUE` і `Boolean.FALSE`.
>     * Метод `valueOf()` завжди повертає один з цих **двох заздалегідь створених об'єктів**, не створюючи нових.
>
> 2. **Застарілий конструктор:**
>     * Конструктор `new Boolean(String)` є **застарілим (deprecated)** з Java 9.
>     * Він **завжди створює новий об'єкт**, що є неефективним і не відповідає семантиці, де булевих значень може бути лише два.
>
> **Висновок:** Використання `valueOf()` є більш продуктивним (не створює зайвих об'єктів) і відповідає сучасним практикам програмування на Java.

#### **📌 Що перевіряють:**

*Знання того, що конструктори wrapper-класів є застарілими, та вміння пояснити переваги статичних фабричних методів.*

* * *

### **15 ❓ Питання:**

Напишіть імутабельний клас `Range`, що містить два поля `Integer`. Чи потрібне для нього захисне копіювання? ⭐️⭐️

> #### **💬 Відповідь:**
>
> **Ні, захисне копіювання не потрібне.**
>
> Клас `Integer` є **імутабельним**. Це означає, що після створення об'єкта `Integer` його внутрішнє значення неможливо змінити.
>
> **Реалізація:**
>
> ```java
> public final class Range {
>     private final Integer start;
>     private final Integer end;
>
>     public Range(Integer start, Integer end) {
>         // Захисне копіювання НЕ потрібне, оскільки Integer імутабельний.
>         // Просто присвоюємо посилання.
>         this.start = start;
>         this.end = end;
>     }
>
>     public Integer getStart() {
>         // Повертати посилання безпечно
>         return start;
>     }
>
>     public Integer getEnd() {
>         return end;
>     }
> }
> ```
>
> Оскільки ніхто не може змінити стан об'єктів `start` та `end` ззовні, передавати та повертати посилання на них є абсолютно безпечним. Клас `Range` залишається імутабельним.
>
> Захисне копіювання потрібне **тільки для мутабельних** полів.

#### **📌 Що перевіряють:**

*Здатність застосувати знання про імутабельність wrapper-класів при проектуванні власних імутабельних класів.*
